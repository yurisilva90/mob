
===== vídeo @ 402974 =====
-filters">
    <div class="feed-chip on" data-type="" onclick="feedSetFilter('',this)">Tudo</div>
    <div class="feed-chip" data-type="blitz" onclick="feedSetFilter('blitz',this)">Blitz</div>
    <div class="feed-chip" data-type="risco" onclick="feedSetFilter('risco',this)">Risco</div>
    <div class="feed-chip" data-type="transito" onclick="feedSetFilter('transito',this)">Trânsito</div>
    <div class="feed-chip" data-type="demanda" onclick="feedSetFilter('demanda',this)">Demanda</div>
    <div class="feed-chip" data-type="dinamico" onclick="feedSetFilter('dinamico',this)">Dinâmico</div>
    <div class="feed-chip" data-type="fila" onclick="feedSetFilter('fila',this)">Fila</div>
  </div>
  <div class="feed-list" id="feed-list"><div class="feed-empty">Carregando alertas…</div></div>
  <div class="feed-fab-wrap">
    <button class="feed-fab" onclick="feedOpenComposer()" aria-label="Publicar alerta">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4z"/></svg>
    </button>
  </div>
</div>

<!-- ══ TELA JORNADA ══════════════════════════════════════════════════════ -->
<div class="screen" id="sc-jornada">
  <div id="jorn-custo-banner" class="cv-banner" style="display:none;margin:8px 16px 0" onclick="openCustoVeiculo()">
    <div class="txt"><b>Veja seu lucro líquido real</b>Configure o custo do seu veículo em 2 min</div>
    <button class="x" onclick="event.stopPropagation();dismissCustoBanner()">&#x2715;</button>
  </div>
  <div style="padding:4px 16px 0">
    <div id="net-status-badge" style="display:none;align-items:center;gap:5px;justify-content:center;padding:4px 10px;border-radius:99px;font-size:10.5px;font-weight:800;margin:0 auto 6px;width:fit-content"><span id="net-status-dot" style="width:6px;height:6px;border-radius:50%"></span><span id="net-status-text"></span></div>
    <div class="daybar">
      <button class="arrow" onclick="chgDay(-1)">&#8249;</button>
      <div class="datebox" onclick="document.getElementById('jorn-date-input').showPicker ? document.getElementById('jorn-date-input').showPicker() : document.getElementById('jorn-date-input').click()">
        <div style="display:flex;align-items:center;justify-content:center;gap:5px">
          <h1 id="dn-date">Hoje</h1>
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="#94A3B8" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
        </div>
        <p id="dn-dow"></p>
      </div>
      <button class="arrow" onclick="chgDay(1)">&#8250;</button>
      <input type="date" id="jorn-date-input" onchange="setJornadaDate(this.value)" style="position:absolute;opacity:0;pointer-events:none;width:1px;height:1px">
    </div>
  </div>

  <div id="pending-confirm-banner" class="pending-confirm-banner" style="display:none" onclick="openImportPrecheck()">
    <div class="pcb-ico">
      <svg viewBox="0 0 24 24" width="19" height="19" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M23 7l-7 5 7 5V7z"/><rect x="1" y="5" width="15" height="14" rx="2"/></svg>
    </div>
    <div class="pcb-txt">
      <div class="pcb-title" id="pcb-title">corridas aguardando confirmação</div>
      <div class="pcb-sub">Toque para enviar o vídeo do histórico</div>
    </div>
    <svg class="pcb-chev" viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18l6-6-6-6"/></svg>
  </div>

  <div class="journey-wrap" id="journey-wrap">
  <div class="journey idle" id="journey-card">
    <div class="j-status" style="display:none"><i></i><span id="jc-status-text">Iniciar</span></div>
    <div class="j-toprow">
      <div class="j-current" style="grid-template-columns:1.3fr 1fr 1fr;flex:1">
        <div><h2 id="jc-timer">00:00:00</h2><p id="jc-sub">Nenhuma sessão iniciada</p></div>
        <div class="j-km"><strong id="jc-km">0,0</strong><span>Km</span></div>
        <div class="j-km"><strong id="jc-trips2">0</strong><span>Corridas</span></div>
      </div>
    </div>
    <div class="j-bottomrow">
      <div class="j-rates2">
        <div class="rate-cell" id="jc-rate-hora2"><strong id="jc-rsh2">R$0,00</strong><span>R$ / hora</span></div>
        <div class="rate-cell" id="jc-rate-km2"><strong id="jc-rskm2">R$0,00</strong><span>R$ / km</span></div>
      </div>
      <div class="j-actions" id="jc-actions"></div>
    </div>
  </div>
  <div class="journey-breakdown" id="jc-breakdown" style="display:none">
    <div class="sbar-wrap" onclick="toggleBar('jc-blist')"><div class="sbar" id="jc-bar"></div></div>
    <div class="sbar-list" id="jc-blist"></div>
  </div>
  </div>

  <div class="section" style="padding:16px 16px 8px"><h2>Total do dia</h2></div>
  <div style="padding:0 16px 14px">
    <div class="td-card">

      <div class="td-row">
        <svg class="td-wm right" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"><path d="M3 13l1.5-4.5A2 2 0 0 1 6.4 7h11.2a2 2 0 0 1 1.9 1.5L21 13"/><path d="M3 13h18v4a1 1 0 0 1-1 1h-1a1 1 0 0 1-1-1v-1H6v1a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1v-4z"/><circle cx="7.5" cy="17" r="1.3"/><circle cx="16.5" cy="17" r="1.3"/></svg>
        <div class="td-corridas"><strong id="jc-trips">0</strong><span>Corridas</span></div>
        <div class="td-sub" id="jc-corridas-sub" style="display:none">
          <span class="td-chip ok"><svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span id="jc-aceitas">0</span> aceitas</span>
          <span class="td-vline"></span>
          <span class="td-chip no"><svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg><span id="jc-recusadas">0</span> recusadas</span>
        </div>
      </div>

      <div class="td-row">
        <div class="tk-split">
          <div class="tk-half left">
            <svg class="td-wm" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9.5"/><path d="M12 6.5v5.5l4 2.3"/></svg>
            <div style="display:flex;align-items:center;justify-content:center;gap:6px;position:relative;z-index:1">
              <strong id="jc-total">0h00</strong>
              <button class="km-edit-btn" onclick="openTempoAdjust()" aria-label="Editar tempo total">
                <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M17 3a2.85 2.85 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"/></svg>
              </button>
            </div>
            <span>Tempo total</span>
          </div>
          <div class="tk-divider"></div>
          <div class="tk-half right">
            <svg class="td-wm" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round"><path d="M4 23L9 1M20 23L15 1"/><path d="M12 3.5v3M12 10v3M12 16.5v3"/></svg>
            <div style="display:flex;align-items:center;justify-content:center;gap:5px;position:relative;z-index:1">
              <strong id="jc-kmtotal">0,0 km</strong>
              <button class="km-edit-btn" onclick="openKmAdjust()" aria-label="Editar km total">
                <svg viewBox="0 0 24 24" width="10" height="10" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M17 3a2.85 2.85 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"/></svg>
              </button>
            </div>
            <span>Km total</span>
          </div>
        </div>
      </div>

      <div class="td-row">
        <svg class="td-wm left" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="0.6" stroke-linecap="round" stroke-linejoin="round"><path d="M12 1v22"/><path d="M17 5.5c0-2-2.2-3.3-5-3.3s-5 1.5-5 3.7c0 2.5 2.2 3.5 5 4.3c3.2.9 5.4 1.9 5.4 4.5c0 2.5-2.4 3.8-5.4 3.8s-5.4-1.4-5.4-3.8"/></svg>
        <div class="td-rates">
          <div class="td-rcard" id="jc-rate-hora"><strong id="jc-rsh">R$0,00</strong><span>R$ / hora</span></div>
          <div class="td-rcard" id="jc-rate-km"><strong id="jc-rskm">R$0,00</strong><span>R$ / km</span></div>
        </div>
        <div class="td-fin-divider"></div>
        <div class="td-fin">
          <div class="cell"><strong class="g" id="d-rev">R$0</strong><span>Receitas</span></div>
          <div class="cell"><strong class="r" id="d-exp">R$0</strong><span>Despesas</span></div>
          <div class="cell"><strong id="d-net">R$0</strong><span>Resultado</span></div>
        </div>
      </div>

      <div class="td-row bd" id="td-bd-row" style="display:none">
        <div class="sbar" id="td-bd-bar"></div>
        <div class="sbar-list open" id="td-bd-list"></div>
      </div>

    </div>
  </div>

  <!-- Metas do dia: pedido do Yuri (23/07/2026) — sai de dentro do card
       azul da jornada (onde competia por espaço com timer/km/corridas) e
       vira seção solta, igual "Lançamentos" logo abaixo (título + link no
       fundo da tela, sem balão/card branco por trás). renderMetasDoDia()
       não muda nada — só o container onde a mesma função escreve. -->
  <div class="section" style="padding:0 16px 8px"><h2>Metas do dia</h2><button class="add" onclick="openMetasHojeModal()">Editar &#8250;</button></div>
  <div style="padding:0 16px 4px">
    <div class="metas-wrap">
      <svg class="metas-wm" viewBox="0 0 24 24" fill="no
===== vídeo @ 497836 =====
">
          <b style="font-size:11px;font-weight:800;color:#64748B">Corridas</b>
        </div>
        <div style="display:flex;align-items:center;justify-content:center;border:1.5px solid #E7EBF1;border-radius:10px;padding:0 8px;height:40px;gap:3px">
          <input id="ipc-99-count" type="number" placeholder="0" style="border:0;outline:0;flex:1;font-size:14px;font-weight:700;color:#0F172A;font-family:inherit;min-width:0;text-align:center">
        </div>
        <div style="font-size:9px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.04em;text-align:center">Valor total</div>
        <div style="display:flex;align-items:center;justify-content:center;border:1.5px solid #E7EBF1;border-radius:10px;padding:0 8px;height:40px;gap:3px">
          <span style="font-size:11px;color:#64748B;font-weight:700;flex-shrink:0">R$</span>
          <input id="ipc-99-value" type="number" placeholder="0,00" style="border:0;outline:0;flex:1;font-size:14px;font-weight:700;color:#0F172A;font-family:inherit;min-width:0;text-align:center">
        </div>
      </div>
    </div>

    <div style="display:flex;gap:7px;background:#EFF6FF;border:1px solid rgba(14,165,233,.2);border-radius:11px;padding:9px 10px;font-size:11px;color:#0369A1;line-height:1.45;margin-bottom:12px">
      <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" style="flex:none;margin-top:1px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
      <span>Serve pra conferir se o import achou tudo — não precisa ser o dia inteiro.</span>
    </div>
    <button class="btn-p" style="margin-top:0" onclick="importPrecheckNext()">Avançar</button>
    <div style="text-align:center;font-size:12px;color:#2563EB;font-weight:700;margin-top:10px;cursor:pointer" onclick="importPrecheckSkip()">Pular essa parte →</div>
  </div>
</div>

<div class="mo" id="mo-import">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2>Importar corridas</h2><button class="xbtn" onclick="closeMo('mo-import')">&#x2715;</button></div>
    <div style="background:#EFF6FF;border:1px solid rgba(14,165,233,.2);border-radius:16px;padding:12px 14px;font-size:12px;color:#0369A1;line-height:1.6;display:flex;gap:8px;margin-bottom:16px">
      <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" style="flex-shrink:0;margin-top:2px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
      <div><strong>Como usar:</strong> grave a tela do celular rolando o histórico (Uber ou 99, tanto faz) e mande o arquivo de vídeo aqui — não dá mais pra mandar print solto. O app identifica a plataforma sozinho e extrai as corridas.</div>
    </div>
    <div style="display:flex;gap:8px;margin-bottom:8px">
      <label style="flex:1;height:56px;border-radius:14px;background:#F0F4FA;border:1px solid #E2E8F0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;cursor:pointer;font-size:11px;font-weight:700;color:#0A2F6B">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="m3 9 4-4 4 4 4-4 4 4"/></svg> Vídeo da galeria
        <input type="file" multiple accept="video/*" style="display:none" onclick="this.value=null" onchange="handleImport(this,'uber')">
      </label>
      <label style="flex:1;height:56px;border-radius:14px;background:#F0F4FA;border:1px solid #E2E8F0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;cursor:pointer;font-size:11px;font-weight:700;color:#0A2F6B">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg> Arquivos
        <input type="file" multiple accept="video/*" style="display:none" onclick="this.value=null" onchange="handleImport(this,'uber')">
      </label>
    </div>
    <div style="font-size:11px;color:#94A3B8;text-align:center;margin-bottom:14px">Só vídeo — grave a tela rolando o histórico e mande o arquivo aqui. Dá pra escolher vários de uma vez.</div>

    <label style="width:100%;height:50px;border-radius:14px;background:linear-gradient(135deg,#0A2F6B,#22C55E);display:flex;align-items:center;justify-content:center;gap:8px;cursor:pointer;font-size:12.5px;font-weight:700;color:#fff;box-shadow:0 8px 18px rgba(10,47,107,.25);position:relative;margin-bottom:8px">
      <span style="position:absolute;top:-8px;right:-6px;background:#F59E0B;color:#fff;font-size:8.5px;font-weight:900;padding:3px 6px;border-radius:999px;letter-spacing:.02em">NOVO</span>
      <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M17 8 12 3 7 8"/><path d="M12 3v12"/></svg>
      Histórico oficial do Uber (.zip)
      <input type="file" accept=".zip" style="display:none" onclick="this.value=null" onchange="handleUberZipFile(this)">
    </label>
    <div style="font-size:11px;color:#94A3B8;text-align:center;line-height:1.5;margin-bottom:12px">Baixe em: app Uber → Conta → Configurações de privacidade → "Solicitar meus dados". Você recebe um e-mail com o arquivo .zip.</div>

    <div style="display:flex;gap:6px;justify-content:center">
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px;display:flex;align-items:center;gap:4px"><i style="width:6px;height:6px;border-radius:50%;display:inline-block;background:#0EA5E9"></i> Uber</span>
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px;display:flex;align-items:center;gap:4px"><i style="width:6px;height:6px;border-radius:50%;display:inline-block;background:#F59E0B"></i> 99</span>
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px">identificados automaticamente</span>
    </div>
  </div>
</div>

<!-- Import confirm -->
<div class="mo" id="mo-import-confirm">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2 id="ic-title">Corridas detectadas</h2><button class="xbtn" onclick="closeMo('mo-import-confirm')">&#x2715;</button></div>
    <div id="ic-body" style="min-height:80px"></div>
    <div style="display:flex;gap:8px;padding-top:8px">
      <button class="btn-s" onclick="closeMo('mo-import-confirm')" style="margin-top:0">Cancelar</button>
      <button class="btn-p" id="ic-btn" onclick="confirmImport()" style="margin-top:0">Importar</button>
    </div>
  </div>
</div>

<!-- Uber ZIP: processando + resultado -->
<div class="mo ctr" id="mo-uberzip">
  <div class="sheet ctr" style="max-width:400px">
    <div id="uz-proc" style="padding:50px 20px;text-align:center">
      <div style="font-size:32px;display:inline-block;animation:uzspin 1.1s linear infinite">&#9881;</div>
      <div id="uz-proc-step" style="font-size:14px;font-weight:800;margin-top:16px">Lendo arquivo...</div>
      <div id="uz-proc-sub" style="font-size:12px;color:#64748B;margin-top:5px">Isso só leva alguns segundos</div>
      <div style="height:8px;background:#EEF2F7;border-radius:99px;overflow:hidden;margin-top:20px">
        <div id="uz-proc-bar" style="height:100%;background:linear-gradient(90deg,#0EA5E9,#22C55E);border-radius:99px;width:8%;transition:width .4s"></div>
      </div>
    </div>

    <div id="uz-res" style="display:none">
      <div style="text-align:center;padding:4px 0 16px">
        <div style="width:52px;height:52px;border-radius:16px;background:#ECFDF3;color:#16A34A;display:flex;align-items:center;justify-content:center;margin:0 auto 10px">
          <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6 9 17l-5-5"/></svg>
        </div>
        <div style="font-size:16px;font-weight:800;letter-spacing:-.02em">Histórico conferido</div>
        <div id="uz-res-sub" style="font-size:12px;color:#64748B;margin-top:3px"></div>
      </div>

      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px">
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Novas pra importar</span>
          <strong id="uz-k-novas" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;color:#16A34A;margin-top:4px">0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Já existiam</span>
          <strong id="uz-k-dup" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;color:#64748B;margin-top:4px">0</strong>
        </div>
        <div style="border:1px solid #E2E
===== vídeo @ 498463 =====
Y+4jIUAqh43PM7NRjkfwa1UaPYLUC/SH8lx5nMwkgHEiJHPls98lF8cV1i6cqPck+ajpxo/A95E8AwBz4rL+QK0xQJEsdow5f0rBKpeQxjumquXqKaFPQgYimQgkr5ALUGorU+3HznMyzVNPnmG/1cp7kXJM9N1FBl0BE0DgjU4rESwhBaWistpasumHpe5IOZ2HAJFof8HxpSql744rxMAAAAASUVORK5CYII=">
          <b style="font-size:11px;font-weight:800;color:#64748B">Corridas</b>
        </div>
        <div style="display:flex;align-items:center;justify-content:center;border:1.5px solid #E7EBF1;border-radius:10px;padding:0 8px;height:40px;gap:3px">
          <input id="ipc-99-count" type="number" placeholder="0" style="border:0;outline:0;flex:1;font-size:14px;font-weight:700;color:#0F172A;font-family:inherit;min-width:0;text-align:center">
        </div>
        <div style="font-size:9px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.04em;text-align:center">Valor total</div>
        <div style="display:flex;align-items:center;justify-content:center;border:1.5px solid #E7EBF1;border-radius:10px;padding:0 8px;height:40px;gap:3px">
          <span style="font-size:11px;color:#64748B;font-weight:700;flex-shrink:0">R$</span>
          <input id="ipc-99-value" type="number" placeholder="0,00" style="border:0;outline:0;flex:1;font-size:14px;font-weight:700;color:#0F172A;font-family:inherit;min-width:0;text-align:center">
        </div>
      </div>
    </div>

    <div style="display:flex;gap:7px;background:#EFF6FF;border:1px solid rgba(14,165,233,.2);border-radius:11px;padding:9px 10px;font-size:11px;color:#0369A1;line-height:1.45;margin-bottom:12px">
      <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" style="flex:none;margin-top:1px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
      <span>Serve pra conferir se o import achou tudo — não precisa ser o dia inteiro.</span>
    </div>
    <button class="btn-p" style="margin-top:0" onclick="importPrecheckNext()">Avançar</button>
    <div style="text-align:center;font-size:12px;color:#2563EB;font-weight:700;margin-top:10px;cursor:pointer" onclick="importPrecheckSkip()">Pular essa parte →</div>
  </div>
</div>

<div class="mo" id="mo-import">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2>Importar corridas</h2><button class="xbtn" onclick="closeMo('mo-import')">&#x2715;</button></div>
    <div style="background:#EFF6FF;border:1px solid rgba(14,165,233,.2);border-radius:16px;padding:12px 14px;font-size:12px;color:#0369A1;line-height:1.6;display:flex;gap:8px;margin-bottom:16px">
      <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" style="flex-shrink:0;margin-top:2px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
      <div><strong>Como usar:</strong> grave a tela do celular rolando o histórico (Uber ou 99, tanto faz) e mande o arquivo de vídeo aqui — não dá mais pra mandar print solto. O app identifica a plataforma sozinho e extrai as corridas.</div>
    </div>
    <div style="display:flex;gap:8px;margin-bottom:8px">
      <label style="flex:1;height:56px;border-radius:14px;background:#F0F4FA;border:1px solid #E2E8F0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;cursor:pointer;font-size:11px;font-weight:700;color:#0A2F6B">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="m3 9 4-4 4 4 4-4 4 4"/></svg> Vídeo da galeria
        <input type="file" multiple accept="video/*" style="display:none" onclick="this.value=null" onchange="handleImport(this,'uber')">
      </label>
      <label style="flex:1;height:56px;border-radius:14px;background:#F0F4FA;border:1px solid #E2E8F0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;cursor:pointer;font-size:11px;font-weight:700;color:#0A2F6B">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg> Arquivos
        <input type="file" multiple accept="video/*" style="display:none" onclick="this.value=null" onchange="handleImport(this,'uber')">
      </label>
    </div>
    <div style="font-size:11px;color:#94A3B8;text-align:center;margin-bottom:14px">Só vídeo — grave a tela rolando o histórico e mande o arquivo aqui. Dá pra escolher vários de uma vez.</div>

    <label style="width:100%;height:50px;border-radius:14px;background:linear-gradient(135deg,#0A2F6B,#22C55E);display:flex;align-items:center;justify-content:center;gap:8px;cursor:pointer;font-size:12.5px;font-weight:700;color:#fff;box-shadow:0 8px 18px rgba(10,47,107,.25);position:relative;margin-bottom:8px">
      <span style="position:absolute;top:-8px;right:-6px;background:#F59E0B;color:#fff;font-size:8.5px;font-weight:900;padding:3px 6px;border-radius:999px;letter-spacing:.02em">NOVO</span>
      <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M17 8 12 3 7 8"/><path d="M12 3v12"/></svg>
      Histórico oficial do Uber (.zip)
      <input type="file" accept=".zip" style="display:none" onclick="this.value=null" onchange="handleUberZipFile(this)">
    </label>
    <div style="font-size:11px;color:#94A3B8;text-align:center;line-height:1.5;margin-bottom:12px">Baixe em: app Uber → Conta → Configurações de privacidade → "Solicitar meus dados". Você recebe um e-mail com o arquivo .zip.</div>

    <div style="display:flex;gap:6px;justify-content:center">
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px;display:flex;align-items:center;gap:4px"><i style="width:6px;height:6px;border-radius:50%;display:inline-block;background:#0EA5E9"></i> Uber</span>
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px;display:flex;align-items:center;gap:4px"><i style="width:6px;height:6px;border-radius:50%;display:inline-block;background:#F59E0B"></i> 99</span>
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px">identificados automaticamente</span>
    </div>
  </div>
</div>

<!-- Import confirm -->
<div class="mo" id="mo-import-confirm">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2 id="ic-title">Corridas detectadas</h2><button class="xbtn" onclick="closeMo('mo-import-confirm')">&#x2715;</button></div>
    <div id="ic-body" style="min-height:80px"></div>
    <div style="display:flex;gap:8px;padding-top:8px">
      <button class="btn-s" onclick="closeMo('mo-import-confirm')" style="margin-top:0">Cancelar</button>
      <button class="btn-p" id="ic-btn" onclick="confirmImport()" style="margin-top:0">Importar</button>
    </div>
  </div>
</div>

<!-- Uber ZIP: processando + resultado -->
<div class="mo ctr" id="mo-uberzip">
  <div class="sheet ctr" style="max-width:400px">
    <div id="uz-proc" style="padding:50px 20px;text-align:center">
      <div style="font-size:32px;display:inline-block;animation:uzspin 1.1s linear infinite">&#9881;</div>
      <div id="uz-proc-step" style="font-size:14px;font-weight:800;margin-top:16px">Lendo arquivo...</div>
      <div id="uz-proc-sub" style="font-size:12px;color:#64748B;margin-top:5px">Isso só leva alguns segundos</div>
      <div style="height:8px;background:#EEF2F7;border-radius:99px;overflow:hidden;margin-top:20px">
        <div id="uz-proc-bar" style="height:100%;background:linear-gradient(90deg,#0EA5E9,#22C55E);border-radius:99px;width:8%;transition:width .4s"></div>
      </div>
    </div>

    <div id="uz-res" style="display:none">
      <div style="text-align:center;padding:4px 0 16px">
        <div style="width:52px;height:52px;border-radius:16px;background:#ECFDF3;color:#16A34A;display:flex;align-items:center;justify-content:center;margin:0 auto 10px">
          <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6 9 17l-5-5"/></svg>
        </div>
        <div style="font-size:16px;font-weight:800;letter-spacing:-.02em">Histórico conferido</div>
        <div id="uz-res-sub" style="font-size:12px;color:#64748B;margin-top:3px"></div>
      </div>

      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px">
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Novas pra importar</span>
          <strong id="uz-k-novas" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;color:#16A34A;margin-top:4px">0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Já existiam</span>
          <strong id="uz-k-dup" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;color:#64748B;margin-top:4px">0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Valor das novas</span>
          <strong id="uz-k-valor" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;margin-top:4px">R$ 0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Período</span>
          <strong id="uz-k-periodo" style="d
===== vídeo @ 499346 =====
ems:center;justify-content:center;border:1.5px solid #E7EBF1;border-radius:10px;padding:0 8px;height:40px;gap:3px">
          <span style="font-size:11px;color:#64748B;font-weight:700;flex-shrink:0">R$</span>
          <input id="ipc-99-value" type="number" placeholder="0,00" style="border:0;outline:0;flex:1;font-size:14px;font-weight:700;color:#0F172A;font-family:inherit;min-width:0;text-align:center">
        </div>
      </div>
    </div>

    <div style="display:flex;gap:7px;background:#EFF6FF;border:1px solid rgba(14,165,233,.2);border-radius:11px;padding:9px 10px;font-size:11px;color:#0369A1;line-height:1.45;margin-bottom:12px">
      <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" style="flex:none;margin-top:1px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
      <span>Serve pra conferir se o import achou tudo — não precisa ser o dia inteiro.</span>
    </div>
    <button class="btn-p" style="margin-top:0" onclick="importPrecheckNext()">Avançar</button>
    <div style="text-align:center;font-size:12px;color:#2563EB;font-weight:700;margin-top:10px;cursor:pointer" onclick="importPrecheckSkip()">Pular essa parte →</div>
  </div>
</div>

<div class="mo" id="mo-import">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2>Importar corridas</h2><button class="xbtn" onclick="closeMo('mo-import')">&#x2715;</button></div>
    <div style="background:#EFF6FF;border:1px solid rgba(14,165,233,.2);border-radius:16px;padding:12px 14px;font-size:12px;color:#0369A1;line-height:1.6;display:flex;gap:8px;margin-bottom:16px">
      <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" style="flex-shrink:0;margin-top:2px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
      <div><strong>Como usar:</strong> grave a tela do celular rolando o histórico (Uber ou 99, tanto faz) e mande o arquivo de vídeo aqui — não dá mais pra mandar print solto. O app identifica a plataforma sozinho e extrai as corridas.</div>
    </div>
    <div style="display:flex;gap:8px;margin-bottom:8px">
      <label style="flex:1;height:56px;border-radius:14px;background:#F0F4FA;border:1px solid #E2E8F0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;cursor:pointer;font-size:11px;font-weight:700;color:#0A2F6B">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="m3 9 4-4 4 4 4-4 4 4"/></svg> Vídeo da galeria
        <input type="file" multiple accept="video/*" style="display:none" onclick="this.value=null" onchange="handleImport(this,'uber')">
      </label>
      <label style="flex:1;height:56px;border-radius:14px;background:#F0F4FA;border:1px solid #E2E8F0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;cursor:pointer;font-size:11px;font-weight:700;color:#0A2F6B">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg> Arquivos
        <input type="file" multiple accept="video/*" style="display:none" onclick="this.value=null" onchange="handleImport(this,'uber')">
      </label>
    </div>
    <div style="font-size:11px;color:#94A3B8;text-align:center;margin-bottom:14px">Só vídeo — grave a tela rolando o histórico e mande o arquivo aqui. Dá pra escolher vários de uma vez.</div>

    <label style="width:100%;height:50px;border-radius:14px;background:linear-gradient(135deg,#0A2F6B,#22C55E);display:flex;align-items:center;justify-content:center;gap:8px;cursor:pointer;font-size:12.5px;font-weight:700;color:#fff;box-shadow:0 8px 18px rgba(10,47,107,.25);position:relative;margin-bottom:8px">
      <span style="position:absolute;top:-8px;right:-6px;background:#F59E0B;color:#fff;font-size:8.5px;font-weight:900;padding:3px 6px;border-radius:999px;letter-spacing:.02em">NOVO</span>
      <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M17 8 12 3 7 8"/><path d="M12 3v12"/></svg>
      Histórico oficial do Uber (.zip)
      <input type="file" accept=".zip" style="display:none" onclick="this.value=null" onchange="handleUberZipFile(this)">
    </label>
    <div style="font-size:11px;color:#94A3B8;text-align:center;line-height:1.5;margin-bottom:12px">Baixe em: app Uber → Conta → Configurações de privacidade → "Solicitar meus dados". Você recebe um e-mail com o arquivo .zip.</div>

    <div style="display:flex;gap:6px;justify-content:center">
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px;display:flex;align-items:center;gap:4px"><i style="width:6px;height:6px;border-radius:50%;display:inline-block;background:#0EA5E9"></i> Uber</span>
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px;display:flex;align-items:center;gap:4px"><i style="width:6px;height:6px;border-radius:50%;display:inline-block;background:#F59E0B"></i> 99</span>
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px">identificados automaticamente</span>
    </div>
  </div>
</div>

<!-- Import confirm -->
<div class="mo" id="mo-import-confirm">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2 id="ic-title">Corridas detectadas</h2><button class="xbtn" onclick="closeMo('mo-import-confirm')">&#x2715;</button></div>
    <div id="ic-body" style="min-height:80px"></div>
    <div style="display:flex;gap:8px;padding-top:8px">
      <button class="btn-s" onclick="closeMo('mo-import-confirm')" style="margin-top:0">Cancelar</button>
      <button class="btn-p" id="ic-btn" onclick="confirmImport()" style="margin-top:0">Importar</button>
    </div>
  </div>
</div>

<!-- Uber ZIP: processando + resultado -->
<div class="mo ctr" id="mo-uberzip">
  <div class="sheet ctr" style="max-width:400px">
    <div id="uz-proc" style="padding:50px 20px;text-align:center">
      <div style="font-size:32px;display:inline-block;animation:uzspin 1.1s linear infinite">&#9881;</div>
      <div id="uz-proc-step" style="font-size:14px;font-weight:800;margin-top:16px">Lendo arquivo...</div>
      <div id="uz-proc-sub" style="font-size:12px;color:#64748B;margin-top:5px">Isso só leva alguns segundos</div>
      <div style="height:8px;background:#EEF2F7;border-radius:99px;overflow:hidden;margin-top:20px">
        <div id="uz-proc-bar" style="height:100%;background:linear-gradient(90deg,#0EA5E9,#22C55E);border-radius:99px;width:8%;transition:width .4s"></div>
      </div>
    </div>

    <div id="uz-res" style="display:none">
      <div style="text-align:center;padding:4px 0 16px">
        <div style="width:52px;height:52px;border-radius:16px;background:#ECFDF3;color:#16A34A;display:flex;align-items:center;justify-content:center;margin:0 auto 10px">
          <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6 9 17l-5-5"/></svg>
        </div>
        <div style="font-size:16px;font-weight:800;letter-spacing:-.02em">Histórico conferido</div>
        <div id="uz-res-sub" style="font-size:12px;color:#64748B;margin-top:3px"></div>
      </div>

      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px">
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Novas pra importar</span>
          <strong id="uz-k-novas" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;color:#16A34A;margin-top:4px">0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Já existiam</span>
          <strong id="uz-k-dup" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;color:#64748B;margin-top:4px">0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Valor das novas</span>
          <strong id="uz-k-valor" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;margin-top:4px">R$ 0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Período</span>
          <strong id="uz-k-periodo" style="display:block;font-size:12.5px;margin-top:5px">—</strong>
        </div>
      </div>

      <div id="uz-dup-callout" style="margin-top:10px;background:#F8FAFC;border:1px solid #E2E8F0;border-radius:14px;padding:11px 12px;display:none;gap:9px;align-items:flex-start">
        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#94A3B8" stroke-width="2" style="flex-shrink:0;margin-top:1px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
        <p id="uz-dup-text" style="font-size:11px;color:#64748B;line-height:1.5;margin:0"></p>
      </div>

      <div id="uz-neg-warn" style="margin-top:8px;font-size:11px;color:#B45309;display:none"></div>

      <div style="display:flex;gap:8px;margin-top:18px">
        <button class="btn-s" onclick="closeMo('mo-uberzip')" style="margin-top:0">Cancelar</button>
        <button class="btn-p" id="uz-con
===== vídeo @ 584697 =====
ElementById('cor-date-btn'); if(db){db.style.background='#EFF6FF';db.style.borderColor='#2563EB';}
  renderCorridasActive();
}
function toggleCorridasPlat(p) {
  // Multi-select agora (pedido do Yuri, 24/07/2026): nenhum marcado = mostra
  // tudo (padrão), marcar um filtra só aquele, marcar os dois volta a
  // mostrar tudo — sem precisar de um botão "Todas" separado.
  if (!S.corridasPlats) S.corridasPlats = new Set();
  if (S.corridasPlats.has(p)) S.corridasPlats.delete(p); else S.corridasPlats.add(p);
  const ub = document.getElementById('cor-plat-uber'), n99 = document.getElementById('cor-plat-99');
  if (ub) ub.classList.toggle('active', S.corridasPlats.has('uber'));
  if (n99) n99.classList.toggle('active', S.corridasPlats.has('99'));
  renderCorridasActive();
}
// ── Corridas: alterna entre Importadas (trips locais) e Automáticas (auto_trips no Supabase) ──
// ── Corridas automáticas → alimentam a Jornada (pedido do Yuri, 23/07/2026) ──
// Busca auto_trips do dia pra somar em ganhos/km/corridas da Jornada e da
// sessão certa dentro dela — sem se misturar com day.trips (Importadas),
// só somando junto na hora de mostrar total. Timestamp de referência:
// trip_started_at (início real da corrida) — cai sempre dentro da sessão
// (Jornada) que estava rodando, diferente de accepted_at que pode ser
// alguns segundos antes ainda na sessão anterior num caso raro de virada.
function autoTripTs(t) {
  const iso = t.trip_started_at || t.accepted_at;
  return iso ? new Date(iso).getTime() : 0;
}
// Corrida cai dentro do horário de alguma jornada do dia dela? Pedido do
// Yuri (24/07/2026) — o vínculo nunca é salvo, é sempre calculado (mesma
// lógica de sessionItem/renderJornada) — então dá pra checar pra QUALQUER
// corrida, de qualquer tela, sem precisar saber de antemão qual sessão.
function isAutoTripLinkedToSession(t) {
  const ts = autoTripTs(t);
  if (!ts) return false;
  const d = new Date(ts);
  const dateStr = d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
  const sessions = (DB.day(dateStr).sessions) || [];
  return sessions.some(s => {
    const start = s.start || 0, end = s.end || 0;
    return ts >= start && (!end || ts <= end);
  });
}

// ── Reconferir corrida usando o texto bruto do log (24/07/2026) ────────────
// Pedido do Yuri: em vez de tentar "adivinhar" o valor certo de uma corrida
// já salva errada, reaplica a MESMA lógica de leitura (já corrigida no app
// nativo em 24/07/2026) em cima do texto ORIGINAL da tela, salvo em
// trip_reader_log — usa o dado real daquele instante, não um chute.
// Porta fiel de parseOffer() do TripReaderService.kt (Kotlin) — mesmas
// regras, mesma ordem, incluindo as 3 correções de 24/07 (R$/km nunca
// compete como valor total; dinâmico soma os 2 tipos da 99 + Uber; linha
// de avaliação/corridas nunca vira endereço).
function moneyToDoubleJS(raw) {
  const clean = raw.replace('R$','').trim().replace(/\./g,'').replace(',','.');
  const n = parseFloat(clean);
  return isNaN(n) ? null : n;
}
function extractMoneyJS(s) {
  // CORRIGIDO (24/07/2026) - mesma correção do Kotlin: centavos passam a
  // ser obrigatórios, evita aceitar "R$96" como valor quando o OCR engoliu
  // a vírgula de "R$9,60".
  const re = /R\$\s?\d{1,4}[.,]\d{2}/g;
  const out = []; let m;
  while ((m = re.exec(s))) if (!out.includes(m[0])) out.push(m[0]);
  return out;
}
// Validação de endereço — GLOBAL (promovida de dentro de parseOfferJS em
// 16/08/2026 pra ser reutilizável também na importação de vídeo, onde
// endereço de corrida nova nunca pode ser aceito sem essa checagem —
// pedido explícito do Yuri: "nunca endereço, a não ser que garanta que
// não vai vir lixo ou inventado"). Mesma lógica usada no Kotlin.
function looksLikeAddress(s) {
  const sl = s.toLowerCase().trim();
  if (sl.length < 6) return false;
  if (sl.includes('parada')) return false;
  if (sl.includes('não afeta') || sl.includes('nao afeta')) return false;
  if (/^\(?\d{1,3}([.,]\d+)?\s*(min|km|m)\b/.test(sl)) return false;
  if (/^[x%\d.,\s]+$/.test(sl)) return false;
  if (sl.includes('r$')) return false;
  if (sl.includes('tarifa') || sl.includes('incl.') || sl.includes('incluído') || sl.includes('incluido')) return false;
  if (sl.includes('taxa de espera') || sl.includes('espera longa')) return false;
  if (sl === 'online' || sl === 'buscando' || sl === 'offline' || sl === 'conectar') return false;
  if (sl.includes('perfil essencial') || sl.includes('perfil premium') || sl.includes('perfil prata')) return false;
  if (sl.includes('pgto. no app') || sl === 'dinheiro' || sl === 'negocia' || sl === 'qr code') return false;
  if (/[1-5][.,]\d{2}.{0,15}corrid/.test(sl)) return false;
  if (sl.includes('verif.') || sl.includes('cpf e cart')) return false;
  const hasStreetWord = /\b(rua|av\.?|avenida|estrada|travessa|alameda|rodovia|pra[çc]a|largo|ladeira|rod\.?|via\b)/i.test(sl);
  const hasNumberPattern = /[^\d],\s*\d{1,5}\b/.test(sl);
  if (!hasStreetWord && !hasNumberPattern) return false;
  return true;
}
function parseOfferJS(texts) {
  const joinedRaw = texts.join('  ');
  const low = joinedRaw.toLowerCase()
    .replace(/\b[lI](?=[.,]\d)/g, '1')
    .replace(/(?<=\d[.,])[lI]\b/g, '1');

  let valor = null;
  let m = /aceitar por\s*r\$\s*([\d.]+,\d{2})/.exec(low);
  if (m) valor = moneyToDoubleJS(m[1]);

  let rkmDirect = null;
  m = /r\$\s*([\d.]+,\d{2})\s*\/\s*km/.exec(low);
  if (m) rkmDirect = moneyToDoubleJS(m[1]);

  let totMin = 0, totKm = 0, legs = 0;
  const legsList = [];
  function addLeg(minsStr, distStr, unit) {
    const mins = parseInt(minsStr, 10);
    const dist = parseFloat(distStr.replace(',', '.'));
    if (isNaN(mins) || isNaN(dist)) return;
    const km2 = unit === 'm' ? dist/1000 : dist;
    if (mins >= 1 && mins <= 90 && km2 >= 0.05 && km2 <= 80) {
      totMin += mins; totKm += km2; legs++; legsList.push([mins, km2]);
    }
  }
  let re = /(\d{1,3})\s*min(?:utos)?\s*\((\d+(?:[.,]\d+)?)\s*(m|km)\)/g;
  while ((m = re.exec(low))) addLeg(m[1], m[2], m[3]);
  re = /\(\s*(\d{1,3})\s*min(?:utos)?\s+(\d+(?:[.,]\d+)?)\s*(m|km)\s*\)/g;
  while ((m = re.exec(low))) addLeg(m[1], m[2], m[3]);

  let km = (legs > 0 && totKm > 0) ? totKm : null;
  let min = (legs > 0 && totMin > 0) ? totMin : null;
  const kmPickup = legsList[0] ? legsList[0][1] : null;
  const kmTrip = legsList[1] ? legsList[1][1] : null;
  const minPickup = legsList[0] ? legsList[0][0] : null;
  const minTrip = legsList[1] ? legsList[1][0] : null;

  let dinamicoBase = 0, dinamicoEspera = 0, dinamicoEmbarque = 0, dinamicoDeslocamento = 0, dinamicoUber = 0;
  m = /r\$\s*([\d.]+,\d{2})\s*tarifa base din[aâ]mica/.exec(low);
  if (m) dinamicoBase = moneyToDoubleJS(m[1]) || 0;
  m = /r\$\s*([\d.]+,\d{2})\s*por espera longa/.exec(low);
  if (m) dinamicoEspera = moneyToDoubleJS(m[1]) || 0;
  m = /r\$\s*([\d.]+,\d{2})\s*por embarque distante/.exec(low);
  if (m) dinamicoEmbarque = moneyToDoubleJS(m[1]) || 0;
  m = /r\$\s*([\d.]+,\d{2})\s*por deslocamento/.exec(low);
  if (m) dinamicoDeslocamento = moneyToDoubleJS(m[1]) || 0;
  m = /\+\s*r\$\s*([\d.]+,\d{2})\s*inclu[ií]do/.exec(low);
  if (m) dinamicoUber = moneyToDoubleJS(m[1]) || 0;
  const dinamico = dinamicoBase + dinamicoEspera + dinamicoEmbarque + dinamicoDeslocamento + dinamicoUber;

  if (valor === null) {
    let monies = extractMoneyJS(joinedRaw).map(moneyToDoubleJS).filter(v => v !== null && v > 0);
    if (rkmDirect !== null) monies = monies.filter(v => Math.abs(v - rkmDirect) > 0.01);
    if (rkmDirect !== null && km !== null) {
      const found = monies.find(v => Math.abs(v/km - rkmDirect)/rkmDirect < 0.35);
      valor = found !== undefined ? found : null;
    } else if (km !== null && km > 0) {
      const found = monies.find(v => (v/km) >= 0.4 && (v/km) <= 12.0);
      valor = found !== undefined ? found : (monies[0] !== undefined ? monies[0] : null);
    } else {
      valor = monies[0] !== undefined ? monies[0] : null;
    }
  }
  if (km === null && valor !== null && rkmDirect !== null && rkmDirect > 0) km = valor / rkmDirect;

  const legLineRe = /^\(?\s*\d{1,3}\s*min(?:utos)?/i;
  const addrCandidates = [];
  for (let i = 0; i < texts.length; i++) {
    if (legLineRe.test(texts[i])) {
      for (let j = i+1; j <= Math.min(i+3, texts.length-1); j++) {
        const cand = (texts[j]||'').trim();
        if (!cand) break;
        const nl = cand.toLowerCase();
        if (legLineRe.test(cand)) break;
        if (nl.includes('aceitar') || nl.includes('selecionar') || nl.includes('escolher')) break;
        if (looksLikeAddress(cand)) { addrCandidates.push(cand); break; }
      }
    }
  }
  return {
    valor, km, min, rkmDirect, dinamico,
    origem: addrCandidates[0] || null, destino: addrCandidates[1] || null,
    kmPickup, kmTrip, minPickup, minTrip
  };
}
// Busca a leitura mais completa (mais linhas no 'raw') da oferta dessa
// corrida, num intervalo de 10min antes do aceite até 30s depois — mesma
// janela usada pelo cache de oferta no app nativo.
async function fetchBestOfferTexts(t) {
  // Retorna { texts, reason } em vez de só null/array — pedido do Yuri
  // (24/07/2026): precisa ver o motivo exato na tela (sem abrir console do
  // navegador) quando não achar nada, pra investigar mais rápido.
  if (!_supaUser) return { texts: null, reason: 'sem sessão ativa' };
  const plat = t.platform === 'uber' ? 'UBER' : '99';
  const anchor = t.accepted_at || t.trip_started_at;
  if (!anchor) return { texts: null, reason: 'corrida sem horário de aceite salvo' };
  const anchorMs = new Date(anchor).getTime();
  const from = new Date(anchorMs - 10*60*1000).toISOString();
  const to = new Date(anchorMs + 30*1000).toISOString();
  const { data, error } = await _SUPA.from('trip_reader_log').select('texts,created_at')
    .eq('user_id', _supaUser.id).eq('platform', plat)
    .gte('created_at', from).lte('created_at', to)
    .order('created_at', { ascending: false })
    // CONFIRMADO EM DADO REAL (24/07/2026): um intervalo de 10min30s desses
    // teve 523 
===== vídeo @ 599222 =====
gth) { showToast('Corrida atualizada com sucesso', 'success'); return; }
    const finish = async () => {
      const saved = await _applyAutoTripRefresh(r.tripId, r.patch);
      if (!saved) { showToast('Erro ao salvar.', 'error'); return; }
      showToast('Corrigido: '+r.changes.join(', '), 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    };
    if (r.valueBigChange) {
      showConfirm(
        `O valor dessa corrida vai mudar de ${DB.fR(r.oldVal)} para ${DB.fR(r.newVal)} — uma diferença grande.\n\nConfirma que ${DB.fR(r.newVal)} é o valor certo?`,
        finish,
        { danger: false, confirmLabel: 'Sim, corrigir', cancelLabel: 'Cancelar' }
      );
    } else {
      await finish();
    }
  } catch (e) {
    console.error('refreshAutoTripFromLog', e);
    showToast('Erro ao reconferir.', 'error');
  } finally {
    if (btnEl) btnEl.classList.remove('spinning');
  }
}
// Rotina automática (24/07/2026, pedido do Yuri) — a cada 5min, tenta
// reconferir sozinho corridas "Aguardando revisão" (status='capturada')
// das últimas 48h. Silenciosa por corrida (não interrompe com toast a
// cada uma) — só avisa se corrigiu alguma, no total. Nunca mexe em
// corrida já confirmada, só nas que ainda não têm valor nenhum.
async function _autoRefreshPendingTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'capturada')
      .gte('accepted_at', new Date(Date.now() - 48*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixed = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (r.ok && r.changes.length) { await _applyAutoTripRefresh(r.tripId, r.patch); fixed++; }
      } catch(e) {}
    }
    if (fixed > 0) {
      showToast(fixed===1 ? '1 corrida corrigida sozinha' : fixed+' corridas corrigidas sozinhas', 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
  } catch (e) { console.error('_autoRefreshPendingTrips', e); }
}
setTimeout(()=>{ _autoRefreshPendingTrips(); setInterval(_autoRefreshPendingTrips, 5*60*1000); }, 6000);

// Rotina automática nº2 (16/08/2026, pedido do Yuri: "não quero usuário
// editando, tem que partir da gente interno... a cada X min confere o
// que tá no registro e corrige"). Diferente da rotina acima (que só olha
// corridas AINDA sem valor confirmado), essa reconfere corridas que JÁ
// estão 'confirmada' — pra pegar o caso real que aconteceu (valor errado
// que já tinha virado card normal na tela). Roda a cada 10min, olha só as
// últimas 24h (corrida mais velha que isso já foi conferida o suficiente
// de vezes). REGRA DE OURO: só aplica sozinha quando a mudança é pequena/
// confiável — mudança grande NUNCA aplica sem humano olhar (mesmo critério
// do botão manual), só marca value_needs_review pra aparecer sinalizada,
// sem sumir da tela nem dos totais (não é "fantasma", é corrida real que
// só precisa de confirmação do valor).
//
// MUDOU (16/08/2026): mirava em status='confirmada', mas agora
// 'confirmada' só nasce depois do vídeo do histórico (ver
// confirmImport()) — reconferir isso aqui não faz mais sentido, vídeo já
// É a fonte mais confiável que existe. Passa a mirar em 'estimada' (ciclo
// da captura ao vivo completo, só com valor da oferta) — mesmo espírito
// de antes, refinando o valor com a melhor leitura do log disponível
// enquanto o vídeo de confirmação não chega.
async function _autoReconcileConfirmedTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'estimada')
      .is('data_quality_flag', null)
      .gte('accepted_at', new Date(Date.now() - 24*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixedQuiet = 0, flaggedForReview = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (!r.ok || !r.changes.length) continue;
        if (r.valueBigChange) {
          await _SUPA.from('auto_trips').update({ value_needs_review: true }).eq('id', row.id);
          flaggedForReview++;
        } else {
          await _applyAutoTripRefresh(r.tripId, r.patch);
          fixedQuiet++;
        }
      } catch(e) {}
    }
    if (fixedQuiet > 0 || flaggedForReview > 0) {
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
    // Silenciosa de propósito — mudança pequena não precisa de aviso, e
    // mudança grande já fica visível pelo badge "Valor a confirmar" na
    // tela Corridas, sem precisar interromper com toast.
  } catch (e) { console.error('_autoReconcileConfirmedTrips', e); }
}
setTimeout(()=>{ _autoReconcileConfirmedTrips(); setInterval(_autoReconcileConfirmedTrips, 10*60*1000); }, 9000);
// Balãozinho discreto ao tocar num símbolo (dinâmico, vínculo à jornada,
// dinheiro) — pedido do Yuri (24/07/2026). Some sozinho, não navega nem
// abre nada.
function showTapLegend(event, text) {
  event.stopPropagation();
  document.querySelectorAll('.tap-legend-tip').forEach(el => el.remove());
  const wrap = event.currentTarget;
  const tip = document.createElement('div');
  tip.className = 'tap-legend-tip';
  tip.textContent = text;
  wrap.appendChild(tip);
  setTimeout(() => tip.remove(), 2500);
}
// Legenda de comparação previsto × real (Deslocamento/Corrida coloridos) —
// abre/fecha um painel fixo embaixo da linha, diferente do balãozinho dos
// símbolos porque tem mais texto (2 linhas de números).
function toggleCmpLegend(event, id) {
  event.stopPropagation();
  const el = document.getElementById(id);
  if (el) el.style.display = el.style.display === 'none' ? '' : 'none';
}
async function loadAutoTripsForDay(dateStr) {
  if (!_supaUser) return [];
  // O dia da Jornada é o dia LOCAL do motorista. Converte meia-noite local
  // para UTC antes de consultar timestamptz — evita perder corridas após 21h
  // no Brasil quando o relógio UTC já virou para o dia seguinte.
  const fromLocal = new Date(dateStr+'T00:00:00');
  const toLocal = new Date(fromLocal); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  // null = falha de rede/consulta; [] = consulta válida sem corridas.
  return error ? null : (data||[]);
}
// Recusadas do dia — mesmo padrão de loadAutoTripsForDay, pra alimentar o
// contador "X aceitas / Y recusadas" do Total do dia. Tabela já existe e já
// é usada na tela Corridas (seção expansível); aqui só reaproveita pro
// recorte de UM dia em vez do período todo.
async function loadDeclinedOffersForDay(dateStr) {
  if (!_supaUser) return [];
  const from = dateStr+'T00:00:00', to = dateStr+'T23:59:59';
  const { data, error } = await _SUPA.from('declined_offers').select('id')
    .eq('user_id', _supaUser.id)
    .gte('seen_at', from).lte('seen_at', to);
  return error ? [] : (data||[]);
}

// ── Corridas automáticas → alimentam a Home (pedido do Yuri, 23/07/2026) ──
// Mesmo gap da Jornada, só que espalhado pela Home inteira: valor, km,
// corridas, todos os gráficos e o insight de IA só olhavam day.trips
// (Importadas) e ficavam cegos pra qualquer dia 100% automático. Em vez de
// corrigir função por função (arriscado, muita superfície), busca TODAS as
// automáticas do período de UMA VEZ (não uma consulta por dia — helper
// abaixo cobre semana/mês/ano inteiros numa chamada só) e injeta como
// corridas "de mentira" no formato de trip normal — todo código que já lê
// day.trips corretamente passa a enxergar as automáticas de graça, sem
// precisar tocar em cada gráfico individualmente.
async function loadAutoTripsIndexed(fromDate, toDate) {
  if (!_supaUser) return {};
  const fromLocal = new Date(fromDate+'T00:00:00');
  const toLocal = new Date(toDate+'T00:00:00'); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  if (error || !data) return {};
  const map = {};
  data.forEach(t => {
    const iso = t.trip_started_at || t.accepted_at;
    if (!iso) return;
    const dt = new Date(iso);
    const dateStr = dt.getFullYear()+'-'+p2(dt.getMonth()+1)+'-'+p2(dt.getDate());
    (map[dateStr] = map[dateStr] || []).push(t);
  });
  return map;
}
// Converte as automáticas de um dia (já indexadas em S.autoTripsByDate) pro
// MESMO formato de objeto que day.trips usa — value/km/time/duration/
// platform/dinheiro — assim qualquer soma/filtro que já existe funciona
// sem mudança nenhuma. NUNCA usar pra salvar (é só leitura/exibição —
// misturar isso em day.trips de verdade duplicaria dado no Supabase).
function autoTripsAsTrips(date) {
  const list = (S.autoTripsByDate && S.autoTripsByDate[date]) || [];
  return list.map(at => {
    const start = at.trip_started_at ? new Date(at.trip_started_at) : null;
    const end   = at.trip_ended_at ? new Date(at.trip_ended_at) : null;
    const durMin = start && end ? Math.max(0, Math.round((end-start)/60000))
                 : Math.round((at.offer_duration_trip_sec||0)/60);
    return {
===== vídeo @ 599552 =====
 null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    };
    if (r.valueBigChange) {
      showConfirm(
        `O valor dessa corrida vai mudar de ${DB.fR(r.oldVal)} para ${DB.fR(r.newVal)} — uma diferença grande.\n\nConfirma que ${DB.fR(r.newVal)} é o valor certo?`,
        finish,
        { danger: false, confirmLabel: 'Sim, corrigir', cancelLabel: 'Cancelar' }
      );
    } else {
      await finish();
    }
  } catch (e) {
    console.error('refreshAutoTripFromLog', e);
    showToast('Erro ao reconferir.', 'error');
  } finally {
    if (btnEl) btnEl.classList.remove('spinning');
  }
}
// Rotina automática (24/07/2026, pedido do Yuri) — a cada 5min, tenta
// reconferir sozinho corridas "Aguardando revisão" (status='capturada')
// das últimas 48h. Silenciosa por corrida (não interrompe com toast a
// cada uma) — só avisa se corrigiu alguma, no total. Nunca mexe em
// corrida já confirmada, só nas que ainda não têm valor nenhum.
async function _autoRefreshPendingTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'capturada')
      .gte('accepted_at', new Date(Date.now() - 48*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixed = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (r.ok && r.changes.length) { await _applyAutoTripRefresh(r.tripId, r.patch); fixed++; }
      } catch(e) {}
    }
    if (fixed > 0) {
      showToast(fixed===1 ? '1 corrida corrigida sozinha' : fixed+' corridas corrigidas sozinhas', 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
  } catch (e) { console.error('_autoRefreshPendingTrips', e); }
}
setTimeout(()=>{ _autoRefreshPendingTrips(); setInterval(_autoRefreshPendingTrips, 5*60*1000); }, 6000);

// Rotina automática nº2 (16/08/2026, pedido do Yuri: "não quero usuário
// editando, tem que partir da gente interno... a cada X min confere o
// que tá no registro e corrige"). Diferente da rotina acima (que só olha
// corridas AINDA sem valor confirmado), essa reconfere corridas que JÁ
// estão 'confirmada' — pra pegar o caso real que aconteceu (valor errado
// que já tinha virado card normal na tela). Roda a cada 10min, olha só as
// últimas 24h (corrida mais velha que isso já foi conferida o suficiente
// de vezes). REGRA DE OURO: só aplica sozinha quando a mudança é pequena/
// confiável — mudança grande NUNCA aplica sem humano olhar (mesmo critério
// do botão manual), só marca value_needs_review pra aparecer sinalizada,
// sem sumir da tela nem dos totais (não é "fantasma", é corrida real que
// só precisa de confirmação do valor).
//
// MUDOU (16/08/2026): mirava em status='confirmada', mas agora
// 'confirmada' só nasce depois do vídeo do histórico (ver
// confirmImport()) — reconferir isso aqui não faz mais sentido, vídeo já
// É a fonte mais confiável que existe. Passa a mirar em 'estimada' (ciclo
// da captura ao vivo completo, só com valor da oferta) — mesmo espírito
// de antes, refinando o valor com a melhor leitura do log disponível
// enquanto o vídeo de confirmação não chega.
async function _autoReconcileConfirmedTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'estimada')
      .is('data_quality_flag', null)
      .gte('accepted_at', new Date(Date.now() - 24*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixedQuiet = 0, flaggedForReview = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (!r.ok || !r.changes.length) continue;
        if (r.valueBigChange) {
          await _SUPA.from('auto_trips').update({ value_needs_review: true }).eq('id', row.id);
          flaggedForReview++;
        } else {
          await _applyAutoTripRefresh(r.tripId, r.patch);
          fixedQuiet++;
        }
      } catch(e) {}
    }
    if (fixedQuiet > 0 || flaggedForReview > 0) {
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
    // Silenciosa de propósito — mudança pequena não precisa de aviso, e
    // mudança grande já fica visível pelo badge "Valor a confirmar" na
    // tela Corridas, sem precisar interromper com toast.
  } catch (e) { console.error('_autoReconcileConfirmedTrips', e); }
}
setTimeout(()=>{ _autoReconcileConfirmedTrips(); setInterval(_autoReconcileConfirmedTrips, 10*60*1000); }, 9000);
// Balãozinho discreto ao tocar num símbolo (dinâmico, vínculo à jornada,
// dinheiro) — pedido do Yuri (24/07/2026). Some sozinho, não navega nem
// abre nada.
function showTapLegend(event, text) {
  event.stopPropagation();
  document.querySelectorAll('.tap-legend-tip').forEach(el => el.remove());
  const wrap = event.currentTarget;
  const tip = document.createElement('div');
  tip.className = 'tap-legend-tip';
  tip.textContent = text;
  wrap.appendChild(tip);
  setTimeout(() => tip.remove(), 2500);
}
// Legenda de comparação previsto × real (Deslocamento/Corrida coloridos) —
// abre/fecha um painel fixo embaixo da linha, diferente do balãozinho dos
// símbolos porque tem mais texto (2 linhas de números).
function toggleCmpLegend(event, id) {
  event.stopPropagation();
  const el = document.getElementById(id);
  if (el) el.style.display = el.style.display === 'none' ? '' : 'none';
}
async function loadAutoTripsForDay(dateStr) {
  if (!_supaUser) return [];
  // O dia da Jornada é o dia LOCAL do motorista. Converte meia-noite local
  // para UTC antes de consultar timestamptz — evita perder corridas após 21h
  // no Brasil quando o relógio UTC já virou para o dia seguinte.
  const fromLocal = new Date(dateStr+'T00:00:00');
  const toLocal = new Date(fromLocal); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  // null = falha de rede/consulta; [] = consulta válida sem corridas.
  return error ? null : (data||[]);
}
// Recusadas do dia — mesmo padrão de loadAutoTripsForDay, pra alimentar o
// contador "X aceitas / Y recusadas" do Total do dia. Tabela já existe e já
// é usada na tela Corridas (seção expansível); aqui só reaproveita pro
// recorte de UM dia em vez do período todo.
async function loadDeclinedOffersForDay(dateStr) {
  if (!_supaUser) return [];
  const from = dateStr+'T00:00:00', to = dateStr+'T23:59:59';
  const { data, error } = await _SUPA.from('declined_offers').select('id')
    .eq('user_id', _supaUser.id)
    .gte('seen_at', from).lte('seen_at', to);
  return error ? [] : (data||[]);
}

// ── Corridas automáticas → alimentam a Home (pedido do Yuri, 23/07/2026) ──
// Mesmo gap da Jornada, só que espalhado pela Home inteira: valor, km,
// corridas, todos os gráficos e o insight de IA só olhavam day.trips
// (Importadas) e ficavam cegos pra qualquer dia 100% automático. Em vez de
// corrigir função por função (arriscado, muita superfície), busca TODAS as
// automáticas do período de UMA VEZ (não uma consulta por dia — helper
// abaixo cobre semana/mês/ano inteiros numa chamada só) e injeta como
// corridas "de mentira" no formato de trip normal — todo código que já lê
// day.trips corretamente passa a enxergar as automáticas de graça, sem
// precisar tocar em cada gráfico individualmente.
async function loadAutoTripsIndexed(fromDate, toDate) {
  if (!_supaUser) return {};
  const fromLocal = new Date(fromDate+'T00:00:00');
  const toLocal = new Date(toDate+'T00:00:00'); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  if (error || !data) return {};
  const map = {};
  data.forEach(t => {
    const iso = t.trip_started_at || t.accepted_at;
    if (!iso) return;
    const dt = new Date(iso);
    const dateStr = dt.getFullYear()+'-'+p2(dt.getMonth()+1)+'-'+p2(dt.getDate());
    (map[dateStr] = map[dateStr] || []).push(t);
  });
  return map;
}
// Converte as automáticas de um dia (já indexadas em S.autoTripsByDate) pro
// MESMO formato de objeto que day.trips usa — value/km/time/duration/
// platform/dinheiro — assim qualquer soma/filtro que já existe funciona
// sem mudança nenhuma. NUNCA usar pra salvar (é só leitura/exibição —
// misturar isso em day.trips de verdade duplicaria dado no Supabase).
function autoTripsAsTrips(date) {
  const list = (S.autoTripsByDate && S.autoTripsByDate[date]) || [];
  return list.map(at => {
    const start = at.trip_started_at ? new Date(at.trip_started_at) : null;
    const end   = at.trip_ended_at ? new Date(at.trip_ended_at) : null;
    const durMin = start && end ? Math.max(0, Math.round((end-start)/60000))
                 : Math.round((at.offer_duration_trip_sec||0)/60);
    return {
      id: 'auto_'+at.id, _auto: true,
      platform: at.platform || 'uber',
      time: start ? p2(start.getHours())+':'+p2(start.getMinutes()) : '',
      duration: durMin,
      value: parseFloat(at.offer_value)||0,
      km: parseFloat(at.real_km_trip ?? at.offer_km_trip) || 0,
      dinheiro: !!at.dinheiro,
      origin:''
===== vídeo @ 640392 =====
tElementById('journey-perm-warning');
  if (!permWarn && jc.parentNode) {
    permWarn = document.createElement('div'); permWarn.id = 'journey-perm-warning'; permWarn.onclick = openFlashPermissionSetup;
    permWarn.style.cssText = 'display:none;align-items:flex-start;justify-content:space-between;gap:10px;background:#FFF7ED;border:1px solid #FDBA74;border-radius:14px;padding:11px 12px;margin:0 0 10px;cursor:pointer';
    permWarn.innerHTML = '<div style="min-width:0"><div style="font-size:12.5px;font-weight:800;color:#9A3412">⚠ Funções do MōB incompletas</div><div id="journey-perm-warning-text" style="font-size:11px;color:#C2410C;line-height:1.35;margin-top:2px"></div></div><span style="font-size:11px;font-weight:800;color:#9A3412;white-space:nowrap">Ativar ›</span>';
    jc.parentNode.insertBefore(permWarn, jc);
  }
  renderFlashPermissionWarnings();
  const cardDate = day?.date || S.curDate || DB.today();
  // Card de sessão atual (timer/km/botão) só faz sentido em "hoje" — em dia
  // passado não existe jornada "em andamento" possível, então esse card
  // some por completo. O restante da função continua rodando igual: os
  // valores que ela calcula (jc-trips, jc-total, jc-kmtotal, jc-kmativo,
  // jc-kmdesl, jc-rsh, jc-rskm) agora moram no card "Total do dia" — que é
  // um elemento IRMÃO de journey-card, não um filho, e precisa continuar
  // sendo populado em qualquer dia, passado ou presente.
  const jcWrap = document.getElementById('journey-wrap');
  if (jcWrap) jcWrap.style.display = isToday ? '' : 'none';
  const g = S.gps;
  const finished = day.sessions || [];
  const finishedElapsed = finished.reduce((s,x)=>s+(x.elapsed||0),0);
  const finishedKm = finished.reduce((s,x)=>s+(x.km||0),0);
  const live = isToday && g.st === 'running';
  const liveElapsed = live ? getElapsed() : 0;
  const liveKm = live ? g.km : 0;
  const totalElapsed = Math.max(0, finishedElapsed + liveElapsed + (day.tempoAdjust || 0));
  const totalKm = finishedKm + liveKm + (day.kmAdjust || 0);
  const state = isToday ? g.st : 'idle';

  let cls = 'idle';
  if (state === 'running') cls = 'running';
  else if (finished.length > 0) cls = 'ended';
  jc.className = 'journey ' + cls;

  setText('jc-status-text',
    state === 'running' ? 'Online' :
    finished.length > 0 ? 'Concluído' : 'Iniciar'
  );
  const th = Math.floor(liveElapsed/3600), tm = Math.floor(liveElapsed%3600/60), ts = liveElapsed%60;
  setText('jc-timer', p2(th)+':'+p2(tm)+':'+p2(ts));
  setText('jc-sub',
    state === 'running' ? 'Sessão atual · iniciada '+fmtHM(g.start) :
    finished.length > 0 ? finished.length+(finished.length===1?' jornada hoje':' jornadas hoje') :
    'Nenhuma sessão iniciada'
  );
  setText('jc-km', liveKm.toFixed(1));
  const tth = Math.floor(totalElapsed/3600), ttm = Math.floor(totalElapsed%3600/60);
  setText('jc-total', tth+'h'+p2(ttm)+'m');
  setText('jc-kmtotal', totalKm.toFixed(1)+' km');
  // CORRIGIDO (23/07/2026, confirmado com dado real pelo Yuri): esses 4
  // KPIs do card principal (corridas, km ativo, R$/h, R$/km) só somavam
  // day.trips (Importadas) — sessão só com automáticas mostrava tudo
  // zerado mesmo com corridas de verdade rodando. Mesma correção que já
  // tinha sido feita em rev/sessionItem/renderMetasDoDia, faltava aqui.
  const autoTripsHoje = day.autoTrips || [];

  // Banner "aguardando confirmação" — pedido do Yuri (16/08/2026), substitui
  // o botão fixo "Importar corridas". Conta corridas que ainda não foram
  // confirmadas por vídeo do histórico (status 'capturada' ou 'estimada') —
  // 'confirmada' só existe depois do vídeo, então isso mede exatamente o
  // que falta conferir naquele dia. Aparece em qualquer dia navegado, some
  // sozinho quando não sobra pendência.
  const pcbEl = document.getElementById('pending-confirm-banner');
  if (pcbEl) {
    // Não interpreta cache ainda carregando como zero corridas. Durante refresh,
    // mantém o último estado visual válido do mesmo dia.
    const autoTripsReady = S._autoTripsDataDate === cardDate;
    if (autoTripsReady) {
      const pendingCount = autoTripsHoje.filter(t => t.status !== 'confirmada' || t.value_needs_review === true).length;
      pcbEl.dataset.dataDate = cardDate;
      if (pendingCount > 0) {
        pcbEl.style.display = 'flex';
        setText('pcb-title', pendingCount + (pendingCount===1 ? ' corrida aguardando confirmação' : ' corridas aguardando confirmação'));
      } else {
        pcbEl.style.display = 'none';
      }
    } else if (pcbEl.dataset.dataDate && pcbEl.dataset.dataDate !== cardDate) {
      pcbEl.style.display = 'none';
      pcbEl.dataset.dataDate = cardDate;
    }
  }

  // Barra Online/Buscando passageiro/Em corrida — real, vinda da máquina de
  // estados do APK (ver computeStatusBreakdown). No card da jornada ATUAL
  // só faz sentido enquanto a sessão está rodando (é o que está sendo
  // contado agora); ao encerrar, o dado sai daqui e já está consolidado no
  // "Total do dia" logo abaixo — pedido do Yuri (16/08/2026), evita
  // duplicar/deixar a mesma barra "grudada" depois de já ter encerrado.
  // As barras são atualizadas exclusivamente pela timeline contínua V3.
  // A rotina base não sobrescreve mais a leitura real com auto_trips.
  const jcBd = document.getElementById('jc-breakdown');
  const tdBdRow = document.getElementById('td-bd-row');

  setText('jc-trips', day.trips.length + autoTripsHoje.length);
  setText('jc-trips2', day.trips.length + autoTripsHoje.length);
  const aceitasCount = day.trips.length + autoTripsHoje.length;
  const declinedCount = (S.declined || []).length;
  setText('jc-aceitas', aceitasCount);
  setText('jc-recusadas', declinedCount);
  const corridasSubEl = document.getElementById('jc-corridas-sub');
  if (corridasSubEl) corridasSubEl.style.display = (aceitasCount>0 || declinedCount>0) ? 'flex' : 'none';
  const kmAtivo = day.trips.reduce((s,t)=>s+(t.km||0),0)
                + autoTripsHoje.reduce((s,t)=>s+(parseFloat(t.real_km_trip ?? t.offer_km_trip)||0),0);
  const kmDesl = Math.max(0, totalKm - kmAtivo);
  setText('jc-kmativo', kmAtivo.toFixed(1));
  setText('jc-kmdesl', kmDesl.toFixed(1));
  const ganhosDia = day.trips.reduce((s,t)=>s+(t.value||0),0)
                  + autoTripsHoje.reduce((s,t)=>s+(t.offer_value||0),0);
  const horasDecimal = totalElapsed > 0 ? totalElapsed/3600 : 0;
  const rsh  = horasDecimal > 0 ? ganhosDia/horasDecimal : null;
  const rskm = totalKm > 0 ? ganhosDia/totalKm : null;
  setText('jc-rsh', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  setText('jc-rsh2', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm2', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  // Cor por indicador — pedido do Yuri (23/07/2026, mockup aprovado):
  // mesmos limiares vermelho/verde que o motorista já ajusta na régua do
  // MōB Flash (Indicadores → R$/km e R$/hora), cada indicador troca de cor
  // por conta própria. Sem dado ainda (sessão não rodou tempo/km) fica
  // neutro (verde), não faz sentido alarmar antes de ter dado de verdade.
  const rateEl = document.getElementById('jc-rate-hora');
  const kmEl   = document.getElementById('jc-rate-km');
  const rateEl2 = document.getElementById('jc-rate-hora2');
  const kmEl2   = document.getElementById('jc-rate-km2');
  const rateCls = rsh !== null ? rateColorClass('rhora', rsh) : '';
  const kmCls   = rskm !== null ? rateColorClass('rkm', rskm) : '';
  if (rateEl)  rateEl.className  = 'td-rcard ' + rateCls;
  if (kmEl)    kmEl.className    = 'td-rcard ' + kmCls;
  if (rateEl2) rateEl2.className = 'rate-cell ' + rateCls;
  if (kmEl2)   kmEl2.className   = 'rate-cell ' + kmCls;

  const actions = document.getElementById('jc-actions');
  if (!actions) return;
  if (!isToday) {
    actions.innerHTML = '';
  } else if (state === 'idle') {
    actions.innerHTML = `<button class="j-round-btn play" onclick="gpsStart()" aria-label="Iniciar jornada"><svg viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg></button>`;
  } else if (state === 'running') {
    actions.innerHTML = '<button class="j-round-btn stop" onclick="confirmStop()" aria-label="Encerrar jornada"><svg viewBox="0 0 24 24" fill="currentColor"><rect x="6" y="6" width="12" height="12"/></svg></button>';
  }
}

function agMinutesBetween(start, end) {
  if (!start || !end) return 0;
  const [h1,m1] = start.split(':').map(Number);
  const [h2,m2] = end.split(':').map(Number);
  return Math.max(0, (h2*60+m2) - (h1*60+m1));
}
function openMetasHojeModal() {
  const date = S.curDate || DB.today();
  const day = DB.day(date);
  const dow = new Date(date+'T12:00').getDay();
  const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
  const meta = DB.meta();
  const ov = day.metaOverride || null;
  const dimMes = new Date(new Date(date+'T12:00').getFullYear(), new Date(date+'T12:00').getMonth()+1, 0).getDate();
  const moneyGoal = (ov && ov.money) ? ov.money : ((plan && plan.moneyGoal) ? plan.moneyGoal : (meta.general ? meta.general/dimMes : 0));
  const kmGoal = (ov && ov.km) ? ov.km : (plan && plan.kmGoal ? plan.kmGoal : 0);
  const schedMin = (ov && ov.hours) ? ov.hours*60 : (plan ? (plan.schedules||[]).reduce((s,b)=>s+agMinutesBetween(b.start,b.end),0) : 0);
  const mEl = document.getElementById('mh-money'); if (mEl) mEl.value = moneyGoal ? moneyGoal.toFixed(2) : '';
  const kEl = document.getElementById('mh-km'); if (kEl) kEl.value = kmGoal || '';
  const hEl = document.getElementById('mh-hours'); if (hEl) hEl.value = schedMin ? (schedMin/60).toFixed(1) : '';
  const subEl = document.getElementById('mh-sub');
  if (subEl) {
    const isToday = date === DB.today();
    subEl.textContent = isToday ? 'Vale só para hoje' : ('Vale só para ' + new Date(date+'T12:00').toLocaleDateString('pt-BR',{weekday:'long',day:'numeric',month:'long'}));
  }
  openMo('mo-metas-hoje');
}
function saveMetasHoje() {
  const date = S.curDate || DB.today();
  const day = DB.day(date);
  const
===== vídeo @ 774027 =====
ValueEdit', e);
    showToast('Erro ao salvar. Tente de novo.', 'error');
  }
}
function openKmAdjust() {
  const day = DB.day(S.curDate);
  const isToday = S.curDate === DB.today();
  const live = isToday && S.gps.st === 'running';
  const liveKm = live ? S.gps.km : 0;
  const finishedKm = (day.sessions || []).reduce((s,x)=>s+(x.km||0),0);
  kmAdjBase = finishedKm + liveKm;
  const current = kmAdjBase + (day.kmAdjust || 0);
  const inp = document.getElementById('kmadj-val');
  if (inp) inp.value = current.toFixed(1);
  openMo('mo-km-adjust');
}
function kmAdjStep(delta) {
  const inp = document.getElementById('kmadj-val');
  if (!inp) return;
  const v = Math.max(0, (parseFloat(inp.value) || 0) + delta);
  inp.value = v.toFixed(1);
}
function saveKmAdjust() {
  const inp = document.getElementById('kmadj-val');
  const newTotal = parseFloat(inp?.value);
  if (isNaN(newTotal) || newTotal < 0) { showToast('Valor inválido', 'error'); return; }
  const day = DB.day(S.curDate);
  day.kmAdjust = parseFloat((newTotal - kmAdjBase).toFixed(2));
  DB.saveDay(day);
  SYNC.push('day_meta', { date: S.curDate, km_adjust: day.kmAdjust, km_total_legacy: day.kmTotal ?? null });
  closeMo('mo-km-adjust');
  renderJornada();
  showToast('Km total ajustado.');
}

// ── Ajuste de tempo total (mesma lógica do km: botões nas laterais + campo editável por texto) ──
let tempoAdjBaseSecs = 0, tempoAdjCurSecs = 0;
function openTempoAdjust() {
  const day = DB.day(S.curDate);
  const isToday = S.curDate === DB.today();
  const live = isToday && S.gps.st === 'running';
  const liveElapsed = live ? getElapsed() : 0;
  const finishedElapsed = (day.sessions || []).reduce((s,x)=>s+(x.elapsed||0),0);
  tempoAdjBaseSecs = finishedElapsed + liveElapsed;
  tempoAdjCurSecs = Math.max(0, tempoAdjBaseSecs + (day.tempoAdjust || 0));
  updateTempoAdjDisplay();
  openMo('mo-tempo-adjust');
}
// Aceita "2h30m", "2h 30m", "2h", "45m" ou número puro (minutos)
function parseTempoAdjInput(text) {
  text = (text || '').trim().toLowerCase().replace(/\s+/g, '');
  if (!text) return null;
  let h = 0, m = 0;
  const full = text.match(/^(\d+)h(\d+)?m?$/);
  const justH = text.match(/^(\d+)h$/);
  const justM = text.match(/^(\d+)m$/);
  const justNum = text.match(/^(\d+)$/);
  if (full) { h = parseInt(full[1]); m = parseInt(full[2] || 0); }
  else if (justH) { h = parseInt(justH[1]); }
  else if (justM) { m = parseInt(justM[1]); }
  else if (justNum) { m = parseInt(justNum[1]); }
  else return null;
  return Math.max(0, h*3600 + m*60);
}
function updateTempoAdjDisplay() {
  const inp = document.getElementById('tempoadj-val');
  if (inp) inp.value = fmtDur(tempoAdjCurSecs / 60);
}
function tempoAdjStep(deltaMin) {
  tempoAdjCurSecs = Math.max(0, tempoAdjCurSecs + deltaMin * 60);
  updateTempoAdjDisplay();
}
function tempoAdjInputCommit() {
  const inp = document.getElementById('tempoadj-val');
  if (!inp) return;
  const parsed = parseTempoAdjInput(inp.value);
  if (parsed === null) { updateTempoAdjDisplay(); return; }
  tempoAdjCurSecs = parsed;
  updateTempoAdjDisplay();
}
function saveTempoAdjust() {
  tempoAdjInputCommit();
  const day = DB.day(S.curDate);
  day.tempoAdjust = Math.round(tempoAdjCurSecs - tempoAdjBaseSecs);
  DB.saveDay(day);
  SYNC.push('day_meta', { date: S.curDate, tempo_adjust_secs: day.tempoAdjust });
  closeMo('mo-tempo-adjust');
  renderJornada();
  showToast('Tempo total ajustado.');
}

// ── IMPORT AI ─────────────────────────────────────────────────
// Detecta vídeo por MIME type, com fallback por extensão do nome do
// arquivo — em alguns celulares (Live Photo/Motion Photo, ou o picker de
// Arquivos) o navegador reporta file.type vazio ou incorreto, e o print
// (imagem) acabava sendo classificado errado e descartado.
function isVideoFile(f) {
  if (f.type) return f.type.startsWith('video/');
  return /\.(mp4|mov|webm|mkv|avi|m4v|3gp)$/i.test(f.name||'');
}
function handleImport(input, plat) {
  if (!input.files.length) return;
  const files = Array.from(input.files);
  closeMo('mo-import');
  const videos = files.filter(isVideoFile);
  const images = files.filter(f => !isVideoFile(f));
  if (!videos.length && !images.length) return;
  processImportBatch(images, videos, plat);
}
// Processa imagens E vídeos da mesma seleção juntos — antes, se um vídeo
// viesse junto (ex: Live Photo confundida com foto), TODAS as imagens da
// seleção eram silenciosamente descartadas (só processava videos[0]).
let importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };

function openImportPrecheck() {
  ['ipc-uber-count','ipc-uber-value','ipc-99-count','ipc-99-value'].forEach(id => {
    const el = document.getElementById(id); if (el) el.value = '';
  });
  openMo('mo-import-precheck');
}
function _readPrecheckField(id) {
  const v = document.getElementById(id)?.value;
  return (v === '' || v === undefined || v === null) ? null : parseFloat(String(v).replace(',','.'));
}
function importPrecheckNext() {
  importPrecheck = {
    uberCount: _readPrecheckField('ipc-uber-count'),
    uberValue: _readPrecheckField('ipc-uber-value'),
    p99Count:  _readPrecheckField('ipc-99-count'),
    p99Value:  _readPrecheckField('ipc-99-value')
  };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}
function importPrecheckSkip() {
  importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}

function renderImportRing(found, target, foundVal, targetVal, statusHtml, isWarn) {
  const hasTarget = target > 0;
  const pct = hasTarget ? Math.min(100, found/target*100) : 0;
  const deg = Math.round(pct*3.6);
  const ringColor = isWarn ? '#F59E0B' : '#22C55E';
  const ringBg = hasTarget ? `conic-gradient(${ringColor} 0deg ${deg}deg, #E2E8F0 ${deg}deg 360deg)` : '#E2E8F0';
  const countHtml = hasTarget ? `${found}<span style="font-size:16px;color:#94A3B8">/${target}</span>` : `${found}`;
  const valHtml = hasTarget ? `${DB.fRI(foundVal)} de ${DB.fRI(targetVal)} já identificados` : `${DB.fRI(foundVal)} identificados até agora`;
  return `<div style="display:flex;flex-direction:column;align-items:center;padding:30px 0 10px">
    <div style="width:130px;height:130px;border-radius:50%;background:${ringBg};display:flex;align-items:center;justify-content:center;margin-bottom:20px;position:relative;transition:background .4s">
      <div style="position:absolute;inset:8px;border-radius:50%;background:#fff"></div>
      <div style="position:relative;text-align:center">
        <div style="font-family:'Poppins',sans-serif;font-weight:900;font-size:28px;color:#0F172A">${countHtml}</div>
        <div style="font-size:10px;color:#64748B;font-weight:700;margin-top:2px">CORRIDAS</div>
      </div>
    </div>
    <div style="font-size:13.5px;font-weight:700;color:#0F172A;margin-bottom:5px;display:flex;align-items:center;gap:6px">${statusHtml}</div>
    <div style="font-size:12px;color:#64748B">${valHtml}</div>
  </div>`;
}

// Tenta de novo (até 3x) antes de desistir de um print/frame — evita que um
// erro passageiro de rede derrube a corrida inteira do import silenciosamente.
async function extractWithRetry(fn, onRetry, maxAttempts) {
  maxAttempts = maxAttempts || 3;
  let lastErr;
  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    try { return await fn(); }
    catch(e) {
      lastErr = e;
      if (attempt < maxAttempts) { onRetry(attempt+1, maxAttempts); await new Promise(r => setTimeout(r, 800)); }
    }
  }
  throw lastErr;
}

// Sobe o vídeo original pro Storage (bucket import-videos, retenção de
// 180 dias) e registra em import_videos — pedido do Yuri (16/08/2026):
// base pra confirmação de valor real (campeonatos/incentivos futuros) e
// pra dar dá pra conferir manualmente qualquer corrida em disputa. Falha
// de upload NUNCA trava o import (retorna null, segue sem vínculo) — o
// vídeo é auditoria extra, não pode ser bloqueio de fluxo.
async function uploadImportVideo(file, plat, operationalDate) {
  if (!_supaUser) return null;
  try {
    const ext = (file.name && file.name.includes('.')) ? file.name.split('.').pop().toLowerCase() : 'mp4';
    const opDate = operationalDate || S.curDate || DB.today();
    const opPlat = normalizeOperationalPlatform(plat);
    const opRange = operationalRangeForPlatform(opPlat, opDate);
    const path = `${_supaUser.id}/${opDate}/${opPlat}/${DB.uid()}.${ext}`;
    const { error: upErr } = await _SUPA.storage.from('import-videos').upload(path, file, {
      contentType: file.type || 'video/mp4'
    });
    if (upErr) { console.warn('upload vídeo falhou (segue sem vínculo):', upErr); return null; }
    const { data, error } = await _SUPA.from('import_videos').insert({
      user_id: _supaUser.id, storage_path: path, file_size_bytes: file.size || null,
      platform: opPlat, operational_date: opDate,
      period_start: new Date(opRange.startMs).toISOString(),
      period_end: new Date(opRange.endMs).toISOString()
    }).select('id').single();
    if (error) { console.warn('registro import_videos falhou (segue sem vínculo):', error); return null; }
    return data.id;
  } catch(e) { console.warn('uploadImportVideo erro:', e); return null; }
}
async function processImportBatch(images, videos, plat) {
  const operationalDate = S.curDate || DB.today();
  const operationalPlat = normalizeOperationalPlatform(plat);
  window._pendingImportOperationalDate = operationalDate;
  window._pendingImportPlatform = operationalPlat;
  const targetCount = (importPrecheck.uberCount||0) + (importPrecheck.p99Count||0);
  const targetValue = (importPrecheck.uberValue||0) + (importPrecheck.p99Value||0);
  const totalImgs = images.length, totalVids = videos.length;
  const label = [totalImgs?`${totalImgs} print${totalImgs>1?'s':''}`:'', totalVids?`${totalVids} vídeo${totalVids>1?'s':''}`:''].filter(Boolean).join(' + ');
  document.getElementById('ic-title').textContent = 'Analisando ' + label;
  document.getElementById('ic-btn').s
===== vídeo @ 774784 =====
eKmAdjust() {
  const inp = document.getElementById('kmadj-val');
  const newTotal = parseFloat(inp?.value);
  if (isNaN(newTotal) || newTotal < 0) { showToast('Valor inválido', 'error'); return; }
  const day = DB.day(S.curDate);
  day.kmAdjust = parseFloat((newTotal - kmAdjBase).toFixed(2));
  DB.saveDay(day);
  SYNC.push('day_meta', { date: S.curDate, km_adjust: day.kmAdjust, km_total_legacy: day.kmTotal ?? null });
  closeMo('mo-km-adjust');
  renderJornada();
  showToast('Km total ajustado.');
}

// ── Ajuste de tempo total (mesma lógica do km: botões nas laterais + campo editável por texto) ──
let tempoAdjBaseSecs = 0, tempoAdjCurSecs = 0;
function openTempoAdjust() {
  const day = DB.day(S.curDate);
  const isToday = S.curDate === DB.today();
  const live = isToday && S.gps.st === 'running';
  const liveElapsed = live ? getElapsed() : 0;
  const finishedElapsed = (day.sessions || []).reduce((s,x)=>s+(x.elapsed||0),0);
  tempoAdjBaseSecs = finishedElapsed + liveElapsed;
  tempoAdjCurSecs = Math.max(0, tempoAdjBaseSecs + (day.tempoAdjust || 0));
  updateTempoAdjDisplay();
  openMo('mo-tempo-adjust');
}
// Aceita "2h30m", "2h 30m", "2h", "45m" ou número puro (minutos)
function parseTempoAdjInput(text) {
  text = (text || '').trim().toLowerCase().replace(/\s+/g, '');
  if (!text) return null;
  let h = 0, m = 0;
  const full = text.match(/^(\d+)h(\d+)?m?$/);
  const justH = text.match(/^(\d+)h$/);
  const justM = text.match(/^(\d+)m$/);
  const justNum = text.match(/^(\d+)$/);
  if (full) { h = parseInt(full[1]); m = parseInt(full[2] || 0); }
  else if (justH) { h = parseInt(justH[1]); }
  else if (justM) { m = parseInt(justM[1]); }
  else if (justNum) { m = parseInt(justNum[1]); }
  else return null;
  return Math.max(0, h*3600 + m*60);
}
function updateTempoAdjDisplay() {
  const inp = document.getElementById('tempoadj-val');
  if (inp) inp.value = fmtDur(tempoAdjCurSecs / 60);
}
function tempoAdjStep(deltaMin) {
  tempoAdjCurSecs = Math.max(0, tempoAdjCurSecs + deltaMin * 60);
  updateTempoAdjDisplay();
}
function tempoAdjInputCommit() {
  const inp = document.getElementById('tempoadj-val');
  if (!inp) return;
  const parsed = parseTempoAdjInput(inp.value);
  if (parsed === null) { updateTempoAdjDisplay(); return; }
  tempoAdjCurSecs = parsed;
  updateTempoAdjDisplay();
}
function saveTempoAdjust() {
  tempoAdjInputCommit();
  const day = DB.day(S.curDate);
  day.tempoAdjust = Math.round(tempoAdjCurSecs - tempoAdjBaseSecs);
  DB.saveDay(day);
  SYNC.push('day_meta', { date: S.curDate, tempo_adjust_secs: day.tempoAdjust });
  closeMo('mo-tempo-adjust');
  renderJornada();
  showToast('Tempo total ajustado.');
}

// ── IMPORT AI ─────────────────────────────────────────────────
// Detecta vídeo por MIME type, com fallback por extensão do nome do
// arquivo — em alguns celulares (Live Photo/Motion Photo, ou o picker de
// Arquivos) o navegador reporta file.type vazio ou incorreto, e o print
// (imagem) acabava sendo classificado errado e descartado.
function isVideoFile(f) {
  if (f.type) return f.type.startsWith('video/');
  return /\.(mp4|mov|webm|mkv|avi|m4v|3gp)$/i.test(f.name||'');
}
function handleImport(input, plat) {
  if (!input.files.length) return;
  const files = Array.from(input.files);
  closeMo('mo-import');
  const videos = files.filter(isVideoFile);
  const images = files.filter(f => !isVideoFile(f));
  if (!videos.length && !images.length) return;
  processImportBatch(images, videos, plat);
}
// Processa imagens E vídeos da mesma seleção juntos — antes, se um vídeo
// viesse junto (ex: Live Photo confundida com foto), TODAS as imagens da
// seleção eram silenciosamente descartadas (só processava videos[0]).
let importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };

function openImportPrecheck() {
  ['ipc-uber-count','ipc-uber-value','ipc-99-count','ipc-99-value'].forEach(id => {
    const el = document.getElementById(id); if (el) el.value = '';
  });
  openMo('mo-import-precheck');
}
function _readPrecheckField(id) {
  const v = document.getElementById(id)?.value;
  return (v === '' || v === undefined || v === null) ? null : parseFloat(String(v).replace(',','.'));
}
function importPrecheckNext() {
  importPrecheck = {
    uberCount: _readPrecheckField('ipc-uber-count'),
    uberValue: _readPrecheckField('ipc-uber-value'),
    p99Count:  _readPrecheckField('ipc-99-count'),
    p99Value:  _readPrecheckField('ipc-99-value')
  };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}
function importPrecheckSkip() {
  importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}

function renderImportRing(found, target, foundVal, targetVal, statusHtml, isWarn) {
  const hasTarget = target > 0;
  const pct = hasTarget ? Math.min(100, found/target*100) : 0;
  const deg = Math.round(pct*3.6);
  const ringColor = isWarn ? '#F59E0B' : '#22C55E';
  const ringBg = hasTarget ? `conic-gradient(${ringColor} 0deg ${deg}deg, #E2E8F0 ${deg}deg 360deg)` : '#E2E8F0';
  const countHtml = hasTarget ? `${found}<span style="font-size:16px;color:#94A3B8">/${target}</span>` : `${found}`;
  const valHtml = hasTarget ? `${DB.fRI(foundVal)} de ${DB.fRI(targetVal)} já identificados` : `${DB.fRI(foundVal)} identificados até agora`;
  return `<div style="display:flex;flex-direction:column;align-items:center;padding:30px 0 10px">
    <div style="width:130px;height:130px;border-radius:50%;background:${ringBg};display:flex;align-items:center;justify-content:center;margin-bottom:20px;position:relative;transition:background .4s">
      <div style="position:absolute;inset:8px;border-radius:50%;background:#fff"></div>
      <div style="position:relative;text-align:center">
        <div style="font-family:'Poppins',sans-serif;font-weight:900;font-size:28px;color:#0F172A">${countHtml}</div>
        <div style="font-size:10px;color:#64748B;font-weight:700;margin-top:2px">CORRIDAS</div>
      </div>
    </div>
    <div style="font-size:13.5px;font-weight:700;color:#0F172A;margin-bottom:5px;display:flex;align-items:center;gap:6px">${statusHtml}</div>
    <div style="font-size:12px;color:#64748B">${valHtml}</div>
  </div>`;
}

// Tenta de novo (até 3x) antes de desistir de um print/frame — evita que um
// erro passageiro de rede derrube a corrida inteira do import silenciosamente.
async function extractWithRetry(fn, onRetry, maxAttempts) {
  maxAttempts = maxAttempts || 3;
  let lastErr;
  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    try { return await fn(); }
    catch(e) {
      lastErr = e;
      if (attempt < maxAttempts) { onRetry(attempt+1, maxAttempts); await new Promise(r => setTimeout(r, 800)); }
    }
  }
  throw lastErr;
}

// Sobe o vídeo original pro Storage (bucket import-videos, retenção de
// 180 dias) e registra em import_videos — pedido do Yuri (16/08/2026):
// base pra confirmação de valor real (campeonatos/incentivos futuros) e
// pra dar dá pra conferir manualmente qualquer corrida em disputa. Falha
// de upload NUNCA trava o import (retorna null, segue sem vínculo) — o
// vídeo é auditoria extra, não pode ser bloqueio de fluxo.
async function uploadImportVideo(file, plat, operationalDate) {
  if (!_supaUser) return null;
  try {
    const ext = (file.name && file.name.includes('.')) ? file.name.split('.').pop().toLowerCase() : 'mp4';
    const opDate = operationalDate || S.curDate || DB.today();
    const opPlat = normalizeOperationalPlatform(plat);
    const opRange = operationalRangeForPlatform(opPlat, opDate);
    const path = `${_supaUser.id}/${opDate}/${opPlat}/${DB.uid()}.${ext}`;
    const { error: upErr } = await _SUPA.storage.from('import-videos').upload(path, file, {
      contentType: file.type || 'video/mp4'
    });
    if (upErr) { console.warn('upload vídeo falhou (segue sem vínculo):', upErr); return null; }
    const { data, error } = await _SUPA.from('import_videos').insert({
      user_id: _supaUser.id, storage_path: path, file_size_bytes: file.size || null,
      platform: opPlat, operational_date: opDate,
      period_start: new Date(opRange.startMs).toISOString(),
      period_end: new Date(opRange.endMs).toISOString()
    }).select('id').single();
    if (error) { console.warn('registro import_videos falhou (segue sem vínculo):', error); return null; }
    return data.id;
  } catch(e) { console.warn('uploadImportVideo erro:', e); return null; }
}
async function processImportBatch(images, videos, plat) {
  const operationalDate = S.curDate || DB.today();
  const operationalPlat = normalizeOperationalPlatform(plat);
  window._pendingImportOperationalDate = operationalDate;
  window._pendingImportPlatform = operationalPlat;
  const targetCount = (importPrecheck.uberCount||0) + (importPrecheck.p99Count||0);
  const targetValue = (importPrecheck.uberValue||0) + (importPrecheck.p99Value||0);
  const totalImgs = images.length, totalVids = videos.length;
  const label = [totalImgs?`${totalImgs} print${totalImgs>1?'s':''}`:'', totalVids?`${totalVids} vídeo${totalVids>1?'s':''}`:''].filter(Boolean).join(' + ');
  document.getElementById('ic-title').textContent = 'Analisando ' + label;
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');

  const unique = [];
  const details = [];
  const seen = new Set();
  let foundCount = 0, foundVal = 0;
  let curDetail = null; // acumulador do fragmento de detalhe em aberto
  const flushDetail = () => { if (curDetail) { details.push(curDetail); curDetail = null; } };
  const mergeDetailFrag = (t) => {
    // Decide se é o MESMO cartão de detalhe (outro pedaço do mesmo scroll)
    // usando horário e endereço — nunca valor. Valor podia vir ligeiramente
    // diferente entre frames ("Você ganhou" vs "Total") e isso fazia o
    // mesmo cartão virar DOIS fragmentos, que depois viravam duas corridas
    // (uma real + uma fantasma) no import.
    const conflicts = (a, b) => a != null && b != 
===== vídeo @ 778069 =====
;
  const videos = files.filter(isVideoFile);
  const images = files.filter(f => !isVideoFile(f));
  if (!videos.length && !images.length) return;
  processImportBatch(images, videos, plat);
}
// Processa imagens E vídeos da mesma seleção juntos — antes, se um vídeo
// viesse junto (ex: Live Photo confundida com foto), TODAS as imagens da
// seleção eram silenciosamente descartadas (só processava videos[0]).
let importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };

function openImportPrecheck() {
  ['ipc-uber-count','ipc-uber-value','ipc-99-count','ipc-99-value'].forEach(id => {
    const el = document.getElementById(id); if (el) el.value = '';
  });
  openMo('mo-import-precheck');
}
function _readPrecheckField(id) {
  const v = document.getElementById(id)?.value;
  return (v === '' || v === undefined || v === null) ? null : parseFloat(String(v).replace(',','.'));
}
function importPrecheckNext() {
  importPrecheck = {
    uberCount: _readPrecheckField('ipc-uber-count'),
    uberValue: _readPrecheckField('ipc-uber-value'),
    p99Count:  _readPrecheckField('ipc-99-count'),
    p99Value:  _readPrecheckField('ipc-99-value')
  };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}
function importPrecheckSkip() {
  importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}

function renderImportRing(found, target, foundVal, targetVal, statusHtml, isWarn) {
  const hasTarget = target > 0;
  const pct = hasTarget ? Math.min(100, found/target*100) : 0;
  const deg = Math.round(pct*3.6);
  const ringColor = isWarn ? '#F59E0B' : '#22C55E';
  const ringBg = hasTarget ? `conic-gradient(${ringColor} 0deg ${deg}deg, #E2E8F0 ${deg}deg 360deg)` : '#E2E8F0';
  const countHtml = hasTarget ? `${found}<span style="font-size:16px;color:#94A3B8">/${target}</span>` : `${found}`;
  const valHtml = hasTarget ? `${DB.fRI(foundVal)} de ${DB.fRI(targetVal)} já identificados` : `${DB.fRI(foundVal)} identificados até agora`;
  return `<div style="display:flex;flex-direction:column;align-items:center;padding:30px 0 10px">
    <div style="width:130px;height:130px;border-radius:50%;background:${ringBg};display:flex;align-items:center;justify-content:center;margin-bottom:20px;position:relative;transition:background .4s">
      <div style="position:absolute;inset:8px;border-radius:50%;background:#fff"></div>
      <div style="position:relative;text-align:center">
        <div style="font-family:'Poppins',sans-serif;font-weight:900;font-size:28px;color:#0F172A">${countHtml}</div>
        <div style="font-size:10px;color:#64748B;font-weight:700;margin-top:2px">CORRIDAS</div>
      </div>
    </div>
    <div style="font-size:13.5px;font-weight:700;color:#0F172A;margin-bottom:5px;display:flex;align-items:center;gap:6px">${statusHtml}</div>
    <div style="font-size:12px;color:#64748B">${valHtml}</div>
  </div>`;
}

// Tenta de novo (até 3x) antes de desistir de um print/frame — evita que um
// erro passageiro de rede derrube a corrida inteira do import silenciosamente.
async function extractWithRetry(fn, onRetry, maxAttempts) {
  maxAttempts = maxAttempts || 3;
  let lastErr;
  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    try { return await fn(); }
    catch(e) {
      lastErr = e;
      if (attempt < maxAttempts) { onRetry(attempt+1, maxAttempts); await new Promise(r => setTimeout(r, 800)); }
    }
  }
  throw lastErr;
}

// Sobe o vídeo original pro Storage (bucket import-videos, retenção de
// 180 dias) e registra em import_videos — pedido do Yuri (16/08/2026):
// base pra confirmação de valor real (campeonatos/incentivos futuros) e
// pra dar dá pra conferir manualmente qualquer corrida em disputa. Falha
// de upload NUNCA trava o import (retorna null, segue sem vínculo) — o
// vídeo é auditoria extra, não pode ser bloqueio de fluxo.
async function uploadImportVideo(file, plat, operationalDate) {
  if (!_supaUser) return null;
  try {
    const ext = (file.name && file.name.includes('.')) ? file.name.split('.').pop().toLowerCase() : 'mp4';
    const opDate = operationalDate || S.curDate || DB.today();
    const opPlat = normalizeOperationalPlatform(plat);
    const opRange = operationalRangeForPlatform(opPlat, opDate);
    const path = `${_supaUser.id}/${opDate}/${opPlat}/${DB.uid()}.${ext}`;
    const { error: upErr } = await _SUPA.storage.from('import-videos').upload(path, file, {
      contentType: file.type || 'video/mp4'
    });
    if (upErr) { console.warn('upload vídeo falhou (segue sem vínculo):', upErr); return null; }
    const { data, error } = await _SUPA.from('import_videos').insert({
      user_id: _supaUser.id, storage_path: path, file_size_bytes: file.size || null,
      platform: opPlat, operational_date: opDate,
      period_start: new Date(opRange.startMs).toISOString(),
      period_end: new Date(opRange.endMs).toISOString()
    }).select('id').single();
    if (error) { console.warn('registro import_videos falhou (segue sem vínculo):', error); return null; }
    return data.id;
  } catch(e) { console.warn('uploadImportVideo erro:', e); return null; }
}
async function processImportBatch(images, videos, plat) {
  const operationalDate = S.curDate || DB.today();
  const operationalPlat = normalizeOperationalPlatform(plat);
  window._pendingImportOperationalDate = operationalDate;
  window._pendingImportPlatform = operationalPlat;
  const targetCount = (importPrecheck.uberCount||0) + (importPrecheck.p99Count||0);
  const targetValue = (importPrecheck.uberValue||0) + (importPrecheck.p99Value||0);
  const totalImgs = images.length, totalVids = videos.length;
  const label = [totalImgs?`${totalImgs} print${totalImgs>1?'s':''}`:'', totalVids?`${totalVids} vídeo${totalVids>1?'s':''}`:''].filter(Boolean).join(' + ');
  document.getElementById('ic-title').textContent = 'Analisando ' + label;
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');

  const unique = [];
  const details = [];
  const seen = new Set();
  let foundCount = 0, foundVal = 0;
  let curDetail = null; // acumulador do fragmento de detalhe em aberto
  const flushDetail = () => { if (curDetail) { details.push(curDetail); curDetail = null; } };
  const mergeDetailFrag = (t) => {
    // Decide se é o MESMO cartão de detalhe (outro pedaço do mesmo scroll)
    // usando horário e endereço — nunca valor. Valor podia vir ligeiramente
    // diferente entre frames ("Você ganhou" vs "Total") e isso fazia o
    // mesmo cartão virar DOIS fragmentos, que depois viravam duas corridas
    // (uma real + uma fantasma) no import.
    const conflicts = (a, b) => a != null && b != null && a !== b;
    const sameRide = curDetail && (
      (t.time && curDetail.time) ? t.time === curDetail.time
        : (!conflicts(curDetail.originFull, t.originFull) && !conflicts(curDetail.destFull, t.destFull))
    );
    if (sameRide) {
      // mesmo cartão — preenche só o que esse fragmento ainda não tinha
      if (curDetail.km == null && t.km != null) curDetail.km = t.km;
      if (curDetail.duration == null && t.duration != null) curDetail.duration = t.duration;
      if (!curDetail.time && t.time) curDetail.time = t.time;
      if (!curDetail.originFull && t.originFull) { curDetail.originFull = t.originFull; curDetail.origin = t.origin; }
      if (!curDetail.destFull && t.destFull) { curDetail.destFull = t.destFull; curDetail.dest = t.dest; }
    } else {
      // horário ou endereço batendo diferente do acumulador — é outra
      // corrida: fecha o anterior e abre um novo
      flushDetail();
      curDetail = t;
    }
  };
  // Valida e deduplica NA HORA, frame a frame — o contador do anel só sobe
  // com corrida nova validada. Antes somava a extração bruta de cada frame,
  // e como os frames de vídeo se sobrepõem, a mesma corrida contava várias
  // vezes (ex: 37/17 na tela sendo que eram 17 corridas reais).
  const absorb = (trips) => {
    let sawList = false;
    (trips||[]).forEach(t => {
      if (t.screen === 'detail') {
        // Tela de detalhe: só km/tempo/endereço interessam — sem valor.
        // Se o fragmento não trouxe NENHUM desses três, não vale a pena.
        if (t.km == null && t.duration == null && !t.originFull && !t.destFull) return;
        mergeDetailFrag(t); return;
      }
      if (!(t.value > 0)) return; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
      sawList = true;
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seen.has(key)) return;
      seen.add(key);
      unique.push(t);
      foundCount++; foundVal += (t.value||0);
    });
    if (sawList) flushDetail(); // voltou pra tela de lista — fecha o detalhe que tava em aberto
  };
  const dot = '<span style="width:6px;height:6px;border-radius:50%;background:#22C55E;display:inline-block;animation:pulseDot 1s infinite ease-in-out"></span>';
  const retryIcon = '<svg style="animation:vspin 1s linear infinite" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#D97706" stroke-width="2.5"><path d="M21 12a9 9 0 1 1-2.6-6.4"/></svg>';
  const updateRing = (statusHtml, isWarn) => {
    document.getElementById('ic-body').innerHTML = renderImportRing(foundCount, targetCount, foundVal, targetValue, statusHtml, isWarn);
  };
  updateRing('Iniciando ' + dot, false);

  for (let i = 0; i < images.length; i++) {
    try {
      const trips = await extractWithRetry(
        () => extractTripsFromImage(images[i], plat),
        () => updateRing(retryIcon + ' Processando novamente...', true)
      );
      absorb(trips);
    } catch(e) { console.warn('Erro print '+(i+1)+' (desistiu após 3 tentativas):', e); }
    updateRing('Analisando print' + (totalImgs>1 ? ' ' + Math.min(i+2,totalImgs) + ' de ' + totalImgs : '') + ' ' + dot, false);
  }
  let videoFrameReadFailed = false;
  for (let v = 0; v < videos.length; v++) {
    try {
      updateRing('Preparando vídeo ' 
===== vídeo @ 779132 =====
value'),
    p99Count:  _readPrecheckField('ipc-99-count'),
    p99Value:  _readPrecheckField('ipc-99-value')
  };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}
function importPrecheckSkip() {
  importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}

function renderImportRing(found, target, foundVal, targetVal, statusHtml, isWarn) {
  const hasTarget = target > 0;
  const pct = hasTarget ? Math.min(100, found/target*100) : 0;
  const deg = Math.round(pct*3.6);
  const ringColor = isWarn ? '#F59E0B' : '#22C55E';
  const ringBg = hasTarget ? `conic-gradient(${ringColor} 0deg ${deg}deg, #E2E8F0 ${deg}deg 360deg)` : '#E2E8F0';
  const countHtml = hasTarget ? `${found}<span style="font-size:16px;color:#94A3B8">/${target}</span>` : `${found}`;
  const valHtml = hasTarget ? `${DB.fRI(foundVal)} de ${DB.fRI(targetVal)} já identificados` : `${DB.fRI(foundVal)} identificados até agora`;
  return `<div style="display:flex;flex-direction:column;align-items:center;padding:30px 0 10px">
    <div style="width:130px;height:130px;border-radius:50%;background:${ringBg};display:flex;align-items:center;justify-content:center;margin-bottom:20px;position:relative;transition:background .4s">
      <div style="position:absolute;inset:8px;border-radius:50%;background:#fff"></div>
      <div style="position:relative;text-align:center">
        <div style="font-family:'Poppins',sans-serif;font-weight:900;font-size:28px;color:#0F172A">${countHtml}</div>
        <div style="font-size:10px;color:#64748B;font-weight:700;margin-top:2px">CORRIDAS</div>
      </div>
    </div>
    <div style="font-size:13.5px;font-weight:700;color:#0F172A;margin-bottom:5px;display:flex;align-items:center;gap:6px">${statusHtml}</div>
    <div style="font-size:12px;color:#64748B">${valHtml}</div>
  </div>`;
}

// Tenta de novo (até 3x) antes de desistir de um print/frame — evita que um
// erro passageiro de rede derrube a corrida inteira do import silenciosamente.
async function extractWithRetry(fn, onRetry, maxAttempts) {
  maxAttempts = maxAttempts || 3;
  let lastErr;
  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    try { return await fn(); }
    catch(e) {
      lastErr = e;
      if (attempt < maxAttempts) { onRetry(attempt+1, maxAttempts); await new Promise(r => setTimeout(r, 800)); }
    }
  }
  throw lastErr;
}

// Sobe o vídeo original pro Storage (bucket import-videos, retenção de
// 180 dias) e registra em import_videos — pedido do Yuri (16/08/2026):
// base pra confirmação de valor real (campeonatos/incentivos futuros) e
// pra dar dá pra conferir manualmente qualquer corrida em disputa. Falha
// de upload NUNCA trava o import (retorna null, segue sem vínculo) — o
// vídeo é auditoria extra, não pode ser bloqueio de fluxo.
async function uploadImportVideo(file, plat, operationalDate) {
  if (!_supaUser) return null;
  try {
    const ext = (file.name && file.name.includes('.')) ? file.name.split('.').pop().toLowerCase() : 'mp4';
    const opDate = operationalDate || S.curDate || DB.today();
    const opPlat = normalizeOperationalPlatform(plat);
    const opRange = operationalRangeForPlatform(opPlat, opDate);
    const path = `${_supaUser.id}/${opDate}/${opPlat}/${DB.uid()}.${ext}`;
    const { error: upErr } = await _SUPA.storage.from('import-videos').upload(path, file, {
      contentType: file.type || 'video/mp4'
    });
    if (upErr) { console.warn('upload vídeo falhou (segue sem vínculo):', upErr); return null; }
    const { data, error } = await _SUPA.from('import_videos').insert({
      user_id: _supaUser.id, storage_path: path, file_size_bytes: file.size || null,
      platform: opPlat, operational_date: opDate,
      period_start: new Date(opRange.startMs).toISOString(),
      period_end: new Date(opRange.endMs).toISOString()
    }).select('id').single();
    if (error) { console.warn('registro import_videos falhou (segue sem vínculo):', error); return null; }
    return data.id;
  } catch(e) { console.warn('uploadImportVideo erro:', e); return null; }
}
async function processImportBatch(images, videos, plat) {
  const operationalDate = S.curDate || DB.today();
  const operationalPlat = normalizeOperationalPlatform(plat);
  window._pendingImportOperationalDate = operationalDate;
  window._pendingImportPlatform = operationalPlat;
  const targetCount = (importPrecheck.uberCount||0) + (importPrecheck.p99Count||0);
  const targetValue = (importPrecheck.uberValue||0) + (importPrecheck.p99Value||0);
  const totalImgs = images.length, totalVids = videos.length;
  const label = [totalImgs?`${totalImgs} print${totalImgs>1?'s':''}`:'', totalVids?`${totalVids} vídeo${totalVids>1?'s':''}`:''].filter(Boolean).join(' + ');
  document.getElementById('ic-title').textContent = 'Analisando ' + label;
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');

  const unique = [];
  const details = [];
  const seen = new Set();
  let foundCount = 0, foundVal = 0;
  let curDetail = null; // acumulador do fragmento de detalhe em aberto
  const flushDetail = () => { if (curDetail) { details.push(curDetail); curDetail = null; } };
  const mergeDetailFrag = (t) => {
    // Decide se é o MESMO cartão de detalhe (outro pedaço do mesmo scroll)
    // usando horário e endereço — nunca valor. Valor podia vir ligeiramente
    // diferente entre frames ("Você ganhou" vs "Total") e isso fazia o
    // mesmo cartão virar DOIS fragmentos, que depois viravam duas corridas
    // (uma real + uma fantasma) no import.
    const conflicts = (a, b) => a != null && b != null && a !== b;
    const sameRide = curDetail && (
      (t.time && curDetail.time) ? t.time === curDetail.time
        : (!conflicts(curDetail.originFull, t.originFull) && !conflicts(curDetail.destFull, t.destFull))
    );
    if (sameRide) {
      // mesmo cartão — preenche só o que esse fragmento ainda não tinha
      if (curDetail.km == null && t.km != null) curDetail.km = t.km;
      if (curDetail.duration == null && t.duration != null) curDetail.duration = t.duration;
      if (!curDetail.time && t.time) curDetail.time = t.time;
      if (!curDetail.originFull && t.originFull) { curDetail.originFull = t.originFull; curDetail.origin = t.origin; }
      if (!curDetail.destFull && t.destFull) { curDetail.destFull = t.destFull; curDetail.dest = t.dest; }
    } else {
      // horário ou endereço batendo diferente do acumulador — é outra
      // corrida: fecha o anterior e abre um novo
      flushDetail();
      curDetail = t;
    }
  };
  // Valida e deduplica NA HORA, frame a frame — o contador do anel só sobe
  // com corrida nova validada. Antes somava a extração bruta de cada frame,
  // e como os frames de vídeo se sobrepõem, a mesma corrida contava várias
  // vezes (ex: 37/17 na tela sendo que eram 17 corridas reais).
  const absorb = (trips) => {
    let sawList = false;
    (trips||[]).forEach(t => {
      if (t.screen === 'detail') {
        // Tela de detalhe: só km/tempo/endereço interessam — sem valor.
        // Se o fragmento não trouxe NENHUM desses três, não vale a pena.
        if (t.km == null && t.duration == null && !t.originFull && !t.destFull) return;
        mergeDetailFrag(t); return;
      }
      if (!(t.value > 0)) return; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
      sawList = true;
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seen.has(key)) return;
      seen.add(key);
      unique.push(t);
      foundCount++; foundVal += (t.value||0);
    });
    if (sawList) flushDetail(); // voltou pra tela de lista — fecha o detalhe que tava em aberto
  };
  const dot = '<span style="width:6px;height:6px;border-radius:50%;background:#22C55E;display:inline-block;animation:pulseDot 1s infinite ease-in-out"></span>';
  const retryIcon = '<svg style="animation:vspin 1s linear infinite" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#D97706" stroke-width="2.5"><path d="M21 12a9 9 0 1 1-2.6-6.4"/></svg>';
  const updateRing = (statusHtml, isWarn) => {
    document.getElementById('ic-body').innerHTML = renderImportRing(foundCount, targetCount, foundVal, targetValue, statusHtml, isWarn);
  };
  updateRing('Iniciando ' + dot, false);

  for (let i = 0; i < images.length; i++) {
    try {
      const trips = await extractWithRetry(
        () => extractTripsFromImage(images[i], plat),
        () => updateRing(retryIcon + ' Processando novamente...', true)
      );
      absorb(trips);
    } catch(e) { console.warn('Erro print '+(i+1)+' (desistiu após 3 tentativas):', e); }
    updateRing('Analisando print' + (totalImgs>1 ? ' ' + Math.min(i+2,totalImgs) + ' de ' + totalImgs : '') + ' ' + dot, false);
  }
  let videoFrameReadFailed = false;
  for (let v = 0; v < videos.length; v++) {
    try {
      updateRing('Preparando vídeo ' + dot, false);
      const frames = await extractVideoFrames(videos[v], (done, total) => {
        updateRing('Preparando vídeo · ' + done + '/' + total + ' quadros ' + dot, false);
      });
      for (let i = 0; i < frames.length; i++) {
        try {
          const trips = await extractWithRetry(
            () => extractTripsFromBase64(frames[i], 'image/jpeg', plat),
            () => updateRing(retryIcon + ' Processando novamente...', true)
          );
          absorb(trips);
        } catch(e) { console.warn('Frame erro (desistiu após 3 tentativas):', e); }
        updateRing('Analisando vídeo · ' + (i+1) + '/' + frames.length + ' quadros ' + dot, false);
      }
    } catch(e) { videoFrameReadFailed = true; console.warn('Erro vídeo '+(v+1)+':', e); }
  }
  if (videos.length && videoFrameReadFailed && unique.length === 0) {
    showToast('Não consegui decodificar os quadros deste vídeo neste aparelho. O arquivo pode ser válido; tente novamente.', 'error');
    closeMo('mo-import-confirm');
    return;
  }

  // Só envia o arquivo original
===== vídeo @ 780354 =====
er;margin-bottom:20px;position:relative;transition:background .4s">
      <div style="position:absolute;inset:8px;border-radius:50%;background:#fff"></div>
      <div style="position:relative;text-align:center">
        <div style="font-family:'Poppins',sans-serif;font-weight:900;font-size:28px;color:#0F172A">${countHtml}</div>
        <div style="font-size:10px;color:#64748B;font-weight:700;margin-top:2px">CORRIDAS</div>
      </div>
    </div>
    <div style="font-size:13.5px;font-weight:700;color:#0F172A;margin-bottom:5px;display:flex;align-items:center;gap:6px">${statusHtml}</div>
    <div style="font-size:12px;color:#64748B">${valHtml}</div>
  </div>`;
}

// Tenta de novo (até 3x) antes de desistir de um print/frame — evita que um
// erro passageiro de rede derrube a corrida inteira do import silenciosamente.
async function extractWithRetry(fn, onRetry, maxAttempts) {
  maxAttempts = maxAttempts || 3;
  let lastErr;
  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    try { return await fn(); }
    catch(e) {
      lastErr = e;
      if (attempt < maxAttempts) { onRetry(attempt+1, maxAttempts); await new Promise(r => setTimeout(r, 800)); }
    }
  }
  throw lastErr;
}

// Sobe o vídeo original pro Storage (bucket import-videos, retenção de
// 180 dias) e registra em import_videos — pedido do Yuri (16/08/2026):
// base pra confirmação de valor real (campeonatos/incentivos futuros) e
// pra dar dá pra conferir manualmente qualquer corrida em disputa. Falha
// de upload NUNCA trava o import (retorna null, segue sem vínculo) — o
// vídeo é auditoria extra, não pode ser bloqueio de fluxo.
async function uploadImportVideo(file, plat, operationalDate) {
  if (!_supaUser) return null;
  try {
    const ext = (file.name && file.name.includes('.')) ? file.name.split('.').pop().toLowerCase() : 'mp4';
    const opDate = operationalDate || S.curDate || DB.today();
    const opPlat = normalizeOperationalPlatform(plat);
    const opRange = operationalRangeForPlatform(opPlat, opDate);
    const path = `${_supaUser.id}/${opDate}/${opPlat}/${DB.uid()}.${ext}`;
    const { error: upErr } = await _SUPA.storage.from('import-videos').upload(path, file, {
      contentType: file.type || 'video/mp4'
    });
    if (upErr) { console.warn('upload vídeo falhou (segue sem vínculo):', upErr); return null; }
    const { data, error } = await _SUPA.from('import_videos').insert({
      user_id: _supaUser.id, storage_path: path, file_size_bytes: file.size || null,
      platform: opPlat, operational_date: opDate,
      period_start: new Date(opRange.startMs).toISOString(),
      period_end: new Date(opRange.endMs).toISOString()
    }).select('id').single();
    if (error) { console.warn('registro import_videos falhou (segue sem vínculo):', error); return null; }
    return data.id;
  } catch(e) { console.warn('uploadImportVideo erro:', e); return null; }
}
async function processImportBatch(images, videos, plat) {
  const operationalDate = S.curDate || DB.today();
  const operationalPlat = normalizeOperationalPlatform(plat);
  window._pendingImportOperationalDate = operationalDate;
  window._pendingImportPlatform = operationalPlat;
  const targetCount = (importPrecheck.uberCount||0) + (importPrecheck.p99Count||0);
  const targetValue = (importPrecheck.uberValue||0) + (importPrecheck.p99Value||0);
  const totalImgs = images.length, totalVids = videos.length;
  const label = [totalImgs?`${totalImgs} print${totalImgs>1?'s':''}`:'', totalVids?`${totalVids} vídeo${totalVids>1?'s':''}`:''].filter(Boolean).join(' + ');
  document.getElementById('ic-title').textContent = 'Analisando ' + label;
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');

  const unique = [];
  const details = [];
  const seen = new Set();
  let foundCount = 0, foundVal = 0;
  let curDetail = null; // acumulador do fragmento de detalhe em aberto
  const flushDetail = () => { if (curDetail) { details.push(curDetail); curDetail = null; } };
  const mergeDetailFrag = (t) => {
    // Decide se é o MESMO cartão de detalhe (outro pedaço do mesmo scroll)
    // usando horário e endereço — nunca valor. Valor podia vir ligeiramente
    // diferente entre frames ("Você ganhou" vs "Total") e isso fazia o
    // mesmo cartão virar DOIS fragmentos, que depois viravam duas corridas
    // (uma real + uma fantasma) no import.
    const conflicts = (a, b) => a != null && b != null && a !== b;
    const sameRide = curDetail && (
      (t.time && curDetail.time) ? t.time === curDetail.time
        : (!conflicts(curDetail.originFull, t.originFull) && !conflicts(curDetail.destFull, t.destFull))
    );
    if (sameRide) {
      // mesmo cartão — preenche só o que esse fragmento ainda não tinha
      if (curDetail.km == null && t.km != null) curDetail.km = t.km;
      if (curDetail.duration == null && t.duration != null) curDetail.duration = t.duration;
      if (!curDetail.time && t.time) curDetail.time = t.time;
      if (!curDetail.originFull && t.originFull) { curDetail.originFull = t.originFull; curDetail.origin = t.origin; }
      if (!curDetail.destFull && t.destFull) { curDetail.destFull = t.destFull; curDetail.dest = t.dest; }
    } else {
      // horário ou endereço batendo diferente do acumulador — é outra
      // corrida: fecha o anterior e abre um novo
      flushDetail();
      curDetail = t;
    }
  };
  // Valida e deduplica NA HORA, frame a frame — o contador do anel só sobe
  // com corrida nova validada. Antes somava a extração bruta de cada frame,
  // e como os frames de vídeo se sobrepõem, a mesma corrida contava várias
  // vezes (ex: 37/17 na tela sendo que eram 17 corridas reais).
  const absorb = (trips) => {
    let sawList = false;
    (trips||[]).forEach(t => {
      if (t.screen === 'detail') {
        // Tela de detalhe: só km/tempo/endereço interessam — sem valor.
        // Se o fragmento não trouxe NENHUM desses três, não vale a pena.
        if (t.km == null && t.duration == null && !t.originFull && !t.destFull) return;
        mergeDetailFrag(t); return;
      }
      if (!(t.value > 0)) return; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
      sawList = true;
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seen.has(key)) return;
      seen.add(key);
      unique.push(t);
      foundCount++; foundVal += (t.value||0);
    });
    if (sawList) flushDetail(); // voltou pra tela de lista — fecha o detalhe que tava em aberto
  };
  const dot = '<span style="width:6px;height:6px;border-radius:50%;background:#22C55E;display:inline-block;animation:pulseDot 1s infinite ease-in-out"></span>';
  const retryIcon = '<svg style="animation:vspin 1s linear infinite" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#D97706" stroke-width="2.5"><path d="M21 12a9 9 0 1 1-2.6-6.4"/></svg>';
  const updateRing = (statusHtml, isWarn) => {
    document.getElementById('ic-body').innerHTML = renderImportRing(foundCount, targetCount, foundVal, targetValue, statusHtml, isWarn);
  };
  updateRing('Iniciando ' + dot, false);

  for (let i = 0; i < images.length; i++) {
    try {
      const trips = await extractWithRetry(
        () => extractTripsFromImage(images[i], plat),
        () => updateRing(retryIcon + ' Processando novamente...', true)
      );
      absorb(trips);
    } catch(e) { console.warn('Erro print '+(i+1)+' (desistiu após 3 tentativas):', e); }
    updateRing('Analisando print' + (totalImgs>1 ? ' ' + Math.min(i+2,totalImgs) + ' de ' + totalImgs : '') + ' ' + dot, false);
  }
  let videoFrameReadFailed = false;
  for (let v = 0; v < videos.length; v++) {
    try {
      updateRing('Preparando vídeo ' + dot, false);
      const frames = await extractVideoFrames(videos[v], (done, total) => {
        updateRing('Preparando vídeo · ' + done + '/' + total + ' quadros ' + dot, false);
      });
      for (let i = 0; i < frames.length; i++) {
        try {
          const trips = await extractWithRetry(
            () => extractTripsFromBase64(frames[i], 'image/jpeg', plat),
            () => updateRing(retryIcon + ' Processando novamente...', true)
          );
          absorb(trips);
        } catch(e) { console.warn('Frame erro (desistiu após 3 tentativas):', e); }
        updateRing('Analisando vídeo · ' + (i+1) + '/' + frames.length + ' quadros ' + dot, false);
      }
    } catch(e) { videoFrameReadFailed = true; console.warn('Erro vídeo '+(v+1)+':', e); }
  }
  if (videos.length && videoFrameReadFailed && unique.length === 0) {
    showToast('Não consegui decodificar os quadros deste vídeo neste aparelho. O arquivo pode ser válido; tente novamente.', 'error');
    closeMo('mo-import-confirm');
    return;
  }

  // Só envia o arquivo original depois que a leitura local terminou.
  // Em celular/WebView, fazer upload e seek/decodificação simultaneamente
  // do mesmo File grande pode bloquear o decoder e deixar a tela parada.
  if (videos.length) updateRing('Salvando vídeo para conferência ' + dot, false);
  const _videoUploadResults = await Promise.all(
    videos.map(file => uploadImportVideo(file, operationalPlat, operationalDate))
  );
  window._pendingImportVideoIds = _videoUploadResults.filter(Boolean);
  if (videos.length && window._pendingImportVideoIds.length < videos.length) {
    showToast('Corridas importadas, mas o vídeo de auditoria não pôde ser salvo — confira sua conexão.', 'error');
  }
  flushDetail(); // fragmento de detalhe que ficou aberto no fim do vídeo

  // Resolve conflitos de horário: a regra pedida pra IA é "cada horário =
  // uma corrida só", mas o dedup acima só barra repetição idêntica
  // (mesmo horário E mesmo valor) — se o mesmo horário veio com valores
  // DIFERENTES em frames diferentes (mistura de cartão vizinho num frame
  // só), os dois passavam pra lista como corridas separadas, inflando a
  // contagem e o total. Aqui, pra cada (plataforma, horário), fica só a
  // leitura de valor que mais se rep
===== vídeo @ 782418 =====
/${opPlat}/${DB.uid()}.${ext}`;
    const { error: upErr } = await _SUPA.storage.from('import-videos').upload(path, file, {
      contentType: file.type || 'video/mp4'
    });
    if (upErr) { console.warn('upload vídeo falhou (segue sem vínculo):', upErr); return null; }
    const { data, error } = await _SUPA.from('import_videos').insert({
      user_id: _supaUser.id, storage_path: path, file_size_bytes: file.size || null,
      platform: opPlat, operational_date: opDate,
      period_start: new Date(opRange.startMs).toISOString(),
      period_end: new Date(opRange.endMs).toISOString()
    }).select('id').single();
    if (error) { console.warn('registro import_videos falhou (segue sem vínculo):', error); return null; }
    return data.id;
  } catch(e) { console.warn('uploadImportVideo erro:', e); return null; }
}
async function processImportBatch(images, videos, plat) {
  const operationalDate = S.curDate || DB.today();
  const operationalPlat = normalizeOperationalPlatform(plat);
  window._pendingImportOperationalDate = operationalDate;
  window._pendingImportPlatform = operationalPlat;
  const targetCount = (importPrecheck.uberCount||0) + (importPrecheck.p99Count||0);
  const targetValue = (importPrecheck.uberValue||0) + (importPrecheck.p99Value||0);
  const totalImgs = images.length, totalVids = videos.length;
  const label = [totalImgs?`${totalImgs} print${totalImgs>1?'s':''}`:'', totalVids?`${totalVids} vídeo${totalVids>1?'s':''}`:''].filter(Boolean).join(' + ');
  document.getElementById('ic-title').textContent = 'Analisando ' + label;
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');

  const unique = [];
  const details = [];
  const seen = new Set();
  let foundCount = 0, foundVal = 0;
  let curDetail = null; // acumulador do fragmento de detalhe em aberto
  const flushDetail = () => { if (curDetail) { details.push(curDetail); curDetail = null; } };
  const mergeDetailFrag = (t) => {
    // Decide se é o MESMO cartão de detalhe (outro pedaço do mesmo scroll)
    // usando horário e endereço — nunca valor. Valor podia vir ligeiramente
    // diferente entre frames ("Você ganhou" vs "Total") e isso fazia o
    // mesmo cartão virar DOIS fragmentos, que depois viravam duas corridas
    // (uma real + uma fantasma) no import.
    const conflicts = (a, b) => a != null && b != null && a !== b;
    const sameRide = curDetail && (
      (t.time && curDetail.time) ? t.time === curDetail.time
        : (!conflicts(curDetail.originFull, t.originFull) && !conflicts(curDetail.destFull, t.destFull))
    );
    if (sameRide) {
      // mesmo cartão — preenche só o que esse fragmento ainda não tinha
      if (curDetail.km == null && t.km != null) curDetail.km = t.km;
      if (curDetail.duration == null && t.duration != null) curDetail.duration = t.duration;
      if (!curDetail.time && t.time) curDetail.time = t.time;
      if (!curDetail.originFull && t.originFull) { curDetail.originFull = t.originFull; curDetail.origin = t.origin; }
      if (!curDetail.destFull && t.destFull) { curDetail.destFull = t.destFull; curDetail.dest = t.dest; }
    } else {
      // horário ou endereço batendo diferente do acumulador — é outra
      // corrida: fecha o anterior e abre um novo
      flushDetail();
      curDetail = t;
    }
  };
  // Valida e deduplica NA HORA, frame a frame — o contador do anel só sobe
  // com corrida nova validada. Antes somava a extração bruta de cada frame,
  // e como os frames de vídeo se sobrepõem, a mesma corrida contava várias
  // vezes (ex: 37/17 na tela sendo que eram 17 corridas reais).
  const absorb = (trips) => {
    let sawList = false;
    (trips||[]).forEach(t => {
      if (t.screen === 'detail') {
        // Tela de detalhe: só km/tempo/endereço interessam — sem valor.
        // Se o fragmento não trouxe NENHUM desses três, não vale a pena.
        if (t.km == null && t.duration == null && !t.originFull && !t.destFull) return;
        mergeDetailFrag(t); return;
      }
      if (!(t.value > 0)) return; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
      sawList = true;
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seen.has(key)) return;
      seen.add(key);
      unique.push(t);
      foundCount++; foundVal += (t.value||0);
    });
    if (sawList) flushDetail(); // voltou pra tela de lista — fecha o detalhe que tava em aberto
  };
  const dot = '<span style="width:6px;height:6px;border-radius:50%;background:#22C55E;display:inline-block;animation:pulseDot 1s infinite ease-in-out"></span>';
  const retryIcon = '<svg style="animation:vspin 1s linear infinite" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#D97706" stroke-width="2.5"><path d="M21 12a9 9 0 1 1-2.6-6.4"/></svg>';
  const updateRing = (statusHtml, isWarn) => {
    document.getElementById('ic-body').innerHTML = renderImportRing(foundCount, targetCount, foundVal, targetValue, statusHtml, isWarn);
  };
  updateRing('Iniciando ' + dot, false);

  for (let i = 0; i < images.length; i++) {
    try {
      const trips = await extractWithRetry(
        () => extractTripsFromImage(images[i], plat),
        () => updateRing(retryIcon + ' Processando novamente...', true)
      );
      absorb(trips);
    } catch(e) { console.warn('Erro print '+(i+1)+' (desistiu após 3 tentativas):', e); }
    updateRing('Analisando print' + (totalImgs>1 ? ' ' + Math.min(i+2,totalImgs) + ' de ' + totalImgs : '') + ' ' + dot, false);
  }
  let videoFrameReadFailed = false;
  for (let v = 0; v < videos.length; v++) {
    try {
      updateRing('Preparando vídeo ' + dot, false);
      const frames = await extractVideoFrames(videos[v], (done, total) => {
        updateRing('Preparando vídeo · ' + done + '/' + total + ' quadros ' + dot, false);
      });
      for (let i = 0; i < frames.length; i++) {
        try {
          const trips = await extractWithRetry(
            () => extractTripsFromBase64(frames[i], 'image/jpeg', plat),
            () => updateRing(retryIcon + ' Processando novamente...', true)
          );
          absorb(trips);
        } catch(e) { console.warn('Frame erro (desistiu após 3 tentativas):', e); }
        updateRing('Analisando vídeo · ' + (i+1) + '/' + frames.length + ' quadros ' + dot, false);
      }
    } catch(e) { videoFrameReadFailed = true; console.warn('Erro vídeo '+(v+1)+':', e); }
  }
  if (videos.length && videoFrameReadFailed && unique.length === 0) {
    showToast('Não consegui decodificar os quadros deste vídeo neste aparelho. O arquivo pode ser válido; tente novamente.', 'error');
    closeMo('mo-import-confirm');
    return;
  }

  // Só envia o arquivo original depois que a leitura local terminou.
  // Em celular/WebView, fazer upload e seek/decodificação simultaneamente
  // do mesmo File grande pode bloquear o decoder e deixar a tela parada.
  if (videos.length) updateRing('Salvando vídeo para conferência ' + dot, false);
  const _videoUploadResults = await Promise.all(
    videos.map(file => uploadImportVideo(file, operationalPlat, operationalDate))
  );
  window._pendingImportVideoIds = _videoUploadResults.filter(Boolean);
  if (videos.length && window._pendingImportVideoIds.length < videos.length) {
    showToast('Corridas importadas, mas o vídeo de auditoria não pôde ser salvo — confira sua conexão.', 'error');
  }
  flushDetail(); // fragmento de detalhe que ficou aberto no fim do vídeo

  // Resolve conflitos de horário: a regra pedida pra IA é "cada horário =
  // uma corrida só", mas o dedup acima só barra repetição idêntica
  // (mesmo horário E mesmo valor) — se o mesmo horário veio com valores
  // DIFERENTES em frames diferentes (mistura de cartão vizinho num frame
  // só), os dois passavam pra lista como corridas separadas, inflando a
  // contagem e o total. Aqui, pra cada (plataforma, horário), fica só a
  // leitura de valor que mais se repetiu entre os frames — a leitura
  // certa tende a se confirmar igual em vários frames seguidos (o vídeo
  // se sobrepõe), enquanto uma leitura errada por mistura de cartão
  // costuma aparecer uma vez só.
  function resolveTimeConflicts(list) {
    const groups = new Map();
    list.forEach(t => {
      const k = (t.platform || '') + '_' + t.time;
      (groups.get(k) || groups.set(k, []).get(k)).push(t);
    });
    const resolved = [];
    groups.forEach(items => {
      if (items.length === 1) { resolved.push(items[0]); return; }
      const byValue = new Map();
      items.forEach(t => {
        const vk = (t.value || 0).toFixed(2);
        (byValue.get(vk) || byValue.set(vk, []).get(vk)).push(t);
      });
      let best = null;
      byValue.forEach(arr => { if (!best || arr.length > best.length) best = arr; });
      // dentro do grupo vencedor, prefere o item com endereço mais completo
      let rep = best[0];
      best.forEach(t => {
        const len = (t.originFull || '').length + (t.destFull || '').length;
        const repLen = (rep.originFull || '').length + (rep.destFull || '').length;
        if (len > repLen) rep = t;
      });
      resolved.push(rep);
    });
    return resolved;
  }
  const resolvedUnique = resolveTimeConflicts(unique);
  unique.length = 0;
  unique.push(...resolvedUnique);

  // Funde os detalhes nas corridas da lista — SÓ completa km/tempo/endereço
  // de uma corrida que a LISTA já confirmou (com valor de verdade). Nunca
  // cria corrida nova a partir de detalhe sozinho: sem valor, não tem como
  // confirmar que é real, e foi exatamente isso que gerava corrida fantasma
  // no import (fragmento de detalhe virando corrida "avulsa" por engano).
  details.forEach(d => {
    let cand = null;
    if (d.time) cand = unique.find(u => u.time === d.time && !u._enriched);
    if (!cand && (d.originFull || d.destFull)) {
      cand = unique.find(u => !u._enriched &&
        ((d.originFull && u.originFull === d.originFull) || (d.destFull && u.destFull === d.destFull)));
    }
    if (cand) {
  
===== vídeo @ 784561 =====
tre frames ("Você ganhou" vs "Total") e isso fazia o
    // mesmo cartão virar DOIS fragmentos, que depois viravam duas corridas
    // (uma real + uma fantasma) no import.
    const conflicts = (a, b) => a != null && b != null && a !== b;
    const sameRide = curDetail && (
      (t.time && curDetail.time) ? t.time === curDetail.time
        : (!conflicts(curDetail.originFull, t.originFull) && !conflicts(curDetail.destFull, t.destFull))
    );
    if (sameRide) {
      // mesmo cartão — preenche só o que esse fragmento ainda não tinha
      if (curDetail.km == null && t.km != null) curDetail.km = t.km;
      if (curDetail.duration == null && t.duration != null) curDetail.duration = t.duration;
      if (!curDetail.time && t.time) curDetail.time = t.time;
      if (!curDetail.originFull && t.originFull) { curDetail.originFull = t.originFull; curDetail.origin = t.origin; }
      if (!curDetail.destFull && t.destFull) { curDetail.destFull = t.destFull; curDetail.dest = t.dest; }
    } else {
      // horário ou endereço batendo diferente do acumulador — é outra
      // corrida: fecha o anterior e abre um novo
      flushDetail();
      curDetail = t;
    }
  };
  // Valida e deduplica NA HORA, frame a frame — o contador do anel só sobe
  // com corrida nova validada. Antes somava a extração bruta de cada frame,
  // e como os frames de vídeo se sobrepõem, a mesma corrida contava várias
  // vezes (ex: 37/17 na tela sendo que eram 17 corridas reais).
  const absorb = (trips) => {
    let sawList = false;
    (trips||[]).forEach(t => {
      if (t.screen === 'detail') {
        // Tela de detalhe: só km/tempo/endereço interessam — sem valor.
        // Se o fragmento não trouxe NENHUM desses três, não vale a pena.
        if (t.km == null && t.duration == null && !t.originFull && !t.destFull) return;
        mergeDetailFrag(t); return;
      }
      if (!(t.value > 0)) return; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
      sawList = true;
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seen.has(key)) return;
      seen.add(key);
      unique.push(t);
      foundCount++; foundVal += (t.value||0);
    });
    if (sawList) flushDetail(); // voltou pra tela de lista — fecha o detalhe que tava em aberto
  };
  const dot = '<span style="width:6px;height:6px;border-radius:50%;background:#22C55E;display:inline-block;animation:pulseDot 1s infinite ease-in-out"></span>';
  const retryIcon = '<svg style="animation:vspin 1s linear infinite" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#D97706" stroke-width="2.5"><path d="M21 12a9 9 0 1 1-2.6-6.4"/></svg>';
  const updateRing = (statusHtml, isWarn) => {
    document.getElementById('ic-body').innerHTML = renderImportRing(foundCount, targetCount, foundVal, targetValue, statusHtml, isWarn);
  };
  updateRing('Iniciando ' + dot, false);

  for (let i = 0; i < images.length; i++) {
    try {
      const trips = await extractWithRetry(
        () => extractTripsFromImage(images[i], plat),
        () => updateRing(retryIcon + ' Processando novamente...', true)
      );
      absorb(trips);
    } catch(e) { console.warn('Erro print '+(i+1)+' (desistiu após 3 tentativas):', e); }
    updateRing('Analisando print' + (totalImgs>1 ? ' ' + Math.min(i+2,totalImgs) + ' de ' + totalImgs : '') + ' ' + dot, false);
  }
  let videoFrameReadFailed = false;
  for (let v = 0; v < videos.length; v++) {
    try {
      updateRing('Preparando vídeo ' + dot, false);
      const frames = await extractVideoFrames(videos[v], (done, total) => {
        updateRing('Preparando vídeo · ' + done + '/' + total + ' quadros ' + dot, false);
      });
      for (let i = 0; i < frames.length; i++) {
        try {
          const trips = await extractWithRetry(
            () => extractTripsFromBase64(frames[i], 'image/jpeg', plat),
            () => updateRing(retryIcon + ' Processando novamente...', true)
          );
          absorb(trips);
        } catch(e) { console.warn('Frame erro (desistiu após 3 tentativas):', e); }
        updateRing('Analisando vídeo · ' + (i+1) + '/' + frames.length + ' quadros ' + dot, false);
      }
    } catch(e) { videoFrameReadFailed = true; console.warn('Erro vídeo '+(v+1)+':', e); }
  }
  if (videos.length && videoFrameReadFailed && unique.length === 0) {
    showToast('Não consegui decodificar os quadros deste vídeo neste aparelho. O arquivo pode ser válido; tente novamente.', 'error');
    closeMo('mo-import-confirm');
    return;
  }

  // Só envia o arquivo original depois que a leitura local terminou.
  // Em celular/WebView, fazer upload e seek/decodificação simultaneamente
  // do mesmo File grande pode bloquear o decoder e deixar a tela parada.
  if (videos.length) updateRing('Salvando vídeo para conferência ' + dot, false);
  const _videoUploadResults = await Promise.all(
    videos.map(file => uploadImportVideo(file, operationalPlat, operationalDate))
  );
  window._pendingImportVideoIds = _videoUploadResults.filter(Boolean);
  if (videos.length && window._pendingImportVideoIds.length < videos.length) {
    showToast('Corridas importadas, mas o vídeo de auditoria não pôde ser salvo — confira sua conexão.', 'error');
  }
  flushDetail(); // fragmento de detalhe que ficou aberto no fim do vídeo

  // Resolve conflitos de horário: a regra pedida pra IA é "cada horário =
  // uma corrida só", mas o dedup acima só barra repetição idêntica
  // (mesmo horário E mesmo valor) — se o mesmo horário veio com valores
  // DIFERENTES em frames diferentes (mistura de cartão vizinho num frame
  // só), os dois passavam pra lista como corridas separadas, inflando a
  // contagem e o total. Aqui, pra cada (plataforma, horário), fica só a
  // leitura de valor que mais se repetiu entre os frames — a leitura
  // certa tende a se confirmar igual em vários frames seguidos (o vídeo
  // se sobrepõe), enquanto uma leitura errada por mistura de cartão
  // costuma aparecer uma vez só.
  function resolveTimeConflicts(list) {
    const groups = new Map();
    list.forEach(t => {
      const k = (t.platform || '') + '_' + t.time;
      (groups.get(k) || groups.set(k, []).get(k)).push(t);
    });
    const resolved = [];
    groups.forEach(items => {
      if (items.length === 1) { resolved.push(items[0]); return; }
      const byValue = new Map();
      items.forEach(t => {
        const vk = (t.value || 0).toFixed(2);
        (byValue.get(vk) || byValue.set(vk, []).get(vk)).push(t);
      });
      let best = null;
      byValue.forEach(arr => { if (!best || arr.length > best.length) best = arr; });
      // dentro do grupo vencedor, prefere o item com endereço mais completo
      let rep = best[0];
      best.forEach(t => {
        const len = (t.originFull || '').length + (t.destFull || '').length;
        const repLen = (rep.originFull || '').length + (rep.destFull || '').length;
        if (len > repLen) rep = t;
      });
      resolved.push(rep);
    });
    return resolved;
  }
  const resolvedUnique = resolveTimeConflicts(unique);
  unique.length = 0;
  unique.push(...resolvedUnique);

  // Funde os detalhes nas corridas da lista — SÓ completa km/tempo/endereço
  // de uma corrida que a LISTA já confirmou (com valor de verdade). Nunca
  // cria corrida nova a partir de detalhe sozinho: sem valor, não tem como
  // confirmar que é real, e foi exatamente isso que gerava corrida fantasma
  // no import (fragmento de detalhe virando corrida "avulsa" por engano).
  details.forEach(d => {
    let cand = null;
    if (d.time) cand = unique.find(u => u.time === d.time && !u._enriched);
    if (!cand && (d.originFull || d.destFull)) {
      cand = unique.find(u => !u._enriched &&
        ((d.originFull && u.originFull === d.originFull) || (d.destFull && u.destFull === d.destFull)));
    }
    if (cand) {
      cand._enriched = true;
      if (d.km != null && d.km > 0)       { cand.km = d.km; cand.kmEstimado = false; }
      if (d.duration != null && d.duration > 0) cand.duration = d.duration;
      // Endereço do detalhe é a linha inteira, sem truncar — prefere ele.
      if (d.originFull) { cand.originFull = d.originFull; cand.origin = d.origin; }
      if (d.destFull)   { cand.destFull = d.destFull; cand.dest = d.dest; }
    }
  });

  const day = DB.day(S.curDate);

  // Casa cada corrida do vídeo com auto_trips do dia ANTES de mostrar a
  // tela (pedido do Yuri, 16/08/2026) — permite separar "vai só corrigir
  // valor" de "é corrida nova" já na seleção, e esconder o que não muda
  // nada em vez de mostrar tudo misturado.
  const autoTripsPool = (await loadAutoTripsForOperationalDay(operationalPlat, operationalDate)) || [];
  const numEq = (a,b,tol) => Math.abs((a||0)-(b||0)) <= (tol||0.02);
  const annotated = unique.map((t,i) => {
    const matched = matchAutoTrip(t, autoTripsPool);
    if (matched) autoTripsPool.splice(autoTripsPool.indexOf(matched), 1); // consumido — nunca casa duas vezes
    const base = {...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _exists: isTripDuplicate(t, day.trips)};
    if (matched) {
      const newVal = parseFloat(t.value)||0;
      return {...base, _matchedAutoTripId: matched.id, _isNew: false,
        _oldValue: matched.offer_value, _changed: !numEq(matched.offer_value, newVal, 0.02)};
    }
    return {...base, _matchedAutoTripId: null, _isNew: true, _changed: true};
  });
  // Só mostra o que de fato vai mudar algo — casada sem diferença de valor
  // não aparece (nada a fazer), nova sem valor legível também não aparece
  // (sem valor não dá pra confirmar que é corrida de verdade).
  window._pendingTrips = annotated.filter(t => t._isNew ? (parseFloat(t.value)||0) > 0 : t._changed);
  // Corrida NOVA nunca confia no endereço do vídeo sem validar — mesma
  // regra do Kotlin (looksLikeAddress). Sem validação, fica sem endereço
  // em vez de arriscar lixo/mensagem de chat virando corrida (pedido
  // explícito do Yuri: "nunca end
===== vídeo @ 785173 =====
     if (curDetail.duration == null && t.duration != null) curDetail.duration = t.duration;
      if (!curDetail.time && t.time) curDetail.time = t.time;
      if (!curDetail.originFull && t.originFull) { curDetail.originFull = t.originFull; curDetail.origin = t.origin; }
      if (!curDetail.destFull && t.destFull) { curDetail.destFull = t.destFull; curDetail.dest = t.dest; }
    } else {
      // horário ou endereço batendo diferente do acumulador — é outra
      // corrida: fecha o anterior e abre um novo
      flushDetail();
      curDetail = t;
    }
  };
  // Valida e deduplica NA HORA, frame a frame — o contador do anel só sobe
  // com corrida nova validada. Antes somava a extração bruta de cada frame,
  // e como os frames de vídeo se sobrepõem, a mesma corrida contava várias
  // vezes (ex: 37/17 na tela sendo que eram 17 corridas reais).
  const absorb = (trips) => {
    let sawList = false;
    (trips||[]).forEach(t => {
      if (t.screen === 'detail') {
        // Tela de detalhe: só km/tempo/endereço interessam — sem valor.
        // Se o fragmento não trouxe NENHUM desses três, não vale a pena.
        if (t.km == null && t.duration == null && !t.originFull && !t.destFull) return;
        mergeDetailFrag(t); return;
      }
      if (!(t.value > 0)) return; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
      sawList = true;
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seen.has(key)) return;
      seen.add(key);
      unique.push(t);
      foundCount++; foundVal += (t.value||0);
    });
    if (sawList) flushDetail(); // voltou pra tela de lista — fecha o detalhe que tava em aberto
  };
  const dot = '<span style="width:6px;height:6px;border-radius:50%;background:#22C55E;display:inline-block;animation:pulseDot 1s infinite ease-in-out"></span>';
  const retryIcon = '<svg style="animation:vspin 1s linear infinite" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#D97706" stroke-width="2.5"><path d="M21 12a9 9 0 1 1-2.6-6.4"/></svg>';
  const updateRing = (statusHtml, isWarn) => {
    document.getElementById('ic-body').innerHTML = renderImportRing(foundCount, targetCount, foundVal, targetValue, statusHtml, isWarn);
  };
  updateRing('Iniciando ' + dot, false);

  for (let i = 0; i < images.length; i++) {
    try {
      const trips = await extractWithRetry(
        () => extractTripsFromImage(images[i], plat),
        () => updateRing(retryIcon + ' Processando novamente...', true)
      );
      absorb(trips);
    } catch(e) { console.warn('Erro print '+(i+1)+' (desistiu após 3 tentativas):', e); }
    updateRing('Analisando print' + (totalImgs>1 ? ' ' + Math.min(i+2,totalImgs) + ' de ' + totalImgs : '') + ' ' + dot, false);
  }
  let videoFrameReadFailed = false;
  for (let v = 0; v < videos.length; v++) {
    try {
      updateRing('Preparando vídeo ' + dot, false);
      const frames = await extractVideoFrames(videos[v], (done, total) => {
        updateRing('Preparando vídeo · ' + done + '/' + total + ' quadros ' + dot, false);
      });
      for (let i = 0; i < frames.length; i++) {
        try {
          const trips = await extractWithRetry(
            () => extractTripsFromBase64(frames[i], 'image/jpeg', plat),
            () => updateRing(retryIcon + ' Processando novamente...', true)
          );
          absorb(trips);
        } catch(e) { console.warn('Frame erro (desistiu após 3 tentativas):', e); }
        updateRing('Analisando vídeo · ' + (i+1) + '/' + frames.length + ' quadros ' + dot, false);
      }
    } catch(e) { videoFrameReadFailed = true; console.warn('Erro vídeo '+(v+1)+':', e); }
  }
  if (videos.length && videoFrameReadFailed && unique.length === 0) {
    showToast('Não consegui decodificar os quadros deste vídeo neste aparelho. O arquivo pode ser válido; tente novamente.', 'error');
    closeMo('mo-import-confirm');
    return;
  }

  // Só envia o arquivo original depois que a leitura local terminou.
  // Em celular/WebView, fazer upload e seek/decodificação simultaneamente
  // do mesmo File grande pode bloquear o decoder e deixar a tela parada.
  if (videos.length) updateRing('Salvando vídeo para conferência ' + dot, false);
  const _videoUploadResults = await Promise.all(
    videos.map(file => uploadImportVideo(file, operationalPlat, operationalDate))
  );
  window._pendingImportVideoIds = _videoUploadResults.filter(Boolean);
  if (videos.length && window._pendingImportVideoIds.length < videos.length) {
    showToast('Corridas importadas, mas o vídeo de auditoria não pôde ser salvo — confira sua conexão.', 'error');
  }
  flushDetail(); // fragmento de detalhe que ficou aberto no fim do vídeo

  // Resolve conflitos de horário: a regra pedida pra IA é "cada horário =
  // uma corrida só", mas o dedup acima só barra repetição idêntica
  // (mesmo horário E mesmo valor) — se o mesmo horário veio com valores
  // DIFERENTES em frames diferentes (mistura de cartão vizinho num frame
  // só), os dois passavam pra lista como corridas separadas, inflando a
  // contagem e o total. Aqui, pra cada (plataforma, horário), fica só a
  // leitura de valor que mais se repetiu entre os frames — a leitura
  // certa tende a se confirmar igual em vários frames seguidos (o vídeo
  // se sobrepõe), enquanto uma leitura errada por mistura de cartão
  // costuma aparecer uma vez só.
  function resolveTimeConflicts(list) {
    const groups = new Map();
    list.forEach(t => {
      const k = (t.platform || '') + '_' + t.time;
      (groups.get(k) || groups.set(k, []).get(k)).push(t);
    });
    const resolved = [];
    groups.forEach(items => {
      if (items.length === 1) { resolved.push(items[0]); return; }
      const byValue = new Map();
      items.forEach(t => {
        const vk = (t.value || 0).toFixed(2);
        (byValue.get(vk) || byValue.set(vk, []).get(vk)).push(t);
      });
      let best = null;
      byValue.forEach(arr => { if (!best || arr.length > best.length) best = arr; });
      // dentro do grupo vencedor, prefere o item com endereço mais completo
      let rep = best[0];
      best.forEach(t => {
        const len = (t.originFull || '').length + (t.destFull || '').length;
        const repLen = (rep.originFull || '').length + (rep.destFull || '').length;
        if (len > repLen) rep = t;
      });
      resolved.push(rep);
    });
    return resolved;
  }
  const resolvedUnique = resolveTimeConflicts(unique);
  unique.length = 0;
  unique.push(...resolvedUnique);

  // Funde os detalhes nas corridas da lista — SÓ completa km/tempo/endereço
  // de uma corrida que a LISTA já confirmou (com valor de verdade). Nunca
  // cria corrida nova a partir de detalhe sozinho: sem valor, não tem como
  // confirmar que é real, e foi exatamente isso que gerava corrida fantasma
  // no import (fragmento de detalhe virando corrida "avulsa" por engano).
  details.forEach(d => {
    let cand = null;
    if (d.time) cand = unique.find(u => u.time === d.time && !u._enriched);
    if (!cand && (d.originFull || d.destFull)) {
      cand = unique.find(u => !u._enriched &&
        ((d.originFull && u.originFull === d.originFull) || (d.destFull && u.destFull === d.destFull)));
    }
    if (cand) {
      cand._enriched = true;
      if (d.km != null && d.km > 0)       { cand.km = d.km; cand.kmEstimado = false; }
      if (d.duration != null && d.duration > 0) cand.duration = d.duration;
      // Endereço do detalhe é a linha inteira, sem truncar — prefere ele.
      if (d.originFull) { cand.originFull = d.originFull; cand.origin = d.origin; }
      if (d.destFull)   { cand.destFull = d.destFull; cand.dest = d.dest; }
    }
  });

  const day = DB.day(S.curDate);

  // Casa cada corrida do vídeo com auto_trips do dia ANTES de mostrar a
  // tela (pedido do Yuri, 16/08/2026) — permite separar "vai só corrigir
  // valor" de "é corrida nova" já na seleção, e esconder o que não muda
  // nada em vez de mostrar tudo misturado.
  const autoTripsPool = (await loadAutoTripsForOperationalDay(operationalPlat, operationalDate)) || [];
  const numEq = (a,b,tol) => Math.abs((a||0)-(b||0)) <= (tol||0.02);
  const annotated = unique.map((t,i) => {
    const matched = matchAutoTrip(t, autoTripsPool);
    if (matched) autoTripsPool.splice(autoTripsPool.indexOf(matched), 1); // consumido — nunca casa duas vezes
    const base = {...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _exists: isTripDuplicate(t, day.trips)};
    if (matched) {
      const newVal = parseFloat(t.value)||0;
      return {...base, _matchedAutoTripId: matched.id, _isNew: false,
        _oldValue: matched.offer_value, _changed: !numEq(matched.offer_value, newVal, 0.02)};
    }
    return {...base, _matchedAutoTripId: null, _isNew: true, _changed: true};
  });
  // Só mostra o que de fato vai mudar algo — casada sem diferença de valor
  // não aparece (nada a fazer), nova sem valor legível também não aparece
  // (sem valor não dá pra confirmar que é corrida de verdade).
  window._pendingTrips = annotated.filter(t => t._isNew ? (parseFloat(t.value)||0) > 0 : t._changed);
  // Corrida NOVA nunca confia no endereço do vídeo sem validar — mesma
  // regra do Kotlin (looksLikeAddress). Sem validação, fica sem endereço
  // em vez de arriscar lixo/mensagem de chat virando corrida (pedido
  // explícito do Yuri: "nunca endereço, a não ser que garanta").
  window._pendingTrips.forEach(t => {
    if (!t._isNew) return;
    if (t.originFull && !looksLikeAddress(t.originFull)) { t.originFull = ''; t.origin = ''; }
    if (t.destFull && !looksLikeAddress(t.destFull)) { t.destFull = ''; t.dest = ''; }
  });

  // Se algo foi declarado no pre-check, mostra a conferencia antes da lista.
  // Se pulou (tudo null), vai direto pra lista de confirmacao de sempre.
  const hasPrecheck = importPrecheck.uberCount != null || importPrecheck.p99Count != null;
  if (hasPrecheck) showImportMatch(unique); else showImportConfirm();
}

function s
===== vídeo @ 785861 =====
a extração bruta de cada frame,
  // e como os frames de vídeo se sobrepõem, a mesma corrida contava várias
  // vezes (ex: 37/17 na tela sendo que eram 17 corridas reais).
  const absorb = (trips) => {
    let sawList = false;
    (trips||[]).forEach(t => {
      if (t.screen === 'detail') {
        // Tela de detalhe: só km/tempo/endereço interessam — sem valor.
        // Se o fragmento não trouxe NENHUM desses três, não vale a pena.
        if (t.km == null && t.duration == null && !t.originFull && !t.destFull) return;
        mergeDetailFrag(t); return;
      }
      if (!(t.value > 0)) return; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
      sawList = true;
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seen.has(key)) return;
      seen.add(key);
      unique.push(t);
      foundCount++; foundVal += (t.value||0);
    });
    if (sawList) flushDetail(); // voltou pra tela de lista — fecha o detalhe que tava em aberto
  };
  const dot = '<span style="width:6px;height:6px;border-radius:50%;background:#22C55E;display:inline-block;animation:pulseDot 1s infinite ease-in-out"></span>';
  const retryIcon = '<svg style="animation:vspin 1s linear infinite" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#D97706" stroke-width="2.5"><path d="M21 12a9 9 0 1 1-2.6-6.4"/></svg>';
  const updateRing = (statusHtml, isWarn) => {
    document.getElementById('ic-body').innerHTML = renderImportRing(foundCount, targetCount, foundVal, targetValue, statusHtml, isWarn);
  };
  updateRing('Iniciando ' + dot, false);

  for (let i = 0; i < images.length; i++) {
    try {
      const trips = await extractWithRetry(
        () => extractTripsFromImage(images[i], plat),
        () => updateRing(retryIcon + ' Processando novamente...', true)
      );
      absorb(trips);
    } catch(e) { console.warn('Erro print '+(i+1)+' (desistiu após 3 tentativas):', e); }
    updateRing('Analisando print' + (totalImgs>1 ? ' ' + Math.min(i+2,totalImgs) + ' de ' + totalImgs : '') + ' ' + dot, false);
  }
  let videoFrameReadFailed = false;
  for (let v = 0; v < videos.length; v++) {
    try {
      updateRing('Preparando vídeo ' + dot, false);
      const frames = await extractVideoFrames(videos[v], (done, total) => {
        updateRing('Preparando vídeo · ' + done + '/' + total + ' quadros ' + dot, false);
      });
      for (let i = 0; i < frames.length; i++) {
        try {
          const trips = await extractWithRetry(
            () => extractTripsFromBase64(frames[i], 'image/jpeg', plat),
            () => updateRing(retryIcon + ' Processando novamente...', true)
          );
          absorb(trips);
        } catch(e) { console.warn('Frame erro (desistiu após 3 tentativas):', e); }
        updateRing('Analisando vídeo · ' + (i+1) + '/' + frames.length + ' quadros ' + dot, false);
      }
    } catch(e) { videoFrameReadFailed = true; console.warn('Erro vídeo '+(v+1)+':', e); }
  }
  if (videos.length && videoFrameReadFailed && unique.length === 0) {
    showToast('Não consegui decodificar os quadros deste vídeo neste aparelho. O arquivo pode ser válido; tente novamente.', 'error');
    closeMo('mo-import-confirm');
    return;
  }

  // Só envia o arquivo original depois que a leitura local terminou.
  // Em celular/WebView, fazer upload e seek/decodificação simultaneamente
  // do mesmo File grande pode bloquear o decoder e deixar a tela parada.
  if (videos.length) updateRing('Salvando vídeo para conferência ' + dot, false);
  const _videoUploadResults = await Promise.all(
    videos.map(file => uploadImportVideo(file, operationalPlat, operationalDate))
  );
  window._pendingImportVideoIds = _videoUploadResults.filter(Boolean);
  if (videos.length && window._pendingImportVideoIds.length < videos.length) {
    showToast('Corridas importadas, mas o vídeo de auditoria não pôde ser salvo — confira sua conexão.', 'error');
  }
  flushDetail(); // fragmento de detalhe que ficou aberto no fim do vídeo

  // Resolve conflitos de horário: a regra pedida pra IA é "cada horário =
  // uma corrida só", mas o dedup acima só barra repetição idêntica
  // (mesmo horário E mesmo valor) — se o mesmo horário veio com valores
  // DIFERENTES em frames diferentes (mistura de cartão vizinho num frame
  // só), os dois passavam pra lista como corridas separadas, inflando a
  // contagem e o total. Aqui, pra cada (plataforma, horário), fica só a
  // leitura de valor que mais se repetiu entre os frames — a leitura
  // certa tende a se confirmar igual em vários frames seguidos (o vídeo
  // se sobrepõe), enquanto uma leitura errada por mistura de cartão
  // costuma aparecer uma vez só.
  function resolveTimeConflicts(list) {
    const groups = new Map();
    list.forEach(t => {
      const k = (t.platform || '') + '_' + t.time;
      (groups.get(k) || groups.set(k, []).get(k)).push(t);
    });
    const resolved = [];
    groups.forEach(items => {
      if (items.length === 1) { resolved.push(items[0]); return; }
      const byValue = new Map();
      items.forEach(t => {
        const vk = (t.value || 0).toFixed(2);
        (byValue.get(vk) || byValue.set(vk, []).get(vk)).push(t);
      });
      let best = null;
      byValue.forEach(arr => { if (!best || arr.length > best.length) best = arr; });
      // dentro do grupo vencedor, prefere o item com endereço mais completo
      let rep = best[0];
      best.forEach(t => {
        const len = (t.originFull || '').length + (t.destFull || '').length;
        const repLen = (rep.originFull || '').length + (rep.destFull || '').length;
        if (len > repLen) rep = t;
      });
      resolved.push(rep);
    });
    return resolved;
  }
  const resolvedUnique = resolveTimeConflicts(unique);
  unique.length = 0;
  unique.push(...resolvedUnique);

  // Funde os detalhes nas corridas da lista — SÓ completa km/tempo/endereço
  // de uma corrida que a LISTA já confirmou (com valor de verdade). Nunca
  // cria corrida nova a partir de detalhe sozinho: sem valor, não tem como
  // confirmar que é real, e foi exatamente isso que gerava corrida fantasma
  // no import (fragmento de detalhe virando corrida "avulsa" por engano).
  details.forEach(d => {
    let cand = null;
    if (d.time) cand = unique.find(u => u.time === d.time && !u._enriched);
    if (!cand && (d.originFull || d.destFull)) {
      cand = unique.find(u => !u._enriched &&
        ((d.originFull && u.originFull === d.originFull) || (d.destFull && u.destFull === d.destFull)));
    }
    if (cand) {
      cand._enriched = true;
      if (d.km != null && d.km > 0)       { cand.km = d.km; cand.kmEstimado = false; }
      if (d.duration != null && d.duration > 0) cand.duration = d.duration;
      // Endereço do detalhe é a linha inteira, sem truncar — prefere ele.
      if (d.originFull) { cand.originFull = d.originFull; cand.origin = d.origin; }
      if (d.destFull)   { cand.destFull = d.destFull; cand.dest = d.dest; }
    }
  });

  const day = DB.day(S.curDate);

  // Casa cada corrida do vídeo com auto_trips do dia ANTES de mostrar a
  // tela (pedido do Yuri, 16/08/2026) — permite separar "vai só corrigir
  // valor" de "é corrida nova" já na seleção, e esconder o que não muda
  // nada em vez de mostrar tudo misturado.
  const autoTripsPool = (await loadAutoTripsForOperationalDay(operationalPlat, operationalDate)) || [];
  const numEq = (a,b,tol) => Math.abs((a||0)-(b||0)) <= (tol||0.02);
  const annotated = unique.map((t,i) => {
    const matched = matchAutoTrip(t, autoTripsPool);
    if (matched) autoTripsPool.splice(autoTripsPool.indexOf(matched), 1); // consumido — nunca casa duas vezes
    const base = {...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _exists: isTripDuplicate(t, day.trips)};
    if (matched) {
      const newVal = parseFloat(t.value)||0;
      return {...base, _matchedAutoTripId: matched.id, _isNew: false,
        _oldValue: matched.offer_value, _changed: !numEq(matched.offer_value, newVal, 0.02)};
    }
    return {...base, _matchedAutoTripId: null, _isNew: true, _changed: true};
  });
  // Só mostra o que de fato vai mudar algo — casada sem diferença de valor
  // não aparece (nada a fazer), nova sem valor legível também não aparece
  // (sem valor não dá pra confirmar que é corrida de verdade).
  window._pendingTrips = annotated.filter(t => t._isNew ? (parseFloat(t.value)||0) > 0 : t._changed);
  // Corrida NOVA nunca confia no endereço do vídeo sem validar — mesma
  // regra do Kotlin (looksLikeAddress). Sem validação, fica sem endereço
  // em vez de arriscar lixo/mensagem de chat virando corrida (pedido
  // explícito do Yuri: "nunca endereço, a não ser que garanta").
  window._pendingTrips.forEach(t => {
    if (!t._isNew) return;
    if (t.originFull && !looksLikeAddress(t.originFull)) { t.originFull = ''; t.origin = ''; }
    if (t.destFull && !looksLikeAddress(t.destFull)) { t.destFull = ''; t.dest = ''; }
  });

  // Se algo foi declarado no pre-check, mostra a conferencia antes da lista.
  // Se pulou (tudo null), vai direto pra lista de confirmacao de sempre.
  const hasPrecheck = importPrecheck.uberCount != null || importPrecheck.p99Count != null;
  if (hasPrecheck) showImportMatch(unique); else showImportConfirm();
}

function showImportMatch(unique) {
  const uberFound = unique.filter(t => t.platform === 'uber');
  const p99Found  = unique.filter(t => t.platform === '99');
  const uberFoundCount = uberFound.length, uberFoundVal = uberFound.reduce((s,t)=>s+(t.value||0),0);
  const p99FoundCount  = p99Found.length,  p99FoundVal  = p99Found.reduce((s,t)=>s+(t.value||0),0);

  const checks = [];
  let anyMissing = false, anyExtra = false;
  // Checagem EXATA: encontrar MAIS do que o declarado é tão errado quanto
  // encontrar menos (significa corrida lida em dobro/fundida). 18/17 não
  // pode aparecer como 'bateu certinho'. Valor com tolerância de 1 centavo.
  const evalPlat = (label, foundCount, foundVa
===== vídeo @ 786229 =====
       // Se o fragmento não trouxe NENHUM desses três, não vale a pena.
        if (t.km == null && t.duration == null && !t.originFull && !t.destFull) return;
        mergeDetailFrag(t); return;
      }
      if (!(t.value > 0)) return; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
      sawList = true;
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seen.has(key)) return;
      seen.add(key);
      unique.push(t);
      foundCount++; foundVal += (t.value||0);
    });
    if (sawList) flushDetail(); // voltou pra tela de lista — fecha o detalhe que tava em aberto
  };
  const dot = '<span style="width:6px;height:6px;border-radius:50%;background:#22C55E;display:inline-block;animation:pulseDot 1s infinite ease-in-out"></span>';
  const retryIcon = '<svg style="animation:vspin 1s linear infinite" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#D97706" stroke-width="2.5"><path d="M21 12a9 9 0 1 1-2.6-6.4"/></svg>';
  const updateRing = (statusHtml, isWarn) => {
    document.getElementById('ic-body').innerHTML = renderImportRing(foundCount, targetCount, foundVal, targetValue, statusHtml, isWarn);
  };
  updateRing('Iniciando ' + dot, false);

  for (let i = 0; i < images.length; i++) {
    try {
      const trips = await extractWithRetry(
        () => extractTripsFromImage(images[i], plat),
        () => updateRing(retryIcon + ' Processando novamente...', true)
      );
      absorb(trips);
    } catch(e) { console.warn('Erro print '+(i+1)+' (desistiu após 3 tentativas):', e); }
    updateRing('Analisando print' + (totalImgs>1 ? ' ' + Math.min(i+2,totalImgs) + ' de ' + totalImgs : '') + ' ' + dot, false);
  }
  let videoFrameReadFailed = false;
  for (let v = 0; v < videos.length; v++) {
    try {
      updateRing('Preparando vídeo ' + dot, false);
      const frames = await extractVideoFrames(videos[v], (done, total) => {
        updateRing('Preparando vídeo · ' + done + '/' + total + ' quadros ' + dot, false);
      });
      for (let i = 0; i < frames.length; i++) {
        try {
          const trips = await extractWithRetry(
            () => extractTripsFromBase64(frames[i], 'image/jpeg', plat),
            () => updateRing(retryIcon + ' Processando novamente...', true)
          );
          absorb(trips);
        } catch(e) { console.warn('Frame erro (desistiu após 3 tentativas):', e); }
        updateRing('Analisando vídeo · ' + (i+1) + '/' + frames.length + ' quadros ' + dot, false);
      }
    } catch(e) { videoFrameReadFailed = true; console.warn('Erro vídeo '+(v+1)+':', e); }
  }
  if (videos.length && videoFrameReadFailed && unique.length === 0) {
    showToast('Não consegui decodificar os quadros deste vídeo neste aparelho. O arquivo pode ser válido; tente novamente.', 'error');
    closeMo('mo-import-confirm');
    return;
  }

  // Só envia o arquivo original depois que a leitura local terminou.
  // Em celular/WebView, fazer upload e seek/decodificação simultaneamente
  // do mesmo File grande pode bloquear o decoder e deixar a tela parada.
  if (videos.length) updateRing('Salvando vídeo para conferência ' + dot, false);
  const _videoUploadResults = await Promise.all(
    videos.map(file => uploadImportVideo(file, operationalPlat, operationalDate))
  );
  window._pendingImportVideoIds = _videoUploadResults.filter(Boolean);
  if (videos.length && window._pendingImportVideoIds.length < videos.length) {
    showToast('Corridas importadas, mas o vídeo de auditoria não pôde ser salvo — confira sua conexão.', 'error');
  }
  flushDetail(); // fragmento de detalhe que ficou aberto no fim do vídeo

  // Resolve conflitos de horário: a regra pedida pra IA é "cada horário =
  // uma corrida só", mas o dedup acima só barra repetição idêntica
  // (mesmo horário E mesmo valor) — se o mesmo horário veio com valores
  // DIFERENTES em frames diferentes (mistura de cartão vizinho num frame
  // só), os dois passavam pra lista como corridas separadas, inflando a
  // contagem e o total. Aqui, pra cada (plataforma, horário), fica só a
  // leitura de valor que mais se repetiu entre os frames — a leitura
  // certa tende a se confirmar igual em vários frames seguidos (o vídeo
  // se sobrepõe), enquanto uma leitura errada por mistura de cartão
  // costuma aparecer uma vez só.
  function resolveTimeConflicts(list) {
    const groups = new Map();
    list.forEach(t => {
      const k = (t.platform || '') + '_' + t.time;
      (groups.get(k) || groups.set(k, []).get(k)).push(t);
    });
    const resolved = [];
    groups.forEach(items => {
      if (items.length === 1) { resolved.push(items[0]); return; }
      const byValue = new Map();
      items.forEach(t => {
        const vk = (t.value || 0).toFixed(2);
        (byValue.get(vk) || byValue.set(vk, []).get(vk)).push(t);
      });
      let best = null;
      byValue.forEach(arr => { if (!best || arr.length > best.length) best = arr; });
      // dentro do grupo vencedor, prefere o item com endereço mais completo
      let rep = best[0];
      best.forEach(t => {
        const len = (t.originFull || '').length + (t.destFull || '').length;
        const repLen = (rep.originFull || '').length + (rep.destFull || '').length;
        if (len > repLen) rep = t;
      });
      resolved.push(rep);
    });
    return resolved;
  }
  const resolvedUnique = resolveTimeConflicts(unique);
  unique.length = 0;
  unique.push(...resolvedUnique);

  // Funde os detalhes nas corridas da lista — SÓ completa km/tempo/endereço
  // de uma corrida que a LISTA já confirmou (com valor de verdade). Nunca
  // cria corrida nova a partir de detalhe sozinho: sem valor, não tem como
  // confirmar que é real, e foi exatamente isso que gerava corrida fantasma
  // no import (fragmento de detalhe virando corrida "avulsa" por engano).
  details.forEach(d => {
    let cand = null;
    if (d.time) cand = unique.find(u => u.time === d.time && !u._enriched);
    if (!cand && (d.originFull || d.destFull)) {
      cand = unique.find(u => !u._enriched &&
        ((d.originFull && u.originFull === d.originFull) || (d.destFull && u.destFull === d.destFull)));
    }
    if (cand) {
      cand._enriched = true;
      if (d.km != null && d.km > 0)       { cand.km = d.km; cand.kmEstimado = false; }
      if (d.duration != null && d.duration > 0) cand.duration = d.duration;
      // Endereço do detalhe é a linha inteira, sem truncar — prefere ele.
      if (d.originFull) { cand.originFull = d.originFull; cand.origin = d.origin; }
      if (d.destFull)   { cand.destFull = d.destFull; cand.dest = d.dest; }
    }
  });

  const day = DB.day(S.curDate);

  // Casa cada corrida do vídeo com auto_trips do dia ANTES de mostrar a
  // tela (pedido do Yuri, 16/08/2026) — permite separar "vai só corrigir
  // valor" de "é corrida nova" já na seleção, e esconder o que não muda
  // nada em vez de mostrar tudo misturado.
  const autoTripsPool = (await loadAutoTripsForOperationalDay(operationalPlat, operationalDate)) || [];
  const numEq = (a,b,tol) => Math.abs((a||0)-(b||0)) <= (tol||0.02);
  const annotated = unique.map((t,i) => {
    const matched = matchAutoTrip(t, autoTripsPool);
    if (matched) autoTripsPool.splice(autoTripsPool.indexOf(matched), 1); // consumido — nunca casa duas vezes
    const base = {...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _exists: isTripDuplicate(t, day.trips)};
    if (matched) {
      const newVal = parseFloat(t.value)||0;
      return {...base, _matchedAutoTripId: matched.id, _isNew: false,
        _oldValue: matched.offer_value, _changed: !numEq(matched.offer_value, newVal, 0.02)};
    }
    return {...base, _matchedAutoTripId: null, _isNew: true, _changed: true};
  });
  // Só mostra o que de fato vai mudar algo — casada sem diferença de valor
  // não aparece (nada a fazer), nova sem valor legível também não aparece
  // (sem valor não dá pra confirmar que é corrida de verdade).
  window._pendingTrips = annotated.filter(t => t._isNew ? (parseFloat(t.value)||0) > 0 : t._changed);
  // Corrida NOVA nunca confia no endereço do vídeo sem validar — mesma
  // regra do Kotlin (looksLikeAddress). Sem validação, fica sem endereço
  // em vez de arriscar lixo/mensagem de chat virando corrida (pedido
  // explícito do Yuri: "nunca endereço, a não ser que garanta").
  window._pendingTrips.forEach(t => {
    if (!t._isNew) return;
    if (t.originFull && !looksLikeAddress(t.originFull)) { t.originFull = ''; t.origin = ''; }
    if (t.destFull && !looksLikeAddress(t.destFull)) { t.destFull = ''; t.dest = ''; }
  });

  // Se algo foi declarado no pre-check, mostra a conferencia antes da lista.
  // Se pulou (tudo null), vai direto pra lista de confirmacao de sempre.
  const hasPrecheck = importPrecheck.uberCount != null || importPrecheck.p99Count != null;
  if (hasPrecheck) showImportMatch(unique); else showImportConfirm();
}

function showImportMatch(unique) {
  const uberFound = unique.filter(t => t.platform === 'uber');
  const p99Found  = unique.filter(t => t.platform === '99');
  const uberFoundCount = uberFound.length, uberFoundVal = uberFound.reduce((s,t)=>s+(t.value||0),0);
  const p99FoundCount  = p99Found.length,  p99FoundVal  = p99Found.reduce((s,t)=>s+(t.value||0),0);

  const checks = [];
  let anyMissing = false, anyExtra = false;
  // Checagem EXATA: encontrar MAIS do que o declarado é tão errado quanto
  // encontrar menos (significa corrida lida em dobro/fundida). 18/17 não
  // pode aparecer como 'bateu certinho'. Valor com tolerância de 1 centavo.
  const evalPlat = (label, foundCount, foundVal, decCount, decVal) => {
    let ok = foundCount === decCount;
    if (ok && decVal != null) ok = Math.abs(foundVal - decVal) < 0.01;
    if (!ok) { if (foundCount > decCount || (decVal != null && foundVal > decVal + 0.009)) anyExtra = true; else anyMissing = true; }
    checks.push({label, ok, txt: `${foundCount}/${decCount} · ${DB.fRI(foundVal)}${decVal != null ?
===== vídeo @ 786954 =====
display:inline-block;animation:pulseDot 1s infinite ease-in-out"></span>';
  const retryIcon = '<svg style="animation:vspin 1s linear infinite" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#D97706" stroke-width="2.5"><path d="M21 12a9 9 0 1 1-2.6-6.4"/></svg>';
  const updateRing = (statusHtml, isWarn) => {
    document.getElementById('ic-body').innerHTML = renderImportRing(foundCount, targetCount, foundVal, targetValue, statusHtml, isWarn);
  };
  updateRing('Iniciando ' + dot, false);

  for (let i = 0; i < images.length; i++) {
    try {
      const trips = await extractWithRetry(
        () => extractTripsFromImage(images[i], plat),
        () => updateRing(retryIcon + ' Processando novamente...', true)
      );
      absorb(trips);
    } catch(e) { console.warn('Erro print '+(i+1)+' (desistiu após 3 tentativas):', e); }
    updateRing('Analisando print' + (totalImgs>1 ? ' ' + Math.min(i+2,totalImgs) + ' de ' + totalImgs : '') + ' ' + dot, false);
  }
  let videoFrameReadFailed = false;
  for (let v = 0; v < videos.length; v++) {
    try {
      updateRing('Preparando vídeo ' + dot, false);
      const frames = await extractVideoFrames(videos[v], (done, total) => {
        updateRing('Preparando vídeo · ' + done + '/' + total + ' quadros ' + dot, false);
      });
      for (let i = 0; i < frames.length; i++) {
        try {
          const trips = await extractWithRetry(
            () => extractTripsFromBase64(frames[i], 'image/jpeg', plat),
            () => updateRing(retryIcon + ' Processando novamente...', true)
          );
          absorb(trips);
        } catch(e) { console.warn('Frame erro (desistiu após 3 tentativas):', e); }
        updateRing('Analisando vídeo · ' + (i+1) + '/' + frames.length + ' quadros ' + dot, false);
      }
    } catch(e) { videoFrameReadFailed = true; console.warn('Erro vídeo '+(v+1)+':', e); }
  }
  if (videos.length && videoFrameReadFailed && unique.length === 0) {
    showToast('Não consegui decodificar os quadros deste vídeo neste aparelho. O arquivo pode ser válido; tente novamente.', 'error');
    closeMo('mo-import-confirm');
    return;
  }

  // Só envia o arquivo original depois que a leitura local terminou.
  // Em celular/WebView, fazer upload e seek/decodificação simultaneamente
  // do mesmo File grande pode bloquear o decoder e deixar a tela parada.
  if (videos.length) updateRing('Salvando vídeo para conferência ' + dot, false);
  const _videoUploadResults = await Promise.all(
    videos.map(file => uploadImportVideo(file, operationalPlat, operationalDate))
  );
  window._pendingImportVideoIds = _videoUploadResults.filter(Boolean);
  if (videos.length && window._pendingImportVideoIds.length < videos.length) {
    showToast('Corridas importadas, mas o vídeo de auditoria não pôde ser salvo — confira sua conexão.', 'error');
  }
  flushDetail(); // fragmento de detalhe que ficou aberto no fim do vídeo

  // Resolve conflitos de horário: a regra pedida pra IA é "cada horário =
  // uma corrida só", mas o dedup acima só barra repetição idêntica
  // (mesmo horário E mesmo valor) — se o mesmo horário veio com valores
  // DIFERENTES em frames diferentes (mistura de cartão vizinho num frame
  // só), os dois passavam pra lista como corridas separadas, inflando a
  // contagem e o total. Aqui, pra cada (plataforma, horário), fica só a
  // leitura de valor que mais se repetiu entre os frames — a leitura
  // certa tende a se confirmar igual em vários frames seguidos (o vídeo
  // se sobrepõe), enquanto uma leitura errada por mistura de cartão
  // costuma aparecer uma vez só.
  function resolveTimeConflicts(list) {
    const groups = new Map();
    list.forEach(t => {
      const k = (t.platform || '') + '_' + t.time;
      (groups.get(k) || groups.set(k, []).get(k)).push(t);
    });
    const resolved = [];
    groups.forEach(items => {
      if (items.length === 1) { resolved.push(items[0]); return; }
      const byValue = new Map();
      items.forEach(t => {
        const vk = (t.value || 0).toFixed(2);
        (byValue.get(vk) || byValue.set(vk, []).get(vk)).push(t);
      });
      let best = null;
      byValue.forEach(arr => { if (!best || arr.length > best.length) best = arr; });
      // dentro do grupo vencedor, prefere o item com endereço mais completo
      let rep = best[0];
      best.forEach(t => {
        const len = (t.originFull || '').length + (t.destFull || '').length;
        const repLen = (rep.originFull || '').length + (rep.destFull || '').length;
        if (len > repLen) rep = t;
      });
      resolved.push(rep);
    });
    return resolved;
  }
  const resolvedUnique = resolveTimeConflicts(unique);
  unique.length = 0;
  unique.push(...resolvedUnique);

  // Funde os detalhes nas corridas da lista — SÓ completa km/tempo/endereço
  // de uma corrida que a LISTA já confirmou (com valor de verdade). Nunca
  // cria corrida nova a partir de detalhe sozinho: sem valor, não tem como
  // confirmar que é real, e foi exatamente isso que gerava corrida fantasma
  // no import (fragmento de detalhe virando corrida "avulsa" por engano).
  details.forEach(d => {
    let cand = null;
    if (d.time) cand = unique.find(u => u.time === d.time && !u._enriched);
    if (!cand && (d.originFull || d.destFull)) {
      cand = unique.find(u => !u._enriched &&
        ((d.originFull && u.originFull === d.originFull) || (d.destFull && u.destFull === d.destFull)));
    }
    if (cand) {
      cand._enriched = true;
      if (d.km != null && d.km > 0)       { cand.km = d.km; cand.kmEstimado = false; }
      if (d.duration != null && d.duration > 0) cand.duration = d.duration;
      // Endereço do detalhe é a linha inteira, sem truncar — prefere ele.
      if (d.originFull) { cand.originFull = d.originFull; cand.origin = d.origin; }
      if (d.destFull)   { cand.destFull = d.destFull; cand.dest = d.dest; }
    }
  });

  const day = DB.day(S.curDate);

  // Casa cada corrida do vídeo com auto_trips do dia ANTES de mostrar a
  // tela (pedido do Yuri, 16/08/2026) — permite separar "vai só corrigir
  // valor" de "é corrida nova" já na seleção, e esconder o que não muda
  // nada em vez de mostrar tudo misturado.
  const autoTripsPool = (await loadAutoTripsForOperationalDay(operationalPlat, operationalDate)) || [];
  const numEq = (a,b,tol) => Math.abs((a||0)-(b||0)) <= (tol||0.02);
  const annotated = unique.map((t,i) => {
    const matched = matchAutoTrip(t, autoTripsPool);
    if (matched) autoTripsPool.splice(autoTripsPool.indexOf(matched), 1); // consumido — nunca casa duas vezes
    const base = {...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _exists: isTripDuplicate(t, day.trips)};
    if (matched) {
      const newVal = parseFloat(t.value)||0;
      return {...base, _matchedAutoTripId: matched.id, _isNew: false,
        _oldValue: matched.offer_value, _changed: !numEq(matched.offer_value, newVal, 0.02)};
    }
    return {...base, _matchedAutoTripId: null, _isNew: true, _changed: true};
  });
  // Só mostra o que de fato vai mudar algo — casada sem diferença de valor
  // não aparece (nada a fazer), nova sem valor legível também não aparece
  // (sem valor não dá pra confirmar que é corrida de verdade).
  window._pendingTrips = annotated.filter(t => t._isNew ? (parseFloat(t.value)||0) > 0 : t._changed);
  // Corrida NOVA nunca confia no endereço do vídeo sem validar — mesma
  // regra do Kotlin (looksLikeAddress). Sem validação, fica sem endereço
  // em vez de arriscar lixo/mensagem de chat virando corrida (pedido
  // explícito do Yuri: "nunca endereço, a não ser que garanta").
  window._pendingTrips.forEach(t => {
    if (!t._isNew) return;
    if (t.originFull && !looksLikeAddress(t.originFull)) { t.originFull = ''; t.origin = ''; }
    if (t.destFull && !looksLikeAddress(t.destFull)) { t.destFull = ''; t.dest = ''; }
  });

  // Se algo foi declarado no pre-check, mostra a conferencia antes da lista.
  // Se pulou (tudo null), vai direto pra lista de confirmacao de sempre.
  const hasPrecheck = importPrecheck.uberCount != null || importPrecheck.p99Count != null;
  if (hasPrecheck) showImportMatch(unique); else showImportConfirm();
}

function showImportMatch(unique) {
  const uberFound = unique.filter(t => t.platform === 'uber');
  const p99Found  = unique.filter(t => t.platform === '99');
  const uberFoundCount = uberFound.length, uberFoundVal = uberFound.reduce((s,t)=>s+(t.value||0),0);
  const p99FoundCount  = p99Found.length,  p99FoundVal  = p99Found.reduce((s,t)=>s+(t.value||0),0);

  const checks = [];
  let anyMissing = false, anyExtra = false;
  // Checagem EXATA: encontrar MAIS do que o declarado é tão errado quanto
  // encontrar menos (significa corrida lida em dobro/fundida). 18/17 não
  // pode aparecer como 'bateu certinho'. Valor com tolerância de 1 centavo.
  const evalPlat = (label, foundCount, foundVal, decCount, decVal) => {
    let ok = foundCount === decCount;
    if (ok && decVal != null) ok = Math.abs(foundVal - decVal) < 0.01;
    if (!ok) { if (foundCount > decCount || (decVal != null && foundVal > decVal + 0.009)) anyExtra = true; else anyMissing = true; }
    checks.push({label, ok, txt: `${foundCount}/${decCount} · ${DB.fRI(foundVal)}${decVal != null ? ' de ' + DB.fRI(decVal) : ''}`});
  };
  if (importPrecheck.uberCount != null) evalPlat('Uber', uberFoundCount, uberFoundVal, importPrecheck.uberCount, importPrecheck.uberValue);
  if (importPrecheck.p99Count != null) evalPlat('99', p99FoundCount, p99FoundVal, importPrecheck.p99Count, importPrecheck.p99Value);
  const allOk = !anyMissing && !anyExtra;
  const pillsHtml = checks.map(c => `<span style="font-size:11px;font-weight:700;padding:4px 10px;border-radius:99px;background:${c.ok?'#DCFCE7':'#FEE2E2'};color:${c.ok?'#16A34A':'#DC2626'}">${c.label} ${c.txt} ${c.ok?'✓':'✗'}</span>`).join('');

  const warnTitle = anyExtra ? 'Achei corridas a mais' : 'Faltou alguma coisa';
  const warnMsg = anyExtra
    ? 'Alguma co
===== vídeo @ 789416 =====
ploadResults = await Promise.all(
    videos.map(file => uploadImportVideo(file, operationalPlat, operationalDate))
  );
  window._pendingImportVideoIds = _videoUploadResults.filter(Boolean);
  if (videos.length && window._pendingImportVideoIds.length < videos.length) {
    showToast('Corridas importadas, mas o vídeo de auditoria não pôde ser salvo — confira sua conexão.', 'error');
  }
  flushDetail(); // fragmento de detalhe que ficou aberto no fim do vídeo

  // Resolve conflitos de horário: a regra pedida pra IA é "cada horário =
  // uma corrida só", mas o dedup acima só barra repetição idêntica
  // (mesmo horário E mesmo valor) — se o mesmo horário veio com valores
  // DIFERENTES em frames diferentes (mistura de cartão vizinho num frame
  // só), os dois passavam pra lista como corridas separadas, inflando a
  // contagem e o total. Aqui, pra cada (plataforma, horário), fica só a
  // leitura de valor que mais se repetiu entre os frames — a leitura
  // certa tende a se confirmar igual em vários frames seguidos (o vídeo
  // se sobrepõe), enquanto uma leitura errada por mistura de cartão
  // costuma aparecer uma vez só.
  function resolveTimeConflicts(list) {
    const groups = new Map();
    list.forEach(t => {
      const k = (t.platform || '') + '_' + t.time;
      (groups.get(k) || groups.set(k, []).get(k)).push(t);
    });
    const resolved = [];
    groups.forEach(items => {
      if (items.length === 1) { resolved.push(items[0]); return; }
      const byValue = new Map();
      items.forEach(t => {
        const vk = (t.value || 0).toFixed(2);
        (byValue.get(vk) || byValue.set(vk, []).get(vk)).push(t);
      });
      let best = null;
      byValue.forEach(arr => { if (!best || arr.length > best.length) best = arr; });
      // dentro do grupo vencedor, prefere o item com endereço mais completo
      let rep = best[0];
      best.forEach(t => {
        const len = (t.originFull || '').length + (t.destFull || '').length;
        const repLen = (rep.originFull || '').length + (rep.destFull || '').length;
        if (len > repLen) rep = t;
      });
      resolved.push(rep);
    });
    return resolved;
  }
  const resolvedUnique = resolveTimeConflicts(unique);
  unique.length = 0;
  unique.push(...resolvedUnique);

  // Funde os detalhes nas corridas da lista — SÓ completa km/tempo/endereço
  // de uma corrida que a LISTA já confirmou (com valor de verdade). Nunca
  // cria corrida nova a partir de detalhe sozinho: sem valor, não tem como
  // confirmar que é real, e foi exatamente isso que gerava corrida fantasma
  // no import (fragmento de detalhe virando corrida "avulsa" por engano).
  details.forEach(d => {
    let cand = null;
    if (d.time) cand = unique.find(u => u.time === d.time && !u._enriched);
    if (!cand && (d.originFull || d.destFull)) {
      cand = unique.find(u => !u._enriched &&
        ((d.originFull && u.originFull === d.originFull) || (d.destFull && u.destFull === d.destFull)));
    }
    if (cand) {
      cand._enriched = true;
      if (d.km != null && d.km > 0)       { cand.km = d.km; cand.kmEstimado = false; }
      if (d.duration != null && d.duration > 0) cand.duration = d.duration;
      // Endereço do detalhe é a linha inteira, sem truncar — prefere ele.
      if (d.originFull) { cand.originFull = d.originFull; cand.origin = d.origin; }
      if (d.destFull)   { cand.destFull = d.destFull; cand.dest = d.dest; }
    }
  });

  const day = DB.day(S.curDate);

  // Casa cada corrida do vídeo com auto_trips do dia ANTES de mostrar a
  // tela (pedido do Yuri, 16/08/2026) — permite separar "vai só corrigir
  // valor" de "é corrida nova" já na seleção, e esconder o que não muda
  // nada em vez de mostrar tudo misturado.
  const autoTripsPool = (await loadAutoTripsForOperationalDay(operationalPlat, operationalDate)) || [];
  const numEq = (a,b,tol) => Math.abs((a||0)-(b||0)) <= (tol||0.02);
  const annotated = unique.map((t,i) => {
    const matched = matchAutoTrip(t, autoTripsPool);
    if (matched) autoTripsPool.splice(autoTripsPool.indexOf(matched), 1); // consumido — nunca casa duas vezes
    const base = {...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _exists: isTripDuplicate(t, day.trips)};
    if (matched) {
      const newVal = parseFloat(t.value)||0;
      return {...base, _matchedAutoTripId: matched.id, _isNew: false,
        _oldValue: matched.offer_value, _changed: !numEq(matched.offer_value, newVal, 0.02)};
    }
    return {...base, _matchedAutoTripId: null, _isNew: true, _changed: true};
  });
  // Só mostra o que de fato vai mudar algo — casada sem diferença de valor
  // não aparece (nada a fazer), nova sem valor legível também não aparece
  // (sem valor não dá pra confirmar que é corrida de verdade).
  window._pendingTrips = annotated.filter(t => t._isNew ? (parseFloat(t.value)||0) > 0 : t._changed);
  // Corrida NOVA nunca confia no endereço do vídeo sem validar — mesma
  // regra do Kotlin (looksLikeAddress). Sem validação, fica sem endereço
  // em vez de arriscar lixo/mensagem de chat virando corrida (pedido
  // explícito do Yuri: "nunca endereço, a não ser que garanta").
  window._pendingTrips.forEach(t => {
    if (!t._isNew) return;
    if (t.originFull && !looksLikeAddress(t.originFull)) { t.originFull = ''; t.origin = ''; }
    if (t.destFull && !looksLikeAddress(t.destFull)) { t.destFull = ''; t.dest = ''; }
  });

  // Se algo foi declarado no pre-check, mostra a conferencia antes da lista.
  // Se pulou (tudo null), vai direto pra lista de confirmacao de sempre.
  const hasPrecheck = importPrecheck.uberCount != null || importPrecheck.p99Count != null;
  if (hasPrecheck) showImportMatch(unique); else showImportConfirm();
}

function showImportMatch(unique) {
  const uberFound = unique.filter(t => t.platform === 'uber');
  const p99Found  = unique.filter(t => t.platform === '99');
  const uberFoundCount = uberFound.length, uberFoundVal = uberFound.reduce((s,t)=>s+(t.value||0),0);
  const p99FoundCount  = p99Found.length,  p99FoundVal  = p99Found.reduce((s,t)=>s+(t.value||0),0);

  const checks = [];
  let anyMissing = false, anyExtra = false;
  // Checagem EXATA: encontrar MAIS do que o declarado é tão errado quanto
  // encontrar menos (significa corrida lida em dobro/fundida). 18/17 não
  // pode aparecer como 'bateu certinho'. Valor com tolerância de 1 centavo.
  const evalPlat = (label, foundCount, foundVal, decCount, decVal) => {
    let ok = foundCount === decCount;
    if (ok && decVal != null) ok = Math.abs(foundVal - decVal) < 0.01;
    if (!ok) { if (foundCount > decCount || (decVal != null && foundVal > decVal + 0.009)) anyExtra = true; else anyMissing = true; }
    checks.push({label, ok, txt: `${foundCount}/${decCount} · ${DB.fRI(foundVal)}${decVal != null ? ' de ' + DB.fRI(decVal) : ''}`});
  };
  if (importPrecheck.uberCount != null) evalPlat('Uber', uberFoundCount, uberFoundVal, importPrecheck.uberCount, importPrecheck.uberValue);
  if (importPrecheck.p99Count != null) evalPlat('99', p99FoundCount, p99FoundVal, importPrecheck.p99Count, importPrecheck.p99Value);
  const allOk = !anyMissing && !anyExtra;
  const pillsHtml = checks.map(c => `<span style="font-size:11px;font-weight:700;padding:4px 10px;border-radius:99px;background:${c.ok?'#DCFCE7':'#FEE2E2'};color:${c.ok?'#16A34A':'#DC2626'}">${c.label} ${c.txt} ${c.ok?'✓':'✗'}</span>`).join('');

  const warnTitle = anyExtra ? 'Achei corridas a mais' : 'Faltou alguma coisa';
  const warnMsg = anyExtra
    ? 'Alguma corrida foi lida em dobro ou misturada. Na lista, desmarque as que estiverem repetidas ou com dados estranhos antes de importar.'
    : 'Confira se subiu todos os prints/vídeos desse app';
  const cardHtml = allOk
    ? `<div style="border-radius:16px;padding:16px;text-align:center;margin-bottom:14px;background:#ECFDF3;border:1.5px solid #BBF7D0">
        <div style="width:44px;height:44px;border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto 10px;background:#DCFCE7;color:#16A34A"><svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg></div>
        <b style="display:block;font-size:14px;color:#0F172A;margin-bottom:4px">Importação validada</b>
        <span style="font-size:12px;color:#64748B;line-height:1.5">Os valores conferem com o que você informou</span>
        <div style="display:flex;gap:8px;justify-content:center;margin-top:10px;flex-wrap:wrap">${pillsHtml}</div>
      </div>`
    : `<div style="border-radius:16px;padding:16px;text-align:center;margin-bottom:14px;background:#FFFBEB;border:1.5px solid #FDE68A">
        <div style="width:44px;height:44px;border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto 10px;background:#FEF3C7;color:#D97706"><svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.4"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg></div>
        <b style="display:block;font-size:14px;color:#0F172A;margin-bottom:4px">${warnTitle}</b>
        <span style="font-size:12px;color:#64748B;line-height:1.5">${warnMsg}</span>
        <div style="display:flex;gap:8px;justify-content:center;margin-top:10px;flex-wrap:wrap">${pillsHtml}</div>
      </div>`;

  document.getElementById('ic-title').textContent = 'Conferência';
  document.getElementById('ic-body').innerHTML = cardHtml;
  const btn = document.getElementById('ic-btn');
  btn.style.display = 'block';
  btn.textContent = allOk ? 'Ver corridas encontradas' : 'Ver o que encontrei';
  btn.onclick = () => { showImportConfirm(); btn.onclick = confirmImport; };
}

function showSpinner(msg) {
  return '<div style="text-align:center;padding:28px 16px">' +
    '<div style="font-size:28px;animation:vspin 1s linear infinite;display:inline-block">&#9881;</div>' +
    '<div style="margin-top:12px;font-size:13px;color:#64748B;font-weight:500">' + msg + '
===== vídeo @ 790857 =====
1) { resolved.push(items[0]); return; }
      const byValue = new Map();
      items.forEach(t => {
        const vk = (t.value || 0).toFixed(2);
        (byValue.get(vk) || byValue.set(vk, []).get(vk)).push(t);
      });
      let best = null;
      byValue.forEach(arr => { if (!best || arr.length > best.length) best = arr; });
      // dentro do grupo vencedor, prefere o item com endereço mais completo
      let rep = best[0];
      best.forEach(t => {
        const len = (t.originFull || '').length + (t.destFull || '').length;
        const repLen = (rep.originFull || '').length + (rep.destFull || '').length;
        if (len > repLen) rep = t;
      });
      resolved.push(rep);
    });
    return resolved;
  }
  const resolvedUnique = resolveTimeConflicts(unique);
  unique.length = 0;
  unique.push(...resolvedUnique);

  // Funde os detalhes nas corridas da lista — SÓ completa km/tempo/endereço
  // de uma corrida que a LISTA já confirmou (com valor de verdade). Nunca
  // cria corrida nova a partir de detalhe sozinho: sem valor, não tem como
  // confirmar que é real, e foi exatamente isso que gerava corrida fantasma
  // no import (fragmento de detalhe virando corrida "avulsa" por engano).
  details.forEach(d => {
    let cand = null;
    if (d.time) cand = unique.find(u => u.time === d.time && !u._enriched);
    if (!cand && (d.originFull || d.destFull)) {
      cand = unique.find(u => !u._enriched &&
        ((d.originFull && u.originFull === d.originFull) || (d.destFull && u.destFull === d.destFull)));
    }
    if (cand) {
      cand._enriched = true;
      if (d.km != null && d.km > 0)       { cand.km = d.km; cand.kmEstimado = false; }
      if (d.duration != null && d.duration > 0) cand.duration = d.duration;
      // Endereço do detalhe é a linha inteira, sem truncar — prefere ele.
      if (d.originFull) { cand.originFull = d.originFull; cand.origin = d.origin; }
      if (d.destFull)   { cand.destFull = d.destFull; cand.dest = d.dest; }
    }
  });

  const day = DB.day(S.curDate);

  // Casa cada corrida do vídeo com auto_trips do dia ANTES de mostrar a
  // tela (pedido do Yuri, 16/08/2026) — permite separar "vai só corrigir
  // valor" de "é corrida nova" já na seleção, e esconder o que não muda
  // nada em vez de mostrar tudo misturado.
  const autoTripsPool = (await loadAutoTripsForOperationalDay(operationalPlat, operationalDate)) || [];
  const numEq = (a,b,tol) => Math.abs((a||0)-(b||0)) <= (tol||0.02);
  const annotated = unique.map((t,i) => {
    const matched = matchAutoTrip(t, autoTripsPool);
    if (matched) autoTripsPool.splice(autoTripsPool.indexOf(matched), 1); // consumido — nunca casa duas vezes
    const base = {...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _exists: isTripDuplicate(t, day.trips)};
    if (matched) {
      const newVal = parseFloat(t.value)||0;
      return {...base, _matchedAutoTripId: matched.id, _isNew: false,
        _oldValue: matched.offer_value, _changed: !numEq(matched.offer_value, newVal, 0.02)};
    }
    return {...base, _matchedAutoTripId: null, _isNew: true, _changed: true};
  });
  // Só mostra o que de fato vai mudar algo — casada sem diferença de valor
  // não aparece (nada a fazer), nova sem valor legível também não aparece
  // (sem valor não dá pra confirmar que é corrida de verdade).
  window._pendingTrips = annotated.filter(t => t._isNew ? (parseFloat(t.value)||0) > 0 : t._changed);
  // Corrida NOVA nunca confia no endereço do vídeo sem validar — mesma
  // regra do Kotlin (looksLikeAddress). Sem validação, fica sem endereço
  // em vez de arriscar lixo/mensagem de chat virando corrida (pedido
  // explícito do Yuri: "nunca endereço, a não ser que garanta").
  window._pendingTrips.forEach(t => {
    if (!t._isNew) return;
    if (t.originFull && !looksLikeAddress(t.originFull)) { t.originFull = ''; t.origin = ''; }
    if (t.destFull && !looksLikeAddress(t.destFull)) { t.destFull = ''; t.dest = ''; }
  });

  // Se algo foi declarado no pre-check, mostra a conferencia antes da lista.
  // Se pulou (tudo null), vai direto pra lista de confirmacao de sempre.
  const hasPrecheck = importPrecheck.uberCount != null || importPrecheck.p99Count != null;
  if (hasPrecheck) showImportMatch(unique); else showImportConfirm();
}

function showImportMatch(unique) {
  const uberFound = unique.filter(t => t.platform === 'uber');
  const p99Found  = unique.filter(t => t.platform === '99');
  const uberFoundCount = uberFound.length, uberFoundVal = uberFound.reduce((s,t)=>s+(t.value||0),0);
  const p99FoundCount  = p99Found.length,  p99FoundVal  = p99Found.reduce((s,t)=>s+(t.value||0),0);

  const checks = [];
  let anyMissing = false, anyExtra = false;
  // Checagem EXATA: encontrar MAIS do que o declarado é tão errado quanto
  // encontrar menos (significa corrida lida em dobro/fundida). 18/17 não
  // pode aparecer como 'bateu certinho'. Valor com tolerância de 1 centavo.
  const evalPlat = (label, foundCount, foundVal, decCount, decVal) => {
    let ok = foundCount === decCount;
    if (ok && decVal != null) ok = Math.abs(foundVal - decVal) < 0.01;
    if (!ok) { if (foundCount > decCount || (decVal != null && foundVal > decVal + 0.009)) anyExtra = true; else anyMissing = true; }
    checks.push({label, ok, txt: `${foundCount}/${decCount} · ${DB.fRI(foundVal)}${decVal != null ? ' de ' + DB.fRI(decVal) : ''}`});
  };
  if (importPrecheck.uberCount != null) evalPlat('Uber', uberFoundCount, uberFoundVal, importPrecheck.uberCount, importPrecheck.uberValue);
  if (importPrecheck.p99Count != null) evalPlat('99', p99FoundCount, p99FoundVal, importPrecheck.p99Count, importPrecheck.p99Value);
  const allOk = !anyMissing && !anyExtra;
  const pillsHtml = checks.map(c => `<span style="font-size:11px;font-weight:700;padding:4px 10px;border-radius:99px;background:${c.ok?'#DCFCE7':'#FEE2E2'};color:${c.ok?'#16A34A':'#DC2626'}">${c.label} ${c.txt} ${c.ok?'✓':'✗'}</span>`).join('');

  const warnTitle = anyExtra ? 'Achei corridas a mais' : 'Faltou alguma coisa';
  const warnMsg = anyExtra
    ? 'Alguma corrida foi lida em dobro ou misturada. Na lista, desmarque as que estiverem repetidas ou com dados estranhos antes de importar.'
    : 'Confira se subiu todos os prints/vídeos desse app';
  const cardHtml = allOk
    ? `<div style="border-radius:16px;padding:16px;text-align:center;margin-bottom:14px;background:#ECFDF3;border:1.5px solid #BBF7D0">
        <div style="width:44px;height:44px;border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto 10px;background:#DCFCE7;color:#16A34A"><svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg></div>
        <b style="display:block;font-size:14px;color:#0F172A;margin-bottom:4px">Importação validada</b>
        <span style="font-size:12px;color:#64748B;line-height:1.5">Os valores conferem com o que você informou</span>
        <div style="display:flex;gap:8px;justify-content:center;margin-top:10px;flex-wrap:wrap">${pillsHtml}</div>
      </div>`
    : `<div style="border-radius:16px;padding:16px;text-align:center;margin-bottom:14px;background:#FFFBEB;border:1.5px solid #FDE68A">
        <div style="width:44px;height:44px;border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto 10px;background:#FEF3C7;color:#D97706"><svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.4"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg></div>
        <b style="display:block;font-size:14px;color:#0F172A;margin-bottom:4px">${warnTitle}</b>
        <span style="font-size:12px;color:#64748B;line-height:1.5">${warnMsg}</span>
        <div style="display:flex;gap:8px;justify-content:center;margin-top:10px;flex-wrap:wrap">${pillsHtml}</div>
      </div>`;

  document.getElementById('ic-title').textContent = 'Conferência';
  document.getElementById('ic-body').innerHTML = cardHtml;
  const btn = document.getElementById('ic-btn');
  btn.style.display = 'block';
  btn.textContent = allOk ? 'Ver corridas encontradas' : 'Ver o que encontrei';
  btn.onclick = () => { showImportConfirm(); btn.onclick = confirmImport; };
}

function showSpinner(msg) {
  return '<div style="text-align:center;padding:28px 16px">' +
    '<div style="font-size:28px;animation:vspin 1s linear infinite;display:inline-block">&#9881;</div>' +
    '<div style="margin-top:12px;font-size:13px;color:#64748B;font-weight:500">' + msg + '</div></div>';
}

// Normaliza "7:42" e "07:42" pro mesmo formato — a IA não le a hora de forma
// 100% consistente entre frames/prints (zero à esquerda variando), e sem isso
// a mesma corrida real passava pelo dedup do import como se fossem duas.
function normTime(t) {
  const m = String(t||'').match(/(\d{1,2}):(\d{2})/);
  return m ? m[1].padStart(2,'0')+':'+m[2] : String(t||'');
}
async function processMultipleImports(files, plat) {
  const total = files.length;
  document.getElementById('ic-title').textContent = 'Analisando ' + total + ' print' + (total > 1 ? 's' : '') + '...';
  document.getElementById('ic-body').innerHTML = showSpinner('Processando 1 de ' + total + '...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  const allTrips = [];
  for (let i = 0; i < files.length; i++) {
    document.getElementById('ic-body').innerHTML = showSpinner('Processando print ' + (i+1) + ' de ' + total + '...');
    try {
      const trips = await extractTripsFromImage(files[i], plat);
      allTrips.push(...trips);
    } catch(e) { console.warn('Erro print '+(i+1)+':', e); }
  }
  const day = DB.day(S.curDate);
  const seen = new Set();
  const unique = allTrips.filter(t => {
    if (!(t.value > 0)) return false; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
    t.time = normTime(t.time);
    const key = t.time+'_'+(t.value||0).toFixed(2);
    if (seen.has(key)) ret
===== vídeo @ 793622 =====
uber',
      _exists: isTripDuplicate(t, day.trips)};
    if (matched) {
      const newVal = parseFloat(t.value)||0;
      return {...base, _matchedAutoTripId: matched.id, _isNew: false,
        _oldValue: matched.offer_value, _changed: !numEq(matched.offer_value, newVal, 0.02)};
    }
    return {...base, _matchedAutoTripId: null, _isNew: true, _changed: true};
  });
  // Só mostra o que de fato vai mudar algo — casada sem diferença de valor
  // não aparece (nada a fazer), nova sem valor legível também não aparece
  // (sem valor não dá pra confirmar que é corrida de verdade).
  window._pendingTrips = annotated.filter(t => t._isNew ? (parseFloat(t.value)||0) > 0 : t._changed);
  // Corrida NOVA nunca confia no endereço do vídeo sem validar — mesma
  // regra do Kotlin (looksLikeAddress). Sem validação, fica sem endereço
  // em vez de arriscar lixo/mensagem de chat virando corrida (pedido
  // explícito do Yuri: "nunca endereço, a não ser que garanta").
  window._pendingTrips.forEach(t => {
    if (!t._isNew) return;
    if (t.originFull && !looksLikeAddress(t.originFull)) { t.originFull = ''; t.origin = ''; }
    if (t.destFull && !looksLikeAddress(t.destFull)) { t.destFull = ''; t.dest = ''; }
  });

  // Se algo foi declarado no pre-check, mostra a conferencia antes da lista.
  // Se pulou (tudo null), vai direto pra lista de confirmacao de sempre.
  const hasPrecheck = importPrecheck.uberCount != null || importPrecheck.p99Count != null;
  if (hasPrecheck) showImportMatch(unique); else showImportConfirm();
}

function showImportMatch(unique) {
  const uberFound = unique.filter(t => t.platform === 'uber');
  const p99Found  = unique.filter(t => t.platform === '99');
  const uberFoundCount = uberFound.length, uberFoundVal = uberFound.reduce((s,t)=>s+(t.value||0),0);
  const p99FoundCount  = p99Found.length,  p99FoundVal  = p99Found.reduce((s,t)=>s+(t.value||0),0);

  const checks = [];
  let anyMissing = false, anyExtra = false;
  // Checagem EXATA: encontrar MAIS do que o declarado é tão errado quanto
  // encontrar menos (significa corrida lida em dobro/fundida). 18/17 não
  // pode aparecer como 'bateu certinho'. Valor com tolerância de 1 centavo.
  const evalPlat = (label, foundCount, foundVal, decCount, decVal) => {
    let ok = foundCount === decCount;
    if (ok && decVal != null) ok = Math.abs(foundVal - decVal) < 0.01;
    if (!ok) { if (foundCount > decCount || (decVal != null && foundVal > decVal + 0.009)) anyExtra = true; else anyMissing = true; }
    checks.push({label, ok, txt: `${foundCount}/${decCount} · ${DB.fRI(foundVal)}${decVal != null ? ' de ' + DB.fRI(decVal) : ''}`});
  };
  if (importPrecheck.uberCount != null) evalPlat('Uber', uberFoundCount, uberFoundVal, importPrecheck.uberCount, importPrecheck.uberValue);
  if (importPrecheck.p99Count != null) evalPlat('99', p99FoundCount, p99FoundVal, importPrecheck.p99Count, importPrecheck.p99Value);
  const allOk = !anyMissing && !anyExtra;
  const pillsHtml = checks.map(c => `<span style="font-size:11px;font-weight:700;padding:4px 10px;border-radius:99px;background:${c.ok?'#DCFCE7':'#FEE2E2'};color:${c.ok?'#16A34A':'#DC2626'}">${c.label} ${c.txt} ${c.ok?'✓':'✗'}</span>`).join('');

  const warnTitle = anyExtra ? 'Achei corridas a mais' : 'Faltou alguma coisa';
  const warnMsg = anyExtra
    ? 'Alguma corrida foi lida em dobro ou misturada. Na lista, desmarque as que estiverem repetidas ou com dados estranhos antes de importar.'
    : 'Confira se subiu todos os prints/vídeos desse app';
  const cardHtml = allOk
    ? `<div style="border-radius:16px;padding:16px;text-align:center;margin-bottom:14px;background:#ECFDF3;border:1.5px solid #BBF7D0">
        <div style="width:44px;height:44px;border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto 10px;background:#DCFCE7;color:#16A34A"><svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg></div>
        <b style="display:block;font-size:14px;color:#0F172A;margin-bottom:4px">Importação validada</b>
        <span style="font-size:12px;color:#64748B;line-height:1.5">Os valores conferem com o que você informou</span>
        <div style="display:flex;gap:8px;justify-content:center;margin-top:10px;flex-wrap:wrap">${pillsHtml}</div>
      </div>`
    : `<div style="border-radius:16px;padding:16px;text-align:center;margin-bottom:14px;background:#FFFBEB;border:1.5px solid #FDE68A">
        <div style="width:44px;height:44px;border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto 10px;background:#FEF3C7;color:#D97706"><svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.4"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg></div>
        <b style="display:block;font-size:14px;color:#0F172A;margin-bottom:4px">${warnTitle}</b>
        <span style="font-size:12px;color:#64748B;line-height:1.5">${warnMsg}</span>
        <div style="display:flex;gap:8px;justify-content:center;margin-top:10px;flex-wrap:wrap">${pillsHtml}</div>
      </div>`;

  document.getElementById('ic-title').textContent = 'Conferência';
  document.getElementById('ic-body').innerHTML = cardHtml;
  const btn = document.getElementById('ic-btn');
  btn.style.display = 'block';
  btn.textContent = allOk ? 'Ver corridas encontradas' : 'Ver o que encontrei';
  btn.onclick = () => { showImportConfirm(); btn.onclick = confirmImport; };
}

function showSpinner(msg) {
  return '<div style="text-align:center;padding:28px 16px">' +
    '<div style="font-size:28px;animation:vspin 1s linear infinite;display:inline-block">&#9881;</div>' +
    '<div style="margin-top:12px;font-size:13px;color:#64748B;font-weight:500">' + msg + '</div></div>';
}

// Normaliza "7:42" e "07:42" pro mesmo formato — a IA não le a hora de forma
// 100% consistente entre frames/prints (zero à esquerda variando), e sem isso
// a mesma corrida real passava pelo dedup do import como se fossem duas.
function normTime(t) {
  const m = String(t||'').match(/(\d{1,2}):(\d{2})/);
  return m ? m[1].padStart(2,'0')+':'+m[2] : String(t||'');
}
async function processMultipleImports(files, plat) {
  const total = files.length;
  document.getElementById('ic-title').textContent = 'Analisando ' + total + ' print' + (total > 1 ? 's' : '') + '...';
  document.getElementById('ic-body').innerHTML = showSpinner('Processando 1 de ' + total + '...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  const allTrips = [];
  for (let i = 0; i < files.length; i++) {
    document.getElementById('ic-body').innerHTML = showSpinner('Processando print ' + (i+1) + ' de ' + total + '...');
    try {
      const trips = await extractTripsFromImage(files[i], plat);
      allTrips.push(...trips);
    } catch(e) { console.warn('Erro print '+(i+1)+':', e); }
  }
  const day = DB.day(S.curDate);
  const seen = new Set();
  const unique = allTrips.filter(t => {
    if (!(t.value > 0)) return false; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
    t.time = normTime(t.time);
    const key = t.time+'_'+(t.value||0).toFixed(2);
    if (seen.has(key)) return false;
    seen.add(key); return true;
  });
  window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
    platform: t.platform === '99' ? '99' : 'uber',
    _exists: isTripDuplicate(t, day.trips) // pre-mark duplicates
  }));
  showImportConfirm();
}

async function processVideoImport(file, plat) {
  document.getElementById('ic-title').textContent = 'Extraindo frames do vídeo...';
  document.getElementById('ic-body').innerHTML = showSpinner('Analisando vídeo...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  try {
    const frames = await extractVideoFrames(file);
    const allTrips = [];
    for (let i = 0; i < frames.length; i++) {
      document.getElementById('ic-body').innerHTML = showSpinner('Analisando frame ' + (i+1) + ' de ' + frames.length + '...');
      try {
        const trips = await extractTripsFromBase64(frames[i], 'image/jpeg', plat);
        allTrips.push(...trips);
      } catch(e) { console.warn('Frame erro:', e); }
    }
    const dayV = DB.day(S.curDate);
    const seenV = new Set();
    const unique = allTrips.filter(t => {
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seenV.has(key)) return false;
      seenV.add(key); return true;
    });
    const _dayV = DB.day(S.curDate);
    window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _dup: isTripDuplicate(t, _dayV.trips)}));
    showImportConfirm();
  } catch(e) {
    closeMo('mo-import-confirm');
    showToast('Erro ao processar vídeo: ' + e.message + '. Use MP4 ou prints do histórico.', 'error');
    console.error('Video error:', e);
  }
}


// APK 1.3.9+: o WebView de alguns aparelhos não consegue decodificar certos
// screen recordings mesmo quando o arquivo é válido. Quando o bridge nativo
// estiver disponível, o Android decodifica o vídeo e devolve só os JPEGs.
const _nativeVideoExtractRequests = new Map();
window.__mobNativeVideoFrame = function(token, dataUrl) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r || !dataUrl) return;
  const raw = String(dataUrl).includes(',') ? String(dataUrl).split(',').pop() : String(dataUrl);
  if (raw) r.frames.push(raw);
};
window.__mobNativeVideoProgress = function(token, done, total) {
  const r = _nativeVideoExtractRequests.get(token);
  if (r && typeof r.onProgress === 'function') r.onProgress(Number(done)||0, Number(total)||0);
};
window.__mobNativeVideoDone = function(token, emitted) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  if (!r.frames.length) r.reject(new Error('O Android não conseguiu 
===== vídeo @ 797749 =====
-size:12px;color:#64748B;line-height:1.5">Os valores conferem com o que você informou</span>
        <div style="display:flex;gap:8px;justify-content:center;margin-top:10px;flex-wrap:wrap">${pillsHtml}</div>
      </div>`
    : `<div style="border-radius:16px;padding:16px;text-align:center;margin-bottom:14px;background:#FFFBEB;border:1.5px solid #FDE68A">
        <div style="width:44px;height:44px;border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto 10px;background:#FEF3C7;color:#D97706"><svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.4"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg></div>
        <b style="display:block;font-size:14px;color:#0F172A;margin-bottom:4px">${warnTitle}</b>
        <span style="font-size:12px;color:#64748B;line-height:1.5">${warnMsg}</span>
        <div style="display:flex;gap:8px;justify-content:center;margin-top:10px;flex-wrap:wrap">${pillsHtml}</div>
      </div>`;

  document.getElementById('ic-title').textContent = 'Conferência';
  document.getElementById('ic-body').innerHTML = cardHtml;
  const btn = document.getElementById('ic-btn');
  btn.style.display = 'block';
  btn.textContent = allOk ? 'Ver corridas encontradas' : 'Ver o que encontrei';
  btn.onclick = () => { showImportConfirm(); btn.onclick = confirmImport; };
}

function showSpinner(msg) {
  return '<div style="text-align:center;padding:28px 16px">' +
    '<div style="font-size:28px;animation:vspin 1s linear infinite;display:inline-block">&#9881;</div>' +
    '<div style="margin-top:12px;font-size:13px;color:#64748B;font-weight:500">' + msg + '</div></div>';
}

// Normaliza "7:42" e "07:42" pro mesmo formato — a IA não le a hora de forma
// 100% consistente entre frames/prints (zero à esquerda variando), e sem isso
// a mesma corrida real passava pelo dedup do import como se fossem duas.
function normTime(t) {
  const m = String(t||'').match(/(\d{1,2}):(\d{2})/);
  return m ? m[1].padStart(2,'0')+':'+m[2] : String(t||'');
}
async function processMultipleImports(files, plat) {
  const total = files.length;
  document.getElementById('ic-title').textContent = 'Analisando ' + total + ' print' + (total > 1 ? 's' : '') + '...';
  document.getElementById('ic-body').innerHTML = showSpinner('Processando 1 de ' + total + '...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  const allTrips = [];
  for (let i = 0; i < files.length; i++) {
    document.getElementById('ic-body').innerHTML = showSpinner('Processando print ' + (i+1) + ' de ' + total + '...');
    try {
      const trips = await extractTripsFromImage(files[i], plat);
      allTrips.push(...trips);
    } catch(e) { console.warn('Erro print '+(i+1)+':', e); }
  }
  const day = DB.day(S.curDate);
  const seen = new Set();
  const unique = allTrips.filter(t => {
    if (!(t.value > 0)) return false; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
    t.time = normTime(t.time);
    const key = t.time+'_'+(t.value||0).toFixed(2);
    if (seen.has(key)) return false;
    seen.add(key); return true;
  });
  window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
    platform: t.platform === '99' ? '99' : 'uber',
    _exists: isTripDuplicate(t, day.trips) // pre-mark duplicates
  }));
  showImportConfirm();
}

async function processVideoImport(file, plat) {
  document.getElementById('ic-title').textContent = 'Extraindo frames do vídeo...';
  document.getElementById('ic-body').innerHTML = showSpinner('Analisando vídeo...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  try {
    const frames = await extractVideoFrames(file);
    const allTrips = [];
    for (let i = 0; i < frames.length; i++) {
      document.getElementById('ic-body').innerHTML = showSpinner('Analisando frame ' + (i+1) + ' de ' + frames.length + '...');
      try {
        const trips = await extractTripsFromBase64(frames[i], 'image/jpeg', plat);
        allTrips.push(...trips);
      } catch(e) { console.warn('Frame erro:', e); }
    }
    const dayV = DB.day(S.curDate);
    const seenV = new Set();
    const unique = allTrips.filter(t => {
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seenV.has(key)) return false;
      seenV.add(key); return true;
    });
    const _dayV = DB.day(S.curDate);
    window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _dup: isTripDuplicate(t, _dayV.trips)}));
    showImportConfirm();
  } catch(e) {
    closeMo('mo-import-confirm');
    showToast('Erro ao processar vídeo: ' + e.message + '. Use MP4 ou prints do histórico.', 'error');
    console.error('Video error:', e);
  }
}


// APK 1.3.9+: o WebView de alguns aparelhos não consegue decodificar certos
// screen recordings mesmo quando o arquivo é válido. Quando o bridge nativo
// estiver disponível, o Android decodifica o vídeo e devolve só os JPEGs.
const _nativeVideoExtractRequests = new Map();
window.__mobNativeVideoFrame = function(token, dataUrl) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r || !dataUrl) return;
  const raw = String(dataUrl).includes(',') ? String(dataUrl).split(',').pop() : String(dataUrl);
  if (raw) r.frames.push(raw);
};
window.__mobNativeVideoProgress = function(token, done, total) {
  const r = _nativeVideoExtractRequests.get(token);
  if (r && typeof r.onProgress === 'function') r.onProgress(Number(done)||0, Number(total)||0);
};
window.__mobNativeVideoDone = function(token, emitted) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  if (!r.frames.length) r.reject(new Error('O Android não conseguiu extrair quadros deste vídeo.'));
  else r.resolve(r.frames);
};
window.__mobNativeVideoError = function(token, message) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  r.reject(new Error(message || 'Falha no decodificador nativo do vídeo.'));
};

function extractVideoFramesNative(file, onProgress) {
  return new Promise((resolve, reject) => {
    const token = 'nv_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2,8);
    const timer = setTimeout(() => {
      _nativeVideoExtractRequests.delete(token);
      reject(new Error('Tempo esgotado ao decodificar o vídeo no Android.'));
    }, 120000);
    _nativeVideoExtractRequests.set(token, {frames:[], onProgress, resolve, reject, timer});
    try {
      const started = SmartMobiNative.extractSelectedVideoFrames(token, 72, 720);
      if (started === false) {
        clearTimeout(timer);
        _nativeVideoExtractRequests.delete(token);
        reject(Object.assign(new Error('O Android não recebeu o arquivo de vídeo selecionado.'), {nativeNotStarted:true}));
      }
    } catch(e) {
      clearTimeout(timer);
      _nativeVideoExtractRequests.delete(token);
      reject(Object.assign(e instanceof Error ? e : new Error(String(e)), {nativeNotStarted:true}));
    }
  });
}

async function extractVideoFrames(file, onProgress) {
  let canNative = false;
  try {
    canNative = !!(window.SmartMobiNative &&
      typeof SmartMobiNative.hasNativeVideoFrameExtraction === 'function' &&
      SmartMobiNative.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');
  } catch(e) {}
  if (canNative) {
    try {
      return await extractVideoFramesNative(file, onProgress);
    } catch(e) {
      // Só cai para o decoder web se a ponte não chegou a iniciar. Se o
      // Android iniciou e reportou erro real de codec, repetir no WebView
      // apenas recriaria o travamento que motivou este caminho nativo.
      if (!e || !e.nativeNotStarted) throw e;
      console.warn('[video] bridge nativo indisponível, usando navegador:', e);
    }
  }
  return extractVideoFramesBrowser(file, onProgress);
}

async function extractVideoFramesBrowser(file, onProgress) {
  return new Promise((resolve, reject) => {
    const video  = document.createElement('video');
    const url    = URL.createObjectURL(file);
    const canvas = document.createElement('canvas');
    const ctx    = canvas.getContext('2d');
    video.style.cssText = 'position:fixed;top:-9999px;left:-9999px;width:1px;height:1px;opacity:0';
    document.body.appendChild(video);
    video.src        = url;
    video.muted      = true;
    video.playsInline = true;
    video.setAttribute('playsinline', '');
    video.setAttribute('webkit-playsinline', '');
    video.preload    = 'auto';

    const metadataTimer = setTimeout(() => {
      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      reject(new Error('O vídeo demorou demais para ser preparado. Tente novamente.'));
    }, 15000);

    video.onloadedmetadata = async () => {
      clearTimeout(metadataTimer);
      try {
      // loadedmetadata só garante dimensões/duração. Em Android/WebView o
      // decoder pode ainda não ter nenhum frame disponível. Aguarda dados
      // reais antes de iniciar os seeks, evitando canvas preto em MP4 válido.
      if (video.readyState < 2) {
        await new Promise((resolve, reject) => {
          let settled = false;
          const finish = () => { if (settled) return; settled = true; cleanup(); resolve(); };
          const fail = () => { if (settled) return; settled = true; cleanup(); reject(new Error('Não foi possível decodificar o vídeo.')); };
          const timer = setTimeout(fail, 12000);
          const cleanup = () => { clearTimeout(timer); video.removeEventListener('loadeddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
  
===== vídeo @ 798977 =====
xtContent = allOk ? 'Ver corridas encontradas' : 'Ver o que encontrei';
  btn.onclick = () => { showImportConfirm(); btn.onclick = confirmImport; };
}

function showSpinner(msg) {
  return '<div style="text-align:center;padding:28px 16px">' +
    '<div style="font-size:28px;animation:vspin 1s linear infinite;display:inline-block">&#9881;</div>' +
    '<div style="margin-top:12px;font-size:13px;color:#64748B;font-weight:500">' + msg + '</div></div>';
}

// Normaliza "7:42" e "07:42" pro mesmo formato — a IA não le a hora de forma
// 100% consistente entre frames/prints (zero à esquerda variando), e sem isso
// a mesma corrida real passava pelo dedup do import como se fossem duas.
function normTime(t) {
  const m = String(t||'').match(/(\d{1,2}):(\d{2})/);
  return m ? m[1].padStart(2,'0')+':'+m[2] : String(t||'');
}
async function processMultipleImports(files, plat) {
  const total = files.length;
  document.getElementById('ic-title').textContent = 'Analisando ' + total + ' print' + (total > 1 ? 's' : '') + '...';
  document.getElementById('ic-body').innerHTML = showSpinner('Processando 1 de ' + total + '...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  const allTrips = [];
  for (let i = 0; i < files.length; i++) {
    document.getElementById('ic-body').innerHTML = showSpinner('Processando print ' + (i+1) + ' de ' + total + '...');
    try {
      const trips = await extractTripsFromImage(files[i], plat);
      allTrips.push(...trips);
    } catch(e) { console.warn('Erro print '+(i+1)+':', e); }
  }
  const day = DB.day(S.curDate);
  const seen = new Set();
  const unique = allTrips.filter(t => {
    if (!(t.value > 0)) return false; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
    t.time = normTime(t.time);
    const key = t.time+'_'+(t.value||0).toFixed(2);
    if (seen.has(key)) return false;
    seen.add(key); return true;
  });
  window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
    platform: t.platform === '99' ? '99' : 'uber',
    _exists: isTripDuplicate(t, day.trips) // pre-mark duplicates
  }));
  showImportConfirm();
}

async function processVideoImport(file, plat) {
  document.getElementById('ic-title').textContent = 'Extraindo frames do vídeo...';
  document.getElementById('ic-body').innerHTML = showSpinner('Analisando vídeo...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  try {
    const frames = await extractVideoFrames(file);
    const allTrips = [];
    for (let i = 0; i < frames.length; i++) {
      document.getElementById('ic-body').innerHTML = showSpinner('Analisando frame ' + (i+1) + ' de ' + frames.length + '...');
      try {
        const trips = await extractTripsFromBase64(frames[i], 'image/jpeg', plat);
        allTrips.push(...trips);
      } catch(e) { console.warn('Frame erro:', e); }
    }
    const dayV = DB.day(S.curDate);
    const seenV = new Set();
    const unique = allTrips.filter(t => {
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seenV.has(key)) return false;
      seenV.add(key); return true;
    });
    const _dayV = DB.day(S.curDate);
    window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _dup: isTripDuplicate(t, _dayV.trips)}));
    showImportConfirm();
  } catch(e) {
    closeMo('mo-import-confirm');
    showToast('Erro ao processar vídeo: ' + e.message + '. Use MP4 ou prints do histórico.', 'error');
    console.error('Video error:', e);
  }
}


// APK 1.3.9+: o WebView de alguns aparelhos não consegue decodificar certos
// screen recordings mesmo quando o arquivo é válido. Quando o bridge nativo
// estiver disponível, o Android decodifica o vídeo e devolve só os JPEGs.
const _nativeVideoExtractRequests = new Map();
window.__mobNativeVideoFrame = function(token, dataUrl) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r || !dataUrl) return;
  const raw = String(dataUrl).includes(',') ? String(dataUrl).split(',').pop() : String(dataUrl);
  if (raw) r.frames.push(raw);
};
window.__mobNativeVideoProgress = function(token, done, total) {
  const r = _nativeVideoExtractRequests.get(token);
  if (r && typeof r.onProgress === 'function') r.onProgress(Number(done)||0, Number(total)||0);
};
window.__mobNativeVideoDone = function(token, emitted) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  if (!r.frames.length) r.reject(new Error('O Android não conseguiu extrair quadros deste vídeo.'));
  else r.resolve(r.frames);
};
window.__mobNativeVideoError = function(token, message) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  r.reject(new Error(message || 'Falha no decodificador nativo do vídeo.'));
};

function extractVideoFramesNative(file, onProgress) {
  return new Promise((resolve, reject) => {
    const token = 'nv_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2,8);
    const timer = setTimeout(() => {
      _nativeVideoExtractRequests.delete(token);
      reject(new Error('Tempo esgotado ao decodificar o vídeo no Android.'));
    }, 120000);
    _nativeVideoExtractRequests.set(token, {frames:[], onProgress, resolve, reject, timer});
    try {
      const started = SmartMobiNative.extractSelectedVideoFrames(token, 72, 720);
      if (started === false) {
        clearTimeout(timer);
        _nativeVideoExtractRequests.delete(token);
        reject(Object.assign(new Error('O Android não recebeu o arquivo de vídeo selecionado.'), {nativeNotStarted:true}));
      }
    } catch(e) {
      clearTimeout(timer);
      _nativeVideoExtractRequests.delete(token);
      reject(Object.assign(e instanceof Error ? e : new Error(String(e)), {nativeNotStarted:true}));
    }
  });
}

async function extractVideoFrames(file, onProgress) {
  let canNative = false;
  try {
    canNative = !!(window.SmartMobiNative &&
      typeof SmartMobiNative.hasNativeVideoFrameExtraction === 'function' &&
      SmartMobiNative.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');
  } catch(e) {}
  if (canNative) {
    try {
      return await extractVideoFramesNative(file, onProgress);
    } catch(e) {
      // Só cai para o decoder web se a ponte não chegou a iniciar. Se o
      // Android iniciou e reportou erro real de codec, repetir no WebView
      // apenas recriaria o travamento que motivou este caminho nativo.
      if (!e || !e.nativeNotStarted) throw e;
      console.warn('[video] bridge nativo indisponível, usando navegador:', e);
    }
  }
  return extractVideoFramesBrowser(file, onProgress);
}

async function extractVideoFramesBrowser(file, onProgress) {
  return new Promise((resolve, reject) => {
    const video  = document.createElement('video');
    const url    = URL.createObjectURL(file);
    const canvas = document.createElement('canvas');
    const ctx    = canvas.getContext('2d');
    video.style.cssText = 'position:fixed;top:-9999px;left:-9999px;width:1px;height:1px;opacity:0';
    document.body.appendChild(video);
    video.src        = url;
    video.muted      = true;
    video.playsInline = true;
    video.setAttribute('playsinline', '');
    video.setAttribute('webkit-playsinline', '');
    video.preload    = 'auto';

    const metadataTimer = setTimeout(() => {
      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      reject(new Error('O vídeo demorou demais para ser preparado. Tente novamente.'));
    }, 15000);

    video.onloadedmetadata = async () => {
      clearTimeout(metadataTimer);
      try {
      // loadedmetadata só garante dimensões/duração. Em Android/WebView o
      // decoder pode ainda não ter nenhum frame disponível. Aguarda dados
      // reais antes de iniciar os seeks, evitando canvas preto em MP4 válido.
      if (video.readyState < 2) {
        await new Promise((resolve, reject) => {
          let settled = false;
          const finish = () => { if (settled) return; settled = true; cleanup(); resolve(); };
          const fail = () => { if (settled) return; settled = true; cleanup(); reject(new Error('Não foi possível decodificar o vídeo.')); };
          const timer = setTimeout(fail, 12000);
          const cleanup = () => { clearTimeout(timer); video.removeEventListener('loadeddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
        });
      }
      // Limit canvas size for performance
      const MAX = 1080;
      const ratio = Math.min(MAX / video.videoWidth, MAX / video.videoHeight, 1);
      canvas.width  = Math.round(video.videoWidth  * ratio);
      canvas.height = Math.round(video.videoHeight * ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas 
===== vídeo @ 799293 =====
y:inline-block">&#9881;</div>' +
    '<div style="margin-top:12px;font-size:13px;color:#64748B;font-weight:500">' + msg + '</div></div>';
}

// Normaliza "7:42" e "07:42" pro mesmo formato — a IA não le a hora de forma
// 100% consistente entre frames/prints (zero à esquerda variando), e sem isso
// a mesma corrida real passava pelo dedup do import como se fossem duas.
function normTime(t) {
  const m = String(t||'').match(/(\d{1,2}):(\d{2})/);
  return m ? m[1].padStart(2,'0')+':'+m[2] : String(t||'');
}
async function processMultipleImports(files, plat) {
  const total = files.length;
  document.getElementById('ic-title').textContent = 'Analisando ' + total + ' print' + (total > 1 ? 's' : '') + '...';
  document.getElementById('ic-body').innerHTML = showSpinner('Processando 1 de ' + total + '...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  const allTrips = [];
  for (let i = 0; i < files.length; i++) {
    document.getElementById('ic-body').innerHTML = showSpinner('Processando print ' + (i+1) + ' de ' + total + '...');
    try {
      const trips = await extractTripsFromImage(files[i], plat);
      allTrips.push(...trips);
    } catch(e) { console.warn('Erro print '+(i+1)+':', e); }
  }
  const day = DB.day(S.curDate);
  const seen = new Set();
  const unique = allTrips.filter(t => {
    if (!(t.value > 0)) return false; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
    t.time = normTime(t.time);
    const key = t.time+'_'+(t.value||0).toFixed(2);
    if (seen.has(key)) return false;
    seen.add(key); return true;
  });
  window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
    platform: t.platform === '99' ? '99' : 'uber',
    _exists: isTripDuplicate(t, day.trips) // pre-mark duplicates
  }));
  showImportConfirm();
}

async function processVideoImport(file, plat) {
  document.getElementById('ic-title').textContent = 'Extraindo frames do vídeo...';
  document.getElementById('ic-body').innerHTML = showSpinner('Analisando vídeo...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  try {
    const frames = await extractVideoFrames(file);
    const allTrips = [];
    for (let i = 0; i < frames.length; i++) {
      document.getElementById('ic-body').innerHTML = showSpinner('Analisando frame ' + (i+1) + ' de ' + frames.length + '...');
      try {
        const trips = await extractTripsFromBase64(frames[i], 'image/jpeg', plat);
        allTrips.push(...trips);
      } catch(e) { console.warn('Frame erro:', e); }
    }
    const dayV = DB.day(S.curDate);
    const seenV = new Set();
    const unique = allTrips.filter(t => {
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seenV.has(key)) return false;
      seenV.add(key); return true;
    });
    const _dayV = DB.day(S.curDate);
    window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _dup: isTripDuplicate(t, _dayV.trips)}));
    showImportConfirm();
  } catch(e) {
    closeMo('mo-import-confirm');
    showToast('Erro ao processar vídeo: ' + e.message + '. Use MP4 ou prints do histórico.', 'error');
    console.error('Video error:', e);
  }
}


// APK 1.3.9+: o WebView de alguns aparelhos não consegue decodificar certos
// screen recordings mesmo quando o arquivo é válido. Quando o bridge nativo
// estiver disponível, o Android decodifica o vídeo e devolve só os JPEGs.
const _nativeVideoExtractRequests = new Map();
window.__mobNativeVideoFrame = function(token, dataUrl) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r || !dataUrl) return;
  const raw = String(dataUrl).includes(',') ? String(dataUrl).split(',').pop() : String(dataUrl);
  if (raw) r.frames.push(raw);
};
window.__mobNativeVideoProgress = function(token, done, total) {
  const r = _nativeVideoExtractRequests.get(token);
  if (r && typeof r.onProgress === 'function') r.onProgress(Number(done)||0, Number(total)||0);
};
window.__mobNativeVideoDone = function(token, emitted) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  if (!r.frames.length) r.reject(new Error('O Android não conseguiu extrair quadros deste vídeo.'));
  else r.resolve(r.frames);
};
window.__mobNativeVideoError = function(token, message) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  r.reject(new Error(message || 'Falha no decodificador nativo do vídeo.'));
};

function extractVideoFramesNative(file, onProgress) {
  return new Promise((resolve, reject) => {
    const token = 'nv_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2,8);
    const timer = setTimeout(() => {
      _nativeVideoExtractRequests.delete(token);
      reject(new Error('Tempo esgotado ao decodificar o vídeo no Android.'));
    }, 120000);
    _nativeVideoExtractRequests.set(token, {frames:[], onProgress, resolve, reject, timer});
    try {
      const started = SmartMobiNative.extractSelectedVideoFrames(token, 72, 720);
      if (started === false) {
        clearTimeout(timer);
        _nativeVideoExtractRequests.delete(token);
        reject(Object.assign(new Error('O Android não recebeu o arquivo de vídeo selecionado.'), {nativeNotStarted:true}));
      }
    } catch(e) {
      clearTimeout(timer);
      _nativeVideoExtractRequests.delete(token);
      reject(Object.assign(e instanceof Error ? e : new Error(String(e)), {nativeNotStarted:true}));
    }
  });
}

async function extractVideoFrames(file, onProgress) {
  let canNative = false;
  try {
    canNative = !!(window.SmartMobiNative &&
      typeof SmartMobiNative.hasNativeVideoFrameExtraction === 'function' &&
      SmartMobiNative.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');
  } catch(e) {}
  if (canNative) {
    try {
      return await extractVideoFramesNative(file, onProgress);
    } catch(e) {
      // Só cai para o decoder web se a ponte não chegou a iniciar. Se o
      // Android iniciou e reportou erro real de codec, repetir no WebView
      // apenas recriaria o travamento que motivou este caminho nativo.
      if (!e || !e.nativeNotStarted) throw e;
      console.warn('[video] bridge nativo indisponível, usando navegador:', e);
    }
  }
  return extractVideoFramesBrowser(file, onProgress);
}

async function extractVideoFramesBrowser(file, onProgress) {
  return new Promise((resolve, reject) => {
    const video  = document.createElement('video');
    const url    = URL.createObjectURL(file);
    const canvas = document.createElement('canvas');
    const ctx    = canvas.getContext('2d');
    video.style.cssText = 'position:fixed;top:-9999px;left:-9999px;width:1px;height:1px;opacity:0';
    document.body.appendChild(video);
    video.src        = url;
    video.muted      = true;
    video.playsInline = true;
    video.setAttribute('playsinline', '');
    video.setAttribute('webkit-playsinline', '');
    video.preload    = 'auto';

    const metadataTimer = setTimeout(() => {
      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      reject(new Error('O vídeo demorou demais para ser preparado. Tente novamente.'));
    }, 15000);

    video.onloadedmetadata = async () => {
      clearTimeout(metadataTimer);
      try {
      // loadedmetadata só garante dimensões/duração. Em Android/WebView o
      // decoder pode ainda não ter nenhum frame disponível. Aguarda dados
      // reais antes de iniciar os seeks, evitando canvas preto em MP4 válido.
      if (video.readyState < 2) {
        await new Promise((resolve, reject) => {
          let settled = false;
          const finish = () => { if (settled) return; settled = true; cleanup(); resolve(); };
          const fail = () => { if (settled) return; settled = true; cleanup(); reject(new Error('Não foi possível decodificar o vídeo.')); };
          const timer = setTimeout(fail, 12000);
          const cleanup = () => { clearTimeout(timer); video.removeEventListener('loadeddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
        });
      }
      // Limit canvas size for performance
      const MAX = 1080;
      const ratio = Math.min(MAX / video.videoWidth, MAX / video.videoHeight, 1);
      canvas.width  = Math.round(video.videoWidth  * ratio);
      canvas.height = Math.round(video.videoHeight * ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas por corrida real pra
      // resolveTimeConflicts() escolher a leitura certa por maioria
      const times = [];
      for (let t = 0.5; t < duration; t += INTERVAL) times.push(parseFloat(t.toFixed(2)));
      if (!times.length) times.push(0);

      // Fingerprint pequeno (32x32 grayscale) pra comparar frames e p
===== vídeo @ 800144 =====
le.display = 'none';
  openMo('mo-import-confirm');
  const allTrips = [];
  for (let i = 0; i < files.length; i++) {
    document.getElementById('ic-body').innerHTML = showSpinner('Processando print ' + (i+1) + ' de ' + total + '...');
    try {
      const trips = await extractTripsFromImage(files[i], plat);
      allTrips.push(...trips);
    } catch(e) { console.warn('Erro print '+(i+1)+':', e); }
  }
  const day = DB.day(S.curDate);
  const seen = new Set();
  const unique = allTrips.filter(t => {
    if (!(t.value > 0)) return false; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
    t.time = normTime(t.time);
    const key = t.time+'_'+(t.value||0).toFixed(2);
    if (seen.has(key)) return false;
    seen.add(key); return true;
  });
  window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
    platform: t.platform === '99' ? '99' : 'uber',
    _exists: isTripDuplicate(t, day.trips) // pre-mark duplicates
  }));
  showImportConfirm();
}

async function processVideoImport(file, plat) {
  document.getElementById('ic-title').textContent = 'Extraindo frames do vídeo...';
  document.getElementById('ic-body').innerHTML = showSpinner('Analisando vídeo...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  try {
    const frames = await extractVideoFrames(file);
    const allTrips = [];
    for (let i = 0; i < frames.length; i++) {
      document.getElementById('ic-body').innerHTML = showSpinner('Analisando frame ' + (i+1) + ' de ' + frames.length + '...');
      try {
        const trips = await extractTripsFromBase64(frames[i], 'image/jpeg', plat);
        allTrips.push(...trips);
      } catch(e) { console.warn('Frame erro:', e); }
    }
    const dayV = DB.day(S.curDate);
    const seenV = new Set();
    const unique = allTrips.filter(t => {
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seenV.has(key)) return false;
      seenV.add(key); return true;
    });
    const _dayV = DB.day(S.curDate);
    window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _dup: isTripDuplicate(t, _dayV.trips)}));
    showImportConfirm();
  } catch(e) {
    closeMo('mo-import-confirm');
    showToast('Erro ao processar vídeo: ' + e.message + '. Use MP4 ou prints do histórico.', 'error');
    console.error('Video error:', e);
  }
}


// APK 1.3.9+: o WebView de alguns aparelhos não consegue decodificar certos
// screen recordings mesmo quando o arquivo é válido. Quando o bridge nativo
// estiver disponível, o Android decodifica o vídeo e devolve só os JPEGs.
const _nativeVideoExtractRequests = new Map();
window.__mobNativeVideoFrame = function(token, dataUrl) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r || !dataUrl) return;
  const raw = String(dataUrl).includes(',') ? String(dataUrl).split(',').pop() : String(dataUrl);
  if (raw) r.frames.push(raw);
};
window.__mobNativeVideoProgress = function(token, done, total) {
  const r = _nativeVideoExtractRequests.get(token);
  if (r && typeof r.onProgress === 'function') r.onProgress(Number(done)||0, Number(total)||0);
};
window.__mobNativeVideoDone = function(token, emitted) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  if (!r.frames.length) r.reject(new Error('O Android não conseguiu extrair quadros deste vídeo.'));
  else r.resolve(r.frames);
};
window.__mobNativeVideoError = function(token, message) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  r.reject(new Error(message || 'Falha no decodificador nativo do vídeo.'));
};

function extractVideoFramesNative(file, onProgress) {
  return new Promise((resolve, reject) => {
    const token = 'nv_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2,8);
    const timer = setTimeout(() => {
      _nativeVideoExtractRequests.delete(token);
      reject(new Error('Tempo esgotado ao decodificar o vídeo no Android.'));
    }, 120000);
    _nativeVideoExtractRequests.set(token, {frames:[], onProgress, resolve, reject, timer});
    try {
      const started = SmartMobiNative.extractSelectedVideoFrames(token, 72, 720);
      if (started === false) {
        clearTimeout(timer);
        _nativeVideoExtractRequests.delete(token);
        reject(Object.assign(new Error('O Android não recebeu o arquivo de vídeo selecionado.'), {nativeNotStarted:true}));
      }
    } catch(e) {
      clearTimeout(timer);
      _nativeVideoExtractRequests.delete(token);
      reject(Object.assign(e instanceof Error ? e : new Error(String(e)), {nativeNotStarted:true}));
    }
  });
}

async function extractVideoFrames(file, onProgress) {
  let canNative = false;
  try {
    canNative = !!(window.SmartMobiNative &&
      typeof SmartMobiNative.hasNativeVideoFrameExtraction === 'function' &&
      SmartMobiNative.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');
  } catch(e) {}
  if (canNative) {
    try {
      return await extractVideoFramesNative(file, onProgress);
    } catch(e) {
      // Só cai para o decoder web se a ponte não chegou a iniciar. Se o
      // Android iniciou e reportou erro real de codec, repetir no WebView
      // apenas recriaria o travamento que motivou este caminho nativo.
      if (!e || !e.nativeNotStarted) throw e;
      console.warn('[video] bridge nativo indisponível, usando navegador:', e);
    }
  }
  return extractVideoFramesBrowser(file, onProgress);
}

async function extractVideoFramesBrowser(file, onProgress) {
  return new Promise((resolve, reject) => {
    const video  = document.createElement('video');
    const url    = URL.createObjectURL(file);
    const canvas = document.createElement('canvas');
    const ctx    = canvas.getContext('2d');
    video.style.cssText = 'position:fixed;top:-9999px;left:-9999px;width:1px;height:1px;opacity:0';
    document.body.appendChild(video);
    video.src        = url;
    video.muted      = true;
    video.playsInline = true;
    video.setAttribute('playsinline', '');
    video.setAttribute('webkit-playsinline', '');
    video.preload    = 'auto';

    const metadataTimer = setTimeout(() => {
      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      reject(new Error('O vídeo demorou demais para ser preparado. Tente novamente.'));
    }, 15000);

    video.onloadedmetadata = async () => {
      clearTimeout(metadataTimer);
      try {
      // loadedmetadata só garante dimensões/duração. Em Android/WebView o
      // decoder pode ainda não ter nenhum frame disponível. Aguarda dados
      // reais antes de iniciar os seeks, evitando canvas preto em MP4 válido.
      if (video.readyState < 2) {
        await new Promise((resolve, reject) => {
          let settled = false;
          const finish = () => { if (settled) return; settled = true; cleanup(); resolve(); };
          const fail = () => { if (settled) return; settled = true; cleanup(); reject(new Error('Não foi possível decodificar o vídeo.')); };
          const timer = setTimeout(fail, 12000);
          const cleanup = () => { clearTimeout(timer); video.removeEventListener('loadeddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
        });
      }
      // Limit canvas size for performance
      const MAX = 1080;
      const ratio = Math.min(MAX / video.videoWidth, MAX / video.videoHeight, 1);
      canvas.width  = Math.round(video.videoWidth  * ratio);
      canvas.height = Math.round(video.videoHeight * ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas por corrida real pra
      // resolveTimeConflicts() escolher a leitura certa por maioria
      const times = [];
      for (let t = 0.5; t < duration; t += INTERVAL) times.push(parseFloat(t.toFixed(2)));
      if (!times.length) times.push(0);

      // Fingerprint pequeno (32x32 grayscale) pra comparar frames e pular
      // quando o scroll tá parado/lento — evita mandar quase-o-mesmo-frame
      // pra IA, que é a causa de valores "fantasma" (leitura funde 2 frames
      // parecidos com corrida cortada/sobreposta).
      const FP = 32;
      const fpCanvas = document.createElement('canvas');
      fpCanvas.width = FP; fpCanvas.height = FP;
      const fpCtx = fpCanvas.getContext('2d');
      function frameFingerprint() {
        fpCtx.drawImage(canvas, 0, 0, FP, FP);
        const d = fpCtx.getImageData(0, 0, FP, FP).data;
        const g = new Float32Array(FP*FP);
        for (let i = 0; i < g.length; i++) {
          const o = i*4;
          g[i] = (d[o] + d[o+1] + d[o+2]) / 3;
        }
        return g;
      }
      function fpDiff(a, b) {
        let sum = 0;
        for (let i = 0; i < a.length; i++) sum += Math.abs(a[i]-b[i]);
        r
===== vídeo @ 800802 =====
'_'+(t.value||0).toFixed(2);
    if (seen.has(key)) return false;
    seen.add(key); return true;
  });
  window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
    platform: t.platform === '99' ? '99' : 'uber',
    _exists: isTripDuplicate(t, day.trips) // pre-mark duplicates
  }));
  showImportConfirm();
}

async function processVideoImport(file, plat) {
  document.getElementById('ic-title').textContent = 'Extraindo frames do vídeo...';
  document.getElementById('ic-body').innerHTML = showSpinner('Analisando vídeo...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  try {
    const frames = await extractVideoFrames(file);
    const allTrips = [];
    for (let i = 0; i < frames.length; i++) {
      document.getElementById('ic-body').innerHTML = showSpinner('Analisando frame ' + (i+1) + ' de ' + frames.length + '...');
      try {
        const trips = await extractTripsFromBase64(frames[i], 'image/jpeg', plat);
        allTrips.push(...trips);
      } catch(e) { console.warn('Frame erro:', e); }
    }
    const dayV = DB.day(S.curDate);
    const seenV = new Set();
    const unique = allTrips.filter(t => {
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seenV.has(key)) return false;
      seenV.add(key); return true;
    });
    const _dayV = DB.day(S.curDate);
    window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _dup: isTripDuplicate(t, _dayV.trips)}));
    showImportConfirm();
  } catch(e) {
    closeMo('mo-import-confirm');
    showToast('Erro ao processar vídeo: ' + e.message + '. Use MP4 ou prints do histórico.', 'error');
    console.error('Video error:', e);
  }
}


// APK 1.3.9+: o WebView de alguns aparelhos não consegue decodificar certos
// screen recordings mesmo quando o arquivo é válido. Quando o bridge nativo
// estiver disponível, o Android decodifica o vídeo e devolve só os JPEGs.
const _nativeVideoExtractRequests = new Map();
window.__mobNativeVideoFrame = function(token, dataUrl) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r || !dataUrl) return;
  const raw = String(dataUrl).includes(',') ? String(dataUrl).split(',').pop() : String(dataUrl);
  if (raw) r.frames.push(raw);
};
window.__mobNativeVideoProgress = function(token, done, total) {
  const r = _nativeVideoExtractRequests.get(token);
  if (r && typeof r.onProgress === 'function') r.onProgress(Number(done)||0, Number(total)||0);
};
window.__mobNativeVideoDone = function(token, emitted) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  if (!r.frames.length) r.reject(new Error('O Android não conseguiu extrair quadros deste vídeo.'));
  else r.resolve(r.frames);
};
window.__mobNativeVideoError = function(token, message) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  r.reject(new Error(message || 'Falha no decodificador nativo do vídeo.'));
};

function extractVideoFramesNative(file, onProgress) {
  return new Promise((resolve, reject) => {
    const token = 'nv_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2,8);
    const timer = setTimeout(() => {
      _nativeVideoExtractRequests.delete(token);
      reject(new Error('Tempo esgotado ao decodificar o vídeo no Android.'));
    }, 120000);
    _nativeVideoExtractRequests.set(token, {frames:[], onProgress, resolve, reject, timer});
    try {
      const started = SmartMobiNative.extractSelectedVideoFrames(token, 72, 720);
      if (started === false) {
        clearTimeout(timer);
        _nativeVideoExtractRequests.delete(token);
        reject(Object.assign(new Error('O Android não recebeu o arquivo de vídeo selecionado.'), {nativeNotStarted:true}));
      }
    } catch(e) {
      clearTimeout(timer);
      _nativeVideoExtractRequests.delete(token);
      reject(Object.assign(e instanceof Error ? e : new Error(String(e)), {nativeNotStarted:true}));
    }
  });
}

async function extractVideoFrames(file, onProgress) {
  let canNative = false;
  try {
    canNative = !!(window.SmartMobiNative &&
      typeof SmartMobiNative.hasNativeVideoFrameExtraction === 'function' &&
      SmartMobiNative.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');
  } catch(e) {}
  if (canNative) {
    try {
      return await extractVideoFramesNative(file, onProgress);
    } catch(e) {
      // Só cai para o decoder web se a ponte não chegou a iniciar. Se o
      // Android iniciou e reportou erro real de codec, repetir no WebView
      // apenas recriaria o travamento que motivou este caminho nativo.
      if (!e || !e.nativeNotStarted) throw e;
      console.warn('[video] bridge nativo indisponível, usando navegador:', e);
    }
  }
  return extractVideoFramesBrowser(file, onProgress);
}

async function extractVideoFramesBrowser(file, onProgress) {
  return new Promise((resolve, reject) => {
    const video  = document.createElement('video');
    const url    = URL.createObjectURL(file);
    const canvas = document.createElement('canvas');
    const ctx    = canvas.getContext('2d');
    video.style.cssText = 'position:fixed;top:-9999px;left:-9999px;width:1px;height:1px;opacity:0';
    document.body.appendChild(video);
    video.src        = url;
    video.muted      = true;
    video.playsInline = true;
    video.setAttribute('playsinline', '');
    video.setAttribute('webkit-playsinline', '');
    video.preload    = 'auto';

    const metadataTimer = setTimeout(() => {
      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      reject(new Error('O vídeo demorou demais para ser preparado. Tente novamente.'));
    }, 15000);

    video.onloadedmetadata = async () => {
      clearTimeout(metadataTimer);
      try {
      // loadedmetadata só garante dimensões/duração. Em Android/WebView o
      // decoder pode ainda não ter nenhum frame disponível. Aguarda dados
      // reais antes de iniciar os seeks, evitando canvas preto em MP4 válido.
      if (video.readyState < 2) {
        await new Promise((resolve, reject) => {
          let settled = false;
          const finish = () => { if (settled) return; settled = true; cleanup(); resolve(); };
          const fail = () => { if (settled) return; settled = true; cleanup(); reject(new Error('Não foi possível decodificar o vídeo.')); };
          const timer = setTimeout(fail, 12000);
          const cleanup = () => { clearTimeout(timer); video.removeEventListener('loadeddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
        });
      }
      // Limit canvas size for performance
      const MAX = 1080;
      const ratio = Math.min(MAX / video.videoWidth, MAX / video.videoHeight, 1);
      canvas.width  = Math.round(video.videoWidth  * ratio);
      canvas.height = Math.round(video.videoHeight * ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas por corrida real pra
      // resolveTimeConflicts() escolher a leitura certa por maioria
      const times = [];
      for (let t = 0.5; t < duration; t += INTERVAL) times.push(parseFloat(t.toFixed(2)));
      if (!times.length) times.push(0);

      // Fingerprint pequeno (32x32 grayscale) pra comparar frames e pular
      // quando o scroll tá parado/lento — evita mandar quase-o-mesmo-frame
      // pra IA, que é a causa de valores "fantasma" (leitura funde 2 frames
      // parecidos com corrida cortada/sobreposta).
      const FP = 32;
      const fpCanvas = document.createElement('canvas');
      fpCanvas.width = FP; fpCanvas.height = FP;
      const fpCtx = fpCanvas.getContext('2d');
      function frameFingerprint() {
        fpCtx.drawImage(canvas, 0, 0, FP, FP);
        const d = fpCtx.getImageData(0, 0, FP, FP).data;
        const g = new Float32Array(FP*FP);
        for (let i = 0; i < g.length; i++) {
          const o = i*4;
          g[i] = (d[o] + d[o+1] + d[o+2]) / 3;
        }
        return g;
      }
      function fpDiff(a, b) {
        let sum = 0;
        for (let i = 0; i < a.length; i++) sum += Math.abs(a[i]-b[i]);
        return sum / (a.length * 255); // normalizado 0..1
      }
      const SIMILARITY_THRESHOLD = 0.02; // <2% de mudança = frame quase idêntico, pula

      const frames = [];
      let lastFp = null;
      let skipped = 0;
      if (typeof onProgress === 'function') onProgress(0, times.length);
      for (let timeIndex = 0; timeIndex < times.length; timeIndex++) {
        const time = times[timeIndex];
        try {
          const frameDrawn = await new Promise((res) => {
            // Só considera o seek concluído quando há um frame decodificado de
            // verdade. O fallback não transforma ausência de frame em imagem preta.
            let do
===== vídeo @ 801211 =====
').textContent = 'Extraindo frames do vídeo...';
  document.getElementById('ic-body').innerHTML = showSpinner('Analisando vídeo...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  try {
    const frames = await extractVideoFrames(file);
    const allTrips = [];
    for (let i = 0; i < frames.length; i++) {
      document.getElementById('ic-body').innerHTML = showSpinner('Analisando frame ' + (i+1) + ' de ' + frames.length + '...');
      try {
        const trips = await extractTripsFromBase64(frames[i], 'image/jpeg', plat);
        allTrips.push(...trips);
      } catch(e) { console.warn('Frame erro:', e); }
    }
    const dayV = DB.day(S.curDate);
    const seenV = new Set();
    const unique = allTrips.filter(t => {
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seenV.has(key)) return false;
      seenV.add(key); return true;
    });
    const _dayV = DB.day(S.curDate);
    window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _dup: isTripDuplicate(t, _dayV.trips)}));
    showImportConfirm();
  } catch(e) {
    closeMo('mo-import-confirm');
    showToast('Erro ao processar vídeo: ' + e.message + '. Use MP4 ou prints do histórico.', 'error');
    console.error('Video error:', e);
  }
}


// APK 1.3.9+: o WebView de alguns aparelhos não consegue decodificar certos
// screen recordings mesmo quando o arquivo é válido. Quando o bridge nativo
// estiver disponível, o Android decodifica o vídeo e devolve só os JPEGs.
const _nativeVideoExtractRequests = new Map();
window.__mobNativeVideoFrame = function(token, dataUrl) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r || !dataUrl) return;
  const raw = String(dataUrl).includes(',') ? String(dataUrl).split(',').pop() : String(dataUrl);
  if (raw) r.frames.push(raw);
};
window.__mobNativeVideoProgress = function(token, done, total) {
  const r = _nativeVideoExtractRequests.get(token);
  if (r && typeof r.onProgress === 'function') r.onProgress(Number(done)||0, Number(total)||0);
};
window.__mobNativeVideoDone = function(token, emitted) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  if (!r.frames.length) r.reject(new Error('O Android não conseguiu extrair quadros deste vídeo.'));
  else r.resolve(r.frames);
};
window.__mobNativeVideoError = function(token, message) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  r.reject(new Error(message || 'Falha no decodificador nativo do vídeo.'));
};

function extractVideoFramesNative(file, onProgress) {
  return new Promise((resolve, reject) => {
    const token = 'nv_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2,8);
    const timer = setTimeout(() => {
      _nativeVideoExtractRequests.delete(token);
      reject(new Error('Tempo esgotado ao decodificar o vídeo no Android.'));
    }, 120000);
    _nativeVideoExtractRequests.set(token, {frames:[], onProgress, resolve, reject, timer});
    try {
      const started = SmartMobiNative.extractSelectedVideoFrames(token, 72, 720);
      if (started === false) {
        clearTimeout(timer);
        _nativeVideoExtractRequests.delete(token);
        reject(Object.assign(new Error('O Android não recebeu o arquivo de vídeo selecionado.'), {nativeNotStarted:true}));
      }
    } catch(e) {
      clearTimeout(timer);
      _nativeVideoExtractRequests.delete(token);
      reject(Object.assign(e instanceof Error ? e : new Error(String(e)), {nativeNotStarted:true}));
    }
  });
}

async function extractVideoFrames(file, onProgress) {
  let canNative = false;
  try {
    canNative = !!(window.SmartMobiNative &&
      typeof SmartMobiNative.hasNativeVideoFrameExtraction === 'function' &&
      SmartMobiNative.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');
  } catch(e) {}
  if (canNative) {
    try {
      return await extractVideoFramesNative(file, onProgress);
    } catch(e) {
      // Só cai para o decoder web se a ponte não chegou a iniciar. Se o
      // Android iniciou e reportou erro real de codec, repetir no WebView
      // apenas recriaria o travamento que motivou este caminho nativo.
      if (!e || !e.nativeNotStarted) throw e;
      console.warn('[video] bridge nativo indisponível, usando navegador:', e);
    }
  }
  return extractVideoFramesBrowser(file, onProgress);
}

async function extractVideoFramesBrowser(file, onProgress) {
  return new Promise((resolve, reject) => {
    const video  = document.createElement('video');
    const url    = URL.createObjectURL(file);
    const canvas = document.createElement('canvas');
    const ctx    = canvas.getContext('2d');
    video.style.cssText = 'position:fixed;top:-9999px;left:-9999px;width:1px;height:1px;opacity:0';
    document.body.appendChild(video);
    video.src        = url;
    video.muted      = true;
    video.playsInline = true;
    video.setAttribute('playsinline', '');
    video.setAttribute('webkit-playsinline', '');
    video.preload    = 'auto';

    const metadataTimer = setTimeout(() => {
      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      reject(new Error('O vídeo demorou demais para ser preparado. Tente novamente.'));
    }, 15000);

    video.onloadedmetadata = async () => {
      clearTimeout(metadataTimer);
      try {
      // loadedmetadata só garante dimensões/duração. Em Android/WebView o
      // decoder pode ainda não ter nenhum frame disponível. Aguarda dados
      // reais antes de iniciar os seeks, evitando canvas preto em MP4 válido.
      if (video.readyState < 2) {
        await new Promise((resolve, reject) => {
          let settled = false;
          const finish = () => { if (settled) return; settled = true; cleanup(); resolve(); };
          const fail = () => { if (settled) return; settled = true; cleanup(); reject(new Error('Não foi possível decodificar o vídeo.')); };
          const timer = setTimeout(fail, 12000);
          const cleanup = () => { clearTimeout(timer); video.removeEventListener('loadeddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
        });
      }
      // Limit canvas size for performance
      const MAX = 1080;
      const ratio = Math.min(MAX / video.videoWidth, MAX / video.videoHeight, 1);
      canvas.width  = Math.round(video.videoWidth  * ratio);
      canvas.height = Math.round(video.videoHeight * ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas por corrida real pra
      // resolveTimeConflicts() escolher a leitura certa por maioria
      const times = [];
      for (let t = 0.5; t < duration; t += INTERVAL) times.push(parseFloat(t.toFixed(2)));
      if (!times.length) times.push(0);

      // Fingerprint pequeno (32x32 grayscale) pra comparar frames e pular
      // quando o scroll tá parado/lento — evita mandar quase-o-mesmo-frame
      // pra IA, que é a causa de valores "fantasma" (leitura funde 2 frames
      // parecidos com corrida cortada/sobreposta).
      const FP = 32;
      const fpCanvas = document.createElement('canvas');
      fpCanvas.width = FP; fpCanvas.height = FP;
      const fpCtx = fpCanvas.getContext('2d');
      function frameFingerprint() {
        fpCtx.drawImage(canvas, 0, 0, FP, FP);
        const d = fpCtx.getImageData(0, 0, FP, FP).data;
        const g = new Float32Array(FP*FP);
        for (let i = 0; i < g.length; i++) {
          const o = i*4;
          g[i] = (d[o] + d[o+1] + d[o+2]) / 3;
        }
        return g;
      }
      function fpDiff(a, b) {
        let sum = 0;
        for (let i = 0; i < a.length; i++) sum += Math.abs(a[i]-b[i]);
        return sum / (a.length * 255); // normalizado 0..1
      }
      const SIMILARITY_THRESHOLD = 0.02; // <2% de mudança = frame quase idêntico, pula

      const frames = [];
      let lastFp = null;
      let skipped = 0;
      if (typeof onProgress === 'function') onProgress(0, times.length);
      for (let timeIndex = 0; timeIndex < times.length; timeIndex++) {
        const time = times[timeIndex];
        try {
          const frameDrawn = await new Promise((res) => {
            // Só considera o seek concluído quando há um frame decodificado de
            // verdade. O fallback não transforma ausência de frame em imagem preta.
            let done = false;
            const finishSeek = (fromEvent) => {
              if (done) return;
              const draw = () => {
                if (done) return;
                done = true;
                clearTimeout(timer);
                video.onseeked = null;
                let ok = false;
                try {
                  if (video.readyState >= 2 && video.videoWidth > 0 && video.videoHeight 
===== vídeo @ 803162 =====
eoProgress = function(token, done, total) {
  const r = _nativeVideoExtractRequests.get(token);
  if (r && typeof r.onProgress === 'function') r.onProgress(Number(done)||0, Number(total)||0);
};
window.__mobNativeVideoDone = function(token, emitted) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  if (!r.frames.length) r.reject(new Error('O Android não conseguiu extrair quadros deste vídeo.'));
  else r.resolve(r.frames);
};
window.__mobNativeVideoError = function(token, message) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  r.reject(new Error(message || 'Falha no decodificador nativo do vídeo.'));
};

function extractVideoFramesNative(file, onProgress) {
  return new Promise((resolve, reject) => {
    const token = 'nv_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2,8);
    const timer = setTimeout(() => {
      _nativeVideoExtractRequests.delete(token);
      reject(new Error('Tempo esgotado ao decodificar o vídeo no Android.'));
    }, 120000);
    _nativeVideoExtractRequests.set(token, {frames:[], onProgress, resolve, reject, timer});
    try {
      const started = SmartMobiNative.extractSelectedVideoFrames(token, 72, 720);
      if (started === false) {
        clearTimeout(timer);
        _nativeVideoExtractRequests.delete(token);
        reject(Object.assign(new Error('O Android não recebeu o arquivo de vídeo selecionado.'), {nativeNotStarted:true}));
      }
    } catch(e) {
      clearTimeout(timer);
      _nativeVideoExtractRequests.delete(token);
      reject(Object.assign(e instanceof Error ? e : new Error(String(e)), {nativeNotStarted:true}));
    }
  });
}

async function extractVideoFrames(file, onProgress) {
  let canNative = false;
  try {
    canNative = !!(window.SmartMobiNative &&
      typeof SmartMobiNative.hasNativeVideoFrameExtraction === 'function' &&
      SmartMobiNative.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');
  } catch(e) {}
  if (canNative) {
    try {
      return await extractVideoFramesNative(file, onProgress);
    } catch(e) {
      // Só cai para o decoder web se a ponte não chegou a iniciar. Se o
      // Android iniciou e reportou erro real de codec, repetir no WebView
      // apenas recriaria o travamento que motivou este caminho nativo.
      if (!e || !e.nativeNotStarted) throw e;
      console.warn('[video] bridge nativo indisponível, usando navegador:', e);
    }
  }
  return extractVideoFramesBrowser(file, onProgress);
}

async function extractVideoFramesBrowser(file, onProgress) {
  return new Promise((resolve, reject) => {
    const video  = document.createElement('video');
    const url    = URL.createObjectURL(file);
    const canvas = document.createElement('canvas');
    const ctx    = canvas.getContext('2d');
    video.style.cssText = 'position:fixed;top:-9999px;left:-9999px;width:1px;height:1px;opacity:0';
    document.body.appendChild(video);
    video.src        = url;
    video.muted      = true;
    video.playsInline = true;
    video.setAttribute('playsinline', '');
    video.setAttribute('webkit-playsinline', '');
    video.preload    = 'auto';

    const metadataTimer = setTimeout(() => {
      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      reject(new Error('O vídeo demorou demais para ser preparado. Tente novamente.'));
    }, 15000);

    video.onloadedmetadata = async () => {
      clearTimeout(metadataTimer);
      try {
      // loadedmetadata só garante dimensões/duração. Em Android/WebView o
      // decoder pode ainda não ter nenhum frame disponível. Aguarda dados
      // reais antes de iniciar os seeks, evitando canvas preto em MP4 válido.
      if (video.readyState < 2) {
        await new Promise((resolve, reject) => {
          let settled = false;
          const finish = () => { if (settled) return; settled = true; cleanup(); resolve(); };
          const fail = () => { if (settled) return; settled = true; cleanup(); reject(new Error('Não foi possível decodificar o vídeo.')); };
          const timer = setTimeout(fail, 12000);
          const cleanup = () => { clearTimeout(timer); video.removeEventListener('loadeddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
        });
      }
      // Limit canvas size for performance
      const MAX = 1080;
      const ratio = Math.min(MAX / video.videoWidth, MAX / video.videoHeight, 1);
      canvas.width  = Math.round(video.videoWidth  * ratio);
      canvas.height = Math.round(video.videoHeight * ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas por corrida real pra
      // resolveTimeConflicts() escolher a leitura certa por maioria
      const times = [];
      for (let t = 0.5; t < duration; t += INTERVAL) times.push(parseFloat(t.toFixed(2)));
      if (!times.length) times.push(0);

      // Fingerprint pequeno (32x32 grayscale) pra comparar frames e pular
      // quando o scroll tá parado/lento — evita mandar quase-o-mesmo-frame
      // pra IA, que é a causa de valores "fantasma" (leitura funde 2 frames
      // parecidos com corrida cortada/sobreposta).
      const FP = 32;
      const fpCanvas = document.createElement('canvas');
      fpCanvas.width = FP; fpCanvas.height = FP;
      const fpCtx = fpCanvas.getContext('2d');
      function frameFingerprint() {
        fpCtx.drawImage(canvas, 0, 0, FP, FP);
        const d = fpCtx.getImageData(0, 0, FP, FP).data;
        const g = new Float32Array(FP*FP);
        for (let i = 0; i < g.length; i++) {
          const o = i*4;
          g[i] = (d[o] + d[o+1] + d[o+2]) / 3;
        }
        return g;
      }
      function fpDiff(a, b) {
        let sum = 0;
        for (let i = 0; i < a.length; i++) sum += Math.abs(a[i]-b[i]);
        return sum / (a.length * 255); // normalizado 0..1
      }
      const SIMILARITY_THRESHOLD = 0.02; // <2% de mudança = frame quase idêntico, pula

      const frames = [];
      let lastFp = null;
      let skipped = 0;
      if (typeof onProgress === 'function') onProgress(0, times.length);
      for (let timeIndex = 0; timeIndex < times.length; timeIndex++) {
        const time = times[timeIndex];
        try {
          const frameDrawn = await new Promise((res) => {
            // Só considera o seek concluído quando há um frame decodificado de
            // verdade. O fallback não transforma ausência de frame em imagem preta.
            let done = false;
            const finishSeek = (fromEvent) => {
              if (done) return;
              const draw = () => {
                if (done) return;
                done = true;
                clearTimeout(timer);
                video.onseeked = null;
                let ok = false;
                try {
                  if (video.readyState >= 2 && video.videoWidth > 0 && video.videoHeight > 0) {
                    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
                    ok = true;
                  }
                } catch(e) {}
                res(ok);
              };
              if (fromEvent && typeof video.requestVideoFrameCallback === 'function') {
                try { video.requestVideoFrameCallback(() => draw()); return; } catch(e) {}
              }
              if (fromEvent) setTimeout(draw, 120); else draw();
            };
            const timer = setTimeout(() => finishSeek(false), 4500);
            video.onseeked = () => finishSeek(true);
            video.currentTime = Math.min(time, Math.max(0, duration - 0.03));
          });
          if (!frameDrawn) continue;
          // Detecta conteúdo no QUADRO INTEIRO. Antes olhava só 20x20 px do
          // canto superior esquerdo; em screen recording esse canto pode ser
          // preto (status bar/recorte) e todos os frames reais eram descartados.
          const probe = document.createElement('canvas');
          probe.width = 24; probe.height = 24;
          const pctx = probe.getContext('2d');
          pctx.drawImage(canvas, 0, 0, probe.width, probe.height);
          const px = pctx.getImageData(0, 0, probe.width, probe.height).data;
          let lit = 0, maxLum = 0;
          for (let pi = 0; pi < px.length; pi += 4) {
            const lum = (px[pi] + px[pi+1] + px[pi+2]) / 3;
            if (lum > 12) lit++;
            if (lum > maxLum) maxLum = lum;
          }
          const hasContent = maxLum > 18 && lit >= 3;
          if (!hasContent) continue;
          const fp = frameFingerprint();
          if (lastFp && fpDiff(fp, lastFp) < SIMILARITY_THRESHOLD) {
            skipped++;
            continue; // scroll parado/lento — quase igual ao frame anterior mantido
          }
          lastFp = fp;
          frames.push(canvas.toDataURL('image/jpeg', 0.7).split(',')[1]);
        } catch(e) { console
===== vídeo @ 803896 =====
ew Error(message || 'Falha no decodificador nativo do vídeo.'));
};

function extractVideoFramesNative(file, onProgress) {
  return new Promise((resolve, reject) => {
    const token = 'nv_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2,8);
    const timer = setTimeout(() => {
      _nativeVideoExtractRequests.delete(token);
      reject(new Error('Tempo esgotado ao decodificar o vídeo no Android.'));
    }, 120000);
    _nativeVideoExtractRequests.set(token, {frames:[], onProgress, resolve, reject, timer});
    try {
      const started = SmartMobiNative.extractSelectedVideoFrames(token, 72, 720);
      if (started === false) {
        clearTimeout(timer);
        _nativeVideoExtractRequests.delete(token);
        reject(Object.assign(new Error('O Android não recebeu o arquivo de vídeo selecionado.'), {nativeNotStarted:true}));
      }
    } catch(e) {
      clearTimeout(timer);
      _nativeVideoExtractRequests.delete(token);
      reject(Object.assign(e instanceof Error ? e : new Error(String(e)), {nativeNotStarted:true}));
    }
  });
}

async function extractVideoFrames(file, onProgress) {
  let canNative = false;
  try {
    canNative = !!(window.SmartMobiNative &&
      typeof SmartMobiNative.hasNativeVideoFrameExtraction === 'function' &&
      SmartMobiNative.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');
  } catch(e) {}
  if (canNative) {
    try {
      return await extractVideoFramesNative(file, onProgress);
    } catch(e) {
      // Só cai para o decoder web se a ponte não chegou a iniciar. Se o
      // Android iniciou e reportou erro real de codec, repetir no WebView
      // apenas recriaria o travamento que motivou este caminho nativo.
      if (!e || !e.nativeNotStarted) throw e;
      console.warn('[video] bridge nativo indisponível, usando navegador:', e);
    }
  }
  return extractVideoFramesBrowser(file, onProgress);
}

async function extractVideoFramesBrowser(file, onProgress) {
  return new Promise((resolve, reject) => {
    const video  = document.createElement('video');
    const url    = URL.createObjectURL(file);
    const canvas = document.createElement('canvas');
    const ctx    = canvas.getContext('2d');
    video.style.cssText = 'position:fixed;top:-9999px;left:-9999px;width:1px;height:1px;opacity:0';
    document.body.appendChild(video);
    video.src        = url;
    video.muted      = true;
    video.playsInline = true;
    video.setAttribute('playsinline', '');
    video.setAttribute('webkit-playsinline', '');
    video.preload    = 'auto';

    const metadataTimer = setTimeout(() => {
      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      reject(new Error('O vídeo demorou demais para ser preparado. Tente novamente.'));
    }, 15000);

    video.onloadedmetadata = async () => {
      clearTimeout(metadataTimer);
      try {
      // loadedmetadata só garante dimensões/duração. Em Android/WebView o
      // decoder pode ainda não ter nenhum frame disponível. Aguarda dados
      // reais antes de iniciar os seeks, evitando canvas preto em MP4 válido.
      if (video.readyState < 2) {
        await new Promise((resolve, reject) => {
          let settled = false;
          const finish = () => { if (settled) return; settled = true; cleanup(); resolve(); };
          const fail = () => { if (settled) return; settled = true; cleanup(); reject(new Error('Não foi possível decodificar o vídeo.')); };
          const timer = setTimeout(fail, 12000);
          const cleanup = () => { clearTimeout(timer); video.removeEventListener('loadeddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
        });
      }
      // Limit canvas size for performance
      const MAX = 1080;
      const ratio = Math.min(MAX / video.videoWidth, MAX / video.videoHeight, 1);
      canvas.width  = Math.round(video.videoWidth  * ratio);
      canvas.height = Math.round(video.videoHeight * ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas por corrida real pra
      // resolveTimeConflicts() escolher a leitura certa por maioria
      const times = [];
      for (let t = 0.5; t < duration; t += INTERVAL) times.push(parseFloat(t.toFixed(2)));
      if (!times.length) times.push(0);

      // Fingerprint pequeno (32x32 grayscale) pra comparar frames e pular
      // quando o scroll tá parado/lento — evita mandar quase-o-mesmo-frame
      // pra IA, que é a causa de valores "fantasma" (leitura funde 2 frames
      // parecidos com corrida cortada/sobreposta).
      const FP = 32;
      const fpCanvas = document.createElement('canvas');
      fpCanvas.width = FP; fpCanvas.height = FP;
      const fpCtx = fpCanvas.getContext('2d');
      function frameFingerprint() {
        fpCtx.drawImage(canvas, 0, 0, FP, FP);
        const d = fpCtx.getImageData(0, 0, FP, FP).data;
        const g = new Float32Array(FP*FP);
        for (let i = 0; i < g.length; i++) {
          const o = i*4;
          g[i] = (d[o] + d[o+1] + d[o+2]) / 3;
        }
        return g;
      }
      function fpDiff(a, b) {
        let sum = 0;
        for (let i = 0; i < a.length; i++) sum += Math.abs(a[i]-b[i]);
        return sum / (a.length * 255); // normalizado 0..1
      }
      const SIMILARITY_THRESHOLD = 0.02; // <2% de mudança = frame quase idêntico, pula

      const frames = [];
      let lastFp = null;
      let skipped = 0;
      if (typeof onProgress === 'function') onProgress(0, times.length);
      for (let timeIndex = 0; timeIndex < times.length; timeIndex++) {
        const time = times[timeIndex];
        try {
          const frameDrawn = await new Promise((res) => {
            // Só considera o seek concluído quando há um frame decodificado de
            // verdade. O fallback não transforma ausência de frame em imagem preta.
            let done = false;
            const finishSeek = (fromEvent) => {
              if (done) return;
              const draw = () => {
                if (done) return;
                done = true;
                clearTimeout(timer);
                video.onseeked = null;
                let ok = false;
                try {
                  if (video.readyState >= 2 && video.videoWidth > 0 && video.videoHeight > 0) {
                    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
                    ok = true;
                  }
                } catch(e) {}
                res(ok);
              };
              if (fromEvent && typeof video.requestVideoFrameCallback === 'function') {
                try { video.requestVideoFrameCallback(() => draw()); return; } catch(e) {}
              }
              if (fromEvent) setTimeout(draw, 120); else draw();
            };
            const timer = setTimeout(() => finishSeek(false), 4500);
            video.onseeked = () => finishSeek(true);
            video.currentTime = Math.min(time, Math.max(0, duration - 0.03));
          });
          if (!frameDrawn) continue;
          // Detecta conteúdo no QUADRO INTEIRO. Antes olhava só 20x20 px do
          // canto superior esquerdo; em screen recording esse canto pode ser
          // preto (status bar/recorte) e todos os frames reais eram descartados.
          const probe = document.createElement('canvas');
          probe.width = 24; probe.height = 24;
          const pctx = probe.getContext('2d');
          pctx.drawImage(canvas, 0, 0, probe.width, probe.height);
          const px = pctx.getImageData(0, 0, probe.width, probe.height).data;
          let lit = 0, maxLum = 0;
          for (let pi = 0; pi < px.length; pi += 4) {
            const lum = (px[pi] + px[pi+1] + px[pi+2]) / 3;
            if (lum > 12) lit++;
            if (lum > maxLum) maxLum = lum;
          }
          const hasContent = maxLum > 18 && lit >= 3;
          if (!hasContent) continue;
          const fp = frameFingerprint();
          if (lastFp && fpDiff(fp, lastFp) < SIMILARITY_THRESHOLD) {
            skipped++;
            continue; // scroll parado/lento — quase igual ao frame anterior mantido
          }
          lastFp = fp;
          frames.push(canvas.toDataURL('image/jpeg', 0.7).split(',')[1]);
        } catch(e) { console.warn('Frame t='+time, e); }
        finally {
          if (typeof onProgress === 'function') onProgress(timeIndex + 1, times.length);
        }
      }
      if (skipped) console.log('[extractVideoFrames] frames quase-duplicados pulados:', skipped);

      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      if (frames.length > 0) resolve(frames);
      else reject(new Error('Nenhum quadro legível foi extraído do vídeo.'));
      } catch(e) {
        try{document.body.removeChild(video);}catch(e2){}
        URL.revokeObjectURL(url);
        reject(e instanceof Error ? e : new Error(String(e)));
      }
    };

    video.onerror = (e) => {
      clearTimeout(metadataTimer);
      try{docume
===== vídeo @ 805207 =====
.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');
  } catch(e) {}
  if (canNative) {
    try {
      return await extractVideoFramesNative(file, onProgress);
    } catch(e) {
      // Só cai para o decoder web se a ponte não chegou a iniciar. Se o
      // Android iniciou e reportou erro real de codec, repetir no WebView
      // apenas recriaria o travamento que motivou este caminho nativo.
      if (!e || !e.nativeNotStarted) throw e;
      console.warn('[video] bridge nativo indisponível, usando navegador:', e);
    }
  }
  return extractVideoFramesBrowser(file, onProgress);
}

async function extractVideoFramesBrowser(file, onProgress) {
  return new Promise((resolve, reject) => {
    const video  = document.createElement('video');
    const url    = URL.createObjectURL(file);
    const canvas = document.createElement('canvas');
    const ctx    = canvas.getContext('2d');
    video.style.cssText = 'position:fixed;top:-9999px;left:-9999px;width:1px;height:1px;opacity:0';
    document.body.appendChild(video);
    video.src        = url;
    video.muted      = true;
    video.playsInline = true;
    video.setAttribute('playsinline', '');
    video.setAttribute('webkit-playsinline', '');
    video.preload    = 'auto';

    const metadataTimer = setTimeout(() => {
      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      reject(new Error('O vídeo demorou demais para ser preparado. Tente novamente.'));
    }, 15000);

    video.onloadedmetadata = async () => {
      clearTimeout(metadataTimer);
      try {
      // loadedmetadata só garante dimensões/duração. Em Android/WebView o
      // decoder pode ainda não ter nenhum frame disponível. Aguarda dados
      // reais antes de iniciar os seeks, evitando canvas preto em MP4 válido.
      if (video.readyState < 2) {
        await new Promise((resolve, reject) => {
          let settled = false;
          const finish = () => { if (settled) return; settled = true; cleanup(); resolve(); };
          const fail = () => { if (settled) return; settled = true; cleanup(); reject(new Error('Não foi possível decodificar o vídeo.')); };
          const timer = setTimeout(fail, 12000);
          const cleanup = () => { clearTimeout(timer); video.removeEventListener('loadeddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
        });
      }
      // Limit canvas size for performance
      const MAX = 1080;
      const ratio = Math.min(MAX / video.videoWidth, MAX / video.videoHeight, 1);
      canvas.width  = Math.round(video.videoWidth  * ratio);
      canvas.height = Math.round(video.videoHeight * ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas por corrida real pra
      // resolveTimeConflicts() escolher a leitura certa por maioria
      const times = [];
      for (let t = 0.5; t < duration; t += INTERVAL) times.push(parseFloat(t.toFixed(2)));
      if (!times.length) times.push(0);

      // Fingerprint pequeno (32x32 grayscale) pra comparar frames e pular
      // quando o scroll tá parado/lento — evita mandar quase-o-mesmo-frame
      // pra IA, que é a causa de valores "fantasma" (leitura funde 2 frames
      // parecidos com corrida cortada/sobreposta).
      const FP = 32;
      const fpCanvas = document.createElement('canvas');
      fpCanvas.width = FP; fpCanvas.height = FP;
      const fpCtx = fpCanvas.getContext('2d');
      function frameFingerprint() {
        fpCtx.drawImage(canvas, 0, 0, FP, FP);
        const d = fpCtx.getImageData(0, 0, FP, FP).data;
        const g = new Float32Array(FP*FP);
        for (let i = 0; i < g.length; i++) {
          const o = i*4;
          g[i] = (d[o] + d[o+1] + d[o+2]) / 3;
        }
        return g;
      }
      function fpDiff(a, b) {
        let sum = 0;
        for (let i = 0; i < a.length; i++) sum += Math.abs(a[i]-b[i]);
        return sum / (a.length * 255); // normalizado 0..1
      }
      const SIMILARITY_THRESHOLD = 0.02; // <2% de mudança = frame quase idêntico, pula

      const frames = [];
      let lastFp = null;
      let skipped = 0;
      if (typeof onProgress === 'function') onProgress(0, times.length);
      for (let timeIndex = 0; timeIndex < times.length; timeIndex++) {
        const time = times[timeIndex];
        try {
          const frameDrawn = await new Promise((res) => {
            // Só considera o seek concluído quando há um frame decodificado de
            // verdade. O fallback não transforma ausência de frame em imagem preta.
            let done = false;
            const finishSeek = (fromEvent) => {
              if (done) return;
              const draw = () => {
                if (done) return;
                done = true;
                clearTimeout(timer);
                video.onseeked = null;
                let ok = false;
                try {
                  if (video.readyState >= 2 && video.videoWidth > 0 && video.videoHeight > 0) {
                    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
                    ok = true;
                  }
                } catch(e) {}
                res(ok);
              };
              if (fromEvent && typeof video.requestVideoFrameCallback === 'function') {
                try { video.requestVideoFrameCallback(() => draw()); return; } catch(e) {}
              }
              if (fromEvent) setTimeout(draw, 120); else draw();
            };
            const timer = setTimeout(() => finishSeek(false), 4500);
            video.onseeked = () => finishSeek(true);
            video.currentTime = Math.min(time, Math.max(0, duration - 0.03));
          });
          if (!frameDrawn) continue;
          // Detecta conteúdo no QUADRO INTEIRO. Antes olhava só 20x20 px do
          // canto superior esquerdo; em screen recording esse canto pode ser
          // preto (status bar/recorte) e todos os frames reais eram descartados.
          const probe = document.createElement('canvas');
          probe.width = 24; probe.height = 24;
          const pctx = probe.getContext('2d');
          pctx.drawImage(canvas, 0, 0, probe.width, probe.height);
          const px = pctx.getImageData(0, 0, probe.width, probe.height).data;
          let lit = 0, maxLum = 0;
          for (let pi = 0; pi < px.length; pi += 4) {
            const lum = (px[pi] + px[pi+1] + px[pi+2]) / 3;
            if (lum > 12) lit++;
            if (lum > maxLum) maxLum = lum;
          }
          const hasContent = maxLum > 18 && lit >= 3;
          if (!hasContent) continue;
          const fp = frameFingerprint();
          if (lastFp && fpDiff(fp, lastFp) < SIMILARITY_THRESHOLD) {
            skipped++;
            continue; // scroll parado/lento — quase igual ao frame anterior mantido
          }
          lastFp = fp;
          frames.push(canvas.toDataURL('image/jpeg', 0.7).split(',')[1]);
        } catch(e) { console.warn('Frame t='+time, e); }
        finally {
          if (typeof onProgress === 'function') onProgress(timeIndex + 1, times.length);
        }
      }
      if (skipped) console.log('[extractVideoFrames] frames quase-duplicados pulados:', skipped);

      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      if (frames.length > 0) resolve(frames);
      else reject(new Error('Nenhum quadro legível foi extraído do vídeo.'));
      } catch(e) {
        try{document.body.removeChild(video);}catch(e2){}
        URL.revokeObjectURL(url);
        reject(e instanceof Error ? e : new Error(String(e)));
      }
    };

    video.onerror = (e) => {
      clearTimeout(metadataTimer);
      try{document.body.removeChild(video);}catch(e2){}
      URL.revokeObjectURL(url);
      const msg = e?.target?.error?.message || '';
      reject(new Error('Formato de vídeo não suportado. Use MP4 gravado pelo celular.\n' + msg));
    };
    video.load();
  });
}

async function extractTripsFromImage(file, plat) {
  const b64 = await new Promise((res,rej) => {
    const r = new FileReader();
    r.onload = () => res(r.result.split(',')[1]); r.onerror = () => rej(r.error); r.readAsDataURL(file);
  });
  return extractTripsFromBase64(b64, file.type||'image/jpeg', plat);
}

async function extractTripsFromBase64(b64, mt, plat) {
  const cashWarning = 'ATENÇÃO — confusão comum: "Dinheiro recebido" (ícone de câmera/recibo) NÃO é gorjeta nem dinâmico, é so a forma de pagamento (o passageiro pagou em espécie). Quando uma corrida tiver "Preço dinâmico" (ícone de raio) E "Dinheiro recebido" juntas, são DUAS linhas diferentes — extraia SÓ o valor do "Preço dinâmico" em dinamico, e NUNCA coloque o valor de "Dinheiro recebido" em gorjeta (nem em dinamico, nem em value). Exemplo: corrida com "R$ 12,50 Preço dinâmico" e "R$ 27,05 Dinheiro recebido" → dinamico:12.50, gorjeta:0.00 (o R$27,05 não entra em lugar nenhum do JSON).';
  // Prompt único — a IA identifica sozinha se é Uber ou 99 pelo layout da
  // imagem (e
===== vídeo @ 810115 =====
ath.abs(a[i]-b[i]);
        return sum / (a.length * 255); // normalizado 0..1
      }
      const SIMILARITY_THRESHOLD = 0.02; // <2% de mudança = frame quase idêntico, pula

      const frames = [];
      let lastFp = null;
      let skipped = 0;
      if (typeof onProgress === 'function') onProgress(0, times.length);
      for (let timeIndex = 0; timeIndex < times.length; timeIndex++) {
        const time = times[timeIndex];
        try {
          const frameDrawn = await new Promise((res) => {
            // Só considera o seek concluído quando há um frame decodificado de
            // verdade. O fallback não transforma ausência de frame em imagem preta.
            let done = false;
            const finishSeek = (fromEvent) => {
              if (done) return;
              const draw = () => {
                if (done) return;
                done = true;
                clearTimeout(timer);
                video.onseeked = null;
                let ok = false;
                try {
                  if (video.readyState >= 2 && video.videoWidth > 0 && video.videoHeight > 0) {
                    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
                    ok = true;
                  }
                } catch(e) {}
                res(ok);
              };
              if (fromEvent && typeof video.requestVideoFrameCallback === 'function') {
                try { video.requestVideoFrameCallback(() => draw()); return; } catch(e) {}
              }
              if (fromEvent) setTimeout(draw, 120); else draw();
            };
            const timer = setTimeout(() => finishSeek(false), 4500);
            video.onseeked = () => finishSeek(true);
            video.currentTime = Math.min(time, Math.max(0, duration - 0.03));
          });
          if (!frameDrawn) continue;
          // Detecta conteúdo no QUADRO INTEIRO. Antes olhava só 20x20 px do
          // canto superior esquerdo; em screen recording esse canto pode ser
          // preto (status bar/recorte) e todos os frames reais eram descartados.
          const probe = document.createElement('canvas');
          probe.width = 24; probe.height = 24;
          const pctx = probe.getContext('2d');
          pctx.drawImage(canvas, 0, 0, probe.width, probe.height);
          const px = pctx.getImageData(0, 0, probe.width, probe.height).data;
          let lit = 0, maxLum = 0;
          for (let pi = 0; pi < px.length; pi += 4) {
            const lum = (px[pi] + px[pi+1] + px[pi+2]) / 3;
            if (lum > 12) lit++;
            if (lum > maxLum) maxLum = lum;
          }
          const hasContent = maxLum > 18 && lit >= 3;
          if (!hasContent) continue;
          const fp = frameFingerprint();
          if (lastFp && fpDiff(fp, lastFp) < SIMILARITY_THRESHOLD) {
            skipped++;
            continue; // scroll parado/lento — quase igual ao frame anterior mantido
          }
          lastFp = fp;
          frames.push(canvas.toDataURL('image/jpeg', 0.7).split(',')[1]);
        } catch(e) { console.warn('Frame t='+time, e); }
        finally {
          if (typeof onProgress === 'function') onProgress(timeIndex + 1, times.length);
        }
      }
      if (skipped) console.log('[extractVideoFrames] frames quase-duplicados pulados:', skipped);

      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      if (frames.length > 0) resolve(frames);
      else reject(new Error('Nenhum quadro legível foi extraído do vídeo.'));
      } catch(e) {
        try{document.body.removeChild(video);}catch(e2){}
        URL.revokeObjectURL(url);
        reject(e instanceof Error ? e : new Error(String(e)));
      }
    };

    video.onerror = (e) => {
      clearTimeout(metadataTimer);
      try{document.body.removeChild(video);}catch(e2){}
      URL.revokeObjectURL(url);
      const msg = e?.target?.error?.message || '';
      reject(new Error('Formato de vídeo não suportado. Use MP4 gravado pelo celular.\n' + msg));
    };
    video.load();
  });
}

async function extractTripsFromImage(file, plat) {
  const b64 = await new Promise((res,rej) => {
    const r = new FileReader();
    r.onload = () => res(r.result.split(',')[1]); r.onerror = () => rej(r.error); r.readAsDataURL(file);
  });
  return extractTripsFromBase64(b64, file.type||'image/jpeg', plat);
}

async function extractTripsFromBase64(b64, mt, plat) {
  const cashWarning = 'ATENÇÃO — confusão comum: "Dinheiro recebido" (ícone de câmera/recibo) NÃO é gorjeta nem dinâmico, é so a forma de pagamento (o passageiro pagou em espécie). Quando uma corrida tiver "Preço dinâmico" (ícone de raio) E "Dinheiro recebido" juntas, são DUAS linhas diferentes — extraia SÓ o valor do "Preço dinâmico" em dinamico, e NUNCA coloque o valor de "Dinheiro recebido" em gorjeta (nem em dinamico, nem em value). Exemplo: corrida com "R$ 12,50 Preço dinâmico" e "R$ 27,05 Dinheiro recebido" → dinamico:12.50, gorjeta:0.00 (o R$27,05 não entra em lugar nenhum do JSON).';
  // Prompt único — a IA identifica sozinha se é Uber ou 99 pelo layout da
  // imagem (em vez de confiar em qual botão o usuário tocou, que pode estar
  // errado). Cada corrida retornada inclui "platform":"uber"|"99".
  const prompt = 'Esta imagem mostra uma tela do app de motorista — pode ser: (1) UBER, tela "Histórico de ganhos" (lista de corridas com linha divisória entre elas, "Preço dinâmico" com ícone de raio, "Valor extra" com ícone de mão/coração = gorjeta); (2) 99 LISTA, tela "Histórico de corridas" do app 99 Motorista (ícone de carro + texto "Pop", tag laranja "Taxa Fixa de X%", "Ganhos pagos no app" ou "Ganhos pagos em dinheiro"); ou (3) 99 DETALHE, tela "Detalhes da corrida" do 99 (título "Detalhes da corrida" no topo, "Você ganhou R$X" em destaque, campos "Duração" e "Distância"). PRIMEIRO identifique qual das três telas é, pelo layout, e siga SOMENTE as regras dessa tela, abaixo. Inclua "platform":"uber" ou "platform":"99" em cada item do JSON (uma imagem só tem UMA plataforma e UM tipo de tela, nunca misture).\n\n' +
    '=== SE FOR UBER ===\n' +
    'Extraia CADA corrida, mas SÓ quando ela aparecer COMPLETA na imagem (com a linha divisória acima e abaixo) — se uma corrida estiver cortada no topo ou na base da imagem (incompleta), IGNORE essa corrida, não tente adivinhar ou completar com dados de outra. Ignore Missão (R$ 0,00). ' + cashWarning + ' Abaixo do valor principal de cada corrida também pode aparecer "Valor extra" (ícone de mão/coração — é a gorjeta do passageiro) — extraia esse valor em gorjeta quando existir. Também pode aparecer "Pedágio R$X,XX" — extraia esse valor em pedagio quando existir (0.00 quando não aparecer). Cada corrida mostra dois endereços (origem e destino): copie a linha de texto INTEIRA, palavra por palavra, exatamente como está escrita na tela, sem resumir ou cortar nenhuma parte, em "originFull"/"destFull". E TAMBÉM extraia só o nome do bairro (sem rua, número, cidade, UF ou CEP) em "origin"/"dest" — ex: se o texto na tela é "Rua Corcovado, 213, Jardim Botânico - Rio de Janeiro - RJ, 22460-050, BR", então origin="Jardim Botânico" e originFull="Rua Corcovado, 213, Jardim Botânico - Rio de Janeiro - RJ, 22460-050, BR" (a linha inteira, não abreviada). Formato de cada item: {"platform":"uber","time":"HH:MM","value":0.00,"km":0.0,"duration":0,"category":"Black","origin":"bairro","originFull":"endereço completo","dest":"bairro","destFull":"endereço completo","dinamico":0.00,"gorjeta":0.00,"pedagio":0.00,"taxaZero":false,"dinheiro":false}. duration=minutos inteiros. dinamico, gorjeta e pedagio ficam 0.00 quando não aparecerem. "taxaZero":true se houver indicação de 0% de taxa. "dinheiro":true se pagamento em dinheiro.\n\n' +
    '=== SE FOR 99 ===\n' +
    'Estrutura de CADA corrida, sempre nesta ordem: ' +
    '(1) Data e hora no formato "DD/MM/AAAA HH:MM" — extraia SOMENTE o HH:MM como campo "time". ' +
    '(2) Forma de pagamento: "Ganhos pagos no app" ou "Ganhos pagos em dinheiro" — IGNORE, não é valor nem corrida. ' +
    '(3) Ícone de carro + nome da categoria (pode ser "Pop", "Negocia", "Comfort" ou outra — leia o que estiver escrito, não assuma que é sempre "Pop") + valor "R$X,XX" à direita — este R$X,XX é o "value" da corrida, não importa qual seja a categoria. Converta vírgula para ponto (R$7,90 → 7.90). Guarde o texto exato da categoria lida em "category" com o prefixo "99" (ex: categoria "Pop" → "99Pop", categoria "Negocia" → "99Negocia"). ' +
    '(4) Tag laranja "Taxa Fixa de X%" — extraia o número X como "serviceFee" em formato decimal (ex: "Taxa Fixa de 15%" → serviceFee:0.15, "Taxa Fixa de 0%" → serviceFee:0). Não é gorjeta nem dinâmico. ' +
    '(5) Endereço de origem (ícone seta pra cima) e endereço de destino (ícone seta pra baixo) — copie a linha de texto INTEIRA, palavra por palavra, exatamente como está escrita na tela, sem resumir ou cortar nenhuma parte (incluindo nome de rua, número e complemento quando visíveis) em "originFull"/"destFull". E TAMBÉM extraia só o nome do bairro (sem rua, número, cidade, UF) em "origin"/"dest" — ex: se o texto na tela é "Rua Carmela Dutra, Agriões, Teresópolis - RJ", então origin="Agriões" e originFull="Rua Carmela Dutra, Agriões, Teresópolis - RJ" (a linha inteira, não abreviada). ' +
    '(5b) Pode aparecer "Pedágio R$X,XX" — extraia esse valor em pedagio quando existir (0.00 quando não aparecer). ' +
    '(6) Status verde "Pago" ou "Pedido finalizado" — marca o FIM desta corrida. ' +
    '(7) CABEÇALHO FIXO QUE NÃO ROLA: no topo da tela ficam fixas as abas "Corridas / Entregas" e a barra de filtros ("18/7 ▼", "Taxas ▼", "Pagamento ▼") — esse bloco NUNCA rola junto com a lista. Enquanto a lista sobe, a linha de data/hora de cada corrida desaparece por baixo desse cabeçalho fixo ANTES do resto do cartão (forma de pagamento, categoria+valor, taxa, endereços, status), que continuam visíveis mais um pouco. Por isso, na MAIORIA dos frames, o primeiro cartão logo abaixo do cabeçalho fixo aparece SEM a linha de
===== vídeo @ 810554 =====
 try {
          const frameDrawn = await new Promise((res) => {
            // Só considera o seek concluído quando há um frame decodificado de
            // verdade. O fallback não transforma ausência de frame em imagem preta.
            let done = false;
            const finishSeek = (fromEvent) => {
              if (done) return;
              const draw = () => {
                if (done) return;
                done = true;
                clearTimeout(timer);
                video.onseeked = null;
                let ok = false;
                try {
                  if (video.readyState >= 2 && video.videoWidth > 0 && video.videoHeight > 0) {
                    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
                    ok = true;
                  }
                } catch(e) {}
                res(ok);
              };
              if (fromEvent && typeof video.requestVideoFrameCallback === 'function') {
                try { video.requestVideoFrameCallback(() => draw()); return; } catch(e) {}
              }
              if (fromEvent) setTimeout(draw, 120); else draw();
            };
            const timer = setTimeout(() => finishSeek(false), 4500);
            video.onseeked = () => finishSeek(true);
            video.currentTime = Math.min(time, Math.max(0, duration - 0.03));
          });
          if (!frameDrawn) continue;
          // Detecta conteúdo no QUADRO INTEIRO. Antes olhava só 20x20 px do
          // canto superior esquerdo; em screen recording esse canto pode ser
          // preto (status bar/recorte) e todos os frames reais eram descartados.
          const probe = document.createElement('canvas');
          probe.width = 24; probe.height = 24;
          const pctx = probe.getContext('2d');
          pctx.drawImage(canvas, 0, 0, probe.width, probe.height);
          const px = pctx.getImageData(0, 0, probe.width, probe.height).data;
          let lit = 0, maxLum = 0;
          for (let pi = 0; pi < px.length; pi += 4) {
            const lum = (px[pi] + px[pi+1] + px[pi+2]) / 3;
            if (lum > 12) lit++;
            if (lum > maxLum) maxLum = lum;
          }
          const hasContent = maxLum > 18 && lit >= 3;
          if (!hasContent) continue;
          const fp = frameFingerprint();
          if (lastFp && fpDiff(fp, lastFp) < SIMILARITY_THRESHOLD) {
            skipped++;
            continue; // scroll parado/lento — quase igual ao frame anterior mantido
          }
          lastFp = fp;
          frames.push(canvas.toDataURL('image/jpeg', 0.7).split(',')[1]);
        } catch(e) { console.warn('Frame t='+time, e); }
        finally {
          if (typeof onProgress === 'function') onProgress(timeIndex + 1, times.length);
        }
      }
      if (skipped) console.log('[extractVideoFrames] frames quase-duplicados pulados:', skipped);

      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      if (frames.length > 0) resolve(frames);
      else reject(new Error('Nenhum quadro legível foi extraído do vídeo.'));
      } catch(e) {
        try{document.body.removeChild(video);}catch(e2){}
        URL.revokeObjectURL(url);
        reject(e instanceof Error ? e : new Error(String(e)));
      }
    };

    video.onerror = (e) => {
      clearTimeout(metadataTimer);
      try{document.body.removeChild(video);}catch(e2){}
      URL.revokeObjectURL(url);
      const msg = e?.target?.error?.message || '';
      reject(new Error('Formato de vídeo não suportado. Use MP4 gravado pelo celular.\n' + msg));
    };
    video.load();
  });
}

async function extractTripsFromImage(file, plat) {
  const b64 = await new Promise((res,rej) => {
    const r = new FileReader();
    r.onload = () => res(r.result.split(',')[1]); r.onerror = () => rej(r.error); r.readAsDataURL(file);
  });
  return extractTripsFromBase64(b64, file.type||'image/jpeg', plat);
}

async function extractTripsFromBase64(b64, mt, plat) {
  const cashWarning = 'ATENÇÃO — confusão comum: "Dinheiro recebido" (ícone de câmera/recibo) NÃO é gorjeta nem dinâmico, é so a forma de pagamento (o passageiro pagou em espécie). Quando uma corrida tiver "Preço dinâmico" (ícone de raio) E "Dinheiro recebido" juntas, são DUAS linhas diferentes — extraia SÓ o valor do "Preço dinâmico" em dinamico, e NUNCA coloque o valor de "Dinheiro recebido" em gorjeta (nem em dinamico, nem em value). Exemplo: corrida com "R$ 12,50 Preço dinâmico" e "R$ 27,05 Dinheiro recebido" → dinamico:12.50, gorjeta:0.00 (o R$27,05 não entra em lugar nenhum do JSON).';
  // Prompt único — a IA identifica sozinha se é Uber ou 99 pelo layout da
  // imagem (em vez de confiar em qual botão o usuário tocou, que pode estar
  // errado). Cada corrida retornada inclui "platform":"uber"|"99".
  const prompt = 'Esta imagem mostra uma tela do app de motorista — pode ser: (1) UBER, tela "Histórico de ganhos" (lista de corridas com linha divisória entre elas, "Preço dinâmico" com ícone de raio, "Valor extra" com ícone de mão/coração = gorjeta); (2) 99 LISTA, tela "Histórico de corridas" do app 99 Motorista (ícone de carro + texto "Pop", tag laranja "Taxa Fixa de X%", "Ganhos pagos no app" ou "Ganhos pagos em dinheiro"); ou (3) 99 DETALHE, tela "Detalhes da corrida" do 99 (título "Detalhes da corrida" no topo, "Você ganhou R$X" em destaque, campos "Duração" e "Distância"). PRIMEIRO identifique qual das três telas é, pelo layout, e siga SOMENTE as regras dessa tela, abaixo. Inclua "platform":"uber" ou "platform":"99" em cada item do JSON (uma imagem só tem UMA plataforma e UM tipo de tela, nunca misture).\n\n' +
    '=== SE FOR UBER ===\n' +
    'Extraia CADA corrida, mas SÓ quando ela aparecer COMPLETA na imagem (com a linha divisória acima e abaixo) — se uma corrida estiver cortada no topo ou na base da imagem (incompleta), IGNORE essa corrida, não tente adivinhar ou completar com dados de outra. Ignore Missão (R$ 0,00). ' + cashWarning + ' Abaixo do valor principal de cada corrida também pode aparecer "Valor extra" (ícone de mão/coração — é a gorjeta do passageiro) — extraia esse valor em gorjeta quando existir. Também pode aparecer "Pedágio R$X,XX" — extraia esse valor em pedagio quando existir (0.00 quando não aparecer). Cada corrida mostra dois endereços (origem e destino): copie a linha de texto INTEIRA, palavra por palavra, exatamente como está escrita na tela, sem resumir ou cortar nenhuma parte, em "originFull"/"destFull". E TAMBÉM extraia só o nome do bairro (sem rua, número, cidade, UF ou CEP) em "origin"/"dest" — ex: se o texto na tela é "Rua Corcovado, 213, Jardim Botânico - Rio de Janeiro - RJ, 22460-050, BR", então origin="Jardim Botânico" e originFull="Rua Corcovado, 213, Jardim Botânico - Rio de Janeiro - RJ, 22460-050, BR" (a linha inteira, não abreviada). Formato de cada item: {"platform":"uber","time":"HH:MM","value":0.00,"km":0.0,"duration":0,"category":"Black","origin":"bairro","originFull":"endereço completo","dest":"bairro","destFull":"endereço completo","dinamico":0.00,"gorjeta":0.00,"pedagio":0.00,"taxaZero":false,"dinheiro":false}. duration=minutos inteiros. dinamico, gorjeta e pedagio ficam 0.00 quando não aparecerem. "taxaZero":true se houver indicação de 0% de taxa. "dinheiro":true se pagamento em dinheiro.\n\n' +
    '=== SE FOR 99 ===\n' +
    'Estrutura de CADA corrida, sempre nesta ordem: ' +
    '(1) Data e hora no formato "DD/MM/AAAA HH:MM" — extraia SOMENTE o HH:MM como campo "time". ' +
    '(2) Forma de pagamento: "Ganhos pagos no app" ou "Ganhos pagos em dinheiro" — IGNORE, não é valor nem corrida. ' +
    '(3) Ícone de carro + nome da categoria (pode ser "Pop", "Negocia", "Comfort" ou outra — leia o que estiver escrito, não assuma que é sempre "Pop") + valor "R$X,XX" à direita — este R$X,XX é o "value" da corrida, não importa qual seja a categoria. Converta vírgula para ponto (R$7,90 → 7.90). Guarde o texto exato da categoria lida em "category" com o prefixo "99" (ex: categoria "Pop" → "99Pop", categoria "Negocia" → "99Negocia"). ' +
    '(4) Tag laranja "Taxa Fixa de X%" — extraia o número X como "serviceFee" em formato decimal (ex: "Taxa Fixa de 15%" → serviceFee:0.15, "Taxa Fixa de 0%" → serviceFee:0). Não é gorjeta nem dinâmico. ' +
    '(5) Endereço de origem (ícone seta pra cima) e endereço de destino (ícone seta pra baixo) — copie a linha de texto INTEIRA, palavra por palavra, exatamente como está escrita na tela, sem resumir ou cortar nenhuma parte (incluindo nome de rua, número e complemento quando visíveis) em "originFull"/"destFull". E TAMBÉM extraia só o nome do bairro (sem rua, número, cidade, UF) em "origin"/"dest" — ex: se o texto na tela é "Rua Carmela Dutra, Agriões, Teresópolis - RJ", então origin="Agriões" e originFull="Rua Carmela Dutra, Agriões, Teresópolis - RJ" (a linha inteira, não abreviada). ' +
    '(5b) Pode aparecer "Pedágio R$X,XX" — extraia esse valor em pedagio quando existir (0.00 quando não aparecer). ' +
    '(6) Status verde "Pago" ou "Pedido finalizado" — marca o FIM desta corrida. ' +
    '(7) CABEÇALHO FIXO QUE NÃO ROLA: no topo da tela ficam fixas as abas "Corridas / Entregas" e a barra de filtros ("18/7 ▼", "Taxas ▼", "Pagamento ▼") — esse bloco NUNCA rola junto com a lista. Enquanto a lista sobe, a linha de data/hora de cada corrida desaparece por baixo desse cabeçalho fixo ANTES do resto do cartão (forma de pagamento, categoria+valor, taxa, endereços, status), que continuam visíveis mais um pouco. Por isso, na MAIORIA dos frames, o primeiro cartão logo abaixo do cabeçalho fixo aparece SEM a linha de data/hora, mesmo com todo o resto do cartão visível e legível. ' +
    'REGRAS: ' +
    'Extraia SOMENTE corridas COMPLETAS que mostrem data+hora, categoria+valor, e pelo menos um endereço. ' +
    'VALIDAÇÃO LINHA A LINHA — REGRA MAIS IMPORTANTE DE TODAS: só extraia uma corrida se o bloco dela estiver 100% delimitado e visível NESTA imagem, do início ao fim: (a) a linha de data/hora "DD/MM/AAAA HH:MM" visível e completa dentro do PRÓ
===== vídeo @ 821367 =====
eixar de fora do que adivinhar: esse mesmo cartão aparece COMPLETO, com a data/hora visível, em um frame anterior (alguns instantes antes, antes de rolar pra debaixo do cabeçalho). NUNCA complete um cartão cortado com dados do vizinho, e NUNCA reaproveite ou "lembre" uma data/hora vista em outro frame — a data/hora tem que estar escrita nesta mesma imagem, colada nesse cartão específico. ' +
    'CADA HORÁRIO (HH:MM) IDENTIFICA UMA ÚNICA CORRIDA: nunca retorne dois itens com o mesmo "time" — se o mesmo horário aparecer repetido, é o mesmo cartão visto duas vezes, retorne só um. ' +
    'LINHAS DIVISÓRIAS: cada corrida fica delimitada por LINHAS HORIZONTAIS FINAS na tela. O bloco de uma corrida é: data/hora → forma de pagamento → Pop + valor → tag de taxa → endereços → linha fina → status verde ("Pago" ou "Pedido finalizado"). Use essas linhas finas como fronteira: TODOS os campos de uma corrida (hora, valor, taxa, endereços, status) ficam juntos dentro do mesmo bloco, entre duas linhas finas. NUNCA combine um campo de um bloco com um campo de outro bloco — se uma linha fina separa a data/hora de um valor ou endereço, eles pertencem a corridas DIFERENTES. ' +
    'O valor R$X,XX de cada corrida está DENTRO do mesmo cartão que a data/hora dela (mesma caixa, entre a linha da data e o status final) — NUNCA use o valor de um cartão vizinho de cima ou de baixo. Se o valor do cartão não estiver visível por inteiro dentro dele, IGNORE a corrida em vez de pegar o valor de outro cartão. ' +
    'BAIRRO NUNCA É A CIDADE: a linha de endereço da 99 termina com a CIDADE e UF (ex: "..., Teresópolis - RJ") e muitas vezes NEM TEM bairro — só rua/estrada, número, CEP e cidade (ex: "Estr. Teresópolis x Friburgo, nº 8500, 25976-655, Teresópolis - RJ"). Nesse caso use o nome da rua/estrada em "origin"/"dest" (no exemplo: "Estr. Teresópolis x Friburgo"). O nome da cidade sozinho NUNCA vai em "origin"/"dest" — cuidado: a palavra "Teresópolis" pode fazer parte do nome de uma estrada (aí pode) mas nunca deve ser usada sozinha por ser a cidade do fim da linha. Sem rua legível, use "" (string vazia). ' +
    'IGNORE corridas com valor R$0,00 ou cujo valor não esteja claramente legível — corrida real da 99 sempre tem valor maior que zero. ' +
    'AUTOCONFERÊNCIA FINAL: antes de devolver o JSON, revise item por item e confirme: (1) o bloco da corrida estava 100% delimitado NESTA imagem — data/hora própria do cartão (não colada no cabeçalho fixo) + status na base + linhas finas; (2) time, value, taxa e endereços vieram TODOS do MESMO cartão; (3) nenhum "time" foi copiado, adivinhado ou "lembrado" de outro cartão/frame — se a data/hora não está escrita nesta imagem colada nesse cartão, o item inteiro sai do JSON. Remova qualquer item que não passe nas três checagens. ' +
    '"km" e "duration" sempre 0 pois não aparecem no histórico. ' +
    '"dinamico" e "gorjeta" sempre 0.00 pois não aparecem no histórico da 99. ' +
    'Formato de cada item: {"platform":"99","time":"HH:MM","value":0.00,"km":0.0,"duration":0,"category":"99Pop","origin":"bairro","originFull":"endereço completo","dest":"bairro","destFull":"endereço completo","dinamico":0.00,"gorjeta":0.00,"pedagio":0.00,"serviceFee":0.00,"dinheiro":false}. "serviceFee" é sempre o número da tag "Taxa Fixa de X%" convertido pra decimal (15% → 0.15). "dinheiro":true quando aparecer "Ganhos pagos em dinheiro".\n\n' +
    '=== SE FOR 99 DETALHE ===\n' +
    'Esta tela mostra UMA única corrida, mas é ROLÁVEL: um frame de vídeo captura só uma PARTE dela por vez. Essa tela serve SÓ pra completar km, duração e endereço completo de uma corrida que a lista já identificou — NÃO extraia valor, dinheiro, taxa, dinâmico, gorjeta nem pedágio aqui (a lista já tem isso, e misturar valor lido nesta tela causava corrida duplicada/fantasma no import). Extraia como FRAGMENTO: pegue os campos abaixo que estiverem legíveis NESTE frame, deixe os que não aparecerem como null. ' +
    'Campos: "duration" = Duração convertida pra MINUTOS INTEIROS arredondados (12m50s → 13; 5m42s → 6), null se não aparecer neste frame. "km" = número de Distância (6,2 km → 6.2), null se não aparecer. "time" = horário HH:MM da data completa (ex "06/07/2026 15:51:42" → "15:51"), null se não aparecer neste frame. "originFull"/"destFull" = endereço completo dos pontos verde(origem)/laranja(destino) quando visíveis, null se não aparecerem; "origin"/"dest" = bairro extraído desses endereços (nunca a cidade sozinha — mesma regra da lista). ' +
    'Formato (retorne um array com este único item; campos não vistos = null): {"platform":"99","screen":"detail","time":null,"km":null,"duration":null,"origin":null,"originFull":null,"dest":null,"destFull":null}.\n\n' +
    'Retorne SOMENTE um único JSON array, sem texto extra. Se não houver corridas completas retorne [].';
  const requestController = new AbortController();
  const requestTimer = setTimeout(() => requestController.abort(), 25000);
  let resp;
  try {
    resp = await fetch('https://smartmobi-proxy.yurisilva1990.workers.dev', {
      method:'POST', headers:{'Content-Type':'application/json'}, signal:requestController.signal,
      body: JSON.stringify({model:'claude-haiku-4-5-20251001', max_tokens:1500,
        messages:[{role:'user', content:[{type:'image',source:{type:'base64',media_type:mt,data:b64}},{type:'text',text:prompt}]}]})
    });
  } catch(e) {
    if (e && e.name === 'AbortError') throw new Error('Tempo esgotado ao analisar quadro');
    throw e;
  } finally {
    clearTimeout(requestTimer);
  }
  if (!resp.ok) throw new Error('Worker error ' + resp.status);
  const data = await resp.json();
  const raw  = ((data.content?.[0]?.text)||'[]').replace(/```json|```/g,'').trim();
  const arr  = JSON.parse(raw);
  return Array.isArray(arr) ? arr.map(t => {
    const isDetail = t.screen === 'detail';
    const numOrNull = v => (v === null || v === undefined || v === '') ? null : (parseFloat(String(v).replace(',','.'))||0);
    const dinamico = isDetail ? numOrNull(t.dinamico) : (parseFloat(String(t.dinamico||0).replace(',','.'))||0);
    const tip = parseFloat(String(t.gorjeta||0).replace(',','.'))||0;
    const pedagioVal = parseFloat(String(t.pedagio||0).replace(',','.'))||0;
    // serviceFee: número real da taxa (0.15 = 15%). taxaZero mantido por
    // compat (código antigo), mas agora sempre derivado do serviceFee real.
    const hasFee = t.serviceFee !== undefined && t.serviceFee !== null && t.serviceFee !== '';
    const serviceFee = hasFee ? parseFloat(String(t.serviceFee).replace(',','.'))||0 : null;
    // Plataforma: confia no que a IA identificou pela imagem. Só cai no
    // botão que o usuário tocou (plat) se a IA não devolveu nada — rede de
    // segurança, não a fonte principal (era o bug antigo: botão errado
    // salvava tudo com a plataforma trocada).
    const detected = String(t.platform||'').toLowerCase();
    const platform = detected === '99' ? '99' : detected === 'uber' ? 'uber'
      : (plat && plat.toLowerCase()==='99' ? '99' : 'uber');
    // Rótulo do trajeto: quando a IA não acha bairro (endereço só com
    // rua/estrada + CEP + cidade), usa a primeira parte do endereço
    // completo, abreviada — melhor "Estr. Teresópolis x Fri…" que "—".
    const shortPlace = s => {
      const seg = String(s||'').split(',')[0].trim();
      return seg.length > 24 ? seg.slice(0,23).trimEnd()+'…' : seg;
    };
    let oFull, dFull, origin, dest;
    if (isDetail) {
      // Fragmento — pode não ter endereço neste frame específico; null fica null.
      oFull = t.originFull || null; dFull = t.destFull || null;
      origin = t.origin ? String(t.origin).trim() : (oFull ? shortPlace(oFull) : null);
      dest   = t.dest   ? String(t.dest).trim()   : (dFull ? shortPlace(dFull) : null);
    } else {
      oFull = t.originFull || t.origin || ''; dFull = t.destFull || t.dest || '';
      origin = (t.origin||'').trim() || shortPlace(oFull);
      dest   = (t.dest||'').trim()   || shortPlace(dFull);
    }
    return {...t, platform, origin, dest,
      value:parseFloat(String(t.value||0).replace(',','.'))||0,
      km: isDetail ? numOrNull(t.km) : (parseFloat(String(t.km||0).replace(',','.'))||0),
      duration: isDetail ? (t.duration==null ? null : (parseInt(t.duration)||0)) : (parseInt(t.duration)||0),
      dinamico, tip, surge: dinamico > 0,
      pedagioValor: pedagioVal, pedagio: pedagioVal > 0,
      originFull: oFull, destFull: dFull,
      serviceFee: serviceFee,
      taxaZero: serviceFee === 0 || t.taxaZero===true||t.taxaZero==='true'||t.taxaZero===1,
      dinheiro: isDetail
        ? (t.dinheiro===true||t.dinheiro==='true'||t.dinheiro===1 ? true : (t.dinheiro===false||t.dinheiro==='false' ? false : null))
        : (t.dinheiro===true||t.dinheiro==='true'||t.dinheiro===1),
      time: isDetail ? (t.time ? normTime(t.time) : null) : t.time };
  }) : [];
}


function buildProgressHTML(current, total, found) {
  return '<div style="text-align:center;padding:28px 16px">' +
    '<div style="font-size:28px;animation:vspin 1s linear infinite;display:inline-block">&#9881;</div>' +
    '<div style="margin-top:12px;font-size:14px;font-weight:700">' + (current||'0') + ' de ' + total + ' ' + (typeof total==='number'&&total>1?'imagens':'frame') + '</div>' +
    '<div style="font-size:12px;color:#64748B;margin-top:4px">' + found + ' corridas encontradas até agora</div>' +
    '<div style="height:6px;background:#E2E8F0;border-radius:3px;margin-top:14px;overflow:hidden">' +
    '<div style="height:100%;background:#22C55E;border-radius:3px;width:' + (total&&typeof total==='number'?Math.round(current/total*100):50) + '%;transition:width .4s"></div></div></div>';
}


function mergeTrips(existing, newTrips) {
  const all = [...existing];
  for (const t of newTrips) {
    const dup = all.find(e =>
      e.time === t.time &&
      Math.abs((parseFloat(e.value)||0) - (parseFloat(t.value)||0)) < 0.5
    );
    if (!dup) all.push(t);
  }
  return all;
}



// Mostra o que foi decla
===== vídeo @ 837382 =====
_sel?chkColor:'#E2E8F0'};background:${t._sel?chkColor:'transparent'};display:flex;align-items:center;justify-content:center;color:#fff;font-size:13px;flex-shrink:0" id="ict-${i}">${t._sel?'✓':''}</div>
    <div style="flex:1;min-width:0"><div style="font-size:12px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${t.origin||'—'} → ${t.dest||'—'}${newBadge}</div><div style="font-size:10px;color:#64748B;margin-top:2px">${platTag} · ${t.time||'—'} · ${t.duration||0}min${t.km?' · '+t.km+'km':''}</div>${exRow}</div>
    <div style="font-family:'Inter',system-ui,sans-serif;font-weight:700;font-size:14px;color:${t._isNew?'#16A34A':'#0F172A'};flex-shrink:0;text-align:right">${valHtml}</div>
  </div>`;
}
function showImportConfirm() {
  const trips = window._pendingTrips || [];
  if (!trips.length) { closeMo('mo-import-confirm'); showToast('Nada pra atualizar — nenhuma corrida com valor diferente ou nova.', 'info'); return; }
  document.getElementById('ic-title').textContent = 'Corridas pra importar ('+trips.length+')';
  document.getElementById('ic-btn').style.display = '';
  updateImportButtonLabel();

  // Dois grupos, na ordem do mockup aprovado (16/08/2026): primeiro o que
  // já existia e só corrige valor (verde), depois o que é novo — nunca
  // visto na captura ao vivo, vira "importada" (âmbar).
  const changedRows = [], newRows = [];
  trips.forEach((t,i) => { (t._isNew ? newRows : changedRows).push(importRowHtml(t,i)); });

  let groupsHtml = '';
  if (changedRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#22C55E"></span><span style="font-size:12.5px;font-weight:800">Valor vai mudar</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${changedRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0;margin-bottom:${newRows.length?'10px':'0'}">${changedRows.join('')}</div>`;
  }
  if (newRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#F59E0B"></span><span style="font-size:12.5px;font-weight:800">Novas</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${newRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0">${newRows.join('')}</div>`;
  }

  document.getElementById('ic-body').innerHTML = '<div id="ic-declared">' + importDeclaredHtml() + '</div>' +
    '<div id="ic-summary">' + importSummaryHtml() + '</div>' + groupsHtml;
}
function toggleImportTrip(i) {
  const t = (window._pendingTrips||[])[i]; if (!t) return;
  t._sel = !t._sel;
  const chkColor = t._isNew ? '#F59E0B' : '#22C55E';
  const el = document.getElementById('ict-'+i);
  if (el) { el.style.background=t._sel?chkColor:'transparent'; el.style.borderColor=t._sel?chkColor:'#E2E8F0'; el.textContent=t._sel?'✓':''; }
  updateImportButtonLabel();
  updateImportSummary();
}

// Detecta corrida duplicada (hora+valor, hora+km, valor+rota)
// Versão do código — atualizar para verificar se cache está correto
const _APP_CODE_VER = "v24-trip99-fix";

// Normaliza texto de rota pra comparação: minúsculas, sem acentos, sem
// espaços extras — "Barra do Imbuí" e "barra do imbui" viram iguais.
// (OCR de frames diferentes do mesmo vídeo varia acentuação → sem isso,
// a mesma corrida escapa do detector de duplicata.)
function _normRoute(s) {
  return (s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')
    .replace(/\s+/g,' ').trim();
}
function isTripDuplicate(newT, existing) {
  return existing.some(t => {
    // Plataformas diferentes = corridas diferentes (nunca duplicata)
    const samePlat = (t.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const sameTime = t.time && newT.time && t.time === newT.time;
    const sameVal  = Math.abs((t.value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && t.km > 0 && Math.abs((t.km||0)-(newT.km||0)) < 0.5;
    // 99 não reporta km — usar origem+destino como sinal de rota nesse caso
    // (comparação normalizada: acento/caixa não separam corridas)
    const sameRoute = !!(t.origin && newT.origin && t.dest && newT.dest
                       && _normRoute(t.origin) === _normRoute(newT.origin)
                       && _normRoute(t.dest) === _normRoute(newT.dest));
    const nearTime = t.time && newT.time && (() => {
      const [h1,m1] = t.time.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    // Mesmo horário + mesmo valor = duplicata clara
    if (sameTime && sameVal) return true;
    // Mesmo horário EXATO + mesma rota = duplicata mesmo com valor diferente.
    // Protege contra fusão de frames no OCR de vídeo (rota de uma corrida +
    // valor do cartão vizinho). Duas corridas reais no MESMO minuto com a
    // MESMA origem e MESMO destino não existem na prática.
    if (sameTime && sameRoute) return true;
    // Mesmo horário + mesma km (proteção contra OCR com valor diferente)
    if (sameTime && sameKm && newT.km > 0) return true;
    // Próximo horário + mesmo valor + mesma km = duplicata provável
    if (sameVal && nearTime && sameKm) return true;
    // Próximo horário + mesmo valor + mesma rota (sem km, ex: 99) = duplicata provável
    if (sameVal && nearTime && sameRoute) return true;
    // REMOVIDO: sameVal && sameRoute sem tempo = falso positivo entre plataformas
    return false;
  });
}

// ── Casar corrida importada com corrida automática já capturada ────────────
// Pedido do Yuri (23/07/2026): ao importar, se a corrida já foi capturada
// automaticamente, ATUALIZA a automática com o valor real (confirmado pela
// tela de histórico da plataforma — mais confiável que a oferta estimada),
// em vez de criar uma corrida manual duplicada. Só corridas GENUINAMENTE
// novas (sem correspondência) seguem pro fluxo normal de criar.
// Mesmos critérios do isTripDuplicate acima, adaptados pros campos de
// auto_trips (trip_started_at em vez de time, offer_value em vez de value,
// real_km_trip em vez de km, origin_address/dest_address em vez de
// origin/dest) — time da automática convertido pro fuso local em HH:MM.
function _autoTripLocalTime(at) {
  const ts = at.trip_started_at || at.accepted_at;
  if (!ts) return null;
  const dt = new Date(ts);
  return String(dt.getHours()).padStart(2,'0')+':'+String(dt.getMinutes()).padStart(2,'0');
}
function matchAutoTrip(newT, autoTrips) {
  return autoTrips.find(at => {
    const samePlat = (at.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const atTime = _autoTripLocalTime(at);
    const sameTime = atTime && newT.time && atTime === newT.time;
    const sameVal  = Math.abs((at.offer_value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && at.real_km_trip > 0 && Math.abs((at.real_km_trip||0)-(newT.km||0)) < 0.5;
    const sameRoute = !!(at.origin_address && newT.origin && at.dest_address && newT.dest
                       && _normRoute(at.origin_address).includes(_normRoute(newT.origin))
                       && _normRoute(at.dest_address).includes(_normRoute(newT.dest)));
    const nearTime = atTime && newT.time && (() => {
      const [h1,m1] = atTime.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    if (sameTime && sameVal) return true;
    if (sameTime && sameRoute) return true;
    if (sameTime && sameKm && newT.km > 0) return true;
    if (sameVal && nearTime && sameKm) return true;
    if (sameVal && nearTime && sameRoute) return true;
    return false;
  }) || null;
}

// ── Reconciliação de sessões × corridas ───────────────────────────────────
// Regra híbrida: < 15 min → agrupa na sessão + ajusta horários
//                >= 15 min → cria nova sessão
function reconcileSessionsForDay(dateStr, dryRun = false) {
  const THRESHOLD = 15 * 60 * 1000; // 15 minutos em ms
  const day = DB.day(dateStr);
  
  const trips = (day.trips || [])
    .map(t => {
      let ts = t.ts || 0;
      if (!ts && t.time) ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
      return { ...t, _ts: ts };
    })
    .filter(t => t._ts > 0)
    .sort((a, b) => a._ts - b._ts);
  
  if (!trips.length) return { grouped: 0, created: 0, expanded: 0, sessions: day.sessions || [] };
  
  // Cópia profunda das sessões para não mutar durante dry run
  const sessions = JSON.parse(JSON.stringify(day.sessions || []));
  let grouped = 0, created = 0, expanded = 0;
  
  for (const trip of trips) {
    const ts  = trip._ts;
    const dur = (trip.duration || 15) * 60 * 1000;
    const end = ts + dur;
    
    // Já está dentro de uma sessão existente?
    const inside = sessions.find(s =>
      s.start && ts >= (s.start - 1000) && (!s.end || ts <= (s.end + 1000))
    );
    if (inside) continue;
    
    // Sessão mais próxima (distância ao início ou fim)
    let best = null, bestDist = Infinity;
    for (const s of sessions) {
      if (!s.start) continue;
      const d = Math.min(
        Math.abs(ts - s.start),
        s.end ? Math.abs(ts - s.end) : Math.abs(ts - s.start),
        s.end ? Math.abs(end - s.end) : Infinity
      );
      if (d < bestDist) { bestDist = d; best = s; }
    }
    
    if (best && bestDist < THRESHOLD) {
      // Agrupar + expandir horários da sessão
      if (ts < best.start) { best.start = ts; expanded++; }
      if (!best.end || end > best.end) { best.end = end; expanded++; }
      grouped++;
    } else {
      // Criar nova sessão para cobrir esta corrida
      sessions.push({
        id: DB.uid(),
        start: ts,
        end:   end,
        km:    parseFloat((trip.km || 0).toFixed(3)),
        elap
===== vídeo @ 838887 =====
changedRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#22C55E"></span><span style="font-size:12.5px;font-weight:800">Valor vai mudar</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${changedRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0;margin-bottom:${newRows.length?'10px':'0'}">${changedRows.join('')}</div>`;
  }
  if (newRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#F59E0B"></span><span style="font-size:12.5px;font-weight:800">Novas</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${newRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0">${newRows.join('')}</div>`;
  }

  document.getElementById('ic-body').innerHTML = '<div id="ic-declared">' + importDeclaredHtml() + '</div>' +
    '<div id="ic-summary">' + importSummaryHtml() + '</div>' + groupsHtml;
}
function toggleImportTrip(i) {
  const t = (window._pendingTrips||[])[i]; if (!t) return;
  t._sel = !t._sel;
  const chkColor = t._isNew ? '#F59E0B' : '#22C55E';
  const el = document.getElementById('ict-'+i);
  if (el) { el.style.background=t._sel?chkColor:'transparent'; el.style.borderColor=t._sel?chkColor:'#E2E8F0'; el.textContent=t._sel?'✓':''; }
  updateImportButtonLabel();
  updateImportSummary();
}

// Detecta corrida duplicada (hora+valor, hora+km, valor+rota)
// Versão do código — atualizar para verificar se cache está correto
const _APP_CODE_VER = "v24-trip99-fix";

// Normaliza texto de rota pra comparação: minúsculas, sem acentos, sem
// espaços extras — "Barra do Imbuí" e "barra do imbui" viram iguais.
// (OCR de frames diferentes do mesmo vídeo varia acentuação → sem isso,
// a mesma corrida escapa do detector de duplicata.)
function _normRoute(s) {
  return (s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')
    .replace(/\s+/g,' ').trim();
}
function isTripDuplicate(newT, existing) {
  return existing.some(t => {
    // Plataformas diferentes = corridas diferentes (nunca duplicata)
    const samePlat = (t.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const sameTime = t.time && newT.time && t.time === newT.time;
    const sameVal  = Math.abs((t.value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && t.km > 0 && Math.abs((t.km||0)-(newT.km||0)) < 0.5;
    // 99 não reporta km — usar origem+destino como sinal de rota nesse caso
    // (comparação normalizada: acento/caixa não separam corridas)
    const sameRoute = !!(t.origin && newT.origin && t.dest && newT.dest
                       && _normRoute(t.origin) === _normRoute(newT.origin)
                       && _normRoute(t.dest) === _normRoute(newT.dest));
    const nearTime = t.time && newT.time && (() => {
      const [h1,m1] = t.time.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    // Mesmo horário + mesmo valor = duplicata clara
    if (sameTime && sameVal) return true;
    // Mesmo horário EXATO + mesma rota = duplicata mesmo com valor diferente.
    // Protege contra fusão de frames no OCR de vídeo (rota de uma corrida +
    // valor do cartão vizinho). Duas corridas reais no MESMO minuto com a
    // MESMA origem e MESMO destino não existem na prática.
    if (sameTime && sameRoute) return true;
    // Mesmo horário + mesma km (proteção contra OCR com valor diferente)
    if (sameTime && sameKm && newT.km > 0) return true;
    // Próximo horário + mesmo valor + mesma km = duplicata provável
    if (sameVal && nearTime && sameKm) return true;
    // Próximo horário + mesmo valor + mesma rota (sem km, ex: 99) = duplicata provável
    if (sameVal && nearTime && sameRoute) return true;
    // REMOVIDO: sameVal && sameRoute sem tempo = falso positivo entre plataformas
    return false;
  });
}

// ── Casar corrida importada com corrida automática já capturada ────────────
// Pedido do Yuri (23/07/2026): ao importar, se a corrida já foi capturada
// automaticamente, ATUALIZA a automática com o valor real (confirmado pela
// tela de histórico da plataforma — mais confiável que a oferta estimada),
// em vez de criar uma corrida manual duplicada. Só corridas GENUINAMENTE
// novas (sem correspondência) seguem pro fluxo normal de criar.
// Mesmos critérios do isTripDuplicate acima, adaptados pros campos de
// auto_trips (trip_started_at em vez de time, offer_value em vez de value,
// real_km_trip em vez de km, origin_address/dest_address em vez de
// origin/dest) — time da automática convertido pro fuso local em HH:MM.
function _autoTripLocalTime(at) {
  const ts = at.trip_started_at || at.accepted_at;
  if (!ts) return null;
  const dt = new Date(ts);
  return String(dt.getHours()).padStart(2,'0')+':'+String(dt.getMinutes()).padStart(2,'0');
}
function matchAutoTrip(newT, autoTrips) {
  return autoTrips.find(at => {
    const samePlat = (at.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const atTime = _autoTripLocalTime(at);
    const sameTime = atTime && newT.time && atTime === newT.time;
    const sameVal  = Math.abs((at.offer_value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && at.real_km_trip > 0 && Math.abs((at.real_km_trip||0)-(newT.km||0)) < 0.5;
    const sameRoute = !!(at.origin_address && newT.origin && at.dest_address && newT.dest
                       && _normRoute(at.origin_address).includes(_normRoute(newT.origin))
                       && _normRoute(at.dest_address).includes(_normRoute(newT.dest)));
    const nearTime = atTime && newT.time && (() => {
      const [h1,m1] = atTime.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    if (sameTime && sameVal) return true;
    if (sameTime && sameRoute) return true;
    if (sameTime && sameKm && newT.km > 0) return true;
    if (sameVal && nearTime && sameKm) return true;
    if (sameVal && nearTime && sameRoute) return true;
    return false;
  }) || null;
}

// ── Reconciliação de sessões × corridas ───────────────────────────────────
// Regra híbrida: < 15 min → agrupa na sessão + ajusta horários
//                >= 15 min → cria nova sessão
function reconcileSessionsForDay(dateStr, dryRun = false) {
  const THRESHOLD = 15 * 60 * 1000; // 15 minutos em ms
  const day = DB.day(dateStr);
  
  const trips = (day.trips || [])
    .map(t => {
      let ts = t.ts || 0;
      if (!ts && t.time) ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
      return { ...t, _ts: ts };
    })
    .filter(t => t._ts > 0)
    .sort((a, b) => a._ts - b._ts);
  
  if (!trips.length) return { grouped: 0, created: 0, expanded: 0, sessions: day.sessions || [] };
  
  // Cópia profunda das sessões para não mutar durante dry run
  const sessions = JSON.parse(JSON.stringify(day.sessions || []));
  let grouped = 0, created = 0, expanded = 0;
  
  for (const trip of trips) {
    const ts  = trip._ts;
    const dur = (trip.duration || 15) * 60 * 1000;
    const end = ts + dur;
    
    // Já está dentro de uma sessão existente?
    const inside = sessions.find(s =>
      s.start && ts >= (s.start - 1000) && (!s.end || ts <= (s.end + 1000))
    );
    if (inside) continue;
    
    // Sessão mais próxima (distância ao início ou fim)
    let best = null, bestDist = Infinity;
    for (const s of sessions) {
      if (!s.start) continue;
      const d = Math.min(
        Math.abs(ts - s.start),
        s.end ? Math.abs(ts - s.end) : Math.abs(ts - s.start),
        s.end ? Math.abs(end - s.end) : Infinity
      );
      if (d < bestDist) { bestDist = d; best = s; }
    }
    
    if (best && bestDist < THRESHOLD) {
      // Agrupar + expandir horários da sessão
      if (ts < best.start) { best.start = ts; expanded++; }
      if (!best.end || end > best.end) { best.end = end; expanded++; }
      grouped++;
    } else {
      // Criar nova sessão para cobrir esta corrida
      sessions.push({
        id: DB.uid(),
        start: ts,
        end:   end,
        km:    parseFloat((trip.km || 0).toFixed(3)),
        elapsed: Math.round((trip.duration || 15) * 60)
      });
      sessions.sort((a, b) => (a.start || 0) - (b.start || 0));
      created++;
    }
  }
  
  if (!dryRun) {
    day.sessions = sessions;
    DB.saveDay(day);
    // Sincronizar novas/atualizadas sessões com Supabase
    if (_supaUser) {
      sessions.forEach(s => {
        _SUPA.from('sessions').upsert({
          id: s.id, user_id: _supaUser.id, date: dateStr,
          start_ms: s.start, end_ms: s.end,
          km: s.km || 0, elapsed_secs: s.elapsed || 0,
          paused_ms: 0, is_paused: false
        }, { onConflict: 'id' }).catch(() => {});
      });
    }
  }
  
  return { grouped, created, expanded, sessions };
}

// Rodar reconciliação em todos os dias com corridas (migração única)
async function reconcileAllExistingDays() {
  if (localStorage.getItem('sm_session_reconcile_v1')) return;
  
  const dates = DB.allDates();
  let totalGrouped = 0, totalCreated = 0;
  
  for (const dateStr of dates) {
    const day = DB.day(dateStr);
    if (!(day.trips || []).length) continue;
    const r = reconcileSessionsForDay(dateStr);
    totalGrouped += r.grouped;
    totalCreated += r.created;
  }
  
  localStorage.setItem('sm_session_reconcile_v1', '1');
  if (totalCreated > 0 || totalGrouped > 0) {
    showToast(
      `${totalCreated} sessão(ões) criada(s) e ${totalGrouped} corrida(s) agrupada(s) automaticamente.`,
      'info', 5000
    );
    setTimeout(() => { try { renderJornada(); } catch(e) {} }, 80);
  }
}

async fun
===== vídeo @ 846268 =====
rip.duration || 15) * 60 * 1000;
    const end = ts + dur;
    
    // Já está dentro de uma sessão existente?
    const inside = sessions.find(s =>
      s.start && ts >= (s.start - 1000) && (!s.end || ts <= (s.end + 1000))
    );
    if (inside) continue;
    
    // Sessão mais próxima (distância ao início ou fim)
    let best = null, bestDist = Infinity;
    for (const s of sessions) {
      if (!s.start) continue;
      const d = Math.min(
        Math.abs(ts - s.start),
        s.end ? Math.abs(ts - s.end) : Math.abs(ts - s.start),
        s.end ? Math.abs(end - s.end) : Infinity
      );
      if (d < bestDist) { bestDist = d; best = s; }
    }
    
    if (best && bestDist < THRESHOLD) {
      // Agrupar + expandir horários da sessão
      if (ts < best.start) { best.start = ts; expanded++; }
      if (!best.end || end > best.end) { best.end = end; expanded++; }
      grouped++;
    } else {
      // Criar nova sessão para cobrir esta corrida
      sessions.push({
        id: DB.uid(),
        start: ts,
        end:   end,
        km:    parseFloat((trip.km || 0).toFixed(3)),
        elapsed: Math.round((trip.duration || 15) * 60)
      });
      sessions.sort((a, b) => (a.start || 0) - (b.start || 0));
      created++;
    }
  }
  
  if (!dryRun) {
    day.sessions = sessions;
    DB.saveDay(day);
    // Sincronizar novas/atualizadas sessões com Supabase
    if (_supaUser) {
      sessions.forEach(s => {
        _SUPA.from('sessions').upsert({
          id: s.id, user_id: _supaUser.id, date: dateStr,
          start_ms: s.start, end_ms: s.end,
          km: s.km || 0, elapsed_secs: s.elapsed || 0,
          paused_ms: 0, is_paused: false
        }, { onConflict: 'id' }).catch(() => {});
      });
    }
  }
  
  return { grouped, created, expanded, sessions };
}

// Rodar reconciliação em todos os dias com corridas (migração única)
async function reconcileAllExistingDays() {
  if (localStorage.getItem('sm_session_reconcile_v1')) return;
  
  const dates = DB.allDates();
  let totalGrouped = 0, totalCreated = 0;
  
  for (const dateStr of dates) {
    const day = DB.day(dateStr);
    if (!(day.trips || []).length) continue;
    const r = reconcileSessionsForDay(dateStr);
    totalGrouped += r.grouped;
    totalCreated += r.created;
  }
  
  localStorage.setItem('sm_session_reconcile_v1', '1');
  if (totalCreated > 0 || totalGrouped > 0) {
    showToast(
      `${totalCreated} sessão(ões) criada(s) e ${totalGrouped} corrida(s) agrupada(s) automaticamente.`,
      'info', 5000
    );
    setTimeout(() => { try { renderJornada(); } catch(e) {} }, 80);
  }
}

async function confirmImport() {
  const selected = (window._pendingTrips||[]).filter(t=>t._sel);
  if (!selected.length) { closeMo('mo-import-confirm'); return; }
  const day = DB.day(S.curDate);
  let added=0, skipped=0, updated=0;
  const toUpdateAuto = [];
  const toPushAuto = [];
  const videoId = (window._pendingImportVideoIds||[])[0] || null;
  selected.forEach(t => {
    // Já casada com uma auto_trip existente (matching já feito em
    // processImportBatch, antes da tela abrir) — só atualiza o valor,
    // nunca o endereço (esse já veio validado pela captura ao vivo/GPS).
    if (t._matchedAutoTripId) {
      toUpdateAuto.push({ id: t._matchedAutoTripId, value: parseFloat(t.value)||0, km: parseFloat(t.km)||0,
        dinheiro: !!t.dinheiro });
      updated++;
      return;
    }
    // Nova — nunca vista na captura ao vivo. Vira auto_trips já Confirmada
    // pelo vídeo; 'Importada' agora é somente a tag de origem — unifica com o resto dos
    // dados em vez de virar corrida "manual" separada. Endereço já
    // passou pela validação looksLikeAddress lá em processImportBatch
    // (se não passou, já veio vazio aqui — nunca inventa).
    let startMs;
    try {
      const opDate = window._pendingImportOperationalDate || S.curDate || DB.today();
      startMs = operationalTimestampForTime(t.platform || window._pendingImportPlatform, opDate, t.time||'00:00');
    } catch(e) { startMs = NaN; }
    if (!startMs || isNaN(startMs)) startMs = Date.now();
    const endMs = t.duration > 0 ? startMs + t.duration*60000 : null;
    toPushAuto.push({
      user_id: _supaUser.id,
      platform: t.platform === '99' ? '99' : 'uber',
      accepted_at: new Date(startMs).toISOString(),
      trip_started_at: new Date(startMs).toISOString(),
      trip_ended_at: endMs ? new Date(endMs).toISOString() : null,
      offer_value: parseFloat(t.value)||0,
      real_km_trip: parseFloat(t.km)||0,
      origin_address: t.originFull || null,
      dest_address: t.destFull || null,
      dinheiro: !!t.dinheiro,
      status: 'confirmada',
      capture_source: 'importada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId
    });
    added++;
  });
  // Aplica as atualizações nas automáticas casadas — valor real por cima do
  // estimado, km real quando a importação trouxe (senão mantém o que já
  // tinha), status vira confirmada (agora tem valor confirmado de verdade).
  toUpdateAuto.forEach(u => {
    _SUPA.from('auto_trips').update({
      offer_value: u.value,
      real_km_trip: u.km > 0 ? u.km : undefined,
      dinheiro: u.dinheiro,
      status: 'confirmada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId || undefined
    }).eq('id', u.id).then(() => {}).catch(() => {});
  });
  // Corridas novas — insere de uma vez em auto_trips.
  if (toPushAuto.length) {
    _SUPA.from('auto_trips').insert(toPushAuto).then(() => {}).catch((e) => console.warn('insert importada falhou', e));
  }
  // Atualiza a contagem de corridas confirmadas/criadas por esse vídeo —
  // só informativo, útil pra revisão manual depois.
  if (videoId) {
    _SUPA.from('import_videos').update({ trip_count: toUpdateAuto.length + toPushAuto.length }).eq('id', videoId).then(()=>{}).catch(()=>{});
  }
  window._pendingImportVideoIds = null;
  if (toUpdateAuto.length || toPushAuto.length) { S.autoTripsDate = null; } // força recarregar no próximo renderJornada
  DB.saveDay(day); closeMo('mo-import-confirm'); window._pendingTrips=null;

  const dateHint = S.curDate !== DB.today()
    ? ` (data: ${S.curDate.split('-').reverse().slice(0,2).join('/')})` : '';
  const parts = [];
  if (added > 0) parts.push(`${added} corrida${added!==1?'s':''} nova${added!==1?'s':''} importada${added!==1?'s':''}${dateHint}`);
  if (updated > 0) parts.push(`${updated} já capturada${updated!==1?'s':''} automaticamente, valor confirmado`);
  if (skipped > 0) parts.push(`${skipped} ignorada${skipped!==1?'s':''} (já existente${skipped!==1?'s':''})`);
  const baseMsg = parts.length ? parts.join('. ')+'.' : 'Nenhuma corrida nova.';

  try{renderJornada();}catch(e){}
  try{renderHome();}catch(e){}
  // Garantir que Corridas mostra a data certa independente do filtro
  if (S.curDate !== DB.today()) S.corridasPeriod = 'tudo';
  try{renderCorridasActive();}catch(e){}
  showToast(baseMsg, skipped > 0 ? 'info' : 'success');
}

// ══════════════════════════════════════════════
// IMPORTAÇÃO: histórico oficial do Uber (.zip)
// driver_lifetime_trips-0.csv já traz, por corrida, o valor líquido
// (driver_upfront_fare_local), km e duração — usamos direto daí, sem
// tentar casar com driver_payments por horário (testado com um export
// real: o casamento por horário troca valores entre corridas vizinhas).
// driver_payments-0.csv só é usado pra somar gorjeta/incentivo do
// período inteiro, como informação, sem tentar atribuir por corrida.
// ══════════════════════════════════════════════

function uzRound2(n) { return Math.round(n * 100) / 100; }

// Aceita "10.50" e "10,50" — exportações da Uber variam o separador por região/locale.
function uzNum(raw) {
  if (raw == null || raw === '') return 0;
  return parseFloat(String(raw).replace(',', '.')) || 0;
}

function uzParseTs(raw) {
  if (!raw) return null;
  // O timestamp do zip vem em UTC (tem 'Z' no final) — converte pra Brasília (UTC-3)
  const raw2 = String(raw).trim();
  try {
    const d = new Date(raw2.includes('T') ? raw2 : raw2.replace(' ', 'T') + 'Z');
    if (isNaN(d.getTime())) return null;
    // subtrai 3h pra Brasília
    const brt = new Date(d.getTime() - 3 * 60 * 60 * 1000);
    const pad = n => String(n).padStart(2, '0');
    const dateStr = brt.getUTCFullYear() + '-' + pad(brt.getUTCMonth()+1) + '-' + pad(brt.getUTCDate());
    const timeStr = pad(brt.getUTCHours()) + ':' + pad(brt.getUTCMinutes());
    return { dateStr, timeStr };
  } catch(e) { return null; }
}

// Soma gorjeta e incentivo/promoção do período inteiro (informativo, não por corrida).
function uzAggregatePayments(rows) {
  let tip = 0, promo = 0;
  for (const r of rows) {
    const amt = uzNum(r['Local Amount']);
    if (r['Category'] === 'tip') tip += amt;
    if (r['Category'] === 'existing_driver_incentive') promo += amt;
  }
  return { tip: uzRound2(tip), promo: uzRound2(promo) };
}

// Monta as corridas concluídas direto do driver_lifetime_trips. Corridas canceladas
// (status diferente de 'completed') são ignoradas — não são corridas de fato.
function uzBuildTrips(rows) {
  const trips = [];
  for (const r of rows) {
    if (r['status'] !== 'completed') continue;
    const begin = uzParseTs(r['begintrip_timestamp_local']) || uzParseTs(r['request_timestamp_local']);
    if (!begin) continue;
    const value = uzNum(r['driver_upfront_fare_local']) || uzNum(r['original_fare_local']);
    trips.push({
      date: begin.dateStr, time: begin.timeStr,
      value: uzRound2(Math.max(0, value)),
      km: r['trip_distance_miles'] ? uzRound2(uzNum(r['trip_distance_miles']) * 1.60934) : 0,
      // o app guarda duração em MINUTOS (a IA de import por foto também usa minutos);
      // o export da Uber vem em segundos, por isso a divisão por 60 aqui.
      duration: r['trip_duration_seconds'] ? Math.round(uzNum(r['trip_duration_seconds']) / 60) : 0,
      category: r['global_product_name'] || '',
      surge: r[
===== vídeo @ 848465 =====
ionsForDay(dateStr);
    totalGrouped += r.grouped;
    totalCreated += r.created;
  }
  
  localStorage.setItem('sm_session_reconcile_v1', '1');
  if (totalCreated > 0 || totalGrouped > 0) {
    showToast(
      `${totalCreated} sessão(ões) criada(s) e ${totalGrouped} corrida(s) agrupada(s) automaticamente.`,
      'info', 5000
    );
    setTimeout(() => { try { renderJornada(); } catch(e) {} }, 80);
  }
}

async function confirmImport() {
  const selected = (window._pendingTrips||[]).filter(t=>t._sel);
  if (!selected.length) { closeMo('mo-import-confirm'); return; }
  const day = DB.day(S.curDate);
  let added=0, skipped=0, updated=0;
  const toUpdateAuto = [];
  const toPushAuto = [];
  const videoId = (window._pendingImportVideoIds||[])[0] || null;
  selected.forEach(t => {
    // Já casada com uma auto_trip existente (matching já feito em
    // processImportBatch, antes da tela abrir) — só atualiza o valor,
    // nunca o endereço (esse já veio validado pela captura ao vivo/GPS).
    if (t._matchedAutoTripId) {
      toUpdateAuto.push({ id: t._matchedAutoTripId, value: parseFloat(t.value)||0, km: parseFloat(t.km)||0,
        dinheiro: !!t.dinheiro });
      updated++;
      return;
    }
    // Nova — nunca vista na captura ao vivo. Vira auto_trips já Confirmada
    // pelo vídeo; 'Importada' agora é somente a tag de origem — unifica com o resto dos
    // dados em vez de virar corrida "manual" separada. Endereço já
    // passou pela validação looksLikeAddress lá em processImportBatch
    // (se não passou, já veio vazio aqui — nunca inventa).
    let startMs;
    try {
      const opDate = window._pendingImportOperationalDate || S.curDate || DB.today();
      startMs = operationalTimestampForTime(t.platform || window._pendingImportPlatform, opDate, t.time||'00:00');
    } catch(e) { startMs = NaN; }
    if (!startMs || isNaN(startMs)) startMs = Date.now();
    const endMs = t.duration > 0 ? startMs + t.duration*60000 : null;
    toPushAuto.push({
      user_id: _supaUser.id,
      platform: t.platform === '99' ? '99' : 'uber',
      accepted_at: new Date(startMs).toISOString(),
      trip_started_at: new Date(startMs).toISOString(),
      trip_ended_at: endMs ? new Date(endMs).toISOString() : null,
      offer_value: parseFloat(t.value)||0,
      real_km_trip: parseFloat(t.km)||0,
      origin_address: t.originFull || null,
      dest_address: t.destFull || null,
      dinheiro: !!t.dinheiro,
      status: 'confirmada',
      capture_source: 'importada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId
    });
    added++;
  });
  // Aplica as atualizações nas automáticas casadas — valor real por cima do
  // estimado, km real quando a importação trouxe (senão mantém o que já
  // tinha), status vira confirmada (agora tem valor confirmado de verdade).
  toUpdateAuto.forEach(u => {
    _SUPA.from('auto_trips').update({
      offer_value: u.value,
      real_km_trip: u.km > 0 ? u.km : undefined,
      dinheiro: u.dinheiro,
      status: 'confirmada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId || undefined
    }).eq('id', u.id).then(() => {}).catch(() => {});
  });
  // Corridas novas — insere de uma vez em auto_trips.
  if (toPushAuto.length) {
    _SUPA.from('auto_trips').insert(toPushAuto).then(() => {}).catch((e) => console.warn('insert importada falhou', e));
  }
  // Atualiza a contagem de corridas confirmadas/criadas por esse vídeo —
  // só informativo, útil pra revisão manual depois.
  if (videoId) {
    _SUPA.from('import_videos').update({ trip_count: toUpdateAuto.length + toPushAuto.length }).eq('id', videoId).then(()=>{}).catch(()=>{});
  }
  window._pendingImportVideoIds = null;
  if (toUpdateAuto.length || toPushAuto.length) { S.autoTripsDate = null; } // força recarregar no próximo renderJornada
  DB.saveDay(day); closeMo('mo-import-confirm'); window._pendingTrips=null;

  const dateHint = S.curDate !== DB.today()
    ? ` (data: ${S.curDate.split('-').reverse().slice(0,2).join('/')})` : '';
  const parts = [];
  if (added > 0) parts.push(`${added} corrida${added!==1?'s':''} nova${added!==1?'s':''} importada${added!==1?'s':''}${dateHint}`);
  if (updated > 0) parts.push(`${updated} já capturada${updated!==1?'s':''} automaticamente, valor confirmado`);
  if (skipped > 0) parts.push(`${skipped} ignorada${skipped!==1?'s':''} (já existente${skipped!==1?'s':''})`);
  const baseMsg = parts.length ? parts.join('. ')+'.' : 'Nenhuma corrida nova.';

  try{renderJornada();}catch(e){}
  try{renderHome();}catch(e){}
  // Garantir que Corridas mostra a data certa independente do filtro
  if (S.curDate !== DB.today()) S.corridasPeriod = 'tudo';
  try{renderCorridasActive();}catch(e){}
  showToast(baseMsg, skipped > 0 ? 'info' : 'success');
}

// ══════════════════════════════════════════════
// IMPORTAÇÃO: histórico oficial do Uber (.zip)
// driver_lifetime_trips-0.csv já traz, por corrida, o valor líquido
// (driver_upfront_fare_local), km e duração — usamos direto daí, sem
// tentar casar com driver_payments por horário (testado com um export
// real: o casamento por horário troca valores entre corridas vizinhas).
// driver_payments-0.csv só é usado pra somar gorjeta/incentivo do
// período inteiro, como informação, sem tentar atribuir por corrida.
// ══════════════════════════════════════════════

function uzRound2(n) { return Math.round(n * 100) / 100; }

// Aceita "10.50" e "10,50" — exportações da Uber variam o separador por região/locale.
function uzNum(raw) {
  if (raw == null || raw === '') return 0;
  return parseFloat(String(raw).replace(',', '.')) || 0;
}

function uzParseTs(raw) {
  if (!raw) return null;
  // O timestamp do zip vem em UTC (tem 'Z' no final) — converte pra Brasília (UTC-3)
  const raw2 = String(raw).trim();
  try {
    const d = new Date(raw2.includes('T') ? raw2 : raw2.replace(' ', 'T') + 'Z');
    if (isNaN(d.getTime())) return null;
    // subtrai 3h pra Brasília
    const brt = new Date(d.getTime() - 3 * 60 * 60 * 1000);
    const pad = n => String(n).padStart(2, '0');
    const dateStr = brt.getUTCFullYear() + '-' + pad(brt.getUTCMonth()+1) + '-' + pad(brt.getUTCDate());
    const timeStr = pad(brt.getUTCHours()) + ':' + pad(brt.getUTCMinutes());
    return { dateStr, timeStr };
  } catch(e) { return null; }
}

// Soma gorjeta e incentivo/promoção do período inteiro (informativo, não por corrida).
function uzAggregatePayments(rows) {
  let tip = 0, promo = 0;
  for (const r of rows) {
    const amt = uzNum(r['Local Amount']);
    if (r['Category'] === 'tip') tip += amt;
    if (r['Category'] === 'existing_driver_incentive') promo += amt;
  }
  return { tip: uzRound2(tip), promo: uzRound2(promo) };
}

// Monta as corridas concluídas direto do driver_lifetime_trips. Corridas canceladas
// (status diferente de 'completed') são ignoradas — não são corridas de fato.
function uzBuildTrips(rows) {
  const trips = [];
  for (const r of rows) {
    if (r['status'] !== 'completed') continue;
    const begin = uzParseTs(r['begintrip_timestamp_local']) || uzParseTs(r['request_timestamp_local']);
    if (!begin) continue;
    const value = uzNum(r['driver_upfront_fare_local']) || uzNum(r['original_fare_local']);
    trips.push({
      date: begin.dateStr, time: begin.timeStr,
      value: uzRound2(Math.max(0, value)),
      km: r['trip_distance_miles'] ? uzRound2(uzNum(r['trip_distance_miles']) * 1.60934) : 0,
      // o app guarda duração em MINUTOS (a IA de import por foto também usa minutos);
      // o export da Uber vem em segundos, por isso a divisão por 60 aqui.
      duration: r['trip_duration_seconds'] ? Math.round(uzNum(r['trip_duration_seconds']) / 60) : 0,
      category: r['global_product_name'] || '',
      surge: r['is_surged'] === 'true',
      airport: r['is_airport_trip'] === 'true',
      // dinamico junta tarifa dinamica (surge) + promocao/desconto da corrida num
      // valor só — já incluso no "value", mostrado separado só pra detalhar a corrida.
      // Pode ficar negativo se o desconto for maior que o surge.
      dinamico:    uzRound2(Math.max(0, uzNum(r['surge_fare_local'])) + uzNum(r['promotion_local'])),
      dinheiro:   r['is_cash_trip'] === 'true',
      pedagio:    uzNum(r['toll_amount_local']) > 0,
      serviceFee: (r['service_fee'] !== undefined && r['service_fee'] !== '') ? parseFloat(r['service_fee']) : null,
      taxaZero:   r['service_fee'] === '0' || r['service_fee'] === '0.0',
    });
  }
  return trips;
}

function uzFindEntry(zip, suffix) {
  const f = Object.values(zip.files).find(x => !x.dir && x.name.endsWith(suffix));
  if (!f) throw new Error('Não encontrei "' + suffix + '" no zip. Confira se é o arquivo certo da Uber.');
  return f;
}

let _uzResult = null;

async function handleUberZipFile(input) {
  const file = input.files[0]; if (!file) return;
  input.value = '';
  openMo('mo-uberzip');
  document.getElementById('uz-proc').style.display = 'block';
  document.getElementById('uz-res').style.display = 'none';
  const stepEl = document.getElementById('uz-proc-step');
  const subEl  = document.getElementById('uz-proc-sub');
  const barEl  = document.getElementById('uz-proc-bar');
  const setStep = (msg, pct) => { if (stepEl) stepEl.textContent = msg; if (barEl) barEl.style.width = pct + '%'; };

  try {
    setStep('Lendo arquivo .zip...', 10);
    const zip = await JSZip.loadAsync(file);
    const paymentsEntry = uzFindEntry(zip, 'driver_payments-0.csv');
    const tripsEntry     = uzFindEntry(zip, 'driver_lifetime_trips-0.csv');

    setStep('Lendo corridas...', 35);
    const [paymentsCsv, tripsCsv] = await Promise.all([
      paymentsEntry.async('string'), tripsEntry.async('string')
    ]);
    const paymentsRows = Papa.parse(paymentsCsv, { header: true, skipEmptyLines: true }).data;
    const tripsRows     = Papa.parse(tripsCsv, { header: true, skipEmptyLines: true }).data;

    setStep('Organizando corridas concluídas...', 55);
===== vídeo @ 903660 =====
, end] da sessão.
  const trips = (day.trips || []).filter(t => {
    let ts = t.ts || 0;
    if (!ts && t.time) ts = new Date(day.date + 'T' + t.time + ':00').getTime();
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  const hasTrips = trips.length > 0;
  const th = Math.floor(totalElapsed/3600), tm = Math.floor(totalElapsed%3600/60);
  const kmh = totalElapsed > 0 ? (totalKm/(totalElapsed/3600)) : 0;

  const kpiCard = (v,l) => `<div style="background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.1);border-radius:12px;padding:9px 8px;text-align:center">
      <div style="font-family:'Poppins',sans-serif;font-weight:700;font-size:16px">${v}</div>
      <div style="font-size:9.5px;color:rgba(255,255,255,.5);font-weight:600;margin-top:2px;text-transform:uppercase;letter-spacing:.04em">${l}</div>
    </div>`;

  let cardInner, extraSection, footer;

  if (!hasTrips) {
    cardInner = `
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
        <span style="font-size:11px;font-weight:700;color:rgba(255,255,255,.55);text-transform:uppercase;letter-spacing:.06em">Resumo da sessão</span>
        <span style="font-size:10px;font-weight:800;background:rgba(34,197,94,.18);color:#4ADE80;border:1px solid rgba(74,222,128,.35);border-radius:99px;padding:3px 10px">ENCERRADA</span>
      </div>
      <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:32px;line-height:1;letter-spacing:-.01em">${th}h ${p2(tm)}min</div>
      <div style="font-size:11px;color:rgba(255,255,255,.55);font-weight:600;margin-top:4px">tempo online hoje</div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px">
        ${totalKm > 0.1 ? kpiCard(totalKm.toFixed(1), 'Km percorrido') : kpiCard('—', 'Km percorrido')}
        ${totalKm > 0.1 ? kpiCard(kmh.toFixed(1), 'Km/h médio') : kpiCard('—', 'Km/h médio')}
      </div>`;
    extraSection = `
      <div class="cv-daily" style="display:flex;gap:10px;background:#FFFBEB;border:1px solid #FDE68A;border-radius:12px;padding:11px 12px;margin:12px 0;align-items:flex-start">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#D97706" stroke-width="2.4" stroke-linecap="round" style="flex:none;margin-top:1px"><circle cx="12" cy="12" r="9"/><path d="M12 8v4l2.5 2.5"/></svg>
        <span style="font-size:12px;color:#92400E;font-weight:500;line-height:1.45">Km ativo, corridas, ganhos, R$/h, R$/km e meta aparecem aqui depois que você <b>importar as corridas de hoje</b>.</span>
      </div>`;
    footer = `
      <button onclick="closeJourneySummary(); ${needsMidnight} ? openPostStopMidnightModal(window._jsSummarySess, openImportPrecheck) : openImportPrecheck()" style="width:100%;height:48px;border:none;border-radius:14px;font-family:'Inter',system-ui,sans-serif;font-size:14px;font-weight:800;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:8px;background:#22C55E;color:#fff;box-shadow:0 6px 16px rgba(34,197,94,.35)">
        <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M7 10l5 5 5-5"/><path d="M12 15V3"/></svg>
        Importar corridas de hoje
      </button>
      <div style="font-size:11px;color:var(--muted);text-align:center;line-height:1.5;margin-top:8px;padding:0 6px">Escolha as fotos/vídeos do histórico Uber ou 99 — pode selecionar vários de uma vez.</div>
      <button onclick="closeJourneySummary(); if(${needsMidnight}) openPostStopMidnightModal(window._jsSummarySess, null)" style="width:100%;height:40px;border:none;background:transparent;color:var(--muted);font-weight:700;font-size:13px;margin-top:4px;cursor:pointer">Deixar para depois</button>`;
  } else {
    const ganhos = trips.reduce((s,t)=>s+(t.value||0),0);
    const kmAtivo = trips.reduce((s,t)=>s+(t.km||0),0);
    const kmDesl = Math.max(0, totalKm - kmAtivo);
    const rsh = totalElapsed>0 ? ganhos/(totalElapsed/3600) : 0;
    const rskm = totalKm>0 ? ganhos/totalKm : 0;
    const uberTrips = trips.filter(t=>(t.platform||'uber')==='uber');
    const p99Trips  = trips.filter(t=>t.platform==='99');
    const uberR = uberTrips.reduce((s,t)=>s+(t.value||0),0);
    const p99R  = p99Trips.reduce((s,t)=>s+(t.value||0),0);

    const dow = new Date(day.date+'T12:00').getDay();
    const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
    const meta = DB.meta();
    const ov = day.metaOverride || null;
    const dimMes = new Date(new Date(day.date+'T12:00').getFullYear(), new Date(day.date+'T12:00').getMonth()+1, 0).getDate();
    const revTotal = ganhos + (day.revenues||[]).reduce((s,r)=>s+(r.amount||0),0);
    const moneyGoal = (ov && ov.money) ? ov.money : ((plan && plan.moneyGoal) ? plan.moneyGoal : (meta.general ? meta.general/dimMes : 0));

    let metaHtml = '';
    if (moneyGoal) {
      const pct = Math.round(revTotal/moneyGoal*100);
      metaHtml = `
        <div style="margin-top:14px">
          <div style="display:flex;justify-content:space-between;font-size:11px;font-weight:700;margin-bottom:6px">
            <span style="color:rgba(255,255,255,.6)">Meta do dia · ${DB.fRI(moneyGoal)}</span>
            <span style="color:#4ADE80">${pct}%${pct>=100?' &#10003;':''}</span>
          </div>
          <div style="height:8px;border-radius:99px;background:rgba(255,255,255,.12);overflow:hidden">
            <div style="height:100%;border-radius:99px;background:linear-gradient(90deg,#22C55E,#4ADE80);width:${Math.min(100,pct)}%"></div>
          </div>
        </div>`;
    }

    cardInner = `
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
        <span style="font-size:11px;font-weight:700;color:rgba(255,255,255,.55);text-transform:uppercase;letter-spacing:.06em">Ganhos do dia</span>
        <span style="font-size:10px;font-weight:800;background:rgba(34,197,94,.18);color:#4ADE80;border:1px solid rgba(74,222,128,.35);border-radius:99px;padding:3px 10px">${trips.length} CORRIDA${trips.length===1?'':'S'}</span>
      </div>
      <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:32px;line-height:1;letter-spacing:-.01em">${DB.fRI(ganhos)}</div>
      <div style="font-size:11px;color:rgba(255,255,255,.55);font-weight:600;margin-top:4px">bruto · ${th}h ${p2(tm)}min online</div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px">
        ${kpiCard(kmAtivo.toFixed(1), 'Km ativo')}
        ${kpiCard(kmDesl.toFixed(1), 'Km desloc.')}
        ${kpiCard(DB.fRI(rsh).replace('R$ ',''), 'R$ / hora')}
        ${kpiCard(rskm.toFixed(2).replace('.',','), 'R$ / km')}
      </div>
      ${metaHtml}`;

    extraSection = `
      <div style="font-size:11px;font-weight:800;color:var(--muted);text-transform:uppercase;letter-spacing:.05em;margin:14px 0 6px">Uber / 99</div>
      <div style="display:flex;gap:8px;margin-bottom:14px">
        <div style="flex:1;border:1px solid var(--line);border-radius:12px;padding:10px 12px;background:var(--soft-line)">
          <div style="font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.05em;color:var(--muted)">Uber</div>
          <div style="font-family:'Poppins',sans-serif;font-weight:700;font-size:15px;color:var(--text);margin-top:2px">${DB.fRI(uberR)}</div>
          <div style="font-size:10px;color:var(--muted2);font-weight:600">${uberTrips.length} corrida${uberTrips.length===1?'':'s'}</div>
        </div>
        <div style="flex:1;border:1px solid var(--line);border-radius:12px;padding:10px 12px;background:var(--soft-line)">
          <div style="font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.05em;color:var(--muted)">99</div>
          <div style="font-family:'Poppins',sans-serif;font-weight:700;font-size:15px;color:var(--text);margin-top:2px">${DB.fRI(p99R)}</div>
          <div style="font-size:10px;color:var(--muted2);font-weight:600">${p99Trips.length} corrida${p99Trips.length===1?'':'s'}</div>
        </div>
      </div>`;

    footer = `<button onclick="closeJourneySummary()" style="width:100%;height:48px;border:none;border-radius:14px;font-family:'Inter',system-ui,sans-serif;font-size:14px;font-weight:800;cursor:pointer;background:#0A2F6B;color:#fff">Fechar</button>`;
  }

  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:flex-end;justify-content:center;background:rgba(15,23,42,.45)';
  const dateLabel = new Date(day.date+'T12:00').toLocaleDateString('pt-BR',{weekday:'long',day:'numeric',month:'long'});
  el.innerHTML = `
    <div style="background:var(--card);border-radius:24px 24px 0 0;padding:10px 18px 26px;box-shadow:0 -12px 40px rgba(10,47,107,.35);max-width:420px;width:100%;max-height:88vh;overflow-y:auto;font-family:'Inter',system-ui,sans-serif">
      <div style="width:44px;height:5px;border-radius:99px;background:var(--line);margin:0 auto 14px"></div>
      <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:18px;color:var(--text);text-align:center">${hasTrips ? 'Resumo do dia' : 'Jornada encerrada'}</div>
      <div style="font-size:12px;color:var(--muted2);text-align:center;margin:3px 0 16px;font-weight:500">${dateLabel}</div>
      <div style="background:linear-gradient(145deg,#0A2F6B 0%,#0D3E8C 60%,#0A2F6B 100%);border-radius:18px;padding:16px;color:#fff;margin-bottom:12px">
        ${cardInner}
      </div>
      ${extraSection}
      ${footer}
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', (e) => { if (e.target === el) closeJourneySummary(); });
}
// Confirmação central (substitui o confirm() nativo do navegador), com a identidade visu
===== video @ 498524 =====
Pck+ajpxo/A95E8AwBz4rL+QK0xQJEsdow5f0rBKpeQxjumquXqKaFPQgYimQgkr5ALUGorU+3HznMyzVNPnmG/1cp7kXJM9N1FBl0BE0DgjU4rESwhBaWistpasumHpe5IOZ2HAJFof8HxpSql744rxMAAAAASUVORK5CYII=">
          <b style="font-size:11px;font-weight:800;color:#64748B">Corridas</b>
        </div>
        <div style="display:flex;align-items:center;justify-content:center;border:1.5px solid #E7EBF1;border-radius:10px;padding:0 8px;height:40px;gap:3px">
          <input id="ipc-99-count" type="number" placeholder="0" style="border:0;outline:0;flex:1;font-size:14px;font-weight:700;color:#0F172A;font-family:inherit;min-width:0;text-align:center">
        </div>
        <div style="font-size:9px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.04em;text-align:center">Valor total</div>
        <div style="display:flex;align-items:center;justify-content:center;border:1.5px solid #E7EBF1;border-radius:10px;padding:0 8px;height:40px;gap:3px">
          <span style="font-size:11px;color:#64748B;font-weight:700;flex-shrink:0">R$</span>
          <input id="ipc-99-value" type="number" placeholder="0,00" style="border:0;outline:0;flex:1;font-size:14px;font-weight:700;color:#0F172A;font-family:inherit;min-width:0;text-align:center">
        </div>
      </div>
    </div>

    <div style="display:flex;gap:7px;background:#EFF6FF;border:1px solid rgba(14,165,233,.2);border-radius:11px;padding:9px 10px;font-size:11px;color:#0369A1;line-height:1.45;margin-bottom:12px">
      <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" style="flex:none;margin-top:1px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
      <span>Serve pra conferir se o import achou tudo — não precisa ser o dia inteiro.</span>
    </div>
    <button class="btn-p" style="margin-top:0" onclick="importPrecheckNext()">Avançar</button>
    <div style="text-align:center;font-size:12px;color:#2563EB;font-weight:700;margin-top:10px;cursor:pointer" onclick="importPrecheckSkip()">Pular essa parte →</div>
  </div>
</div>

<div class="mo" id="mo-import">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2>Importar corridas</h2><button class="xbtn" onclick="closeMo('mo-import')">&#x2715;</button></div>
    <div style="background:#EFF6FF;border:1px solid rgba(14,165,233,.2);border-radius:16px;padding:12px 14px;font-size:12px;color:#0369A1;line-height:1.6;display:flex;gap:8px;margin-bottom:16px">
      <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" style="flex-shrink:0;margin-top:2px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
      <div><strong>Como usar:</strong> grave a tela do celular rolando o histórico (Uber ou 99, tanto faz) e mande o arquivo de vídeo aqui — não dá mais pra mandar print solto. O app identifica a plataforma sozinho e extrai as corridas.</div>
    </div>
    <div style="display:flex;gap:8px;margin-bottom:8px">
      <label style="flex:1;height:56px;border-radius:14px;background:#F0F4FA;border:1px solid #E2E8F0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;cursor:pointer;font-size:11px;font-weight:700;color:#0A2F6B">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="m3 9 4-4 4 4 4-4 4 4"/></svg> Vídeo da galeria
        <input type="file" multiple accept="video/*" style="display:none" onclick="this.value=null" onchange="handleImport(this,'uber')">
      </label>
      <label style="flex:1;height:56px;border-radius:14px;background:#F0F4FA;border:1px solid #E2E8F0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;cursor:pointer;font-size:11px;font-weight:700;color:#0A2F6B">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg> Arquivos
        <input type="file" multiple accept="video/*" style="display:none" onclick="this.value=null" onchange="handleImport(this,'uber')">
      </label>
    </div>
    <div style="font-size:11px;color:#94A3B8;text-align:center;margin-bottom:14px">Só vídeo — grave a tela rolando o histórico e mande o arquivo aqui. Dá pra escolher vários de uma vez.</div>

    <label style="width:100%;height:50px;border-radius:14px;background:linear-gradient(135deg,#0A2F6B,#22C55E);display:flex;align-items:center;justify-content:center;gap:8px;cursor:pointer;font-size:12.5px;font-weight:700;color:#fff;box-shadow:0 8px 18px rgba(10,47,107,.25);position:relative;margin-bottom:8px">
      <span style="position:absolute;top:-8px;right:-6px;background:#F59E0B;color:#fff;font-size:8.5px;font-weight:900;padding:3px 6px;border-radius:999px;letter-spacing:.02em">NOVO</span>
      <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M17 8 12 3 7 8"/><path d="M12 3v12"/></svg>
      Histórico oficial do Uber (.zip)
      <input type="file" accept=".zip" style="display:none" onclick="this.value=null" onchange="handleUberZipFile(this)">
    </label>
    <div style="font-size:11px;color:#94A3B8;text-align:center;line-height:1.5;margin-bottom:12px">Baixe em: app Uber → Conta → Configurações de privacidade → "Solicitar meus dados". Você recebe um e-mail com o arquivo .zip.</div>

    <div style="display:flex;gap:6px;justify-content:center">
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px;display:flex;align-items:center;gap:4px"><i style="width:6px;height:6px;border-radius:50%;display:inline-block;background:#0EA5E9"></i> Uber</span>
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px;display:flex;align-items:center;gap:4px"><i style="width:6px;height:6px;border-radius:50%;display:inline-block;background:#F59E0B"></i> 99</span>
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px">identificados automaticamente</span>
    </div>
  </div>
</div>

<!-- Import confirm -->
<div class="mo" id="mo-import-confirm">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2 id="ic-title">Corridas detectadas</h2><button class="xbtn" onclick="closeMo('mo-import-confirm')">&#x2715;</button></div>
    <div id="ic-body" style="min-height:80px"></div>
    <div style="display:flex;gap:8px;padding-top:8px">
      <button class="btn-s" onclick="closeMo('mo-import-confirm')" style="margin-top:0">Cancelar</button>
      <button class="btn-p" id="ic-btn" onclick="confirmImport()" style="margin-top:0">Importar</button>
    </div>
  </div>
</div>

<!-- Uber ZIP: processando + resultado -->
<div class="mo ctr" id="mo-uberzip">
  <div class="sheet ctr" style="max-width:400px">
    <div id="uz-proc" style="padding:50px 20px;text-align:center">
      <div style="font-size:32px;display:inline-block;animation:uzspin 1.1s linear infinite">&#9881;</div>
      <div id="uz-proc-step" style="font-size:14px;font-weight:800;margin-top:16px">Lendo arquivo...</div>
      <div id="uz-proc-sub" style="font-size:12px;color:#64748B;margin-top:5px">Isso só leva alguns segundos</div>
      <div style="height:8px;background:#EEF2F7;border-radius:99px;overflow:hidden;margin-top:20px">
        <div id="uz-proc-bar" style="height:100%;background:linear-gradient(90deg,#0EA5E9,#22C55E);border-radius:99px;width:8%;transition:width .4s"></div>
      </div>
    </div>

    <div id="uz-res" style="display:none">
      <div style="text-align:center;padding:4px 0 16px">
        <div style="width:52px;height:52px;border-radius:16px;background:#ECFDF3;color:#16A34A;display:flex;align-items:center;justify-content:center;margin:0 auto 10px">
          <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6 9 17l-5-5"/></svg>
        </div>
        <div style="font-size:16px;font-weight:800;letter-spacing:-.02em">Histórico conferido</div>
        <div id="uz-res-sub" style="font-size:12px;color:#64748B;margin-top:3px"></div>
      </div>

      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px">
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Novas pra importar</span>
          <strong id="uz-k-novas" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;color:#16A34A;margin-top:4px">0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Já existiam</span>
          <strong id="uz-k-dup" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;color:#64748B;margin-top:4px">0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Valor das novas</span>
          <strong id="uz-k-valor" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;margin-top:4px">R$ 0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Período</span>
          <strong id="uz-k-periodo" style="display:block;font-size:12.5px;margin-top:5px">—</strong>
    
===== video @ 778509 =====
ount: null, uberValue: null, p99Count: null, p99Value: null };

function openImportPrecheck() {
  ['ipc-uber-count','ipc-uber-value','ipc-99-count','ipc-99-value'].forEach(id => {
    const el = document.getElementById(id); if (el) el.value = '';
  });
  openMo('mo-import-precheck');
}
function _readPrecheckField(id) {
  const v = document.getElementById(id)?.value;
  return (v === '' || v === undefined || v === null) ? null : parseFloat(String(v).replace(',','.'));
}
function importPrecheckNext() {
  importPrecheck = {
    uberCount: _readPrecheckField('ipc-uber-count'),
    uberValue: _readPrecheckField('ipc-uber-value'),
    p99Count:  _readPrecheckField('ipc-99-count'),
    p99Value:  _readPrecheckField('ipc-99-value')
  };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}
function importPrecheckSkip() {
  importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}

function renderImportRing(found, target, foundVal, targetVal, statusHtml, isWarn) {
  const hasTarget = target > 0;
  const pct = hasTarget ? Math.min(100, found/target*100) : 0;
  const deg = Math.round(pct*3.6);
  const ringColor = isWarn ? '#F59E0B' : '#22C55E';
  const ringBg = hasTarget ? `conic-gradient(${ringColor} 0deg ${deg}deg, #E2E8F0 ${deg}deg 360deg)` : '#E2E8F0';
  const countHtml = hasTarget ? `${found}<span style="font-size:16px;color:#94A3B8">/${target}</span>` : `${found}`;
  const valHtml = hasTarget ? `${DB.fRI(foundVal)} de ${DB.fRI(targetVal)} já identificados` : `${DB.fRI(foundVal)} identificados até agora`;
  return `<div style="display:flex;flex-direction:column;align-items:center;padding:30px 0 10px">
    <div style="width:130px;height:130px;border-radius:50%;background:${ringBg};display:flex;align-items:center;justify-content:center;margin-bottom:20px;position:relative;transition:background .4s">
      <div style="position:absolute;inset:8px;border-radius:50%;background:#fff"></div>
      <div style="position:relative;text-align:center">
        <div style="font-family:'Poppins',sans-serif;font-weight:900;font-size:28px;color:#0F172A">${countHtml}</div>
        <div style="font-size:10px;color:#64748B;font-weight:700;margin-top:2px">CORRIDAS</div>
      </div>
    </div>
    <div style="font-size:13.5px;font-weight:700;color:#0F172A;margin-bottom:5px;display:flex;align-items:center;gap:6px">${statusHtml}</div>
    <div style="font-size:12px;color:#64748B">${valHtml}</div>
  </div>`;
}

// Tenta de novo (até 3x) antes de desistir de um print/frame — evita que um
// erro passageiro de rede derrube a corrida inteira do import silenciosamente.
async function extractWithRetry(fn, onRetry, maxAttempts) {
  maxAttempts = maxAttempts || 3;
  let lastErr;
  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    try { return await fn(); }
    catch(e) {
      lastErr = e;
      if (attempt < maxAttempts) { onRetry(attempt+1, maxAttempts); await new Promise(r => setTimeout(r, 800)); }
    }
  }
  throw lastErr;
}

// Sobe o vídeo original pro Storage (bucket import-videos, retenção de
// 180 dias) e registra em import_videos — pedido do Yuri (16/08/2026):
// base pra confirmação de valor real (campeonatos/incentivos futuros) e
// pra dar dá pra conferir manualmente qualquer corrida em disputa. Falha
// de upload NUNCA trava o import (retorna null, segue sem vínculo) — o
// vídeo é auditoria extra, não pode ser bloqueio de fluxo.
async function uploadImportVideo(file, plat, operationalDate) {
  if (!_supaUser) return null;
  try {
    const ext = (file.name && file.name.includes('.')) ? file.name.split('.').pop().toLowerCase() : 'mp4';
    const opDate = operationalDate || S.curDate || DB.today();
    const opPlat = normalizeOperationalPlatform(plat);
    const opRange = operationalRangeForPlatform(opPlat, opDate);
    const path = `${_supaUser.id}/${opDate}/${opPlat}/${DB.uid()}.${ext}`;
    const { error: upErr } = await _SUPA.storage.from('import-videos').upload(path, file, {
      contentType: file.type || 'video/mp4'
    });
    if (upErr) { console.warn('upload vídeo falhou (segue sem vínculo):', upErr); return null; }
    const { data, error } = await _SUPA.from('import_videos').insert({
      user_id: _supaUser.id, storage_path: path, file_size_bytes: file.size || null,
      platform: opPlat, operational_date: opDate,
      period_start: new Date(opRange.startMs).toISOString(),
      period_end: new Date(opRange.endMs).toISOString()
    }).select('id').single();
    if (error) { console.warn('registro import_videos falhou (segue sem vínculo):', error); return null; }
    return data.id;
  } catch(e) { console.warn('uploadImportVideo erro:', e); return null; }
}
async function processImportBatch(images, videos, plat) {
  const operationalDate = S.curDate || DB.today();
  const operationalPlat = normalizeOperationalPlatform(plat);
  window._pendingImportOperationalDate = operationalDate;
  window._pendingImportPlatform = operationalPlat;
  const targetCount = (importPrecheck.uberCount||0) + (importPrecheck.p99Count||0);
  const targetValue = (importPrecheck.uberValue||0) + (importPrecheck.p99Value||0);
  const totalImgs = images.length, totalVids = videos.length;
  const label = [totalImgs?`${totalImgs} print${totalImgs>1?'s':''}`:'', totalVids?`${totalVids} vídeo${totalVids>1?'s':''}`:''].filter(Boolean).join(' + ');
  document.getElementById('ic-title').textContent = 'Analisando ' + label;
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');

  const unique = [];
  const details = [];
  const seen = new Set();
  let foundCount = 0, foundVal = 0;
  let curDetail = null; // acumulador do fragmento de detalhe em aberto
  const flushDetail = () => { if (curDetail) { details.push(curDetail); curDetail = null; } };
  const mergeDetailFrag = (t) => {
    // Decide se é o MESMO cartão de detalhe (outro pedaço do mesmo scroll)
    // usando horário e endereço — nunca valor. Valor podia vir ligeiramente
    // diferente entre frames ("Você ganhou" vs "Total") e isso fazia o
    // mesmo cartão virar DOIS fragmentos, que depois viravam duas corridas
    // (uma real + uma fantasma) no import.
    const conflicts = (a, b) => a != null && b != null && a !== b;
    const sameRide = curDetail && (
      (t.time && curDetail.time) ? t.time === curDetail.time
        : (!conflicts(curDetail.originFull, t.originFull) && !conflicts(curDetail.destFull, t.destFull))
    );
    if (sameRide) {
      // mesmo cartão — preenche só o que esse fragmento ainda não tinha
      if (curDetail.km == null && t.km != null) curDetail.km = t.km;
      if (curDetail.duration == null && t.duration != null) curDetail.duration = t.duration;
      if (!curDetail.time && t.time) curDetail.time = t.time;
      if (!curDetail.originFull && t.originFull) { curDetail.originFull = t.originFull; curDetail.origin = t.origin; }
      if (!curDetail.destFull && t.destFull) { curDetail.destFull = t.destFull; curDetail.dest = t.dest; }
    } else {
      // horário ou endereço batendo diferente do acumulador — é outra
      // corrida: fecha o anterior e abre um novo
      flushDetail();
      curDetail = t;
    }
  };
  // Valida e deduplica NA HORA, frame a frame — o contador do anel só sobe
  // com corrida nova validada. Antes somava a extração bruta de cada frame,
  // e como os frames de vídeo se sobrepõem, a mesma corrida contava várias
  // vezes (ex: 37/17 na tela sendo que eram 17 corridas reais).
  const absorb = (trips) => {
    let sawList = false;
    (trips||[]).forEach(t => {
      if (t.screen === 'detail') {
        // Tela de detalhe: só km/tempo/endereço interessam — sem valor.
        // Se o fragmento não trouxe NENHUM desses três, não vale a pena.
        if (t.km == null && t.duration == null && !t.originFull && !t.destFull) return;
        mergeDetailFrag(t); return;
      }
      if (!(t.value > 0)) return; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
      sawList = true;
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seen.has(key)) return;
      seen.add(key);
      unique.push(t);
      foundCount++; foundVal += (t.value||0);
    });
    if (sawList) flushDetail(); // voltou pra tela de lista — fecha o detalhe que tava em aberto
  };
  const dot = '<span style="width:6px;height:6px;border-radius:50%;background:#22C55E;display:inline-block;animation:pulseDot 1s infinite ease-in-out"></span>';
  const retryIcon = '<svg style="animation:vspin 1s linear infinite" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#D97706" stroke-width="2.5"><path d="M21 12a9 9 0 1 1-2.6-6.4"/></svg>';
  const updateRing = (statusHtml, isWarn) => {
    document.getElementById('ic-body').innerHTML = renderImportRing(foundCount, targetCount, foundVal, targetValue, statusHtml, isWarn);
  };
  updateRing('Iniciando ' + dot, false);

  for (let i = 0; i < images.length; i++) {
    try {
      const trips = await extractWithRetry(
        () => extractTripsFromImage(images[i], plat),
        () => updateRing(retryIcon + ' Processando novamente...', true)
      );
      absorb(trips);
    } catch(e) { console.warn('Erro print '+(i+1)+' (desistiu após 3 tentativas):', e); }
    updateRing('Analisando print' + (totalImgs>1 ? ' ' + Math.min(i+2,totalImgs) + ' de ' + totalImgs : '') + ' ' + dot, false);
  }
  let videoFrameReadFailed = false;
  for (let v = 0; v < videos.length; v++) {
    try {
      updateRing('Preparando vídeo ' + dot, false);
      const frames = await extractVideoFrames(videos[v], (done, total) => {
        updateRing('Preparando vídeo · ' + done + '/' + total + ' quadros ' + dot, false);
      });
      for (let i = 0; i < frames.length; i++) {
        try {
          const trips = await extractWithRetry(
            () => extractTripsFromBase64(frames[i], 'image/jpeg', plat),
            () => updateRing(retryIcon + ' Processando novamente.
===== video @ 779591 =====
rget > 0;
  const pct = hasTarget ? Math.min(100, found/target*100) : 0;
  const deg = Math.round(pct*3.6);
  const ringColor = isWarn ? '#F59E0B' : '#22C55E';
  const ringBg = hasTarget ? `conic-gradient(${ringColor} 0deg ${deg}deg, #E2E8F0 ${deg}deg 360deg)` : '#E2E8F0';
  const countHtml = hasTarget ? `${found}<span style="font-size:16px;color:#94A3B8">/${target}</span>` : `${found}`;
  const valHtml = hasTarget ? `${DB.fRI(foundVal)} de ${DB.fRI(targetVal)} já identificados` : `${DB.fRI(foundVal)} identificados até agora`;
  return `<div style="display:flex;flex-direction:column;align-items:center;padding:30px 0 10px">
    <div style="width:130px;height:130px;border-radius:50%;background:${ringBg};display:flex;align-items:center;justify-content:center;margin-bottom:20px;position:relative;transition:background .4s">
      <div style="position:absolute;inset:8px;border-radius:50%;background:#fff"></div>
      <div style="position:relative;text-align:center">
        <div style="font-family:'Poppins',sans-serif;font-weight:900;font-size:28px;color:#0F172A">${countHtml}</div>
        <div style="font-size:10px;color:#64748B;font-weight:700;margin-top:2px">CORRIDAS</div>
      </div>
    </div>
    <div style="font-size:13.5px;font-weight:700;color:#0F172A;margin-bottom:5px;display:flex;align-items:center;gap:6px">${statusHtml}</div>
    <div style="font-size:12px;color:#64748B">${valHtml}</div>
  </div>`;
}

// Tenta de novo (até 3x) antes de desistir de um print/frame — evita que um
// erro passageiro de rede derrube a corrida inteira do import silenciosamente.
async function extractWithRetry(fn, onRetry, maxAttempts) {
  maxAttempts = maxAttempts || 3;
  let lastErr;
  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    try { return await fn(); }
    catch(e) {
      lastErr = e;
      if (attempt < maxAttempts) { onRetry(attempt+1, maxAttempts); await new Promise(r => setTimeout(r, 800)); }
    }
  }
  throw lastErr;
}

// Sobe o vídeo original pro Storage (bucket import-videos, retenção de
// 180 dias) e registra em import_videos — pedido do Yuri (16/08/2026):
// base pra confirmação de valor real (campeonatos/incentivos futuros) e
// pra dar dá pra conferir manualmente qualquer corrida em disputa. Falha
// de upload NUNCA trava o import (retorna null, segue sem vínculo) — o
// vídeo é auditoria extra, não pode ser bloqueio de fluxo.
async function uploadImportVideo(file, plat, operationalDate) {
  if (!_supaUser) return null;
  try {
    const ext = (file.name && file.name.includes('.')) ? file.name.split('.').pop().toLowerCase() : 'mp4';
    const opDate = operationalDate || S.curDate || DB.today();
    const opPlat = normalizeOperationalPlatform(plat);
    const opRange = operationalRangeForPlatform(opPlat, opDate);
    const path = `${_supaUser.id}/${opDate}/${opPlat}/${DB.uid()}.${ext}`;
    const { error: upErr } = await _SUPA.storage.from('import-videos').upload(path, file, {
      contentType: file.type || 'video/mp4'
    });
    if (upErr) { console.warn('upload vídeo falhou (segue sem vínculo):', upErr); return null; }
    const { data, error } = await _SUPA.from('import_videos').insert({
      user_id: _supaUser.id, storage_path: path, file_size_bytes: file.size || null,
      platform: opPlat, operational_date: opDate,
      period_start: new Date(opRange.startMs).toISOString(),
      period_end: new Date(opRange.endMs).toISOString()
    }).select('id').single();
    if (error) { console.warn('registro import_videos falhou (segue sem vínculo):', error); return null; }
    return data.id;
  } catch(e) { console.warn('uploadImportVideo erro:', e); return null; }
}
async function processImportBatch(images, videos, plat) {
  const operationalDate = S.curDate || DB.today();
  const operationalPlat = normalizeOperationalPlatform(plat);
  window._pendingImportOperationalDate = operationalDate;
  window._pendingImportPlatform = operationalPlat;
  const targetCount = (importPrecheck.uberCount||0) + (importPrecheck.p99Count||0);
  const targetValue = (importPrecheck.uberValue||0) + (importPrecheck.p99Value||0);
  const totalImgs = images.length, totalVids = videos.length;
  const label = [totalImgs?`${totalImgs} print${totalImgs>1?'s':''}`:'', totalVids?`${totalVids} vídeo${totalVids>1?'s':''}`:''].filter(Boolean).join(' + ');
  document.getElementById('ic-title').textContent = 'Analisando ' + label;
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');

  const unique = [];
  const details = [];
  const seen = new Set();
  let foundCount = 0, foundVal = 0;
  let curDetail = null; // acumulador do fragmento de detalhe em aberto
  const flushDetail = () => { if (curDetail) { details.push(curDetail); curDetail = null; } };
  const mergeDetailFrag = (t) => {
    // Decide se é o MESMO cartão de detalhe (outro pedaço do mesmo scroll)
    // usando horário e endereço — nunca valor. Valor podia vir ligeiramente
    // diferente entre frames ("Você ganhou" vs "Total") e isso fazia o
    // mesmo cartão virar DOIS fragmentos, que depois viravam duas corridas
    // (uma real + uma fantasma) no import.
    const conflicts = (a, b) => a != null && b != null && a !== b;
    const sameRide = curDetail && (
      (t.time && curDetail.time) ? t.time === curDetail.time
        : (!conflicts(curDetail.originFull, t.originFull) && !conflicts(curDetail.destFull, t.destFull))
    );
    if (sameRide) {
      // mesmo cartão — preenche só o que esse fragmento ainda não tinha
      if (curDetail.km == null && t.km != null) curDetail.km = t.km;
      if (curDetail.duration == null && t.duration != null) curDetail.duration = t.duration;
      if (!curDetail.time && t.time) curDetail.time = t.time;
      if (!curDetail.originFull && t.originFull) { curDetail.originFull = t.originFull; curDetail.origin = t.origin; }
      if (!curDetail.destFull && t.destFull) { curDetail.destFull = t.destFull; curDetail.dest = t.dest; }
    } else {
      // horário ou endereço batendo diferente do acumulador — é outra
      // corrida: fecha o anterior e abre um novo
      flushDetail();
      curDetail = t;
    }
  };
  // Valida e deduplica NA HORA, frame a frame — o contador do anel só sobe
  // com corrida nova validada. Antes somava a extração bruta de cada frame,
  // e como os frames de vídeo se sobrepõem, a mesma corrida contava várias
  // vezes (ex: 37/17 na tela sendo que eram 17 corridas reais).
  const absorb = (trips) => {
    let sawList = false;
    (trips||[]).forEach(t => {
      if (t.screen === 'detail') {
        // Tela de detalhe: só km/tempo/endereço interessam — sem valor.
        // Se o fragmento não trouxe NENHUM desses três, não vale a pena.
        if (t.km == null && t.duration == null && !t.originFull && !t.destFull) return;
        mergeDetailFrag(t); return;
      }
      if (!(t.value > 0)) return; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
      sawList = true;
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seen.has(key)) return;
      seen.add(key);
      unique.push(t);
      foundCount++; foundVal += (t.value||0);
    });
    if (sawList) flushDetail(); // voltou pra tela de lista — fecha o detalhe que tava em aberto
  };
  const dot = '<span style="width:6px;height:6px;border-radius:50%;background:#22C55E;display:inline-block;animation:pulseDot 1s infinite ease-in-out"></span>';
  const retryIcon = '<svg style="animation:vspin 1s linear infinite" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#D97706" stroke-width="2.5"><path d="M21 12a9 9 0 1 1-2.6-6.4"/></svg>';
  const updateRing = (statusHtml, isWarn) => {
    document.getElementById('ic-body').innerHTML = renderImportRing(foundCount, targetCount, foundVal, targetValue, statusHtml, isWarn);
  };
  updateRing('Iniciando ' + dot, false);

  for (let i = 0; i < images.length; i++) {
    try {
      const trips = await extractWithRetry(
        () => extractTripsFromImage(images[i], plat),
        () => updateRing(retryIcon + ' Processando novamente...', true)
      );
      absorb(trips);
    } catch(e) { console.warn('Erro print '+(i+1)+' (desistiu após 3 tentativas):', e); }
    updateRing('Analisando print' + (totalImgs>1 ? ' ' + Math.min(i+2,totalImgs) + ' de ' + totalImgs : '') + ' ' + dot, false);
  }
  let videoFrameReadFailed = false;
  for (let v = 0; v < videos.length; v++) {
    try {
      updateRing('Preparando vídeo ' + dot, false);
      const frames = await extractVideoFrames(videos[v], (done, total) => {
        updateRing('Preparando vídeo · ' + done + '/' + total + ' quadros ' + dot, false);
      });
      for (let i = 0; i < frames.length; i++) {
        try {
          const trips = await extractWithRetry(
            () => extractTripsFromBase64(frames[i], 'image/jpeg', plat),
            () => updateRing(retryIcon + ' Processando novamente...', true)
          );
          absorb(trips);
        } catch(e) { console.warn('Frame erro (desistiu após 3 tentativas):', e); }
        updateRing('Analisando vídeo · ' + (i+1) + '/' + frames.length + ' quadros ' + dot, false);
      }
    } catch(e) { videoFrameReadFailed = true; console.warn('Erro vídeo '+(v+1)+':', e); }
  }
  if (videos.length && videoFrameReadFailed && unique.length === 0) {
    showToast('Não consegui decodificar os quadros deste vídeo neste aparelho. O arquivo pode ser válido; tente novamente.', 'error');
    closeMo('mo-import-confirm');
    return;
  }

  // Só envia o arquivo original depois que a leitura local terminou.
  // Em celular/WebView, fazer upload e seek/decodificação simultaneamente
  // do mesmo File grande pode bloquear o decoder e deixar a tela parada.
  if (videos.length) updateRing('Salvando vídeo para conferência ' + dot, false);
  const _videoUploadResults = await Promise.all(
    videos.map(file => uploadImportVideo(file, operationalPlat, operationalDate))
  );
  window._pendingImportVideoIds = _videoUploadResults.
===== video @ 784448 =====
do mesmo scroll)
    // usando horário e endereço — nunca valor. Valor podia vir ligeiramente
    // diferente entre frames ("Você ganhou" vs "Total") e isso fazia o
    // mesmo cartão virar DOIS fragmentos, que depois viravam duas corridas
    // (uma real + uma fantasma) no import.
    const conflicts = (a, b) => a != null && b != null && a !== b;
    const sameRide = curDetail && (
      (t.time && curDetail.time) ? t.time === curDetail.time
        : (!conflicts(curDetail.originFull, t.originFull) && !conflicts(curDetail.destFull, t.destFull))
    );
    if (sameRide) {
      // mesmo cartão — preenche só o que esse fragmento ainda não tinha
      if (curDetail.km == null && t.km != null) curDetail.km = t.km;
      if (curDetail.duration == null && t.duration != null) curDetail.duration = t.duration;
      if (!curDetail.time && t.time) curDetail.time = t.time;
      if (!curDetail.originFull && t.originFull) { curDetail.originFull = t.originFull; curDetail.origin = t.origin; }
      if (!curDetail.destFull && t.destFull) { curDetail.destFull = t.destFull; curDetail.dest = t.dest; }
    } else {
      // horário ou endereço batendo diferente do acumulador — é outra
      // corrida: fecha o anterior e abre um novo
      flushDetail();
      curDetail = t;
    }
  };
  // Valida e deduplica NA HORA, frame a frame — o contador do anel só sobe
  // com corrida nova validada. Antes somava a extração bruta de cada frame,
  // e como os frames de vídeo se sobrepõem, a mesma corrida contava várias
  // vezes (ex: 37/17 na tela sendo que eram 17 corridas reais).
  const absorb = (trips) => {
    let sawList = false;
    (trips||[]).forEach(t => {
      if (t.screen === 'detail') {
        // Tela de detalhe: só km/tempo/endereço interessam — sem valor.
        // Se o fragmento não trouxe NENHUM desses três, não vale a pena.
        if (t.km == null && t.duration == null && !t.originFull && !t.destFull) return;
        mergeDetailFrag(t); return;
      }
      if (!(t.value > 0)) return; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
      sawList = true;
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seen.has(key)) return;
      seen.add(key);
      unique.push(t);
      foundCount++; foundVal += (t.value||0);
    });
    if (sawList) flushDetail(); // voltou pra tela de lista — fecha o detalhe que tava em aberto
  };
  const dot = '<span style="width:6px;height:6px;border-radius:50%;background:#22C55E;display:inline-block;animation:pulseDot 1s infinite ease-in-out"></span>';
  const retryIcon = '<svg style="animation:vspin 1s linear infinite" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#D97706" stroke-width="2.5"><path d="M21 12a9 9 0 1 1-2.6-6.4"/></svg>';
  const updateRing = (statusHtml, isWarn) => {
    document.getElementById('ic-body').innerHTML = renderImportRing(foundCount, targetCount, foundVal, targetValue, statusHtml, isWarn);
  };
  updateRing('Iniciando ' + dot, false);

  for (let i = 0; i < images.length; i++) {
    try {
      const trips = await extractWithRetry(
        () => extractTripsFromImage(images[i], plat),
        () => updateRing(retryIcon + ' Processando novamente...', true)
      );
      absorb(trips);
    } catch(e) { console.warn('Erro print '+(i+1)+' (desistiu após 3 tentativas):', e); }
    updateRing('Analisando print' + (totalImgs>1 ? ' ' + Math.min(i+2,totalImgs) + ' de ' + totalImgs : '') + ' ' + dot, false);
  }
  let videoFrameReadFailed = false;
  for (let v = 0; v < videos.length; v++) {
    try {
      updateRing('Preparando vídeo ' + dot, false);
      const frames = await extractVideoFrames(videos[v], (done, total) => {
        updateRing('Preparando vídeo · ' + done + '/' + total + ' quadros ' + dot, false);
      });
      for (let i = 0; i < frames.length; i++) {
        try {
          const trips = await extractWithRetry(
            () => extractTripsFromBase64(frames[i], 'image/jpeg', plat),
            () => updateRing(retryIcon + ' Processando novamente...', true)
          );
          absorb(trips);
        } catch(e) { console.warn('Frame erro (desistiu após 3 tentativas):', e); }
        updateRing('Analisando vídeo · ' + (i+1) + '/' + frames.length + ' quadros ' + dot, false);
      }
    } catch(e) { videoFrameReadFailed = true; console.warn('Erro vídeo '+(v+1)+':', e); }
  }
  if (videos.length && videoFrameReadFailed && unique.length === 0) {
    showToast('Não consegui decodificar os quadros deste vídeo neste aparelho. O arquivo pode ser válido; tente novamente.', 'error');
    closeMo('mo-import-confirm');
    return;
  }

  // Só envia o arquivo original depois que a leitura local terminou.
  // Em celular/WebView, fazer upload e seek/decodificação simultaneamente
  // do mesmo File grande pode bloquear o decoder e deixar a tela parada.
  if (videos.length) updateRing('Salvando vídeo para conferência ' + dot, false);
  const _videoUploadResults = await Promise.all(
    videos.map(file => uploadImportVideo(file, operationalPlat, operationalDate))
  );
  window._pendingImportVideoIds = _videoUploadResults.filter(Boolean);
  if (videos.length && window._pendingImportVideoIds.length < videos.length) {
    showToast('Corridas importadas, mas o vídeo de auditoria não pôde ser salvo — confira sua conexão.', 'error');
  }
  flushDetail(); // fragmento de detalhe que ficou aberto no fim do vídeo

  // Resolve conflitos de horário: a regra pedida pra IA é "cada horário =
  // uma corrida só", mas o dedup acima só barra repetição idêntica
  // (mesmo horário E mesmo valor) — se o mesmo horário veio com valores
  // DIFERENTES em frames diferentes (mistura de cartão vizinho num frame
  // só), os dois passavam pra lista como corridas separadas, inflando a
  // contagem e o total. Aqui, pra cada (plataforma, horário), fica só a
  // leitura de valor que mais se repetiu entre os frames — a leitura
  // certa tende a se confirmar igual em vários frames seguidos (o vídeo
  // se sobrepõe), enquanto uma leitura errada por mistura de cartão
  // costuma aparecer uma vez só.
  function resolveTimeConflicts(list) {
    const groups = new Map();
    list.forEach(t => {
      const k = (t.platform || '') + '_' + t.time;
      (groups.get(k) || groups.set(k, []).get(k)).push(t);
    });
    const resolved = [];
    groups.forEach(items => {
      if (items.length === 1) { resolved.push(items[0]); return; }
      const byValue = new Map();
      items.forEach(t => {
        const vk = (t.value || 0).toFixed(2);
        (byValue.get(vk) || byValue.set(vk, []).get(vk)).push(t);
      });
      let best = null;
      byValue.forEach(arr => { if (!best || arr.length > best.length) best = arr; });
      // dentro do grupo vencedor, prefere o item com endereço mais completo
      let rep = best[0];
      best.forEach(t => {
        const len = (t.originFull || '').length + (t.destFull || '').length;
        const repLen = (rep.originFull || '').length + (rep.destFull || '').length;
        if (len > repLen) rep = t;
      });
      resolved.push(rep);
    });
    return resolved;
  }
  const resolvedUnique = resolveTimeConflicts(unique);
  unique.length = 0;
  unique.push(...resolvedUnique);

  // Funde os detalhes nas corridas da lista — SÓ completa km/tempo/endereço
  // de uma corrida que a LISTA já confirmou (com valor de verdade). Nunca
  // cria corrida nova a partir de detalhe sozinho: sem valor, não tem como
  // confirmar que é real, e foi exatamente isso que gerava corrida fantasma
  // no import (fragmento de detalhe virando corrida "avulsa" por engano).
  details.forEach(d => {
    let cand = null;
    if (d.time) cand = unique.find(u => u.time === d.time && !u._enriched);
    if (!cand && (d.originFull || d.destFull)) {
      cand = unique.find(u => !u._enriched &&
        ((d.originFull && u.originFull === d.originFull) || (d.destFull && u.destFull === d.destFull)));
    }
    if (cand) {
      cand._enriched = true;
      if (d.km != null && d.km > 0)       { cand.km = d.km; cand.kmEstimado = false; }
      if (d.duration != null && d.duration > 0) cand.duration = d.duration;
      // Endereço do detalhe é a linha inteira, sem truncar — prefere ele.
      if (d.originFull) { cand.originFull = d.originFull; cand.origin = d.origin; }
      if (d.destFull)   { cand.destFull = d.destFull; cand.dest = d.dest; }
    }
  });

  const day = DB.day(S.curDate);

  // Casa cada corrida do vídeo com auto_trips do dia ANTES de mostrar a
  // tela (pedido do Yuri, 16/08/2026) — permite separar "vai só corrigir
  // valor" de "é corrida nova" já na seleção, e esconder o que não muda
  // nada em vez de mostrar tudo misturado.
  const autoTripsPool = (await loadAutoTripsForOperationalDay(operationalPlat, operationalDate)) || [];
  const numEq = (a,b,tol) => Math.abs((a||0)-(b||0)) <= (tol||0.02);
  const annotated = unique.map((t,i) => {
    const matched = matchAutoTrip(t, autoTripsPool);
    if (matched) autoTripsPool.splice(autoTripsPool.indexOf(matched), 1); // consumido — nunca casa duas vezes
    const base = {...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _exists: isTripDuplicate(t, day.trips)};
    if (matched) {
      const newVal = parseFloat(t.value)||0;
      return {...base, _matchedAutoTripId: matched.id, _isNew: false,
        _oldValue: matched.offer_value, _changed: !numEq(matched.offer_value, newVal, 0.02)};
    }
    return {...base, _matchedAutoTripId: null, _isNew: true, _changed: true};
  });
  // Só mostra o que de fato vai mudar algo — casada sem diferença de valor
  // não aparece (nada a fazer), nova sem valor legível também não aparece
  // (sem valor não dá pra confirmar que é corrida de verdade).
  window._pendingTrips = annotated.filter(t => t._isNew ? (parseFloat(t.value)||0) > 0 : t._changed);
  // Corrida NOVA nunca confia no endereço do vídeo sem validar — mesma
  // regra do Kotlin (looksLikeAddress). Sem validação, fica sem 
===== video @ 799659 =====
duas.
function normTime(t) {
  const m = String(t||'').match(/(\d{1,2}):(\d{2})/);
  return m ? m[1].padStart(2,'0')+':'+m[2] : String(t||'');
}
async function processMultipleImports(files, plat) {
  const total = files.length;
  document.getElementById('ic-title').textContent = 'Analisando ' + total + ' print' + (total > 1 ? 's' : '') + '...';
  document.getElementById('ic-body').innerHTML = showSpinner('Processando 1 de ' + total + '...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  const allTrips = [];
  for (let i = 0; i < files.length; i++) {
    document.getElementById('ic-body').innerHTML = showSpinner('Processando print ' + (i+1) + ' de ' + total + '...');
    try {
      const trips = await extractTripsFromImage(files[i], plat);
      allTrips.push(...trips);
    } catch(e) { console.warn('Erro print '+(i+1)+':', e); }
  }
  const day = DB.day(S.curDate);
  const seen = new Set();
  const unique = allTrips.filter(t => {
    if (!(t.value > 0)) return false; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
    t.time = normTime(t.time);
    const key = t.time+'_'+(t.value||0).toFixed(2);
    if (seen.has(key)) return false;
    seen.add(key); return true;
  });
  window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
    platform: t.platform === '99' ? '99' : 'uber',
    _exists: isTripDuplicate(t, day.trips) // pre-mark duplicates
  }));
  showImportConfirm();
}

async function processVideoImport(file, plat) {
  document.getElementById('ic-title').textContent = 'Extraindo frames do vídeo...';
  document.getElementById('ic-body').innerHTML = showSpinner('Analisando vídeo...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  try {
    const frames = await extractVideoFrames(file);
    const allTrips = [];
    for (let i = 0; i < frames.length; i++) {
      document.getElementById('ic-body').innerHTML = showSpinner('Analisando frame ' + (i+1) + ' de ' + frames.length + '...');
      try {
        const trips = await extractTripsFromBase64(frames[i], 'image/jpeg', plat);
        allTrips.push(...trips);
      } catch(e) { console.warn('Frame erro:', e); }
    }
    const dayV = DB.day(S.curDate);
    const seenV = new Set();
    const unique = allTrips.filter(t => {
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seenV.has(key)) return false;
      seenV.add(key); return true;
    });
    const _dayV = DB.day(S.curDate);
    window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _dup: isTripDuplicate(t, _dayV.trips)}));
    showImportConfirm();
  } catch(e) {
    closeMo('mo-import-confirm');
    showToast('Erro ao processar vídeo: ' + e.message + '. Use MP4 ou prints do histórico.', 'error');
    console.error('Video error:', e);
  }
}


// APK 1.3.9+: o WebView de alguns aparelhos não consegue decodificar certos
// screen recordings mesmo quando o arquivo é válido. Quando o bridge nativo
// estiver disponível, o Android decodifica o vídeo e devolve só os JPEGs.
const _nativeVideoExtractRequests = new Map();
window.__mobNativeVideoFrame = function(token, dataUrl) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r || !dataUrl) return;
  const raw = String(dataUrl).includes(',') ? String(dataUrl).split(',').pop() : String(dataUrl);
  if (raw) r.frames.push(raw);
};
window.__mobNativeVideoProgress = function(token, done, total) {
  const r = _nativeVideoExtractRequests.get(token);
  if (r && typeof r.onProgress === 'function') r.onProgress(Number(done)||0, Number(total)||0);
};
window.__mobNativeVideoDone = function(token, emitted) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  if (!r.frames.length) r.reject(new Error('O Android não conseguiu extrair quadros deste vídeo.'));
  else r.resolve(r.frames);
};
window.__mobNativeVideoError = function(token, message) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  r.reject(new Error(message || 'Falha no decodificador nativo do vídeo.'));
};

function extractVideoFramesNative(file, onProgress) {
  return new Promise((resolve, reject) => {
    const token = 'nv_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2,8);
    const timer = setTimeout(() => {
      _nativeVideoExtractRequests.delete(token);
      reject(new Error('Tempo esgotado ao decodificar o vídeo no Android.'));
    }, 120000);
    _nativeVideoExtractRequests.set(token, {frames:[], onProgress, resolve, reject, timer});
    try {
      const started = SmartMobiNative.extractSelectedVideoFrames(token, 72, 720);
      if (started === false) {
        clearTimeout(timer);
        _nativeVideoExtractRequests.delete(token);
        reject(Object.assign(new Error('O Android não recebeu o arquivo de vídeo selecionado.'), {nativeNotStarted:true}));
      }
    } catch(e) {
      clearTimeout(timer);
      _nativeVideoExtractRequests.delete(token);
      reject(Object.assign(e instanceof Error ? e : new Error(String(e)), {nativeNotStarted:true}));
    }
  });
}

async function extractVideoFrames(file, onProgress) {
  let canNative = false;
  try {
    canNative = !!(window.SmartMobiNative &&
      typeof SmartMobiNative.hasNativeVideoFrameExtraction === 'function' &&
      SmartMobiNative.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');
  } catch(e) {}
  if (canNative) {
    try {
      return await extractVideoFramesNative(file, onProgress);
    } catch(e) {
      // Só cai para o decoder web se a ponte não chegou a iniciar. Se o
      // Android iniciou e reportou erro real de codec, repetir no WebView
      // apenas recriaria o travamento que motivou este caminho nativo.
      if (!e || !e.nativeNotStarted) throw e;
      console.warn('[video] bridge nativo indisponível, usando navegador:', e);
    }
  }
  return extractVideoFramesBrowser(file, onProgress);
}

async function extractVideoFramesBrowser(file, onProgress) {
  return new Promise((resolve, reject) => {
    const video  = document.createElement('video');
    const url    = URL.createObjectURL(file);
    const canvas = document.createElement('canvas');
    const ctx    = canvas.getContext('2d');
    video.style.cssText = 'position:fixed;top:-9999px;left:-9999px;width:1px;height:1px;opacity:0';
    document.body.appendChild(video);
    video.src        = url;
    video.muted      = true;
    video.playsInline = true;
    video.setAttribute('playsinline', '');
    video.setAttribute('webkit-playsinline', '');
    video.preload    = 'auto';

    const metadataTimer = setTimeout(() => {
      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      reject(new Error('O vídeo demorou demais para ser preparado. Tente novamente.'));
    }, 15000);

    video.onloadedmetadata = async () => {
      clearTimeout(metadataTimer);
      try {
      // loadedmetadata só garante dimensões/duração. Em Android/WebView o
      // decoder pode ainda não ter nenhum frame disponível. Aguarda dados
      // reais antes de iniciar os seeks, evitando canvas preto em MP4 válido.
      if (video.readyState < 2) {
        await new Promise((resolve, reject) => {
          let settled = false;
          const finish = () => { if (settled) return; settled = true; cleanup(); resolve(); };
          const fail = () => { if (settled) return; settled = true; cleanup(); reject(new Error('Não foi possível decodificar o vídeo.')); };
          const timer = setTimeout(fail, 12000);
          const cleanup = () => { clearTimeout(timer); video.removeEventListener('loadeddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
        });
      }
      // Limit canvas size for performance
      const MAX = 1080;
      const ratio = Math.min(MAX / video.videoWidth, MAX / video.videoHeight, 1);
      canvas.width  = Math.round(video.videoWidth  * ratio);
      canvas.height = Math.round(video.videoHeight * ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas por corrida real pra
      // resolveTimeConflicts() escolher a leitura certa por maioria
      const times = [];
      for (let t = 0.5; t < duration; t += INTERVAL) times.push(parseFloat(t.toFixed(2)));
      if (!times.length) times.push(0);

      // Fingerprint pequeno (32x32 grayscale) pra comparar frames e pular
      // quando o scroll tá parado/lento — evita mandar quase-o-mesmo-frame
      // pra IA, que é a causa de valores "fantasma" (leitura funde 2 frames
      // parecidos com corrida cortada/sobreposta).
      const FP = 32;
      const fpCanvas = document.createElement('canvas');
      fpCanvas.width = FP; fpCanvas.height = FP;
      const fpCtx = fpCanvas.
===== video @ 801647 =====
e ' + (i+1) + ' de ' + frames.length + '...');
      try {
        const trips = await extractTripsFromBase64(frames[i], 'image/jpeg', plat);
        allTrips.push(...trips);
      } catch(e) { console.warn('Frame erro:', e); }
    }
    const dayV = DB.day(S.curDate);
    const seenV = new Set();
    const unique = allTrips.filter(t => {
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seenV.has(key)) return false;
      seenV.add(key); return true;
    });
    const _dayV = DB.day(S.curDate);
    window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _dup: isTripDuplicate(t, _dayV.trips)}));
    showImportConfirm();
  } catch(e) {
    closeMo('mo-import-confirm');
    showToast('Erro ao processar vídeo: ' + e.message + '. Use MP4 ou prints do histórico.', 'error');
    console.error('Video error:', e);
  }
}


// APK 1.3.9+: o WebView de alguns aparelhos não consegue decodificar certos
// screen recordings mesmo quando o arquivo é válido. Quando o bridge nativo
// estiver disponível, o Android decodifica o vídeo e devolve só os JPEGs.
const _nativeVideoExtractRequests = new Map();
window.__mobNativeVideoFrame = function(token, dataUrl) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r || !dataUrl) return;
  const raw = String(dataUrl).includes(',') ? String(dataUrl).split(',').pop() : String(dataUrl);
  if (raw) r.frames.push(raw);
};
window.__mobNativeVideoProgress = function(token, done, total) {
  const r = _nativeVideoExtractRequests.get(token);
  if (r && typeof r.onProgress === 'function') r.onProgress(Number(done)||0, Number(total)||0);
};
window.__mobNativeVideoDone = function(token, emitted) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  if (!r.frames.length) r.reject(new Error('O Android não conseguiu extrair quadros deste vídeo.'));
  else r.resolve(r.frames);
};
window.__mobNativeVideoError = function(token, message) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  r.reject(new Error(message || 'Falha no decodificador nativo do vídeo.'));
};

function extractVideoFramesNative(file, onProgress) {
  return new Promise((resolve, reject) => {
    const token = 'nv_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2,8);
    const timer = setTimeout(() => {
      _nativeVideoExtractRequests.delete(token);
      reject(new Error('Tempo esgotado ao decodificar o vídeo no Android.'));
    }, 120000);
    _nativeVideoExtractRequests.set(token, {frames:[], onProgress, resolve, reject, timer});
    try {
      const started = SmartMobiNative.extractSelectedVideoFrames(token, 72, 720);
      if (started === false) {
        clearTimeout(timer);
        _nativeVideoExtractRequests.delete(token);
        reject(Object.assign(new Error('O Android não recebeu o arquivo de vídeo selecionado.'), {nativeNotStarted:true}));
      }
    } catch(e) {
      clearTimeout(timer);
      _nativeVideoExtractRequests.delete(token);
      reject(Object.assign(e instanceof Error ? e : new Error(String(e)), {nativeNotStarted:true}));
    }
  });
}

async function extractVideoFrames(file, onProgress) {
  let canNative = false;
  try {
    canNative = !!(window.SmartMobiNative &&
      typeof SmartMobiNative.hasNativeVideoFrameExtraction === 'function' &&
      SmartMobiNative.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');
  } catch(e) {}
  if (canNative) {
    try {
      return await extractVideoFramesNative(file, onProgress);
    } catch(e) {
      // Só cai para o decoder web se a ponte não chegou a iniciar. Se o
      // Android iniciou e reportou erro real de codec, repetir no WebView
      // apenas recriaria o travamento que motivou este caminho nativo.
      if (!e || !e.nativeNotStarted) throw e;
      console.warn('[video] bridge nativo indisponível, usando navegador:', e);
    }
  }
  return extractVideoFramesBrowser(file, onProgress);
}

async function extractVideoFramesBrowser(file, onProgress) {
  return new Promise((resolve, reject) => {
    const video  = document.createElement('video');
    const url    = URL.createObjectURL(file);
    const canvas = document.createElement('canvas');
    const ctx    = canvas.getContext('2d');
    video.style.cssText = 'position:fixed;top:-9999px;left:-9999px;width:1px;height:1px;opacity:0';
    document.body.appendChild(video);
    video.src        = url;
    video.muted      = true;
    video.playsInline = true;
    video.setAttribute('playsinline', '');
    video.setAttribute('webkit-playsinline', '');
    video.preload    = 'auto';

    const metadataTimer = setTimeout(() => {
      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      reject(new Error('O vídeo demorou demais para ser preparado. Tente novamente.'));
    }, 15000);

    video.onloadedmetadata = async () => {
      clearTimeout(metadataTimer);
      try {
      // loadedmetadata só garante dimensões/duração. Em Android/WebView o
      // decoder pode ainda não ter nenhum frame disponível. Aguarda dados
      // reais antes de iniciar os seeks, evitando canvas preto em MP4 válido.
      if (video.readyState < 2) {
        await new Promise((resolve, reject) => {
          let settled = false;
          const finish = () => { if (settled) return; settled = true; cleanup(); resolve(); };
          const fail = () => { if (settled) return; settled = true; cleanup(); reject(new Error('Não foi possível decodificar o vídeo.')); };
          const timer = setTimeout(fail, 12000);
          const cleanup = () => { clearTimeout(timer); video.removeEventListener('loadeddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
        });
      }
      // Limit canvas size for performance
      const MAX = 1080;
      const ratio = Math.min(MAX / video.videoWidth, MAX / video.videoHeight, 1);
      canvas.width  = Math.round(video.videoWidth  * ratio);
      canvas.height = Math.round(video.videoHeight * ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas por corrida real pra
      // resolveTimeConflicts() escolher a leitura certa por maioria
      const times = [];
      for (let t = 0.5; t < duration; t += INTERVAL) times.push(parseFloat(t.toFixed(2)));
      if (!times.length) times.push(0);

      // Fingerprint pequeno (32x32 grayscale) pra comparar frames e pular
      // quando o scroll tá parado/lento — evita mandar quase-o-mesmo-frame
      // pra IA, que é a causa de valores "fantasma" (leitura funde 2 frames
      // parecidos com corrida cortada/sobreposta).
      const FP = 32;
      const fpCanvas = document.createElement('canvas');
      fpCanvas.width = FP; fpCanvas.height = FP;
      const fpCtx = fpCanvas.getContext('2d');
      function frameFingerprint() {
        fpCtx.drawImage(canvas, 0, 0, FP, FP);
        const d = fpCtx.getImageData(0, 0, FP, FP).data;
        const g = new Float32Array(FP*FP);
        for (let i = 0; i < g.length; i++) {
          const o = i*4;
          g[i] = (d[o] + d[o+1] + d[o+2]) / 3;
        }
        return g;
      }
      function fpDiff(a, b) {
        let sum = 0;
        for (let i = 0; i < a.length; i++) sum += Math.abs(a[i]-b[i]);
        return sum / (a.length * 255); // normalizado 0..1
      }
      const SIMILARITY_THRESHOLD = 0.02; // <2% de mudança = frame quase idêntico, pula

      const frames = [];
      let lastFp = null;
      let skipped = 0;
      if (typeof onProgress === 'function') onProgress(0, times.length);
      for (let timeIndex = 0; timeIndex < times.length; timeIndex++) {
        const time = times[timeIndex];
        try {
          const frameDrawn = await new Promise((res) => {
            // Só considera o seek concluído quando há um frame decodificado de
            // verdade. O fallback não transforma ausência de frame em imagem preta.
            let done = false;
            const finishSeek = (fromEvent) => {
              if (done) return;
              const draw = () => {
                if (done) return;
                done = true;
                clearTimeout(timer);
                video.onseeked = null;
                let ok = false;
                try {
                  if (video.readyState >= 2 && video.videoWidth > 0 && video.videoHeight > 0) {
                    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
                    ok = true;
                  }
                } catch(e) {}
                res(ok);
              };
              if (fromEvent && typeof video.requestVideoFrameCallback === 'function') {
                try { video.requestVideoFrameCallback(() => draw()); return; } catch(e) {}
              }
              if (fromEvent) setTi
===== video @ 802228 =====
.map((t,i) => ({...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _dup: isTripDuplicate(t, _dayV.trips)}));
    showImportConfirm();
  } catch(e) {
    closeMo('mo-import-confirm');
    showToast('Erro ao processar vídeo: ' + e.message + '. Use MP4 ou prints do histórico.', 'error');
    console.error('Video error:', e);
  }
}


// APK 1.3.9+: o WebView de alguns aparelhos não consegue decodificar certos
// screen recordings mesmo quando o arquivo é válido. Quando o bridge nativo
// estiver disponível, o Android decodifica o vídeo e devolve só os JPEGs.
const _nativeVideoExtractRequests = new Map();
window.__mobNativeVideoFrame = function(token, dataUrl) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r || !dataUrl) return;
  const raw = String(dataUrl).includes(',') ? String(dataUrl).split(',').pop() : String(dataUrl);
  if (raw) r.frames.push(raw);
};
window.__mobNativeVideoProgress = function(token, done, total) {
  const r = _nativeVideoExtractRequests.get(token);
  if (r && typeof r.onProgress === 'function') r.onProgress(Number(done)||0, Number(total)||0);
};
window.__mobNativeVideoDone = function(token, emitted) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  if (!r.frames.length) r.reject(new Error('O Android não conseguiu extrair quadros deste vídeo.'));
  else r.resolve(r.frames);
};
window.__mobNativeVideoError = function(token, message) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  r.reject(new Error(message || 'Falha no decodificador nativo do vídeo.'));
};

function extractVideoFramesNative(file, onProgress) {
  return new Promise((resolve, reject) => {
    const token = 'nv_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2,8);
    const timer = setTimeout(() => {
      _nativeVideoExtractRequests.delete(token);
      reject(new Error('Tempo esgotado ao decodificar o vídeo no Android.'));
    }, 120000);
    _nativeVideoExtractRequests.set(token, {frames:[], onProgress, resolve, reject, timer});
    try {
      const started = SmartMobiNative.extractSelectedVideoFrames(token, 72, 720);
      if (started === false) {
        clearTimeout(timer);
        _nativeVideoExtractRequests.delete(token);
        reject(Object.assign(new Error('O Android não recebeu o arquivo de vídeo selecionado.'), {nativeNotStarted:true}));
      }
    } catch(e) {
      clearTimeout(timer);
      _nativeVideoExtractRequests.delete(token);
      reject(Object.assign(e instanceof Error ? e : new Error(String(e)), {nativeNotStarted:true}));
    }
  });
}

async function extractVideoFrames(file, onProgress) {
  let canNative = false;
  try {
    canNative = !!(window.SmartMobiNative &&
      typeof SmartMobiNative.hasNativeVideoFrameExtraction === 'function' &&
      SmartMobiNative.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');
  } catch(e) {}
  if (canNative) {
    try {
      return await extractVideoFramesNative(file, onProgress);
    } catch(e) {
      // Só cai para o decoder web se a ponte não chegou a iniciar. Se o
      // Android iniciou e reportou erro real de codec, repetir no WebView
      // apenas recriaria o travamento que motivou este caminho nativo.
      if (!e || !e.nativeNotStarted) throw e;
      console.warn('[video] bridge nativo indisponível, usando navegador:', e);
    }
  }
  return extractVideoFramesBrowser(file, onProgress);
}

async function extractVideoFramesBrowser(file, onProgress) {
  return new Promise((resolve, reject) => {
    const video  = document.createElement('video');
    const url    = URL.createObjectURL(file);
    const canvas = document.createElement('canvas');
    const ctx    = canvas.getContext('2d');
    video.style.cssText = 'position:fixed;top:-9999px;left:-9999px;width:1px;height:1px;opacity:0';
    document.body.appendChild(video);
    video.src        = url;
    video.muted      = true;
    video.playsInline = true;
    video.setAttribute('playsinline', '');
    video.setAttribute('webkit-playsinline', '');
    video.preload    = 'auto';

    const metadataTimer = setTimeout(() => {
      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      reject(new Error('O vídeo demorou demais para ser preparado. Tente novamente.'));
    }, 15000);

    video.onloadedmetadata = async () => {
      clearTimeout(metadataTimer);
      try {
      // loadedmetadata só garante dimensões/duração. Em Android/WebView o
      // decoder pode ainda não ter nenhum frame disponível. Aguarda dados
      // reais antes de iniciar os seeks, evitando canvas preto em MP4 válido.
      if (video.readyState < 2) {
        await new Promise((resolve, reject) => {
          let settled = false;
          const finish = () => { if (settled) return; settled = true; cleanup(); resolve(); };
          const fail = () => { if (settled) return; settled = true; cleanup(); reject(new Error('Não foi possível decodificar o vídeo.')); };
          const timer = setTimeout(fail, 12000);
          const cleanup = () => { clearTimeout(timer); video.removeEventListener('loadeddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
        });
      }
      // Limit canvas size for performance
      const MAX = 1080;
      const ratio = Math.min(MAX / video.videoWidth, MAX / video.videoHeight, 1);
      canvas.width  = Math.round(video.videoWidth  * ratio);
      canvas.height = Math.round(video.videoHeight * ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas por corrida real pra
      // resolveTimeConflicts() escolher a leitura certa por maioria
      const times = [];
      for (let t = 0.5; t < duration; t += INTERVAL) times.push(parseFloat(t.toFixed(2)));
      if (!times.length) times.push(0);

      // Fingerprint pequeno (32x32 grayscale) pra comparar frames e pular
      // quando o scroll tá parado/lento — evita mandar quase-o-mesmo-frame
      // pra IA, que é a causa de valores "fantasma" (leitura funde 2 frames
      // parecidos com corrida cortada/sobreposta).
      const FP = 32;
      const fpCanvas = document.createElement('canvas');
      fpCanvas.width = FP; fpCanvas.height = FP;
      const fpCtx = fpCanvas.getContext('2d');
      function frameFingerprint() {
        fpCtx.drawImage(canvas, 0, 0, FP, FP);
        const d = fpCtx.getImageData(0, 0, FP, FP).data;
        const g = new Float32Array(FP*FP);
        for (let i = 0; i < g.length; i++) {
          const o = i*4;
          g[i] = (d[o] + d[o+1] + d[o+2]) / 3;
        }
        return g;
      }
      function fpDiff(a, b) {
        let sum = 0;
        for (let i = 0; i < a.length; i++) sum += Math.abs(a[i]-b[i]);
        return sum / (a.length * 255); // normalizado 0..1
      }
      const SIMILARITY_THRESHOLD = 0.02; // <2% de mudança = frame quase idêntico, pula

      const frames = [];
      let lastFp = null;
      let skipped = 0;
      if (typeof onProgress === 'function') onProgress(0, times.length);
      for (let timeIndex = 0; timeIndex < times.length; timeIndex++) {
        const time = times[timeIndex];
        try {
          const frameDrawn = await new Promise((res) => {
            // Só considera o seek concluído quando há um frame decodificado de
            // verdade. O fallback não transforma ausência de frame em imagem preta.
            let done = false;
            const finishSeek = (fromEvent) => {
              if (done) return;
              const draw = () => {
                if (done) return;
                done = true;
                clearTimeout(timer);
                video.onseeked = null;
                let ok = false;
                try {
                  if (video.readyState >= 2 && video.videoWidth > 0 && video.videoHeight > 0) {
                    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
                    ok = true;
                  }
                } catch(e) {}
                res(ok);
              };
              if (fromEvent && typeof video.requestVideoFrameCallback === 'function') {
                try { video.requestVideoFrameCallback(() => draw()); return; } catch(e) {}
              }
              if (fromEvent) setTimeout(draw, 120); else draw();
            };
            const timer = setTimeout(() => finishSeek(false), 4500);
            video.onseeked = () => finishSeek(true);
            video.currentTime = Math.min(time, Math.max(0, duration - 0.03));
          });
          if (!frameDrawn) continue;
          // Detecta conteúdo no QUADRO INTEIRO. Antes olhava só 20x20 px do
          // canto superior esquerdo; em screen recording esse canto pode ser
          // preto (status bar/recorte) e todos os frames reais eram descartados.
          const probe = document.createElement(
===== video @ 802502 =====
 Use MP4 ou prints do histórico.', 'error');
    console.error('Video error:', e);
  }
}


// APK 1.3.9+: o WebView de alguns aparelhos não consegue decodificar certos
// screen recordings mesmo quando o arquivo é válido. Quando o bridge nativo
// estiver disponível, o Android decodifica o vídeo e devolve só os JPEGs.
const _nativeVideoExtractRequests = new Map();
window.__mobNativeVideoFrame = function(token, dataUrl) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r || !dataUrl) return;
  const raw = String(dataUrl).includes(',') ? String(dataUrl).split(',').pop() : String(dataUrl);
  if (raw) r.frames.push(raw);
};
window.__mobNativeVideoProgress = function(token, done, total) {
  const r = _nativeVideoExtractRequests.get(token);
  if (r && typeof r.onProgress === 'function') r.onProgress(Number(done)||0, Number(total)||0);
};
window.__mobNativeVideoDone = function(token, emitted) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  if (!r.frames.length) r.reject(new Error('O Android não conseguiu extrair quadros deste vídeo.'));
  else r.resolve(r.frames);
};
window.__mobNativeVideoError = function(token, message) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  r.reject(new Error(message || 'Falha no decodificador nativo do vídeo.'));
};

function extractVideoFramesNative(file, onProgress) {
  return new Promise((resolve, reject) => {
    const token = 'nv_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2,8);
    const timer = setTimeout(() => {
      _nativeVideoExtractRequests.delete(token);
      reject(new Error('Tempo esgotado ao decodificar o vídeo no Android.'));
    }, 120000);
    _nativeVideoExtractRequests.set(token, {frames:[], onProgress, resolve, reject, timer});
    try {
      const started = SmartMobiNative.extractSelectedVideoFrames(token, 72, 720);
      if (started === false) {
        clearTimeout(timer);
        _nativeVideoExtractRequests.delete(token);
        reject(Object.assign(new Error('O Android não recebeu o arquivo de vídeo selecionado.'), {nativeNotStarted:true}));
      }
    } catch(e) {
      clearTimeout(timer);
      _nativeVideoExtractRequests.delete(token);
      reject(Object.assign(e instanceof Error ? e : new Error(String(e)), {nativeNotStarted:true}));
    }
  });
}

async function extractVideoFrames(file, onProgress) {
  let canNative = false;
  try {
    canNative = !!(window.SmartMobiNative &&
      typeof SmartMobiNative.hasNativeVideoFrameExtraction === 'function' &&
      SmartMobiNative.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');
  } catch(e) {}
  if (canNative) {
    try {
      return await extractVideoFramesNative(file, onProgress);
    } catch(e) {
      // Só cai para o decoder web se a ponte não chegou a iniciar. Se o
      // Android iniciou e reportou erro real de codec, repetir no WebView
      // apenas recriaria o travamento que motivou este caminho nativo.
      if (!e || !e.nativeNotStarted) throw e;
      console.warn('[video] bridge nativo indisponível, usando navegador:', e);
    }
  }
  return extractVideoFramesBrowser(file, onProgress);
}

async function extractVideoFramesBrowser(file, onProgress) {
  return new Promise((resolve, reject) => {
    const video  = document.createElement('video');
    const url    = URL.createObjectURL(file);
    const canvas = document.createElement('canvas');
    const ctx    = canvas.getContext('2d');
    video.style.cssText = 'position:fixed;top:-9999px;left:-9999px;width:1px;height:1px;opacity:0';
    document.body.appendChild(video);
    video.src        = url;
    video.muted      = true;
    video.playsInline = true;
    video.setAttribute('playsinline', '');
    video.setAttribute('webkit-playsinline', '');
    video.preload    = 'auto';

    const metadataTimer = setTimeout(() => {
      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      reject(new Error('O vídeo demorou demais para ser preparado. Tente novamente.'));
    }, 15000);

    video.onloadedmetadata = async () => {
      clearTimeout(metadataTimer);
      try {
      // loadedmetadata só garante dimensões/duração. Em Android/WebView o
      // decoder pode ainda não ter nenhum frame disponível. Aguarda dados
      // reais antes de iniciar os seeks, evitando canvas preto em MP4 válido.
      if (video.readyState < 2) {
        await new Promise((resolve, reject) => {
          let settled = false;
          const finish = () => { if (settled) return; settled = true; cleanup(); resolve(); };
          const fail = () => { if (settled) return; settled = true; cleanup(); reject(new Error('Não foi possível decodificar o vídeo.')); };
          const timer = setTimeout(fail, 12000);
          const cleanup = () => { clearTimeout(timer); video.removeEventListener('loadeddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
        });
      }
      // Limit canvas size for performance
      const MAX = 1080;
      const ratio = Math.min(MAX / video.videoWidth, MAX / video.videoHeight, 1);
      canvas.width  = Math.round(video.videoWidth  * ratio);
      canvas.height = Math.round(video.videoHeight * ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas por corrida real pra
      // resolveTimeConflicts() escolher a leitura certa por maioria
      const times = [];
      for (let t = 0.5; t < duration; t += INTERVAL) times.push(parseFloat(t.toFixed(2)));
      if (!times.length) times.push(0);

      // Fingerprint pequeno (32x32 grayscale) pra comparar frames e pular
      // quando o scroll tá parado/lento — evita mandar quase-o-mesmo-frame
      // pra IA, que é a causa de valores "fantasma" (leitura funde 2 frames
      // parecidos com corrida cortada/sobreposta).
      const FP = 32;
      const fpCanvas = document.createElement('canvas');
      fpCanvas.width = FP; fpCanvas.height = FP;
      const fpCtx = fpCanvas.getContext('2d');
      function frameFingerprint() {
        fpCtx.drawImage(canvas, 0, 0, FP, FP);
        const d = fpCtx.getImageData(0, 0, FP, FP).data;
        const g = new Float32Array(FP*FP);
        for (let i = 0; i < g.length; i++) {
          const o = i*4;
          g[i] = (d[o] + d[o+1] + d[o+2]) / 3;
        }
        return g;
      }
      function fpDiff(a, b) {
        let sum = 0;
        for (let i = 0; i < a.length; i++) sum += Math.abs(a[i]-b[i]);
        return sum / (a.length * 255); // normalizado 0..1
      }
      const SIMILARITY_THRESHOLD = 0.02; // <2% de mudança = frame quase idêntico, pula

      const frames = [];
      let lastFp = null;
      let skipped = 0;
      if (typeof onProgress === 'function') onProgress(0, times.length);
      for (let timeIndex = 0; timeIndex < times.length; timeIndex++) {
        const time = times[timeIndex];
        try {
          const frameDrawn = await new Promise((res) => {
            // Só considera o seek concluído quando há um frame decodificado de
            // verdade. O fallback não transforma ausência de frame em imagem preta.
            let done = false;
            const finishSeek = (fromEvent) => {
              if (done) return;
              const draw = () => {
                if (done) return;
                done = true;
                clearTimeout(timer);
                video.onseeked = null;
                let ok = false;
                try {
                  if (video.readyState >= 2 && video.videoWidth > 0 && video.videoHeight > 0) {
                    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
                    ok = true;
                  }
                } catch(e) {}
                res(ok);
              };
              if (fromEvent && typeof video.requestVideoFrameCallback === 'function') {
                try { video.requestVideoFrameCallback(() => draw()); return; } catch(e) {}
              }
              if (fromEvent) setTimeout(draw, 120); else draw();
            };
            const timer = setTimeout(() => finishSeek(false), 4500);
            video.onseeked = () => finishSeek(true);
            video.currentTime = Math.min(time, Math.max(0, duration - 0.03));
          });
          if (!frameDrawn) continue;
          // Detecta conteúdo no QUADRO INTEIRO. Antes olhava só 20x20 px do
          // canto superior esquerdo; em screen recording esse canto pode ser
          // preto (status bar/recorte) e todos os frames reais eram descartados.
          const probe = document.createElement('canvas');
          probe.width = 24; probe.height = 24;
          const pctx = probe.getContext('2d');
          pctx.drawImage(canvas, 0, 0, probe.width, probe.height);
          const px = pctx.getImageData(0, 0, probe.width, probe.height).data;
          let lit = 0, m
===== video @ 804014 =====
s) {
  return new Promise((resolve, reject) => {
    const token = 'nv_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2,8);
    const timer = setTimeout(() => {
      _nativeVideoExtractRequests.delete(token);
      reject(new Error('Tempo esgotado ao decodificar o vídeo no Android.'));
    }, 120000);
    _nativeVideoExtractRequests.set(token, {frames:[], onProgress, resolve, reject, timer});
    try {
      const started = SmartMobiNative.extractSelectedVideoFrames(token, 72, 720);
      if (started === false) {
        clearTimeout(timer);
        _nativeVideoExtractRequests.delete(token);
        reject(Object.assign(new Error('O Android não recebeu o arquivo de vídeo selecionado.'), {nativeNotStarted:true}));
      }
    } catch(e) {
      clearTimeout(timer);
      _nativeVideoExtractRequests.delete(token);
      reject(Object.assign(e instanceof Error ? e : new Error(String(e)), {nativeNotStarted:true}));
    }
  });
}

async function extractVideoFrames(file, onProgress) {
  let canNative = false;
  try {
    canNative = !!(window.SmartMobiNative &&
      typeof SmartMobiNative.hasNativeVideoFrameExtraction === 'function' &&
      SmartMobiNative.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');
  } catch(e) {}
  if (canNative) {
    try {
      return await extractVideoFramesNative(file, onProgress);
    } catch(e) {
      // Só cai para o decoder web se a ponte não chegou a iniciar. Se o
      // Android iniciou e reportou erro real de codec, repetir no WebView
      // apenas recriaria o travamento que motivou este caminho nativo.
      if (!e || !e.nativeNotStarted) throw e;
      console.warn('[video] bridge nativo indisponível, usando navegador:', e);
    }
  }
  return extractVideoFramesBrowser(file, onProgress);
}

async function extractVideoFramesBrowser(file, onProgress) {
  return new Promise((resolve, reject) => {
    const video  = document.createElement('video');
    const url    = URL.createObjectURL(file);
    const canvas = document.createElement('canvas');
    const ctx    = canvas.getContext('2d');
    video.style.cssText = 'position:fixed;top:-9999px;left:-9999px;width:1px;height:1px;opacity:0';
    document.body.appendChild(video);
    video.src        = url;
    video.muted      = true;
    video.playsInline = true;
    video.setAttribute('playsinline', '');
    video.setAttribute('webkit-playsinline', '');
    video.preload    = 'auto';

    const metadataTimer = setTimeout(() => {
      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      reject(new Error('O vídeo demorou demais para ser preparado. Tente novamente.'));
    }, 15000);

    video.onloadedmetadata = async () => {
      clearTimeout(metadataTimer);
      try {
      // loadedmetadata só garante dimensões/duração. Em Android/WebView o
      // decoder pode ainda não ter nenhum frame disponível. Aguarda dados
      // reais antes de iniciar os seeks, evitando canvas preto em MP4 válido.
      if (video.readyState < 2) {
        await new Promise((resolve, reject) => {
          let settled = false;
          const finish = () => { if (settled) return; settled = true; cleanup(); resolve(); };
          const fail = () => { if (settled) return; settled = true; cleanup(); reject(new Error('Não foi possível decodificar o vídeo.')); };
          const timer = setTimeout(fail, 12000);
          const cleanup = () => { clearTimeout(timer); video.removeEventListener('loadeddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
        });
      }
      // Limit canvas size for performance
      const MAX = 1080;
      const ratio = Math.min(MAX / video.videoWidth, MAX / video.videoHeight, 1);
      canvas.width  = Math.round(video.videoWidth  * ratio);
      canvas.height = Math.round(video.videoHeight * ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas por corrida real pra
      // resolveTimeConflicts() escolher a leitura certa por maioria
      const times = [];
      for (let t = 0.5; t < duration; t += INTERVAL) times.push(parseFloat(t.toFixed(2)));
      if (!times.length) times.push(0);

      // Fingerprint pequeno (32x32 grayscale) pra comparar frames e pular
      // quando o scroll tá parado/lento — evita mandar quase-o-mesmo-frame
      // pra IA, que é a causa de valores "fantasma" (leitura funde 2 frames
      // parecidos com corrida cortada/sobreposta).
      const FP = 32;
      const fpCanvas = document.createElement('canvas');
      fpCanvas.width = FP; fpCanvas.height = FP;
      const fpCtx = fpCanvas.getContext('2d');
      function frameFingerprint() {
        fpCtx.drawImage(canvas, 0, 0, FP, FP);
        const d = fpCtx.getImageData(0, 0, FP, FP).data;
        const g = new Float32Array(FP*FP);
        for (let i = 0; i < g.length; i++) {
          const o = i*4;
          g[i] = (d[o] + d[o+1] + d[o+2]) / 3;
        }
        return g;
      }
      function fpDiff(a, b) {
        let sum = 0;
        for (let i = 0; i < a.length; i++) sum += Math.abs(a[i]-b[i]);
        return sum / (a.length * 255); // normalizado 0..1
      }
      const SIMILARITY_THRESHOLD = 0.02; // <2% de mudança = frame quase idêntico, pula

      const frames = [];
      let lastFp = null;
      let skipped = 0;
      if (typeof onProgress === 'function') onProgress(0, times.length);
      for (let timeIndex = 0; timeIndex < times.length; timeIndex++) {
        const time = times[timeIndex];
        try {
          const frameDrawn = await new Promise((res) => {
            // Só considera o seek concluído quando há um frame decodificado de
            // verdade. O fallback não transforma ausência de frame em imagem preta.
            let done = false;
            const finishSeek = (fromEvent) => {
              if (done) return;
              const draw = () => {
                if (done) return;
                done = true;
                clearTimeout(timer);
                video.onseeked = null;
                let ok = false;
                try {
                  if (video.readyState >= 2 && video.videoWidth > 0 && video.videoHeight > 0) {
                    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
                    ok = true;
                  }
                } catch(e) {}
                res(ok);
              };
              if (fromEvent && typeof video.requestVideoFrameCallback === 'function') {
                try { video.requestVideoFrameCallback(() => draw()); return; } catch(e) {}
              }
              if (fromEvent) setTimeout(draw, 120); else draw();
            };
            const timer = setTimeout(() => finishSeek(false), 4500);
            video.onseeked = () => finishSeek(true);
            video.currentTime = Math.min(time, Math.max(0, duration - 0.03));
          });
          if (!frameDrawn) continue;
          // Detecta conteúdo no QUADRO INTEIRO. Antes olhava só 20x20 px do
          // canto superior esquerdo; em screen recording esse canto pode ser
          // preto (status bar/recorte) e todos os frames reais eram descartados.
          const probe = document.createElement('canvas');
          probe.width = 24; probe.height = 24;
          const pctx = probe.getContext('2d');
          pctx.drawImage(canvas, 0, 0, probe.width, probe.height);
          const px = pctx.getImageData(0, 0, probe.width, probe.height).data;
          let lit = 0, maxLum = 0;
          for (let pi = 0; pi < px.length; pi += 4) {
            const lum = (px[pi] + px[pi+1] + px[pi+2]) / 3;
            if (lum > 12) lit++;
            if (lum > maxLum) maxLum = lum;
          }
          const hasContent = maxLum > 18 && lit >= 3;
          if (!hasContent) continue;
          const fp = frameFingerprint();
          if (lastFp && fpDiff(fp, lastFp) < SIMILARITY_THRESHOLD) {
            skipped++;
            continue; // scroll parado/lento — quase igual ao frame anterior mantido
          }
          lastFp = fp;
          frames.push(canvas.toDataURL('image/jpeg', 0.7).split(',')[1]);
        } catch(e) { console.warn('Frame t='+time, e); }
        finally {
          if (typeof onProgress === 'function') onProgress(timeIndex + 1, times.length);
        }
      }
      if (skipped) console.log('[extractVideoFrames] frames quase-duplicados pulados:', skipped);

      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      if (frames.length > 0) resolve(frames);
      else reject(new Error('Nenhum quadro legível foi extraído do vídeo.'));
      } catch(e) {
        try{document.body.removeChild(video);}catch(e2){}
        URL.revokeObjectURL(url);
        reject(e instanceof Error ? e : new Error(String(e)));
      }
    };

    video.onerror = (e) => {
      clearTimeout(metadataTimer);
      try{document.body.removeChild(video);}catch(e2){}
      URL.revokeObjectURL(url);
      const msg = e?.target?.error?.message ||
===== video @ 804510 =====
ken, 72, 720);
      if (started === false) {
        clearTimeout(timer);
        _nativeVideoExtractRequests.delete(token);
        reject(Object.assign(new Error('O Android não recebeu o arquivo de vídeo selecionado.'), {nativeNotStarted:true}));
      }
    } catch(e) {
      clearTimeout(timer);
      _nativeVideoExtractRequests.delete(token);
      reject(Object.assign(e instanceof Error ? e : new Error(String(e)), {nativeNotStarted:true}));
    }
  });
}

async function extractVideoFrames(file, onProgress) {
  let canNative = false;
  try {
    canNative = !!(window.SmartMobiNative &&
      typeof SmartMobiNative.hasNativeVideoFrameExtraction === 'function' &&
      SmartMobiNative.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');
  } catch(e) {}
  if (canNative) {
    try {
      return await extractVideoFramesNative(file, onProgress);
    } catch(e) {
      // Só cai para o decoder web se a ponte não chegou a iniciar. Se o
      // Android iniciou e reportou erro real de codec, repetir no WebView
      // apenas recriaria o travamento que motivou este caminho nativo.
      if (!e || !e.nativeNotStarted) throw e;
      console.warn('[video] bridge nativo indisponível, usando navegador:', e);
    }
  }
  return extractVideoFramesBrowser(file, onProgress);
}

async function extractVideoFramesBrowser(file, onProgress) {
  return new Promise((resolve, reject) => {
    const video  = document.createElement('video');
    const url    = URL.createObjectURL(file);
    const canvas = document.createElement('canvas');
    const ctx    = canvas.getContext('2d');
    video.style.cssText = 'position:fixed;top:-9999px;left:-9999px;width:1px;height:1px;opacity:0';
    document.body.appendChild(video);
    video.src        = url;
    video.muted      = true;
    video.playsInline = true;
    video.setAttribute('playsinline', '');
    video.setAttribute('webkit-playsinline', '');
    video.preload    = 'auto';

    const metadataTimer = setTimeout(() => {
      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      reject(new Error('O vídeo demorou demais para ser preparado. Tente novamente.'));
    }, 15000);

    video.onloadedmetadata = async () => {
      clearTimeout(metadataTimer);
      try {
      // loadedmetadata só garante dimensões/duração. Em Android/WebView o
      // decoder pode ainda não ter nenhum frame disponível. Aguarda dados
      // reais antes de iniciar os seeks, evitando canvas preto em MP4 válido.
      if (video.readyState < 2) {
        await new Promise((resolve, reject) => {
          let settled = false;
          const finish = () => { if (settled) return; settled = true; cleanup(); resolve(); };
          const fail = () => { if (settled) return; settled = true; cleanup(); reject(new Error('Não foi possível decodificar o vídeo.')); };
          const timer = setTimeout(fail, 12000);
          const cleanup = () => { clearTimeout(timer); video.removeEventListener('loadeddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
        });
      }
      // Limit canvas size for performance
      const MAX = 1080;
      const ratio = Math.min(MAX / video.videoWidth, MAX / video.videoHeight, 1);
      canvas.width  = Math.round(video.videoWidth  * ratio);
      canvas.height = Math.round(video.videoHeight * ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas por corrida real pra
      // resolveTimeConflicts() escolher a leitura certa por maioria
      const times = [];
      for (let t = 0.5; t < duration; t += INTERVAL) times.push(parseFloat(t.toFixed(2)));
      if (!times.length) times.push(0);

      // Fingerprint pequeno (32x32 grayscale) pra comparar frames e pular
      // quando o scroll tá parado/lento — evita mandar quase-o-mesmo-frame
      // pra IA, que é a causa de valores "fantasma" (leitura funde 2 frames
      // parecidos com corrida cortada/sobreposta).
      const FP = 32;
      const fpCanvas = document.createElement('canvas');
      fpCanvas.width = FP; fpCanvas.height = FP;
      const fpCtx = fpCanvas.getContext('2d');
      function frameFingerprint() {
        fpCtx.drawImage(canvas, 0, 0, FP, FP);
        const d = fpCtx.getImageData(0, 0, FP, FP).data;
        const g = new Float32Array(FP*FP);
        for (let i = 0; i < g.length; i++) {
          const o = i*4;
          g[i] = (d[o] + d[o+1] + d[o+2]) / 3;
        }
        return g;
      }
      function fpDiff(a, b) {
        let sum = 0;
        for (let i = 0; i < a.length; i++) sum += Math.abs(a[i]-b[i]);
        return sum / (a.length * 255); // normalizado 0..1
      }
      const SIMILARITY_THRESHOLD = 0.02; // <2% de mudança = frame quase idêntico, pula

      const frames = [];
      let lastFp = null;
      let skipped = 0;
      if (typeof onProgress === 'function') onProgress(0, times.length);
      for (let timeIndex = 0; timeIndex < times.length; timeIndex++) {
        const time = times[timeIndex];
        try {
          const frameDrawn = await new Promise((res) => {
            // Só considera o seek concluído quando há um frame decodificado de
            // verdade. O fallback não transforma ausência de frame em imagem preta.
            let done = false;
            const finishSeek = (fromEvent) => {
              if (done) return;
              const draw = () => {
                if (done) return;
                done = true;
                clearTimeout(timer);
                video.onseeked = null;
                let ok = false;
                try {
                  if (video.readyState >= 2 && video.videoWidth > 0 && video.videoHeight > 0) {
                    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
                    ok = true;
                  }
                } catch(e) {}
                res(ok);
              };
              if (fromEvent && typeof video.requestVideoFrameCallback === 'function') {
                try { video.requestVideoFrameCallback(() => draw()); return; } catch(e) {}
              }
              if (fromEvent) setTimeout(draw, 120); else draw();
            };
            const timer = setTimeout(() => finishSeek(false), 4500);
            video.onseeked = () => finishSeek(true);
            video.currentTime = Math.min(time, Math.max(0, duration - 0.03));
          });
          if (!frameDrawn) continue;
          // Detecta conteúdo no QUADRO INTEIRO. Antes olhava só 20x20 px do
          // canto superior esquerdo; em screen recording esse canto pode ser
          // preto (status bar/recorte) e todos os frames reais eram descartados.
          const probe = document.createElement('canvas');
          probe.width = 24; probe.height = 24;
          const pctx = probe.getContext('2d');
          pctx.drawImage(canvas, 0, 0, probe.width, probe.height);
          const px = pctx.getImageData(0, 0, probe.width, probe.height).data;
          let lit = 0, maxLum = 0;
          for (let pi = 0; pi < px.length; pi += 4) {
            const lum = (px[pi] + px[pi+1] + px[pi+2]) / 3;
            if (lum > 12) lit++;
            if (lum > maxLum) maxLum = lum;
          }
          const hasContent = maxLum > 18 && lit >= 3;
          if (!hasContent) continue;
          const fp = frameFingerprint();
          if (lastFp && fpDiff(fp, lastFp) < SIMILARITY_THRESHOLD) {
            skipped++;
            continue; // scroll parado/lento — quase igual ao frame anterior mantido
          }
          lastFp = fp;
          frames.push(canvas.toDataURL('image/jpeg', 0.7).split(',')[1]);
        } catch(e) { console.warn('Frame t='+time, e); }
        finally {
          if (typeof onProgress === 'function') onProgress(timeIndex + 1, times.length);
        }
      }
      if (skipped) console.log('[extractVideoFrames] frames quase-duplicados pulados:', skipped);

      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      if (frames.length > 0) resolve(frames);
      else reject(new Error('Nenhum quadro legível foi extraído do vídeo.'));
      } catch(e) {
        try{document.body.removeChild(video);}catch(e2){}
        URL.revokeObjectURL(url);
        reject(e instanceof Error ? e : new Error(String(e)));
      }
    };

    video.onerror = (e) => {
      clearTimeout(metadataTimer);
      try{document.body.removeChild(video);}catch(e2){}
      URL.revokeObjectURL(url);
      const msg = e?.target?.error?.message || '';
      reject(new Error('Formato de vídeo não suportado. Use MP4 gravado pelo celular.\n' + msg));
    };
    video.load();
  });
}

async function extractTripsFromImage(file, plat) {
  const b64 = await new Promise((res,rej) => {
    const r = new FileReader();
    r.onload = () => res(r.result.split(',')[1]); r.onerror = () => rej(r.error); r.readAsDataURL(file);
  });
  return extractTripsFromBase64(b64, file.type||'image/jpeg', plat);
}

async function extractTripsFromBase64(b64, mt,
===== video @ 807545 =====
eddata', finish); video.removeEventListener('canplay', finish); };
          video.addEventListener('loadeddata', finish, {once:true});
          video.addEventListener('canplay', finish, {once:true});
        });
      }
      // Limit canvas size for performance
      const MAX = 1080;
      const ratio = Math.min(MAX / video.videoWidth, MAX / video.videoHeight, 1);
      canvas.width  = Math.round(video.videoWidth  * ratio);
      canvas.height = Math.round(video.videoHeight * ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas por corrida real pra
      // resolveTimeConflicts() escolher a leitura certa por maioria
      const times = [];
      for (let t = 0.5; t < duration; t += INTERVAL) times.push(parseFloat(t.toFixed(2)));
      if (!times.length) times.push(0);

      // Fingerprint pequeno (32x32 grayscale) pra comparar frames e pular
      // quando o scroll tá parado/lento — evita mandar quase-o-mesmo-frame
      // pra IA, que é a causa de valores "fantasma" (leitura funde 2 frames
      // parecidos com corrida cortada/sobreposta).
      const FP = 32;
      const fpCanvas = document.createElement('canvas');
      fpCanvas.width = FP; fpCanvas.height = FP;
      const fpCtx = fpCanvas.getContext('2d');
      function frameFingerprint() {
        fpCtx.drawImage(canvas, 0, 0, FP, FP);
        const d = fpCtx.getImageData(0, 0, FP, FP).data;
        const g = new Float32Array(FP*FP);
        for (let i = 0; i < g.length; i++) {
          const o = i*4;
          g[i] = (d[o] + d[o+1] + d[o+2]) / 3;
        }
        return g;
      }
      function fpDiff(a, b) {
        let sum = 0;
        for (let i = 0; i < a.length; i++) sum += Math.abs(a[i]-b[i]);
        return sum / (a.length * 255); // normalizado 0..1
      }
      const SIMILARITY_THRESHOLD = 0.02; // <2% de mudança = frame quase idêntico, pula

      const frames = [];
      let lastFp = null;
      let skipped = 0;
      if (typeof onProgress === 'function') onProgress(0, times.length);
      for (let timeIndex = 0; timeIndex < times.length; timeIndex++) {
        const time = times[timeIndex];
        try {
          const frameDrawn = await new Promise((res) => {
            // Só considera o seek concluído quando há um frame decodificado de
            // verdade. O fallback não transforma ausência de frame em imagem preta.
            let done = false;
            const finishSeek = (fromEvent) => {
              if (done) return;
              const draw = () => {
                if (done) return;
                done = true;
                clearTimeout(timer);
                video.onseeked = null;
                let ok = false;
                try {
                  if (video.readyState >= 2 && video.videoWidth > 0 && video.videoHeight > 0) {
                    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
                    ok = true;
                  }
                } catch(e) {}
                res(ok);
              };
              if (fromEvent && typeof video.requestVideoFrameCallback === 'function') {
                try { video.requestVideoFrameCallback(() => draw()); return; } catch(e) {}
              }
              if (fromEvent) setTimeout(draw, 120); else draw();
            };
            const timer = setTimeout(() => finishSeek(false), 4500);
            video.onseeked = () => finishSeek(true);
            video.currentTime = Math.min(time, Math.max(0, duration - 0.03));
          });
          if (!frameDrawn) continue;
          // Detecta conteúdo no QUADRO INTEIRO. Antes olhava só 20x20 px do
          // canto superior esquerdo; em screen recording esse canto pode ser
          // preto (status bar/recorte) e todos os frames reais eram descartados.
          const probe = document.createElement('canvas');
          probe.width = 24; probe.height = 24;
          const pctx = probe.getContext('2d');
          pctx.drawImage(canvas, 0, 0, probe.width, probe.height);
          const px = pctx.getImageData(0, 0, probe.width, probe.height).data;
          let lit = 0, maxLum = 0;
          for (let pi = 0; pi < px.length; pi += 4) {
            const lum = (px[pi] + px[pi+1] + px[pi+2]) / 3;
            if (lum > 12) lit++;
            if (lum > maxLum) maxLum = lum;
          }
          const hasContent = maxLum > 18 && lit >= 3;
          if (!hasContent) continue;
          const fp = frameFingerprint();
          if (lastFp && fpDiff(fp, lastFp) < SIMILARITY_THRESHOLD) {
            skipped++;
            continue; // scroll parado/lento — quase igual ao frame anterior mantido
          }
          lastFp = fp;
          frames.push(canvas.toDataURL('image/jpeg', 0.7).split(',')[1]);
        } catch(e) { console.warn('Frame t='+time, e); }
        finally {
          if (typeof onProgress === 'function') onProgress(timeIndex + 1, times.length);
        }
      }
      if (skipped) console.log('[extractVideoFrames] frames quase-duplicados pulados:', skipped);

      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      if (frames.length > 0) resolve(frames);
      else reject(new Error('Nenhum quadro legível foi extraído do vídeo.'));
      } catch(e) {
        try{document.body.removeChild(video);}catch(e2){}
        URL.revokeObjectURL(url);
        reject(e instanceof Error ? e : new Error(String(e)));
      }
    };

    video.onerror = (e) => {
      clearTimeout(metadataTimer);
      try{document.body.removeChild(video);}catch(e2){}
      URL.revokeObjectURL(url);
      const msg = e?.target?.error?.message || '';
      reject(new Error('Formato de vídeo não suportado. Use MP4 gravado pelo celular.\n' + msg));
    };
    video.load();
  });
}

async function extractTripsFromImage(file, plat) {
  const b64 = await new Promise((res,rej) => {
    const r = new FileReader();
    r.onload = () => res(r.result.split(',')[1]); r.onerror = () => rej(r.error); r.readAsDataURL(file);
  });
  return extractTripsFromBase64(b64, file.type||'image/jpeg', plat);
}

async function extractTripsFromBase64(b64, mt, plat) {
  const cashWarning = 'ATENÇÃO — confusão comum: "Dinheiro recebido" (ícone de câmera/recibo) NÃO é gorjeta nem dinâmico, é so a forma de pagamento (o passageiro pagou em espécie). Quando uma corrida tiver "Preço dinâmico" (ícone de raio) E "Dinheiro recebido" juntas, são DUAS linhas diferentes — extraia SÓ o valor do "Preço dinâmico" em dinamico, e NUNCA coloque o valor de "Dinheiro recebido" em gorjeta (nem em dinamico, nem em value). Exemplo: corrida com "R$ 12,50 Preço dinâmico" e "R$ 27,05 Dinheiro recebido" → dinamico:12.50, gorjeta:0.00 (o R$27,05 não entra em lugar nenhum do JSON).';
  // Prompt único — a IA identifica sozinha se é Uber ou 99 pelo layout da
  // imagem (em vez de confiar em qual botão o usuário tocou, que pode estar
  // errado). Cada corrida retornada inclui "platform":"uber"|"99".
  const prompt = 'Esta imagem mostra uma tela do app de motorista — pode ser: (1) UBER, tela "Histórico de ganhos" (lista de corridas com linha divisória entre elas, "Preço dinâmico" com ícone de raio, "Valor extra" com ícone de mão/coração = gorjeta); (2) 99 LISTA, tela "Histórico de corridas" do app 99 Motorista (ícone de carro + texto "Pop", tag laranja "Taxa Fixa de X%", "Ganhos pagos no app" ou "Ganhos pagos em dinheiro"); ou (3) 99 DETALHE, tela "Detalhes da corrida" do 99 (título "Detalhes da corrida" no topo, "Você ganhou R$X" em destaque, campos "Duração" e "Distância"). PRIMEIRO identifique qual das três telas é, pelo layout, e siga SOMENTE as regras dessa tela, abaixo. Inclua "platform":"uber" ou "platform":"99" em cada item do JSON (uma imagem só tem UMA plataforma e UM tipo de tela, nunca misture).\n\n' +
    '=== SE FOR UBER ===\n' +
    'Extraia CADA corrida, mas SÓ quando ela aparecer COMPLETA na imagem (com a linha divisória acima e abaixo) — se uma corrida estiver cortada no topo ou na base da imagem (incompleta), IGNORE essa corrida, não tente adivinhar ou completar com dados de outra. Ignore Missão (R$ 0,00). ' + cashWarning + ' Abaixo do valor principal de cada corrida também pode aparecer "Valor extra" (ícone de mão/coração — é a gorjeta do passageiro) — extraia esse valor em gorjeta quando existir. Também pode aparecer "Pedágio R$X,XX" — extraia esse valor em pedagio quando existir (0.00 quando não aparecer). Cada corrida mostra dois endereços (origem e destino): copie a linha de texto INTEIRA, palavra por palavra, exatamente como está escrita na tela, sem resumir ou cortar nenhuma parte, em "originFull"/"destFull". E TAMBÉM extraia só o nome do bairro (sem rua, número, cidade, UF ou CEP) em "origin"/"dest" — ex: se o texto na tela é "Rua Corcovado, 213, Jardim Botânico - Rio de Janeiro - RJ, 22460-050, BR", então origin="Jardim Botânico" e originFull="Rua Corcovado, 213, Jardim Botânico - Rio de Janeiro - RJ, 22460-050, BR" (a linha inteira, não abreviada). Formato de cada item: {"platform":"uber","time":"HH:MM","value":0.00,"km":0.0,"duration":0,"category":"Black","origin":"bairro","originFull":"endereço completo","dest":"bairro","destFull":"en
===== video @ 808028 =====
* ratio);

      const duration = video.duration;
      if (!isFinite(duration) || isNaN(duration) || duration <= 0) {
        // Duration unknown — try capturing current frame
        try {
          await new Promise(r => setTimeout(r, 500));
          ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
          const frame = canvas.toDataURL('image/jpeg', 0.7).split(',')[1];
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          resolve([frame]);
        } catch(e) {
          try{document.body.removeChild(video);}catch(e2){}
          URL.revokeObjectURL(url);
          reject(new Error('Não foi possível ler o vídeo. Tente em formato MP4.'));
        }
        return;
      }

      const INTERVAL = 0.6; // frame a cada 0.6s (era 1s) — menos distância de
      // rolagem entre frames reduz a chance de misturar campos de cartões
      // vizinhos, e dá mais leituras repetidas por corrida real pra
      // resolveTimeConflicts() escolher a leitura certa por maioria
      const times = [];
      for (let t = 0.5; t < duration; t += INTERVAL) times.push(parseFloat(t.toFixed(2)));
      if (!times.length) times.push(0);

      // Fingerprint pequeno (32x32 grayscale) pra comparar frames e pular
      // quando o scroll tá parado/lento — evita mandar quase-o-mesmo-frame
      // pra IA, que é a causa de valores "fantasma" (leitura funde 2 frames
      // parecidos com corrida cortada/sobreposta).
      const FP = 32;
      const fpCanvas = document.createElement('canvas');
      fpCanvas.width = FP; fpCanvas.height = FP;
      const fpCtx = fpCanvas.getContext('2d');
      function frameFingerprint() {
        fpCtx.drawImage(canvas, 0, 0, FP, FP);
        const d = fpCtx.getImageData(0, 0, FP, FP).data;
        const g = new Float32Array(FP*FP);
        for (let i = 0; i < g.length; i++) {
          const o = i*4;
          g[i] = (d[o] + d[o+1] + d[o+2]) / 3;
        }
        return g;
      }
      function fpDiff(a, b) {
        let sum = 0;
        for (let i = 0; i < a.length; i++) sum += Math.abs(a[i]-b[i]);
        return sum / (a.length * 255); // normalizado 0..1
      }
      const SIMILARITY_THRESHOLD = 0.02; // <2% de mudança = frame quase idêntico, pula

      const frames = [];
      let lastFp = null;
      let skipped = 0;
      if (typeof onProgress === 'function') onProgress(0, times.length);
      for (let timeIndex = 0; timeIndex < times.length; timeIndex++) {
        const time = times[timeIndex];
        try {
          const frameDrawn = await new Promise((res) => {
            // Só considera o seek concluído quando há um frame decodificado de
            // verdade. O fallback não transforma ausência de frame em imagem preta.
            let done = false;
            const finishSeek = (fromEvent) => {
              if (done) return;
              const draw = () => {
                if (done) return;
                done = true;
                clearTimeout(timer);
                video.onseeked = null;
                let ok = false;
                try {
                  if (video.readyState >= 2 && video.videoWidth > 0 && video.videoHeight > 0) {
                    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
                    ok = true;
                  }
                } catch(e) {}
                res(ok);
              };
              if (fromEvent && typeof video.requestVideoFrameCallback === 'function') {
                try { video.requestVideoFrameCallback(() => draw()); return; } catch(e) {}
              }
              if (fromEvent) setTimeout(draw, 120); else draw();
            };
            const timer = setTimeout(() => finishSeek(false), 4500);
            video.onseeked = () => finishSeek(true);
            video.currentTime = Math.min(time, Math.max(0, duration - 0.03));
          });
          if (!frameDrawn) continue;
          // Detecta conteúdo no QUADRO INTEIRO. Antes olhava só 20x20 px do
          // canto superior esquerdo; em screen recording esse canto pode ser
          // preto (status bar/recorte) e todos os frames reais eram descartados.
          const probe = document.createElement('canvas');
          probe.width = 24; probe.height = 24;
          const pctx = probe.getContext('2d');
          pctx.drawImage(canvas, 0, 0, probe.width, probe.height);
          const px = pctx.getImageData(0, 0, probe.width, probe.height).data;
          let lit = 0, maxLum = 0;
          for (let pi = 0; pi < px.length; pi += 4) {
            const lum = (px[pi] + px[pi+1] + px[pi+2]) / 3;
            if (lum > 12) lit++;
            if (lum > maxLum) maxLum = lum;
          }
          const hasContent = maxLum > 18 && lit >= 3;
          if (!hasContent) continue;
          const fp = frameFingerprint();
          if (lastFp && fpDiff(fp, lastFp) < SIMILARITY_THRESHOLD) {
            skipped++;
            continue; // scroll parado/lento — quase igual ao frame anterior mantido
          }
          lastFp = fp;
          frames.push(canvas.toDataURL('image/jpeg', 0.7).split(',')[1]);
        } catch(e) { console.warn('Frame t='+time, e); }
        finally {
          if (typeof onProgress === 'function') onProgress(timeIndex + 1, times.length);
        }
      }
      if (skipped) console.log('[extractVideoFrames] frames quase-duplicados pulados:', skipped);

      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      if (frames.length > 0) resolve(frames);
      else reject(new Error('Nenhum quadro legível foi extraído do vídeo.'));
      } catch(e) {
        try{document.body.removeChild(video);}catch(e2){}
        URL.revokeObjectURL(url);
        reject(e instanceof Error ? e : new Error(String(e)));
      }
    };

    video.onerror = (e) => {
      clearTimeout(metadataTimer);
      try{document.body.removeChild(video);}catch(e2){}
      URL.revokeObjectURL(url);
      const msg = e?.target?.error?.message || '';
      reject(new Error('Formato de vídeo não suportado. Use MP4 gravado pelo celular.\n' + msg));
    };
    video.load();
  });
}

async function extractTripsFromImage(file, plat) {
  const b64 = await new Promise((res,rej) => {
    const r = new FileReader();
    r.onload = () => res(r.result.split(',')[1]); r.onerror = () => rej(r.error); r.readAsDataURL(file);
  });
  return extractTripsFromBase64(b64, file.type||'image/jpeg', plat);
}

async function extractTripsFromBase64(b64, mt, plat) {
  const cashWarning = 'ATENÇÃO — confusão comum: "Dinheiro recebido" (ícone de câmera/recibo) NÃO é gorjeta nem dinâmico, é so a forma de pagamento (o passageiro pagou em espécie). Quando uma corrida tiver "Preço dinâmico" (ícone de raio) E "Dinheiro recebido" juntas, são DUAS linhas diferentes — extraia SÓ o valor do "Preço dinâmico" em dinamico, e NUNCA coloque o valor de "Dinheiro recebido" em gorjeta (nem em dinamico, nem em value). Exemplo: corrida com "R$ 12,50 Preço dinâmico" e "R$ 27,05 Dinheiro recebido" → dinamico:12.50, gorjeta:0.00 (o R$27,05 não entra em lugar nenhum do JSON).';
  // Prompt único — a IA identifica sozinha se é Uber ou 99 pelo layout da
  // imagem (em vez de confiar em qual botão o usuário tocou, que pode estar
  // errado). Cada corrida retornada inclui "platform":"uber"|"99".
  const prompt = 'Esta imagem mostra uma tela do app de motorista — pode ser: (1) UBER, tela "Histórico de ganhos" (lista de corridas com linha divisória entre elas, "Preço dinâmico" com ícone de raio, "Valor extra" com ícone de mão/coração = gorjeta); (2) 99 LISTA, tela "Histórico de corridas" do app 99 Motorista (ícone de carro + texto "Pop", tag laranja "Taxa Fixa de X%", "Ganhos pagos no app" ou "Ganhos pagos em dinheiro"); ou (3) 99 DETALHE, tela "Detalhes da corrida" do 99 (título "Detalhes da corrida" no topo, "Você ganhou R$X" em destaque, campos "Duração" e "Distância"). PRIMEIRO identifique qual das três telas é, pelo layout, e siga SOMENTE as regras dessa tela, abaixo. Inclua "platform":"uber" ou "platform":"99" em cada item do JSON (uma imagem só tem UMA plataforma e UM tipo de tela, nunca misture).\n\n' +
    '=== SE FOR UBER ===\n' +
    'Extraia CADA corrida, mas SÓ quando ela aparecer COMPLETA na imagem (com a linha divisória acima e abaixo) — se uma corrida estiver cortada no topo ou na base da imagem (incompleta), IGNORE essa corrida, não tente adivinhar ou completar com dados de outra. Ignore Missão (R$ 0,00). ' + cashWarning + ' Abaixo do valor principal de cada corrida também pode aparecer "Valor extra" (ícone de mão/coração — é a gorjeta do passageiro) — extraia esse valor em gorjeta quando existir. Também pode aparecer "Pedágio R$X,XX" — extraia esse valor em pedagio quando existir (0.00 quando não aparecer). Cada corrida mostra dois endereços (origem e destino): copie a linha de texto INTEIRA, palavra por palavra, exatamente como está escrita na tela, sem resumir ou cortar nenhuma parte, em "originFull"/"destFull". E TAMBÉM extraia só o nome do bairro (sem rua, número, cidade, UF ou CEP) em "origin"/"dest" — ex: se o texto na tela é "Rua Corcovado, 213, Jardim Botânico - Rio de Janeiro - RJ, 22460-050, BR", então origin="Jardim Botânico" e originFull="Rua Corcovado, 213, Jardim Botânico - Rio de Janeiro - RJ, 22460-050, BR" (a linha inteira, não abreviada). Formato de cada item: {"platform":"uber","time":"HH:MM","value":0.00,"km":0.0,"duration":0,"category":"Black","origin":"bairro","originFull":"endereço completo","dest":"bairro","destFull":"endereço completo","dinamico":0.00,"gorjeta":0.00,"pedagio":0.00,"taxaZero":false,"dinheiro":false}. duration=minutos inteiros. dinamico, gorjeta e pedagio ficam 0.00 quando não aparecerem. "taxaZero":true se houver indicação de 0% de taxa. "dinheiro":true se pagamento em dinheiro.\n\n' +
    '=== SE FOR 99 ===\n' +
    'Estrutura de CADA corrida, sempre nesta ordem: ' +
    '(1) Data e hora no formato "DD/MM/AAAA HH:MM" — extraia SOMENTE o HH:MM como campo "time". ' +
    '(2) Fo
===== video @ 809856 =====
P);
        for (let i = 0; i < g.length; i++) {
          const o = i*4;
          g[i] = (d[o] + d[o+1] + d[o+2]) / 3;
        }
        return g;
      }
      function fpDiff(a, b) {
        let sum = 0;
        for (let i = 0; i < a.length; i++) sum += Math.abs(a[i]-b[i]);
        return sum / (a.length * 255); // normalizado 0..1
      }
      const SIMILARITY_THRESHOLD = 0.02; // <2% de mudança = frame quase idêntico, pula

      const frames = [];
      let lastFp = null;
      let skipped = 0;
      if (typeof onProgress === 'function') onProgress(0, times.length);
      for (let timeIndex = 0; timeIndex < times.length; timeIndex++) {
        const time = times[timeIndex];
        try {
          const frameDrawn = await new Promise((res) => {
            // Só considera o seek concluído quando há um frame decodificado de
            // verdade. O fallback não transforma ausência de frame em imagem preta.
            let done = false;
            const finishSeek = (fromEvent) => {
              if (done) return;
              const draw = () => {
                if (done) return;
                done = true;
                clearTimeout(timer);
                video.onseeked = null;
                let ok = false;
                try {
                  if (video.readyState >= 2 && video.videoWidth > 0 && video.videoHeight > 0) {
                    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
                    ok = true;
                  }
                } catch(e) {}
                res(ok);
              };
              if (fromEvent && typeof video.requestVideoFrameCallback === 'function') {
                try { video.requestVideoFrameCallback(() => draw()); return; } catch(e) {}
              }
              if (fromEvent) setTimeout(draw, 120); else draw();
            };
            const timer = setTimeout(() => finishSeek(false), 4500);
            video.onseeked = () => finishSeek(true);
            video.currentTime = Math.min(time, Math.max(0, duration - 0.03));
          });
          if (!frameDrawn) continue;
          // Detecta conteúdo no QUADRO INTEIRO. Antes olhava só 20x20 px do
          // canto superior esquerdo; em screen recording esse canto pode ser
          // preto (status bar/recorte) e todos os frames reais eram descartados.
          const probe = document.createElement('canvas');
          probe.width = 24; probe.height = 24;
          const pctx = probe.getContext('2d');
          pctx.drawImage(canvas, 0, 0, probe.width, probe.height);
          const px = pctx.getImageData(0, 0, probe.width, probe.height).data;
          let lit = 0, maxLum = 0;
          for (let pi = 0; pi < px.length; pi += 4) {
            const lum = (px[pi] + px[pi+1] + px[pi+2]) / 3;
            if (lum > 12) lit++;
            if (lum > maxLum) maxLum = lum;
          }
          const hasContent = maxLum > 18 && lit >= 3;
          if (!hasContent) continue;
          const fp = frameFingerprint();
          if (lastFp && fpDiff(fp, lastFp) < SIMILARITY_THRESHOLD) {
            skipped++;
            continue; // scroll parado/lento — quase igual ao frame anterior mantido
          }
          lastFp = fp;
          frames.push(canvas.toDataURL('image/jpeg', 0.7).split(',')[1]);
        } catch(e) { console.warn('Frame t='+time, e); }
        finally {
          if (typeof onProgress === 'function') onProgress(timeIndex + 1, times.length);
        }
      }
      if (skipped) console.log('[extractVideoFrames] frames quase-duplicados pulados:', skipped);

      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      if (frames.length > 0) resolve(frames);
      else reject(new Error('Nenhum quadro legível foi extraído do vídeo.'));
      } catch(e) {
        try{document.body.removeChild(video);}catch(e2){}
        URL.revokeObjectURL(url);
        reject(e instanceof Error ? e : new Error(String(e)));
      }
    };

    video.onerror = (e) => {
      clearTimeout(metadataTimer);
      try{document.body.removeChild(video);}catch(e2){}
      URL.revokeObjectURL(url);
      const msg = e?.target?.error?.message || '';
      reject(new Error('Formato de vídeo não suportado. Use MP4 gravado pelo celular.\n' + msg));
    };
    video.load();
  });
}

async function extractTripsFromImage(file, plat) {
  const b64 = await new Promise((res,rej) => {
    const r = new FileReader();
    r.onload = () => res(r.result.split(',')[1]); r.onerror = () => rej(r.error); r.readAsDataURL(file);
  });
  return extractTripsFromBase64(b64, file.type||'image/jpeg', plat);
}

async function extractTripsFromBase64(b64, mt, plat) {
  const cashWarning = 'ATENÇÃO — confusão comum: "Dinheiro recebido" (ícone de câmera/recibo) NÃO é gorjeta nem dinâmico, é so a forma de pagamento (o passageiro pagou em espécie). Quando uma corrida tiver "Preço dinâmico" (ícone de raio) E "Dinheiro recebido" juntas, são DUAS linhas diferentes — extraia SÓ o valor do "Preço dinâmico" em dinamico, e NUNCA coloque o valor de "Dinheiro recebido" em gorjeta (nem em dinamico, nem em value). Exemplo: corrida com "R$ 12,50 Preço dinâmico" e "R$ 27,05 Dinheiro recebido" → dinamico:12.50, gorjeta:0.00 (o R$27,05 não entra em lugar nenhum do JSON).';
  // Prompt único — a IA identifica sozinha se é Uber ou 99 pelo layout da
  // imagem (em vez de confiar em qual botão o usuário tocou, que pode estar
  // errado). Cada corrida retornada inclui "platform":"uber"|"99".
  const prompt = 'Esta imagem mostra uma tela do app de motorista — pode ser: (1) UBER, tela "Histórico de ganhos" (lista de corridas com linha divisória entre elas, "Preço dinâmico" com ícone de raio, "Valor extra" com ícone de mão/coração = gorjeta); (2) 99 LISTA, tela "Histórico de corridas" do app 99 Motorista (ícone de carro + texto "Pop", tag laranja "Taxa Fixa de X%", "Ganhos pagos no app" ou "Ganhos pagos em dinheiro"); ou (3) 99 DETALHE, tela "Detalhes da corrida" do 99 (título "Detalhes da corrida" no topo, "Você ganhou R$X" em destaque, campos "Duração" e "Distância"). PRIMEIRO identifique qual das três telas é, pelo layout, e siga SOMENTE as regras dessa tela, abaixo. Inclua "platform":"uber" ou "platform":"99" em cada item do JSON (uma imagem só tem UMA plataforma e UM tipo de tela, nunca misture).\n\n' +
    '=== SE FOR UBER ===\n' +
    'Extraia CADA corrida, mas SÓ quando ela aparecer COMPLETA na imagem (com a linha divisória acima e abaixo) — se uma corrida estiver cortada no topo ou na base da imagem (incompleta), IGNORE essa corrida, não tente adivinhar ou completar com dados de outra. Ignore Missão (R$ 0,00). ' + cashWarning + ' Abaixo do valor principal de cada corrida também pode aparecer "Valor extra" (ícone de mão/coração — é a gorjeta do passageiro) — extraia esse valor em gorjeta quando existir. Também pode aparecer "Pedágio R$X,XX" — extraia esse valor em pedagio quando existir (0.00 quando não aparecer). Cada corrida mostra dois endereços (origem e destino): copie a linha de texto INTEIRA, palavra por palavra, exatamente como está escrita na tela, sem resumir ou cortar nenhuma parte, em "originFull"/"destFull". E TAMBÉM extraia só o nome do bairro (sem rua, número, cidade, UF ou CEP) em "origin"/"dest" — ex: se o texto na tela é "Rua Corcovado, 213, Jardim Botânico - Rio de Janeiro - RJ, 22460-050, BR", então origin="Jardim Botânico" e originFull="Rua Corcovado, 213, Jardim Botânico - Rio de Janeiro - RJ, 22460-050, BR" (a linha inteira, não abreviada). Formato de cada item: {"platform":"uber","time":"HH:MM","value":0.00,"km":0.0,"duration":0,"category":"Black","origin":"bairro","originFull":"endereço completo","dest":"bairro","destFull":"endereço completo","dinamico":0.00,"gorjeta":0.00,"pedagio":0.00,"taxaZero":false,"dinheiro":false}. duration=minutos inteiros. dinamico, gorjeta e pedagio ficam 0.00 quando não aparecerem. "taxaZero":true se houver indicação de 0% de taxa. "dinheiro":true se pagamento em dinheiro.\n\n' +
    '=== SE FOR 99 ===\n' +
    'Estrutura de CADA corrida, sempre nesta ordem: ' +
    '(1) Data e hora no formato "DD/MM/AAAA HH:MM" — extraia SOMENTE o HH:MM como campo "time". ' +
    '(2) Forma de pagamento: "Ganhos pagos no app" ou "Ganhos pagos em dinheiro" — IGNORE, não é valor nem corrida. ' +
    '(3) Ícone de carro + nome da categoria (pode ser "Pop", "Negocia", "Comfort" ou outra — leia o que estiver escrito, não assuma que é sempre "Pop") + valor "R$X,XX" à direita — este R$X,XX é o "value" da corrida, não importa qual seja a categoria. Converta vírgula para ponto (R$7,90 → 7.90). Guarde o texto exato da categoria lida em "category" com o prefixo "99" (ex: categoria "Pop" → "99Pop", categoria "Negocia" → "99Negocia"). ' +
    '(4) Tag laranja "Taxa Fixa de X%" — extraia o número X como "serviceFee" em formato decimal (ex: "Taxa Fixa de 15%" → serviceFee:0.15, "Taxa Fixa de 0%" → serviceFee:0). Não é gorjeta nem dinâmico. ' +
    '(5) Endereço de origem (ícone seta pra cima) e endereço de destino (ícone seta pra baixo) — copie a linha de texto INTEIRA, palavra por palavra, exatamente como está escrita na tela, sem resumir ou cortar nenhuma parte (incluindo nome de rua, número e complemento quando visíveis) em "originFull"/"destFull". E TAMBÉM extraia só o nome do bairro (sem rua, número, cidade, UF) em "origin"/"dest" — ex: se o texto na tela é "Rua Carmela Dutra, Agriões, Teresópolis - RJ", então origin="Agriões" e originFull="Rua Carmela Dutra, Agriões, Teresópolis - RJ" (a linha inteira, não abreviada). ' +
    '(5b) Pode aparecer "Pedágio R$X,XX" — extraia esse valor em pedagio quando existir (0.00 quando não aparecer). ' +
    '(6) Status verde "Pago" ou "Pedido finalizado" — marca o FIM desta corrida. ' +
    '(7) CABEÇALHO FIXO QUE NÃO ROLA: no topo da tela ficam fixas as abas "Corridas / Entregas" e a barra de filtros ("18/7 ▼", "Taxas ▼", "Pagamento ▼") — esse bloco NUNCA rola junto com a lista. Enquanto a lista sobe, a linha de data/hora de cada corrida desaparece po
===== video @ 845672 =====
ips || [])
    .map(t => {
      let ts = t.ts || 0;
      if (!ts && t.time) ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
      return { ...t, _ts: ts };
    })
    .filter(t => t._ts > 0)
    .sort((a, b) => a._ts - b._ts);
  
  if (!trips.length) return { grouped: 0, created: 0, expanded: 0, sessions: day.sessions || [] };
  
  // Cópia profunda das sessões para não mutar durante dry run
  const sessions = JSON.parse(JSON.stringify(day.sessions || []));
  let grouped = 0, created = 0, expanded = 0;
  
  for (const trip of trips) {
    const ts  = trip._ts;
    const dur = (trip.duration || 15) * 60 * 1000;
    const end = ts + dur;
    
    // Já está dentro de uma sessão existente?
    const inside = sessions.find(s =>
      s.start && ts >= (s.start - 1000) && (!s.end || ts <= (s.end + 1000))
    );
    if (inside) continue;
    
    // Sessão mais próxima (distância ao início ou fim)
    let best = null, bestDist = Infinity;
    for (const s of sessions) {
      if (!s.start) continue;
      const d = Math.min(
        Math.abs(ts - s.start),
        s.end ? Math.abs(ts - s.end) : Math.abs(ts - s.start),
        s.end ? Math.abs(end - s.end) : Infinity
      );
      if (d < bestDist) { bestDist = d; best = s; }
    }
    
    if (best && bestDist < THRESHOLD) {
      // Agrupar + expandir horários da sessão
      if (ts < best.start) { best.start = ts; expanded++; }
      if (!best.end || end > best.end) { best.end = end; expanded++; }
      grouped++;
    } else {
      // Criar nova sessão para cobrir esta corrida
      sessions.push({
        id: DB.uid(),
        start: ts,
        end:   end,
        km:    parseFloat((trip.km || 0).toFixed(3)),
        elapsed: Math.round((trip.duration || 15) * 60)
      });
      sessions.sort((a, b) => (a.start || 0) - (b.start || 0));
      created++;
    }
  }
  
  if (!dryRun) {
    day.sessions = sessions;
    DB.saveDay(day);
    // Sincronizar novas/atualizadas sessões com Supabase
    if (_supaUser) {
      sessions.forEach(s => {
        _SUPA.from('sessions').upsert({
          id: s.id, user_id: _supaUser.id, date: dateStr,
          start_ms: s.start, end_ms: s.end,
          km: s.km || 0, elapsed_secs: s.elapsed || 0,
          paused_ms: 0, is_paused: false
        }, { onConflict: 'id' }).catch(() => {});
      });
    }
  }
  
  return { grouped, created, expanded, sessions };
}

// Rodar reconciliação em todos os dias com corridas (migração única)
async function reconcileAllExistingDays() {
  if (localStorage.getItem('sm_session_reconcile_v1')) return;
  
  const dates = DB.allDates();
  let totalGrouped = 0, totalCreated = 0;
  
  for (const dateStr of dates) {
    const day = DB.day(dateStr);
    if (!(day.trips || []).length) continue;
    const r = reconcileSessionsForDay(dateStr);
    totalGrouped += r.grouped;
    totalCreated += r.created;
  }
  
  localStorage.setItem('sm_session_reconcile_v1', '1');
  if (totalCreated > 0 || totalGrouped > 0) {
    showToast(
      `${totalCreated} sessão(ões) criada(s) e ${totalGrouped} corrida(s) agrupada(s) automaticamente.`,
      'info', 5000
    );
    setTimeout(() => { try { renderJornada(); } catch(e) {} }, 80);
  }
}

async function confirmImport() {
  const selected = (window._pendingTrips||[]).filter(t=>t._sel);
  if (!selected.length) { closeMo('mo-import-confirm'); return; }
  const day = DB.day(S.curDate);
  let added=0, skipped=0, updated=0;
  const toUpdateAuto = [];
  const toPushAuto = [];
  const videoId = (window._pendingImportVideoIds||[])[0] || null;
  selected.forEach(t => {
    // Já casada com uma auto_trip existente (matching já feito em
    // processImportBatch, antes da tela abrir) — só atualiza o valor,
    // nunca o endereço (esse já veio validado pela captura ao vivo/GPS).
    if (t._matchedAutoTripId) {
      toUpdateAuto.push({ id: t._matchedAutoTripId, value: parseFloat(t.value)||0, km: parseFloat(t.km)||0,
        dinheiro: !!t.dinheiro });
      updated++;
      return;
    }
    // Nova — nunca vista na captura ao vivo. Vira auto_trips já Confirmada
    // pelo vídeo; 'Importada' agora é somente a tag de origem — unifica com o resto dos
    // dados em vez de virar corrida "manual" separada. Endereço já
    // passou pela validação looksLikeAddress lá em processImportBatch
    // (se não passou, já veio vazio aqui — nunca inventa).
    let startMs;
    try {
      const opDate = window._pendingImportOperationalDate || S.curDate || DB.today();
      startMs = operationalTimestampForTime(t.platform || window._pendingImportPlatform, opDate, t.time||'00:00');
    } catch(e) { startMs = NaN; }
    if (!startMs || isNaN(startMs)) startMs = Date.now();
    const endMs = t.duration > 0 ? startMs + t.duration*60000 : null;
    toPushAuto.push({
      user_id: _supaUser.id,
      platform: t.platform === '99' ? '99' : 'uber',
      accepted_at: new Date(startMs).toISOString(),
      trip_started_at: new Date(startMs).toISOString(),
      trip_ended_at: endMs ? new Date(endMs).toISOString() : null,
      offer_value: parseFloat(t.value)||0,
      real_km_trip: parseFloat(t.km)||0,
      origin_address: t.originFull || null,
      dest_address: t.destFull || null,
      dinheiro: !!t.dinheiro,
      status: 'confirmada',
      capture_source: 'importada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId
    });
    added++;
  });
  // Aplica as atualizações nas automáticas casadas — valor real por cima do
  // estimado, km real quando a importação trouxe (senão mantém o que já
  // tinha), status vira confirmada (agora tem valor confirmado de verdade).
  toUpdateAuto.forEach(u => {
    _SUPA.from('auto_trips').update({
      offer_value: u.value,
      real_km_trip: u.km > 0 ? u.km : undefined,
      dinheiro: u.dinheiro,
      status: 'confirmada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId || undefined
    }).eq('id', u.id).then(() => {}).catch(() => {});
  });
  // Corridas novas — insere de uma vez em auto_trips.
  if (toPushAuto.length) {
    _SUPA.from('auto_trips').insert(toPushAuto).then(() => {}).catch((e) => console.warn('insert importada falhou', e));
  }
  // Atualiza a contagem de corridas confirmadas/criadas por esse vídeo —
  // só informativo, útil pra revisão manual depois.
  if (videoId) {
    _SUPA.from('import_videos').update({ trip_count: toUpdateAuto.length + toPushAuto.length }).eq('id', videoId).then(()=>{}).catch(()=>{});
  }
  window._pendingImportVideoIds = null;
  if (toUpdateAuto.length || toPushAuto.length) { S.autoTripsDate = null; } // força recarregar no próximo renderJornada
  DB.saveDay(day); closeMo('mo-import-confirm'); window._pendingTrips=null;

  const dateHint = S.curDate !== DB.today()
    ? ` (data: ${S.curDate.split('-').reverse().slice(0,2).join('/')})` : '';
  const parts = [];
  if (added > 0) parts.push(`${added} corrida${added!==1?'s':''} nova${added!==1?'s':''} importada${added!==1?'s':''}${dateHint}`);
  if (updated > 0) parts.push(`${updated} já capturada${updated!==1?'s':''} automaticamente, valor confirmado`);
  if (skipped > 0) parts.push(`${skipped} ignorada${skipped!==1?'s':''} (já existente${skipped!==1?'s':''})`);
  const baseMsg = parts.length ? parts.join('. ')+'.' : 'Nenhuma corrida nova.';

  try{renderJornada();}catch(e){}
  try{renderHome();}catch(e){}
  // Garantir que Corridas mostra a data certa independente do filtro
  if (S.curDate !== DB.today()) S.corridasPeriod = 'tudo';
  try{renderCorridasActive();}catch(e){}
  showToast(baseMsg, skipped > 0 ? 'info' : 'success');
}

// ══════════════════════════════════════════════
// IMPORTAÇÃO: histórico oficial do Uber (.zip)
// driver_lifetime_trips-0.csv já traz, por corrida, o valor líquido
// (driver_upfront_fare_local), km e duração — usamos direto daí, sem
// tentar casar com driver_payments por horário (testado com um export
// real: o casamento por horário troca valores entre corridas vizinhas).
// driver_payments-0.csv só é usado pra somar gorjeta/incentivo do
// período inteiro, como informação, sem tentar atribuir por corrida.
// ══════════════════════════════════════════════

function uzRound2(n) { return Math.round(n * 100) / 100; }

// Aceita "10.50" e "10,50" — exportações da Uber variam o separador por região/locale.
function uzNum(raw) {
  if (raw == null || raw === '') return 0;
  return parseFloat(String(raw).replace(',', '.')) || 0;
}

function uzParseTs(raw) {
  if (!raw) return null;
  // O timestamp do zip vem em UTC (tem 'Z' no final) — converte pra Brasília (UTC-3)
  const raw2 = String(raw).trim();
  try {
    const d = new Date(raw2.includes('T') ? raw2 : raw2.replace(' ', 'T') + 'Z');
    if (isNaN(d.getTime())) return null;
    // subtrai 3h pra Brasília
    const brt = new Date(d.getTime() - 3 * 60 * 60 * 1000);
    const pad = n => String(n).padStart(2, '0');
    const dateStr = brt.getUTCFullYear() + '-' + pad(brt.getUTCMonth()+1) + '-' + pad(brt.getUTCDate());
    const timeStr = pad(brt.getUTCHours()) + ':' + pad(brt.getUTCMinutes());
    return { dateStr, timeStr };
  } catch(e) { return null; }
}

// Soma gorjeta e incentivo/promoção do período inteiro (informativo, não por corrida).
function uzAggregatePayments(rows) {
  let tip = 0, promo = 0;
  for (const r of rows) {
    const amt = uzNum(r['Local Amount']);
    if (r['Category'] === 'tip') tip += amt;
    if (r['Category'] === 'existing_driver_incentive') promo += amt;
  }
  return { tip: uzRound2(tip), promo: uzRound2(promo) };
}

// Monta as corridas concluídas direto do driver_lifetime_trips. Corridas canceladas
// (status diferente de 'completed') são ignoradas — não são corridas de fato.
function uzBuildTrips(rows) {
  const trips = [];
  for (const r of rows) {
    if (r['status'] !== 'completed') continue;
    const begin = uzParseTs(r['begintrip_timestamp_local']) || uzParseTs(r['request_timestamp_local']);
    if (!begin) continue;
    const value = uzNum(
===== video @ 847551 =====
day.sessions = sessions;
    DB.saveDay(day);
    // Sincronizar novas/atualizadas sessões com Supabase
    if (_supaUser) {
      sessions.forEach(s => {
        _SUPA.from('sessions').upsert({
          id: s.id, user_id: _supaUser.id, date: dateStr,
          start_ms: s.start, end_ms: s.end,
          km: s.km || 0, elapsed_secs: s.elapsed || 0,
          paused_ms: 0, is_paused: false
        }, { onConflict: 'id' }).catch(() => {});
      });
    }
  }
  
  return { grouped, created, expanded, sessions };
}

// Rodar reconciliação em todos os dias com corridas (migração única)
async function reconcileAllExistingDays() {
  if (localStorage.getItem('sm_session_reconcile_v1')) return;
  
  const dates = DB.allDates();
  let totalGrouped = 0, totalCreated = 0;
  
  for (const dateStr of dates) {
    const day = DB.day(dateStr);
    if (!(day.trips || []).length) continue;
    const r = reconcileSessionsForDay(dateStr);
    totalGrouped += r.grouped;
    totalCreated += r.created;
  }
  
  localStorage.setItem('sm_session_reconcile_v1', '1');
  if (totalCreated > 0 || totalGrouped > 0) {
    showToast(
      `${totalCreated} sessão(ões) criada(s) e ${totalGrouped} corrida(s) agrupada(s) automaticamente.`,
      'info', 5000
    );
    setTimeout(() => { try { renderJornada(); } catch(e) {} }, 80);
  }
}

async function confirmImport() {
  const selected = (window._pendingTrips||[]).filter(t=>t._sel);
  if (!selected.length) { closeMo('mo-import-confirm'); return; }
  const day = DB.day(S.curDate);
  let added=0, skipped=0, updated=0;
  const toUpdateAuto = [];
  const toPushAuto = [];
  const videoId = (window._pendingImportVideoIds||[])[0] || null;
  selected.forEach(t => {
    // Já casada com uma auto_trip existente (matching já feito em
    // processImportBatch, antes da tela abrir) — só atualiza o valor,
    // nunca o endereço (esse já veio validado pela captura ao vivo/GPS).
    if (t._matchedAutoTripId) {
      toUpdateAuto.push({ id: t._matchedAutoTripId, value: parseFloat(t.value)||0, km: parseFloat(t.km)||0,
        dinheiro: !!t.dinheiro });
      updated++;
      return;
    }
    // Nova — nunca vista na captura ao vivo. Vira auto_trips já Confirmada
    // pelo vídeo; 'Importada' agora é somente a tag de origem — unifica com o resto dos
    // dados em vez de virar corrida "manual" separada. Endereço já
    // passou pela validação looksLikeAddress lá em processImportBatch
    // (se não passou, já veio vazio aqui — nunca inventa).
    let startMs;
    try {
      const opDate = window._pendingImportOperationalDate || S.curDate || DB.today();
      startMs = operationalTimestampForTime(t.platform || window._pendingImportPlatform, opDate, t.time||'00:00');
    } catch(e) { startMs = NaN; }
    if (!startMs || isNaN(startMs)) startMs = Date.now();
    const endMs = t.duration > 0 ? startMs + t.duration*60000 : null;
    toPushAuto.push({
      user_id: _supaUser.id,
      platform: t.platform === '99' ? '99' : 'uber',
      accepted_at: new Date(startMs).toISOString(),
      trip_started_at: new Date(startMs).toISOString(),
      trip_ended_at: endMs ? new Date(endMs).toISOString() : null,
      offer_value: parseFloat(t.value)||0,
      real_km_trip: parseFloat(t.km)||0,
      origin_address: t.originFull || null,
      dest_address: t.destFull || null,
      dinheiro: !!t.dinheiro,
      status: 'confirmada',
      capture_source: 'importada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId
    });
    added++;
  });
  // Aplica as atualizações nas automáticas casadas — valor real por cima do
  // estimado, km real quando a importação trouxe (senão mantém o que já
  // tinha), status vira confirmada (agora tem valor confirmado de verdade).
  toUpdateAuto.forEach(u => {
    _SUPA.from('auto_trips').update({
      offer_value: u.value,
      real_km_trip: u.km > 0 ? u.km : undefined,
      dinheiro: u.dinheiro,
      status: 'confirmada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId || undefined
    }).eq('id', u.id).then(() => {}).catch(() => {});
  });
  // Corridas novas — insere de uma vez em auto_trips.
  if (toPushAuto.length) {
    _SUPA.from('auto_trips').insert(toPushAuto).then(() => {}).catch((e) => console.warn('insert importada falhou', e));
  }
  // Atualiza a contagem de corridas confirmadas/criadas por esse vídeo —
  // só informativo, útil pra revisão manual depois.
  if (videoId) {
    _SUPA.from('import_videos').update({ trip_count: toUpdateAuto.length + toPushAuto.length }).eq('id', videoId).then(()=>{}).catch(()=>{});
  }
  window._pendingImportVideoIds = null;
  if (toUpdateAuto.length || toPushAuto.length) { S.autoTripsDate = null; } // força recarregar no próximo renderJornada
  DB.saveDay(day); closeMo('mo-import-confirm'); window._pendingTrips=null;

  const dateHint = S.curDate !== DB.today()
    ? ` (data: ${S.curDate.split('-').reverse().slice(0,2).join('/')})` : '';
  const parts = [];
  if (added > 0) parts.push(`${added} corrida${added!==1?'s':''} nova${added!==1?'s':''} importada${added!==1?'s':''}${dateHint}`);
  if (updated > 0) parts.push(`${updated} já capturada${updated!==1?'s':''} automaticamente, valor confirmado`);
  if (skipped > 0) parts.push(`${skipped} ignorada${skipped!==1?'s':''} (já existente${skipped!==1?'s':''})`);
  const baseMsg = parts.length ? parts.join('. ')+'.' : 'Nenhuma corrida nova.';

  try{renderJornada();}catch(e){}
  try{renderHome();}catch(e){}
  // Garantir que Corridas mostra a data certa independente do filtro
  if (S.curDate !== DB.today()) S.corridasPeriod = 'tudo';
  try{renderCorridasActive();}catch(e){}
  showToast(baseMsg, skipped > 0 ? 'info' : 'success');
}

// ══════════════════════════════════════════════
// IMPORTAÇÃO: histórico oficial do Uber (.zip)
// driver_lifetime_trips-0.csv já traz, por corrida, o valor líquido
// (driver_upfront_fare_local), km e duração — usamos direto daí, sem
// tentar casar com driver_payments por horário (testado com um export
// real: o casamento por horário troca valores entre corridas vizinhas).
// driver_payments-0.csv só é usado pra somar gorjeta/incentivo do
// período inteiro, como informação, sem tentar atribuir por corrida.
// ══════════════════════════════════════════════

function uzRound2(n) { return Math.round(n * 100) / 100; }

// Aceita "10.50" e "10,50" — exportações da Uber variam o separador por região/locale.
function uzNum(raw) {
  if (raw == null || raw === '') return 0;
  return parseFloat(String(raw).replace(',', '.')) || 0;
}

function uzParseTs(raw) {
  if (!raw) return null;
  // O timestamp do zip vem em UTC (tem 'Z' no final) — converte pra Brasília (UTC-3)
  const raw2 = String(raw).trim();
  try {
    const d = new Date(raw2.includes('T') ? raw2 : raw2.replace(' ', 'T') + 'Z');
    if (isNaN(d.getTime())) return null;
    // subtrai 3h pra Brasília
    const brt = new Date(d.getTime() - 3 * 60 * 60 * 1000);
    const pad = n => String(n).padStart(2, '0');
    const dateStr = brt.getUTCFullYear() + '-' + pad(brt.getUTCMonth()+1) + '-' + pad(brt.getUTCDate());
    const timeStr = pad(brt.getUTCHours()) + ':' + pad(brt.getUTCMinutes());
    return { dateStr, timeStr };
  } catch(e) { return null; }
}

// Soma gorjeta e incentivo/promoção do período inteiro (informativo, não por corrida).
function uzAggregatePayments(rows) {
  let tip = 0, promo = 0;
  for (const r of rows) {
    const amt = uzNum(r['Local Amount']);
    if (r['Category'] === 'tip') tip += amt;
    if (r['Category'] === 'existing_driver_incentive') promo += amt;
  }
  return { tip: uzRound2(tip), promo: uzRound2(promo) };
}

// Monta as corridas concluídas direto do driver_lifetime_trips. Corridas canceladas
// (status diferente de 'completed') são ignoradas — não são corridas de fato.
function uzBuildTrips(rows) {
  const trips = [];
  for (const r of rows) {
    if (r['status'] !== 'completed') continue;
    const begin = uzParseTs(r['begintrip_timestamp_local']) || uzParseTs(r['request_timestamp_local']);
    if (!begin) continue;
    const value = uzNum(r['driver_upfront_fare_local']) || uzNum(r['original_fare_local']);
    trips.push({
      date: begin.dateStr, time: begin.timeStr,
      value: uzRound2(Math.max(0, value)),
      km: r['trip_distance_miles'] ? uzRound2(uzNum(r['trip_distance_miles']) * 1.60934) : 0,
      // o app guarda duração em MINUTOS (a IA de import por foto também usa minutos);
      // o export da Uber vem em segundos, por isso a divisão por 60 aqui.
      duration: r['trip_duration_seconds'] ? Math.round(uzNum(r['trip_duration_seconds']) / 60) : 0,
      category: r['global_product_name'] || '',
      surge: r['is_surged'] === 'true',
      airport: r['is_airport_trip'] === 'true',
      // dinamico junta tarifa dinamica (surge) + promocao/desconto da corrida num
      // valor só — já incluso no "value", mostrado separado só pra detalhar a corrida.
      // Pode ficar negativo se o desconto for maior que o surge.
      dinamico:    uzRound2(Math.max(0, uzNum(r['surge_fare_local'])) + uzNum(r['promotion_local'])),
      dinheiro:   r['is_cash_trip'] === 'true',
      pedagio:    uzNum(r['toll_amount_local']) > 0,
      serviceFee: (r['service_fee'] !== undefined && r['service_fee'] !== '') ? parseFloat(r['service_fee']) : null,
      taxaZero:   r['service_fee'] === '0' || r['service_fee'] === '0.0',
    });
  }
  return trips;
}

function uzFindEntry(zip, suffix) {
  const f = Object.values(zip.files).find(x => !x.dir && x.name.endsWith(suffix));
  if (!f) throw new Error('Não encontrei "' + suffix + '" no zip. Confira se é o arquivo certo da Uber.');
  return f;
}

let _uzResult = null;

async function handleUberZipFile(input) {
  const file = input.files[0]; if (!file) return;
  input.value = '';
  openMo('mo-uberzip');
  document.getElementById('uz-proc').style.display = 'block';
  document.getElementById('uz-res').style.display = 'none';
  const stepEl = document.
===== video @ 848532 =====
d += r.created;
  }
  
  localStorage.setItem('sm_session_reconcile_v1', '1');
  if (totalCreated > 0 || totalGrouped > 0) {
    showToast(
      `${totalCreated} sessão(ões) criada(s) e ${totalGrouped} corrida(s) agrupada(s) automaticamente.`,
      'info', 5000
    );
    setTimeout(() => { try { renderJornada(); } catch(e) {} }, 80);
  }
}

async function confirmImport() {
  const selected = (window._pendingTrips||[]).filter(t=>t._sel);
  if (!selected.length) { closeMo('mo-import-confirm'); return; }
  const day = DB.day(S.curDate);
  let added=0, skipped=0, updated=0;
  const toUpdateAuto = [];
  const toPushAuto = [];
  const videoId = (window._pendingImportVideoIds||[])[0] || null;
  selected.forEach(t => {
    // Já casada com uma auto_trip existente (matching já feito em
    // processImportBatch, antes da tela abrir) — só atualiza o valor,
    // nunca o endereço (esse já veio validado pela captura ao vivo/GPS).
    if (t._matchedAutoTripId) {
      toUpdateAuto.push({ id: t._matchedAutoTripId, value: parseFloat(t.value)||0, km: parseFloat(t.km)||0,
        dinheiro: !!t.dinheiro });
      updated++;
      return;
    }
    // Nova — nunca vista na captura ao vivo. Vira auto_trips já Confirmada
    // pelo vídeo; 'Importada' agora é somente a tag de origem — unifica com o resto dos
    // dados em vez de virar corrida "manual" separada. Endereço já
    // passou pela validação looksLikeAddress lá em processImportBatch
    // (se não passou, já veio vazio aqui — nunca inventa).
    let startMs;
    try {
      const opDate = window._pendingImportOperationalDate || S.curDate || DB.today();
      startMs = operationalTimestampForTime(t.platform || window._pendingImportPlatform, opDate, t.time||'00:00');
    } catch(e) { startMs = NaN; }
    if (!startMs || isNaN(startMs)) startMs = Date.now();
    const endMs = t.duration > 0 ? startMs + t.duration*60000 : null;
    toPushAuto.push({
      user_id: _supaUser.id,
      platform: t.platform === '99' ? '99' : 'uber',
      accepted_at: new Date(startMs).toISOString(),
      trip_started_at: new Date(startMs).toISOString(),
      trip_ended_at: endMs ? new Date(endMs).toISOString() : null,
      offer_value: parseFloat(t.value)||0,
      real_km_trip: parseFloat(t.km)||0,
      origin_address: t.originFull || null,
      dest_address: t.destFull || null,
      dinheiro: !!t.dinheiro,
      status: 'confirmada',
      capture_source: 'importada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId
    });
    added++;
  });
  // Aplica as atualizações nas automáticas casadas — valor real por cima do
  // estimado, km real quando a importação trouxe (senão mantém o que já
  // tinha), status vira confirmada (agora tem valor confirmado de verdade).
  toUpdateAuto.forEach(u => {
    _SUPA.from('auto_trips').update({
      offer_value: u.value,
      real_km_trip: u.km > 0 ? u.km : undefined,
      dinheiro: u.dinheiro,
      status: 'confirmada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId || undefined
    }).eq('id', u.id).then(() => {}).catch(() => {});
  });
  // Corridas novas — insere de uma vez em auto_trips.
  if (toPushAuto.length) {
    _SUPA.from('auto_trips').insert(toPushAuto).then(() => {}).catch((e) => console.warn('insert importada falhou', e));
  }
  // Atualiza a contagem de corridas confirmadas/criadas por esse vídeo —
  // só informativo, útil pra revisão manual depois.
  if (videoId) {
    _SUPA.from('import_videos').update({ trip_count: toUpdateAuto.length + toPushAuto.length }).eq('id', videoId).then(()=>{}).catch(()=>{});
  }
  window._pendingImportVideoIds = null;
  if (toUpdateAuto.length || toPushAuto.length) { S.autoTripsDate = null; } // força recarregar no próximo renderJornada
  DB.saveDay(day); closeMo('mo-import-confirm'); window._pendingTrips=null;

  const dateHint = S.curDate !== DB.today()
    ? ` (data: ${S.curDate.split('-').reverse().slice(0,2).join('/')})` : '';
  const parts = [];
  if (added > 0) parts.push(`${added} corrida${added!==1?'s':''} nova${added!==1?'s':''} importada${added!==1?'s':''}${dateHint}`);
  if (updated > 0) parts.push(`${updated} já capturada${updated!==1?'s':''} automaticamente, valor confirmado`);
  if (skipped > 0) parts.push(`${skipped} ignorada${skipped!==1?'s':''} (já existente${skipped!==1?'s':''})`);
  const baseMsg = parts.length ? parts.join('. ')+'.' : 'Nenhuma corrida nova.';

  try{renderJornada();}catch(e){}
  try{renderHome();}catch(e){}
  // Garantir que Corridas mostra a data certa independente do filtro
  if (S.curDate !== DB.today()) S.corridasPeriod = 'tudo';
  try{renderCorridasActive();}catch(e){}
  showToast(baseMsg, skipped > 0 ? 'info' : 'success');
}

// ══════════════════════════════════════════════
// IMPORTAÇÃO: histórico oficial do Uber (.zip)
// driver_lifetime_trips-0.csv já traz, por corrida, o valor líquido
// (driver_upfront_fare_local), km e duração — usamos direto daí, sem
// tentar casar com driver_payments por horário (testado com um export
// real: o casamento por horário troca valores entre corridas vizinhas).
// driver_payments-0.csv só é usado pra somar gorjeta/incentivo do
// período inteiro, como informação, sem tentar atribuir por corrida.
// ══════════════════════════════════════════════

function uzRound2(n) { return Math.round(n * 100) / 100; }

// Aceita "10.50" e "10,50" — exportações da Uber variam o separador por região/locale.
function uzNum(raw) {
  if (raw == null || raw === '') return 0;
  return parseFloat(String(raw).replace(',', '.')) || 0;
}

function uzParseTs(raw) {
  if (!raw) return null;
  // O timestamp do zip vem em UTC (tem 'Z' no final) — converte pra Brasília (UTC-3)
  const raw2 = String(raw).trim();
  try {
    const d = new Date(raw2.includes('T') ? raw2 : raw2.replace(' ', 'T') + 'Z');
    if (isNaN(d.getTime())) return null;
    // subtrai 3h pra Brasília
    const brt = new Date(d.getTime() - 3 * 60 * 60 * 1000);
    const pad = n => String(n).padStart(2, '0');
    const dateStr = brt.getUTCFullYear() + '-' + pad(brt.getUTCMonth()+1) + '-' + pad(brt.getUTCDate());
    const timeStr = pad(brt.getUTCHours()) + ':' + pad(brt.getUTCMinutes());
    return { dateStr, timeStr };
  } catch(e) { return null; }
}

// Soma gorjeta e incentivo/promoção do período inteiro (informativo, não por corrida).
function uzAggregatePayments(rows) {
  let tip = 0, promo = 0;
  for (const r of rows) {
    const amt = uzNum(r['Local Amount']);
    if (r['Category'] === 'tip') tip += amt;
    if (r['Category'] === 'existing_driver_incentive') promo += amt;
  }
  return { tip: uzRound2(tip), promo: uzRound2(promo) };
}

// Monta as corridas concluídas direto do driver_lifetime_trips. Corridas canceladas
// (status diferente de 'completed') são ignoradas — não são corridas de fato.
function uzBuildTrips(rows) {
  const trips = [];
  for (const r of rows) {
    if (r['status'] !== 'completed') continue;
    const begin = uzParseTs(r['begintrip_timestamp_local']) || uzParseTs(r['request_timestamp_local']);
    if (!begin) continue;
    const value = uzNum(r['driver_upfront_fare_local']) || uzNum(r['original_fare_local']);
    trips.push({
      date: begin.dateStr, time: begin.timeStr,
      value: uzRound2(Math.max(0, value)),
      km: r['trip_distance_miles'] ? uzRound2(uzNum(r['trip_distance_miles']) * 1.60934) : 0,
      // o app guarda duração em MINUTOS (a IA de import por foto também usa minutos);
      // o export da Uber vem em segundos, por isso a divisão por 60 aqui.
      duration: r['trip_duration_seconds'] ? Math.round(uzNum(r['trip_duration_seconds']) / 60) : 0,
      category: r['global_product_name'] || '',
      surge: r['is_surged'] === 'true',
      airport: r['is_airport_trip'] === 'true',
      // dinamico junta tarifa dinamica (surge) + promocao/desconto da corrida num
      // valor só — já incluso no "value", mostrado separado só pra detalhar a corrida.
      // Pode ficar negativo se o desconto for maior que o surge.
      dinamico:    uzRound2(Math.max(0, uzNum(r['surge_fare_local'])) + uzNum(r['promotion_local'])),
      dinheiro:   r['is_cash_trip'] === 'true',
      pedagio:    uzNum(r['toll_amount_local']) > 0,
      serviceFee: (r['service_fee'] !== undefined && r['service_fee'] !== '') ? parseFloat(r['service_fee']) : null,
      taxaZero:   r['service_fee'] === '0' || r['service_fee'] === '0.0',
    });
  }
  return trips;
}

function uzFindEntry(zip, suffix) {
  const f = Object.values(zip.files).find(x => !x.dir && x.name.endsWith(suffix));
  if (!f) throw new Error('Não encontrei "' + suffix + '" no zip. Confira se é o arquivo certo da Uber.');
  return f;
}

let _uzResult = null;

async function handleUberZipFile(input) {
  const file = input.files[0]; if (!file) return;
  input.value = '';
  openMo('mo-uberzip');
  document.getElementById('uz-proc').style.display = 'block';
  document.getElementById('uz-res').style.display = 'none';
  const stepEl = document.getElementById('uz-proc-step');
  const subEl  = document.getElementById('uz-proc-sub');
  const barEl  = document.getElementById('uz-proc-bar');
  const setStep = (msg, pct) => { if (stepEl) stepEl.textContent = msg; if (barEl) barEl.style.width = pct + '%'; };

  try {
    setStep('Lendo arquivo .zip...', 10);
    const zip = await JSZip.loadAsync(file);
    const paymentsEntry = uzFindEntry(zip, 'driver_payments-0.csv');
    const tripsEntry     = uzFindEntry(zip, 'driver_lifetime_trips-0.csv');

    setStep('Lendo corridas...', 35);
    const [paymentsCsv, tripsCsv] = await Promise.all([
      paymentsEntry.async('string'), tripsEntry.async('string')
    ]);
    const paymentsRows = Papa.parse(paymentsCsv, { header: true, skipEmptyLines: true }).data;
    const tripsRows     = Papa.parse(tripsCsv, { header: true, skipEmptyLines: true }).data;

    setStep('Organizando corridas concluídas...', 55);
    await new Promise(r => setTimeout(r, 0));
    const trips = uz
===== revisão @ 431632 =====
 width="16" height="16" fill="none" stroke="#94A3B8" stroke-width="2.3"><path d="M6 9l6 6 6-6"/></svg>
  </div>
  <div class="decl-body" id="decl-body" style="display:none"></div>
  <div style="padding:12px 16px 30px" id="cor-list"></div>
</div>

<!-- ═══════════════════════════════════════════════════════════
     TELA AGENDA — MōB
     Design: GPT  |  Funcionalidade: Claude
     ═══════════════════════════════════════════════════════════ -->
<div class="screen" id="sc-agenda">

  <!-- Cabecalho fixo: mes (expande calendario) + Hoje + regua 5 dias -->
  <div class="ag-head">
    <div class="ag-head-row">
      <button class="ag-month-btn" id="ag-month-btn" onclick="agToggleCal()">
        <span id="ag-month-label">&nbsp;</span>
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M6 9l6 6 6-6"/></svg>
      </button>
      <button class="ag-pill" onclick="agGoToday()">Hoje</button>
    </div>
    <div class="ag-strip" id="ag-strip"></div>

    <!-- Calendário flutua por cima (nunca empurra o cabeçalho/régua); fica dentro do
         .ag-head (sticky) pra ficar ancorado nele durante o scroll, não na timeline.
         Backdrop opaco esconde a timeline colorida atrás enquanto está aberto. -->
    <div class="ag-backdrop" id="ag-backdrop" onclick="agCloseCal()"></div>
    <div class="ag-calpanel" id="ag-calpanel">
      <div class="agcal-nav">
        <button onclick="agCalNav(-1)">&#8249;</button>
        <h2 id="agcal-title">&nbsp;</h2>
        <button onclick="agCalNav(1)">&#8250;</button>
      </div>
      <div class="agcal-grid">
        <div class="agcal-wd">DOM</div><div class="agcal-wd">SEG</div><div class="agcal-wd">TER</div>
        <div class="agcal-wd">QUA</div><div class="agcal-wd">QUI</div><div class="agcal-wd">SEX</div><div class="agcal-wd">S&#193;B</div>
        <div id="agcal-days" style="display:contents"></div>
      </div>
      <div class="agcal-legend">
        <span><i style="background:#22C55E"></i>Jornada</span>
        <span><i style="background:#8b5cf6"></i>Pessoal</span>
        <span><i style="background:#3B82F6"></i>Operacional</span>
      </div>
    </div>
  </div>

  <!-- Conteudo: dia unico (timeline) -->
  <div class="ag-body">
    <div class="ag-day-title" id="ag-d-date">&nbsp;</div>
    <div class="ag-legend">
      <span><i style="background:#9AA5B5"></i>Planejado</span>
      <span><i style="background:#22C55E"></i>Online</span>
      <span><i style="background:#7C3AED"></i>Pessoal</span>
      <span><i style="background:#3B82F6"></i>Operacional</span>
    </div>
    <div class="ag-timeline" id="ag-timeline"></div>
    <div style="height:20px"></div>
  </div>

  <button class="ag-fab" onclick="openNovoEv(agSelDate,null)" aria-label="Novo compromisso">
    <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M12 5v14M5 12h14"/></svg>
  </button>
</div>

<!-- ═══ MODAL: NOVO COMPROMISSO ═══════════════════════════════ -->
<div class="mo" id="mo-novo-ev">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="ag-sheet-head">
      <h2 id="ag-sheet-title">Novo compromisso</h2>
      <button class="ag-close" onclick="closeMo('mo-novo-ev')">&#x2715;</button>
    </div>
    <div class="ag-form-card">
      <div class="ag-field"><label class="ag-label">Título</label><input class="ag-input" id="ag-titulo" placeholder="Ex.: Revisão do veículo"></div>
      <div class="ag-field"><label class="ag-label">Data</label><input class="ag-input" type="date" id="ag-data"></div>
      <div class="ag-row2">
        <div class="ag-field" style="margin-bottom:0"><label class="ag-label">Início</label><input class="ag-input" type="time" id="ag-inicio"></div>
        <div class="ag-field" style="margin-bottom:0"><label class="ag-label">Fim</label><input class="ag-input" type="time" id="ag-fim"></div>
      </div>
      <div class="ag-field">
        <label class="ag-label">Tipo</label>
        <div class="ag-type-row">
          <button class="ag-type-btn on" id="ag-t-operacional" onclick="agSetEvType('operacional')">Operacional</button>
          <button class="ag-type-btn" id="ag-t-pessoal" onclick="agSetEvType('pessoal')">Pessoal</button>
        </div>
      </div>
      <div class="ag-field"><label class="ag-label">Observação</label><textarea class="ag-input" id="ag-obs" placeholder="Detalhes opcionais"></textarea></div>
      <button class="ag-save" onclick="saveEvento()">Salvar compromisso</button>
      <button class="ag-del" id="ag-del-btn" style="display:none" onclick="delEventoFromModal()">Excluir compromisso</button>
    </div>
  </div>
</div>




<!-- ══ TELA METAS ══════════════════════════════════════════════════════════ -->
<div class="screen" id="sc-planejar-jornada">
  <div class="wrap">
    <div style="display:flex;align-items:center;gap:10px;margin-bottom:18px">
      <button onclick="navTo('mais')" style="width:36px;height:36px;border-radius:14px;border:1px solid var(--line);background:#fff;display:flex;align-items:center;justify-content:center;color:#334155;cursor:pointer;flex-shrink:0">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M15 18l-6-6 6-6"/></svg>
      </button>
      <div style="font-size:20px;font-weight:800;letter-spacing:-.04em">Planejar jornada</div>
    </div>
    <div class="ag-form-card" style="margin-top:0">
      <div class="ag-field">
        <label class="ag-label">Selecione o dia</label>
        <div class="pj-days">
          <button class="pj-day" data-d="0" onclick="pjToggleDay(0)">Dom</button>
          <button class="pj-day" data-d="1" onclick="pjToggleDay(1)">Seg</button>
          <button class="pj-day" data-d="2" onclick="pjToggleDay(2)">Ter</button>
          <button class="pj-day" data-d="3" onclick="pjToggleDay(3)">Qua</button>
          <button class="pj-day" data-d="4" onclick="pjToggleDay(4)">Qui</button>
          <button class="pj-day" data-d="5" onclick="pjToggleDay(5)">Sex</button>
          <button class="pj-day" data-d="6" onclick="pjToggleDay(6)">Sáb</button>
        </div>
      </div>
      <div class="ag-row2">
        <div class="ag-field" style="margin-bottom:0"><label class="ag-label">Meta financeira (R$)</label><input class="ag-input" type="number" inputmode="decimal" id="pj-money" placeholder="Opcional"></div>
        <div class="ag-field" style="margin-bottom:0"><label class="ag-label">Quilometragem (km)</label><input class="ag-input" type="number" inputmode="decimal" id="pj-km" placeholder="Opcional"></div>
      </div>
      <div class="ag-field">
        <label class="ag-label">Horários de trabalho</label>
        <div id="pj-schedules"></div>
        <button class="ag-addbtn" style="margin-top:4px" onclick="pjAddSchedule()">
          <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M12 5v14M5 12h14"/></svg>
          Adicionar horário
        </button>
      </div>
      <button class="ag-save" onclick="pjSaveEntry()">Salvar jornada</button>
    </div>
    <div class="ag-form-card" style="margin-bottom:24px">
      <label class="ag-label" style="margin-bottom:10px;display:block">Jornadas planejadas</label>
      <div id="pj-list"><div class="empty">Nenhuma jornada planejada ainda</div></div>
    </div>
  </div>
</div>

<!-- ══ TELA MAIS ══════════════════════════════════════════════════════════ -->
<div class="screen" id="sc-informes">
  <!-- topbar: linha 1 = lupa (colapsa/expande) + cidade | linha 2 = busca (só quando aberta) -->
  <div class="inf-topbar" id="inf-topbar">
    <div class="inf-topbar-row1">
      <button class="inf-search-toggle" id="inf-search-toggle" onclick="infToggleSearch()" aria-label="Buscar local">
        <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
      </button>
      <div class="inf-city" id="inf-city-btn" onclick="infAbrirCidade()"><div class="inf-city-dot"></div><span id="inf-city-name">Localizando…</span><svg viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2.5" style="color:#94A3B8;margin-left:1px"><path d="M6 9l6 6 6-6"/></svg></div>
    </div>
    <div class="inf-search-fixed" id="inf-search-fixed" style="display:none">
      <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
      <input id="inf-search-bar" type="text" placeholder="Buscar local…" oninput="infBuscarGlobal(this.value)">
    </div>
  </div>
  <div class="inf-search-dd" id="inf-search-dd"></div>

  <!-- abas -->
  <div class="inf-tabs-wrap">
    <div class="inf-tabs" id="inf-tabs">
      <button class="inf-tab active" data-tab="agora" onclick="infTab('agora')">Agora</button>
      <button class="inf-tab" data-tab="aero" onclick="infTab('aero')">Aeroportos</button>
      <button class="inf-tab" data-tab="rod" onclick="infTab('rod')">Rodoviárias</button>
      <button class="inf-tab" data-tab="bairros" onclick="infTab('bairros')">Bairros</button>
      <button class="inf-tab" data-tab="shop" onclick="infTab('shop')">Shoppings</button>
      <button class="inf-tab" data-tab="bares" onclick="infTab('bares')">Bares</button>
      <button class="inf-tab" data-tab="hoteis" onclick="infTab('hoteis')">Hotéis</button>
      <button class="inf-tab" data-tab="eventos" onclick="infTab('eventos')">Eventos</button>
      <button class="inf-tab" data-tab="tur" onclick="infTab('tur')">Turístico</button>
    </div>
  </div>

  <div class="inf-relatar-bar">
    <button onclick="infOpenRelatar()">
      <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="#fff" stroke-width="2.3"><path d="M12 5v14M5 12h1
===== revisão @ 596909 =====
= parsed.dinamico;
  if (parsed.kmPickup != null) patch.offer_km_pickup = parsed.kmPickup;
  if (parsed.kmTrip != null) patch.offer_km_trip = parsed.kmTrip;
  if (parsed.minPickup != null) patch.offer_duration_pickup_sec = Math.round(parsed.minPickup*60);
  if (parsed.minTrip != null) patch.offer_duration_trip_sec = Math.round(parsed.minTrip*60);
  // Compara ANTES de salvar — pedido do Yuri (24/07/2026): mensagem
  // precisa dizer o que de fato mudou, não só "reconferida" genérico.
  const numEq = (a,b,tol) => (a==null && b==null) || (a!=null && b!=null && Math.abs(a-b) <= (tol||0.005));
  const changes = [];
  if (!numEq(t.offer_value, patch.offer_value, 0.005)) changes.push('valor '+DB.fR(patch.offer_value));
  if (patch.offer_dinamico != null && !numEq(t.offer_dinamico, patch.offer_dinamico, 0.005)) changes.push('dinâmico '+DB.fR(patch.offer_dinamico));
  if (patch.offer_km_trip != null && !numEq(t.offer_km_trip, patch.offer_km_trip, 0.05)) changes.push('km previsto');
  if (patch.offer_duration_trip_sec != null && !numEq(t.offer_duration_trip_sec, patch.offer_duration_trip_sec, 30)) changes.push('tempo previsto');
  // Diferença grande de valor (>15%, ou >R$5 pra valores pequenos) — pede
  // confirmação em vez de aplicar direto. Pedido do Yuri (16/08/2026):
  // caso real onde o valor pulou de R$42 pra R$26,20 sem aviso nenhum —
  // pode ser a correção certa (leitura mais completa) ou pode ser a nova
  // leitura que tá errada; sem ver as duas, não dá pra saber qual confiar.
  const oldVal = t.offer_value != null ? Number(t.offer_value) : null;
  const newVal = patch.offer_value;
  let valueBigChange = false;
  if (oldVal != null && oldVal > 0) {
    const diff = Math.abs(newVal - oldVal);
    valueBigChange = diff > 5 && (diff / oldVal) > 0.15;
  }
  return { ok:true, tripId, patch, changes, oldVal, newVal, valueBigChange };
}
async function _applyAutoTripRefresh(tripId, patch) {
  const { error } = await _SUPA.from('auto_trips').update({ ...patch, value_needs_review: false }).eq('id', tripId);
  return !error;
}
async function refreshAutoTripFromLog(tripId, btnEl) {
  if (btnEl) btnEl.classList.add('spinning');
  try {
    const r = await _computeAutoTripRefresh(tripId);
    if (!r.ok) { showToast('Não achei: '+r.reason, 'error'); return; }
    if (!r.changes.length) { showToast('Corrida atualizada com sucesso', 'success'); return; }
    const finish = async () => {
      const saved = await _applyAutoTripRefresh(r.tripId, r.patch);
      if (!saved) { showToast('Erro ao salvar.', 'error'); return; }
      showToast('Corrigido: '+r.changes.join(', '), 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    };
    if (r.valueBigChange) {
      showConfirm(
        `O valor dessa corrida vai mudar de ${DB.fR(r.oldVal)} para ${DB.fR(r.newVal)} — uma diferença grande.\n\nConfirma que ${DB.fR(r.newVal)} é o valor certo?`,
        finish,
        { danger: false, confirmLabel: 'Sim, corrigir', cancelLabel: 'Cancelar' }
      );
    } else {
      await finish();
    }
  } catch (e) {
    console.error('refreshAutoTripFromLog', e);
    showToast('Erro ao reconferir.', 'error');
  } finally {
    if (btnEl) btnEl.classList.remove('spinning');
  }
}
// Rotina automática (24/07/2026, pedido do Yuri) — a cada 5min, tenta
// reconferir sozinho corridas "Aguardando revisão" (status='capturada')
// das últimas 48h. Silenciosa por corrida (não interrompe com toast a
// cada uma) — só avisa se corrigiu alguma, no total. Nunca mexe em
// corrida já confirmada, só nas que ainda não têm valor nenhum.
async function _autoRefreshPendingTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'capturada')
      .gte('accepted_at', new Date(Date.now() - 48*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixed = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (r.ok && r.changes.length) { await _applyAutoTripRefresh(r.tripId, r.patch); fixed++; }
      } catch(e) {}
    }
    if (fixed > 0) {
      showToast(fixed===1 ? '1 corrida corrigida sozinha' : fixed+' corridas corrigidas sozinhas', 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
  } catch (e) { console.error('_autoRefreshPendingTrips', e); }
}
setTimeout(()=>{ _autoRefreshPendingTrips(); setInterval(_autoRefreshPendingTrips, 5*60*1000); }, 6000);

// Rotina automática nº2 (16/08/2026, pedido do Yuri: "não quero usuário
// editando, tem que partir da gente interno... a cada X min confere o
// que tá no registro e corrige"). Diferente da rotina acima (que só olha
// corridas AINDA sem valor confirmado), essa reconfere corridas que JÁ
// estão 'confirmada' — pra pegar o caso real que aconteceu (valor errado
// que já tinha virado card normal na tela). Roda a cada 10min, olha só as
// últimas 24h (corrida mais velha que isso já foi conferida o suficiente
// de vezes). REGRA DE OURO: só aplica sozinha quando a mudança é pequena/
// confiável — mudança grande NUNCA aplica sem humano olhar (mesmo critério
// do botão manual), só marca value_needs_review pra aparecer sinalizada,
// sem sumir da tela nem dos totais (não é "fantasma", é corrida real que
// só precisa de confirmação do valor).
//
// MUDOU (16/08/2026): mirava em status='confirmada', mas agora
// 'confirmada' só nasce depois do vídeo do histórico (ver
// confirmImport()) — reconferir isso aqui não faz mais sentido, vídeo já
// É a fonte mais confiável que existe. Passa a mirar em 'estimada' (ciclo
// da captura ao vivo completo, só com valor da oferta) — mesmo espírito
// de antes, refinando o valor com a melhor leitura do log disponível
// enquanto o vídeo de confirmação não chega.
async function _autoReconcileConfirmedTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'estimada')
      .is('data_quality_flag', null)
      .gte('accepted_at', new Date(Date.now() - 24*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixedQuiet = 0, flaggedForReview = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (!r.ok || !r.changes.length) continue;
        if (r.valueBigChange) {
          await _SUPA.from('auto_trips').update({ value_needs_review: true }).eq('id', row.id);
          flaggedForReview++;
        } else {
          await _applyAutoTripRefresh(r.tripId, r.patch);
          fixedQuiet++;
        }
      } catch(e) {}
    }
    if (fixedQuiet > 0 || flaggedForReview > 0) {
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
    // Silenciosa de propósito — mudança pequena não precisa de aviso, e
    // mudança grande já fica visível pelo badge "Valor a confirmar" na
    // tela Corridas, sem precisar interromper com toast.
  } catch (e) { console.error('_autoReconcileConfirmedTrips', e); }
}
setTimeout(()=>{ _autoReconcileConfirmedTrips(); setInterval(_autoReconcileConfirmedTrips, 10*60*1000); }, 9000);
// Balãozinho discreto ao tocar num símbolo (dinâmico, vínculo à jornada,
// dinheiro) — pedido do Yuri (24/07/2026). Some sozinho, não navega nem
// abre nada.
function showTapLegend(event, text) {
  event.stopPropagation();
  document.querySelectorAll('.tap-legend-tip').forEach(el => el.remove());
  const wrap = event.currentTarget;
  const tip = document.createElement('div');
  tip.className = 'tap-legend-tip';
  tip.textContent = text;
  wrap.appendChild(tip);
  setTimeout(() => tip.remove(), 2500);
}
// Legenda de comparação previsto × real (Deslocamento/Corrida coloridos) —
// abre/fecha um painel fixo embaixo da linha, diferente do balãozinho dos
// símbolos porque tem mais texto (2 linhas de números).
function toggleCmpLegend(event, id) {
  event.stopPropagation();
  const el = document.getElementById(id);
  if (el) el.style.display = el.style.display === 'none' ? '' : 'none';
}
async function loadAutoTripsForDay(dateStr) {
  if (!_supaUser) return [];
  // O dia da Jornada é o dia LOCAL do motorista. Converte meia-noite local
  // para UTC antes de consultar timestamptz — evita perder corridas após 21h
  // no Brasil quando o relógio UTC já virou para o dia seguinte.
  const fromLocal = new Date(dateStr+'T00:00:00');
  const toLocal = new Date(fromLocal); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  // null = falha de rede/consulta; [] = consulta válida sem corridas.
  return error ? null : (data||[]);
}
// Recusadas do dia — mesmo padrão de loadAutoTripsForDay, pra alimentar o
// contador "X aceitas / Y recusadas" do Total do dia. Tabela já existe e já
// é usada na tela Corridas (seção expansível); aqui só reaproveita pro
// recorte de UM dia em vez do período todo.
async function loadDeclinedOffersForDay(dateStr) {
  if (!_supaUser) return [];
  const from = dateStr+'T00:00:00', to = dateStr+'T23:59:59';
  const { data, error } = await _SUPA.from('declined_offers').select('id')
    .eq('user_id', _supaUser.id)
    .gte('seen_at', from).lte('seen_at', to);
  return error ? [] : (data||[]);
}

===== conferência @ 795316 =====
99');
  const uberFoundCount = uberFound.length, uberFoundVal = uberFound.reduce((s,t)=>s+(t.value||0),0);
  const p99FoundCount  = p99Found.length,  p99FoundVal  = p99Found.reduce((s,t)=>s+(t.value||0),0);

  const checks = [];
  let anyMissing = false, anyExtra = false;
  // Checagem EXATA: encontrar MAIS do que o declarado é tão errado quanto
  // encontrar menos (significa corrida lida em dobro/fundida). 18/17 não
  // pode aparecer como 'bateu certinho'. Valor com tolerância de 1 centavo.
  const evalPlat = (label, foundCount, foundVal, decCount, decVal) => {
    let ok = foundCount === decCount;
    if (ok && decVal != null) ok = Math.abs(foundVal - decVal) < 0.01;
    if (!ok) { if (foundCount > decCount || (decVal != null && foundVal > decVal + 0.009)) anyExtra = true; else anyMissing = true; }
    checks.push({label, ok, txt: `${foundCount}/${decCount} · ${DB.fRI(foundVal)}${decVal != null ? ' de ' + DB.fRI(decVal) : ''}`});
  };
  if (importPrecheck.uberCount != null) evalPlat('Uber', uberFoundCount, uberFoundVal, importPrecheck.uberCount, importPrecheck.uberValue);
  if (importPrecheck.p99Count != null) evalPlat('99', p99FoundCount, p99FoundVal, importPrecheck.p99Count, importPrecheck.p99Value);
  const allOk = !anyMissing && !anyExtra;
  const pillsHtml = checks.map(c => `<span style="font-size:11px;font-weight:700;padding:4px 10px;border-radius:99px;background:${c.ok?'#DCFCE7':'#FEE2E2'};color:${c.ok?'#16A34A':'#DC2626'}">${c.label} ${c.txt} ${c.ok?'✓':'✗'}</span>`).join('');

  const warnTitle = anyExtra ? 'Achei corridas a mais' : 'Faltou alguma coisa';
  const warnMsg = anyExtra
    ? 'Alguma corrida foi lida em dobro ou misturada. Na lista, desmarque as que estiverem repetidas ou com dados estranhos antes de importar.'
    : 'Confira se subiu todos os prints/vídeos desse app';
  const cardHtml = allOk
    ? `<div style="border-radius:16px;padding:16px;text-align:center;margin-bottom:14px;background:#ECFDF3;border:1.5px solid #BBF7D0">
        <div style="width:44px;height:44px;border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto 10px;background:#DCFCE7;color:#16A34A"><svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg></div>
        <b style="display:block;font-size:14px;color:#0F172A;margin-bottom:4px">Importação validada</b>
        <span style="font-size:12px;color:#64748B;line-height:1.5">Os valores conferem com o que você informou</span>
        <div style="display:flex;gap:8px;justify-content:center;margin-top:10px;flex-wrap:wrap">${pillsHtml}</div>
      </div>`
    : `<div style="border-radius:16px;padding:16px;text-align:center;margin-bottom:14px;background:#FFFBEB;border:1.5px solid #FDE68A">
        <div style="width:44px;height:44px;border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto 10px;background:#FEF3C7;color:#D97706"><svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.4"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg></div>
        <b style="display:block;font-size:14px;color:#0F172A;margin-bottom:4px">${warnTitle}</b>
        <span style="font-size:12px;color:#64748B;line-height:1.5">${warnMsg}</span>
        <div style="display:flex;gap:8px;justify-content:center;margin-top:10px;flex-wrap:wrap">${pillsHtml}</div>
      </div>`;

  document.getElementById('ic-title').textContent = 'Conferência';
  document.getElementById('ic-body').innerHTML = cardHtml;
  const btn = document.getElementById('ic-btn');
  btn.style.display = 'block';
  btn.textContent = allOk ? 'Ver corridas encontradas' : 'Ver o que encontrei';
  btn.onclick = () => { showImportConfirm(); btn.onclick = confirmImport; };
}

function showSpinner(msg) {
  return '<div style="text-align:center;padding:28px 16px">' +
    '<div style="font-size:28px;animation:vspin 1s linear infinite;display:inline-block">&#9881;</div>' +
    '<div style="margin-top:12px;font-size:13px;color:#64748B;font-weight:500">' + msg + '</div></div>';
}

// Normaliza "7:42" e "07:42" pro mesmo formato — a IA não le a hora de forma
// 100% consistente entre frames/prints (zero à esquerda variando), e sem isso
// a mesma corrida real passava pelo dedup do import como se fossem duas.
function normTime(t) {
  const m = String(t||'').match(/(\d{1,2}):(\d{2})/);
  return m ? m[1].padStart(2,'0')+':'+m[2] : String(t||'');
}
async function processMultipleImports(files, plat) {
  const total = files.length;
  document.getElementById('ic-title').textContent = 'Analisando ' + total + ' print' + (total > 1 ? 's' : '') + '...';
  document.getElementById('ic-body').innerHTML = showSpinner('Processando 1 de ' + total + '...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  const allTrips = [];
  for (let i = 0; i < files.length; i++) {
    document.getElementById('ic-body').innerHTML = showSpinner('Processando print ' + (i+1) + ' de ' + total + '...');
    try {
      const trips = await extractTripsFromImage(files[i], plat);
      allTrips.push(...trips);
    } catch(e) { console.warn('Erro print '+(i+1)+':', e); }
  }
  const day = DB.day(S.curDate);
  const seen = new Set();
  const unique = allTrips.filter(t => {
    if (!(t.value > 0)) return false; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
    t.time = normTime(t.time);
    const key = t.time+'_'+(t.value||0).toFixed(2);
    if (seen.has(key)) return false;
    seen.add(key); return true;
  });
  window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
    platform: t.platform === '99' ? '99' : 'uber',
    _exists: isTripDuplicate(t, day.trips) // pre-mark duplicates
  }));
  showImportConfirm();
}

async function processVideoImport(file, plat) {
  document.getElementById('ic-title').textContent = 'Extraindo frames do vídeo...';
  document.getElementById('ic-body').innerHTML = showSpinner('Analisando vídeo...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  try {
    const frames = await extractVideoFrames(file);
    const allTrips = [];
    for (let i = 0; i < frames.length; i++) {
      document.getElementById('ic-body').innerHTML = showSpinner('Analisando frame ' + (i+1) + ' de ' + frames.length + '...');
      try {
        const trips = await extractTripsFromBase64(frames[i], 'image/jpeg', plat);
        allTrips.push(...trips);
      } catch(e) { console.warn('Frame erro:', e); }
    }
    const dayV = DB.day(S.curDate);
    const seenV = new Set();
    const unique = allTrips.filter(t => {
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seenV.has(key)) return false;
      seenV.add(key); return true;
    });
    const _dayV = DB.day(S.curDate);
    window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _dup: isTripDuplicate(t, _dayV.trips)}));
    showImportConfirm();
  } catch(e) {
    closeMo('mo-import-confirm');
    showToast('Erro ao processar vídeo: ' + e.message + '. Use MP4 ou prints do histórico.', 'error');
    console.error('Video error:', e);
  }
}


// APK 1.3.9+: o WebView de alguns aparelhos não consegue decodificar certos
// screen recordings mesmo quando o arquivo é válido. Quando o bridge nativo
// estiver disponível, o Android decodifica o vídeo e devolve só os JPEGs.
const _nativeVideoExtractRequests = new Map();
window.__mobNativeVideoFrame = function(token, dataUrl) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r || !dataUrl) return;
  const raw = String(dataUrl).includes(',') ? String(dataUrl).split(',').pop() : String(dataUrl);
  if (raw) r.frames.push(raw);
};
window.__mobNativeVideoProgress = function(token, done, total) {
  const r = _nativeVideoExtractRequests.get(token);
  if (r && typeof r.onProgress === 'function') r.onProgress(Number(done)||0, Number(total)||0);
};
window.__mobNativeVideoDone = function(token, emitted) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  if (!r.frames.length) r.reject(new Error('O Android não conseguiu extrair quadros deste vídeo.'));
  else r.resolve(r.frames);
};
window.__mobNativeVideoError = function(token, message) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r) return;
  clearTimeout(r.timer);
  _nativeVideoExtractRequests.delete(token);
  r.reject(new Error(message || 'Falha no decodificador nativo do vídeo.'));
};

function extractVideoFramesNative(file, onProgress) {
  return new Promise((resolve, reject) => {
    const token = 'nv_' + Date.now().toString(36) + '_' + Math.random().toString(36).slice(2,8);
    const timer = setTimeout(() => {
      _nativeVideoExtractRequests.delete(token);
      reject(new Error('Tempo esgotado ao decodificar o vídeo no Android.'));
    }, 120000);
    _nativeVideoExtractRequests.set(token, {frames:[], onProgress, resolve, reject, timer});
    try {
      const started = SmartMobiNative.extractSelectedVideoFrames(token, 72, 720);
      if (started === false) {
        clearTimeout(timer);
        _nativeVideoExtractRequests.delete(token);
        reject(Object.assign(new Error('O Android não recebeu o arquivo de vídeo selecionado.'), {nativeNotStarted:true}));
      }
    } catch(e) {
      clearTimeout(timer);
      _nativeVideoExtractRequests.delete(token);
      reject(Object.assign(e instanceof Error ? e : new Error(String(e)), {nativeNotStarted:true}));
    }
  });
}

async function extractVideoFrames(file, onProgress) {
  let canNative = false;
  try {
    canNative = !!(window.SmartMobiNative &&
      typeof SmartMobiNative.hasNativeVideoFrameExtraction === 'function' &&
      SmartMobiNative.hasNativeVideoFrameExtraction() &&
      typeof SmartMobiNative.extractSelectedVideoFrames === 'function');

===== conferência @ 820131 =====
o com todo o resto do cartão visível e legível. ' +
    'REGRAS: ' +
    'Extraia SOMENTE corridas COMPLETAS que mostrem data+hora, categoria+valor, e pelo menos um endereço. ' +
    'VALIDAÇÃO LINHA A LINHA — REGRA MAIS IMPORTANTE DE TODAS: só extraia uma corrida se o bloco dela estiver 100% delimitado e visível NESTA imagem, do início ao fim: (a) a linha de data/hora "DD/MM/AAAA HH:MM" visível e completa dentro do PRÓPRIO cartão, imediatamente acima da forma de pagamento — nunca herdada do cabeçalho fixo nem de outro frame, (b) o status verde ("Pago" ou "Pedido finalizado") visível na BASE do bloco, e (c) a LINHA FINA divisória visível separando este bloco do vizinho. Se QUALQUER uma dessas três fronteiras não estiver visível — cartão cortado no topo da imagem, cortado na base, sem linha fina clara entre as informações, OU cartão que começa colado no cabeçalho fixo (abas "Corridas/Entregas" + filtros) sem nenhuma linha de data/hora entre eles — NÃO EXTRAIA essa corrida. Um cartão logo abaixo do cabeçalho fixo com categoria+valor/taxa/endereços/status visíveis mas SEM data/hora acima já teve a data/hora dele engolida pelo cabeçalho — trate exatamente como cartão cortado, mesmo que o resto pareça completo. É melhor deixar de fora do que adivinhar: esse mesmo cartão aparece COMPLETO, com a data/hora visível, em um frame anterior (alguns instantes antes, antes de rolar pra debaixo do cabeçalho). NUNCA complete um cartão cortado com dados do vizinho, e NUNCA reaproveite ou "lembre" uma data/hora vista em outro frame — a data/hora tem que estar escrita nesta mesma imagem, colada nesse cartão específico. ' +
    'CADA HORÁRIO (HH:MM) IDENTIFICA UMA ÚNICA CORRIDA: nunca retorne dois itens com o mesmo "time" — se o mesmo horário aparecer repetido, é o mesmo cartão visto duas vezes, retorne só um. ' +
    'LINHAS DIVISÓRIAS: cada corrida fica delimitada por LINHAS HORIZONTAIS FINAS na tela. O bloco de uma corrida é: data/hora → forma de pagamento → Pop + valor → tag de taxa → endereços → linha fina → status verde ("Pago" ou "Pedido finalizado"). Use essas linhas finas como fronteira: TODOS os campos de uma corrida (hora, valor, taxa, endereços, status) ficam juntos dentro do mesmo bloco, entre duas linhas finas. NUNCA combine um campo de um bloco com um campo de outro bloco — se uma linha fina separa a data/hora de um valor ou endereço, eles pertencem a corridas DIFERENTES. ' +
    'O valor R$X,XX de cada corrida está DENTRO do mesmo cartão que a data/hora dela (mesma caixa, entre a linha da data e o status final) — NUNCA use o valor de um cartão vizinho de cima ou de baixo. Se o valor do cartão não estiver visível por inteiro dentro dele, IGNORE a corrida em vez de pegar o valor de outro cartão. ' +
    'BAIRRO NUNCA É A CIDADE: a linha de endereço da 99 termina com a CIDADE e UF (ex: "..., Teresópolis - RJ") e muitas vezes NEM TEM bairro — só rua/estrada, número, CEP e cidade (ex: "Estr. Teresópolis x Friburgo, nº 8500, 25976-655, Teresópolis - RJ"). Nesse caso use o nome da rua/estrada em "origin"/"dest" (no exemplo: "Estr. Teresópolis x Friburgo"). O nome da cidade sozinho NUNCA vai em "origin"/"dest" — cuidado: a palavra "Teresópolis" pode fazer parte do nome de uma estrada (aí pode) mas nunca deve ser usada sozinha por ser a cidade do fim da linha. Sem rua legível, use "" (string vazia). ' +
    'IGNORE corridas com valor R$0,00 ou cujo valor não esteja claramente legível — corrida real da 99 sempre tem valor maior que zero. ' +
    'AUTOCONFERÊNCIA FINAL: antes de devolver o JSON, revise item por item e confirme: (1) o bloco da corrida estava 100% delimitado NESTA imagem — data/hora própria do cartão (não colada no cabeçalho fixo) + status na base + linhas finas; (2) time, value, taxa e endereços vieram TODOS do MESMO cartão; (3) nenhum "time" foi copiado, adivinhado ou "lembrado" de outro cartão/frame — se a data/hora não está escrita nesta imagem colada nesse cartão, o item inteiro sai do JSON. Remova qualquer item que não passe nas três checagens. ' +
    '"km" e "duration" sempre 0 pois não aparecem no histórico. ' +
    '"dinamico" e "gorjeta" sempre 0.00 pois não aparecem no histórico da 99. ' +
    'Formato de cada item: {"platform":"99","time":"HH:MM","value":0.00,"km":0.0,"duration":0,"category":"99Pop","origin":"bairro","originFull":"endereço completo","dest":"bairro","destFull":"endereço completo","dinamico":0.00,"gorjeta":0.00,"pedagio":0.00,"serviceFee":0.00,"dinheiro":false}. "serviceFee" é sempre o número da tag "Taxa Fixa de X%" convertido pra decimal (15% → 0.15). "dinheiro":true quando aparecer "Ganhos pagos em dinheiro".\n\n' +
    '=== SE FOR 99 DETALHE ===\n' +
    'Esta tela mostra UMA única corrida, mas é ROLÁVEL: um frame de vídeo captura só uma PARTE dela por vez. Essa tela serve SÓ pra completar km, duração e endereço completo de uma corrida que a lista já identificou — NÃO extraia valor, dinheiro, taxa, dinâmico, gorjeta nem pedágio aqui (a lista já tem isso, e misturar valor lido nesta tela causava corrida duplicada/fantasma no import). Extraia como FRAGMENTO: pegue os campos abaixo que estiverem legíveis NESTE frame, deixe os que não aparecerem como null. ' +
    'Campos: "duration" = Duração convertida pra MINUTOS INTEIROS arredondados (12m50s → 13; 5m42s → 6), null se não aparecer neste frame. "km" = número de Distância (6,2 km → 6.2), null se não aparecer. "time" = horário HH:MM da data completa (ex "06/07/2026 15:51:42" → "15:51"), null se não aparecer neste frame. "originFull"/"destFull" = endereço completo dos pontos verde(origem)/laranja(destino) quando visíveis, null se não aparecerem; "origin"/"dest" = bairro extraído desses endereços (nunca a cidade sozinha — mesma regra da lista). ' +
    'Formato (retorne um array com este único item; campos não vistos = null): {"platform":"99","screen":"detail","time":null,"km":null,"duration":null,"origin":null,"originFull":null,"dest":null,"destFull":null}.\n\n' +
    'Retorne SOMENTE um único JSON array, sem texto extra. Se não houver corridas completas retorne [].';
  const requestController = new AbortController();
  const requestTimer = setTimeout(() => requestController.abort(), 25000);
  let resp;
  try {
    resp = await fetch('https://smartmobi-proxy.yurisilva1990.workers.dev', {
      method:'POST', headers:{'Content-Type':'application/json'}, signal:requestController.signal,
      body: JSON.stringify({model:'claude-haiku-4-5-20251001', max_tokens:1500,
        messages:[{role:'user', content:[{type:'image',source:{type:'base64',media_type:mt,data:b64}},{type:'text',text:prompt}]}]})
    });
  } catch(e) {
    if (e && e.name === 'AbortError') throw new Error('Tempo esgotado ao analisar quadro');
    throw e;
  } finally {
    clearTimeout(requestTimer);
  }
  if (!resp.ok) throw new Error('Worker error ' + resp.status);
  const data = await resp.json();
  const raw  = ((data.content?.[0]?.text)||'[]').replace(/```json|```/g,'').trim();
  const arr  = JSON.parse(raw);
  return Array.isArray(arr) ? arr.map(t => {
    const isDetail = t.screen === 'detail';
    const numOrNull = v => (v === null || v === undefined || v === '') ? null : (parseFloat(String(v).replace(',','.'))||0);
    const dinamico = isDetail ? numOrNull(t.dinamico) : (parseFloat(String(t.dinamico||0).replace(',','.'))||0);
    const tip = parseFloat(String(t.gorjeta||0).replace(',','.'))||0;
    const pedagioVal = parseFloat(String(t.pedagio||0).replace(',','.'))||0;
    // serviceFee: número real da taxa (0.15 = 15%). taxaZero mantido por
    // compat (código antigo), mas agora sempre derivado do serviceFee real.
    const hasFee = t.serviceFee !== undefined && t.serviceFee !== null && t.serviceFee !== '';
    const serviceFee = hasFee ? parseFloat(String(t.serviceFee).replace(',','.'))||0 : null;
    // Plataforma: confia no que a IA identificou pela imagem. Só cai no
    // botão que o usuário tocou (plat) se a IA não devolveu nada — rede de
    // segurança, não a fonte principal (era o bug antigo: botão errado
    // salvava tudo com a plataforma trocada).
    const detected = String(t.platform||'').toLowerCase();
    const platform = detected === '99' ? '99' : detected === 'uber' ? 'uber'
      : (plat && plat.toLowerCase()==='99' ? '99' : 'uber');
    // Rótulo do trajeto: quando a IA não acha bairro (endereço só com
    // rua/estrada + CEP + cidade), usa a primeira parte do endereço
    // completo, abreviada — melhor "Estr. Teresópolis x Fri…" que "—".
    const shortPlace = s => {
      const seg = String(s||'').split(',')[0].trim();
      return seg.length > 24 ? seg.slice(0,23).trimEnd()+'…' : seg;
    };
    let oFull, dFull, origin, dest;
    if (isDetail) {
      // Fragmento — pode não ter endereço neste frame específico; null fica null.
      oFull = t.originFull || null; dFull = t.destFull || null;
      origin = t.origin ? String(t.origin).trim() : (oFull ? shortPlace(oFull) : null);
      dest   = t.dest   ? String(t.dest).trim()   : (dFull ? shortPlace(dFull) : null);
    } else {
      oFull = t.originFull || t.origin || ''; dFull = t.destFull || t.dest || '';
      origin = (t.origin||'').trim() || shortPlace(oFull);
      dest   = (t.dest||'').trim()   || shortPlace(dFull);
    }
    return {...t, platform, origin, dest,
      value:parseFloat(String(t.value||0).replace(',','.'))||0,
      km: isDetail ? numOrNull(t.km) : (parseFloat(String(t.km||0).replace(',','.'))||0),
      duration: isDetail ? (t.duration==null ? null : (parseInt(t.duration)||0)) : (parseInt(t.duration)||0),
      dinamico, tip, surge: dinamico > 0,
      pedagioValor: pedagioVal, pedagio: pedagioVal > 0,
      originFull: oFull, destFull: dFull,
      serviceFee: serviceFee,
      taxaZero: serviceFee === 0 || t.taxaZero===true||t.taxaZero==='true'||t.taxaZero===1,
      dinheiro: isDetail
        ? (t.dinheiro===true||t.dinheiro==='true'||t.dinheiro===1 ? true : (t.dinheiro===false||t.dinheiro==='false' ? false : null))
        : (t.dinheiro===true||t.dinheir
===== importar corridas @ 473306 =====
150px;height:64px;text-align:center;font-size:26px;font-family:'Inter',system-ui,sans-serif;font-weight:700;border:1.5px solid #E2E8F0;border-radius:16px;background:#F8FAFC;color:#0F172A;outline:none;box-sizing:border-box">
    </div>
    <button class="btn-p" onclick="saveAutoTripValueEdit()">Confirmar</button>
  </div>
</div>

<div class="mo ctr" id="mo-km-adjust">
  <div class="sheet ctr">
    <div class="sheet-head"><h2>Ajustar km total</h2><button class="xbtn" onclick="closeMo('mo-km-adjust')">&#x2715;</button></div>
    <div style="font-size:12.5px;color:#64748B;margin-bottom:18px;line-height:1.4">Você pode ajustar o km total rodado hoje sempre que precisar.</div>
    <div style="display:flex;align-items:center;justify-content:center;gap:10px;margin-bottom:8px">
      <div style="display:flex;flex-direction:column;gap:6px">
        <button class="kmadj-step" onclick="kmAdjStep(-10)">-10</button>
        <button class="kmadj-step" onclick="kmAdjStep(-1)">-1</button>
        <button class="kmadj-step" onclick="kmAdjStep(-0.5)">-0,5</button>
      </div>
      <input id="kmadj-val" type="number" inputmode="decimal" step="0.1"
        style="width:118px;height:114px;text-align:center;font-size:28px;font-family:'Inter',system-ui,sans-serif;font-weight:700;border:1.5px solid #E2E8F0;border-radius:18px;background:#F8FAFC;color:#0F172A;outline:none;box-sizing:border-box;flex-shrink:0">
      <div style="display:flex;flex-direction:column;gap:6px">
        <button class="kmadj-step" onclick="kmAdjStep(0.5)">+0,5</button>
        <button class="kmadj-step" onclick="kmAdjStep(1)">+1</button>
        <button class="kmadj-step" onclick="kmAdjStep(10)">+10</button>
      </div>
    </div>
    <button class="btn-p" onclick="saveKmAdjust()">Confirmar</button>
  </div>
</div>

<div class="mo ctr" id="mo-tempo-adjust">
  <div class="sheet ctr">
    <div class="sheet-head"><h2>Ajustar tempo total</h2><button class="xbtn" onclick="closeMo('mo-tempo-adjust')">&#x2715;</button></div>
    <div style="font-size:12.5px;color:#64748B;margin-bottom:18px;line-height:1.4">Você pode ajustar o tempo total da jornada hoje sempre que precisar.</div>
    <div style="display:flex;align-items:center;justify-content:center;gap:10px;margin-bottom:8px">
      <div style="display:flex;flex-direction:column;gap:6px">
        <button class="kmadj-step" onclick="tempoAdjStep(-60)">-1h</button>
        <button class="kmadj-step" onclick="tempoAdjStep(-10)">-10m</button>
        <button class="kmadj-step" onclick="tempoAdjStep(-5)">-5m</button>
      </div>
      <input id="tempoadj-val" type="text" inputmode="text" autocomplete="off"
        onblur="tempoAdjInputCommit()" onfocus="this.select()"
        style="width:118px;height:114px;text-align:center;font-size:25px;font-family:'Inter',system-ui,sans-serif;font-weight:700;border:1.5px solid #E2E8F0;border-radius:18px;background:#F8FAFC;color:#0F172A;outline:none;box-sizing:border-box;flex-shrink:0">
      <div style="display:flex;flex-direction:column;gap:6px">
        <button class="kmadj-step" onclick="tempoAdjStep(5)">+5m</button>
        <button class="kmadj-step" onclick="tempoAdjStep(10)">+10m</button>
        <button class="kmadj-step" onclick="tempoAdjStep(60)">+1h</button>
      </div>
    </div>
    <div style="text-align:center;font-size:11px;color:#94A3B8;margin-bottom:14px">Toque pra digitar. Ex.: 2h30m ou 45m</div>
    <button class="btn-p" onclick="saveTempoAdjust()">Confirmar</button>
  </div>
</div>

<!-- Importar corridas -->
<!-- Import pre-check: quantas corridas e valor, por plataforma -->
<div class="mo" id="mo-import-precheck">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <h2 style="font-family:'Poppins',sans-serif;font-weight:800;font-size:16px;color:#0F172A;margin-bottom:4px">Quantas corridas e quanto?</h2>
    <div style="font-size:11.5px;color:#64748B;line-height:1.45;margin-bottom:14px">Preencha o que aparece em cada app — pode deixar um dos dois em branco.</div>

    <div style="display:flex;gap:8px;margin-bottom:14px">
      <div style="flex:1;min-width:0;border:1.5px solid #E7EBF1;border-radius:14px;padding:10px;display:flex;flex-direction:column;gap:8px">
        <div style="display:flex;flex-direction:column;align-items:center;gap:5px;margin-bottom:2px">
          <div style="background:#000;border-radius:6px;padding:5px 10px;display:inline-flex"><img style="height:14px;width:auto" src="data:image/png;base64,
===== importar corridas @ 497203 =====
">
          <b style="font-size:11px;font-weight:800;color:#64748B">Corridas</b>
        </div>
        <div style="display:flex;align-items:center;justify-content:center;border:1.5px solid #E7EBF1;border-radius:10px;padding:0 8px;height:40px;gap:3px">
          <input id="ipc-99-count" type="number" placeholder="0" style="border:0;outline:0;flex:1;font-size:14px;font-weight:700;color:#0F172A;font-family:inherit;min-width:0;text-align:center">
        </div>
        <div style="font-size:9px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.04em;text-align:center">Valor total</div>
        <div style="display:flex;align-items:center;justify-content:center;border:1.5px solid #E7EBF1;border-radius:10px;padding:0 8px;height:40px;gap:3px">
          <span style="font-size:11px;color:#64748B;font-weight:700;flex-shrink:0">R$</span>
          <input id="ipc-99-value" type="number" placeholder="0,00" style="border:0;outline:0;flex:1;font-size:14px;font-weight:700;color:#0F172A;font-family:inherit;min-width:0;text-align:center">
        </div>
      </div>
    </div>

    <div style="display:flex;gap:7px;background:#EFF6FF;border:1px solid rgba(14,165,233,.2);border-radius:11px;padding:9px 10px;font-size:11px;color:#0369A1;line-height:1.45;margin-bottom:12px">
      <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" style="flex:none;margin-top:1px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
      <span>Serve pra conferir se o import achou tudo — não precisa ser o dia inteiro.</span>
    </div>
    <button class="btn-p" style="margin-top:0" onclick="importPrecheckNext()">Avançar</button>
    <div style="text-align:center;font-size:12px;color:#2563EB;font-weight:700;margin-top:10px;cursor:pointer" onclick="importPrecheckSkip()">Pular essa parte →</div>
  </div>
</div>

<div class="mo" id="mo-import">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2>Importar corridas</h2><button class="xbtn" onclick="closeMo('mo-import')">&#x2715;</button></div>
    <div style="background:#EFF6FF;border:1px solid rgba(14,165,233,.2);border-radius:16px;padding:12px 14px;font-size:12px;color:#0369A1;line-height:1.6;display:flex;gap:8px;margin-bottom:16px">
      <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" style="flex-shrink:0;margin-top:2px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
      <div><strong>Como usar:</strong> grave a tela do celular rolando o histórico (Uber ou 99, tanto faz) e mande o arquivo de vídeo aqui — não dá mais pra mandar print solto. O app identifica a plataforma sozinho e extrai as corridas.</div>
    </div>
    <div style="display:flex;gap:8px;margin-bottom:8px">
      <label style="flex:1;height:56px;border-radius:14px;background:#F0F4FA;border:1px solid #E2E8F0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;cursor:pointer;font-size:11px;font-weight:700;color:#0A2F6B">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="m3 9 4-4 4 4 4-4 4 4"/></svg> Vídeo da galeria
        <input type="file" multiple accept="video/*" style="display:none" onclick="this.value=null" onchange="handleImport(this,'uber')">
      </label>
      <label style="flex:1;height:56px;border-radius:14px;background:#F0F4FA;border:1px solid #E2E8F0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;cursor:pointer;font-size:11px;font-weight:700;color:#0A2F6B">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg> Arquivos
        <input type="file" multiple accept="video/*" style="display:none" onclick="this.value=null" onchange="handleImport(this,'uber')">
      </label>
    </div>
    <div style="font-size:11px;color:#94A3B8;text-align:center;margin-bottom:14px">Só vídeo — grave a tela rolando o histórico e mande o arquivo aqui. Dá pra escolher vários de uma vez.</div>

    <label style="width:100%;height:50px;border-radius:14px;background:linear-gradient(135deg,#0A2F6B,#22C55E);display:flex;align-items:center;justify-content:center;gap:8px;cursor:pointer;font-size:12.5px;font-weight:700;color:#fff;box-shadow:0 8px 18px rgba(10,47,107,.25);position:relative;margin-bottom:8px">
      <span style="position:absolute;top:-8px;right:-6px;background:#F59E0B;color:#fff;font-size:8.5px;font-weight:900;padding:3px 6px;border-radius:999px;letter-spacing:.02em">NOVO</span>
      <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M17 8 12 3 7 8"/><path d="M12 3v12"/></svg>
      Histórico oficial do Uber (.zip)
      <input type="file" accept=".zip" style="display:none" onclick="this.value=null" onchange="handleUberZipFile(this)">
    </label>
    <div style="font-size:11px;color:#94A3B8;text-align:center;line-height:1.5;margin-bottom:12px">Baixe em: app Uber → Conta → Configurações de privacidade → "Solicitar meus dados". Você recebe um e-mail com o arquivo .zip.</div>

    <div style="display:flex;gap:6px;justify-content:center">
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px;display:flex;align-items:center;gap:4px"><i style="width:6px;height:6px;border-radius:50%;display:inline-block;background:#0EA5E9"></i> Uber</span>
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px;display:flex;align-items:center;gap:4px"><i style="width:6px;height:6px;border-radius:50%;display:inline-block;background:#F59E0B"></i> 99</span>
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px">identificados automaticamente</span>
    </div>
  </div>
</div>

<!-- Import confirm -->
<div class="mo" id="mo-import-confirm">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2 id="ic-title">Corridas detectadas</h2><button class="xbtn" onclick="closeMo('mo-import-confirm')">&#x2715;</button></div>
    <div id="ic-body" style="min-height:80px"></div>
    <div style="display:flex;gap:8px;padding-top:8px">
      <button class="btn-s" onclick="closeMo('mo-import-confirm')" style="margin-top:0">Cancelar</button>
      <button class="btn-p" id="ic-btn" onclick="confirmImport()" style="margin-top:0">Importar</button>
    </div>
  </div>
</div>

<!-- Uber ZIP: processando + resultado -->
<div class="mo ctr" id="mo-uberzip">
  <div class="sheet ctr" style="max-width:400px">
    <div id="uz-proc" style="padding:50px 20px;text-align:center">
      <div style="font-size:32px;display:inline-block;animation:uzspin 1.1s linear infinite">&#9881;</div>
      <div id="uz-proc-step" style="font-size:14px;font-weight:800;margin-top:16px">Lendo arquivo...</div>
      <div id="uz-proc-sub" style="font-size:12px;color:#64748B;margin-top:5px">Isso só leva alguns segundos</div>
      <div style="height:8px;background:#EEF2F7;border-radius:99px;overflow:hidden;margin-top:20px">
        <div id="uz-proc-bar" style="height:100%;background:linear-gradient(90deg,#0EA5E9,#22C55E);border-radius:99px;width:8%;transition:width .4s"></div>
      </div>
    </div>

    <div id="uz-res" style="display:none">
      <div style="text-align:center;padding:4px 0 16px">
        <div style="width:52px;height:52px;border-radius:16px;background:#ECFDF3;color:#16A34A;display:flex;align-items:center;justify-content:center;margin:0 auto 10px">
          <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6 9 17l-5-5"/></svg>
        </div>
        <div style="font-size:16px;font-weight:800;letter-spacing:-.02em">Histórico conferido</div>
        <div id="uz-res-sub" style="font-size:12px;color:#64748B;margin-top:3px"></div>
      </div>

      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px">
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">
===== importar corridas @ 903485 =====
para os onclick do footer
  window._jsSummarySess = sess;
  // Mesma lógica de sessionItem(): corrida pertence a esta sessão se o
  // horário dela cai dentro da janela [start, end] da sessão.
  const trips = (day.trips || []).filter(t => {
    let ts = t.ts || 0;
    if (!ts && t.time) ts = new Date(day.date + 'T' + t.time + ':00').getTime();
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  const hasTrips = trips.length > 0;
  const th = Math.floor(totalElapsed/3600), tm = Math.floor(totalElapsed%3600/60);
  const kmh = totalElapsed > 0 ? (totalKm/(totalElapsed/3600)) : 0;

  const kpiCard = (v,l) => `<div style="background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.1);border-radius:12px;padding:9px 8px;text-align:center">
      <div style="font-family:'Poppins',sans-serif;font-weight:700;font-size:16px">${v}</div>
      <div style="font-size:9.5px;color:rgba(255,255,255,.5);font-weight:600;margin-top:2px;text-transform:uppercase;letter-spacing:.04em">${l}</div>
    </div>`;

  let cardInner, extraSection, footer;

  if (!hasTrips) {
    cardInner = `
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
        <span style="font-size:11px;font-weight:700;color:rgba(255,255,255,.55);text-transform:uppercase;letter-spacing:.06em">Resumo da sessão</span>
        <span style="font-size:10px;font-weight:800;background:rgba(34,197,94,.18);color:#4ADE80;border:1px solid rgba(74,222,128,.35);border-radius:99px;padding:3px 10px">ENCERRADA</span>
      </div>
      <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:32px;line-height:1;letter-spacing:-.01em">${th}h ${p2(tm)}min</div>
      <div style="font-size:11px;color:rgba(255,255,255,.55);font-weight:600;margin-top:4px">tempo online hoje</div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px">
        ${totalKm > 0.1 ? kpiCard(totalKm.toFixed(1), 'Km percorrido') : kpiCard('—', 'Km percorrido')}
        ${totalKm > 0.1 ? kpiCard(kmh.toFixed(1), 'Km/h médio') : kpiCard('—', 'Km/h médio')}
      </div>`;
    extraSection = `
      <div class="cv-daily" style="display:flex;gap:10px;background:#FFFBEB;border:1px solid #FDE68A;border-radius:12px;padding:11px 12px;margin:12px 0;align-items:flex-start">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#D97706" stroke-width="2.4" stroke-linecap="round" style="flex:none;margin-top:1px"><circle cx="12" cy="12" r="9"/><path d="M12 8v4l2.5 2.5"/></svg>
        <span style="font-size:12px;color:#92400E;font-weight:500;line-height:1.45">Km ativo, corridas, ganhos, R$/h, R$/km e meta aparecem aqui depois que você <b>importar as corridas de hoje</b>.</span>
      </div>`;
    footer = `
      <button onclick="closeJourneySummary(); ${needsMidnight} ? openPostStopMidnightModal(window._jsSummarySess, openImportPrecheck) : openImportPrecheck()" style="width:100%;height:48px;border:none;border-radius:14px;font-family:'Inter',system-ui,sans-serif;font-size:14px;font-weight:800;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:8px;background:#22C55E;color:#fff;box-shadow:0 6px 16px rgba(34,197,94,.35)">
        <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M7 10l5 5 5-5"/><path d="M12 15V3"/></svg>
        Importar corridas de hoje
      </button>
      <div style="font-size:11px;color:var(--muted);text-align:center;line-height:1.5;margin-top:8px;padding:0 6px">Escolha as fotos/vídeos do histórico Uber ou 99 — pode selecionar vários de uma vez.</div>
      <button onclick="closeJourneySummary(); if(${needsMidnight}) openPostStopMidnightModal(window._jsSummarySess, null)" style="width:100%;height:40px;border:none;background:transparent;color:var(--muted);font-weight:700;font-size:13px;margin-top:4px;cursor:pointer">Deixar para depois</button>`;
  } else {
    const ganhos = trips.reduce((s,t)=>s+(t.value||0),0);
    const kmAtivo = trips.reduce((s,t)=>s+(t.km||0),0);
    const kmDesl = Math.max(0, totalKm - kmAtivo);
    const rsh = totalElapsed>0 ? ganhos/(totalElapsed/3600) : 0;
    const rskm = totalKm>0 ? ganhos/totalKm : 0;
    const uberTrips = trips.filter(t=>(t.platform||'uber')==='uber');
    const p99Trips  = trips.filter(t=>t.platform==='99');
    const uberR = uberTrips.reduce((s,t)=>s+(t.value||0),0);
    const p99R  = p99Trips.reduce((s,t)=>s+(t.value||0),0);

    const dow = new Date(day.date+'T12:00').getDay();
    const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
    const meta = DB.meta();
    const ov = day.metaOverride || null;
    const dimMes = new Date(new Date(day.date+'T12:00').getFullYear(), new Date(day.date+'T12:00').getMonth()+1, 0).getDate();
    const revTotal = ganhos + (day.revenues||[]).reduce((s,r)=>s+(r.amount||0),0);
    const moneyGoal = (ov && ov.money) ? ov.money : ((plan && plan.moneyGoal) ? plan.moneyGoal : (meta.general ? meta.general/dimMes : 0));

    let metaHtml = '';
    if (moneyGoal) {
      const pct = Math.round(revTotal/moneyGoal*100);
      metaHtml = `
        <div style="margin-top:14px">
          <div style="display:flex;justify-content:space-between;font-size:11px;font-weight:700;margin-bottom:6px">
            <span style="color:rgba(255,255,255,.6)">Meta do dia · ${DB.fRI(moneyGoal)}</span>
            <span style="color:#4ADE80">${pct}%${pct>=100?' &#10003;':''}</span>
          </div>
          <div style="height:8px;border-radius:99px;background:rgba(255,255,255,.12);overflow:hidden">
            <div style="height:100%;border-radius:99px;background:linear-gradient(90deg,#22C55E,#4ADE80);width:${Math.min(100,pct)}%"></div>
          </div>
        </div>`;
    }

    cardInner = `
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
        <span style="font-size:11px;font-weight:700;color:rgba(255,255,255,.55);text-transform:uppercase;letter-spacing:.06em">Ganhos do dia</span>
        <span style="font-size:10px;font-weight:800;background:rgba(34,197,94,.18);color:#4ADE80;border:1px solid rgba(74,222,128,.35);border-radius:99px;padding:3px 10px">${trips.length} CORRIDA${trips.length===1?'':'S'}</span>
      </div>
      <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:32px;line-height:1;letter-spacing:-.01em">${DB.fRI(ganhos)}</div>
      <div style="font-size:11px;color:rgba(255,255,255,.55);font-weight:600;margin-top:4px">bruto · ${th}h ${p2(tm)}min online</div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px">
        ${kpiCard(kmAtivo.toFixed(1), 'Km ativo')}
        ${kpiCard(kmDesl.toFixed(1), 'Km desloc.')}
        ${kpiCard(DB.fRI(rsh).replace('R$ ',''), 'R$ / hora')}
        ${kpiCard(rskm.toFixed(2).replace('.',','), 'R$ / km')}
      </div>
      ${metaHtml}`;

    extraSection = `
      <div style="font-size:11px;font-weight:800;color:var(--muted);text-transform:uppercase;letter-spacing:.05em;margin:14px 0 6px">Uber / 99</div>
      <div style="display:flex;gap:8px;margin-bottom:14px">
        <div style="flex:1;border:1px solid var(--line);border-radius:12px;padding:10px 12px;background:var(--soft-line)">
          <div style="font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.05em;color:var(--muted)">Uber</div>
          <div style="font-family:'Poppins',sans-serif;font-weight:700;font-size:15px;color:var(--text);margin-top:2px">${DB.fRI(uberR)}</div>
          <div style="font-size:10px;color:var(--muted2);font-weight:600">${uberTrips.length} corrida${uberTrips.length===1?'':'s'}</div>
        </div>
        <div style="flex:1;border:1px solid var(--line);border-radius:12px;padding:10px 12px;background:var(--soft-line)">
          <div style="font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.05em;color:var(--muted)">99</div>
          <div style="font-family:'Poppins',sans-serif;font-weight:700;font-size:15px;color:var(--text);margin-top:2px">${DB.fRI(p99R)}</div>
          <div style="font-size:10px;color:var(--muted2);font-weight:600">${p99Trips.length} corrida${p99Trips.length===1?'':'s'}</div>
        </div>
      </div>`;

    footer = `<button onclick="closeJourneySummary()" style="width:100%;height:48px;border:none;border-radius:14px;font-family:'Inter',system-ui,sans-serif;font-size:14px;font-weight:800;cursor:pointer;background:#0A2F6B;color:#fff">Fechar</button>`;
  }

  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:flex-end;justify-content:center;background:rgba(15,23,42,.45)';
  const dateLabel = new Date(day.date+'T12:00').toLocaleDateString('pt-BR',{weekday:'long',day:'numeric',month:'long'});
  el.innerHTML = `
    <div style="background:var(--card);border-radius:24px 24px 0 0;padding:10px 18px 26px;box-shadow:0 -12px 40px rgba(10,47,107,.35);max-width:420px;width:100%;max-height:88vh;overflow-y:auto;font-family:'Inter',system-ui,sans-serif">
      <div style="width:44px;height:5px;border-radius:99px;background:var(--line);margin:0 auto 14px"></div>
      <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:18px;color:var(--text);text-align:center">${hasTrips ? 'Resumo do dia' : 'Jornada encerrada'}</div>
      <div style="font-size:12px;color:var(--muted2);text-align:center;margin:3px 0 16px;font-weight:500">${dateLabel}</div>
      <div style="background:linear-gradient(145deg,#0A2F6B 0%,#0D3E8C 60%,#0A2F6B 100%);border-radius:18px;padding:16px;color:#fff;margin-bottom:12px">
        ${cardInner}
      </div>
      ${extraSection}
      ${footer}
    </div>`;
  document.body.appendChild(el);
  el
===== #F59E0B @ 2292 =====
<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: blob: https:; connect-src 'self' https://jlsrpptslwfhmkvelaro.supabase.co wss://jlsrpptslwfhmkvelaro.supabase.co https://wrmgmsmpszsdmntketfq.supabase.co wss://wrmgmsmpszsdmntketfq.supabase.co https://nominatim.openstreetmap.org https://router.project-osrm.org https://overpass-api.de https://smartmobi-proxy.yurisilva1990.workers.dev; object-src 'none'; base-uri 'self'; form-action 'self'">
<meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="default">
<meta name="description" content="MōB — a ferramenta da sua jornada">
<meta name="theme-color" content="#0A2F6B">
<meta name="mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-title" content="MōB">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<link rel="manifest" href="manifest.json">
<link rel="apple-touch-icon" href="apple-touch-icon.png">
<title>MōB</title>
<script>
// Aplica o modo escuro antes da primeira pintura, pra nao dar aquele flash de tela clara.
try {
  const cfg = JSON.parse(localStorage.getItem('sm_cfg'));
  if (cfg && cfg.darkMode) document.documentElement.classList.add('dark');
} catch(e) {}
</script>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
<style>

:root{--font-brand:'Poppins',sans-serif;--font-app:'Inter',system-ui,-apple-system,BlinkMacSystemFont,sans-serif;
  /* Shared tokens */
  --bg:#F7F9FC;--card:#FFFFFF;--text:#0F172A;--muted:#64748B;--muted2:#94A3B8;
  --line:#E2E8F0;--soft-line:#E8EEF5;
  --blue:#0A2F6B;--mob-blue:#0A2F6B;--mob-gray:#9CA3AF;--blue2:#0284C7;
  --green:#22C55E;--green2:#16A34A;
  --red:#EF4444;--orange:#F59E0B;--amber:#FACC15;--purple:#7C3AED;
  --greenBg:#ECFDF3;--orangeBg:#FFF7ED;--redBg:#FEF2F2;--purpleBg:#F5F3FF;--blueBg:#EFF6FF;
  --shadow:0 10px 28px rgba(15,23,42,.08);--soft:0 3px 14px rgba(15,23,42,.06);
  --r:24px;--radius:24px;
  --safe-top:max(env(safe-area-inset-top),14px);
  --safe-bottom:max(env(safe-area-inset-bottom),10px);
  --top:0px;--bottom:78px;
}
/* ── Modo escuro: paleta neutra, cor só em status/valores ── */
html.dark{
  --bg:#0A0F1A;--card:#111723;--text:#E2E8F0;--muted:#94A3B8;--muted2:#64748B;
  --line:#1E2735;--soft-line:#1A2230;
  --green:#10B981;--green2:#059669;
  --red:#EF4444;--orange:#F59E0B;--amber:#EAB308;--purple:#8B5CF6;
  --greenBg:rgba(16,185,129,.12);--orangeBg:rgba(245,158,11,.12);--redBg:rgba(239,68,68,.12);
  --purpleBg:rgba(139,92,246,.12);--blueBg:rgba(14,165,233,.12);
  --shadow:0 10px 28px rgba(0,0,0,.35);--soft:0 3px 14px rgba(0,0,0,.25);
}
html.dark body{background:var(--bg)}
/* Cards com gradiente colorido fixo (nao usam variavel) ficam neutros no escuro,
   seguindo a direcao aprovada — cor só em pontos pontuais, nao no fundo todo */
html.dark .journey.idle,html.dark .journey.ended,html.dark .journey.running{
  background:var(--card);border:1px solid var(--line);box-shadow:none
}
html.dark .hero{background:var(--card);border:1px solid var(--line);box-shadow:none}
html.dark .amount-card.receita,html.dark .amount-card.despesa{background:var(--card);border:1px solid var(--line)}
html.dark input,html.dark select,html.dark textarea{background:#0F1726;color:var(--text);border-color:var(--line)}
html.dark .inp,html.dark .cat-inp2{background:#0F1726;color:var(--text)}
html.dark .sheet,html.dark .mo .sheet{background:var(--card)}
html.dark img.invert-dark{filter:invert(1) brightness(1.4)}
/* Ícones do menu Mais viram neutros no escuro — cor só sobra pra dinheiro
   (receita/despesa), não pra decoração de cada item de menu */
html.dark .mais-ico,html.dark .mais-ico.blue,html.dark .mais-ico.orange,html.dark .mais-ico.purple,html.dark .mais-ico.green{background:#1A2230;color:#94A3B8}
html.dark .mais-item{background:var(--card);border-color:var(--line)}
/* ── Patch dark mode: componentes novos (resumo jornada, wizard custo,
   Lançamentos, import) que ainda usavam cor fixa ── */
html.dark .pchip{background:var(--card)}
html.dark .pchip.active{background:#2563EB;border-color:#2563EB;color:#fff}
html.dark .cv-inp-wrap{background:var(--card);border-color:var(--line)}
html.dark .cv-inp-wrap input{color:var(--text)}
html.dark .cv-opt-card,html.dark .cv-opt-row{background:var(--card);border-color:var(--line)}
html.dark .cv-opt-card.sel,html.dark .cv-opt-row.sel{background:rgba(37,99,235,.12);border-color:#2563EB}
html.dark .cv-opt-ico{background:#1A2230;color:var(--muted)}
html.dark .cv-back{background:var(--card);border-color:var(--line);color:var(--text)}
html.dark .cv-pct-btn{background:var(--card);border-color:var(--line);color:var(--muted)}
html.dark .cv-pct-btn.sel{background:#2563EB;border-color:#2563EB;color:#fff}
html.dark .cv-brow{background:var(--soft-line)}
html.dark .cv-daily{background:rgba(251,191,36,.1)!important;border-color:rgba(251,191,36,.3)!important;color:#FDE68A}
html.dark .cv-daily span{color:#FDE68A!important}
html.dark .cv-sug{background:rgba(37,99,235,.15)}
html.dark .li-row,html.dark .li-row-j{background:var(--card);border-color:var(--line)}
html.dark .li-ico.plat-99{background:var(--soft-line)}
html.dark .lanc-total-card,html.dark .cv-result{box-shadow:0 10px 28px rgba(0,0,0,.4)}
/* ── SPA core ── */
*{box-sizing:border-box;margin:0;padding:0;-webkit-tap-highlight-color:transparent}
html,body{height:100%;background:var(--bg);font-family:Inter,system-ui,-apple-system,sans-serif;color:var(--text);overflow:hidden;-webkit-font-smoothing:antialiased}
.app{max-width:430px;margin:0 auto;height:100dvh;background:var(--bg);position:relative;overflow:hidden;padding-top:calc(var(--top) + var(--safe-top));padding-bottom:calc(var(--bottom) + var(--safe-bottom))}
.scroll{height:100%;overflow-y:auto;overflow-x:hidden;-webkit-overflow-scrolling:touch;padding:12px 0 24px}.scroll::-webkit-scrollbar{display:none}
.screen{display:none}.screen.on{display:block}
/* Topbar */
.topbar{position:fixed;top:0;left:50%;transform:translateX(-50%);width:min(430px,100vw);height:calc(var(--top) + var(--safe-top));padding:var(--safe-top) 12px 0 16px;display:flex;align-items:center;background:rgba(255,255,255,.97);backdrop-filter:blur(18px);border-bottom:1px solid var(--line);z-index:100;gap:8px}
.brand{display:flex;align-items:center;gap:9px;flex:1}.mark{width:34px;height:34px;border-radius:13px;background:#0F172A;color:#fff;display:flex;align-items:center;justify-content:center;font-weight:800;font-size:11px}.name{font-size:18px;font-weight:800;letter-spacing:-.04em}.name b{color:#0A2F6B}
.tbicons{display:flex;gap:2px}.iconbtn{width:36px;height:36px;border-radius:13px;background:transparent;border:none;color:#475569;display:flex;align-items:center;justify-content:center;cursor:pointer}.iconbtn:active{background:#F1F5F9}
/* Bottom nav */
.bottom{position:fixed;left:50%;bottom:0;transform:translateX(-50%);width:min(430px,100vw);height:calc(var(--bottom) + var(--safe-bottom));padding:8px 10px var(--safe-bottom);background:rgba(255,255,255,.97);backdrop-filter:blur(18px);border-top:1px solid var(--line);display:flex;align-items:center;z-index:100;box-shadow:0 -10px 28px rgba(15,23,42,.07)}
.nav{flex:1;height:54px;border-radius:16px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;color:#94A3B8;font-size:10px;font-weight:800;cursor:pointer;touch-action:manipulation;transition:all .12s}
.nav.active{background:#F8FAFC;color:#0A2F6B;box-shadow:0 2px 10px rgba(15,23,42,.06)}
.nav svg{width:21px;height:21px;flex-shrink:0}
.center{width:84px;display:flex;justify-content:center}
.start{display:flex;align-items:center;justify-content:center;flex-direction:column;gap:2px;width:76px;height:68px;border-radius:20px;background:#0A2F6B;border:3px solid var(--bg);box-shadow:0 10px 28px rgba(10,47,107,.35);margin-top:-36px;cursor:pointer;touch-action:manipulation;transition:background .2s}
.start svg{fill:none!important}
.start.running{background:#22C55E;box-shadow:0 10px 28px rgba(34,197,94,.38)}
.start.paused{background:#9CA3AF;box-shadow:0 10px 28px rgba(156,163,175,.38)}
.start.ended{background:#0A2F6B;box-shadow:0 10px 28px rgba(10,47,107,.38)}
.start svg{flex-shrink:0;filter:drop-shadow(0 1px 2px rgba(0,0,0,.15))}.start span{f
===== #F59E0B @ 2919 =====
<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: blob: https:; connect-src 'self' https://jlsrpptslwfhmkvelaro.supabase.co wss://jlsrpptslwfhmkvelaro.supabase.co https://wrmgmsmpszsdmntketfq.supabase.co wss://wrmgmsmpszsdmntketfq.supabase.co https://nominatim.openstreetmap.org https://router.project-osrm.org https://overpass-api.de https://smartmobi-proxy.yurisilva1990.workers.dev; object-src 'none'; base-uri 'self'; form-action 'self'">
<meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="default">
<meta name="description" content="MōB — a ferramenta da sua jornada">
<meta name="theme-color" content="#0A2F6B">
<meta name="mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-title" content="MōB">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<link rel="manifest" href="manifest.json">
<link rel="apple-touch-icon" href="apple-touch-icon.png">
<title>MōB</title>
<script>
// Aplica o modo escuro antes da primeira pintura, pra nao dar aquele flash de tela clara.
try {
  const cfg = JSON.parse(localStorage.getItem('sm_cfg'));
  if (cfg && cfg.darkMode) document.documentElement.classList.add('dark');
} catch(e) {}
</script>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
<style>

:root{--font-brand:'Poppins',sans-serif;--font-app:'Inter',system-ui,-apple-system,BlinkMacSystemFont,sans-serif;
  /* Shared tokens */
  --bg:#F7F9FC;--card:#FFFFFF;--text:#0F172A;--muted:#64748B;--muted2:#94A3B8;
  --line:#E2E8F0;--soft-line:#E8EEF5;
  --blue:#0A2F6B;--mob-blue:#0A2F6B;--mob-gray:#9CA3AF;--blue2:#0284C7;
  --green:#22C55E;--green2:#16A34A;
  --red:#EF4444;--orange:#F59E0B;--amber:#FACC15;--purple:#7C3AED;
  --greenBg:#ECFDF3;--orangeBg:#FFF7ED;--redBg:#FEF2F2;--purpleBg:#F5F3FF;--blueBg:#EFF6FF;
  --shadow:0 10px 28px rgba(15,23,42,.08);--soft:0 3px 14px rgba(15,23,42,.06);
  --r:24px;--radius:24px;
  --safe-top:max(env(safe-area-inset-top),14px);
  --safe-bottom:max(env(safe-area-inset-bottom),10px);
  --top:0px;--bottom:78px;
}
/* ── Modo escuro: paleta neutra, cor só em status/valores ── */
html.dark{
  --bg:#0A0F1A;--card:#111723;--text:#E2E8F0;--muted:#94A3B8;--muted2:#64748B;
  --line:#1E2735;--soft-line:#1A2230;
  --green:#10B981;--green2:#059669;
  --red:#EF4444;--orange:#F59E0B;--amber:#EAB308;--purple:#8B5CF6;
  --greenBg:rgba(16,185,129,.12);--orangeBg:rgba(245,158,11,.12);--redBg:rgba(239,68,68,.12);
  --purpleBg:rgba(139,92,246,.12);--blueBg:rgba(14,165,233,.12);
  --shadow:0 10px 28px rgba(0,0,0,.35);--soft:0 3px 14px rgba(0,0,0,.25);
}
html.dark body{background:var(--bg)}
/* Cards com gradiente colorido fixo (nao usam variavel) ficam neutros no escuro,
   seguindo a direcao aprovada — cor só em pontos pontuais, nao no fundo todo */
html.dark .journey.idle,html.dark .journey.ended,html.dark .journey.running{
  background:var(--card);border:1px solid var(--line);box-shadow:none
}
html.dark .hero{background:var(--card);border:1px solid var(--line);box-shadow:none}
html.dark .amount-card.receita,html.dark .amount-card.despesa{background:var(--card);border:1px solid var(--line)}
html.dark input,html.dark select,html.dark textarea{background:#0F1726;color:var(--text);border-color:var(--line)}
html.dark .inp,html.dark .cat-inp2{background:#0F1726;color:var(--text)}
html.dark .sheet,html.dark .mo .sheet{background:var(--card)}
html.dark img.invert-dark{filter:invert(1) brightness(1.4)}
/* Ícones do menu Mais viram neutros no escuro — cor só sobra pra dinheiro
   (receita/despesa), não pra decoração de cada item de menu */
html.dark .mais-ico,html.dark .mais-ico.blue,html.dark .mais-ico.orange,html.dark .mais-ico.purple,html.dark .mais-ico.green{background:#1A2230;color:#94A3B8}
html.dark .mais-item{background:var(--card);border-color:var(--line)}
/* ── Patch dark mode: componentes novos (resumo jornada, wizard custo,
   Lançamentos, import) que ainda usavam cor fixa ── */
html.dark .pchip{background:var(--card)}
html.dark .pchip.active{background:#2563EB;border-color:#2563EB;color:#fff}
html.dark .cv-inp-wrap{background:var(--card);border-color:var(--line)}
html.dark .cv-inp-wrap input{color:var(--text)}
html.dark .cv-opt-card,html.dark .cv-opt-row{background:var(--card);border-color:var(--line)}
html.dark .cv-opt-card.sel,html.dark .cv-opt-row.sel{background:rgba(37,99,235,.12);border-color:#2563EB}
html.dark .cv-opt-ico{background:#1A2230;color:var(--muted)}
html.dark .cv-back{background:var(--card);border-color:var(--line);color:var(--text)}
html.dark .cv-pct-btn{background:var(--card);border-color:var(--line);color:var(--muted)}
html.dark .cv-pct-btn.sel{background:#2563EB;border-color:#2563EB;color:#fff}
html.dark .cv-brow{background:var(--soft-line)}
html.dark .cv-daily{background:rgba(251,191,36,.1)!important;border-color:rgba(251,191,36,.3)!important;color:#FDE68A}
html.dark .cv-daily span{color:#FDE68A!important}
html.dark .cv-sug{background:rgba(37,99,235,.15)}
html.dark .li-row,html.dark .li-row-j{background:var(--card);border-color:var(--line)}
html.dark .li-ico.plat-99{background:var(--soft-line)}
html.dark .lanc-total-card,html.dark .cv-result{box-shadow:0 10px 28px rgba(0,0,0,.4)}
/* ── SPA core ── */
*{box-sizing:border-box;margin:0;padding:0;-webkit-tap-highlight-color:transparent}
html,body{height:100%;background:var(--bg);font-family:Inter,system-ui,-apple-system,sans-serif;color:var(--text);overflow:hidden;-webkit-font-smoothing:antialiased}
.app{max-width:430px;margin:0 auto;height:100dvh;background:var(--bg);position:relative;overflow:hidden;padding-top:calc(var(--top) + var(--safe-top));padding-bottom:calc(var(--bottom) + var(--safe-bottom))}
.scroll{height:100%;overflow-y:auto;overflow-x:hidden;-webkit-overflow-scrolling:touch;padding:12px 0 24px}.scroll::-webkit-scrollbar{display:none}
.screen{display:none}.screen.on{display:block}
/* Topbar */
.topbar{position:fixed;top:0;left:50%;transform:translateX(-50%);width:min(430px,100vw);height:calc(var(--top) + var(--safe-top));padding:var(--safe-top) 12px 0 16px;display:flex;align-items:center;background:rgba(255,255,255,.97);backdrop-filter:blur(18px);border-bottom:1px solid var(--line);z-index:100;gap:8px}
.brand{display:flex;align-items:center;gap:9px;flex:1}.mark{width:34px;height:34px;border-radius:13px;background:#0F172A;color:#fff;display:flex;align-items:center;justify-content:center;font-weight:800;font-size:11px}.name{font-size:18px;font-weight:800;letter-spacing:-.04em}.name b{color:#0A2F6B}
.tbicons{display:flex;gap:2px}.iconbtn{width:36px;height:36px;border-radius:13px;background:transparent;border:none;color:#475569;display:flex;align-items:center;justify-content:center;cursor:pointer}.iconbtn:active{background:#F1F5F9}
/* Bottom nav */
.bottom{position:fixed;left:50%;bottom:0;transform:translateX(-50%);width:min(430px,100vw);height:calc(var(--bottom) + var(--safe-bottom));padding:8px 10px var(--safe-bottom);background:rgba(255,255,255,.97);backdrop-filter:blur(18px);border-top:1px solid var(--line);display:flex;align-items:center;z-index:100;box-shadow:0 -10px 28px rgba(15,23,42,.07)}
.nav{flex:1;height:54px;border-radius:16px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;color:#94A3B8;font-size:10px;font-weight:800;cursor:pointer;touch-action:manipulation;transition:all .12s}
.nav.active{background:#F8FAFC;color:#0A2F6B;box-shadow:0 2px 10px rgba(15,23,42,.06)}
.nav svg{width:21px;height:21px;flex-shrink:0}
.center{width:84px;display:flex;justify-content:center}
.start{display:flex;align-items:center;justify-content:center;flex-direction:column;gap:2px;width:76px;height:68px;border-radius:20px;background:#0A2F6B;border:3px solid var(--bg);box-shadow:0 10px 28px rgba(10,47,107,.35);margin-top:-36px;cursor:pointer;touch-action:manipulation;transition:background .2s}
.start svg{fill:none!important}
.start.running{background:#22C55E;box-shadow:0 10px 28px rgba(34,197,94,.38)}
.start.paused{background:#9CA3AF;box-shadow:0 10px 28px rgba(156,163,175,.38)}
.start.ended{background:#0A2F6B;box-shadow:0 10px 28px rgba(10,47,107,.38)}
.start svg{flex-shrink:0;filter:drop-shadow(0 1px 2px rgba(0,0,0,.15))}.start span{font-size:9px;font-weight:800;text-transform:uppercase;letter-spacing:.04em;color:white;line-height:1;margin-top:1px}
/* Modal */
.mo{position:fixed;inset:0;background:rgba(15,23,42,.45);display:none;align-items:flex-end;z-index:200;backdrop-filter:blur(4px)}
.mo.on{display:flex}.mo.ctr{align-items:center;justify-content:center;padding:16px}
.sheet{width:100%;max-width:430px;margin:0 auto;background:#fff;border-radius:28px 28px 0 0;padding:16px;max-height:88dvh;overflow-y:auto;animation:slideUp .22s cubic-bezier(.4,0,.2,1)}
.sheet.ctr{border-radius:28px;max-width:420px;animation:popIn .2s cubic-bezier(.34,1.56,.64,1)}
.s
===== #F59E0B @ 16314 =====
{font-size:12px;color:#64748b;flex:1}.adbtn{font-size:11px;font-weight:800;color:#0A2F6B;background:#e0f2fe;border-radius:99px;padding:6px 10px}
.hero{margin:0 16px 14px;border-radius:30px;min-height:306px;padding:18px 17px 18px;background:linear-gradient(135deg,#0f172a 0%,#0b7fc1 58%,#0ea5e9 100%);box-shadow:0 18px 42px rgba(14,165,233,.24);color:#fff;position:relative;overflow:hidden}.hero:before{content:"";position:absolute;right:-70px;top:-65px;width:190px;height:190px;border-radius:50%;background:rgba(255,255,255,.08)}.hero:after{content:"";position:absolute;left:-75px;bottom:-90px;width:210px;height:210px;border-radius:50%;background:rgba(255,255,255,.055)}.hero>*{position:relative;z-index:1}.period{display:flex;align-items:center;gap:8px;margin-bottom:18px}.seg{display:flex;background:rgba(255,255,255,.16);padding:4px;border-radius:999px;gap:3px;flex:1}.seg button{flex:1;border:0;border-radius:999px;background:transparent;color:rgba(255,255,255,.76);font-size:12px;font-weight:800;padding:8px 0}.seg button.on{background:#fff;color:#0f172a;box-shadow:0 3px 10px rgba(0,0,0,.10)}.filter{width:36px;height:36px;border-radius:50%;display:grid;place-items:center;border:1px solid rgba(255,255,255,.25);background:rgba(255,255,255,.12);color:#fff}.kicker{font-size:11px;font-weight:700;color:rgba(255,255,255,.72);letter-spacing:.12em;text-transform:uppercase;text-align:center}.amount{font-family:'Inter',system-ui,sans-serif;text-align:center;font-size:47px;font-weight:600;line-height:1;margin-top:6px;letter-spacing:-.05em}.sub{text-align:center;font-size:13px;font-weight:700;color:rgba(255,255,255,.78);margin-top:6px}.metrics{margin-top:19px;background:rgba(255,255,255,.115);border:1px solid rgba(255,255,255,.13);border-radius:20px;overflow:hidden}.mrow{display:grid;grid-template-columns:repeat(3,1fr)}.mrow:first-child{border-bottom:1px solid rgba(255,255,255,.12)}.metric{padding:11px 6px;text-align:center;border-right:1px solid rgba(255,255,255,.12)}.metric:last-child{border-right:0}.mv{font-family:'Inter',system-ui,sans-serif;font-size:16px;font-weight:700}.ml{font-size:10px;margin-top:4px;color:rgba(255,255,255,.7);font-weight:700}.mv.good{color:#86efac}.mv.warn{color:#fde68a}.mv.bad{color:#fdba74}.goal{margin-top:14px}.goal-top{display:flex;justify-content:space-between;font-size:12px;font-weight:700;color:rgba(255,255,255,.78);margin-bottom:7px}.goal a{color:#fff;text-decoration:underline;text-underline-offset:3px}.bar{height:10px;border-radius:10px;background:rgba(255,255,255,.18);overflow:hidden}.fill{height:100%;border-radius:10px;background:linear-gradient(90deg,#fde047,#22c55e);width:86%}
.chart{margin:12px 16px 0;background:#fff;border:1px solid var(--line);box-shadow:var(--soft);border-radius:24px;padding:14px 14px 13px}.chead{display:flex;align-items:center;gap:10px;margin-bottom:10px}.ctitle{font-size:14px;font-weight:800;flex:1}.mini-tabs{display:flex;background:#f1f5f9;border-radius:999px;padding:3px;gap:2px}.mini-tabs span{font-size:10px;font-weight:800;color:#64748b;padding:5px 8px;border-radius:999px}.mini-tabs span.on{background:#fff;color:#0f172a;box-shadow:0 2px 8px rgba(15,23,42,.08)}.bars{height:128px;display:flex;align-items:end;gap:7px;padding:10px 2px 0;border-bottom:1px solid #eef2f7}.baritem{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end;gap:7px;min-width:0}.col{width:min(18px,80%);background:#0ea5e9;border-radius:3px 3px 0 0}.col.alt{background:#22c55e}.col.warn{background:#f59e0b}.lbl{font-size:9px;font-weight:800;color:#94a3b8;white-space:nowrap}.bars.many{gap:3px}.bars.many .col{width:min(8px,72%);border-radius:2px 2px 0 0}.bars.medium .col{width:min(13px,78%)}.legend{display:flex;gap:12px;flex-wrap:wrap;margin-top:10px}.leg{font-size:11px;color:#64748b;font-weight:700;display:flex;align-items:center;gap:6px}.dot{width:8px;height:8px;border-radius:50%;background:#0A2F6B}.dot.g{background:var(--green)}.dot.o{background:var(--orange)}.pie-wrap{display:grid;grid-template-columns:130px 1fr;gap:12px;align-items:center;padding:8px 2px}.pie{width:124px;height:124px;border-radius:50%;background:conic-gradient(#0ea5e9 0 46%,#22c55e 46% 68%,#f59e0b 68% 83%,#ef4444 83% 100%);display:grid;place-items:center}.pie:after{content:"";width:64px;height:64px;border-radius:50%;background:#fff}.list{display:flex;flex-direction:column;gap:8px}.li{display:flex;justify-content:space-between;font-size:12px;color:#475569;font-weight:700}.li b{font-family:'Inter',system-ui,sans-serif;color:#0f172a}.partner{margin:12px 16px;background:#fff;border:1px solid var(--line);box-shadow:var(--soft);border-radius:22px;padding:15px 16px}.eyebrow{font-size:10px;color:#f59e0b;font-weight:900;letter-spacing:.08em;text-transform:uppercase}.pt{font-size:14px;font-weight:800;margin-top:3px}.ps{font-size:12px;color:#64748b;margin-top:2px}
@media(max-width:360px){.amount{font-size:40px}.hero{min-height:292px}.pie-wrap{grid-template-columns:1fr}.pie{margin:auto}}



*{box-sizing:border-box}body{margin:0;background:var(--bg);font-family:var(--font-app);color:var(--text);-webkit-font-smoothing:antialiased}.app{max-width:430px;margin:0 auto;min-height:100vh;background:var(--bg);padding-bottom:100px}.top{position:sticky;top:0;z-index:20;background:rgba(255,255,255,.94);backdrop-filter:blur(16px);border-bottom:1px solid var(--line);padding:14px 16px;display:flex;align-items:center;gap:12px}.back{width:36px;height:36px;border-radius:14px;border:1px solid var(--line);background:#fff;display:grid;place-items:center;color:#334155}.title{flex:1}.title h1{margin:0;font-size:20px;letter-spacing:-.04em}.title p{margin:2px 0 0;color:var(--muted);font-size:12px}.iconBtn{width:36px;height:36px;border-radius:14px;background:#fff;border:1px solid var(--line);display:grid;place-items:center;color:#334155}.wrap{padding:12px 16px}.period{display:flex;background:#eaf0f7;border:1px solid var(--line);border-radius:18px;padding:4px;margin-bottom:12px;gap:4px}.period button{flex:1;border:0;background:transparent;border-radius:14px;padding:10px 4px;font-weight:800;color:#64748b}.period button.on{background:#fff;color:#0f172a;box-shadow:var(--soft)}.hero{background:linear-gradient(145deg,#0f172a,#0ea5e9);color:#fff;border-radius:30px;padding:22px 20px 18px;box-shadow:0 18px 42px rgba(14,165,233,.22);overflow:hidden;position:relative}.hero:after{content:"";position:absolute;width:190px;height:190px;border-radius:999px;background:rgba(255,255,255,.08);right:-58px;top:-50px}.heroTop{display:flex;justify-content:space-between;align-items:flex-start;position:relative;z-index:1}.label{font-size:11px;font-weight:800;letter-spacing:.12em;text-transform:uppercase;opacity:.72}.amount{font-size:39px;font-weight:800;letter-spacing:-.06em;margin:10px 0 4px}.sub{font-size:13px;opacity:.8}.filter{border:1px solid rgba(255,255,255,.24);background:rgba(255,255,255,.12);color:#fff;border-radius:14px;padding:8px 10px;font-weight:800;font-size:12px}.heroGrid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:18px;position:relative;z-index:1}.heroCell{background:rgba(255,255,255,.12);border:1px solid rgba(255,255,255,.12);border-radius:17px;padding:10px;text-align:center}.heroCell strong{display:block;font-size:15px;letter-spacing:-.03em}.heroCell span{font-size:10px;opacity:.72;font-weight:700}.card{background:var(--card);border:1px solid var(--line);border-radius:var(--r);box-shadow:var(--soft);margin-top:12px;overflow:hidden}.cardPad{padding:16px}.cardHead{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}.cardHead h2{margin:0;font-size:16px;letter-spacing:-.03em}.cardHead a,.link{font-size:12px;color:var(--blue2);font-weight:800;text-decoration:none}.seg{display:flex;background:#f1f5f9;border:1px solid var(--line);border-radius:14px;padding:3px;gap:3px}.seg button{border:0;background:transparent;border-radius:11px;padding:7px 10px;font-size:11px;font-weight:800;color:#64748b}.seg button.on{background:#fff;color:#0f172a;box-shadow:var(--soft)}.kpis{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.kpi{border:1px solid var(--line);border-radius:18px;padding:14px;background:#fff}.kpi .row{display:flex;align-items:center;gap:8px;margin-bottom:9px}.ico{width:34px;height:34px;border-radius:13px;display:grid;place-items:center;border:1px solid transparent}.ico svg{width:18px;height:18px;stroke-width:2;fill:none;stroke:currentColor;stroke-linecap:round;stroke-linejoin:round}.kpi span{color:var(--muted);font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.06em}.kpi strong{display:block;font-size:21px;letter-spacing:-.04em}.bar{height:8px;background:#edf2f7;border-radius:999px;overflow:hidden;margin-top:12px}.bar i{display:block;height:100%;background:linear-gradient(90deg,#0EA5E9,var(--green));width:72%;border-radius:999px}.list{display:flex;flex-direction:column}.item{display:flex;align-items:center;gap:12px;padding:13px 16px;border-top:1px solid var(--line)}.item:first-child{border-top:0}.itemBody{flex:1;min-width:0}.itemBody b{display:block;font-size:13px}.itemBody span{display:block;color:var(--muted);font-size:11px;margin-top:2px}.value{font-weight:800;font-size:14px}.in{color:#16a34a}.out{color:#dc2626}.badge{font-size:10px;font-weight:800;padding:4px 8px;border-radius:999px;background:#f1f5f9;color:#64748b}.chart{height:150px;display:flex;align-items:flex-end;gap:8px;border-bottom:1px solid var(--line);padding:10px 0 0}.barCol{flex:1;display:flex;flex-direction:column;align-items:center;gap:6px;height:100%;justify-content:flex-end}.barCol i{width:10px;max-width:22px;background:#0ea5e9;border-radius:2px 2px 0 0;display:block}.barCol small{font-size:9px;color:#94a3b8;font-weight:800}.split{display:grid;grid-template-columns:1fr 1fr;gap:10px}.donut{width:120px;height:120px;border-radius:999px;background:conic-gradient(#0ea5e9 0 46%,#22c55e 46% 72%,#f59e0b 72% 88%,#ef4444 88% 100%);margin:4px auto;position:relative}.donut:after{content:"";position:absolute;inset:28
===== #F59E0B @ 16988 =====
ro>*{position:relative;z-index:1}.period{display:flex;align-items:center;gap:8px;margin-bottom:18px}.seg{display:flex;background:rgba(255,255,255,.16);padding:4px;border-radius:999px;gap:3px;flex:1}.seg button{flex:1;border:0;border-radius:999px;background:transparent;color:rgba(255,255,255,.76);font-size:12px;font-weight:800;padding:8px 0}.seg button.on{background:#fff;color:#0f172a;box-shadow:0 3px 10px rgba(0,0,0,.10)}.filter{width:36px;height:36px;border-radius:50%;display:grid;place-items:center;border:1px solid rgba(255,255,255,.25);background:rgba(255,255,255,.12);color:#fff}.kicker{font-size:11px;font-weight:700;color:rgba(255,255,255,.72);letter-spacing:.12em;text-transform:uppercase;text-align:center}.amount{font-family:'Inter',system-ui,sans-serif;text-align:center;font-size:47px;font-weight:600;line-height:1;margin-top:6px;letter-spacing:-.05em}.sub{text-align:center;font-size:13px;font-weight:700;color:rgba(255,255,255,.78);margin-top:6px}.metrics{margin-top:19px;background:rgba(255,255,255,.115);border:1px solid rgba(255,255,255,.13);border-radius:20px;overflow:hidden}.mrow{display:grid;grid-template-columns:repeat(3,1fr)}.mrow:first-child{border-bottom:1px solid rgba(255,255,255,.12)}.metric{padding:11px 6px;text-align:center;border-right:1px solid rgba(255,255,255,.12)}.metric:last-child{border-right:0}.mv{font-family:'Inter',system-ui,sans-serif;font-size:16px;font-weight:700}.ml{font-size:10px;margin-top:4px;color:rgba(255,255,255,.7);font-weight:700}.mv.good{color:#86efac}.mv.warn{color:#fde68a}.mv.bad{color:#fdba74}.goal{margin-top:14px}.goal-top{display:flex;justify-content:space-between;font-size:12px;font-weight:700;color:rgba(255,255,255,.78);margin-bottom:7px}.goal a{color:#fff;text-decoration:underline;text-underline-offset:3px}.bar{height:10px;border-radius:10px;background:rgba(255,255,255,.18);overflow:hidden}.fill{height:100%;border-radius:10px;background:linear-gradient(90deg,#fde047,#22c55e);width:86%}
.chart{margin:12px 16px 0;background:#fff;border:1px solid var(--line);box-shadow:var(--soft);border-radius:24px;padding:14px 14px 13px}.chead{display:flex;align-items:center;gap:10px;margin-bottom:10px}.ctitle{font-size:14px;font-weight:800;flex:1}.mini-tabs{display:flex;background:#f1f5f9;border-radius:999px;padding:3px;gap:2px}.mini-tabs span{font-size:10px;font-weight:800;color:#64748b;padding:5px 8px;border-radius:999px}.mini-tabs span.on{background:#fff;color:#0f172a;box-shadow:0 2px 8px rgba(15,23,42,.08)}.bars{height:128px;display:flex;align-items:end;gap:7px;padding:10px 2px 0;border-bottom:1px solid #eef2f7}.baritem{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end;gap:7px;min-width:0}.col{width:min(18px,80%);background:#0ea5e9;border-radius:3px 3px 0 0}.col.alt{background:#22c55e}.col.warn{background:#f59e0b}.lbl{font-size:9px;font-weight:800;color:#94a3b8;white-space:nowrap}.bars.many{gap:3px}.bars.many .col{width:min(8px,72%);border-radius:2px 2px 0 0}.bars.medium .col{width:min(13px,78%)}.legend{display:flex;gap:12px;flex-wrap:wrap;margin-top:10px}.leg{font-size:11px;color:#64748b;font-weight:700;display:flex;align-items:center;gap:6px}.dot{width:8px;height:8px;border-radius:50%;background:#0A2F6B}.dot.g{background:var(--green)}.dot.o{background:var(--orange)}.pie-wrap{display:grid;grid-template-columns:130px 1fr;gap:12px;align-items:center;padding:8px 2px}.pie{width:124px;height:124px;border-radius:50%;background:conic-gradient(#0ea5e9 0 46%,#22c55e 46% 68%,#f59e0b 68% 83%,#ef4444 83% 100%);display:grid;place-items:center}.pie:after{content:"";width:64px;height:64px;border-radius:50%;background:#fff}.list{display:flex;flex-direction:column;gap:8px}.li{display:flex;justify-content:space-between;font-size:12px;color:#475569;font-weight:700}.li b{font-family:'Inter',system-ui,sans-serif;color:#0f172a}.partner{margin:12px 16px;background:#fff;border:1px solid var(--line);box-shadow:var(--soft);border-radius:22px;padding:15px 16px}.eyebrow{font-size:10px;color:#f59e0b;font-weight:900;letter-spacing:.08em;text-transform:uppercase}.pt{font-size:14px;font-weight:800;margin-top:3px}.ps{font-size:12px;color:#64748b;margin-top:2px}
@media(max-width:360px){.amount{font-size:40px}.hero{min-height:292px}.pie-wrap{grid-template-columns:1fr}.pie{margin:auto}}



*{box-sizing:border-box}body{margin:0;background:var(--bg);font-family:var(--font-app);color:var(--text);-webkit-font-smoothing:antialiased}.app{max-width:430px;margin:0 auto;min-height:100vh;background:var(--bg);padding-bottom:100px}.top{position:sticky;top:0;z-index:20;background:rgba(255,255,255,.94);backdrop-filter:blur(16px);border-bottom:1px solid var(--line);padding:14px 16px;display:flex;align-items:center;gap:12px}.back{width:36px;height:36px;border-radius:14px;border:1px solid var(--line);background:#fff;display:grid;place-items:center;color:#334155}.title{flex:1}.title h1{margin:0;font-size:20px;letter-spacing:-.04em}.title p{margin:2px 0 0;color:var(--muted);font-size:12px}.iconBtn{width:36px;height:36px;border-radius:14px;background:#fff;border:1px solid var(--line);display:grid;place-items:center;color:#334155}.wrap{padding:12px 16px}.period{display:flex;background:#eaf0f7;border:1px solid var(--line);border-radius:18px;padding:4px;margin-bottom:12px;gap:4px}.period button{flex:1;border:0;background:transparent;border-radius:14px;padding:10px 4px;font-weight:800;color:#64748b}.period button.on{background:#fff;color:#0f172a;box-shadow:var(--soft)}.hero{background:linear-gradient(145deg,#0f172a,#0ea5e9);color:#fff;border-radius:30px;padding:22px 20px 18px;box-shadow:0 18px 42px rgba(14,165,233,.22);overflow:hidden;position:relative}.hero:after{content:"";position:absolute;width:190px;height:190px;border-radius:999px;background:rgba(255,255,255,.08);right:-58px;top:-50px}.heroTop{display:flex;justify-content:space-between;align-items:flex-start;position:relative;z-index:1}.label{font-size:11px;font-weight:800;letter-spacing:.12em;text-transform:uppercase;opacity:.72}.amount{font-size:39px;font-weight:800;letter-spacing:-.06em;margin:10px 0 4px}.sub{font-size:13px;opacity:.8}.filter{border:1px solid rgba(255,255,255,.24);background:rgba(255,255,255,.12);color:#fff;border-radius:14px;padding:8px 10px;font-weight:800;font-size:12px}.heroGrid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:18px;position:relative;z-index:1}.heroCell{background:rgba(255,255,255,.12);border:1px solid rgba(255,255,255,.12);border-radius:17px;padding:10px;text-align:center}.heroCell strong{display:block;font-size:15px;letter-spacing:-.03em}.heroCell span{font-size:10px;opacity:.72;font-weight:700}.card{background:var(--card);border:1px solid var(--line);border-radius:var(--r);box-shadow:var(--soft);margin-top:12px;overflow:hidden}.cardPad{padding:16px}.cardHead{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}.cardHead h2{margin:0;font-size:16px;letter-spacing:-.03em}.cardHead a,.link{font-size:12px;color:var(--blue2);font-weight:800;text-decoration:none}.seg{display:flex;background:#f1f5f9;border:1px solid var(--line);border-radius:14px;padding:3px;gap:3px}.seg button{border:0;background:transparent;border-radius:11px;padding:7px 10px;font-size:11px;font-weight:800;color:#64748b}.seg button.on{background:#fff;color:#0f172a;box-shadow:var(--soft)}.kpis{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.kpi{border:1px solid var(--line);border-radius:18px;padding:14px;background:#fff}.kpi .row{display:flex;align-items:center;gap:8px;margin-bottom:9px}.ico{width:34px;height:34px;border-radius:13px;display:grid;place-items:center;border:1px solid transparent}.ico svg{width:18px;height:18px;stroke-width:2;fill:none;stroke:currentColor;stroke-linecap:round;stroke-linejoin:round}.kpi span{color:var(--muted);font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.06em}.kpi strong{display:block;font-size:21px;letter-spacing:-.04em}.bar{height:8px;background:#edf2f7;border-radius:999px;overflow:hidden;margin-top:12px}.bar i{display:block;height:100%;background:linear-gradient(90deg,#0EA5E9,var(--green));width:72%;border-radius:999px}.list{display:flex;flex-direction:column}.item{display:flex;align-items:center;gap:12px;padding:13px 16px;border-top:1px solid var(--line)}.item:first-child{border-top:0}.itemBody{flex:1;min-width:0}.itemBody b{display:block;font-size:13px}.itemBody span{display:block;color:var(--muted);font-size:11px;margin-top:2px}.value{font-weight:800;font-size:14px}.in{color:#16a34a}.out{color:#dc2626}.badge{font-size:10px;font-weight:800;padding:4px 8px;border-radius:999px;background:#f1f5f9;color:#64748b}.chart{height:150px;display:flex;align-items:flex-end;gap:8px;border-bottom:1px solid var(--line);padding:10px 0 0}.barCol{flex:1;display:flex;flex-direction:column;align-items:center;gap:6px;height:100%;justify-content:flex-end}.barCol i{width:10px;max-width:22px;background:#0ea5e9;border-radius:2px 2px 0 0;display:block}.barCol small{font-size:9px;color:#94a3b8;font-weight:800}.split{display:grid;grid-template-columns:1fr 1fr;gap:10px}.donut{width:120px;height:120px;border-radius:999px;background:conic-gradient(#0ea5e9 0 46%,#22c55e 46% 72%,#f59e0b 72% 88%,#ef4444 88% 100%);margin:4px auto;position:relative}.donut:after{content:"";position:absolute;inset:28px;background:#fff;border-radius:999px}.catRow{display:flex;align-items:center;gap:9px;margin:9px 0}.dot{width:10px;height:10px;border-radius:99px}.actions{display:grid;grid-template-columns:1fr 1fr;gap:10px}.action{border:1px solid var(--line);background:#fff;border-radius:18px;padding:13px;display:flex;align-items:center;gap:10px;text-align:left}.action strong{font-size:13px;display:block}.action span{font-size:11px;color:var(--muted)}.budgetRow{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-top:13px}.budgetRow:first-child{margin-top:0}.budgetRow b{font-size:13px}.budgetRow span{font-size:12px;color:var(--muted)}.budgetTrack{height:7
===== #F59E0B @ 17496 =====
rder:1px solid rgba(255,255,255,.25);background:rgba(255,255,255,.12);color:#fff}.kicker{font-size:11px;font-weight:700;color:rgba(255,255,255,.72);letter-spacing:.12em;text-transform:uppercase;text-align:center}.amount{font-family:'Inter',system-ui,sans-serif;text-align:center;font-size:47px;font-weight:600;line-height:1;margin-top:6px;letter-spacing:-.05em}.sub{text-align:center;font-size:13px;font-weight:700;color:rgba(255,255,255,.78);margin-top:6px}.metrics{margin-top:19px;background:rgba(255,255,255,.115);border:1px solid rgba(255,255,255,.13);border-radius:20px;overflow:hidden}.mrow{display:grid;grid-template-columns:repeat(3,1fr)}.mrow:first-child{border-bottom:1px solid rgba(255,255,255,.12)}.metric{padding:11px 6px;text-align:center;border-right:1px solid rgba(255,255,255,.12)}.metric:last-child{border-right:0}.mv{font-family:'Inter',system-ui,sans-serif;font-size:16px;font-weight:700}.ml{font-size:10px;margin-top:4px;color:rgba(255,255,255,.7);font-weight:700}.mv.good{color:#86efac}.mv.warn{color:#fde68a}.mv.bad{color:#fdba74}.goal{margin-top:14px}.goal-top{display:flex;justify-content:space-between;font-size:12px;font-weight:700;color:rgba(255,255,255,.78);margin-bottom:7px}.goal a{color:#fff;text-decoration:underline;text-underline-offset:3px}.bar{height:10px;border-radius:10px;background:rgba(255,255,255,.18);overflow:hidden}.fill{height:100%;border-radius:10px;background:linear-gradient(90deg,#fde047,#22c55e);width:86%}
.chart{margin:12px 16px 0;background:#fff;border:1px solid var(--line);box-shadow:var(--soft);border-radius:24px;padding:14px 14px 13px}.chead{display:flex;align-items:center;gap:10px;margin-bottom:10px}.ctitle{font-size:14px;font-weight:800;flex:1}.mini-tabs{display:flex;background:#f1f5f9;border-radius:999px;padding:3px;gap:2px}.mini-tabs span{font-size:10px;font-weight:800;color:#64748b;padding:5px 8px;border-radius:999px}.mini-tabs span.on{background:#fff;color:#0f172a;box-shadow:0 2px 8px rgba(15,23,42,.08)}.bars{height:128px;display:flex;align-items:end;gap:7px;padding:10px 2px 0;border-bottom:1px solid #eef2f7}.baritem{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end;gap:7px;min-width:0}.col{width:min(18px,80%);background:#0ea5e9;border-radius:3px 3px 0 0}.col.alt{background:#22c55e}.col.warn{background:#f59e0b}.lbl{font-size:9px;font-weight:800;color:#94a3b8;white-space:nowrap}.bars.many{gap:3px}.bars.many .col{width:min(8px,72%);border-radius:2px 2px 0 0}.bars.medium .col{width:min(13px,78%)}.legend{display:flex;gap:12px;flex-wrap:wrap;margin-top:10px}.leg{font-size:11px;color:#64748b;font-weight:700;display:flex;align-items:center;gap:6px}.dot{width:8px;height:8px;border-radius:50%;background:#0A2F6B}.dot.g{background:var(--green)}.dot.o{background:var(--orange)}.pie-wrap{display:grid;grid-template-columns:130px 1fr;gap:12px;align-items:center;padding:8px 2px}.pie{width:124px;height:124px;border-radius:50%;background:conic-gradient(#0ea5e9 0 46%,#22c55e 46% 68%,#f59e0b 68% 83%,#ef4444 83% 100%);display:grid;place-items:center}.pie:after{content:"";width:64px;height:64px;border-radius:50%;background:#fff}.list{display:flex;flex-direction:column;gap:8px}.li{display:flex;justify-content:space-between;font-size:12px;color:#475569;font-weight:700}.li b{font-family:'Inter',system-ui,sans-serif;color:#0f172a}.partner{margin:12px 16px;background:#fff;border:1px solid var(--line);box-shadow:var(--soft);border-radius:22px;padding:15px 16px}.eyebrow{font-size:10px;color:#f59e0b;font-weight:900;letter-spacing:.08em;text-transform:uppercase}.pt{font-size:14px;font-weight:800;margin-top:3px}.ps{font-size:12px;color:#64748b;margin-top:2px}
@media(max-width:360px){.amount{font-size:40px}.hero{min-height:292px}.pie-wrap{grid-template-columns:1fr}.pie{margin:auto}}



*{box-sizing:border-box}body{margin:0;background:var(--bg);font-family:var(--font-app);color:var(--text);-webkit-font-smoothing:antialiased}.app{max-width:430px;margin:0 auto;min-height:100vh;background:var(--bg);padding-bottom:100px}.top{position:sticky;top:0;z-index:20;background:rgba(255,255,255,.94);backdrop-filter:blur(16px);border-bottom:1px solid var(--line);padding:14px 16px;display:flex;align-items:center;gap:12px}.back{width:36px;height:36px;border-radius:14px;border:1px solid var(--line);background:#fff;display:grid;place-items:center;color:#334155}.title{flex:1}.title h1{margin:0;font-size:20px;letter-spacing:-.04em}.title p{margin:2px 0 0;color:var(--muted);font-size:12px}.iconBtn{width:36px;height:36px;border-radius:14px;background:#fff;border:1px solid var(--line);display:grid;place-items:center;color:#334155}.wrap{padding:12px 16px}.period{display:flex;background:#eaf0f7;border:1px solid var(--line);border-radius:18px;padding:4px;margin-bottom:12px;gap:4px}.period button{flex:1;border:0;background:transparent;border-radius:14px;padding:10px 4px;font-weight:800;color:#64748b}.period button.on{background:#fff;color:#0f172a;box-shadow:var(--soft)}.hero{background:linear-gradient(145deg,#0f172a,#0ea5e9);color:#fff;border-radius:30px;padding:22px 20px 18px;box-shadow:0 18px 42px rgba(14,165,233,.22);overflow:hidden;position:relative}.hero:after{content:"";position:absolute;width:190px;height:190px;border-radius:999px;background:rgba(255,255,255,.08);right:-58px;top:-50px}.heroTop{display:flex;justify-content:space-between;align-items:flex-start;position:relative;z-index:1}.label{font-size:11px;font-weight:800;letter-spacing:.12em;text-transform:uppercase;opacity:.72}.amount{font-size:39px;font-weight:800;letter-spacing:-.06em;margin:10px 0 4px}.sub{font-size:13px;opacity:.8}.filter{border:1px solid rgba(255,255,255,.24);background:rgba(255,255,255,.12);color:#fff;border-radius:14px;padding:8px 10px;font-weight:800;font-size:12px}.heroGrid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:18px;position:relative;z-index:1}.heroCell{background:rgba(255,255,255,.12);border:1px solid rgba(255,255,255,.12);border-radius:17px;padding:10px;text-align:center}.heroCell strong{display:block;font-size:15px;letter-spacing:-.03em}.heroCell span{font-size:10px;opacity:.72;font-weight:700}.card{background:var(--card);border:1px solid var(--line);border-radius:var(--r);box-shadow:var(--soft);margin-top:12px;overflow:hidden}.cardPad{padding:16px}.cardHead{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}.cardHead h2{margin:0;font-size:16px;letter-spacing:-.03em}.cardHead a,.link{font-size:12px;color:var(--blue2);font-weight:800;text-decoration:none}.seg{display:flex;background:#f1f5f9;border:1px solid var(--line);border-radius:14px;padding:3px;gap:3px}.seg button{border:0;background:transparent;border-radius:11px;padding:7px 10px;font-size:11px;font-weight:800;color:#64748b}.seg button.on{background:#fff;color:#0f172a;box-shadow:var(--soft)}.kpis{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.kpi{border:1px solid var(--line);border-radius:18px;padding:14px;background:#fff}.kpi .row{display:flex;align-items:center;gap:8px;margin-bottom:9px}.ico{width:34px;height:34px;border-radius:13px;display:grid;place-items:center;border:1px solid transparent}.ico svg{width:18px;height:18px;stroke-width:2;fill:none;stroke:currentColor;stroke-linecap:round;stroke-linejoin:round}.kpi span{color:var(--muted);font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.06em}.kpi strong{display:block;font-size:21px;letter-spacing:-.04em}.bar{height:8px;background:#edf2f7;border-radius:999px;overflow:hidden;margin-top:12px}.bar i{display:block;height:100%;background:linear-gradient(90deg,#0EA5E9,var(--green));width:72%;border-radius:999px}.list{display:flex;flex-direction:column}.item{display:flex;align-items:center;gap:12px;padding:13px 16px;border-top:1px solid var(--line)}.item:first-child{border-top:0}.itemBody{flex:1;min-width:0}.itemBody b{display:block;font-size:13px}.itemBody span{display:block;color:var(--muted);font-size:11px;margin-top:2px}.value{font-weight:800;font-size:14px}.in{color:#16a34a}.out{color:#dc2626}.badge{font-size:10px;font-weight:800;padding:4px 8px;border-radius:999px;background:#f1f5f9;color:#64748b}.chart{height:150px;display:flex;align-items:flex-end;gap:8px;border-bottom:1px solid var(--line);padding:10px 0 0}.barCol{flex:1;display:flex;flex-direction:column;align-items:center;gap:6px;height:100%;justify-content:flex-end}.barCol i{width:10px;max-width:22px;background:#0ea5e9;border-radius:2px 2px 0 0;display:block}.barCol small{font-size:9px;color:#94a3b8;font-weight:800}.split{display:grid;grid-template-columns:1fr 1fr;gap:10px}.donut{width:120px;height:120px;border-radius:999px;background:conic-gradient(#0ea5e9 0 46%,#22c55e 46% 72%,#f59e0b 72% 88%,#ef4444 88% 100%);margin:4px auto;position:relative}.donut:after{content:"";position:absolute;inset:28px;background:#fff;border-radius:999px}.catRow{display:flex;align-items:center;gap:9px;margin:9px 0}.dot{width:10px;height:10px;border-radius:99px}.actions{display:grid;grid-template-columns:1fr 1fr;gap:10px}.action{border:1px solid var(--line);background:#fff;border-radius:18px;padding:13px;display:flex;align-items:center;gap:10px;text-align:left}.action strong{font-size:13px;display:block}.action span{font-size:11px;color:var(--muted)}.budgetRow{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-top:13px}.budgetRow:first-child{margin-top:0}.budgetRow b{font-size:13px}.budgetRow span{font-size:12px;color:var(--muted)}.budgetTrack{height:7px;background:#eef2f7;border-radius:99px;overflow:hidden;margin-top:7px}.budgetTrack i{height:100%;display:block;border-radius:99px}.sheet{background:#fff;width:100%;max-width:430px;margin:0 auto;border-radius:28px 28px 0 0;padding:18px 16px 26px;box-shadow:0 -12px 35px rgba(15,23,42,.2)}.sheet h3{margin:0 0 14px;font-size:18px}.field{margin:12px 0}.field label{display:block;font-size:11px;text-transform:uppercase;font-weight:800;color:#64748b;margin-bottom:7px}.field input,.field select{width:100%;bord
===== #F59E0B @ 22696 =====
ute;width:190px;height:190px;border-radius:999px;background:rgba(255,255,255,.08);right:-58px;top:-50px}.heroTop{display:flex;justify-content:space-between;align-items:flex-start;position:relative;z-index:1}.label{font-size:11px;font-weight:800;letter-spacing:.12em;text-transform:uppercase;opacity:.72}.amount{font-size:39px;font-weight:800;letter-spacing:-.06em;margin:10px 0 4px}.sub{font-size:13px;opacity:.8}.filter{border:1px solid rgba(255,255,255,.24);background:rgba(255,255,255,.12);color:#fff;border-radius:14px;padding:8px 10px;font-weight:800;font-size:12px}.heroGrid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:18px;position:relative;z-index:1}.heroCell{background:rgba(255,255,255,.12);border:1px solid rgba(255,255,255,.12);border-radius:17px;padding:10px;text-align:center}.heroCell strong{display:block;font-size:15px;letter-spacing:-.03em}.heroCell span{font-size:10px;opacity:.72;font-weight:700}.card{background:var(--card);border:1px solid var(--line);border-radius:var(--r);box-shadow:var(--soft);margin-top:12px;overflow:hidden}.cardPad{padding:16px}.cardHead{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}.cardHead h2{margin:0;font-size:16px;letter-spacing:-.03em}.cardHead a,.link{font-size:12px;color:var(--blue2);font-weight:800;text-decoration:none}.seg{display:flex;background:#f1f5f9;border:1px solid var(--line);border-radius:14px;padding:3px;gap:3px}.seg button{border:0;background:transparent;border-radius:11px;padding:7px 10px;font-size:11px;font-weight:800;color:#64748b}.seg button.on{background:#fff;color:#0f172a;box-shadow:var(--soft)}.kpis{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.kpi{border:1px solid var(--line);border-radius:18px;padding:14px;background:#fff}.kpi .row{display:flex;align-items:center;gap:8px;margin-bottom:9px}.ico{width:34px;height:34px;border-radius:13px;display:grid;place-items:center;border:1px solid transparent}.ico svg{width:18px;height:18px;stroke-width:2;fill:none;stroke:currentColor;stroke-linecap:round;stroke-linejoin:round}.kpi span{color:var(--muted);font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.06em}.kpi strong{display:block;font-size:21px;letter-spacing:-.04em}.bar{height:8px;background:#edf2f7;border-radius:999px;overflow:hidden;margin-top:12px}.bar i{display:block;height:100%;background:linear-gradient(90deg,#0EA5E9,var(--green));width:72%;border-radius:999px}.list{display:flex;flex-direction:column}.item{display:flex;align-items:center;gap:12px;padding:13px 16px;border-top:1px solid var(--line)}.item:first-child{border-top:0}.itemBody{flex:1;min-width:0}.itemBody b{display:block;font-size:13px}.itemBody span{display:block;color:var(--muted);font-size:11px;margin-top:2px}.value{font-weight:800;font-size:14px}.in{color:#16a34a}.out{color:#dc2626}.badge{font-size:10px;font-weight:800;padding:4px 8px;border-radius:999px;background:#f1f5f9;color:#64748b}.chart{height:150px;display:flex;align-items:flex-end;gap:8px;border-bottom:1px solid var(--line);padding:10px 0 0}.barCol{flex:1;display:flex;flex-direction:column;align-items:center;gap:6px;height:100%;justify-content:flex-end}.barCol i{width:10px;max-width:22px;background:#0ea5e9;border-radius:2px 2px 0 0;display:block}.barCol small{font-size:9px;color:#94a3b8;font-weight:800}.split{display:grid;grid-template-columns:1fr 1fr;gap:10px}.donut{width:120px;height:120px;border-radius:999px;background:conic-gradient(#0ea5e9 0 46%,#22c55e 46% 72%,#f59e0b 72% 88%,#ef4444 88% 100%);margin:4px auto;position:relative}.donut:after{content:"";position:absolute;inset:28px;background:#fff;border-radius:999px}.catRow{display:flex;align-items:center;gap:9px;margin:9px 0}.dot{width:10px;height:10px;border-radius:99px}.actions{display:grid;grid-template-columns:1fr 1fr;gap:10px}.action{border:1px solid var(--line);background:#fff;border-radius:18px;padding:13px;display:flex;align-items:center;gap:10px;text-align:left}.action strong{font-size:13px;display:block}.action span{font-size:11px;color:var(--muted)}.budgetRow{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-top:13px}.budgetRow:first-child{margin-top:0}.budgetRow b{font-size:13px}.budgetRow span{font-size:12px;color:var(--muted)}.budgetTrack{height:7px;background:#eef2f7;border-radius:99px;overflow:hidden;margin-top:7px}.budgetTrack i{height:100%;display:block;border-radius:99px}.sheet{background:#fff;width:100%;max-width:430px;margin:0 auto;border-radius:28px 28px 0 0;padding:18px 16px 26px;box-shadow:0 -12px 35px rgba(15,23,42,.2)}.sheet h3{margin:0 0 14px;font-size:18px}.field{margin:12px 0}.field label{display:block;font-size:11px;text-transform:uppercase;font-weight:800;color:#64748b;margin-bottom:7px}.field input,.field select{width:100%;border:1px solid var(--line);border-radius:16px;background:#f8fafc;padding:14px;font:inherit;font-weight:600}@media(max-width:380px){.amount{font-size:34px}.kpi strong{font-size:19px}.heroGrid{gap:6px}.chart{gap:5px}.barCol i{width:8px}}

/* ── Jornada screen extras ── */
.daybar{display:flex;align-items:center;gap:10px;margin:4px 0 12px;padding:0 16px}.arrow{width:38px;height:38px;border-radius:14px;background:#fff;border:1px solid var(--line);box-shadow:0 3px 14px rgba(15,23,42,.06);display:flex;align-items:center;justify-content:center;color:var(--muted);font-size:20px;cursor:pointer;touch-action:manipulation}.datebox{flex:1;text-align:center}.datebox h1{font-size:19px;line-height:1.1;letter-spacing:-.04em}.datebox p{font-size:12px;color:var(--muted);margin-top:3px;font-weight:600}.today{font-size:10px;padding:2px 8px;border-radius:999px;background:rgba(14,165,233,.12);color:#0A2F6B;margin-left:5px}
.stats{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;padding:0 16px;margin-bottom:12px}
.stats-cards{display:grid;grid-template-columns:repeat(3,1fr);gap:9px}
.stats-cards .stat{background:var(--card);border:1px solid var(--line);border-radius:16px;padding:14px 6px;text-align:center;box-shadow:0 3px 14px rgba(15,23,42,.06)}
.stats-cards .v{font-size:15px;font-weight:800;line-height:1}
.stats-cards .v.green{color:var(--green2)}
.stats-cards .v.red{color:#EF4444}
.stats-cards .l{font-size:9.5px;color:var(--muted2);font-weight:700;text-transform:uppercase;letter-spacing:.04em;margin-top:6px}
/* R$/h e R$/km — largura total, preenchendo até as bordas do card */
/* Faixa de destaque R$/h + R$/km (23/07/2026, mockup aprovado — Opção A).
   Cor dinâmica POR INDICADOR (independente um do outro — R$/h pode estar
   verde com R$/km em vermelho ao mesmo tempo): compara com os mesmos
   limiares vermelho/verde que o MōB Flash já usa
   (DB.cfg().flashReader.kpis.rhora/.rkm), aplicada em renderJourneyCard(). */
.rate-strip{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:14px}
.rate-cell{text-align:center;border-radius:14px;padding:11px 6px;background:rgba(34,197,94,.16);border:1px solid rgba(34,197,94,.4);transition:background .3s,border-color .3s}
.rate-cell.o{background:rgba(245,158,11,.18);border-color:rgba(245,158,11,.45)}
.rate-cell.r{background:rgba(239,68,68,.20);border-color:rgba(239,68,68,.5)}
.rate-cell strong{display:block;font-size:21px;font-weight:800;color:#fff;letter-spacing:-.02em}
.rate-cell span{display:block;font-size:9.5px;font-weight:800;text-transform:uppercase;letter-spacing:.05em;color:#86efac;margin-top:2px}
.rate-cell.o span{color:#fde68a}
.rate-cell.r span{color:#fca5a5}
/* Metas do dia: CSS movido pra seção solta (ver .goal-item/.gbar mais
   abaixo, estilo "grande" já existente) — removido daqui em 23/07/2026,
   não fica mais embutido no fundo escuro do card da jornada. */
.stat{background:#fff;border:1px solid var(--line);border-radius:18px;padding:12px 8px;text-align:center;box-shadow:0 3px 14px rgba(15,23,42,.06)}.stat .v{font-family:'Inter',system-ui,sans-serif;font-size:15px;font-weight:700;white-space:nowrap}.stat .l{font-size:9px;color:var(--muted);font-weight:800;text-transform:uppercase;letter-spacing:.04em;margin-top:5px}.green{color:var(--green2)}.red{color:var(--red)}.blue{color:#0A2F6B}

/* ── Journey card (controles embutidos) ── */
.journey-wrap{margin:0 16px 12px;border-radius:26px;overflow:hidden;box-shadow:0 10px 30px rgba(15,23,42,.10)}
.journey{padding:16px;color:#fff;transition:background .25s;box-sizing:border-box}
.journey.idle{background:var(--blue)}
.journey.ended{background:var(--blue)}
.journey.running{background:#15803D}
.journey-breakdown{background:#fff;padding:14px}
.sbar-wrap{cursor:pointer;touch-action:manipulation}
.sbar{height:22px;border-radius:8px;overflow:hidden;display:flex;background:#F1F5F9;margin:0;pointer-events:none}
.sbar i{display:flex;align-items:center;justify-content:center;height:100%;font-size:9.5px;font-weight:800;color:#fff}
.sbar-list{max-height:0;overflow:hidden;transition:max-height .3s ease}
.sbar-list.open{max-height:220px;margin-top:10px}
.sbar-list .srow{display:flex;align-items:center;gap:8px;padding:6px 0;border-top:1px solid var(--line)}
.sbar-list .srow:first-child{border-top:0}
.sbar-list .sdot{width:8px;height:8px;border-radius:50%;flex-shrink:0}
.sbar-list .srow b{flex:1;font-size:11px;font-weight:800;color:var(--text);text-transform:uppercase;letter-spacing:.03em}
.sbar-list .srow .pct{font-size:10px;font-weight:800;color:var(--muted);width:28px;text-align:right}
.sbar-list .srow .stempo{font-size:10.5px;font-weight:800;color:var(--text);width:48px;text-align:right}
.sbar-list .srow .skm{font-size:10.5px;font-weight:700;color:var(--muted);width:56px;text-align:right}
.td-row.bd .sbar{background:rgba(255,255,255,.10)}
.td-row.bd .sbar-list.open{margin-top:12px}
.td-row.bd .sbar-list .srow{border-top:1px solid rgba(255,255,255,.14)}
.td-row.bd .sbar-list .srow b,.td-row.bd .sbar-list .srow .stempo{color:#fff}
.td-row.bd .sbar-list .srow .pct,.td-row.bd .sbar-list .srow .skm{color:rgba(255,255,255,.6)}
.jss-statusbar{padding:2px 14px 12px}
/* .journey.paused removido */
.j-head{display:flex;justify-co
===== #F59E0B @ 36580 =====
;pointer-events:none;z-index:0;top:-18%;height:136%;aspect-ratio:1/1}
.td-wm.left{left:-36px}
.td-wm.right{right:-40px}
.td-row>*:not(.td-wm){position:relative;z-index:1}
.td-corridas{display:flex;align-items:baseline;gap:9px}
.td-corridas strong{font-size:32px;font-weight:800;color:#fff;letter-spacing:-.03em;line-height:1}
.td-corridas span{font-size:12px;font-weight:800;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.04em}
.td-sub{display:flex;align-items:center;gap:8px;margin-top:8px}
.td-chip{display:inline-flex;align-items:center;gap:5px;font-size:11.5px;font-weight:800}
.td-chip.ok{color:#4ADE80}
.td-chip.no{color:#F87171}
.td-vline{width:1px;height:12px;background:rgba(255,255,255,.2)}
.td-grid1{text-align:center}
.td-grid1 strong{display:block;font-size:22px;font-weight:800;color:#fff;letter-spacing:-.02em}
.td-grid1 span{display:block;font-size:9.5px;font-weight:800;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.04em;margin-top:5px}
.tk-split{display:flex;align-items:stretch}
.tk-half{flex:1;position:relative;overflow:hidden;min-width:0;text-align:center;padding:2px 0}
.tk-half.left{padding-left:8px}
.tk-half.right{padding-right:8px}
.tk-divider{width:1px;background:rgba(255,255,255,.18);align-self:stretch;margin:0 4px}
.tk-half strong{display:block;font-size:22px;font-weight:800;color:#fff;letter-spacing:-.02em;line-height:1;position:relative;z-index:1}
.tk-half span{display:block;font-size:9.5px;font-weight:800;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.04em;margin-top:6px;position:relative;z-index:1}
.tk-half .td-wm{top:50%;transform:translateY(-50%);height:150px;width:150px}
.td-grid3{display:grid;grid-template-columns:repeat(3,1fr);padding-right:56px}
.td-grid3 .cell{text-align:center}
.td-grid3 .cell+.cell{border-left:1px solid rgba(255,255,255,.16)}
.td-grid3 strong{display:block;font-size:18px;font-weight:800;color:#fff;letter-spacing:-.02em}
.td-grid3 span{display:block;font-size:8.5px;font-weight:800;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.03em;margin-top:5px}
.td-rates{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:11px}
.td-rcard{border-radius:12px;padding:8px 6px;text-align:center;border:1.5px solid rgba(34,197,94,.4);background:rgba(34,197,94,.10)}
.td-rcard.o{border-color:rgba(245,158,11,.4);background:rgba(245,158,11,.10)}
.td-rcard.r{border-color:rgba(239,68,68,.4);background:rgba(239,68,68,.10)}
.td-rcard strong{display:block;font-size:16px;font-weight:800;color:#fff}
.td-rcard span{display:block;font-size:8.5px;font-weight:800;color:rgba(255,255,255,.85);text-transform:uppercase;letter-spacing:.04em;margin-top:2px}
.td-fin-divider{border-top:1px dashed rgba(255,255,255,.22);margin:0 0 11px}
.td-fin{display:grid;grid-template-columns:repeat(3,1fr);gap:6px}
.td-fin .cell{text-align:center}
.td-fin strong{display:block;font-size:15px;font-weight:800;letter-spacing:-.02em}
.td-fin strong.g{color:#4ADE80}
.td-fin strong.r{color:#F87171}
.td-fin span{display:block;font-size:8.5px;font-weight:800;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.03em;margin-top:4px}

/* ── Metas do dia ── */
.gbar{height:10px;background:#eef2f7;border-radius:999px;overflow:hidden}
.gbar i{display:block;height:100%;border-radius:999px}
.gbar.money i{background:linear-gradient(90deg,#8b5cf6,#0ea5e9)}
.gbar.km i{background:linear-gradient(90deg,#22C55E,#0EA5E9)}
.gbar.time i{background:linear-gradient(90deg,#F59E0B,#EF4444)}
.goal-item{display:flex;align-items:center;gap:10px;padding:10px 0}
.goal-item:first-child{padding-top:0}
.goal-item:last-child{padding-bottom:0}
.obj-goal-item{border-bottom:1px solid #edf1f6;padding-bottom:13px;margin-bottom:13px}
.obj-goal-item:last-child{border-bottom:0;padding-bottom:0;margin-bottom:0}
.g-ico{width:28px;height:28px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.g-ico svg{width:20px;height:20px}
.g-ico.money{color:#8b5cf6}
.g-ico.km{color:#0EA5E9}
.g-ico.time{color:#F59E0B}
.goal-body{flex:1;min-width:0}
.metas-wrap{position:relative;overflow:hidden;background:var(--card);border-radius:20px;padding:16px;box-shadow:0 4px 18px rgba(15,23,42,.07)}
.metas-wm{position:absolute;width:230px;height:230px;top:50%;left:-86px;transform:translateY(-50%);color:#3B82F6;opacity:.12;pointer-events:none;z-index:0}
.metas-wrap #metas-dia-stack{position:relative;z-index:1;padding-left:58px}
.goal-top{display:flex;justify-content:space-between;align-items:center;font-size:12.5px;font-weight:650;color:#344054;margin-bottom:8px}
.goal-top span:last-child{color:var(--muted);font-weight:600}
.goal-locked{display:flex;align-items:center;justify-content:space-between;background:#F8FAFC;border:1px dashed #D8E0EA;border-radius:14px;padding:10px 12px;cursor:pointer;touch-action:manipulation;flex:1;min-width:0}
.goal-locked span{font-size:12px;color:var(--muted);font-weight:600}
.goal-edit-link{display:block;width:100%;border:0;background:transparent;color:var(--muted);font-size:11.5px;font-weight:700;text-align:center;margin-top:12px;padding-top:12px;border-top:1px solid #edf1f6;cursor:pointer;touch-action:manipulation}
.goal-locked b{font-size:12px;color:#0A2F6B;font-weight:800}

/* ── Listas (sessões / corridas / lançamentos) ── */
.li-row{display:flex;gap:12px;align-items:center;background:#fff;border:1px solid var(--line);border-radius:20px;padding:12px;box-shadow:0 3px 14px rgba(15,23,42,.06)}
.li-row+.li-row{margin-top:9px}
.li-ico{width:38px;height:38px;border-radius:14px;display:flex;align-items:center;justify-content:center;flex:0 0 38px;overflow:hidden}
.li-ico svg{width:18px;height:18px;stroke-width:2;fill:none;stroke:currentColor}
.li-ico.green{background:#ECFDF3;color:var(--green2)}.li-ico.red{background:#FEF2F2;color:var(--red)}
.li-ico.plat-uber{background:#000}.li-ico.plat-uber img{width:19px}
.li-ico.plat-99{background:#F1F5F9}.li-ico.plat-99 img{width:26px;height:26px}
.li-ico.plat-generic{background:#F1F5F9;color:#64748B}
.li-info{flex:1;min-width:0}.li-info h3{font-size:13.5px;font-weight:760}.li-info p{font-size:11.5px;color:var(--muted);margin-top:2px}
.trip-title{font-size:12.5px!important;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.li-val{text-align:right;font-size:11px;color:var(--muted);font-weight:600;flex:0 0 auto}.li-val strong{display:block;color:var(--text);font-size:13.5px;font-weight:760;font-family:'Inter',system-ui,sans-serif}.li-time{display:block;color:var(--muted2);font-size:10.5px;font-weight:600;margin-top:2px}
.li-liq{display:block;color:#16A34A;font-size:11px;font-weight:800;margin-top:2px}
.li-extra{display:flex;gap:6px;margin-top:5px}.chip{display:inline-flex;align-items:center;gap:3px;font-size:10.5px;font-weight:800;padding:2.5px 7px;border-radius:8px;font-family:'Inter',system-ui,sans-serif;letter-spacing:-.01em}.chip svg{flex-shrink:0}.chip-d{background:#FFF7ED;color:#F59E0B}.chip-g{background:#F5F3FF;color:#7C3AED}.chip-taxa{background:#FEF9C3;color:#854D0E;display:inline-flex;align-items:center;font-size:10.5px;font-weight:800;padding:2.5px 7px;border-radius:8px}.chip-toll{background:#F1F5F9;color:#475569;display:inline-flex;align-items:center;justify-content:center;width:22px;height:20px;border-radius:7px;flex-shrink:0}.chip-icon{display:inline-flex;align-items:center;justify-content:center;width:22px;height:20px;border-radius:7px;flex-shrink:0}.chip-cash{background:#DCFCE7;color:#15803D}.chip-auto{background:#EFF6FF;color:#0EA5E9}.chip-import{background:#F5F3FF;color:#7C3AED}.auto-status{display:inline-block;font-size:9.5px;font-weight:800;padding:2.5px 7px;border-radius:99px;margin-top:4px;white-space:nowrap}.auto-status.pend{background:#FEF3C7;color:#92400E}.auto-status.estim{background:#EFF6FF;color:#0369A1}.auto-status.conf{background:#DCFCE7;color:#15803D}.auto-status.susp{background:#FEE2E2;color:#B91C1C}.trip-item-wrap{margin-bottom:9px}
.li-ico-col{display:flex;flex-direction:column;align-items:center;gap:4px;flex:0 0 38px}
.li-ico-time{font-size:10.5px;font-weight:800;color:var(--blue);white-space:nowrap;line-height:1.3}
/* corrida automática: card único (li-row de sempre), ícone+horário à
   esquerda, e título/km-tempo/tags empilhados juntos numa coluna central —
   pedido do Yuri (16/08/2026, mockup "Opção 1" aprovado): antes o card
   tinha 3 recuos diferentes (cabeçalho, categoria com margin-left:62px, e
   painel de detalhe sem recuo), o que fazia o card parecer "encolher" de
   largura ao abrir o detalhe. Agora é tudo uma coluna só, mesmo recuo. */
.li-row.trip-top{align-items:flex-start}
.trip-content-col{flex:1;min-width:0}
.trip-title-line{display:flex;align-items:flex-start;gap:6px}
.trip-subline{font-size:11px;color:var(--muted);margin:4px 0 0;font-weight:600}
.trip-top .li-extra{margin-top:5px}
.link-tag{width:15px;height:15px;flex-shrink:0;display:inline-flex;cursor:pointer}
.link-tag.on{color:#16A34A}
.link-tag.off{color:#CBD5E1}
.tap-legend-wrap{position:relative;display:inline-flex;cursor:pointer}
.tap-legend-tip{position:absolute;bottom:calc(100% + 7px);left:50%;transform:translateX(-50%);background:#0F172A;color:#fff;font-size:10.5px;font-weight:700;padding:6px 10px;border-radius:9px;white-space:nowrap;box-shadow:0 6px 16px rgba(0,0,0,.25);z-index:5;pointer-events:none}
.tap-legend-tip::after{content:'';position:absolute;top:100%;left:50%;transform:translateX(-50%);border:5px solid transparent;border-top-color:#0F172A}
.cmp{cursor:pointer}
.cmp.g{color:#16A34A}
.cmp.r{color:#DC2626}
.cmp-legend{background:#F8FAFC;border:1px solid var(--line);border-radius:12px;padding:9px 11px;margin:2px 0 8px;font-size:11px;color:#64748B;line-height:1.7}
.cmp-legend .row{display:flex;justify-content:space-between}
.cmp-legend .row b{color:#0F172A}
.cmp-legend.g{background:#F0FDF4;border-color:#BBF7D0}
.cmp-legend.r{background:#FEF2F2;border-color:#FECACA}
/* ── Rota de 3 pontos (Aceite/Embarque/Destino) — corridas automáticas ── */
.route-block{position:relative;padd
===== #F59E0B @ 37113 =====
ine-flex;align-items:center;gap:5px;font-size:11.5px;font-weight:800}
.td-chip.ok{color:#4ADE80}
.td-chip.no{color:#F87171}
.td-vline{width:1px;height:12px;background:rgba(255,255,255,.2)}
.td-grid1{text-align:center}
.td-grid1 strong{display:block;font-size:22px;font-weight:800;color:#fff;letter-spacing:-.02em}
.td-grid1 span{display:block;font-size:9.5px;font-weight:800;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.04em;margin-top:5px}
.tk-split{display:flex;align-items:stretch}
.tk-half{flex:1;position:relative;overflow:hidden;min-width:0;text-align:center;padding:2px 0}
.tk-half.left{padding-left:8px}
.tk-half.right{padding-right:8px}
.tk-divider{width:1px;background:rgba(255,255,255,.18);align-self:stretch;margin:0 4px}
.tk-half strong{display:block;font-size:22px;font-weight:800;color:#fff;letter-spacing:-.02em;line-height:1;position:relative;z-index:1}
.tk-half span{display:block;font-size:9.5px;font-weight:800;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.04em;margin-top:6px;position:relative;z-index:1}
.tk-half .td-wm{top:50%;transform:translateY(-50%);height:150px;width:150px}
.td-grid3{display:grid;grid-template-columns:repeat(3,1fr);padding-right:56px}
.td-grid3 .cell{text-align:center}
.td-grid3 .cell+.cell{border-left:1px solid rgba(255,255,255,.16)}
.td-grid3 strong{display:block;font-size:18px;font-weight:800;color:#fff;letter-spacing:-.02em}
.td-grid3 span{display:block;font-size:8.5px;font-weight:800;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.03em;margin-top:5px}
.td-rates{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:11px}
.td-rcard{border-radius:12px;padding:8px 6px;text-align:center;border:1.5px solid rgba(34,197,94,.4);background:rgba(34,197,94,.10)}
.td-rcard.o{border-color:rgba(245,158,11,.4);background:rgba(245,158,11,.10)}
.td-rcard.r{border-color:rgba(239,68,68,.4);background:rgba(239,68,68,.10)}
.td-rcard strong{display:block;font-size:16px;font-weight:800;color:#fff}
.td-rcard span{display:block;font-size:8.5px;font-weight:800;color:rgba(255,255,255,.85);text-transform:uppercase;letter-spacing:.04em;margin-top:2px}
.td-fin-divider{border-top:1px dashed rgba(255,255,255,.22);margin:0 0 11px}
.td-fin{display:grid;grid-template-columns:repeat(3,1fr);gap:6px}
.td-fin .cell{text-align:center}
.td-fin strong{display:block;font-size:15px;font-weight:800;letter-spacing:-.02em}
.td-fin strong.g{color:#4ADE80}
.td-fin strong.r{color:#F87171}
.td-fin span{display:block;font-size:8.5px;font-weight:800;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.03em;margin-top:4px}

/* ── Metas do dia ── */
.gbar{height:10px;background:#eef2f7;border-radius:999px;overflow:hidden}
.gbar i{display:block;height:100%;border-radius:999px}
.gbar.money i{background:linear-gradient(90deg,#8b5cf6,#0ea5e9)}
.gbar.km i{background:linear-gradient(90deg,#22C55E,#0EA5E9)}
.gbar.time i{background:linear-gradient(90deg,#F59E0B,#EF4444)}
.goal-item{display:flex;align-items:center;gap:10px;padding:10px 0}
.goal-item:first-child{padding-top:0}
.goal-item:last-child{padding-bottom:0}
.obj-goal-item{border-bottom:1px solid #edf1f6;padding-bottom:13px;margin-bottom:13px}
.obj-goal-item:last-child{border-bottom:0;padding-bottom:0;margin-bottom:0}
.g-ico{width:28px;height:28px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.g-ico svg{width:20px;height:20px}
.g-ico.money{color:#8b5cf6}
.g-ico.km{color:#0EA5E9}
.g-ico.time{color:#F59E0B}
.goal-body{flex:1;min-width:0}
.metas-wrap{position:relative;overflow:hidden;background:var(--card);border-radius:20px;padding:16px;box-shadow:0 4px 18px rgba(15,23,42,.07)}
.metas-wm{position:absolute;width:230px;height:230px;top:50%;left:-86px;transform:translateY(-50%);color:#3B82F6;opacity:.12;pointer-events:none;z-index:0}
.metas-wrap #metas-dia-stack{position:relative;z-index:1;padding-left:58px}
.goal-top{display:flex;justify-content:space-between;align-items:center;font-size:12.5px;font-weight:650;color:#344054;margin-bottom:8px}
.goal-top span:last-child{color:var(--muted);font-weight:600}
.goal-locked{display:flex;align-items:center;justify-content:space-between;background:#F8FAFC;border:1px dashed #D8E0EA;border-radius:14px;padding:10px 12px;cursor:pointer;touch-action:manipulation;flex:1;min-width:0}
.goal-locked span{font-size:12px;color:var(--muted);font-weight:600}
.goal-edit-link{display:block;width:100%;border:0;background:transparent;color:var(--muted);font-size:11.5px;font-weight:700;text-align:center;margin-top:12px;padding-top:12px;border-top:1px solid #edf1f6;cursor:pointer;touch-action:manipulation}
.goal-locked b{font-size:12px;color:#0A2F6B;font-weight:800}

/* ── Listas (sessões / corridas / lançamentos) ── */
.li-row{display:flex;gap:12px;align-items:center;background:#fff;border:1px solid var(--line);border-radius:20px;padding:12px;box-shadow:0 3px 14px rgba(15,23,42,.06)}
.li-row+.li-row{margin-top:9px}
.li-ico{width:38px;height:38px;border-radius:14px;display:flex;align-items:center;justify-content:center;flex:0 0 38px;overflow:hidden}
.li-ico svg{width:18px;height:18px;stroke-width:2;fill:none;stroke:currentColor}
.li-ico.green{background:#ECFDF3;color:var(--green2)}.li-ico.red{background:#FEF2F2;color:var(--red)}
.li-ico.plat-uber{background:#000}.li-ico.plat-uber img{width:19px}
.li-ico.plat-99{background:#F1F5F9}.li-ico.plat-99 img{width:26px;height:26px}
.li-ico.plat-generic{background:#F1F5F9;color:#64748B}
.li-info{flex:1;min-width:0}.li-info h3{font-size:13.5px;font-weight:760}.li-info p{font-size:11.5px;color:var(--muted);margin-top:2px}
.trip-title{font-size:12.5px!important;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.li-val{text-align:right;font-size:11px;color:var(--muted);font-weight:600;flex:0 0 auto}.li-val strong{display:block;color:var(--text);font-size:13.5px;font-weight:760;font-family:'Inter',system-ui,sans-serif}.li-time{display:block;color:var(--muted2);font-size:10.5px;font-weight:600;margin-top:2px}
.li-liq{display:block;color:#16A34A;font-size:11px;font-weight:800;margin-top:2px}
.li-extra{display:flex;gap:6px;margin-top:5px}.chip{display:inline-flex;align-items:center;gap:3px;font-size:10.5px;font-weight:800;padding:2.5px 7px;border-radius:8px;font-family:'Inter',system-ui,sans-serif;letter-spacing:-.01em}.chip svg{flex-shrink:0}.chip-d{background:#FFF7ED;color:#F59E0B}.chip-g{background:#F5F3FF;color:#7C3AED}.chip-taxa{background:#FEF9C3;color:#854D0E;display:inline-flex;align-items:center;font-size:10.5px;font-weight:800;padding:2.5px 7px;border-radius:8px}.chip-toll{background:#F1F5F9;color:#475569;display:inline-flex;align-items:center;justify-content:center;width:22px;height:20px;border-radius:7px;flex-shrink:0}.chip-icon{display:inline-flex;align-items:center;justify-content:center;width:22px;height:20px;border-radius:7px;flex-shrink:0}.chip-cash{background:#DCFCE7;color:#15803D}.chip-auto{background:#EFF6FF;color:#0EA5E9}.chip-import{background:#F5F3FF;color:#7C3AED}.auto-status{display:inline-block;font-size:9.5px;font-weight:800;padding:2.5px 7px;border-radius:99px;margin-top:4px;white-space:nowrap}.auto-status.pend{background:#FEF3C7;color:#92400E}.auto-status.estim{background:#EFF6FF;color:#0369A1}.auto-status.conf{background:#DCFCE7;color:#15803D}.auto-status.susp{background:#FEE2E2;color:#B91C1C}.trip-item-wrap{margin-bottom:9px}
.li-ico-col{display:flex;flex-direction:column;align-items:center;gap:4px;flex:0 0 38px}
.li-ico-time{font-size:10.5px;font-weight:800;color:var(--blue);white-space:nowrap;line-height:1.3}
/* corrida automática: card único (li-row de sempre), ícone+horário à
   esquerda, e título/km-tempo/tags empilhados juntos numa coluna central —
   pedido do Yuri (16/08/2026, mockup "Opção 1" aprovado): antes o card
   tinha 3 recuos diferentes (cabeçalho, categoria com margin-left:62px, e
   painel de detalhe sem recuo), o que fazia o card parecer "encolher" de
   largura ao abrir o detalhe. Agora é tudo uma coluna só, mesmo recuo. */
.li-row.trip-top{align-items:flex-start}
.trip-content-col{flex:1;min-width:0}
.trip-title-line{display:flex;align-items:flex-start;gap:6px}
.trip-subline{font-size:11px;color:var(--muted);margin:4px 0 0;font-weight:600}
.trip-top .li-extra{margin-top:5px}
.link-tag{width:15px;height:15px;flex-shrink:0;display:inline-flex;cursor:pointer}
.link-tag.on{color:#16A34A}
.link-tag.off{color:#CBD5E1}
.tap-legend-wrap{position:relative;display:inline-flex;cursor:pointer}
.tap-legend-tip{position:absolute;bottom:calc(100% + 7px);left:50%;transform:translateX(-50%);background:#0F172A;color:#fff;font-size:10.5px;font-weight:700;padding:6px 10px;border-radius:9px;white-space:nowrap;box-shadow:0 6px 16px rgba(0,0,0,.25);z-index:5;pointer-events:none}
.tap-legend-tip::after{content:'';position:absolute;top:100%;left:50%;transform:translateX(-50%);border:5px solid transparent;border-top-color:#0F172A}
.cmp{cursor:pointer}
.cmp.g{color:#16A34A}
.cmp.r{color:#DC2626}
.cmp-legend{background:#F8FAFC;border:1px solid var(--line);border-radius:12px;padding:9px 11px;margin:2px 0 8px;font-size:11px;color:#64748B;line-height:1.7}
.cmp-legend .row{display:flex;justify-content:space-between}
.cmp-legend .row b{color:#0F172A}
.cmp-legend.g{background:#F0FDF4;border-color:#BBF7D0}
.cmp-legend.r{background:#FEF2F2;border-color:#FECACA}
/* ── Rota de 3 pontos (Aceite/Embarque/Destino) — corridas automáticas ── */
.route-block{position:relative;padding-left:26px;margin:6px 0 2px}
.route-line{position:absolute;left:10px;top:8px;width:2px;background:var(--line)}
.route-row{position:relative;display:flex;align-items:flex-start;padding:8px 0}
.route-row:first-child{padding-top:0}
.route-row:last-child{padding-bottom:0}
.route-mark{position:absolute;left:-26px;top:0;display:flex;align-items:flex-start;justify-content:center;width:22px}
.mk-accept{width:12px;height:12px;border-radius:50%;background:#fff;border:2.5px solid #22C55E;margin-top:1px}
.mk-pickup{width:11px;height:11p
===== #F59E0B @ 40006 =====
ent(90deg,#22C55E,#0EA5E9)}
.gbar.time i{background:linear-gradient(90deg,#F59E0B,#EF4444)}
.goal-item{display:flex;align-items:center;gap:10px;padding:10px 0}
.goal-item:first-child{padding-top:0}
.goal-item:last-child{padding-bottom:0}
.obj-goal-item{border-bottom:1px solid #edf1f6;padding-bottom:13px;margin-bottom:13px}
.obj-goal-item:last-child{border-bottom:0;padding-bottom:0;margin-bottom:0}
.g-ico{width:28px;height:28px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.g-ico svg{width:20px;height:20px}
.g-ico.money{color:#8b5cf6}
.g-ico.km{color:#0EA5E9}
.g-ico.time{color:#F59E0B}
.goal-body{flex:1;min-width:0}
.metas-wrap{position:relative;overflow:hidden;background:var(--card);border-radius:20px;padding:16px;box-shadow:0 4px 18px rgba(15,23,42,.07)}
.metas-wm{position:absolute;width:230px;height:230px;top:50%;left:-86px;transform:translateY(-50%);color:#3B82F6;opacity:.12;pointer-events:none;z-index:0}
.metas-wrap #metas-dia-stack{position:relative;z-index:1;padding-left:58px}
.goal-top{display:flex;justify-content:space-between;align-items:center;font-size:12.5px;font-weight:650;color:#344054;margin-bottom:8px}
.goal-top span:last-child{color:var(--muted);font-weight:600}
.goal-locked{display:flex;align-items:center;justify-content:space-between;background:#F8FAFC;border:1px dashed #D8E0EA;border-radius:14px;padding:10px 12px;cursor:pointer;touch-action:manipulation;flex:1;min-width:0}
.goal-locked span{font-size:12px;color:var(--muted);font-weight:600}
.goal-edit-link{display:block;width:100%;border:0;background:transparent;color:var(--muted);font-size:11.5px;font-weight:700;text-align:center;margin-top:12px;padding-top:12px;border-top:1px solid #edf1f6;cursor:pointer;touch-action:manipulation}
.goal-locked b{font-size:12px;color:#0A2F6B;font-weight:800}

/* ── Listas (sessões / corridas / lançamentos) ── */
.li-row{display:flex;gap:12px;align-items:center;background:#fff;border:1px solid var(--line);border-radius:20px;padding:12px;box-shadow:0 3px 14px rgba(15,23,42,.06)}
.li-row+.li-row{margin-top:9px}
.li-ico{width:38px;height:38px;border-radius:14px;display:flex;align-items:center;justify-content:center;flex:0 0 38px;overflow:hidden}
.li-ico svg{width:18px;height:18px;stroke-width:2;fill:none;stroke:currentColor}
.li-ico.green{background:#ECFDF3;color:var(--green2)}.li-ico.red{background:#FEF2F2;color:var(--red)}
.li-ico.plat-uber{background:#000}.li-ico.plat-uber img{width:19px}
.li-ico.plat-99{background:#F1F5F9}.li-ico.plat-99 img{width:26px;height:26px}
.li-ico.plat-generic{background:#F1F5F9;color:#64748B}
.li-info{flex:1;min-width:0}.li-info h3{font-size:13.5px;font-weight:760}.li-info p{font-size:11.5px;color:var(--muted);margin-top:2px}
.trip-title{font-size:12.5px!important;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.li-val{text-align:right;font-size:11px;color:var(--muted);font-weight:600;flex:0 0 auto}.li-val strong{display:block;color:var(--text);font-size:13.5px;font-weight:760;font-family:'Inter',system-ui,sans-serif}.li-time{display:block;color:var(--muted2);font-size:10.5px;font-weight:600;margin-top:2px}
.li-liq{display:block;color:#16A34A;font-size:11px;font-weight:800;margin-top:2px}
.li-extra{display:flex;gap:6px;margin-top:5px}.chip{display:inline-flex;align-items:center;gap:3px;font-size:10.5px;font-weight:800;padding:2.5px 7px;border-radius:8px;font-family:'Inter',system-ui,sans-serif;letter-spacing:-.01em}.chip svg{flex-shrink:0}.chip-d{background:#FFF7ED;color:#F59E0B}.chip-g{background:#F5F3FF;color:#7C3AED}.chip-taxa{background:#FEF9C3;color:#854D0E;display:inline-flex;align-items:center;font-size:10.5px;font-weight:800;padding:2.5px 7px;border-radius:8px}.chip-toll{background:#F1F5F9;color:#475569;display:inline-flex;align-items:center;justify-content:center;width:22px;height:20px;border-radius:7px;flex-shrink:0}.chip-icon{display:inline-flex;align-items:center;justify-content:center;width:22px;height:20px;border-radius:7px;flex-shrink:0}.chip-cash{background:#DCFCE7;color:#15803D}.chip-auto{background:#EFF6FF;color:#0EA5E9}.chip-import{background:#F5F3FF;color:#7C3AED}.auto-status{display:inline-block;font-size:9.5px;font-weight:800;padding:2.5px 7px;border-radius:99px;margin-top:4px;white-space:nowrap}.auto-status.pend{background:#FEF3C7;color:#92400E}.auto-status.estim{background:#EFF6FF;color:#0369A1}.auto-status.conf{background:#DCFCE7;color:#15803D}.auto-status.susp{background:#FEE2E2;color:#B91C1C}.trip-item-wrap{margin-bottom:9px}
.li-ico-col{display:flex;flex-direction:column;align-items:center;gap:4px;flex:0 0 38px}
.li-ico-time{font-size:10.5px;font-weight:800;color:var(--blue);white-space:nowrap;line-height:1.3}
/* corrida automática: card único (li-row de sempre), ícone+horário à
   esquerda, e título/km-tempo/tags empilhados juntos numa coluna central —
   pedido do Yuri (16/08/2026, mockup "Opção 1" aprovado): antes o card
   tinha 3 recuos diferentes (cabeçalho, categoria com margin-left:62px, e
   painel de detalhe sem recuo), o que fazia o card parecer "encolher" de
   largura ao abrir o detalhe. Agora é tudo uma coluna só, mesmo recuo. */
.li-row.trip-top{align-items:flex-start}
.trip-content-col{flex:1;min-width:0}
.trip-title-line{display:flex;align-items:flex-start;gap:6px}
.trip-subline{font-size:11px;color:var(--muted);margin:4px 0 0;font-weight:600}
.trip-top .li-extra{margin-top:5px}
.link-tag{width:15px;height:15px;flex-shrink:0;display:inline-flex;cursor:pointer}
.link-tag.on{color:#16A34A}
.link-tag.off{color:#CBD5E1}
.tap-legend-wrap{position:relative;display:inline-flex;cursor:pointer}
.tap-legend-tip{position:absolute;bottom:calc(100% + 7px);left:50%;transform:translateX(-50%);background:#0F172A;color:#fff;font-size:10.5px;font-weight:700;padding:6px 10px;border-radius:9px;white-space:nowrap;box-shadow:0 6px 16px rgba(0,0,0,.25);z-index:5;pointer-events:none}
.tap-legend-tip::after{content:'';position:absolute;top:100%;left:50%;transform:translateX(-50%);border:5px solid transparent;border-top-color:#0F172A}
.cmp{cursor:pointer}
.cmp.g{color:#16A34A}
.cmp.r{color:#DC2626}
.cmp-legend{background:#F8FAFC;border:1px solid var(--line);border-radius:12px;padding:9px 11px;margin:2px 0 8px;font-size:11px;color:#64748B;line-height:1.7}
.cmp-legend .row{display:flex;justify-content:space-between}
.cmp-legend .row b{color:#0F172A}
.cmp-legend.g{background:#F0FDF4;border-color:#BBF7D0}
.cmp-legend.r{background:#FEF2F2;border-color:#FECACA}
/* ── Rota de 3 pontos (Aceite/Embarque/Destino) — corridas automáticas ── */
.route-block{position:relative;padding-left:26px;margin:6px 0 2px}
.route-line{position:absolute;left:10px;top:8px;width:2px;background:var(--line)}
.route-row{position:relative;display:flex;align-items:flex-start;padding:8px 0}
.route-row:first-child{padding-top:0}
.route-row:last-child{padding-bottom:0}
.route-mark{position:absolute;left:-26px;top:0;display:flex;align-items:flex-start;justify-content:center;width:22px}
.mk-accept{width:12px;height:12px;border-radius:50%;background:#fff;border:2.5px solid #22C55E;margin-top:1px}
.mk-pickup{width:11px;height:11px;border-radius:50%;background:#22C55E;border:2px solid #fff;box-shadow:0 0 0 1.5px #22C55E;margin-top:1px}
.mk-dest{width:22px;height:22px;color:#22C55E;display:block;margin-top:-3px}
.route-body{flex:1;min-width:0;display:flex;justify-content:space-between;align-items:flex-start;gap:8px}
.route-label{font-size:9px;font-weight:800;color:var(--muted2);text-transform:uppercase;letter-spacing:.05em;margin-bottom:2px}
.route-addr{font-size:11.5px;font-weight:700;color:var(--text);line-height:1.4}
.route-time{font-size:10.5px;font-weight:700;color:var(--muted);white-space:nowrap;flex-shrink:0;padding-top:1px}
.route-block .cmp-legend{margin:5px 0 0 22px}
/* ── Corridas: toggle Importadas / Automáticas (fonte dos dados) ── */
.cor-subnote{padding:0 16px 10px;font-size:11.5px;color:var(--muted2);line-height:1.4}.li-row.open{border-radius:14px 14px 0 0;border-bottom:0!important}.trip-detail-panel{background:var(--card);border:1.5px solid var(--line);border-top:0;border-radius:0 0 14px 14px;padding:8px 12px 10px}.det-row{display:flex;justify-content:space-between;align-items:center;font-size:12px;color:var(--muted);padding:4px 0;font-weight:500}.det-row:not(:last-child){border-bottom:1px solid var(--line)}.det-row strong{color:var(--text);font-weight:700}.det-del-btn{display:flex;align-items:center;gap:6px;width:100%;margin-top:8px;padding:8px 0 2px;background:none;border:none;border-top:1px solid var(--line);font-size:12px;font-weight:700;color:#EF4444;cursor:pointer;font-family:inherit}
.perf-strip{display:grid;gap:7px;margin-bottom:10px}
.perf-cell{border-radius:13px;padding:9px 6px;text-align:center;background:#F0FDF4;border:1px solid #BBF7D0}
.perf-cell.o{background:#FFF7ED;border-color:#FED7AA}
.perf-cell.r{background:#FEF2F2;border-color:#FECACA}
.perf-cell strong{display:block;font-size:13.5px;font-weight:800;color:var(--text)}
.perf-cell span{display:block;font-size:8.5px;font-weight:800;text-transform:uppercase;letter-spacing:.04em;color:#15803D;margin-top:3px}
.perf-cell.o span{color:#C2410C}
.perf-cell.r span{color:#B91C1C}
.cfg-note{display:flex;align-items:center;gap:8px;background:#F8FAFC;border:1px solid var(--line);border-radius:12px;padding:9px 11px;font-size:11px;color:var(--muted);margin-bottom:10px}
.cfg-note a{color:#0A2F6B;font-weight:700;text-decoration:none;cursor:pointer}
.decl-toggle{display:flex;align-items:center;gap:8px;padding:12px 16px;background:#fff;border-bottom:1px solid var(--line);cursor:pointer}
.decl-toggle svg.chev{margin-left:auto;transition:transform .2s}
.decl-toggle.open svg.chev{transform:rotate(180deg)}
.decl-icon{width:30px;height:30px;border-radius:9px;background:#FEF2F2;color:#DC2626;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.decl-toggle h3{font-size:13px;margin:0;color:var(--text)}
.decl-toggle p{font-size:11px;color:var(--muted);margin:1px 0 0}
.decl-body{background:#fff
===== #F59E0B @ 52662 =====
4px}
.acc-row{display:flex;align-items:center;justify-content:space-between;gap:10px}
.acc-email{font-size:13.5px;font-weight:700;color:var(--text);word-break:break-all}
.acc-plan-lbl{font-size:10.5px;color:#94A3B8;font-weight:700;text-transform:uppercase;letter-spacing:.04em;margin-top:3px}
.acc-badge{font-size:11px;font-weight:800;padding:5px 10px;border-radius:999px;background:#EFF6FF;color:#0EA5E9;flex-shrink:0}
.acc-badge.pro{background:#ECFDF3;color:#16A34A}
.acc-exp{font-size:11.5px;color:#64748B;margin-top:8px}
.acc-upgrade{width:100%;height:44px;border:0;border-radius:14px;background:linear-gradient(135deg,#0EA5E9,#0284C7);color:#fff;font-size:13.5px;font-weight:800;cursor:pointer;margin-top:10px;touch-action:manipulation;font-family:var(--font-app)}
/* ── Perfil: avatar, stats, action sheet de foto ── */
.av-sec{display:flex;flex-direction:column;align-items:center;padding:8px 0 18px}
.av-wrap{position:relative;width:88px;height:88px;cursor:pointer}
.av-photo{width:88px;height:88px;border-radius:99px;object-fit:cover;display:block;border:3px solid #fff;box-shadow:0 0 0 1px var(--line)}
.av-initials{width:88px;height:88px;border-radius:99px;display:flex;align-items:center;justify-content:center;font-family:var(--font-brand);font-weight:800;font-size:28px;color:#fff;background:linear-gradient(135deg,#0A2F6B,#123B85);border:3px solid #fff;box-shadow:0 0 0 1px var(--line)}
.av-cam{position:absolute;right:-2px;bottom:-2px;width:30px;height:30px;border-radius:99px;background:var(--green);border:3px solid #fff;display:flex;align-items:center;justify-content:center;pointer-events:none}
.av-cam svg{stroke:#fff}
.av-name{font-family:var(--font-brand);font-weight:800;font-size:18px;margin-top:11px;letter-spacing:-.02em;color:var(--text)}
.av-sub{font-size:12px;color:#64748B;font-weight:500;margin-top:3px;display:flex;align-items:center;gap:5px;text-align:center}
.av-sub svg{stroke:#94A3B8;flex-shrink:0}
.p-stats{display:flex;gap:8px;margin-bottom:16px}
.p-stat{flex:1;background:#F8FAFC;border:1px solid var(--line);border-radius:14px;padding:10px 8px;text-align:center}
.p-stat .v{font-family:var(--font-brand);font-weight:800;font-size:16px;color:var(--blue)}
.p-stat .l{font-size:9.5px;font-weight:600;color:#64748B;text-transform:uppercase;letter-spacing:.04em;margin-top:2px}
.p-sec-lbl{font-size:11px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.06em;margin:4px 0 10px}
/* Action sheet de foto — sheet pequeno, sem handle/head, opções em lista */
#mo-perfil-foto .sheet{padding:8px 0 0}
.pf-opt{display:flex;align-items:center;gap:13px;padding:15px 20px;font-size:14.5px;font-weight:700;color:var(--text);cursor:pointer;border-bottom:1px solid #F1F5F9}
.pf-opt svg{stroke:var(--blue)}
.pf-opt.danger{color:#EF4444}
.pf-opt.danger svg{stroke:#EF4444}
.pf-cancel{display:block;width:calc(100% - 32px);margin:10px 16px 18px;height:48px;background:#F1F5F9;border:none;border-radius:16px;color:#64748B;font-size:14px;font-weight:700;cursor:pointer;font-family:var(--font-app)}
.veiculo-btn{width:100%;height:50px;border:1.5px solid var(--line);background:#F8FAFC;border-radius:16px;padding:0 14px;display:flex;align-items:center;justify-content:space-between;font-size:14.5px;font-weight:700;color:var(--text);cursor:pointer;touch-action:manipulation;font-family:var(--font-app)}
.veiculo-btn svg{color:#94A3B8;flex-shrink:0}
.sync-dot{width:10px;height:10px;border-radius:50%;background:#22C55E;flex-shrink:0}
.sync-dot.pending{background:#F59E0B}
.action{background:#fff;border:1px solid rgba(14,165,233,.18);box-shadow:0 3px 14px rgba(15,23,42,.06);border-radius:22px;padding:15px 16px;margin-bottom:12px;display:flex;align-items:center;gap:12px;cursor:pointer;touch-action:manipulation}.action .glyph{width:42px;height:42px;border-radius:16px;background:rgba(14,165,233,.1);display:flex;align-items:center;justify-content:center;color:#0A2F6B}.action h3{font-size:14px;font-weight:700}.action p{font-size:12px;color:var(--muted);margin-top:3px}.chev{margin-left:auto;color:#94A3B8;font-size:22px}
.kmcard{background:#fff;border:1px solid var(--line);border-radius:24px;padding:16px;box-shadow:0 3px 14px rgba(15,23,42,.06)}.cardhead{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}.cardhead h2{font-size:15px;letter-spacing:-.02em}.linkbtn{border:0;background:rgba(14,165,233,.1);color:#0A2F6B;border-radius:999px;padding:7px 12px;font-size:12px;font-weight:800;cursor:pointer;touch-action:manipulation}.row{display:flex;align-items:center;justify-content:space-between;padding:9px 0;border-bottom:1px solid var(--line)}.row:last-child{border-bottom:0}.row span{font-size:12px;color:var(--muted);font-weight:600}.row strong{font-family:'Inter',system-ui,sans-serif;font-size:13px}.bar{height:10px;background:#E2E8F0;border-radius:999px;overflow:hidden;margin-top:12px}.fill{height:100%;background:linear-gradient(90deg,var(--green),var(--blue));border-radius:999px;transition:width .5s}.note{font-size:11px;color:var(--muted);text-align:center;margin-top:8px;font-weight:600}
.section{display:flex;align-items:center;justify-content:space-between}.section h2{font-size:16px;font-weight:700}.add{border:0;background:rgba(14,165,233,.1);color:#0A2F6B;font-weight:800;border-radius:999px;padding:7px 12px;font-size:12px;cursor:pointer;touch-action:manipulation}.list{background:#fff;border:1px solid var(--line);border-radius:22px;box-shadow:0 3px 14px rgba(15,23,42,.06);overflow:hidden}.tag{width:38px;height:38px;border-radius:14px;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:900;flex-shrink:0}
/* ── Lançamentos form extras ── */
.type-switch,.scope-switch{display:flex;background:#EAF0F6;border:1px solid rgba(15,23,42,.06);padding:4px;border-radius:18px}.type-btn,.scope-btn{flex:1;border:none;background:transparent;color:var(--muted);padding:11px 8px;border-radius:14px;font-size:14px;font-weight:900;cursor:pointer;touch-action:manipulation}.type-btn.active,.scope-btn.active{background:#fff;color:var(--text);box-shadow:0 4px 12px rgba(15,23,42,.08)}
.pchip{flex:1;border:1.5px solid var(--line);background:#fff;color:var(--muted);font-size:11.5px;font-weight:700;padding:7px 4px;border-radius:999px;cursor:pointer;touch-action:manipulation;font-family:var(--font-app);text-align:center;white-space:nowrap}.pchip.active{background:var(--text);color:#fff;border-color:var(--text)}
.lanc-total-card{display:flex;justify-content:space-between;align-items:center;background:linear-gradient(145deg,#0A2F6B 0%,#0D3E8C 60%,#0A2F6B 100%);border-radius:16px;padding:14px 16px;color:#fff;margin-bottom:12px}
.lanc-total-card .l{font-size:10px;font-weight:700;color:rgba(255,255,255,.55);text-transform:uppercase;letter-spacing:.06em}
.lanc-total-card .v{font-family:'Poppins',sans-serif;font-weight:800;font-size:20px;margin-top:2px}
.lanc-total-card .side{text-align:right}
.lanc-total-card .v2{font-size:12px;font-weight:700;margin-top:2px;color:rgba(255,255,255,.75)}
.lanc-status-row,.lanc-period-row{display:flex;gap:6px;margin-bottom:8px}
.del-btn{width:30px;height:30px;border-radius:10px;background:#FEF2F2;color:#EF4444;border:0;display:flex;align-items:center;justify-content:center;cursor:pointer;flex-shrink:0;touch-action:manipulation}
.day-sep{font-size:11px;font-weight:800;color:var(--muted);text-transform:uppercase;letter-spacing:.04em;margin:16px 4px 8px}.day-sep:first-child{margin-top:0}
.amount-card{border-radius:30px;padding:24px 20px 22px;color:#fff;box-shadow:0 10px 26px rgba(15,23,42,.07);margin-bottom:14px;cursor:text}.amount-card.receita{background:linear-gradient(135deg,#16A34A,#22C55E)}.amount-card.despesa{background:linear-gradient(135deg,#DC2626,#EF4444)}.amount-label{font-size:12px;text-transform:uppercase;letter-spacing:.12em;font-weight:900;opacity:.78;margin-bottom:8px}.amount-input-wrap{display:flex;align-items:flex-end;gap:8px}.currency{font-size:24px;font-weight:900;opacity:.9;padding-bottom:7px}.amount-input{width:100%;border:none;outline:none;background:transparent;color:#fff;font-size:52px;line-height:1;font-weight:900;letter-spacing:-.06em;font-family:var(--font-app)}.amount-input::placeholder{color:rgba(255,255,255,.55)}
.cat-wrap{margin:0 -16px;padding-left:16px;overflow-x:auto;-webkit-overflow-scrolling:touch;scrollbar-width:none}.cat-wrap::-webkit-scrollbar{display:none}.cat-rail{display:grid;grid-auto-flow:column;grid-template-rows:repeat(2,76px);grid-auto-columns:82px;gap:8px;padding-right:16px}.cat{border:1.5px solid var(--line);background:#fff;border-radius:18px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:7px;color:#334155;cursor:pointer;touch-action:manipulation}.cat.active{border-color:#0A2F6B;background:rgba(14,165,233,.08);color:#0A2F6B}.cat svg{width:23px;height:23px}.cat span{font-size:10px;font-weight:900;line-height:1;text-align:center}
.recurring{background:#fff;border:1px solid rgba(15,23,42,.07);border-radius:24px;box-shadow:0 3px 14px rgba(15,23,42,.06);overflow:hidden}.rec-row{display:flex;align-items:center;justify-content:space-between;padding:15px 16px}.rec-title{font-size:14px;font-weight:900}.rec-sub{font-size:12px;color:var(--muted);margin-top:3px}.toggle{width:50px;height:30px;border:none;border-radius:999px;background:#CBD5E1;position:relative;cursor:pointer;transition:.2s}.toggle::after{content:"";position:absolute;width:24px;height:24px;border-radius:50%;background:#fff;top:3px;left:3px;box-shadow:0 2px 6px rgba(15,23,42,.2);transition:.2s}.toggle.on{background:#0A2F6B}.toggle.on::after{left:23px}.freq{display:none;gap:8px;padding:0 16px 16px}.freq.show{display:flex}.freq button{flex:1;height:40px;border-radius:14px;border:1.5px solid var(--line);background:#F8FAFC;color:var(--muted);font-size:12px;font-weight:900;cursor:pointer;touch-action:manipulation}.freq button.active{background:rgba(14,165,233,.10);border-color:#0A2F6B;color:#0A2F6B}.save{width:100%;height:56px;border:none;border-radius:20px;color:#fff
===== #F59E0B @ 59831 =====
;display:flex;align-items:center;justify-content:center;cursor:pointer;flex-shrink:0;touch-action:manipulation}
.day-sep{font-size:11px;font-weight:800;color:var(--muted);text-transform:uppercase;letter-spacing:.04em;margin:16px 4px 8px}.day-sep:first-child{margin-top:0}
.amount-card{border-radius:30px;padding:24px 20px 22px;color:#fff;box-shadow:0 10px 26px rgba(15,23,42,.07);margin-bottom:14px;cursor:text}.amount-card.receita{background:linear-gradient(135deg,#16A34A,#22C55E)}.amount-card.despesa{background:linear-gradient(135deg,#DC2626,#EF4444)}.amount-label{font-size:12px;text-transform:uppercase;letter-spacing:.12em;font-weight:900;opacity:.78;margin-bottom:8px}.amount-input-wrap{display:flex;align-items:flex-end;gap:8px}.currency{font-size:24px;font-weight:900;opacity:.9;padding-bottom:7px}.amount-input{width:100%;border:none;outline:none;background:transparent;color:#fff;font-size:52px;line-height:1;font-weight:900;letter-spacing:-.06em;font-family:var(--font-app)}.amount-input::placeholder{color:rgba(255,255,255,.55)}
.cat-wrap{margin:0 -16px;padding-left:16px;overflow-x:auto;-webkit-overflow-scrolling:touch;scrollbar-width:none}.cat-wrap::-webkit-scrollbar{display:none}.cat-rail{display:grid;grid-auto-flow:column;grid-template-rows:repeat(2,76px);grid-auto-columns:82px;gap:8px;padding-right:16px}.cat{border:1.5px solid var(--line);background:#fff;border-radius:18px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:7px;color:#334155;cursor:pointer;touch-action:manipulation}.cat.active{border-color:#0A2F6B;background:rgba(14,165,233,.08);color:#0A2F6B}.cat svg{width:23px;height:23px}.cat span{font-size:10px;font-weight:900;line-height:1;text-align:center}
.recurring{background:#fff;border:1px solid rgba(15,23,42,.07);border-radius:24px;box-shadow:0 3px 14px rgba(15,23,42,.06);overflow:hidden}.rec-row{display:flex;align-items:center;justify-content:space-between;padding:15px 16px}.rec-title{font-size:14px;font-weight:900}.rec-sub{font-size:12px;color:var(--muted);margin-top:3px}.toggle{width:50px;height:30px;border:none;border-radius:999px;background:#CBD5E1;position:relative;cursor:pointer;transition:.2s}.toggle::after{content:"";position:absolute;width:24px;height:24px;border-radius:50%;background:#fff;top:3px;left:3px;box-shadow:0 2px 6px rgba(15,23,42,.2);transition:.2s}.toggle.on{background:#0A2F6B}.toggle.on::after{left:23px}.freq{display:none;gap:8px;padding:0 16px 16px}.freq.show{display:flex}.freq button{flex:1;height:40px;border-radius:14px;border:1.5px solid var(--line);background:#F8FAFC;color:var(--muted);font-size:12px;font-weight:900;cursor:pointer;touch-action:manipulation}.freq button.active{background:rgba(14,165,233,.10);border-color:#0A2F6B;color:#0A2F6B}.save{width:100%;height:56px;border:none;border-radius:20px;color:#fff;font-size:16px;font-weight:900;box-shadow:0 12px 26px rgba(15,23,42,.12);cursor:pointer;touch-action:manipulation;font-family:var(--font-app)}.save.receita{background:var(--green)}.save.despesa{background:var(--red)}
/* ── Mais screen ── */
.mais-item{background:#fff;border:1px solid var(--line);border-radius:20px;box-shadow:0 2px 10px rgba(15,23,42,.05);padding:14px 16px;display:flex;align-items:center;gap:12px;cursor:pointer;touch-action:manipulation}.mais-ico{width:42px;height:42px;border-radius:14px;display:flex;align-items:center;justify-content:center;flex-shrink:0}.mais-ico.blue{background:#EFF6FF;color:#0EA5E9}.mais-ico.orange{background:#FFF7ED;color:#F59E0B}.mais-ico.purple{background:#F5F3FF;color:#7C3AED}.mais-ico.green{background:#ECFDF3;color:#16A34A}.mais-t{font-size:14px;font-weight:700}.mais-s{font-size:12px;color:var(--muted);margin-top:2px}.mais-arr{font-size:22px;color:#CBD5E1;margin-left:auto}
/* Wizard Custo do Veículo — prefixo cv- pra não colidir com nada existente */
.cv-dots{display:flex;gap:4px;justify-content:center;flex:1}
.cv-dot{width:16px;height:4px;border-radius:99px;background:#E2E8F0}
.cv-dot.on{background:var(--blue)}
.cv-head{display:flex;align-items:center;gap:10px;margin-bottom:16px}
.cv-back{width:32px;height:32px;border-radius:10px;border:1.5px solid var(--line);background:#fff;flex:0 0 32px;display:flex;align-items:center;justify-content:center;color:#475569;cursor:pointer}
.cv-h2{font-weight:800;font-size:17px;color:var(--text);margin-bottom:4px}
.cv-sub{font-size:12px;color:var(--muted);line-height:1.5;margin-bottom:16px}
.cv-inp-wrap{display:flex;align-items:center;border:1.5px solid var(--line);border-radius:12px;padding:0 12px;height:46px;gap:6px}
.cv-inp-wrap span{font-size:13px;color:var(--muted);font-weight:700}
.cv-inp-wrap input{border:0;outline:0;flex:1;font-size:15px;font-weight:700;color:var(--text);font-family:inherit;min-width:0}
.cv-lbl{display:block;font-size:11px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.05em;margin-bottom:6px}
.cv-hint{font-size:10.5px;color:#94A3B8;margin-top:5px}
.cv-sug{display:inline-flex;align-items:center;gap:5px;font-size:11px;font-weight:700;color:#2563EB;background:#EFF6FF;padding:4px 10px;border-radius:99px;margin-top:6px;cursor:pointer;border:0;font-family:inherit}
.cv-btn{width:100%;height:48px;border:0;border-radius:14px;font-size:14px;font-weight:800;cursor:pointer;background:var(--blue);color:#fff;font-family:inherit}
.cv-btn-ghost{width:100%;background:transparent;color:var(--muted);height:38px;font-size:12.5px;margin-top:6px;border:0;cursor:pointer;font-family:inherit}
.cv-opt-grid{display:flex;gap:8px;margin-bottom:16px}
.cv-opt-card{flex:1;border:1.5px solid var(--line);border-radius:14px;padding:12px 8px;text-align:center;cursor:pointer;display:flex;flex-direction:column;align-items:center;gap:6px;background:#fff}
.cv-opt-card.sel{border-color:var(--blue);background:#EFF3FB}
.cv-opt-ico{width:34px;height:34px;border-radius:10px;background:#F1F5F9;display:flex;align-items:center;justify-content:center;color:#475569}
.cv-opt-card.sel .cv-opt-ico{background:var(--blue);color:#fff}
.cv-opt-card span{font-size:11.5px;font-weight:700;color:var(--text)}
.cv-opt-row{display:flex;align-items:center;gap:10px;border:1.5px solid var(--line);border-radius:14px;padding:12px;margin-bottom:8px;cursor:pointer;background:#fff}
.cv-opt-row.sel{border-color:var(--blue);background:#EFF3FB}
.cv-opt-row .cv-opt-ico{width:34px;height:34px;flex:0 0 34px}
.cv-opt-row .cv-txt{flex:1}
.cv-opt-row .cv-txt b{display:block;font-size:13px;font-weight:700;color:var(--text)}
.cv-opt-row .cv-txt small{font-size:10.5px;color:var(--muted)}
.cv-radio{width:20px;height:20px;border-radius:50%;border:2px solid var(--line);flex:0 0 20px}
.cv-radio.on{border-color:var(--blue);background:var(--blue);position:relative}
.cv-radio.on::after{content:'';position:absolute;inset:4px;border-radius:50%;background:#fff}
.cv-pct-row{display:flex;gap:8px;margin-bottom:14px}
.cv-pct-btn{flex:1;height:44px;border:1.5px solid var(--line);border-radius:12px;font-size:13px;font-weight:800;color:var(--muted);background:#fff;cursor:pointer;font-family:inherit}
.cv-pct-btn.sel{border-color:var(--blue);background:var(--blue);color:#fff}
.cv-result{background:linear-gradient(145deg,#0A2F6B 0%,#0D3E8C 60%,#0A2F6B 100%);border-radius:18px;padding:20px;color:#fff;text-align:center;margin-bottom:14px}
.cv-result .l{font-size:11px;font-weight:700;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.06em}
.cv-result .v{font-family:'Poppins',sans-serif;font-weight:900;font-size:32px;margin:6px 0 2px}
.cv-result .v small{font-size:14px;font-weight:700;color:rgba(255,255,255,.7)}
.cv-breakdown{display:flex;flex-direction:column;gap:7px;margin-bottom:12px}
.cv-brow{display:flex;justify-content:space-between;align-items:center;font-size:12.5px;padding:9px 12px;background:#F8FAFC;border-radius:10px}
.cv-brow .k{color:var(--muted);font-weight:600}
.cv-brow .v{font-weight:800;color:var(--text)}
.cv-daily{display:flex;gap:8px;background:#FFFBEB;border:1px solid #FDE68A;border-radius:12px;padding:10px 12px;font-size:11.5px;color:#92400E;line-height:1.5;margin-bottom:14px}
.cv-banner{display:flex;gap:10px;align-items:center;background:linear-gradient(135deg,#0A2F6B,#0D3E8C);border-radius:16px;padding:12px 14px;margin:0 16px 12px;color:#fff}
.cv-banner .txt{flex:1;font-size:12px;font-weight:600;line-height:1.4}
.cv-banner .txt b{display:block;font-size:12.5px;font-weight:800;margin-bottom:1px}
.cv-banner .x{width:22px;height:22px;border-radius:50%;background:rgba(255,255,255,.15);border:0;color:#fff;display:flex;align-items:center;justify-content:center;flex:0 0 22px;cursor:pointer}
.pending-confirm-banner{display:flex;align-items:center;gap:12px;background:linear-gradient(135deg,#F59E0B,#EA580C);border-radius:16px;padding:12px 14px;margin:0 16px 12px;box-shadow:0 4px 14px rgba(234,88,12,.28);cursor:pointer;touch-action:manipulation}
.pcb-ico{width:36px;height:36px;border-radius:11px;background:rgba(255,255,255,.16);display:flex;align-items:center;justify-content:center;flex-shrink:0;color:#fff}
.pcb-txt{flex:1;min-width:0}
.pcb-title{font-size:12px;font-weight:800;color:#fff;line-height:1.35}
.pcb-sub{font-size:10.5px;color:rgba(255,255,255,.7);margin-top:2px;font-weight:600}
.pcb-chev{color:rgba(255,255,255,.55);flex-shrink:0}
/* ── Financeiro specific ── */
.wrap{padding:12px 16px}.period{display:flex;gap:4px;margin-bottom:14px}.period button{flex:1;padding:9px 6px;border-radius:16px;border:1.5px solid var(--line);background:#F8FAFC;font-size:12px;font-weight:700;color:var(--muted);cursor:pointer;touch-action:manipulation}.period button.on{background:rgba(14,165,233,.1);border-color:rgba(14,165,233,.3);color:#0A2F6B}.hero{background:linear-gradient(135deg,#0F172A,#0EA5E9);border-radius:28px;padding:22px 20px;color:#fff;box-shadow:0 10px 28px rgba(15,23,42,.08)}.heroTop .label{font-size:11px;text-transform:uppercase;letter-spacing:.1em;opacity:.75;font-weight:700;margin-bottom:6px}.heroTop .amount{font-size:42px;font-weight:900;letter-spacing:-.06em;line-height:1}.heroTop .
===== #F59E0B @ 65010 =====
:48px;border:0;border-radius:14px;font-size:14px;font-weight:800;cursor:pointer;background:var(--blue);color:#fff;font-family:inherit}
.cv-btn-ghost{width:100%;background:transparent;color:var(--muted);height:38px;font-size:12.5px;margin-top:6px;border:0;cursor:pointer;font-family:inherit}
.cv-opt-grid{display:flex;gap:8px;margin-bottom:16px}
.cv-opt-card{flex:1;border:1.5px solid var(--line);border-radius:14px;padding:12px 8px;text-align:center;cursor:pointer;display:flex;flex-direction:column;align-items:center;gap:6px;background:#fff}
.cv-opt-card.sel{border-color:var(--blue);background:#EFF3FB}
.cv-opt-ico{width:34px;height:34px;border-radius:10px;background:#F1F5F9;display:flex;align-items:center;justify-content:center;color:#475569}
.cv-opt-card.sel .cv-opt-ico{background:var(--blue);color:#fff}
.cv-opt-card span{font-size:11.5px;font-weight:700;color:var(--text)}
.cv-opt-row{display:flex;align-items:center;gap:10px;border:1.5px solid var(--line);border-radius:14px;padding:12px;margin-bottom:8px;cursor:pointer;background:#fff}
.cv-opt-row.sel{border-color:var(--blue);background:#EFF3FB}
.cv-opt-row .cv-opt-ico{width:34px;height:34px;flex:0 0 34px}
.cv-opt-row .cv-txt{flex:1}
.cv-opt-row .cv-txt b{display:block;font-size:13px;font-weight:700;color:var(--text)}
.cv-opt-row .cv-txt small{font-size:10.5px;color:var(--muted)}
.cv-radio{width:20px;height:20px;border-radius:50%;border:2px solid var(--line);flex:0 0 20px}
.cv-radio.on{border-color:var(--blue);background:var(--blue);position:relative}
.cv-radio.on::after{content:'';position:absolute;inset:4px;border-radius:50%;background:#fff}
.cv-pct-row{display:flex;gap:8px;margin-bottom:14px}
.cv-pct-btn{flex:1;height:44px;border:1.5px solid var(--line);border-radius:12px;font-size:13px;font-weight:800;color:var(--muted);background:#fff;cursor:pointer;font-family:inherit}
.cv-pct-btn.sel{border-color:var(--blue);background:var(--blue);color:#fff}
.cv-result{background:linear-gradient(145deg,#0A2F6B 0%,#0D3E8C 60%,#0A2F6B 100%);border-radius:18px;padding:20px;color:#fff;text-align:center;margin-bottom:14px}
.cv-result .l{font-size:11px;font-weight:700;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.06em}
.cv-result .v{font-family:'Poppins',sans-serif;font-weight:900;font-size:32px;margin:6px 0 2px}
.cv-result .v small{font-size:14px;font-weight:700;color:rgba(255,255,255,.7)}
.cv-breakdown{display:flex;flex-direction:column;gap:7px;margin-bottom:12px}
.cv-brow{display:flex;justify-content:space-between;align-items:center;font-size:12.5px;padding:9px 12px;background:#F8FAFC;border-radius:10px}
.cv-brow .k{color:var(--muted);font-weight:600}
.cv-brow .v{font-weight:800;color:var(--text)}
.cv-daily{display:flex;gap:8px;background:#FFFBEB;border:1px solid #FDE68A;border-radius:12px;padding:10px 12px;font-size:11.5px;color:#92400E;line-height:1.5;margin-bottom:14px}
.cv-banner{display:flex;gap:10px;align-items:center;background:linear-gradient(135deg,#0A2F6B,#0D3E8C);border-radius:16px;padding:12px 14px;margin:0 16px 12px;color:#fff}
.cv-banner .txt{flex:1;font-size:12px;font-weight:600;line-height:1.4}
.cv-banner .txt b{display:block;font-size:12.5px;font-weight:800;margin-bottom:1px}
.cv-banner .x{width:22px;height:22px;border-radius:50%;background:rgba(255,255,255,.15);border:0;color:#fff;display:flex;align-items:center;justify-content:center;flex:0 0 22px;cursor:pointer}
.pending-confirm-banner{display:flex;align-items:center;gap:12px;background:linear-gradient(135deg,#F59E0B,#EA580C);border-radius:16px;padding:12px 14px;margin:0 16px 12px;box-shadow:0 4px 14px rgba(234,88,12,.28);cursor:pointer;touch-action:manipulation}
.pcb-ico{width:36px;height:36px;border-radius:11px;background:rgba(255,255,255,.16);display:flex;align-items:center;justify-content:center;flex-shrink:0;color:#fff}
.pcb-txt{flex:1;min-width:0}
.pcb-title{font-size:12px;font-weight:800;color:#fff;line-height:1.35}
.pcb-sub{font-size:10.5px;color:rgba(255,255,255,.7);margin-top:2px;font-weight:600}
.pcb-chev{color:rgba(255,255,255,.55);flex-shrink:0}
/* ── Financeiro specific ── */
.wrap{padding:12px 16px}.period{display:flex;gap:4px;margin-bottom:14px}.period button{flex:1;padding:9px 6px;border-radius:16px;border:1.5px solid var(--line);background:#F8FAFC;font-size:12px;font-weight:700;color:var(--muted);cursor:pointer;touch-action:manipulation}.period button.on{background:rgba(14,165,233,.1);border-color:rgba(14,165,233,.3);color:#0A2F6B}.hero{background:linear-gradient(135deg,#0F172A,#0EA5E9);border-radius:28px;padding:22px 20px;color:#fff;box-shadow:0 10px 28px rgba(15,23,42,.08)}.heroTop .label{font-size:11px;text-transform:uppercase;letter-spacing:.1em;opacity:.75;font-weight:700;margin-bottom:6px}.heroTop .amount{font-size:42px;font-weight:900;letter-spacing:-.06em;line-height:1}.heroTop .sub{font-size:12px;opacity:.75;margin-top:4px}.heroGrid{display:grid;grid-template-columns:repeat(3,1fr);gap:0;margin-top:16px;background:rgba(255,255,255,.1);border-radius:16px;overflow:hidden}.heroCell{padding:12px;text-align:center;border-right:1px solid rgba(255,255,255,.12)}.heroCell:last-child{border-right:none}.heroCell strong{font-size:18px;font-weight:800;display:block;margin-bottom:3px}.heroCell span{font-size:11px;opacity:.75}
.card{background:#fff;border:1px solid var(--line);border-radius:var(--r);box-shadow:0 2px 10px rgba(15,23,42,.05)}.cardPad{padding:16px}.cardHead{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px}.cardHead h2{font-size:16px;font-weight:700;letter-spacing:-.03em}.cardHead a,.cardHead button{font-size:12px;color:#0A2F6B;font-weight:700;background:none;border:none;cursor:pointer}
.seg{display:flex;gap:4px}.seg button{padding:7px 12px;border-radius:12px;border:none;background:#F1F5F9;color:var(--muted);font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation}.seg button.on{background:rgba(14,165,233,.1);color:#0A2F6B}
.kpis{display:grid;grid-template-columns:1fr 1fr;gap:10px}.kpi{background:#F8FAFC;border-radius:16px;padding:14px}.kpi .row{display:flex;align-items:center;gap:8px;margin-bottom:6px}.ico{width:34px;height:34px;border-radius:12px;display:flex;align-items:center;justify-content:center;flex-shrink:0}.ico.green{background:#ECFDF3;color:#16A34A}.ico.red{background:#FEF2F2;color:#EF4444}.ico.blue{background:#EFF6FF;color:#0EA5E9}.ico.orange{background:#FFF7ED;color:#F59E0B}.ico.purple{background:#F5F3FF;color:#7C3AED}.kpiVal{font-family:'Inter',system-ui,sans-serif;font-size:20px;font-weight:700;line-height:1}.kpiLbl{font-size:11px;color:var(--muted);font-weight:600;margin-top:2px}
.fin-hero-top{display:flex;align-items:center;gap:8px;margin-bottom:16px}
.fin-ptabs{display:flex;background:rgba(255,255,255,.16);border:1px solid rgba(255,255,255,.14);border-radius:999px;padding:4px;gap:3px;flex:1}
.fin-ptabs button{flex:1;border:0;background:transparent;color:rgba(255,255,255,.76);font-size:12px;font-weight:800;padding:8px 0;border-radius:999px;cursor:pointer;touch-action:manipulation;font-family:var(--font-app)}
.fin-ptabs button.on{background:#fff;color:#0f172a;box-shadow:0 3px 10px rgba(0,0,0,.10)}
.fin-filter-btn{width:36px;height:36px;flex-shrink:0;border-radius:50%;display:flex;align-items:center;justify-content:center;border:1px solid rgba(255,255,255,.25);background:rgba(255,255,255,.12);color:#fff;cursor:pointer;touch-action:manipulation}
.fin-filter-btn.active{background:#fff;color:#0A2F6B;border-color:#fff}
.fin-range-chip{display:flex;align-items:center;gap:6px;margin-top:12px;background:rgba(255,255,255,.14);border:1px solid rgba(255,255,255,.18);border-radius:12px;padding:8px 12px;font-size:11.5px;font-weight:700;color:#fff}
.fin-range-chip button{margin-left:auto;background:none;border:0;color:#fff;font-size:14px;cursor:pointer;line-height:1;opacity:.8}
.fin-metrics{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.fin-metric{background:#FAFBFD;border:1px solid #EEF2F7;border-radius:16px;padding:12px;text-align:left}.fin-metric strong{display:block;font-size:16px;font-weight:800;letter-spacing:-.03em;font-family:'Inter',system-ui,sans-serif}.fin-metric span{display:block;font-size:10px;font-weight:750;text-transform:uppercase;letter-spacing:.05em;color:var(--muted);margin-bottom:3px}.fin-metric small{display:block;font-size:10px;color:#94A3B8;margin-top:3px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.fin-metric.g strong{color:#16A34A}.fin-metric.r strong{color:#EF4444}
.fin-flow{display:flex;gap:10px;align-items:flex-end;height:96px;padding-top:6px}
.fin-fcol{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end;height:100%;gap:5px}
.fin-fbars{display:flex;gap:3px;align-items:flex-end;height:100%}
.fin-fbars i{width:9px;border-radius:3px 3px 0 0;display:block;min-height:3px}
.fin-fbars i.in{background:#22C55E}
.fin-fbars i.out{background:#FCA5A5}
.fin-fcol small{font-size:9px;color:#94A3B8;font-weight:800}
.fin-flow-legend{display:flex;gap:14px;justify-content:center;margin-top:10px;font-size:10.5px;font-weight:600;color:#64748B}
.fin-flow-legend i{display:inline-block;width:8px;height:8px;border-radius:3px;margin-right:4px}
.ld-actions{display:flex;gap:8px;margin-top:10px}
.ld-btn{flex:1;height:40px;border-radius:12px;border:1.5px solid #E2E8F0;background:#fff;font-size:12px;font-weight:800;cursor:pointer;touch-action:manipulation;display:flex;align-items:center;justify-content:center;gap:6px;font-family:var(--font-app)}
.ld-btn svg{width:14px;height:14px;stroke:currentColor;fill:none;stroke-width:2.4;stroke-linecap:round;stroke-linejoin:round;flex-shrink:0}
.ld-btn.pago{color:#16A34A;border-color:#BBF7D0;background:#F0FDF4}
.ld-btn.edit{color:#0A2F6B;border-color:#C7D8F5;background:#F0F6FF}
.ld-btn.del{color:#EF4444;border-color:#FECACA;background:#FEF2F2;flex:0 0 44px}
.obj-tag{display:inline-block;font-size:9px;font-weight:800;padding:2px 7px;border-radius:99px;margin-right:6px;vertical-align:1px}
.obj-tag.rec{b
===== #F59E0B @ 67906 =====
play:flex;gap:10px;align-items:center;background:linear-gradient(135deg,#0A2F6B,#0D3E8C);border-radius:16px;padding:12px 14px;margin:0 16px 12px;color:#fff}
.cv-banner .txt{flex:1;font-size:12px;font-weight:600;line-height:1.4}
.cv-banner .txt b{display:block;font-size:12.5px;font-weight:800;margin-bottom:1px}
.cv-banner .x{width:22px;height:22px;border-radius:50%;background:rgba(255,255,255,.15);border:0;color:#fff;display:flex;align-items:center;justify-content:center;flex:0 0 22px;cursor:pointer}
.pending-confirm-banner{display:flex;align-items:center;gap:12px;background:linear-gradient(135deg,#F59E0B,#EA580C);border-radius:16px;padding:12px 14px;margin:0 16px 12px;box-shadow:0 4px 14px rgba(234,88,12,.28);cursor:pointer;touch-action:manipulation}
.pcb-ico{width:36px;height:36px;border-radius:11px;background:rgba(255,255,255,.16);display:flex;align-items:center;justify-content:center;flex-shrink:0;color:#fff}
.pcb-txt{flex:1;min-width:0}
.pcb-title{font-size:12px;font-weight:800;color:#fff;line-height:1.35}
.pcb-sub{font-size:10.5px;color:rgba(255,255,255,.7);margin-top:2px;font-weight:600}
.pcb-chev{color:rgba(255,255,255,.55);flex-shrink:0}
/* ── Financeiro specific ── */
.wrap{padding:12px 16px}.period{display:flex;gap:4px;margin-bottom:14px}.period button{flex:1;padding:9px 6px;border-radius:16px;border:1.5px solid var(--line);background:#F8FAFC;font-size:12px;font-weight:700;color:var(--muted);cursor:pointer;touch-action:manipulation}.period button.on{background:rgba(14,165,233,.1);border-color:rgba(14,165,233,.3);color:#0A2F6B}.hero{background:linear-gradient(135deg,#0F172A,#0EA5E9);border-radius:28px;padding:22px 20px;color:#fff;box-shadow:0 10px 28px rgba(15,23,42,.08)}.heroTop .label{font-size:11px;text-transform:uppercase;letter-spacing:.1em;opacity:.75;font-weight:700;margin-bottom:6px}.heroTop .amount{font-size:42px;font-weight:900;letter-spacing:-.06em;line-height:1}.heroTop .sub{font-size:12px;opacity:.75;margin-top:4px}.heroGrid{display:grid;grid-template-columns:repeat(3,1fr);gap:0;margin-top:16px;background:rgba(255,255,255,.1);border-radius:16px;overflow:hidden}.heroCell{padding:12px;text-align:center;border-right:1px solid rgba(255,255,255,.12)}.heroCell:last-child{border-right:none}.heroCell strong{font-size:18px;font-weight:800;display:block;margin-bottom:3px}.heroCell span{font-size:11px;opacity:.75}
.card{background:#fff;border:1px solid var(--line);border-radius:var(--r);box-shadow:0 2px 10px rgba(15,23,42,.05)}.cardPad{padding:16px}.cardHead{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px}.cardHead h2{font-size:16px;font-weight:700;letter-spacing:-.03em}.cardHead a,.cardHead button{font-size:12px;color:#0A2F6B;font-weight:700;background:none;border:none;cursor:pointer}
.seg{display:flex;gap:4px}.seg button{padding:7px 12px;border-radius:12px;border:none;background:#F1F5F9;color:var(--muted);font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation}.seg button.on{background:rgba(14,165,233,.1);color:#0A2F6B}
.kpis{display:grid;grid-template-columns:1fr 1fr;gap:10px}.kpi{background:#F8FAFC;border-radius:16px;padding:14px}.kpi .row{display:flex;align-items:center;gap:8px;margin-bottom:6px}.ico{width:34px;height:34px;border-radius:12px;display:flex;align-items:center;justify-content:center;flex-shrink:0}.ico.green{background:#ECFDF3;color:#16A34A}.ico.red{background:#FEF2F2;color:#EF4444}.ico.blue{background:#EFF6FF;color:#0EA5E9}.ico.orange{background:#FFF7ED;color:#F59E0B}.ico.purple{background:#F5F3FF;color:#7C3AED}.kpiVal{font-family:'Inter',system-ui,sans-serif;font-size:20px;font-weight:700;line-height:1}.kpiLbl{font-size:11px;color:var(--muted);font-weight:600;margin-top:2px}
.fin-hero-top{display:flex;align-items:center;gap:8px;margin-bottom:16px}
.fin-ptabs{display:flex;background:rgba(255,255,255,.16);border:1px solid rgba(255,255,255,.14);border-radius:999px;padding:4px;gap:3px;flex:1}
.fin-ptabs button{flex:1;border:0;background:transparent;color:rgba(255,255,255,.76);font-size:12px;font-weight:800;padding:8px 0;border-radius:999px;cursor:pointer;touch-action:manipulation;font-family:var(--font-app)}
.fin-ptabs button.on{background:#fff;color:#0f172a;box-shadow:0 3px 10px rgba(0,0,0,.10)}
.fin-filter-btn{width:36px;height:36px;flex-shrink:0;border-radius:50%;display:flex;align-items:center;justify-content:center;border:1px solid rgba(255,255,255,.25);background:rgba(255,255,255,.12);color:#fff;cursor:pointer;touch-action:manipulation}
.fin-filter-btn.active{background:#fff;color:#0A2F6B;border-color:#fff}
.fin-range-chip{display:flex;align-items:center;gap:6px;margin-top:12px;background:rgba(255,255,255,.14);border:1px solid rgba(255,255,255,.18);border-radius:12px;padding:8px 12px;font-size:11.5px;font-weight:700;color:#fff}
.fin-range-chip button{margin-left:auto;background:none;border:0;color:#fff;font-size:14px;cursor:pointer;line-height:1;opacity:.8}
.fin-metrics{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.fin-metric{background:#FAFBFD;border:1px solid #EEF2F7;border-radius:16px;padding:12px;text-align:left}.fin-metric strong{display:block;font-size:16px;font-weight:800;letter-spacing:-.03em;font-family:'Inter',system-ui,sans-serif}.fin-metric span{display:block;font-size:10px;font-weight:750;text-transform:uppercase;letter-spacing:.05em;color:var(--muted);margin-bottom:3px}.fin-metric small{display:block;font-size:10px;color:#94A3B8;margin-top:3px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.fin-metric.g strong{color:#16A34A}.fin-metric.r strong{color:#EF4444}
.fin-flow{display:flex;gap:10px;align-items:flex-end;height:96px;padding-top:6px}
.fin-fcol{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end;height:100%;gap:5px}
.fin-fbars{display:flex;gap:3px;align-items:flex-end;height:100%}
.fin-fbars i{width:9px;border-radius:3px 3px 0 0;display:block;min-height:3px}
.fin-fbars i.in{background:#22C55E}
.fin-fbars i.out{background:#FCA5A5}
.fin-fcol small{font-size:9px;color:#94A3B8;font-weight:800}
.fin-flow-legend{display:flex;gap:14px;justify-content:center;margin-top:10px;font-size:10.5px;font-weight:600;color:#64748B}
.fin-flow-legend i{display:inline-block;width:8px;height:8px;border-radius:3px;margin-right:4px}
.ld-actions{display:flex;gap:8px;margin-top:10px}
.ld-btn{flex:1;height:40px;border-radius:12px;border:1.5px solid #E2E8F0;background:#fff;font-size:12px;font-weight:800;cursor:pointer;touch-action:manipulation;display:flex;align-items:center;justify-content:center;gap:6px;font-family:var(--font-app)}
.ld-btn svg{width:14px;height:14px;stroke:currentColor;fill:none;stroke-width:2.4;stroke-linecap:round;stroke-linejoin:round;flex-shrink:0}
.ld-btn.pago{color:#16A34A;border-color:#BBF7D0;background:#F0FDF4}
.ld-btn.edit{color:#0A2F6B;border-color:#C7D8F5;background:#F0F6FF}
.ld-btn.del{color:#EF4444;border-color:#FECACA;background:#FEF2F2;flex:0 0 44px}
.obj-tag{display:inline-block;font-size:9px;font-weight:800;padding:2px 7px;border-radius:99px;margin-right:6px;vertical-align:1px}
.obj-tag.rec{background:#E9FBEF;color:#16A34A}
.obj-tag.desp{background:#FEF2F2;color:#EF4444}
.obj-switch,.type-switch2{display:flex;background:#EAF0F6;border:1px solid rgba(15,23,42,.06);padding:4px;border-radius:18px;margin-bottom:16px}
.obj-btn,.type-btn2{flex:1;border:none;background:transparent;color:var(--muted);padding:10px 8px;border-radius:14px;font-size:13.5px;font-weight:900;cursor:pointer;font-family:var(--font-app)}
.obj-btn.active,.type-btn2.active{background:#fff;color:var(--text);box-shadow:0 4px 12px rgba(15,23,42,.08)}
.gbar.rec i{background:linear-gradient(90deg,#22C55E,#0EA5E9)}.gbar.exp i{background:linear-gradient(90deg,#F59E0B,#F59E0B)}.gbar.exp.over i,.gbar.rec.over i{background:linear-gradient(90deg,#EF4444,#EF4444)}
.goal-locked{display:flex;align-items:center;justify-content:space-between;background:#F8FAFC;border:1px dashed #D8E0EA;border-radius:14px;padding:10px 12px;cursor:pointer;flex:1;min-width:0}.goal-locked span{font-size:12px;color:var(--muted);font-weight:600}.goal-locked b{font-size:12px;color:#0A2F6B;font-weight:800}
.cfg-link{display:block;text-align:center;font-size:12.5px;font-weight:700;color:#0A2F6B;background:#F8FAFC;border:1px solid var(--line);border-radius:14px;padding:11px;margin-top:4px;cursor:pointer;font-family:var(--font-app)}
.cat-row2{display:flex;align-items:center;gap:12px;padding:11px 0;border-bottom:1px solid #edf1f6}.cat-row2:last-child{border-bottom:0}.cat-lbl2{flex:1;font-size:14px;font-weight:650}
.cat-inp-wrap2{position:relative;width:118px}.cat-inp-wrap2 span{position:absolute;left:12px;top:50%;transform:translateY(-50%);font-size:13px;color:var(--muted);font-weight:700}
.cat-inp2{width:100%;height:42px;border:1.5px solid var(--line);background:#F8FAFC;border-radius:13px;padding:0 12px 0 32px;font-size:14px;font-weight:700;color:var(--text);outline:none;font-family:'Inter',system-ui,sans-serif;text-align:right}.cat-inp2:focus{border-color:#0A2F6B;background:#fff}
.chart{padding:4px 0}.barCol{flex:1;display:flex;flex-direction:column;align-items:center;gap:4px}.barCol i{width:100%;background:linear-gradient(180deg,var(--blue),var(--blue2));border-radius:6px 6px 0 0;min-height:8px}.barCol small{font-size:10px;color:var(--muted);font-weight:700}
.actions{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.action{border:1px solid var(--line);border-radius:16px;background:#F8FAFC;padding:14px 8px;display:flex;flex-direction:column;align-items:center;gap:8px;font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation;color:var(--text)}.action .ico{background:transparent}.action:active{background:var(--line)}
.list{padding:0 0 2px}.item{display:flex;align-items:center;gap:12px;padding:12px 0}.itemBody{flex:1;min-width:0}
.budgetRow{display:flex;align-items:center;gap:12px;padding:10px 0;border-bottom:1px solid var(--line)}.budgetRow:last-child{border-bottom:none}.budgetTrack{height:
===== #F59E0B @ 72147 =====
0,.10)}
.fin-filter-btn{width:36px;height:36px;flex-shrink:0;border-radius:50%;display:flex;align-items:center;justify-content:center;border:1px solid rgba(255,255,255,.25);background:rgba(255,255,255,.12);color:#fff;cursor:pointer;touch-action:manipulation}
.fin-filter-btn.active{background:#fff;color:#0A2F6B;border-color:#fff}
.fin-range-chip{display:flex;align-items:center;gap:6px;margin-top:12px;background:rgba(255,255,255,.14);border:1px solid rgba(255,255,255,.18);border-radius:12px;padding:8px 12px;font-size:11.5px;font-weight:700;color:#fff}
.fin-range-chip button{margin-left:auto;background:none;border:0;color:#fff;font-size:14px;cursor:pointer;line-height:1;opacity:.8}
.fin-metrics{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.fin-metric{background:#FAFBFD;border:1px solid #EEF2F7;border-radius:16px;padding:12px;text-align:left}.fin-metric strong{display:block;font-size:16px;font-weight:800;letter-spacing:-.03em;font-family:'Inter',system-ui,sans-serif}.fin-metric span{display:block;font-size:10px;font-weight:750;text-transform:uppercase;letter-spacing:.05em;color:var(--muted);margin-bottom:3px}.fin-metric small{display:block;font-size:10px;color:#94A3B8;margin-top:3px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.fin-metric.g strong{color:#16A34A}.fin-metric.r strong{color:#EF4444}
.fin-flow{display:flex;gap:10px;align-items:flex-end;height:96px;padding-top:6px}
.fin-fcol{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end;height:100%;gap:5px}
.fin-fbars{display:flex;gap:3px;align-items:flex-end;height:100%}
.fin-fbars i{width:9px;border-radius:3px 3px 0 0;display:block;min-height:3px}
.fin-fbars i.in{background:#22C55E}
.fin-fbars i.out{background:#FCA5A5}
.fin-fcol small{font-size:9px;color:#94A3B8;font-weight:800}
.fin-flow-legend{display:flex;gap:14px;justify-content:center;margin-top:10px;font-size:10.5px;font-weight:600;color:#64748B}
.fin-flow-legend i{display:inline-block;width:8px;height:8px;border-radius:3px;margin-right:4px}
.ld-actions{display:flex;gap:8px;margin-top:10px}
.ld-btn{flex:1;height:40px;border-radius:12px;border:1.5px solid #E2E8F0;background:#fff;font-size:12px;font-weight:800;cursor:pointer;touch-action:manipulation;display:flex;align-items:center;justify-content:center;gap:6px;font-family:var(--font-app)}
.ld-btn svg{width:14px;height:14px;stroke:currentColor;fill:none;stroke-width:2.4;stroke-linecap:round;stroke-linejoin:round;flex-shrink:0}
.ld-btn.pago{color:#16A34A;border-color:#BBF7D0;background:#F0FDF4}
.ld-btn.edit{color:#0A2F6B;border-color:#C7D8F5;background:#F0F6FF}
.ld-btn.del{color:#EF4444;border-color:#FECACA;background:#FEF2F2;flex:0 0 44px}
.obj-tag{display:inline-block;font-size:9px;font-weight:800;padding:2px 7px;border-radius:99px;margin-right:6px;vertical-align:1px}
.obj-tag.rec{background:#E9FBEF;color:#16A34A}
.obj-tag.desp{background:#FEF2F2;color:#EF4444}
.obj-switch,.type-switch2{display:flex;background:#EAF0F6;border:1px solid rgba(15,23,42,.06);padding:4px;border-radius:18px;margin-bottom:16px}
.obj-btn,.type-btn2{flex:1;border:none;background:transparent;color:var(--muted);padding:10px 8px;border-radius:14px;font-size:13.5px;font-weight:900;cursor:pointer;font-family:var(--font-app)}
.obj-btn.active,.type-btn2.active{background:#fff;color:var(--text);box-shadow:0 4px 12px rgba(15,23,42,.08)}
.gbar.rec i{background:linear-gradient(90deg,#22C55E,#0EA5E9)}.gbar.exp i{background:linear-gradient(90deg,#F59E0B,#F59E0B)}.gbar.exp.over i,.gbar.rec.over i{background:linear-gradient(90deg,#EF4444,#EF4444)}
.goal-locked{display:flex;align-items:center;justify-content:space-between;background:#F8FAFC;border:1px dashed #D8E0EA;border-radius:14px;padding:10px 12px;cursor:pointer;flex:1;min-width:0}.goal-locked span{font-size:12px;color:var(--muted);font-weight:600}.goal-locked b{font-size:12px;color:#0A2F6B;font-weight:800}
.cfg-link{display:block;text-align:center;font-size:12.5px;font-weight:700;color:#0A2F6B;background:#F8FAFC;border:1px solid var(--line);border-radius:14px;padding:11px;margin-top:4px;cursor:pointer;font-family:var(--font-app)}
.cat-row2{display:flex;align-items:center;gap:12px;padding:11px 0;border-bottom:1px solid #edf1f6}.cat-row2:last-child{border-bottom:0}.cat-lbl2{flex:1;font-size:14px;font-weight:650}
.cat-inp-wrap2{position:relative;width:118px}.cat-inp-wrap2 span{position:absolute;left:12px;top:50%;transform:translateY(-50%);font-size:13px;color:var(--muted);font-weight:700}
.cat-inp2{width:100%;height:42px;border:1.5px solid var(--line);background:#F8FAFC;border-radius:13px;padding:0 12px 0 32px;font-size:14px;font-weight:700;color:var(--text);outline:none;font-family:'Inter',system-ui,sans-serif;text-align:right}.cat-inp2:focus{border-color:#0A2F6B;background:#fff}
.chart{padding:4px 0}.barCol{flex:1;display:flex;flex-direction:column;align-items:center;gap:4px}.barCol i{width:100%;background:linear-gradient(180deg,var(--blue),var(--blue2));border-radius:6px 6px 0 0;min-height:8px}.barCol small{font-size:10px;color:var(--muted);font-weight:700}
.actions{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.action{border:1px solid var(--line);border-radius:16px;background:#F8FAFC;padding:14px 8px;display:flex;flex-direction:column;align-items:center;gap:8px;font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation;color:var(--text)}.action .ico{background:transparent}.action:active{background:var(--line)}
.list{padding:0 0 2px}.item{display:flex;align-items:center;gap:12px;padding:12px 0}.itemBody{flex:1;min-width:0}
.budgetRow{display:flex;align-items:center;gap:12px;padding:10px 0;border-bottom:1px solid var(--line)}.budgetRow:last-child{border-bottom:none}.budgetTrack{height:6px;background:#E2E8F0;border-radius:3px;overflow:hidden;margin-top:6px}.budgetTrack i{display:block;height:100%;border-radius:3px;background:#0A2F6B;transition:width .4s}
.split{display:grid;grid-template-columns:auto 1fr;gap:16px;align-items:center}.donut{width:80px;height:80px;border-radius:50%;background:conic-gradient(var(--blue) 0 57%,var(--green) 57% 86%,var(--orange) 86% 97%,var(--red) 97% 100%);flex-shrink:0}.catRow{display:flex;align-items:center;gap:8px;margin-bottom:8px}.dot{width:8px;height:8px;border-radius:50%;background:#0A2F6B;flex-shrink:0}
/* ── Home screen extras ── */
.ad-carousel{margin:14px 16px 0;border-radius:18px;overflow:hidden;position:relative;touch-action:pan-y;cursor:pointer;height:40px}
.ad-carousel img{width:100%;height:40px;object-fit:cover;object-position:center;display:block;transition:transform .4s ease;pointer-events:none}
.ad-carousel .adtag{position:absolute;top:8px;left:10px;font-size:8px;font-weight:900;text-transform:uppercase;letter-spacing:.08em;color:#64748B;background:rgba(255,255,255,0.92);padding:3px 7px;border-radius:6px;z-index:2;backdrop-filter:blur(4px)}
.entries{background:#fff;border:1px solid var(--line);border-radius:24px;padding:16px;box-shadow:0 2px 10px rgba(15,23,42,.05)}.eheader{display:flex;align-items:center;justify-content:space-between;margin-bottom:10px}.eheader h3{font-size:15px;font-weight:700}.eheader button{font-size:12px;font-weight:700;color:#0A2F6B;background:none;border:none;cursor:pointer;touch-action:manipulation}
.partner{background:linear-gradient(135deg,#0F172A,#1E293B);border-radius:22px;padding:16px;color:#fff;box-shadow:0 10px 28px rgba(15,23,42,.08)}.eyebrow{font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.1em;opacity:.6;margin-bottom:4px}.pt{font-size:16px;font-weight:800;letter-spacing:-.03em}.ps{font-size:12px;opacity:.65;margin-top:4px}
.pie-wrap{display:flex;align-items:center;gap:14px;padding:8px 0}.pie{width:72px;height:72px;border-radius:50%;background:conic-gradient(var(--blue) 0 61%,var(--green) 61% 89%,var(--orange) 89% 100%);flex-shrink:0}.pie-wrap .list{background:none;border:none;box-shadow:none;padding:0;flex:1}.li{display:flex;align-items:center;justify-content:space-between;font-size:12px;padding:3px 0}.li span{color:var(--muted);font-weight:600}.li b{font-family:'Inter',system-ui,sans-serif;font-weight:700}.legend{display:flex;gap:12px;margin-top:8px}.leg{display:flex;align-items:center;gap:4px;font-size:11px;color:var(--muted);font-weight:600}.dot{width:7px;height:7px;border-radius:50%;background:#0A2F6B;flex-shrink:0}.dot.g{background:var(--green)}.dot.o{background:var(--orange)}

.hero{border-radius:28px;padding:20px;color:#fff;box-shadow:0 10px 28px rgba(15,23,42,.08)}.period{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px}.seg button{flex:1;padding:8px 6px;border-radius:14px;border:none;background:transparent;color:rgba(255,255,255,.65);font-size:13px;font-weight:800;cursor:pointer;touch-action:manipulation}.seg button.on{background:rgba(255,255,255,.22);color:#fff}.filter{color:rgba(255,255,255,.7);cursor:pointer}
.kicker{font-size:11px;text-transform:uppercase;letter-spacing:.12em;opacity:.78;font-weight:800;margin-bottom:6px}.amount{font-size:42px;font-weight:900;letter-spacing:-.06em;line-height:1;margin-bottom:4px}.sub{font-size:13px;opacity:.78}
.metrics{margin-top:16px;display:flex;flex-direction:column;gap:8px}.mrow{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.metric{background:rgba(255,255,255,.14);border-radius:14px;padding:10px 8px;text-align:center}.mv{font-family:'Inter',system-ui,sans-serif;font-size:16px;font-weight:800}.mv.good{color:#86EFAC}.mv.bad{color:#FCA5A5}.mv.warn{color:#FDE68A}.ml{font-size:10px;opacity:.72;margin-top:3px;font-weight:700}
.goalbar{height:4px;background:rgba(255,255,255,.2);border-radius:2px;margin-top:16px;overflow:hidden}.goalfill{height:100%;background:rgba(255,255,255,.9);border-radius:2px;transition:width .6s}.goaltext{font-size:11px;opacity:.75;margin-top:6px;font-weight:600}
.chart{background:#fff;border:1px solid var(--line);box-shadow:0 2px 10px rgba(15,23,42,.05);border-radius:24px;padding:16px}.chead{display:flex;align-items:center;justify
===== #F59E0B @ 81908 =====
goaltext{font-size:11px;opacity:.75;margin-top:6px;font-weight:600}
.chart{background:#fff;border:1px solid var(--line);box-shadow:0 2px 10px rgba(15,23,42,.05);border-radius:24px;padding:16px}.chead{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}.ctitle{font-size:14px;font-weight:700;letter-spacing:-.02em}.mini-tabs{display:flex;gap:4px}.mini-tabs span{font-size:11px;font-weight:700;padding:4px 10px;border-radius:10px;cursor:pointer;color:var(--muted)}.mini-tabs span.on{background:rgba(14,165,233,.1);color:#0A2F6B}
.bars{display:flex;gap:4px;align-items:flex-end;height:80px;padding:0 2px}.baritem{flex:1;display:flex;flex-direction:column;align-items:center;gap:6px}.col{width:100%;background:#0A2F6B;border-radius:6px 6px 0 0;min-height:6px;opacity:.85;transition:height .5s}.col.alt{background:var(--green)}.col.warn{background:var(--orange)}.lbl{font-size:10px;color:var(--muted);font-weight:700}.bars.many .baritem{flex:0 0 calc(10% - 4px)}.bars.medium .baritem{flex:0 0 calc(14.28% - 4px)}

@keyframes spin{from{transform:rotate(0deg)}to{transform:rotate(360deg)}}

/* fin-action buttons (Financeiro screen) */
.fin-actions{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}
.fin-action{border:1px solid var(--line);border-radius:16px;background:#F8FAFC;padding:14px 8px;display:flex;flex-direction:column;align-items:center;gap:8px;font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation;color:var(--text);font-family:var(--font-app)}
.fin-action:active{background:var(--line)}
.fin-action .ico{background:transparent}
@keyframes vspin{from{transform:rotate(0deg)}to{transform:rotate(360deg)}}
@keyframes pulseDot{0%,100%{opacity:.25}50%{opacity:1}}


/* ═══════════════════════════════════════════════════
   AGENDA — CSS isolado (prefixo ag-)
   ═══════════════════════════════════════════════════ */
#sc-agenda{padding:0}
.ag-wrap{padding:12px 16px 24px}
.ag-tabs{height:52px;background:#eaf0f6;border:1px solid #dbe3ec;border-radius:28px;padding:5px;display:flex;margin-bottom:16px}
.ag-tabs button{flex:1;border:0;background:transparent;color:#667085;font-size:14px;font-weight:700;border-radius:23px;cursor:pointer;touch-action:manipulation;font-family:var(--font-app)}
.ag-tabs button.on{background:#fff;color:#101828;box-shadow:0 8px 20px rgba(15,23,42,.08)}
.ag-date-head{display:flex;align-items:center;justify-content:space-between;margin:8px 0 14px}
.ag-date-head h1{font-size:22px;font-weight:800;letter-spacing:-.04em;line-height:1.1}
.ag-date-head p{font-size:12px;font-weight:500;color:#667085;margin-top:4px}
.ag-pill{background:#e6f5ff;color:#2592d9;border:0;border-radius:999px;padding:9px 15px;font-weight:700;font-size:13px;cursor:pointer;touch-action:manipulation;font-family:var(--font-app);white-space:nowrap}
.ag-sec{font-size:11px;letter-spacing:.14em;font-weight:800;color:#6b778c;margin:20px 0 10px;text-transform:uppercase}
.ag-item{background:#fff;border:1px solid rgba(15,23,42,.06);border-radius:24px;padding:14px;box-shadow:0 10px 28px rgba(15,23,42,.06);display:flex;align-items:center;gap:12px;margin-bottom:10px}
.ag-ico{width:48px;height:48px;border-radius:16px;display:flex;align-items:center;justify-content:center;flex:0 0 48px}
.ag-ico svg{width:20px;height:20px;stroke:currentColor;fill:none;stroke-width:2.25;stroke-linecap:round;stroke-linejoin:round}
.ag-ico.green{background:#e9fbef;color:#22c55e}.ag-ico.blue{background:#e8f5ff;color:#0ea5e9}.ag-ico.orange{background:#fff5df;color:#f59e0b}.ag-ico.red{background:#fdecef;color:#ef4444}.ag-ico.purple{background:#f1ecff;color:#8b5cf6}
.ag-info{flex:1;min-width:0}
.ag-ititle{font-size:15px;font-weight:700;letter-spacing:-.02em;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.ag-imeta{font-size:12px;color:#667085;font-weight:500;line-height:1.4;margin-top:3px}
.ag-ivalue{font-size:14px;font-weight:700;color:#344054;white-space:nowrap;flex-shrink:0}
.ag-paybtn{border:0;background:#e9fbef;color:#16a34a;border-radius:999px;padding:8px 12px;font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation;font-family:var(--font-app);flex-shrink:0}
.ag-paybtn.paid{background:#f3f4f6;color:#9ca3af}
.ag-empty{padding:32px 16px;text-align:center;color:#94A3B8;font-size:13px;font-weight:500}
/* Agenda Dia */
.ag-strip{display:flex;gap:8px}
.ag-strip::-webkit-scrollbar{display:none}
.ag-chip{flex:1;min-width:0;height:70px;background:#fff;border:1px solid #e4eaf2;border-radius:20px;box-shadow:0 4px 12px rgba(15,23,42,.045);display:flex;flex-direction:column;align-items:center;justify-content:center;cursor:pointer;touch-action:manipulation;color:#667085}
.ag-chip span{font-size:11px;letter-spacing:.07em;text-transform:uppercase;font-weight:700}
.ag-chip strong{font-size:22px;color:#101828;font-weight:800;margin-top:3px}
.ag-chip.on{background:#0ea5e9;border-color:#0ea5e9;color:#fff}
.ag-chip.on strong{color:#fff}
.ag-chip.today-chip{border-color:#22c55e;color:#22c55e}
.ag-chip.today-chip strong{color:#22c55e}
.ag-jdot{width:5px;height:5px;border-radius:50%;background:#22C55E;margin-top:3px;visibility:hidden}
.ag-chip.has-j .ag-jdot{visibility:visible}
.ag-chip.on .ag-jdot{background:#fff}
.ag-head{position:sticky;top:0;z-index:20;background:var(--bg);padding:14px 16px 12px;border-bottom:1px solid rgba(15,23,42,.05)}
.ag-head-row{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}
.ag-month-btn{display:flex;align-items:center;gap:6px;border:0;background:transparent;cursor:pointer;touch-action:manipulation;font-family:var(--font-brand);font-size:19px;font-weight:800;color:var(--text);letter-spacing:-.03em;padding:0}
.ag-month-btn svg{transition:transform .2s}
.ag-month-btn.open svg{transform:rotate(180deg)}
.ag-backdrop{position:absolute;left:-16px;right:-16px;top:100%;height:3000px;background:var(--bg);opacity:0;pointer-events:none;transition:opacity .18s ease;z-index:15}
.ag-backdrop.show{opacity:.98;pointer-events:auto}
.ag-calpanel{position:absolute;left:0;right:0;top:calc(100% + 8px);background:var(--card);border:1px solid rgba(15,23,42,.08);border-radius:22px;box-shadow:0 20px 44px rgba(15,23,42,.16);padding:14px;z-index:21;opacity:0;transform:translateY(-8px);pointer-events:none;transition:opacity .18s ease,transform .18s ease}
.ag-calpanel.show{opacity:1;transform:translateY(0);pointer-events:auto}
.agcal-nav{display:flex;align-items:center;justify-content:space-between;margin:4px 0 8px}
.agcal-nav button{width:34px;height:34px;border-radius:12px;border:1px solid #e4eaf2;background:#fff;font-size:17px;color:#0A2F6B;cursor:pointer;touch-action:manipulation}
.agcal-nav h2{font-size:14px;font-weight:800}
.agcal-legend{display:flex;gap:14px;justify-content:center;padding:0 0 12px;font-size:10.5px;font-weight:600;color:#64748B}
.agcal-legend i{display:inline-block;width:7px;height:7px;border-radius:50%;margin-right:4px}
.ag-body{padding:14px 16px 0}
.ag-day-title{font-size:12px;font-weight:800;color:#6b778c;text-transform:uppercase;letter-spacing:.1em;margin-bottom:10px}
.ag-fab{position:fixed;right:max(18px,calc(50% - 215px + 18px));bottom:calc(var(--bottom) + var(--safe-bottom) + 16px);width:54px;height:54px;border-radius:18px;border:0;background:#0A2F6B;color:#fff;display:flex;align-items:center;justify-content:center;box-shadow:0 10px 26px rgba(10,47,107,.35);cursor:pointer;touch-action:manipulation;z-index:30}
.ag-timeline{position:relative;background:#fff;border:1px solid rgba(15,23,42,.06);border-radius:26px;box-shadow:0 10px 28px rgba(15,23,42,.06);overflow:hidden;margin-top:8px}
.ag-hour{height:64px;border-bottom:1px solid #edf1f6;display:grid;grid-template-columns:64px 1fr;position:relative;z-index:2}
.ag-hour span{font-size:12px;color:#98a4b8;font-weight:600;padding:12px 10px;line-height:1;transition:color .15s}
.ag-hour.in-plan span{color:#fff}
.ag-hlane{border-left:1px solid #edf1f6}
.ag-legend{display:flex;gap:10px;flex-wrap:wrap;margin:0 0 10px;font-size:10.5px;color:#475569;font-weight:700}
.ag-legend span{display:inline-flex;align-items:center;gap:5px}
.ag-legend i{width:10px;height:10px;border-radius:3px;display:inline-block}
.ag-planned{position:absolute;left:0;right:0;background:#9AA5B5;z-index:1;pointer-events:none}
.ag-card{position:absolute;left:64px;right:6px;border-radius:6px;padding:8px 12px;z-index:3;overflow:hidden}
.ag-card .ft{font-weight:800;font-size:13px;color:#fff;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.ag-card .fm{font-size:11px;opacity:.92;margin-top:2px;font-family:'Inter',system-ui,sans-serif;color:#fff;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.ag-card.compact{display:flex;align-items:center;gap:8px;padding:0 12px}
.ag-card.compact .fm{margin-top:0;opacity:.85}
.ag-card.online{background:#22C55E}
.ag-card.pessoal{background:#7C3AED;cursor:grab}
.ag-card.operacional{background:#3B82F6;cursor:grab}
.ag-card[data-ev-id]{cursor:grab;touch-action:none}
.ag-card[data-ev-id].dragging{cursor:grabbing}
.ag-rsz{position:absolute;left:0;right:0;height:14px;z-index:5;touch-action:none}
.ag-rsz.top{top:-4px;cursor:ns-resize}
.ag-rsz.bot{bottom:-4px;cursor:ns-resize}
.ag-card.dragging{outline:2px solid #fff;outline-offset:-2px}
.ag-add-row{display:flex;gap:10px;margin-top:14px}
.ag-addbtn{flex:1;height:50px;border:1.5px dashed #dbe3ec;border-radius:18px;background:#f8fafc;color:#667085;font-size:13px;font-weight:700;cursor:pointer;touch-action:manipulation;font-family:var(--font-app);display:flex;align-items:center;justify-content:center;gap:7px}
.ag-addbtn:active{background:#f1f5f9}
/* Modal Novo Compromisso */
#mo-novo-ev .sheet{border-radius:32px 32px 0 0;background:#f7f9fc}
.ag-sheet-head{display:flex;justify-content:space-between;align-items:center;margin-bottom:4px}
.ag-sheet-head h2{font-size:22px;font-weight:800;letter-spacing:-.04em}
.ag-close{width:38px;height:38px;border:0;background:#fff;border-radius:14px;box-shadow:0 4px 12px rgba(15,23,42,.07);color:#344054;font-size:18px;cursor:pointer;display:flex
===== #F59E0B @ 93057 =====
px;background:#22c55e;color:#fff;font-size:15px;font-weight:800;width:100%;box-shadow:0 12px 26px rgba(34,197,94,.25);margin-top:4px;cursor:pointer;touch-action:manipulation;font-family:var(--font-app)}
.ag-del{height:48px;border:0;border-radius:18px;background:#fdecef;color:#ef4444;font-size:14px;font-weight:800;width:100%;margin-top:10px;cursor:pointer;touch-action:manipulation;font-family:var(--font-app)}
.pj-days{display:flex;gap:6px}
.pj-day{flex:1;height:42px;border:1.5px solid #e4eaf2;border-radius:12px;background:#f8fafc;color:#667085;font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation;font-family:var(--font-app)}
.pj-day.on{background:#e6f5ff;border-color:#bee3f8;color:#0ea5e9}
.pj-sched-row{display:flex;align-items:center;gap:8px;margin-bottom:8px}
.pj-sched-row .ag-input{height:46px}
.pj-sched-row span{color:var(--muted);font-weight:700;flex-shrink:0}
.pj-sched-del{width:36px;height:36px;border:0;border-radius:10px;background:#FEF2F2;color:#EF4444;font-size:13px;cursor:pointer;touch-action:manipulation;flex-shrink:0}
.pj-card{background:#fff;border:1px solid rgba(15,23,42,.06);border-radius:18px;padding:13px 14px;margin-bottom:10px;box-shadow:0 6px 18px rgba(15,23,42,.05)}
.pj-card-top{display:flex;justify-content:space-between;align-items:center;margin-bottom:6px}
.pj-card-days{font-size:13.5px;font-weight:800;color:#101828}
.pj-card-del{width:28px;height:28px;border:0;border-radius:9px;background:#F8FAFC;color:#667085;cursor:pointer;touch-action:manipulation;font-size:12px}
.pj-card-goal{font-size:12px;color:#667085;font-weight:600;margin-bottom:8px}
.pj-card-times{display:flex;gap:6px;flex-wrap:wrap}
.pj-chip{background:#F0F9FF;color:#0EA5E9;font-size:11px;font-weight:700;padding:5px 9px;border-radius:999px;font-family:'Inter',system-ui,sans-serif}
.ag-save:active{opacity:.9}


/* ── Agenda Calendário ── */
.agcal-wrap{padding:0 16px 24px}
.agcal-month{display:flex;align-items:center;justify-content:space-between;margin:6px 0 16px}
.agcal-month h2{font-size:20px;font-weight:800;letter-spacing:-.04em}
.agcal-marr{width:36px;height:36px;border:1px solid #e4eaf2;border-radius:12px;background:#fff;color:#344054;font-size:18px;cursor:pointer;touch-action:manipulation;display:flex;align-items:center;justify-content:center;box-shadow:0 2px 8px rgba(15,23,42,.05)}
.agcal-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:4px;margin-bottom:16px}
.agcal-wd{font-size:10px;font-weight:800;text-align:center;color:#94A3B8;letter-spacing:.07em;padding:4px 0}
.agcal-day{min-height:52px;border-radius:16px;display:flex;flex-direction:column;align-items:center;justify-content:flex-start;padding:7px 4px 4px;cursor:pointer;touch-action:manipulation;background:#fff;border:1px solid transparent;transition:all .12s}
.agcal-day:active{background:#f0f9ff}
.agcal-day.muted{background:transparent;border-color:transparent;box-shadow:none}
.agcal-day.muted span{color:#CBD5E1}
.agcal-day.today-d span{color:#0ea5e9;font-weight:800}
.agcal-day.today-d{border-color:rgba(14,165,233,.25);background:rgba(14,165,233,.05)}
.agcal-day.sel{background:#0ea5e9;border-color:#0ea5e9}
.agcal-day.sel span{color:#fff;font-weight:800}
.agcal-day span{font-size:14px;font-weight:600;color:#101828;line-height:1}
.agcal-dots{display:flex;gap:3px;margin-top:4px;flex-wrap:wrap;justify-content:center}
.agcal-dot{width:6px;height:6px;border-radius:50%}
.agcal-dot.green{background:#22c55e}.agcal-dot.blue{background:#3b82f6}
.agcal-dot.orange{background:#f59e0b}.agcal-dot.purple{background:#8b5cf6}
.agcal-dot.red{background:#ef4444}.agcal-dot.gray{background:#CBD5E1}
.agcal-sel-card{background:#fff;border:1px solid rgba(15,23,42,.06);border-radius:24px;box-shadow:0 8px 24px rgba(15,23,42,.06);overflow:hidden;margin-bottom:14px}
.agcal-sel-head{display:flex;align-items:center;justify-content:space-between;padding:14px 16px 10px}
.agcal-sel-head h3{font-size:17px;font-weight:800;letter-spacing:-.03em}
.agcal-sel-head p{font-size:12px;color:#667085;margin-top:2px}
.agcal-ev{display:flex;align-items:center;gap:12px;padding:11px 16px;border-top:1px solid #f1f5f9}
.agcal-ev-bar{width:4px;height:40px;border-radius:4px;flex-shrink:0}
.agcal-ev-ico{width:36px;height:36px;border-radius:12px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.agcal-ev-ico svg{width:18px;height:18px;stroke:currentColor;fill:none;stroke-width:2.25;stroke-linecap:round;stroke-linejoin:round}
.agcal-ev-title{font-size:14px;font-weight:700;letter-spacing:-.02em}
.agcal-ev-meta{font-size:11px;color:#667085;margin-top:2px}
.agcal-empty{padding:20px;text-align:center;color:#94A3B8;font-size:13px;border-top:1px solid #f1f5f9}
.agcal-fab{position:sticky;bottom:80px;float:right;margin-right:0;width:52px;height:52px;border-radius:50%;background:#22c55e;border:none;color:#fff;font-size:26px;font-weight:300;cursor:pointer;touch-action:manipulation;box-shadow:0 10px 24px rgba(34,197,94,.3);display:flex;align-items:center;justify-content:center;line-height:1;margin-bottom:8px}

/* ── HOME GRAFICOS (hg-) ────────────────────────────────────────── */
.hg-period-card{background:radial-gradient(circle at 16% 0%,rgba(255,255,255,.14),transparent 34%),radial-gradient(circle at 92% 88%,rgba(34,197,94,.15),transparent 34%),linear-gradient(135deg,#061830 0%,#0A2F6B 55%,#0d3d87 100%);color:#fff;border-radius:32px;padding:18px;box-shadow:0 22px 54px rgba(14,165,233,.22),inset 0 1px 0 rgba(255,255,255,.16)}
.hg-period-top{display:flex;align-items:center;margin-bottom:20px}
.hg-period-tabs{height:40px;background:rgba(255,255,255,.16);border:1px solid rgba(255,255,255,.18);border-radius:999px;padding:4px;display:flex;gap:4px;flex:1;margin-right:10px}
.hg-period-tabs button{flex:1;border:0;background:transparent;color:rgba(255,255,255,.76);font-size:12px;font-weight:720;border-radius:999px;cursor:pointer;font-family:var(--font-app)}
.hg-period-tabs button.active{background:#fff;color:#101828;box-shadow:0 8px 18px rgba(15,23,42,.08)}.hg-insight{display:none;align-items:flex-start;gap:9px;margin-top:14px;padding-top:13px;border-top:1px solid rgba(255,255,255,.13)}.hg-insight p{font-size:12.5px;font-style:italic;font-weight:500;opacity:.72;line-height:1.5;color:inherit}
.hg-filter-btn{width:40px;height:40px;border:1px solid rgba(255,255,255,.2);border-radius:15px;background:rgba(255,255,255,.12);color:#fff;display:flex;align-items:center;justify-content:center;cursor:pointer;flex-shrink:0}
.hg-filter-btn svg{width:18px;height:18px;stroke:currentColor;fill:none;stroke-width:2;stroke-linecap:round}
.hg-main-value{text-align:center;margin-bottom:18px}
.hg-main-value label{display:block;font-size:11px;text-transform:uppercase;letter-spacing:.14em;font-weight:720;opacity:.72;margin-bottom:8px}
.hg-main-value h1{font-size:40px;line-height:1;font-weight:800;letter-spacing:-.04em}
.hg-main-value p{font-size:13px;font-weight:520;opacity:.78;margin-top:7px}
.hg-metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}
.hg-metric{background:rgba(255,255,255,.115);border:1px solid rgba(255,255,255,.15);border-radius:18px;padding:10px 7px;text-align:center}
.hg-metric strong{display:block;font-size:15px;font-weight:760;font-family:'Inter',system-ui,sans-serif}
.hg-metric span{display:block;font-size:9.5px;font-weight:720;text-transform:uppercase;letter-spacing:.055em;opacity:.74;margin-top:4px}
.hg-goals{margin-top:14px;background:rgba(255,255,255,.11);border:1px solid rgba(255,255,255,.15);border-radius:18px;padding:11px;display:flex;flex-direction:column;gap:11px}
.hg-goal-row{display:flex;justify-content:space-between;font-size:12px;font-weight:650;opacity:.88;margin-bottom:7px}
.hg-goal-bar{height:14px;background:rgba(255,255,255,.20);border-radius:999px;overflow:hidden}
.hg-goal-bar i{display:block;height:100%;border-radius:999px}
.hg-goal-bar.money i{background:linear-gradient(90deg,#f59e0b,#22c55e)}
.hg-goal-bar.time i{background:linear-gradient(90deg,#0ea5e9,#22c55e)}
.hg-goal-bar.km i{background:linear-gradient(90deg,#8b5cf6,#0ea5e9)}
.hg-goal-locked{display:flex;justify-content:space-between;align-items:center;font-size:12px;font-weight:650;opacity:.85;cursor:pointer}
.hg-goal-locked b{color:#fff;text-decoration:underline;font-weight:760}
.hg-chart-card{background:var(--card);border:1px solid var(--line);border-radius:26px;box-shadow:0 12px 32px rgba(15,23,42,.065);padding:16px}
html.dark .hg-chart-card{box-shadow:0 12px 28px rgba(0,0,0,.35)}
.hg-chart-head{display:flex;align-items:flex-start;justify-content:space-between;gap:10px;margin-bottom:12px}
.hg-chart-head h2{font-size:16.5px;font-weight:760;letter-spacing:-.025em;color:var(--text)}
.hg-info-btn{width:18px;height:18px;border-radius:50%;border:1.5px solid var(--muted2);background:transparent;color:var(--muted2);font-size:11px;font-weight:800;line-height:1;cursor:pointer;display:inline-flex;align-items:center;justify-content:center;vertical-align:middle;margin-left:2px;padding:0;touch-action:manipulation;font-family:var(--font-app)}
.hg-info-tip{position:fixed;left:0;top:0;max-width:240px;background:var(--card);border:1px solid var(--line);color:var(--text);padding:9px 13px;border-radius:13px;font-size:12px;font-weight:600;line-height:1.4;text-align:left;pointer-events:none;opacity:0;transform:translateY(4px);transition:opacity .15s,transform .15s;z-index:50;box-shadow:0 8px 20px rgba(15,23,42,.16)}
.hg-info-tip.show{opacity:1;transform:translateY(0)}
.hg-info-tip:after{content:'';position:absolute;top:100%;left:var(--hg-tip-arrow,20px);border:6px solid transparent;border-top-color:var(--card)}
.hg-chart-head p{font-size:12px;color:var(--muted);font-weight:500;margin-top:3px;line-height:1.35}
.hg-mini-tabs,.hg-category-tabs{height:34px;background:var(--soft-line);border:1px solid var(--line);border-radius:999px;padding:3px;display:flex;gap:3px;flex:0 0 auto}
.hg-mini-tabs button,.hg-category-tabs button{min-width:48px;border:0;background:transparent;color:var(--muted);font-size:11px;font-weight:720;border-r
===== #F59E0B @ 97431 =====
ill:none;stroke-width:2.25;stroke-linecap:round;stroke-linejoin:round}
.agcal-ev-title{font-size:14px;font-weight:700;letter-spacing:-.02em}
.agcal-ev-meta{font-size:11px;color:#667085;margin-top:2px}
.agcal-empty{padding:20px;text-align:center;color:#94A3B8;font-size:13px;border-top:1px solid #f1f5f9}
.agcal-fab{position:sticky;bottom:80px;float:right;margin-right:0;width:52px;height:52px;border-radius:50%;background:#22c55e;border:none;color:#fff;font-size:26px;font-weight:300;cursor:pointer;touch-action:manipulation;box-shadow:0 10px 24px rgba(34,197,94,.3);display:flex;align-items:center;justify-content:center;line-height:1;margin-bottom:8px}

/* ── HOME GRAFICOS (hg-) ────────────────────────────────────────── */
.hg-period-card{background:radial-gradient(circle at 16% 0%,rgba(255,255,255,.14),transparent 34%),radial-gradient(circle at 92% 88%,rgba(34,197,94,.15),transparent 34%),linear-gradient(135deg,#061830 0%,#0A2F6B 55%,#0d3d87 100%);color:#fff;border-radius:32px;padding:18px;box-shadow:0 22px 54px rgba(14,165,233,.22),inset 0 1px 0 rgba(255,255,255,.16)}
.hg-period-top{display:flex;align-items:center;margin-bottom:20px}
.hg-period-tabs{height:40px;background:rgba(255,255,255,.16);border:1px solid rgba(255,255,255,.18);border-radius:999px;padding:4px;display:flex;gap:4px;flex:1;margin-right:10px}
.hg-period-tabs button{flex:1;border:0;background:transparent;color:rgba(255,255,255,.76);font-size:12px;font-weight:720;border-radius:999px;cursor:pointer;font-family:var(--font-app)}
.hg-period-tabs button.active{background:#fff;color:#101828;box-shadow:0 8px 18px rgba(15,23,42,.08)}.hg-insight{display:none;align-items:flex-start;gap:9px;margin-top:14px;padding-top:13px;border-top:1px solid rgba(255,255,255,.13)}.hg-insight p{font-size:12.5px;font-style:italic;font-weight:500;opacity:.72;line-height:1.5;color:inherit}
.hg-filter-btn{width:40px;height:40px;border:1px solid rgba(255,255,255,.2);border-radius:15px;background:rgba(255,255,255,.12);color:#fff;display:flex;align-items:center;justify-content:center;cursor:pointer;flex-shrink:0}
.hg-filter-btn svg{width:18px;height:18px;stroke:currentColor;fill:none;stroke-width:2;stroke-linecap:round}
.hg-main-value{text-align:center;margin-bottom:18px}
.hg-main-value label{display:block;font-size:11px;text-transform:uppercase;letter-spacing:.14em;font-weight:720;opacity:.72;margin-bottom:8px}
.hg-main-value h1{font-size:40px;line-height:1;font-weight:800;letter-spacing:-.04em}
.hg-main-value p{font-size:13px;font-weight:520;opacity:.78;margin-top:7px}
.hg-metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}
.hg-metric{background:rgba(255,255,255,.115);border:1px solid rgba(255,255,255,.15);border-radius:18px;padding:10px 7px;text-align:center}
.hg-metric strong{display:block;font-size:15px;font-weight:760;font-family:'Inter',system-ui,sans-serif}
.hg-metric span{display:block;font-size:9.5px;font-weight:720;text-transform:uppercase;letter-spacing:.055em;opacity:.74;margin-top:4px}
.hg-goals{margin-top:14px;background:rgba(255,255,255,.11);border:1px solid rgba(255,255,255,.15);border-radius:18px;padding:11px;display:flex;flex-direction:column;gap:11px}
.hg-goal-row{display:flex;justify-content:space-between;font-size:12px;font-weight:650;opacity:.88;margin-bottom:7px}
.hg-goal-bar{height:14px;background:rgba(255,255,255,.20);border-radius:999px;overflow:hidden}
.hg-goal-bar i{display:block;height:100%;border-radius:999px}
.hg-goal-bar.money i{background:linear-gradient(90deg,#f59e0b,#22c55e)}
.hg-goal-bar.time i{background:linear-gradient(90deg,#0ea5e9,#22c55e)}
.hg-goal-bar.km i{background:linear-gradient(90deg,#8b5cf6,#0ea5e9)}
.hg-goal-locked{display:flex;justify-content:space-between;align-items:center;font-size:12px;font-weight:650;opacity:.85;cursor:pointer}
.hg-goal-locked b{color:#fff;text-decoration:underline;font-weight:760}
.hg-chart-card{background:var(--card);border:1px solid var(--line);border-radius:26px;box-shadow:0 12px 32px rgba(15,23,42,.065);padding:16px}
html.dark .hg-chart-card{box-shadow:0 12px 28px rgba(0,0,0,.35)}
.hg-chart-head{display:flex;align-items:flex-start;justify-content:space-between;gap:10px;margin-bottom:12px}
.hg-chart-head h2{font-size:16.5px;font-weight:760;letter-spacing:-.025em;color:var(--text)}
.hg-info-btn{width:18px;height:18px;border-radius:50%;border:1.5px solid var(--muted2);background:transparent;color:var(--muted2);font-size:11px;font-weight:800;line-height:1;cursor:pointer;display:inline-flex;align-items:center;justify-content:center;vertical-align:middle;margin-left:2px;padding:0;touch-action:manipulation;font-family:var(--font-app)}
.hg-info-tip{position:fixed;left:0;top:0;max-width:240px;background:var(--card);border:1px solid var(--line);color:var(--text);padding:9px 13px;border-radius:13px;font-size:12px;font-weight:600;line-height:1.4;text-align:left;pointer-events:none;opacity:0;transform:translateY(4px);transition:opacity .15s,transform .15s;z-index:50;box-shadow:0 8px 20px rgba(15,23,42,.16)}
.hg-info-tip.show{opacity:1;transform:translateY(0)}
.hg-info-tip:after{content:'';position:absolute;top:100%;left:var(--hg-tip-arrow,20px);border:6px solid transparent;border-top-color:var(--card)}
.hg-chart-head p{font-size:12px;color:var(--muted);font-weight:500;margin-top:3px;line-height:1.35}
.hg-mini-tabs,.hg-category-tabs{height:34px;background:var(--soft-line);border:1px solid var(--line);border-radius:999px;padding:3px;display:flex;gap:3px;flex:0 0 auto}
.hg-mini-tabs button,.hg-category-tabs button{min-width:48px;border:0;background:transparent;color:var(--muted);font-size:11px;font-weight:720;border-radius:999px;padding:0 10px;cursor:pointer;font-family:var(--font-app)}
.hg-mini-tabs button.active,.hg-category-tabs button.active{background:var(--card);color:var(--text);box-shadow:0 6px 14px rgba(15,23,42,.06)}
html.dark .hg-mini-tabs button.active,html.dark .hg-category-tabs button.active{box-shadow:none;border:1px solid var(--line)}
.hg-category-tabs{margin:2px 0 15px}.hg-category-tabs button{flex:1}
.hg-chart-highlight{font-size:11.5px;font-weight:700;color:#0A2F6B;margin:-4px 0 10px;min-height:14px}
.hg-bar-chart{height:154px;position:relative;margin-top:6px;z-index:1}
.hg-grid-line{position:absolute;left:0;right:0;border-top:1px dashed var(--line)}
.hg-grid-line span{position:absolute;left:0;top:-7px;font-size:9px;color:var(--muted2);font-weight:700;background:var(--card);padding-right:4px}
.hg-bars-row{position:absolute;left:30px;right:0;bottom:0;top:0;display:flex;align-items:flex-end;gap:6px;overflow-x:auto;overflow-y:visible}
.hg-bar-col{flex:1;min-width:0;display:flex;flex-direction:column;align-items:center;gap:7px;height:100%;justify-content:flex-end;position:relative}
.hg-bar{width:100%;border-radius:5px 5px 0 0;background:#0A2F6B;min-height:2px;cursor:pointer;transition:opacity .15s,transform .1s;touch-action:manipulation}
.hg-bar:active{transform:scaleY(.98)}
.hg-bar.dim{opacity:.35}
.hg-bar.sel{background:var(--blue2)}
.hg-bar-col span.hg-bar-lbl{font-size:10px;color:var(--muted);font-weight:650;white-space:nowrap}
.hg-bar-col span.hg-bar-lbl.sel{color:#0A2F6B;font-weight:800}
.hg-bar-bubble{position:absolute;background:var(--card);border:1px solid var(--line);color:var(--text);padding:8px 12px;border-radius:13px;font-size:12px;font-weight:700;white-space:nowrap;pointer-events:none;opacity:0;transition:opacity .15s,transform .15s;z-index:20;text-align:center;box-shadow:0 8px 20px rgba(15,23,42,.16);transform:translateY(4px)}
.hg-bar-bubble.show{opacity:1;transform:translateY(0)}
.hg-bar-bubble:after{content:'';position:absolute;top:100%;left:50%;transform:translateX(-50%);border:6px solid transparent;border-top-color:var(--card)}
.hg-bar-bubble .hg-bb-val{font-size:13.5px;font-weight:800}
.hg-bar-bubble .hg-bb-qtd{font-size:10.5px;color:var(--muted);font-weight:700}
.hg-bar-bubble .hg-bb-row{font-size:10.5px;color:var(--muted);font-weight:650;margin-top:3px;white-space:nowrap}
.hg-bar-chart.hour .hg-bar{background:#22c55e}
.hg-bar-chart.month .hg-bar{background:#8b5cf6}
.hg-bar-empty{width:100%;text-align:center;font-size:12px;color:#94A3B8;padding:50px 0}
.hg-pie-wrap{display:grid;grid-template-columns:118px 1fr;gap:16px;align-items:center;padding-top:4px}
.hg-pie{width:118px;height:118px;border-radius:50%;position:relative;box-shadow:inset 0 0 0 1px rgba(15,23,42,.05);flex-shrink:0}
.hg-pie:after{content:"";position:absolute;inset:30px;background:#fff;border-radius:50%}
.hg-pie-list{display:flex;flex-direction:column;gap:8px}
.hg-pie-item{display:flex;align-items:center;justify-content:space-between;font-size:12px;color:#344054;font-weight:650}
.hg-pie-item div{display:flex;align-items:center;gap:7px}
.hg-pie-item i{width:9px;height:9px;border-radius:50%;display:block;flex-shrink:0}
.hg-pie-item strong{font-size:12.5px;color:#101828}
.hg-category-panel{display:flex;flex-direction:column;gap:11px;margin-top:14px}
.hg-cat-row{display:grid;grid-template-columns:86px 1fr 44px;gap:10px;align-items:center}
.hg-cat-name{font-size:12px;color:#344054;font-weight:650;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.hg-cat-value{text-align:right;font-size:12px;color:#101828;font-weight:720}
.hg-cat-track{height:12px;background:#eef2f7;border-radius:999px;overflow:hidden}
.hg-cat-track i{display:block;height:100%;border-radius:999px}
.hg-filter-row{display:flex;gap:8px}
.hg-filter-row input{flex:1;height:44px;border:1.5px solid #E2E8F0;border-radius:12px;padding:0 12px;font-size:13px;font-family:var(--font-app)}
/* ── INFORMES ── */
#sc-informes{padding:0;background:var(--bg);overflow-y:auto}
.inf-topbar{display:flex;align-items:center;gap:6px;padding:9px 14px 7px;background:var(--bg);position:sticky;top:0;z-index:10}
.inf-home-btn-blue{width:34px;height:34px;border-radius:10px;border:0;background:#0A2F6B;display:flex;align-items:center;justify-content:center;cursor:pointer;flex-shrink:0}
.inf-search-fixed{flex:1;display:flex;align-items:center;height:34px;border-radius:10p
===== #F59E0B @ 121002 =====
inf-vbtn.dn.on{color:var(--red)}
.inf-empty-feed{padding:18px 14px;text-align:center;color:var(--muted);font-size:12.5px;font-weight:600}
/* áreas atendidas (bairros) — chips maiores, roláveis, agrupadas */
.inf-area-grp-lbl{display:flex;align-items:center;gap:6px;padding:8px 14px 6px}
.inf-area-grp-name{font-size:12.5px;font-weight:800;color:var(--text)}
.inf-area-grp-dot{width:6px;height:6px;border-radius:50%;background:#22C55E}
.inf-bchip-scroll{overflow-x:auto;-webkit-overflow-scrolling:touch;scrollbar-width:none;padding:0 14px}
.inf-bchip-scroll::-webkit-scrollbar{display:none}
.inf-bchip-scroll.inf-fill-scroll{flex:1;display:flex;align-items:stretch}
.inf-bchip-2row{display:grid;grid-template-rows:repeat(2,auto);grid-auto-flow:column;grid-auto-columns:192px;gap:10px;min-width:max-content}
.inf-bchip-1row{display:grid;grid-template-rows:repeat(1,auto);grid-auto-flow:column;grid-auto-columns:208px;gap:10px;min-width:max-content}
.inf-fill-scroll .inf-bchip-1row{height:100%}
.inf-bchip{border-radius:16px!important;padding:16px 15px 15px!important;gap:9px!important}
.inf-fill-scroll .inf-bchip{height:100%;justify-content:center}
.inf-bchip-ic{width:36px!important;height:36px!important;border-radius:11px!important}
.inf-bchip-name{font-size:14px!important;line-height:1.25!important}
.inf-bchip-ct{font-size:10px!important;padding:2px 7px!important}
/* listas de venues (bares/hotéis/eventos/turístico/shoppings) */
.inf-vlist{padding:0 14px}
.inf-vitem{display:flex;align-items:center;gap:10px;padding:10px 12px;background:var(--card);border:1.5px solid var(--line);border-radius:13px;margin-bottom:7px;cursor:pointer}
.inf-vitem:active{background:var(--soft)}
.inf-vitem-ic{width:30px;height:30px;border-radius:9px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-vitem-name{font-size:12.5px;font-weight:700}
.inf-vitem-sub{font-size:10.5px;color:var(--muted);margin-top:1px}
.inf-vitem-ct{margin-left:auto;font-size:9px;font-weight:800;background:var(--red);color:#fff;border-radius:99px;padding:2px 7px;flex-shrink:0}
.inf-vitem-ct.z{background:var(--line);color:var(--muted)}
.inf-ccard-ct{position:absolute;top:5px;right:5px;font-size:8px;font-weight:800;background:var(--red);color:#fff;border-radius:99px;padding:1px 4px}
/* painel de informes full-screen */
.inf-panel{position:fixed;left:0;right:0;top:100%;bottom:0;background:var(--card);z-index:110;transition:top .3s cubic-bezier(.4,0,.2,1);display:flex;flex-direction:column}
.inf-panel.open{top:0;bottom:0}
.inf-panel-drag{flex-shrink:0;padding:10px 0 6px;display:flex;align-items:center;justify-content:center;cursor:grab;touch-action:none}
.inf-panel-drag-bar{width:40px;height:4px;border-radius:2px;background:var(--line)}
.inf-panel-head{padding:6px 14px 10px;border-bottom:1px solid var(--line);flex-shrink:0;display:flex;align-items:center;gap:9px}
.inf-panel-hic{width:34px;height:34px;border-radius:10px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-panel-hbody{flex:1;min-width:0}
.inf-panel-tipo{font-size:9px;font-weight:800;text-transform:uppercase;letter-spacing:.07em}
.inf-panel-loc{font-size:15px;font-weight:800;margin-top:1px}
.inf-panel-meta{font-size:10.5px;color:var(--muted);margin-top:1px}
.inf-panel-fav{width:34px;height:34px;border-radius:9px;border:1.5px solid var(--line);background:var(--soft);display:flex;align-items:center;justify-content:center;cursor:pointer;flex-shrink:0;transition:all .15s}
.inf-panel-fav.on{border-color:#F59E0B;background:rgba(245,158,11,.1)}
/* status 2+3 */
.inf-status{padding:9px 14px 4px;flex-shrink:0}
.inf-st-r1{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-bottom:7px}
.inf-st-r2{display:grid;gap:7px}
.inf-chip{border-radius:13px;padding:0;text-align:left;border:1px solid transparent}
.inf-chip.big{padding:11px 12px}
.inf-chip.sm{padding:8px 10px}
.inf-chip.ok{background:#DCFCE7;border-color:#BBF7D0}
.inf-chip.warn{background:#FEF3C7;border-color:#FDE68A}
.inf-chip.bad{background:#FEE2E2;border-color:#FECACA}
.inf-chip.neu{background:var(--soft);border-color:var(--line)}
.inf-chip-lbl{font-size:8.5px;font-weight:800;opacity:.65;display:block;text-transform:uppercase;letter-spacing:.06em;margin-bottom:3px}
.inf-chip.big .inf-chip-lbl{font-size:9px;margin-bottom:4px}
.inf-chip-val{font-weight:800;line-height:1.1;display:block}
.inf-chip.big .inf-chip-val{font-size:17px}
.inf-chip.sm .inf-chip-val{font-size:12.5px}
.inf-chip.ok .inf-chip-lbl,.inf-chip.ok .inf-chip-val{color:#15803D}
.inf-chip.warn .inf-chip-lbl,.inf-chip.warn .inf-chip-val{color:#92400E}
.inf-chip.bad .inf-chip-lbl,.inf-chip.bad .inf-chip-val{color:#991B1B}
.inf-chip.neu .inf-chip-lbl,.inf-chip.neu .inf-chip-val{color:var(--muted)}
/* lista de informes */
.inf-sep{height:1px;background:var(--line);margin:4px 14px 0;flex-shrink:0}
.inf-list-wrap{flex:1;overflow-y:auto;padding:0 14px}
/* Card de informe — versão final aprovada (16/08/2026) */
.inf-post-card{background:var(--card);border:1px solid var(--line);border-radius:16px;padding:2px 14px}
.inf-post-row{padding:12px 0;border-bottom:1px solid #F1F5F9}
.inf-post-row:last-child{border-bottom:0}
.inf-post-top{display:flex;align-items:flex-start;gap:11px}
.inf-post-typecol{display:flex;flex-direction:column;align-items:center;gap:3px;flex-shrink:0;width:44px}
.inf-post-time{font-size:9px;font-weight:800;color:var(--muted)}
.inf-post-ic{width:38px;height:38px;border-radius:12px;display:flex;align-items:center;justify-content:center}
.inf-post-typelbl{font-size:7.5px;font-weight:800;text-align:center;line-height:1.1}
.inf-post-body{flex:1;min-width:0;padding-top:1px}
.inf-post-txt{font-size:13px;font-weight:800}
.inf-post-locchip{display:inline-flex;align-items:center;gap:5px;font-size:11px;font-weight:700;color:#1E40AF;background:#EFF6FF;padding:3px 9px;border-radius:8px;margin-top:5px;max-width:100%}
.inf-post-locchip span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.inf-post-actions{display:flex;align-items:center;gap:14px;margin-top:8px}
.inf-post-act{display:flex;align-items:center;gap:4px;font-size:11px;font-weight:700;color:var(--muted);cursor:pointer;background:none;border:0;font-family:inherit;padding:0;touch-action:manipulation}
.inf-post-act.on-like{color:#22C55E}
.inf-post-act.on-dislike{color:var(--red)}
.inf-post-avs{display:flex;align-items:center;flex-direction:row-reverse;flex-shrink:0;margin-top:2px}
.inf-post-av{width:32px;height:32px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:10.5px;font-weight:800;color:#fff;border:2.5px solid var(--card);box-shadow:0 1px 3px rgba(15,23,42,.16);margin-left:-10px}
.inf-post-av.more{background:#94A3B8}
.inf-post-av:last-child{margin-left:0}
/* Expansão: motivo do "não curtir" */
.inf-post-dislike-panel{margin-top:11px;margin-left:55px;padding:12px;background:#FEF2F2;border:1.5px solid #FECACA;border-radius:13px}
.inf-post-dislike-q{font-size:11.5px;font-weight:800;color:#991B1B;margin-bottom:9px}
.inf-post-dislike-opts{display:flex;gap:8px}
.inf-post-dislike-opt{flex:1;padding:9px 6px;border-radius:10px;border:1.5px solid #FECACA;background:#fff;font-size:11px;font-weight:700;color:#B91C1C;text-align:center;cursor:pointer;font-family:inherit;touch-action:manipulation}
/* Expansão: comentários */
.inf-post-comments{margin-top:11px;margin-left:55px;padding-top:11px;border-top:1px solid #F1F5F9}
.inf-comment-item{display:flex;gap:8px;margin-bottom:9px}
.inf-comment-av{width:22px;height:22px;border-radius:50%;background:#DBEAFE;color:#2563EB;font-size:8px;font-weight:800;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-comment-txt{font-size:11px;line-height:1.4}
.inf-comment-txt b{font-weight:800}
.inf-comment-input-row{display:flex;gap:8px;align-items:center;margin-top:4px}
.inf-comment-input{flex:1;border:1.5px solid var(--line);border-radius:10px;padding:8px 11px;font-size:11px;font-family:inherit;outline:none}
.inf-comment-send{width:32px;height:32px;border-radius:10px;background:transparent;display:flex;align-items:center;justify-content:center;flex-shrink:0;border:0;cursor:pointer;touch-action:manipulation}
/* ── Badges Alta demanda / Tarifa dinâmica — header e listagem
   compartilham o MESMO visual (ícone grande + texto embaixo) ── */
.inf-badge-btn{width:44px;height:44px;border-radius:12px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:1px;flex-shrink:0}
.inf-badge-btn span{font-size:6.5px;font-weight:800;color:#fff;line-height:1;text-align:center}
.inf-badge-btn.peak{background:linear-gradient(135deg,#F59E0B,#EA580C)}
.inf-badge-btn.dyn{background:linear-gradient(135deg,#FBBF24,#F59E0B)}
.inf-vitem-badges{display:flex;gap:5px;flex-shrink:0;margin-left:auto}
.inf-vitem-badges .inf-badge-btn{width:32px;height:32px;border-radius:9px}
.inf-vitem-badges .inf-badge-btn span{font-size:4.6px;margin-top:1px}
/* ── Painel Agora — 5 indicadores ao vivo ── */
.inf-agora-hd{display:flex;align-items:center;gap:6px;font-size:11px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.05em;margin:2px 0 8px}
.inf-live-dot{width:6px;height:6px;border-radius:50%;background:#22C55E;animation:infPulseLive 1.2s infinite}
@keyframes infPulseLive{0%,100%{opacity:1}50%{opacity:.3}}
.inf-live-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:16px}
.inf-live-tile{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:11px 12px}
.inf-live-tile-lbl{font-size:9.5px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.04em}
.inf-live-tile-val{font-size:14px;font-weight:800;margin-top:4px;color:var(--text);line-height:1.2}
.inf-live-tile-val.empty{color:#CBD5E1;font-weight:700}
.inf-live-tile-age{font-size:9.5px;color:var(--muted);margin-top:2px;font-weight:600}
/* Dinâmico — 2 cards, Uber e 99, com logo real de cada plataforma */
.inf-dyn-grid{grid-column:1/-1;display:grid;grid-template-columns:1fr 1fr;gap:8px}
.inf-dyn-tile{backgrou
===== #F59E0B @ 126096 =====
px solid #F1F5F9}
.inf-post-row:last-child{border-bottom:0}
.inf-post-top{display:flex;align-items:flex-start;gap:11px}
.inf-post-typecol{display:flex;flex-direction:column;align-items:center;gap:3px;flex-shrink:0;width:44px}
.inf-post-time{font-size:9px;font-weight:800;color:var(--muted)}
.inf-post-ic{width:38px;height:38px;border-radius:12px;display:flex;align-items:center;justify-content:center}
.inf-post-typelbl{font-size:7.5px;font-weight:800;text-align:center;line-height:1.1}
.inf-post-body{flex:1;min-width:0;padding-top:1px}
.inf-post-txt{font-size:13px;font-weight:800}
.inf-post-locchip{display:inline-flex;align-items:center;gap:5px;font-size:11px;font-weight:700;color:#1E40AF;background:#EFF6FF;padding:3px 9px;border-radius:8px;margin-top:5px;max-width:100%}
.inf-post-locchip span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.inf-post-actions{display:flex;align-items:center;gap:14px;margin-top:8px}
.inf-post-act{display:flex;align-items:center;gap:4px;font-size:11px;font-weight:700;color:var(--muted);cursor:pointer;background:none;border:0;font-family:inherit;padding:0;touch-action:manipulation}
.inf-post-act.on-like{color:#22C55E}
.inf-post-act.on-dislike{color:var(--red)}
.inf-post-avs{display:flex;align-items:center;flex-direction:row-reverse;flex-shrink:0;margin-top:2px}
.inf-post-av{width:32px;height:32px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:10.5px;font-weight:800;color:#fff;border:2.5px solid var(--card);box-shadow:0 1px 3px rgba(15,23,42,.16);margin-left:-10px}
.inf-post-av.more{background:#94A3B8}
.inf-post-av:last-child{margin-left:0}
/* Expansão: motivo do "não curtir" */
.inf-post-dislike-panel{margin-top:11px;margin-left:55px;padding:12px;background:#FEF2F2;border:1.5px solid #FECACA;border-radius:13px}
.inf-post-dislike-q{font-size:11.5px;font-weight:800;color:#991B1B;margin-bottom:9px}
.inf-post-dislike-opts{display:flex;gap:8px}
.inf-post-dislike-opt{flex:1;padding:9px 6px;border-radius:10px;border:1.5px solid #FECACA;background:#fff;font-size:11px;font-weight:700;color:#B91C1C;text-align:center;cursor:pointer;font-family:inherit;touch-action:manipulation}
/* Expansão: comentários */
.inf-post-comments{margin-top:11px;margin-left:55px;padding-top:11px;border-top:1px solid #F1F5F9}
.inf-comment-item{display:flex;gap:8px;margin-bottom:9px}
.inf-comment-av{width:22px;height:22px;border-radius:50%;background:#DBEAFE;color:#2563EB;font-size:8px;font-weight:800;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-comment-txt{font-size:11px;line-height:1.4}
.inf-comment-txt b{font-weight:800}
.inf-comment-input-row{display:flex;gap:8px;align-items:center;margin-top:4px}
.inf-comment-input{flex:1;border:1.5px solid var(--line);border-radius:10px;padding:8px 11px;font-size:11px;font-family:inherit;outline:none}
.inf-comment-send{width:32px;height:32px;border-radius:10px;background:transparent;display:flex;align-items:center;justify-content:center;flex-shrink:0;border:0;cursor:pointer;touch-action:manipulation}
/* ── Badges Alta demanda / Tarifa dinâmica — header e listagem
   compartilham o MESMO visual (ícone grande + texto embaixo) ── */
.inf-badge-btn{width:44px;height:44px;border-radius:12px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:1px;flex-shrink:0}
.inf-badge-btn span{font-size:6.5px;font-weight:800;color:#fff;line-height:1;text-align:center}
.inf-badge-btn.peak{background:linear-gradient(135deg,#F59E0B,#EA580C)}
.inf-badge-btn.dyn{background:linear-gradient(135deg,#FBBF24,#F59E0B)}
.inf-vitem-badges{display:flex;gap:5px;flex-shrink:0;margin-left:auto}
.inf-vitem-badges .inf-badge-btn{width:32px;height:32px;border-radius:9px}
.inf-vitem-badges .inf-badge-btn span{font-size:4.6px;margin-top:1px}
/* ── Painel Agora — 5 indicadores ao vivo ── */
.inf-agora-hd{display:flex;align-items:center;gap:6px;font-size:11px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.05em;margin:2px 0 8px}
.inf-live-dot{width:6px;height:6px;border-radius:50%;background:#22C55E;animation:infPulseLive 1.2s infinite}
@keyframes infPulseLive{0%,100%{opacity:1}50%{opacity:.3}}
.inf-live-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:16px}
.inf-live-tile{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:11px 12px}
.inf-live-tile-lbl{font-size:9.5px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.04em}
.inf-live-tile-val{font-size:14px;font-weight:800;margin-top:4px;color:var(--text);line-height:1.2}
.inf-live-tile-val.empty{color:#CBD5E1;font-weight:700}
.inf-live-tile-age{font-size:9.5px;color:var(--muted);margin-top:2px;font-weight:600}
/* Dinâmico — 2 cards, Uber e 99, com logo real de cada plataforma */
.inf-dyn-grid{grid-column:1/-1;display:grid;grid-template-columns:1fr 1fr;gap:8px}
.inf-dyn-tile{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:10px 12px;display:flex;align-items:center;gap:8px}
.inf-dyn-tile img{display:block;flex-shrink:0}
.inf-dyn-info{flex:1;min-width:0}
.inf-dyn-val{font-size:13px;font-weight:800;color:var(--text)}
.inf-dyn-val.empty{color:#CBD5E1}
.inf-dyn-age{font-size:8.5px;color:var(--muted);font-weight:600}
/* ── Dicas do local ── */
.inf-tips-card{background:var(--card);border:1px solid var(--line);border-radius:16px;padding:6px 14px;margin-bottom:16px}
.inf-tip-row{display:flex;align-items:center;gap:10px;padding:10px 0;border-bottom:1px solid #F1F5F9}
.inf-tip-row:last-child{border-bottom:0}
.inf-tip-ic{width:28px;height:28px;border-radius:9px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-tip-txt{font-size:12.5px;font-weight:700}
/* ── Horário de alta demanda ── */
.inf-peak-card{background:var(--card);border:1px solid var(--line);border-radius:16px;padding:14px;margin-bottom:16px}
.inf-peak-bars{display:flex;align-items:flex-end;gap:5px;height:60px;margin-top:6px}
.inf-peak-bar-wrap{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end;height:100%}
.inf-peak-bar{width:100%;border-radius:5px 5px 0 0;background:var(--line);min-height:2px}
.inf-peak-bar.now{background:linear-gradient(180deg,#F59E0B,#EA580C);box-shadow:0 0 0 2px #FDBA74}
.inf-peak-lbl-row{display:flex;gap:5px;margin-top:4px}
.inf-peak-lbl{flex:1;font-size:7.5px;color:var(--muted);font-weight:700;text-align:center}
.inf-peak-now-row{display:flex;gap:5px;margin-top:2px}
.inf-peak-now-cell{flex:1;text-align:center}
.inf-peak-now-mark{font-size:7.5px;color:#EA580C;font-weight:800}
/* ── Colabore com a comunidade ── */
.inf-collab-banner{display:flex;align-items:center;gap:11px;background:linear-gradient(135deg,#0A2F6B,#0D3A7D);border-radius:16px;padding:13px 14px;color:#fff;margin-bottom:16px;cursor:pointer;border:0;width:100%;font-family:inherit;text-align:left}
.inf-collab-ic{width:36px;height:36px;border-radius:11px;background:rgba(255,255,255,.15);display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-collab-txt b{display:block;font-size:12.5px;font-weight:800}
.inf-collab-txt span{font-size:10.5px;color:rgba(255,255,255,.7)}
/* Modal do formulário */
.inf-qblock{margin-bottom:20px}
.inf-qnum{display:inline-flex;align-items:center;justify-content:center;width:18px;height:18px;border-radius:50%;background:var(--blue);color:#fff;font-size:9.5px;font-weight:800;margin-right:6px;flex-shrink:0}
.inf-qtitle{font-size:13px;font-weight:800}
.inf-qopts2{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px}
.inf-qopt{padding:13px 8px;border-radius:13px;border:1.5px solid var(--line);background:var(--soft);font-size:12.5px;font-weight:700;text-align:center;color:var(--text);cursor:pointer;touch-action:manipulation}
.inf-qopt.picked{background:var(--blue);border-color:var(--blue);color:#fff}
.inf-qsub-lbl{font-size:10.5px;color:#94A3B8;font-weight:700;margin:12px 0 8px;padding-left:2px}
.inf-qsub-opts{display:grid;grid-template-columns:1fr 1fr;gap:8px}
.inf-qsub-opt{padding:13px 8px;border-radius:13px;border:1.5px solid var(--line);background:var(--soft);font-size:12.5px;font-weight:700;text-align:center;color:var(--text);cursor:pointer;touch-action:manipulation}
.inf-qsub-opt.picked{background:#EFF6FF;border-color:#2563EB;color:#1E40AF}
.inf-qtime-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:10px}
.inf-qtime-chip{padding:10px 6px;border-radius:11px;border:1.5px solid var(--line);background:var(--soft);font-size:12px;font-weight:700;text-align:center;color:var(--text);cursor:pointer;touch-action:manipulation}
.inf-qtime-chip.picked{background:var(--blue);border-color:var(--blue);color:#fff}
.inf-qtextarea{width:100%;border:1.5px solid var(--line);border-radius:13px;padding:11px 12px;font-size:12.5px;font-family:inherit;color:var(--text);min-height:64px;resize:none;margin-top:10px}
.inf-qnote{display:flex;align-items:center;gap:5px;font-size:9.5px;color:#94A3B8;margin-top:6px}
.inf-qsend{width:100%;height:50px;border-radius:14px;background:var(--blue);color:#fff;border:0;font-size:14px;font-weight:800;margin-top:6px;cursor:pointer;font-family:inherit}
/* reportar botão */
.inf-rep-wrap{padding:9px 14px 14px;flex-shrink:0}
.inf-rep-btn{width:100%;height:48px;border-radius:14px;border:0;background:#F59E0B;color:#fff;font-size:14px;font-weight:700;font-family:inherit;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:7px}
/* Overlay sheets */
.inf-ov{position:fixed;inset:0;background:rgba(0,0,0,.55);z-index:120;opacity:0;pointer-events:none;transition:opacity .2s;display:flex;align-items:flex-end;justify-content:center}
.inf-ov.open{opacity:1;pointer-events:all}
.inf-sheet{background:var(--card);border-radius:24px 24px 0 0;padding:14px 16px 38px;width:100%;max-width:430px;transform:translateY(100%);transition:transform .28s cubic-bezier(.4,0,.2,1);max-height:88vh;overflow-y:auto}
.inf-ov.open .inf-sheet{transform:translateY(0)}
.inf-sh-handle{width:34px;he
===== #F59E0B @ 128830 =====
{flex:1;border:1.5px solid var(--line);border-radius:10px;padding:8px 11px;font-size:11px;font-family:inherit;outline:none}
.inf-comment-send{width:32px;height:32px;border-radius:10px;background:transparent;display:flex;align-items:center;justify-content:center;flex-shrink:0;border:0;cursor:pointer;touch-action:manipulation}
/* ── Badges Alta demanda / Tarifa dinâmica — header e listagem
   compartilham o MESMO visual (ícone grande + texto embaixo) ── */
.inf-badge-btn{width:44px;height:44px;border-radius:12px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:1px;flex-shrink:0}
.inf-badge-btn span{font-size:6.5px;font-weight:800;color:#fff;line-height:1;text-align:center}
.inf-badge-btn.peak{background:linear-gradient(135deg,#F59E0B,#EA580C)}
.inf-badge-btn.dyn{background:linear-gradient(135deg,#FBBF24,#F59E0B)}
.inf-vitem-badges{display:flex;gap:5px;flex-shrink:0;margin-left:auto}
.inf-vitem-badges .inf-badge-btn{width:32px;height:32px;border-radius:9px}
.inf-vitem-badges .inf-badge-btn span{font-size:4.6px;margin-top:1px}
/* ── Painel Agora — 5 indicadores ao vivo ── */
.inf-agora-hd{display:flex;align-items:center;gap:6px;font-size:11px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.05em;margin:2px 0 8px}
.inf-live-dot{width:6px;height:6px;border-radius:50%;background:#22C55E;animation:infPulseLive 1.2s infinite}
@keyframes infPulseLive{0%,100%{opacity:1}50%{opacity:.3}}
.inf-live-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:16px}
.inf-live-tile{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:11px 12px}
.inf-live-tile-lbl{font-size:9.5px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.04em}
.inf-live-tile-val{font-size:14px;font-weight:800;margin-top:4px;color:var(--text);line-height:1.2}
.inf-live-tile-val.empty{color:#CBD5E1;font-weight:700}
.inf-live-tile-age{font-size:9.5px;color:var(--muted);margin-top:2px;font-weight:600}
/* Dinâmico — 2 cards, Uber e 99, com logo real de cada plataforma */
.inf-dyn-grid{grid-column:1/-1;display:grid;grid-template-columns:1fr 1fr;gap:8px}
.inf-dyn-tile{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:10px 12px;display:flex;align-items:center;gap:8px}
.inf-dyn-tile img{display:block;flex-shrink:0}
.inf-dyn-info{flex:1;min-width:0}
.inf-dyn-val{font-size:13px;font-weight:800;color:var(--text)}
.inf-dyn-val.empty{color:#CBD5E1}
.inf-dyn-age{font-size:8.5px;color:var(--muted);font-weight:600}
/* ── Dicas do local ── */
.inf-tips-card{background:var(--card);border:1px solid var(--line);border-radius:16px;padding:6px 14px;margin-bottom:16px}
.inf-tip-row{display:flex;align-items:center;gap:10px;padding:10px 0;border-bottom:1px solid #F1F5F9}
.inf-tip-row:last-child{border-bottom:0}
.inf-tip-ic{width:28px;height:28px;border-radius:9px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-tip-txt{font-size:12.5px;font-weight:700}
/* ── Horário de alta demanda ── */
.inf-peak-card{background:var(--card);border:1px solid var(--line);border-radius:16px;padding:14px;margin-bottom:16px}
.inf-peak-bars{display:flex;align-items:flex-end;gap:5px;height:60px;margin-top:6px}
.inf-peak-bar-wrap{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end;height:100%}
.inf-peak-bar{width:100%;border-radius:5px 5px 0 0;background:var(--line);min-height:2px}
.inf-peak-bar.now{background:linear-gradient(180deg,#F59E0B,#EA580C);box-shadow:0 0 0 2px #FDBA74}
.inf-peak-lbl-row{display:flex;gap:5px;margin-top:4px}
.inf-peak-lbl{flex:1;font-size:7.5px;color:var(--muted);font-weight:700;text-align:center}
.inf-peak-now-row{display:flex;gap:5px;margin-top:2px}
.inf-peak-now-cell{flex:1;text-align:center}
.inf-peak-now-mark{font-size:7.5px;color:#EA580C;font-weight:800}
/* ── Colabore com a comunidade ── */
.inf-collab-banner{display:flex;align-items:center;gap:11px;background:linear-gradient(135deg,#0A2F6B,#0D3A7D);border-radius:16px;padding:13px 14px;color:#fff;margin-bottom:16px;cursor:pointer;border:0;width:100%;font-family:inherit;text-align:left}
.inf-collab-ic{width:36px;height:36px;border-radius:11px;background:rgba(255,255,255,.15);display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-collab-txt b{display:block;font-size:12.5px;font-weight:800}
.inf-collab-txt span{font-size:10.5px;color:rgba(255,255,255,.7)}
/* Modal do formulário */
.inf-qblock{margin-bottom:20px}
.inf-qnum{display:inline-flex;align-items:center;justify-content:center;width:18px;height:18px;border-radius:50%;background:var(--blue);color:#fff;font-size:9.5px;font-weight:800;margin-right:6px;flex-shrink:0}
.inf-qtitle{font-size:13px;font-weight:800}
.inf-qopts2{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px}
.inf-qopt{padding:13px 8px;border-radius:13px;border:1.5px solid var(--line);background:var(--soft);font-size:12.5px;font-weight:700;text-align:center;color:var(--text);cursor:pointer;touch-action:manipulation}
.inf-qopt.picked{background:var(--blue);border-color:var(--blue);color:#fff}
.inf-qsub-lbl{font-size:10.5px;color:#94A3B8;font-weight:700;margin:12px 0 8px;padding-left:2px}
.inf-qsub-opts{display:grid;grid-template-columns:1fr 1fr;gap:8px}
.inf-qsub-opt{padding:13px 8px;border-radius:13px;border:1.5px solid var(--line);background:var(--soft);font-size:12.5px;font-weight:700;text-align:center;color:var(--text);cursor:pointer;touch-action:manipulation}
.inf-qsub-opt.picked{background:#EFF6FF;border-color:#2563EB;color:#1E40AF}
.inf-qtime-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:10px}
.inf-qtime-chip{padding:10px 6px;border-radius:11px;border:1.5px solid var(--line);background:var(--soft);font-size:12px;font-weight:700;text-align:center;color:var(--text);cursor:pointer;touch-action:manipulation}
.inf-qtime-chip.picked{background:var(--blue);border-color:var(--blue);color:#fff}
.inf-qtextarea{width:100%;border:1.5px solid var(--line);border-radius:13px;padding:11px 12px;font-size:12.5px;font-family:inherit;color:var(--text);min-height:64px;resize:none;margin-top:10px}
.inf-qnote{display:flex;align-items:center;gap:5px;font-size:9.5px;color:#94A3B8;margin-top:6px}
.inf-qsend{width:100%;height:50px;border-radius:14px;background:var(--blue);color:#fff;border:0;font-size:14px;font-weight:800;margin-top:6px;cursor:pointer;font-family:inherit}
/* reportar botão */
.inf-rep-wrap{padding:9px 14px 14px;flex-shrink:0}
.inf-rep-btn{width:100%;height:48px;border-radius:14px;border:0;background:#F59E0B;color:#fff;font-size:14px;font-weight:700;font-family:inherit;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:7px}
/* Overlay sheets */
.inf-ov{position:fixed;inset:0;background:rgba(0,0,0,.55);z-index:120;opacity:0;pointer-events:none;transition:opacity .2s;display:flex;align-items:flex-end;justify-content:center}
.inf-ov.open{opacity:1;pointer-events:all}
.inf-sheet{background:var(--card);border-radius:24px 24px 0 0;padding:14px 16px 38px;width:100%;max-width:430px;transform:translateY(100%);transition:transform .28s cubic-bezier(.4,0,.2,1);max-height:88vh;overflow-y:auto}
.inf-ov.open .inf-sheet{transform:translateY(0)}
.inf-sh-handle{width:34px;height:3px;border-radius:2px;background:var(--line);margin:0 auto 13px}
.inf-sh-head{display:flex;align-items:center;gap:9px;margin-bottom:3px}
.inf-sh-ic{width:32px;height:32px;border-radius:9px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-sh-title{font-size:16px;font-weight:800}
.inf-sh-sub{font-size:11.5px;color:var(--muted);margin-bottom:11px}
.inf-sh-search{position:relative;margin-bottom:11px}
.inf-sh-search svg{position:absolute;left:11px;top:50%;transform:translateY(-50%);opacity:.4;pointer-events:none}
.inf-sh-sinput{width:100%;height:42px;border-radius:12px;border:1.5px solid var(--line);background:var(--soft);font-family:inherit;font-size:13px;font-weight:600;color:var(--text);padding:0 12px 0 36px;outline:none}
.inf-loc-item{display:flex;align-items:center;gap:9px;padding:11px 0;border-bottom:1px solid var(--line);cursor:pointer}
.inf-loc-item:last-child{border-bottom:0}
.inf-loc-item:active{opacity:.6}
.inf-loc-ic{width:30px;height:30px;border-radius:9px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-loc-body{flex:1;min-width:0}
.inf-loc-name{font-size:13.5px;font-weight:700}
.inf-loc-sub{font-size:10px;color:var(--muted);margin-top:1px}
.inf-loc-right{display:flex;flex-direction:column;align-items:flex-end;gap:3px;flex-shrink:0}
.inf-loc-dist{font-size:10px;font-weight:700;color:var(--muted2)}
.inf-loc-ct{font-size:9px;font-weight:800;background:var(--red);color:#fff;border-radius:99px;padding:2px 6px}
.inf-loc-ct.z{background:var(--line);color:var(--muted)}
/* Modal reportar */
.inf-rep-tipo-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin-bottom:13px}
.inf-rtipo{background:var(--soft);border:2px solid var(--line);border-radius:12px;padding:10px 5px 9px;cursor:pointer;text-align:center;transition:border-color .15s}
.inf-rtipo.sel{border-color:#0A2F6B}
.inf-rtipo-ic{display:flex;justify-content:center;margin-bottom:5px}
.inf-rtipo-lbl{font-size:10.5px;font-weight:700}
.inf-ropts{display:flex;flex-wrap:wrap;gap:7px;margin-bottom:13px;display:none}
.inf-ropts.show{display:flex}
.inf-ropt-lbl{font-size:10px;font-weight:800;color:var(--muted2);text-transform:uppercase;letter-spacing:.06em;margin-bottom:7px;width:100%}
.inf-ropt{height:36px;padding:0 13px;border-radius:99px;border:1.5px solid var(--line);background:var(--soft);font-size:12px;font-weight:700;color:var(--muted);cursor:pointer;font-family:inherit;transition:all .15s;white-space:nowrap}
.inf-ropt.sel{border-color:#0A2F6B;color:#0A2F6B;background:rgba(14,165,233,.08)}
.inf-ropt.sel.r{border-color:var(--red);color:var(--red);background:rgba(239,68,68,.08)}
.inf-ropt.sel.g{border-color:#22C55E;color:#22C55E;background:rgba(34,197,94,.08)}
.in
===== #F59E0B @ 131903 =====
;border:1px solid var(--line);border-radius:16px;padding:14px;margin-bottom:16px}
.inf-peak-bars{display:flex;align-items:flex-end;gap:5px;height:60px;margin-top:6px}
.inf-peak-bar-wrap{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end;height:100%}
.inf-peak-bar{width:100%;border-radius:5px 5px 0 0;background:var(--line);min-height:2px}
.inf-peak-bar.now{background:linear-gradient(180deg,#F59E0B,#EA580C);box-shadow:0 0 0 2px #FDBA74}
.inf-peak-lbl-row{display:flex;gap:5px;margin-top:4px}
.inf-peak-lbl{flex:1;font-size:7.5px;color:var(--muted);font-weight:700;text-align:center}
.inf-peak-now-row{display:flex;gap:5px;margin-top:2px}
.inf-peak-now-cell{flex:1;text-align:center}
.inf-peak-now-mark{font-size:7.5px;color:#EA580C;font-weight:800}
/* ── Colabore com a comunidade ── */
.inf-collab-banner{display:flex;align-items:center;gap:11px;background:linear-gradient(135deg,#0A2F6B,#0D3A7D);border-radius:16px;padding:13px 14px;color:#fff;margin-bottom:16px;cursor:pointer;border:0;width:100%;font-family:inherit;text-align:left}
.inf-collab-ic{width:36px;height:36px;border-radius:11px;background:rgba(255,255,255,.15);display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-collab-txt b{display:block;font-size:12.5px;font-weight:800}
.inf-collab-txt span{font-size:10.5px;color:rgba(255,255,255,.7)}
/* Modal do formulário */
.inf-qblock{margin-bottom:20px}
.inf-qnum{display:inline-flex;align-items:center;justify-content:center;width:18px;height:18px;border-radius:50%;background:var(--blue);color:#fff;font-size:9.5px;font-weight:800;margin-right:6px;flex-shrink:0}
.inf-qtitle{font-size:13px;font-weight:800}
.inf-qopts2{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px}
.inf-qopt{padding:13px 8px;border-radius:13px;border:1.5px solid var(--line);background:var(--soft);font-size:12.5px;font-weight:700;text-align:center;color:var(--text);cursor:pointer;touch-action:manipulation}
.inf-qopt.picked{background:var(--blue);border-color:var(--blue);color:#fff}
.inf-qsub-lbl{font-size:10.5px;color:#94A3B8;font-weight:700;margin:12px 0 8px;padding-left:2px}
.inf-qsub-opts{display:grid;grid-template-columns:1fr 1fr;gap:8px}
.inf-qsub-opt{padding:13px 8px;border-radius:13px;border:1.5px solid var(--line);background:var(--soft);font-size:12.5px;font-weight:700;text-align:center;color:var(--text);cursor:pointer;touch-action:manipulation}
.inf-qsub-opt.picked{background:#EFF6FF;border-color:#2563EB;color:#1E40AF}
.inf-qtime-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:10px}
.inf-qtime-chip{padding:10px 6px;border-radius:11px;border:1.5px solid var(--line);background:var(--soft);font-size:12px;font-weight:700;text-align:center;color:var(--text);cursor:pointer;touch-action:manipulation}
.inf-qtime-chip.picked{background:var(--blue);border-color:var(--blue);color:#fff}
.inf-qtextarea{width:100%;border:1.5px solid var(--line);border-radius:13px;padding:11px 12px;font-size:12.5px;font-family:inherit;color:var(--text);min-height:64px;resize:none;margin-top:10px}
.inf-qnote{display:flex;align-items:center;gap:5px;font-size:9.5px;color:#94A3B8;margin-top:6px}
.inf-qsend{width:100%;height:50px;border-radius:14px;background:var(--blue);color:#fff;border:0;font-size:14px;font-weight:800;margin-top:6px;cursor:pointer;font-family:inherit}
/* reportar botão */
.inf-rep-wrap{padding:9px 14px 14px;flex-shrink:0}
.inf-rep-btn{width:100%;height:48px;border-radius:14px;border:0;background:#F59E0B;color:#fff;font-size:14px;font-weight:700;font-family:inherit;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:7px}
/* Overlay sheets */
.inf-ov{position:fixed;inset:0;background:rgba(0,0,0,.55);z-index:120;opacity:0;pointer-events:none;transition:opacity .2s;display:flex;align-items:flex-end;justify-content:center}
.inf-ov.open{opacity:1;pointer-events:all}
.inf-sheet{background:var(--card);border-radius:24px 24px 0 0;padding:14px 16px 38px;width:100%;max-width:430px;transform:translateY(100%);transition:transform .28s cubic-bezier(.4,0,.2,1);max-height:88vh;overflow-y:auto}
.inf-ov.open .inf-sheet{transform:translateY(0)}
.inf-sh-handle{width:34px;height:3px;border-radius:2px;background:var(--line);margin:0 auto 13px}
.inf-sh-head{display:flex;align-items:center;gap:9px;margin-bottom:3px}
.inf-sh-ic{width:32px;height:32px;border-radius:9px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-sh-title{font-size:16px;font-weight:800}
.inf-sh-sub{font-size:11.5px;color:var(--muted);margin-bottom:11px}
.inf-sh-search{position:relative;margin-bottom:11px}
.inf-sh-search svg{position:absolute;left:11px;top:50%;transform:translateY(-50%);opacity:.4;pointer-events:none}
.inf-sh-sinput{width:100%;height:42px;border-radius:12px;border:1.5px solid var(--line);background:var(--soft);font-family:inherit;font-size:13px;font-weight:600;color:var(--text);padding:0 12px 0 36px;outline:none}
.inf-loc-item{display:flex;align-items:center;gap:9px;padding:11px 0;border-bottom:1px solid var(--line);cursor:pointer}
.inf-loc-item:last-child{border-bottom:0}
.inf-loc-item:active{opacity:.6}
.inf-loc-ic{width:30px;height:30px;border-radius:9px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-loc-body{flex:1;min-width:0}
.inf-loc-name{font-size:13.5px;font-weight:700}
.inf-loc-sub{font-size:10px;color:var(--muted);margin-top:1px}
.inf-loc-right{display:flex;flex-direction:column;align-items:flex-end;gap:3px;flex-shrink:0}
.inf-loc-dist{font-size:10px;font-weight:700;color:var(--muted2)}
.inf-loc-ct{font-size:9px;font-weight:800;background:var(--red);color:#fff;border-radius:99px;padding:2px 6px}
.inf-loc-ct.z{background:var(--line);color:var(--muted)}
/* Modal reportar */
.inf-rep-tipo-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin-bottom:13px}
.inf-rtipo{background:var(--soft);border:2px solid var(--line);border-radius:12px;padding:10px 5px 9px;cursor:pointer;text-align:center;transition:border-color .15s}
.inf-rtipo.sel{border-color:#0A2F6B}
.inf-rtipo-ic{display:flex;justify-content:center;margin-bottom:5px}
.inf-rtipo-lbl{font-size:10.5px;font-weight:700}
.inf-ropts{display:flex;flex-wrap:wrap;gap:7px;margin-bottom:13px;display:none}
.inf-ropts.show{display:flex}
.inf-ropt-lbl{font-size:10px;font-weight:800;color:var(--muted2);text-transform:uppercase;letter-spacing:.06em;margin-bottom:7px;width:100%}
.inf-ropt{height:36px;padding:0 13px;border-radius:99px;border:1.5px solid var(--line);background:var(--soft);font-size:12px;font-weight:700;color:var(--muted);cursor:pointer;font-family:inherit;transition:all .15s;white-space:nowrap}
.inf-ropt.sel{border-color:#0A2F6B;color:#0A2F6B;background:rgba(14,165,233,.08)}
.inf-ropt.sel.r{border-color:var(--red);color:var(--red);background:rgba(239,68,68,.08)}
.inf-ropt.sel.g{border-color:#22C55E;color:#22C55E;background:rgba(34,197,94,.08)}
.inf-ropt.sel.a{border-color:#F59E0B;color:#F59E0B;background:rgba(245,158,11,.08)}
.inf-send-btn{width:100%;height:46px;border-radius:13px;border:0;background:#0A2F6B;color:#fff;font-size:13.5px;font-weight:700;font-family:inherit;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:7px}
.inf-send-btn:disabled{opacity:.35}
.inf-empty{padding:28px 0;text-align:center;color:var(--muted);font-size:13px;font-weight:600}

/* ── Sessão card expansível ── */
.jss-card{background:var(--card);border:1px solid var(--line);border-left:3px solid var(--line);border-radius:18px;margin-bottom:9px;overflow:hidden;box-shadow:0 3px 14px rgba(15,23,42,.05)}
.jss-card.live{border-left-color:#22C55E}
.jss-card.end{border-left-color:#0EA5E9}
.jss-row{display:flex;align-items:center;gap:10px;padding:12px 14px;cursor:pointer}
.jss-info{flex:1;min-width:0}
.jss-top{display:flex;align-items:center;gap:7px;margin-bottom:3px}
.jss-name{font-size:14px;font-weight:800}
.jss-badge{font-size:9px;font-weight:800;padding:2px 8px;border-radius:99px}
.jss-badge.live{background:#DCFCE7;color:#15803D}
.jss-badge.pau{background:#F1F5F9;color:#475569}
.jss-badge.end{background:#EFF6FF;color:#0369A1}
.jss-sub{font-size:11.5px;color:var(--muted);font-weight:600}
.jss-chev{flex-shrink:0;color:var(--muted2);transition:transform .22s;display:flex;align-items:center}
.jss-kpi-row{display:none;align-items:center;padding:0 14px 11px;gap:0}
.jss-card.open .jss-kpi-row{display:grid;grid-template-columns:repeat(3,1fr);row-gap:10px;column-gap:0;align-items:start}
.jss-kpi{flex:1;text-align:center}
.jss-kpi .jv{font-size:14px;font-weight:800;letter-spacing:-.03em;display:block}
.jss-kpi .jv.g{color:var(--green)}
.jss-kpi .jv.b{color:var(--blue)}
.jss-kpi .jl{font-size:8px;font-weight:700;color:var(--muted2);text-transform:uppercase;letter-spacing:.05em;margin-top:1px;display:block}
.jss-acts{display:none;gap:4px;justify-content:flex-end;padding:2px 14px 0}
.jss-card.open .jss-acts{display:flex}
.jss-trips{display:none;border-top:1px solid var(--line);padding:10px 10px 6px}
.jss-card.open .jss-trips{display:block}
.jss-trips-head{display:flex;align-items:center;justify-content:space-between;padding:4px 14px 8px}
.jss-trips-head h4{font-size:10.5px;font-weight:800;color:var(--muted2);text-transform:uppercase;letter-spacing:.06em}
.jss-trips-head span{font-size:11px;font-weight:700;color:var(--muted2)}
.jss-trips-empty{padding:12px 14px;font-size:12px;color:var(--muted2);text-align:center}
.jss-act{border:0;background:#F1F5F9;cursor:pointer;color:#64748B;padding:5px;display:flex;align-items:center;border-radius:8px;touch-action:manipulation;flex-shrink:0}.jss-act.del{color:#EF4444;background:#FEF2F2}
.jss-ganhos{font-size:12px;font-weight:800;color:var(--green2)}
.jss-rate-row{display:none;grid-template-columns:repeat(3,1fr);gap:8px;padding:2px 14px 12px}
.jss-card.open .jss-rate-row{display:grid}
.jss-statusbar{display:none}
.jss-card.open .jss-statusbar{display:block}
.jss-rcard{border-radius:12px;padding:8px 4px;text-align:center;border:1.5px solid rg
===== #F59E0B @ 135356 =====
ht:48px;border-radius:14px;border:0;background:#F59E0B;color:#fff;font-size:14px;font-weight:700;font-family:inherit;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:7px}
/* Overlay sheets */
.inf-ov{position:fixed;inset:0;background:rgba(0,0,0,.55);z-index:120;opacity:0;pointer-events:none;transition:opacity .2s;display:flex;align-items:flex-end;justify-content:center}
.inf-ov.open{opacity:1;pointer-events:all}
.inf-sheet{background:var(--card);border-radius:24px 24px 0 0;padding:14px 16px 38px;width:100%;max-width:430px;transform:translateY(100%);transition:transform .28s cubic-bezier(.4,0,.2,1);max-height:88vh;overflow-y:auto}
.inf-ov.open .inf-sheet{transform:translateY(0)}
.inf-sh-handle{width:34px;height:3px;border-radius:2px;background:var(--line);margin:0 auto 13px}
.inf-sh-head{display:flex;align-items:center;gap:9px;margin-bottom:3px}
.inf-sh-ic{width:32px;height:32px;border-radius:9px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-sh-title{font-size:16px;font-weight:800}
.inf-sh-sub{font-size:11.5px;color:var(--muted);margin-bottom:11px}
.inf-sh-search{position:relative;margin-bottom:11px}
.inf-sh-search svg{position:absolute;left:11px;top:50%;transform:translateY(-50%);opacity:.4;pointer-events:none}
.inf-sh-sinput{width:100%;height:42px;border-radius:12px;border:1.5px solid var(--line);background:var(--soft);font-family:inherit;font-size:13px;font-weight:600;color:var(--text);padding:0 12px 0 36px;outline:none}
.inf-loc-item{display:flex;align-items:center;gap:9px;padding:11px 0;border-bottom:1px solid var(--line);cursor:pointer}
.inf-loc-item:last-child{border-bottom:0}
.inf-loc-item:active{opacity:.6}
.inf-loc-ic{width:30px;height:30px;border-radius:9px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-loc-body{flex:1;min-width:0}
.inf-loc-name{font-size:13.5px;font-weight:700}
.inf-loc-sub{font-size:10px;color:var(--muted);margin-top:1px}
.inf-loc-right{display:flex;flex-direction:column;align-items:flex-end;gap:3px;flex-shrink:0}
.inf-loc-dist{font-size:10px;font-weight:700;color:var(--muted2)}
.inf-loc-ct{font-size:9px;font-weight:800;background:var(--red);color:#fff;border-radius:99px;padding:2px 6px}
.inf-loc-ct.z{background:var(--line);color:var(--muted)}
/* Modal reportar */
.inf-rep-tipo-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin-bottom:13px}
.inf-rtipo{background:var(--soft);border:2px solid var(--line);border-radius:12px;padding:10px 5px 9px;cursor:pointer;text-align:center;transition:border-color .15s}
.inf-rtipo.sel{border-color:#0A2F6B}
.inf-rtipo-ic{display:flex;justify-content:center;margin-bottom:5px}
.inf-rtipo-lbl{font-size:10.5px;font-weight:700}
.inf-ropts{display:flex;flex-wrap:wrap;gap:7px;margin-bottom:13px;display:none}
.inf-ropts.show{display:flex}
.inf-ropt-lbl{font-size:10px;font-weight:800;color:var(--muted2);text-transform:uppercase;letter-spacing:.06em;margin-bottom:7px;width:100%}
.inf-ropt{height:36px;padding:0 13px;border-radius:99px;border:1.5px solid var(--line);background:var(--soft);font-size:12px;font-weight:700;color:var(--muted);cursor:pointer;font-family:inherit;transition:all .15s;white-space:nowrap}
.inf-ropt.sel{border-color:#0A2F6B;color:#0A2F6B;background:rgba(14,165,233,.08)}
.inf-ropt.sel.r{border-color:var(--red);color:var(--red);background:rgba(239,68,68,.08)}
.inf-ropt.sel.g{border-color:#22C55E;color:#22C55E;background:rgba(34,197,94,.08)}
.inf-ropt.sel.a{border-color:#F59E0B;color:#F59E0B;background:rgba(245,158,11,.08)}
.inf-send-btn{width:100%;height:46px;border-radius:13px;border:0;background:#0A2F6B;color:#fff;font-size:13.5px;font-weight:700;font-family:inherit;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:7px}
.inf-send-btn:disabled{opacity:.35}
.inf-empty{padding:28px 0;text-align:center;color:var(--muted);font-size:13px;font-weight:600}

/* ── Sessão card expansível ── */
.jss-card{background:var(--card);border:1px solid var(--line);border-left:3px solid var(--line);border-radius:18px;margin-bottom:9px;overflow:hidden;box-shadow:0 3px 14px rgba(15,23,42,.05)}
.jss-card.live{border-left-color:#22C55E}
.jss-card.end{border-left-color:#0EA5E9}
.jss-row{display:flex;align-items:center;gap:10px;padding:12px 14px;cursor:pointer}
.jss-info{flex:1;min-width:0}
.jss-top{display:flex;align-items:center;gap:7px;margin-bottom:3px}
.jss-name{font-size:14px;font-weight:800}
.jss-badge{font-size:9px;font-weight:800;padding:2px 8px;border-radius:99px}
.jss-badge.live{background:#DCFCE7;color:#15803D}
.jss-badge.pau{background:#F1F5F9;color:#475569}
.jss-badge.end{background:#EFF6FF;color:#0369A1}
.jss-sub{font-size:11.5px;color:var(--muted);font-weight:600}
.jss-chev{flex-shrink:0;color:var(--muted2);transition:transform .22s;display:flex;align-items:center}
.jss-kpi-row{display:none;align-items:center;padding:0 14px 11px;gap:0}
.jss-card.open .jss-kpi-row{display:grid;grid-template-columns:repeat(3,1fr);row-gap:10px;column-gap:0;align-items:start}
.jss-kpi{flex:1;text-align:center}
.jss-kpi .jv{font-size:14px;font-weight:800;letter-spacing:-.03em;display:block}
.jss-kpi .jv.g{color:var(--green)}
.jss-kpi .jv.b{color:var(--blue)}
.jss-kpi .jl{font-size:8px;font-weight:700;color:var(--muted2);text-transform:uppercase;letter-spacing:.05em;margin-top:1px;display:block}
.jss-acts{display:none;gap:4px;justify-content:flex-end;padding:2px 14px 0}
.jss-card.open .jss-acts{display:flex}
.jss-trips{display:none;border-top:1px solid var(--line);padding:10px 10px 6px}
.jss-card.open .jss-trips{display:block}
.jss-trips-head{display:flex;align-items:center;justify-content:space-between;padding:4px 14px 8px}
.jss-trips-head h4{font-size:10.5px;font-weight:800;color:var(--muted2);text-transform:uppercase;letter-spacing:.06em}
.jss-trips-head span{font-size:11px;font-weight:700;color:var(--muted2)}
.jss-trips-empty{padding:12px 14px;font-size:12px;color:var(--muted2);text-align:center}
.jss-act{border:0;background:#F1F5F9;cursor:pointer;color:#64748B;padding:5px;display:flex;align-items:center;border-radius:8px;touch-action:manipulation;flex-shrink:0}.jss-act.del{color:#EF4444;background:#FEF2F2}
.jss-ganhos{font-size:12px;font-weight:800;color:var(--green2)}
.jss-rate-row{display:none;grid-template-columns:repeat(3,1fr);gap:8px;padding:2px 14px 12px}
.jss-card.open .jss-rate-row{display:grid}
.jss-statusbar{display:none}
.jss-card.open .jss-statusbar{display:block}
.jss-rcard{border-radius:12px;padding:8px 4px;text-align:center;border:1.5px solid rgba(34,197,94,.35);background:rgba(34,197,94,.08)}
.jss-rcard.o{border-color:rgba(245,158,11,.35);background:rgba(245,158,11,.08)}
.jss-rcard.r{border-color:rgba(239,68,68,.35);background:rgba(239,68,68,.08)}
.jss-rcard strong{display:block;font-size:13px;font-weight:800;color:#15803D}
.jss-rcard.o strong{color:#C2410C}
.jss-rcard.r strong{color:#B91C1C}
.jss-rcard span{display:block;font-size:8px;font-weight:800;color:inherit;opacity:.8;text-transform:uppercase;letter-spacing:.03em;margin-top:2px}
/* ── Lançamentos — tabs pago / a pagar ── */
.lanc-tabs{display:flex;background:var(--soft);border:1px solid var(--line);border-radius:14px;padding:3px;gap:3px;margin-bottom:12px}
.lanc-tab{flex:1;height:34px;border-radius:11px;border:0;background:transparent;font-family:inherit;font-size:11.5px;font-weight:800;color:var(--muted);cursor:pointer;transition:.15s}
.lanc-tab.on{background:var(--card);color:var(--text);box-shadow:0 2px 8px rgba(15,23,42,.08)}
.lanc-status-badge{font-size:9px;font-weight:800;padding:2px 7px;border-radius:99px;display:inline-block;margin-top:3px}
.lsb-pago{background:#DCFCE7;color:#15803D}
.lsb-apagar{background:#FEF3C7;color:#92400E}
.lsb-vencido{background:#FEE2E2;color:#991B1B}
.lanc-act-btn{border:0;background:transparent;cursor:pointer;display:flex;align-items:center;justify-content:center;width:32px;height:32px;border-radius:9px;flex-shrink:0;transition:.12s}
.lanc-act-btn.confirmar{background:#DCFCE7;color:#15803D}
.lanc-act-btn.cancelar{background:#FEE2E2;color:#991B1B}
/* ── Lançamentos Jornada: card compacto + detalhe inline ── */
.lanc-item-wrap{margin-bottom:8px}
.li-row-j{display:flex;gap:10px;align-items:center;background:var(--card);border:1.5px solid var(--line);border-left:3px solid var(--line);border-radius:14px;padding:10px 12px;cursor:pointer;touch-action:manipulation}
.li-row-j.pago{border-left-color:#22C55E}
.li-row-j.vencido{border-color:#FECACA;border-left-color:#DC2626}
.li-row-j.apagar{border-left-color:#F59E0B}
.li-row-j.open{border-radius:14px 14px 0 0;border-bottom:0}
.li-line1{display:flex;align-items:center;gap:6px;margin-bottom:2px}
.li-title-j{font-size:13px;font-weight:700;color:var(--text);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.li-line2{display:flex;justify-content:space-between;align-items:center}
.li-sub-j{font-size:11px;color:var(--muted2);font-weight:500}
.li-val-j{font-size:14px;font-weight:800;flex-shrink:0;margin-left:4px}
.li-val-j.neg{color:var(--red)}.li-val-j.pos{color:var(--green)}
.st-badge{display:inline-block;font-size:9.5px;font-weight:800;padding:2.5px 8px;border-radius:99px;white-space:nowrap}
.st-badge.st-pago{background:#DCFCE7;color:#15803D}
.st-badge.st-apagar{background:#FEF9C3;color:#854D0E}
.st-badge.st-vencido{background:#FEE2E2;color:#B91C1C}
.lanc-detail-panel{background:var(--card);border:1.5px solid var(--line);border-top:0;border-radius:0 0 14px 14px;padding:10px 12px 12px}
.lanc-detail-row{display:flex;justify-content:space-between;align-items:center;font-size:12px;color:var(--muted);padding:5px 0;font-weight:500}
.lanc-detail-row:not(:last-of-type){border-bottom:1px solid var(--line)}
.lanc-detail-row strong{color:var(--text);font-weight:700}
.btn-marcar-pago{width:100%;height:40px;border:0;border-radius:10px;background:var(--green);color:#fff;font-size:13px;font-weight:800;cursor:pointer;margin-top:10px;display:flex;align-items:center;justify-content:center;gap:6px;font-family:var(
===== #F59E0B @ 140387 =====
.jss-kpi{flex:1;text-align:center}
.jss-kpi .jv{font-size:14px;font-weight:800;letter-spacing:-.03em;display:block}
.jss-kpi .jv.g{color:var(--green)}
.jss-kpi .jv.b{color:var(--blue)}
.jss-kpi .jl{font-size:8px;font-weight:700;color:var(--muted2);text-transform:uppercase;letter-spacing:.05em;margin-top:1px;display:block}
.jss-acts{display:none;gap:4px;justify-content:flex-end;padding:2px 14px 0}
.jss-card.open .jss-acts{display:flex}
.jss-trips{display:none;border-top:1px solid var(--line);padding:10px 10px 6px}
.jss-card.open .jss-trips{display:block}
.jss-trips-head{display:flex;align-items:center;justify-content:space-between;padding:4px 14px 8px}
.jss-trips-head h4{font-size:10.5px;font-weight:800;color:var(--muted2);text-transform:uppercase;letter-spacing:.06em}
.jss-trips-head span{font-size:11px;font-weight:700;color:var(--muted2)}
.jss-trips-empty{padding:12px 14px;font-size:12px;color:var(--muted2);text-align:center}
.jss-act{border:0;background:#F1F5F9;cursor:pointer;color:#64748B;padding:5px;display:flex;align-items:center;border-radius:8px;touch-action:manipulation;flex-shrink:0}.jss-act.del{color:#EF4444;background:#FEF2F2}
.jss-ganhos{font-size:12px;font-weight:800;color:var(--green2)}
.jss-rate-row{display:none;grid-template-columns:repeat(3,1fr);gap:8px;padding:2px 14px 12px}
.jss-card.open .jss-rate-row{display:grid}
.jss-statusbar{display:none}
.jss-card.open .jss-statusbar{display:block}
.jss-rcard{border-radius:12px;padding:8px 4px;text-align:center;border:1.5px solid rgba(34,197,94,.35);background:rgba(34,197,94,.08)}
.jss-rcard.o{border-color:rgba(245,158,11,.35);background:rgba(245,158,11,.08)}
.jss-rcard.r{border-color:rgba(239,68,68,.35);background:rgba(239,68,68,.08)}
.jss-rcard strong{display:block;font-size:13px;font-weight:800;color:#15803D}
.jss-rcard.o strong{color:#C2410C}
.jss-rcard.r strong{color:#B91C1C}
.jss-rcard span{display:block;font-size:8px;font-weight:800;color:inherit;opacity:.8;text-transform:uppercase;letter-spacing:.03em;margin-top:2px}
/* ── Lançamentos — tabs pago / a pagar ── */
.lanc-tabs{display:flex;background:var(--soft);border:1px solid var(--line);border-radius:14px;padding:3px;gap:3px;margin-bottom:12px}
.lanc-tab{flex:1;height:34px;border-radius:11px;border:0;background:transparent;font-family:inherit;font-size:11.5px;font-weight:800;color:var(--muted);cursor:pointer;transition:.15s}
.lanc-tab.on{background:var(--card);color:var(--text);box-shadow:0 2px 8px rgba(15,23,42,.08)}
.lanc-status-badge{font-size:9px;font-weight:800;padding:2px 7px;border-radius:99px;display:inline-block;margin-top:3px}
.lsb-pago{background:#DCFCE7;color:#15803D}
.lsb-apagar{background:#FEF3C7;color:#92400E}
.lsb-vencido{background:#FEE2E2;color:#991B1B}
.lanc-act-btn{border:0;background:transparent;cursor:pointer;display:flex;align-items:center;justify-content:center;width:32px;height:32px;border-radius:9px;flex-shrink:0;transition:.12s}
.lanc-act-btn.confirmar{background:#DCFCE7;color:#15803D}
.lanc-act-btn.cancelar{background:#FEE2E2;color:#991B1B}
/* ── Lançamentos Jornada: card compacto + detalhe inline ── */
.lanc-item-wrap{margin-bottom:8px}
.li-row-j{display:flex;gap:10px;align-items:center;background:var(--card);border:1.5px solid var(--line);border-left:3px solid var(--line);border-radius:14px;padding:10px 12px;cursor:pointer;touch-action:manipulation}
.li-row-j.pago{border-left-color:#22C55E}
.li-row-j.vencido{border-color:#FECACA;border-left-color:#DC2626}
.li-row-j.apagar{border-left-color:#F59E0B}
.li-row-j.open{border-radius:14px 14px 0 0;border-bottom:0}
.li-line1{display:flex;align-items:center;gap:6px;margin-bottom:2px}
.li-title-j{font-size:13px;font-weight:700;color:var(--text);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.li-line2{display:flex;justify-content:space-between;align-items:center}
.li-sub-j{font-size:11px;color:var(--muted2);font-weight:500}
.li-val-j{font-size:14px;font-weight:800;flex-shrink:0;margin-left:4px}
.li-val-j.neg{color:var(--red)}.li-val-j.pos{color:var(--green)}
.st-badge{display:inline-block;font-size:9.5px;font-weight:800;padding:2.5px 8px;border-radius:99px;white-space:nowrap}
.st-badge.st-pago{background:#DCFCE7;color:#15803D}
.st-badge.st-apagar{background:#FEF9C3;color:#854D0E}
.st-badge.st-vencido{background:#FEE2E2;color:#B91C1C}
.lanc-detail-panel{background:var(--card);border:1.5px solid var(--line);border-top:0;border-radius:0 0 14px 14px;padding:10px 12px 12px}
.lanc-detail-row{display:flex;justify-content:space-between;align-items:center;font-size:12px;color:var(--muted);padding:5px 0;font-weight:500}
.lanc-detail-row:not(:last-of-type){border-bottom:1px solid var(--line)}
.lanc-detail-row strong{color:var(--text);font-weight:700}
.btn-marcar-pago{width:100%;height:40px;border:0;border-radius:10px;background:var(--green);color:#fff;font-size:13px;font-weight:800;cursor:pointer;margin-top:10px;display:flex;align-items:center;justify-content:center;gap:6px;font-family:var(--font-app);touch-action:manipulation}

/* ── Tipografia MōB ─────────────────────────────────────────── */
.logo,.brand-title,.hero-title,.banner-title,.onboarding-title,
.hd-title,.auth-logo-title,.auth-hero,
.hg-amount,.period-amount h1,.j-timer,
h1.hd-title{font-family:var(--font-brand)!important}

/* ── Feed social (Home) ─────────────────────────────────────── */
.feed-kpis{display:flex;gap:8px;padding:10px 16px 4px}
.feed-kpi{flex:1;background:var(--card);border:1px solid var(--line);border-radius:14px;padding:9px 8px;text-align:center}
.feed-kpi .v{font-family:var(--font-brand);font-weight:800;font-size:14px;color:var(--blue);line-height:1.1}
.feed-kpi .v.g{color:var(--green2)}
.feed-kpi .l{font-size:9px;font-weight:600;color:var(--muted);text-transform:uppercase;letter-spacing:.05em;margin-top:3px}
.feed-filters{display:flex;gap:7px;padding:10px 16px;overflow-x:auto;scrollbar-width:none}
.feed-filters::-webkit-scrollbar{display:none}
.feed-chip{flex:0 0 auto;border-radius:99px;padding:6px 13px;font-size:12px;font-weight:700;border:1px solid var(--line);background:var(--card);color:var(--muted);white-space:nowrap;cursor:pointer}
.feed-chip.on{background:var(--blue);border-color:var(--blue);color:#fff}
.feed-list{padding-bottom:90px}
.feed-post{display:flex;gap:10px;padding:13px 16px 11px;border-bottom:1px solid var(--soft-line);cursor:pointer}
.feed-av{width:38px;height:38px;border-radius:99px;flex:0 0 auto;display:flex;align-items:center;justify-content:center;font-family:var(--font-brand);font-weight:700;font-size:13px;color:#fff;object-fit:cover}
.feed-body{flex:1;min-width:0}
.feed-head{display:flex;align-items:center;gap:6px;flex-wrap:wrap}
.feed-name{font-weight:800;font-size:13px;color:var(--text)}
.feed-time{font-size:11.5px;color:var(--muted2);font-weight:500}
.feed-type{display:inline-flex;align-items:center;gap:5px;font-size:11px;font-weight:800;border-radius:8px;padding:2.5px 8px;margin-top:4px}
.feed-type svg{stroke:currentColor}
.feed-text{font-size:13.5px;line-height:1.45;color:var(--text);margin-top:6px;white-space:pre-wrap;word-break:break-word}
.feed-loc{display:inline-flex;align-items:center;gap:4px;font-size:11px;font-weight:600;color:var(--muted);margin-top:7px}
.feed-loc svg{stroke:var(--green2)}
.feed-acts{display:flex;align-items:center;gap:22px;margin-top:9px}
.feed-act{display:flex;align-items:center;gap:5px;font-size:11.5px;font-weight:700;color:var(--muted2);background:none;border:none;padding:0;font-family:var(--font-app);cursor:pointer}
.feed-act svg{stroke:currentColor;fill:none}
.feed-act.up-on{color:var(--green2)}
.feed-act.dn-on{color:var(--red)}
.feed-post.closed{opacity:.5}
.feed-closed-badge{display:inline-block;font-size:10px;font-weight:800;color:var(--muted2);background:var(--bg);border-radius:6px;padding:2px 7px;margin-top:6px;text-transform:uppercase;letter-spacing:.04em}
.feed-empty{padding:60px 16px;text-align:center;color:var(--muted)}
.feed-fab-wrap{position:fixed;left:50%;transform:translateX(-50%);width:100%;max-width:430px;bottom:calc(var(--bottom) + var(--safe-bottom) + 12px);pointer-events:none;z-index:150}
.feed-fab{position:absolute;right:16px;bottom:0;width:52px;height:52px;border:none;border-radius:18px;background:var(--green);display:flex;align-items:center;justify-content:center;box-shadow:0 6px 18px rgba(34,197,94,.4);cursor:pointer;pointer-events:auto}
.feed-fab svg{stroke:#fff}
/* Composer */
.feed-cp-body{display:flex;gap:10px;padding:14px 16px 4px}
.feed-cp-input{flex:1;font-size:14.5px;line-height:1.5;padding-top:8px;border:none;background:none;outline:none;resize:none;color:var(--text);font-family:var(--font-app);min-height:70px}
.feed-cp-input::placeholder{color:var(--muted2)}
.feed-cp-types{display:flex;gap:7px;padding:10px 16px;overflow-x:auto;scrollbar-width:none}
.feed-cp-types::-webkit-scrollbar{display:none}
.feed-t-chip{flex:0 0 auto;display:flex;align-items:center;gap:6px;border-radius:12px;padding:8px 12px;font-size:11.5px;font-weight:800;border:1.5px solid var(--line);background:var(--card);color:var(--muted);white-space:nowrap;cursor:pointer;font-family:var(--font-app)}
.qr-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}
.inf-relatar-loc-chip{display:flex;align-items:center;gap:9px;background:#EFF6FF;border:1.5px solid #BFDBFE;border-radius:14px;padding:12px 12px 12px 13px;margin:0 0 14px}
.inf-city{cursor:pointer;touch-action:manipulation}
.inf-city-search-box{display:flex;align-items:center;gap:8px;background:var(--soft);border-radius:12px;padding:11px 13px;margin-bottom:14px}
.inf-city-search-box input{border:0;background:transparent;outline:none;font-size:13px;flex:1;font-family:inherit;color:var(--text)}
.inf-city-item{display:flex;align-items:center;gap:11px;padding:11px 4px;border-bottom:1px solid #F1F5F9;cursor:pointer;touch-action:manipulation}
.inf-city-item:last-child{border-bottom:0}
.inf-city-item-ic{width:32px;height:32px;border-radius:9px;background:var(--soft);display:flex;align-items:center;justify-content:center;color:var
===== #F59E0B @ 443053 =====
>Rodoviária Novo Rio <svg class="inf-featF-mode" viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2"><rect x="4" y="4" width="16" height="12" rx="2.5"/><path d="M4 11h16"/><path d="M7.5 16v2M16.5 16v2"/></svg></div><div class="inf-featF-sub">Santo Cristo</div></div>
          </div>
          <div class="inf-featF-ct" id="inf-ct-rod">–</div>
        </div>
        <div class="inf-featF-mini" id="inf-mini-rod"></div>
        <div class="inf-featF-arriving"><svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.2"><rect x="4" y="4" width="16" height="12" rx="2.5"/><path d="M4 11h16"/><path d="M7.5 16v2M16.5 16v2"/></svg>Chegando: — (em breve via sistema da rodoviária)</div>
      </div>
    </div>

    <!-- ═══ ABA: BAIRROS ═══ -->
    <div class="inf-panelbody hide inf-fill" id="inf-tab-bairros">
      <div class="inf-area-grp-lbl"><div class="inf-area-grp-dot"></div><span class="inf-area-grp-name" id="inf-bairros-city-label">Bairros da cidade</span></div>
      <div class="inf-bchip-scroll inf-fill-scroll">
        <div class="inf-bchip-2row" id="inf-bairros-dynamic"><div class="inf-empty-feed">Localizando bairros…</div></div>
      </div>
    </div>

    <!-- ═══ ABAS: SHOPPINGS / BARES / HOTÉIS / EVENTOS / TURÍSTICO ═══ -->
    <div class="inf-panelbody hide" id="inf-tab-shop"><div class="inf-vlist" id="inf-vlist-shoppings"></div></div>
    <div class="inf-panelbody hide" id="inf-tab-bares"><div class="inf-vlist" id="inf-vlist-bares"></div></div>
    <div class="inf-panelbody hide" id="inf-tab-hoteis"><div class="inf-vlist" id="inf-vlist-hoteis"></div></div>
    <div class="inf-panelbody hide" id="inf-tab-eventos"><div class="inf-vlist" id="inf-vlist-eventos"></div></div>
    <div class="inf-panelbody hide" id="inf-tab-tur"><div class="inf-vlist" id="inf-vlist-turistico"></div></div>

  </div><!-- /inf-scroll-content -->

  <!-- painel de informes full-screen -->
  <div class="inf-panel" id="inf-panel">
    <div class="inf-panel-drag" id="inf-panel-drag"><div class="inf-panel-drag-bar"></div></div>
    <div class="inf-panel-head">
      <button onclick="infFecharPainel()" style="width:32px;height:32px;border-radius:9px;border:1.5px solid var(--line);background:var(--soft);display:flex;align-items:center;justify-content:center;cursor:pointer;flex-shrink:0">
        <svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="15 18 9 12 15 6"/></svg>
      </button>
      <div class="inf-panel-hic" id="inf-panel-hic"></div>
      <div class="inf-panel-hbody">
        <div class="inf-panel-tipo" id="inf-panel-tipo"></div>
        <div class="inf-panel-loc" id="inf-panel-loc">—</div>
        <div class="inf-panel-meta" id="inf-panel-meta"></div>
      </div>
      <div class="inf-head-badges" id="inf-head-badges" style="display:flex;gap:6px;flex-shrink:0"></div>
      <button class="inf-panel-fav" id="inf-panel-map" onclick="infAbrirNoMapa()" style="display:none" aria-label="Abrir no mapa">
        <svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="#0EA5E9" stroke-width="2.2"><path d="M9 20l-5.5 2V6L9 4l6 2 5.5-2v16L15 22l-6-2z"/><line x1="9" y1="4" x2="9" y2="18"/><line x1="15" y1="6" x2="15" y2="20"/></svg>
      </button>
      <button class="inf-panel-fav" id="inf-panel-fav" onclick="infToggleFav()">
        <svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="#F59E0B" stroke-width="2"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
      </button>
    </div>
    <div class="inf-status" id="inf-status"></div>
    <div class="inf-sep"></div>
    <div class="inf-list-wrap" id="inf-list-wrap"><div class="inf-empty">Carregando informes…</div></div>
    <div class="inf-rep-wrap">
      <button class="inf-rep-btn" onclick="infAbrirReportar()">
        <svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="#fff" stroke-width="2.5"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
        Reportar agora
      </button>
    </div>
  </div>

  <!-- Sheet: GPS ruas -->
  <div class="inf-ov" id="inf-ov-gps">
    <div class="inf-sheet">
      <div class="inf-sh-handle"></div>
      <div class="inf-sh-head"><div class="inf-sh-ic" style="background:#CCFBF1"><svg viewBox="0 0 24 24" width="17" height="17" fill="none" stroke="#14B8A6" stroke-width="2"><circle cx="12" cy="12" r="3"/><path d="M12 2v3M12 19v3M2 12h3M19 12h3"/></svg></div><div class="inf-sh-title">Minha rua</div></div>
      <div class="inf-sh-sub">Raio de 300m · mais próxima primeiro</div>
      <div class="inf-sh-search"><svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg><input class="inf-sh-sinput" type="text" placeholder="Buscar rua…" oninput="infFiltrarSheet(this,'inf-gps-list')"></div>
      <div id="inf-gps-list"><div class="inf-empty">Aguardando GPS…</div></div>
    </div>
  </div>

  <!-- Sheet: venue list (usado como fallback de busca detalhada) -->
  <div class="inf-ov" id="inf-ov-venue">
    <div class="inf-sheet">
      <div class="inf-sh-handle"></div>
      <div class="inf-sh-head"><div class="inf-sh-ic" id="inf-venue-ic"></div><div class="inf-sh-title" id="inf-venue-title"></div></div>
      <div class="inf-sh-sub" id="inf-venue-sub"></div>
      <div class="inf-sh-search"><svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg><input class="inf-sh-sinput" type="text" placeholder="Buscar…" oninput="infFiltrarSheet(this,'inf-venue-list')"></div>
      <div id="inf-venue-list"></div>
    </div>
  </div>

  <!-- Sheet: reportar -->
  <div class="inf-ov" id="inf-ov-rep">
    <div class="inf-sheet">
      <div class="inf-sh-handle"></div>
      <div class="inf-sh-head"><div class="inf-sh-ic" id="inf-rep-ic"></div><div><div class="inf-sh-title">O que está acontecendo?</div><div style="font-size:11px;color:var(--muted);margin-top:2px" id="inf-rep-sub"></div></div></div>
      <div style="margin:10px 0 0">
        <div class="inf-rep-tipo-grid" id="inf-rep-tipos"></div>
        <div id="inf-rep-opcoes-wrap">
          <!-- opções dinâmicas conforme tipo selecionado -->
        </div>
        <button class="inf-send-btn" id="inf-send-btn" disabled onclick="infEnviarReporte()">
          <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#fff" stroke-width="2.5"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
          Enviar informe
        </button>
      </div>
    </div>
  </div>
</div>
<div class="screen" id="sc-mais">
  <div style="padding:16px 16px 8px"><div style="font-size:22px;font-weight:800;letter-spacing:-.04em">Menu</div></div>
  <div id="mais-modules" style="padding:0 16px;display:flex;flex-direction:column;gap:10px"></div>
  <div style="padding:16px 16px 8px"><div style="font-size:13px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:.06em;margin-bottom:10px">Ferramentas</div></div>
  <div style="padding:0 16px;display:flex;flex-direction:column;gap:8px">
    <div class="mais-item" onclick="navTo('corridas')"><div class="mais-ico" style="background:#EFF6FF"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="#2563EB" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M9 21V9"/></svg></div><div><div class="mais-t">Corridas</div><div class="mais-s">Histórico e filtros de corridas</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="navTo('relatorios')"><div class="mais-ico" style="background:#FFF7ED"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="#F59E0B" stroke-width="2"><path d="M3 3v18h18"/><path d="m19 9-5 5-4-4-4 4"/></svg></div><div><div class="mais-t">Relatório</div><div class="mais-s">Gráficos, metas e visão geral do período</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="navTo('agenda')"><div class="mais-ico blue"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18"/></svg></div><div><div class="mais-t">Agenda</div><div class="mais-s">Planejamento e horários de trabalho</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="openFlashConfig()"><div class="mais-ico" style="background:#F0FDF4"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="#16A34A" stroke-width="2"><path d="M13 2 4 14h6l-1 8 9-12h-6l1-8z"/></svg></div><div><div class="mais-t">MōB Flash</div><div class="mais-s">Nota da oferta em tempo real</div></div><div class="mais-arr">›</div></div>
  </div>
  <div style="padding:16px 16px 8px"><div style="font-size:13px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:.06em;margin-bottom:10px">Configurações</div></div>
  <div style="padding:0 16px;display:flex;flex-direction:column;gap:8px">
    <div class="mais-item" onclick="openPerfil()"><div class="mais-ico blue"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21a8 8 0 0 0-16 0"/><circle cx="12" cy="7" r="4"/></svg></div><div><div class="mais-t">Perfil</div><div class="mais-s" id="mais-perfil-sub">Configure nome, placa e veículo</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="openConfig()"><div class="mais-ico green"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><circle cx="1
===== #F59E0B @ 447643 =====
14B8A6" stroke-width="2"><circle cx="12" cy="12" r="3"/><path d="M12 2v3M12 19v3M2 12h3M19 12h3"/></svg></div><div class="inf-sh-title">Minha rua</div></div>
      <div class="inf-sh-sub">Raio de 300m · mais próxima primeiro</div>
      <div class="inf-sh-search"><svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg><input class="inf-sh-sinput" type="text" placeholder="Buscar rua…" oninput="infFiltrarSheet(this,'inf-gps-list')"></div>
      <div id="inf-gps-list"><div class="inf-empty">Aguardando GPS…</div></div>
    </div>
  </div>

  <!-- Sheet: venue list (usado como fallback de busca detalhada) -->
  <div class="inf-ov" id="inf-ov-venue">
    <div class="inf-sheet">
      <div class="inf-sh-handle"></div>
      <div class="inf-sh-head"><div class="inf-sh-ic" id="inf-venue-ic"></div><div class="inf-sh-title" id="inf-venue-title"></div></div>
      <div class="inf-sh-sub" id="inf-venue-sub"></div>
      <div class="inf-sh-search"><svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg><input class="inf-sh-sinput" type="text" placeholder="Buscar…" oninput="infFiltrarSheet(this,'inf-venue-list')"></div>
      <div id="inf-venue-list"></div>
    </div>
  </div>

  <!-- Sheet: reportar -->
  <div class="inf-ov" id="inf-ov-rep">
    <div class="inf-sheet">
      <div class="inf-sh-handle"></div>
      <div class="inf-sh-head"><div class="inf-sh-ic" id="inf-rep-ic"></div><div><div class="inf-sh-title">O que está acontecendo?</div><div style="font-size:11px;color:var(--muted);margin-top:2px" id="inf-rep-sub"></div></div></div>
      <div style="margin:10px 0 0">
        <div class="inf-rep-tipo-grid" id="inf-rep-tipos"></div>
        <div id="inf-rep-opcoes-wrap">
          <!-- opções dinâmicas conforme tipo selecionado -->
        </div>
        <button class="inf-send-btn" id="inf-send-btn" disabled onclick="infEnviarReporte()">
          <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#fff" stroke-width="2.5"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
          Enviar informe
        </button>
      </div>
    </div>
  </div>
</div>
<div class="screen" id="sc-mais">
  <div style="padding:16px 16px 8px"><div style="font-size:22px;font-weight:800;letter-spacing:-.04em">Menu</div></div>
  <div id="mais-modules" style="padding:0 16px;display:flex;flex-direction:column;gap:10px"></div>
  <div style="padding:16px 16px 8px"><div style="font-size:13px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:.06em;margin-bottom:10px">Ferramentas</div></div>
  <div style="padding:0 16px;display:flex;flex-direction:column;gap:8px">
    <div class="mais-item" onclick="navTo('corridas')"><div class="mais-ico" style="background:#EFF6FF"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="#2563EB" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M9 21V9"/></svg></div><div><div class="mais-t">Corridas</div><div class="mais-s">Histórico e filtros de corridas</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="navTo('relatorios')"><div class="mais-ico" style="background:#FFF7ED"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="#F59E0B" stroke-width="2"><path d="M3 3v18h18"/><path d="m19 9-5 5-4-4-4 4"/></svg></div><div><div class="mais-t">Relatório</div><div class="mais-s">Gráficos, metas e visão geral do período</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="navTo('agenda')"><div class="mais-ico blue"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18"/></svg></div><div><div class="mais-t">Agenda</div><div class="mais-s">Planejamento e horários de trabalho</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="openFlashConfig()"><div class="mais-ico" style="background:#F0FDF4"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="#16A34A" stroke-width="2"><path d="M13 2 4 14h6l-1 8 9-12h-6l1-8z"/></svg></div><div><div class="mais-t">MōB Flash</div><div class="mais-s">Nota da oferta em tempo real</div></div><div class="mais-arr">›</div></div>
  </div>
  <div style="padding:16px 16px 8px"><div style="font-size:13px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:.06em;margin-bottom:10px">Configurações</div></div>
  <div style="padding:0 16px;display:flex;flex-direction:column;gap:8px">
    <div class="mais-item" onclick="openPerfil()"><div class="mais-ico blue"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21a8 8 0 0 0-16 0"/><circle cx="12" cy="7" r="4"/></svg></div><div><div class="mais-t">Perfil</div><div class="mais-s" id="mais-perfil-sub">Configure nome, placa e veículo</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="openConfig()"><div class="mais-ico green"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1Z"/></svg></div><div><div class="mais-t">Configurações</div><div class="mais-s">Ícone flutuante, modo escuro e mais</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="navTo('categorias')"><div class="mais-ico orange"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 7h16M4 12h16M4 17h8"/></svg></div><div><div class="mais-t">Categorias</div><div class="mais-s">Gerencie categorias de despesa</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="openCustoVeiculo()"><div class="mais-ico green"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 17h14M6 17l1-6 3-4h4l3 4 1 6"/><circle cx="7.5" cy="17" r="1.5"/><circle cx="16.5" cy="17" r="1.5"/></svg></div><div><div class="mais-t">Custo do veículo</div><div class="mais-s" id="mais-custo-sub">Configure pra ver o lucro líquido real</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="openSuporte()"><div class="mais-ico purple"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M9.1 9a3 3 0 0 1 5.8 1c0 2-3 3-3 3M12 17h.01"/></svg></div><div><div class="mais-t">Suporte</div><div class="mais-s">Ajuda e feedback</div></div><div class="mais-arr">›</div></div>
  </div>
  <div style="height:24px"></div>
</div>

</div><!-- /scroll -->

<!-- ════ NAV ════ -->
<nav class="bottom" id="app-nav" style="display:flex">
  <div class="nav active" id="bn-home" onclick="navTo('home')">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m3 10 9-7 9 7v10a1 1 0 0 1-1 1h-5v-6H9v6H4a1 1 0 0 1-1-1z"/></svg><span>Início</span>
  </div>
  <div class="nav" id="bn-agenda" onclick="navTo('informes')">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m3 11 18-5v12L3 13"/><path d="M11.6 16.8a3 3 0 1 1-5.8-1.6"/></svg><span>Informes</span>
  </div>
  <div class="center" id="bn-jornada">
    <div class="start" id="gps-btn" onclick="navTo('jornada')">
      <img id="gps-icon-img" src="data:image/png;base64," alt="" style="width:40px;height:40px;object-fit:contain;pointer-events:none">
      <span id="gps-lbl" style="font-size:9px;font-weight:800;text-transform:uppercase;letter-spacing:.04em;color:white;line-height:1">Jornada</span>
    </div>
  </div>
  <div class="nav" id="bn-fav" onclick="navToFav()">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" id="fav-icon"><path d="M12 3v18M17 7H9.5a3.5 3.5 0 0 0 0 7H14a3.5 3.5 0 0 1 0 7H6"/></svg>
    <span id="fav-lbl">Finanças</span>
  </div>
  <div class="nav" id="bn-mais" onclick="navTo('mais')">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M4 7h16M4 12h16M4 17h16"/></svg><span>Menu</span>
  </div>
</nav>


<!-- ════ MODAIS ════ -->

<!-- Informes: trocar cidade (16/08/2026, pedido do Yuri) -->
<div class="mo" id="mo-inf-cidade">
  <div cla
===== #F59E0B @ 499834 =====
ground:#EFF6FF;border:1px solid rgba(14,165,233,.2);border-radius:11px;padding:9px 10px;font-size:11px;color:#0369A1;line-height:1.45;margin-bottom:12px">
      <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" style="flex:none;margin-top:1px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
      <span>Serve pra conferir se o import achou tudo — não precisa ser o dia inteiro.</span>
    </div>
    <button class="btn-p" style="margin-top:0" onclick="importPrecheckNext()">Avançar</button>
    <div style="text-align:center;font-size:12px;color:#2563EB;font-weight:700;margin-top:10px;cursor:pointer" onclick="importPrecheckSkip()">Pular essa parte →</div>
  </div>
</div>

<div class="mo" id="mo-import">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2>Importar corridas</h2><button class="xbtn" onclick="closeMo('mo-import')">&#x2715;</button></div>
    <div style="background:#EFF6FF;border:1px solid rgba(14,165,233,.2);border-radius:16px;padding:12px 14px;font-size:12px;color:#0369A1;line-height:1.6;display:flex;gap:8px;margin-bottom:16px">
      <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" style="flex-shrink:0;margin-top:2px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
      <div><strong>Como usar:</strong> grave a tela do celular rolando o histórico (Uber ou 99, tanto faz) e mande o arquivo de vídeo aqui — não dá mais pra mandar print solto. O app identifica a plataforma sozinho e extrai as corridas.</div>
    </div>
    <div style="display:flex;gap:8px;margin-bottom:8px">
      <label style="flex:1;height:56px;border-radius:14px;background:#F0F4FA;border:1px solid #E2E8F0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;cursor:pointer;font-size:11px;font-weight:700;color:#0A2F6B">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="m3 9 4-4 4 4 4-4 4 4"/></svg> Vídeo da galeria
        <input type="file" multiple accept="video/*" style="display:none" onclick="this.value=null" onchange="handleImport(this,'uber')">
      </label>
      <label style="flex:1;height:56px;border-radius:14px;background:#F0F4FA;border:1px solid #E2E8F0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;cursor:pointer;font-size:11px;font-weight:700;color:#0A2F6B">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg> Arquivos
        <input type="file" multiple accept="video/*" style="display:none" onclick="this.value=null" onchange="handleImport(this,'uber')">
      </label>
    </div>
    <div style="font-size:11px;color:#94A3B8;text-align:center;margin-bottom:14px">Só vídeo — grave a tela rolando o histórico e mande o arquivo aqui. Dá pra escolher vários de uma vez.</div>

    <label style="width:100%;height:50px;border-radius:14px;background:linear-gradient(135deg,#0A2F6B,#22C55E);display:flex;align-items:center;justify-content:center;gap:8px;cursor:pointer;font-size:12.5px;font-weight:700;color:#fff;box-shadow:0 8px 18px rgba(10,47,107,.25);position:relative;margin-bottom:8px">
      <span style="position:absolute;top:-8px;right:-6px;background:#F59E0B;color:#fff;font-size:8.5px;font-weight:900;padding:3px 6px;border-radius:999px;letter-spacing:.02em">NOVO</span>
      <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M17 8 12 3 7 8"/><path d="M12 3v12"/></svg>
      Histórico oficial do Uber (.zip)
      <input type="file" accept=".zip" style="display:none" onclick="this.value=null" onchange="handleUberZipFile(this)">
    </label>
    <div style="font-size:11px;color:#94A3B8;text-align:center;line-height:1.5;margin-bottom:12px">Baixe em: app Uber → Conta → Configurações de privacidade → "Solicitar meus dados". Você recebe um e-mail com o arquivo .zip.</div>

    <div style="display:flex;gap:6px;justify-content:center">
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px;display:flex;align-items:center;gap:4px"><i style="width:6px;height:6px;border-radius:50%;display:inline-block;background:#0EA5E9"></i> Uber</span>
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px;display:flex;align-items:center;gap:4px"><i style="width:6px;height:6px;border-radius:50%;display:inline-block;background:#F59E0B"></i> 99</span>
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px">identificados automaticamente</span>
    </div>
  </div>
</div>

<!-- Import confirm -->
<div class="mo" id="mo-import-confirm">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2 id="ic-title">Corridas detectadas</h2><button class="xbtn" onclick="closeMo('mo-import-confirm')">&#x2715;</button></div>
    <div id="ic-body" style="min-height:80px"></div>
    <div style="display:flex;gap:8px;padding-top:8px">
      <button class="btn-s" onclick="closeMo('mo-import-confirm')" style="margin-top:0">Cancelar</button>
      <button class="btn-p" id="ic-btn" onclick="confirmImport()" style="margin-top:0">Importar</button>
    </div>
  </div>
</div>

<!-- Uber ZIP: processando + resultado -->
<div class="mo ctr" id="mo-uberzip">
  <div class="sheet ctr" style="max-width:400px">
    <div id="uz-proc" style="padding:50px 20px;text-align:center">
      <div style="font-size:32px;display:inline-block;animation:uzspin 1.1s linear infinite">&#9881;</div>
      <div id="uz-proc-step" style="font-size:14px;font-weight:800;margin-top:16px">Lendo arquivo...</div>
      <div id="uz-proc-sub" style="font-size:12px;color:#64748B;margin-top:5px">Isso só leva alguns segundos</div>
      <div style="height:8px;background:#EEF2F7;border-radius:99px;overflow:hidden;margin-top:20px">
        <div id="uz-proc-bar" style="height:100%;background:linear-gradient(90deg,#0EA5E9,#22C55E);border-radius:99px;width:8%;transition:width .4s"></div>
      </div>
    </div>

    <div id="uz-res" style="display:none">
      <div style="text-align:center;padding:4px 0 16px">
        <div style="width:52px;height:52px;border-radius:16px;background:#ECFDF3;color:#16A34A;display:flex;align-items:center;justify-content:center;margin:0 auto 10px">
          <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6 9 17l-5-5"/></svg>
        </div>
        <div style="font-size:16px;font-weight:800;letter-spacing:-.02em">Histórico conferido</div>
        <div id="uz-res-sub" style="font-size:12px;color:#64748B;margin-top:3px"></div>
      </div>

      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px">
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Novas pra importar</span>
          <strong id="uz-k-novas" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;color:#16A34A;margin-top:4px">0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Já existiam</span>
          <strong id="uz-k-dup" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;color:#64748B;margin-top:4px">0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Valor das novas</span>
          <strong id="uz-k-valor" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;margin-top:4px">R$ 0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Período</span>
          <strong id="uz-k-periodo" style="display:block;font-size:12.5px;margin-top:5px">—</strong>
        </div>
      </div>

      <div id="uz-dup-callout" style="margin-top:10px;background:#F8FAFC;border:1px solid #E2E8F0;border-radius:14px;padding:11px 12px;display:none;gap:9px;align-items:flex-start">
        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#94A3B8" stroke-width="2" style="flex-shrink:0;margin-top:1px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
        <p id="uz-dup-text" style="font-size:11px;color:#64748B;line-height:1.5;margin:0"></p>
      </div>

      <div id="uz-neg-warn" style="margin-top:8px;font-size:11px;color:#B45309;display:none"></div>

      <div style="display:flex;gap:8px;margin-top:18px">
        <button class="btn-s" onclick="closeMo('mo-uberzip')" style="margin-top:0">Cancelar</button>
        <button class="btn-p" id="uz-confirm-btn" onclick="confirmUberZipImport()" style="margin-top:0">Importar</button>
      </div>
    </div>
  </div>
</div>
<style>@keyframes uzspin{to{transform:rotate(360deg)}}</style>

<!-- Perfil -->
<div class="mo" id="mo-perfil">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2>Perfil</h2><button class="xbtn" onclick="closeMo('mo-perfil')">&#x2715;</button></div>

    <div class="av-sec">
      <div class="av-wrap" onclick="perfilOpenPhot
===== #F59E0B @ 501144 =====
le cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
      <div><strong>Como usar:</strong> grave a tela do celular rolando o histórico (Uber ou 99, tanto faz) e mande o arquivo de vídeo aqui — não dá mais pra mandar print solto. O app identifica a plataforma sozinho e extrai as corridas.</div>
    </div>
    <div style="display:flex;gap:8px;margin-bottom:8px">
      <label style="flex:1;height:56px;border-radius:14px;background:#F0F4FA;border:1px solid #E2E8F0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;cursor:pointer;font-size:11px;font-weight:700;color:#0A2F6B">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="m3 9 4-4 4 4 4-4 4 4"/></svg> Vídeo da galeria
        <input type="file" multiple accept="video/*" style="display:none" onclick="this.value=null" onchange="handleImport(this,'uber')">
      </label>
      <label style="flex:1;height:56px;border-radius:14px;background:#F0F4FA;border:1px solid #E2E8F0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;cursor:pointer;font-size:11px;font-weight:700;color:#0A2F6B">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg> Arquivos
        <input type="file" multiple accept="video/*" style="display:none" onclick="this.value=null" onchange="handleImport(this,'uber')">
      </label>
    </div>
    <div style="font-size:11px;color:#94A3B8;text-align:center;margin-bottom:14px">Só vídeo — grave a tela rolando o histórico e mande o arquivo aqui. Dá pra escolher vários de uma vez.</div>

    <label style="width:100%;height:50px;border-radius:14px;background:linear-gradient(135deg,#0A2F6B,#22C55E);display:flex;align-items:center;justify-content:center;gap:8px;cursor:pointer;font-size:12.5px;font-weight:700;color:#fff;box-shadow:0 8px 18px rgba(10,47,107,.25);position:relative;margin-bottom:8px">
      <span style="position:absolute;top:-8px;right:-6px;background:#F59E0B;color:#fff;font-size:8.5px;font-weight:900;padding:3px 6px;border-radius:999px;letter-spacing:.02em">NOVO</span>
      <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M17 8 12 3 7 8"/><path d="M12 3v12"/></svg>
      Histórico oficial do Uber (.zip)
      <input type="file" accept=".zip" style="display:none" onclick="this.value=null" onchange="handleUberZipFile(this)">
    </label>
    <div style="font-size:11px;color:#94A3B8;text-align:center;line-height:1.5;margin-bottom:12px">Baixe em: app Uber → Conta → Configurações de privacidade → "Solicitar meus dados". Você recebe um e-mail com o arquivo .zip.</div>

    <div style="display:flex;gap:6px;justify-content:center">
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px;display:flex;align-items:center;gap:4px"><i style="width:6px;height:6px;border-radius:50%;display:inline-block;background:#0EA5E9"></i> Uber</span>
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px;display:flex;align-items:center;gap:4px"><i style="width:6px;height:6px;border-radius:50%;display:inline-block;background:#F59E0B"></i> 99</span>
      <span style="font-size:9.5px;font-weight:800;color:#64748B;background:#F1F5F9;border-radius:99px;padding:4px 10px">identificados automaticamente</span>
    </div>
  </div>
</div>

<!-- Import confirm -->
<div class="mo" id="mo-import-confirm">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2 id="ic-title">Corridas detectadas</h2><button class="xbtn" onclick="closeMo('mo-import-confirm')">&#x2715;</button></div>
    <div id="ic-body" style="min-height:80px"></div>
    <div style="display:flex;gap:8px;padding-top:8px">
      <button class="btn-s" onclick="closeMo('mo-import-confirm')" style="margin-top:0">Cancelar</button>
      <button class="btn-p" id="ic-btn" onclick="confirmImport()" style="margin-top:0">Importar</button>
    </div>
  </div>
</div>

<!-- Uber ZIP: processando + resultado -->
<div class="mo ctr" id="mo-uberzip">
  <div class="sheet ctr" style="max-width:400px">
    <div id="uz-proc" style="padding:50px 20px;text-align:center">
      <div style="font-size:32px;display:inline-block;animation:uzspin 1.1s linear infinite">&#9881;</div>
      <div id="uz-proc-step" style="font-size:14px;font-weight:800;margin-top:16px">Lendo arquivo...</div>
      <div id="uz-proc-sub" style="font-size:12px;color:#64748B;margin-top:5px">Isso só leva alguns segundos</div>
      <div style="height:8px;background:#EEF2F7;border-radius:99px;overflow:hidden;margin-top:20px">
        <div id="uz-proc-bar" style="height:100%;background:linear-gradient(90deg,#0EA5E9,#22C55E);border-radius:99px;width:8%;transition:width .4s"></div>
      </div>
    </div>

    <div id="uz-res" style="display:none">
      <div style="text-align:center;padding:4px 0 16px">
        <div style="width:52px;height:52px;border-radius:16px;background:#ECFDF3;color:#16A34A;display:flex;align-items:center;justify-content:center;margin:0 auto 10px">
          <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6 9 17l-5-5"/></svg>
        </div>
        <div style="font-size:16px;font-weight:800;letter-spacing:-.02em">Histórico conferido</div>
        <div id="uz-res-sub" style="font-size:12px;color:#64748B;margin-top:3px"></div>
      </div>

      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px">
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Novas pra importar</span>
          <strong id="uz-k-novas" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;color:#16A34A;margin-top:4px">0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Já existiam</span>
          <strong id="uz-k-dup" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;color:#64748B;margin-top:4px">0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Valor das novas</span>
          <strong id="uz-k-valor" style="display:block;font-family:'Inter',system-ui,sans-serif;font-size:18px;margin-top:4px">R$ 0</strong>
        </div>
        <div style="border:1px solid #E2E8F0;border-radius:16px;padding:11px 12px;background:#fff">
          <span style="color:#64748B;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em">Período</span>
          <strong id="uz-k-periodo" style="display:block;font-size:12.5px;margin-top:5px">—</strong>
        </div>
      </div>

      <div id="uz-dup-callout" style="margin-top:10px;background:#F8FAFC;border:1px solid #E2E8F0;border-radius:14px;padding:11px 12px;display:none;gap:9px;align-items:flex-start">
        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#94A3B8" stroke-width="2" style="flex-shrink:0;margin-top:1px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
        <p id="uz-dup-text" style="font-size:11px;color:#64748B;line-height:1.5;margin:0"></p>
      </div>

      <div id="uz-neg-warn" style="margin-top:8px;font-size:11px;color:#B45309;display:none"></div>

      <div style="display:flex;gap:8px;margin-top:18px">
        <button class="btn-s" onclick="closeMo('mo-uberzip')" style="margin-top:0">Cancelar</button>
        <button class="btn-p" id="uz-confirm-btn" onclick="confirmUberZipImport()" style="margin-top:0">Importar</button>
      </div>
    </div>
  </div>
</div>
<style>@keyframes uzspin{to{transform:rotate(360deg)}}</style>

<!-- Perfil -->
<div class="mo" id="mo-perfil">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2>Perfil</h2><button class="xbtn" onclick="closeMo('mo-perfil')">&#x2715;</button></div>

    <div class="av-sec">
      <div class="av-wrap" onclick="perfilOpenPhotoSheet()">
        <div class="av-initials" id="p-av-initials" style="display:none"></div>
        <img class="av-photo" id="p-av-photo" style="display:none" src="" alt="Foto de perfil">
        <div class="av-cam"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg></div>
      </div>
      <div class="av-name" id="p-av-name">—</div>
      <div class="av-sub">
        <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
        <span id="p-av-sub">—</span>
      </div>
    </div>

    <div class="p-stats">
      <div class="p-stat"><div class="v" id="p-stat-corridas">—</div><div class="l">Corridas</div></div>
      <div class="p-stat"><div class="v" id="p-stat-alertas">—</div><div class="l">Alertas</div></div>
      <div class="p-stat"><div class="v" id="p-stat-conf">—</div><div class="l">Confirmações</div></div>
    </div>

    <input type="file" id="p-file-camera" accept="image/*" capture="user" style="display:none" onch
===== #F59E0B @ 523997 =====
:#0F172A">Acessibilidade</div>
            <div style="font-size:11px;color:#94A3B8;margin-top:1px">Identifica telas e estados da corrida na Uber/99</div>
          </div>
          <div id="flash-perm-a11y" style="display:flex;flex-direction:column;align-items:flex-end;gap:6px"></div>
        </div>
        <div style="display:flex;align-items:center;justify-content:space-between;gap:10px;padding:11px 0;border-bottom:1px solid #F1F5F9">
          <div style="flex:1">
            <div style="font-size:13.5px;font-weight:600;color:#0F172A">Sobrepor outros apps</div>
            <div style="font-size:11px;color:#94A3B8;margin-top:1px">Mostra o Flash e o status por cima da Uber/99</div>
          </div>
          <div id="flash-perm-overlay" style="display:flex;flex-direction:column;align-items:flex-end;gap:6px"></div>
        </div>
        <div style="display:flex;align-items:center;justify-content:space-between;gap:10px;padding:11px 0;border-bottom:1px solid #F1F5F9">
          <div style="flex:1"><div style="font-size:13.5px;font-weight:600;color:#0F172A">Acesso às notificações</div><div style="font-size:11px;color:#94A3B8;margin-top:1px">Lê eventos oficiais da Uber/99 e identifica a plataforma da oferta</div></div>
          <div id="flash-perm-notification" style="display:flex;flex-direction:column;align-items:flex-end;gap:6px"></div>
        </div>
        <div style="display:flex;align-items:center;justify-content:space-between;gap:10px;padding:11px 0">
          <div style="flex:1">
            <div style="font-size:13.5px;font-weight:600;color:#0F172A">Leitura da tela</div>
            <div style="font-size:11px;color:#94A3B8;margin-top:1px">Completa dados da oferta que não vierem pelos eventos oficiais</div>
          </div>
          <div id="flash-perm-capture" style="display:flex;flex-direction:column;align-items:flex-end;gap:6px"></div>
        </div>
      </div>

      <button id="flash-step2-primary" class="btn-p" style="margin-top:16px;background:#22C55E" onclick="flashWizardFinish(true)">Iniciar jornada</button>
      <button id="flash-skip2" class="xbtn" style="width:100%;text-align:center;font-size:12.5px;color:#64748B;font-weight:700;margin-top:12px;background:none;border:0;padding:8px;text-decoration:underline;height:auto" onclick="flashWizardFinish(false)">Iniciar sem o MōB Flash</button>
    </div>

    <!-- Painel da régua — edição de limite vermelho/verde de um KPI -->
    <div id="flash-ruler" style="display:none">
      <div class="sheet-head">
        <button style="font-size:12px;color:#94A3B8;font-weight:700;background:none;border:0;padding:0" onclick="flashRulerClose()">&#8249; Voltar</button>
        <button class="xbtn" onclick="closeMo('mo-flash')">&#x2715;</button>
      </div>
      <div style="display:flex;align-items:center;gap:9px;margin:6px 0 4px">
        <span id="flash-ruler-ico" style="width:26px;height:26px;border-radius:8px;background:#F1F5F9;display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:900;color:#0A2F6B"></span>
        <h3 id="flash-ruler-title" style="font-family:var(--font-brand);font-size:15px;font-weight:800;color:#0F172A"></h3>
      </div>
      <div style="font-size:11.5px;color:#94A3B8;margin-bottom:26px;line-height:1.5">Arraste os pontos pra ajustar onde o card vira vermelho, amarelo ou verde.</div>

      <div id="flash-ruler-track" style="position:relative;height:10px;border-radius:99px;background:linear-gradient(90deg,#EF4444 0%,#EF4444 30%,#F59E0B 30%,#F59E0B 70%,#10B981 70%,#10B981 100%);margin:0 12px 34px">
        <div id="flash-ruler-red" style="position:absolute;top:50%;width:24px;height:24px;border-radius:50%;background:#fff;border:3px solid #0A2F6B;transform:translate(-50%,-50%);box-shadow:0 2px 6px rgba(0,0,0,.25);touch-action:none;cursor:grab;left:30%">
          <span id="flash-ruler-red-val" style="position:absolute;top:-30px;left:50%;transform:translateX(-50%);font-size:12px;font-weight:800;color:#0F172A;background:#fff;padding:2px 8px;border-radius:8px;border:1px solid #E2E8F0;white-space:nowrap"></span>
        </div>
        <div id="flash-ruler-green" style="position:absolute;top:50%;width:24px;height:24px;border-radius:50%;background:#fff;border:3px solid #0A2F6B;transform:translate(-50%,-50%);box-shadow:0 2px 6px rgba(0,0,0,.25);touch-action:none;cursor:grab;left:70%">
          <span id="flash-ruler-green-val" style="position:absolute;top:-30px;left:50%;transform:translateX(-50%);font-size:12px;font-weight:800;color:#0F172A;background:#fff;padding:2px 8px;border-radius:8px;border:1px solid #E2E8F0;white-space:nowrap"></span>
        </div>
      </div>
      <div style="display:flex;justify-content:space-between;font-size:9.5px;font-weight:800;color:#94A3B8;text-transform:uppercase;margin:0 12px 26px">
        <span>Vermelho</span><span>Amarelo</span><span>Verde</span>
      </div>

      <button class="btn-p" onclick="flashRulerClose()">Pronto</button>
    </div>

    <!-- Painel de valor único — recusas numéricas (nota/passageiro novo/distância) -->
    <div id="flash-recusa-num" style="display:none">
      <div class="sheet-head">
        <button style="font-size:12px;color:#94A3B8;font-weight:700;background:none;border:0;padding:0" onclick="flashRecusaNumClose()">&#8249; Voltar</button>
        <button class="xbtn" onclick="closeMo('mo-flash')">&#x2715;</button>
      </div>
      <div style="display:flex;align-items:center;gap:9px;margin:6px 0 4px">
        <span id="flash-recusa-num-ico" style="width:26px;height:26px;border-radius:8px;background:#FEF2F2;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:900;color:#DC2626"></span>
        <h3 id="flash-recusa-num-title" style="font-family:var(--font-brand);font-size:15px;font-weight:800;color:#0F172A"></h3>
      </div>
      <div id="flash-recusa-num-phrase" style="font-size:12.5px;color:#DC2626;font-weight:700;background:#FEF2F2;border-radius:12px;padding:10px 12px;margin:10px 0 24px;line-height:1.5"></div>

      <div style="display:flex;align-items:center;justify-content:center;gap:16px;margin-bottom:18px">
        <button onclick="flashRecusaNumStep(-1)" style="width:42px;height:42px;border-radius:50%;background:#F1F5F9;border:0;font-size:20px;font-weight:800;color:#0A2F6B">&#8722;</button>
        <div style="min-width:96px;text-align:center">
          <div id="flash-recusa-num-val" style="font-family:var(--font-brand);font-size:32px;font-weight:800;color:#0F172A"></div>
          <div id="flash-recusa-num-unit" style="font-size:11px;color:#94A3B8;font-weight:700;text-transform:uppercase;margin-top:2px"></div>
        </div>
        <button onclick="flashRecusaNumStep(1)" style="width:42px;height:42px;border-radius:50%;background:#F1F5F9;border:0;font-size:20px;font-weight:800;color:#0A2F6B">+</button>
      </div>
      <input id="flash-recusa-num-slider" type="range" style="width:100%;margin-bottom:26px;accent-color:#0A2F6B" oninput="flashRecusaNumFromSlider(this.value)">

      <button class="btn-p" onclick="flashRecusaNumClose()">Pronto</button>
    </div>

    <!-- Tela de endereço recusado — embarque OU destino -->
    <div id="flash-enderecos" style="display:none;flex-direction:column;min-height:72vh">
      <div class="sheet-head">
        <button style="font-size:12px;color:#94A3B8;font-weight:700;background:none;border:0;padding:0" onclick="flashEnderecosClose()">&#8249; Voltar</button>
        <button class="xbtn" onclick="closeMo('mo-flash')">&#x2715;</button>
      </div>
      <h3 style="font-family:var(--font-brand);font-size:15px;font-weight:800;color:#0F172A;margin:6px 0 4px">Endereço recusado</h3>
      <div style="font-size:12px;color:#64748B;line-height:1.5;margin-bottom:14px">Corridas com <b>embarque ou destino</b> nesses locais são recusadas automaticamente.</div>

      <div style="display:flex;gap:6px">
        <input id="flash-endereco-input" placeholder="Bairro ou rua…" autocomplete="off" style="flex:1;border:1.5px solid #0A2F6B;border-radius:12px;padding:10px 12px;font-size:12.5px;font:inherit" oninput="flashEnderecoSearch(this.value)" onkeydown="if(event.key==='Enter'){flashEnderecoAdd();}">
        <button onclick="flashEnderecoAdd()" style="background:#0A2F6B;color:#fff;border:0;border-radius:12px;padding:0 18px;font-weight:800;font-size:15px">+</button>
      </div>
      <div id="flash-endereco-suggestions" style="display:none;background:#fff;border:1px solid #E2E8F0;border-radius:12px;margin-top:6px;overflow:hidden;box-shadow:0 6px 18px rgba(15,23,42,.08)"></div>

      <div style="font-size:11px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.05em;margin:18px 0 8px">Bloqueados</div>
      <div id="flash-enderecos-list" style="margin-bottom:12px"></div>

      <div style="flex:1"></div>
      <button class="btn-p" style="margin-top:20px" onclick="flashEnderecosClose()">Pronto</button>
    </div>

    <div style="font-size:11px;color:#94A3B8;text-align:center;margin-top:10px;line-height:1.5">Os indicadores usam o valor/km/min já lidos hoje + seu custo/km cadastrado em Financeiro.</div>
  </div>
</div>



<!-- MODAL: Importar Histórico -->
<div class="mo ctr" id="mo-hist-import">
  <div class="sheet ctr" style="max-width:400px">
    <div class="sheet-head"><h2>Importar Histórico</h2><button class="xbtn" onclick="closeMo('mo-hist-import')">&#x2715;</button></div>
    <div style="background:#EFF6FF;border:1px solid rgba(14,165,233,.2);border-radius:16px;padding:12px 14px;font-size:12px;color:#0EA5E9;line-height:1.6;margin-bottom:16px;display:flex;gap:8px">
      <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" style="flex-shrink:0;margin-top:2px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
      <div>Selecione o arquivo <strong>smartmobi_historico_uber.json</strong> gerado pelo sistema. Corridas j&#225; existentes n&#227;o ser&#227;o duplicadas.</div>
    </div>
    <label styl
===== #F59E0B @ 524009 =====
essibilidade</div>
            <div style="font-size:11px;color:#94A3B8;margin-top:1px">Identifica telas e estados da corrida na Uber/99</div>
          </div>
          <div id="flash-perm-a11y" style="display:flex;flex-direction:column;align-items:flex-end;gap:6px"></div>
        </div>
        <div style="display:flex;align-items:center;justify-content:space-between;gap:10px;padding:11px 0;border-bottom:1px solid #F1F5F9">
          <div style="flex:1">
            <div style="font-size:13.5px;font-weight:600;color:#0F172A">Sobrepor outros apps</div>
            <div style="font-size:11px;color:#94A3B8;margin-top:1px">Mostra o Flash e o status por cima da Uber/99</div>
          </div>
          <div id="flash-perm-overlay" style="display:flex;flex-direction:column;align-items:flex-end;gap:6px"></div>
        </div>
        <div style="display:flex;align-items:center;justify-content:space-between;gap:10px;padding:11px 0;border-bottom:1px solid #F1F5F9">
          <div style="flex:1"><div style="font-size:13.5px;font-weight:600;color:#0F172A">Acesso às notificações</div><div style="font-size:11px;color:#94A3B8;margin-top:1px">Lê eventos oficiais da Uber/99 e identifica a plataforma da oferta</div></div>
          <div id="flash-perm-notification" style="display:flex;flex-direction:column;align-items:flex-end;gap:6px"></div>
        </div>
        <div style="display:flex;align-items:center;justify-content:space-between;gap:10px;padding:11px 0">
          <div style="flex:1">
            <div style="font-size:13.5px;font-weight:600;color:#0F172A">Leitura da tela</div>
            <div style="font-size:11px;color:#94A3B8;margin-top:1px">Completa dados da oferta que não vierem pelos eventos oficiais</div>
          </div>
          <div id="flash-perm-capture" style="display:flex;flex-direction:column;align-items:flex-end;gap:6px"></div>
        </div>
      </div>

      <button id="flash-step2-primary" class="btn-p" style="margin-top:16px;background:#22C55E" onclick="flashWizardFinish(true)">Iniciar jornada</button>
      <button id="flash-skip2" class="xbtn" style="width:100%;text-align:center;font-size:12.5px;color:#64748B;font-weight:700;margin-top:12px;background:none;border:0;padding:8px;text-decoration:underline;height:auto" onclick="flashWizardFinish(false)">Iniciar sem o MōB Flash</button>
    </div>

    <!-- Painel da régua — edição de limite vermelho/verde de um KPI -->
    <div id="flash-ruler" style="display:none">
      <div class="sheet-head">
        <button style="font-size:12px;color:#94A3B8;font-weight:700;background:none;border:0;padding:0" onclick="flashRulerClose()">&#8249; Voltar</button>
        <button class="xbtn" onclick="closeMo('mo-flash')">&#x2715;</button>
      </div>
      <div style="display:flex;align-items:center;gap:9px;margin:6px 0 4px">
        <span id="flash-ruler-ico" style="width:26px;height:26px;border-radius:8px;background:#F1F5F9;display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:900;color:#0A2F6B"></span>
        <h3 id="flash-ruler-title" style="font-family:var(--font-brand);font-size:15px;font-weight:800;color:#0F172A"></h3>
      </div>
      <div style="font-size:11.5px;color:#94A3B8;margin-bottom:26px;line-height:1.5">Arraste os pontos pra ajustar onde o card vira vermelho, amarelo ou verde.</div>

      <div id="flash-ruler-track" style="position:relative;height:10px;border-radius:99px;background:linear-gradient(90deg,#EF4444 0%,#EF4444 30%,#F59E0B 30%,#F59E0B 70%,#10B981 70%,#10B981 100%);margin:0 12px 34px">
        <div id="flash-ruler-red" style="position:absolute;top:50%;width:24px;height:24px;border-radius:50%;background:#fff;border:3px solid #0A2F6B;transform:translate(-50%,-50%);box-shadow:0 2px 6px rgba(0,0,0,.25);touch-action:none;cursor:grab;left:30%">
          <span id="flash-ruler-red-val" style="position:absolute;top:-30px;left:50%;transform:translateX(-50%);font-size:12px;font-weight:800;color:#0F172A;background:#fff;padding:2px 8px;border-radius:8px;border:1px solid #E2E8F0;white-space:nowrap"></span>
        </div>
        <div id="flash-ruler-green" style="position:absolute;top:50%;width:24px;height:24px;border-radius:50%;background:#fff;border:3px solid #0A2F6B;transform:translate(-50%,-50%);box-shadow:0 2px 6px rgba(0,0,0,.25);touch-action:none;cursor:grab;left:70%">
          <span id="flash-ruler-green-val" style="position:absolute;top:-30px;left:50%;transform:translateX(-50%);font-size:12px;font-weight:800;color:#0F172A;background:#fff;padding:2px 8px;border-radius:8px;border:1px solid #E2E8F0;white-space:nowrap"></span>
        </div>
      </div>
      <div style="display:flex;justify-content:space-between;font-size:9.5px;font-weight:800;color:#94A3B8;text-transform:uppercase;margin:0 12px 26px">
        <span>Vermelho</span><span>Amarelo</span><span>Verde</span>
      </div>

      <button class="btn-p" onclick="flashRulerClose()">Pronto</button>
    </div>

    <!-- Painel de valor único — recusas numéricas (nota/passageiro novo/distância) -->
    <div id="flash-recusa-num" style="display:none">
      <div class="sheet-head">
        <button style="font-size:12px;color:#94A3B8;font-weight:700;background:none;border:0;padding:0" onclick="flashRecusaNumClose()">&#8249; Voltar</button>
        <button class="xbtn" onclick="closeMo('mo-flash')">&#x2715;</button>
      </div>
      <div style="display:flex;align-items:center;gap:9px;margin:6px 0 4px">
        <span id="flash-recusa-num-ico" style="width:26px;height:26px;border-radius:8px;background:#FEF2F2;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:900;color:#DC2626"></span>
        <h3 id="flash-recusa-num-title" style="font-family:var(--font-brand);font-size:15px;font-weight:800;color:#0F172A"></h3>
      </div>
      <div id="flash-recusa-num-phrase" style="font-size:12.5px;color:#DC2626;font-weight:700;background:#FEF2F2;border-radius:12px;padding:10px 12px;margin:10px 0 24px;line-height:1.5"></div>

      <div style="display:flex;align-items:center;justify-content:center;gap:16px;margin-bottom:18px">
        <button onclick="flashRecusaNumStep(-1)" style="width:42px;height:42px;border-radius:50%;background:#F1F5F9;border:0;font-size:20px;font-weight:800;color:#0A2F6B">&#8722;</button>
        <div style="min-width:96px;text-align:center">
          <div id="flash-recusa-num-val" style="font-family:var(--font-brand);font-size:32px;font-weight:800;color:#0F172A"></div>
          <div id="flash-recusa-num-unit" style="font-size:11px;color:#94A3B8;font-weight:700;text-transform:uppercase;margin-top:2px"></div>
        </div>
        <button onclick="flashRecusaNumStep(1)" style="width:42px;height:42px;border-radius:50%;background:#F1F5F9;border:0;font-size:20px;font-weight:800;color:#0A2F6B">+</button>
      </div>
      <input id="flash-recusa-num-slider" type="range" style="width:100%;margin-bottom:26px;accent-color:#0A2F6B" oninput="flashRecusaNumFromSlider(this.value)">

      <button class="btn-p" onclick="flashRecusaNumClose()">Pronto</button>
    </div>

    <!-- Tela de endereço recusado — embarque OU destino -->
    <div id="flash-enderecos" style="display:none;flex-direction:column;min-height:72vh">
      <div class="sheet-head">
        <button style="font-size:12px;color:#94A3B8;font-weight:700;background:none;border:0;padding:0" onclick="flashEnderecosClose()">&#8249; Voltar</button>
        <button class="xbtn" onclick="closeMo('mo-flash')">&#x2715;</button>
      </div>
      <h3 style="font-family:var(--font-brand);font-size:15px;font-weight:800;color:#0F172A;margin:6px 0 4px">Endereço recusado</h3>
      <div style="font-size:12px;color:#64748B;line-height:1.5;margin-bottom:14px">Corridas com <b>embarque ou destino</b> nesses locais são recusadas automaticamente.</div>

      <div style="display:flex;gap:6px">
        <input id="flash-endereco-input" placeholder="Bairro ou rua…" autocomplete="off" style="flex:1;border:1.5px solid #0A2F6B;border-radius:12px;padding:10px 12px;font-size:12.5px;font:inherit" oninput="flashEnderecoSearch(this.value)" onkeydown="if(event.key==='Enter'){flashEnderecoAdd();}">
        <button onclick="flashEnderecoAdd()" style="background:#0A2F6B;color:#fff;border:0;border-radius:12px;padding:0 18px;font-weight:800;font-size:15px">+</button>
      </div>
      <div id="flash-endereco-suggestions" style="display:none;background:#fff;border:1px solid #E2E8F0;border-radius:12px;margin-top:6px;overflow:hidden;box-shadow:0 6px 18px rgba(15,23,42,.08)"></div>

      <div style="font-size:11px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.05em;margin:18px 0 8px">Bloqueados</div>
      <div id="flash-enderecos-list" style="margin-bottom:12px"></div>

      <div style="flex:1"></div>
      <button class="btn-p" style="margin-top:20px" onclick="flashEnderecosClose()">Pronto</button>
    </div>

    <div style="font-size:11px;color:#94A3B8;text-align:center;margin-top:10px;line-height:1.5">Os indicadores usam o valor/km/min já lidos hoje + seu custo/km cadastrado em Financeiro.</div>
  </div>
</div>



<!-- MODAL: Importar Histórico -->
<div class="mo ctr" id="mo-hist-import">
  <div class="sheet ctr" style="max-width:400px">
    <div class="sheet-head"><h2>Importar Histórico</h2><button class="xbtn" onclick="closeMo('mo-hist-import')">&#x2715;</button></div>
    <div style="background:#EFF6FF;border:1px solid rgba(14,165,233,.2);border-radius:16px;padding:12px 14px;font-size:12px;color:#0EA5E9;line-height:1.6;margin-bottom:16px;display:flex;gap:8px">
      <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" style="flex-shrink:0;margin-top:2px"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
      <div>Selecione o arquivo <strong>smartmobi_historico_uber.json</strong> gerado pelo sistema. Corridas j&#225; existentes n&#227;o ser&#227;o duplicadas.</div>
    </div>
    <label style="display:f
===== #F59E0B @ 543380 =====
l:'Combustível', icon:'fuel'},
    {id:'alim', l:'Alimentação', icon:'food'},
    {id:'man',  l:'Manutenção', icon:'wrench'},
    {id:'park', l:'Estacionamento', icon:'parking'},
    {id:'ped',  l:'Pedágio', icon:'road'},
    {id:'lav',  l:'Lavagem', icon:'car'},
    {id:'seg',  l:'Seguro', icon:'card'},
    {id:'out',  l:'Outros', icon:'plus'}
  ],
  despesa_pessoal: [
    {id:'alug', l:'Aluguel', icon:'home'},
    {id:'merc', l:'Mercado', icon:'card'},
    {id:'alim', l:'Alimentação', icon:'food'},
    {id:'cart', l:'Cartão', icon:'card'},
    {id:'trans', l:'Transporte', icon:'car'},
    {id:'saude', l:'Saúde', icon:'plus'},
    {id:'lazer', l:'Lazer', icon:'gift'},
    {id:'out',  l:'Outros', icon:'plus'}
  ],
  receita_operacional: [
    {id:'gort', l:'Gorjeta', icon:'coin'},
    {id:'bon',  l:'Bônus', icon:'gift'},
    {id:'reemb', l:'Reembolso', icon:'card'},
    {id:'serv', l:'Serviço extra', icon:'briefcase'},
    {id:'part', l:'Particular', icon:'user'},
    {id:'inc',  l:'Incentivo', icon:'plus'}
  ],
  receita_pessoal: [
    {id:'sal',  l:'Salário', icon:'briefcase'},
    {id:'rend', l:'Renda extra', icon:'coin'},
    {id:'pix',  l:'Pix recebido', icon:'card'},
    {id:'reemb', l:'Reembolso', icon:'card'},
    {id:'pres', l:'Presente', icon:'gift'},
    {id:'out',  l:'Outros', icon:'plus'}
  ]
};

// ── GLOBAL STATE ──────────────────────────────────────────────
let S = {
  sec: 'home',
  homePeriod: 'w',
  hgCustomStart: null, hgCustomEnd: null,
  hgDiaMode: 'd', hgHoraMode: 'h', hgMesMode: 'm', hgRevDesp: 'receita',
  finPer: 'month',
  finScope: 'all',
  lancFilter: 'all',
  lancStatus: 'all',
  lancPeriod: '7d',
  lancDate: null,
  curDate: DB.today(),
  lancType: 'receita',
  lancScope: 'operacional',
  lancRecFreq: 'Semanal',
  corPlat: 'uber',
  corridasPeriod: 'hoje', corridasDate: null,
  corridasPlat: 'all',
  tripsExpanded: false,
  sessionsExpanded: false,
  lancExpanded: false,
  _midnightIsPostStop: false,
  _postStopMidnightSess: null,
  _postMidnightCallback: null,
  _midnightModalOpen: false,
  gps: {
    st: 'idle',  // idle | running
    start: null, pausedMs: 0, pauseAt: null,
    km: 0, pts: [], wid: null, ti: null,
    forceDate: null // override manual (opção 'deixar em ontem' na virada de meia-noite)
  }
};

// ── MODULES (for Mais screen) ─────────────────────────────────
const MODULES = [
  {id:'financeiro', label:'Financeiro', sub:'Gestão e análise financeira', color:'blue',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 3v18M17 7H9.5a3.5 3.5 0 0 0 0 7H14a3.5 3.5 0 0 1 0 7H6"/></svg>'},
  {id:'planejar-jornada', label:'Planejar jornada', sub:'Defina dias, metas e horários de trabalho', color:'green',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18M8 14h.01M12 14h.01M16 14h.01M8 18h.01M12 18h.01"/></svg>'},
  {id:'lancamentos', label:'Lançamentos', sub:'Histórico de receitas e despesas', color:'orange',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 19V5a2 2 0 0 1 2-2h12v18H6a2 2 0 0 1-2-2Z"/><path d="M8 7h8M8 11h8M8 15h5"/></svg>'},
];

const MODULE_COLORS = {blue:'#EFF6FF', purple:'#F5F3FF', green:'#ECFDF3', orange:'#FFF7ED'};
const MODULE_ICONS_COLOR = {blue:'#0A2F6B', purple:'#7C3AED', green:'#22C55E', orange:'#F59E0B'};

// ── NAVIGATION ────────────────────────────────────────────────
function navTo(sec) {
  // Ocultar/mostrar nav na tela de Informes (o painel usa z-index alto mas o cat-row precisa da nav escondida)
  const bottomNav = document.querySelector('.bottom');
  if (bottomNav) bottomNav.style.display = 'flex';
  document.querySelectorAll('.screen').forEach(s => {
    s.classList.remove('on');
    s.style.display = '';  // limpa inline style para CSS funcionar
  });
  document.querySelectorAll('.nav').forEach(n => n.classList.remove('active'));
  const sc = document.getElementById('sc-'+sec);
  if (sc) { sc.classList.add('on'); sc.style.display = 'block'; }
  // Map sub-screens to their nav parent
  const navMap = {financeiro:'fav', lancamentos:'fav', relatorios:'fav', 'planejar-jornada':'mais', corridas:'jornada', agenda:'mais', informes:'agenda', categorias:'mais'};
  const navId = navMap[sec] || sec;
  const bn = document.getElementById('bn-'+navId);
  if (bn) bn.classList.add('active');
  S.sec = sec;
  try { localStorage.setItem('sm_last_screen', JSON.stringify({sec, ts: Date.now()})); } catch(e) {}
  const ms = document.getElementById('main-scroll');
  if (ms) ms.scrollTop = 0;
  if (sec === 'home')       renderFeedHome();
  if (sec === 'relatorios') renderHome();
  if (sec === 'jornada') {
    renderJornada(); // render imediato com dado local (nunca trava esperando rede)
    updateNetStatus();
    if (_supaUser && navigator.onLine) {
      reconcileJornadaHoje().then(() => renderJornada()).catch(()=>{});
      // Se o app local acha que está idle mas existe jornada aberta no
      // Supabase há mais de 15min (provável órfã), fecha automaticamente
      if (S.gps.st === 'idle') {
        _closeOrphanSessions().then(n => { if (n) renderJornada(); }).catch(()=>{});
      }
    }
  }
  if (sec === 'financeiro') renderFin();
  if (sec === 'lancamentos') renderLancamentos();
  if (sec === 'corridas') {
    S.corridasPlats = new Set(); // reseta sempre — nenhum filtro marcado = mostra tudo
    document.getElementById('cor-plat-uber')?.classList.remove('active');
    document.getElementById('cor-plat-99')?.classList.remove('active');
    renderCorridasActive();
  }
  if (sec === 'mais')       renderMais();
  if (sec === 'categorias') renderCatsModal();
  if (sec === 'agenda')     { renderAgenda(); agScrollToFirst(); }
  if (sec === 'informes')   renderInformes();
}
function openSuporte() {
  const el = document.getElementById('suporte-version');
  if (el) {
    const v = window.SmartMobiNative?.getVersion?.();
    el.textContent = v ? ('App nativo v' + v) : 'Acessando pelo navegador (sem app nativo)';
  }
  openMo('mo-suporte');
}
function navToFav() {
  const cfg = DB.cfg();
  navTo(cfg.favModule || 'financeiro');
}
function openMo(id) { const el = document.getElementById(id); if (el) el.classList.add('on'); }
function closeMo(id) { const el = document.getElementById(id); if (el) el.classList.remove('on'); }
document.addEventListener('click', e => {
  if (e.target.classList.contains('mo')) e.target.classList.remove('on');
});

// ── MAIS SCREEN ───────────────────────────────────────────────
function renderMais() {
  const vEl = document.getElementById("app-code-ver");
  if (vEl) vEl.textContent = typeof _APP_CODE_VER !== "undefined" ? _APP_CODE_VER : "?";

  const cfg = DB.cfg();
  const favId = cfg.favModule || 'financeiro';
  const container = document.getElementById('mais-modules');
  if (!container) return;
  container.innerHTML = MODULES.map(m => `
    <div style="background:#fff;border-radius:20px;border:1px solid #E2E8F0;box-shadow:0 2px 10px rgba(15,23,42,.05);padding:14px 16px;display:flex;align-items:center;gap:12px;cursor:pointer" onclick="navTo('${m.id}')">
      <div style="width:42px;height:42px;border-radius:14px;background:${MODULE_COLORS[m.color]};display:flex;align-items:center;justify-content:center;color:${MODULE_ICONS_COLOR[m.color]};flex-shrink:0">${m.icon}</div>
      <div style="flex:1"><div style="font-size:14px;font-weight:700">${m.label}</div><div style="font-size:12px;color:#64748B;margin-top:2px">${m.sub}</div></div>
      <button onclick="event.stopPropagation();setFavModule('${m.id}')" style="border:none;background:${m.id===favId?MODULE_COLORS[m.color]:'transparent'};border-radius:8px;padding:6px;cursor:pointer;color:${m.id===favId?MODULE_ICONS_COLOR[m.color]:'#94A3B8'};display:flex;align-items:center;justify-content:center" title="Fixar no menu">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="${m.id===favId?'currentColor':'none'}" stroke="currentColor" stroke-width="2"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
      </button>
    </div>`
  ).join('');
  // Update perfil sub
  const sub = document.getElementById('mais-perfil-sub');
  if (sub && cfg.name) sub.textContent = cfg.name + (cfg.vehicle ? ' · ' + cfg.vehicle : '');
  const custoSub = document.getElementById('mais-custo-sub');
  if (custoSub) {
    custoSub.textContent = cfg.custoVeiculo?.configurado
      ? DB.fRI(cfg.custoVeiculo.custoPorKm) + '/km configurado'
      : 'Configure pra ver o lucro líquido real';
  }
}
function setFavModule(id) {
  const cfg = DB.cfg(); cfg.favModule = id; DB.saveCfg(cfg);
  updateFavBtn();
  renderMais();
}
function updateFavBtn() {
  const cfg = DB.cfg();
  const mod = MODULES.find(m => m.id === (cfg.favModule || 'financeiro')) || MODULES[0];
  const lblEl = document.getElementById('fav-lbl');
  const icoEl = document.getElementById('fav-icon');
  if (lblEl) lblEl.textContent = mod.label;
  if (icoEl) icoEl.outerHTML = mod.icon.replace('<svg', '<svg id="fav-icon"');
}

// ── HOME ──────────────────────────────────────────────────────
function setHomePeriod(p) {
  S.homePeriod = p;
  ['w','m','y'].forEach(x => {
    const el = document.getElementById('hg-tab-'+x);
    if (el) el.classList.toggle('active', x === p);
  });
  renderHomeGraphs();
}
function openHgFilter() {
  const sEl = document.getElementById('hg-filter-start'), eEl = document.getElementById('hg-filter-end');
  if (sEl) sEl.value = S.hgCustomStart || DB.today();
  if (eEl) eEl.value = S.hgCustomEnd || DB.today();
  openMo('mo-hg-filter');
}
function applyHgFilter() {
  const s = document.getElementById('hg-filter-start')?.value;
  const e = document.getElementById('hg-filter-end')?.value;
  if (!s || !e || s > e) { showToast('Escolha um intervalo de datas válido', 'error'); return; }
  S.hgC
===== #F59E0B @ 556565 =====
sPrev ? getHomeDaysPrev() : null;
  let revPrev = null, tcPrev = null;
  if (prevDates && prevDates.length) {
    const pd = prevDates.map(d => ({ ...DB.day(d), trips: DB.day(d).trips.concat(autoTripsAsTrips(d)) }));
    revPrev = pd.reduce((s,d) => s + d.trips.reduce((a,t)=>a+(t.value||0),0) + d.revenues.reduce((a,r)=>a+(r.amount||0),0), 0);
    tcPrev  = pd.reduce((s,d) => s + d.trips.length, 0);
  }

  // melhor/pior dia da semana
  const lblsDia = ['Seg','Ter','Qua','Qui','Sex','Sáb','Dom'];
  const dates = getHomeDays();
  const diaData = hgBucketStats(dates,
    date => lblsDia[(new Date(date+'T00:00:00').getDay()+6)%7],
    hgDayTotal, lblsDia).filter(d => d.value > 0);
  const melhorDia = diaData.length ? diaData.reduce((a,b)=>b.avg>a.avg?b:a).label : null;
  const piorDia   = diaData.length > 1 ? diaData.reduce((a,b)=>b.avg<a.avg?b:a).label : null;

  // melhor hora
  const horaOrder = Array.from({length:24},(_,h)=>h+'h');
  const horaKeyFn = date => {
    const trips = dayTripsAll(date).filter(t => t.time);
    const pk = {};
    trips.forEach(t => { const h = parseInt(t.time.split(':')[0])+'h'; pk[h]=(pk[h]||0)+(t.value||0); });
    return Object.keys(pk).map(k=>({key:k,value:pk[k]}));
  };
  const horaData = hgBucketStats(dates, horaKeyFn, null, horaOrder).filter(d=>d.value>0);
  const melhorHora = horaData.length ? horaData.reduce((a,b)=>b.avg>a.avg?b:a).label : null;

  // % meta
  let metaPct = null;
  try {
    const plan = DB.get ? DB.get('sm_plan_jornada') : null;
    if (plan && plan.moneyGoal > 0) metaPct = Math.round(rev / plan.moneyGoal * 100);
  } catch(e) {}

  const diasTrabalhados = dates.filter(d => dayTripsAll(d).length > 0).length || 1;
  const nDays = diasTrabalhados; // ritmo por dia TRABALHADO, não por dia do período
  const rpkm  = km > 0 ? rev/km : 0;
  const rphr  = hrs > 0 ? rev/hrs : 0;

  // confiabilidade dos dados de hora — só conta corridas que vieram com hora registrada
  const corridasComHora = dates.reduce((s,d) => s + dayTripsAll(d).filter(t => t.time && t.time.trim()).length, 0);

  try {
    const { data:{ session } } = await _SUPA.auth.getSession();
    if (!session) throw new Error('no session');
    const resp = await fetch(
      'https://jlsrpptslwfhmkvelaro.supabase.co/functions/v1/gerar-insight',
      { method: 'POST',
        headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + session.access_token },
        body: JSON.stringify({ periodo: S.homePeriod||'w', faturamento: rev, corridas: tc, km,
          rpkm, rphr, lucro_pct: pctL, despesas: exp,
          faturamento_prev: revPrev, corridas_prev: tcPrev,
          melhor_dia: melhorDia, pior_dia: piorDia, melhor_hora: melhorHora,
          corridas_com_hora: corridasComHora,
          ritmo_dia: rev/nDays, dias_trabalhados: nDays, meta_pct: metaPct }) });
    if (!resp.ok) throw new Error('api '+resp.status);
    const json = await resp.json();
    const insight = json.insight || '';
    if (insight) { localStorage.setItem(cacheKey, insight); txt.textContent = insight; }
    else { el.style.display = 'none'; }
  } catch(e) {
    const refLbl = {w:'semana passada',m:'mês passado',y:'ano passado'}[S.homePeriod]||'período anterior';
    const ritmo = DB.fR(rev/nDays);
    txt.textContent = revPrev > 0
      ? `${rev>revPrev?'+':''}${Math.round((rev-revPrev)/revPrev*100)}% vs ${refLbl}. Ritmo de ${ritmo}/dia.`
      : `Ritmo de ${ritmo}/dia no período.`;
  }
}
const HG_PALETTE = ['#0ea5e9','#22c55e','#8b5cf6','#f59e0b','#ef4444','#14b8a6','#64748b'];
function hgBuildPie(items, pieElId, listElId, valueFmt) {
  const filtered = items.filter(i => i.value > 0.001);
  const pieEl = document.getElementById(pieElId);
  const listEl = document.getElementById(listElId);
  if (!filtered.length) {
    if (pieEl) pieEl.style.background = '#EEF2F7';
    if (listEl) listEl.innerHTML = '<div style="font-size:12px;color:#94A3B8">Sem dados nesse período</div>';
    return;
  }
  const total = filtered.reduce((s,i)=>s+i.value,0);
  let acc = 0;
  const stops = filtered.map((it,i) => {
    const color = it.color || HG_PALETTE[i % HG_PALETTE.length];
    const from = acc, pct = it.value/total*100;
    acc += pct;
    return color+' '+from.toFixed(2)+'% '+acc.toFixed(2)+'%';
  }).join(', ');
  if (pieEl) pieEl.style.background = 'conic-gradient('+stops+')';
  if (listEl) listEl.innerHTML = filtered.map((it,i) => {
    const color = it.color || HG_PALETTE[i % HG_PALETTE.length];
    const pct = Math.round(it.value/total*100);
    return '<div class="hg-pie-item"><div><i style="background:'+color+'"></i>'+it.label+'</div><strong>'+(valueFmt ? valueFmt(it.value) : pct+'%')+'</strong></div>';
  }).join('');
}
function hgCatPanelHtml(items) {
  const filtered = items.filter(i=>i.value>0.001);
  if (!filtered.length) return '<div style="font-size:12px;color:#94A3B8;text-align:center;padding:10px 0">Sem dados nesse período</div>';
  const total = filtered.reduce((s,i)=>s+i.value,0);
  const max = Math.max(...filtered.map(i=>i.value));
  return filtered.map((it,i) => {
    const color = it.color || HG_PALETTE[i % HG_PALETTE.length];
    const pct = Math.round(it.value/total*100);
    const w = Math.max(4, Math.round(it.value/max*100));
    return '<div class="hg-cat-row"><div class="hg-cat-name">'+it.label+'</div><div class="hg-cat-track"><i style="width:'+w+'%;background:'+color+'"></i></div><div class="hg-cat-value">'+pct+'%</div></div>';
  }).join('');
}
function hgNiceMax(v) {
  const mag = Math.pow(10, Math.floor(Math.log10(v || 1)));
  const n = v / mag;
  let f; if (n <= 1) f = 1; else if (n <= 2) f = 2; else if (n <= 5) f = 5; else f = 10;
  return f * mag;
}
function hgFmtAxis(v) {
  if (v >= 1000) return (v/1000).toFixed(v % 1000 === 0 ? 0 : 1) + 'k';
  return Math.round(v).toString();
}
function hgBarsHtml(data, chartId, rich) {
  if (rich == null) rich = true;
  const items = data.filter(d => d.value > 0.001);
  if (!items.length) return '<div class="hg-bar-empty">Sem dados nesse período</div>';
  const rawMax = Math.max(...items.map(d=>d.value), 1);
  const top = hgNiceMax(rawMax * 1.08);
  const H = 124;

  // tooltip único por chart, fora do container de rolagem — evita o clip do overflow-x:auto
  // que ignorava overflow-y:visible por especificação do CSS
  const bubHtml = `<div class="hg-bar-bubble" id="hgbub-${chartId}"></div>`;

  let gridHtml = '';
  for (let i = 0; i <= 3; i++) {
    const frac = i/3, val = top*(1-frac);
    gridHtml += `<div class="hg-grid-line" style="top:${Math.round(frac*H)}px"><span>${hgFmtAxis(val)}</span></div>`;
  }
  // armazena os dados de cada barra em data-* pra hgToggleBar poder acessar sem recomputar
  const barsHtml = items.map((d,i) => {
    const h = Math.max(3, Math.round(d.value/top*H));
    return `<div class="hg-bar-col">
      <div class="hg-bar" id="hgbar-${chartId}-${i}" style="height:${h}px"
        data-val="${d.value}" data-avg="${d.avg||d.value}" data-max="${d.max||d.value}"
        data-min="${d.min||d.value}" data-qtd="${d.qtd||1}" data-lbl="${d.label}"
        data-prev="${i>0?items[i-1].value:''}" data-prevlbl="${i>0?items[i-1].label:''}"
        data-rich="${rich?1:0}"
        onclick="hgToggleBar('${chartId}',${i})"></div>
      <span class="hg-bar-lbl" id="hglbl-${chartId}-${i}">${d.label}</span>
    </div>`;
  }).join('');
  return bubHtml + gridHtml + `<div class="hg-bars-row">${barsHtml}</div>`;
}
function hgFitColWidths(containerId) {
  const row = document.querySelector('#'+containerId+' .hg-bars-row');
  if (!row) return;
  const cols = row.querySelectorAll('.hg-bar-col');
  const n = cols.length;
  if (!n) return;
  const visibleN = Math.min(n, 12);
  const w = Math.max(24, row.clientWidth / visibleN);
  const barW = Math.min(64, Math.round(w * 0.62));
  cols.forEach(c => {
    c.style.flex = '0 0 '+w+'px';
    const bar = c.querySelector('.hg-bar');
    if (bar) bar.style.maxWidth = barW+'px';
  });
}
function showChartInfo(btn, text) {
  const tip = document.getElementById('hg-info-tip');
  if (!tip) return;
  const sameOpen = tip.classList.contains('show') && tip._btn === btn;
  tip.classList.remove('show');
  if (sameOpen) { tip._btn = null; return; }
  tip.textContent = text;
  tip._btn = btn;
  requestAnimationFrame(() => {
    const r = btn.getBoundingClientRect();
    const margin = 12;
    const tw = tip.offsetWidth, th = tip.offsetHeight;
    let left = r.left + r.width/2 - tw/2;
    if (left < margin) left = margin;
    if (left + tw > window.innerWidth - margin) left = window.innerWidth - margin - tw;
    const top = r.top - th - 10;
    tip.style.left = left+'px';
    tip.style.top = Math.max(margin, top)+'px';
    tip.style.setProperty('--hg-tip-arrow', (r.left + r.width/2 - left - 6)+'px');
    tip.classList.add('show');
  });
}
const S_hgActiveBar = {};
function hgToggleBar(chartId, i) {
  const wasActive = S_hgActiveBar[chartId] === i;
  const root = document.getElementById('hg-chart-'+chartId);
  if (root) {
    root.querySelectorAll('.hg-bar.dim,.hg-bar.sel').forEach(b=>b.classList.remove('dim','sel'));
    root.querySelectorAll('.hg-bar-lbl.sel').forEach(s=>s.classList.remove('sel'));
  }
  const bub = root ? root.querySelector('.hg-bar-bubble') : null;
  if (bub) bub.classList.remove('show');
  if (wasActive) { delete S_hgActiveBar[chartId]; return; }
  S_hgActiveBar[chartId] = i;
  const bar = document.getElementById('hgbar-'+chartId+'-'+i);
  const lbl = document.getElementById('hglbl-'+chartId+'-'+i);
  if (bar) bar.classList.add('sel');
  if (lbl) lbl.classList.add('sel');
  if (root) root.querySelectorAll('.hg-bar').forEach(b => { if (b !== bar) b.classList.add('dim'); });
  if (bub && bar) {
    // monta o conteúdo do tooltip com os dados em data-*
    const val  = parseFloat(bar.dataset.val);
    const avg  = parseFloat(bar.dataset.avg);
    const max  = parseFloat(bar.dataset.max);
    const min  = parseFloat(bar.dataset.min);
    const qtd  = parseInt(bar.dataset.qtd);
    const lbl_ = bar.dataset.lbl;
 
===== #F59E0B @ 576519 =====
ment.getElementById('hg-chart-hora');
  if (!el) return;
  const dates = getHomeDays();
  const turnos = [
    {label:'Madrugada', test:h=>h>=0&&h<6}, {label:'Manhã', test:h=>h>=6&&h<12},
    {label:'Tarde', test:h=>h>=12&&h<18}, {label:'Noite', test:h=>h>=18&&h<24}
  ];
  // pra cada dia, quebra os valores por hora (ou turno) ANTES de agregar entre dias —
  // assim um dia com 2 corridas na mesma hora conta certo, e dias diferentes na mesma
  // hora ficam no mesmo balde.
  const keyFn = date => {
    const trips = dayTripsAll(date).filter(t => t.time);
    const perKey = {};
    trips.forEach(t => {
      const hr = parseInt(t.time.split(':')[0]);
      const k = S.hgHoraMode === 'h' ? (hr+'h') : turnos.find(tu=>tu.test(hr)).label;
      perKey[k] = (perKey[k]||0) + (t.value||0);
    });
    return Object.keys(perKey).map(k => ({key:k, value:perKey[k]}));
  };
  const order = S.hgHoraMode === 'h' ? Array.from({length:24},(_,h)=>h+'h') : turnos.map(t=>t.label);
  const data = hgBucketStats(dates, keyFn, null, order);
  el.innerHTML = hgBarsHtml(data, 'hora', false);
  hgFitColWidths('hg-chart-hora');
  renderHgHighlight('hg-hora-insight', data, 'forte', d => 'Melhor horário: '+d.label+' (média '+DB.fR(d.avg)+')');
}

// ── Por Mês/Ano ──
function setHgMesMode(m) {
  S.hgMesMode = m;
  ['m','y'].forEach(x => { const e=document.getElementById('hg-mes-'+x); if(e) e.classList.toggle('active', x===m); });
  renderHgChartMes();
}
function renderHgChartMes() {
  const el = document.getElementById('hg-chart-mes');
  if (!el) return;
  const dates = getHomeDays();
  const mesesLbl = ['Jan','Fev','Mar','Abr','Mai','Jun','Jul','Ago','Set','Out','Nov','Dez'];
  let data;
  if (S.hgMesMode === 'm') {
    const keyFn = date => mesesLbl[parseInt(date.split('-')[1],10)-1];
    const order = [...new Set(dates.map(keyFn))];
    data = hgBucketStats(dates, keyFn, hgDayTotal, order);
  } else {
    const keyFn = date => date.split('-')[0];
    const order = [...new Set(dates.map(keyFn))];
    data = hgBucketStats(dates, keyFn, hgDayTotal, order);
  }
  // Com período Semana/Mês selecionado em cima, isso aqui naturalmente vira 1 barra só
  // (só existe 1 mês/ano dentro do recorte) — é o esperado, segue a mesma regra dos outros.
  el.innerHTML = hgBarsHtml(data, 'mes');
  hgFitColWidths('hg-chart-mes');
}

// Destaque automático (sem precisar tocar) — pega o balde de maior/menor média.
// tipo 'forte' pega o maior, 'fraco' pega o menor (entre os que têm dado).
function renderHgHighlight(elId, data, tipo, fmt) {
  const el = document.getElementById(elId);
  if (!el) return;
  if (!data.length) { el.textContent = ''; return; }
  const best = data.reduce((a,b) => (tipo==='forte' ? (b.avg>a.avg?b:a) : (b.avg<a.avg?b:a)));
  el.textContent = fmt(best);
}

// ── Plataformas (fixo, sem tabs) ──
function renderHgPlatformPie(days) {
  const allTrips = days.flatMap(d => d.trips);
  const uberR = allTrips.filter(t=>t.platform==='uber').reduce((s,t)=>s+(t.value||0),0);
  const p99R  = allTrips.filter(t=>t.platform==='99').reduce((s,t)=>s+(t.value||0),0);
  const privR = allTrips.filter(t=>t.platform==='particular').reduce((s,t)=>s+(t.value||0),0);
  const outR  = allTrips.filter(t=>!['uber','99','particular'].includes(t.platform)).reduce((s,t)=>s+(t.value||0),0);
  hgBuildPie([
    {label:'Uber', value:uberR, color:'#0ea5e9'},
    {label:'99', value:p99R, color:'#22c55e'},
    {label:'Particular', value:privR, color:'#8b5cf6'},
    {label:'Outros', value:outR, color:'#f59e0b'}
  ], 'hg-pie-plat', 'hg-pie-plat-list');
}

// ── Receitas x Despesas (com tabs) ──
function setHgRevDesp(m) {
  S.hgRevDesp = m;
  ['receita','despesa'].forEach(x => { const e=document.getElementById('hg-rd-'+x); if(e) e.classList.toggle('active', x===m); });
  renderHgCatPie(getHomeDays().map(d => ({ ...DB.day(d), trips: DB.day(d).trips.concat(autoTripsAsTrips(d)) })));
}
function renderHgCatPie(days) {
  let items;
  if (S.hgRevDesp === 'receita') {
    const allTrips = days.flatMap(d => d.trips);
    const byPlat = [
      {label:'Uber', value: allTrips.filter(t=>t.platform==='uber').reduce((s,t)=>s+(t.value||0),0)},
      {label:'99', value: allTrips.filter(t=>t.platform==='99').reduce((s,t)=>s+(t.value||0),0)},
      {label:'Particular', value: allTrips.filter(t=>t.platform==='particular').reduce((s,t)=>s+(t.value||0),0)}
    ];
    const allRev = days.flatMap(d => d.revenues);
    const byCat = hgGroupBy(allRev, r => r.categoryLabel || r.category || 'Outros', r => r.amount||0);
    const catItems = Object.keys(byCat).map(k => ({label:k, value: byCat[k]}));
    items = byPlat.concat(catItems);
  } else {
    const allExp = days.flatMap(d => d.expenses);
    const byCat = hgGroupBy(allExp, e => e.categoryLabel || e.category || 'Outros', e => e.amount||0);
    items = Object.keys(byCat).map(k => ({label:k, value: byCat[k]}));
  }
  items.sort((a,b) => b.value - a.value);
  if (items.length > 6) {
    const top = items.slice(0,6);
    const restSum = items.slice(6).reduce((s,i)=>s+i.value,0);
    items = top.concat(restSum > 0 ? [{label:'Outros', value: restSum}] : []);
  }
  hgBuildPie(items, 'hg-pie-cat', 'hg-pie-cat-list', v => DB.fRI(v));
  const panelEl = document.getElementById('hg-cat-panel');
  if (panelEl) panelEl.innerHTML = hgCatPanelHtml(items);
}
function entryCard(e) {
  const isRev  = e._type === 'corrida' || e._type === 'receita';
  const val    = e.value || e.amount || 0;
  const label  = e._type === 'corrida' ? ((e.origin||'—')+(e.dest?' → '+e.dest:'')) : (e.desc || e.category || e._type);
  const color  = isRev ? 'var(--green2)' : 'var(--red)';
  const sign   = isRev ? '+' : '-';
  return `<div style="display:flex;align-items:center;gap:10px;padding:11px 0;border-bottom:1px solid #E2E8F0">
    <div style="flex:1;min-width:0"><div style="font-size:13px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${label}</div><div style="font-size:11px;color:#64748B;margin-top:2px">${e._type} · ${e._date}</div></div>
    <div style="font-family:'Inter',system-ui,sans-serif;font-weight:700;font-size:14px;color:${color};flex-shrink:0">${sign}${DB.fRI(val)}</div>
  </div>`;
}

// ── JORNADA ───────────────────────────────────────────────────
function chgDay(d) {
  const dt = new Date(S.curDate+'T12:00'); dt.setDate(dt.getDate()+d);
  S.curDate = dt.getFullYear()+'-'+p2(dt.getMonth()+1)+'-'+p2(dt.getDate());
  renderJornada();
}
function setJornadaDate(dateStr) {
  if (!dateStr) return;
  S.curDate = dateStr;
  renderJornada();
}
function fmtHM(ms) { if (!ms) return '--:--'; const dd=new Date(ms); return p2(dd.getHours())+':'+p2(dd.getMinutes()); }
function toggleTripsView() { S.tripsExpanded = !S.tripsExpanded; renderJornada(); }

// ── TELA CORRIDAS (lista dedicada, com filtro de período/plataforma e exclusão) ──
function getCorridasDays() {
  const today=new Date();
  const fmt=d=>d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
  const p=S.corridasPeriod;
  if(p==='date'&&S.corridasDate) return [S.corridasDate];
  if(p==='hoje') return [fmt(today)];
  if(p==='7d') { const out=[]; for(let i=0;i<7;i++){const d=new Date(today);d.setDate(d.getDate()-i);out.push(fmt(d));} return out; }
  if(p==='mes') { const m=today.getMonth(),y=today.getFullYear(); return DB.allDates().filter(d=>d.startsWith(y+'-'+p2(m+1))); }
  return DB.allDates();
}
function setCorridasPeriod(p) {
  S.corridasPeriod=p; S.corridasDate=null;
  ['hoje','7d','mes','tudo'].forEach(id=>{ const b=document.getElementById('cpb-'+id); if(b) b.classList.toggle('active',id===p); });
  const dp=document.getElementById('cor-date-picker'),db=document.getElementById('cor-date-btn');
  if(dp) dp.style.display='none'; if(db){db.style.background='#fff';db.style.borderColor='#E2E8F0';}
  renderCorridasActive();
}
function toggleCorDate() {
  const inp=document.getElementById('cor-date-input');
  if(inp) inp.showPicker ? inp.showPicker() : inp.click();
}
function setCorridasDate(d) {
  if(!d) return; S.corridasDate=d; S.corridasPeriod='date';
  ['hoje','7d','mes','tudo'].forEach(id=>{ const b=document.getElementById('cpb-'+id); if(b) b.classList.remove('active'); });
  const db=document.getElementById('cor-date-btn'); if(db){db.style.background='#EFF6FF';db.style.borderColor='#2563EB';}
  renderCorridasActive();
}
function toggleCorridasPlat(p) {
  // Multi-select agora (pedido do Yuri, 24/07/2026): nenhum marcado = mostra
  // tudo (padrão), marcar um filtra só aquele, marcar os dois volta a
  // mostrar tudo — sem precisar de um botão "Todas" separado.
  if (!S.corridasPlats) S.corridasPlats = new Set();
  if (S.corridasPlats.has(p)) S.corridasPlats.delete(p); else S.corridasPlats.add(p);
  const ub = document.getElementById('cor-plat-uber'), n99 = document.getElementById('cor-plat-99');
  if (ub) ub.classList.toggle('active', S.corridasPlats.has('uber'));
  if (n99) n99.classList.toggle('active', S.corridasPlats.has('99'));
  renderCorridasActive();
}
// ── Corridas: alterna entre Importadas (trips locais) e Automáticas (auto_trips no Supabase) ──
// ── Corridas automáticas → alimentam a Jornada (pedido do Yuri, 23/07/2026) ──
// Busca auto_trips do dia pra somar em ganhos/km/corridas da Jornada e da
// sessão certa dentro dela — sem se misturar com day.trips (Importadas),
// só somando junto na hora de mostrar total. Timestamp de referência:
// trip_started_at (início real da corrida) — cai sempre dentro da sessão
// (Jornada) que estava rodando, diferente de accepted_at que pode ser
// alguns segundos antes ainda na sessão anterior num caso raro de virada.
function autoTripTs(t) {
  const iso = t.trip_started_at || t.accepted_at;
  return iso ? new Date(iso).getTime() : 0;
}
// Corrida cai dentro do horário de alguma jornada do dia dela? Pedido do
// Yuri (24/07/2026) — o vínculo nunca é salvo, é sempre calculado (mesma
// lógica de sessionItem/renderJornada) — então dá pra checar pra QUALQUER
// corrida, de qualquer tela, sem precisar saber de antemão qual sessão.
functi
===== #F59E0B @ 709845 =====
4748B" stroke-width="2" style="flex:none"><circle cx="12" cy="12" r="9"/><path d="M12 8v5M12 16h.01"/></svg><span>Configure o <a onclick="event.stopPropagation();openCustoVeiculo()">custo do veículo</a> pra ver o lucro real de cada corrida.</span></div>`;
  }
  return html;
}
function tripItem(t, tripDate) {
  const plat = {uber:'Uber','99':'99',particular:'Particular'}[t.platform||'uber'] || 'Uber';
  const dest = t.dest ? ' → '+esc(t.dest) : '';
  const dur = t.duration ? ' · '+fmtDur(t.duration) : '';
  // tid sempre único por renderização (nunca reusa t.id) — evita colisão de
  // DOM id quando a mesma corrida aparece em mais de uma tela ao mesmo tempo
  // (ex: dentro da Jornada e na tela Corridas), o que fazia o clique parecer
  // não fazer nada (abria o painel de detalhe da OUTRA instância, escondida)
  const tid = 'ti'+Date.now().toString(36)+Math.random().toString(36).slice(2,8);
  const cashSvg = `<svg viewBox="0 0 24 24" width="13" height="13" fill="currentColor"><rect x="2" y="7" width="20" height="13" rx="2"/><path d="M2 10h20" stroke="white" stroke-width="1.2" fill="none"/><path d="M2 17h20" stroke="white" stroke-width="1.2" fill="none"/><circle cx="12" cy="13" r="2.5" fill="white"/></svg>`;
  const extras = [];
  if (t.dinamico>0) extras.push(`<span class="chip chip-d"><svg viewBox="0 0 24 24" width="9" height="9" fill="currentColor"><path d="M13 2 3 14h7l-1 8 10-12h-7z"/></svg><strong>${DB.fR(t.dinamico)}</strong></span>`);
  if (t.tip>0)     extras.push(`<span class="chip chip-g"><svg viewBox="0 0 24 24" width="9" height="9" fill="currentColor"><path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.29 1.5 4.05 3 5.5l7 7Z"/></svg><strong>${DB.fR(t.tip)}</strong></span>`);
  // Taxa: só mostra quando há dado real do zip ou OCR confirmando 0%
  let _feePct = null;
  if (t.serviceFee !== undefined && t.serviceFee !== null) {
    _feePct = t.serviceFee === 0 ? '0%' : (t.serviceFee*100).toFixed(1).replace('.',',')+'%';
  } else if (t.taxaZero) { _feePct = '0%'; }
  if (_feePct !== null) extras.push(`<span class="chip chip-taxa">${_feePct}</span>`);
  if (t.pedagio)    extras.push(`<span class="chip-icon chip-toll"><svg viewBox="0 0 24 24" width="11" height="11" fill="currentColor"><rect x="3" y="3" width="2" height="18" rx="1"/><rect x="19" y="3" width="2" height="18" rx="1"/><rect x="5" y="10" width="11" height="3" rx="1.5"/></svg></span>`);
  if (t.dinheiro)  extras.push(`<span class="chip-icon chip-cash">${cashSvg}</span>`);
  const extraRow = extras.length?`<div class="li-extra">${extras.join('')}</div>`:'';
  const perfStrip = perfStripHtml(t.value, t.km, t.duration);
  const detRows = [
    `<div class="det-row"><span>Plataforma</span><strong>${plat}</strong></div>`,
    t.origin?`<div class="det-row"><span>Origem</span><strong>${esc(t.originFull||t.origin)}</strong></div>`:'',
    t.dest?`<div class="det-row"><span>Destino</span><strong>${esc(t.destFull||t.dest)}</strong></div>`:'',
    t.km>0?`<div class="det-row"><span>Distância${t.kmEstimado?' (estimada)':''}</span><strong>${t.kmEstimado?'~':''}${(t.km||0).toFixed(1)} km</strong></div>`:'',
    t.pedagioValor>0?`<div class="det-row"><span>Pedágio</span><strong>${DB.fR(t.pedagioValor)}</strong></div>`:'',
    t.duration>0?`<div class="det-row"><span>Duração</span><strong>${fmtDur(t.duration)}</strong></div>`:'',
    t.dinamico>0?`<div class="det-row"><span>Preço dinâmico</span><strong style="color:#F59E0B">+ ${DB.fR(t.dinamico)}</strong></div>`:'',
    t.tip>0?`<div class="det-row"><span>Gorjeta</span><strong style="color:#7C3AED">+ ${DB.fR(t.tip)}</strong></div>`:'',
    (_feePct!==null)?`<div class="det-row"><span>Taxa Uber</span><strong style="${_feePct==='0%'?'color:#15803D':''}">${_feePct}</strong></div>`:'',
    t.pedagio?`<div class="det-row"><span>Pedágio</span><strong>Sim</strong></div>`:'',
    t.dinheiro?`<div class="det-row"><span>Pagamento</span><strong>Dinheiro</strong></div>`:'',
    tripDate?`<button class="det-del-btn" onclick="event.stopPropagation();confirmDeleteTrip('${tripDate}','${t.id}')"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/></svg> Excluir corrida</button>`:'',
  ].filter(Boolean).join('');
  const cvCfg = DB.cfg().custoVeiculo;
  let liqRow = '';
  if (cvCfg?.configurado && t.value > 0) {
    const liq = t.value - (cvCfg.custoPorKm||0) * (t.km||0);
    const pct = Math.round(liq / t.value * 100);
    liqRow = `<span class="li-liq">${DB.fR(liq)} (${pct}%)</span>`;
  }
  return `<div class="trip-item-wrap" id="tw-${tid}">
    <div class="li-row" id="tr-${tid}" onclick="toggleTripDetail('${tid}')">
      ${platIcon(t.platform)}
      <div class="li-info"><h3 class="trip-title">${t.origin?esc(t.origin):'—'}${dest}</h3><p>${plat} · ${t.kmEstimado?'~':''}${(t.km||0).toFixed(1)} km${t.kmEstimado?' (estim.)':''}${dur}</p>${extraRow}</div>
      <div class="li-val"><strong>${DB.fR(t.value)}</strong>${t.time?`<span class="li-time">${t.time}</span>`:''}${liqRow}</div>
    </div>
    <div class="trip-detail-panel" id="td-${tid}" style="display:none">${perfStrip}${detRows}</div>
  </div>`;
}
function lancItemJornada(it) {
  const isRec = it._type === 'receita';
  const catKey = (it._type || 'despesa') + '_' + (it.scope || 'operacional');
  const cats = DB.cats(catKey) || DEFAULT_CATS[catKey] || [];
  const cat = cats.find(c => c.id === it.category);
  const icoSvg = CAT_ICONS[cat?.icon] || CAT_ICONS.plus;
  // Categoria como título principal (descrição vai pro detalhe). Se o
  // lançamento estiver vinculado a uma corrida (it.platform), usa o
  // resumo do trajeto (ex: "Centro - Botafogo") em vez do nome da categoria.
  const routeTitle = (it.platform && (it.origin || it.dest))
    ? (it.origin||'—') + (it.dest ? ' - '+it.dest : '') : null;
  const categoryTitle = routeTitle || it.categoryLabel || cat?.label || it.category || (isRec ? 'Receita' : 'Despesa');
  const platBox = it.platform ? platIcon(it.platform) : `<div class="li-ico ${isRec?'green':'red'}">${icoSvg}</div>`;
  const val = isRec ? '+'+DB.fR(it.amount) : '-'+DB.fR(it.amount);
  const today = DB.today();
  const status = it.status || 'pago';
  const vencido = status === 'a_pagar' && it.date && it.date < today;
  const badgeCls = status==='pago' ? 'st-badge st-pago' : vencido ? 'st-badge st-vencido' : 'st-badge st-apagar';
  const badgeTxt = status==='pago' ? (isRec?'Recebido':'Pago') : vencido ? 'Vencido' : 'A pagar';
  const rowCls = 'li-row-j ' + (status==='pago' ? 'pago' : vencido ? 'vencido' : 'apagar');
  const fmtD = d => d ? d.split('-').slice(1).reverse().join('/') : '';
  const subTxt = (it.scope==='pessoal'?'Pessoal':'Operacional') + (it.date?' · '+fmtD(it.date):'');

  const rows = [
    it.desc    ? `<div class="lanc-detail-row"><span>Descrição</span><strong>${it.desc}</strong></div>` : '',
    it.date    ? `<div class="lanc-detail-row"><span>Data</span><strong>${fmtD(it.date)}</strong></div>` : '',
    `<div class="lanc-detail-row"><span>Tipo</span><strong>${isRec?'Receita':'Despesa'}</strong></div>`,
  ].filter(Boolean).join('');

  const refArgs = `'${it.id}','${it.date||today}','${it._type||'despesa'}'`;
  const pagarBtn = status !== 'pago'
    ? `<button class="ld-btn pago" onclick="event.stopPropagation();markLancPago(${refArgs})">
        <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>Marcar pago
      </button>` : '';
  const actionsRow = `<div class="ld-actions">
      <button class="ld-btn edit" onclick="event.stopPropagation();editLanc(${refArgs})">
        <svg viewBox="0 0 24 24"><path d="M17 3a2.85 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"/></svg>Editar
      </button>
      ${pagarBtn}
      <button class="ld-btn del" onclick="event.stopPropagation();delLanc(${refArgs})" aria-label="Excluir">
        <svg viewBox="0 0 24 24"><path d="M3 6h18M8 6V4h8v2M19 6l-1 14H6L5 6"/></svg>
      </button>
    </div>`;

  return `<div class="lanc-item-wrap" id="li-wrap-${it.id}">
    <div class="${rowCls}" id="li-row-${it.id}" onclick="toggleLancDetail('${it.id}')">
      ${platBox}
      <div style="flex:1;min-width:0">
        <div class="li-line1">
          <span class="li-title-j">${categoryTitle}</span>
          <span class="${badgeCls}">${badgeTxt}</span>
        </div>
        <div class="li-line2">
          <span class="li-sub-j">${subTxt}</span>
          <span class="li-val-j ${isRec?'pos':'neg'}">${val}</span>
        </div>
      </div>
    </div>
    <div class="lanc-detail-panel" id="li-detail-${it.id}" style="display:none">
      ${rows}${actionsRow}
    </div>
  </div>`;
}

function toggleLancDetail(id) {
  const detail = document.getElementById('li-detail-' + id);
  const row    = document.getElementById('li-row-' + id);
  if (!detail || !row) return;
  const isOpen = detail.style.display !== 'none';
  // Fechar todos
  document.querySelectorAll('.lanc-detail-panel').forEach(el => el.style.display = 'none');
  document.querySelectorAll('.li-row-j').forEach(el => el.classList.remove('open'));
  // Abrir este se estava fechado
  if (!isOpen) {
    detail.style.display = 'block';
    row.classList.add('open');
  }
}

function markLancPago(id, date, type) {
  const day = DB.day(date);
  const arr = type === 'receita' ? day.revenues : day.expenses;
  const item = arr.find(x => x.id === id);
  if (!item) return;
  item.status = 'pago';
  DB.saveDay(day);
  SYNC.push(type === 'receita' ? 'revenues' : 'expenses',
    Object.assign({}, item, { date, status: 'pago' }));
  renderJornada();
  showToast(type === 'receita' ? 'Receita marcada como recebida' : 'Despesa marcada como paga');
}

function lancItem(it, showActBtn) {
  const isRec = it._type === 'receita';
  const catKey = (it._type || 'despesa') + '_' + (it.scope || 'operacional');
  const cats = DB.cats(catKey) || DEFAU
===== #F59E0B @ 776229 =====
match(/^(\d+)m$/);
  const justNum = text.match(/^(\d+)$/);
  if (full) { h = parseInt(full[1]); m = parseInt(full[2] || 0); }
  else if (justH) { h = parseInt(justH[1]); }
  else if (justM) { m = parseInt(justM[1]); }
  else if (justNum) { m = parseInt(justNum[1]); }
  else return null;
  return Math.max(0, h*3600 + m*60);
}
function updateTempoAdjDisplay() {
  const inp = document.getElementById('tempoadj-val');
  if (inp) inp.value = fmtDur(tempoAdjCurSecs / 60);
}
function tempoAdjStep(deltaMin) {
  tempoAdjCurSecs = Math.max(0, tempoAdjCurSecs + deltaMin * 60);
  updateTempoAdjDisplay();
}
function tempoAdjInputCommit() {
  const inp = document.getElementById('tempoadj-val');
  if (!inp) return;
  const parsed = parseTempoAdjInput(inp.value);
  if (parsed === null) { updateTempoAdjDisplay(); return; }
  tempoAdjCurSecs = parsed;
  updateTempoAdjDisplay();
}
function saveTempoAdjust() {
  tempoAdjInputCommit();
  const day = DB.day(S.curDate);
  day.tempoAdjust = Math.round(tempoAdjCurSecs - tempoAdjBaseSecs);
  DB.saveDay(day);
  SYNC.push('day_meta', { date: S.curDate, tempo_adjust_secs: day.tempoAdjust });
  closeMo('mo-tempo-adjust');
  renderJornada();
  showToast('Tempo total ajustado.');
}

// ── IMPORT AI ─────────────────────────────────────────────────
// Detecta vídeo por MIME type, com fallback por extensão do nome do
// arquivo — em alguns celulares (Live Photo/Motion Photo, ou o picker de
// Arquivos) o navegador reporta file.type vazio ou incorreto, e o print
// (imagem) acabava sendo classificado errado e descartado.
function isVideoFile(f) {
  if (f.type) return f.type.startsWith('video/');
  return /\.(mp4|mov|webm|mkv|avi|m4v|3gp)$/i.test(f.name||'');
}
function handleImport(input, plat) {
  if (!input.files.length) return;
  const files = Array.from(input.files);
  closeMo('mo-import');
  const videos = files.filter(isVideoFile);
  const images = files.filter(f => !isVideoFile(f));
  if (!videos.length && !images.length) return;
  processImportBatch(images, videos, plat);
}
// Processa imagens E vídeos da mesma seleção juntos — antes, se um vídeo
// viesse junto (ex: Live Photo confundida com foto), TODAS as imagens da
// seleção eram silenciosamente descartadas (só processava videos[0]).
let importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };

function openImportPrecheck() {
  ['ipc-uber-count','ipc-uber-value','ipc-99-count','ipc-99-value'].forEach(id => {
    const el = document.getElementById(id); if (el) el.value = '';
  });
  openMo('mo-import-precheck');
}
function _readPrecheckField(id) {
  const v = document.getElementById(id)?.value;
  return (v === '' || v === undefined || v === null) ? null : parseFloat(String(v).replace(',','.'));
}
function importPrecheckNext() {
  importPrecheck = {
    uberCount: _readPrecheckField('ipc-uber-count'),
    uberValue: _readPrecheckField('ipc-uber-value'),
    p99Count:  _readPrecheckField('ipc-99-count'),
    p99Value:  _readPrecheckField('ipc-99-value')
  };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}
function importPrecheckSkip() {
  importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}

function renderImportRing(found, target, foundVal, targetVal, statusHtml, isWarn) {
  const hasTarget = target > 0;
  const pct = hasTarget ? Math.min(100, found/target*100) : 0;
  const deg = Math.round(pct*3.6);
  const ringColor = isWarn ? '#F59E0B' : '#22C55E';
  const ringBg = hasTarget ? `conic-gradient(${ringColor} 0deg ${deg}deg, #E2E8F0 ${deg}deg 360deg)` : '#E2E8F0';
  const countHtml = hasTarget ? `${found}<span style="font-size:16px;color:#94A3B8">/${target}</span>` : `${found}`;
  const valHtml = hasTarget ? `${DB.fRI(foundVal)} de ${DB.fRI(targetVal)} já identificados` : `${DB.fRI(foundVal)} identificados até agora`;
  return `<div style="display:flex;flex-direction:column;align-items:center;padding:30px 0 10px">
    <div style="width:130px;height:130px;border-radius:50%;background:${ringBg};display:flex;align-items:center;justify-content:center;margin-bottom:20px;position:relative;transition:background .4s">
      <div style="position:absolute;inset:8px;border-radius:50%;background:#fff"></div>
      <div style="position:relative;text-align:center">
        <div style="font-family:'Poppins',sans-serif;font-weight:900;font-size:28px;color:#0F172A">${countHtml}</div>
        <div style="font-size:10px;color:#64748B;font-weight:700;margin-top:2px">CORRIDAS</div>
      </div>
    </div>
    <div style="font-size:13.5px;font-weight:700;color:#0F172A;margin-bottom:5px;display:flex;align-items:center;gap:6px">${statusHtml}</div>
    <div style="font-size:12px;color:#64748B">${valHtml}</div>
  </div>`;
}

// Tenta de novo (até 3x) antes de desistir de um print/frame — evita que um
// erro passageiro de rede derrube a corrida inteira do import silenciosamente.
async function extractWithRetry(fn, onRetry, maxAttempts) {
  maxAttempts = maxAttempts || 3;
  let lastErr;
  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    try { return await fn(); }
    catch(e) {
      lastErr = e;
      if (attempt < maxAttempts) { onRetry(attempt+1, maxAttempts); await new Promise(r => setTimeout(r, 800)); }
    }
  }
  throw lastErr;
}

// Sobe o vídeo original pro Storage (bucket import-videos, retenção de
// 180 dias) e registra em import_videos — pedido do Yuri (16/08/2026):
// base pra confirmação de valor real (campeonatos/incentivos futuros) e
// pra dar dá pra conferir manualmente qualquer corrida em disputa. Falha
// de upload NUNCA trava o import (retorna null, segue sem vínculo) — o
// vídeo é auditoria extra, não pode ser bloqueio de fluxo.
async function uploadImportVideo(file, plat, operationalDate) {
  if (!_supaUser) return null;
  try {
    const ext = (file.name && file.name.includes('.')) ? file.name.split('.').pop().toLowerCase() : 'mp4';
    const opDate = operationalDate || S.curDate || DB.today();
    const opPlat = normalizeOperationalPlatform(plat);
    const opRange = operationalRangeForPlatform(opPlat, opDate);
    const path = `${_supaUser.id}/${opDate}/${opPlat}/${DB.uid()}.${ext}`;
    const { error: upErr } = await _SUPA.storage.from('import-videos').upload(path, file, {
      contentType: file.type || 'video/mp4'
    });
    if (upErr) { console.warn('upload vídeo falhou (segue sem vínculo):', upErr); return null; }
    const { data, error } = await _SUPA.from('import_videos').insert({
      user_id: _supaUser.id, storage_path: path, file_size_bytes: file.size || null,
      platform: opPlat, operational_date: opDate,
      period_start: new Date(opRange.startMs).toISOString(),
      period_end: new Date(opRange.endMs).toISOString()
    }).select('id').single();
    if (error) { console.warn('registro import_videos falhou (segue sem vínculo):', error); return null; }
    return data.id;
  } catch(e) { console.warn('uploadImportVideo erro:', e); return null; }
}
async function processImportBatch(images, videos, plat) {
  const operationalDate = S.curDate || DB.today();
  const operationalPlat = normalizeOperationalPlatform(plat);
  window._pendingImportOperationalDate = operationalDate;
  window._pendingImportPlatform = operationalPlat;
  const targetCount = (importPrecheck.uberCount||0) + (importPrecheck.p99Count||0);
  const targetValue = (importPrecheck.uberValue||0) + (importPrecheck.p99Value||0);
  const totalImgs = images.length, totalVids = videos.length;
  const label = [totalImgs?`${totalImgs} print${totalImgs>1?'s':''}`:'', totalVids?`${totalVids} vídeo${totalVids>1?'s':''}`:''].filter(Boolean).join(' + ');
  document.getElementById('ic-title').textContent = 'Analisando ' + label;
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');

  const unique = [];
  const details = [];
  const seen = new Set();
  let foundCount = 0, foundVal = 0;
  let curDetail = null; // acumulador do fragmento de detalhe em aberto
  const flushDetail = () => { if (curDetail) { details.push(curDetail); curDetail = null; } };
  const mergeDetailFrag = (t) => {
    // Decide se é o MESMO cartão de detalhe (outro pedaço do mesmo scroll)
    // usando horário e endereço — nunca valor. Valor podia vir ligeiramente
    // diferente entre frames ("Você ganhou" vs "Total") e isso fazia o
    // mesmo cartão virar DOIS fragmentos, que depois viravam duas corridas
    // (uma real + uma fantasma) no import.
    const conflicts = (a, b) => a != null && b != null && a !== b;
    const sameRide = curDetail && (
      (t.time && curDetail.time) ? t.time === curDetail.time
        : (!conflicts(curDetail.originFull, t.originFull) && !conflicts(curDetail.destFull, t.destFull))
    );
    if (sameRide) {
      // mesmo cartão — preenche só o que esse fragmento ainda não tinha
      if (curDetail.km == null && t.km != null) curDetail.km = t.km;
      if (curDetail.duration == null && t.duration != null) curDetail.duration = t.duration;
      if (!curDetail.time && t.time) curDetail.time = t.time;
      if (!curDetail.originFull && t.originFull) { curDetail.originFull = t.originFull; curDetail.origin = t.origin; }
      if (!curDetail.destFull && t.destFull) { curDetail.destFull = t.destFull; curDetail.dest = t.dest; }
    } else {
      // horário ou endereço batendo diferente do acumulador — é outra
      // corrida: fecha o anterior e abre um novo
      flushDetail();
      curDetail = t;
    }
  };
  // Valida e deduplica NA HORA, frame a frame — o contador do anel só sobe
  // com corrida nova validada. Antes somava a extração bruta de cada frame,
  // e como os frames de vídeo se sobrepõem, a mesma corrida contava várias
  // vezes (ex: 37/17 na tela sendo que eram 17 corridas reais).
  const absorb = (trips) => {
    let sawList = false;
    (trips||[]).forEach(t => {
      if (t.screen === 'detail') {
        // Tela de detalhe: só km/tempo/endereço interessam — sem valor.
 
===== #F59E0B @ 829500 =====
(t.km) : (parseFloat(String(t.km||0).replace(',','.'))||0),
      duration: isDetail ? (t.duration==null ? null : (parseInt(t.duration)||0)) : (parseInt(t.duration)||0),
      dinamico, tip, surge: dinamico > 0,
      pedagioValor: pedagioVal, pedagio: pedagioVal > 0,
      originFull: oFull, destFull: dFull,
      serviceFee: serviceFee,
      taxaZero: serviceFee === 0 || t.taxaZero===true||t.taxaZero==='true'||t.taxaZero===1,
      dinheiro: isDetail
        ? (t.dinheiro===true||t.dinheiro==='true'||t.dinheiro===1 ? true : (t.dinheiro===false||t.dinheiro==='false' ? false : null))
        : (t.dinheiro===true||t.dinheiro==='true'||t.dinheiro===1),
      time: isDetail ? (t.time ? normTime(t.time) : null) : t.time };
  }) : [];
}


function buildProgressHTML(current, total, found) {
  return '<div style="text-align:center;padding:28px 16px">' +
    '<div style="font-size:28px;animation:vspin 1s linear infinite;display:inline-block">&#9881;</div>' +
    '<div style="margin-top:12px;font-size:14px;font-weight:700">' + (current||'0') + ' de ' + total + ' ' + (typeof total==='number'&&total>1?'imagens':'frame') + '</div>' +
    '<div style="font-size:12px;color:#64748B;margin-top:4px">' + found + ' corridas encontradas até agora</div>' +
    '<div style="height:6px;background:#E2E8F0;border-radius:3px;margin-top:14px;overflow:hidden">' +
    '<div style="height:100%;background:#22C55E;border-radius:3px;width:' + (total&&typeof total==='number'?Math.round(current/total*100):50) + '%;transition:width .4s"></div></div></div>';
}


function mergeTrips(existing, newTrips) {
  const all = [...existing];
  for (const t of newTrips) {
    const dup = all.find(e =>
      e.time === t.time &&
      Math.abs((parseFloat(e.value)||0) - (parseFloat(t.value)||0)) < 0.5
    );
    if (!dup) all.push(t);
  }
  return all;
}



// Mostra o que foi declarado no pré-check (antes de importar) lado a lado
// com o que está selecionado agora na lista, com um ✓/✗ por plataforma —
// pra dar pra comparar sem ter que guardar o número de cabeça. Só aparece
// se algo foi de fato declarado (senão fica vazio, não polui a tela).
function importDeclaredHtml() {
  const pc = importPrecheck || {};
  if (pc.uberCount == null && pc.p99Count == null) return '';
  const sel = (window._pendingTrips || []).filter(t => t._sel);
  const uber = sel.filter(t => t.platform !== '99');
  const p99  = sel.filter(t => t.platform === '99');
  const row = (label, color, decCount, decVal, foundArr) => {
    if (decCount == null) return '';
    const foundCount = foundArr.length;
    const foundVal = foundArr.reduce((s, t) => s + (t.value || 0), 0);
    let ok = foundCount === decCount;
    if (ok && decVal != null) ok = Math.abs(foundVal - decVal) < 0.01;
    const decTxt = decCount + ' corrida' + (decCount === 1 ? '' : 's') + (decVal != null ? ' · ' + DB.fRI(decVal) : '');
    return `<div style="display:flex;justify-content:space-between;align-items:center;font-size:12px;padding:3px 0">
      <span style="font-weight:800;color:${color}">${label}</span>
      <span style="font-weight:800;color:#334155">${decTxt}</span>
      <span style="display:inline-flex;align-items:center;justify-content:center;width:16px;height:16px;border-radius:50%;margin-left:6px;flex-shrink:0;background:${ok ? '#DCFCE7' : '#FEE2E2'};color:${ok ? '#16A34A' : '#DC2626'};font-size:10px">${ok ? '✓' : '✕'}</span>
    </div>`;
  };
  const rows = row('Uber', '#0EA5E9', pc.uberCount, pc.uberValue, uber) + row('99', '#F59E0B', pc.p99Count, pc.p99Value, p99);
  if (!rows) return '';
  return `<div style="font-size:9px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.06em;margin-bottom:6px">Você declarou antes de importar</div>
    <div style="background:#EFF6FF;border:1.5px solid #BFDBFE;border-radius:12px;padding:10px 12px;margin-bottom:8px">${rows}</div>`;
}
function importSummaryHtml() {
  const sel = (window._pendingTrips||[]).filter(t => t._sel);
  const total = sel.reduce((s,t)=>s+(t.value||0),0);
  const uber = sel.filter(t=>t.platform!=='99');
  const p99  = sel.filter(t=>t.platform==='99');
  const parts = [];
  if (uber.length) parts.push(`<span style="color:#0EA5E9;font-weight:800">Uber ${uber.length} · ${DB.fRI(uber.reduce((s,t)=>s+(t.value||0),0))}</span>`);
  if (p99.length)  parts.push(`<span style="color:#F59E0B;font-weight:800">99 ${p99.length} · ${DB.fRI(p99.reduce((s,t)=>s+(t.value||0),0))}</span>`);
  return `<div style="display:flex;justify-content:space-between;align-items:center;gap:8px;flex-wrap:wrap;background:#F0FDF4;border:1.5px solid #BBF7D0;border-radius:12px;padding:10px 12px;margin-bottom:10px">
    <div style="font-size:12px;font-weight:800;color:#0F172A">${sel.length} selecionada${sel.length===1?'':'s'} · <span style="color:#16A34A">${DB.fRI(total)}</span></div>
    <div style="display:flex;gap:10px;font-size:10.5px">${parts.join('')}</div>
  </div>`;
}
function updateImportSummary() {
  const elD = document.getElementById('ic-declared');
  if (elD) elD.outerHTML = '<div id="ic-declared">' + importDeclaredHtml() + '</div>';
  const el = document.getElementById('ic-summary');
  if (el) el.outerHTML = '<div id="ic-summary">' + importSummaryHtml() + '</div>';
}
function updateImportButtonLabel() {
  const trips = window._pendingTrips || [];
  const sel = trips.filter(t=>t._sel);
  const changedSel = sel.filter(t=>!t._isNew).length;
  const newSel = sel.filter(t=>t._isNew).length;
  const btn = document.getElementById('ic-btn');
  if (!btn) return;
  let sub = '';
  if (changedSel || newSel) {
    const parts = [];
    if (changedSel) parts.push(changedSel + (changedSel>1?' corrigem':' corrige') + ' valor');
    if (newSel) parts.push(newSel + (newSel>1?' criam':' cria') + ' corrida nova');
    sub = `<small style="display:block;font-size:10.5px;font-weight:600;opacity:.8;margin-top:1px">${parts.join(' · ')}</small>`;
  }
  btn.innerHTML = 'Importar ('+sel.length+')' + sub;
}
// Linha de uma corrida — cor do checkbox e do valor muda por grupo:
// verde = corrige valor de uma auto_trip já existente; âmbar = corrida
// nova (não foi vista na captura ao vivo, vira status "importada").
function importRowHtml(t, i) {
  const ex = [];
  if (t.dinamico > 0) ex.push(`<span class="chip chip-d"><svg viewBox="0 0 24 24" width="9" height="9" fill="currentColor"><path d="M13 2 3 14h7l-1 8 10-12h-7z"/></svg> ${DB.fR(t.dinamico)}</span>`);
  if (t.tip > 0) ex.push(`<span class="chip chip-g"><svg viewBox="0 0 24 24" width="9" height="9" fill="currentColor"><path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.29 1.5 4.05 3 5.5l7 7Z"/></svg> ${DB.fR(t.tip)}</span>`);
  const exRow = ex.length ? `<div class="li-extra" style="margin-top:4px">${ex.join('')}</div>` : '';
  const platColor = t.platform === '99' ? '#F59E0B' : '#0EA5E9';
  const platLabel = t.platform === '99' ? '99' : 'Uber';
  const platTag = `<span style="display:inline-flex;align-items:center;gap:3px;font-weight:800;color:${platColor}"><i style="width:5px;height:5px;border-radius:50%;display:inline-block;background:${platColor}"></i>${platLabel}</span>`;
  const newBadge = t._isNew ? '<span style="display:inline-block;font-size:8.5px;font-weight:800;padding:1.5px 6px;border-radius:99px;background:#FEF3C7;color:#92400E;margin-left:5px">Nova</span>' : '';
  const chkColor = t._isNew ? '#F59E0B' : '#22C55E';
  const valHtml = t._isNew
    ? `R$${(t.value||0).toFixed(2)}`
    : `R$${(t.value||0).toFixed(2)}<small style="display:block;font-size:9px;color:#64748B;font-weight:700">era ${DB.fR(t._oldValue||0)}</small>`;
  return `<div style="display:flex;align-items:center;gap:10px;padding:11px 14px;border-bottom:1px solid #E2E8F0;cursor:pointer" onclick="toggleImportTrip(${i})">
    <div style="width:22px;height:22px;border-radius:6px;border:2px solid ${t._sel?chkColor:'#E2E8F0'};background:${t._sel?chkColor:'transparent'};display:flex;align-items:center;justify-content:center;color:#fff;font-size:13px;flex-shrink:0" id="ict-${i}">${t._sel?'✓':''}</div>
    <div style="flex:1;min-width:0"><div style="font-size:12px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${t.origin||'—'} → ${t.dest||'—'}${newBadge}</div><div style="font-size:10px;color:#64748B;margin-top:2px">${platTag} · ${t.time||'—'} · ${t.duration||0}min${t.km?' · '+t.km+'km':''}</div>${exRow}</div>
    <div style="font-family:'Inter',system-ui,sans-serif;font-weight:700;font-size:14px;color:${t._isNew?'#16A34A':'#0F172A'};flex-shrink:0;text-align:right">${valHtml}</div>
  </div>`;
}
function showImportConfirm() {
  const trips = window._pendingTrips || [];
  if (!trips.length) { closeMo('mo-import-confirm'); showToast('Nada pra atualizar — nenhuma corrida com valor diferente ou nova.', 'info'); return; }
  document.getElementById('ic-title').textContent = 'Corridas pra importar ('+trips.length+')';
  document.getElementById('ic-btn').style.display = '';
  updateImportButtonLabel();

  // Dois grupos, na ordem do mockup aprovado (16/08/2026): primeiro o que
  // já existia e só corrige valor (verde), depois o que é novo — nunca
  // visto na captura ao vivo, vira "importada" (âmbar).
  const changedRows = [], newRows = [];
  trips.forEach((t,i) => { (t._isNew ? newRows : changedRows).push(importRowHtml(t,i)); });

  let groupsHtml = '';
  if (changedRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#22C55E"></span><span style="font-size:12.5px;font-weight:800">Valor vai mudar</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${changedRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0;margin-bottom:${newRows.length?'10px':'0'}">${changedRows.join('')}</div>`;
  }
  if (newRows.length) {
    groupsHtml += `<div style="display
===== #F59E0B @ 830340 =====
ding:28px 16px">' +
    '<div style="font-size:28px;animation:vspin 1s linear infinite;display:inline-block">&#9881;</div>' +
    '<div style="margin-top:12px;font-size:14px;font-weight:700">' + (current||'0') + ' de ' + total + ' ' + (typeof total==='number'&&total>1?'imagens':'frame') + '</div>' +
    '<div style="font-size:12px;color:#64748B;margin-top:4px">' + found + ' corridas encontradas até agora</div>' +
    '<div style="height:6px;background:#E2E8F0;border-radius:3px;margin-top:14px;overflow:hidden">' +
    '<div style="height:100%;background:#22C55E;border-radius:3px;width:' + (total&&typeof total==='number'?Math.round(current/total*100):50) + '%;transition:width .4s"></div></div></div>';
}


function mergeTrips(existing, newTrips) {
  const all = [...existing];
  for (const t of newTrips) {
    const dup = all.find(e =>
      e.time === t.time &&
      Math.abs((parseFloat(e.value)||0) - (parseFloat(t.value)||0)) < 0.5
    );
    if (!dup) all.push(t);
  }
  return all;
}



// Mostra o que foi declarado no pré-check (antes de importar) lado a lado
// com o que está selecionado agora na lista, com um ✓/✗ por plataforma —
// pra dar pra comparar sem ter que guardar o número de cabeça. Só aparece
// se algo foi de fato declarado (senão fica vazio, não polui a tela).
function importDeclaredHtml() {
  const pc = importPrecheck || {};
  if (pc.uberCount == null && pc.p99Count == null) return '';
  const sel = (window._pendingTrips || []).filter(t => t._sel);
  const uber = sel.filter(t => t.platform !== '99');
  const p99  = sel.filter(t => t.platform === '99');
  const row = (label, color, decCount, decVal, foundArr) => {
    if (decCount == null) return '';
    const foundCount = foundArr.length;
    const foundVal = foundArr.reduce((s, t) => s + (t.value || 0), 0);
    let ok = foundCount === decCount;
    if (ok && decVal != null) ok = Math.abs(foundVal - decVal) < 0.01;
    const decTxt = decCount + ' corrida' + (decCount === 1 ? '' : 's') + (decVal != null ? ' · ' + DB.fRI(decVal) : '');
    return `<div style="display:flex;justify-content:space-between;align-items:center;font-size:12px;padding:3px 0">
      <span style="font-weight:800;color:${color}">${label}</span>
      <span style="font-weight:800;color:#334155">${decTxt}</span>
      <span style="display:inline-flex;align-items:center;justify-content:center;width:16px;height:16px;border-radius:50%;margin-left:6px;flex-shrink:0;background:${ok ? '#DCFCE7' : '#FEE2E2'};color:${ok ? '#16A34A' : '#DC2626'};font-size:10px">${ok ? '✓' : '✕'}</span>
    </div>`;
  };
  const rows = row('Uber', '#0EA5E9', pc.uberCount, pc.uberValue, uber) + row('99', '#F59E0B', pc.p99Count, pc.p99Value, p99);
  if (!rows) return '';
  return `<div style="font-size:9px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.06em;margin-bottom:6px">Você declarou antes de importar</div>
    <div style="background:#EFF6FF;border:1.5px solid #BFDBFE;border-radius:12px;padding:10px 12px;margin-bottom:8px">${rows}</div>`;
}
function importSummaryHtml() {
  const sel = (window._pendingTrips||[]).filter(t => t._sel);
  const total = sel.reduce((s,t)=>s+(t.value||0),0);
  const uber = sel.filter(t=>t.platform!=='99');
  const p99  = sel.filter(t=>t.platform==='99');
  const parts = [];
  if (uber.length) parts.push(`<span style="color:#0EA5E9;font-weight:800">Uber ${uber.length} · ${DB.fRI(uber.reduce((s,t)=>s+(t.value||0),0))}</span>`);
  if (p99.length)  parts.push(`<span style="color:#F59E0B;font-weight:800">99 ${p99.length} · ${DB.fRI(p99.reduce((s,t)=>s+(t.value||0),0))}</span>`);
  return `<div style="display:flex;justify-content:space-between;align-items:center;gap:8px;flex-wrap:wrap;background:#F0FDF4;border:1.5px solid #BBF7D0;border-radius:12px;padding:10px 12px;margin-bottom:10px">
    <div style="font-size:12px;font-weight:800;color:#0F172A">${sel.length} selecionada${sel.length===1?'':'s'} · <span style="color:#16A34A">${DB.fRI(total)}</span></div>
    <div style="display:flex;gap:10px;font-size:10.5px">${parts.join('')}</div>
  </div>`;
}
function updateImportSummary() {
  const elD = document.getElementById('ic-declared');
  if (elD) elD.outerHTML = '<div id="ic-declared">' + importDeclaredHtml() + '</div>';
  const el = document.getElementById('ic-summary');
  if (el) el.outerHTML = '<div id="ic-summary">' + importSummaryHtml() + '</div>';
}
function updateImportButtonLabel() {
  const trips = window._pendingTrips || [];
  const sel = trips.filter(t=>t._sel);
  const changedSel = sel.filter(t=>!t._isNew).length;
  const newSel = sel.filter(t=>t._isNew).length;
  const btn = document.getElementById('ic-btn');
  if (!btn) return;
  let sub = '';
  if (changedSel || newSel) {
    const parts = [];
    if (changedSel) parts.push(changedSel + (changedSel>1?' corrigem':' corrige') + ' valor');
    if (newSel) parts.push(newSel + (newSel>1?' criam':' cria') + ' corrida nova');
    sub = `<small style="display:block;font-size:10.5px;font-weight:600;opacity:.8;margin-top:1px">${parts.join(' · ')}</small>`;
  }
  btn.innerHTML = 'Importar ('+sel.length+')' + sub;
}
// Linha de uma corrida — cor do checkbox e do valor muda por grupo:
// verde = corrige valor de uma auto_trip já existente; âmbar = corrida
// nova (não foi vista na captura ao vivo, vira status "importada").
function importRowHtml(t, i) {
  const ex = [];
  if (t.dinamico > 0) ex.push(`<span class="chip chip-d"><svg viewBox="0 0 24 24" width="9" height="9" fill="currentColor"><path d="M13 2 3 14h7l-1 8 10-12h-7z"/></svg> ${DB.fR(t.dinamico)}</span>`);
  if (t.tip > 0) ex.push(`<span class="chip chip-g"><svg viewBox="0 0 24 24" width="9" height="9" fill="currentColor"><path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.29 1.5 4.05 3 5.5l7 7Z"/></svg> ${DB.fR(t.tip)}</span>`);
  const exRow = ex.length ? `<div class="li-extra" style="margin-top:4px">${ex.join('')}</div>` : '';
  const platColor = t.platform === '99' ? '#F59E0B' : '#0EA5E9';
  const platLabel = t.platform === '99' ? '99' : 'Uber';
  const platTag = `<span style="display:inline-flex;align-items:center;gap:3px;font-weight:800;color:${platColor}"><i style="width:5px;height:5px;border-radius:50%;display:inline-block;background:${platColor}"></i>${platLabel}</span>`;
  const newBadge = t._isNew ? '<span style="display:inline-block;font-size:8.5px;font-weight:800;padding:1.5px 6px;border-radius:99px;background:#FEF3C7;color:#92400E;margin-left:5px">Nova</span>' : '';
  const chkColor = t._isNew ? '#F59E0B' : '#22C55E';
  const valHtml = t._isNew
    ? `R$${(t.value||0).toFixed(2)}`
    : `R$${(t.value||0).toFixed(2)}<small style="display:block;font-size:9px;color:#64748B;font-weight:700">era ${DB.fR(t._oldValue||0)}</small>`;
  return `<div style="display:flex;align-items:center;gap:10px;padding:11px 14px;border-bottom:1px solid #E2E8F0;cursor:pointer" onclick="toggleImportTrip(${i})">
    <div style="width:22px;height:22px;border-radius:6px;border:2px solid ${t._sel?chkColor:'#E2E8F0'};background:${t._sel?chkColor:'transparent'};display:flex;align-items:center;justify-content:center;color:#fff;font-size:13px;flex-shrink:0" id="ict-${i}">${t._sel?'✓':''}</div>
    <div style="flex:1;min-width:0"><div style="font-size:12px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${t.origin||'—'} → ${t.dest||'—'}${newBadge}</div><div style="font-size:10px;color:#64748B;margin-top:2px">${platTag} · ${t.time||'—'} · ${t.duration||0}min${t.km?' · '+t.km+'km':''}</div>${exRow}</div>
    <div style="font-family:'Inter',system-ui,sans-serif;font-weight:700;font-size:14px;color:${t._isNew?'#16A34A':'#0F172A'};flex-shrink:0;text-align:right">${valHtml}</div>
  </div>`;
}
function showImportConfirm() {
  const trips = window._pendingTrips || [];
  if (!trips.length) { closeMo('mo-import-confirm'); showToast('Nada pra atualizar — nenhuma corrida com valor diferente ou nova.', 'info'); return; }
  document.getElementById('ic-title').textContent = 'Corridas pra importar ('+trips.length+')';
  document.getElementById('ic-btn').style.display = '';
  updateImportButtonLabel();

  // Dois grupos, na ordem do mockup aprovado (16/08/2026): primeiro o que
  // já existia e só corrige valor (verde), depois o que é novo — nunca
  // visto na captura ao vivo, vira "importada" (âmbar).
  const changedRows = [], newRows = [];
  trips.forEach((t,i) => { (t._isNew ? newRows : changedRows).push(importRowHtml(t,i)); });

  let groupsHtml = '';
  if (changedRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#22C55E"></span><span style="font-size:12.5px;font-weight:800">Valor vai mudar</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${changedRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0;margin-bottom:${newRows.length?'10px':'0'}">${changedRows.join('')}</div>`;
  }
  if (newRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#F59E0B"></span><span style="font-size:12.5px;font-weight:800">Novas</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${newRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0">${newRows.join('')}</div>`;
  }

  document.getElementById('ic-body').innerHTML = '<div id="ic-declared">' + importDeclaredHtml() + '</div>' +
    '<div id="ic-summary">' + importSummaryHtml() + '</div>' + groupsHtml;
}
function toggleImportTrip(i) {
  const t = (window._pendingTrips||[])[i]; if (!t) return;
  t._sel = !t._sel;
  const chkColor = t._isNew ? '#F59E0B' : '#22C55E';
  const el = document.getElementById('ict-'+i);
  if (el) {
===== #F59E0B @ 832859 =====
t-size:10px">${ok ? '✓' : '✕'}</span>
    </div>`;
  };
  const rows = row('Uber', '#0EA5E9', pc.uberCount, pc.uberValue, uber) + row('99', '#F59E0B', pc.p99Count, pc.p99Value, p99);
  if (!rows) return '';
  return `<div style="font-size:9px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.06em;margin-bottom:6px">Você declarou antes de importar</div>
    <div style="background:#EFF6FF;border:1.5px solid #BFDBFE;border-radius:12px;padding:10px 12px;margin-bottom:8px">${rows}</div>`;
}
function importSummaryHtml() {
  const sel = (window._pendingTrips||[]).filter(t => t._sel);
  const total = sel.reduce((s,t)=>s+(t.value||0),0);
  const uber = sel.filter(t=>t.platform!=='99');
  const p99  = sel.filter(t=>t.platform==='99');
  const parts = [];
  if (uber.length) parts.push(`<span style="color:#0EA5E9;font-weight:800">Uber ${uber.length} · ${DB.fRI(uber.reduce((s,t)=>s+(t.value||0),0))}</span>`);
  if (p99.length)  parts.push(`<span style="color:#F59E0B;font-weight:800">99 ${p99.length} · ${DB.fRI(p99.reduce((s,t)=>s+(t.value||0),0))}</span>`);
  return `<div style="display:flex;justify-content:space-between;align-items:center;gap:8px;flex-wrap:wrap;background:#F0FDF4;border:1.5px solid #BBF7D0;border-radius:12px;padding:10px 12px;margin-bottom:10px">
    <div style="font-size:12px;font-weight:800;color:#0F172A">${sel.length} selecionada${sel.length===1?'':'s'} · <span style="color:#16A34A">${DB.fRI(total)}</span></div>
    <div style="display:flex;gap:10px;font-size:10.5px">${parts.join('')}</div>
  </div>`;
}
function updateImportSummary() {
  const elD = document.getElementById('ic-declared');
  if (elD) elD.outerHTML = '<div id="ic-declared">' + importDeclaredHtml() + '</div>';
  const el = document.getElementById('ic-summary');
  if (el) el.outerHTML = '<div id="ic-summary">' + importSummaryHtml() + '</div>';
}
function updateImportButtonLabel() {
  const trips = window._pendingTrips || [];
  const sel = trips.filter(t=>t._sel);
  const changedSel = sel.filter(t=>!t._isNew).length;
  const newSel = sel.filter(t=>t._isNew).length;
  const btn = document.getElementById('ic-btn');
  if (!btn) return;
  let sub = '';
  if (changedSel || newSel) {
    const parts = [];
    if (changedSel) parts.push(changedSel + (changedSel>1?' corrigem':' corrige') + ' valor');
    if (newSel) parts.push(newSel + (newSel>1?' criam':' cria') + ' corrida nova');
    sub = `<small style="display:block;font-size:10.5px;font-weight:600;opacity:.8;margin-top:1px">${parts.join(' · ')}</small>`;
  }
  btn.innerHTML = 'Importar ('+sel.length+')' + sub;
}
// Linha de uma corrida — cor do checkbox e do valor muda por grupo:
// verde = corrige valor de uma auto_trip já existente; âmbar = corrida
// nova (não foi vista na captura ao vivo, vira status "importada").
function importRowHtml(t, i) {
  const ex = [];
  if (t.dinamico > 0) ex.push(`<span class="chip chip-d"><svg viewBox="0 0 24 24" width="9" height="9" fill="currentColor"><path d="M13 2 3 14h7l-1 8 10-12h-7z"/></svg> ${DB.fR(t.dinamico)}</span>`);
  if (t.tip > 0) ex.push(`<span class="chip chip-g"><svg viewBox="0 0 24 24" width="9" height="9" fill="currentColor"><path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.29 1.5 4.05 3 5.5l7 7Z"/></svg> ${DB.fR(t.tip)}</span>`);
  const exRow = ex.length ? `<div class="li-extra" style="margin-top:4px">${ex.join('')}</div>` : '';
  const platColor = t.platform === '99' ? '#F59E0B' : '#0EA5E9';
  const platLabel = t.platform === '99' ? '99' : 'Uber';
  const platTag = `<span style="display:inline-flex;align-items:center;gap:3px;font-weight:800;color:${platColor}"><i style="width:5px;height:5px;border-radius:50%;display:inline-block;background:${platColor}"></i>${platLabel}</span>`;
  const newBadge = t._isNew ? '<span style="display:inline-block;font-size:8.5px;font-weight:800;padding:1.5px 6px;border-radius:99px;background:#FEF3C7;color:#92400E;margin-left:5px">Nova</span>' : '';
  const chkColor = t._isNew ? '#F59E0B' : '#22C55E';
  const valHtml = t._isNew
    ? `R$${(t.value||0).toFixed(2)}`
    : `R$${(t.value||0).toFixed(2)}<small style="display:block;font-size:9px;color:#64748B;font-weight:700">era ${DB.fR(t._oldValue||0)}</small>`;
  return `<div style="display:flex;align-items:center;gap:10px;padding:11px 14px;border-bottom:1px solid #E2E8F0;cursor:pointer" onclick="toggleImportTrip(${i})">
    <div style="width:22px;height:22px;border-radius:6px;border:2px solid ${t._sel?chkColor:'#E2E8F0'};background:${t._sel?chkColor:'transparent'};display:flex;align-items:center;justify-content:center;color:#fff;font-size:13px;flex-shrink:0" id="ict-${i}">${t._sel?'✓':''}</div>
    <div style="flex:1;min-width:0"><div style="font-size:12px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${t.origin||'—'} → ${t.dest||'—'}${newBadge}</div><div style="font-size:10px;color:#64748B;margin-top:2px">${platTag} · ${t.time||'—'} · ${t.duration||0}min${t.km?' · '+t.km+'km':''}</div>${exRow}</div>
    <div style="font-family:'Inter',system-ui,sans-serif;font-weight:700;font-size:14px;color:${t._isNew?'#16A34A':'#0F172A'};flex-shrink:0;text-align:right">${valHtml}</div>
  </div>`;
}
function showImportConfirm() {
  const trips = window._pendingTrips || [];
  if (!trips.length) { closeMo('mo-import-confirm'); showToast('Nada pra atualizar — nenhuma corrida com valor diferente ou nova.', 'info'); return; }
  document.getElementById('ic-title').textContent = 'Corridas pra importar ('+trips.length+')';
  document.getElementById('ic-btn').style.display = '';
  updateImportButtonLabel();

  // Dois grupos, na ordem do mockup aprovado (16/08/2026): primeiro o que
  // já existia e só corrige valor (verde), depois o que é novo — nunca
  // visto na captura ao vivo, vira "importada" (âmbar).
  const changedRows = [], newRows = [];
  trips.forEach((t,i) => { (t._isNew ? newRows : changedRows).push(importRowHtml(t,i)); });

  let groupsHtml = '';
  if (changedRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#22C55E"></span><span style="font-size:12.5px;font-weight:800">Valor vai mudar</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${changedRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0;margin-bottom:${newRows.length?'10px':'0'}">${changedRows.join('')}</div>`;
  }
  if (newRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#F59E0B"></span><span style="font-size:12.5px;font-weight:800">Novas</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${newRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0">${newRows.join('')}</div>`;
  }

  document.getElementById('ic-body').innerHTML = '<div id="ic-declared">' + importDeclaredHtml() + '</div>' +
    '<div id="ic-summary">' + importSummaryHtml() + '</div>' + groupsHtml;
}
function toggleImportTrip(i) {
  const t = (window._pendingTrips||[])[i]; if (!t) return;
  t._sel = !t._sel;
  const chkColor = t._isNew ? '#F59E0B' : '#22C55E';
  const el = document.getElementById('ict-'+i);
  if (el) { el.style.background=t._sel?chkColor:'transparent'; el.style.borderColor=t._sel?chkColor:'#E2E8F0'; el.textContent=t._sel?'✓':''; }
  updateImportButtonLabel();
  updateImportSummary();
}

// Detecta corrida duplicada (hora+valor, hora+km, valor+rota)
// Versão do código — atualizar para verificar se cache está correto
const _APP_CODE_VER = "v24-trip99-fix";

// Normaliza texto de rota pra comparação: minúsculas, sem acentos, sem
// espaços extras — "Barra do Imbuí" e "barra do imbui" viram iguais.
// (OCR de frames diferentes do mesmo vídeo varia acentuação → sem isso,
// a mesma corrida escapa do detector de duplicata.)
function _normRoute(s) {
  return (s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')
    .replace(/\s+/g,' ').trim();
}
function isTripDuplicate(newT, existing) {
  return existing.some(t => {
    // Plataformas diferentes = corridas diferentes (nunca duplicata)
    const samePlat = (t.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const sameTime = t.time && newT.time && t.time === newT.time;
    const sameVal  = Math.abs((t.value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && t.km > 0 && Math.abs((t.km||0)-(newT.km||0)) < 0.5;
    // 99 não reporta km — usar origem+destino como sinal de rota nesse caso
    // (comparação normalizada: acento/caixa não separam corridas)
    const sameRoute = !!(t.origin && newT.origin && t.dest && newT.dest
                       && _normRoute(t.origin) === _normRoute(newT.origin)
                       && _normRoute(t.dest) === _normRoute(newT.dest));
    const nearTime = t.time && newT.time && (() => {
      const [h1,m1] = t.time.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    // Mesmo horário + mesmo valor = duplicata clara
    if (sameTime && sameVal) return true;
    // Mesmo horário EXATO + mesma rota = duplicata mesmo com valor diferente.
    // Protege contra fusão de frames no OCR de vídeo (rota de uma corrida +
    // valor do cartão vizinho). Duas corridas reais no MESMO minuto com a
    // MESMA origem e MESMO destino não existem na prática.
    if (sameTime && sameRoute) return true;
    // Mesmo horário + mesma km (proteção contra OCR com valor diferente)
    if (sameTime && sameKm && newT.km > 0) return true;
    // Próximo horário + mesmo valor + mesma km = duplicata provável
    if (sameVal && nearTime && sameKm) return true;
    // Próxim
===== #F59E0B @ 833408 =====
t sel = (window._pendingTrips||[]).filter(t => t._sel);
  const total = sel.reduce((s,t)=>s+(t.value||0),0);
  const uber = sel.filter(t=>t.platform!=='99');
  const p99  = sel.filter(t=>t.platform==='99');
  const parts = [];
  if (uber.length) parts.push(`<span style="color:#0EA5E9;font-weight:800">Uber ${uber.length} · ${DB.fRI(uber.reduce((s,t)=>s+(t.value||0),0))}</span>`);
  if (p99.length)  parts.push(`<span style="color:#F59E0B;font-weight:800">99 ${p99.length} · ${DB.fRI(p99.reduce((s,t)=>s+(t.value||0),0))}</span>`);
  return `<div style="display:flex;justify-content:space-between;align-items:center;gap:8px;flex-wrap:wrap;background:#F0FDF4;border:1.5px solid #BBF7D0;border-radius:12px;padding:10px 12px;margin-bottom:10px">
    <div style="font-size:12px;font-weight:800;color:#0F172A">${sel.length} selecionada${sel.length===1?'':'s'} · <span style="color:#16A34A">${DB.fRI(total)}</span></div>
    <div style="display:flex;gap:10px;font-size:10.5px">${parts.join('')}</div>
  </div>`;
}
function updateImportSummary() {
  const elD = document.getElementById('ic-declared');
  if (elD) elD.outerHTML = '<div id="ic-declared">' + importDeclaredHtml() + '</div>';
  const el = document.getElementById('ic-summary');
  if (el) el.outerHTML = '<div id="ic-summary">' + importSummaryHtml() + '</div>';
}
function updateImportButtonLabel() {
  const trips = window._pendingTrips || [];
  const sel = trips.filter(t=>t._sel);
  const changedSel = sel.filter(t=>!t._isNew).length;
  const newSel = sel.filter(t=>t._isNew).length;
  const btn = document.getElementById('ic-btn');
  if (!btn) return;
  let sub = '';
  if (changedSel || newSel) {
    const parts = [];
    if (changedSel) parts.push(changedSel + (changedSel>1?' corrigem':' corrige') + ' valor');
    if (newSel) parts.push(newSel + (newSel>1?' criam':' cria') + ' corrida nova');
    sub = `<small style="display:block;font-size:10.5px;font-weight:600;opacity:.8;margin-top:1px">${parts.join(' · ')}</small>`;
  }
  btn.innerHTML = 'Importar ('+sel.length+')' + sub;
}
// Linha de uma corrida — cor do checkbox e do valor muda por grupo:
// verde = corrige valor de uma auto_trip já existente; âmbar = corrida
// nova (não foi vista na captura ao vivo, vira status "importada").
function importRowHtml(t, i) {
  const ex = [];
  if (t.dinamico > 0) ex.push(`<span class="chip chip-d"><svg viewBox="0 0 24 24" width="9" height="9" fill="currentColor"><path d="M13 2 3 14h7l-1 8 10-12h-7z"/></svg> ${DB.fR(t.dinamico)}</span>`);
  if (t.tip > 0) ex.push(`<span class="chip chip-g"><svg viewBox="0 0 24 24" width="9" height="9" fill="currentColor"><path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.29 1.5 4.05 3 5.5l7 7Z"/></svg> ${DB.fR(t.tip)}</span>`);
  const exRow = ex.length ? `<div class="li-extra" style="margin-top:4px">${ex.join('')}</div>` : '';
  const platColor = t.platform === '99' ? '#F59E0B' : '#0EA5E9';
  const platLabel = t.platform === '99' ? '99' : 'Uber';
  const platTag = `<span style="display:inline-flex;align-items:center;gap:3px;font-weight:800;color:${platColor}"><i style="width:5px;height:5px;border-radius:50%;display:inline-block;background:${platColor}"></i>${platLabel}</span>`;
  const newBadge = t._isNew ? '<span style="display:inline-block;font-size:8.5px;font-weight:800;padding:1.5px 6px;border-radius:99px;background:#FEF3C7;color:#92400E;margin-left:5px">Nova</span>' : '';
  const chkColor = t._isNew ? '#F59E0B' : '#22C55E';
  const valHtml = t._isNew
    ? `R$${(t.value||0).toFixed(2)}`
    : `R$${(t.value||0).toFixed(2)}<small style="display:block;font-size:9px;color:#64748B;font-weight:700">era ${DB.fR(t._oldValue||0)}</small>`;
  return `<div style="display:flex;align-items:center;gap:10px;padding:11px 14px;border-bottom:1px solid #E2E8F0;cursor:pointer" onclick="toggleImportTrip(${i})">
    <div style="width:22px;height:22px;border-radius:6px;border:2px solid ${t._sel?chkColor:'#E2E8F0'};background:${t._sel?chkColor:'transparent'};display:flex;align-items:center;justify-content:center;color:#fff;font-size:13px;flex-shrink:0" id="ict-${i}">${t._sel?'✓':''}</div>
    <div style="flex:1;min-width:0"><div style="font-size:12px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${t.origin||'—'} → ${t.dest||'—'}${newBadge}</div><div style="font-size:10px;color:#64748B;margin-top:2px">${platTag} · ${t.time||'—'} · ${t.duration||0}min${t.km?' · '+t.km+'km':''}</div>${exRow}</div>
    <div style="font-family:'Inter',system-ui,sans-serif;font-weight:700;font-size:14px;color:${t._isNew?'#16A34A':'#0F172A'};flex-shrink:0;text-align:right">${valHtml}</div>
  </div>`;
}
function showImportConfirm() {
  const trips = window._pendingTrips || [];
  if (!trips.length) { closeMo('mo-import-confirm'); showToast('Nada pra atualizar — nenhuma corrida com valor diferente ou nova.', 'info'); return; }
  document.getElementById('ic-title').textContent = 'Corridas pra importar ('+trips.length+')';
  document.getElementById('ic-btn').style.display = '';
  updateImportButtonLabel();

  // Dois grupos, na ordem do mockup aprovado (16/08/2026): primeiro o que
  // já existia e só corrige valor (verde), depois o que é novo — nunca
  // visto na captura ao vivo, vira "importada" (âmbar).
  const changedRows = [], newRows = [];
  trips.forEach((t,i) => { (t._isNew ? newRows : changedRows).push(importRowHtml(t,i)); });

  let groupsHtml = '';
  if (changedRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#22C55E"></span><span style="font-size:12.5px;font-weight:800">Valor vai mudar</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${changedRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0;margin-bottom:${newRows.length?'10px':'0'}">${changedRows.join('')}</div>`;
  }
  if (newRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#F59E0B"></span><span style="font-size:12.5px;font-weight:800">Novas</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${newRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0">${newRows.join('')}</div>`;
  }

  document.getElementById('ic-body').innerHTML = '<div id="ic-declared">' + importDeclaredHtml() + '</div>' +
    '<div id="ic-summary">' + importSummaryHtml() + '</div>' + groupsHtml;
}
function toggleImportTrip(i) {
  const t = (window._pendingTrips||[])[i]; if (!t) return;
  t._sel = !t._sel;
  const chkColor = t._isNew ? '#F59E0B' : '#22C55E';
  const el = document.getElementById('ict-'+i);
  if (el) { el.style.background=t._sel?chkColor:'transparent'; el.style.borderColor=t._sel?chkColor:'#E2E8F0'; el.textContent=t._sel?'✓':''; }
  updateImportButtonLabel();
  updateImportSummary();
}

// Detecta corrida duplicada (hora+valor, hora+km, valor+rota)
// Versão do código — atualizar para verificar se cache está correto
const _APP_CODE_VER = "v24-trip99-fix";

// Normaliza texto de rota pra comparação: minúsculas, sem acentos, sem
// espaços extras — "Barra do Imbuí" e "barra do imbui" viram iguais.
// (OCR de frames diferentes do mesmo vídeo varia acentuação → sem isso,
// a mesma corrida escapa do detector de duplicata.)
function _normRoute(s) {
  return (s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')
    .replace(/\s+/g,' ').trim();
}
function isTripDuplicate(newT, existing) {
  return existing.some(t => {
    // Plataformas diferentes = corridas diferentes (nunca duplicata)
    const samePlat = (t.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const sameTime = t.time && newT.time && t.time === newT.time;
    const sameVal  = Math.abs((t.value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && t.km > 0 && Math.abs((t.km||0)-(newT.km||0)) < 0.5;
    // 99 não reporta km — usar origem+destino como sinal de rota nesse caso
    // (comparação normalizada: acento/caixa não separam corridas)
    const sameRoute = !!(t.origin && newT.origin && t.dest && newT.dest
                       && _normRoute(t.origin) === _normRoute(newT.origin)
                       && _normRoute(t.dest) === _normRoute(newT.dest));
    const nearTime = t.time && newT.time && (() => {
      const [h1,m1] = t.time.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    // Mesmo horário + mesmo valor = duplicata clara
    if (sameTime && sameVal) return true;
    // Mesmo horário EXATO + mesma rota = duplicata mesmo com valor diferente.
    // Protege contra fusão de frames no OCR de vídeo (rota de uma corrida +
    // valor do cartão vizinho). Duas corridas reais no MESMO minuto com a
    // MESMA origem e MESMO destino não existem na prática.
    if (sameTime && sameRoute) return true;
    // Mesmo horário + mesma km (proteção contra OCR com valor diferente)
    if (sameTime && sameKm && newT.km > 0) return true;
    // Próximo horário + mesmo valor + mesma km = duplicata provável
    if (sameVal && nearTime && sameKm) return true;
    // Próximo horário + mesmo valor + mesma rota (sem km, ex: 99) = duplicata provável
    if (sameVal && nearTime && sameRoute) return true;
    // REMOVIDO: sameVal && sameRoute sem tempo = falso positivo entre plataformas
    return false;
  });
}

// ── Casar corrida importada com corrida automática já capturada ────────────
// Pedido do Yuri (23/07/2026): ao importar, se a corrida já foi capturada
// automaticamente, ATUALIZA a automática com o valor real (confirmado pela
// tela de histórico da plataforma — mais confiável que a oferta estimada),
// 
===== #F59E0B @ 836117 =====
5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.29 1.5 4.05 3 5.5l7 7Z"/></svg> ${DB.fR(t.tip)}</span>`);
  const exRow = ex.length ? `<div class="li-extra" style="margin-top:4px">${ex.join('')}</div>` : '';
  const platColor = t.platform === '99' ? '#F59E0B' : '#0EA5E9';
  const platLabel = t.platform === '99' ? '99' : 'Uber';
  const platTag = `<span style="display:inline-flex;align-items:center;gap:3px;font-weight:800;color:${platColor}"><i style="width:5px;height:5px;border-radius:50%;display:inline-block;background:${platColor}"></i>${platLabel}</span>`;
  const newBadge = t._isNew ? '<span style="display:inline-block;font-size:8.5px;font-weight:800;padding:1.5px 6px;border-radius:99px;background:#FEF3C7;color:#92400E;margin-left:5px">Nova</span>' : '';
  const chkColor = t._isNew ? '#F59E0B' : '#22C55E';
  const valHtml = t._isNew
    ? `R$${(t.value||0).toFixed(2)}`
    : `R$${(t.value||0).toFixed(2)}<small style="display:block;font-size:9px;color:#64748B;font-weight:700">era ${DB.fR(t._oldValue||0)}</small>`;
  return `<div style="display:flex;align-items:center;gap:10px;padding:11px 14px;border-bottom:1px solid #E2E8F0;cursor:pointer" onclick="toggleImportTrip(${i})">
    <div style="width:22px;height:22px;border-radius:6px;border:2px solid ${t._sel?chkColor:'#E2E8F0'};background:${t._sel?chkColor:'transparent'};display:flex;align-items:center;justify-content:center;color:#fff;font-size:13px;flex-shrink:0" id="ict-${i}">${t._sel?'✓':''}</div>
    <div style="flex:1;min-width:0"><div style="font-size:12px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${t.origin||'—'} → ${t.dest||'—'}${newBadge}</div><div style="font-size:10px;color:#64748B;margin-top:2px">${platTag} · ${t.time||'—'} · ${t.duration||0}min${t.km?' · '+t.km+'km':''}</div>${exRow}</div>
    <div style="font-family:'Inter',system-ui,sans-serif;font-weight:700;font-size:14px;color:${t._isNew?'#16A34A':'#0F172A'};flex-shrink:0;text-align:right">${valHtml}</div>
  </div>`;
}
function showImportConfirm() {
  const trips = window._pendingTrips || [];
  if (!trips.length) { closeMo('mo-import-confirm'); showToast('Nada pra atualizar — nenhuma corrida com valor diferente ou nova.', 'info'); return; }
  document.getElementById('ic-title').textContent = 'Corridas pra importar ('+trips.length+')';
  document.getElementById('ic-btn').style.display = '';
  updateImportButtonLabel();

  // Dois grupos, na ordem do mockup aprovado (16/08/2026): primeiro o que
  // já existia e só corrige valor (verde), depois o que é novo — nunca
  // visto na captura ao vivo, vira "importada" (âmbar).
  const changedRows = [], newRows = [];
  trips.forEach((t,i) => { (t._isNew ? newRows : changedRows).push(importRowHtml(t,i)); });

  let groupsHtml = '';
  if (changedRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#22C55E"></span><span style="font-size:12.5px;font-weight:800">Valor vai mudar</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${changedRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0;margin-bottom:${newRows.length?'10px':'0'}">${changedRows.join('')}</div>`;
  }
  if (newRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#F59E0B"></span><span style="font-size:12.5px;font-weight:800">Novas</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${newRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0">${newRows.join('')}</div>`;
  }

  document.getElementById('ic-body').innerHTML = '<div id="ic-declared">' + importDeclaredHtml() + '</div>' +
    '<div id="ic-summary">' + importSummaryHtml() + '</div>' + groupsHtml;
}
function toggleImportTrip(i) {
  const t = (window._pendingTrips||[])[i]; if (!t) return;
  t._sel = !t._sel;
  const chkColor = t._isNew ? '#F59E0B' : '#22C55E';
  const el = document.getElementById('ict-'+i);
  if (el) { el.style.background=t._sel?chkColor:'transparent'; el.style.borderColor=t._sel?chkColor:'#E2E8F0'; el.textContent=t._sel?'✓':''; }
  updateImportButtonLabel();
  updateImportSummary();
}

// Detecta corrida duplicada (hora+valor, hora+km, valor+rota)
// Versão do código — atualizar para verificar se cache está correto
const _APP_CODE_VER = "v24-trip99-fix";

// Normaliza texto de rota pra comparação: minúsculas, sem acentos, sem
// espaços extras — "Barra do Imbuí" e "barra do imbui" viram iguais.
// (OCR de frames diferentes do mesmo vídeo varia acentuação → sem isso,
// a mesma corrida escapa do detector de duplicata.)
function _normRoute(s) {
  return (s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')
    .replace(/\s+/g,' ').trim();
}
function isTripDuplicate(newT, existing) {
  return existing.some(t => {
    // Plataformas diferentes = corridas diferentes (nunca duplicata)
    const samePlat = (t.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const sameTime = t.time && newT.time && t.time === newT.time;
    const sameVal  = Math.abs((t.value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && t.km > 0 && Math.abs((t.km||0)-(newT.km||0)) < 0.5;
    // 99 não reporta km — usar origem+destino como sinal de rota nesse caso
    // (comparação normalizada: acento/caixa não separam corridas)
    const sameRoute = !!(t.origin && newT.origin && t.dest && newT.dest
                       && _normRoute(t.origin) === _normRoute(newT.origin)
                       && _normRoute(t.dest) === _normRoute(newT.dest));
    const nearTime = t.time && newT.time && (() => {
      const [h1,m1] = t.time.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    // Mesmo horário + mesmo valor = duplicata clara
    if (sameTime && sameVal) return true;
    // Mesmo horário EXATO + mesma rota = duplicata mesmo com valor diferente.
    // Protege contra fusão de frames no OCR de vídeo (rota de uma corrida +
    // valor do cartão vizinho). Duas corridas reais no MESMO minuto com a
    // MESMA origem e MESMO destino não existem na prática.
    if (sameTime && sameRoute) return true;
    // Mesmo horário + mesma km (proteção contra OCR com valor diferente)
    if (sameTime && sameKm && newT.km > 0) return true;
    // Próximo horário + mesmo valor + mesma km = duplicata provável
    if (sameVal && nearTime && sameKm) return true;
    // Próximo horário + mesmo valor + mesma rota (sem km, ex: 99) = duplicata provável
    if (sameVal && nearTime && sameRoute) return true;
    // REMOVIDO: sameVal && sameRoute sem tempo = falso positivo entre plataformas
    return false;
  });
}

// ── Casar corrida importada com corrida automática já capturada ────────────
// Pedido do Yuri (23/07/2026): ao importar, se a corrida já foi capturada
// automaticamente, ATUALIZA a automática com o valor real (confirmado pela
// tela de histórico da plataforma — mais confiável que a oferta estimada),
// em vez de criar uma corrida manual duplicada. Só corridas GENUINAMENTE
// novas (sem correspondência) seguem pro fluxo normal de criar.
// Mesmos critérios do isTripDuplicate acima, adaptados pros campos de
// auto_trips (trip_started_at em vez de time, offer_value em vez de value,
// real_km_trip em vez de km, origin_address/dest_address em vez de
// origin/dest) — time da automática convertido pro fuso local em HH:MM.
function _autoTripLocalTime(at) {
  const ts = at.trip_started_at || at.accepted_at;
  if (!ts) return null;
  const dt = new Date(ts);
  return String(dt.getHours()).padStart(2,'0')+':'+String(dt.getMinutes()).padStart(2,'0');
}
function matchAutoTrip(newT, autoTrips) {
  return autoTrips.find(at => {
    const samePlat = (at.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const atTime = _autoTripLocalTime(at);
    const sameTime = atTime && newT.time && atTime === newT.time;
    const sameVal  = Math.abs((at.offer_value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && at.real_km_trip > 0 && Math.abs((at.real_km_trip||0)-(newT.km||0)) < 0.5;
    const sameRoute = !!(at.origin_address && newT.origin && at.dest_address && newT.dest
                       && _normRoute(at.origin_address).includes(_normRoute(newT.origin))
                       && _normRoute(at.dest_address).includes(_normRoute(newT.dest)));
    const nearTime = atTime && newT.time && (() => {
      const [h1,m1] = atTime.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    if (sameTime && sameVal) return true;
    if (sameTime && sameRoute) return true;
    if (sameTime && sameKm && newT.km > 0) return true;
    if (sameVal && nearTime && sameKm) return true;
    if (sameVal && nearTime && sameRoute) return true;
    return false;
  }) || null;
}

// ── Reconciliação de sessões × corridas ───────────────────────────────────
// Regra híbrida: < 15 min → agrupa na sessão + ajusta horários
//                >= 15 min → cria nova sessão
function reconcileSessionsForDay(dateStr, dryRun = false) {
  const THRESHOLD = 15 * 60 * 1000; // 15 minutos em ms
  const day = DB.day(dateStr);
  
  const trips = (day.trips || [])
    .map(t => {
      let ts = t.ts || 0;
      if (!ts && t.time) ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
      return { ...t, _ts: ts };
    })
    .filter(t => t._ts > 0)
    .sort((a, b) => a._ts - b._ts);
  
  if (!trips.length) return { grouped: 0, created: 0, expanded: 0, sessions: day.sessions || [] };
  
  // Cópia profunda das sessões para não mutar durante dry run
  const sessions = JSON.parse(JSON.stri
===== #F59E0B @ 836759 =====
-weight:800;padding:1.5px 6px;border-radius:99px;background:#FEF3C7;color:#92400E;margin-left:5px">Nova</span>' : '';
  const chkColor = t._isNew ? '#F59E0B' : '#22C55E';
  const valHtml = t._isNew
    ? `R$${(t.value||0).toFixed(2)}`
    : `R$${(t.value||0).toFixed(2)}<small style="display:block;font-size:9px;color:#64748B;font-weight:700">era ${DB.fR(t._oldValue||0)}</small>`;
  return `<div style="display:flex;align-items:center;gap:10px;padding:11px 14px;border-bottom:1px solid #E2E8F0;cursor:pointer" onclick="toggleImportTrip(${i})">
    <div style="width:22px;height:22px;border-radius:6px;border:2px solid ${t._sel?chkColor:'#E2E8F0'};background:${t._sel?chkColor:'transparent'};display:flex;align-items:center;justify-content:center;color:#fff;font-size:13px;flex-shrink:0" id="ict-${i}">${t._sel?'✓':''}</div>
    <div style="flex:1;min-width:0"><div style="font-size:12px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${t.origin||'—'} → ${t.dest||'—'}${newBadge}</div><div style="font-size:10px;color:#64748B;margin-top:2px">${platTag} · ${t.time||'—'} · ${t.duration||0}min${t.km?' · '+t.km+'km':''}</div>${exRow}</div>
    <div style="font-family:'Inter',system-ui,sans-serif;font-weight:700;font-size:14px;color:${t._isNew?'#16A34A':'#0F172A'};flex-shrink:0;text-align:right">${valHtml}</div>
  </div>`;
}
function showImportConfirm() {
  const trips = window._pendingTrips || [];
  if (!trips.length) { closeMo('mo-import-confirm'); showToast('Nada pra atualizar — nenhuma corrida com valor diferente ou nova.', 'info'); return; }
  document.getElementById('ic-title').textContent = 'Corridas pra importar ('+trips.length+')';
  document.getElementById('ic-btn').style.display = '';
  updateImportButtonLabel();

  // Dois grupos, na ordem do mockup aprovado (16/08/2026): primeiro o que
  // já existia e só corrige valor (verde), depois o que é novo — nunca
  // visto na captura ao vivo, vira "importada" (âmbar).
  const changedRows = [], newRows = [];
  trips.forEach((t,i) => { (t._isNew ? newRows : changedRows).push(importRowHtml(t,i)); });

  let groupsHtml = '';
  if (changedRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#22C55E"></span><span style="font-size:12.5px;font-weight:800">Valor vai mudar</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${changedRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0;margin-bottom:${newRows.length?'10px':'0'}">${changedRows.join('')}</div>`;
  }
  if (newRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#F59E0B"></span><span style="font-size:12.5px;font-weight:800">Novas</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${newRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0">${newRows.join('')}</div>`;
  }

  document.getElementById('ic-body').innerHTML = '<div id="ic-declared">' + importDeclaredHtml() + '</div>' +
    '<div id="ic-summary">' + importSummaryHtml() + '</div>' + groupsHtml;
}
function toggleImportTrip(i) {
  const t = (window._pendingTrips||[])[i]; if (!t) return;
  t._sel = !t._sel;
  const chkColor = t._isNew ? '#F59E0B' : '#22C55E';
  const el = document.getElementById('ict-'+i);
  if (el) { el.style.background=t._sel?chkColor:'transparent'; el.style.borderColor=t._sel?chkColor:'#E2E8F0'; el.textContent=t._sel?'✓':''; }
  updateImportButtonLabel();
  updateImportSummary();
}

// Detecta corrida duplicada (hora+valor, hora+km, valor+rota)
// Versão do código — atualizar para verificar se cache está correto
const _APP_CODE_VER = "v24-trip99-fix";

// Normaliza texto de rota pra comparação: minúsculas, sem acentos, sem
// espaços extras — "Barra do Imbuí" e "barra do imbui" viram iguais.
// (OCR de frames diferentes do mesmo vídeo varia acentuação → sem isso,
// a mesma corrida escapa do detector de duplicata.)
function _normRoute(s) {
  return (s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')
    .replace(/\s+/g,' ').trim();
}
function isTripDuplicate(newT, existing) {
  return existing.some(t => {
    // Plataformas diferentes = corridas diferentes (nunca duplicata)
    const samePlat = (t.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const sameTime = t.time && newT.time && t.time === newT.time;
    const sameVal  = Math.abs((t.value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && t.km > 0 && Math.abs((t.km||0)-(newT.km||0)) < 0.5;
    // 99 não reporta km — usar origem+destino como sinal de rota nesse caso
    // (comparação normalizada: acento/caixa não separam corridas)
    const sameRoute = !!(t.origin && newT.origin && t.dest && newT.dest
                       && _normRoute(t.origin) === _normRoute(newT.origin)
                       && _normRoute(t.dest) === _normRoute(newT.dest));
    const nearTime = t.time && newT.time && (() => {
      const [h1,m1] = t.time.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    // Mesmo horário + mesmo valor = duplicata clara
    if (sameTime && sameVal) return true;
    // Mesmo horário EXATO + mesma rota = duplicata mesmo com valor diferente.
    // Protege contra fusão de frames no OCR de vídeo (rota de uma corrida +
    // valor do cartão vizinho). Duas corridas reais no MESMO minuto com a
    // MESMA origem e MESMO destino não existem na prática.
    if (sameTime && sameRoute) return true;
    // Mesmo horário + mesma km (proteção contra OCR com valor diferente)
    if (sameTime && sameKm && newT.km > 0) return true;
    // Próximo horário + mesmo valor + mesma km = duplicata provável
    if (sameVal && nearTime && sameKm) return true;
    // Próximo horário + mesmo valor + mesma rota (sem km, ex: 99) = duplicata provável
    if (sameVal && nearTime && sameRoute) return true;
    // REMOVIDO: sameVal && sameRoute sem tempo = falso positivo entre plataformas
    return false;
  });
}

// ── Casar corrida importada com corrida automática já capturada ────────────
// Pedido do Yuri (23/07/2026): ao importar, se a corrida já foi capturada
// automaticamente, ATUALIZA a automática com o valor real (confirmado pela
// tela de histórico da plataforma — mais confiável que a oferta estimada),
// em vez de criar uma corrida manual duplicada. Só corridas GENUINAMENTE
// novas (sem correspondência) seguem pro fluxo normal de criar.
// Mesmos critérios do isTripDuplicate acima, adaptados pros campos de
// auto_trips (trip_started_at em vez de time, offer_value em vez de value,
// real_km_trip em vez de km, origin_address/dest_address em vez de
// origin/dest) — time da automática convertido pro fuso local em HH:MM.
function _autoTripLocalTime(at) {
  const ts = at.trip_started_at || at.accepted_at;
  if (!ts) return null;
  const dt = new Date(ts);
  return String(dt.getHours()).padStart(2,'0')+':'+String(dt.getMinutes()).padStart(2,'0');
}
function matchAutoTrip(newT, autoTrips) {
  return autoTrips.find(at => {
    const samePlat = (at.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const atTime = _autoTripLocalTime(at);
    const sameTime = atTime && newT.time && atTime === newT.time;
    const sameVal  = Math.abs((at.offer_value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && at.real_km_trip > 0 && Math.abs((at.real_km_trip||0)-(newT.km||0)) < 0.5;
    const sameRoute = !!(at.origin_address && newT.origin && at.dest_address && newT.dest
                       && _normRoute(at.origin_address).includes(_normRoute(newT.origin))
                       && _normRoute(at.dest_address).includes(_normRoute(newT.dest)));
    const nearTime = atTime && newT.time && (() => {
      const [h1,m1] = atTime.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    if (sameTime && sameVal) return true;
    if (sameTime && sameRoute) return true;
    if (sameTime && sameKm && newT.km > 0) return true;
    if (sameVal && nearTime && sameKm) return true;
    if (sameVal && nearTime && sameRoute) return true;
    return false;
  }) || null;
}

// ── Reconciliação de sessões × corridas ───────────────────────────────────
// Regra híbrida: < 15 min → agrupa na sessão + ajusta horários
//                >= 15 min → cria nova sessão
function reconcileSessionsForDay(dateStr, dryRun = false) {
  const THRESHOLD = 15 * 60 * 1000; // 15 minutos em ms
  const day = DB.day(dateStr);
  
  const trips = (day.trips || [])
    .map(t => {
      let ts = t.ts || 0;
      if (!ts && t.time) ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
      return { ...t, _ts: ts };
    })
    .filter(t => t._ts > 0)
    .sort((a, b) => a._ts - b._ts);
  
  if (!trips.length) return { grouped: 0, created: 0, expanded: 0, sessions: day.sessions || [] };
  
  // Cópia profunda das sessões para não mutar durante dry run
  const sessions = JSON.parse(JSON.stringify(day.sessions || []));
  let grouped = 0, created = 0, expanded = 0;
  
  for (const trip of trips) {
    const ts  = trip._ts;
    const dur = (trip.duration || 15) * 60 * 1000;
    const end = ts + dur;
    
    // Já está dentro de uma sessão existente?
    const inside = sessions.find(s =>
      s.start && ts >= (s.start - 1000) && (!s.end || ts <= (s.end + 1000))
    );
    if (inside) continue;
    
    // Sessão mais próxima (distância ao início ou fim)
    let best = null, bestDist = Infinity;
    for (const s of sessions) {
      if (!s.start) continue;
      const d = Math.min(
        Math.abs(ts - s.start),
        s.
===== #F59E0B @ 872699 =====
const o = _cleanBairro(t.origin||''), d = _cleanBairro(t.dest||'');
        if (o !== (t.origin||'')) { t.origin = o; ch = true; }
        if (d !== (t.dest||''))   { t.dest = d;  ch = true; }
      });
      if (ch) DB.saveDay(day);
    });
    localStorage.setItem('sm_bairro_clean_v1','1');
  } catch(e) { console.warn('[_migrateBairroNames]', e); }
})();
setTimeout(()=>{ _syncTick(); setInterval(_syncTick, 60*1000); }, 3000); // primeira vez em 3s, depois a cada 1 minuto

// Corridas automáticas novas capturadas durante o dia (pedido do Yuri,
// 23/07/2026) — INTERVALO PRÓPRIO, separado do _syncTick de propósito: se
// qualquer etapa do _syncTick (correção de trips, auto-sync, backfill de
// km) travar ou lançar erro, essa atualização não pode ficar refém disso —
// foi exatamente isso que aconteceu (corridas paravam de aparecer na
// Jornada ativa depois de um tempo, travadas no que existia no momento do
// primeiro carregamento do dia). 25s pra sentir a corrida nova rápido sem
// martelar o Supabase.
async function _refreshAutoTripsIfOnJornada() {
  if (!(S && S.sec === 'jornada' && S.curDate)) return;
  try {
    const list = await loadAutoTripsForDay(S.curDate);
    if (!Array.isArray(list)) return; // falha não vira "zero corridas"
    S.autoTrips = list; S.autoTripsDate = S.curDate; S._autoTripsDataDate = S.curDate;
    renderJornada();
  } catch(e) {}
}
setTimeout(()=>{ _refreshAutoTripsIfOnJornada(); setInterval(_refreshAutoTripsIfOnJornada, 25*1000); }, 4000);

// CORRIGIDO (24/07/2026, confirmado pelo Yuri: corridas terminadas até
// 22:21 não apareciam numa tela aberta desde antes) — a tela Corridas
// nunca tinha refresh automático como a Jornada já tem. Se ficasse parado
// nela sem trocar filtro/período nem sair e voltar, ficava presa no que
// carregou da última vez, mesmo com corridas novas terminando por trás.
// Mesmo padrão de intervalo da Jornada.
async function _refreshCorridasIfOnScreen() {
  if (!(S && S.sec === 'corridas')) return;
  try { await renderCorridasActive(); } catch(e) {}
}
setTimeout(()=>{ _refreshCorridasIfOnScreen(); setInterval(_refreshCorridasIfOnScreen, 25*1000); }, 4500);

// Mesma lógica pra Home (pedido do Yuri, 23/07/2026) — a checagem de cache
// em renderHomeGraphs() só refaz a busca quando o INTERVALO DE DATAS muda
// (troca de semana/mês/etc), nunca por tempo — uma corrida nova no meio do
// dia nunca apareceria sozinha sem isso. 60s (não precisa ser tão rápido
// quanto a Jornada, que é tempo real; Home é histórico/agregado).
async function _refreshAutoTripsIfOnHome() {
  if (!(S && S.sec === 'relatorios')) return;
  S.autoTripsHomeRange = null; // força o próximo renderHomeGraphs a rebuscar
  try { renderHomeGraphs(); } catch(e) {}
}
setTimeout(()=>{ setInterval(_refreshAutoTripsIfOnHome, 60*1000); }, 5000);

// ── Status de rede: indicador visual + reconciliação ao voltar online ──
let _netSyncing = false;
function updateNetStatus() {
  const badge = document.getElementById("net-status-badge");
  const dot = document.getElementById("net-status-dot");
  const txt = document.getElementById("net-status-text");
  if (!badge) return;
  if (!navigator.onLine) {
    badge.style.display = "flex";
    badge.style.background = "#FEF2F2";
    dot.style.background = "#EF4444";
    txt.style.color = "#B91C1C";
    txt.textContent = "Offline — dados salvos localmente";
  } else if (_netSyncing) {
    badge.style.display = "flex";
    badge.style.background = "#FFF7ED";
    dot.style.background = "#F59E0B";
    txt.style.color = "#C2410C";
    txt.textContent = "Sincronizando...";
  } else {
    badge.style.display = "none"; // tudo ok, não precisa mostrar nada
  }
}
window.addEventListener("offline", updateNetStatus);
window.addEventListener("online", async () => {
  _netSyncing = true; updateNetStatus();
  try { await SYNC.flushQueue(); await reconcileJornadaHoje(); } catch(e) {}
  _netSyncing = false; updateNetStatus();
  try { renderJornada(); } catch(e) {}
});

// ── Reconciliação: Supabase é a fonte de verdade, local é o cache ──────
async function reconcileJornadaHoje() {
  if (!_supaUser || !navigator.onLine) return;
  const today = DB.today();
  try {
    const {data: remote, error} = await _SUPA.from("sessions").select("*")
      .eq("user_id", _supaUser.id).eq("date", today);
    if (error || !remote) return;
    const day = DB.day(today);
    if (!day.sessions) day.sessions = [];
    let changed = false;
    // 1. Toda sessão remota vira a verdade local (upsert por id)
    remote.forEach(r => {
      if (!r.end_ms) return; // sessão ainda ativa remotamente — não mexe (pode ser a atual)
      const local = { id: r.id, start: r.start_ms, end: r.end_ms, km: parseFloat(r.km)||0, elapsed: r.elapsed_secs||0 };
      const idx = day.sessions.findIndex(s => s.id === r.id);
      if (idx === -1) { day.sessions.push(local); changed = true; }
      else if (day.sessions[idx].end !== local.end || day.sessions[idx].km !== local.km) {
        day.sessions[idx] = local; changed = true;
      }
    });
    // 2. Sessão local encerrada que não existe no remoto — reenviar (não apagar)
    const remoteIds = new Set(remote.map(r => r.id));
    for (const s of day.sessions) {
      if (!remoteIds.has(s.id) && s.end) {
        await _SUPA.from("sessions").upsert({
          id: s.id, user_id: _supaUser.id, date: today,
          start_ms: s.start, end_ms: s.end, km: s.km, elapsed_secs: s.elapsed,
          paused_ms: 0, is_paused: false
        }, {onConflict:"id"}).catch(()=>{});
      }
    }
    // 3. Sessão ativa: se remoto diz que já encerrou mas o app local acha que continua
    if (S.gps.st !== "idle" && S.gps.sessionId) {
      const remoteActive = remote.find(r => r.id === S.gps.sessionId);
      if (remoteActive && remoteActive.end_ms) {
        S.gps.st = "idle"; S.gps.start = null;
        clearInterval(S.gps.ti);
        changed = true;
      }
    }
    if (changed) DB.saveDay(day);
  } catch(e) { console.warn("[reconcileJornadaHoje]", e); }
}

// ── Estimativa de km por endereço (Nominatim geocode + OSRM route) ────
// Usado quando a plataforma não informa km (ex: 99 Motorista). Gratuito,
// mas com throttle de 1 req/s no Nominatim — processar em fila sequencial.
let _geoQueueBusy = false;
const _geoQueue = [];
// Remove prefixo "Bairro da/do/de/das/dos" do nome de bairro (a 99 escreve
// "Bairro da São Pedro" — queremos exibir/geocodificar só "São Pedro")
function _cleanBairro(name) {
  if (!name) return name;
  return name.replace(/^\s*bairro\s+(d[aeo]s?\s+)?/i, '').trim();
}
// Limpa lixo comum do OCR no endereço antes de geocodificar: vírgulas soltas,
// país truncado ("Bra"/"Bras"), número de prédio grudado ("1211- Rua X"),
// e o prefixo "Bairro da" que confunde o Nominatim.
function _cleanGeoAddr(addr) {
  if (!addr) return addr;
  let a = addr.trim();
  a = a.replace(/,\s*(BR|Brasil|Bras?i?l?|Bra)\.?\s*$/i, '');
  a = a.replace(/[,\s]+$/, '');
  a = a.replace(/^\d+\s*-\s*/, '');
  a = a.replace(/\bbairro\s+(d[aeo]s?\s+)/gi, '');
  return a.replace(/\s{2,}/g, ' ').trim();
}
// Cache persistente de geocodificação: os mesmos bairros/endereços repetem
// muito no dia a dia — resolver 1x e reaproveitar corta o tempo da fila de
// ~5s/corrida pra quase zero nas repetidas (e economiza chamadas ao Nominatim).
const _geoCache = (() => {
  try { return JSON.parse(localStorage.getItem('sm_geo_cache_v1') || '{}'); }
  catch(e) { return {}; }
})();
function _saveGeoCache() {
  try {
    // limita a 400 entradas pra não inchar o localStorage
    const keys = Object.keys(_geoCache);
    if (keys.length > 400) keys.slice(0, keys.length - 400).forEach(k => delete _geoCache[k]);
    localStorage.setItem('sm_geo_cache_v1', JSON.stringify(_geoCache));
  } catch(e) {}
}
// Throttle global do Nominatim (1 req/s) — embutido aqui pra que cache hits
// não paguem espera nenhuma.
let _lastNominatimMs = 0;
async function _nominatimThrottle() {
  const wait = 1100 - (Date.now() - _lastNominatimMs);
  if (wait > 0) await new Promise(r => setTimeout(r, wait));
  _lastNominatimMs = Date.now();
}
async function _geocodeAddress(query) {
  if (!query) return null;
  const key = query.toLowerCase().trim();
  if (_geoCache[key]) return _geoCache[key];
  try {
    await _nominatimThrottle();
    const url = "https://nominatim.openstreetmap.org/search?format=json&limit=1&countrycodes=br&q=" + encodeURIComponent(query);
    const r = await fetch(url, { headers: { "Accept-Language": "pt-BR" } });
    const data = await r.json();
    if (!data || !data.length) return null;
    const pt = { lat: parseFloat(data[0].lat), lon: parseFloat(data[0].lon) };
    _geoCache[key] = pt; _saveGeoCache();
    return pt;
  } catch(e) { console.warn("[_geocodeAddress]", e); return null; }
}
async function _routeDistanceOSRM(p1, p2) {
  try {
    const url = `https://router.project-osrm.org/route/v1/driving/${p1.lon},${p1.lat};${p2.lon},${p2.lat}?overview=false`;
    const r = await fetch(url);
    const data = await r.json();
    if (!data.routes || !data.routes.length) return null;
    return data.routes[0].distance / 1000; // metros → km
  } catch(e) { console.warn("[_routeDistanceOSRM]", e); return null; }
}
// Verifica se o endereço tem informação suficiente pra geocodificar com
// confiança (nome de rua real) — 'Tijuca' ou '230, Tijuca' sozinhos são
// ambíguos demais (podem cair em qualquer ponto do bairro).
function _hasReliableAddress(addr) {
  if (!addr) return false;
  const firstPart = addr.split(',')[0].trim();
  // Primeira parte só com dígitos (número de prédio solto, sem rua) → não confiável
  if (/^\d+$/.test(firstPart)) return false;
  // Primeira parte com menos de 4 letras (ex: nome de bairro sozinho) → não confiável
  if (firstPart.replace(/[^a-zA-ZÀ-ÿ]/g,'').length < 4) return false;
  return true;
}
// Geocodifica com fallback: tenta o endereço completo; se falhar, tenta só
// da parte do bairro em diante (ex: "Rua X, Ermitage, Teresópolis - RJ" →
// "Ermitage, Teresópolis - RJ") — menos prec
===== #F59E0B @ 980265 =====
   type:      agEvType,
    color:     agColor,
    notes:     document.getElementById('ag-obs')?.value   || '',
    value:     parseFloat(document.getElementById('ag-valor')?.value||0)||null,
  };
  const evs = DB.events();
  if (agEditingId) {
    const ev = evs.find(e => e.id === agEditingId);
    if (ev) Object.assign(ev, fields);
  } else {
    evs.push({ id: DB.uid(), paid: false, ...fields });
  }
  DB.saveEvents(evs);
  agEditingId = null;
  closeMo('mo-novo-ev');
  renderAgenda();
}
function delEvento(id) {
  showConfirm('Excluir compromisso?', () => {
    DB.saveEvents(DB.events().filter(e => e.id !== id));
    renderAgenda();
  });
}

// ── HELPERS ─────────────────────────────────────────────────
function getEventsForDate(d) {
  return DB.events().filter(e => e.date === d);
}
function offsetDate(base, days) {
  const d = new Date(base+'T12:00'); d.setDate(d.getDate()+days);
  return d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
}
function formatElapsed(secs) {
  if (!secs) return '';
  const h = Math.floor(secs/3600), m = Math.floor(secs%3600/60);
  return (h ? h+'h' : '') + (m ? p2(m)+'min' : '') + ' online';
}



// ============================================================
// AGENDA CALENDÁRIO
// ============================================================
let agCalY    = new Date().getFullYear();
let agCalM    = new Date().getMonth();

const MONTH_NAMES = ['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];
const DAY_ABVS    = ['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'];

function renderAgCal() {
  const titleEl = document.getElementById('agcal-title');
  if (titleEl) titleEl.textContent = MONTH_NAMES[agCalM] + ' ' + agCalY;
  const daysEl = document.getElementById('agcal-days');
  if (!daysEl) return;
  // Build grid
  const firstDay  = new Date(agCalY, agCalM, 1).getDay();  // 0=Sun
  const daysInMon = new Date(agCalY, agCalM+1, 0).getDate();
  const prevDays  = new Date(agCalY, agCalM, 0).getDate();
  const today     = DB.today();
  const allEvs    = DB.events();
  let html = '';
  // Prev month padding
  for (let i = firstDay-1; i >= 0; i--) {
    html += `<div class="agcal-day muted"><span>${prevDays-i}</span></div>`;
  }
  // Current month
  for (let d = 1; d <= daysInMon; d++) {
    const ds  = agCalY+'-'+p2(agCalM+1)+'-'+p2(d);
    const isTod = ds === today;
    const isSel = ds === agSelDate;
    const cls   = 'agcal-day'+(isSel?' sel':isTod?' today-d':'');
    // Dots: GPS session + events (max 3)
    const hasSess = !!agDaySess(ds);
    const dayEvs  = allEvs.filter(e => e.date === ds);
    const dots    = [];
    if (hasSess) dots.push('green');
    dayEvs.forEach(e => { if (dots.length < 3) dots.push(colorClass(e.color||'#3b82f6')); });
    const dotsHtml = dots.length ? '<div class="agcal-dots">'+dots.map(cl=>`<div class="agcal-dot ${cl}"></div>`).join('')+'</div>' : '';
    html += `<div class="${cls}" onclick="agCalSelect('${ds}')"><span>${d}</span>${dotsHtml}</div>`;
  }
  // Next month padding
  const used  = firstDay + daysInMon;
  const rows  = Math.ceil(used/7);
  const total = rows * 7;
  for (let i = 1; i <= total-used; i++) {
    html += `<div class="agcal-day muted"><span>${i}</span></div>`;
  }
  // Replace all days (grid is 7 cols but days div fills span)
  daysEl.outerHTML = `<div id="agcal-days" style="display:contents">${html}</div>`;
}

function colorClass(hex) {
  const m = {'#22c55e':'green','#3b82f6':'blue','#f59e0b':'orange','#8b5cf6':'purple','#ef4444':'red'};
  return m[hex] || 'blue';
}

function agCalSelect(ds) {
  agSetDate(ds);
}
function agCalNav(d) {
  agCalM += d;
  if (agCalM < 0)  { agCalM = 11; agCalY--; }
  if (agCalM > 11) { agCalM = 0;  agCalY++; }
  renderAgCal();
}




// ── IMPORTAR HISTÓRICO ────────────────────────────────────────────────────
function openHistImport() { openMo('mo-hist-import'); }

async function loadHistFile(input) {
  if (!input.files[0]) return;
  const file = input.files[0];
  const progEl = document.getElementById('hist-progress');
  const barEl  = document.getElementById('hist-bar');
  const msgEl  = document.getElementById('hist-msg');
  if (progEl) progEl.style.display = 'block';
  if (msgEl) msgEl.textContent = 'Lendo arquivo...';

  try {
    const text = await new Promise((res, rej) => {
      const r = new FileReader();
      r.onload = () => res(r.result);
      r.onerror = () => rej(r.error);
      r.readAsText(file, 'utf-8');
    });

    const data = JSON.parse(text);
    const keys = Object.keys(data).filter(k => k.startsWith('sm_day_'));
    let added = 0, skipped = 0, days_count = keys.length;

    for (let i = 0; i < keys.length; i++) {
      const key    = keys[i];
      const newDay = data[key];
      const curDay = DB.get(key) || {date: newDay.date, trips:[], expenses:[], revenues:[], kmTotal:null, session:null};

      // Add only non-duplicate trips
      newDay.trips.forEach(t => {
        const dup = curDay.trips.some(ex =>
          ex.time === t.time && Math.abs((ex.value||0)-(t.value||0)) < 0.02
        );
        if (!dup) { curDay.trips.push(t); added++; }
        else skipped++;
      });

      // Set kmTotal if not already set
      if (!curDay.kmTotal && newDay.kmTotal) curDay.kmTotal = newDay.kmTotal;

      DB.set(key, curDay);

      // Update progress
      const pct = Math.round((i+1)/days_count*100);
      if (barEl) barEl.style.width = pct+'%';
      if (msgEl) msgEl.textContent = `Importando... dia ${i+1} de ${days_count} (${pct}%)`;

      // Yield to UI every 10 days
      if (i % 10 === 0) await new Promise(r => setTimeout(r, 0));
    }

    if (msgEl) msgEl.textContent = `Concluído! ${added} corridas importadas, ${skipped} já existiam.`;
    if (barEl) barEl.style.width = '100%';
    setTimeout(() => {
      closeMo('mo-hist-import');
      try { renderHome(); } catch(e) {}
      try { renderJornada(); } catch(e) {}
      showToast(`Histórico importado com sucesso!\n${added} corridas adicionadas em ${days_count} dias.`, 'success');
    }, 1200);

  } catch(err) {
    if (msgEl) msgEl.textContent = 'Erro: ' + err.message;
    console.error(err);
  }
}



// ── WAKE LOCK (mantém GPS ativo em segundo plano) ─────────────────────────
let _wakeLock = null;

async function requestWakeLock() {
  if (!('wakeLock' in navigator)) return;
  try {
    _wakeLock = await navigator.wakeLock.request('screen');
    _wakeLock.addEventListener('release', () => {
      _wakeLock = null;
      // Se GPS ainda ativo, tenta reativar automaticamente
      if (S.gps.st === 'running') requestWakeLock();
    });
    console.log('SmartMobi: Wake Lock ativado — GPS continua em segundo plano');
  } catch(e) {
    console.warn('Wake Lock não disponível:', e.message);
  }
}

async function releaseWakeLock() {
  if (_wakeLock) {
    await _wakeLock.release();
    _wakeLock = null;
  }
}

// Reativa Wake Lock quando usuário volta pro app com GPS ativo
document.addEventListener('visibilitychange', async () => {
  if (document.visibilityState === 'visible' && S.gps.st === 'running') {
    await requestWakeLock();
  }
});



// ── NATIVE OVERLAY BRIDGE ──────────────────────────────────────────────────
// Chamado pelo app nativo quando uma corrida é capturada pelo Accessibility Service
function receiveOverlayTrip(trip) {
  if (!trip || !trip.value) return;
  const day = DB.day(S.curDate);
  if (isTripDuplicate(trip, day.trips)) return;
  trip.id = trip.id || DB.uid();
  day.trips.push(trip);
  DB.saveDay(day);
  SYNC.push('trips', { id: trip.id, date: S.curDate, platform: trip.platform || 'uber',
    time: trip.time || null, duration: trip.duration || 0, origin: trip.origin || '',
    dest: trip.dest || '', value: parseFloat(trip.value) || 0, km: parseFloat(trip.km) || 0,
    category: trip.category || '', surge: trip.surge || false, airport: trip.airport || false,
    source: 'overlay' });
  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
  }, 80);
  // Vibração de confirmação
  if (window.SmartMobiNative) SmartMobiNative.vibrate(80);
}

// Banner de configuração para o app nativo
function showNativeBanner(msg) {
  const existing = document.getElementById('native-banner');
  if (existing) existing.remove();
  const el = document.createElement('div');
  el.id = 'native-banner';
  el.style.cssText = 'position:fixed;top:0;left:0;right:0;z-index:9999;background:#0F172A;color:#fff;font-size:12px;font-weight:600;padding:10px 16px;display:flex;align-items:center;justify-content:space-between;gap:8px';
  el.innerHTML = '<span>' + msg + '</span><button onclick="SmartMobiNative&&SmartMobiNative.openA11ySettings()" style="background:#22C55E;border:0;color:#fff;border-radius:8px;padding:4px 10px;font-size:11px;font-weight:700;cursor:pointer">Configurar</button>';
  document.body.prepend(el);
  setTimeout(() => el.remove(), 6000);
}

function onNativeReady() {
  // App rodando como APK nativo
  console.log('SmartMobi Native v' + (SmartMobiNative?.getVersion?.() || '?'));
  syncFlashConfigToNative();
}

// ── MōB Flash: "Indicadores" que aparecem no card (números coloridos) ──
const FLASH_KPI_DEFS = [
  { key:'rkm',    label:'R$ por km',          short:'R$/KM',   icon:'$',  defRed:1.2, defGreen:2.0 },
  { key:'rhora',  label:'R$ por hora',        short:'R$/HORA', icon:'\u23F1', defRed:25,  defGreen:45 },
  { key:'rmin',   label:'R$ por minuto',      short:'R$/MIN',  icon:'\u23F2', defRed:0.6, defGreen:1.0 },
  { key:'margem', label:'% Lucro',            short:'% LUCRO', icon:'%',  defRed:15,  defGreen:40 },
  { key:'lucro',  label:'Lucro em R$',        short:'R$ LUCRO',icon:'R$', defRed:5,   defGreen:15 },
];
const FLASH_DEFAULT_ON = { rkm:true, rhora:true, rmin:false, margem:true, lucro:true };
const FLASH_PREVIEW_SAMPLE = { rkm:2.37, rhora:49, rmin:0.58, margem:12, lucro:4 };

// Reaproveita o mesmo limiar vermelho/verde do MōB Flash (Indicadores) pra
// colorir a faixa de destaque R$/h · R$/km 
===== #F59E0B @ 1018145 =====
cata (bairro pesquisado em rua+cidade diferentes às vezes
    // volta repetido do Nominatim com o mesmo rótulo limpo).
    const seen = new Set();
    const labeled = data.map(d => _flashBuildLabel(d)).filter(x => {
      const k = x.label.toLowerCase();
      if (seen.has(k)) return false;
      seen.add(k); return true;
    });
    box.innerHTML = labeled.map(x => `
      <div onclick="flashEnderecoAddSuggestion('${escJsAttr(x.label)}','${x.type}')" style="padding:10px 12px;border-bottom:1px solid #F8FAFC;font-size:12px;color:#0F172A;font-weight:600">
        <span style="font-size:8px;font-weight:800;color:${x.type === 'rua' ? '#0A2F6B' : '#B91C1C'};margin-right:5px">${x.type === 'rua' ? 'RUA' : 'BAIRRO'}</span>${esc(x.label)}
      </div>
    `).join('');
  } catch (e) {
    box.innerHTML = '<div style="padding:10px 12px;font-size:11.5px;color:#94A3B8">Sem conexão — aperte + pra adicionar do jeito que digitou.</div>';
  }
}
// BUG CONFIRMADO (17/07/2026): endereço só era salvo quando o assistente
// inteiro era concluído (botão final "Salvar configurações" no passo 2).
// O "Pronto" da tela de endereço só volta pra lista — não salva nada. Se a
// pessoa adicionava um endereço e fechava o modal (X) sem passar pelo
// passo final, o endereço sumia. Agora qualquer mexida na lista grava na
// hora, sem depender do resto do assistente.
function _flashPersistNow() {
  const cfg = DB.cfg();
  cfg.flashReader = {
    enabled: flashDraft.enabled,
    kpis: JSON.parse(JSON.stringify(flashDraft.kpis)),
    recusas: JSON.parse(JSON.stringify(flashDraft.recusas))
  };
  DB.saveCfg(cfg);
  SYNC.pushSettings();
  syncFlashConfigToNative();
}

function flashEnderecoAddSuggestion(text, type) {
  const list = flashDraft.recusas.endereco.list;
  if (!list.some(a => a.label.toLowerCase() === text.toLowerCase())) list.push({ label: text, type: type === 'rua' ? 'rua' : 'bairro' });
  const inp = document.getElementById('flash-endereco-input');
  inp.value = '';
  document.getElementById('flash-endereco-suggestions').style.display = 'none';
  renderFlashEnderecos();
  _flashPersistNow();
}

function flashOpenEnderecos() {
  renderFlashEnderecos();
  _flashGeoFetch();
  document.getElementById('flash-step1').style.display = 'none';
  document.getElementById('flash-step2').style.display = 'none';
  document.getElementById('flash-ruler').style.display = 'none';
  document.getElementById('flash-recusa-num').style.display = 'none';
  document.getElementById('flash-enderecos').style.display = 'flex';
}
function flashEnderecoAdd() {
  const inp = document.getElementById('flash-endereco-input');
  const v = inp.value.trim();
  if (!v) return;
  const list = flashDraft.recusas.endereco.list;
  // Sem dado estruturado (digitado na mão) — adivinha pelo prefixo comum
  // de rua ("Rua ", "Av ", etc); sem prefixo reconhecido, assume bairro.
  if (!list.some(a => a.label.toLowerCase() === v.toLowerCase())) list.push({ label: v, type: _flashGuessType(v) });
  inp.value = '';
  document.getElementById('flash-endereco-suggestions').style.display = 'none';
  renderFlashEnderecos();
  _flashPersistNow();
}
function flashEnderecoRemove(i) {
  flashDraft.recusas.endereco.list.splice(i, 1);
  renderFlashEnderecos();
  _flashPersistNow();
}
function flashEnderecosClose() {
  _flashWizardShowStep(1);
  renderFlashRecusaList();
}

function flashGradeFor(key, v) {
  const k = flashDraft.kpis[key];
  if (v < k.red) return '#EF4444';
  if (v >= k.green) return '#10B981';
  return '#F59E0B';
}

function flashFmtVal(key, v) {
  if (key === 'margem') return String(Math.round(v));
  if (key === 'lucro') return v.toFixed(2).replace('.', ',');
  return v.toFixed(2).replace('.', ',');
}

function renderFlashPreview() {
  const wrap = document.getElementById('flash-preview-wrap');
  if (!flashDraft.enabled) {
    wrap.innerHTML = '<div style="color:#8A8A99;font-size:12px;padding:20px 0">MōB Flash desativado</div>';
    return;
  }
  const active = FLASH_KPI_DEFS.filter(d => flashDraft.kpis[d.key].enabled).slice(0, 4);
  if (!active.length) {
    wrap.innerHTML = '<div style="color:#8A8A99;font-size:12px;padding:20px 0">Selecione ao menos 1 KPI</div>';
    return;
  }
  const colors = active.map(d => flashGradeFor(d.key, FLASH_PREVIEW_SAMPLE[d.key]));
  const overall = colors.includes('#EF4444') ? '#EF4444' : (colors.includes('#F59E0B') ? '#F59E0B' : '#10B981');
  const cardWidth = 46 + 74 * active.length; // espelha a largura dinâmica do card nativo

  const metricsHtml = `
    <div style="display:flex">
      ${active.map((d, i) => `<div style="flex:1;text-align:center;min-width:0;position:relative;padding:0 2px">
        ${i < active.length - 1 ? '<div style="position:absolute;right:0;top:3px;bottom:3px;width:1px;background:rgba(255,255,255,.15)"></div>' : ''}
        <b style="display:block;font-size:26px;font-weight:800;color:${flashGradeFor(d.key, FLASH_PREVIEW_SAMPLE[d.key])};line-height:1.05">${flashFmtVal(d.key, FLASH_PREVIEW_SAMPLE[d.key])}</b>
        <span style="font-size:6.8px;color:#8A8A99;font-weight:800">${d.short}</span>
      </div>`).join('')}
    </div>`;

  wrap.innerHTML = `
    <div style="width:${cardWidth}px;background:#0E0E17E6;border-radius:16px;display:flex;overflow:hidden">
      <div style="width:8px;background:${overall}"></div>
      <div style="flex:1;padding:10px 11px 9px;display:flex;flex-direction:column;min-width:0">
        ${metricsHtml}
        <div style="height:1px;background:rgba(255,255,255,.12);margin:9px 0 7px"></div>
        <div style="display:flex;align-items:center;gap:8px">
          <span style="background:#FFC800;color:#111;font-size:10px;font-weight:900;padding:3px 7px;border-radius:5px">99</span>
          <span style="flex:1;color:#fff;font-size:15.5px;font-weight:800">18m</span>
          <span style="color:#fff;font-size:15.5px;font-weight:800">7.8 km</span>
        </div>
      </div>
      <div style="width:8px;background:${overall}"></div>
    </div>`;
}

function flashConfigSave(fromWizard) {
  const cfg = DB.cfg();
  cfg.flashReader = {
    enabled: flashDraft.enabled,
    kpis: JSON.parse(JSON.stringify(flashDraft.kpis)),
    recusas: JSON.parse(JSON.stringify(flashDraft.recusas))
  };
  DB.saveCfg(cfg);
  SYNC.pushSettings();
  syncFlashConfigToNative();
  closeMo('mo-flash');
  if (fromWizard && flashWizardMode === 'start') {
    // Veio do fluxo de Iniciar Jornada — sem toast (ia distrair bem na hora
    // que a jornada começa) e dispara o callback que efetivamente inicia.
    const cb = flashWizardOnComplete;
    flashWizardOnComplete = null;
    if (cb) cb();
  } else {
    showToast('Configurações do MōB Flash salvas!');
  }
}

// Manda pro nativo (TripReaderService) o custo/km configurado em Financeiro
// + os Indicadores + as Recusas escolhidas na tela MōB Flash.
function syncFlashConfigToNative() {
  if (!window.SmartMobiNative || typeof SmartMobiNative.saveFlashConfig !== 'function') return;
  const cfg = DB.cfg();
  const fr = cfg.flashReader || getFlashDefaults();
  const custoPorKm = (cfg.custoVeiculo && cfg.custoVeiculo.configurado) ? (cfg.custoVeiculo.custoPorKm || 0) : 0;
  const recusas = fr.recusas || getFlashDefaults().recusas;
  // "parada" fica solto no topo (recusarComParada) — é assim que o Kotlin
  // já lê desde a v1.0.69, antes do agrupamento "Recusas" existir. Os
  // outros 4 (nota/passageiroNovo/distancia/endereco) vão aninhados em
  // "recusas", com os mesmos nomes de campo que o Kotlin já espera
  // (enabled/value/list).
  const payload = {
    enabled: fr.enabled !== false,
    custoPorKm: custoPorKm,
    kpis: fr.kpis || getFlashDefaults().kpis,
    recusarComParada: recusas.parada ? recusas.parada.enabled : true,
    recusas: {
      nota: recusas.nota,
      passageiroNovo: recusas.passageiroNovo,
      distancia: recusas.distancia,
      endereco: recusas.endereco
    }
  };
  try { SmartMobiNative.saveFlashConfig(JSON.stringify(payload)); } catch (e) {}
}



// ══════════════════════════════════════════════
// SUPABASE AUTH — email + senha
// ══════════════════════════════════════════════
let _supaUser = null;

// ── SINCRONIZAÇÃO COM SUPABASE ──────────────────────────────────────────
// Push: grava cada alteração local também na nuvem (silencioso, não trava a UI).
// Pull: no primeiro boot logado, busca tudo da nuvem e mescla no localStorage,
// pra dados sobreviverem a troca de aparelho ou reinstalação.
const SYNC = {
  // Enfileira uma operacao que falhou pra tentar de novo depois (ex: sem internet
  // no meio de uma corrida). Fica salva no localStorage, sobrevive a fechar o app.
  enqueue(op) {
    const q = DB.syncQueue();
    const id = Date.now()+'_'+Math.random().toString(36).slice(2);
    q.push(Object.assign({ id, ts: Date.now() }, op));
    DB.saveSyncQueue(q);
    this._updateBadge();
    return id;
  },
  dequeue(id) {
    const q = DB.syncQueue().filter(op => op.id !== id);
    DB.saveSyncQueue(q);
    this._updateBadge();
  },
  _updateBadge() {
    const n = DB.syncQueue().length;
    const el = document.getElementById('sync-pending-badge');
    if (el) el.style.display = n > 0 ? 'flex' : 'none';
  },
  async push(table, row) {
    if (!_supaUser) return;
    try {
      await _SUPA.from(table).upsert(Object.assign({}, row, {
        user_id: _supaUser.id, synced_at: new Date().toISOString()
      }));
    } catch (e) {
      console.error('SYNC.push', table, e);
      this.enqueue({ type: 'push', table, row });
    }
  },
  async del(table, match) {
    if (!_supaUser) return;
    try {
      let q = _SUPA.from(table).delete();
      Object.entries(match).forEach(([k, v]) => { q = q.eq(k, v); });
      await q;
    } catch (e) {
      console.error('SYNC.del', table, e);
      this.enqueue({ type: 'del', table, match });
    }
  },
  async pushCats(key) {
    if (!_supaUser) return;
    try {
      await _SUPA.from('categories').upsert({
        user_id: _supaUser.id, cat_key: key, items: DB.cats(key) || DEFAULT_CATS[key] || [],
        goal
===== #F59E0B @ 1018999 =====
;color:#94A3B8">Sem conexão — aperte + pra adicionar do jeito que digitou.</div>';
  }
}
// BUG CONFIRMADO (17/07/2026): endereço só era salvo quando o assistente
// inteiro era concluído (botão final "Salvar configurações" no passo 2).
// O "Pronto" da tela de endereço só volta pra lista — não salva nada. Se a
// pessoa adicionava um endereço e fechava o modal (X) sem passar pelo
// passo final, o endereço sumia. Agora qualquer mexida na lista grava na
// hora, sem depender do resto do assistente.
function _flashPersistNow() {
  const cfg = DB.cfg();
  cfg.flashReader = {
    enabled: flashDraft.enabled,
    kpis: JSON.parse(JSON.stringify(flashDraft.kpis)),
    recusas: JSON.parse(JSON.stringify(flashDraft.recusas))
  };
  DB.saveCfg(cfg);
  SYNC.pushSettings();
  syncFlashConfigToNative();
}

function flashEnderecoAddSuggestion(text, type) {
  const list = flashDraft.recusas.endereco.list;
  if (!list.some(a => a.label.toLowerCase() === text.toLowerCase())) list.push({ label: text, type: type === 'rua' ? 'rua' : 'bairro' });
  const inp = document.getElementById('flash-endereco-input');
  inp.value = '';
  document.getElementById('flash-endereco-suggestions').style.display = 'none';
  renderFlashEnderecos();
  _flashPersistNow();
}

function flashOpenEnderecos() {
  renderFlashEnderecos();
  _flashGeoFetch();
  document.getElementById('flash-step1').style.display = 'none';
  document.getElementById('flash-step2').style.display = 'none';
  document.getElementById('flash-ruler').style.display = 'none';
  document.getElementById('flash-recusa-num').style.display = 'none';
  document.getElementById('flash-enderecos').style.display = 'flex';
}
function flashEnderecoAdd() {
  const inp = document.getElementById('flash-endereco-input');
  const v = inp.value.trim();
  if (!v) return;
  const list = flashDraft.recusas.endereco.list;
  // Sem dado estruturado (digitado na mão) — adivinha pelo prefixo comum
  // de rua ("Rua ", "Av ", etc); sem prefixo reconhecido, assume bairro.
  if (!list.some(a => a.label.toLowerCase() === v.toLowerCase())) list.push({ label: v, type: _flashGuessType(v) });
  inp.value = '';
  document.getElementById('flash-endereco-suggestions').style.display = 'none';
  renderFlashEnderecos();
  _flashPersistNow();
}
function flashEnderecoRemove(i) {
  flashDraft.recusas.endereco.list.splice(i, 1);
  renderFlashEnderecos();
  _flashPersistNow();
}
function flashEnderecosClose() {
  _flashWizardShowStep(1);
  renderFlashRecusaList();
}

function flashGradeFor(key, v) {
  const k = flashDraft.kpis[key];
  if (v < k.red) return '#EF4444';
  if (v >= k.green) return '#10B981';
  return '#F59E0B';
}

function flashFmtVal(key, v) {
  if (key === 'margem') return String(Math.round(v));
  if (key === 'lucro') return v.toFixed(2).replace('.', ',');
  return v.toFixed(2).replace('.', ',');
}

function renderFlashPreview() {
  const wrap = document.getElementById('flash-preview-wrap');
  if (!flashDraft.enabled) {
    wrap.innerHTML = '<div style="color:#8A8A99;font-size:12px;padding:20px 0">MōB Flash desativado</div>';
    return;
  }
  const active = FLASH_KPI_DEFS.filter(d => flashDraft.kpis[d.key].enabled).slice(0, 4);
  if (!active.length) {
    wrap.innerHTML = '<div style="color:#8A8A99;font-size:12px;padding:20px 0">Selecione ao menos 1 KPI</div>';
    return;
  }
  const colors = active.map(d => flashGradeFor(d.key, FLASH_PREVIEW_SAMPLE[d.key]));
  const overall = colors.includes('#EF4444') ? '#EF4444' : (colors.includes('#F59E0B') ? '#F59E0B' : '#10B981');
  const cardWidth = 46 + 74 * active.length; // espelha a largura dinâmica do card nativo

  const metricsHtml = `
    <div style="display:flex">
      ${active.map((d, i) => `<div style="flex:1;text-align:center;min-width:0;position:relative;padding:0 2px">
        ${i < active.length - 1 ? '<div style="position:absolute;right:0;top:3px;bottom:3px;width:1px;background:rgba(255,255,255,.15)"></div>' : ''}
        <b style="display:block;font-size:26px;font-weight:800;color:${flashGradeFor(d.key, FLASH_PREVIEW_SAMPLE[d.key])};line-height:1.05">${flashFmtVal(d.key, FLASH_PREVIEW_SAMPLE[d.key])}</b>
        <span style="font-size:6.8px;color:#8A8A99;font-weight:800">${d.short}</span>
      </div>`).join('')}
    </div>`;

  wrap.innerHTML = `
    <div style="width:${cardWidth}px;background:#0E0E17E6;border-radius:16px;display:flex;overflow:hidden">
      <div style="width:8px;background:${overall}"></div>
      <div style="flex:1;padding:10px 11px 9px;display:flex;flex-direction:column;min-width:0">
        ${metricsHtml}
        <div style="height:1px;background:rgba(255,255,255,.12);margin:9px 0 7px"></div>
        <div style="display:flex;align-items:center;gap:8px">
          <span style="background:#FFC800;color:#111;font-size:10px;font-weight:900;padding:3px 7px;border-radius:5px">99</span>
          <span style="flex:1;color:#fff;font-size:15.5px;font-weight:800">18m</span>
          <span style="color:#fff;font-size:15.5px;font-weight:800">7.8 km</span>
        </div>
      </div>
      <div style="width:8px;background:${overall}"></div>
    </div>`;
}

function flashConfigSave(fromWizard) {
  const cfg = DB.cfg();
  cfg.flashReader = {
    enabled: flashDraft.enabled,
    kpis: JSON.parse(JSON.stringify(flashDraft.kpis)),
    recusas: JSON.parse(JSON.stringify(flashDraft.recusas))
  };
  DB.saveCfg(cfg);
  SYNC.pushSettings();
  syncFlashConfigToNative();
  closeMo('mo-flash');
  if (fromWizard && flashWizardMode === 'start') {
    // Veio do fluxo de Iniciar Jornada — sem toast (ia distrair bem na hora
    // que a jornada começa) e dispara o callback que efetivamente inicia.
    const cb = flashWizardOnComplete;
    flashWizardOnComplete = null;
    if (cb) cb();
  } else {
    showToast('Configurações do MōB Flash salvas!');
  }
}

// Manda pro nativo (TripReaderService) o custo/km configurado em Financeiro
// + os Indicadores + as Recusas escolhidas na tela MōB Flash.
function syncFlashConfigToNative() {
  if (!window.SmartMobiNative || typeof SmartMobiNative.saveFlashConfig !== 'function') return;
  const cfg = DB.cfg();
  const fr = cfg.flashReader || getFlashDefaults();
  const custoPorKm = (cfg.custoVeiculo && cfg.custoVeiculo.configurado) ? (cfg.custoVeiculo.custoPorKm || 0) : 0;
  const recusas = fr.recusas || getFlashDefaults().recusas;
  // "parada" fica solto no topo (recusarComParada) — é assim que o Kotlin
  // já lê desde a v1.0.69, antes do agrupamento "Recusas" existir. Os
  // outros 4 (nota/passageiroNovo/distancia/endereco) vão aninhados em
  // "recusas", com os mesmos nomes de campo que o Kotlin já espera
  // (enabled/value/list).
  const payload = {
    enabled: fr.enabled !== false,
    custoPorKm: custoPorKm,
    kpis: fr.kpis || getFlashDefaults().kpis,
    recusarComParada: recusas.parada ? recusas.parada.enabled : true,
    recusas: {
      nota: recusas.nota,
      passageiroNovo: recusas.passageiroNovo,
      distancia: recusas.distancia,
      endereco: recusas.endereco
    }
  };
  try { SmartMobiNative.saveFlashConfig(JSON.stringify(payload)); } catch (e) {}
}



// ══════════════════════════════════════════════
// SUPABASE AUTH — email + senha
// ══════════════════════════════════════════════
let _supaUser = null;

// ── SINCRONIZAÇÃO COM SUPABASE ──────────────────────────────────────────
// Push: grava cada alteração local também na nuvem (silencioso, não trava a UI).
// Pull: no primeiro boot logado, busca tudo da nuvem e mescla no localStorage,
// pra dados sobreviverem a troca de aparelho ou reinstalação.
const SYNC = {
  // Enfileira uma operacao que falhou pra tentar de novo depois (ex: sem internet
  // no meio de uma corrida). Fica salva no localStorage, sobrevive a fechar o app.
  enqueue(op) {
    const q = DB.syncQueue();
    const id = Date.now()+'_'+Math.random().toString(36).slice(2);
    q.push(Object.assign({ id, ts: Date.now() }, op));
    DB.saveSyncQueue(q);
    this._updateBadge();
    return id;
  },
  dequeue(id) {
    const q = DB.syncQueue().filter(op => op.id !== id);
    DB.saveSyncQueue(q);
    this._updateBadge();
  },
  _updateBadge() {
    const n = DB.syncQueue().length;
    const el = document.getElementById('sync-pending-badge');
    if (el) el.style.display = n > 0 ? 'flex' : 'none';
  },
  async push(table, row) {
    if (!_supaUser) return;
    try {
      await _SUPA.from(table).upsert(Object.assign({}, row, {
        user_id: _supaUser.id, synced_at: new Date().toISOString()
      }));
    } catch (e) {
      console.error('SYNC.push', table, e);
      this.enqueue({ type: 'push', table, row });
    }
  },
  async del(table, match) {
    if (!_supaUser) return;
    try {
      let q = _SUPA.from(table).delete();
      Object.entries(match).forEach(([k, v]) => { q = q.eq(k, v); });
      await q;
    } catch (e) {
      console.error('SYNC.del', table, e);
      this.enqueue({ type: 'del', table, match });
    }
  },
  async pushCats(key) {
    if (!_supaUser) return;
    try {
      await _SUPA.from('categories').upsert({
        user_id: _supaUser.id, cat_key: key, items: DB.cats(key) || DEFAULT_CATS[key] || [],
        goals: DB.objGoals(key) || {},
        synced_at: new Date().toISOString()
      });
    } catch (e) {
      console.error('SYNC.pushCats', e);
      this.enqueue({ type: 'pushCats', key });
    }
  },
  // Tenta reenviar tudo que ficou pendente, na ordem em que foi criado. O que
  // funcionar sai da fila; o que falhar de novo fica pra proxima tentativa.
  async flushQueue() {
    if (!_supaUser || !navigator.onLine) return;
    const q = DB.syncQueue();
    if (!q.length) return;
    const remaining = [];
    for (const op of q) {
      try {
        if (op.type === 'push') {
          await _SUPA.from(op.table).upsert(Object.assign({}, op.row, {
            user_id: _supaUser.id, synced_at: new Date().toISOString()
          }));
        } else if (op.type === 'del') {
          let dq = _SUPA.from(op.table).delete();
          Object.entries(
===== #F59E0B @ 1019012 =====
8">Sem conexão — aperte + pra adicionar do jeito que digitou.</div>';
  }
}
// BUG CONFIRMADO (17/07/2026): endereço só era salvo quando o assistente
// inteiro era concluído (botão final "Salvar configurações" no passo 2).
// O "Pronto" da tela de endereço só volta pra lista — não salva nada. Se a
// pessoa adicionava um endereço e fechava o modal (X) sem passar pelo
// passo final, o endereço sumia. Agora qualquer mexida na lista grava na
// hora, sem depender do resto do assistente.
function _flashPersistNow() {
  const cfg = DB.cfg();
  cfg.flashReader = {
    enabled: flashDraft.enabled,
    kpis: JSON.parse(JSON.stringify(flashDraft.kpis)),
    recusas: JSON.parse(JSON.stringify(flashDraft.recusas))
  };
  DB.saveCfg(cfg);
  SYNC.pushSettings();
  syncFlashConfigToNative();
}

function flashEnderecoAddSuggestion(text, type) {
  const list = flashDraft.recusas.endereco.list;
  if (!list.some(a => a.label.toLowerCase() === text.toLowerCase())) list.push({ label: text, type: type === 'rua' ? 'rua' : 'bairro' });
  const inp = document.getElementById('flash-endereco-input');
  inp.value = '';
  document.getElementById('flash-endereco-suggestions').style.display = 'none';
  renderFlashEnderecos();
  _flashPersistNow();
}

function flashOpenEnderecos() {
  renderFlashEnderecos();
  _flashGeoFetch();
  document.getElementById('flash-step1').style.display = 'none';
  document.getElementById('flash-step2').style.display = 'none';
  document.getElementById('flash-ruler').style.display = 'none';
  document.getElementById('flash-recusa-num').style.display = 'none';
  document.getElementById('flash-enderecos').style.display = 'flex';
}
function flashEnderecoAdd() {
  const inp = document.getElementById('flash-endereco-input');
  const v = inp.value.trim();
  if (!v) return;
  const list = flashDraft.recusas.endereco.list;
  // Sem dado estruturado (digitado na mão) — adivinha pelo prefixo comum
  // de rua ("Rua ", "Av ", etc); sem prefixo reconhecido, assume bairro.
  if (!list.some(a => a.label.toLowerCase() === v.toLowerCase())) list.push({ label: v, type: _flashGuessType(v) });
  inp.value = '';
  document.getElementById('flash-endereco-suggestions').style.display = 'none';
  renderFlashEnderecos();
  _flashPersistNow();
}
function flashEnderecoRemove(i) {
  flashDraft.recusas.endereco.list.splice(i, 1);
  renderFlashEnderecos();
  _flashPersistNow();
}
function flashEnderecosClose() {
  _flashWizardShowStep(1);
  renderFlashRecusaList();
}

function flashGradeFor(key, v) {
  const k = flashDraft.kpis[key];
  if (v < k.red) return '#EF4444';
  if (v >= k.green) return '#10B981';
  return '#F59E0B';
}

function flashFmtVal(key, v) {
  if (key === 'margem') return String(Math.round(v));
  if (key === 'lucro') return v.toFixed(2).replace('.', ',');
  return v.toFixed(2).replace('.', ',');
}

function renderFlashPreview() {
  const wrap = document.getElementById('flash-preview-wrap');
  if (!flashDraft.enabled) {
    wrap.innerHTML = '<div style="color:#8A8A99;font-size:12px;padding:20px 0">MōB Flash desativado</div>';
    return;
  }
  const active = FLASH_KPI_DEFS.filter(d => flashDraft.kpis[d.key].enabled).slice(0, 4);
  if (!active.length) {
    wrap.innerHTML = '<div style="color:#8A8A99;font-size:12px;padding:20px 0">Selecione ao menos 1 KPI</div>';
    return;
  }
  const colors = active.map(d => flashGradeFor(d.key, FLASH_PREVIEW_SAMPLE[d.key]));
  const overall = colors.includes('#EF4444') ? '#EF4444' : (colors.includes('#F59E0B') ? '#F59E0B' : '#10B981');
  const cardWidth = 46 + 74 * active.length; // espelha a largura dinâmica do card nativo

  const metricsHtml = `
    <div style="display:flex">
      ${active.map((d, i) => `<div style="flex:1;text-align:center;min-width:0;position:relative;padding:0 2px">
        ${i < active.length - 1 ? '<div style="position:absolute;right:0;top:3px;bottom:3px;width:1px;background:rgba(255,255,255,.15)"></div>' : ''}
        <b style="display:block;font-size:26px;font-weight:800;color:${flashGradeFor(d.key, FLASH_PREVIEW_SAMPLE[d.key])};line-height:1.05">${flashFmtVal(d.key, FLASH_PREVIEW_SAMPLE[d.key])}</b>
        <span style="font-size:6.8px;color:#8A8A99;font-weight:800">${d.short}</span>
      </div>`).join('')}
    </div>`;

  wrap.innerHTML = `
    <div style="width:${cardWidth}px;background:#0E0E17E6;border-radius:16px;display:flex;overflow:hidden">
      <div style="width:8px;background:${overall}"></div>
      <div style="flex:1;padding:10px 11px 9px;display:flex;flex-direction:column;min-width:0">
        ${metricsHtml}
        <div style="height:1px;background:rgba(255,255,255,.12);margin:9px 0 7px"></div>
        <div style="display:flex;align-items:center;gap:8px">
          <span style="background:#FFC800;color:#111;font-size:10px;font-weight:900;padding:3px 7px;border-radius:5px">99</span>
          <span style="flex:1;color:#fff;font-size:15.5px;font-weight:800">18m</span>
          <span style="color:#fff;font-size:15.5px;font-weight:800">7.8 km</span>
        </div>
      </div>
      <div style="width:8px;background:${overall}"></div>
    </div>`;
}

function flashConfigSave(fromWizard) {
  const cfg = DB.cfg();
  cfg.flashReader = {
    enabled: flashDraft.enabled,
    kpis: JSON.parse(JSON.stringify(flashDraft.kpis)),
    recusas: JSON.parse(JSON.stringify(flashDraft.recusas))
  };
  DB.saveCfg(cfg);
  SYNC.pushSettings();
  syncFlashConfigToNative();
  closeMo('mo-flash');
  if (fromWizard && flashWizardMode === 'start') {
    // Veio do fluxo de Iniciar Jornada — sem toast (ia distrair bem na hora
    // que a jornada começa) e dispara o callback que efetivamente inicia.
    const cb = flashWizardOnComplete;
    flashWizardOnComplete = null;
    if (cb) cb();
  } else {
    showToast('Configurações do MōB Flash salvas!');
  }
}

// Manda pro nativo (TripReaderService) o custo/km configurado em Financeiro
// + os Indicadores + as Recusas escolhidas na tela MōB Flash.
function syncFlashConfigToNative() {
  if (!window.SmartMobiNative || typeof SmartMobiNative.saveFlashConfig !== 'function') return;
  const cfg = DB.cfg();
  const fr = cfg.flashReader || getFlashDefaults();
  const custoPorKm = (cfg.custoVeiculo && cfg.custoVeiculo.configurado) ? (cfg.custoVeiculo.custoPorKm || 0) : 0;
  const recusas = fr.recusas || getFlashDefaults().recusas;
  // "parada" fica solto no topo (recusarComParada) — é assim que o Kotlin
  // já lê desde a v1.0.69, antes do agrupamento "Recusas" existir. Os
  // outros 4 (nota/passageiroNovo/distancia/endereco) vão aninhados em
  // "recusas", com os mesmos nomes de campo que o Kotlin já espera
  // (enabled/value/list).
  const payload = {
    enabled: fr.enabled !== false,
    custoPorKm: custoPorKm,
    kpis: fr.kpis || getFlashDefaults().kpis,
    recusarComParada: recusas.parada ? recusas.parada.enabled : true,
    recusas: {
      nota: recusas.nota,
      passageiroNovo: recusas.passageiroNovo,
      distancia: recusas.distancia,
      endereco: recusas.endereco
    }
  };
  try { SmartMobiNative.saveFlashConfig(JSON.stringify(payload)); } catch (e) {}
}



// ══════════════════════════════════════════════
// SUPABASE AUTH — email + senha
// ══════════════════════════════════════════════
let _supaUser = null;

// ── SINCRONIZAÇÃO COM SUPABASE ──────────────────────────────────────────
// Push: grava cada alteração local também na nuvem (silencioso, não trava a UI).
// Pull: no primeiro boot logado, busca tudo da nuvem e mescla no localStorage,
// pra dados sobreviverem a troca de aparelho ou reinstalação.
const SYNC = {
  // Enfileira uma operacao que falhou pra tentar de novo depois (ex: sem internet
  // no meio de uma corrida). Fica salva no localStorage, sobrevive a fechar o app.
  enqueue(op) {
    const q = DB.syncQueue();
    const id = Date.now()+'_'+Math.random().toString(36).slice(2);
    q.push(Object.assign({ id, ts: Date.now() }, op));
    DB.saveSyncQueue(q);
    this._updateBadge();
    return id;
  },
  dequeue(id) {
    const q = DB.syncQueue().filter(op => op.id !== id);
    DB.saveSyncQueue(q);
    this._updateBadge();
  },
  _updateBadge() {
    const n = DB.syncQueue().length;
    const el = document.getElementById('sync-pending-badge');
    if (el) el.style.display = n > 0 ? 'flex' : 'none';
  },
  async push(table, row) {
    if (!_supaUser) return;
    try {
      await _SUPA.from(table).upsert(Object.assign({}, row, {
        user_id: _supaUser.id, synced_at: new Date().toISOString()
      }));
    } catch (e) {
      console.error('SYNC.push', table, e);
      this.enqueue({ type: 'push', table, row });
    }
  },
  async del(table, match) {
    if (!_supaUser) return;
    try {
      let q = _SUPA.from(table).delete();
      Object.entries(match).forEach(([k, v]) => { q = q.eq(k, v); });
      await q;
    } catch (e) {
      console.error('SYNC.del', table, e);
      this.enqueue({ type: 'del', table, match });
    }
  },
  async pushCats(key) {
    if (!_supaUser) return;
    try {
      await _SUPA.from('categories').upsert({
        user_id: _supaUser.id, cat_key: key, items: DB.cats(key) || DEFAULT_CATS[key] || [],
        goals: DB.objGoals(key) || {},
        synced_at: new Date().toISOString()
      });
    } catch (e) {
      console.error('SYNC.pushCats', e);
      this.enqueue({ type: 'pushCats', key });
    }
  },
  // Tenta reenviar tudo que ficou pendente, na ordem em que foi criado. O que
  // funcionar sai da fila; o que falhar de novo fica pra proxima tentativa.
  async flushQueue() {
    if (!_supaUser || !navigator.onLine) return;
    const q = DB.syncQueue();
    if (!q.length) return;
    const remaining = [];
    for (const op of q) {
      try {
        if (op.type === 'push') {
          await _SUPA.from(op.table).upsert(Object.assign({}, op.row, {
            user_id: _supaUser.id, synced_at: new Date().toISOString()
          }));
        } else if (op.type === 'del') {
          let dq = _SUPA.from(op.table).delete();
          Object.entries(op.match).for
===== #F59E0B @ 1624171 =====
.gps.st === 'idle') return;
  if (DB.cfg().overlayEnabled === false) return;
  try {
    const overlayAlive = (typeof SmartMobiNative.hasOverlay === 'function') ? SmartMobiNative.hasOverlay() : true;
    if (!overlayAlive) {
      console.log('[SmartMobi] Overlay sumiu — recriando bolinha');
      if (typeof SmartMobiNative.startFloating === 'function')
        SmartMobiNative.startFloating(S.gps.start, S.gps.km || 0);
      if (typeof SmartMobiNative.updateFloatingStatus === 'function')
        SmartMobiNative.updateFloatingStatus('running');
    }
  } catch(e) {}
}, 20000); // verifica a cada 20 segundos

// ── Fila de sincronizacao: tenta reenviar o que ficou pendente quando a
// conexao voltar, e periodicamente como rede de seguranca (sinal fraco pode
// nao disparar o evento 'online' de forma confiavel).
window.addEventListener('online', () => { SYNC.flushQueue().catch(()=>{}); });
setInterval(() => { SYNC.flushQueue().catch(()=>{}); }, 30000);

// ── Service worker: registra, força checagem de atualização e recarrega
// sozinho quando uma versão nova assumir o controle. Sem isso, correções
// ficavam "presas" numa versão antiga no celular sem nada avisar.
// CORRIGIDO (24/07/2026, confirmado pelo Yuri): reg.update() só rodava
// UMA VEZ, na abertura do app — uma sessão que fica aberta o dia inteiro
// sem fechar de verdade nunca checava de novo, ficando presa na versão
// carregada de manhã mesmo com correções novas no ar há horas. Agora
// checa a cada 3min também.
if ('serviceWorker' in navigator) {
  navigator.serviceWorker.register('/sw.js').then(reg => {
    reg.update();
    setInterval(() => { reg.update().catch(()=>{}); }, 3*60*1000);
    let reloaded = false;
    navigator.serviceWorker.addEventListener('controllerchange', () => {
      if (reloaded) return;
      reloaded = true;
      location.reload();
    });
  }).catch(() => {});
}

// Reporta qual versão da PWA o usuário está rodando de verdade (pedido do
// Yuri, 24/07/2026) — pra investigar problema de cache/versão desatualizada
// sem precisar perguntar "que versão você tá vendo?". IMPORTANTE: manter
// igual ao CACHE_NAME de sw.js sempre que um dos dois mudar.
const PWA_VERSION = 'mob-v25-sw-update-check';
async function reportClientVersion() {
  if (!_supaUser) return;
  try {
    await _SUPA.from('client_versions').upsert(
      { user_id: _supaUser.id, platform: 'pwa', version: PWA_VERSION, updated_at: new Date().toISOString() },
      { onConflict: 'user_id,platform' }
    );
  } catch (e) {}
}
setTimeout(()=>{ reportClientVersion(); setInterval(reportClientVersion, 3*60*1000); }, 5000);

// ══════════════════════════════════════════════
// INFORMES
// ══════════════════════════════════════════════
const INF_TIPOS = {
  aero:  {cor:'#0A2F6B',bg:'#EFF6FF',label:'Aeroporto',
    r1:['fila','lotacao'], r2:['transito','fiscalizacao','dinamico','demanda'],
    modal:['lotacao','fila','dinamico','demanda','fiscalizacao','transito']},
  term:  {cor:'#6366F1',bg:'#EEF2FF',label:'Terminal',
    r1:['fila','lotacao'], r2:['transito','fiscalizacao','dinamico','demanda'],
    modal:['lotacao','fila','demanda','dinamico','fiscalizacao','transito']},
  bairro:{cor:'#475569',bg:'#F1F5F9',label:'Bairro',
    r1:['dinamico','demanda'], r2:[],
    modal:['dinamico','demanda']},
  rua:   {cor:'#14B8A6',bg:'#CCFBF1',label:'Rua',
    r1:['transito','acidente'], r2:['fiscalizacao','dinamico','demanda'],
    modal:['transito','fiscalizacao','acidente','dinamico']},
  ev:    {cor:'#F59E0B',bg:'#FFF7ED',label:'Evento',
    r1:['dinamico','demanda'], r2:['transito','fiscalizacao','fila','lotacao'],
    modal:['fila','lotacao','demanda','dinamico','fiscalizacao']},
  tur:   {cor:'#22C55E',bg:'#F0FDF4',label:'Turístico',
    r1:['dinamico','demanda'], r2:['transito','fiscalizacao','fila','lotacao'],
    modal:['fila','lotacao','demanda','fiscalizacao']},
  bar:   {cor:'#8B5CF6',bg:'#EDE9FE',label:'Bar / Restaurante',
    r1:['demanda','lotacao'], r2:['fiscalizacao','dinamico','fila'],
    modal:['fila','lotacao','demanda','fiscalizacao']},
  hotel: {cor:'#EC4899',bg:'#FDF2F8',label:'Hotel',
    r1:['demanda','lotacao'], r2:['dinamico'],
    modal:['lotacao','fila','demanda','fiscalizacao']},
  shop:  {cor:'#B45309',bg:'#FEF3C7',label:'Shopping',
    r1:['demanda','lotacao'], r2:['transito','fiscalizacao','dinamico'],
    modal:['lotacao','fila','demanda','fiscalizacao','dinamico']},
};
const INF_PARAMS = {
  transito:    {label:'Trânsito',     expMin:30, opts:[{v:'Fluindo',c:'g'},{v:'Leve',c:''},{v:'Lento',c:'a'},{v:'Parado',c:'r'}]},
  fiscalizacao:{label:'Fiscalização', expMin:60, sub:[{v:'PM',c:'r'},{v:'GM',c:'r'},{v:'Lei Seca',c:'r'},{v:'Detro',c:'a'}], subacao:[{v:'Multando',c:'r'},{v:'Abordando',c:'a'},{v:'Posicionado',c:''}]},
  dinamico:    {label:'Dinâmico',     expMin:15, opts:[{v:'R$ 0',c:''},{v:'+R$2,50',c:'g'},{v:'+R$5',c:'g'},{v:'+R$8',c:'g'},{v:'+R$10',c:'g'},{v:'+R$15',c:'g'},{v:'+R$20',c:'g'},{v:'+R$25',c:'g'}]},
  demanda:     {label:'Demanda',      expMin:30, opts:[{v:'Tocando bem',c:'g'},{v:'Esporádico',c:'a'},{v:'Não toca nada',c:'r'}]},
  fila:        {label:'Fila',         expMin:20, opts:[{v:'Vazia',c:'g'},{v:'Pequena',c:''},{v:'Média',c:'a'},{v:'Grande',c:'r'}]},
  lotacao:     {label:'Lotação',      expMin:60, opts:[{v:'Vazio',c:'g'},{v:'Pouco movimentado',c:'g'},{v:'Movimentado',c:'a'},{v:'Muito movimentado',c:'r'},{v:'Cheio',c:'r'}]},
  acidente:    {label:'Acidente',     expMin:30, opts:[{v:'Na via · ativo',c:'r'},{v:'Acostamento',c:'a'},{v:'Resolvido',c:'g'}]},
};
const INF_PLBL = {transito:'Trânsito',fiscalizacao:'Fiscalização',dinamico:'Dinâmico',demanda:'Demanda',fila:'Fila',lotacao:'Lotação',acidente:'Acidente'};
const INF_BAIRROS = {
  sul:   ['Copacabana','Ipanema','Leblon','Botafogo','Flamengo','Leme','Gávea','Laranjeiras','Urca'],
  norte: ['Tijuca','Maracanã','Vila Isabel','Méier','Penha','Niterói','Ilha do Gov.','Ramos','Engenho'],
  oeste: ['Barra da Tijuca','Recreio','Jacarepaguá','Campo Grande','Bangu','Santa Cruz'],
  centro:['Lapa','Cinelândia','Glória','Santa Teresa','Catete','Cosme Velho','Gamboa'],
  // cobertura inicial da aba Bairros: só Zona Sul + Barra/Recreio (ver Contexto do projeto)
  barra: ['Barra da Tijuca','Recreio','Vargem Grande','Vargem Pequena','Anil','Joá','Itanhangá','Mangaratiba'],
};
const INF_ZNOMES = {sul:'Zona Sul',norte:'Zona Norte',oeste:'Zona Oeste',centro:'Centro'};
const INF_VENUES = {
  bares:{titulo:'Bares e Restaurantes',tipo:'bar',itens:[{n:'Bar do Mineiro',sub:'Santa Teresa'},{n:'Casa da Cachaça',sub:'Lapa'},{n:'Academia da Cachaça',sub:'Leblon'},{n:'Bar Urca',sub:'Urca'},{n:'Devassa',sub:'Vários'},{n:'Confeitaria Colombo',sub:'Centro'}]},
  hoteis:{titulo:'Hotéis',tipo:'hotel',itens:[{n:'Copacabana Palace',sub:'Copacabana'},{n:'Fairmont Rio',sub:'Copacabana'},{n:'Intercontinental',sub:'São Conrado'},{n:'Windsor Barra',sub:'Barra'},{n:'Sheraton Grand',sub:'Leblon'},{n:'Ibis Copacabana',sub:'Copacabana'},{n:'Arena Hotel',sub:'Barra'}]},
  eventos:{titulo:'Eventos e Shows',tipo:'ev',itens:[{n:'Vivo Rio',sub:'Glória'},{n:'Fundição Progresso',sub:'Lapa'},{n:'Circo Voador',sub:'Lapa'},{n:'Teatro Municipal',sub:'Centro'},{n:'MAM',sub:'Flamengo'},{n:'Maracanã',sub:'Zona Norte'},{n:'Arena Jeunesse',sub:'Barra'}]},
  turistico:{titulo:'Pontos Turísticos',tipo:'tur',itens:[{n:'Museu do Amanhã',sub:'Porto Maravilha'},{n:'Pão de Açúcar',sub:'Urca'},{n:'Arcos da Lapa',sub:'Lapa'},{n:'Cristo Redentor',sub:'Corcovado'},{n:'Jardim Botânico',sub:'Lagoa'},{n:'MAM',sub:'Flamengo'},{n:'Quinta da Boa Vista',sub:'São Cristóvão'}]},
  shoppings:{titulo:'Shoppings',tipo:'shop',itens:[{n:'BarraShopping',sub:'Barra da Tijuca'},{n:'VillageMall',sub:'Barra da Tijuca'},{n:'NorteShopping',sub:'Zona Norte'},{n:'Rio Design Leblon',sub:'Leblon'}]},
};
const INF_SDBDB = [
  // Aeroportos e terminais
  {n:'Galeão (GIG)',t:'aero'},{n:'Santos Dumont (SDU)',t:'aero'},{n:'Rodoviária Novo Rio',t:'term'},
  // Bairros
  {n:'Copacabana',t:'bairro'},{n:'Ipanema',t:'bairro'},{n:'Leblon',t:'bairro'},
  {n:'Lapa',t:'bairro'},{n:'Botafogo',t:'bairro'},{n:'Barra da Tijuca',t:'bairro'},
  {n:'Tijuca',t:'bairro'},{n:'Maracanã',t:'bairro'},{n:'Niterói',t:'bairro'},
  {n:'Flamengo',t:'bairro'},{n:'Glória',t:'bairro'},{n:'Recreio',t:'bairro'},
  {n:'Vila Isabel',t:'bairro'},{n:'Méier',t:'bairro'},{n:'Penha',t:'bairro'},
  // Eventos
  {n:'Vivo Rio',t:'ev'},{n:'Maracanã',t:'ev'},{n:'Fundição Progresso',t:'ev'},
  {n:'Circo Voador',t:'ev'},{n:'Teatro Municipal',t:'ev'},{n:'MAM',t:'ev'},
  {n:'Arena Jeunesse',t:'ev'},
  // Turístico
  {n:'Cristo Redentor',t:'tur'},{n:'Pão de Açúcar',t:'tur'},{n:'Arcos da Lapa',t:'tur'},
  {n:'Museu do Amanhã',t:'tur'},{n:'Jardim Botânico',t:'tur'},{n:'Quinta da Boa Vista',t:'tur'},
  // Bares e Restaurantes
  {n:'Bar do Mineiro',t:'bar'},{n:'Casa da Cachaça',t:'bar'},{n:'Academia da Cachaça',t:'bar'},
  {n:'Bar Urca',t:'bar'},{n:'Devassa',t:'bar'},{n:'Confeitaria Colombo',t:'bar'},
  // Hotéis
  {n:'Copacabana Palace',t:'hotel'},{n:'Fairmont Rio',t:'hotel'},{n:'Intercontinental',t:'hotel'},
  {n:'Windsor Barra',t:'hotel'},{n:'Sheraton Grand',t:'hotel'},{n:'Ibis Copacabana',t:'hotel'},
];
let _infLoc=null,_infTipoKey=null,_infMinutos=60,_infRepTipo=null,_infRepValor=null,_infRepDetalhe=null,_infPanelVenue=null;
// Cidade do Informes (16/08/2026, pedido do Yuri) — antes fixa "Rio de
// Janeiro" no HTML, agora clicável e persistida.
let _infCity = JSON.parse(localStorage.getItem('sm_inf_city') || '{"name":"Localizando…","uf":""}');
let _infGps=null;
let _infFavs=JSON.parse(localStorage.getItem('sm_inf_favs')||'[]');
const INF_AVC=['#6D28D9','#0369A1','#B45309','#0F766E','#15803D','#BE123C','#7C3AED','#0284C7'];
const INF_BADGE={dinamico:'infbd',transito:'infbt',fiscalizacao:'infbf',fila:'infbfi',demanda:'infbde',lotacao:'infblo',acidente:'infbac'};

function renderInformes(){
  const cityEl=document.getElementById('inf-city-name');
  if(cityEl)cityEl.textContent=(_infCity?.name&&_infCity.name!=='Rio de J
===== #F59E0B @ 1711279 =====
Fiscalização', color:'#2563EB', icon:FEED_TYPES.blitz.icon,    expMin:FEED_TYPES.blitz.expMin}
  };
  _infRelState.types = types;
  document.getElementById('inf-relatar-title').textContent = 'Informe';
  document.getElementById('inf-relatar-back').style.display = 'none';
  document.getElementById('inf-relatar-body').innerHTML = `<div class="qr-grid">` +
    Object.entries(types).map(([key,t]) =>
      `<div class="qr-btn" style="background:${t.color}" onclick="infRelatarSelType('${key}')">
        <svg viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2">${t.icon}</svg>
        <span class="qr-label">${t.label}</span>
      </div>`
    ).join('') + `</div>`;
  const locTxt = document.getElementById('inf-relatar-loc-txt');
  locTxt.textContent = 'Localizando…';
  openMo('mo-inf-relatar');
  infRelatarBuscarLocalizacao();
}
// Pede o GPS e preenche o chip de endereço — separado do resto pra poder
// ser chamado de novo depois que a pessoa toca "Alterar" e volta.
function infRelatarBuscarLocalizacao(){
  const locTxt = document.getElementById('inf-relatar-loc-txt');
  if (!navigator.geolocation) { if (locTxt) locTxt.textContent = 'GPS indisponível'; return; }
  navigator.geolocation.getCurrentPosition(async pos=>{
    _infRelState.lat = pos.coords.latitude; _infRelState.lng = pos.coords.longitude;
    try{
      const revR = await fetch(`https://nominatim.openstreetmap.org/reverse?lat=${pos.coords.latitude}&lon=${pos.coords.longitude}&zoom=16&format=json&accept-language=pt`,{headers:{'User-Agent':'MoB/1.0'}});
      const rev = await revR.json();
      _infRelState.address = rev.address?.road || rev.address?.suburb || rev.address?.neighbourhood || null;
    }catch(e){ _infRelState.address = null; }
    const el = document.getElementById('inf-relatar-loc-txt');
    if (el) el.textContent = _infRelState.address || 'Localização atual';
  }, ()=>{ const el=document.getElementById('inf-relatar-loc-txt'); if(el) el.textContent='Localização indisponível'; },
  {timeout:7000, maximumAge:60000});
}
// "Alterar" (16/08/2026, pedido do Yuri) — reaproveita a MESMA busca de
// rua próxima que "Minha área" já usa (infAbrirGps), só que em vez de
// abrir o painel de navegação, seta o endereço da corrida atual e volta
// pro Informe. Motorista pode não estar exatamente onde o GPS achou, ou
// o GPS errou — dá pra corrigir manualmente antes de publicar.
function infRelatarAlterarLocal(){
  closeMo('mo-inf-relatar');
  infAbrirGps('relatar');
}
function infRelatarPickLocal(nome){
  _infRelState.address = nome;
  infFecharOv('inf-ov-gps');
  openMo('mo-inf-relatar');
  const el = document.getElementById('inf-relatar-loc-txt');
  if (el) el.textContent = nome;
}
function infRelatarShowBack(label, fn){
  _infRelState.backFn = fn;
  const back = document.getElementById('inf-relatar-back');
  back.style.display = 'flex';
  document.getElementById('inf-relatar-back-lbl').textContent = label;
}
function infRelatarBack(){ if (_infRelState.backFn) _infRelState.backFn(); }

function infRelatarSelType(key){
  _infRelState.type = key;
  if (key === 'transito') return infRelatarShowTransito();
  if (key === 'blitz') return infRelatarShowPoliciamento();
  if (key === 'risco') return infRelatarShowAlertaStep1();
}

// Trânsito — barra 0 a 5, 1 toque publica.
function infRelatarShowTransito(){
  document.getElementById('inf-relatar-title').textContent = 'Como tá o trânsito?';
  infRelatarShowBack('Trânsito', infOpenRelatar);
  const cores = ['#DC2626','#EF4444','#F59E0B','#EAB308','#84CC16','#22C55E'];
  document.getElementById('inf-relatar-body').innerHTML =
    `<div class="inf-tbar">${cores.map((c,i)=>`<button class="inf-tbar-seg" style="background:${c}" onclick="infRelatarPublish({nivel:${i}})">${i}</button>`).join('')}</div>
     <div class="inf-tbar-labels"><span>Parado</span><span>Livre</span></div>`;
}

// Policiamento — "quem" (2 col) + "fazendo o quê" (3 col) na mesma tela;
// publica sozinho assim que os dois têm uma opção marcada.
function infRelatarShowPoliciamento(){
  document.getElementById('inf-relatar-title').textContent = 'Fiscalização';
  infRelatarShowBack('Fiscalização', infOpenRelatar);
  _infRelState.orgao = null; _infRelState.acao = null;
  infRelatarRenderPoliciamento();
}
function infRelatarRenderPoliciamento(){
  const orgaos = [['Polícia Militar','pm'],['Guarda Municipal','gm'],['Lei Seca','leiseca'],['Detro','detro']];
  const acoes = [['Multando','multando'],['Abordando','abordando'],['Posicionado','posicionado']];
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-combo-row">
      <div class="inf-combo-lbl">Quem</div>
      <div class="inf-tile-grid2">${orgaos.map(([lbl,v])=>`<div class="inf-tile ${_infRelState.orgao===v?'picked':''}" onclick="infRelatarPickOrgao('${v}',${JSON.stringify(lbl)})">${lbl}</div>`).join('')}</div>
    </div>
    <div class="inf-combo-row" style="margin-bottom:4px">
      <div class="inf-combo-lbl">Fazendo o quê</div>
      <div class="inf-tile-grid3">${acoes.map(([lbl,v])=>`<div class="inf-tile ${_infRelState.acao===v?'picked':''}" onclick="infRelatarPickAcao('${v}',${JSON.stringify(lbl)})">${lbl}</div>`).join('')}</div>
    </div>`;
}
function infRelatarPickOrgao(v,lbl){ _infRelState.orgao=v; _infRelState.orgaoLbl=lbl; infRelatarRenderPoliciamento(); infRelatarMaybePublishPoliciamento(); }
function infRelatarPickAcao(v,lbl){ _infRelState.acao=v; _infRelState.acaoLbl=lbl; infRelatarRenderPoliciamento(); infRelatarMaybePublishPoliciamento(); }
function infRelatarMaybePublishPoliciamento(){
  if (_infRelState.orgao && _infRelState.acao) {
    // Pequena pausa só pra pessoa ver a segunda opção marcada antes de
    // sumir a tela — publica sozinho, sem precisar de botão confirmar.
    setTimeout(()=>infRelatarPublish({param_value:_infRelState.orgaoLbl, param_detail:_infRelState.acaoLbl}), 350);
  }
}

// Alerta — 2 etapas de verdade, a segunda pergunta depende da primeira
// resposta. Cores fortes (não pastel) — pedido do Yuri: Acidente=laranja,
// Área de risco=vermelho (invertido do que eu tinha proposto antes).
function infRelatarShowAlertaStep1(){
  document.getElementById('inf-relatar-title').textContent = 'Qual alerta?';
  infRelatarShowBack('Alerta', infOpenRelatar);
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-tile-grid2">
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarShowAcidenteStep2()">Acidente</div>
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarShowAreaRiscoStep2()">Área de risco</div>
    </div>`;
}
function infRelatarShowAcidenteStep2(){
  document.getElementById('inf-relatar-title').textContent = 'Onde exatamente?';
  infRelatarShowBack('Acidente', infRelatarShowAlertaStep1);
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-tile-grid2">
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarPublish({param_value:'Acidente', param_detail:'Na via'})">Na via</div>
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarPublish({param_value:'Acidente', param_detail:'No acostamento'})">No acostamento</div>
    </div>`;
}
function infRelatarShowAreaRiscoStep2(){
  document.getElementById('inf-relatar-title').textContent = 'Área de Risco?';
  infRelatarShowBack('Área de risco', infRelatarShowAlertaStep1);
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-tile-grid2">
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Via de acesso'})">Via de acesso</div>
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Área de abordagem'})">Área de abordagem</div>
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Proibido a entrada'})">Proibido a entrada</div>
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Barricada'})">Barricada</div>
    </div>`;
}

// Publica de verdade em feed_posts — usa a localização já detectada
// quando o modal abriu (mais rápido que pedir de novo agora).
function infRelatarPublish(extra){
  document.getElementById('inf-relatar-title').textContent = 'Publicando…';
  document.getElementById('inf-relatar-back').style.display = 'none';
  document.getElementById('inf-relatar-body').innerHTML = `
    <div style="display:flex;flex-direction:column;align-items:center;padding:24px 10px 8px;text-align:center">
      <div style="width:40px;height:40px;border-radius:50%;border:3.5px solid #DBEAFE;border-top-color:#2563EB;animation:spin .8s linear infinite;margin-bottom:14px"></div>
      <div style="font-size:12.5px;color:var(--muted)">Confirmando localização</div>
    </div>`;
  infRelatarDoPublish(extra);
}
async function infRelatarDoPublish(extra){
  if (!_supaUser) { closeMo('mo-inf-relatar'); showToast('Faça login pra relatar.', 'error'); return; }
  const t = (_infRelState.types||{})[_infRelState.type];
  const expMin = t?.expMin || 30;
  const row = {
    user_id:_supaUser.id, type:_infRelState.type, audience:'comunidade', source:'user',
    lat:_infRelState.lat ?? null, lng:_infRelState.lng ?? null,
    address:_infRelState.address || null, city:'rio',
    param_value:extra.param_value ?? null, param_detail:extra.param_detail ?? null,
    nivel: extra.nivel ?? null,
    expires_at:new Date(Date.now()+expMin*60000).toISOString(),
    votes_up:1, votes_dn:0, comments_count:0, status:'ativo'
  };
  const { error } = await _SUPA.from('feed_posts').insert(row);
  setTimeout(()=>{
    closeMo('mo-inf-relatar');
    if (error) { console.error('infRelatarDoPublish', error); showToast('Erro ao publicar. Tenta de novo.', 'error'); return; }
    showToast('Publicado!', 'success');
    if (_infLoc) infCarregarInformes(_infLoc, _infTipoKey);
  }, 500);
}
function infSetTime(btn,min){
  docum
===== #F59E0B @ 1722609 =====
 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"/></svg>`,tur:`<svg ${a}><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>`,bar:`<svg ${a}><path d="M17 11H3v-1a5 5 0 0 1 5-5h4a5 5 0 0 1 5 5v1z"/><path d="M3 11v5a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2v-5"/></svg>`,hotel:`<svg ${a}><rect x="2" y="7" width="20" height="15" rx="2"/><path d="M2 12h20"/><path d="M8 7V5a4 4 0 0 1 8 0v2"/></svg>`,shop:`<svg ${a}><path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4Z"/><path d="M3 6h18"/><path d="M16 10a4 4 0 0 1-8 0"/></svg>`};
  return m[tipo]||m.bairro;
}
// Gesto swipe down para fechar o painel
(function() {
  const panel = document.getElementById('inf-panel');
  const drag  = document.getElementById('inf-panel-drag');
  if (!panel || !drag) return;
  let startY = 0, isDragging = false;
  function onStart(e) {
    startY = (e.touches ? e.touches[0].clientY : e.clientY);
    isDragging = true;
    panel.style.transition = 'none';
  }
  function onMove(e) {
    if (!isDragging) return;
    const dy = (e.touches ? e.touches[0].clientY : e.clientY) - startY;
    if (dy > 0) panel.style.transform = 'translateY(' + dy + 'px)';
  }
  function onEnd(e) {
    if (!isDragging) return;
    isDragging = false;
    panel.style.transition = '';
    const dy = (e.changedTouches ? e.changedTouches[0].clientY : e.clientY) - startY;
    if (dy > 100) { panel.style.transform = ''; infFecharPainel(); }
    else panel.style.transform = '';
  }
  drag.addEventListener('touchstart', onStart, {passive:true});
  drag.addEventListener('touchmove',  onMove,  {passive:true});
  drag.addEventListener('touchend',   onEnd);
  drag.addEventListener('mousedown',  onStart);
  document.addEventListener('mousemove', onMove);
  document.addEventListener('mouseup',   onEnd);
})();

['inf-ov-gps','inf-ov-venue','inf-ov-rep'].forEach(id=>{
  const el=document.getElementById(id);if(el)el.addEventListener('click',function(e){if(e.target===this)infFecharOv(id);});
});
document.addEventListener('click',e=>{
  if(!e.target.closest('#inf-search-bar')&&!e.target.closest('#inf-search-btn')&&!e.target.closest('#inf-search-dd')){
    const dd=document.getElementById('inf-search-dd');if(dd)dd.classList.remove('show');
  }
});

/* ══════════════════════════════════════════════════════════════
   FEED SOCIAL (Home) — v1
   Tabelas: feed_posts / feed_reactions / feed_comments (Supabase)
   Contadores de voto/comentário e auto-encerramento por downvote
   são calculados via trigger no banco — o client só insere/upserta
   e depois relê o post pra pegar os números atualizados.
   ══════════════════════════════════════════════════════════════ */

const FEED_TYPES = {
  blitz:    {label:'Blitz',        color:'#2563EB', expMin:60,
    icon:'<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>'},
  risco:    {label:'Risco',        color:'#EF4444', expMin:90,
    icon:'<path d="M10.3 3.9L1.8 18a2 2 0 0 0 1.7 3h17a2 2 0 0 0 1.7-3L13.7 3.9a2 2 0 0 0-3.4 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/>'},
  transito: {label:'Trânsito',     color:'#F97316', expMin:45,
    icon:'<rect x="3" y="4" width="18" height="16" rx="2"/><line x1="3" y1="10" x2="21" y2="10"/><line x1="3" y1="15" x2="21" y2="15"/>'},
  demanda:  {label:'Demanda alta', color:'#16A34A', expMin:30,
    icon:'<polyline points="23 6 13.5 15.5 8.5 10.5 1 18"/><polyline points="17 6 23 6 23 12"/>'},
  dinamico: {label:'Dinâmico',     color:'#F59E0B', expMin:30,
    icon:'<polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/>'},
  fila:     {label:'Fila',         color:'#7C3AED', expMin:45,
    icon:'<path d="M17.8 19.2L16 11l3.5-3.5C21 6 21.5 4 21 3c-1-.5-3 0-4.5 1.5L13 8 4.8 6.2c-.5-.1-.9.1-1.1.5l-.3.5c-.2.5-.1 1 .3 1.3L9 12l-2 3H4l-1 1 3 2 2 3 1-1v-3l3-2 3.5 5.3c.3.4.8.5 1.3.3l.5-.2c.4-.3.6-.7.5-1.2z"/>'}
};

let _feedFilter = '';
let _feedCoords = null;       // {lat,lng,address}
let _feedPosts  = [];
let _feedNames  = {};         // user_id -> nome
let _feedChannel = null;
let _feedCpType  = 'blitz';
let _feedCpLocOn = true;
let _feedMicActive = false;
let _feedCommentsPostId = null;

function feedHaversineKm(lat1,lng1,lat2,lng2){
  const R=6371, toRad=d=>d*Math.PI/180;
  const dLat=toRad(lat2-lat1), dLng=toRad(lng2-lng1);
  const a=Math.sin(dLat/2)**2+Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLng/2)**2;
  return R*2*Math.atan2(Math.sqrt(a),Math.sqrt(1-a));
}
function feedDistLabel(p){
  if(!_feedCoords||p.lat==null||p.lng==null) return '';
  const km=feedHaversineKm(_feedCoords.lat,_feedCoords.lng,p.lat,p.lng);
  return km<1 ? Math.round(km*1000)+' m' : km.toFixed(1).replace('.',',')+' km';
}
function feedAgeLabel(iso){
  const min=Math.max(0,Math.round((Date.now()-new Date(iso))/60000));
  if(min<1) return 'agora';
  if(min<60) return min+' min';
  return Math.floor(min/60)+'h'+(min%60?p2(min%60)+'m':'');
}
function feedInitials(name){
  const parts=(name||'').trim().split(/\s+/).filter(Boolean);
  if(!parts.length) return 'MO';
  return parts.length===1 ? parts[0].slice(0,2).toUpperCase() : (parts[0][0]+parts[parts.length-1][0]).toUpperCase();
}
function feedAvColor(uid){
  return INF_AVC[Math.abs((uid||'').split('').reduce((a,c)=>a+c.charCodeAt(0),0))%INF_AVC.length];
}

async function renderFeedHome(){
  feedComputeKpis();
  feedGetLocation();
  await feedLoadPosts();
  feedSubscribeRealtime();
}

function feedComputeKpis(){
  const dstr=DB.today();
  const day=DB.day(dstr);
  const tripsRev=(day.trips||[]).reduce((s,t)=>s+(t.value||0),0);
  const autoInfo=(typeof finAutoDay==='function')?finAutoDay(dstr):{sum:0,count:0};
  const manRev=(day.revenues||[]).reduce((s,r)=>s+(r.amount||0),0);
  const rev=tripsRev+autoInfo.sum+manRev;
  const trips=(day.trips||[]).length+autoInfo.count;
  const km=(day.sessions||[]).reduce((s,ss)=>s+(ss.km||0),0);
  const onlineSecs=(day.sessions||[]).reduce((s,ss)=>s+(ss.elapsed_secs||0),0);
  setText('feed-k-rev', DB.fRI(rev));
  setText('feed-k-trips', trips);
  setText('feed-k-rpkm', km>0 ? DB.fRI(rev/km) : '—');
  setText('feed-k-online', onlineSecs>0 ? Math.floor(onlineSecs/3600)+'h'+p2(Math.floor(onlineSecs%3600/60))+'m' : '—');
}

function feedGetLocation(){
  if(!navigator.geolocation) return;
  navigator.geolocation.getCurrentPosition(async pos=>{
    const {latitude:lat,longitude:lon}=pos.coords;
    _feedCoords={lat,lng:lon,address:null};
    try{
      const r=await fetch(`https://nominatim.openstreetmap.org/reverse?lat=${lat}&lon=${lon}&zoom=17&format=json&accept-language=pt`,{headers:{'User-Agent':'MoB/1.0'}});
      const j=await r.json();
      const addr=j.address||{};
      _feedCoords.address=addr.road||addr.pedestrian||addr.suburb||addr.neighbourhood||'';
    }catch(e){console.warn('feed loc',e);}
    feedUpdateComposerLoc();
    feedRenderList(); // recalcula distâncias com a posição atual
  },()=>{_feedCoords=null;},{timeout:10000,enableHighAccuracy:true});
}

async function feedLoadPosts(){
  const el=document.getElementById('feed-list');
  if(!_SUPA){ if(el) el.innerHTML='<div class="feed-empty">Sem conexão</div>'; return; }
  try{
    let q=_SUPA.from('feed_posts').select('*')
      .eq('status','ativo').eq('audience','comunidade')
      .gt('expires_at', new Date().toISOString())
      .order('created_at',{ascending:false}).limit(40);
    if(_feedFilter) q=q.eq('type',_feedFilter);
    const {data,error}=await q;
    if(error) throw error;
    _feedPosts=data||[];
    const uids=[...new Set(_feedPosts.map(p=>p.user_id))];
    if(uids.length){
      const {data:names}=await _SUPA.rpc('get_profile_names',{uids});
      _feedNames={}; (names||[]).forEach(n=>{_feedNames[n.id]={name:n.name, avatar:n.avatar_url};});
    }
    feedRenderList();
  }catch(e){console.warn('feed load',e); if(el) el.innerHTML='<div class="feed-empty">Erro ao carregar o feed</div>';}
}

// Avatar compartilhado entre feed, comentários e composer: foto real
// quando existe, senão iniciais coloridas (mesma cor sempre pro mesmo uid).
function feedAvatarHtml(uid, name, avatarUrl, extraClass){
  const cls = 'feed-av' + (extraClass?(' '+extraClass):'');
  if (avatarUrl) return `<img class="${cls}" src="${esc(avatarUrl)}" alt="">`;
  return `<div class="${cls}" style="background:${feedAvColor(uid)}">${esc(feedInitials(name))}</div>`;
}

function feedRenderList(){
  const el=document.getElementById('feed-list');if(!el)return;
  if(!_feedPosts.length){el.innerHTML='<div class="feed-empty">Nenhum alerta ativo agora — seja o primeiro a avisar a comunidade.</div>';return;}
  el.innerHTML=_feedPosts.map(feedPostCardHtml).join('');
}

function feedPostCardHtml(p){
  const t=FEED_TYPES[p.type]||FEED_TYPES.blitz;
  const isOwn = _supaUser && p.user_id===_supaUser.id;
  const cfg = DB.cfg();
  const myName = isOwn ? (cfg.name||'Você') : ((_feedNames[p.user_id]&&_feedNames[p.user_id].name)||'Motorista MōB');
  const myAvatar = isOwn ? (cfg.avatarUrl||null) : ((_feedNames[p.user_id]&&_feedNames[p.user_id].avatar)||null);
  const dist=feedDistLabel(p);
  const closed=p.status==='encerrado';
  const myVote=p._myVote; // preenchido depois de votar, só nesta sessão
  return `<div class="feed-post${closed?' closed':''}" onclick="feedAbrirComentarios('${p.id}')">
    ${feedAvatarHtml(p.user_id, myName, myAvatar)}
    <div class="feed-body">
      <div class="feed-head">
        <span class="feed-name">${esc(myName)}</span>
        <span class="feed-time">· ${feedAgeLabel(p.created_at)}</span>
      </div>
      <span class="feed-type" style="color:${t.color};background:color-mix(in srgb,${t.color} 10%,transparent);border:1px solid color-mix(in srgb,${t.color} 25%,transparent)"><svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">${t.icon}</svg>${t.label}</span>
      ${p.body ? `<div class="feed-text">${esc(p.body)}</div>` : ''}
      ${p.address ? `<div class="feed-loc"><svg width="11" height="11" viewBox="0 0 24 24
===== #F97316 @ 538922 =====
(localStorage).filter(k => k.startsWith('sm_day_')).map(k => k.slice(7)).sort(),
  events: () => DB.get('sm_events') || [],
  saveEvents: e => DB.set('sm_events', e),
  planJornada: () => DB.get('sm_plan_jornada') || [],
  savePlanJornada: p => DB.set('sm_plan_jornada', p),
  cfg: () => DB.get('sm_cfg') || {name:'', plate:'', vehicle:'', goal:0, favModule:'financeiro'},
  saveCfg: c => DB.set('sm_cfg', c),
  cats: t => DB.get('sm_cats_'+t) || DEFAULT_CATS[t],
  saveCats: (t,c) => DB.set('sm_cats_'+t, c),
  objGoals: t => DB.get('sm_obj_'+t) || {},
  saveObjGoals: (t,g) => DB.set('sm_obj_'+t, g),
  syncQueue: () => DB.get('sm_sync_queue') || [],
  saveSyncQueue: q => DB.set('sm_sync_queue', q),
  meta: () => DB.get('sm_meta') || {type:'monthly', general:9000, varyByDay:false, days:{}},
  saveMeta: m => DB.set('sm_meta', m),
  uid: () => Date.now().toString(36) + Math.random().toString(36).slice(2,5),
  today: () => { const d=new Date(); return d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate()); },
  fR: v => 'R$ ' + Number(v||0).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2}),
  fRI: v => 'R$ ' + Number(v||0).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2}),
};
function p2(n) { return String(n).padStart(2,'0'); }
// Formata duração em minutos: "5m", "32m", "1h05m" (sem zero à esquerda quando é só minutos).
function fmtDur(totalMin) {
  const min = Math.max(0, Math.round(totalMin || 0));
  const h = Math.floor(min / 60), m = min % 60;
  return (h > 0 ? h + 'h' + p2(m) : String(m)) + 'm';
}

// Detalhamento Online/Buscando passageiro/Em corrida — puxado direto da
// máquina de estados do APK (accepted_at/pickup_started_at/trip_started_at/
// trip_ended_at, gravados em auto_trips a cada transição real de tela).
// BUSCANDO = trip_started_at - pickup_started_at (deslocamento até o
// passageiro); EM CORRIDA = trip_ended_at - trip_started_at; ONLINE = todo
// o resto do tempo da sessão (esperando oferta / avaliando oferta). Só usa
// corridas automáticas (autoTrips) — corridas importadas manualmente não
// têm esses timestamps porque não vieram da leitura de tela em tempo real.
function computeStatusBreakdown(elapsedSec, kmTotal, autoTrips) {
  let corridaSec = 0, buscandoSec = 0, corridaKm = 0, buscandoKm = 0;
  (autoTrips || []).forEach(t => {
    if (t.trip_started_at && t.trip_ended_at) {
      const s = (new Date(t.trip_ended_at) - new Date(t.trip_started_at)) / 1000;
      if (s > 0) corridaSec += s;
      corridaKm += parseFloat(t.real_km_trip ?? t.offer_km_trip) || 0;
    }
    if (t.pickup_started_at && t.trip_started_at) {
      const s = (new Date(t.trip_started_at) - new Date(t.pickup_started_at)) / 1000;
      if (s > 0) buscandoSec += s;
      buscandoKm += parseFloat(t.real_km_pickup ?? t.offer_km_pickup) || 0;
    }
  });
  const total = Math.max(0, elapsedSec || 0);
  const onlineSec = Math.max(0, total - corridaSec - buscandoSec);
  const onlineKm = Math.max(0, (kmTotal || 0) - corridaKm - buscandoKm);
  const pct = s => total > 0 ? Math.round(s / total * 100) : 0;
  return {
    total,
    online:   { sec: onlineSec,   km: onlineKm,   pct: pct(onlineSec) },
    buscando: { sec: buscandoSec, km: buscandoKm, pct: pct(buscandoSec) },
    corrida:  { sec: corridaSec,  km: corridaKm,  pct: pct(corridaSec) },
  };
}

const STATUS_SEGMENTS = [
  { key: 'online',   label: 'ONLINE',              color: '#22C55E' },
  { key: 'buscando', label: 'BUSCANDO PASSAGEIRO',  color: '#F97316' },
  { key: 'corrida',  label: 'EM CORRIDA',           color: '#3B82F6' },
];

function buildSbarHtml(bd) {
  return STATUS_SEGMENTS.map(s => {
    const pct = bd[s.key].pct;
    if (pct <= 0) return '';
    const showText = pct >= 12;
    return `<i style="width:${pct}%;background:${s.color}">${showText ? pct+'%' : ''}</i>`;
  }).join('');
}

function buildSrowsHtml(bd) {
  return STATUS_SEGMENTS.map(s => {
    const seg = bd[s.key];
    return `<div class="srow"><span class="sdot" style="background:${s.color}"></span><b>${s.label}</b><span class="pct">${seg.pct}%</span><span class="stempo">${fmtDur(seg.sec/60)}</span><span class="skm">${seg.km.toFixed(1)} km</span></div>`;
  }).join('');
}

function toggleBar(listId) {
  const el = document.getElementById(listId);
  if (el) el.classList.toggle('open');
}

// ── DEFAULT CATEGORIES ────────────────────────────────────────
const DEFAULT_CATS = {
  despesa_operacional: [
    {id:'comb', l:'Combustível', icon:'fuel'},
    {id:'alim', l:'Alimentação', icon:'food'},
    {id:'man',  l:'Manutenção', icon:'wrench'},
    {id:'park', l:'Estacionamento', icon:'parking'},
    {id:'ped',  l:'Pedágio', icon:'road'},
    {id:'lav',  l:'Lavagem', icon:'car'},
    {id:'seg',  l:'Seguro', icon:'card'},
    {id:'out',  l:'Outros', icon:'plus'}
  ],
  despesa_pessoal: [
    {id:'alug', l:'Aluguel', icon:'home'},
    {id:'merc', l:'Mercado', icon:'card'},
    {id:'alim', l:'Alimentação', icon:'food'},
    {id:'cart', l:'Cartão', icon:'card'},
    {id:'trans', l:'Transporte', icon:'car'},
    {id:'saude', l:'Saúde', icon:'plus'},
    {id:'lazer', l:'Lazer', icon:'gift'},
    {id:'out',  l:'Outros', icon:'plus'}
  ],
  receita_operacional: [
    {id:'gort', l:'Gorjeta', icon:'coin'},
    {id:'bon',  l:'Bônus', icon:'gift'},
    {id:'reemb', l:'Reembolso', icon:'card'},
    {id:'serv', l:'Serviço extra', icon:'briefcase'},
    {id:'part', l:'Particular', icon:'user'},
    {id:'inc',  l:'Incentivo', icon:'plus'}
  ],
  receita_pessoal: [
    {id:'sal',  l:'Salário', icon:'briefcase'},
    {id:'rend', l:'Renda extra', icon:'coin'},
    {id:'pix',  l:'Pix recebido', icon:'card'},
    {id:'reemb', l:'Reembolso', icon:'card'},
    {id:'pres', l:'Presente', icon:'gift'},
    {id:'out',  l:'Outros', icon:'plus'}
  ]
};

// ── GLOBAL STATE ──────────────────────────────────────────────
let S = {
  sec: 'home',
  homePeriod: 'w',
  hgCustomStart: null, hgCustomEnd: null,
  hgDiaMode: 'd', hgHoraMode: 'h', hgMesMode: 'm', hgRevDesp: 'receita',
  finPer: 'month',
  finScope: 'all',
  lancFilter: 'all',
  lancStatus: 'all',
  lancPeriod: '7d',
  lancDate: null,
  curDate: DB.today(),
  lancType: 'receita',
  lancScope: 'operacional',
  lancRecFreq: 'Semanal',
  corPlat: 'uber',
  corridasPeriod: 'hoje', corridasDate: null,
  corridasPlat: 'all',
  tripsExpanded: false,
  sessionsExpanded: false,
  lancExpanded: false,
  _midnightIsPostStop: false,
  _postStopMidnightSess: null,
  _postMidnightCallback: null,
  _midnightModalOpen: false,
  gps: {
    st: 'idle',  // idle | running
    start: null, pausedMs: 0, pauseAt: null,
    km: 0, pts: [], wid: null, ti: null,
    forceDate: null // override manual (opção 'deixar em ontem' na virada de meia-noite)
  }
};

// ── MODULES (for Mais screen) ─────────────────────────────────
const MODULES = [
  {id:'financeiro', label:'Financeiro', sub:'Gestão e análise financeira', color:'blue',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 3v18M17 7H9.5a3.5 3.5 0 0 0 0 7H14a3.5 3.5 0 0 1 0 7H6"/></svg>'},
  {id:'planejar-jornada', label:'Planejar jornada', sub:'Defina dias, metas e horários de trabalho', color:'green',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18M8 14h.01M12 14h.01M16 14h.01M8 18h.01M12 18h.01"/></svg>'},
  {id:'lancamentos', label:'Lançamentos', sub:'Histórico de receitas e despesas', color:'orange',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 19V5a2 2 0 0 1 2-2h12v18H6a2 2 0 0 1-2-2Z"/><path d="M8 7h8M8 11h8M8 15h5"/></svg>'},
];

const MODULE_COLORS = {blue:'#EFF6FF', purple:'#F5F3FF', green:'#ECFDF3', orange:'#FFF7ED'};
const MODULE_ICONS_COLOR = {blue:'#0A2F6B', purple:'#7C3AED', green:'#22C55E', orange:'#F59E0B'};

// ── NAVIGATION ────────────────────────────────────────────────
function navTo(sec) {
  // Ocultar/mostrar nav na tela de Informes (o painel usa z-index alto mas o cat-row precisa da nav escondida)
  const bottomNav = document.querySelector('.bottom');
  if (bottomNav) bottomNav.style.display = 'flex';
  document.querySelectorAll('.screen').forEach(s => {
    s.classList.remove('on');
    s.style.display = '';  // limpa inline style para CSS funcionar
  });
  document.querySelectorAll('.nav').forEach(n => n.classList.remove('active'));
  const sc = document.getElementById('sc-'+sec);
  if (sc) { sc.classList.add('on'); sc.style.display = 'block'; }
  // Map sub-screens to their nav parent
  const navMap = {financeiro:'fav', lancamentos:'fav', relatorios:'fav', 'planejar-jornada':'mais', corridas:'jornada', agenda:'mais', informes:'agenda', categorias:'mais'};
  const navId = navMap[sec] || sec;
  const bn = document.getElementById('bn-'+navId);
  if (bn) bn.classList.add('active');
  S.sec = sec;
  try { localStorage.setItem('sm_last_screen', JSON.stringify({sec, ts: Date.now()})); } catch(e) {}
  const ms = document.getElementById('main-scroll');
  if (ms) ms.scrollTop = 0;
  if (sec === 'home')       renderFeedHome();
  if (sec === 'relatorios') renderHome();
  if (sec === 'jornada') {
    renderJornada(); // render imediato com dado local (nunca trava esperando rede)
    updateNetStatus();
    if (_supaUser && navigator.onLine) {
      reconcileJornadaHoje().then(() => renderJornada()).catch(()=>{});
      // Se o app local acha que está idle mas existe jornada aberta no
      // Supabase há mais de 15min (provável órfã), fecha automaticamente
      if (S.gps.st === 'idle') {
        _closeOrphanSessions().then(n => { if (n) renderJornada(); }).catch(()=>{});
      }
    }
  }
  if (sec === 'financeiro') renderFin();
  if (sec === 'lancamentos') renderLancamentos();
  if (sec === 'corridas') {
    S.corridasPlats = new Set(); // reseta sempre — nenhum filtro marcado = mostra tudo
    docume
===== #F97316 @ 1707562 =====
}</div></div>
    ${isActive ? '<svg class="inf-city-item-check" viewBox="0 0 24 24" width="17" height="17" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>' : ''}
  </div>`;
}
let _infCitySearchDebounce = null;
function infBuscarCidade(q){
  clearTimeout(_infCitySearchDebounce);
  if (!q || q.trim().length < 2) { infRenderCidadeDefaults(); return; }
  _infCitySearchDebounce = setTimeout(async () => {
    try {
      const r = await fetch(`https://nominatim.openstreetmap.org/search?city=${encodeURIComponent(q)}&country=Brazil&format=json&addressdetails=1&limit=8`, {headers:{'User-Agent':'MoB/1.0'}});
      const data = await r.json();
      const cidades = data
        .filter(d => d.address && (d.address.city || d.address.town || d.address.municipality))
        .map(d => ({name: d.address.city || d.address.town || d.address.municipality, uf: d.address.state_code || (d.address['ISO3166-2-lvl4']||'').split('-')[1] || ''}));
      const vistos = new Set();
      const unicos = cidades.filter(c => { const k = c.name+c.uf; if (vistos.has(k)) return false; vistos.add(k); return true; });
      document.getElementById('inf-city-list').innerHTML = unicos.length
        ? unicos.map(infCidadeItemHtml).join('')
        : '<div class="inf-empty">Nenhuma cidade encontrada</div>';
    } catch(e) { console.warn('busca cidade', e); }
  }, 400);
}
async function infEscolherCidade(name, uf){
  _infCity={name,uf};localStorage.setItem('sm_inf_city',JSON.stringify(_infCity));document.getElementById('inf-city-name').textContent=name;closeMo('mo-inf-cidade');
  try{
    const u=`https://nominatim.openstreetmap.org/search?format=jsonv2&limit=1&countrycodes=br&q=${encodeURIComponent(name+', '+uf+', Brasil')}`;
    const r=await fetch(u,{headers:{Accept:'application/json'}}),d=await r.json();
    if(d?.[0]){const lat=Number(d[0].lat),lon=Number(d[0].lon);_infGps={lat,lon,at:Date.now()};infRenderNearbyVenueLists(lat,lon);infRenderBairrosCidade(lat,lon,_infCity);infRenderAeroportosRaio(lat,lon);}
  }catch(e){}
  try{infCarregarFeedAgora?.();}catch(e){}
}
function infToggleSearch(){
  const el = document.getElementById('inf-search-fixed');
  const btn = document.getElementById('inf-search-toggle');
  if(!el) return;
  const show = el.style.display === 'none';
  el.style.display = show ? 'flex' : 'none';
  if(btn){ btn.style.background = show ? '#0A2F6B' : ''; btn.style.color = show ? '#fff' : ''; }
  if(show){ document.getElementById('inf-search-bar')?.focus(); }
  else {
    const inp = document.getElementById('inf-search-bar'); if(inp) inp.value='';
    const dd = document.getElementById('inf-search-dd'); if(dd) dd.classList.remove('show');
  }
}
// Botão "Relatar agora" (barra fixa embaixo, Opção B aprovada) — abre o
// seletor de tipo. Reaproveita ícone/expMin do FEED_TYPES (blitz/risco/
// transito), só troca label/cor pro contexto do Informes (Policiamento/
// Alerta/Trânsito) — mesma chave de type no banco, então o composer do
// Início (que ainda usa os nomes antigos) continua compatível com o
// mesmo dado. Construído dentro da função (não como const no topo do
// arquivo) porque FEED_TYPES só existe mais abaixo — na hora que essa
// função É CHAMADA (clique do usuário) o resto do script já rodou, mas
// declarar como const aqui em cima quebraria (referência antes de existir).
let _infRelState = {};
function infOpenRelatar(){
  _infRelState = {};
  const types = {
    transito: {label:'Trânsito',     color:'#F97316', icon:FEED_TYPES.transito.icon, expMin:FEED_TYPES.transito.expMin},
    risco:    {label:'Alerta',       color:'#DC2626', icon:FEED_TYPES.risco.icon,    expMin:FEED_TYPES.risco.expMin},
    blitz:    {label:'Fiscalização', color:'#2563EB', icon:FEED_TYPES.blitz.icon,    expMin:FEED_TYPES.blitz.expMin}
  };
  _infRelState.types = types;
  document.getElementById('inf-relatar-title').textContent = 'Informe';
  document.getElementById('inf-relatar-back').style.display = 'none';
  document.getElementById('inf-relatar-body').innerHTML = `<div class="qr-grid">` +
    Object.entries(types).map(([key,t]) =>
      `<div class="qr-btn" style="background:${t.color}" onclick="infRelatarSelType('${key}')">
        <svg viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2">${t.icon}</svg>
        <span class="qr-label">${t.label}</span>
      </div>`
    ).join('') + `</div>`;
  const locTxt = document.getElementById('inf-relatar-loc-txt');
  locTxt.textContent = 'Localizando…';
  openMo('mo-inf-relatar');
  infRelatarBuscarLocalizacao();
}
// Pede o GPS e preenche o chip de endereço — separado do resto pra poder
// ser chamado de novo depois que a pessoa toca "Alterar" e volta.
function infRelatarBuscarLocalizacao(){
  const locTxt = document.getElementById('inf-relatar-loc-txt');
  if (!navigator.geolocation) { if (locTxt) locTxt.textContent = 'GPS indisponível'; return; }
  navigator.geolocation.getCurrentPosition(async pos=>{
    _infRelState.lat = pos.coords.latitude; _infRelState.lng = pos.coords.longitude;
    try{
      const revR = await fetch(`https://nominatim.openstreetmap.org/reverse?lat=${pos.coords.latitude}&lon=${pos.coords.longitude}&zoom=16&format=json&accept-language=pt`,{headers:{'User-Agent':'MoB/1.0'}});
      const rev = await revR.json();
      _infRelState.address = rev.address?.road || rev.address?.suburb || rev.address?.neighbourhood || null;
    }catch(e){ _infRelState.address = null; }
    const el = document.getElementById('inf-relatar-loc-txt');
    if (el) el.textContent = _infRelState.address || 'Localização atual';
  }, ()=>{ const el=document.getElementById('inf-relatar-loc-txt'); if(el) el.textContent='Localização indisponível'; },
  {timeout:7000, maximumAge:60000});
}
// "Alterar" (16/08/2026, pedido do Yuri) — reaproveita a MESMA busca de
// rua próxima que "Minha área" já usa (infAbrirGps), só que em vez de
// abrir o painel de navegação, seta o endereço da corrida atual e volta
// pro Informe. Motorista pode não estar exatamente onde o GPS achou, ou
// o GPS errou — dá pra corrigir manualmente antes de publicar.
function infRelatarAlterarLocal(){
  closeMo('mo-inf-relatar');
  infAbrirGps('relatar');
}
function infRelatarPickLocal(nome){
  _infRelState.address = nome;
  infFecharOv('inf-ov-gps');
  openMo('mo-inf-relatar');
  const el = document.getElementById('inf-relatar-loc-txt');
  if (el) el.textContent = nome;
}
function infRelatarShowBack(label, fn){
  _infRelState.backFn = fn;
  const back = document.getElementById('inf-relatar-back');
  back.style.display = 'flex';
  document.getElementById('inf-relatar-back-lbl').textContent = label;
}
function infRelatarBack(){ if (_infRelState.backFn) _infRelState.backFn(); }

function infRelatarSelType(key){
  _infRelState.type = key;
  if (key === 'transito') return infRelatarShowTransito();
  if (key === 'blitz') return infRelatarShowPoliciamento();
  if (key === 'risco') return infRelatarShowAlertaStep1();
}

// Trânsito — barra 0 a 5, 1 toque publica.
function infRelatarShowTransito(){
  document.getElementById('inf-relatar-title').textContent = 'Como tá o trânsito?';
  infRelatarShowBack('Trânsito', infOpenRelatar);
  const cores = ['#DC2626','#EF4444','#F59E0B','#EAB308','#84CC16','#22C55E'];
  document.getElementById('inf-relatar-body').innerHTML =
    `<div class="inf-tbar">${cores.map((c,i)=>`<button class="inf-tbar-seg" style="background:${c}" onclick="infRelatarPublish({nivel:${i}})">${i}</button>`).join('')}</div>
     <div class="inf-tbar-labels"><span>Parado</span><span>Livre</span></div>`;
}

// Policiamento — "quem" (2 col) + "fazendo o quê" (3 col) na mesma tela;
// publica sozinho assim que os dois têm uma opção marcada.
function infRelatarShowPoliciamento(){
  document.getElementById('inf-relatar-title').textContent = 'Fiscalização';
  infRelatarShowBack('Fiscalização', infOpenRelatar);
  _infRelState.orgao = null; _infRelState.acao = null;
  infRelatarRenderPoliciamento();
}
function infRelatarRenderPoliciamento(){
  const orgaos = [['Polícia Militar','pm'],['Guarda Municipal','gm'],['Lei Seca','leiseca'],['Detro','detro']];
  const acoes = [['Multando','multando'],['Abordando','abordando'],['Posicionado','posicionado']];
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-combo-row">
      <div class="inf-combo-lbl">Quem</div>
      <div class="inf-tile-grid2">${orgaos.map(([lbl,v])=>`<div class="inf-tile ${_infRelState.orgao===v?'picked':''}" onclick="infRelatarPickOrgao('${v}',${JSON.stringify(lbl)})">${lbl}</div>`).join('')}</div>
    </div>
    <div class="inf-combo-row" style="margin-bottom:4px">
      <div class="inf-combo-lbl">Fazendo o quê</div>
      <div class="inf-tile-grid3">${acoes.map(([lbl,v])=>`<div class="inf-tile ${_infRelState.acao===v?'picked':''}" onclick="infRelatarPickAcao('${v}',${JSON.stringify(lbl)})">${lbl}</div>`).join('')}</div>
    </div>`;
}
function infRelatarPickOrgao(v,lbl){ _infRelState.orgao=v; _infRelState.orgaoLbl=lbl; infRelatarRenderPoliciamento(); infRelatarMaybePublishPoliciamento(); }
function infRelatarPickAcao(v,lbl){ _infRelState.acao=v; _infRelState.acaoLbl=lbl; infRelatarRenderPoliciamento(); infRelatarMaybePublishPoliciamento(); }
function infRelatarMaybePublishPoliciamento(){
  if (_infRelState.orgao && _infRelState.acao) {
    // Pequena pausa só pra pessoa ver a segunda opção marcada antes de
    // sumir a tela — publica sozinho, sem precisar de botão confirmar.
    setTimeout(()=>infRelatarPublish({param_value:_infRelState.orgaoLbl, param_detail:_infRelState.acaoLbl}), 350);
  }
}

// Alerta — 2 etapas de verdade, a segunda pergunta depende da primeira
// resposta. Cores fortes (não pastel) — pedido do Yuri: Acidente=laranja,
// Área de risco=vermelho (invertido do que eu tinha proposto antes).
function infRelatarShowAlertaStep1(){
  document.getElementById('inf-relatar-title').textContent = 'Qual alerta?';
  infRelatarShowBack('Alerta', infOpenRelatar);
  document.getElementById('inf-r
===== #F97316 @ 1722245 =====
"2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>`,bairro:`<svg ${a}><circle cx="12" cy="10" r="3"/><path d="M12 2a8 8 0 0 1 8 8c0 5.333-8 14-8 14S4 15.333 4 10a8 8 0 0 1 8-8z"/></svg>`,rua:`<svg ${a}><circle cx="12" cy="12" r="3"/><path d="M12 2v3M12 19v3M2 12h3M19 12h3"/></svg>`,ev:`<svg ${a}><rect x="2" y="7" width="20" height="14" rx="2"/><path d="M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"/></svg>`,tur:`<svg ${a}><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>`,bar:`<svg ${a}><path d="M17 11H3v-1a5 5 0 0 1 5-5h4a5 5 0 0 1 5 5v1z"/><path d="M3 11v5a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2v-5"/></svg>`,hotel:`<svg ${a}><rect x="2" y="7" width="20" height="15" rx="2"/><path d="M2 12h20"/><path d="M8 7V5a4 4 0 0 1 8 0v2"/></svg>`,shop:`<svg ${a}><path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4Z"/><path d="M3 6h18"/><path d="M16 10a4 4 0 0 1-8 0"/></svg>`};
  return m[tipo]||m.bairro;
}
// Gesto swipe down para fechar o painel
(function() {
  const panel = document.getElementById('inf-panel');
  const drag  = document.getElementById('inf-panel-drag');
  if (!panel || !drag) return;
  let startY = 0, isDragging = false;
  function onStart(e) {
    startY = (e.touches ? e.touches[0].clientY : e.clientY);
    isDragging = true;
    panel.style.transition = 'none';
  }
  function onMove(e) {
    if (!isDragging) return;
    const dy = (e.touches ? e.touches[0].clientY : e.clientY) - startY;
    if (dy > 0) panel.style.transform = 'translateY(' + dy + 'px)';
  }
  function onEnd(e) {
    if (!isDragging) return;
    isDragging = false;
    panel.style.transition = '';
    const dy = (e.changedTouches ? e.changedTouches[0].clientY : e.clientY) - startY;
    if (dy > 100) { panel.style.transform = ''; infFecharPainel(); }
    else panel.style.transform = '';
  }
  drag.addEventListener('touchstart', onStart, {passive:true});
  drag.addEventListener('touchmove',  onMove,  {passive:true});
  drag.addEventListener('touchend',   onEnd);
  drag.addEventListener('mousedown',  onStart);
  document.addEventListener('mousemove', onMove);
  document.addEventListener('mouseup',   onEnd);
})();

['inf-ov-gps','inf-ov-venue','inf-ov-rep'].forEach(id=>{
  const el=document.getElementById(id);if(el)el.addEventListener('click',function(e){if(e.target===this)infFecharOv(id);});
});
document.addEventListener('click',e=>{
  if(!e.target.closest('#inf-search-bar')&&!e.target.closest('#inf-search-btn')&&!e.target.closest('#inf-search-dd')){
    const dd=document.getElementById('inf-search-dd');if(dd)dd.classList.remove('show');
  }
});

/* ══════════════════════════════════════════════════════════════
   FEED SOCIAL (Home) — v1
   Tabelas: feed_posts / feed_reactions / feed_comments (Supabase)
   Contadores de voto/comentário e auto-encerramento por downvote
   são calculados via trigger no banco — o client só insere/upserta
   e depois relê o post pra pegar os números atualizados.
   ══════════════════════════════════════════════════════════════ */

const FEED_TYPES = {
  blitz:    {label:'Blitz',        color:'#2563EB', expMin:60,
    icon:'<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>'},
  risco:    {label:'Risco',        color:'#EF4444', expMin:90,
    icon:'<path d="M10.3 3.9L1.8 18a2 2 0 0 0 1.7 3h17a2 2 0 0 0 1.7-3L13.7 3.9a2 2 0 0 0-3.4 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/>'},
  transito: {label:'Trânsito',     color:'#F97316', expMin:45,
    icon:'<rect x="3" y="4" width="18" height="16" rx="2"/><line x1="3" y1="10" x2="21" y2="10"/><line x1="3" y1="15" x2="21" y2="15"/>'},
  demanda:  {label:'Demanda alta', color:'#16A34A', expMin:30,
    icon:'<polyline points="23 6 13.5 15.5 8.5 10.5 1 18"/><polyline points="17 6 23 6 23 12"/>'},
  dinamico: {label:'Dinâmico',     color:'#F59E0B', expMin:30,
    icon:'<polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/>'},
  fila:     {label:'Fila',         color:'#7C3AED', expMin:45,
    icon:'<path d="M17.8 19.2L16 11l3.5-3.5C21 6 21.5 4 21 3c-1-.5-3 0-4.5 1.5L13 8 4.8 6.2c-.5-.1-.9.1-1.1.5l-.3.5c-.2.5-.1 1 .3 1.3L9 12l-2 3H4l-1 1 3 2 2 3 1-1v-3l3-2 3.5 5.3c.3.4.8.5 1.3.3l.5-.2c.4-.3.6-.7.5-1.2z"/>'}
};

let _feedFilter = '';
let _feedCoords = null;       // {lat,lng,address}
let _feedPosts  = [];
let _feedNames  = {};         // user_id -> nome
let _feedChannel = null;
let _feedCpType  = 'blitz';
let _feedCpLocOn = true;
let _feedMicActive = false;
let _feedCommentsPostId = null;

function feedHaversineKm(lat1,lng1,lat2,lng2){
  const R=6371, toRad=d=>d*Math.PI/180;
  const dLat=toRad(lat2-lat1), dLng=toRad(lng2-lng1);
  const a=Math.sin(dLat/2)**2+Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLng/2)**2;
  return R*2*Math.atan2(Math.sqrt(a),Math.sqrt(1-a));
}
function feedDistLabel(p){
  if(!_feedCoords||p.lat==null||p.lng==null) return '';
  const km=feedHaversineKm(_feedCoords.lat,_feedCoords.lng,p.lat,p.lng);
  return km<1 ? Math.round(km*1000)+' m' : km.toFixed(1).replace('.',',')+' km';
}
function feedAgeLabel(iso){
  const min=Math.max(0,Math.round((Date.now()-new Date(iso))/60000));
  if(min<1) return 'agora';
  if(min<60) return min+' min';
  return Math.floor(min/60)+'h'+(min%60?p2(min%60)+'m':'');
}
function feedInitials(name){
  const parts=(name||'').trim().split(/\s+/).filter(Boolean);
  if(!parts.length) return 'MO';
  return parts.length===1 ? parts[0].slice(0,2).toUpperCase() : (parts[0][0]+parts[parts.length-1][0]).toUpperCase();
}
function feedAvColor(uid){
  return INF_AVC[Math.abs((uid||'').split('').reduce((a,c)=>a+c.charCodeAt(0),0))%INF_AVC.length];
}

async function renderFeedHome(){
  feedComputeKpis();
  feedGetLocation();
  await feedLoadPosts();
  feedSubscribeRealtime();
}

function feedComputeKpis(){
  const dstr=DB.today();
  const day=DB.day(dstr);
  const tripsRev=(day.trips||[]).reduce((s,t)=>s+(t.value||0),0);
  const autoInfo=(typeof finAutoDay==='function')?finAutoDay(dstr):{sum:0,count:0};
  const manRev=(day.revenues||[]).reduce((s,r)=>s+(r.amount||0),0);
  const rev=tripsRev+autoInfo.sum+manRev;
  const trips=(day.trips||[]).length+autoInfo.count;
  const km=(day.sessions||[]).reduce((s,ss)=>s+(ss.km||0),0);
  const onlineSecs=(day.sessions||[]).reduce((s,ss)=>s+(ss.elapsed_secs||0),0);
  setText('feed-k-rev', DB.fRI(rev));
  setText('feed-k-trips', trips);
  setText('feed-k-rpkm', km>0 ? DB.fRI(rev/km) : '—');
  setText('feed-k-online', onlineSecs>0 ? Math.floor(onlineSecs/3600)+'h'+p2(Math.floor(onlineSecs%3600/60))+'m' : '—');
}

function feedGetLocation(){
  if(!navigator.geolocation) return;
  navigator.geolocation.getCurrentPosition(async pos=>{
    const {latitude:lat,longitude:lon}=pos.coords;
    _feedCoords={lat,lng:lon,address:null};
    try{
      const r=await fetch(`https://nominatim.openstreetmap.org/reverse?lat=${lat}&lon=${lon}&zoom=17&format=json&accept-language=pt`,{headers:{'User-Agent':'MoB/1.0'}});
      const j=await r.json();
      const addr=j.address||{};
      _feedCoords.address=addr.road||addr.pedestrian||addr.suburb||addr.neighbourhood||'';
    }catch(e){console.warn('feed loc',e);}
    feedUpdateComposerLoc();
    feedRenderList(); // recalcula distâncias com a posição atual
  },()=>{_feedCoords=null;},{timeout:10000,enableHighAccuracy:true});
}

async function feedLoadPosts(){
  const el=document.getElementById('feed-list');
  if(!_SUPA){ if(el) el.innerHTML='<div class="feed-empty">Sem conexão</div>'; return; }
  try{
    let q=_SUPA.from('feed_posts').select('*')
      .eq('status','ativo').eq('audience','comunidade')
      .gt('expires_at', new Date().toISOString())
      .order('created_at',{ascending:false}).limit(40);
    if(_feedFilter) q=q.eq('type',_feedFilter);
    const {data,error}=await q;
    if(error) throw error;
    _feedPosts=data||[];
    const uids=[...new Set(_feedPosts.map(p=>p.user_id))];
    if(uids.length){
      const {data:names}=await _SUPA.rpc('get_profile_names',{uids});
      _feedNames={}; (names||[]).forEach(n=>{_feedNames[n.id]={name:n.name, avatar:n.avatar_url};});
    }
    feedRenderList();
  }catch(e){console.warn('feed load',e); if(el) el.innerHTML='<div class="feed-empty">Erro ao carregar o feed</div>';}
}

// Avatar compartilhado entre feed, comentários e composer: foto real
// quando existe, senão iniciais coloridas (mesma cor sempre pro mesmo uid).
function feedAvatarHtml(uid, name, avatarUrl, extraClass){
  const cls = 'feed-av' + (extraClass?(' '+extraClass):'');
  if (avatarUrl) return `<img class="${cls}" src="${esc(avatarUrl)}" alt="">`;
  return `<div class="${cls}" style="background:${feedAvColor(uid)}">${esc(feedInitials(name))}</div>`;
}

function feedRenderList(){
  const el=document.getElementById('feed-list');if(!el)return;
  if(!_feedPosts.length){el.innerHTML='<div class="feed-empty">Nenhum alerta ativo agora — seja o primeiro a avisar a comunidade.</div>';return;}
  el.innerHTML=_feedPosts.map(feedPostCardHtml).join('');
}

function feedPostCardHtml(p){
  const t=FEED_TYPES[p.type]||FEED_TYPES.blitz;
  const isOwn = _supaUser && p.user_id===_supaUser.id;
  const cfg = DB.cfg();
  const myName = isOwn ? (cfg.name||'Você') : ((_feedNames[p.user_id]&&_feedNames[p.user_id].name)||'Motorista MōB');
  const myAvatar = isOwn ? (cfg.avatarUrl||null) : ((_feedNames[p.user_id]&&_feedNames[p.user_id].avatar)||null);
  const dist=feedDistLabel(p);
  const closed=p.status==='encerrado';
  const myVote=p._myVote; // preenchido depois de votar, só nesta sessão
  return `<div class="feed-post${closed?' closed':''}" onclick="feedAbrirComentarios('${p.id}')">
    ${feedAvatarHtml(p.user_id, myName, myAvatar)}
    <div class="feed-body">
      <div class="feed-head">
        <span class="feed-name">${esc(myName)}</span>
        <span class="feed-time">· ${feedAgeLabel(p.created_at)}</span>
      </div>
      <span class="feed-type" style="color:${t.color};background:color-mix(in srgb,${t.color} 10%,transparent);border:1px solid 
===== orange @ 4259 =====
ce-width,initial-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="default">
<meta name="description" content="MōB — a ferramenta da sua jornada">
<meta name="theme-color" content="#0A2F6B">
<meta name="mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-title" content="MōB">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<link rel="manifest" href="manifest.json">
<link rel="apple-touch-icon" href="apple-touch-icon.png">
<title>MōB</title>
<script>
// Aplica o modo escuro antes da primeira pintura, pra nao dar aquele flash de tela clara.
try {
  const cfg = JSON.parse(localStorage.getItem('sm_cfg'));
  if (cfg && cfg.darkMode) document.documentElement.classList.add('dark');
} catch(e) {}
</script>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
<style>

:root{--font-brand:'Poppins',sans-serif;--font-app:'Inter',system-ui,-apple-system,BlinkMacSystemFont,sans-serif;
  /* Shared tokens */
  --bg:#F7F9FC;--card:#FFFFFF;--text:#0F172A;--muted:#64748B;--muted2:#94A3B8;
  --line:#E2E8F0;--soft-line:#E8EEF5;
  --blue:#0A2F6B;--mob-blue:#0A2F6B;--mob-gray:#9CA3AF;--blue2:#0284C7;
  --green:#22C55E;--green2:#16A34A;
  --red:#EF4444;--orange:#F59E0B;--amber:#FACC15;--purple:#7C3AED;
  --greenBg:#ECFDF3;--orangeBg:#FFF7ED;--redBg:#FEF2F2;--purpleBg:#F5F3FF;--blueBg:#EFF6FF;
  --shadow:0 10px 28px rgba(15,23,42,.08);--soft:0 3px 14px rgba(15,23,42,.06);
  --r:24px;--radius:24px;
  --safe-top:max(env(safe-area-inset-top),14px);
  --safe-bottom:max(env(safe-area-inset-bottom),10px);
  --top:0px;--bottom:78px;
}
/* ── Modo escuro: paleta neutra, cor só em status/valores ── */
html.dark{
  --bg:#0A0F1A;--card:#111723;--text:#E2E8F0;--muted:#94A3B8;--muted2:#64748B;
  --line:#1E2735;--soft-line:#1A2230;
  --green:#10B981;--green2:#059669;
  --red:#EF4444;--orange:#F59E0B;--amber:#EAB308;--purple:#8B5CF6;
  --greenBg:rgba(16,185,129,.12);--orangeBg:rgba(245,158,11,.12);--redBg:rgba(239,68,68,.12);
  --purpleBg:rgba(139,92,246,.12);--blueBg:rgba(14,165,233,.12);
  --shadow:0 10px 28px rgba(0,0,0,.35);--soft:0 3px 14px rgba(0,0,0,.25);
}
html.dark body{background:var(--bg)}
/* Cards com gradiente colorido fixo (nao usam variavel) ficam neutros no escuro,
   seguindo a direcao aprovada — cor só em pontos pontuais, nao no fundo todo */
html.dark .journey.idle,html.dark .journey.ended,html.dark .journey.running{
  background:var(--card);border:1px solid var(--line);box-shadow:none
}
html.dark .hero{background:var(--card);border:1px solid var(--line);box-shadow:none}
html.dark .amount-card.receita,html.dark .amount-card.despesa{background:var(--card);border:1px solid var(--line)}
html.dark input,html.dark select,html.dark textarea{background:#0F1726;color:var(--text);border-color:var(--line)}
html.dark .inp,html.dark .cat-inp2{background:#0F1726;color:var(--text)}
html.dark .sheet,html.dark .mo .sheet{background:var(--card)}
html.dark img.invert-dark{filter:invert(1) brightness(1.4)}
/* Ícones do menu Mais viram neutros no escuro — cor só sobra pra dinheiro
   (receita/despesa), não pra decoração de cada item de menu */
html.dark .mais-ico,html.dark .mais-ico.blue,html.dark .mais-ico.orange,html.dark .mais-ico.purple,html.dark .mais-ico.green{background:#1A2230;color:#94A3B8}
html.dark .mais-item{background:var(--card);border-color:var(--line)}
/* ── Patch dark mode: componentes novos (resumo jornada, wizard custo,
   Lançamentos, import) que ainda usavam cor fixa ── */
html.dark .pchip{background:var(--card)}
html.dark .pchip.active{background:#2563EB;border-color:#2563EB;color:#fff}
html.dark .cv-inp-wrap{background:var(--card);border-color:var(--line)}
html.dark .cv-inp-wrap input{color:var(--text)}
html.dark .cv-opt-card,html.dark .cv-opt-row{background:var(--card);border-color:var(--line)}
html.dark .cv-opt-card.sel,html.dark .cv-opt-row.sel{background:rgba(37,99,235,.12);border-color:#2563EB}
html.dark .cv-opt-ico{background:#1A2230;color:var(--muted)}
html.dark .cv-back{background:var(--card);border-color:var(--line);color:var(--text)}
html.dark .cv-pct-btn{background:var(--card);border-color:var(--line);color:var(--muted)}
html.dark .cv-pct-btn.sel{background:#2563EB;border-color:#2563EB;color:#fff}
html.dark .cv-brow{background:var(--soft-line)}
html.dark .cv-daily{background:rgba(251,191,36,.1)!important;border-color:rgba(251,191,36,.3)!important;color:#FDE68A}
html.dark .cv-daily span{color:#FDE68A!important}
html.dark .cv-sug{background:rgba(37,99,235,.15)}
html.dark .li-row,html.dark .li-row-j{background:var(--card);border-color:var(--line)}
html.dark .li-ico.plat-99{background:var(--soft-line)}
html.dark .lanc-total-card,html.dark .cv-result{box-shadow:0 10px 28px rgba(0,0,0,.4)}
/* ── SPA core ── */
*{box-sizing:border-box;margin:0;padding:0;-webkit-tap-highlight-color:transparent}
html,body{height:100%;background:var(--bg);font-family:Inter,system-ui,-apple-system,sans-serif;color:var(--text);overflow:hidden;-webkit-font-smoothing:antialiased}
.app{max-width:430px;margin:0 auto;height:100dvh;background:var(--bg);position:relative;overflow:hidden;padding-top:calc(var(--top) + var(--safe-top));padding-bottom:calc(var(--bottom) + var(--safe-bottom))}
.scroll{height:100%;overflow-y:auto;overflow-x:hidden;-webkit-overflow-scrolling:touch;padding:12px 0 24px}.scroll::-webkit-scrollbar{display:none}
.screen{display:none}.screen.on{display:block}
/* Topbar */
.topbar{position:fixed;top:0;left:50%;transform:translateX(-50%);width:min(430px,100vw);height:calc(var(--top) + var(--safe-top));padding:var(--safe-top) 12px 0 16px;display:flex;align-items:center;background:rgba(255,255,255,.97);backdrop-filter:blur(18px);border-bottom:1px solid var(--line);z-index:100;gap:8px}
.brand{display:flex;align-items:center;gap:9px;flex:1}.mark{width:34px;height:34px;border-radius:13px;background:#0F172A;color:#fff;display:flex;align-items:center;justify-content:center;font-weight:800;font-size:11px}.name{font-size:18px;font-weight:800;letter-spacing:-.04em}.name b{color:#0A2F6B}
.tbicons{display:flex;gap:2px}.iconbtn{width:36px;height:36px;border-radius:13px;background:transparent;border:none;color:#475569;display:flex;align-items:center;justify-content:center;cursor:pointer}.iconbtn:active{background:#F1F5F9}
/* Bottom nav */
.bottom{position:fixed;left:50%;bottom:0;transform:translateX(-50%);width:min(430px,100vw);height:calc(var(--bottom) + var(--safe-bottom));padding:8px 10px var(--safe-bottom);background:rgba(255,255,255,.97);backdrop-filter:blur(18px);border-top:1px solid var(--line);display:flex;align-items:center;z-index:100;box-shadow:0 -10px 28px rgba(15,23,42,.07)}
.nav{flex:1;height:54px;border-radius:16px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;color:#94A3B8;font-size:10px;font-weight:800;cursor:pointer;touch-action:manipulation;transition:all .12s}
.nav.active{background:#F8FAFC;color:#0A2F6B;box-shadow:0 2px 10px rgba(15,23,42,.06)}
.nav svg{width:21px;height:21px;flex-shrink:0}
.center{width:84px;display:flex;justify-content:center}
.start{display:flex;align-items:center;justify-content:center;flex-direction:column;gap:2px;width:76px;height:68px;border-radius:20px;background:#0A2F6B;border:3px solid var(--bg);box-shadow:0 10px 28px rgba(10,47,107,.35);margin-top:-36px;cursor:pointer;touch-action:manipulation;transition:background .2s}
.start svg{fill:none!important}
.start.running{background:#22C55E;box-shadow:0 10px 28px rgba(34,197,94,.38)}
.start.paused{background:#9CA3AF;box-shadow:0 10px 28px rgba(156,163,175,.38)}
.start.ended{background:#0A2F6B;box-shadow:0 10px 28px rgba(10,47,107,.38)}
.start svg{flex-shrink:0;filter:drop-shadow(0 1px 2px rgba(0,0,0,.15))}.start span{font-size:9px;font-weight:800;text-transform:uppercase;letter-spacing:.04em;color:white;line-height:1;margin-top:1px}
/* Modal */
.mo{position:fixed;inset:0;background:rgba(15,23,42,.45);display:none;align-items:flex-end;z-index:200;backdrop-filter:blur(4px)}
.mo.on{display:flex}.mo.ctr{align-items:center;justify-content:center;padding:16px}
.sheet{width:100%;max-width:430px;margin:0 auto;background:#fff;border-radius:28px 28px 0 0;padding:16px;max-height:88dvh;overflow-y:auto;animation:slideUp .22s cubic-bezier(.4,0,.2,1)}
.sheet.ctr{border-radius:28px;max-width:420px;animation:popIn .2s cubic-bezier(.34,1.56,.64,1)}
.sheet::-webkit-scrollbar{display:none}
@keyframes slideUp{from{transform:translateY(100%)}to{transform:translateY(0)}}
@keyframes popIn{from{opacity:0;transform:scale(.92)}to{opacity:1;transform:scale(1)}}
.sheet-handle{width:36px;height:4px;background:#E2E8F0;border-radius:2px;margin:8px auto 12px}
.sheet-head{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px}
.sheet-head h2{font-size:18px;font-weight:800;letter-spacing:-.03em}
.xbtn{width:32px;height:32px;border:none;border-radius:10px;background:#F1F5F9;color:#64748B;font-size:18px;cursor:pointer;display:flex;align-items:center;justify-content:center}
/* Shared form elements */
.field{margin-bottom:14px}.field:last-child{margin-bottom:0}
label.lbl{display:block;font-size:11px;text-transform:uppercase;letter-spacing:.08em;color:var(--muted);font-weight:900;margin-bottom:7px}
.inp{width:100%;height:50px;border:1.5px solid var(--line);background:#F8FAFC;border-radius:16px;padding:0 14px;font-size:15px;font-weight:700;color:var(--text);outline:none;font-family:var(--font-app);transition:border-color .12s}
.inp:focus{border-color:#0A2F6B;background:#fff;box-shadow:0 0 0 3px rgba(14,165,233,.10)}
.btn-p{width:100%;height:54px;border:none;border-radius:18px;background:#0A2F6B;color:#fff;font-size:16px;font-weight:800;cursor:pointer;font-family
===== orange @ 74776 =====
0F6FF}
.ld-btn.del{color:#EF4444;border-color:#FECACA;background:#FEF2F2;flex:0 0 44px}
.obj-tag{display:inline-block;font-size:9px;font-weight:800;padding:2px 7px;border-radius:99px;margin-right:6px;vertical-align:1px}
.obj-tag.rec{background:#E9FBEF;color:#16A34A}
.obj-tag.desp{background:#FEF2F2;color:#EF4444}
.obj-switch,.type-switch2{display:flex;background:#EAF0F6;border:1px solid rgba(15,23,42,.06);padding:4px;border-radius:18px;margin-bottom:16px}
.obj-btn,.type-btn2{flex:1;border:none;background:transparent;color:var(--muted);padding:10px 8px;border-radius:14px;font-size:13.5px;font-weight:900;cursor:pointer;font-family:var(--font-app)}
.obj-btn.active,.type-btn2.active{background:#fff;color:var(--text);box-shadow:0 4px 12px rgba(15,23,42,.08)}
.gbar.rec i{background:linear-gradient(90deg,#22C55E,#0EA5E9)}.gbar.exp i{background:linear-gradient(90deg,#F59E0B,#F59E0B)}.gbar.exp.over i,.gbar.rec.over i{background:linear-gradient(90deg,#EF4444,#EF4444)}
.goal-locked{display:flex;align-items:center;justify-content:space-between;background:#F8FAFC;border:1px dashed #D8E0EA;border-radius:14px;padding:10px 12px;cursor:pointer;flex:1;min-width:0}.goal-locked span{font-size:12px;color:var(--muted);font-weight:600}.goal-locked b{font-size:12px;color:#0A2F6B;font-weight:800}
.cfg-link{display:block;text-align:center;font-size:12.5px;font-weight:700;color:#0A2F6B;background:#F8FAFC;border:1px solid var(--line);border-radius:14px;padding:11px;margin-top:4px;cursor:pointer;font-family:var(--font-app)}
.cat-row2{display:flex;align-items:center;gap:12px;padding:11px 0;border-bottom:1px solid #edf1f6}.cat-row2:last-child{border-bottom:0}.cat-lbl2{flex:1;font-size:14px;font-weight:650}
.cat-inp-wrap2{position:relative;width:118px}.cat-inp-wrap2 span{position:absolute;left:12px;top:50%;transform:translateY(-50%);font-size:13px;color:var(--muted);font-weight:700}
.cat-inp2{width:100%;height:42px;border:1.5px solid var(--line);background:#F8FAFC;border-radius:13px;padding:0 12px 0 32px;font-size:14px;font-weight:700;color:var(--text);outline:none;font-family:'Inter',system-ui,sans-serif;text-align:right}.cat-inp2:focus{border-color:#0A2F6B;background:#fff}
.chart{padding:4px 0}.barCol{flex:1;display:flex;flex-direction:column;align-items:center;gap:4px}.barCol i{width:100%;background:linear-gradient(180deg,var(--blue),var(--blue2));border-radius:6px 6px 0 0;min-height:8px}.barCol small{font-size:10px;color:var(--muted);font-weight:700}
.actions{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.action{border:1px solid var(--line);border-radius:16px;background:#F8FAFC;padding:14px 8px;display:flex;flex-direction:column;align-items:center;gap:8px;font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation;color:var(--text)}.action .ico{background:transparent}.action:active{background:var(--line)}
.list{padding:0 0 2px}.item{display:flex;align-items:center;gap:12px;padding:12px 0}.itemBody{flex:1;min-width:0}
.budgetRow{display:flex;align-items:center;gap:12px;padding:10px 0;border-bottom:1px solid var(--line)}.budgetRow:last-child{border-bottom:none}.budgetTrack{height:6px;background:#E2E8F0;border-radius:3px;overflow:hidden;margin-top:6px}.budgetTrack i{display:block;height:100%;border-radius:3px;background:#0A2F6B;transition:width .4s}
.split{display:grid;grid-template-columns:auto 1fr;gap:16px;align-items:center}.donut{width:80px;height:80px;border-radius:50%;background:conic-gradient(var(--blue) 0 57%,var(--green) 57% 86%,var(--orange) 86% 97%,var(--red) 97% 100%);flex-shrink:0}.catRow{display:flex;align-items:center;gap:8px;margin-bottom:8px}.dot{width:8px;height:8px;border-radius:50%;background:#0A2F6B;flex-shrink:0}
/* ── Home screen extras ── */
.ad-carousel{margin:14px 16px 0;border-radius:18px;overflow:hidden;position:relative;touch-action:pan-y;cursor:pointer;height:40px}
.ad-carousel img{width:100%;height:40px;object-fit:cover;object-position:center;display:block;transition:transform .4s ease;pointer-events:none}
.ad-carousel .adtag{position:absolute;top:8px;left:10px;font-size:8px;font-weight:900;text-transform:uppercase;letter-spacing:.08em;color:#64748B;background:rgba(255,255,255,0.92);padding:3px 7px;border-radius:6px;z-index:2;backdrop-filter:blur(4px)}
.entries{background:#fff;border:1px solid var(--line);border-radius:24px;padding:16px;box-shadow:0 2px 10px rgba(15,23,42,.05)}.eheader{display:flex;align-items:center;justify-content:space-between;margin-bottom:10px}.eheader h3{font-size:15px;font-weight:700}.eheader button{font-size:12px;font-weight:700;color:#0A2F6B;background:none;border:none;cursor:pointer;touch-action:manipulation}
.partner{background:linear-gradient(135deg,#0F172A,#1E293B);border-radius:22px;padding:16px;color:#fff;box-shadow:0 10px 28px rgba(15,23,42,.08)}.eyebrow{font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.1em;opacity:.6;margin-bottom:4px}.pt{font-size:16px;font-weight:800;letter-spacing:-.03em}.ps{font-size:12px;opacity:.65;margin-top:4px}
.pie-wrap{display:flex;align-items:center;gap:14px;padding:8px 0}.pie{width:72px;height:72px;border-radius:50%;background:conic-gradient(var(--blue) 0 61%,var(--green) 61% 89%,var(--orange) 89% 100%);flex-shrink:0}.pie-wrap .list{background:none;border:none;box-shadow:none;padding:0;flex:1}.li{display:flex;align-items:center;justify-content:space-between;font-size:12px;padding:3px 0}.li span{color:var(--muted);font-weight:600}.li b{font-family:'Inter',system-ui,sans-serif;font-weight:700}.legend{display:flex;gap:12px;margin-top:8px}.leg{display:flex;align-items:center;gap:4px;font-size:11px;color:var(--muted);font-weight:600}.dot{width:7px;height:7px;border-radius:50%;background:#0A2F6B;flex-shrink:0}.dot.g{background:var(--green)}.dot.o{background:var(--orange)}

.hero{border-radius:28px;padding:20px;color:#fff;box-shadow:0 10px 28px rgba(15,23,42,.08)}.period{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px}.seg button{flex:1;padding:8px 6px;border-radius:14px;border:none;background:transparent;color:rgba(255,255,255,.65);font-size:13px;font-weight:800;cursor:pointer;touch-action:manipulation}.seg button.on{background:rgba(255,255,255,.22);color:#fff}.filter{color:rgba(255,255,255,.7);cursor:pointer}
.kicker{font-size:11px;text-transform:uppercase;letter-spacing:.12em;opacity:.78;font-weight:800;margin-bottom:6px}.amount{font-size:42px;font-weight:900;letter-spacing:-.06em;line-height:1;margin-bottom:4px}.sub{font-size:13px;opacity:.78}
.metrics{margin-top:16px;display:flex;flex-direction:column;gap:8px}.mrow{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.metric{background:rgba(255,255,255,.14);border-radius:14px;padding:10px 8px;text-align:center}.mv{font-family:'Inter',system-ui,sans-serif;font-size:16px;font-weight:800}.mv.good{color:#86EFAC}.mv.bad{color:#FCA5A5}.mv.warn{color:#FDE68A}.ml{font-size:10px;opacity:.72;margin-top:3px;font-weight:700}
.goalbar{height:4px;background:rgba(255,255,255,.2);border-radius:2px;margin-top:16px;overflow:hidden}.goalfill{height:100%;background:rgba(255,255,255,.9);border-radius:2px;transition:width .6s}.goaltext{font-size:11px;opacity:.75;margin-top:6px;font-weight:600}
.chart{background:#fff;border:1px solid var(--line);box-shadow:0 2px 10px rgba(15,23,42,.05);border-radius:24px;padding:16px}.chead{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}.ctitle{font-size:14px;font-weight:700;letter-spacing:-.02em}.mini-tabs{display:flex;gap:4px}.mini-tabs span{font-size:11px;font-weight:700;padding:4px 10px;border-radius:10px;cursor:pointer;color:var(--muted)}.mini-tabs span.on{background:rgba(14,165,233,.1);color:#0A2F6B}
.bars{display:flex;gap:4px;align-items:flex-end;height:80px;padding:0 2px}.baritem{flex:1;display:flex;flex-direction:column;align-items:center;gap:6px}.col{width:100%;background:#0A2F6B;border-radius:6px 6px 0 0;min-height:6px;opacity:.85;transition:height .5s}.col.alt{background:var(--green)}.col.warn{background:var(--orange)}.lbl{font-size:10px;color:var(--muted);font-weight:700}.bars.many .baritem{flex:0 0 calc(10% - 4px)}.bars.medium .baritem{flex:0 0 calc(14.28% - 4px)}

@keyframes spin{from{transform:rotate(0deg)}to{transform:rotate(360deg)}}

/* fin-action buttons (Financeiro screen) */
.fin-actions{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}
.fin-action{border:1px solid var(--line);border-radius:16px;background:#F8FAFC;padding:14px 8px;display:flex;flex-direction:column;align-items:center;gap:8px;font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation;color:var(--text);font-family:var(--font-app)}
.fin-action:active{background:var(--line)}
.fin-action .ico{background:transparent}
@keyframes vspin{from{transform:rotate(0deg)}to{transform:rotate(360deg)}}
@keyframes pulseDot{0%,100%{opacity:.25}50%{opacity:1}}


/* ═══════════════════════════════════════════════════
   AGENDA — CSS isolado (prefixo ag-)
   ═══════════════════════════════════════════════════ */
#sc-agenda{padding:0}
.ag-wrap{padding:12px 16px 24px}
.ag-tabs{height:52px;background:#eaf0f6;border:1px solid #dbe3ec;border-radius:28px;padding:5px;display:flex;margin-bottom:16px}
.ag-tabs button{flex:1;border:0;background:transparent;color:#667085;font-size:14px;font-weight:700;border-radius:23px;cursor:pointer;touch-action:manipulation;font-family:var(--font-app)}
.ag-tabs button.on{background:#fff;color:#101828;box-shadow:0 8px 20px rgba(15,23,42,.08)}
.ag-date-head{display:flex;align-items:center;justify-content:space-between;margin:8px 0 14px}
.ag-date-head h1{font-size:22px;font-weight:800;letter-spacing:-.04em;line-height:1.1}
.ag-date-head p{font-size:12px;font-weight:500;color:#667085;margin-top:4px}
.ag-pill{background:#e6f5ff;color:#2592d9;border:0;border-radius:999px;padding:9px 15px;font-weight:700;font-size:13px;cursor:pointer;touch-action:manipulation;font-family:var(--font-app);white-space:nowrap}
.ag-sec{font-size:11px;letter-spacing:.14em;font-weight:800;col
===== orange @ 76466 =====
nt-weight:650}
.cat-inp-wrap2{position:relative;width:118px}.cat-inp-wrap2 span{position:absolute;left:12px;top:50%;transform:translateY(-50%);font-size:13px;color:var(--muted);font-weight:700}
.cat-inp2{width:100%;height:42px;border:1.5px solid var(--line);background:#F8FAFC;border-radius:13px;padding:0 12px 0 32px;font-size:14px;font-weight:700;color:var(--text);outline:none;font-family:'Inter',system-ui,sans-serif;text-align:right}.cat-inp2:focus{border-color:#0A2F6B;background:#fff}
.chart{padding:4px 0}.barCol{flex:1;display:flex;flex-direction:column;align-items:center;gap:4px}.barCol i{width:100%;background:linear-gradient(180deg,var(--blue),var(--blue2));border-radius:6px 6px 0 0;min-height:8px}.barCol small{font-size:10px;color:var(--muted);font-weight:700}
.actions{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.action{border:1px solid var(--line);border-radius:16px;background:#F8FAFC;padding:14px 8px;display:flex;flex-direction:column;align-items:center;gap:8px;font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation;color:var(--text)}.action .ico{background:transparent}.action:active{background:var(--line)}
.list{padding:0 0 2px}.item{display:flex;align-items:center;gap:12px;padding:12px 0}.itemBody{flex:1;min-width:0}
.budgetRow{display:flex;align-items:center;gap:12px;padding:10px 0;border-bottom:1px solid var(--line)}.budgetRow:last-child{border-bottom:none}.budgetTrack{height:6px;background:#E2E8F0;border-radius:3px;overflow:hidden;margin-top:6px}.budgetTrack i{display:block;height:100%;border-radius:3px;background:#0A2F6B;transition:width .4s}
.split{display:grid;grid-template-columns:auto 1fr;gap:16px;align-items:center}.donut{width:80px;height:80px;border-radius:50%;background:conic-gradient(var(--blue) 0 57%,var(--green) 57% 86%,var(--orange) 86% 97%,var(--red) 97% 100%);flex-shrink:0}.catRow{display:flex;align-items:center;gap:8px;margin-bottom:8px}.dot{width:8px;height:8px;border-radius:50%;background:#0A2F6B;flex-shrink:0}
/* ── Home screen extras ── */
.ad-carousel{margin:14px 16px 0;border-radius:18px;overflow:hidden;position:relative;touch-action:pan-y;cursor:pointer;height:40px}
.ad-carousel img{width:100%;height:40px;object-fit:cover;object-position:center;display:block;transition:transform .4s ease;pointer-events:none}
.ad-carousel .adtag{position:absolute;top:8px;left:10px;font-size:8px;font-weight:900;text-transform:uppercase;letter-spacing:.08em;color:#64748B;background:rgba(255,255,255,0.92);padding:3px 7px;border-radius:6px;z-index:2;backdrop-filter:blur(4px)}
.entries{background:#fff;border:1px solid var(--line);border-radius:24px;padding:16px;box-shadow:0 2px 10px rgba(15,23,42,.05)}.eheader{display:flex;align-items:center;justify-content:space-between;margin-bottom:10px}.eheader h3{font-size:15px;font-weight:700}.eheader button{font-size:12px;font-weight:700;color:#0A2F6B;background:none;border:none;cursor:pointer;touch-action:manipulation}
.partner{background:linear-gradient(135deg,#0F172A,#1E293B);border-radius:22px;padding:16px;color:#fff;box-shadow:0 10px 28px rgba(15,23,42,.08)}.eyebrow{font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.1em;opacity:.6;margin-bottom:4px}.pt{font-size:16px;font-weight:800;letter-spacing:-.03em}.ps{font-size:12px;opacity:.65;margin-top:4px}
.pie-wrap{display:flex;align-items:center;gap:14px;padding:8px 0}.pie{width:72px;height:72px;border-radius:50%;background:conic-gradient(var(--blue) 0 61%,var(--green) 61% 89%,var(--orange) 89% 100%);flex-shrink:0}.pie-wrap .list{background:none;border:none;box-shadow:none;padding:0;flex:1}.li{display:flex;align-items:center;justify-content:space-between;font-size:12px;padding:3px 0}.li span{color:var(--muted);font-weight:600}.li b{font-family:'Inter',system-ui,sans-serif;font-weight:700}.legend{display:flex;gap:12px;margin-top:8px}.leg{display:flex;align-items:center;gap:4px;font-size:11px;color:var(--muted);font-weight:600}.dot{width:7px;height:7px;border-radius:50%;background:#0A2F6B;flex-shrink:0}.dot.g{background:var(--green)}.dot.o{background:var(--orange)}

.hero{border-radius:28px;padding:20px;color:#fff;box-shadow:0 10px 28px rgba(15,23,42,.08)}.period{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px}.seg button{flex:1;padding:8px 6px;border-radius:14px;border:none;background:transparent;color:rgba(255,255,255,.65);font-size:13px;font-weight:800;cursor:pointer;touch-action:manipulation}.seg button.on{background:rgba(255,255,255,.22);color:#fff}.filter{color:rgba(255,255,255,.7);cursor:pointer}
.kicker{font-size:11px;text-transform:uppercase;letter-spacing:.12em;opacity:.78;font-weight:800;margin-bottom:6px}.amount{font-size:42px;font-weight:900;letter-spacing:-.06em;line-height:1;margin-bottom:4px}.sub{font-size:13px;opacity:.78}
.metrics{margin-top:16px;display:flex;flex-direction:column;gap:8px}.mrow{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.metric{background:rgba(255,255,255,.14);border-radius:14px;padding:10px 8px;text-align:center}.mv{font-family:'Inter',system-ui,sans-serif;font-size:16px;font-weight:800}.mv.good{color:#86EFAC}.mv.bad{color:#FCA5A5}.mv.warn{color:#FDE68A}.ml{font-size:10px;opacity:.72;margin-top:3px;font-weight:700}
.goalbar{height:4px;background:rgba(255,255,255,.2);border-radius:2px;margin-top:16px;overflow:hidden}.goalfill{height:100%;background:rgba(255,255,255,.9);border-radius:2px;transition:width .6s}.goaltext{font-size:11px;opacity:.75;margin-top:6px;font-weight:600}
.chart{background:#fff;border:1px solid var(--line);box-shadow:0 2px 10px rgba(15,23,42,.05);border-radius:24px;padding:16px}.chead{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}.ctitle{font-size:14px;font-weight:700;letter-spacing:-.02em}.mini-tabs{display:flex;gap:4px}.mini-tabs span{font-size:11px;font-weight:700;padding:4px 10px;border-radius:10px;cursor:pointer;color:var(--muted)}.mini-tabs span.on{background:rgba(14,165,233,.1);color:#0A2F6B}
.bars{display:flex;gap:4px;align-items:flex-end;height:80px;padding:0 2px}.baritem{flex:1;display:flex;flex-direction:column;align-items:center;gap:6px}.col{width:100%;background:#0A2F6B;border-radius:6px 6px 0 0;min-height:6px;opacity:.85;transition:height .5s}.col.alt{background:var(--green)}.col.warn{background:var(--orange)}.lbl{font-size:10px;color:var(--muted);font-weight:700}.bars.many .baritem{flex:0 0 calc(10% - 4px)}.bars.medium .baritem{flex:0 0 calc(14.28% - 4px)}

@keyframes spin{from{transform:rotate(0deg)}to{transform:rotate(360deg)}}

/* fin-action buttons (Financeiro screen) */
.fin-actions{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}
.fin-action{border:1px solid var(--line);border-radius:16px;background:#F8FAFC;padding:14px 8px;display:flex;flex-direction:column;align-items:center;gap:8px;font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation;color:var(--text);font-family:var(--font-app)}
.fin-action:active{background:var(--line)}
.fin-action .ico{background:transparent}
@keyframes vspin{from{transform:rotate(0deg)}to{transform:rotate(360deg)}}
@keyframes pulseDot{0%,100%{opacity:.25}50%{opacity:1}}


/* ═══════════════════════════════════════════════════
   AGENDA — CSS isolado (prefixo ag-)
   ═══════════════════════════════════════════════════ */
#sc-agenda{padding:0}
.ag-wrap{padding:12px 16px 24px}
.ag-tabs{height:52px;background:#eaf0f6;border:1px solid #dbe3ec;border-radius:28px;padding:5px;display:flex;margin-bottom:16px}
.ag-tabs button{flex:1;border:0;background:transparent;color:#667085;font-size:14px;font-weight:700;border-radius:23px;cursor:pointer;touch-action:manipulation;font-family:var(--font-app)}
.ag-tabs button.on{background:#fff;color:#101828;box-shadow:0 8px 20px rgba(15,23,42,.08)}
.ag-date-head{display:flex;align-items:center;justify-content:space-between;margin:8px 0 14px}
.ag-date-head h1{font-size:22px;font-weight:800;letter-spacing:-.04em;line-height:1.1}
.ag-date-head p{font-size:12px;font-weight:500;color:#667085;margin-top:4px}
.ag-pill{background:#e6f5ff;color:#2592d9;border:0;border-radius:999px;padding:9px 15px;font-weight:700;font-size:13px;cursor:pointer;touch-action:manipulation;font-family:var(--font-app);white-space:nowrap}
.ag-sec{font-size:11px;letter-spacing:.14em;font-weight:800;color:#6b778c;margin:20px 0 10px;text-transform:uppercase}
.ag-item{background:#fff;border:1px solid rgba(15,23,42,.06);border-radius:24px;padding:14px;box-shadow:0 10px 28px rgba(15,23,42,.06);display:flex;align-items:center;gap:12px;margin-bottom:10px}
.ag-ico{width:48px;height:48px;border-radius:16px;display:flex;align-items:center;justify-content:center;flex:0 0 48px}
.ag-ico svg{width:20px;height:20px;stroke:currentColor;fill:none;stroke-width:2.25;stroke-linecap:round;stroke-linejoin:round}
.ag-ico.green{background:#e9fbef;color:#22c55e}.ag-ico.blue{background:#e8f5ff;color:#0ea5e9}.ag-ico.orange{background:#fff5df;color:#f59e0b}.ag-ico.red{background:#fdecef;color:#ef4444}.ag-ico.purple{background:#f1ecff;color:#8b5cf6}
.ag-info{flex:1;min-width:0}
.ag-ititle{font-size:15px;font-weight:700;letter-spacing:-.02em;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.ag-imeta{font-size:12px;color:#667085;font-weight:500;line-height:1.4;margin-top:3px}
.ag-ivalue{font-size:14px;font-weight:700;color:#344054;white-space:nowrap;flex-shrink:0}
.ag-paybtn{border:0;background:#e9fbef;color:#16a34a;border-radius:999px;padding:8px 12px;font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation;font-family:var(--font-app);flex-shrink:0}
.ag-paybtn.paid{background:#f3f4f6;color:#9ca3af}
.ag-empty{padding:32px 16px;text-align:center;color:#94A3B8;font-size:13px;font-weight:500}
/* Agenda Dia */
.ag-strip{display:flex;gap:8px}
.ag-strip::-webkit-scrollbar{display:none}
.ag-chip{flex:1;min-width:0;height:70px;background:#fff;border:1px solid #e4eaf2;border-radius:20px;box-shadow:0 4px 12px rgba(15,23,42,.045);display:flex;flex-direction:column;align-items
===== orange @ 77049 =====
ap:4px}.barCol i{width:100%;background:linear-gradient(180deg,var(--blue),var(--blue2));border-radius:6px 6px 0 0;min-height:8px}.barCol small{font-size:10px;color:var(--muted);font-weight:700}
.actions{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.action{border:1px solid var(--line);border-radius:16px;background:#F8FAFC;padding:14px 8px;display:flex;flex-direction:column;align-items:center;gap:8px;font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation;color:var(--text)}.action .ico{background:transparent}.action:active{background:var(--line)}
.list{padding:0 0 2px}.item{display:flex;align-items:center;gap:12px;padding:12px 0}.itemBody{flex:1;min-width:0}
.budgetRow{display:flex;align-items:center;gap:12px;padding:10px 0;border-bottom:1px solid var(--line)}.budgetRow:last-child{border-bottom:none}.budgetTrack{height:6px;background:#E2E8F0;border-radius:3px;overflow:hidden;margin-top:6px}.budgetTrack i{display:block;height:100%;border-radius:3px;background:#0A2F6B;transition:width .4s}
.split{display:grid;grid-template-columns:auto 1fr;gap:16px;align-items:center}.donut{width:80px;height:80px;border-radius:50%;background:conic-gradient(var(--blue) 0 57%,var(--green) 57% 86%,var(--orange) 86% 97%,var(--red) 97% 100%);flex-shrink:0}.catRow{display:flex;align-items:center;gap:8px;margin-bottom:8px}.dot{width:8px;height:8px;border-radius:50%;background:#0A2F6B;flex-shrink:0}
/* ── Home screen extras ── */
.ad-carousel{margin:14px 16px 0;border-radius:18px;overflow:hidden;position:relative;touch-action:pan-y;cursor:pointer;height:40px}
.ad-carousel img{width:100%;height:40px;object-fit:cover;object-position:center;display:block;transition:transform .4s ease;pointer-events:none}
.ad-carousel .adtag{position:absolute;top:8px;left:10px;font-size:8px;font-weight:900;text-transform:uppercase;letter-spacing:.08em;color:#64748B;background:rgba(255,255,255,0.92);padding:3px 7px;border-radius:6px;z-index:2;backdrop-filter:blur(4px)}
.entries{background:#fff;border:1px solid var(--line);border-radius:24px;padding:16px;box-shadow:0 2px 10px rgba(15,23,42,.05)}.eheader{display:flex;align-items:center;justify-content:space-between;margin-bottom:10px}.eheader h3{font-size:15px;font-weight:700}.eheader button{font-size:12px;font-weight:700;color:#0A2F6B;background:none;border:none;cursor:pointer;touch-action:manipulation}
.partner{background:linear-gradient(135deg,#0F172A,#1E293B);border-radius:22px;padding:16px;color:#fff;box-shadow:0 10px 28px rgba(15,23,42,.08)}.eyebrow{font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.1em;opacity:.6;margin-bottom:4px}.pt{font-size:16px;font-weight:800;letter-spacing:-.03em}.ps{font-size:12px;opacity:.65;margin-top:4px}
.pie-wrap{display:flex;align-items:center;gap:14px;padding:8px 0}.pie{width:72px;height:72px;border-radius:50%;background:conic-gradient(var(--blue) 0 61%,var(--green) 61% 89%,var(--orange) 89% 100%);flex-shrink:0}.pie-wrap .list{background:none;border:none;box-shadow:none;padding:0;flex:1}.li{display:flex;align-items:center;justify-content:space-between;font-size:12px;padding:3px 0}.li span{color:var(--muted);font-weight:600}.li b{font-family:'Inter',system-ui,sans-serif;font-weight:700}.legend{display:flex;gap:12px;margin-top:8px}.leg{display:flex;align-items:center;gap:4px;font-size:11px;color:var(--muted);font-weight:600}.dot{width:7px;height:7px;border-radius:50%;background:#0A2F6B;flex-shrink:0}.dot.g{background:var(--green)}.dot.o{background:var(--orange)}

.hero{border-radius:28px;padding:20px;color:#fff;box-shadow:0 10px 28px rgba(15,23,42,.08)}.period{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px}.seg button{flex:1;padding:8px 6px;border-radius:14px;border:none;background:transparent;color:rgba(255,255,255,.65);font-size:13px;font-weight:800;cursor:pointer;touch-action:manipulation}.seg button.on{background:rgba(255,255,255,.22);color:#fff}.filter{color:rgba(255,255,255,.7);cursor:pointer}
.kicker{font-size:11px;text-transform:uppercase;letter-spacing:.12em;opacity:.78;font-weight:800;margin-bottom:6px}.amount{font-size:42px;font-weight:900;letter-spacing:-.06em;line-height:1;margin-bottom:4px}.sub{font-size:13px;opacity:.78}
.metrics{margin-top:16px;display:flex;flex-direction:column;gap:8px}.mrow{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.metric{background:rgba(255,255,255,.14);border-radius:14px;padding:10px 8px;text-align:center}.mv{font-family:'Inter',system-ui,sans-serif;font-size:16px;font-weight:800}.mv.good{color:#86EFAC}.mv.bad{color:#FCA5A5}.mv.warn{color:#FDE68A}.ml{font-size:10px;opacity:.72;margin-top:3px;font-weight:700}
.goalbar{height:4px;background:rgba(255,255,255,.2);border-radius:2px;margin-top:16px;overflow:hidden}.goalfill{height:100%;background:rgba(255,255,255,.9);border-radius:2px;transition:width .6s}.goaltext{font-size:11px;opacity:.75;margin-top:6px;font-weight:600}
.chart{background:#fff;border:1px solid var(--line);box-shadow:0 2px 10px rgba(15,23,42,.05);border-radius:24px;padding:16px}.chead{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}.ctitle{font-size:14px;font-weight:700;letter-spacing:-.02em}.mini-tabs{display:flex;gap:4px}.mini-tabs span{font-size:11px;font-weight:700;padding:4px 10px;border-radius:10px;cursor:pointer;color:var(--muted)}.mini-tabs span.on{background:rgba(14,165,233,.1);color:#0A2F6B}
.bars{display:flex;gap:4px;align-items:flex-end;height:80px;padding:0 2px}.baritem{flex:1;display:flex;flex-direction:column;align-items:center;gap:6px}.col{width:100%;background:#0A2F6B;border-radius:6px 6px 0 0;min-height:6px;opacity:.85;transition:height .5s}.col.alt{background:var(--green)}.col.warn{background:var(--orange)}.lbl{font-size:10px;color:var(--muted);font-weight:700}.bars.many .baritem{flex:0 0 calc(10% - 4px)}.bars.medium .baritem{flex:0 0 calc(14.28% - 4px)}

@keyframes spin{from{transform:rotate(0deg)}to{transform:rotate(360deg)}}

/* fin-action buttons (Financeiro screen) */
.fin-actions{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}
.fin-action{border:1px solid var(--line);border-radius:16px;background:#F8FAFC;padding:14px 8px;display:flex;flex-direction:column;align-items:center;gap:8px;font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation;color:var(--text);font-family:var(--font-app)}
.fin-action:active{background:var(--line)}
.fin-action .ico{background:transparent}
@keyframes vspin{from{transform:rotate(0deg)}to{transform:rotate(360deg)}}
@keyframes pulseDot{0%,100%{opacity:.25}50%{opacity:1}}


/* ═══════════════════════════════════════════════════
   AGENDA — CSS isolado (prefixo ag-)
   ═══════════════════════════════════════════════════ */
#sc-agenda{padding:0}
.ag-wrap{padding:12px 16px 24px}
.ag-tabs{height:52px;background:#eaf0f6;border:1px solid #dbe3ec;border-radius:28px;padding:5px;display:flex;margin-bottom:16px}
.ag-tabs button{flex:1;border:0;background:transparent;color:#667085;font-size:14px;font-weight:700;border-radius:23px;cursor:pointer;touch-action:manipulation;font-family:var(--font-app)}
.ag-tabs button.on{background:#fff;color:#101828;box-shadow:0 8px 20px rgba(15,23,42,.08)}
.ag-date-head{display:flex;align-items:center;justify-content:space-between;margin:8px 0 14px}
.ag-date-head h1{font-size:22px;font-weight:800;letter-spacing:-.04em;line-height:1.1}
.ag-date-head p{font-size:12px;font-weight:500;color:#667085;margin-top:4px}
.ag-pill{background:#e6f5ff;color:#2592d9;border:0;border-radius:999px;padding:9px 15px;font-weight:700;font-size:13px;cursor:pointer;touch-action:manipulation;font-family:var(--font-app);white-space:nowrap}
.ag-sec{font-size:11px;letter-spacing:.14em;font-weight:800;color:#6b778c;margin:20px 0 10px;text-transform:uppercase}
.ag-item{background:#fff;border:1px solid rgba(15,23,42,.06);border-radius:24px;padding:14px;box-shadow:0 10px 28px rgba(15,23,42,.06);display:flex;align-items:center;gap:12px;margin-bottom:10px}
.ag-ico{width:48px;height:48px;border-radius:16px;display:flex;align-items:center;justify-content:center;flex:0 0 48px}
.ag-ico svg{width:20px;height:20px;stroke:currentColor;fill:none;stroke-width:2.25;stroke-linecap:round;stroke-linejoin:round}
.ag-ico.green{background:#e9fbef;color:#22c55e}.ag-ico.blue{background:#e8f5ff;color:#0ea5e9}.ag-ico.orange{background:#fff5df;color:#f59e0b}.ag-ico.red{background:#fdecef;color:#ef4444}.ag-ico.purple{background:#f1ecff;color:#8b5cf6}
.ag-info{flex:1;min-width:0}
.ag-ititle{font-size:15px;font-weight:700;letter-spacing:-.02em;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.ag-imeta{font-size:12px;color:#667085;font-weight:500;line-height:1.4;margin-top:3px}
.ag-ivalue{font-size:14px;font-weight:700;color:#344054;white-space:nowrap;flex-shrink:0}
.ag-paybtn{border:0;background:#e9fbef;color:#16a34a;border-radius:999px;padding:8px 12px;font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation;font-family:var(--font-app);flex-shrink:0}
.ag-paybtn.paid{background:#f3f4f6;color:#9ca3af}
.ag-empty{padding:32px 16px;text-align:center;color:#94A3B8;font-size:13px;font-weight:500}
/* Agenda Dia */
.ag-strip{display:flex;gap:8px}
.ag-strip::-webkit-scrollbar{display:none}
.ag-chip{flex:1;min-width:0;height:70px;background:#fff;border:1px solid #e4eaf2;border-radius:20px;box-shadow:0 4px 12px rgba(15,23,42,.045);display:flex;flex-direction:column;align-items:center;justify-content:center;cursor:pointer;touch-action:manipulation;color:#667085}
.ag-chip span{font-size:11px;letter-spacing:.07em;text-transform:uppercase;font-weight:700}
.ag-chip strong{font-size:22px;color:#101828;font-weight:800;margin-top:3px}
.ag-chip.on{background:#0ea5e9;border-color:#0ea5e9;color:#fff}
.ag-chip.on strong{color:#fff}
.ag-chip.today-chip{border-color:#22c55e;color:#22c55e}
.ag-chip.today-chip strong{color:#22c55e}
.ag-jdot{width:5px;height:5px;border-radius:50%;background:#22C55E;margin-top:3px;visibility:hidden}
.ag-chip.has-j .ag-jdot{visibilit
===== orange @ 79286 =====
700}.eheader button{font-size:12px;font-weight:700;color:#0A2F6B;background:none;border:none;cursor:pointer;touch-action:manipulation}
.partner{background:linear-gradient(135deg,#0F172A,#1E293B);border-radius:22px;padding:16px;color:#fff;box-shadow:0 10px 28px rgba(15,23,42,.08)}.eyebrow{font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.1em;opacity:.6;margin-bottom:4px}.pt{font-size:16px;font-weight:800;letter-spacing:-.03em}.ps{font-size:12px;opacity:.65;margin-top:4px}
.pie-wrap{display:flex;align-items:center;gap:14px;padding:8px 0}.pie{width:72px;height:72px;border-radius:50%;background:conic-gradient(var(--blue) 0 61%,var(--green) 61% 89%,var(--orange) 89% 100%);flex-shrink:0}.pie-wrap .list{background:none;border:none;box-shadow:none;padding:0;flex:1}.li{display:flex;align-items:center;justify-content:space-between;font-size:12px;padding:3px 0}.li span{color:var(--muted);font-weight:600}.li b{font-family:'Inter',system-ui,sans-serif;font-weight:700}.legend{display:flex;gap:12px;margin-top:8px}.leg{display:flex;align-items:center;gap:4px;font-size:11px;color:var(--muted);font-weight:600}.dot{width:7px;height:7px;border-radius:50%;background:#0A2F6B;flex-shrink:0}.dot.g{background:var(--green)}.dot.o{background:var(--orange)}

.hero{border-radius:28px;padding:20px;color:#fff;box-shadow:0 10px 28px rgba(15,23,42,.08)}.period{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px}.seg button{flex:1;padding:8px 6px;border-radius:14px;border:none;background:transparent;color:rgba(255,255,255,.65);font-size:13px;font-weight:800;cursor:pointer;touch-action:manipulation}.seg button.on{background:rgba(255,255,255,.22);color:#fff}.filter{color:rgba(255,255,255,.7);cursor:pointer}
.kicker{font-size:11px;text-transform:uppercase;letter-spacing:.12em;opacity:.78;font-weight:800;margin-bottom:6px}.amount{font-size:42px;font-weight:900;letter-spacing:-.06em;line-height:1;margin-bottom:4px}.sub{font-size:13px;opacity:.78}
.metrics{margin-top:16px;display:flex;flex-direction:column;gap:8px}.mrow{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.metric{background:rgba(255,255,255,.14);border-radius:14px;padding:10px 8px;text-align:center}.mv{font-family:'Inter',system-ui,sans-serif;font-size:16px;font-weight:800}.mv.good{color:#86EFAC}.mv.bad{color:#FCA5A5}.mv.warn{color:#FDE68A}.ml{font-size:10px;opacity:.72;margin-top:3px;font-weight:700}
.goalbar{height:4px;background:rgba(255,255,255,.2);border-radius:2px;margin-top:16px;overflow:hidden}.goalfill{height:100%;background:rgba(255,255,255,.9);border-radius:2px;transition:width .6s}.goaltext{font-size:11px;opacity:.75;margin-top:6px;font-weight:600}
.chart{background:#fff;border:1px solid var(--line);box-shadow:0 2px 10px rgba(15,23,42,.05);border-radius:24px;padding:16px}.chead{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}.ctitle{font-size:14px;font-weight:700;letter-spacing:-.02em}.mini-tabs{display:flex;gap:4px}.mini-tabs span{font-size:11px;font-weight:700;padding:4px 10px;border-radius:10px;cursor:pointer;color:var(--muted)}.mini-tabs span.on{background:rgba(14,165,233,.1);color:#0A2F6B}
.bars{display:flex;gap:4px;align-items:flex-end;height:80px;padding:0 2px}.baritem{flex:1;display:flex;flex-direction:column;align-items:center;gap:6px}.col{width:100%;background:#0A2F6B;border-radius:6px 6px 0 0;min-height:6px;opacity:.85;transition:height .5s}.col.alt{background:var(--green)}.col.warn{background:var(--orange)}.lbl{font-size:10px;color:var(--muted);font-weight:700}.bars.many .baritem{flex:0 0 calc(10% - 4px)}.bars.medium .baritem{flex:0 0 calc(14.28% - 4px)}

@keyframes spin{from{transform:rotate(0deg)}to{transform:rotate(360deg)}}

/* fin-action buttons (Financeiro screen) */
.fin-actions{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}
.fin-action{border:1px solid var(--line);border-radius:16px;background:#F8FAFC;padding:14px 8px;display:flex;flex-direction:column;align-items:center;gap:8px;font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation;color:var(--text);font-family:var(--font-app)}
.fin-action:active{background:var(--line)}
.fin-action .ico{background:transparent}
@keyframes vspin{from{transform:rotate(0deg)}to{transform:rotate(360deg)}}
@keyframes pulseDot{0%,100%{opacity:.25}50%{opacity:1}}


/* ═══════════════════════════════════════════════════
   AGENDA — CSS isolado (prefixo ag-)
   ═══════════════════════════════════════════════════ */
#sc-agenda{padding:0}
.ag-wrap{padding:12px 16px 24px}
.ag-tabs{height:52px;background:#eaf0f6;border:1px solid #dbe3ec;border-radius:28px;padding:5px;display:flex;margin-bottom:16px}
.ag-tabs button{flex:1;border:0;background:transparent;color:#667085;font-size:14px;font-weight:700;border-radius:23px;cursor:pointer;touch-action:manipulation;font-family:var(--font-app)}
.ag-tabs button.on{background:#fff;color:#101828;box-shadow:0 8px 20px rgba(15,23,42,.08)}
.ag-date-head{display:flex;align-items:center;justify-content:space-between;margin:8px 0 14px}
.ag-date-head h1{font-size:22px;font-weight:800;letter-spacing:-.04em;line-height:1.1}
.ag-date-head p{font-size:12px;font-weight:500;color:#667085;margin-top:4px}
.ag-pill{background:#e6f5ff;color:#2592d9;border:0;border-radius:999px;padding:9px 15px;font-weight:700;font-size:13px;cursor:pointer;touch-action:manipulation;font-family:var(--font-app);white-space:nowrap}
.ag-sec{font-size:11px;letter-spacing:.14em;font-weight:800;color:#6b778c;margin:20px 0 10px;text-transform:uppercase}
.ag-item{background:#fff;border:1px solid rgba(15,23,42,.06);border-radius:24px;padding:14px;box-shadow:0 10px 28px rgba(15,23,42,.06);display:flex;align-items:center;gap:12px;margin-bottom:10px}
.ag-ico{width:48px;height:48px;border-radius:16px;display:flex;align-items:center;justify-content:center;flex:0 0 48px}
.ag-ico svg{width:20px;height:20px;stroke:currentColor;fill:none;stroke-width:2.25;stroke-linecap:round;stroke-linejoin:round}
.ag-ico.green{background:#e9fbef;color:#22c55e}.ag-ico.blue{background:#e8f5ff;color:#0ea5e9}.ag-ico.orange{background:#fff5df;color:#f59e0b}.ag-ico.red{background:#fdecef;color:#ef4444}.ag-ico.purple{background:#f1ecff;color:#8b5cf6}
.ag-info{flex:1;min-width:0}
.ag-ititle{font-size:15px;font-weight:700;letter-spacing:-.02em;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.ag-imeta{font-size:12px;color:#667085;font-weight:500;line-height:1.4;margin-top:3px}
.ag-ivalue{font-size:14px;font-weight:700;color:#344054;white-space:nowrap;flex-shrink:0}
.ag-paybtn{border:0;background:#e9fbef;color:#16a34a;border-radius:999px;padding:8px 12px;font-size:12px;font-weight:700;cursor:pointer;touch-action:manipulation;font-family:var(--font-app);flex-shrink:0}
.ag-paybtn.paid{background:#f3f4f6;color:#9ca3af}
.ag-empty{padding:32px 16px;text-align:center;color:#94A3B8;font-size:13px;font-weight:500}
/* Agenda Dia */
.ag-strip{display:flex;gap:8px}
.ag-strip::-webkit-scrollbar{display:none}
.ag-chip{flex:1;min-width:0;height:70px;background:#fff;border:1px solid #e4eaf2;border-radius:20px;box-shadow:0 4px 12px rgba(15,23,42,.045);display:flex;flex-direction:column;align-items:center;justify-content:center;cursor:pointer;touch-action:manipulation;color:#667085}
.ag-chip span{font-size:11px;letter-spacing:.07em;text-transform:uppercase;font-weight:700}
.ag-chip strong{font-size:22px;color:#101828;font-weight:800;margin-top:3px}
.ag-chip.on{background:#0ea5e9;border-color:#0ea5e9;color:#fff}
.ag-chip.on strong{color:#fff}
.ag-chip.today-chip{border-color:#22c55e;color:#22c55e}
.ag-chip.today-chip strong{color:#22c55e}
.ag-jdot{width:5px;height:5px;border-radius:50%;background:#22C55E;margin-top:3px;visibility:hidden}
.ag-chip.has-j .ag-jdot{visibility:visible}
.ag-chip.on .ag-jdot{background:#fff}
.ag-head{position:sticky;top:0;z-index:20;background:var(--bg);padding:14px 16px 12px;border-bottom:1px solid rgba(15,23,42,.05)}
.ag-head-row{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}
.ag-month-btn{display:flex;align-items:center;gap:6px;border:0;background:transparent;cursor:pointer;touch-action:manipulation;font-family:var(--font-brand);font-size:19px;font-weight:800;color:var(--text);letter-spacing:-.03em;padding:0}
.ag-month-btn svg{transition:transform .2s}
.ag-month-btn.open svg{transform:rotate(180deg)}
.ag-backdrop{position:absolute;left:-16px;right:-16px;top:100%;height:3000px;background:var(--bg);opacity:0;pointer-events:none;transition:opacity .18s ease;z-index:15}
.ag-backdrop.show{opacity:.98;pointer-events:auto}
.ag-calpanel{position:absolute;left:0;right:0;top:calc(100% + 8px);background:var(--card);border:1px solid rgba(15,23,42,.08);border-radius:22px;box-shadow:0 20px 44px rgba(15,23,42,.16);padding:14px;z-index:21;opacity:0;transform:translateY(-8px);pointer-events:none;transition:opacity .18s ease,transform .18s ease}
.ag-calpanel.show{opacity:1;transform:translateY(0);pointer-events:auto}
.agcal-nav{display:flex;align-items:center;justify-content:space-between;margin:4px 0 8px}
.agcal-nav button{width:34px;height:34px;border-radius:12px;border:1px solid #e4eaf2;background:#fff;font-size:17px;color:#0A2F6B;cursor:pointer;touch-action:manipulation}
.agcal-nav h2{font-size:14px;font-weight:800}
.agcal-legend{display:flex;gap:14px;justify-content:center;padding:0 0 12px;font-size:10.5px;font-weight:600;color:#64748B}
.agcal-legend i{display:inline-block;width:7px;height:7px;border-radius:50%;margin-right:4px}
.ag-body{padding:14px 16px 0}
.ag-day-title{font-size:12px;font-weight:800;color:#6b778c;text-transform:uppercase;letter-spacing:.1em;margin-bottom:10px}
.ag-fab{position:fixed;right:max(18px,calc(50% - 215px + 18px));bottom:calc(var(--bottom) + var(--safe-bottom) + 16px);width:54px;height:54px;border-radius:18px;border:0;background:#0A2F6B;color:#fff;display:flex;align-items:center;justify-content:center;box-shadow:0 10px 26px rgba(10,47,107,.35);cursor:pointer;touch-action:manipulation;
===== orange @ 149129 =====
erflow-x:auto;scrollbar-width:none}
.feed-cp-types::-webkit-scrollbar{display:none}
.feed-t-chip{flex:0 0 auto;display:flex;align-items:center;gap:6px;border-radius:12px;padding:8px 12px;font-size:11.5px;font-weight:800;border:1.5px solid var(--line);background:var(--card);color:var(--muted);white-space:nowrap;cursor:pointer;font-family:var(--font-app)}
.qr-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}
.inf-relatar-loc-chip{display:flex;align-items:center;gap:9px;background:#EFF6FF;border:1.5px solid #BFDBFE;border-radius:14px;padding:12px 12px 12px 13px;margin:0 0 14px}
.inf-city{cursor:pointer;touch-action:manipulation}
.inf-city-search-box{display:flex;align-items:center;gap:8px;background:var(--soft);border-radius:12px;padding:11px 13px;margin-bottom:14px}
.inf-city-search-box input{border:0;background:transparent;outline:none;font-size:13px;flex:1;font-family:inherit;color:var(--text)}
.inf-city-item{display:flex;align-items:center;gap:11px;padding:11px 4px;border-bottom:1px solid #F1F5F9;cursor:pointer;touch-action:manipulation}
.inf-city-item:last-child{border-bottom:0}
.inf-city-item-ic{width:32px;height:32px;border-radius:9px;background:var(--soft);display:flex;align-items:center;justify-content:center;color:var(--muted);flex-shrink:0}
.inf-city-item-name{font-size:12.5px;font-weight:700}
.inf-city-item-uf{font-size:10px;color:var(--muted)}
.inf-city-item.active{background:#EFF6FF;margin:0 -8px;padding:11px 8px;border-radius:10px}
.inf-city-item.active .inf-city-item-name{color:#1E40AF}
.inf-city-item-check{margin-left:auto;color:#2563EB;flex-shrink:0}
.inf-relatar-loc-ic{width:30px;height:30px;border-radius:9px;background:#CCFBF1;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.inf-relatar-loc-txt{flex:1;min-width:0;font-size:13.5px;font-weight:800;color:#1E40AF;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.inf-relatar-loc-btn{flex-shrink:0;border:0;background:#2563EB;color:#fff;font-size:11px;font-weight:800;padding:7px 12px;border-radius:9px;cursor:pointer;touch-action:manipulation}
.qr-btn{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:9px;padding:20px 6px;border-radius:18px;border:0;cursor:pointer;touch-action:manipulation;color:#fff}
.qr-btn svg{width:30px;height:30px}
.qr-label{font-size:12px;font-weight:800;color:#fff}
.back-link{display:flex;align-items:center;gap:5px;font-size:11px;font-weight:700;color:var(--muted);margin:-6px 0 12px;cursor:pointer;touch-action:manipulation}
.inf-tile-grid2{display:grid;grid-template-columns:1fr 1fr;gap:8px}
.inf-tile-grid3{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}
.inf-combo-lbl{font-size:11px;font-weight:800;color:#94A3B8;text-transform:uppercase;letter-spacing:.04em;margin-bottom:8px}
.inf-combo-row{margin-bottom:16px}
.inf-tile{min-height:56px;border-radius:15px;border:1.5px solid var(--line);background:var(--soft);font-size:13px;font-weight:800;text-align:center;cursor:pointer;color:var(--text);display:flex;align-items:center;justify-content:center;padding:8px;touch-action:manipulation;font-family:inherit}
.inf-tile.picked{background:var(--navy);border-color:var(--navy);color:#fff}
.inf-tile-strong{min-height:64px;border-radius:15px;border:0;font-size:13.5px;font-weight:800;text-align:center;cursor:pointer;color:#fff;display:flex;align-items:center;justify-content:center;padding:8px;touch-action:manipulation;font-family:inherit}
.inf-strong-red{background:#DC2626}
.inf-strong-orange{background:#EA580C}
.inf-tbar{display:flex;gap:4px;margin:8px 0 10px}
.inf-tbar-seg{flex:1;height:52px;border-radius:11px;border:0;display:flex;align-items:center;justify-content:center;font-weight:800;font-size:15px;color:#fff;cursor:pointer;touch-action:manipulation;font-family:inherit}
.inf-tbar-labels{display:flex;justify-content:space-between;font-size:10.5px;color:var(--muted);font-weight:700;padding:0 2px}
.feed-t-chip svg{stroke:currentColor}
.feed-cp-loc{margin:6px 16px 0;border:1px solid var(--line);border-radius:14px;padding:11px 13px;display:flex;align-items:center;gap:9px;font-size:12.5px;font-weight:700;color:var(--text);cursor:pointer}
.feed-cp-loc svg{stroke:var(--green2);flex:0 0 auto}
.feed-cp-loc .sub{font-size:10.5px;font-weight:500;color:var(--muted2);margin-top:1px}
.feed-cp-loc.off{opacity:.5}
.feed-cp-tools{display:flex;align-items:center;gap:14px;padding:14px 18px 4px;border-top:1px solid var(--soft-line);margin-top:12px}
.feed-mic{width:36px;height:36px;border-radius:99px;display:flex;align-items:center;justify-content:center;border:1.5px solid var(--line);background:var(--card);cursor:pointer}
.feed-mic svg{stroke:var(--blue)}
.feed-mic.rec{background:var(--red);border-color:var(--red);position:relative}
.feed-mic.rec svg{stroke:#fff}
.feed-mic.rec::after{content:"";position:absolute;inset:-6px;border-radius:99px;border:2px solid rgba(239,68,68,.45);animation:feedPulse 1.4s ease-out infinite}
@keyframes feedPulse{0%{transform:scale(.85);opacity:1}100%{transform:scale(1.35);opacity:0}}
.feed-cp-count{margin-left:auto;font-size:11px;font-weight:700;color:var(--muted2)}
.feed-rec-bar{display:flex;align-items:center;gap:10px;margin:8px 16px 0;background:var(--redBg);border:1px solid #FECACA;border-radius:14px;padding:9px 13px}
.feed-rec-dot{width:8px;height:8px;border-radius:99px;background:var(--red);animation:feedBlink 1s ease-in-out infinite}
@keyframes feedBlink{0%,100%{opacity:1}50%{opacity:.25}}
.feed-rec-txt{font-size:11.5px;font-weight:800;color:#B91C1C}
@media (prefers-reduced-motion: reduce){.feed-mic.rec::after,.feed-rec-dot{animation:none}}
/* Comentários */
.feed-cm-post{border-bottom:6px solid var(--bg)}
.feed-cm-reply{display:flex;gap:10px;padding:12px 16px 10px 26px;border-bottom:1px solid var(--soft-line)}
.feed-cm-reply .feed-av{width:30px;height:30px;font-size:11px}
.feed-cm-bar{position:sticky;bottom:0;display:flex;align-items:center;gap:10px;padding:10px 12px;border-top:1px solid var(--line);background:var(--card)}
.feed-cm-input{flex:1;background:var(--bg);border:1px solid var(--line);border-radius:99px;padding:10px 15px;font-size:13px;color:var(--text);outline:none;font-family:var(--font-app)}
.feed-cm-input::placeholder{color:var(--muted2)}
.feed-cm-send{width:38px;height:38px;border-radius:99px;background:var(--green);display:flex;align-items:center;justify-content:center;flex:0 0 auto;border:none;cursor:pointer}
.feed-cm-send svg{stroke:#fff}
</style>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.112.3" integrity="sha384-l8ah+VgaWtk1mvOe9VC+OirC6qHFF4yH7l7mKRidV9MSti3E9F463bMp6ZVN4kuC" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/jszip@3.10.1/dist/jszip.min.js" integrity="sha384-+mbV2IY1Zk/X1p/nWllGySJSUN8uMs+gUAN10Or95UBH0fpj6GfKgPmgC5EXieXG" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/papaparse@5.4.1/papaparse.min.js" integrity="sha384-D/t0ZMqQW31H3az8ktEiNb39wyKnS82iFY52QPACM+IjKW3jDUhyIgh2PApRqJZs" crossorigin="anonymous"></script>
  <script>
    // Ambiente de teste (Cloudflare Pages sempre publica em *.pages.dev,
    // seja a URL de produção do projeto ou o link de preview de uma branch)
    // fala com um banco Supabase separado do de produção — assim nenhum
    // teste grava dado real de motorista. Regra "por enquanto": revisar
    // quando a produção ganhar um domínio próprio no Cloudflare.
    const _MOB_IS_TEST_ENV = /\.pages\.dev$/i.test(location.hostname);
    const _MOB_SUPA_CONFIG = _MOB_IS_TEST_ENV ? {
      url: 'https://wrmgmsmpszsdmntketfq.supabase.co',
      key: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6IndybWdtc21wc3pzZG1udGtldGZxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODc0MTMzNDQsImV4cCI6MjEwMjk4OTM0NH0.OMZliNHYTj_R6HhHI9yizWAzREMGFfv2_yHk4s6z9N0'
    } : {
      url: 'https://jlsrpptslwfhmkvelaro.supabase.co',
      key: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Impsc3JwcHRzbHdmaG1rdmVsYXJvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM4NjYxNzIsImV4cCI6MjA4OTQ0MjE3Mn0.4gD4dKx05QaOAAkY1gAx2HuH_CN31Xg3kkDMdvZ4kh0'
    };
    console.info('[MōB] ambiente:', _MOB_IS_TEST_ENV ? 'TESTE (mob-teste)' : 'produção');
    const _SUPA = supabase.createClient(
      _MOB_SUPA_CONFIG.url,
      _MOB_SUPA_CONFIG.key,
      {
        auth: {
          persistSession: true,
          autoRefreshToken: true,
          detectSessionInUrl: false,
          storage: window.localStorage
        }
      }
    );
  </script>
<link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin><link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Poppins:wght@600;700;800;900&display=swap" rel="stylesheet"></head>
<body style="background:#0F172A">
<div class="app">
<!-- ════ TOPBAR (fixed, shared) ════ -->
<!-- ════ SCROLL WRAPPER ════ -->
<div class="scroll" id="main-scroll">

<!-- ══ TELA LOGIN ══════════════════════════════════════════════════════════ -->
<div id="sc-login" style="display:none;position:fixed;inset:0;z-index:9999;flex-direction:column;align-items:center;justify-content:center;padding:32px 24px;background:#F8FAFC;overflow-y:auto">

  <div style="background:#0A2F6B;width:calc(100% + 48px);margin:-32px -24px 28px;padding:44px 24px 32px;display:flex;flex-direction:column;align-items:center;border-radius:0 0 28px 28px">
    <img src="data:image/png;base64,
===== orange @ 415207 =====
="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
        <span id="fin-range-txt"></span>
        <button onclick="event.stopPropagation();clearFinFilter()">&#x2715;</button>
      </div>
      <div class="heroGrid">
        <div class="heroCell"><strong id="fin-pct-lucro">0%</strong><span>% lucro</span></div>
        <div class="heroCell"><strong id="fin-media">R$ 0</strong><span>média dia</span></div>
        <div class="heroCell"><strong id="fin-vs-ant">—</strong><span id="fin-vs-ant-lbl">vs mês ant.</span></div>
      </div>
    </section>
    <section class="card cardPad" style="margin-bottom:14px">
      <div class="cardHead"><h2>Resumo</h2><div class="seg"><button class="on" id="fs-all" onclick="setFinScope('all')">Tudo</button><button id="fs-prof" onclick="setFinScope('prof')">Prof.</button><button id="fs-pes" onclick="setFinScope('pes')">Pessoal</button></div></div>
      <div class="fin-metrics">
        <div class="fin-metric g"><span>Corridas</span><strong id="fin-corr">R$ 0,00</strong><small id="fin-corr-sub">&nbsp;</small></div>
        <div class="fin-metric g"><span>Receitas extras</span><strong id="fin-rec">R$ 0,00</strong><small>gorjetas, b&#244;nus...</small></div>
        <div class="fin-metric r"><span>Despesas</span><strong id="fin-desp">R$ 0,00</strong><small id="fin-desp-sub">&nbsp;</small></div>
        <div class="fin-metric"><span>Lucro</span><strong id="fin-lucro">R$ 0,00</strong><small id="fin-lucro-sub">&nbsp;</small></div>
      </div>
    </section>
    <section class="card cardPad" style="margin-bottom:14px">
      <div class="cardHead"><h2>Objetivos</h2></div>
      <div class="obj-switch">
        <button class="obj-btn active" id="ob-rec" onclick="setObjTab('rec')">Receitas</button>
        <button class="obj-btn" id="ob-desp" onclick="setObjTab('desp')">Despesas</button>
      </div>
      <div id="obj-rec-list"></div>
      <div id="obj-desp-list" style="display:none"></div>
      <div class="cfg-link" onclick="openObjConfig()">Configurar objetivos</div>
    </section>
    <section class="card cardPad" style="margin-bottom:14px">
      <div class="cardHead"><h2>Fluxo</h2></div>
      <div class="fin-flow" id="fin-flow"></div>
      <div class="fin-flow-legend">
        <span><i style="background:#22C55E"></i>Entradas (com corridas)</span>
        <span><i style="background:#FCA5A5"></i>Sa&#237;das</span>
      </div>
    </section>
    <section class="card cardPad" style="margin-bottom:14px">
      <div class="cardHead"><h2>Ações rápidas</h2></div>
      <div class="fin-actions">
        <button class="fin-action" onclick="openLanc('receita')"><div class="ico green"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12h14"/></svg></div>Receita</button>
        <button class="fin-action" onclick="openLanc('despesa')"><div class="ico red"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12h14"/></svg></div>Despesa</button>
        <button class="fin-action" onclick="navTo('lancamentos')"><div class="ico purple"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 19V5a2 2 0 0 1 2-2h12v18H6a2 2 0 0 1-2-2Z"/></svg></div>Histórico</button>
        <button class="fin-action" onclick="navTo('relatorios')"><div class="ico orange"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 3v18h18"/><path d="m19 9-5 5-4-4-4 4"/></svg></div>Relatório</button>
      </div>
    </section>
    <section class="card" style="margin-bottom:14px">
      <div class="cardPad cardHead"><h2>Últimas movimentações</h2><button onclick="navTo('lancamentos')">Ver tudo</button></div>
      <div id="fin-mov"><div class="empty">Nenhuma movimentação</div></div>
    </section>
    <div style="height:8px"></div>
  </div>
</div>

<!-- ══ TELA RELATÓRIOS (placeholder — visão geral + alertas vêm na sequência) ══ -->
<div class="screen" id="sc-relatorios">
  <div style="display:flex;align-items:center;gap:10px;padding:14px 16px 6px">
    <button onclick="navTo('financeiro')" style="width:32px;height:32px;border-radius:10px;background:#F1F5F9;border:0;display:flex;align-items:center;justify-content:center;color:#0F172A;cursor:pointer;flex-shrink:0"><svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M19 12H5M12 19l-7-7 7-7"/></svg></button>
    <h2 style="font-size:17px;letter-spacing:-.02em;flex:1">Relatórios</h2>
  </div>
  <div style="padding:0 16px">
    <div class="ad-carousel" id="ad-carousel-0"></div>
  </div>
  <div class="hg-info-tip" id="hg-info-tip"></div>
  <section class="hg-period-card" style="margin:12px 16px 0">
    <div class="hg-period-top">
      <div class="hg-period-tabs">
        <button id="hg-tab-w" class="active" onclick="setHomePeriod('w')">Semana</button>
        <button id="hg-tab-m" onclick="setHomePeriod('m')">Mês</button>
        <button id="hg-tab-y" onclick="setHomePeriod('y')">Ano</button>
      </div>
      <button class="hg-filter-btn" onclick="openHgFilter()"><svg viewBox="0 0 24 24"><path d="M3 6h18"/><path d="M7 12h10"/><path d="M10 18h4"/></svg></button>
    </div>
    <div class="hg-main-value">
      <label>Faturamento</label>
      <h1 id="hg-amount">R$ 0</h1>
      <p id="hg-sub">0 corridas · 0,0 km</p>
    </div>
    <div class="hg-insight" id="hg-insight" style="display:none">
      <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;opacity:.5;margin-top:2px"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
      <p id="hg-insight-txt"></p>
    </div>
    <div class="hg-metrics">
      <div class="hg-metric"><strong id="hg-rpkm">—</strong><span>R$/Km</span></div>
      <div class="hg-metric"><strong id="hg-rphr">—</strong><span>R$/Hora</span></div>
      <div class="hg-metric"><strong id="hg-trips">0</strong><span>Corridas</span></div>
      <div class="hg-metric"><strong id="hg-exp">R$ 0</strong><span>Despesas</span></div>
      <div class="hg-metric"><strong id="hg-profit">R$ 0</strong><span>Lucro</span></div>
      <div class="hg-metric"><strong id="hg-pct">0%</strong><span>% Lucro</span></div>
    </div>
    <div class="hg-goals" id="hg-goals"></div>
  </section>

  <section class="hg-chart-card" style="margin:14px 16px 0">
    <div class="hg-chart-head"><div><h2>Dia/Semana <button class="hg-info-btn" onclick="showChartInfo(this,'Soma por dia da semana, dentro do período selecionado acima.')">?</button></h2></div>
      <div class="hg-mini-tabs"><button class="active" id="hg-dia-d" onclick="setHgDiaMode('d')">Dia</button><button id="hg-dia-w" onclick="setHgDiaMode('w')">Semana</button></div>
    </div>
    <p class="hg-chart-highlight" id="hg-dia-insight"></p>
    <div class="hg-bar-chart" id="hg-chart-dia"></div>
  </section>

  <section class="hg-chart-card" style="margin:14px 16px 0">
    <div class="hg-chart-head"><div><h2>Hora/Turno <button class="hg-info-btn" onclick="showChartInfo(this,'Soma por horário, dentro do período selecionado acima.')">?</button></h2></div>
      <div class="hg-mini-tabs"><button class="active" id="hg-hora-h" onclick="setHgHoraMode('h')">Hora</button><button id="hg-hora-t" onclick="setHgHoraMode('t')">Turno</button></div>
    </div>
    <p class="hg-chart-highlight" id="hg-hora-insight"></p>
    <div class="hg-bar-chart hour" id="hg-chart-hora"></div>
  </section>

  <div class="ad-carousel" id="ad-carousel-1"></div>

  <section class="hg-chart-card" style="margin:14px 16px 0">
    <div class="hg-chart-head"><div><h2>Mês/Ano <button class="hg-info-btn" onclick="showChartInfo(this,'Soma por mês/ano, dentro do período selecionado acima — com Semana ou Mês escolhido aqui em cima, vira 1 barra só.')">?</button></h2></div>
      <div class="hg-mini-tabs"><button class="active" id="hg-mes-m" onclick="setHgMesMode('m')">Mês</button><button id="hg-mes-y" onclick="setHgMesMode('y')">Ano</button></div>
    </div>
    <div class="hg-bar-chart month" id="hg-chart-mes"></div>
  </section>

  <section class="hg-chart-card" style="margin:14px 16px 0">
    <div class="hg-chart-head"><div><h2>Plataformas <button class="hg-info-btn" onclick="showChartInfo(this,'Distribuição por app ou origem.')">?</button></h2></div></div>
    <div class="hg-pie-wrap">
      <div class="hg-pie" id="hg-pie-plat"></div>
      <div class="hg-pie-list" id="hg-pie-plat-list"></div>
    </div>
  </section>

  <section class="hg-chart-card" style="margin:14px 16px 0">
    <div class="hg-chart-head"><div><h2>Receitas x Despesas <button class="hg-info-btn" onclick="showChartInfo(this,'Distribuição por categoria conforme o tipo selecionado.')">?</button></h2></div></div>
    <div class="hg-category-tabs"><button class="active" id="hg-rd-receita" onclick="setHgRevDesp('receita')">Receitas</button><button id="hg-rd-despesa" onclick="setHgRevDesp('despesa')">Despesas</button></div>
    <div class="hg-pie-wrap">
      <div class="hg-pie" id="hg-pie-cat"></div>
      <div class="hg-pie-list" id="hg-pie-cat-list"></div>
    </div>
    <div class="hg-category-panel" id="hg-cat-panel"></div>
  </section>
  <section class="entries" style="margin:14px 16px 0">
    <div class="eheader"><h3>Últimos lançamentos</h3><button onclick="navTo('lancamentos')">Ver tudo</button></div>
    <div id="home-entries"><div class="empty">Nenhum lançamento ainda</div></div>
  </section>
  <div style="height:8px"></div>
</div>

<!-- ══ TELA LANÇAMENTOS ═══════════════════════════════════════════════════ -->
<div class="screen" id="sc-lancamentos">
  <div style="padding:8px 16px 14px"
===== orange @ 450442 =====
10px">Ferramentas</div></div>
  <div style="padding:0 16px;display:flex;flex-direction:column;gap:8px">
    <div class="mais-item" onclick="navTo('corridas')"><div class="mais-ico" style="background:#EFF6FF"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="#2563EB" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M9 21V9"/></svg></div><div><div class="mais-t">Corridas</div><div class="mais-s">Histórico e filtros de corridas</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="navTo('relatorios')"><div class="mais-ico" style="background:#FFF7ED"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="#F59E0B" stroke-width="2"><path d="M3 3v18h18"/><path d="m19 9-5 5-4-4-4 4"/></svg></div><div><div class="mais-t">Relatório</div><div class="mais-s">Gráficos, metas e visão geral do período</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="navTo('agenda')"><div class="mais-ico blue"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18"/></svg></div><div><div class="mais-t">Agenda</div><div class="mais-s">Planejamento e horários de trabalho</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="openFlashConfig()"><div class="mais-ico" style="background:#F0FDF4"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="#16A34A" stroke-width="2"><path d="M13 2 4 14h6l-1 8 9-12h-6l1-8z"/></svg></div><div><div class="mais-t">MōB Flash</div><div class="mais-s">Nota da oferta em tempo real</div></div><div class="mais-arr">›</div></div>
  </div>
  <div style="padding:16px 16px 8px"><div style="font-size:13px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:.06em;margin-bottom:10px">Configurações</div></div>
  <div style="padding:0 16px;display:flex;flex-direction:column;gap:8px">
    <div class="mais-item" onclick="openPerfil()"><div class="mais-ico blue"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21a8 8 0 0 0-16 0"/><circle cx="12" cy="7" r="4"/></svg></div><div><div class="mais-t">Perfil</div><div class="mais-s" id="mais-perfil-sub">Configure nome, placa e veículo</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="openConfig()"><div class="mais-ico green"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1Z"/></svg></div><div><div class="mais-t">Configurações</div><div class="mais-s">Ícone flutuante, modo escuro e mais</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="navTo('categorias')"><div class="mais-ico orange"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 7h16M4 12h16M4 17h8"/></svg></div><div><div class="mais-t">Categorias</div><div class="mais-s">Gerencie categorias de despesa</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="openCustoVeiculo()"><div class="mais-ico green"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 17h14M6 17l1-6 3-4h4l3 4 1 6"/><circle cx="7.5" cy="17" r="1.5"/><circle cx="16.5" cy="17" r="1.5"/></svg></div><div><div class="mais-t">Custo do veículo</div><div class="mais-s" id="mais-custo-sub">Configure pra ver o lucro líquido real</div></div><div class="mais-arr">›</div></div>
    <div class="mais-item" onclick="openSuporte()"><div class="mais-ico purple"><svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M9.1 9a3 3 0 0 1 5.8 1c0 2-3 3-3 3M12 17h.01"/></svg></div><div><div class="mais-t">Suporte</div><div class="mais-s">Ajuda e feedback</div></div><div class="mais-arr">›</div></div>
  </div>
  <div style="height:24px"></div>
</div>

</div><!-- /scroll -->

<!-- ════ NAV ════ -->
<nav class="bottom" id="app-nav" style="display:flex">
  <div class="nav active" id="bn-home" onclick="navTo('home')">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m3 10 9-7 9 7v10a1 1 0 0 1-1 1h-5v-6H9v6H4a1 1 0 0 1-1-1z"/></svg><span>Início</span>
  </div>
  <div class="nav" id="bn-agenda" onclick="navTo('informes')">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m3 11 18-5v12L3 13"/><path d="M11.6 16.8a3 3 0 1 1-5.8-1.6"/></svg><span>Informes</span>
  </div>
  <div class="center" id="bn-jornada">
    <div class="start" id="gps-btn" onclick="navTo('jornada')">
      <img id="gps-icon-img" src="data:image/png;base64," alt="" style="width:40px;height:40px;object-fit:contain;pointer-events:none">
      <span id="gps-lbl" style="font-size:9px;font-weight:800;text-transform:uppercase;letter-spacing:.04em;color:white;line-height:1">Jornada</span>
    </div>
  </div>
  <div class="nav" id="bn-fav" onclick="navToFav()">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" id="fav-icon"><path d="M12 3v18M17 7H9.5a3.5 3.5 0 0 0 0 7H14a3.5 3.5 0 0 1 0 7H6"/></svg>
    <span id="fav-lbl">Finanças</span>
  </div>
  <div class="nav" id="bn-mais" onclick="navTo('mais')">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M4 7h16M4 12h16M4 17h16"/></svg><span>Menu</span>
  </div>
</nav>


<!-- ════ MODAIS ════ -->

<!-- Informes: trocar cidade (16/08/2026, pedido do Yuri) -->
<div class="mo" id="mo-inf-cidade">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2>Escolher cidade</h2><button class="xbtn" onclick="closeMo('mo-inf-cidade')">&#x2715;</button></div>
    <div class="inf-city-search-box">
      <svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="#94A3B8" stroke-width="2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
      <input id="inf-city-search-input" placeholder="Buscar cidade…" oninput="infBuscarCidade(this.value)">
    </div>
    <div id="inf-city-list"></div>
  </div>
</div>

<!-- Informes: Relatar (seleção de tipo — sem texto livre, pedido do Yuri
     16/08/2026. Fluxo com etapas de detalhe por tipo, aprovado.) -->
<div class="mo" id="mo-inf-relatar">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head">
      <h2 id="inf-relatar-title">Informe</h2>
      <button class="xbtn" onclick="closeMo('mo-inf-relatar')">&#x2715;</button>
    </div>
    <div id="inf-relatar-back" class="back-link" style="display:none" onclick="infRelatarBack()">
      <svg viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M15 18l-6-6 6-6"/></svg>
      <span id="inf-relatar-back-lbl"></span>
    </div>
    <div id="inf-relatar-loc" class="inf-relatar-loc-chip">
      <div class="inf-relatar-loc-ic" id="inf-relatar-loc-ic"><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#14B8A6" stroke-width="2.3"><circle cx="12" cy="12" r="4"/></svg></div>
      <span id="inf-relatar-loc-txt" class="inf-relatar-loc-txt">Localizando…</span>
      <button class="inf-relatar-loc-btn" onclick="infRelatarAlterarLocal()">Alterar</button>
    </div>
    <div id="inf-relatar-body"></div>
  </div>
</div>

<div class="mo" id="mo-inf-colaborar">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head">
      <h2>Colabore com a comunidade</h2>
      <button class="xbtn" onclick="closeMo('mo-inf-colaborar')">&#x2715;</button>
    </div>
    <div style="font-size:11.5px;color:var(--muted);margin:-6px 0 4px;line-height:1.5" id="inf-colab-sub"></div>
    <div id="inf-colab-body"></div>
  </div>
</div>

<!-- Feed: Composer (publicar alerta) -->
<div class="mo" id="mo-feed-composer">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head"><h2>Novo alerta</h2><button class="xbtn" onclick="feedCloseComposer()">&#x2715;</button></div>
    <div class="feed-cp-body">
      <span id="feed-cp-avatar"></span>
      <textarea class="feed-cp-input" id="feed-cp-text" maxlength="180" placeholder="O que está acontecendo na rua? (opcional)" oninput="feedCpCount()"></textarea>
    </div>
    <div class="feed-cp-types" i
===== orange @ 542980 =====
lass="pct">${seg.pct}%</span><span class="stempo">${fmtDur(seg.sec/60)}</span><span class="skm">${seg.km.toFixed(1)} km</span></div>`;
  }).join('');
}

function toggleBar(listId) {
  const el = document.getElementById(listId);
  if (el) el.classList.toggle('open');
}

// ── DEFAULT CATEGORIES ────────────────────────────────────────
const DEFAULT_CATS = {
  despesa_operacional: [
    {id:'comb', l:'Combustível', icon:'fuel'},
    {id:'alim', l:'Alimentação', icon:'food'},
    {id:'man',  l:'Manutenção', icon:'wrench'},
    {id:'park', l:'Estacionamento', icon:'parking'},
    {id:'ped',  l:'Pedágio', icon:'road'},
    {id:'lav',  l:'Lavagem', icon:'car'},
    {id:'seg',  l:'Seguro', icon:'card'},
    {id:'out',  l:'Outros', icon:'plus'}
  ],
  despesa_pessoal: [
    {id:'alug', l:'Aluguel', icon:'home'},
    {id:'merc', l:'Mercado', icon:'card'},
    {id:'alim', l:'Alimentação', icon:'food'},
    {id:'cart', l:'Cartão', icon:'card'},
    {id:'trans', l:'Transporte', icon:'car'},
    {id:'saude', l:'Saúde', icon:'plus'},
    {id:'lazer', l:'Lazer', icon:'gift'},
    {id:'out',  l:'Outros', icon:'plus'}
  ],
  receita_operacional: [
    {id:'gort', l:'Gorjeta', icon:'coin'},
    {id:'bon',  l:'Bônus', icon:'gift'},
    {id:'reemb', l:'Reembolso', icon:'card'},
    {id:'serv', l:'Serviço extra', icon:'briefcase'},
    {id:'part', l:'Particular', icon:'user'},
    {id:'inc',  l:'Incentivo', icon:'plus'}
  ],
  receita_pessoal: [
    {id:'sal',  l:'Salário', icon:'briefcase'},
    {id:'rend', l:'Renda extra', icon:'coin'},
    {id:'pix',  l:'Pix recebido', icon:'card'},
    {id:'reemb', l:'Reembolso', icon:'card'},
    {id:'pres', l:'Presente', icon:'gift'},
    {id:'out',  l:'Outros', icon:'plus'}
  ]
};

// ── GLOBAL STATE ──────────────────────────────────────────────
let S = {
  sec: 'home',
  homePeriod: 'w',
  hgCustomStart: null, hgCustomEnd: null,
  hgDiaMode: 'd', hgHoraMode: 'h', hgMesMode: 'm', hgRevDesp: 'receita',
  finPer: 'month',
  finScope: 'all',
  lancFilter: 'all',
  lancStatus: 'all',
  lancPeriod: '7d',
  lancDate: null,
  curDate: DB.today(),
  lancType: 'receita',
  lancScope: 'operacional',
  lancRecFreq: 'Semanal',
  corPlat: 'uber',
  corridasPeriod: 'hoje', corridasDate: null,
  corridasPlat: 'all',
  tripsExpanded: false,
  sessionsExpanded: false,
  lancExpanded: false,
  _midnightIsPostStop: false,
  _postStopMidnightSess: null,
  _postMidnightCallback: null,
  _midnightModalOpen: false,
  gps: {
    st: 'idle',  // idle | running
    start: null, pausedMs: 0, pauseAt: null,
    km: 0, pts: [], wid: null, ti: null,
    forceDate: null // override manual (opção 'deixar em ontem' na virada de meia-noite)
  }
};

// ── MODULES (for Mais screen) ─────────────────────────────────
const MODULES = [
  {id:'financeiro', label:'Financeiro', sub:'Gestão e análise financeira', color:'blue',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 3v18M17 7H9.5a3.5 3.5 0 0 0 0 7H14a3.5 3.5 0 0 1 0 7H6"/></svg>'},
  {id:'planejar-jornada', label:'Planejar jornada', sub:'Defina dias, metas e horários de trabalho', color:'green',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18M8 14h.01M12 14h.01M16 14h.01M8 18h.01M12 18h.01"/></svg>'},
  {id:'lancamentos', label:'Lançamentos', sub:'Histórico de receitas e despesas', color:'orange',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 19V5a2 2 0 0 1 2-2h12v18H6a2 2 0 0 1-2-2Z"/><path d="M8 7h8M8 11h8M8 15h5"/></svg>'},
];

const MODULE_COLORS = {blue:'#EFF6FF', purple:'#F5F3FF', green:'#ECFDF3', orange:'#FFF7ED'};
const MODULE_ICONS_COLOR = {blue:'#0A2F6B', purple:'#7C3AED', green:'#22C55E', orange:'#F59E0B'};

// ── NAVIGATION ────────────────────────────────────────────────
function navTo(sec) {
  // Ocultar/mostrar nav na tela de Informes (o painel usa z-index alto mas o cat-row precisa da nav escondida)
  const bottomNav = document.querySelector('.bottom');
  if (bottomNav) bottomNav.style.display = 'flex';
  document.querySelectorAll('.screen').forEach(s => {
    s.classList.remove('on');
    s.style.display = '';  // limpa inline style para CSS funcionar
  });
  document.querySelectorAll('.nav').forEach(n => n.classList.remove('active'));
  const sc = document.getElementById('sc-'+sec);
  if (sc) { sc.classList.add('on'); sc.style.display = 'block'; }
  // Map sub-screens to their nav parent
  const navMap = {financeiro:'fav', lancamentos:'fav', relatorios:'fav', 'planejar-jornada':'mais', corridas:'jornada', agenda:'mais', informes:'agenda', categorias:'mais'};
  const navId = navMap[sec] || sec;
  const bn = document.getElementById('bn-'+navId);
  if (bn) bn.classList.add('active');
  S.sec = sec;
  try { localStorage.setItem('sm_last_screen', JSON.stringify({sec, ts: Date.now()})); } catch(e) {}
  const ms = document.getElementById('main-scroll');
  if (ms) ms.scrollTop = 0;
  if (sec === 'home')       renderFeedHome();
  if (sec === 'relatorios') renderHome();
  if (sec === 'jornada') {
    renderJornada(); // render imediato com dado local (nunca trava esperando rede)
    updateNetStatus();
    if (_supaUser && navigator.onLine) {
      reconcileJornadaHoje().then(() => renderJornada()).catch(()=>{});
      // Se o app local acha que está idle mas existe jornada aberta no
      // Supabase há mais de 15min (provável órfã), fecha automaticamente
      if (S.gps.st === 'idle') {
        _closeOrphanSessions().then(n => { if (n) renderJornada(); }).catch(()=>{});
      }
    }
  }
  if (sec === 'financeiro') renderFin();
  if (sec === 'lancamentos') renderLancamentos();
  if (sec === 'corridas') {
    S.corridasPlats = new Set(); // reseta sempre — nenhum filtro marcado = mostra tudo
    document.getElementById('cor-plat-uber')?.classList.remove('active');
    document.getElementById('cor-plat-99')?.classList.remove('active');
    renderCorridasActive();
  }
  if (sec === 'mais')       renderMais();
  if (sec === 'categorias') renderCatsModal();
  if (sec === 'agenda')     { renderAgenda(); agScrollToFirst(); }
  if (sec === 'informes')   renderInformes();
}
function openSuporte() {
  const el = document.getElementById('suporte-version');
  if (el) {
    const v = window.SmartMobiNative?.getVersion?.();
    el.textContent = v ? ('App nativo v' + v) : 'Acessando pelo navegador (sem app nativo)';
  }
  openMo('mo-suporte');
}
function navToFav() {
  const cfg = DB.cfg();
  navTo(cfg.favModule || 'financeiro');
}
function openMo(id) { const el = document.getElementById(id); if (el) el.classList.add('on'); }
function closeMo(id) { const el = document.getElementById(id); if (el) el.classList.remove('on'); }
document.addEventListener('click', e => {
  if (e.target.classList.contains('mo')) e.target.classList.remove('on');
});

// ── MAIS SCREEN ───────────────────────────────────────────────
function renderMais() {
  const vEl = document.getElementById("app-code-ver");
  if (vEl) vEl.textContent = typeof _APP_CODE_VER !== "undefined" ? _APP_CODE_VER : "?";

  const cfg = DB.cfg();
  const favId = cfg.favModule || 'financeiro';
  const container = document.getElementById('mais-modules');
  if (!container) return;
  container.innerHTML = MODULES.map(m => `
    <div style="background:#fff;border-radius:20px;border:1px solid #E2E8F0;box-shadow:0 2px 10px rgba(15,23,42,.05);padding:14px 16px;display:flex;align-items:center;gap:12px;cursor:pointer" onclick="navTo('${m.id}')">
      <div style="width:42px;height:42px;border-radius:14px;background:${MODULE_COLORS[m.color]};display:flex;align-items:center;justify-content:center;color:${MODULE_ICONS_COLOR[m.color]};flex-shrink:0">${m.icon}</div>
      <div style="flex:1"><div style="font-size:14px;font-weight:700">${m.label}</div><div style="font-size:12px;color:#64748B;margin-top:2px">${m.sub}</div></div>
      <button onclick="event.stopPropagation();setFavModule('${m.id}')" style="border:none;background:${m.id===favId?MODULE_COLORS[m.color]:'transparent'};border-radius:8px;padding:6px;cursor:pointer;color:${m.id===favId?MODULE_ICONS_COLOR[m.color]:'#94A3B8'};display:flex;align-items:center;justify-content:center" title="Fixar no menu">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="${m.id===favId?'currentColor':'none'}" stroke="currentColor" stroke-width="2"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
      </button>
    </div>`
  ).join('');
  // Update perfil sub
  const sub = document.getElementById('mais-perfil-sub');
  if (sub && cfg.name) sub.textContent = cfg.name + (cfg.vehicle ? ' · ' + cfg.vehicle : '');
  const custoSub = document.getElementById('mais-custo-sub');
  if (custoSub) {
    custoSub.textContent = cfg.custoVeiculo?.configurado
      ? DB.fRI(cfg.custoVeiculo.custoPorKm) + '/km configurado'
      : 'Configure pra ver o lucro líquido real';
  }
}
function setFavModule(id) {
  const cfg = DB.cfg(); cfg.favModule = id; DB.saveCfg(cfg);
  updateFavBtn();
  renderMais();
}
function updateFavBtn() {
  const cfg = DB.cfg();
  const mod = MODULES.find(m => m.id === (cfg.favModule || 'financeiro')) || MODULES[0];
  const lblEl = document.getElementById('fav-lbl');
  const icoEl = document.getElementById('fav-icon');
  if (lblEl) lblEl.textContent = mod.label;
  if (icoEl) icoEl.outerHTML = mod.icon.replace('<svg', '<svg id="fav-icon"');
}

// ── HOME ──────────────────────────────────────────────────────
function setHomePeriod(p) {
  S.homePeriod = p;
  ['w','m','y'].forEach(x => {
    const el = document.getElementById('hg-tab-'+x);
    if (el) el.classList.toggle('active', x === p);
  });
  renderHomeGraphs();
}
function openHgFilter() {
  const sEl = document.getElementById('hg-filter-start'), eEl = document.getElementById('hg-fil
===== orange @ 1714168 =====
elatar-back-lbl').textContent = label;
}
function infRelatarBack(){ if (_infRelState.backFn) _infRelState.backFn(); }

function infRelatarSelType(key){
  _infRelState.type = key;
  if (key === 'transito') return infRelatarShowTransito();
  if (key === 'blitz') return infRelatarShowPoliciamento();
  if (key === 'risco') return infRelatarShowAlertaStep1();
}

// Trânsito — barra 0 a 5, 1 toque publica.
function infRelatarShowTransito(){
  document.getElementById('inf-relatar-title').textContent = 'Como tá o trânsito?';
  infRelatarShowBack('Trânsito', infOpenRelatar);
  const cores = ['#DC2626','#EF4444','#F59E0B','#EAB308','#84CC16','#22C55E'];
  document.getElementById('inf-relatar-body').innerHTML =
    `<div class="inf-tbar">${cores.map((c,i)=>`<button class="inf-tbar-seg" style="background:${c}" onclick="infRelatarPublish({nivel:${i}})">${i}</button>`).join('')}</div>
     <div class="inf-tbar-labels"><span>Parado</span><span>Livre</span></div>`;
}

// Policiamento — "quem" (2 col) + "fazendo o quê" (3 col) na mesma tela;
// publica sozinho assim que os dois têm uma opção marcada.
function infRelatarShowPoliciamento(){
  document.getElementById('inf-relatar-title').textContent = 'Fiscalização';
  infRelatarShowBack('Fiscalização', infOpenRelatar);
  _infRelState.orgao = null; _infRelState.acao = null;
  infRelatarRenderPoliciamento();
}
function infRelatarRenderPoliciamento(){
  const orgaos = [['Polícia Militar','pm'],['Guarda Municipal','gm'],['Lei Seca','leiseca'],['Detro','detro']];
  const acoes = [['Multando','multando'],['Abordando','abordando'],['Posicionado','posicionado']];
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-combo-row">
      <div class="inf-combo-lbl">Quem</div>
      <div class="inf-tile-grid2">${orgaos.map(([lbl,v])=>`<div class="inf-tile ${_infRelState.orgao===v?'picked':''}" onclick="infRelatarPickOrgao('${v}',${JSON.stringify(lbl)})">${lbl}</div>`).join('')}</div>
    </div>
    <div class="inf-combo-row" style="margin-bottom:4px">
      <div class="inf-combo-lbl">Fazendo o quê</div>
      <div class="inf-tile-grid3">${acoes.map(([lbl,v])=>`<div class="inf-tile ${_infRelState.acao===v?'picked':''}" onclick="infRelatarPickAcao('${v}',${JSON.stringify(lbl)})">${lbl}</div>`).join('')}</div>
    </div>`;
}
function infRelatarPickOrgao(v,lbl){ _infRelState.orgao=v; _infRelState.orgaoLbl=lbl; infRelatarRenderPoliciamento(); infRelatarMaybePublishPoliciamento(); }
function infRelatarPickAcao(v,lbl){ _infRelState.acao=v; _infRelState.acaoLbl=lbl; infRelatarRenderPoliciamento(); infRelatarMaybePublishPoliciamento(); }
function infRelatarMaybePublishPoliciamento(){
  if (_infRelState.orgao && _infRelState.acao) {
    // Pequena pausa só pra pessoa ver a segunda opção marcada antes de
    // sumir a tela — publica sozinho, sem precisar de botão confirmar.
    setTimeout(()=>infRelatarPublish({param_value:_infRelState.orgaoLbl, param_detail:_infRelState.acaoLbl}), 350);
  }
}

// Alerta — 2 etapas de verdade, a segunda pergunta depende da primeira
// resposta. Cores fortes (não pastel) — pedido do Yuri: Acidente=laranja,
// Área de risco=vermelho (invertido do que eu tinha proposto antes).
function infRelatarShowAlertaStep1(){
  document.getElementById('inf-relatar-title').textContent = 'Qual alerta?';
  infRelatarShowBack('Alerta', infOpenRelatar);
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-tile-grid2">
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarShowAcidenteStep2()">Acidente</div>
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarShowAreaRiscoStep2()">Área de risco</div>
    </div>`;
}
function infRelatarShowAcidenteStep2(){
  document.getElementById('inf-relatar-title').textContent = 'Onde exatamente?';
  infRelatarShowBack('Acidente', infRelatarShowAlertaStep1);
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-tile-grid2">
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarPublish({param_value:'Acidente', param_detail:'Na via'})">Na via</div>
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarPublish({param_value:'Acidente', param_detail:'No acostamento'})">No acostamento</div>
    </div>`;
}
function infRelatarShowAreaRiscoStep2(){
  document.getElementById('inf-relatar-title').textContent = 'Área de Risco?';
  infRelatarShowBack('Área de risco', infRelatarShowAlertaStep1);
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-tile-grid2">
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Via de acesso'})">Via de acesso</div>
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Área de abordagem'})">Área de abordagem</div>
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Proibido a entrada'})">Proibido a entrada</div>
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Barricada'})">Barricada</div>
    </div>`;
}

// Publica de verdade em feed_posts — usa a localização já detectada
// quando o modal abriu (mais rápido que pedir de novo agora).
function infRelatarPublish(extra){
  document.getElementById('inf-relatar-title').textContent = 'Publicando…';
  document.getElementById('inf-relatar-back').style.display = 'none';
  document.getElementById('inf-relatar-body').innerHTML = `
    <div style="display:flex;flex-direction:column;align-items:center;padding:24px 10px 8px;text-align:center">
      <div style="width:40px;height:40px;border-radius:50%;border:3.5px solid #DBEAFE;border-top-color:#2563EB;animation:spin .8s linear infinite;margin-bottom:14px"></div>
      <div style="font-size:12.5px;color:var(--muted)">Confirmando localização</div>
    </div>`;
  infRelatarDoPublish(extra);
}
async function infRelatarDoPublish(extra){
  if (!_supaUser) { closeMo('mo-inf-relatar'); showToast('Faça login pra relatar.', 'error'); return; }
  const t = (_infRelState.types||{})[_infRelState.type];
  const expMin = t?.expMin || 30;
  const row = {
    user_id:_supaUser.id, type:_infRelState.type, audience:'comunidade', source:'user',
    lat:_infRelState.lat ?? null, lng:_infRelState.lng ?? null,
    address:_infRelState.address || null, city:'rio',
    param_value:extra.param_value ?? null, param_detail:extra.param_detail ?? null,
    nivel: extra.nivel ?? null,
    expires_at:new Date(Date.now()+expMin*60000).toISOString(),
    votes_up:1, votes_dn:0, comments_count:0, status:'ativo'
  };
  const { error } = await _SUPA.from('feed_posts').insert(row);
  setTimeout(()=>{
    closeMo('mo-inf-relatar');
    if (error) { console.error('infRelatarDoPublish', error); showToast('Erro ao publicar. Tenta de novo.', 'error'); return; }
    showToast('Publicado!', 'success');
    if (_infLoc) infCarregarInformes(_infLoc, _infTipoKey);
  }, 500);
}
function infSetTime(btn,min){
  document.querySelectorAll('.inf-tp').forEach(b=>b.classList.remove('active'));btn.classList.add('active');
  _infMinutos=parseInt(min);infCarregarContagens();
  if(_infLoc)infCarregarInformes(_infLoc,_infTipoKey);
}
function infFecharOv(id){document.getElementById(id).classList.remove('open');}
function infFiltrarSheet(inp,listId){
  const v=inp.value.toLowerCase();
  document.querySelectorAll('#'+listId+' .inf-loc-item').forEach(el=>{const t=el.querySelector('.inf-loc-name')?.textContent.toLowerCase()||'';el.style.display=(!v||t.includes(v))?'':'none';});
}
function infIconSvg(tipo,cor,sz){
  const a=`viewBox="0 0 24 24" width="${sz}" height="${sz}" fill="none" stroke="${cor}" stroke-width="2"`;
  const m={aero:`<svg ${a}><path d="M21 16v-2l-8-5V3.5a1.5 1.5 0 0 0-3 0V9l-8 5v2l8-2.5V19l-2 1.5V22l3.5-1 3.5 1v-1.5L13 19v-5.5z"/></svg>`,term:`<svg ${a}><rect x="1" y="3" width="15" height="13" rx="2"/><path d="M16 8h4l3 3v5h-7V8z"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>`,bairro:`<svg ${a}><circle cx="12" cy="10" r="3"/><path d="M12 2a8 8 0 0 1 8 8c0 5.333-8 14-8 14S4 15.333 4 10a8 8 0 0 1 8-8z"/></svg>`,rua:`<svg ${a}><circle cx="12" cy="12" r="3"/><path d="M12 2v3M12 19v3M2 12h3M19 12h3"/></svg>`,ev:`<svg ${a}><rect x="2" y="7" width="20" height="14" rx="2"/><path d="M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"/></svg>`,tur:`<svg ${a}><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>`,bar:`<svg ${a}><path d="M17 11H3v-1a5 5 0 0 1 5-5h4a5 5 0 0 1 5 5v1z"/><path d="M3 11v5a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2v-5"/></svg>`,hotel:`<svg ${a}><rect x="2" y="7" width="20" height="15" rx="2"/><path d="M2 12h20"/><path d="M8 7V5a4 4 0 0 1 8 0v2"/></svg>`,shop:`<svg ${a}><path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4Z"/><path d="M3 6h18"/><path d="M16 10a4 4 0 0 1-8 0"/></svg>`};
  return m[tipo]||m.bairro;
}
// Gesto swipe down para fechar o painel
(function() {
  const panel = document.getElementById('inf-panel');
  const drag  = document.getElementById('inf-panel-drag');
  if (!panel || !drag) return;
  let startY = 0, isDragging = false;
  function onStart(e) {
    startY = (e.touches ? e.touches[0].clientY : e.clientY);
    isDragging = true;
    panel.style.transition = 'none';
  }
  function onMove(e) {
    if (!isDragging) return;
    const dy = (e.touches ? e.touches[0].clientY : e.clientY) - startY;
    if (dy > 0) panel.style.transform = 'translateY(' + dy + 'px)';
  }
  function onEnd(e) {
    if (!isDragging) return;
    isDragging = false;
    panel.style.transition = '';
    const dy = (e.changedTouches ? e.changedTouches[0].clientY : e.clientY) - startY;
    if (dy > 100) { panel.style.transform = ''; infFecharPainel(); }
    else panel.style.transform = '';
  }
  drag.addEventListener('touchstart', onStart, {passive
===== orange @ 1714817 =====
];
  document.getElementById('inf-relatar-body').innerHTML =
    `<div class="inf-tbar">${cores.map((c,i)=>`<button class="inf-tbar-seg" style="background:${c}" onclick="infRelatarPublish({nivel:${i}})">${i}</button>`).join('')}</div>
     <div class="inf-tbar-labels"><span>Parado</span><span>Livre</span></div>`;
}

// Policiamento — "quem" (2 col) + "fazendo o quê" (3 col) na mesma tela;
// publica sozinho assim que os dois têm uma opção marcada.
function infRelatarShowPoliciamento(){
  document.getElementById('inf-relatar-title').textContent = 'Fiscalização';
  infRelatarShowBack('Fiscalização', infOpenRelatar);
  _infRelState.orgao = null; _infRelState.acao = null;
  infRelatarRenderPoliciamento();
}
function infRelatarRenderPoliciamento(){
  const orgaos = [['Polícia Militar','pm'],['Guarda Municipal','gm'],['Lei Seca','leiseca'],['Detro','detro']];
  const acoes = [['Multando','multando'],['Abordando','abordando'],['Posicionado','posicionado']];
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-combo-row">
      <div class="inf-combo-lbl">Quem</div>
      <div class="inf-tile-grid2">${orgaos.map(([lbl,v])=>`<div class="inf-tile ${_infRelState.orgao===v?'picked':''}" onclick="infRelatarPickOrgao('${v}',${JSON.stringify(lbl)})">${lbl}</div>`).join('')}</div>
    </div>
    <div class="inf-combo-row" style="margin-bottom:4px">
      <div class="inf-combo-lbl">Fazendo o quê</div>
      <div class="inf-tile-grid3">${acoes.map(([lbl,v])=>`<div class="inf-tile ${_infRelState.acao===v?'picked':''}" onclick="infRelatarPickAcao('${v}',${JSON.stringify(lbl)})">${lbl}</div>`).join('')}</div>
    </div>`;
}
function infRelatarPickOrgao(v,lbl){ _infRelState.orgao=v; _infRelState.orgaoLbl=lbl; infRelatarRenderPoliciamento(); infRelatarMaybePublishPoliciamento(); }
function infRelatarPickAcao(v,lbl){ _infRelState.acao=v; _infRelState.acaoLbl=lbl; infRelatarRenderPoliciamento(); infRelatarMaybePublishPoliciamento(); }
function infRelatarMaybePublishPoliciamento(){
  if (_infRelState.orgao && _infRelState.acao) {
    // Pequena pausa só pra pessoa ver a segunda opção marcada antes de
    // sumir a tela — publica sozinho, sem precisar de botão confirmar.
    setTimeout(()=>infRelatarPublish({param_value:_infRelState.orgaoLbl, param_detail:_infRelState.acaoLbl}), 350);
  }
}

// Alerta — 2 etapas de verdade, a segunda pergunta depende da primeira
// resposta. Cores fortes (não pastel) — pedido do Yuri: Acidente=laranja,
// Área de risco=vermelho (invertido do que eu tinha proposto antes).
function infRelatarShowAlertaStep1(){
  document.getElementById('inf-relatar-title').textContent = 'Qual alerta?';
  infRelatarShowBack('Alerta', infOpenRelatar);
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-tile-grid2">
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarShowAcidenteStep2()">Acidente</div>
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarShowAreaRiscoStep2()">Área de risco</div>
    </div>`;
}
function infRelatarShowAcidenteStep2(){
  document.getElementById('inf-relatar-title').textContent = 'Onde exatamente?';
  infRelatarShowBack('Acidente', infRelatarShowAlertaStep1);
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-tile-grid2">
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarPublish({param_value:'Acidente', param_detail:'Na via'})">Na via</div>
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarPublish({param_value:'Acidente', param_detail:'No acostamento'})">No acostamento</div>
    </div>`;
}
function infRelatarShowAreaRiscoStep2(){
  document.getElementById('inf-relatar-title').textContent = 'Área de Risco?';
  infRelatarShowBack('Área de risco', infRelatarShowAlertaStep1);
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-tile-grid2">
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Via de acesso'})">Via de acesso</div>
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Área de abordagem'})">Área de abordagem</div>
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Proibido a entrada'})">Proibido a entrada</div>
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Barricada'})">Barricada</div>
    </div>`;
}

// Publica de verdade em feed_posts — usa a localização já detectada
// quando o modal abriu (mais rápido que pedir de novo agora).
function infRelatarPublish(extra){
  document.getElementById('inf-relatar-title').textContent = 'Publicando…';
  document.getElementById('inf-relatar-back').style.display = 'none';
  document.getElementById('inf-relatar-body').innerHTML = `
    <div style="display:flex;flex-direction:column;align-items:center;padding:24px 10px 8px;text-align:center">
      <div style="width:40px;height:40px;border-radius:50%;border:3.5px solid #DBEAFE;border-top-color:#2563EB;animation:spin .8s linear infinite;margin-bottom:14px"></div>
      <div style="font-size:12.5px;color:var(--muted)">Confirmando localização</div>
    </div>`;
  infRelatarDoPublish(extra);
}
async function infRelatarDoPublish(extra){
  if (!_supaUser) { closeMo('mo-inf-relatar'); showToast('Faça login pra relatar.', 'error'); return; }
  const t = (_infRelState.types||{})[_infRelState.type];
  const expMin = t?.expMin || 30;
  const row = {
    user_id:_supaUser.id, type:_infRelState.type, audience:'comunidade', source:'user',
    lat:_infRelState.lat ?? null, lng:_infRelState.lng ?? null,
    address:_infRelState.address || null, city:'rio',
    param_value:extra.param_value ?? null, param_detail:extra.param_detail ?? null,
    nivel: extra.nivel ?? null,
    expires_at:new Date(Date.now()+expMin*60000).toISOString(),
    votes_up:1, votes_dn:0, comments_count:0, status:'ativo'
  };
  const { error } = await _SUPA.from('feed_posts').insert(row);
  setTimeout(()=>{
    closeMo('mo-inf-relatar');
    if (error) { console.error('infRelatarDoPublish', error); showToast('Erro ao publicar. Tenta de novo.', 'error'); return; }
    showToast('Publicado!', 'success');
    if (_infLoc) infCarregarInformes(_infLoc, _infTipoKey);
  }, 500);
}
function infSetTime(btn,min){
  document.querySelectorAll('.inf-tp').forEach(b=>b.classList.remove('active'));btn.classList.add('active');
  _infMinutos=parseInt(min);infCarregarContagens();
  if(_infLoc)infCarregarInformes(_infLoc,_infTipoKey);
}
function infFecharOv(id){document.getElementById(id).classList.remove('open');}
function infFiltrarSheet(inp,listId){
  const v=inp.value.toLowerCase();
  document.querySelectorAll('#'+listId+' .inf-loc-item').forEach(el=>{const t=el.querySelector('.inf-loc-name')?.textContent.toLowerCase()||'';el.style.display=(!v||t.includes(v))?'':'none';});
}
function infIconSvg(tipo,cor,sz){
  const a=`viewBox="0 0 24 24" width="${sz}" height="${sz}" fill="none" stroke="${cor}" stroke-width="2"`;
  const m={aero:`<svg ${a}><path d="M21 16v-2l-8-5V3.5a1.5 1.5 0 0 0-3 0V9l-8 5v2l8-2.5V19l-2 1.5V22l3.5-1 3.5 1v-1.5L13 19v-5.5z"/></svg>`,term:`<svg ${a}><rect x="1" y="3" width="15" height="13" rx="2"/><path d="M16 8h4l3 3v5h-7V8z"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>`,bairro:`<svg ${a}><circle cx="12" cy="10" r="3"/><path d="M12 2a8 8 0 0 1 8 8c0 5.333-8 14-8 14S4 15.333 4 10a8 8 0 0 1 8-8z"/></svg>`,rua:`<svg ${a}><circle cx="12" cy="12" r="3"/><path d="M12 2v3M12 19v3M2 12h3M19 12h3"/></svg>`,ev:`<svg ${a}><rect x="2" y="7" width="20" height="14" rx="2"/><path d="M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"/></svg>`,tur:`<svg ${a}><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>`,bar:`<svg ${a}><path d="M17 11H3v-1a5 5 0 0 1 5-5h4a5 5 0 0 1 5 5v1z"/><path d="M3 11v5a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2v-5"/></svg>`,hotel:`<svg ${a}><rect x="2" y="7" width="20" height="15" rx="2"/><path d="M2 12h20"/><path d="M8 7V5a4 4 0 0 1 8 0v2"/></svg>`,shop:`<svg ${a}><path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4Z"/><path d="M3 6h18"/><path d="M16 10a4 4 0 0 1-8 0"/></svg>`};
  return m[tipo]||m.bairro;
}
// Gesto swipe down para fechar o painel
(function() {
  const panel = document.getElementById('inf-panel');
  const drag  = document.getElementById('inf-panel-drag');
  if (!panel || !drag) return;
  let startY = 0, isDragging = false;
  function onStart(e) {
    startY = (e.touches ? e.touches[0].clientY : e.clientY);
    isDragging = true;
    panel.style.transition = 'none';
  }
  function onMove(e) {
    if (!isDragging) return;
    const dy = (e.touches ? e.touches[0].clientY : e.clientY) - startY;
    if (dy > 0) panel.style.transform = 'translateY(' + dy + 'px)';
  }
  function onEnd(e) {
    if (!isDragging) return;
    isDragging = false;
    panel.style.transition = '';
    const dy = (e.changedTouches ? e.changedTouches[0].clientY : e.clientY) - startY;
    if (dy > 100) { panel.style.transform = ''; infFecharPainel(); }
    else panel.style.transform = '';
  }
  drag.addEventListener('touchstart', onStart, {passive:true});
  drag.addEventListener('touchmove',  onMove,  {passive:true});
  drag.addEventListener('touchend',   onEnd);
  drag.addEventListener('mousedown',  onStart);
  document.addEventListener('mousemove', onMove);
  document.addEventListener('mouseup',   onEnd);
})();

['inf-ov-gps','inf-ov-venue','inf-ov-rep'].forEach(id=>{
  const el=document.getElementById(id);if(el)el.addEventListener('click',function(e){if(e.target===this)infFecharOv(id);});
});
document.addEventListener('click',e=>{
  if(!e.target.closest('#inf-search-bar')&&!e.target.closest('#inf-search-btn')&&!e.target.closest('#inf-search-dd')){
    const dd=document.getElementB
===== orange @ 1715270 =====
unction infRelatarShowPoliciamento(){
  document.getElementById('inf-relatar-title').textContent = 'Fiscalização';
  infRelatarShowBack('Fiscalização', infOpenRelatar);
  _infRelState.orgao = null; _infRelState.acao = null;
  infRelatarRenderPoliciamento();
}
function infRelatarRenderPoliciamento(){
  const orgaos = [['Polícia Militar','pm'],['Guarda Municipal','gm'],['Lei Seca','leiseca'],['Detro','detro']];
  const acoes = [['Multando','multando'],['Abordando','abordando'],['Posicionado','posicionado']];
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-combo-row">
      <div class="inf-combo-lbl">Quem</div>
      <div class="inf-tile-grid2">${orgaos.map(([lbl,v])=>`<div class="inf-tile ${_infRelState.orgao===v?'picked':''}" onclick="infRelatarPickOrgao('${v}',${JSON.stringify(lbl)})">${lbl}</div>`).join('')}</div>
    </div>
    <div class="inf-combo-row" style="margin-bottom:4px">
      <div class="inf-combo-lbl">Fazendo o quê</div>
      <div class="inf-tile-grid3">${acoes.map(([lbl,v])=>`<div class="inf-tile ${_infRelState.acao===v?'picked':''}" onclick="infRelatarPickAcao('${v}',${JSON.stringify(lbl)})">${lbl}</div>`).join('')}</div>
    </div>`;
}
function infRelatarPickOrgao(v,lbl){ _infRelState.orgao=v; _infRelState.orgaoLbl=lbl; infRelatarRenderPoliciamento(); infRelatarMaybePublishPoliciamento(); }
function infRelatarPickAcao(v,lbl){ _infRelState.acao=v; _infRelState.acaoLbl=lbl; infRelatarRenderPoliciamento(); infRelatarMaybePublishPoliciamento(); }
function infRelatarMaybePublishPoliciamento(){
  if (_infRelState.orgao && _infRelState.acao) {
    // Pequena pausa só pra pessoa ver a segunda opção marcada antes de
    // sumir a tela — publica sozinho, sem precisar de botão confirmar.
    setTimeout(()=>infRelatarPublish({param_value:_infRelState.orgaoLbl, param_detail:_infRelState.acaoLbl}), 350);
  }
}

// Alerta — 2 etapas de verdade, a segunda pergunta depende da primeira
// resposta. Cores fortes (não pastel) — pedido do Yuri: Acidente=laranja,
// Área de risco=vermelho (invertido do que eu tinha proposto antes).
function infRelatarShowAlertaStep1(){
  document.getElementById('inf-relatar-title').textContent = 'Qual alerta?';
  infRelatarShowBack('Alerta', infOpenRelatar);
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-tile-grid2">
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarShowAcidenteStep2()">Acidente</div>
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarShowAreaRiscoStep2()">Área de risco</div>
    </div>`;
}
function infRelatarShowAcidenteStep2(){
  document.getElementById('inf-relatar-title').textContent = 'Onde exatamente?';
  infRelatarShowBack('Acidente', infRelatarShowAlertaStep1);
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-tile-grid2">
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarPublish({param_value:'Acidente', param_detail:'Na via'})">Na via</div>
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarPublish({param_value:'Acidente', param_detail:'No acostamento'})">No acostamento</div>
    </div>`;
}
function infRelatarShowAreaRiscoStep2(){
  document.getElementById('inf-relatar-title').textContent = 'Área de Risco?';
  infRelatarShowBack('Área de risco', infRelatarShowAlertaStep1);
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-tile-grid2">
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Via de acesso'})">Via de acesso</div>
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Área de abordagem'})">Área de abordagem</div>
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Proibido a entrada'})">Proibido a entrada</div>
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Barricada'})">Barricada</div>
    </div>`;
}

// Publica de verdade em feed_posts — usa a localização já detectada
// quando o modal abriu (mais rápido que pedir de novo agora).
function infRelatarPublish(extra){
  document.getElementById('inf-relatar-title').textContent = 'Publicando…';
  document.getElementById('inf-relatar-back').style.display = 'none';
  document.getElementById('inf-relatar-body').innerHTML = `
    <div style="display:flex;flex-direction:column;align-items:center;padding:24px 10px 8px;text-align:center">
      <div style="width:40px;height:40px;border-radius:50%;border:3.5px solid #DBEAFE;border-top-color:#2563EB;animation:spin .8s linear infinite;margin-bottom:14px"></div>
      <div style="font-size:12.5px;color:var(--muted)">Confirmando localização</div>
    </div>`;
  infRelatarDoPublish(extra);
}
async function infRelatarDoPublish(extra){
  if (!_supaUser) { closeMo('mo-inf-relatar'); showToast('Faça login pra relatar.', 'error'); return; }
  const t = (_infRelState.types||{})[_infRelState.type];
  const expMin = t?.expMin || 30;
  const row = {
    user_id:_supaUser.id, type:_infRelState.type, audience:'comunidade', source:'user',
    lat:_infRelState.lat ?? null, lng:_infRelState.lng ?? null,
    address:_infRelState.address || null, city:'rio',
    param_value:extra.param_value ?? null, param_detail:extra.param_detail ?? null,
    nivel: extra.nivel ?? null,
    expires_at:new Date(Date.now()+expMin*60000).toISOString(),
    votes_up:1, votes_dn:0, comments_count:0, status:'ativo'
  };
  const { error } = await _SUPA.from('feed_posts').insert(row);
  setTimeout(()=>{
    closeMo('mo-inf-relatar');
    if (error) { console.error('infRelatarDoPublish', error); showToast('Erro ao publicar. Tenta de novo.', 'error'); return; }
    showToast('Publicado!', 'success');
    if (_infLoc) infCarregarInformes(_infLoc, _infTipoKey);
  }, 500);
}
function infSetTime(btn,min){
  document.querySelectorAll('.inf-tp').forEach(b=>b.classList.remove('active'));btn.classList.add('active');
  _infMinutos=parseInt(min);infCarregarContagens();
  if(_infLoc)infCarregarInformes(_infLoc,_infTipoKey);
}
function infFecharOv(id){document.getElementById(id).classList.remove('open');}
function infFiltrarSheet(inp,listId){
  const v=inp.value.toLowerCase();
  document.querySelectorAll('#'+listId+' .inf-loc-item').forEach(el=>{const t=el.querySelector('.inf-loc-name')?.textContent.toLowerCase()||'';el.style.display=(!v||t.includes(v))?'':'none';});
}
function infIconSvg(tipo,cor,sz){
  const a=`viewBox="0 0 24 24" width="${sz}" height="${sz}" fill="none" stroke="${cor}" stroke-width="2"`;
  const m={aero:`<svg ${a}><path d="M21 16v-2l-8-5V3.5a1.5 1.5 0 0 0-3 0V9l-8 5v2l8-2.5V19l-2 1.5V22l3.5-1 3.5 1v-1.5L13 19v-5.5z"/></svg>`,term:`<svg ${a}><rect x="1" y="3" width="15" height="13" rx="2"/><path d="M16 8h4l3 3v5h-7V8z"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>`,bairro:`<svg ${a}><circle cx="12" cy="10" r="3"/><path d="M12 2a8 8 0 0 1 8 8c0 5.333-8 14-8 14S4 15.333 4 10a8 8 0 0 1 8-8z"/></svg>`,rua:`<svg ${a}><circle cx="12" cy="12" r="3"/><path d="M12 2v3M12 19v3M2 12h3M19 12h3"/></svg>`,ev:`<svg ${a}><rect x="2" y="7" width="20" height="14" rx="2"/><path d="M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"/></svg>`,tur:`<svg ${a}><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>`,bar:`<svg ${a}><path d="M17 11H3v-1a5 5 0 0 1 5-5h4a5 5 0 0 1 5 5v1z"/><path d="M3 11v5a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2v-5"/></svg>`,hotel:`<svg ${a}><rect x="2" y="7" width="20" height="15" rx="2"/><path d="M2 12h20"/><path d="M8 7V5a4 4 0 0 1 8 0v2"/></svg>`,shop:`<svg ${a}><path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4Z"/><path d="M3 6h18"/><path d="M16 10a4 4 0 0 1-8 0"/></svg>`};
  return m[tipo]||m.bairro;
}
// Gesto swipe down para fechar o painel
(function() {
  const panel = document.getElementById('inf-panel');
  const drag  = document.getElementById('inf-panel-drag');
  if (!panel || !drag) return;
  let startY = 0, isDragging = false;
  function onStart(e) {
    startY = (e.touches ? e.touches[0].clientY : e.clientY);
    isDragging = true;
    panel.style.transition = 'none';
  }
  function onMove(e) {
    if (!isDragging) return;
    const dy = (e.touches ? e.touches[0].clientY : e.clientY) - startY;
    if (dy > 0) panel.style.transform = 'translateY(' + dy + 'px)';
  }
  function onEnd(e) {
    if (!isDragging) return;
    isDragging = false;
    panel.style.transition = '';
    const dy = (e.changedTouches ? e.changedTouches[0].clientY : e.clientY) - startY;
    if (dy > 100) { panel.style.transform = ''; infFecharPainel(); }
    else panel.style.transform = '';
  }
  drag.addEventListener('touchstart', onStart, {passive:true});
  drag.addEventListener('touchmove',  onMove,  {passive:true});
  drag.addEventListener('touchend',   onEnd);
  drag.addEventListener('mousedown',  onStart);
  document.addEventListener('mousemove', onMove);
  document.addEventListener('mouseup',   onEnd);
})();

['inf-ov-gps','inf-ov-venue','inf-ov-rep'].forEach(id=>{
  const el=document.getElementById(id);if(el)el.addEventListener('click',function(e){if(e.target===this)infFecharOv(id);});
});
document.addEventListener('click',e=>{
  if(!e.target.closest('#inf-search-bar')&&!e.target.closest('#inf-search-btn')&&!e.target.closest('#inf-search-dd')){
    const dd=document.getElementById('inf-search-dd');if(dd)dd.classList.remove('show');
  }
});

/* ══════════════════════════════════════════════════════════════
   FEED SOCIAL (Home) — v1
   Tabelas: feed_posts / feed_reactions / feed_comments (Supabase)
   Contadores de voto/comentário e auto-encerramento por downvote
   são calculados via trigger no banco — o client só insere/upserta
   e depois relê o post pra pegar os números atualizados.
   ═════════════════════════════════
===== laranja @ 619414 =====
arar:</b> algumas recusadas renderiam mais por km/hora que a média das aceitas — pode valer olhar os motivos com mais atenção.'}</div>`}
    ${sortedDates.map(d => `
      <div class="dsec">${fmtSep(d)}</div>
      ${byDate[d].sort((a,b)=>b.ts-a.ts).map(({o,km,minTot}) => {
        const plat = o.platform==='uber' ? 'Uber' : '99';
        const platCls = o.platform==='uber' ? 'uber' : 'n99';
        const rkm = km>0 ? DB.fR((o.offer_value||0)/km)+'/km' : '—';
        const hh = new Date(o.seen_at);
        const hhmm = p2(hh.getHours())+':'+p2(hh.getMinutes());
        const title = o.origin_address ? esc(extractBairro(o.origin_address)) + (o.dest_address?' → '+esc(extractBairro(o.dest_address)):'') : (plat+' — sem endereço');
        return `<div class="di-row">
          <div class="di-plat ${platCls}">${o.platform==='uber'?'U':'99'}</div>
          <div class="di-info"><h4>${title}</h4><p>${hhmm}${km>0?' · '+km.toFixed(1)+' km':''}${minTot>0?' · '+Math.round(minTot)+'m':''}</p></div>
          <div class="di-val"><strong>${DB.fR(o.offer_value||0)}</strong><span>${rkm}</span></div>
        </div>`;
      }).join('')}
    `).join('')}
  `;
}
function fmtKmComp(real, offer) {
  if (real==null && offer==null) return '—';
  let s = real!=null ? (+real).toFixed(1)+' km' : '—';
  if (offer!=null) s += ' (previsto: '+(+offer).toFixed(1)+' km)';
  return s;
}
function fmtDurComp(realMin, offerMin) {
  if (realMin==null && offerMin==null) return '—';
  let s = realMin!=null ? fmtDur(realMin) : '—';
  if (offerMin!=null) s += ' (previsto: '+fmtDur(offerMin)+')';
  return s;
}
// Extrai só o bairro de um endereço completo, pra mostrar no card da lista
// (endereço completo continua no detalhe). Formatos que a gente já viu:
// "Rua X, 123, Bairro Y" / "Loja Z, Av. W, 123 - Bairro Y" / "Rua X, 123,
// Bairro Y, Teresópoli" (com cidade grudada no fim).
function extractBairro(addr) {
  if (!addr) return '';
  let s = String(addr).trim();
  s = s.replace(/,?\s*\d{5}-?\d{3}.*$/i, '');           // CEP e o que vier depois
  s = s.replace(/,?\s*Brasil\s*$/i, '');                 // país no fim
  s = s.replace(/,?\s*-?\s*RJ\s*$/i, '');                // UF no fim
  s = s.replace(/,\s*Teresópolis?\s*$/i, '');            // cidade grudada no fim
  const dashIdx = s.lastIndexOf(' - ');
  if (dashIdx !== -1) return s.slice(dashIdx + 3).trim() || s.trim();
  const parts = s.split(',').map(p => p.trim()).filter(Boolean);
  for (let i = parts.length - 1; i >= 0; i--) {
    if (!/^\d+$/.test(parts[i])) return parts[i];
  }
  return parts[parts.length - 1] || s;
}
function autoTripItem(t) {
  const dest = t.dest_address ? ' → '+esc(extractBairro(t.dest_address)) : '';
  const tid = 'ta'+Date.now().toString(36)+Math.random().toString(36).slice(2,8);
  const cashSvg = `<svg viewBox="0 0 24 24" width="13" height="13" fill="currentColor"><rect x="2" y="7" width="20" height="13" rx="2"/><path d="M2 10h20" stroke="white" stroke-width="1.2" fill="none"/><path d="M2 17h20" stroke="white" stroke-width="1.2" fill="none"/><circle cx="12" cy="13" r="2.5" fill="white"/></svg>`;
  const sourceTag = t.capture_source === 'importada' ? `<span class="chip chip-import">Importada</span>` : `<span class="chip chip-auto">Captura automática</span>`;
  const extras = [sourceTag];
  if (t.dinheiro) extras.push(`<span class="tap-legend-wrap chip-icon chip-cash" onclick="showTapLegend(event,'Pago em dinheiro')">${cashSvg}</span>`);
  // Mesmo chip/ícone já usado nas corridas manuais (raio laranja + valor) —
  // pedido do Yuri (24/07/2026): dinâmico da 99 (base + espera longa, já
  // somados no app) e da Uber aparecerem na listagem, não só no detalhe.
  // Chip do dinâmico — pedido do Yuri (16/08/2026, confirmado em dado real
  // do trip_reader_log): só o 99 mostra multiplicador na tela (ex "1,1x",
  // colado no valor da oferta), a Uber nunca mostra esse número, só o
  // valor em R$ incluído. Corridas do 99 capturadas antes dessa versão do
  // APK (ou sem tarifa dinâmica ativa naquele momento) não têm
  // offer_multiplicador — cai pro valor em R$ como já era antes.
  if (t.platform === '99' && t.offer_multiplicador > 0) {
    const multTxt = (+t.offer_multiplicador).toFixed(1).replace('.', ',') + 'x';
    extras.push(`<span class="tap-legend-wrap chip chip-d" onclick="showTapLegend(event,'Multiplicador de tarifa dinâmica: ${multTxt}')"><svg viewBox="0 0 24 24" width="9" height="9" fill="currentColor"><path d="M13 2 3 14h7l-1 8 10-12h-7z"/></svg><strong>${multTxt}</strong></span>`);
  } else if (t.offer_dinamico > 0) {
    extras.push(`<span class="tap-legend-wrap chip chip-d" onclick="showTapLegend(event,'Tarifa dinâmica: +${DB.fR(t.offer_dinamico).replace(/'/g,"")}')"><svg viewBox="0 0 24 24" width="9" height="9" fill="currentColor"><path d="M13 2 3 14h7l-1 8 10-12h-7z"/></svg><strong>${DB.fR(t.offer_dinamico)}</strong></span>`);
  }
  const statusCls = t.status==='confirmada' ? 'conf' : 'estim';
  const statusTxt = t.status==='confirmada' ? 'Confirmada' : 'Aguardando confirmação';
  const realDurTrip = (t.trip_started_at && t.trip_ended_at) ? (new Date(t.trip_ended_at)-new Date(t.trip_started_at))/60000 : null;
  const realDurPickup = (t.pickup_started_at && t.trip_started_at) ? (new Date(t.trip_started_at)-new Date(t.pickup_started_at))/60000 : null;
  const offerDurPickupMin = t.offer_duration_pickup_sec!=null ? t.offer_duration_pickup_sec/60 : null;
  const offerDurTripMin = t.offer_duration_trip_sec!=null ? t.offer_duration_trip_sec/60 : null;
  // Horário real de início de cada perna — pedido do Yuri (23/07/2026):
  // instante em que o status virou buscar (pickup_started_at) e em que
  // virou corrida (trip_started_at), no fuso local.
  const hhmm = iso => { if (!iso) return null; const d = new Date(iso); return p2(d.getHours())+':'+p2(d.getMinutes()); };
  const hhmmPickup = hhmm(t.pickup_started_at);
  const hhmmTrip = hhmm(t.trip_started_at);
  // Linha minimizada: horário + km + tempo da perna CORRIDA (real se já
  // rodou, senão previsto da oferta) — sem repetir a plataforma, que já
  // aparece no ícone ao lado.
  const kmDestVal = t.real_km_trip!=null ? t.real_km_trip : t.offer_km_trip;
  const durDestVal = realDurTrip!=null ? realDurTrip : offerDurTripMin;
  const subParts = [];
  if (kmDestVal!=null) subParts.push((t.real_km_trip==null?'~':'')+(+kmDestVal).toFixed(1)+' km');
  if (durDestVal!=null) subParts.push(fmtDur(durDestVal));
  const subLine = subParts.join(' · ');
  // Deslocamento/Corrida: horário real de início + km (tempo), uma linha
  // cada — pedido do Yuri (23/07/2026) pra simplificar o detalhe, tirando
  // a comparação real-vs-previsto de 4 linhas separadas.
  const kmPickupVal = t.real_km_pickup!=null ? t.real_km_pickup : t.offer_km_pickup;
  const durPickupVal = realDurPickup!=null ? realDurPickup : offerDurPickupMin;
  const deslocTxt = kmPickupVal!=null
    ? `${hhmmPickup?hhmmPickup+' · ':''}${(t.real_km_pickup==null?'~':'')+(+kmPickupVal).toFixed(1)} km${durPickupVal!=null?' ('+fmtDur(durPickupVal)+')':''}`
    : '—';
  const corridaTxt = kmDestVal!=null
    ? `${hhmmTrip?hhmmTrip+' · ':''}${(t.real_km_trip==null?'~':'')+(+kmDestVal).toFixed(1)} km${durDestVal!=null?' ('+fmtDur(durDestVal)+')':''}`
    : '—';
  // Cor por comparação previsto × real — pedido do Yuri (24/07/2026): se
  // km OU tempo (basta um dos dois) ficou pior que o previsto, vermelho
  // (prioridade — é o que mais importa notar); se algum ficou melhor,
  // verde; bateu nos dois, preto normal (não clicável). Pequena tolerância
  // pra não colorir por ruído mínimo de GPS/OCR.
  function cmpColor(realKm, offerKm, realMin, offerMin) {
    if (realKm==null || offerKm==null || realMin==null || offerMin==null) return null;
    const kmWorse = realKm > offerKm + 0.15, kmBetter = realKm < offerKm - 0.15;
    const minWorse = realMin > offerMin + 1, minBetter = realMin < offerMin - 1;
    if (kmWorse || minWorse) return 'r';
    if (kmBetter || minBetter) return 'g';
    return null;
  }
  function routeRow(realKm, offerKm, realMin, offerMin, txt, rowId) {
    const cls = cmpColor(realKm, offerKm, realMin, offerMin);
    if (!cls) return { cls: '', timeHtml: `<span class="route-time">${txt}</span>` };
    const previstoTxt = (+offerKm).toFixed(1)+' km · '+fmtDur(offerMin);
    const realTxt = (+realKm).toFixed(1)+' km · '+fmtDur(realMin);
    return { cls, timeHtml: `<span class="route-time cmp ${cls}" onclick="toggleCmpLegend(event,'${rowId}')">${txt}</span>
      <div class="cmp-legend ${cls}" id="${rowId}" style="display:none">
        <div class="row"><span>Previsto na oferta</span><b>${previstoTxt}</b></div>
        <div class="row"><span>Real (GPS)</span><b>${realTxt}</b></div>
      </div>` };
  }
  const embarque = routeRow(t.real_km_pickup, t.offer_km_pickup, realDurPickup, offerDurPickupMin, deslocTxt, 'cmp-'+tid+'-d');
  const destino  = routeRow(t.real_km_trip, t.offer_km_trip, realDurTrip, offerDurTripMin, corridaTxt, 'cmp-'+tid+'-c');
  // 3 pontos: Aceite (só horário — é o único momento sem endereço próprio,
  // reaproveita o de origem, que é o que a oferta já mostra ao aceitar),
  // Embarque (chegada no ponto de origem) e Destino (fim da corrida) —
  // pedido do Yuri (15/08/2026, mockup aprovado). Só monta a rota quando
  // tem pelo menos o endereço de origem — sem isso não tem o que desenhar.
  const hhmmAccept = hhmm(t.accepted_at);
  const routeBlock = t.origin_address ? `<div class="route-block">
    <div class="route-line"></div>
    <div class="route-row">
      <div class="route-mark"><div class="mk-accept"></div></div>
      <div class="route-body">
        <div><div class="route-label">Aceite</div><span class="route-addr">${esc(t.origin_address)}</span></div>
        ${hhmmAccept?`<span class="route-time">${hhmmAccept}</span>`:''}
      </div>
    </div>
    <div class="route-row">
      <div class="route-mark"><div class="mk-pickup"></div></div>
      <div class="route-body">
        <div><div class="route-label">Embarque<
===== laranja @ 812189 =====
  const probe = document.createElement('canvas');
          probe.width = 24; probe.height = 24;
          const pctx = probe.getContext('2d');
          pctx.drawImage(canvas, 0, 0, probe.width, probe.height);
          const px = pctx.getImageData(0, 0, probe.width, probe.height).data;
          let lit = 0, maxLum = 0;
          for (let pi = 0; pi < px.length; pi += 4) {
            const lum = (px[pi] + px[pi+1] + px[pi+2]) / 3;
            if (lum > 12) lit++;
            if (lum > maxLum) maxLum = lum;
          }
          const hasContent = maxLum > 18 && lit >= 3;
          if (!hasContent) continue;
          const fp = frameFingerprint();
          if (lastFp && fpDiff(fp, lastFp) < SIMILARITY_THRESHOLD) {
            skipped++;
            continue; // scroll parado/lento — quase igual ao frame anterior mantido
          }
          lastFp = fp;
          frames.push(canvas.toDataURL('image/jpeg', 0.7).split(',')[1]);
        } catch(e) { console.warn('Frame t='+time, e); }
        finally {
          if (typeof onProgress === 'function') onProgress(timeIndex + 1, times.length);
        }
      }
      if (skipped) console.log('[extractVideoFrames] frames quase-duplicados pulados:', skipped);

      try{document.body.removeChild(video);}catch(e){}
      URL.revokeObjectURL(url);
      if (frames.length > 0) resolve(frames);
      else reject(new Error('Nenhum quadro legível foi extraído do vídeo.'));
      } catch(e) {
        try{document.body.removeChild(video);}catch(e2){}
        URL.revokeObjectURL(url);
        reject(e instanceof Error ? e : new Error(String(e)));
      }
    };

    video.onerror = (e) => {
      clearTimeout(metadataTimer);
      try{document.body.removeChild(video);}catch(e2){}
      URL.revokeObjectURL(url);
      const msg = e?.target?.error?.message || '';
      reject(new Error('Formato de vídeo não suportado. Use MP4 gravado pelo celular.\n' + msg));
    };
    video.load();
  });
}

async function extractTripsFromImage(file, plat) {
  const b64 = await new Promise((res,rej) => {
    const r = new FileReader();
    r.onload = () => res(r.result.split(',')[1]); r.onerror = () => rej(r.error); r.readAsDataURL(file);
  });
  return extractTripsFromBase64(b64, file.type||'image/jpeg', plat);
}

async function extractTripsFromBase64(b64, mt, plat) {
  const cashWarning = 'ATENÇÃO — confusão comum: "Dinheiro recebido" (ícone de câmera/recibo) NÃO é gorjeta nem dinâmico, é so a forma de pagamento (o passageiro pagou em espécie). Quando uma corrida tiver "Preço dinâmico" (ícone de raio) E "Dinheiro recebido" juntas, são DUAS linhas diferentes — extraia SÓ o valor do "Preço dinâmico" em dinamico, e NUNCA coloque o valor de "Dinheiro recebido" em gorjeta (nem em dinamico, nem em value). Exemplo: corrida com "R$ 12,50 Preço dinâmico" e "R$ 27,05 Dinheiro recebido" → dinamico:12.50, gorjeta:0.00 (o R$27,05 não entra em lugar nenhum do JSON).';
  // Prompt único — a IA identifica sozinha se é Uber ou 99 pelo layout da
  // imagem (em vez de confiar em qual botão o usuário tocou, que pode estar
  // errado). Cada corrida retornada inclui "platform":"uber"|"99".
  const prompt = 'Esta imagem mostra uma tela do app de motorista — pode ser: (1) UBER, tela "Histórico de ganhos" (lista de corridas com linha divisória entre elas, "Preço dinâmico" com ícone de raio, "Valor extra" com ícone de mão/coração = gorjeta); (2) 99 LISTA, tela "Histórico de corridas" do app 99 Motorista (ícone de carro + texto "Pop", tag laranja "Taxa Fixa de X%", "Ganhos pagos no app" ou "Ganhos pagos em dinheiro"); ou (3) 99 DETALHE, tela "Detalhes da corrida" do 99 (título "Detalhes da corrida" no topo, "Você ganhou R$X" em destaque, campos "Duração" e "Distância"). PRIMEIRO identifique qual das três telas é, pelo layout, e siga SOMENTE as regras dessa tela, abaixo. Inclua "platform":"uber" ou "platform":"99" em cada item do JSON (uma imagem só tem UMA plataforma e UM tipo de tela, nunca misture).\n\n' +
    '=== SE FOR UBER ===\n' +
    'Extraia CADA corrida, mas SÓ quando ela aparecer COMPLETA na imagem (com a linha divisória acima e abaixo) — se uma corrida estiver cortada no topo ou na base da imagem (incompleta), IGNORE essa corrida, não tente adivinhar ou completar com dados de outra. Ignore Missão (R$ 0,00). ' + cashWarning + ' Abaixo do valor principal de cada corrida também pode aparecer "Valor extra" (ícone de mão/coração — é a gorjeta do passageiro) — extraia esse valor em gorjeta quando existir. Também pode aparecer "Pedágio R$X,XX" — extraia esse valor em pedagio quando existir (0.00 quando não aparecer). Cada corrida mostra dois endereços (origem e destino): copie a linha de texto INTEIRA, palavra por palavra, exatamente como está escrita na tela, sem resumir ou cortar nenhuma parte, em "originFull"/"destFull". E TAMBÉM extraia só o nome do bairro (sem rua, número, cidade, UF ou CEP) em "origin"/"dest" — ex: se o texto na tela é "Rua Corcovado, 213, Jardim Botânico - Rio de Janeiro - RJ, 22460-050, BR", então origin="Jardim Botânico" e originFull="Rua Corcovado, 213, Jardim Botânico - Rio de Janeiro - RJ, 22460-050, BR" (a linha inteira, não abreviada). Formato de cada item: {"platform":"uber","time":"HH:MM","value":0.00,"km":0.0,"duration":0,"category":"Black","origin":"bairro","originFull":"endereço completo","dest":"bairro","destFull":"endereço completo","dinamico":0.00,"gorjeta":0.00,"pedagio":0.00,"taxaZero":false,"dinheiro":false}. duration=minutos inteiros. dinamico, gorjeta e pedagio ficam 0.00 quando não aparecerem. "taxaZero":true se houver indicação de 0% de taxa. "dinheiro":true se pagamento em dinheiro.\n\n' +
    '=== SE FOR 99 ===\n' +
    'Estrutura de CADA corrida, sempre nesta ordem: ' +
    '(1) Data e hora no formato "DD/MM/AAAA HH:MM" — extraia SOMENTE o HH:MM como campo "time". ' +
    '(2) Forma de pagamento: "Ganhos pagos no app" ou "Ganhos pagos em dinheiro" — IGNORE, não é valor nem corrida. ' +
    '(3) Ícone de carro + nome da categoria (pode ser "Pop", "Negocia", "Comfort" ou outra — leia o que estiver escrito, não assuma que é sempre "Pop") + valor "R$X,XX" à direita — este R$X,XX é o "value" da corrida, não importa qual seja a categoria. Converta vírgula para ponto (R$7,90 → 7.90). Guarde o texto exato da categoria lida em "category" com o prefixo "99" (ex: categoria "Pop" → "99Pop", categoria "Negocia" → "99Negocia"). ' +
    '(4) Tag laranja "Taxa Fixa de X%" — extraia o número X como "serviceFee" em formato decimal (ex: "Taxa Fixa de 15%" → serviceFee:0.15, "Taxa Fixa de 0%" → serviceFee:0). Não é gorjeta nem dinâmico. ' +
    '(5) Endereço de origem (ícone seta pra cima) e endereço de destino (ícone seta pra baixo) — copie a linha de texto INTEIRA, palavra por palavra, exatamente como está escrita na tela, sem resumir ou cortar nenhuma parte (incluindo nome de rua, número e complemento quando visíveis) em "originFull"/"destFull". E TAMBÉM extraia só o nome do bairro (sem rua, número, cidade, UF) em "origin"/"dest" — ex: se o texto na tela é "Rua Carmela Dutra, Agriões, Teresópolis - RJ", então origin="Agriões" e originFull="Rua Carmela Dutra, Agriões, Teresópolis - RJ" (a linha inteira, não abreviada). ' +
    '(5b) Pode aparecer "Pedágio R$X,XX" — extraia esse valor em pedagio quando existir (0.00 quando não aparecer). ' +
    '(6) Status verde "Pago" ou "Pedido finalizado" — marca o FIM desta corrida. ' +
    '(7) CABEÇALHO FIXO QUE NÃO ROLA: no topo da tela ficam fixas as abas "Corridas / Entregas" e a barra de filtros ("18/7 ▼", "Taxas ▼", "Pagamento ▼") — esse bloco NUNCA rola junto com a lista. Enquanto a lista sobe, a linha de data/hora de cada corrida desaparece por baixo desse cabeçalho fixo ANTES do resto do cartão (forma de pagamento, categoria+valor, taxa, endereços, status), que continuam visíveis mais um pouco. Por isso, na MAIORIA dos frames, o primeiro cartão logo abaixo do cabeçalho fixo aparece SEM a linha de data/hora, mesmo com todo o resto do cartão visível e legível. ' +
    'REGRAS: ' +
    'Extraia SOMENTE corridas COMPLETAS que mostrem data+hora, categoria+valor, e pelo menos um endereço. ' +
    'VALIDAÇÃO LINHA A LINHA — REGRA MAIS IMPORTANTE DE TODAS: só extraia uma corrida se o bloco dela estiver 100% delimitado e visível NESTA imagem, do início ao fim: (a) a linha de data/hora "DD/MM/AAAA HH:MM" visível e completa dentro do PRÓPRIO cartão, imediatamente acima da forma de pagamento — nunca herdada do cabeçalho fixo nem de outro frame, (b) o status verde ("Pago" ou "Pedido finalizado") visível na BASE do bloco, e (c) a LINHA FINA divisória visível separando este bloco do vizinho. Se QUALQUER uma dessas três fronteiras não estiver visível — cartão cortado no topo da imagem, cortado na base, sem linha fina clara entre as informações, OU cartão que começa colado no cabeçalho fixo (abas "Corridas/Entregas" + filtros) sem nenhuma linha de data/hora entre eles — NÃO EXTRAIA essa corrida. Um cartão logo abaixo do cabeçalho fixo com categoria+valor/taxa/endereços/status visíveis mas SEM data/hora acima já teve a data/hora dele engolida pelo cabeçalho — trate exatamente como cartão cortado, mesmo que o resto pareça completo. É melhor deixar de fora do que adivinhar: esse mesmo cartão aparece COMPLETO, com a data/hora visível, em um frame anterior (alguns instantes antes, antes de rolar pra debaixo do cabeçalho). NUNCA complete um cartão cortado com dados do vizinho, e NUNCA reaproveite ou "lembre" uma data/hora vista em outro frame — a data/hora tem que estar escrita nesta mesma imagem, colada nesse cartão específico. ' +
    'CADA HORÁRIO (HH:MM) IDENTIFICA UMA ÚNICA CORRIDA: nunca retorne dois itens com o mesmo "time" — se o mesmo horário aparecer repetido, é o mesmo cartão visto duas vezes, retorne só um. ' +
    'LINHAS DIVISÓRIAS: cada corrida fica delimitada por LINHAS HORIZONTAIS FINAS na tela. O bloco de uma corrida é: data/hora → forma de pagamento → Pop + valor → tag de taxa → endereços → linha fina → status verde ("Pago" ou "Pedid
===== laranja @ 815091 =====
m lugar nenhum do JSON).';
  // Prompt único — a IA identifica sozinha se é Uber ou 99 pelo layout da
  // imagem (em vez de confiar em qual botão o usuário tocou, que pode estar
  // errado). Cada corrida retornada inclui "platform":"uber"|"99".
  const prompt = 'Esta imagem mostra uma tela do app de motorista — pode ser: (1) UBER, tela "Histórico de ganhos" (lista de corridas com linha divisória entre elas, "Preço dinâmico" com ícone de raio, "Valor extra" com ícone de mão/coração = gorjeta); (2) 99 LISTA, tela "Histórico de corridas" do app 99 Motorista (ícone de carro + texto "Pop", tag laranja "Taxa Fixa de X%", "Ganhos pagos no app" ou "Ganhos pagos em dinheiro"); ou (3) 99 DETALHE, tela "Detalhes da corrida" do 99 (título "Detalhes da corrida" no topo, "Você ganhou R$X" em destaque, campos "Duração" e "Distância"). PRIMEIRO identifique qual das três telas é, pelo layout, e siga SOMENTE as regras dessa tela, abaixo. Inclua "platform":"uber" ou "platform":"99" em cada item do JSON (uma imagem só tem UMA plataforma e UM tipo de tela, nunca misture).\n\n' +
    '=== SE FOR UBER ===\n' +
    'Extraia CADA corrida, mas SÓ quando ela aparecer COMPLETA na imagem (com a linha divisória acima e abaixo) — se uma corrida estiver cortada no topo ou na base da imagem (incompleta), IGNORE essa corrida, não tente adivinhar ou completar com dados de outra. Ignore Missão (R$ 0,00). ' + cashWarning + ' Abaixo do valor principal de cada corrida também pode aparecer "Valor extra" (ícone de mão/coração — é a gorjeta do passageiro) — extraia esse valor em gorjeta quando existir. Também pode aparecer "Pedágio R$X,XX" — extraia esse valor em pedagio quando existir (0.00 quando não aparecer). Cada corrida mostra dois endereços (origem e destino): copie a linha de texto INTEIRA, palavra por palavra, exatamente como está escrita na tela, sem resumir ou cortar nenhuma parte, em "originFull"/"destFull". E TAMBÉM extraia só o nome do bairro (sem rua, número, cidade, UF ou CEP) em "origin"/"dest" — ex: se o texto na tela é "Rua Corcovado, 213, Jardim Botânico - Rio de Janeiro - RJ, 22460-050, BR", então origin="Jardim Botânico" e originFull="Rua Corcovado, 213, Jardim Botânico - Rio de Janeiro - RJ, 22460-050, BR" (a linha inteira, não abreviada). Formato de cada item: {"platform":"uber","time":"HH:MM","value":0.00,"km":0.0,"duration":0,"category":"Black","origin":"bairro","originFull":"endereço completo","dest":"bairro","destFull":"endereço completo","dinamico":0.00,"gorjeta":0.00,"pedagio":0.00,"taxaZero":false,"dinheiro":false}. duration=minutos inteiros. dinamico, gorjeta e pedagio ficam 0.00 quando não aparecerem. "taxaZero":true se houver indicação de 0% de taxa. "dinheiro":true se pagamento em dinheiro.\n\n' +
    '=== SE FOR 99 ===\n' +
    'Estrutura de CADA corrida, sempre nesta ordem: ' +
    '(1) Data e hora no formato "DD/MM/AAAA HH:MM" — extraia SOMENTE o HH:MM como campo "time". ' +
    '(2) Forma de pagamento: "Ganhos pagos no app" ou "Ganhos pagos em dinheiro" — IGNORE, não é valor nem corrida. ' +
    '(3) Ícone de carro + nome da categoria (pode ser "Pop", "Negocia", "Comfort" ou outra — leia o que estiver escrito, não assuma que é sempre "Pop") + valor "R$X,XX" à direita — este R$X,XX é o "value" da corrida, não importa qual seja a categoria. Converta vírgula para ponto (R$7,90 → 7.90). Guarde o texto exato da categoria lida em "category" com o prefixo "99" (ex: categoria "Pop" → "99Pop", categoria "Negocia" → "99Negocia"). ' +
    '(4) Tag laranja "Taxa Fixa de X%" — extraia o número X como "serviceFee" em formato decimal (ex: "Taxa Fixa de 15%" → serviceFee:0.15, "Taxa Fixa de 0%" → serviceFee:0). Não é gorjeta nem dinâmico. ' +
    '(5) Endereço de origem (ícone seta pra cima) e endereço de destino (ícone seta pra baixo) — copie a linha de texto INTEIRA, palavra por palavra, exatamente como está escrita na tela, sem resumir ou cortar nenhuma parte (incluindo nome de rua, número e complemento quando visíveis) em "originFull"/"destFull". E TAMBÉM extraia só o nome do bairro (sem rua, número, cidade, UF) em "origin"/"dest" — ex: se o texto na tela é "Rua Carmela Dutra, Agriões, Teresópolis - RJ", então origin="Agriões" e originFull="Rua Carmela Dutra, Agriões, Teresópolis - RJ" (a linha inteira, não abreviada). ' +
    '(5b) Pode aparecer "Pedágio R$X,XX" — extraia esse valor em pedagio quando existir (0.00 quando não aparecer). ' +
    '(6) Status verde "Pago" ou "Pedido finalizado" — marca o FIM desta corrida. ' +
    '(7) CABEÇALHO FIXO QUE NÃO ROLA: no topo da tela ficam fixas as abas "Corridas / Entregas" e a barra de filtros ("18/7 ▼", "Taxas ▼", "Pagamento ▼") — esse bloco NUNCA rola junto com a lista. Enquanto a lista sobe, a linha de data/hora de cada corrida desaparece por baixo desse cabeçalho fixo ANTES do resto do cartão (forma de pagamento, categoria+valor, taxa, endereços, status), que continuam visíveis mais um pouco. Por isso, na MAIORIA dos frames, o primeiro cartão logo abaixo do cabeçalho fixo aparece SEM a linha de data/hora, mesmo com todo o resto do cartão visível e legível. ' +
    'REGRAS: ' +
    'Extraia SOMENTE corridas COMPLETAS que mostrem data+hora, categoria+valor, e pelo menos um endereço. ' +
    'VALIDAÇÃO LINHA A LINHA — REGRA MAIS IMPORTANTE DE TODAS: só extraia uma corrida se o bloco dela estiver 100% delimitado e visível NESTA imagem, do início ao fim: (a) a linha de data/hora "DD/MM/AAAA HH:MM" visível e completa dentro do PRÓPRIO cartão, imediatamente acima da forma de pagamento — nunca herdada do cabeçalho fixo nem de outro frame, (b) o status verde ("Pago" ou "Pedido finalizado") visível na BASE do bloco, e (c) a LINHA FINA divisória visível separando este bloco do vizinho. Se QUALQUER uma dessas três fronteiras não estiver visível — cartão cortado no topo da imagem, cortado na base, sem linha fina clara entre as informações, OU cartão que começa colado no cabeçalho fixo (abas "Corridas/Entregas" + filtros) sem nenhuma linha de data/hora entre eles — NÃO EXTRAIA essa corrida. Um cartão logo abaixo do cabeçalho fixo com categoria+valor/taxa/endereços/status visíveis mas SEM data/hora acima já teve a data/hora dele engolida pelo cabeçalho — trate exatamente como cartão cortado, mesmo que o resto pareça completo. É melhor deixar de fora do que adivinhar: esse mesmo cartão aparece COMPLETO, com a data/hora visível, em um frame anterior (alguns instantes antes, antes de rolar pra debaixo do cabeçalho). NUNCA complete um cartão cortado com dados do vizinho, e NUNCA reaproveite ou "lembre" uma data/hora vista em outro frame — a data/hora tem que estar escrita nesta mesma imagem, colada nesse cartão específico. ' +
    'CADA HORÁRIO (HH:MM) IDENTIFICA UMA ÚNICA CORRIDA: nunca retorne dois itens com o mesmo "time" — se o mesmo horário aparecer repetido, é o mesmo cartão visto duas vezes, retorne só um. ' +
    'LINHAS DIVISÓRIAS: cada corrida fica delimitada por LINHAS HORIZONTAIS FINAS na tela. O bloco de uma corrida é: data/hora → forma de pagamento → Pop + valor → tag de taxa → endereços → linha fina → status verde ("Pago" ou "Pedido finalizado"). Use essas linhas finas como fronteira: TODOS os campos de uma corrida (hora, valor, taxa, endereços, status) ficam juntos dentro do mesmo bloco, entre duas linhas finas. NUNCA combine um campo de um bloco com um campo de outro bloco — se uma linha fina separa a data/hora de um valor ou endereço, eles pertencem a corridas DIFERENTES. ' +
    'O valor R$X,XX de cada corrida está DENTRO do mesmo cartão que a data/hora dela (mesma caixa, entre a linha da data e o status final) — NUNCA use o valor de um cartão vizinho de cima ou de baixo. Se o valor do cartão não estiver visível por inteiro dentro dele, IGNORE a corrida em vez de pegar o valor de outro cartão. ' +
    'BAIRRO NUNCA É A CIDADE: a linha de endereço da 99 termina com a CIDADE e UF (ex: "..., Teresópolis - RJ") e muitas vezes NEM TEM bairro — só rua/estrada, número, CEP e cidade (ex: "Estr. Teresópolis x Friburgo, nº 8500, 25976-655, Teresópolis - RJ"). Nesse caso use o nome da rua/estrada em "origin"/"dest" (no exemplo: "Estr. Teresópolis x Friburgo"). O nome da cidade sozinho NUNCA vai em "origin"/"dest" — cuidado: a palavra "Teresópolis" pode fazer parte do nome de uma estrada (aí pode) mas nunca deve ser usada sozinha por ser a cidade do fim da linha. Sem rua legível, use "" (string vazia). ' +
    'IGNORE corridas com valor R$0,00 ou cujo valor não esteja claramente legível — corrida real da 99 sempre tem valor maior que zero. ' +
    'AUTOCONFERÊNCIA FINAL: antes de devolver o JSON, revise item por item e confirme: (1) o bloco da corrida estava 100% delimitado NESTA imagem — data/hora própria do cartão (não colada no cabeçalho fixo) + status na base + linhas finas; (2) time, value, taxa e endereços vieram TODOS do MESMO cartão; (3) nenhum "time" foi copiado, adivinhado ou "lembrado" de outro cartão/frame — se a data/hora não está escrita nesta imagem colada nesse cartão, o item inteiro sai do JSON. Remova qualquer item que não passe nas três checagens. ' +
    '"km" e "duration" sempre 0 pois não aparecem no histórico. ' +
    '"dinamico" e "gorjeta" sempre 0.00 pois não aparecem no histórico da 99. ' +
    'Formato de cada item: {"platform":"99","time":"HH:MM","value":0.00,"km":0.0,"duration":0,"category":"99Pop","origin":"bairro","originFull":"endereço completo","dest":"bairro","destFull":"endereço completo","dinamico":0.00,"gorjeta":0.00,"pedagio":0.00,"serviceFee":0.00,"dinheiro":false}. "serviceFee" é sempre o número da tag "Taxa Fixa de X%" convertido pra decimal (15% → 0.15). "dinheiro":true quando aparecer "Ganhos pagos em dinheiro".\n\n' +
    '=== SE FOR 99 DETALHE ===\n' +
    'Esta tela mostra UMA única corrida, mas é ROLÁVEL: um frame de vídeo captura só uma PARTE dela por vez. Essa tela serve SÓ pra completar km, duração e endereço completo de uma corrida que a lista já identificou — NÃO extraia valor, dinheiro, taxa, dinâmico, gorjeta nem pedágio aqui (a l
===== laranja @ 822200 =====
o"). Use essas linhas finas como fronteira: TODOS os campos de uma corrida (hora, valor, taxa, endereços, status) ficam juntos dentro do mesmo bloco, entre duas linhas finas. NUNCA combine um campo de um bloco com um campo de outro bloco — se uma linha fina separa a data/hora de um valor ou endereço, eles pertencem a corridas DIFERENTES. ' +
    'O valor R$X,XX de cada corrida está DENTRO do mesmo cartão que a data/hora dela (mesma caixa, entre a linha da data e o status final) — NUNCA use o valor de um cartão vizinho de cima ou de baixo. Se o valor do cartão não estiver visível por inteiro dentro dele, IGNORE a corrida em vez de pegar o valor de outro cartão. ' +
    'BAIRRO NUNCA É A CIDADE: a linha de endereço da 99 termina com a CIDADE e UF (ex: "..., Teresópolis - RJ") e muitas vezes NEM TEM bairro — só rua/estrada, número, CEP e cidade (ex: "Estr. Teresópolis x Friburgo, nº 8500, 25976-655, Teresópolis - RJ"). Nesse caso use o nome da rua/estrada em "origin"/"dest" (no exemplo: "Estr. Teresópolis x Friburgo"). O nome da cidade sozinho NUNCA vai em "origin"/"dest" — cuidado: a palavra "Teresópolis" pode fazer parte do nome de uma estrada (aí pode) mas nunca deve ser usada sozinha por ser a cidade do fim da linha. Sem rua legível, use "" (string vazia). ' +
    'IGNORE corridas com valor R$0,00 ou cujo valor não esteja claramente legível — corrida real da 99 sempre tem valor maior que zero. ' +
    'AUTOCONFERÊNCIA FINAL: antes de devolver o JSON, revise item por item e confirme: (1) o bloco da corrida estava 100% delimitado NESTA imagem — data/hora própria do cartão (não colada no cabeçalho fixo) + status na base + linhas finas; (2) time, value, taxa e endereços vieram TODOS do MESMO cartão; (3) nenhum "time" foi copiado, adivinhado ou "lembrado" de outro cartão/frame — se a data/hora não está escrita nesta imagem colada nesse cartão, o item inteiro sai do JSON. Remova qualquer item que não passe nas três checagens. ' +
    '"km" e "duration" sempre 0 pois não aparecem no histórico. ' +
    '"dinamico" e "gorjeta" sempre 0.00 pois não aparecem no histórico da 99. ' +
    'Formato de cada item: {"platform":"99","time":"HH:MM","value":0.00,"km":0.0,"duration":0,"category":"99Pop","origin":"bairro","originFull":"endereço completo","dest":"bairro","destFull":"endereço completo","dinamico":0.00,"gorjeta":0.00,"pedagio":0.00,"serviceFee":0.00,"dinheiro":false}. "serviceFee" é sempre o número da tag "Taxa Fixa de X%" convertido pra decimal (15% → 0.15). "dinheiro":true quando aparecer "Ganhos pagos em dinheiro".\n\n' +
    '=== SE FOR 99 DETALHE ===\n' +
    'Esta tela mostra UMA única corrida, mas é ROLÁVEL: um frame de vídeo captura só uma PARTE dela por vez. Essa tela serve SÓ pra completar km, duração e endereço completo de uma corrida que a lista já identificou — NÃO extraia valor, dinheiro, taxa, dinâmico, gorjeta nem pedágio aqui (a lista já tem isso, e misturar valor lido nesta tela causava corrida duplicada/fantasma no import). Extraia como FRAGMENTO: pegue os campos abaixo que estiverem legíveis NESTE frame, deixe os que não aparecerem como null. ' +
    'Campos: "duration" = Duração convertida pra MINUTOS INTEIROS arredondados (12m50s → 13; 5m42s → 6), null se não aparecer neste frame. "km" = número de Distância (6,2 km → 6.2), null se não aparecer. "time" = horário HH:MM da data completa (ex "06/07/2026 15:51:42" → "15:51"), null se não aparecer neste frame. "originFull"/"destFull" = endereço completo dos pontos verde(origem)/laranja(destino) quando visíveis, null se não aparecerem; "origin"/"dest" = bairro extraído desses endereços (nunca a cidade sozinha — mesma regra da lista). ' +
    'Formato (retorne um array com este único item; campos não vistos = null): {"platform":"99","screen":"detail","time":null,"km":null,"duration":null,"origin":null,"originFull":null,"dest":null,"destFull":null}.\n\n' +
    'Retorne SOMENTE um único JSON array, sem texto extra. Se não houver corridas completas retorne [].';
  const requestController = new AbortController();
  const requestTimer = setTimeout(() => requestController.abort(), 25000);
  let resp;
  try {
    resp = await fetch('https://smartmobi-proxy.yurisilva1990.workers.dev', {
      method:'POST', headers:{'Content-Type':'application/json'}, signal:requestController.signal,
      body: JSON.stringify({model:'claude-haiku-4-5-20251001', max_tokens:1500,
        messages:[{role:'user', content:[{type:'image',source:{type:'base64',media_type:mt,data:b64}},{type:'text',text:prompt}]}]})
    });
  } catch(e) {
    if (e && e.name === 'AbortError') throw new Error('Tempo esgotado ao analisar quadro');
    throw e;
  } finally {
    clearTimeout(requestTimer);
  }
  if (!resp.ok) throw new Error('Worker error ' + resp.status);
  const data = await resp.json();
  const raw  = ((data.content?.[0]?.text)||'[]').replace(/```json|```/g,'').trim();
  const arr  = JSON.parse(raw);
  return Array.isArray(arr) ? arr.map(t => {
    const isDetail = t.screen === 'detail';
    const numOrNull = v => (v === null || v === undefined || v === '') ? null : (parseFloat(String(v).replace(',','.'))||0);
    const dinamico = isDetail ? numOrNull(t.dinamico) : (parseFloat(String(t.dinamico||0).replace(',','.'))||0);
    const tip = parseFloat(String(t.gorjeta||0).replace(',','.'))||0;
    const pedagioVal = parseFloat(String(t.pedagio||0).replace(',','.'))||0;
    // serviceFee: número real da taxa (0.15 = 15%). taxaZero mantido por
    // compat (código antigo), mas agora sempre derivado do serviceFee real.
    const hasFee = t.serviceFee !== undefined && t.serviceFee !== null && t.serviceFee !== '';
    const serviceFee = hasFee ? parseFloat(String(t.serviceFee).replace(',','.'))||0 : null;
    // Plataforma: confia no que a IA identificou pela imagem. Só cai no
    // botão que o usuário tocou (plat) se a IA não devolveu nada — rede de
    // segurança, não a fonte principal (era o bug antigo: botão errado
    // salvava tudo com a plataforma trocada).
    const detected = String(t.platform||'').toLowerCase();
    const platform = detected === '99' ? '99' : detected === 'uber' ? 'uber'
      : (plat && plat.toLowerCase()==='99' ? '99' : 'uber');
    // Rótulo do trajeto: quando a IA não acha bairro (endereço só com
    // rua/estrada + CEP + cidade), usa a primeira parte do endereço
    // completo, abreviada — melhor "Estr. Teresópolis x Fri…" que "—".
    const shortPlace = s => {
      const seg = String(s||'').split(',')[0].trim();
      return seg.length > 24 ? seg.slice(0,23).trimEnd()+'…' : seg;
    };
    let oFull, dFull, origin, dest;
    if (isDetail) {
      // Fragmento — pode não ter endereço neste frame específico; null fica null.
      oFull = t.originFull || null; dFull = t.destFull || null;
      origin = t.origin ? String(t.origin).trim() : (oFull ? shortPlace(oFull) : null);
      dest   = t.dest   ? String(t.dest).trim()   : (dFull ? shortPlace(dFull) : null);
    } else {
      oFull = t.originFull || t.origin || ''; dFull = t.destFull || t.dest || '';
      origin = (t.origin||'').trim() || shortPlace(oFull);
      dest   = (t.dest||'').trim()   || shortPlace(dFull);
    }
    return {...t, platform, origin, dest,
      value:parseFloat(String(t.value||0).replace(',','.'))||0,
      km: isDetail ? numOrNull(t.km) : (parseFloat(String(t.km||0).replace(',','.'))||0),
      duration: isDetail ? (t.duration==null ? null : (parseInt(t.duration)||0)) : (parseInt(t.duration)||0),
      dinamico, tip, surge: dinamico > 0,
      pedagioValor: pedagioVal, pedagio: pedagioVal > 0,
      originFull: oFull, destFull: dFull,
      serviceFee: serviceFee,
      taxaZero: serviceFee === 0 || t.taxaZero===true||t.taxaZero==='true'||t.taxaZero===1,
      dinheiro: isDetail
        ? (t.dinheiro===true||t.dinheiro==='true'||t.dinheiro===1 ? true : (t.dinheiro===false||t.dinheiro==='false' ? false : null))
        : (t.dinheiro===true||t.dinheiro==='true'||t.dinheiro===1),
      time: isDetail ? (t.time ? normTime(t.time) : null) : t.time };
  }) : [];
}


function buildProgressHTML(current, total, found) {
  return '<div style="text-align:center;padding:28px 16px">' +
    '<div style="font-size:28px;animation:vspin 1s linear infinite;display:inline-block">&#9881;</div>' +
    '<div style="margin-top:12px;font-size:14px;font-weight:700">' + (current||'0') + ' de ' + total + ' ' + (typeof total==='number'&&total>1?'imagens':'frame') + '</div>' +
    '<div style="font-size:12px;color:#64748B;margin-top:4px">' + found + ' corridas encontradas até agora</div>' +
    '<div style="height:6px;background:#E2E8F0;border-radius:3px;margin-top:14px;overflow:hidden">' +
    '<div style="height:100%;background:#22C55E;border-radius:3px;width:' + (total&&typeof total==='number'?Math.round(current/total*100):50) + '%;transition:width .4s"></div></div></div>';
}


function mergeTrips(existing, newTrips) {
  const all = [...existing];
  for (const t of newTrips) {
    const dup = all.find(e =>
      e.time === t.time &&
      Math.abs((parseFloat(e.value)||0) - (parseFloat(t.value)||0)) < 0.5
    );
    if (!dup) all.push(t);
  }
  return all;
}



// Mostra o que foi declarado no pré-check (antes de importar) lado a lado
// com o que está selecionado agora na lista, com um ✓/✗ por plataforma —
// pra dar pra comparar sem ter que guardar o número de cabeça. Só aparece
// se algo foi de fato declarado (senão fica vazio, não polui a tela).
function importDeclaredHtml() {
  const pc = importPrecheck || {};
  if (pc.uberCount == null && pc.p99Count == null) return '';
  const sel = (window._pendingTrips || []).filter(t => t._sel);
  const uber = sel.filter(t => t.platform !== '99');
  const p99  = sel.filter(t => t.platform === '99');
  const row = (label, color, decCount, decVal, foundArr) => {
    if (decCount == null) return '';
    const foundCount = foundArr.length;
    const foundVal = foundArr.reduce((s, t) => s + (t.value || 0), 0);
    let ok = foundCount === decCount;
    if (ok && de
===== laranja @ 1713788 =====
me){
  _infRelState.address = nome;
  infFecharOv('inf-ov-gps');
  openMo('mo-inf-relatar');
  const el = document.getElementById('inf-relatar-loc-txt');
  if (el) el.textContent = nome;
}
function infRelatarShowBack(label, fn){
  _infRelState.backFn = fn;
  const back = document.getElementById('inf-relatar-back');
  back.style.display = 'flex';
  document.getElementById('inf-relatar-back-lbl').textContent = label;
}
function infRelatarBack(){ if (_infRelState.backFn) _infRelState.backFn(); }

function infRelatarSelType(key){
  _infRelState.type = key;
  if (key === 'transito') return infRelatarShowTransito();
  if (key === 'blitz') return infRelatarShowPoliciamento();
  if (key === 'risco') return infRelatarShowAlertaStep1();
}

// Trânsito — barra 0 a 5, 1 toque publica.
function infRelatarShowTransito(){
  document.getElementById('inf-relatar-title').textContent = 'Como tá o trânsito?';
  infRelatarShowBack('Trânsito', infOpenRelatar);
  const cores = ['#DC2626','#EF4444','#F59E0B','#EAB308','#84CC16','#22C55E'];
  document.getElementById('inf-relatar-body').innerHTML =
    `<div class="inf-tbar">${cores.map((c,i)=>`<button class="inf-tbar-seg" style="background:${c}" onclick="infRelatarPublish({nivel:${i}})">${i}</button>`).join('')}</div>
     <div class="inf-tbar-labels"><span>Parado</span><span>Livre</span></div>`;
}

// Policiamento — "quem" (2 col) + "fazendo o quê" (3 col) na mesma tela;
// publica sozinho assim que os dois têm uma opção marcada.
function infRelatarShowPoliciamento(){
  document.getElementById('inf-relatar-title').textContent = 'Fiscalização';
  infRelatarShowBack('Fiscalização', infOpenRelatar);
  _infRelState.orgao = null; _infRelState.acao = null;
  infRelatarRenderPoliciamento();
}
function infRelatarRenderPoliciamento(){
  const orgaos = [['Polícia Militar','pm'],['Guarda Municipal','gm'],['Lei Seca','leiseca'],['Detro','detro']];
  const acoes = [['Multando','multando'],['Abordando','abordando'],['Posicionado','posicionado']];
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-combo-row">
      <div class="inf-combo-lbl">Quem</div>
      <div class="inf-tile-grid2">${orgaos.map(([lbl,v])=>`<div class="inf-tile ${_infRelState.orgao===v?'picked':''}" onclick="infRelatarPickOrgao('${v}',${JSON.stringify(lbl)})">${lbl}</div>`).join('')}</div>
    </div>
    <div class="inf-combo-row" style="margin-bottom:4px">
      <div class="inf-combo-lbl">Fazendo o quê</div>
      <div class="inf-tile-grid3">${acoes.map(([lbl,v])=>`<div class="inf-tile ${_infRelState.acao===v?'picked':''}" onclick="infRelatarPickAcao('${v}',${JSON.stringify(lbl)})">${lbl}</div>`).join('')}</div>
    </div>`;
}
function infRelatarPickOrgao(v,lbl){ _infRelState.orgao=v; _infRelState.orgaoLbl=lbl; infRelatarRenderPoliciamento(); infRelatarMaybePublishPoliciamento(); }
function infRelatarPickAcao(v,lbl){ _infRelState.acao=v; _infRelState.acaoLbl=lbl; infRelatarRenderPoliciamento(); infRelatarMaybePublishPoliciamento(); }
function infRelatarMaybePublishPoliciamento(){
  if (_infRelState.orgao && _infRelState.acao) {
    // Pequena pausa só pra pessoa ver a segunda opção marcada antes de
    // sumir a tela — publica sozinho, sem precisar de botão confirmar.
    setTimeout(()=>infRelatarPublish({param_value:_infRelState.orgaoLbl, param_detail:_infRelState.acaoLbl}), 350);
  }
}

// Alerta — 2 etapas de verdade, a segunda pergunta depende da primeira
// resposta. Cores fortes (não pastel) — pedido do Yuri: Acidente=laranja,
// Área de risco=vermelho (invertido do que eu tinha proposto antes).
function infRelatarShowAlertaStep1(){
  document.getElementById('inf-relatar-title').textContent = 'Qual alerta?';
  infRelatarShowBack('Alerta', infOpenRelatar);
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-tile-grid2">
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarShowAcidenteStep2()">Acidente</div>
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarShowAreaRiscoStep2()">Área de risco</div>
    </div>`;
}
function infRelatarShowAcidenteStep2(){
  document.getElementById('inf-relatar-title').textContent = 'Onde exatamente?';
  infRelatarShowBack('Acidente', infRelatarShowAlertaStep1);
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-tile-grid2">
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarPublish({param_value:'Acidente', param_detail:'Na via'})">Na via</div>
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarPublish({param_value:'Acidente', param_detail:'No acostamento'})">No acostamento</div>
    </div>`;
}
function infRelatarShowAreaRiscoStep2(){
  document.getElementById('inf-relatar-title').textContent = 'Área de Risco?';
  infRelatarShowBack('Área de risco', infRelatarShowAlertaStep1);
  document.getElementById('inf-relatar-body').innerHTML = `
    <div class="inf-tile-grid2">
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Via de acesso'})">Via de acesso</div>
      <div class="inf-tile-strong inf-strong-orange" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Área de abordagem'})">Área de abordagem</div>
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Proibido a entrada'})">Proibido a entrada</div>
      <div class="inf-tile-strong inf-strong-red" onclick="infRelatarPublish({param_value:'Área de risco', param_detail:'Barricada'})">Barricada</div>
    </div>`;
}

// Publica de verdade em feed_posts — usa a localização já detectada
// quando o modal abriu (mais rápido que pedir de novo agora).
function infRelatarPublish(extra){
  document.getElementById('inf-relatar-title').textContent = 'Publicando…';
  document.getElementById('inf-relatar-back').style.display = 'none';
  document.getElementById('inf-relatar-body').innerHTML = `
    <div style="display:flex;flex-direction:column;align-items:center;padding:24px 10px 8px;text-align:center">
      <div style="width:40px;height:40px;border-radius:50%;border:3.5px solid #DBEAFE;border-top-color:#2563EB;animation:spin .8s linear infinite;margin-bottom:14px"></div>
      <div style="font-size:12.5px;color:var(--muted)">Confirmando localização</div>
    </div>`;
  infRelatarDoPublish(extra);
}
async function infRelatarDoPublish(extra){
  if (!_supaUser) { closeMo('mo-inf-relatar'); showToast('Faça login pra relatar.', 'error'); return; }
  const t = (_infRelState.types||{})[_infRelState.type];
  const expMin = t?.expMin || 30;
  const row = {
    user_id:_supaUser.id, type:_infRelState.type, audience:'comunidade', source:'user',
    lat:_infRelState.lat ?? null, lng:_infRelState.lng ?? null,
    address:_infRelState.address || null, city:'rio',
    param_value:extra.param_value ?? null, param_detail:extra.param_detail ?? null,
    nivel: extra.nivel ?? null,
    expires_at:new Date(Date.now()+expMin*60000).toISOString(),
    votes_up:1, votes_dn:0, comments_count:0, status:'ativo'
  };
  const { error } = await _SUPA.from('feed_posts').insert(row);
  setTimeout(()=>{
    closeMo('mo-inf-relatar');
    if (error) { console.error('infRelatarDoPublish', error); showToast('Erro ao publicar. Tenta de novo.', 'error'); return; }
    showToast('Publicado!', 'success');
    if (_infLoc) infCarregarInformes(_infLoc, _infTipoKey);
  }, 500);
}
function infSetTime(btn,min){
  document.querySelectorAll('.inf-tp').forEach(b=>b.classList.remove('active'));btn.classList.add('active');
  _infMinutos=parseInt(min);infCarregarContagens();
  if(_infLoc)infCarregarInformes(_infLoc,_infTipoKey);
}
function infFecharOv(id){document.getElementById(id).classList.remove('open');}
function infFiltrarSheet(inp,listId){
  const v=inp.value.toLowerCase();
  document.querySelectorAll('#'+listId+' .inf-loc-item').forEach(el=>{const t=el.querySelector('.inf-loc-name')?.textContent.toLowerCase()||'';el.style.display=(!v||t.includes(v))?'':'none';});
}
function infIconSvg(tipo,cor,sz){
  const a=`viewBox="0 0 24 24" width="${sz}" height="${sz}" fill="none" stroke="${cor}" stroke-width="2"`;
  const m={aero:`<svg ${a}><path d="M21 16v-2l-8-5V3.5a1.5 1.5 0 0 0-3 0V9l-8 5v2l8-2.5V19l-2 1.5V22l3.5-1 3.5 1v-1.5L13 19v-5.5z"/></svg>`,term:`<svg ${a}><rect x="1" y="3" width="15" height="13" rx="2"/><path d="M16 8h4l3 3v5h-7V8z"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>`,bairro:`<svg ${a}><circle cx="12" cy="10" r="3"/><path d="M12 2a8 8 0 0 1 8 8c0 5.333-8 14-8 14S4 15.333 4 10a8 8 0 0 1 8-8z"/></svg>`,rua:`<svg ${a}><circle cx="12" cy="12" r="3"/><path d="M12 2v3M12 19v3M2 12h3M19 12h3"/></svg>`,ev:`<svg ${a}><rect x="2" y="7" width="20" height="14" rx="2"/><path d="M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"/></svg>`,tur:`<svg ${a}><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>`,bar:`<svg ${a}><path d="M17 11H3v-1a5 5 0 0 1 5-5h4a5 5 0 0 1 5 5v1z"/><path d="M3 11v5a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2v-5"/></svg>`,hotel:`<svg ${a}><rect x="2" y="7" width="20" height="15" rx="2"/><path d="M2 12h20"/><path d="M8 7V5a4 4 0 0 1 8 0v2"/></svg>`,shop:`<svg ${a}><path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4Z"/><path d="M3 6h18"/><path d="M16 10a4 4 0 0 1-8 0"/></svg>`};
  return m[tipo]||m.bairro;
}
// Gesto swipe down para fechar o painel
(function() {
  const panel = document.getElementById('inf-panel');
  const drag  = document.getElementById('inf-panel-drag');
  if (!panel || !drag) return;
  let startY = 0, isDragging = false;
  function onStart(e) {
    startY = (e.touches ? e.touches[0].clientY : e.clientY);
    isDragging = true;
    panel.style.transition = 'none';
  }
  function onMove(e) {
    if (!isDragging) return;
    const dy = (e.touches ? e.touches[0].clientY : e.clientY) - startY;
    if (dy > 0) panel.style.transform = 'translateY
===== upload @ 955556 =====
xão') : 'Tudo sincronizado';

  const verEl = document.getElementById('cfg-version');
  if (verEl) verEl.textContent = 'MōB · v' + (window.SmartMobiNative?.getVersion?.() || '—');

  openMo('mo-config');
}
function toggleDarkMode(el) {
  const cfg = DB.cfg();
  cfg.darkMode = el.checked;
  DB.saveCfg(cfg);
  applyDarkMode(cfg.darkMode);
}
function applyDarkMode(on) {
  document.documentElement.classList.toggle('dark', !!on);
}
function toggleOverlayPref(el) {
  const cfg = DB.cfg();
  cfg.overlayEnabled = el.checked;
  DB.saveCfg(cfg);
  if (!window.SmartMobiNative) return;
  try {
    if (el.checked) {
      // Se a jornada já estiver rolando, mostra o ícone na hora, sem esperar o próximo sync.
      if (S.gps.st === 'running') {
        SmartMobiNative.startFloating?.(S.gps.start, S.gps.km || 0);
        SmartMobiNative.updateFloatingStatus?.(S.gps.st);
      }
    } else {
      SmartMobiNative.hideFloatingOnly?.();
    }
  } catch(e) {}
}
async function savePerfil() {
  const cfg = DB.cfg();
  cfg.name    = document.getElementById('p-nome')?.value || '';
  cfg.city    = document.getElementById('p-cidade')?.value || '';
  cfg.plate   = document.getElementById('p-placa')?.value || '';

  DB.saveCfg(cfg);
  // Grava na nuvem também — antes esta função só salvava local, então uma
  // edição de nome nunca chegava em profiles.name, e o feed (que lê de lá
  // pros posts de outros usuários) continuava mostrando o nome antigo.
  if (_SUPA && _supaUser) {
    try {
      await _SUPA.from('profiles').upsert(
        { id:_supaUser.id, name:cfg.name, city:cfg.city, plate:cfg.plate, vehicle:cfg.vehicle||null },
        { onConflict:'id' }
      );
    } catch(e) { console.warn('perfil upsert', e); }
  }
  closeMo('mo-perfil');
  renderMais();
  if (S.sec==='relatorios') renderHome();
  showToast('Perfil salvo!', 'success');
}

// ── Foto de perfil ────────────────────────────────────────────
function perfilOpenPhotoSheet() { openMo('mo-perfil-foto'); }
function perfilPickCamera()  { closeMo('mo-perfil-foto'); document.getElementById('p-file-camera')?.click(); }
function perfilPickGallery() { closeMo('mo-perfil-foto'); document.getElementById('p-file-gallery')?.click(); }

// Corta em quadrado (cover, centralizado) e comprime antes de subir —
// evita gastar dados do motorista com foto de câmera em resolução total.
function perfilResizeImage(file, size, quality) {
  size = size || 480; quality = quality || 0.85;
  return new Promise((resolve, reject) => {
    const img = new Image();
    const url = URL.createObjectURL(file);
    img.onload = () => {
      URL.revokeObjectURL(url);
      const side = Math.min(img.width, img.height);
      const sx = (img.width - side) / 2, sy = (img.height - side) / 2;
      const canvas = document.createElement('canvas');
      canvas.width = size; canvas.height = size;
      const ctx = canvas.getContext('2d');
      ctx.drawImage(img, sx, sy, side, side, 0, 0, size, size);
      canvas.toBlob(blob => blob ? resolve(blob) : reject(new Error('toBlob falhou')), 'image/jpeg', quality);
    };
    img.onerror = () => { URL.revokeObjectURL(url); reject(new Error('Imagem inválida')); };
    img.src = url;
  });
}

async function perfilFileSelected(inputEl) {
  const file = inputEl.files && inputEl.files[0];
  inputEl.value = ''; // permite selecionar o mesmo arquivo de novo depois
  if (!file) return;
  if (!file.type || !file.type.startsWith('image/')) { showToast('Selecione uma imagem.'); return; }
  await perfilUploadAvatar(file);
}

async function perfilUploadAvatar(file) {
  if (!_SUPA || !_supaUser) return;
  showToast('Enviando foto…', 'info');
  try {
    const blob = await perfilResizeImage(file, 480, 0.85);
    const path = _supaUser.id + '/avatar.jpg';
    const { error: upErr } = await _SUPA.storage.from('avatars').upload(path, blob, { upsert: true, contentType: 'image/jpeg' });
    if (upErr) throw upErr;
    const { data: pub } = _SUPA.storage.from('avatars').getPublicUrl(path);
    const url = pub.publicUrl + '?t=' + Date.now(); // cache-busting — mesmo path, arquivo novo
    await _SUPA.from('profiles').upsert({ id:_supaUser.id, avatar_url:url }, { onConflict:'id' });
    const cfg = DB.cfg(); cfg.avatarUrl = url; DB.saveCfg(cfg);
    perfilRenderAvatar();
    showToast('Foto atualizada!', 'success');
  } catch (e) {
    console.warn('avatar upload', e);
    showToast('Erro ao enviar a foto. Tente de novo.');
  }
}

async function perfilRemovePhoto() {
  closeMo('mo-perfil-foto');
  const cfg = DB.cfg();
  if (!cfg.avatarUrl) return;
  if (_SUPA && _supaUser) {
    try {
      await _SUPA.storage.from('avatars').remove([_supaUser.id + '/avatar.jpg']);
      await _SUPA.from('profiles').upsert({ id:_supaUser.id, avatar_url:null }, { onConflict:'id' });
    } catch (e) { console.warn('remove avatar', e); }
  }
  cfg.avatarUrl = null; DB.saveCfg(cfg);
  perfilRenderAvatar();
  showToast('Foto removida.');
}

// ── SELETOR DE VEÍCULO (marca → modelo → ano) ────────────────────
const VEHICLE_BRANDS = {
  'Chevrolet': ['Onix','Onix Plus','Prisma','Spin','Tracker','Joy','Cobalt'],
  'Fiat': ['Mobi','Argo','Cronos','Pulse','Strada','Uno','Siena'],
  'Volkswagen': ['Gol','Voyage','Polo','Virtus','T-Cross','Nivus','Saveiro','Up!'],
  'Hyundai': ['HB20','HB20S','Creta','Tucson','i30'],
  'Toyota': ['Etios','Yaris','Corolla','Corolla Cross','SW4'],
  'Honda': ['Fit','City','HR-V','Civic','WR-V'],
  'Renault': ['Kwid','Sandero','Logan','Duster','Stepway'],
  'Nissan': ['March','Versa','Kicks','Sentra'],
  'Ford': ['Ka','Ka Sedan','EcoSport','Fiesta'],
  'Jeep': ['Renegade','Compass'],
  'Citroën': ['C3','C4 Cactus'],
  'Peugeot': ['208','2008'],
  'Mitsubishi': ['Space Star','ASX'],
  'Kia': ['Picanto','Soul','Sportage'],
  'BYD': ['Dolphin Mini','Dolphin','Yuan Plus','Song Plus','Seal'],
  'GWM': ['Haval H6'],
  'Caoa Chery': ['Tiggo 2','Tiggo 3X','Tiggo 5X'],
  'Outra marca': []
};
function vpPopulateMarcas() {
  const sel = document.getElementById('vp-marca');
  if (!sel) return;
  sel.innerHTML = Object.keys(VEHICLE_BRANDS).map(b => `<option value="${b}">${b}</option>`).join('');
}
function vpPopulateAnos() {
  const sel = document.getElementById('vp-ano');
  if (!sel) return;
  const max = new Date().getFullYear() + 1;
  let opts = '';
  for (let y = max; y >= 1995; y--) opts += `<option value="${y}">${y}</option>`;
  sel.innerHTML = opts;
}
function vpOnMarcaChange() {
  const marca = document.getElementById('vp-marca')?.value;
  const outraWrap = document.getElementById('vp-marca-outra-wrap');
  if (outraWrap) outraWrap.style.display = marca === 'Outra marca' ? 'block' : 'none';
  const modelos = VEHICLE_BRANDS[marca] || [];
  const modSel = document.getElementById('vp-modelo');
  if (modSel) modSel.innerHTML = modelos.map(m => `<option value="${m}">${m}</option>`).join('') + '<option value="Outro modelo">Outro modelo</option>';
  vpOnModeloChange();
}
function vpOnModeloChange() {
  const modelo = document.getElementById('vp-modelo')?.value;
  const outroWrap = document.getElementById('vp-modelo-outro-wrap');
  if (outroWrap) outroWrap.style.display = modelo === 'Outro modelo' ? 'block' : 'none';
}
function openVeiculoPicker() {
  vpPopulateMarcas();
  vpPopulateAnos();
  const cfg = DB.cfg();
  const marcaSel = document.getElementById('vp-marca');
  if (marcaSel && cfg.vehicleBrand && VEHICLE_BRANDS[cfg.vehicleBrand] !== undefined) marcaSel.value = cfg.vehicleBrand;
  vpOnMarcaChange();
  const modSel = document.getElementById('vp-modelo');
  if (modSel && cfg.vehicleModel) modSel.value = cfg.vehicleModel;
  vpOnModeloChange();
  const anoSel = document.getElementById('vp-ano');
  if (anoSel && cfg.vehicleYear) anoSel.value = cfg.vehicleYear;
  if (cfg.vehicleBrand === 'Outra marca') { const el=document.getElementById('vp-marca-outra'); if (el) el.value = cfg.vehicleBrandCustom || ''; }
  if (cfg.vehicleModel === 'Outro modelo') { const el=document.getElementById('vp-modelo-outro'); if (el) el.value = cfg.vehicleModelCustom || ''; }
  openMo('mo-veiculo');
}
function saveVeiculoPicker() {
  const cfg = DB.cfg();
  let marca = document.getElementById('vp-marca')?.value || '';
  let modelo = document.getElementById('vp-modelo')?.value || '';
  const ano = document.getElementById('vp-ano')?.value || '';
  cfg.vehicleBrand = marca; cfg.vehicleModel = modelo; cfg.vehicleYear = ano;
  if (marca === 'Outra marca') { marca = document.getElementById('vp-marca-outra')?.value || 'Outra'; cfg.vehicleBrandCustom = marca; }
  if (modelo === 'Outro modelo') { modelo = document.getElementById('vp-modelo-outro')?.value || ''; cfg.vehicleModelCustom = modelo; }
  cfg.vehicle = [marca, modelo, ano].filter(Boolean).join(' ');
  DB.saveCfg(cfg);
  const vTxt = document.getElementById('p-veiculo-txt');
  if (vTxt) vTxt.textContent = cfg.vehicle || 'Selecionar veículo';
  closeMo('mo-veiculo');
}

// ── UTILS ─────────────────────────────────────────────────────
function setText(id, text) { const el=document.getElementById(id); if(el) el.textContent=text; }
function setHTML(id, html) { const el=document.getElementById(id); if(el) el.innerHTML=html; }
function setStyle(id, prop, val) { const el=document.getElementById(id); if(el) el.style[prop]=val; }

// ============================================================
// AGENDA — SmartMobi 1.1.0.0
// ============================================================

let agSelDate = DB.today();
let agEvType  = 'normal';
let agColor   = '#3B82F6';
const EV_TYPE_COLOR = { operacional: '#3B82F6', pessoal: '#7C3AED' };

// ── ICON SVGs por tipo ──────────────────────────────────────
const AG_ICONS = {
  jornada:  '<svg viewBox="0 0 24 24"><rect x="3" y="7" width="18" height="13" rx="2"/><path d="M8 7V5a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/><path d="M3 12h18"/></svg>',
  finance:  '<svg viewBox="0 0 24 24"><rect x="3" y="5" width="18" height="14" rx="2"/><path d="M3 10h18"/></svg>',
  default:  '<svg viewBox="0 0 24 24"><rect x="3" y="4" width="18" height="18" rx="2"
===== upload @ 956416 =====
ps.st);
      }
    } else {
      SmartMobiNative.hideFloatingOnly?.();
    }
  } catch(e) {}
}
async function savePerfil() {
  const cfg = DB.cfg();
  cfg.name    = document.getElementById('p-nome')?.value || '';
  cfg.city    = document.getElementById('p-cidade')?.value || '';
  cfg.plate   = document.getElementById('p-placa')?.value || '';

  DB.saveCfg(cfg);
  // Grava na nuvem também — antes esta função só salvava local, então uma
  // edição de nome nunca chegava em profiles.name, e o feed (que lê de lá
  // pros posts de outros usuários) continuava mostrando o nome antigo.
  if (_SUPA && _supaUser) {
    try {
      await _SUPA.from('profiles').upsert(
        { id:_supaUser.id, name:cfg.name, city:cfg.city, plate:cfg.plate, vehicle:cfg.vehicle||null },
        { onConflict:'id' }
      );
    } catch(e) { console.warn('perfil upsert', e); }
  }
  closeMo('mo-perfil');
  renderMais();
  if (S.sec==='relatorios') renderHome();
  showToast('Perfil salvo!', 'success');
}

// ── Foto de perfil ────────────────────────────────────────────
function perfilOpenPhotoSheet() { openMo('mo-perfil-foto'); }
function perfilPickCamera()  { closeMo('mo-perfil-foto'); document.getElementById('p-file-camera')?.click(); }
function perfilPickGallery() { closeMo('mo-perfil-foto'); document.getElementById('p-file-gallery')?.click(); }

// Corta em quadrado (cover, centralizado) e comprime antes de subir —
// evita gastar dados do motorista com foto de câmera em resolução total.
function perfilResizeImage(file, size, quality) {
  size = size || 480; quality = quality || 0.85;
  return new Promise((resolve, reject) => {
    const img = new Image();
    const url = URL.createObjectURL(file);
    img.onload = () => {
      URL.revokeObjectURL(url);
      const side = Math.min(img.width, img.height);
      const sx = (img.width - side) / 2, sy = (img.height - side) / 2;
      const canvas = document.createElement('canvas');
      canvas.width = size; canvas.height = size;
      const ctx = canvas.getContext('2d');
      ctx.drawImage(img, sx, sy, side, side, 0, 0, size, size);
      canvas.toBlob(blob => blob ? resolve(blob) : reject(new Error('toBlob falhou')), 'image/jpeg', quality);
    };
    img.onerror = () => { URL.revokeObjectURL(url); reject(new Error('Imagem inválida')); };
    img.src = url;
  });
}

async function perfilFileSelected(inputEl) {
  const file = inputEl.files && inputEl.files[0];
  inputEl.value = ''; // permite selecionar o mesmo arquivo de novo depois
  if (!file) return;
  if (!file.type || !file.type.startsWith('image/')) { showToast('Selecione uma imagem.'); return; }
  await perfilUploadAvatar(file);
}

async function perfilUploadAvatar(file) {
  if (!_SUPA || !_supaUser) return;
  showToast('Enviando foto…', 'info');
  try {
    const blob = await perfilResizeImage(file, 480, 0.85);
    const path = _supaUser.id + '/avatar.jpg';
    const { error: upErr } = await _SUPA.storage.from('avatars').upload(path, blob, { upsert: true, contentType: 'image/jpeg' });
    if (upErr) throw upErr;
    const { data: pub } = _SUPA.storage.from('avatars').getPublicUrl(path);
    const url = pub.publicUrl + '?t=' + Date.now(); // cache-busting — mesmo path, arquivo novo
    await _SUPA.from('profiles').upsert({ id:_supaUser.id, avatar_url:url }, { onConflict:'id' });
    const cfg = DB.cfg(); cfg.avatarUrl = url; DB.saveCfg(cfg);
    perfilRenderAvatar();
    showToast('Foto atualizada!', 'success');
  } catch (e) {
    console.warn('avatar upload', e);
    showToast('Erro ao enviar a foto. Tente de novo.');
  }
}

async function perfilRemovePhoto() {
  closeMo('mo-perfil-foto');
  const cfg = DB.cfg();
  if (!cfg.avatarUrl) return;
  if (_SUPA && _supaUser) {
    try {
      await _SUPA.storage.from('avatars').remove([_supaUser.id + '/avatar.jpg']);
      await _SUPA.from('profiles').upsert({ id:_supaUser.id, avatar_url:null }, { onConflict:'id' });
    } catch (e) { console.warn('remove avatar', e); }
  }
  cfg.avatarUrl = null; DB.saveCfg(cfg);
  perfilRenderAvatar();
  showToast('Foto removida.');
}

// ── SELETOR DE VEÍCULO (marca → modelo → ano) ────────────────────
const VEHICLE_BRANDS = {
  'Chevrolet': ['Onix','Onix Plus','Prisma','Spin','Tracker','Joy','Cobalt'],
  'Fiat': ['Mobi','Argo','Cronos','Pulse','Strada','Uno','Siena'],
  'Volkswagen': ['Gol','Voyage','Polo','Virtus','T-Cross','Nivus','Saveiro','Up!'],
  'Hyundai': ['HB20','HB20S','Creta','Tucson','i30'],
  'Toyota': ['Etios','Yaris','Corolla','Corolla Cross','SW4'],
  'Honda': ['Fit','City','HR-V','Civic','WR-V'],
  'Renault': ['Kwid','Sandero','Logan','Duster','Stepway'],
  'Nissan': ['March','Versa','Kicks','Sentra'],
  'Ford': ['Ka','Ka Sedan','EcoSport','Fiesta'],
  'Jeep': ['Renegade','Compass'],
  'Citroën': ['C3','C4 Cactus'],
  'Peugeot': ['208','2008'],
  'Mitsubishi': ['Space Star','ASX'],
  'Kia': ['Picanto','Soul','Sportage'],
  'BYD': ['Dolphin Mini','Dolphin','Yuan Plus','Song Plus','Seal'],
  'GWM': ['Haval H6'],
  'Caoa Chery': ['Tiggo 2','Tiggo 3X','Tiggo 5X'],
  'Outra marca': []
};
function vpPopulateMarcas() {
  const sel = document.getElementById('vp-marca');
  if (!sel) return;
  sel.innerHTML = Object.keys(VEHICLE_BRANDS).map(b => `<option value="${b}">${b}</option>`).join('');
}
function vpPopulateAnos() {
  const sel = document.getElementById('vp-ano');
  if (!sel) return;
  const max = new Date().getFullYear() + 1;
  let opts = '';
  for (let y = max; y >= 1995; y--) opts += `<option value="${y}">${y}</option>`;
  sel.innerHTML = opts;
}
function vpOnMarcaChange() {
  const marca = document.getElementById('vp-marca')?.value;
  const outraWrap = document.getElementById('vp-marca-outra-wrap');
  if (outraWrap) outraWrap.style.display = marca === 'Outra marca' ? 'block' : 'none';
  const modelos = VEHICLE_BRANDS[marca] || [];
  const modSel = document.getElementById('vp-modelo');
  if (modSel) modSel.innerHTML = modelos.map(m => `<option value="${m}">${m}</option>`).join('') + '<option value="Outro modelo">Outro modelo</option>';
  vpOnModeloChange();
}
function vpOnModeloChange() {
  const modelo = document.getElementById('vp-modelo')?.value;
  const outroWrap = document.getElementById('vp-modelo-outro-wrap');
  if (outroWrap) outroWrap.style.display = modelo === 'Outro modelo' ? 'block' : 'none';
}
function openVeiculoPicker() {
  vpPopulateMarcas();
  vpPopulateAnos();
  const cfg = DB.cfg();
  const marcaSel = document.getElementById('vp-marca');
  if (marcaSel && cfg.vehicleBrand && VEHICLE_BRANDS[cfg.vehicleBrand] !== undefined) marcaSel.value = cfg.vehicleBrand;
  vpOnMarcaChange();
  const modSel = document.getElementById('vp-modelo');
  if (modSel && cfg.vehicleModel) modSel.value = cfg.vehicleModel;
  vpOnModeloChange();
  const anoSel = document.getElementById('vp-ano');
  if (anoSel && cfg.vehicleYear) anoSel.value = cfg.vehicleYear;
  if (cfg.vehicleBrand === 'Outra marca') { const el=document.getElementById('vp-marca-outra'); if (el) el.value = cfg.vehicleBrandCustom || ''; }
  if (cfg.vehicleModel === 'Outro modelo') { const el=document.getElementById('vp-modelo-outro'); if (el) el.value = cfg.vehicleModelCustom || ''; }
  openMo('mo-veiculo');
}
function saveVeiculoPicker() {
  const cfg = DB.cfg();
  let marca = document.getElementById('vp-marca')?.value || '';
  let modelo = document.getElementById('vp-modelo')?.value || '';
  const ano = document.getElementById('vp-ano')?.value || '';
  cfg.vehicleBrand = marca; cfg.vehicleModel = modelo; cfg.vehicleYear = ano;
  if (marca === 'Outra marca') { marca = document.getElementById('vp-marca-outra')?.value || 'Outra'; cfg.vehicleBrandCustom = marca; }
  if (modelo === 'Outro modelo') { modelo = document.getElementById('vp-modelo-outro')?.value || ''; cfg.vehicleModelCustom = modelo; }
  cfg.vehicle = [marca, modelo, ano].filter(Boolean).join(' ');
  DB.saveCfg(cfg);
  const vTxt = document.getElementById('p-veiculo-txt');
  if (vTxt) vTxt.textContent = cfg.vehicle || 'Selecionar veículo';
  closeMo('mo-veiculo');
}

// ── UTILS ─────────────────────────────────────────────────────
function setText(id, text) { const el=document.getElementById(id); if(el) el.textContent=text; }
function setHTML(id, html) { const el=document.getElementById(id); if(el) el.innerHTML=html; }
function setStyle(id, prop, val) { const el=document.getElementById(id); if(el) el.style[prop]=val; }

// ============================================================
// AGENDA — SmartMobi 1.1.0.0
// ============================================================

let agSelDate = DB.today();
let agEvType  = 'normal';
let agColor   = '#3B82F6';
const EV_TYPE_COLOR = { operacional: '#3B82F6', pessoal: '#7C3AED' };

// ── ICON SVGs por tipo ──────────────────────────────────────
const AG_ICONS = {
  jornada:  '<svg viewBox="0 0 24 24"><rect x="3" y="7" width="18" height="13" rx="2"/><path d="M8 7V5a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/><path d="M3 12h18"/></svg>',
  finance:  '<svg viewBox="0 0 24 24"><rect x="3" y="5" width="18" height="14" rx="2"/><path d="M3 10h18"/></svg>',
  default:  '<svg viewBox="0 0 24 24"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18"/></svg>',
  car:      '<svg viewBox="0 0 24 24"><path d="M5 13l1.5-5A3 3 0 0 1 9.4 6h5.2a3 3 0 0 1 2.9 2l1.5 5"/><path d="M3 13h18v6H3z"/></svg>',
  wrench:   '<svg viewBox="0 0 24 24"><path d="M14.7 6.3a4 4 0 0 0-5 5L3 18l3 3 6.7-6.7a4 4 0 0 0 5-5z"/></svg>',
};

function agIconFor(ev) {
  if (ev.type === 'jornada')  return AG_ICONS.jornada;
  if (ev.type === 'finance')  return AG_ICONS.finance;
  return AG_ICONS.default;
}
function agColorClass(hex) {
  const map = {'#22c55e':'green', '#3B82F6':'blue', '#7C3AED':'purple'};
  return map[hex] || 'blue';
}

// ── NAVIGATION ──────────────────────────────────────────────
// Agenda de tela unica: cabecalho sticky (mes expansivel + regua 5 dias) + timeline do dia.
function renderAgenda() {
  renderAgStrip();
  renderAgTimeline();
  const panel = document.getElementById