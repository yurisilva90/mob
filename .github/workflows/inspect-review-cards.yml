name: Inspecionar revisão e Jornada
on:
  push:
    paths:
      - '.github/workflows/inspect-review-cards.yml'
permissions:
  contents: write
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Salvar blocos relevantes
        run: |
          python3 - <<'PY'
          from pathlib import Path
          lines=Path('index.html').read_text(encoding='utf-8').splitlines()
          def block(path,a,b):
            Path(path).write_text('\n'.join(f'{i}: {lines[i-1]}' for i in range(a,min(b,len(lines))+1)),encoding='utf-8')
          block('.tmp-status-block.txt',14280,14610)
          block('.tmp-jornada-block.txt',4870,5400)
          block('.tmp-operational-block.txt',7265,7325)
          PY
      - name: Commit da inspeção
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .tmp-status-block.txt .tmp-jornada-block.txt .tmp-operational-block.txt
          git diff --cached --quiet || git commit -m "chore: registrar blocos temporários"
          git push
