
===== function renderJornada @ 631418 =====
${esc(t.dest_address)}</span></div>
        ${destino.timeHtml}
      </div>
    </div>` : ''}
  </div>` : '';
  const refreshSvg = `<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12a9 9 0 1 1-2.64-6.36M21 3v6h-6"/></svg>`;
  const detRows = [
    t.passenger_name?`<div class="det-row"><span>Passageiro</span><strong>${esc(t.passenger_name)}</strong></div>`:'',
    routeBlock,
    // Reconferir com o log — pedido do Yuri (24/07/2026): reaplica a
    // leitura corrigida em cima do texto original da tela daquele
    // instante, em vez de tentar adivinhar o valor certo.
    `<button class="det-refresh-btn" id="refbtn-${tid}" onclick="event.stopPropagation();refreshAutoTripFromLog('${t.id}',document.getElementById('refbtn-${tid}'))">${refreshSvg}Atualizar informações</button>`,
  ].filter(Boolean).join('');
  const pencilSvg = `<svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M17 3a2.85 2.85 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"/></svg>`;
  // Vínculo à jornada — pedido do Yuri (24/07/2026): ícone verde se a
  // corrida cai dentro do horário de alguma jornada, cinza se não (jornada
  // excluída, ou nunca teve jornada aberta ali) — sinal pra ajustar horário
  // de uma jornada se quiser trazer ela de volta.
  const linked = isAutoTripLinkedToSession(t);
  const linkSvg = `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round"><path d="M9 17H7A5 5 0 0 1 7 7h2M15 7h2a5 5 0 1 1 0 10h-2M8 12h8"/></svg>`;
  const linkTag = `<span class="tap-legend-wrap link-tag ${linked?'on':'off'}" onclick="showTapLegend(event,'${linked?'Vinculada a uma jornada':'Não está em nenhuma jornada'}')">${linkSvg}</span>`;
  const perfStrip = perfStripHtml(t.offer_value, (kmPickupVal||0)+(kmDestVal||0), (durPickupVal||0)+(durDestVal||0));
  return `<div class="trip-item-wrap" id="tw-${tid}">
    <div class="li-row trip-top ${statusCls}" id="tr-${tid}" onclick="toggleTripDetail('${tid}')">
      <div class="li-ico-col">${hhmmTrip?`<span class="li-ico-time">${hhmmTrip}</span>`:''}${platIcon(t.platform)}</div>
      <div class="trip-content-col">
        <div class="trip-title-line"><h3 class="trip-title" style="white-space:normal">${t.origin_address?esc(extractBairro(t.origin_address)):'—'}${dest}</h3>${linkTag}</div>
        <p class="trip-subline">${subLine}</p>
        <div class="li-extra">${extras.join('')}</div>
      </div>
      <div class="li-val">
        <div style="display:flex;align-items:center;gap:5px;justify-content:flex-end">
          <strong>${DB.fR(t.offer_value)}</strong>
          <button class="val-edit-btn" onclick="event.stopPropagation();openAutoTripValueEdit('${t.id}',${t.offer_value||0})" aria-label="Editar valor da corrida">${pencilSvg}</button>
        </div>
        <span class="auto-status ${statusCls}">${statusTxt}</span>
      </div>
    </div>
    <div class="trip-detail-panel" id="td-${tid}" style="display:none">${perfStrip}${detRows}</div>
  </div>`;
}
// Corrige o caso em que uma corrida foi apagada direto no Supabase (ex:
// correção manual) mas continua aparecendo no app. Desde que o pullAll()
// passou a reconciliar automaticamente (removendo local o que já foi
// confirmado sincronizado antes e sumiu da nuvem, sem tocar em nada
// pendente na fila), esse botão só precisa rodar o mesmo pullAll() de
// novo — sem duplicar a lógica de segurança em outro lugar.
async function resyncCorridasFromCloud() {
  if (!_supaUser) { showToast('Sem conexão com a nuvem.'); return; }
  showConfirm('Recarregar da nuvem agora?\nCorridas confirmadas sincronizadas antes que sumiram do servidor saem do celular. Nada que ainda esteja pendente de subir é tocado.', async () => {
    try {
      await SYNC.pullAll();
      renderCorridasActive();
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHome();
      /* sincronização silenciosa: sem toast de sucesso */
    } catch (e) {
      console.error('resyncCorridasFromCloud', e);
      showToast('Erro ao recarregar da nuvem.');
    }
  }, { confirmLabel: 'Recarregar' });
}
function confirmDeleteTrip(date, id) {
  const day = DB.day(date);
  const t = (day.trips||[]).find(x => x.id === id);
  if (!t) return;
  const dest = t.dest ? ' → '+esc(t.dest) : '';
  showConfirm(`Excluir essa corrida?\n${DB.fR(t.value)} · ${t.origin?esc(t.origin):'—'}${dest}`, () => {
    day.trips = day.trips.filter(x => x.id !== id);
    DB.saveDay(day);
    SYNC.del('trips', { id });
    renderCorridasActive();
    if (date === S.curDate) { renderJornada(); }
    showToast('Corrida excluída.');
  }, { confirmLabel: 'Excluir' });
}
function toggleSessionsView() { S.sessionsExpanded = !S.sessionsExpanded; renderJornada(); }
function toggleLancView() { S.lancExpanded = !S.lancExpanded; renderJornada(); }

function renderJornada() {
  if (!S || !DB) return;
  try { updateCustoBanner(); } catch(e) {}
  if (!S.curDate) S.curDate = DB.today();
  const today = DB.today(), d = S.curDate, isToday = d === today;
  const dt    = new Date(d+'T12:00');
  const dows  = ['Domingo','Segunda-feira','Terça-feira','Quarta-feira','Quinta-feira','Sexta-feira','Sábado'];
  const ms    = ['jan','fev','mar','abr','mai','jun','jul','ago','set','out','nov','dez'];
  const msLong= ['janeiro','fevereiro','março','abril','maio','junho','julho','agosto','setembro','outubro','novembro','dezembro'];
  const dateEl = document.getElementById('dn-date');
  // Formato compacto: "23 jun 2026" — cabe em 1 linha
  if (dateEl) dateEl.textContent = isToday ? 'Hoje' : (dt.getDate()+' de '+msLong[dt.getMonth()]+' de '+dt.getFullYear());
  const dowEl = document.getElementById('dn-dow');
  // Dia da semana + badge "Hoje" na mesma linha
  if (dowEl) dowEl.innerHTML = dows[dt.getDay()];
  const day = DB.day(d);
  // Corridas automáticas do dia — busca assíncrona, cacheada por data (não
  // refaz a busca em toda chamada de renderJornada, que roda com frequência
  // inclusive a cada tick do timer da jornada ativa). Refresh periódico via
  // _syncTick (a cada 1min) cobre corridas novas capturadas durante o dia.
  if (S.autoTripsDate !== d) {
    const keepCurrent = S._autoTripsDataDate === d ? (S.autoTrips || []) : [];
    S.autoTripsDate = d;
    S.autoTrips = keepCurrent;
    loadAutoTripsForDay(d).then(list => {
      if (S.autoTripsDate !== d || !Array.isArray(list)) return; // erro mantém último dado válido
      S.autoTrips = list;
      S._autoTripsDataDate = d;
      renderJornada();
    });
  }
  day.autoTrips = S.autoTrips || [];
  // Recusadas do dia — mesmo padrão de cache do autoTrips acima. Lida aqui
  // (S.declined) e usada dentro de renderJourneyCard(), que roda logo a
  // seguir e populate o card "Total do dia".
  if (S.declinedDate !== d) {
    S.declinedDate = d;
    S.declined = [];
    loadDeclinedOffersForDay(d).then(list => {
      if (S.declinedDate !== d) return;
      S.declined = list;
      renderJornada();
    });
  }
  const rev = day.trips.reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0)
            + day.autoTrips.reduce((s,t)=>s+(t.offer_value||0),0);
  const exp = day.expenses.reduce((s,e)=>s+(e.amount||0),0);
  setText('d-rev', DB.fRI(rev));
  setText('d-exp', DB.fRI(exp));
  const netEl = document.getElementById('d-net');
  if (netEl) { netEl.textContent = DB.fRI(rev-exp); netEl.className = 'v ' + (rev-exp>=0 ? 'green' : 'red'); }

  renderJourneyCard(day, isToday);
  renderMetasDoDia(day);

  // Sessões do dia (3 mais recentes, ou todas — sessão atual sempre aparece)
  const sessEl = document.getElementById('sessions-list');
  const sessToggleBtn = document.getElementById('sessions-toggle');
  if (sessEl) {
    const total = (day.sessions||[]).length;
    // Numera pela ORDEM DE HORÁRIO real (start), não pela ordem em que foi
    // salva/sincronizada no aparelho — evita "Jornada 2" mostrar um horário
    // mais tarde que "Jornada 3". origIdx continua apontando pro índice
    // verdadeiro em day.sessions, então editar/apagar continua certo.
    const byTime = (day.sessions||[]).map((s,i)=>i)
      .sort((a,b) => (day.sessions[a].start||0) - (day.sessions[b].start||0));
    const finishedAll = byTime.slice().reverse()
      .map((origIdx,ri)=>sessionItem('Jornada '+(total-ri), day.sessions[origIdx], origIdx, S.curDate, day, false));
    const finishedShown = S.sessionsExpanded ? finishedAll : finishedAll.slice(0,3);
    const liveRow = (isToday && S.gps.st==='running') ? [sessionItem('Jornada atual', {start:S.gps.start,end:null,km:S.gps.km,elapsed:getElapsed()}, -1, S.curDate, day, true)] : [];
    const rows = [...liveRow, ...finishedShown];
    sessEl.innerHTML = rows.length ? rows.join('') : '<div class="empty">Nenhuma sessão registrada</div>';
  }
  if (sessToggleBtn) {
    sessToggleBtn.style.display = (day.sessions||[]).length > 3 ? '' : 'none';
    sessToggleBtn.innerHTML = S.sessionsExpanded ? 'Ver menos' : 'Ver todas &#8250;';
  }
  // Corridas: removido daqui (23/07/2026, pedido do Yuri) — já ficam
  // acessíveis dentro do detalhe de cada jornada (seta expande e lista via
  // sessionItem, que já mescla Importadas + Automáticas). Lista de
  // Importadas ainda mora na tela Corridas separada (navTo('corridas')).
  // Lançamentos (5 mais recentes, ou todos)
  const lEl = document.getElementById('jorn-lanc-list');
  const lancToggleBtn = document.getElementById('lanc-toggle');
  if (lEl) {
    const today = DB.today();
    const allItems = [
      ...day.revenues.map(r=>Object.assign({},r,{_type:'receita'})),
      ...day.expenses.map(e=>Object.assign({},e,{_type:'despesa'}))
    ];
    // Mostrar todos os lançamentos do dia sem filtro de tab
    const items = allItems;
    const show = S.lancExpanded ? items : items.slice(0,5);
    lEl.innerHTML = show.length ? show.map(it => lancItemJornada(it)).join('') : '<div class="empty">Nenhum lançamento registrado</div>';
    if (lancToggleBtn) {
      lancToggleBtn.style.display = items.length > 5 ? '' : 'none';
      lancToggleBtn.innerHTML = S.lancExpanded ? 'Ver menos' : 'Ver todas &#8250;';
    }
  }
  reapplyOpenTripDetail();
  reapplyOpenJss();
}

function renderJourneyCard(day, isToday) {
  const jc = document.getElementById('journey-card');
  if (!jc) return;
  let permWarn = document.getElementById('journey-perm-warning');
  if (!permWarn && jc.parentNode) {
    permWarn = document.createElement('div'); permWarn.id = 'journey-perm-warning'; permWarn.onclick = openFlashPermissionSetup;
    permWarn.style.cssText = 'display:none;align-items:flex-start;justify-content:space-between;gap:10px;background:#FFF7ED;border:1px solid #FDBA74;border-radius:14px;padding:11px 12px;margin:0 0 10px;cursor:pointer';
    permWarn.innerHTML = '<div style="min-width:0"><div style="font-size:12.5px;font-weight:800;color:#9A3412">⚠ Funções do MōB incompletas</div><div id="journey-perm-warning-text" style="font-size:11px;color:#C2410C;line-height:1.35;margin-top:2px"></div></div><span style="font-size:11px;font-weight:800;color:#9A3412;white-space:nowrap">Ativar ›</span>';
    jc.parentNode.insertBefore(permWarn, jc);
  }
  renderFlashPermissionWarnings();
  const cardDate = day?.date || S.curDate || DB.today();
  // Card de sessão atual (timer/km/botão) só faz sentido em "hoje" — em dia
  // passado não existe jornada "em andamento" possível, então esse card
  // some por completo. O restante da função continua rodando igual: os
  // valores que ela calcula (jc-trips, jc-total, jc-kmtotal, jc-kmativo,
  // jc-kmdesl, jc-rsh, jc-rskm) agora moram no card "Total do dia" — que é
  // um elemento IRMÃO de journey-card, não um filho, e precisa continuar
  // sendo populado em qualquer dia, passado ou presente.
  const jcWrap = document.getElementById('journey-wrap');
  if (jcWrap) jcWrap.style.display = isToday ? '' : 'none';
  const g = S.gps;
  const finished = day.sessions || [];
  const finishedElapsed = finished.reduce((s,x)=>s+(x.elapsed||0),0);
  const finishedKm = finished.reduce((s,x)=>s+(x.km||0),0);
  const live = isToday && g.st === 'running';
  const liveElapsed = live ? getElapsed() : 0;
  const liveKm = live ? g.km : 0;
  const totalElapsed = Math.max(0, finishedElapsed + liveElapsed + (day.tempoAdjust || 0));
  const totalKm = finishedKm + liveKm + (day.kmAdjust || 0);
  const state = isToday ? g.st : 'idle';

  let cls = 'idle';
  if (state === 'running') cls = 'running';
  else if (finished.length > 0) cls = 'ended';
  jc.className = 'journey ' + cls;

  setText('jc-status-text',
    state === 'running' ? 'Online' :
    finished.length > 0 ? 'Concluído' : 'Iniciar'
  );
  const th = Math.floor(liveElapsed/3600), tm = Math.floor(liveElapsed%3600/60), ts = liveElapsed%60;
  setText('jc-timer', p2(th)+':'+p2(tm)+':'+p2(ts));
  setText('jc-sub',
    state === 'running' ? 'Sessão atual · iniciada '+fmtHM(g.start) :
    finished.length > 0 ? finished.length+(finished.length===1?' jornada hoje':' jornadas hoje') :
    'Nenhuma sessão iniciada'
  );
  setText('jc-km', liveKm.toFixed(1));
  const tth = Math.floor(totalElapsed/3600), ttm = Math.floor(totalElapsed%3600/60);
  setText('jc-total', tth+'h'+p2(ttm)+'m');
  setText('jc-kmtotal', totalKm.toFixed(1)+' km');
  // CORRIGIDO (23/07/2026, confirmado com dado real pelo Yuri): esses 4
  // KPIs do card principal (corridas, km ativo, R$/h, R$/km) só somavam
  // day.trips (Importadas) — sessão só com automáticas mostrava tudo
  // zerado mesmo com corridas de verdade rodando. Mesma correção que já
  // tinha sido feita em rev/sessionItem/renderMetasDoDia, faltava aqui.
  const autoTripsHoje = day.autoTrips || [];

  // Banner "aguardando confirmação" — pedido do Yuri (16/08/2026), substitui
  // o botão fixo "Importar corridas". Conta corridas que ainda não foram
  // confirmadas por vídeo do histórico (status 'capturada' ou 'estimada') —
  // 'confirmada' só existe depois do vídeo, então isso mede exatamente o
  // que falta conferir naquele dia. Aparece em qualquer dia navegado, some
  // sozinho quando não sobra pendência.
  const pcbEl = document.getElementById('pending-confirm-banner');
  if (pcbEl) {
    // Não interpreta cache ainda carregando como zero corridas. Durante refresh,
    // mantém o último estado visual válido do mesmo dia.
    const autoTripsReady = S._autoTripsDataDate === cardDate;
    if (autoTripsReady) {
      const pendingCount = autoTripsHoje.filter(t => t.status !== 'confirmada' || t.value_needs_review === true).length;
      pcbEl.dataset.dataDate = cardDate;
      if (pendingCount > 0) {
        pcbEl.style.display = 'flex';
        setText('pcb-title', pendingCount + (pendingCount===1 ? ' corrida aguardando confirmação' : ' corridas aguardando confirmação'));
      } else {
        pcbEl.style.display = 'none';
      }
    } else if (pcbEl.dataset.dataDate &&
===== auto_trips @ 537175 =====
1a2 2 0 0 1-3.4 0"/></svg>
      <div style="font-size:14px;font-weight:600;color:#64748B;margin-bottom:6px">Nenhuma notificação</div>
      <div style="font-size:12px">Novidades do MōB aparecerão aqui</div>
    </div>
    <button class="btn-p" onclick="closeMo('mo-notif')" style="margin-top:8px">Fechar</button>
  </div>
</div>
<!-- MODAL: Categorias -->
<div class="screen" id="sc-categorias">
  <div style="display:flex;align-items:center;gap:10px;padding:14px 16px 6px">
    <button onclick="navTo('mais')" style="width:32px;height:32px;border-radius:10px;background:#F1F5F9;border:0;display:flex;align-items:center;justify-content:center;color:#0F172A;cursor:pointer;flex-shrink:0"><svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M19 12H5M12 19l-7-7 7-7"/></svg></button>
    <h2 style="font-size:17px;letter-spacing:-.02em;flex:1">Categorias</h2>
  </div>
  <div style="font-size:13px;color:#64748B;margin:0 16px 14px">Gerencie suas categorias de despesa e receita.</div>
  <div id="cats-groups" style="padding:0 16px 24px"></div>
</div>

<div class="mo" id="mo-obj-config">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head" style="padding:0 0 12px"><h2>Configurar objetivos</h2><button class="xbtn" onclick="closeMo('mo-obj-config')">&#x2715;</button></div>
    <div class="obj-switch">
      <button class="obj-btn active" id="ocfg-rec" onclick="setObjCfgTab('rec')">Receitas</button>
      <button class="obj-btn" id="ocfg-desp" onclick="setObjCfgTab('desp')">Despesas</button>
    </div>
    <div id="objcfg-rec">
      <div class="type-switch2">
        <button class="type-btn2 active" id="ocfg-rec-operacional" onclick="setObjCfgGroup('rec','operacional')">Operacional</button>
        <button class="type-btn2" id="ocfg-rec-pessoal" onclick="setObjCfgGroup('rec','pessoal')">Pessoal</button>
      </div>
      <div id="objcfg-rec-rows"></div>
    </div>
    <div id="objcfg-desp" style="display:none">
      <div class="type-switch2">
        <button class="type-btn2 active" id="ocfg-desp-operacional" onclick="setObjCfgGroup('desp','operacional')">Operacional</button>
        <button class="type-btn2" id="ocfg-desp-pessoal" onclick="setObjCfgGroup('desp','pessoal')">Pessoal</button>
      </div>
      <div id="objcfg-desp-rows"></div>
    </div>
    <button class="btn-p" onclick="saveObjCfg()">Salvar objetivos</button>
  </div>
</div>


</div>
<script>
// ============================================================
// SmartMobi v20 — Camada de funcionalidade
// Design: GPT  |  JS/Dados: Claude
// ============================================================

// ── DATA LAYER ───────────────────────────────────────────────
const DB = {
  get: k => { try { return JSON.parse(localStorage.getItem(k)); } catch(e) { return null; } },
  set: (k,v) => localStorage.setItem(k, JSON.stringify(v)),
  del: k => localStorage.removeItem(k),
  day: d => { const x = DB.get('sm_day_'+d) || {date:d, trips:[], expenses:[], revenues:[], kmTotal:null, session:null}; if (!x.sessions) x.sessions = []; return x; },
  saveDay: d => DB.set('sm_day_'+d.date, d),
  allDates: () => Object.keys(localStorage).filter(k => k.startsWith('sm_day_')).map(k => k.slice(7)).sort(),
  events: () => DB.get('sm_events') || [],
  saveEvents: e => DB.set('sm_events', e),
  planJornada: () => DB.get('sm_plan_jornada') || [],
  savePlanJornada: p => DB.set('sm_plan_jornada', p),
  cfg: () => DB.get('sm_cfg') || {name:'', plate:'', vehicle:'', goal:0, favModule:'financeiro'},
  saveCfg: c => DB.set('sm_cfg', c),
  cats: t => DB.get('sm_cats_'+t) || DEFAULT_CATS[t],
  saveCats: (t,c) => DB.set('sm_cats_'+t, c),
  objGoals: t => DB.get('sm_obj_'+t) || {},
  saveObjGoals: (t,g) => DB.set('sm_obj_'+t, g),
  syncQueue: () => DB.get('sm_sync_queue') || [],
  saveSyncQueue: q => DB.set('sm_sync_queue', q),
  meta: () => DB.get('sm_meta') || {type:'monthly', general:9000, varyByDay:false, days:{}},
  saveMeta: m => DB.set('sm_meta', m),
  uid: () => Date.now().toString(36) + Math.random().toString(36).slice(2,5),
  today: () => { const d=new Date(); return d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate()); },
  fR: v => 'R$ ' + Number(v||0).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2}),
  fRI: v => 'R$ ' + Number(v||0).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2}),
};
function p2(n) { return String(n).padStart(2,'0'); }
// Formata duração em minutos: "5m", "32m", "1h05m" (sem zero à esquerda quando é só minutos).
function fmtDur(totalMin) {
  const min = Math.max(0, Math.round(totalMin || 0));
  const h = Math.floor(min / 60), m = min % 60;
  return (h > 0 ? h + 'h' + p2(m) : String(m)) + 'm';
}

// Detalhamento Online/Buscando passageiro/Em corrida — puxado direto da
// máquina de estados do APK (accepted_at/pickup_started_at/trip_started_at/
// trip_ended_at, gravados em auto_trips a cada transição real de tela).
// BUSCANDO = trip_started_at - pickup_started_at (deslocamento até o
// passageiro); EM CORRIDA = trip_ended_at - trip_started_at; ONLINE = todo
// o resto do tempo da sessão (esperando oferta / avaliando oferta). Só usa
// corridas automáticas (autoTrips) — corridas importadas manualmente não
// têm esses timestamps porque não vieram da leitura de tela em tempo real.
function computeStatusBreakdown(elapsedSec, kmTotal, autoTrips) {
  let corridaSec = 0, buscandoSec = 0, corridaKm = 0, buscandoKm = 0;
  (autoTrips || []).forEach(t => {
    if (t.trip_started_at && t.trip_ended_at) {
      const s = (new Date(t.trip_ended_at) - new Date(t.trip_started_at)) / 1000;
      if (s > 0) corridaSec += s;
      corridaKm += parseFloat(t.real_km_trip ?? t.offer_km_trip) || 0;
    }
    if (t.pickup_started_at && t.trip_started_at) {
      const s = (new Date(t.trip_started_at) - new Date(t.pickup_started_at)) / 1000;
      if (s > 0) buscandoSec += s;
      buscandoKm += parseFloat(t.real_km_pickup ?? t.offer_km_pickup) || 0;
    }
  });
  const total = Math.max(0, elapsedSec || 0);
  const onlineSec = Math.max(0, total - corridaSec - buscandoSec);
  const onlineKm = Math.max(0, (kmTotal || 0) - corridaKm - buscandoKm);
  const pct = s => total > 0 ? Math.round(s / total * 100) : 0;
  return {
    total,
    online:   { sec: onlineSec,   km: onlineKm,   pct: pct(onlineSec) },
    buscando: { sec: buscandoSec, km: buscandoKm, pct: pct(buscandoSec) },
    corrida:  { sec: corridaSec,  km: corridaKm,  pct: pct(corridaSec) },
  };
}

const STATUS_SEGMENTS = [
  { key: 'online',   label: 'ONLINE',              color: '#22C55E' },
  { key: 'buscando', label: 'BUSCANDO PASSAGEIRO',  color: '#F97316' },
  { key: 'corrida',  label: 'EM CORRIDA',           color: '#3B82F6' },
];

function buildSbarHtml(bd) {
  return STATUS_SEGMENTS.map(s => {
    const pct = bd[s.key].pct;
    if (pct <= 0) return '';
    const showText = pct >= 12;
    return `<i style="width:${pct}%;background:${s.color}">${showText ? pct+'%' : ''}</i>`;
  }).join('');
}

function buildSrowsHtml(bd) {
  return STATUS_SEGMENTS.map(s => {
    const seg = bd[s.key];
    return `<div class="srow"><span class="sdot" style="background:${s.color}"></span><b>${s.label}</b><span class="pct">${seg.pct}%</span><span class="stempo">${fmtDur(seg.sec/60)}</span><span class="skm">${seg.km.toFixed(1)} km</span></div>`;
  }).join('');
}

function toggleBar(listId) {
  const el = document.getElementById(listId);
  if (el) el.classList.toggle('open');
}

// ── DEFAULT CATEGORIES ────────────────────────────────────────
const DEFAULT_CATS = {
  despesa_operacional: [
    {id:'comb', l:'Combustível', icon:'fuel'},
    {id:'alim', l:'Alimentação', icon:'food'},
    {id:'man',  l:'Manutenção', icon:'wrench'},
    {id:'park', l:'Estacionamento', icon:'parking'},
    {id:'ped',  l:'Pedágio', icon:'road'},
    {id:'lav',  l:'Lavagem', icon:'car'},
    {id:'seg',  l:'Seguro', icon:'card'},
    {id:'out',  l:'Outros', icon:'plus'}
  ],
  despesa_pessoal: [
    {id:'alug', l:'Aluguel', icon:'home'},
    {id:'merc', l:'Mercado', icon:'card'},
    {id:'alim', l:'Alimentação', icon:'food'},
    {id:'cart', l:'Cartão', icon:'card'},
    {id:'trans', l:'Transporte', icon:'car'},
    {id:'saude', l:'Saúde', icon:'plus'},
    {id:'lazer', l:'Lazer', icon:'gift'},
    {id:'out',  l:'Outros', icon:'plus'}
  ],
  receita_operacional: [
    {id:'gort', l:'Gorjeta', icon:'coin'},
    {id:'bon',  l:'Bônus', icon:'gift'},
    {id:'reemb', l:'Reembolso', icon:'card'},
    {id:'serv', l:'Serviço extra', icon:'briefcase'},
    {id:'part', l:'Particular', icon:'user'},
    {id:'inc',  l:'Incentivo', icon:'plus'}
  ],
  receita_pessoal: [
    {id:'sal',  l:'Salário', icon:'briefcase'},
    {id:'rend', l:'Renda extra', icon:'coin'},
    {id:'pix',  l:'Pix recebido', icon:'card'},
    {id:'reemb', l:'Reembolso', icon:'card'},
    {id:'pres', l:'Presente', icon:'gift'},
    {id:'out',  l:'Outros', icon:'plus'}
  ]
};

// ── GLOBAL STATE ──────────────────────────────────────────────
let S = {
  sec: 'home',
  homePeriod: 'w',
  hgCustomStart: null, hgCustomEnd: null,
  hgDiaMode: 'd', hgHoraMode: 'h', hgMesMode: 'm', hgRevDesp: 'receita',
  finPer: 'month',
  finScope: 'all',
  lancFilter: 'all',
  lancStatus: 'all',
  lancPeriod: '7d',
  lancDate: null,
  curDate: DB.today(),
  lancType: 'receita',
  lancScope: 'operacional',
  lancRecFreq: 'Semanal',
  corPlat: 'uber',
  corridasPeriod: 'hoje', corridasDate: null,
  corridasPlat: 'all',
  tripsExpanded: false,
  sessionsExpanded: false,
  lancExpanded: false,
  _midnightIsPostStop: false,
  _postStopMidnightSess: null,
  _postMidnightCallback: null,
  _midnightModalOpen: false,
  gps: {
    st: 'idle',  // idle | running
    start: null, pausedMs: 0, pauseAt: null,
    km: 0, pts: [], wid: null, ti: null,
    forceDate: null // override manual (opção 'deixar em ontem' na virada de meia-noite)
  }
};

// ── MODULES (for Mais screen) ─────────────────────────────────
const MODULES = [
  {id:'financeiro', label:'Financeiro', sub:'Gestão e análise financeira', color:'blue',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 3v18M17 7H9.5a3.5 3.5 0 0 0 0 7H14a3.5 3.5 0 0 1 0 7H6"/></svg>'},
  {id:'planejar-jornada', label:'Planejar jornada', sub:'Defina dias, metas e horários de trabalho', color:'green',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18M8 14h.01M12 14h.01M16 14h.01M8 18h.01M12 18h.01"/></svg>'},
  {id:'lancamentos', label:'Lançamentos', sub:'Histórico de receitas e despesas', color:'orange',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 19V5a2 2 0 0 1 2-2h12v18H6a2 2 0 0 1-2-2Z"/><path d="M8 7h8M8 11h8M8 15h5"/></svg>'},
];

const MODULE_COLORS = {blue:'#EFF6FF', purple:'#F5F3FF', green:'#ECFDF3', orange:'#FFF7ED'};
const MODULE_ICONS_COLOR = {blue:'#0A2F6B', purple:'#7C3AED', green:'#22C55E', orange:'#F59E0B'};

// ── NAVIGATION ────────────────────────────────────────────────
function navTo(sec) {
  // Ocultar/mostrar nav na tela de Informes (o painel usa z-index alto mas o cat-row precisa da nav escondida)
  const bottomNav = document.querySelector('.bottom');
  if (bottomNav) bottomNav.style.display = 'flex';
  document.querySelectorAll('.screen').forEach(s => {
    s.classList.remove('on');
    s.style.display = '';  // limpa inline style para CSS funcionar
  });
  document.querySelectorAll('.nav').forEach(n => n.classList.remove('active'));
  const sc = document.getElementById('sc-'+sec);
  if (sc) { sc.classList.add('on'); sc.style.display = 'block'; }
  // Map sub-screens to their nav parent
  const navMap = {financeiro:'fav', lancamentos:'fav', relatorios:'fav', 'planejar-jornada':'mais', corridas:'jornada', agenda:'mais', informes:'agenda', categorias:'mais'};
  const navId = navMap[sec] || sec;
  const bn = document.getElementById('bn-'+navId);
  if (bn) bn.classList.add('active');
  S.sec = sec;
  try { localStorage.setItem('sm_last_screen', JSON.stringify({sec, ts: Date.now()})); } catch(e) {}
  const ms = document.getElementById('main-scroll');
  if (ms) ms.scrollTop = 0;
  if (sec === 'home')       renderFeedHome();
  if (sec === 'relatorios') renderHome();
  if (sec === 'jornada') {
    renderJornada(); // render imediato com dado local (nunca trava esperando rede)
    updateNetStatus();
    if (_supaUser && navigator.onLine) {
      reconcileJornadaHoje().then(() => renderJornada()).catch(()=>{});
      // Se o app local acha que está idle mas existe jornada aberta no
      // Supabase há mais de 15min (provável órfã), fecha automaticamente
      if (S.gps.st === 'idle') {
        _closeOrphanSessions().then(n => { if (n) renderJornada(); }).catch(()=>{});
      }
    }
  }
  if (sec === 'financeiro') renderFin();
  if (sec === 'lancamentos') renderLancamentos();
  if (sec === 'corridas') {
    S.corridasPlats = new Set(); // reseta sempre — nenhum filtro marcado = mostra tudo
    document.getElementById('cor-plat-uber')?.classList.remove('active');
    document.getElementById('cor-plat-99')?.classList.remove('active');
    renderCorridasActive();
  }
  if (sec === 'mais')       renderMais();
  if (sec === 'categorias') renderCatsModal();
  if (sec === 'agenda')     { renderAgenda(); agScrollToFirst(); }
  if (sec === 'informes')   renderInformes();
}
function openSuporte() {
  const el = document.getElementById('suporte-version');
  if (el) {
    const v = window.SmartMobiNative?.getVersion?.();
    el.textContent = v ? ('App nativo v' + v) : 'Acessando pelo navegador (sem app nativo)';
  }
  openMo('mo-suporte');
}
function navToFav() {
  const cfg = DB.cfg();
  navTo(cfg.favModule || 'financeiro');
}
function openMo(id) { const el = document.getElementById(id); if (el) el.classList.add('on'); }
function closeMo(id) { const el = document.getElementById(id); if (el) el.classList.remove('on'); }
document.addEventListener('click', e => {
  if (e.target.classList.contains('mo')) e.target.classList.remove('on');
});

// ── MAIS SCREEN ───────────────────────────────────────────────
function renderMais() {
  const vEl = document.getElementById("app-code-ver");
  if (vEl) vEl.textContent = typeof _APP_CODE_VER !== "undefined" ? _APP_CODE_VER : "?";

  const cfg = DB.cfg();
  const favId = cfg.favModule || 'financeiro';
  const container = document.getElementById('mais-modules');
  if (!container) return;
  container.innerHTML = MODULES.map(m => `
    <div style="background:#fff;border-radius:20px;border:1px solid #E2E8F0;box-shadow:0 2px 10px rgba(15,23,42,.05);padding:14px 16px;display:flex;align-items:center;gap:12px;cursor:pointer" onclick="navTo('${m.id}')">
      <div style="width:42px;height:42px;border-r
===== auto_trips @ 582048 =====
lat = [
      {label:'Uber', value: allTrips.filter(t=>t.platform==='uber').reduce((s,t)=>s+(t.value||0),0)},
      {label:'99', value: allTrips.filter(t=>t.platform==='99').reduce((s,t)=>s+(t.value||0),0)},
      {label:'Particular', value: allTrips.filter(t=>t.platform==='particular').reduce((s,t)=>s+(t.value||0),0)}
    ];
    const allRev = days.flatMap(d => d.revenues);
    const byCat = hgGroupBy(allRev, r => r.categoryLabel || r.category || 'Outros', r => r.amount||0);
    const catItems = Object.keys(byCat).map(k => ({label:k, value: byCat[k]}));
    items = byPlat.concat(catItems);
  } else {
    const allExp = days.flatMap(d => d.expenses);
    const byCat = hgGroupBy(allExp, e => e.categoryLabel || e.category || 'Outros', e => e.amount||0);
    items = Object.keys(byCat).map(k => ({label:k, value: byCat[k]}));
  }
  items.sort((a,b) => b.value - a.value);
  if (items.length > 6) {
    const top = items.slice(0,6);
    const restSum = items.slice(6).reduce((s,i)=>s+i.value,0);
    items = top.concat(restSum > 0 ? [{label:'Outros', value: restSum}] : []);
  }
  hgBuildPie(items, 'hg-pie-cat', 'hg-pie-cat-list', v => DB.fRI(v));
  const panelEl = document.getElementById('hg-cat-panel');
  if (panelEl) panelEl.innerHTML = hgCatPanelHtml(items);
}
function entryCard(e) {
  const isRev  = e._type === 'corrida' || e._type === 'receita';
  const val    = e.value || e.amount || 0;
  const label  = e._type === 'corrida' ? ((e.origin||'—')+(e.dest?' → '+e.dest:'')) : (e.desc || e.category || e._type);
  const color  = isRev ? 'var(--green2)' : 'var(--red)';
  const sign   = isRev ? '+' : '-';
  return `<div style="display:flex;align-items:center;gap:10px;padding:11px 0;border-bottom:1px solid #E2E8F0">
    <div style="flex:1;min-width:0"><div style="font-size:13px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${label}</div><div style="font-size:11px;color:#64748B;margin-top:2px">${e._type} · ${e._date}</div></div>
    <div style="font-family:'Inter',system-ui,sans-serif;font-weight:700;font-size:14px;color:${color};flex-shrink:0">${sign}${DB.fRI(val)}</div>
  </div>`;
}

// ── JORNADA ───────────────────────────────────────────────────
function chgDay(d) {
  const dt = new Date(S.curDate+'T12:00'); dt.setDate(dt.getDate()+d);
  S.curDate = dt.getFullYear()+'-'+p2(dt.getMonth()+1)+'-'+p2(dt.getDate());
  renderJornada();
}
function setJornadaDate(dateStr) {
  if (!dateStr) return;
  S.curDate = dateStr;
  renderJornada();
}
function fmtHM(ms) { if (!ms) return '--:--'; const dd=new Date(ms); return p2(dd.getHours())+':'+p2(dd.getMinutes()); }
function toggleTripsView() { S.tripsExpanded = !S.tripsExpanded; renderJornada(); }

// ── TELA CORRIDAS (lista dedicada, com filtro de período/plataforma e exclusão) ──
function getCorridasDays() {
  const today=new Date();
  const fmt=d=>d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
  const p=S.corridasPeriod;
  if(p==='date'&&S.corridasDate) return [S.corridasDate];
  if(p==='hoje') return [fmt(today)];
  if(p==='7d') { const out=[]; for(let i=0;i<7;i++){const d=new Date(today);d.setDate(d.getDate()-i);out.push(fmt(d));} return out; }
  if(p==='mes') { const m=today.getMonth(),y=today.getFullYear(); return DB.allDates().filter(d=>d.startsWith(y+'-'+p2(m+1))); }
  return DB.allDates();
}
function setCorridasPeriod(p) {
  S.corridasPeriod=p; S.corridasDate=null;
  ['hoje','7d','mes','tudo'].forEach(id=>{ const b=document.getElementById('cpb-'+id); if(b) b.classList.toggle('active',id===p); });
  const dp=document.getElementById('cor-date-picker'),db=document.getElementById('cor-date-btn');
  if(dp) dp.style.display='none'; if(db){db.style.background='#fff';db.style.borderColor='#E2E8F0';}
  renderCorridasActive();
}
function toggleCorDate() {
  const inp=document.getElementById('cor-date-input');
  if(inp) inp.showPicker ? inp.showPicker() : inp.click();
}
function setCorridasDate(d) {
  if(!d) return; S.corridasDate=d; S.corridasPeriod='date';
  ['hoje','7d','mes','tudo'].forEach(id=>{ const b=document.getElementById('cpb-'+id); if(b) b.classList.remove('active'); });
  const db=document.getElementById('cor-date-btn'); if(db){db.style.background='#EFF6FF';db.style.borderColor='#2563EB';}
  renderCorridasActive();
}
function toggleCorridasPlat(p) {
  // Multi-select agora (pedido do Yuri, 24/07/2026): nenhum marcado = mostra
  // tudo (padrão), marcar um filtra só aquele, marcar os dois volta a
  // mostrar tudo — sem precisar de um botão "Todas" separado.
  if (!S.corridasPlats) S.corridasPlats = new Set();
  if (S.corridasPlats.has(p)) S.corridasPlats.delete(p); else S.corridasPlats.add(p);
  const ub = document.getElementById('cor-plat-uber'), n99 = document.getElementById('cor-plat-99');
  if (ub) ub.classList.toggle('active', S.corridasPlats.has('uber'));
  if (n99) n99.classList.toggle('active', S.corridasPlats.has('99'));
  renderCorridasActive();
}
// ── Corridas: alterna entre Importadas (trips locais) e Automáticas (auto_trips no Supabase) ──
// ── Corridas automáticas → alimentam a Jornada (pedido do Yuri, 23/07/2026) ──
// Busca auto_trips do dia pra somar em ganhos/km/corridas da Jornada e da
// sessão certa dentro dela — sem se misturar com day.trips (Importadas),
// só somando junto na hora de mostrar total. Timestamp de referência:
// trip_started_at (início real da corrida) — cai sempre dentro da sessão
// (Jornada) que estava rodando, diferente de accepted_at que pode ser
// alguns segundos antes ainda na sessão anterior num caso raro de virada.
function autoTripTs(t) {
  const iso = t.trip_started_at || t.accepted_at;
  return iso ? new Date(iso).getTime() : 0;
}
// Corrida cai dentro do horário de alguma jornada do dia dela? Pedido do
// Yuri (24/07/2026) — o vínculo nunca é salvo, é sempre calculado (mesma
// lógica de sessionItem/renderJornada) — então dá pra checar pra QUALQUER
// corrida, de qualquer tela, sem precisar saber de antemão qual sessão.
function isAutoTripLinkedToSession(t) {
  const ts = autoTripTs(t);
  if (!ts) return false;
  const d = new Date(ts);
  const dateStr = d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
  const sessions = (DB.day(dateStr).sessions) || [];
  return sessions.some(s => {
    const start = s.start || 0, end = s.end || 0;
    return ts >= start && (!end || ts <= end);
  });
}

// ── Reconferir corrida usando o texto bruto do log (24/07/2026) ────────────
// Pedido do Yuri: em vez de tentar "adivinhar" o valor certo de uma corrida
// já salva errada, reaplica a MESMA lógica de leitura (já corrigida no app
// nativo em 24/07/2026) em cima do texto ORIGINAL da tela, salvo em
// trip_reader_log — usa o dado real daquele instante, não um chute.
// Porta fiel de parseOffer() do TripReaderService.kt (Kotlin) — mesmas
// regras, mesma ordem, incluindo as 3 correções de 24/07 (R$/km nunca
// compete como valor total; dinâmico soma os 2 tipos da 99 + Uber; linha
// de avaliação/corridas nunca vira endereço).
function moneyToDoubleJS(raw) {
  const clean = raw.replace('R$','').trim().replace(/\./g,'').replace(',','.');
  const n = parseFloat(clean);
  return isNaN(n) ? null : n;
}
function extractMoneyJS(s) {
  // CORRIGIDO (24/07/2026) - mesma correção do Kotlin: centavos passam a
  // ser obrigatórios, evita aceitar "R$96" como valor quando o OCR engoliu
  // a vírgula de "R$9,60".
  const re = /R\$\s?\d{1,4}[.,]\d{2}/g;
  const out = []; let m;
  while ((m = re.exec(s))) if (!out.includes(m[0])) out.push(m[0]);
  return out;
}
// Validação de endereço — GLOBAL (promovida de dentro de parseOfferJS em
// 16/08/2026 pra ser reutilizável também na importação de vídeo, onde
// endereço de corrida nova nunca pode ser aceito sem essa checagem —
// pedido explícito do Yuri: "nunca endereço, a não ser que garanta que
// não vai vir lixo ou inventado"). Mesma lógica usada no Kotlin.
function looksLikeAddress(s) {
  const sl = s.toLowerCase().trim();
  if (sl.length < 6) return false;
  if (sl.includes('parada')) return false;
  if (sl.includes('não afeta') || sl.includes('nao afeta')) return false;
  if (/^\(?\d{1,3}([.,]\d+)?\s*(min|km|m)\b/.test(sl)) return false;
  if (/^[x%\d.,\s]+$/.test(sl)) return false;
  if (sl.includes('r$')) return false;
  if (sl.includes('tarifa') || sl.includes('incl.') || sl.includes('incluído') || sl.includes('incluido')) return false;
  if (sl.includes('taxa de espera') || sl.includes('espera longa')) return false;
  if (sl === 'online' || sl === 'buscando' || sl === 'offline' || sl === 'conectar') return false;
  if (sl.includes('perfil essencial') || sl.includes('perfil premium') || sl.includes('perfil prata')) return false;
  if (sl.includes('pgto. no app') || sl === 'dinheiro' || sl === 'negocia' || sl === 'qr code') return false;
  if (/[1-5][.,]\d{2}.{0,15}corrid/.test(sl)) return false;
  if (sl.includes('verif.') || sl.includes('cpf e cart')) return false;
  const hasStreetWord = /\b(rua|av\.?|avenida|estrada|travessa|alameda|rodovia|pra[çc]a|largo|ladeira|rod\.?|via\b)/i.test(sl);
  const hasNumberPattern = /[^\d],\s*\d{1,5}\b/.test(sl);
  if (!hasStreetWord && !hasNumberPattern) return false;
  return true;
}
function parseOfferJS(texts) {
  const joinedRaw = texts.join('  ');
  const low = joinedRaw.toLowerCase()
    .replace(/\b[lI](?=[.,]\d)/g, '1')
    .replace(/(?<=\d[.,])[lI]\b/g, '1');

  let valor = null;
  let m = /aceitar por\s*r\$\s*([\d.]+,\d{2})/.exec(low);
  if (m) valor = moneyToDoubleJS(m[1]);

  let rkmDirect = null;
  m = /r\$\s*([\d.]+,\d{2})\s*\/\s*km/.exec(low);
  if (m) rkmDirect = moneyToDoubleJS(m[1]);

  let totMin = 0, totKm = 0, legs = 0;
  const legsList = [];
  function addLeg(minsStr, distStr, unit) {
    const mins = parseInt(minsStr, 10);
    const dist = parseFloat(distStr.replace(',', '.'));
    if (isNaN(mins) || isNaN(dist)) return;
    const km2 = unit === 'm' ? dist/1000 : dist;
    if (mins >= 1 && mins <= 90 && km2 >= 0.05 && km2 <= 80) {
      totMin += mins; totKm += km2; legs++; legsList.push([mins, km2]);
    }
  }
  let re = /(\d{1,3})\s*min(?:utos)?\s*\((\d+(?:[.,]\d+)?)\s*(m|km)\)/g;
  while ((m = re.exec(low))) addLeg(m[1], m[2], m[3]);
  re = /\(\s*(\d{1,3})\s*min(?:utos)?\s+(\d+(?:[.,]\d+)?)\s*(m|km)\s*\)/g;
  while ((m = re.exec(low))) addLeg(m[1], m[2], m[3]);

  let km = (legs > 0 && totKm > 0) ? totKm : null;
  let min = (legs > 0 && totMin > 0) ? totMin : null;
  const kmPickup = legsList[0] ? legsList[0][1] : null;
  const kmTrip = legsList[1] ? legsList[1][1] : null;
  const minPickup = legsList[0] ? legsList[0][0] : null;
  const minTrip = legsList[1] ? legsList[1][0] : null;

  let dinamicoBase = 0, dinamicoEspera = 0, dinamicoEmbarque = 0, dinamicoDeslocamento = 0, dinamicoUber = 0;
  m = /r\$\s*([\d.]+,\d{2})\s*tarifa base din[aâ]mica/.exec(low);
  if (m) dinamicoBase = moneyToDoubleJS(m[1]) || 0;
  m = /r\$\s*([\d.]+,\d{2})\s*por espera longa/.exec(low);
  if (m) dinamicoEspera = moneyToDoubleJS(m[1]) || 0;
  m = /r\$\s*([\d.]+,\d{2})\s*por embarque distante/.exec(low);
  if (m) dinamicoEmbarque = moneyToDoubleJS(m[1]) || 0;
  m = /r\$\s*([\d.]+,\d{2})\s*por deslocamento/.exec(low);
  if (m) dinamicoDeslocamento = moneyToDoubleJS(m[1]) || 0;
  m = /\+\s*r\$\s*([\d.]+,\d{2})\s*inclu[ií]do/.exec(low);
  if (m) dinamicoUber = moneyToDoubleJS(m[1]) || 0;
  const dinamico = dinamicoBase + dinamicoEspera + dinamicoEmbarque + dinamicoDeslocamento + dinamicoUber;

  if (valor === null) {
    let monies = extractMoneyJS(joinedRaw).map(moneyToDoubleJS).filter(v => v !== null && v > 0);
    if (rkmDirect !== null) monies = monies.filter(v => Math.abs(v - rkmDirect) > 0.01);
    if (rkmDirect !== null && km !== null) {
      const found = monies.find(v => Math.abs(v/km - rkmDirect)/rkmDirect < 0.35);
      valor = found !== undefined ? found : null;
    } else if (km !== null && km > 0) {
      const found = monies.find(v => (v/km) >= 0.4 && (v/km) <= 12.0);
      valor = found !== undefined ? found : (monies[0] !== undefined ? monies[0] : null);
    } else {
      valor = monies[0] !== undefined ? monies[0] : null;
    }
  }
  if (km === null && valor !== null && rkmDirect !== null && rkmDirect > 0) km = valor / rkmDirect;

  const legLineRe = /^\(?\s*\d{1,3}\s*min(?:utos)?/i;
  const addrCandidates = [];
  for (let i = 0; i < texts.length; i++) {
    if (legLineRe.test(texts[i])) {
      for (let j = i+1; j <= Math.min(i+3, texts.length-1); j++) {
        const cand = (texts[j]||'').trim();
        if (!cand) break;
        const nl = cand.toLowerCase();
        if (legLineRe.test(cand)) break;
        if (nl.includes('aceitar') || nl.includes('selecionar') || nl.includes('escolher')) break;
        if (looksLikeAddress(cand)) { addrCandidates.push(cand); break; }
      }
    }
  }
  return {
    valor, km, min, rkmDirect, dinamico,
    origem: addrCandidates[0] || null, destino: addrCandidates[1] || null,
    kmPickup, kmTrip, minPickup, minTrip
  };
}
// Busca a leitura mais completa (mais linhas no 'raw') da oferta dessa
// corrida, num intervalo de 10min antes do aceite até 30s depois — mesma
// janela usada pelo cache de oferta no app nativo.
async function fetchBestOfferTexts(t) {
  // Retorna { texts, reason } em vez de só null/array — pedido do Yuri
  // (24/07/2026): precisa ver o motivo exato na tela (sem abrir console do
  // navegador) quando não achar nada, pra investigar mais rápido.
  if (!_supaUser) return { texts: null, reason: 'sem sessão ativa' };
  const plat = t.platform === 'uber' ? 'UBER' : '99';
  const anchor = t.accepted_at || t.trip_started_at;
  if (!anchor) return { texts: null, reason: 'corrida sem horário de aceite salvo' };
  const anchorMs = new Date(anchor).getTime();
  const from = new Date(anchorMs - 10*60*1000).toISOString();
  const to = new Date(anchorMs + 30*1000).toISOString();
  const { data, error } = await _SUPA.from('trip_reader_log').select('texts,created_at')
    .eq('user_id', _supaUser.id).eq('platform', plat)
    .gte('created_at', from).lte('created_at', to)
    .order('created_at', { ascending: false })
    // CONFIRMADO EM DADO REAL (24/07/2026): um intervalo de 10min30s desses
    // teve 523 linhas no total (todo tipo de leitura, não só oferta) — o
    // limite de 500 cortava fora justamente as leituras mais antigas, que
    // eram as que tinham a oferta. Margem bem folgada agora.
    .limit(3000);
  if (error) {
    console.error('fetchBestOfferTexts', error);
    return { texts: null, reason: 'erro na busca: '+(error.message || error.code || '?') };
  }
  if (!data || !data.length) return { texts: null, reason: `0 linhas no log entre ${from.slice(11,19)} e ${to.slice(11,19)} (plataforma ${plat})` };
  let best = null, bestLen = -1, offerRows = 0;
  data.forEach(row => {
    const st = row.texts && row.texts.state;
    if (st !== 'OFERTA' && st !== 'OFERTA_OCR') return;
    offerRows++;
    const raw = row.texts && row.texts.raw;
    if (Array.isArray(raw) && raw.length > bestLen) { best = raw; bestLen = raw.length; }
  });
===== auto_trips @ 592621 =====
rip = legsList[1] ? legsList[1][0] : null;

  let dinamicoBase = 0, dinamicoEspera = 0, dinamicoEmbarque = 0, dinamicoDeslocamento = 0, dinamicoUber = 0;
  m = /r\$\s*([\d.]+,\d{2})\s*tarifa base din[aâ]mica/.exec(low);
  if (m) dinamicoBase = moneyToDoubleJS(m[1]) || 0;
  m = /r\$\s*([\d.]+,\d{2})\s*por espera longa/.exec(low);
  if (m) dinamicoEspera = moneyToDoubleJS(m[1]) || 0;
  m = /r\$\s*([\d.]+,\d{2})\s*por embarque distante/.exec(low);
  if (m) dinamicoEmbarque = moneyToDoubleJS(m[1]) || 0;
  m = /r\$\s*([\d.]+,\d{2})\s*por deslocamento/.exec(low);
  if (m) dinamicoDeslocamento = moneyToDoubleJS(m[1]) || 0;
  m = /\+\s*r\$\s*([\d.]+,\d{2})\s*inclu[ií]do/.exec(low);
  if (m) dinamicoUber = moneyToDoubleJS(m[1]) || 0;
  const dinamico = dinamicoBase + dinamicoEspera + dinamicoEmbarque + dinamicoDeslocamento + dinamicoUber;

  if (valor === null) {
    let monies = extractMoneyJS(joinedRaw).map(moneyToDoubleJS).filter(v => v !== null && v > 0);
    if (rkmDirect !== null) monies = monies.filter(v => Math.abs(v - rkmDirect) > 0.01);
    if (rkmDirect !== null && km !== null) {
      const found = monies.find(v => Math.abs(v/km - rkmDirect)/rkmDirect < 0.35);
      valor = found !== undefined ? found : null;
    } else if (km !== null && km > 0) {
      const found = monies.find(v => (v/km) >= 0.4 && (v/km) <= 12.0);
      valor = found !== undefined ? found : (monies[0] !== undefined ? monies[0] : null);
    } else {
      valor = monies[0] !== undefined ? monies[0] : null;
    }
  }
  if (km === null && valor !== null && rkmDirect !== null && rkmDirect > 0) km = valor / rkmDirect;

  const legLineRe = /^\(?\s*\d{1,3}\s*min(?:utos)?/i;
  const addrCandidates = [];
  for (let i = 0; i < texts.length; i++) {
    if (legLineRe.test(texts[i])) {
      for (let j = i+1; j <= Math.min(i+3, texts.length-1); j++) {
        const cand = (texts[j]||'').trim();
        if (!cand) break;
        const nl = cand.toLowerCase();
        if (legLineRe.test(cand)) break;
        if (nl.includes('aceitar') || nl.includes('selecionar') || nl.includes('escolher')) break;
        if (looksLikeAddress(cand)) { addrCandidates.push(cand); break; }
      }
    }
  }
  return {
    valor, km, min, rkmDirect, dinamico,
    origem: addrCandidates[0] || null, destino: addrCandidates[1] || null,
    kmPickup, kmTrip, minPickup, minTrip
  };
}
// Busca a leitura mais completa (mais linhas no 'raw') da oferta dessa
// corrida, num intervalo de 10min antes do aceite até 30s depois — mesma
// janela usada pelo cache de oferta no app nativo.
async function fetchBestOfferTexts(t) {
  // Retorna { texts, reason } em vez de só null/array — pedido do Yuri
  // (24/07/2026): precisa ver o motivo exato na tela (sem abrir console do
  // navegador) quando não achar nada, pra investigar mais rápido.
  if (!_supaUser) return { texts: null, reason: 'sem sessão ativa' };
  const plat = t.platform === 'uber' ? 'UBER' : '99';
  const anchor = t.accepted_at || t.trip_started_at;
  if (!anchor) return { texts: null, reason: 'corrida sem horário de aceite salvo' };
  const anchorMs = new Date(anchor).getTime();
  const from = new Date(anchorMs - 10*60*1000).toISOString();
  const to = new Date(anchorMs + 30*1000).toISOString();
  const { data, error } = await _SUPA.from('trip_reader_log').select('texts,created_at')
    .eq('user_id', _supaUser.id).eq('platform', plat)
    .gte('created_at', from).lte('created_at', to)
    .order('created_at', { ascending: false })
    // CONFIRMADO EM DADO REAL (24/07/2026): um intervalo de 10min30s desses
    // teve 523 linhas no total (todo tipo de leitura, não só oferta) — o
    // limite de 500 cortava fora justamente as leituras mais antigas, que
    // eram as que tinham a oferta. Margem bem folgada agora.
    .limit(3000);
  if (error) {
    console.error('fetchBestOfferTexts', error);
    return { texts: null, reason: 'erro na busca: '+(error.message || error.code || '?') };
  }
  if (!data || !data.length) return { texts: null, reason: `0 linhas no log entre ${from.slice(11,19)} e ${to.slice(11,19)} (plataforma ${plat})` };
  let best = null, bestLen = -1, offerRows = 0;
  data.forEach(row => {
    const st = row.texts && row.texts.state;
    if (st !== 'OFERTA' && st !== 'OFERTA_OCR') return;
    offerRows++;
    const raw = row.texts && row.texts.raw;
    if (Array.isArray(raw) && raw.length > bestLen) { best = raw; bestLen = raw.length; }
  });
  if (!best) return { texts: null, reason: `${data.length} linhas no log, nenhuma de oferta (plataforma ${plat})` };
  return { texts: best, reason: null };
}
// Lógica central, sem toast/spinner — usada tanto pelo botão manual quanto
// pela rotina automática (24/07/2026). Só CALCULA, não salva — quem chama
// decide se aplica direto ou pede confirmação primeiro (ver
// refreshAutoTripFromLog, 16/08/2026: mudança de valor grande demais pra
// aplicar sem avisar).
async function _computeAutoTripRefresh(tripId) {
  const { data: rows, error: e1 } = await _SUPA.from('auto_trips').select('*').eq('id', tripId).limit(1);
  if (e1 || !rows || !rows.length) return { ok:false, reason:'corrida não encontrada' };
  const t = rows[0];
  const { texts, reason } = await fetchBestOfferTexts(t);
  if (!texts) return { ok:false, reason };
  const parsed = parseOfferJS(texts);
  if (parsed.valor == null) return { ok:false, reason:'texto insuficiente pra reconferir' };
  // Só os campos NUMÉRICOS — testado contra casos reais, o valor sai
  // confiável em várias leituras diferentes da mesma oferta, mas o
  // endereço às vezes sai lixo ("% LUCRO" etc) mesmo na leitura mais
  // completa. Melhor não arriscar sobrescrever um endereço bom.
  const patch = { offer_value: parsed.valor, status: 'confirmada' };
  if (parsed.dinamico != null) patch.offer_dinamico = parsed.dinamico;
  if (parsed.kmPickup != null) patch.offer_km_pickup = parsed.kmPickup;
  if (parsed.kmTrip != null) patch.offer_km_trip = parsed.kmTrip;
  if (parsed.minPickup != null) patch.offer_duration_pickup_sec = Math.round(parsed.minPickup*60);
  if (parsed.minTrip != null) patch.offer_duration_trip_sec = Math.round(parsed.minTrip*60);
  // Compara ANTES de salvar — pedido do Yuri (24/07/2026): mensagem
  // precisa dizer o que de fato mudou, não só "reconferida" genérico.
  const numEq = (a,b,tol) => (a==null && b==null) || (a!=null && b!=null && Math.abs(a-b) <= (tol||0.005));
  const changes = [];
  if (!numEq(t.offer_value, patch.offer_value, 0.005)) changes.push('valor '+DB.fR(patch.offer_value));
  if (patch.offer_dinamico != null && !numEq(t.offer_dinamico, patch.offer_dinamico, 0.005)) changes.push('dinâmico '+DB.fR(patch.offer_dinamico));
  if (patch.offer_km_trip != null && !numEq(t.offer_km_trip, patch.offer_km_trip, 0.05)) changes.push('km previsto');
  if (patch.offer_duration_trip_sec != null && !numEq(t.offer_duration_trip_sec, patch.offer_duration_trip_sec, 30)) changes.push('tempo previsto');
  // Diferença grande de valor (>15%, ou >R$5 pra valores pequenos) — pede
  // confirmação em vez de aplicar direto. Pedido do Yuri (16/08/2026):
  // caso real onde o valor pulou de R$42 pra R$26,20 sem aviso nenhum —
  // pode ser a correção certa (leitura mais completa) ou pode ser a nova
  // leitura que tá errada; sem ver as duas, não dá pra saber qual confiar.
  const oldVal = t.offer_value != null ? Number(t.offer_value) : null;
  const newVal = patch.offer_value;
  let valueBigChange = false;
  if (oldVal != null && oldVal > 0) {
    const diff = Math.abs(newVal - oldVal);
    valueBigChange = diff > 5 && (diff / oldVal) > 0.15;
  }
  return { ok:true, tripId, patch, changes, oldVal, newVal, valueBigChange };
}
async function _applyAutoTripRefresh(tripId, patch) {
  const { error } = await _SUPA.from('auto_trips').update({ ...patch, value_needs_review: false }).eq('id', tripId);
  return !error;
}
async function refreshAutoTripFromLog(tripId, btnEl) {
  if (btnEl) btnEl.classList.add('spinning');
  try {
    const r = await _computeAutoTripRefresh(tripId);
    if (!r.ok) { showToast('Não achei: '+r.reason, 'error'); return; }
    if (!r.changes.length) { showToast('Corrida atualizada com sucesso', 'success'); return; }
    const finish = async () => {
      const saved = await _applyAutoTripRefresh(r.tripId, r.patch);
      if (!saved) { showToast('Erro ao salvar.', 'error'); return; }
      showToast('Corrigido: '+r.changes.join(', '), 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    };
    if (r.valueBigChange) {
      showConfirm(
        `O valor dessa corrida vai mudar de ${DB.fR(r.oldVal)} para ${DB.fR(r.newVal)} — uma diferença grande.\n\nConfirma que ${DB.fR(r.newVal)} é o valor certo?`,
        finish,
        { danger: false, confirmLabel: 'Sim, corrigir', cancelLabel: 'Cancelar' }
      );
    } else {
      await finish();
    }
  } catch (e) {
    console.error('refreshAutoTripFromLog', e);
    showToast('Erro ao reconferir.', 'error');
  } finally {
    if (btnEl) btnEl.classList.remove('spinning');
  }
}
// Rotina automática (24/07/2026, pedido do Yuri) — a cada 5min, tenta
// reconferir sozinho corridas "Aguardando revisão" (status='capturada')
// das últimas 48h. Silenciosa por corrida (não interrompe com toast a
// cada uma) — só avisa se corrigiu alguma, no total. Nunca mexe em
// corrida já confirmada, só nas que ainda não têm valor nenhum.
async function _autoRefreshPendingTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'capturada')
      .gte('accepted_at', new Date(Date.now() - 48*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixed = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (r.ok && r.changes.length) { await _applyAutoTripRefresh(r.tripId, r.patch); fixed++; }
      } catch(e) {}
    }
    if (fixed > 0) {
      showToast(fixed===1 ? '1 corrida corrigida sozinha' : fixed+' corridas corrigidas sozinhas', 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
  } catch (e) { console.error('_autoRefreshPendingTrips', e); }
}
setTimeout(()=>{ _autoRefreshPendingTrips(); setInterval(_autoRefreshPendingTrips, 5*60*1000); }, 6000);

// Rotina automática nº2 (16/08/2026, pedido do Yuri: "não quero usuário
// editando, tem que partir da gente interno... a cada X min confere o
// que tá no registro e corrige"). Diferente da rotina acima (que só olha
// corridas AINDA sem valor confirmado), essa reconfere corridas que JÁ
// estão 'confirmada' — pra pegar o caso real que aconteceu (valor errado
// que já tinha virado card normal na tela). Roda a cada 10min, olha só as
// últimas 24h (corrida mais velha que isso já foi conferida o suficiente
// de vezes). REGRA DE OURO: só aplica sozinha quando a mudança é pequena/
// confiável — mudança grande NUNCA aplica sem humano olhar (mesmo critério
// do botão manual), só marca value_needs_review pra aparecer sinalizada,
// sem sumir da tela nem dos totais (não é "fantasma", é corrida real que
// só precisa de confirmação do valor).
//
// MUDOU (16/08/2026): mirava em status='confirmada', mas agora
// 'confirmada' só nasce depois do vídeo do histórico (ver
// confirmImport()) — reconferir isso aqui não faz mais sentido, vídeo já
// É a fonte mais confiável que existe. Passa a mirar em 'estimada' (ciclo
// da captura ao vivo completo, só com valor da oferta) — mesmo espírito
// de antes, refinando o valor com a melhor leitura do log disponível
// enquanto o vídeo de confirmação não chega.
async function _autoReconcileConfirmedTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'estimada')
      .is('data_quality_flag', null)
      .gte('accepted_at', new Date(Date.now() - 24*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixedQuiet = 0, flaggedForReview = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (!r.ok || !r.changes.length) continue;
        if (r.valueBigChange) {
          await _SUPA.from('auto_trips').update({ value_needs_review: true }).eq('id', row.id);
          flaggedForReview++;
        } else {
          await _applyAutoTripRefresh(r.tripId, r.patch);
          fixedQuiet++;
        }
      } catch(e) {}
    }
    if (fixedQuiet > 0 || flaggedForReview > 0) {
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
    // Silenciosa de propósito — mudança pequena não precisa de aviso, e
    // mudança grande já fica visível pelo badge "Valor a confirmar" na
    // tela Corridas, sem precisar interromper com toast.
  } catch (e) { console.error('_autoReconcileConfirmedTrips', e); }
}
setTimeout(()=>{ _autoReconcileConfirmedTrips(); setInterval(_autoReconcileConfirmedTrips, 10*60*1000); }, 9000);
// Balãozinho discreto ao tocar num símbolo (dinâmico, vínculo à jornada,
// dinheiro) — pedido do Yuri (24/07/2026). Some sozinho, não navega nem
// abre nada.
function showTapLegend(event, text) {
  event.stopPropagation();
  document.querySelectorAll('.tap-legend-tip').forEach(el => el.remove());
  const wrap = event.currentTarget;
  const tip = document.createElement('div');
  tip.className = 'tap-legend-tip';
  tip.textContent = text;
  wrap.appendChild(tip);
  setTimeout(() => tip.remove(), 2500);
}
// Legenda de comparação previsto × real (Deslocamento/Corrida coloridos) —
// abre/fecha um painel fixo embaixo da linha, diferente do balãozinho dos
// símbolos porque tem mais texto (2 linhas de números).
function toggleCmpLegend(event, id) {
  event.stopPropagation();
  const el = document.getElementById(id);
  if (el) el.style.display = el.style.display === 'none' ? '' : 'none';
}
async function loadAutoTripsForDay(dateStr) {
  if (!_supaUser) return [];
  // O dia da Jornada é o dia LOCAL do motorista. Converte meia-noite local
  // para UTC antes de consultar timestamptz — evita perder corridas após 21h
  // no Brasil quando o relógio UTC já virou para o dia seguinte.
  const fromLocal = new Date(dateStr+'T00:00:00');
  const toLocal = new Date(fromLocal); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started
===== auto_trips @ 595369 =====
// navegador) quando não achar nada, pra investigar mais rápido.
  if (!_supaUser) return { texts: null, reason: 'sem sessão ativa' };
  const plat = t.platform === 'uber' ? 'UBER' : '99';
  const anchor = t.accepted_at || t.trip_started_at;
  if (!anchor) return { texts: null, reason: 'corrida sem horário de aceite salvo' };
  const anchorMs = new Date(anchor).getTime();
  const from = new Date(anchorMs - 10*60*1000).toISOString();
  const to = new Date(anchorMs + 30*1000).toISOString();
  const { data, error } = await _SUPA.from('trip_reader_log').select('texts,created_at')
    .eq('user_id', _supaUser.id).eq('platform', plat)
    .gte('created_at', from).lte('created_at', to)
    .order('created_at', { ascending: false })
    // CONFIRMADO EM DADO REAL (24/07/2026): um intervalo de 10min30s desses
    // teve 523 linhas no total (todo tipo de leitura, não só oferta) — o
    // limite de 500 cortava fora justamente as leituras mais antigas, que
    // eram as que tinham a oferta. Margem bem folgada agora.
    .limit(3000);
  if (error) {
    console.error('fetchBestOfferTexts', error);
    return { texts: null, reason: 'erro na busca: '+(error.message || error.code || '?') };
  }
  if (!data || !data.length) return { texts: null, reason: `0 linhas no log entre ${from.slice(11,19)} e ${to.slice(11,19)} (plataforma ${plat})` };
  let best = null, bestLen = -1, offerRows = 0;
  data.forEach(row => {
    const st = row.texts && row.texts.state;
    if (st !== 'OFERTA' && st !== 'OFERTA_OCR') return;
    offerRows++;
    const raw = row.texts && row.texts.raw;
    if (Array.isArray(raw) && raw.length > bestLen) { best = raw; bestLen = raw.length; }
  });
  if (!best) return { texts: null, reason: `${data.length} linhas no log, nenhuma de oferta (plataforma ${plat})` };
  return { texts: best, reason: null };
}
// Lógica central, sem toast/spinner — usada tanto pelo botão manual quanto
// pela rotina automática (24/07/2026). Só CALCULA, não salva — quem chama
// decide se aplica direto ou pede confirmação primeiro (ver
// refreshAutoTripFromLog, 16/08/2026: mudança de valor grande demais pra
// aplicar sem avisar).
async function _computeAutoTripRefresh(tripId) {
  const { data: rows, error: e1 } = await _SUPA.from('auto_trips').select('*').eq('id', tripId).limit(1);
  if (e1 || !rows || !rows.length) return { ok:false, reason:'corrida não encontrada' };
  const t = rows[0];
  const { texts, reason } = await fetchBestOfferTexts(t);
  if (!texts) return { ok:false, reason };
  const parsed = parseOfferJS(texts);
  if (parsed.valor == null) return { ok:false, reason:'texto insuficiente pra reconferir' };
  // Só os campos NUMÉRICOS — testado contra casos reais, o valor sai
  // confiável em várias leituras diferentes da mesma oferta, mas o
  // endereço às vezes sai lixo ("% LUCRO" etc) mesmo na leitura mais
  // completa. Melhor não arriscar sobrescrever um endereço bom.
  const patch = { offer_value: parsed.valor, status: 'confirmada' };
  if (parsed.dinamico != null) patch.offer_dinamico = parsed.dinamico;
  if (parsed.kmPickup != null) patch.offer_km_pickup = parsed.kmPickup;
  if (parsed.kmTrip != null) patch.offer_km_trip = parsed.kmTrip;
  if (parsed.minPickup != null) patch.offer_duration_pickup_sec = Math.round(parsed.minPickup*60);
  if (parsed.minTrip != null) patch.offer_duration_trip_sec = Math.round(parsed.minTrip*60);
  // Compara ANTES de salvar — pedido do Yuri (24/07/2026): mensagem
  // precisa dizer o que de fato mudou, não só "reconferida" genérico.
  const numEq = (a,b,tol) => (a==null && b==null) || (a!=null && b!=null && Math.abs(a-b) <= (tol||0.005));
  const changes = [];
  if (!numEq(t.offer_value, patch.offer_value, 0.005)) changes.push('valor '+DB.fR(patch.offer_value));
  if (patch.offer_dinamico != null && !numEq(t.offer_dinamico, patch.offer_dinamico, 0.005)) changes.push('dinâmico '+DB.fR(patch.offer_dinamico));
  if (patch.offer_km_trip != null && !numEq(t.offer_km_trip, patch.offer_km_trip, 0.05)) changes.push('km previsto');
  if (patch.offer_duration_trip_sec != null && !numEq(t.offer_duration_trip_sec, patch.offer_duration_trip_sec, 30)) changes.push('tempo previsto');
  // Diferença grande de valor (>15%, ou >R$5 pra valores pequenos) — pede
  // confirmação em vez de aplicar direto. Pedido do Yuri (16/08/2026):
  // caso real onde o valor pulou de R$42 pra R$26,20 sem aviso nenhum —
  // pode ser a correção certa (leitura mais completa) ou pode ser a nova
  // leitura que tá errada; sem ver as duas, não dá pra saber qual confiar.
  const oldVal = t.offer_value != null ? Number(t.offer_value) : null;
  const newVal = patch.offer_value;
  let valueBigChange = false;
  if (oldVal != null && oldVal > 0) {
    const diff = Math.abs(newVal - oldVal);
    valueBigChange = diff > 5 && (diff / oldVal) > 0.15;
  }
  return { ok:true, tripId, patch, changes, oldVal, newVal, valueBigChange };
}
async function _applyAutoTripRefresh(tripId, patch) {
  const { error } = await _SUPA.from('auto_trips').update({ ...patch, value_needs_review: false }).eq('id', tripId);
  return !error;
}
async function refreshAutoTripFromLog(tripId, btnEl) {
  if (btnEl) btnEl.classList.add('spinning');
  try {
    const r = await _computeAutoTripRefresh(tripId);
    if (!r.ok) { showToast('Não achei: '+r.reason, 'error'); return; }
    if (!r.changes.length) { showToast('Corrida atualizada com sucesso', 'success'); return; }
    const finish = async () => {
      const saved = await _applyAutoTripRefresh(r.tripId, r.patch);
      if (!saved) { showToast('Erro ao salvar.', 'error'); return; }
      showToast('Corrigido: '+r.changes.join(', '), 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    };
    if (r.valueBigChange) {
      showConfirm(
        `O valor dessa corrida vai mudar de ${DB.fR(r.oldVal)} para ${DB.fR(r.newVal)} — uma diferença grande.\n\nConfirma que ${DB.fR(r.newVal)} é o valor certo?`,
        finish,
        { danger: false, confirmLabel: 'Sim, corrigir', cancelLabel: 'Cancelar' }
      );
    } else {
      await finish();
    }
  } catch (e) {
    console.error('refreshAutoTripFromLog', e);
    showToast('Erro ao reconferir.', 'error');
  } finally {
    if (btnEl) btnEl.classList.remove('spinning');
  }
}
// Rotina automática (24/07/2026, pedido do Yuri) — a cada 5min, tenta
// reconferir sozinho corridas "Aguardando revisão" (status='capturada')
// das últimas 48h. Silenciosa por corrida (não interrompe com toast a
// cada uma) — só avisa se corrigiu alguma, no total. Nunca mexe em
// corrida já confirmada, só nas que ainda não têm valor nenhum.
async function _autoRefreshPendingTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'capturada')
      .gte('accepted_at', new Date(Date.now() - 48*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixed = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (r.ok && r.changes.length) { await _applyAutoTripRefresh(r.tripId, r.patch); fixed++; }
      } catch(e) {}
    }
    if (fixed > 0) {
      showToast(fixed===1 ? '1 corrida corrigida sozinha' : fixed+' corridas corrigidas sozinhas', 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
  } catch (e) { console.error('_autoRefreshPendingTrips', e); }
}
setTimeout(()=>{ _autoRefreshPendingTrips(); setInterval(_autoRefreshPendingTrips, 5*60*1000); }, 6000);

// Rotina automática nº2 (16/08/2026, pedido do Yuri: "não quero usuário
// editando, tem que partir da gente interno... a cada X min confere o
// que tá no registro e corrige"). Diferente da rotina acima (que só olha
// corridas AINDA sem valor confirmado), essa reconfere corridas que JÁ
// estão 'confirmada' — pra pegar o caso real que aconteceu (valor errado
// que já tinha virado card normal na tela). Roda a cada 10min, olha só as
// últimas 24h (corrida mais velha que isso já foi conferida o suficiente
// de vezes). REGRA DE OURO: só aplica sozinha quando a mudança é pequena/
// confiável — mudança grande NUNCA aplica sem humano olhar (mesmo critério
// do botão manual), só marca value_needs_review pra aparecer sinalizada,
// sem sumir da tela nem dos totais (não é "fantasma", é corrida real que
// só precisa de confirmação do valor).
//
// MUDOU (16/08/2026): mirava em status='confirmada', mas agora
// 'confirmada' só nasce depois do vídeo do histórico (ver
// confirmImport()) — reconferir isso aqui não faz mais sentido, vídeo já
// É a fonte mais confiável que existe. Passa a mirar em 'estimada' (ciclo
// da captura ao vivo completo, só com valor da oferta) — mesmo espírito
// de antes, refinando o valor com a melhor leitura do log disponível
// enquanto o vídeo de confirmação não chega.
async function _autoReconcileConfirmedTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'estimada')
      .is('data_quality_flag', null)
      .gte('accepted_at', new Date(Date.now() - 24*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixedQuiet = 0, flaggedForReview = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (!r.ok || !r.changes.length) continue;
        if (r.valueBigChange) {
          await _SUPA.from('auto_trips').update({ value_needs_review: true }).eq('id', row.id);
          flaggedForReview++;
        } else {
          await _applyAutoTripRefresh(r.tripId, r.patch);
          fixedQuiet++;
        }
      } catch(e) {}
    }
    if (fixedQuiet > 0 || flaggedForReview > 0) {
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
    // Silenciosa de propósito — mudança pequena não precisa de aviso, e
    // mudança grande já fica visível pelo badge "Valor a confirmar" na
    // tela Corridas, sem precisar interromper com toast.
  } catch (e) { console.error('_autoReconcileConfirmedTrips', e); }
}
setTimeout(()=>{ _autoReconcileConfirmedTrips(); setInterval(_autoReconcileConfirmedTrips, 10*60*1000); }, 9000);
// Balãozinho discreto ao tocar num símbolo (dinâmico, vínculo à jornada,
// dinheiro) — pedido do Yuri (24/07/2026). Some sozinho, não navega nem
// abre nada.
function showTapLegend(event, text) {
  event.stopPropagation();
  document.querySelectorAll('.tap-legend-tip').forEach(el => el.remove());
  const wrap = event.currentTarget;
  const tip = document.createElement('div');
  tip.className = 'tap-legend-tip';
  tip.textContent = text;
  wrap.appendChild(tip);
  setTimeout(() => tip.remove(), 2500);
}
// Legenda de comparação previsto × real (Deslocamento/Corrida coloridos) —
// abre/fecha um painel fixo embaixo da linha, diferente do balãozinho dos
// símbolos porque tem mais texto (2 linhas de números).
function toggleCmpLegend(event, id) {
  event.stopPropagation();
  const el = document.getElementById(id);
  if (el) el.style.display = el.style.display === 'none' ? '' : 'none';
}
async function loadAutoTripsForDay(dateStr) {
  if (!_supaUser) return [];
  // O dia da Jornada é o dia LOCAL do motorista. Converte meia-noite local
  // para UTC antes de consultar timestamptz — evita perder corridas após 21h
  // no Brasil quando o relógio UTC já virou para o dia seguinte.
  const fromLocal = new Date(dateStr+'T00:00:00');
  const toLocal = new Date(fromLocal); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  // null = falha de rede/consulta; [] = consulta válida sem corridas.
  return error ? null : (data||[]);
}
// Recusadas do dia — mesmo padrão de loadAutoTripsForDay, pra alimentar o
// contador "X aceitas / Y recusadas" do Total do dia. Tabela já existe e já
// é usada na tela Corridas (seção expansível); aqui só reaproveita pro
// recorte de UM dia em vez do período todo.
async function loadDeclinedOffersForDay(dateStr) {
  if (!_supaUser) return [];
  const from = dateStr+'T00:00:00', to = dateStr+'T23:59:59';
  const { data, error } = await _SUPA.from('declined_offers').select('id')
    .eq('user_id', _supaUser.id)
    .gte('seen_at', from).lte('seen_at', to);
  return error ? [] : (data||[]);
}

// ── Corridas automáticas → alimentam a Home (pedido do Yuri, 23/07/2026) ──
// Mesmo gap da Jornada, só que espalhado pela Home inteira: valor, km,
// corridas, todos os gráficos e o insight de IA só olhavam day.trips
// (Importadas) e ficavam cegos pra qualquer dia 100% automático. Em vez de
// corrigir função por função (arriscado, muita superfície), busca TODAS as
// automáticas do período de UMA VEZ (não uma consulta por dia — helper
// abaixo cobre semana/mês/ano inteiros numa chamada só) e injeta como
// corridas "de mentira" no formato de trip normal — todo código que já lê
// day.trips corretamente passa a enxergar as automáticas de graça, sem
// precisar tocar em cada gráfico individualmente.
async function loadAutoTripsIndexed(fromDate, toDate) {
  if (!_supaUser) return {};
  const fromLocal = new Date(fromDate+'T00:00:00');
  const toLocal = new Date(toDate+'T00:00:00'); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  if (error || !data) return {};
  const map = {};
  data.forEach(t => {
    const iso = t.trip_started_at || t.accepted_at;
    if (!iso) return;
    const dt = new Date(iso);
    const dateStr = dt.getFullYear()+'-'+p2(dt.getMonth()+1)+'-'+p2(dt.getDate());
    (map[dateStr] = map[dateStr] || []).push(t);
  });
  return map;
}
// Converte as automáticas de um dia (já indexadas em S.autoTripsByDate) pro
// MESMO formato de objeto que day.trips usa — value/km/time/duration/
// platform/dinheiro — assim qualquer soma/filtro que já existe funciona
// sem mudança nenhuma. NUNCA usar pra salvar (é só leitura/exibição —
// misturar isso em day.trips de verdade duplicaria dado no Supabase).
function autoTripsAsTrips(date) {
  const list = (S.autoTripsByDate && S.autoTri
===== auto_trips @ 597267 =====
ão manual quanto
// pela rotina automática (24/07/2026). Só CALCULA, não salva — quem chama
// decide se aplica direto ou pede confirmação primeiro (ver
// refreshAutoTripFromLog, 16/08/2026: mudança de valor grande demais pra
// aplicar sem avisar).
async function _computeAutoTripRefresh(tripId) {
  const { data: rows, error: e1 } = await _SUPA.from('auto_trips').select('*').eq('id', tripId).limit(1);
  if (e1 || !rows || !rows.length) return { ok:false, reason:'corrida não encontrada' };
  const t = rows[0];
  const { texts, reason } = await fetchBestOfferTexts(t);
  if (!texts) return { ok:false, reason };
  const parsed = parseOfferJS(texts);
  if (parsed.valor == null) return { ok:false, reason:'texto insuficiente pra reconferir' };
  // Só os campos NUMÉRICOS — testado contra casos reais, o valor sai
  // confiável em várias leituras diferentes da mesma oferta, mas o
  // endereço às vezes sai lixo ("% LUCRO" etc) mesmo na leitura mais
  // completa. Melhor não arriscar sobrescrever um endereço bom.
  const patch = { offer_value: parsed.valor, status: 'confirmada' };
  if (parsed.dinamico != null) patch.offer_dinamico = parsed.dinamico;
  if (parsed.kmPickup != null) patch.offer_km_pickup = parsed.kmPickup;
  if (parsed.kmTrip != null) patch.offer_km_trip = parsed.kmTrip;
  if (parsed.minPickup != null) patch.offer_duration_pickup_sec = Math.round(parsed.minPickup*60);
  if (parsed.minTrip != null) patch.offer_duration_trip_sec = Math.round(parsed.minTrip*60);
  // Compara ANTES de salvar — pedido do Yuri (24/07/2026): mensagem
  // precisa dizer o que de fato mudou, não só "reconferida" genérico.
  const numEq = (a,b,tol) => (a==null && b==null) || (a!=null && b!=null && Math.abs(a-b) <= (tol||0.005));
  const changes = [];
  if (!numEq(t.offer_value, patch.offer_value, 0.005)) changes.push('valor '+DB.fR(patch.offer_value));
  if (patch.offer_dinamico != null && !numEq(t.offer_dinamico, patch.offer_dinamico, 0.005)) changes.push('dinâmico '+DB.fR(patch.offer_dinamico));
  if (patch.offer_km_trip != null && !numEq(t.offer_km_trip, patch.offer_km_trip, 0.05)) changes.push('km previsto');
  if (patch.offer_duration_trip_sec != null && !numEq(t.offer_duration_trip_sec, patch.offer_duration_trip_sec, 30)) changes.push('tempo previsto');
  // Diferença grande de valor (>15%, ou >R$5 pra valores pequenos) — pede
  // confirmação em vez de aplicar direto. Pedido do Yuri (16/08/2026):
  // caso real onde o valor pulou de R$42 pra R$26,20 sem aviso nenhum —
  // pode ser a correção certa (leitura mais completa) ou pode ser a nova
  // leitura que tá errada; sem ver as duas, não dá pra saber qual confiar.
  const oldVal = t.offer_value != null ? Number(t.offer_value) : null;
  const newVal = patch.offer_value;
  let valueBigChange = false;
  if (oldVal != null && oldVal > 0) {
    const diff = Math.abs(newVal - oldVal);
    valueBigChange = diff > 5 && (diff / oldVal) > 0.15;
  }
  return { ok:true, tripId, patch, changes, oldVal, newVal, valueBigChange };
}
async function _applyAutoTripRefresh(tripId, patch) {
  const { error } = await _SUPA.from('auto_trips').update({ ...patch, value_needs_review: false }).eq('id', tripId);
  return !error;
}
async function refreshAutoTripFromLog(tripId, btnEl) {
  if (btnEl) btnEl.classList.add('spinning');
  try {
    const r = await _computeAutoTripRefresh(tripId);
    if (!r.ok) { showToast('Não achei: '+r.reason, 'error'); return; }
    if (!r.changes.length) { showToast('Corrida atualizada com sucesso', 'success'); return; }
    const finish = async () => {
      const saved = await _applyAutoTripRefresh(r.tripId, r.patch);
      if (!saved) { showToast('Erro ao salvar.', 'error'); return; }
      showToast('Corrigido: '+r.changes.join(', '), 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    };
    if (r.valueBigChange) {
      showConfirm(
        `O valor dessa corrida vai mudar de ${DB.fR(r.oldVal)} para ${DB.fR(r.newVal)} — uma diferença grande.\n\nConfirma que ${DB.fR(r.newVal)} é o valor certo?`,
        finish,
        { danger: false, confirmLabel: 'Sim, corrigir', cancelLabel: 'Cancelar' }
      );
    } else {
      await finish();
    }
  } catch (e) {
    console.error('refreshAutoTripFromLog', e);
    showToast('Erro ao reconferir.', 'error');
  } finally {
    if (btnEl) btnEl.classList.remove('spinning');
  }
}
// Rotina automática (24/07/2026, pedido do Yuri) — a cada 5min, tenta
// reconferir sozinho corridas "Aguardando revisão" (status='capturada')
// das últimas 48h. Silenciosa por corrida (não interrompe com toast a
// cada uma) — só avisa se corrigiu alguma, no total. Nunca mexe em
// corrida já confirmada, só nas que ainda não têm valor nenhum.
async function _autoRefreshPendingTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'capturada')
      .gte('accepted_at', new Date(Date.now() - 48*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixed = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (r.ok && r.changes.length) { await _applyAutoTripRefresh(r.tripId, r.patch); fixed++; }
      } catch(e) {}
    }
    if (fixed > 0) {
      showToast(fixed===1 ? '1 corrida corrigida sozinha' : fixed+' corridas corrigidas sozinhas', 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
  } catch (e) { console.error('_autoRefreshPendingTrips', e); }
}
setTimeout(()=>{ _autoRefreshPendingTrips(); setInterval(_autoRefreshPendingTrips, 5*60*1000); }, 6000);

// Rotina automática nº2 (16/08/2026, pedido do Yuri: "não quero usuário
// editando, tem que partir da gente interno... a cada X min confere o
// que tá no registro e corrige"). Diferente da rotina acima (que só olha
// corridas AINDA sem valor confirmado), essa reconfere corridas que JÁ
// estão 'confirmada' — pra pegar o caso real que aconteceu (valor errado
// que já tinha virado card normal na tela). Roda a cada 10min, olha só as
// últimas 24h (corrida mais velha que isso já foi conferida o suficiente
// de vezes). REGRA DE OURO: só aplica sozinha quando a mudança é pequena/
// confiável — mudança grande NUNCA aplica sem humano olhar (mesmo critério
// do botão manual), só marca value_needs_review pra aparecer sinalizada,
// sem sumir da tela nem dos totais (não é "fantasma", é corrida real que
// só precisa de confirmação do valor).
//
// MUDOU (16/08/2026): mirava em status='confirmada', mas agora
// 'confirmada' só nasce depois do vídeo do histórico (ver
// confirmImport()) — reconferir isso aqui não faz mais sentido, vídeo já
// É a fonte mais confiável que existe. Passa a mirar em 'estimada' (ciclo
// da captura ao vivo completo, só com valor da oferta) — mesmo espírito
// de antes, refinando o valor com a melhor leitura do log disponível
// enquanto o vídeo de confirmação não chega.
async function _autoReconcileConfirmedTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'estimada')
      .is('data_quality_flag', null)
      .gte('accepted_at', new Date(Date.now() - 24*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixedQuiet = 0, flaggedForReview = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (!r.ok || !r.changes.length) continue;
        if (r.valueBigChange) {
          await _SUPA.from('auto_trips').update({ value_needs_review: true }).eq('id', row.id);
          flaggedForReview++;
        } else {
          await _applyAutoTripRefresh(r.tripId, r.patch);
          fixedQuiet++;
        }
      } catch(e) {}
    }
    if (fixedQuiet > 0 || flaggedForReview > 0) {
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
    // Silenciosa de propósito — mudança pequena não precisa de aviso, e
    // mudança grande já fica visível pelo badge "Valor a confirmar" na
    // tela Corridas, sem precisar interromper com toast.
  } catch (e) { console.error('_autoReconcileConfirmedTrips', e); }
}
setTimeout(()=>{ _autoReconcileConfirmedTrips(); setInterval(_autoReconcileConfirmedTrips, 10*60*1000); }, 9000);
// Balãozinho discreto ao tocar num símbolo (dinâmico, vínculo à jornada,
// dinheiro) — pedido do Yuri (24/07/2026). Some sozinho, não navega nem
// abre nada.
function showTapLegend(event, text) {
  event.stopPropagation();
  document.querySelectorAll('.tap-legend-tip').forEach(el => el.remove());
  const wrap = event.currentTarget;
  const tip = document.createElement('div');
  tip.className = 'tap-legend-tip';
  tip.textContent = text;
  wrap.appendChild(tip);
  setTimeout(() => tip.remove(), 2500);
}
// Legenda de comparação previsto × real (Deslocamento/Corrida coloridos) —
// abre/fecha um painel fixo embaixo da linha, diferente do balãozinho dos
// símbolos porque tem mais texto (2 linhas de números).
function toggleCmpLegend(event, id) {
  event.stopPropagation();
  const el = document.getElementById(id);
  if (el) el.style.display = el.style.display === 'none' ? '' : 'none';
}
async function loadAutoTripsForDay(dateStr) {
  if (!_supaUser) return [];
  // O dia da Jornada é o dia LOCAL do motorista. Converte meia-noite local
  // para UTC antes de consultar timestamptz — evita perder corridas após 21h
  // no Brasil quando o relógio UTC já virou para o dia seguinte.
  const fromLocal = new Date(dateStr+'T00:00:00');
  const toLocal = new Date(fromLocal); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  // null = falha de rede/consulta; [] = consulta válida sem corridas.
  return error ? null : (data||[]);
}
// Recusadas do dia — mesmo padrão de loadAutoTripsForDay, pra alimentar o
// contador "X aceitas / Y recusadas" do Total do dia. Tabela já existe e já
// é usada na tela Corridas (seção expansível); aqui só reaproveita pro
// recorte de UM dia em vez do período todo.
async function loadDeclinedOffersForDay(dateStr) {
  if (!_supaUser) return [];
  const from = dateStr+'T00:00:00', to = dateStr+'T23:59:59';
  const { data, error } = await _SUPA.from('declined_offers').select('id')
    .eq('user_id', _supaUser.id)
    .gte('seen_at', from).lte('seen_at', to);
  return error ? [] : (data||[]);
}

// ── Corridas automáticas → alimentam a Home (pedido do Yuri, 23/07/2026) ──
// Mesmo gap da Jornada, só que espalhado pela Home inteira: valor, km,
// corridas, todos os gráficos e o insight de IA só olhavam day.trips
// (Importadas) e ficavam cegos pra qualquer dia 100% automático. Em vez de
// corrigir função por função (arriscado, muita superfície), busca TODAS as
// automáticas do período de UMA VEZ (não uma consulta por dia — helper
// abaixo cobre semana/mês/ano inteiros numa chamada só) e injeta como
// corridas "de mentira" no formato de trip normal — todo código que já lê
// day.trips corretamente passa a enxergar as automáticas de graça, sem
// precisar tocar em cada gráfico individualmente.
async function loadAutoTripsIndexed(fromDate, toDate) {
  if (!_supaUser) return {};
  const fromLocal = new Date(fromDate+'T00:00:00');
  const toLocal = new Date(toDate+'T00:00:00'); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  if (error || !data) return {};
  const map = {};
  data.forEach(t => {
    const iso = t.trip_started_at || t.accepted_at;
    if (!iso) return;
    const dt = new Date(iso);
    const dateStr = dt.getFullYear()+'-'+p2(dt.getMonth()+1)+'-'+p2(dt.getDate());
    (map[dateStr] = map[dateStr] || []).push(t);
  });
  return map;
}
// Converte as automáticas de um dia (já indexadas em S.autoTripsByDate) pro
// MESMO formato de objeto que day.trips usa — value/km/time/duration/
// platform/dinheiro — assim qualquer soma/filtro que já existe funciona
// sem mudança nenhuma. NUNCA usar pra salvar (é só leitura/exibição —
// misturar isso em day.trips de verdade duplicaria dado no Supabase).
function autoTripsAsTrips(date) {
  const list = (S.autoTripsByDate && S.autoTripsByDate[date]) || [];
  return list.map(at => {
    const start = at.trip_started_at ? new Date(at.trip_started_at) : null;
    const end   = at.trip_ended_at ? new Date(at.trip_ended_at) : null;
    const durMin = start && end ? Math.max(0, Math.round((end-start)/60000))
                 : Math.round((at.offer_duration_trip_sec||0)/60);
    return {
      id: 'auto_'+at.id, _auto: true,
      platform: at.platform || 'uber',
      time: start ? p2(start.getHours())+':'+p2(start.getMinutes()) : '',
      duration: durMin,
      value: parseFloat(at.offer_value)||0,
      km: parseFloat(at.real_km_trip ?? at.offer_km_trip) || 0,
      dinheiro: !!at.dinheiro,
      origin:'', dest:'', category:''
    };
  });
}
// Todas as corridas de um dia — Importadas + Automáticas juntas, no mesmo
// formato. Uso exclusivo de leitura/agregação (gráficos, insights, somas).
function dayTripsAll(date) {
  return DB.day(date).trips.concat(autoTripsAsTrips(date));
}

// ── Corridas automaticas no Financeiro (soma por dia, cacheada em memoria) ──
// auto_trips vivem so no Supabase; o Financeiro cobre Mes/Ano, entao busca em
// lote (paginado) o intervalo do periodo + periodo anterior + mes corrente,
// agrupa por data LOCAL (fuso: margem de 1 dia em cada ponta da query).
let _finAuto = { key:'', byDate:{}, loading:false, ts:0 };
function finAutoDay(ds) { return _finAuto.byDate[ds] || { sum:0, count:0, items:[] }; }
async function loadFinAutoTrips() {
  if (!_supaUser) return;
  const today = new Date();
  const dim = new Date(today.getFullYear(), today.getMonth()+1, 0).getDate();
  const all = [...getFinDays(), ...getFinDaysPrev(),
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-01',
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-'+p2(dim)];
  let from = all[0], to = all[0];
  all.forEach(d => { if (d < from) from = d; if (d > to) to = d; });
  const key = from + '|' + to;

===== auto_trips @ 599712 =====
e o valor pulou de R$42 pra R$26,20 sem aviso nenhum —
  // pode ser a correção certa (leitura mais completa) ou pode ser a nova
  // leitura que tá errada; sem ver as duas, não dá pra saber qual confiar.
  const oldVal = t.offer_value != null ? Number(t.offer_value) : null;
  const newVal = patch.offer_value;
  let valueBigChange = false;
  if (oldVal != null && oldVal > 0) {
    const diff = Math.abs(newVal - oldVal);
    valueBigChange = diff > 5 && (diff / oldVal) > 0.15;
  }
  return { ok:true, tripId, patch, changes, oldVal, newVal, valueBigChange };
}
async function _applyAutoTripRefresh(tripId, patch) {
  const { error } = await _SUPA.from('auto_trips').update({ ...patch, value_needs_review: false }).eq('id', tripId);
  return !error;
}
async function refreshAutoTripFromLog(tripId, btnEl) {
  if (btnEl) btnEl.classList.add('spinning');
  try {
    const r = await _computeAutoTripRefresh(tripId);
    if (!r.ok) { showToast('Não achei: '+r.reason, 'error'); return; }
    if (!r.changes.length) { showToast('Corrida atualizada com sucesso', 'success'); return; }
    const finish = async () => {
      const saved = await _applyAutoTripRefresh(r.tripId, r.patch);
      if (!saved) { showToast('Erro ao salvar.', 'error'); return; }
      showToast('Corrigido: '+r.changes.join(', '), 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    };
    if (r.valueBigChange) {
      showConfirm(
        `O valor dessa corrida vai mudar de ${DB.fR(r.oldVal)} para ${DB.fR(r.newVal)} — uma diferença grande.\n\nConfirma que ${DB.fR(r.newVal)} é o valor certo?`,
        finish,
        { danger: false, confirmLabel: 'Sim, corrigir', cancelLabel: 'Cancelar' }
      );
    } else {
      await finish();
    }
  } catch (e) {
    console.error('refreshAutoTripFromLog', e);
    showToast('Erro ao reconferir.', 'error');
  } finally {
    if (btnEl) btnEl.classList.remove('spinning');
  }
}
// Rotina automática (24/07/2026, pedido do Yuri) — a cada 5min, tenta
// reconferir sozinho corridas "Aguardando revisão" (status='capturada')
// das últimas 48h. Silenciosa por corrida (não interrompe com toast a
// cada uma) — só avisa se corrigiu alguma, no total. Nunca mexe em
// corrida já confirmada, só nas que ainda não têm valor nenhum.
async function _autoRefreshPendingTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'capturada')
      .gte('accepted_at', new Date(Date.now() - 48*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixed = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (r.ok && r.changes.length) { await _applyAutoTripRefresh(r.tripId, r.patch); fixed++; }
      } catch(e) {}
    }
    if (fixed > 0) {
      showToast(fixed===1 ? '1 corrida corrigida sozinha' : fixed+' corridas corrigidas sozinhas', 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
  } catch (e) { console.error('_autoRefreshPendingTrips', e); }
}
setTimeout(()=>{ _autoRefreshPendingTrips(); setInterval(_autoRefreshPendingTrips, 5*60*1000); }, 6000);

// Rotina automática nº2 (16/08/2026, pedido do Yuri: "não quero usuário
// editando, tem que partir da gente interno... a cada X min confere o
// que tá no registro e corrige"). Diferente da rotina acima (que só olha
// corridas AINDA sem valor confirmado), essa reconfere corridas que JÁ
// estão 'confirmada' — pra pegar o caso real que aconteceu (valor errado
// que já tinha virado card normal na tela). Roda a cada 10min, olha só as
// últimas 24h (corrida mais velha que isso já foi conferida o suficiente
// de vezes). REGRA DE OURO: só aplica sozinha quando a mudança é pequena/
// confiável — mudança grande NUNCA aplica sem humano olhar (mesmo critério
// do botão manual), só marca value_needs_review pra aparecer sinalizada,
// sem sumir da tela nem dos totais (não é "fantasma", é corrida real que
// só precisa de confirmação do valor).
//
// MUDOU (16/08/2026): mirava em status='confirmada', mas agora
// 'confirmada' só nasce depois do vídeo do histórico (ver
// confirmImport()) — reconferir isso aqui não faz mais sentido, vídeo já
// É a fonte mais confiável que existe. Passa a mirar em 'estimada' (ciclo
// da captura ao vivo completo, só com valor da oferta) — mesmo espírito
// de antes, refinando o valor com a melhor leitura do log disponível
// enquanto o vídeo de confirmação não chega.
async function _autoReconcileConfirmedTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'estimada')
      .is('data_quality_flag', null)
      .gte('accepted_at', new Date(Date.now() - 24*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixedQuiet = 0, flaggedForReview = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (!r.ok || !r.changes.length) continue;
        if (r.valueBigChange) {
          await _SUPA.from('auto_trips').update({ value_needs_review: true }).eq('id', row.id);
          flaggedForReview++;
        } else {
          await _applyAutoTripRefresh(r.tripId, r.patch);
          fixedQuiet++;
        }
      } catch(e) {}
    }
    if (fixedQuiet > 0 || flaggedForReview > 0) {
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
    // Silenciosa de propósito — mudança pequena não precisa de aviso, e
    // mudança grande já fica visível pelo badge "Valor a confirmar" na
    // tela Corridas, sem precisar interromper com toast.
  } catch (e) { console.error('_autoReconcileConfirmedTrips', e); }
}
setTimeout(()=>{ _autoReconcileConfirmedTrips(); setInterval(_autoReconcileConfirmedTrips, 10*60*1000); }, 9000);
// Balãozinho discreto ao tocar num símbolo (dinâmico, vínculo à jornada,
// dinheiro) — pedido do Yuri (24/07/2026). Some sozinho, não navega nem
// abre nada.
function showTapLegend(event, text) {
  event.stopPropagation();
  document.querySelectorAll('.tap-legend-tip').forEach(el => el.remove());
  const wrap = event.currentTarget;
  const tip = document.createElement('div');
  tip.className = 'tap-legend-tip';
  tip.textContent = text;
  wrap.appendChild(tip);
  setTimeout(() => tip.remove(), 2500);
}
// Legenda de comparação previsto × real (Deslocamento/Corrida coloridos) —
// abre/fecha um painel fixo embaixo da linha, diferente do balãozinho dos
// símbolos porque tem mais texto (2 linhas de números).
function toggleCmpLegend(event, id) {
  event.stopPropagation();
  const el = document.getElementById(id);
  if (el) el.style.display = el.style.display === 'none' ? '' : 'none';
}
async function loadAutoTripsForDay(dateStr) {
  if (!_supaUser) return [];
  // O dia da Jornada é o dia LOCAL do motorista. Converte meia-noite local
  // para UTC antes de consultar timestamptz — evita perder corridas após 21h
  // no Brasil quando o relógio UTC já virou para o dia seguinte.
  const fromLocal = new Date(dateStr+'T00:00:00');
  const toLocal = new Date(fromLocal); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  // null = falha de rede/consulta; [] = consulta válida sem corridas.
  return error ? null : (data||[]);
}
// Recusadas do dia — mesmo padrão de loadAutoTripsForDay, pra alimentar o
// contador "X aceitas / Y recusadas" do Total do dia. Tabela já existe e já
// é usada na tela Corridas (seção expansível); aqui só reaproveita pro
// recorte de UM dia em vez do período todo.
async function loadDeclinedOffersForDay(dateStr) {
  if (!_supaUser) return [];
  const from = dateStr+'T00:00:00', to = dateStr+'T23:59:59';
  const { data, error } = await _SUPA.from('declined_offers').select('id')
    .eq('user_id', _supaUser.id)
    .gte('seen_at', from).lte('seen_at', to);
  return error ? [] : (data||[]);
}

// ── Corridas automáticas → alimentam a Home (pedido do Yuri, 23/07/2026) ──
// Mesmo gap da Jornada, só que espalhado pela Home inteira: valor, km,
// corridas, todos os gráficos e o insight de IA só olhavam day.trips
// (Importadas) e ficavam cegos pra qualquer dia 100% automático. Em vez de
// corrigir função por função (arriscado, muita superfície), busca TODAS as
// automáticas do período de UMA VEZ (não uma consulta por dia — helper
// abaixo cobre semana/mês/ano inteiros numa chamada só) e injeta como
// corridas "de mentira" no formato de trip normal — todo código que já lê
// day.trips corretamente passa a enxergar as automáticas de graça, sem
// precisar tocar em cada gráfico individualmente.
async function loadAutoTripsIndexed(fromDate, toDate) {
  if (!_supaUser) return {};
  const fromLocal = new Date(fromDate+'T00:00:00');
  const toLocal = new Date(toDate+'T00:00:00'); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  if (error || !data) return {};
  const map = {};
  data.forEach(t => {
    const iso = t.trip_started_at || t.accepted_at;
    if (!iso) return;
    const dt = new Date(iso);
    const dateStr = dt.getFullYear()+'-'+p2(dt.getMonth()+1)+'-'+p2(dt.getDate());
    (map[dateStr] = map[dateStr] || []).push(t);
  });
  return map;
}
// Converte as automáticas de um dia (já indexadas em S.autoTripsByDate) pro
// MESMO formato de objeto que day.trips usa — value/km/time/duration/
// platform/dinheiro — assim qualquer soma/filtro que já existe funciona
// sem mudança nenhuma. NUNCA usar pra salvar (é só leitura/exibição —
// misturar isso em day.trips de verdade duplicaria dado no Supabase).
function autoTripsAsTrips(date) {
  const list = (S.autoTripsByDate && S.autoTripsByDate[date]) || [];
  return list.map(at => {
    const start = at.trip_started_at ? new Date(at.trip_started_at) : null;
    const end   = at.trip_ended_at ? new Date(at.trip_ended_at) : null;
    const durMin = start && end ? Math.max(0, Math.round((end-start)/60000))
                 : Math.round((at.offer_duration_trip_sec||0)/60);
    return {
      id: 'auto_'+at.id, _auto: true,
      platform: at.platform || 'uber',
      time: start ? p2(start.getHours())+':'+p2(start.getMinutes()) : '',
      duration: durMin,
      value: parseFloat(at.offer_value)||0,
      km: parseFloat(at.real_km_trip ?? at.offer_km_trip) || 0,
      dinheiro: !!at.dinheiro,
      origin:'', dest:'', category:''
    };
  });
}
// Todas as corridas de um dia — Importadas + Automáticas juntas, no mesmo
// formato. Uso exclusivo de leitura/agregação (gráficos, insights, somas).
function dayTripsAll(date) {
  return DB.day(date).trips.concat(autoTripsAsTrips(date));
}

// ── Corridas automaticas no Financeiro (soma por dia, cacheada em memoria) ──
// auto_trips vivem so no Supabase; o Financeiro cobre Mes/Ano, entao busca em
// lote (paginado) o intervalo do periodo + periodo anterior + mes corrente,
// agrupa por data LOCAL (fuso: margem de 1 dia em cada ponta da query).
let _finAuto = { key:'', byDate:{}, loading:false, ts:0 };
function finAutoDay(ds) { return _finAuto.byDate[ds] || { sum:0, count:0, items:[] }; }
async function loadFinAutoTrips() {
  if (!_supaUser) return;
  const today = new Date();
  const dim = new Date(today.getFullYear(), today.getMonth()+1, 0).getDate();
  const all = [...getFinDays(), ...getFinDaysPrev(),
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-01',
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-'+p2(dim)];
  let from = all[0], to = all[0];
  all.forEach(d => { if (d < from) from = d; if (d > to) to = d; });
  const key = from + '|' + to;
  if (_finAuto.loading) return;
  if (_finAuto.key === key && (Date.now() - _finAuto.ts) < 120000) return;
  _finAuto.loading = true;
  try {
    const fromIso = new Date(new Date(from+'T00:00:00').getTime() - 86400000).toISOString();
    const toIso   = new Date(new Date(to+'T23:59:59').getTime() + 86400000).toISOString();
    const rows = [];
    let off = 0;
    while (true) {
      const { data, error } = await _SUPA.from('auto_trips')
        .select('id,trip_started_at,accepted_at,offer_value,platform')
        .eq('user_id', _supaUser.id)
        .gte('trip_started_at', fromIso).lte('trip_started_at', toIso)
        .order('trip_started_at', {ascending:true})
        .range(off, off + 999);
      if (error || !data || !data.length) break;
      rows.push(...data);
      if (data.length < 1000) break;
      off += 1000;
    }
    const byDate = {};
    rows.forEach(t => {
      const ts = autoTripTs(t); if (!ts) return;
      const d = new Date(ts);
      const ds = d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
      const b = byDate[ds] || (byDate[ds] = { sum:0, count:0, items:[] });
      b.sum += (t.offer_value || 0);
      b.count++;
      b.items.push(t);
    });
    _finAuto = { key, byDate, loading:false, ts: Date.now() };
    if (S.sec === 'financeiro') { try { renderFin(); } catch(e) {} }
  } catch(e) { _finAuto.loading = false; console.warn('[loadFinAutoTrips]', e); }
}

// Corridas: mescla Importadas + Automáticas numa lista só, ordenadas por
// horário — pedido do Yuri (24/07/2026). A tag "Captura automática" (já
// existente) identifica a origem, então não precisa mais de aba pra isso.
async function renderCorridasActive() {
  const dates = getCorridasDays().slice().sort().reverse(); // mais recente primeiro
  const plats = S.corridasPlats || new Set();
  const platFilter = t => plats.size === 0 || plats.has(t.platform||'uber');

  // Automáticas do mesmo intervalo — busca uma vez só, agrupa por data local.
  let autoByDate = {};
  if (_supaUser) {
    const range = autoCorridasDateRange();
    let q = _SUPA.from('auto_trips').select('*').eq('user_id', _supaUser.id).order('trip_started_at', {ascending:false});
    if (range) q = q.gte('trip_started_at', range.from).lte('trip_started_at', range.to);
    if (plats.size === 1) q = q.eq('platform', [...plats][0]);
    const { data, error } = await q;
    (error ? [] : (data||[])).forEach(t => {
      const ts = autoTripTs(t
===== auto_trips @ 600234 =====
anges, oldVal, newVal, valueBigChange };
}
async function _applyAutoTripRefresh(tripId, patch) {
  const { error } = await _SUPA.from('auto_trips').update({ ...patch, value_needs_review: false }).eq('id', tripId);
  return !error;
}
async function refreshAutoTripFromLog(tripId, btnEl) {
  if (btnEl) btnEl.classList.add('spinning');
  try {
    const r = await _computeAutoTripRefresh(tripId);
    if (!r.ok) { showToast('Não achei: '+r.reason, 'error'); return; }
    if (!r.changes.length) { showToast('Corrida atualizada com sucesso', 'success'); return; }
    const finish = async () => {
      const saved = await _applyAutoTripRefresh(r.tripId, r.patch);
      if (!saved) { showToast('Erro ao salvar.', 'error'); return; }
      showToast('Corrigido: '+r.changes.join(', '), 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    };
    if (r.valueBigChange) {
      showConfirm(
        `O valor dessa corrida vai mudar de ${DB.fR(r.oldVal)} para ${DB.fR(r.newVal)} — uma diferença grande.\n\nConfirma que ${DB.fR(r.newVal)} é o valor certo?`,
        finish,
        { danger: false, confirmLabel: 'Sim, corrigir', cancelLabel: 'Cancelar' }
      );
    } else {
      await finish();
    }
  } catch (e) {
    console.error('refreshAutoTripFromLog', e);
    showToast('Erro ao reconferir.', 'error');
  } finally {
    if (btnEl) btnEl.classList.remove('spinning');
  }
}
// Rotina automática (24/07/2026, pedido do Yuri) — a cada 5min, tenta
// reconferir sozinho corridas "Aguardando revisão" (status='capturada')
// das últimas 48h. Silenciosa por corrida (não interrompe com toast a
// cada uma) — só avisa se corrigiu alguma, no total. Nunca mexe em
// corrida já confirmada, só nas que ainda não têm valor nenhum.
async function _autoRefreshPendingTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'capturada')
      .gte('accepted_at', new Date(Date.now() - 48*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixed = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (r.ok && r.changes.length) { await _applyAutoTripRefresh(r.tripId, r.patch); fixed++; }
      } catch(e) {}
    }
    if (fixed > 0) {
      showToast(fixed===1 ? '1 corrida corrigida sozinha' : fixed+' corridas corrigidas sozinhas', 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
  } catch (e) { console.error('_autoRefreshPendingTrips', e); }
}
setTimeout(()=>{ _autoRefreshPendingTrips(); setInterval(_autoRefreshPendingTrips, 5*60*1000); }, 6000);

// Rotina automática nº2 (16/08/2026, pedido do Yuri: "não quero usuário
// editando, tem que partir da gente interno... a cada X min confere o
// que tá no registro e corrige"). Diferente da rotina acima (que só olha
// corridas AINDA sem valor confirmado), essa reconfere corridas que JÁ
// estão 'confirmada' — pra pegar o caso real que aconteceu (valor errado
// que já tinha virado card normal na tela). Roda a cada 10min, olha só as
// últimas 24h (corrida mais velha que isso já foi conferida o suficiente
// de vezes). REGRA DE OURO: só aplica sozinha quando a mudança é pequena/
// confiável — mudança grande NUNCA aplica sem humano olhar (mesmo critério
// do botão manual), só marca value_needs_review pra aparecer sinalizada,
// sem sumir da tela nem dos totais (não é "fantasma", é corrida real que
// só precisa de confirmação do valor).
//
// MUDOU (16/08/2026): mirava em status='confirmada', mas agora
// 'confirmada' só nasce depois do vídeo do histórico (ver
// confirmImport()) — reconferir isso aqui não faz mais sentido, vídeo já
// É a fonte mais confiável que existe. Passa a mirar em 'estimada' (ciclo
// da captura ao vivo completo, só com valor da oferta) — mesmo espírito
// de antes, refinando o valor com a melhor leitura do log disponível
// enquanto o vídeo de confirmação não chega.
async function _autoReconcileConfirmedTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'estimada')
      .is('data_quality_flag', null)
      .gte('accepted_at', new Date(Date.now() - 24*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixedQuiet = 0, flaggedForReview = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (!r.ok || !r.changes.length) continue;
        if (r.valueBigChange) {
          await _SUPA.from('auto_trips').update({ value_needs_review: true }).eq('id', row.id);
          flaggedForReview++;
        } else {
          await _applyAutoTripRefresh(r.tripId, r.patch);
          fixedQuiet++;
        }
      } catch(e) {}
    }
    if (fixedQuiet > 0 || flaggedForReview > 0) {
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
    // Silenciosa de propósito — mudança pequena não precisa de aviso, e
    // mudança grande já fica visível pelo badge "Valor a confirmar" na
    // tela Corridas, sem precisar interromper com toast.
  } catch (e) { console.error('_autoReconcileConfirmedTrips', e); }
}
setTimeout(()=>{ _autoReconcileConfirmedTrips(); setInterval(_autoReconcileConfirmedTrips, 10*60*1000); }, 9000);
// Balãozinho discreto ao tocar num símbolo (dinâmico, vínculo à jornada,
// dinheiro) — pedido do Yuri (24/07/2026). Some sozinho, não navega nem
// abre nada.
function showTapLegend(event, text) {
  event.stopPropagation();
  document.querySelectorAll('.tap-legend-tip').forEach(el => el.remove());
  const wrap = event.currentTarget;
  const tip = document.createElement('div');
  tip.className = 'tap-legend-tip';
  tip.textContent = text;
  wrap.appendChild(tip);
  setTimeout(() => tip.remove(), 2500);
}
// Legenda de comparação previsto × real (Deslocamento/Corrida coloridos) —
// abre/fecha um painel fixo embaixo da linha, diferente do balãozinho dos
// símbolos porque tem mais texto (2 linhas de números).
function toggleCmpLegend(event, id) {
  event.stopPropagation();
  const el = document.getElementById(id);
  if (el) el.style.display = el.style.display === 'none' ? '' : 'none';
}
async function loadAutoTripsForDay(dateStr) {
  if (!_supaUser) return [];
  // O dia da Jornada é o dia LOCAL do motorista. Converte meia-noite local
  // para UTC antes de consultar timestamptz — evita perder corridas após 21h
  // no Brasil quando o relógio UTC já virou para o dia seguinte.
  const fromLocal = new Date(dateStr+'T00:00:00');
  const toLocal = new Date(fromLocal); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  // null = falha de rede/consulta; [] = consulta válida sem corridas.
  return error ? null : (data||[]);
}
// Recusadas do dia — mesmo padrão de loadAutoTripsForDay, pra alimentar o
// contador "X aceitas / Y recusadas" do Total do dia. Tabela já existe e já
// é usada na tela Corridas (seção expansível); aqui só reaproveita pro
// recorte de UM dia em vez do período todo.
async function loadDeclinedOffersForDay(dateStr) {
  if (!_supaUser) return [];
  const from = dateStr+'T00:00:00', to = dateStr+'T23:59:59';
  const { data, error } = await _SUPA.from('declined_offers').select('id')
    .eq('user_id', _supaUser.id)
    .gte('seen_at', from).lte('seen_at', to);
  return error ? [] : (data||[]);
}

// ── Corridas automáticas → alimentam a Home (pedido do Yuri, 23/07/2026) ──
// Mesmo gap da Jornada, só que espalhado pela Home inteira: valor, km,
// corridas, todos os gráficos e o insight de IA só olhavam day.trips
// (Importadas) e ficavam cegos pra qualquer dia 100% automático. Em vez de
// corrigir função por função (arriscado, muita superfície), busca TODAS as
// automáticas do período de UMA VEZ (não uma consulta por dia — helper
// abaixo cobre semana/mês/ano inteiros numa chamada só) e injeta como
// corridas "de mentira" no formato de trip normal — todo código que já lê
// day.trips corretamente passa a enxergar as automáticas de graça, sem
// precisar tocar em cada gráfico individualmente.
async function loadAutoTripsIndexed(fromDate, toDate) {
  if (!_supaUser) return {};
  const fromLocal = new Date(fromDate+'T00:00:00');
  const toLocal = new Date(toDate+'T00:00:00'); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  if (error || !data) return {};
  const map = {};
  data.forEach(t => {
    const iso = t.trip_started_at || t.accepted_at;
    if (!iso) return;
    const dt = new Date(iso);
    const dateStr = dt.getFullYear()+'-'+p2(dt.getMonth()+1)+'-'+p2(dt.getDate());
    (map[dateStr] = map[dateStr] || []).push(t);
  });
  return map;
}
// Converte as automáticas de um dia (já indexadas em S.autoTripsByDate) pro
// MESMO formato de objeto que day.trips usa — value/km/time/duration/
// platform/dinheiro — assim qualquer soma/filtro que já existe funciona
// sem mudança nenhuma. NUNCA usar pra salvar (é só leitura/exibição —
// misturar isso em day.trips de verdade duplicaria dado no Supabase).
function autoTripsAsTrips(date) {
  const list = (S.autoTripsByDate && S.autoTripsByDate[date]) || [];
  return list.map(at => {
    const start = at.trip_started_at ? new Date(at.trip_started_at) : null;
    const end   = at.trip_ended_at ? new Date(at.trip_ended_at) : null;
    const durMin = start && end ? Math.max(0, Math.round((end-start)/60000))
                 : Math.round((at.offer_duration_trip_sec||0)/60);
    return {
      id: 'auto_'+at.id, _auto: true,
      platform: at.platform || 'uber',
      time: start ? p2(start.getHours())+':'+p2(start.getMinutes()) : '',
      duration: durMin,
      value: parseFloat(at.offer_value)||0,
      km: parseFloat(at.real_km_trip ?? at.offer_km_trip) || 0,
      dinheiro: !!at.dinheiro,
      origin:'', dest:'', category:''
    };
  });
}
// Todas as corridas de um dia — Importadas + Automáticas juntas, no mesmo
// formato. Uso exclusivo de leitura/agregação (gráficos, insights, somas).
function dayTripsAll(date) {
  return DB.day(date).trips.concat(autoTripsAsTrips(date));
}

// ── Corridas automaticas no Financeiro (soma por dia, cacheada em memoria) ──
// auto_trips vivem so no Supabase; o Financeiro cobre Mes/Ano, entao busca em
// lote (paginado) o intervalo do periodo + periodo anterior + mes corrente,
// agrupa por data LOCAL (fuso: margem de 1 dia em cada ponta da query).
let _finAuto = { key:'', byDate:{}, loading:false, ts:0 };
function finAutoDay(ds) { return _finAuto.byDate[ds] || { sum:0, count:0, items:[] }; }
async function loadFinAutoTrips() {
  if (!_supaUser) return;
  const today = new Date();
  const dim = new Date(today.getFullYear(), today.getMonth()+1, 0).getDate();
  const all = [...getFinDays(), ...getFinDaysPrev(),
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-01',
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-'+p2(dim)];
  let from = all[0], to = all[0];
  all.forEach(d => { if (d < from) from = d; if (d > to) to = d; });
  const key = from + '|' + to;
  if (_finAuto.loading) return;
  if (_finAuto.key === key && (Date.now() - _finAuto.ts) < 120000) return;
  _finAuto.loading = true;
  try {
    const fromIso = new Date(new Date(from+'T00:00:00').getTime() - 86400000).toISOString();
    const toIso   = new Date(new Date(to+'T23:59:59').getTime() + 86400000).toISOString();
    const rows = [];
    let off = 0;
    while (true) {
      const { data, error } = await _SUPA.from('auto_trips')
        .select('id,trip_started_at,accepted_at,offer_value,platform')
        .eq('user_id', _supaUser.id)
        .gte('trip_started_at', fromIso).lte('trip_started_at', toIso)
        .order('trip_started_at', {ascending:true})
        .range(off, off + 999);
      if (error || !data || !data.length) break;
      rows.push(...data);
      if (data.length < 1000) break;
      off += 1000;
    }
    const byDate = {};
    rows.forEach(t => {
      const ts = autoTripTs(t); if (!ts) return;
      const d = new Date(ts);
      const ds = d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
      const b = byDate[ds] || (byDate[ds] = { sum:0, count:0, items:[] });
      b.sum += (t.offer_value || 0);
      b.count++;
      b.items.push(t);
    });
    _finAuto = { key, byDate, loading:false, ts: Date.now() };
    if (S.sec === 'financeiro') { try { renderFin(); } catch(e) {} }
  } catch(e) { _finAuto.loading = false; console.warn('[loadFinAutoTrips]', e); }
}

// Corridas: mescla Importadas + Automáticas numa lista só, ordenadas por
// horário — pedido do Yuri (24/07/2026). A tag "Captura automática" (já
// existente) identifica a origem, então não precisa mais de aba pra isso.
async function renderCorridasActive() {
  const dates = getCorridasDays().slice().sort().reverse(); // mais recente primeiro
  const plats = S.corridasPlats || new Set();
  const platFilter = t => plats.size === 0 || plats.has(t.platform||'uber');

  // Automáticas do mesmo intervalo — busca uma vez só, agrupa por data local.
  let autoByDate = {};
  if (_supaUser) {
    const range = autoCorridasDateRange();
    let q = _SUPA.from('auto_trips').select('*').eq('user_id', _supaUser.id).order('trip_started_at', {ascending:false});
    if (range) q = q.gte('trip_started_at', range.from).lte('trip_started_at', range.to);
    if (plats.size === 1) q = q.eq('platform', [...plats][0]);
    const { data, error } = await q;
    (error ? [] : (data||[])).forEach(t => {
      const ts = autoTripTs(t);
      if (!ts) return;
      const d = new Date(ts);
      const onePlat = plats.size === 1 ? [...plats][0] : null;
      const dateStr = onePlat ? operationalDateForTimestamp(onePlat, ts) : localYmdFromMs(ts);
      (autoByDate[dateStr] = autoByDate[dateStr] || []).push(t);
    });
  }

  let totalVal = 0, totalKm = 0, totalN = 0, totalMin = 0;
  const groups = [];
  dates.forEach(d => {
    const day = DB.day(d);
    const manual = (day.trips||[]).filter(platFilter)
      .map(t => ({ kind:'manual', ts: t.ts || 
===== auto_trips @ 602494 =====
rn;
    let fixed = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (r.ok && r.changes.length) { await _applyAutoTripRefresh(r.tripId, r.patch); fixed++; }
      } catch(e) {}
    }
    if (fixed > 0) {
      showToast(fixed===1 ? '1 corrida corrigida sozinha' : fixed+' corridas corrigidas sozinhas', 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
  } catch (e) { console.error('_autoRefreshPendingTrips', e); }
}
setTimeout(()=>{ _autoRefreshPendingTrips(); setInterval(_autoRefreshPendingTrips, 5*60*1000); }, 6000);

// Rotina automática nº2 (16/08/2026, pedido do Yuri: "não quero usuário
// editando, tem que partir da gente interno... a cada X min confere o
// que tá no registro e corrige"). Diferente da rotina acima (que só olha
// corridas AINDA sem valor confirmado), essa reconfere corridas que JÁ
// estão 'confirmada' — pra pegar o caso real que aconteceu (valor errado
// que já tinha virado card normal na tela). Roda a cada 10min, olha só as
// últimas 24h (corrida mais velha que isso já foi conferida o suficiente
// de vezes). REGRA DE OURO: só aplica sozinha quando a mudança é pequena/
// confiável — mudança grande NUNCA aplica sem humano olhar (mesmo critério
// do botão manual), só marca value_needs_review pra aparecer sinalizada,
// sem sumir da tela nem dos totais (não é "fantasma", é corrida real que
// só precisa de confirmação do valor).
//
// MUDOU (16/08/2026): mirava em status='confirmada', mas agora
// 'confirmada' só nasce depois do vídeo do histórico (ver
// confirmImport()) — reconferir isso aqui não faz mais sentido, vídeo já
// É a fonte mais confiável que existe. Passa a mirar em 'estimada' (ciclo
// da captura ao vivo completo, só com valor da oferta) — mesmo espírito
// de antes, refinando o valor com a melhor leitura do log disponível
// enquanto o vídeo de confirmação não chega.
async function _autoReconcileConfirmedTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'estimada')
      .is('data_quality_flag', null)
      .gte('accepted_at', new Date(Date.now() - 24*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixedQuiet = 0, flaggedForReview = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (!r.ok || !r.changes.length) continue;
        if (r.valueBigChange) {
          await _SUPA.from('auto_trips').update({ value_needs_review: true }).eq('id', row.id);
          flaggedForReview++;
        } else {
          await _applyAutoTripRefresh(r.tripId, r.patch);
          fixedQuiet++;
        }
      } catch(e) {}
    }
    if (fixedQuiet > 0 || flaggedForReview > 0) {
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
    // Silenciosa de propósito — mudança pequena não precisa de aviso, e
    // mudança grande já fica visível pelo badge "Valor a confirmar" na
    // tela Corridas, sem precisar interromper com toast.
  } catch (e) { console.error('_autoReconcileConfirmedTrips', e); }
}
setTimeout(()=>{ _autoReconcileConfirmedTrips(); setInterval(_autoReconcileConfirmedTrips, 10*60*1000); }, 9000);
// Balãozinho discreto ao tocar num símbolo (dinâmico, vínculo à jornada,
// dinheiro) — pedido do Yuri (24/07/2026). Some sozinho, não navega nem
// abre nada.
function showTapLegend(event, text) {
  event.stopPropagation();
  document.querySelectorAll('.tap-legend-tip').forEach(el => el.remove());
  const wrap = event.currentTarget;
  const tip = document.createElement('div');
  tip.className = 'tap-legend-tip';
  tip.textContent = text;
  wrap.appendChild(tip);
  setTimeout(() => tip.remove(), 2500);
}
// Legenda de comparação previsto × real (Deslocamento/Corrida coloridos) —
// abre/fecha um painel fixo embaixo da linha, diferente do balãozinho dos
// símbolos porque tem mais texto (2 linhas de números).
function toggleCmpLegend(event, id) {
  event.stopPropagation();
  const el = document.getElementById(id);
  if (el) el.style.display = el.style.display === 'none' ? '' : 'none';
}
async function loadAutoTripsForDay(dateStr) {
  if (!_supaUser) return [];
  // O dia da Jornada é o dia LOCAL do motorista. Converte meia-noite local
  // para UTC antes de consultar timestamptz — evita perder corridas após 21h
  // no Brasil quando o relógio UTC já virou para o dia seguinte.
  const fromLocal = new Date(dateStr+'T00:00:00');
  const toLocal = new Date(fromLocal); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  // null = falha de rede/consulta; [] = consulta válida sem corridas.
  return error ? null : (data||[]);
}
// Recusadas do dia — mesmo padrão de loadAutoTripsForDay, pra alimentar o
// contador "X aceitas / Y recusadas" do Total do dia. Tabela já existe e já
// é usada na tela Corridas (seção expansível); aqui só reaproveita pro
// recorte de UM dia em vez do período todo.
async function loadDeclinedOffersForDay(dateStr) {
  if (!_supaUser) return [];
  const from = dateStr+'T00:00:00', to = dateStr+'T23:59:59';
  const { data, error } = await _SUPA.from('declined_offers').select('id')
    .eq('user_id', _supaUser.id)
    .gte('seen_at', from).lte('seen_at', to);
  return error ? [] : (data||[]);
}

// ── Corridas automáticas → alimentam a Home (pedido do Yuri, 23/07/2026) ──
// Mesmo gap da Jornada, só que espalhado pela Home inteira: valor, km,
// corridas, todos os gráficos e o insight de IA só olhavam day.trips
// (Importadas) e ficavam cegos pra qualquer dia 100% automático. Em vez de
// corrigir função por função (arriscado, muita superfície), busca TODAS as
// automáticas do período de UMA VEZ (não uma consulta por dia — helper
// abaixo cobre semana/mês/ano inteiros numa chamada só) e injeta como
// corridas "de mentira" no formato de trip normal — todo código que já lê
// day.trips corretamente passa a enxergar as automáticas de graça, sem
// precisar tocar em cada gráfico individualmente.
async function loadAutoTripsIndexed(fromDate, toDate) {
  if (!_supaUser) return {};
  const fromLocal = new Date(fromDate+'T00:00:00');
  const toLocal = new Date(toDate+'T00:00:00'); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  if (error || !data) return {};
  const map = {};
  data.forEach(t => {
    const iso = t.trip_started_at || t.accepted_at;
    if (!iso) return;
    const dt = new Date(iso);
    const dateStr = dt.getFullYear()+'-'+p2(dt.getMonth()+1)+'-'+p2(dt.getDate());
    (map[dateStr] = map[dateStr] || []).push(t);
  });
  return map;
}
// Converte as automáticas de um dia (já indexadas em S.autoTripsByDate) pro
// MESMO formato de objeto que day.trips usa — value/km/time/duration/
// platform/dinheiro — assim qualquer soma/filtro que já existe funciona
// sem mudança nenhuma. NUNCA usar pra salvar (é só leitura/exibição —
// misturar isso em day.trips de verdade duplicaria dado no Supabase).
function autoTripsAsTrips(date) {
  const list = (S.autoTripsByDate && S.autoTripsByDate[date]) || [];
  return list.map(at => {
    const start = at.trip_started_at ? new Date(at.trip_started_at) : null;
    const end   = at.trip_ended_at ? new Date(at.trip_ended_at) : null;
    const durMin = start && end ? Math.max(0, Math.round((end-start)/60000))
                 : Math.round((at.offer_duration_trip_sec||0)/60);
    return {
      id: 'auto_'+at.id, _auto: true,
      platform: at.platform || 'uber',
      time: start ? p2(start.getHours())+':'+p2(start.getMinutes()) : '',
      duration: durMin,
      value: parseFloat(at.offer_value)||0,
      km: parseFloat(at.real_km_trip ?? at.offer_km_trip) || 0,
      dinheiro: !!at.dinheiro,
      origin:'', dest:'', category:''
    };
  });
}
// Todas as corridas de um dia — Importadas + Automáticas juntas, no mesmo
// formato. Uso exclusivo de leitura/agregação (gráficos, insights, somas).
function dayTripsAll(date) {
  return DB.day(date).trips.concat(autoTripsAsTrips(date));
}

// ── Corridas automaticas no Financeiro (soma por dia, cacheada em memoria) ──
// auto_trips vivem so no Supabase; o Financeiro cobre Mes/Ano, entao busca em
// lote (paginado) o intervalo do periodo + periodo anterior + mes corrente,
// agrupa por data LOCAL (fuso: margem de 1 dia em cada ponta da query).
let _finAuto = { key:'', byDate:{}, loading:false, ts:0 };
function finAutoDay(ds) { return _finAuto.byDate[ds] || { sum:0, count:0, items:[] }; }
async function loadFinAutoTrips() {
  if (!_supaUser) return;
  const today = new Date();
  const dim = new Date(today.getFullYear(), today.getMonth()+1, 0).getDate();
  const all = [...getFinDays(), ...getFinDaysPrev(),
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-01',
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-'+p2(dim)];
  let from = all[0], to = all[0];
  all.forEach(d => { if (d < from) from = d; if (d > to) to = d; });
  const key = from + '|' + to;
  if (_finAuto.loading) return;
  if (_finAuto.key === key && (Date.now() - _finAuto.ts) < 120000) return;
  _finAuto.loading = true;
  try {
    const fromIso = new Date(new Date(from+'T00:00:00').getTime() - 86400000).toISOString();
    const toIso   = new Date(new Date(to+'T23:59:59').getTime() + 86400000).toISOString();
    const rows = [];
    let off = 0;
    while (true) {
      const { data, error } = await _SUPA.from('auto_trips')
        .select('id,trip_started_at,accepted_at,offer_value,platform')
        .eq('user_id', _supaUser.id)
        .gte('trip_started_at', fromIso).lte('trip_started_at', toIso)
        .order('trip_started_at', {ascending:true})
        .range(off, off + 999);
      if (error || !data || !data.length) break;
      rows.push(...data);
      if (data.length < 1000) break;
      off += 1000;
    }
    const byDate = {};
    rows.forEach(t => {
      const ts = autoTripTs(t); if (!ts) return;
      const d = new Date(ts);
      const ds = d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
      const b = byDate[ds] || (byDate[ds] = { sum:0, count:0, items:[] });
      b.sum += (t.offer_value || 0);
      b.count++;
      b.items.push(t);
    });
    _finAuto = { key, byDate, loading:false, ts: Date.now() };
    if (S.sec === 'financeiro') { try { renderFin(); } catch(e) {} }
  } catch(e) { _finAuto.loading = false; console.warn('[loadFinAutoTrips]', e); }
}

// Corridas: mescla Importadas + Automáticas numa lista só, ordenadas por
// horário — pedido do Yuri (24/07/2026). A tag "Captura automática" (já
// existente) identifica a origem, então não precisa mais de aba pra isso.
async function renderCorridasActive() {
  const dates = getCorridasDays().slice().sort().reverse(); // mais recente primeiro
  const plats = S.corridasPlats || new Set();
  const platFilter = t => plats.size === 0 || plats.has(t.platform||'uber');

  // Automáticas do mesmo intervalo — busca uma vez só, agrupa por data local.
  let autoByDate = {};
  if (_supaUser) {
    const range = autoCorridasDateRange();
    let q = _SUPA.from('auto_trips').select('*').eq('user_id', _supaUser.id).order('trip_started_at', {ascending:false});
    if (range) q = q.gte('trip_started_at', range.from).lte('trip_started_at', range.to);
    if (plats.size === 1) q = q.eq('platform', [...plats][0]);
    const { data, error } = await q;
    (error ? [] : (data||[])).forEach(t => {
      const ts = autoTripTs(t);
      if (!ts) return;
      const d = new Date(ts);
      const onePlat = plats.size === 1 ? [...plats][0] : null;
      const dateStr = onePlat ? operationalDateForTimestamp(onePlat, ts) : localYmdFromMs(ts);
      (autoByDate[dateStr] = autoByDate[dateStr] || []).push(t);
    });
  }

  let totalVal = 0, totalKm = 0, totalN = 0, totalMin = 0;
  const groups = [];
  dates.forEach(d => {
    const day = DB.day(d);
    const manual = (day.trips||[]).filter(platFilter)
      .map(t => ({ kind:'manual', ts: t.ts || (t.time?new Date(d+'T'+t.time+':00').getTime():0), t }));
    const auto = (autoByDate[d]||[]).filter(platFilter)
      .map(t => ({ kind:'auto', ts: autoTripTs(t), t }));
    const merged = [...manual, ...auto].sort((a,b)=>b.ts-a.ts);
    if (!merged.length) return;
    merged.forEach(x => {
      if (x.kind==='manual') { totalVal += (x.t.value||0); totalKm += (x.t.km||0); totalMin += (x.t.duration||0); }
      else {
        totalVal += (x.t.offer_value||0); totalKm += (x.t.real_km_trip||0);
        if (x.t.accepted_at && x.t.trip_ended_at) totalMin += Math.max(0,(new Date(x.t.trip_ended_at)-new Date(x.t.accepted_at))/60000);
      }
      totalN++;
    });
    groups.push({ date: d, items: merged });
  });

  setText('cor-sum-val', DB.fR(totalVal));
  setText('cor-sum-km', totalKm.toLocaleString('pt-BR',{minimumFractionDigits:1,maximumFractionDigits:1}));
  setText('cor-sum-n', totalN);
  renderDeclinedOffers(dates, plats, totalVal, totalKm, totalMin);

  const listEl = document.getElementById('cor-list');
  if (!listEl) return;
  if (!groups.length) {
    listEl.innerHTML = `<div class="empty" style="text-align:center;padding:50px 20px;color:#64748B">
      <svg viewBox="0 0 24 24" width="36" height="36" fill="none" stroke="currentColor" stroke-width="1.6" style="color:#94A3B8;margin-bottom:10px"><path d="M5 17h14M6 17l1-6 3-4h4l3 4 1 6"/><circle cx="9" cy="20" r="1.4" fill="currentColor"/><circle cx="17" cy="20" r="1.4" fill="currentColor"/></svg>
      <div style="font-weight:700;font-size:14px;color:#0F172A;margin-bottom:4px">Nenhuma corrida encontrada</div>
      <div style="font-size:12.5px">Tente mudar o período ou a plataforma do filtro</div>
    </div>`;
    reapplyOpenTripDetail();
    return;
  }
  const todayStr = DB.today();
  const fmtSep = d => {
    if (d === todayStr) return 'Hoje · ' + d.split('-').reverse().slice(0,2).join('/');
    return d.split('-').reverse().slice(0,2).join('/') + '/' + d.slice(0,4);
  };
  listEl.innerHTML = groups.map(g =>
    `<div class="day-sep">${fmtSep(g.date)}</div>`
    + g.items.map(x => x.kind==='manual' ? tripItem(x.t, g.date) : autoTripItem(x.t)).join('')
  ).join('');
  reapplyOpenTripDetail();
}
function autoCorridasDateRange() {
  const dates = getCorridasDays().
===== auto_trips @ 604390 =====
iclo
// da captura ao vivo completo, só com valor da oferta) — mesmo espírito
// de antes, refinando o valor com a melhor leitura do log disponível
// enquanto o vídeo de confirmação não chega.
async function _autoReconcileConfirmedTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'estimada')
      .is('data_quality_flag', null)
      .gte('accepted_at', new Date(Date.now() - 24*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixedQuiet = 0, flaggedForReview = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (!r.ok || !r.changes.length) continue;
        if (r.valueBigChange) {
          await _SUPA.from('auto_trips').update({ value_needs_review: true }).eq('id', row.id);
          flaggedForReview++;
        } else {
          await _applyAutoTripRefresh(r.tripId, r.patch);
          fixedQuiet++;
        }
      } catch(e) {}
    }
    if (fixedQuiet > 0 || flaggedForReview > 0) {
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
    // Silenciosa de propósito — mudança pequena não precisa de aviso, e
    // mudança grande já fica visível pelo badge "Valor a confirmar" na
    // tela Corridas, sem precisar interromper com toast.
  } catch (e) { console.error('_autoReconcileConfirmedTrips', e); }
}
setTimeout(()=>{ _autoReconcileConfirmedTrips(); setInterval(_autoReconcileConfirmedTrips, 10*60*1000); }, 9000);
// Balãozinho discreto ao tocar num símbolo (dinâmico, vínculo à jornada,
// dinheiro) — pedido do Yuri (24/07/2026). Some sozinho, não navega nem
// abre nada.
function showTapLegend(event, text) {
  event.stopPropagation();
  document.querySelectorAll('.tap-legend-tip').forEach(el => el.remove());
  const wrap = event.currentTarget;
  const tip = document.createElement('div');
  tip.className = 'tap-legend-tip';
  tip.textContent = text;
  wrap.appendChild(tip);
  setTimeout(() => tip.remove(), 2500);
}
// Legenda de comparação previsto × real (Deslocamento/Corrida coloridos) —
// abre/fecha um painel fixo embaixo da linha, diferente do balãozinho dos
// símbolos porque tem mais texto (2 linhas de números).
function toggleCmpLegend(event, id) {
  event.stopPropagation();
  const el = document.getElementById(id);
  if (el) el.style.display = el.style.display === 'none' ? '' : 'none';
}
async function loadAutoTripsForDay(dateStr) {
  if (!_supaUser) return [];
  // O dia da Jornada é o dia LOCAL do motorista. Converte meia-noite local
  // para UTC antes de consultar timestamptz — evita perder corridas após 21h
  // no Brasil quando o relógio UTC já virou para o dia seguinte.
  const fromLocal = new Date(dateStr+'T00:00:00');
  const toLocal = new Date(fromLocal); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  // null = falha de rede/consulta; [] = consulta válida sem corridas.
  return error ? null : (data||[]);
}
// Recusadas do dia — mesmo padrão de loadAutoTripsForDay, pra alimentar o
// contador "X aceitas / Y recusadas" do Total do dia. Tabela já existe e já
// é usada na tela Corridas (seção expansível); aqui só reaproveita pro
// recorte de UM dia em vez do período todo.
async function loadDeclinedOffersForDay(dateStr) {
  if (!_supaUser) return [];
  const from = dateStr+'T00:00:00', to = dateStr+'T23:59:59';
  const { data, error } = await _SUPA.from('declined_offers').select('id')
    .eq('user_id', _supaUser.id)
    .gte('seen_at', from).lte('seen_at', to);
  return error ? [] : (data||[]);
}

// ── Corridas automáticas → alimentam a Home (pedido do Yuri, 23/07/2026) ──
// Mesmo gap da Jornada, só que espalhado pela Home inteira: valor, km,
// corridas, todos os gráficos e o insight de IA só olhavam day.trips
// (Importadas) e ficavam cegos pra qualquer dia 100% automático. Em vez de
// corrigir função por função (arriscado, muita superfície), busca TODAS as
// automáticas do período de UMA VEZ (não uma consulta por dia — helper
// abaixo cobre semana/mês/ano inteiros numa chamada só) e injeta como
// corridas "de mentira" no formato de trip normal — todo código que já lê
// day.trips corretamente passa a enxergar as automáticas de graça, sem
// precisar tocar em cada gráfico individualmente.
async function loadAutoTripsIndexed(fromDate, toDate) {
  if (!_supaUser) return {};
  const fromLocal = new Date(fromDate+'T00:00:00');
  const toLocal = new Date(toDate+'T00:00:00'); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  if (error || !data) return {};
  const map = {};
  data.forEach(t => {
    const iso = t.trip_started_at || t.accepted_at;
    if (!iso) return;
    const dt = new Date(iso);
    const dateStr = dt.getFullYear()+'-'+p2(dt.getMonth()+1)+'-'+p2(dt.getDate());
    (map[dateStr] = map[dateStr] || []).push(t);
  });
  return map;
}
// Converte as automáticas de um dia (já indexadas em S.autoTripsByDate) pro
// MESMO formato de objeto que day.trips usa — value/km/time/duration/
// platform/dinheiro — assim qualquer soma/filtro que já existe funciona
// sem mudança nenhuma. NUNCA usar pra salvar (é só leitura/exibição —
// misturar isso em day.trips de verdade duplicaria dado no Supabase).
function autoTripsAsTrips(date) {
  const list = (S.autoTripsByDate && S.autoTripsByDate[date]) || [];
  return list.map(at => {
    const start = at.trip_started_at ? new Date(at.trip_started_at) : null;
    const end   = at.trip_ended_at ? new Date(at.trip_ended_at) : null;
    const durMin = start && end ? Math.max(0, Math.round((end-start)/60000))
                 : Math.round((at.offer_duration_trip_sec||0)/60);
    return {
      id: 'auto_'+at.id, _auto: true,
      platform: at.platform || 'uber',
      time: start ? p2(start.getHours())+':'+p2(start.getMinutes()) : '',
      duration: durMin,
      value: parseFloat(at.offer_value)||0,
      km: parseFloat(at.real_km_trip ?? at.offer_km_trip) || 0,
      dinheiro: !!at.dinheiro,
      origin:'', dest:'', category:''
    };
  });
}
// Todas as corridas de um dia — Importadas + Automáticas juntas, no mesmo
// formato. Uso exclusivo de leitura/agregação (gráficos, insights, somas).
function dayTripsAll(date) {
  return DB.day(date).trips.concat(autoTripsAsTrips(date));
}

// ── Corridas automaticas no Financeiro (soma por dia, cacheada em memoria) ──
// auto_trips vivem so no Supabase; o Financeiro cobre Mes/Ano, entao busca em
// lote (paginado) o intervalo do periodo + periodo anterior + mes corrente,
// agrupa por data LOCAL (fuso: margem de 1 dia em cada ponta da query).
let _finAuto = { key:'', byDate:{}, loading:false, ts:0 };
function finAutoDay(ds) { return _finAuto.byDate[ds] || { sum:0, count:0, items:[] }; }
async function loadFinAutoTrips() {
  if (!_supaUser) return;
  const today = new Date();
  const dim = new Date(today.getFullYear(), today.getMonth()+1, 0).getDate();
  const all = [...getFinDays(), ...getFinDaysPrev(),
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-01',
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-'+p2(dim)];
  let from = all[0], to = all[0];
  all.forEach(d => { if (d < from) from = d; if (d > to) to = d; });
  const key = from + '|' + to;
  if (_finAuto.loading) return;
  if (_finAuto.key === key && (Date.now() - _finAuto.ts) < 120000) return;
  _finAuto.loading = true;
  try {
    const fromIso = new Date(new Date(from+'T00:00:00').getTime() - 86400000).toISOString();
    const toIso   = new Date(new Date(to+'T23:59:59').getTime() + 86400000).toISOString();
    const rows = [];
    let off = 0;
    while (true) {
      const { data, error } = await _SUPA.from('auto_trips')
        .select('id,trip_started_at,accepted_at,offer_value,platform')
        .eq('user_id', _supaUser.id)
        .gte('trip_started_at', fromIso).lte('trip_started_at', toIso)
        .order('trip_started_at', {ascending:true})
        .range(off, off + 999);
      if (error || !data || !data.length) break;
      rows.push(...data);
      if (data.length < 1000) break;
      off += 1000;
    }
    const byDate = {};
    rows.forEach(t => {
      const ts = autoTripTs(t); if (!ts) return;
      const d = new Date(ts);
      const ds = d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
      const b = byDate[ds] || (byDate[ds] = { sum:0, count:0, items:[] });
      b.sum += (t.offer_value || 0);
      b.count++;
      b.items.push(t);
    });
    _finAuto = { key, byDate, loading:false, ts: Date.now() };
    if (S.sec === 'financeiro') { try { renderFin(); } catch(e) {} }
  } catch(e) { _finAuto.loading = false; console.warn('[loadFinAutoTrips]', e); }
}

// Corridas: mescla Importadas + Automáticas numa lista só, ordenadas por
// horário — pedido do Yuri (24/07/2026). A tag "Captura automática" (já
// existente) identifica a origem, então não precisa mais de aba pra isso.
async function renderCorridasActive() {
  const dates = getCorridasDays().slice().sort().reverse(); // mais recente primeiro
  const plats = S.corridasPlats || new Set();
  const platFilter = t => plats.size === 0 || plats.has(t.platform||'uber');

  // Automáticas do mesmo intervalo — busca uma vez só, agrupa por data local.
  let autoByDate = {};
  if (_supaUser) {
    const range = autoCorridasDateRange();
    let q = _SUPA.from('auto_trips').select('*').eq('user_id', _supaUser.id).order('trip_started_at', {ascending:false});
    if (range) q = q.gte('trip_started_at', range.from).lte('trip_started_at', range.to);
    if (plats.size === 1) q = q.eq('platform', [...plats][0]);
    const { data, error } = await q;
    (error ? [] : (data||[])).forEach(t => {
      const ts = autoTripTs(t);
      if (!ts) return;
      const d = new Date(ts);
      const onePlat = plats.size === 1 ? [...plats][0] : null;
      const dateStr = onePlat ? operationalDateForTimestamp(onePlat, ts) : localYmdFromMs(ts);
      (autoByDate[dateStr] = autoByDate[dateStr] || []).push(t);
    });
  }

  let totalVal = 0, totalKm = 0, totalN = 0, totalMin = 0;
  const groups = [];
  dates.forEach(d => {
    const day = DB.day(d);
    const manual = (day.trips||[]).filter(platFilter)
      .map(t => ({ kind:'manual', ts: t.ts || (t.time?new Date(d+'T'+t.time+':00').getTime():0), t }));
    const auto = (autoByDate[d]||[]).filter(platFilter)
      .map(t => ({ kind:'auto', ts: autoTripTs(t), t }));
    const merged = [...manual, ...auto].sort((a,b)=>b.ts-a.ts);
    if (!merged.length) return;
    merged.forEach(x => {
      if (x.kind==='manual') { totalVal += (x.t.value||0); totalKm += (x.t.km||0); totalMin += (x.t.duration||0); }
      else {
        totalVal += (x.t.offer_value||0); totalKm += (x.t.real_km_trip||0);
        if (x.t.accepted_at && x.t.trip_ended_at) totalMin += Math.max(0,(new Date(x.t.trip_ended_at)-new Date(x.t.accepted_at))/60000);
      }
      totalN++;
    });
    groups.push({ date: d, items: merged });
  });

  setText('cor-sum-val', DB.fR(totalVal));
  setText('cor-sum-km', totalKm.toLocaleString('pt-BR',{minimumFractionDigits:1,maximumFractionDigits:1}));
  setText('cor-sum-n', totalN);
  renderDeclinedOffers(dates, plats, totalVal, totalKm, totalMin);

  const listEl = document.getElementById('cor-list');
  if (!listEl) return;
  if (!groups.length) {
    listEl.innerHTML = `<div class="empty" style="text-align:center;padding:50px 20px;color:#64748B">
      <svg viewBox="0 0 24 24" width="36" height="36" fill="none" stroke="currentColor" stroke-width="1.6" style="color:#94A3B8;margin-bottom:10px"><path d="M5 17h14M6 17l1-6 3-4h4l3 4 1 6"/><circle cx="9" cy="20" r="1.4" fill="currentColor"/><circle cx="17" cy="20" r="1.4" fill="currentColor"/></svg>
      <div style="font-weight:700;font-size:14px;color:#0F172A;margin-bottom:4px">Nenhuma corrida encontrada</div>
      <div style="font-size:12.5px">Tente mudar o período ou a plataforma do filtro</div>
    </div>`;
    reapplyOpenTripDetail();
    return;
  }
  const todayStr = DB.today();
  const fmtSep = d => {
    if (d === todayStr) return 'Hoje · ' + d.split('-').reverse().slice(0,2).join('/');
    return d.split('-').reverse().slice(0,2).join('/') + '/' + d.slice(0,4);
  };
  listEl.innerHTML = groups.map(g =>
    `<div class="day-sep">${fmtSep(g.date)}</div>`
    + g.items.map(x => x.kind==='manual' ? tripItem(x.t, g.date) : autoTripItem(x.t)).join('')
  ).join('');
  reapplyOpenTripDetail();
}
function autoCorridasDateRange() {
  const dates = getCorridasDays().slice().sort();
  if (!dates.length) return null;
  return { from: dates[0]+'T00:00:00', to: dates[dates.length-1]+'T23:59:59' };
}

// Corridas recusadas/expiradas — pedido do Yuri (24/07/2026): seção
// expansível em Corridas, R$/km e R$/h somando deslocamento (até o
// embarque) + corrida, comparando com o que foi de fato aceito. Respeita
// os mesmos filtros de plataforma/período já usados na lista principal.
function toggleDeclinedOffers() {
  const body = document.getElementById('decl-body');
  const toggle = document.getElementById('decl-toggle');
  if (!body || !toggle) return;
  const open = body.style.display !== 'none';
  body.style.display = open ? 'none' : 'block';
  toggle.classList.toggle('open', !open);
}
async function renderDeclinedOffers(dates, plats, acceptedVal, acceptedKm, acceptedMin) {
  const toggle = document.getElementById('decl-toggle');
  const body = document.getElementById('decl-body');
  const subtitle = document.getElementById('decl-subtitle');
  if (!toggle || !body || !_supaUser) return;
  const range = autoCorridasDateRange();
  if (!range) { toggle.style.display = 'none'; return; }
  let q = _SUPA.from('declined_offers').select('*').eq('user_id', _supaUser.id)
    .gte('seen_at', range.from).lte('seen_at', range.to)
    .order('seen_at', { ascending:false });
  if (plats.size === 1) q = q.eq('platform', [...plats][0]);
  const { data, error } = await q;
  const list = error ? [] : (data||[]);
  if (!list.length) { toggle.style.display = 'none'; body.style.display = 'none'; return; }
  toggle.style.display = 'flex';
  setText('decl-subtitle', list.length===1 ? '1 oferta não aceita' : list.length+' ofertas não aceitas');

  let declVal = 0, declKm = 0, declMin = 0;
  const byDate = {};
  list.forEach(o => {
    const km = (o.offer_km_pickup||0) + (o.offer_km_trip||0);
    const minTot = ((o.offer_duration_pickup_sec||0) + (o.offe
===== auto_trips @ 606416 =====
.remove());
  const wrap = event.currentTarget;
  const tip = document.createElement('div');
  tip.className = 'tap-legend-tip';
  tip.textContent = text;
  wrap.appendChild(tip);
  setTimeout(() => tip.remove(), 2500);
}
// Legenda de comparação previsto × real (Deslocamento/Corrida coloridos) —
// abre/fecha um painel fixo embaixo da linha, diferente do balãozinho dos
// símbolos porque tem mais texto (2 linhas de números).
function toggleCmpLegend(event, id) {
  event.stopPropagation();
  const el = document.getElementById(id);
  if (el) el.style.display = el.style.display === 'none' ? '' : 'none';
}
async function loadAutoTripsForDay(dateStr) {
  if (!_supaUser) return [];
  // O dia da Jornada é o dia LOCAL do motorista. Converte meia-noite local
  // para UTC antes de consultar timestamptz — evita perder corridas após 21h
  // no Brasil quando o relógio UTC já virou para o dia seguinte.
  const fromLocal = new Date(dateStr+'T00:00:00');
  const toLocal = new Date(fromLocal); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  // null = falha de rede/consulta; [] = consulta válida sem corridas.
  return error ? null : (data||[]);
}
// Recusadas do dia — mesmo padrão de loadAutoTripsForDay, pra alimentar o
// contador "X aceitas / Y recusadas" do Total do dia. Tabela já existe e já
// é usada na tela Corridas (seção expansível); aqui só reaproveita pro
// recorte de UM dia em vez do período todo.
async function loadDeclinedOffersForDay(dateStr) {
  if (!_supaUser) return [];
  const from = dateStr+'T00:00:00', to = dateStr+'T23:59:59';
  const { data, error } = await _SUPA.from('declined_offers').select('id')
    .eq('user_id', _supaUser.id)
    .gte('seen_at', from).lte('seen_at', to);
  return error ? [] : (data||[]);
}

// ── Corridas automáticas → alimentam a Home (pedido do Yuri, 23/07/2026) ──
// Mesmo gap da Jornada, só que espalhado pela Home inteira: valor, km,
// corridas, todos os gráficos e o insight de IA só olhavam day.trips
// (Importadas) e ficavam cegos pra qualquer dia 100% automático. Em vez de
// corrigir função por função (arriscado, muita superfície), busca TODAS as
// automáticas do período de UMA VEZ (não uma consulta por dia — helper
// abaixo cobre semana/mês/ano inteiros numa chamada só) e injeta como
// corridas "de mentira" no formato de trip normal — todo código que já lê
// day.trips corretamente passa a enxergar as automáticas de graça, sem
// precisar tocar em cada gráfico individualmente.
async function loadAutoTripsIndexed(fromDate, toDate) {
  if (!_supaUser) return {};
  const fromLocal = new Date(fromDate+'T00:00:00');
  const toLocal = new Date(toDate+'T00:00:00'); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  if (error || !data) return {};
  const map = {};
  data.forEach(t => {
    const iso = t.trip_started_at || t.accepted_at;
    if (!iso) return;
    const dt = new Date(iso);
    const dateStr = dt.getFullYear()+'-'+p2(dt.getMonth()+1)+'-'+p2(dt.getDate());
    (map[dateStr] = map[dateStr] || []).push(t);
  });
  return map;
}
// Converte as automáticas de um dia (já indexadas em S.autoTripsByDate) pro
// MESMO formato de objeto que day.trips usa — value/km/time/duration/
// platform/dinheiro — assim qualquer soma/filtro que já existe funciona
// sem mudança nenhuma. NUNCA usar pra salvar (é só leitura/exibição —
// misturar isso em day.trips de verdade duplicaria dado no Supabase).
function autoTripsAsTrips(date) {
  const list = (S.autoTripsByDate && S.autoTripsByDate[date]) || [];
  return list.map(at => {
    const start = at.trip_started_at ? new Date(at.trip_started_at) : null;
    const end   = at.trip_ended_at ? new Date(at.trip_ended_at) : null;
    const durMin = start && end ? Math.max(0, Math.round((end-start)/60000))
                 : Math.round((at.offer_duration_trip_sec||0)/60);
    return {
      id: 'auto_'+at.id, _auto: true,
      platform: at.platform || 'uber',
      time: start ? p2(start.getHours())+':'+p2(start.getMinutes()) : '',
      duration: durMin,
      value: parseFloat(at.offer_value)||0,
      km: parseFloat(at.real_km_trip ?? at.offer_km_trip) || 0,
      dinheiro: !!at.dinheiro,
      origin:'', dest:'', category:''
    };
  });
}
// Todas as corridas de um dia — Importadas + Automáticas juntas, no mesmo
// formato. Uso exclusivo de leitura/agregação (gráficos, insights, somas).
function dayTripsAll(date) {
  return DB.day(date).trips.concat(autoTripsAsTrips(date));
}

// ── Corridas automaticas no Financeiro (soma por dia, cacheada em memoria) ──
// auto_trips vivem so no Supabase; o Financeiro cobre Mes/Ano, entao busca em
// lote (paginado) o intervalo do periodo + periodo anterior + mes corrente,
// agrupa por data LOCAL (fuso: margem de 1 dia em cada ponta da query).
let _finAuto = { key:'', byDate:{}, loading:false, ts:0 };
function finAutoDay(ds) { return _finAuto.byDate[ds] || { sum:0, count:0, items:[] }; }
async function loadFinAutoTrips() {
  if (!_supaUser) return;
  const today = new Date();
  const dim = new Date(today.getFullYear(), today.getMonth()+1, 0).getDate();
  const all = [...getFinDays(), ...getFinDaysPrev(),
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-01',
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-'+p2(dim)];
  let from = all[0], to = all[0];
  all.forEach(d => { if (d < from) from = d; if (d > to) to = d; });
  const key = from + '|' + to;
  if (_finAuto.loading) return;
  if (_finAuto.key === key && (Date.now() - _finAuto.ts) < 120000) return;
  _finAuto.loading = true;
  try {
    const fromIso = new Date(new Date(from+'T00:00:00').getTime() - 86400000).toISOString();
    const toIso   = new Date(new Date(to+'T23:59:59').getTime() + 86400000).toISOString();
    const rows = [];
    let off = 0;
    while (true) {
      const { data, error } = await _SUPA.from('auto_trips')
        .select('id,trip_started_at,accepted_at,offer_value,platform')
        .eq('user_id', _supaUser.id)
        .gte('trip_started_at', fromIso).lte('trip_started_at', toIso)
        .order('trip_started_at', {ascending:true})
        .range(off, off + 999);
      if (error || !data || !data.length) break;
      rows.push(...data);
      if (data.length < 1000) break;
      off += 1000;
    }
    const byDate = {};
    rows.forEach(t => {
      const ts = autoTripTs(t); if (!ts) return;
      const d = new Date(ts);
      const ds = d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
      const b = byDate[ds] || (byDate[ds] = { sum:0, count:0, items:[] });
      b.sum += (t.offer_value || 0);
      b.count++;
      b.items.push(t);
    });
    _finAuto = { key, byDate, loading:false, ts: Date.now() };
    if (S.sec === 'financeiro') { try { renderFin(); } catch(e) {} }
  } catch(e) { _finAuto.loading = false; console.warn('[loadFinAutoTrips]', e); }
}

// Corridas: mescla Importadas + Automáticas numa lista só, ordenadas por
// horário — pedido do Yuri (24/07/2026). A tag "Captura automática" (já
// existente) identifica a origem, então não precisa mais de aba pra isso.
async function renderCorridasActive() {
  const dates = getCorridasDays().slice().sort().reverse(); // mais recente primeiro
  const plats = S.corridasPlats || new Set();
  const platFilter = t => plats.size === 0 || plats.has(t.platform||'uber');

  // Automáticas do mesmo intervalo — busca uma vez só, agrupa por data local.
  let autoByDate = {};
  if (_supaUser) {
    const range = autoCorridasDateRange();
    let q = _SUPA.from('auto_trips').select('*').eq('user_id', _supaUser.id).order('trip_started_at', {ascending:false});
    if (range) q = q.gte('trip_started_at', range.from).lte('trip_started_at', range.to);
    if (plats.size === 1) q = q.eq('platform', [...plats][0]);
    const { data, error } = await q;
    (error ? [] : (data||[])).forEach(t => {
      const ts = autoTripTs(t);
      if (!ts) return;
      const d = new Date(ts);
      const onePlat = plats.size === 1 ? [...plats][0] : null;
      const dateStr = onePlat ? operationalDateForTimestamp(onePlat, ts) : localYmdFromMs(ts);
      (autoByDate[dateStr] = autoByDate[dateStr] || []).push(t);
    });
  }

  let totalVal = 0, totalKm = 0, totalN = 0, totalMin = 0;
  const groups = [];
  dates.forEach(d => {
    const day = DB.day(d);
    const manual = (day.trips||[]).filter(platFilter)
      .map(t => ({ kind:'manual', ts: t.ts || (t.time?new Date(d+'T'+t.time+':00').getTime():0), t }));
    const auto = (autoByDate[d]||[]).filter(platFilter)
      .map(t => ({ kind:'auto', ts: autoTripTs(t), t }));
    const merged = [...manual, ...auto].sort((a,b)=>b.ts-a.ts);
    if (!merged.length) return;
    merged.forEach(x => {
      if (x.kind==='manual') { totalVal += (x.t.value||0); totalKm += (x.t.km||0); totalMin += (x.t.duration||0); }
      else {
        totalVal += (x.t.offer_value||0); totalKm += (x.t.real_km_trip||0);
        if (x.t.accepted_at && x.t.trip_ended_at) totalMin += Math.max(0,(new Date(x.t.trip_ended_at)-new Date(x.t.accepted_at))/60000);
      }
      totalN++;
    });
    groups.push({ date: d, items: merged });
  });

  setText('cor-sum-val', DB.fR(totalVal));
  setText('cor-sum-km', totalKm.toLocaleString('pt-BR',{minimumFractionDigits:1,maximumFractionDigits:1}));
  setText('cor-sum-n', totalN);
  renderDeclinedOffers(dates, plats, totalVal, totalKm, totalMin);

  const listEl = document.getElementById('cor-list');
  if (!listEl) return;
  if (!groups.length) {
    listEl.innerHTML = `<div class="empty" style="text-align:center;padding:50px 20px;color:#64748B">
      <svg viewBox="0 0 24 24" width="36" height="36" fill="none" stroke="currentColor" stroke-width="1.6" style="color:#94A3B8;margin-bottom:10px"><path d="M5 17h14M6 17l1-6 3-4h4l3 4 1 6"/><circle cx="9" cy="20" r="1.4" fill="currentColor"/><circle cx="17" cy="20" r="1.4" fill="currentColor"/></svg>
      <div style="font-weight:700;font-size:14px;color:#0F172A;margin-bottom:4px">Nenhuma corrida encontrada</div>
      <div style="font-size:12.5px">Tente mudar o período ou a plataforma do filtro</div>
    </div>`;
    reapplyOpenTripDetail();
    return;
  }
  const todayStr = DB.today();
  const fmtSep = d => {
    if (d === todayStr) return 'Hoje · ' + d.split('-').reverse().slice(0,2).join('/');
    return d.split('-').reverse().slice(0,2).join('/') + '/' + d.slice(0,4);
  };
  listEl.innerHTML = groups.map(g =>
    `<div class="day-sep">${fmtSep(g.date)}</div>`
    + g.items.map(x => x.kind==='manual' ? tripItem(x.t, g.date) : autoTripItem(x.t)).join('')
  ).join('');
  reapplyOpenTripDetail();
}
function autoCorridasDateRange() {
  const dates = getCorridasDays().slice().sort();
  if (!dates.length) return null;
  return { from: dates[0]+'T00:00:00', to: dates[dates.length-1]+'T23:59:59' };
}

// Corridas recusadas/expiradas — pedido do Yuri (24/07/2026): seção
// expansível em Corridas, R$/km e R$/h somando deslocamento (até o
// embarque) + corrida, comparando com o que foi de fato aceito. Respeita
// os mesmos filtros de plataforma/período já usados na lista principal.
function toggleDeclinedOffers() {
  const body = document.getElementById('decl-body');
  const toggle = document.getElementById('decl-toggle');
  if (!body || !toggle) return;
  const open = body.style.display !== 'none';
  body.style.display = open ? 'none' : 'block';
  toggle.classList.toggle('open', !open);
}
async function renderDeclinedOffers(dates, plats, acceptedVal, acceptedKm, acceptedMin) {
  const toggle = document.getElementById('decl-toggle');
  const body = document.getElementById('decl-body');
  const subtitle = document.getElementById('decl-subtitle');
  if (!toggle || !body || !_supaUser) return;
  const range = autoCorridasDateRange();
  if (!range) { toggle.style.display = 'none'; return; }
  let q = _SUPA.from('declined_offers').select('*').eq('user_id', _supaUser.id)
    .gte('seen_at', range.from).lte('seen_at', range.to)
    .order('seen_at', { ascending:false });
  if (plats.size === 1) q = q.eq('platform', [...plats][0]);
  const { data, error } = await q;
  const list = error ? [] : (data||[]);
  if (!list.length) { toggle.style.display = 'none'; body.style.display = 'none'; return; }
  toggle.style.display = 'flex';
  setText('decl-subtitle', list.length===1 ? '1 oferta não aceita' : list.length+' ofertas não aceitas');

  let declVal = 0, declKm = 0, declMin = 0;
  const byDate = {};
  list.forEach(o => {
    const km = (o.offer_km_pickup||0) + (o.offer_km_trip||0);
    const minTot = ((o.offer_duration_pickup_sec||0) + (o.offer_duration_trip_sec||0)) / 60;
    declVal += (o.offer_value||0); declKm += km; declMin += minTot;
    const d = new Date(o.seen_at);
    const onePlat = plats.size === 1 ? [...plats][0] : null;
    const dateStr = onePlat ? operationalDateForTimestamp(onePlat, d.getTime()) : localYmdFromMs(d.getTime());
    (byDate[dateStr] = byDate[dateStr]||[]).push({ o, km, minTot, ts: d.getTime() });
  });

  const acceptedRkm = acceptedKm>0 ? acceptedVal/acceptedKm : 0;
  const acceptedRph = acceptedMin>0 ? acceptedVal/(acceptedMin/60) : 0;
  const declRkm = declKm>0 ? declVal/declKm : 0;
  const declRph = declMin>0 ? declVal/(declMin/60) : 0;
  const better = acceptedRkm===0 || declRkm===0 ? null : declRkm < acceptedRkm;

  const todayStr = DB.today();
  const fmtSep = d => d===todayStr ? 'Hoje · '+d.split('-').reverse().slice(0,2).join('/') : d.split('-').reverse().slice(0,2).join('/')+'/'+d.slice(0,4);
  const sortedDates = Object.keys(byDate).sort().reverse();

  body.innerHTML = `
    <div class="cmp-strip">
      <div class="cmp-card g"><div class="t">Aceitas</div><div class="v">${DB.fR(acceptedRkm)}/km</div><div class="v2">${DB.fR(acceptedRph)}/h</div></div>
      <div class="cmp-card r"><div class="t">Recusadas</div><div class="v">${DB.fR(declRkm)}/km</div><div class="v2">${DB.fR(declRph)}/h</div></div>
    </div>
    ${better===null ? '' : `<div class="cmp-note">${better
      ? '<b>No geral, boa triagem:</b> o que você recusou rendia menos por km e por hora do que o que aceitou.'
      : '<b>Vale reparar:</b> algumas recusadas renderiam mais por km/hora que a média das aceitas — pode valer olhar os motivos com mais atenção.'}</div>`}
    ${sortedDates.map(d => `
      <div class="dsec">${fmtSep(d)}</div>
      ${byDate[d].sort((a,b)=>b.ts-a.ts).map(({o,km,minTot}) => {
        const plat = o.platform==='uber' ? 'Uber' : '99';
        const platCls = o.platform==='uber' ? 'uber' : 'n99';
        const rkm = km>0 ? DB.fR((o.offer_value||0)/km)+'/km' : '—';
        const hh = new Date(o.seen_at)
===== auto_trips @ 607698 =====

  // null = falha de rede/consulta; [] = consulta válida sem corridas.
  return error ? null : (data||[]);
}
// Recusadas do dia — mesmo padrão de loadAutoTripsForDay, pra alimentar o
// contador "X aceitas / Y recusadas" do Total do dia. Tabela já existe e já
// é usada na tela Corridas (seção expansível); aqui só reaproveita pro
// recorte de UM dia em vez do período todo.
async function loadDeclinedOffersForDay(dateStr) {
  if (!_supaUser) return [];
  const from = dateStr+'T00:00:00', to = dateStr+'T23:59:59';
  const { data, error } = await _SUPA.from('declined_offers').select('id')
    .eq('user_id', _supaUser.id)
    .gte('seen_at', from).lte('seen_at', to);
  return error ? [] : (data||[]);
}

// ── Corridas automáticas → alimentam a Home (pedido do Yuri, 23/07/2026) ──
// Mesmo gap da Jornada, só que espalhado pela Home inteira: valor, km,
// corridas, todos os gráficos e o insight de IA só olhavam day.trips
// (Importadas) e ficavam cegos pra qualquer dia 100% automático. Em vez de
// corrigir função por função (arriscado, muita superfície), busca TODAS as
// automáticas do período de UMA VEZ (não uma consulta por dia — helper
// abaixo cobre semana/mês/ano inteiros numa chamada só) e injeta como
// corridas "de mentira" no formato de trip normal — todo código que já lê
// day.trips corretamente passa a enxergar as automáticas de graça, sem
// precisar tocar em cada gráfico individualmente.
async function loadAutoTripsIndexed(fromDate, toDate) {
  if (!_supaUser) return {};
  const fromLocal = new Date(fromDate+'T00:00:00');
  const toLocal = new Date(toDate+'T00:00:00'); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  if (error || !data) return {};
  const map = {};
  data.forEach(t => {
    const iso = t.trip_started_at || t.accepted_at;
    if (!iso) return;
    const dt = new Date(iso);
    const dateStr = dt.getFullYear()+'-'+p2(dt.getMonth()+1)+'-'+p2(dt.getDate());
    (map[dateStr] = map[dateStr] || []).push(t);
  });
  return map;
}
// Converte as automáticas de um dia (já indexadas em S.autoTripsByDate) pro
// MESMO formato de objeto que day.trips usa — value/km/time/duration/
// platform/dinheiro — assim qualquer soma/filtro que já existe funciona
// sem mudança nenhuma. NUNCA usar pra salvar (é só leitura/exibição —
// misturar isso em day.trips de verdade duplicaria dado no Supabase).
function autoTripsAsTrips(date) {
  const list = (S.autoTripsByDate && S.autoTripsByDate[date]) || [];
  return list.map(at => {
    const start = at.trip_started_at ? new Date(at.trip_started_at) : null;
    const end   = at.trip_ended_at ? new Date(at.trip_ended_at) : null;
    const durMin = start && end ? Math.max(0, Math.round((end-start)/60000))
                 : Math.round((at.offer_duration_trip_sec||0)/60);
    return {
      id: 'auto_'+at.id, _auto: true,
      platform: at.platform || 'uber',
      time: start ? p2(start.getHours())+':'+p2(start.getMinutes()) : '',
      duration: durMin,
      value: parseFloat(at.offer_value)||0,
      km: parseFloat(at.real_km_trip ?? at.offer_km_trip) || 0,
      dinheiro: !!at.dinheiro,
      origin:'', dest:'', category:''
    };
  });
}
// Todas as corridas de um dia — Importadas + Automáticas juntas, no mesmo
// formato. Uso exclusivo de leitura/agregação (gráficos, insights, somas).
function dayTripsAll(date) {
  return DB.day(date).trips.concat(autoTripsAsTrips(date));
}

// ── Corridas automaticas no Financeiro (soma por dia, cacheada em memoria) ──
// auto_trips vivem so no Supabase; o Financeiro cobre Mes/Ano, entao busca em
// lote (paginado) o intervalo do periodo + periodo anterior + mes corrente,
// agrupa por data LOCAL (fuso: margem de 1 dia em cada ponta da query).
let _finAuto = { key:'', byDate:{}, loading:false, ts:0 };
function finAutoDay(ds) { return _finAuto.byDate[ds] || { sum:0, count:0, items:[] }; }
async function loadFinAutoTrips() {
  if (!_supaUser) return;
  const today = new Date();
  const dim = new Date(today.getFullYear(), today.getMonth()+1, 0).getDate();
  const all = [...getFinDays(), ...getFinDaysPrev(),
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-01',
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-'+p2(dim)];
  let from = all[0], to = all[0];
  all.forEach(d => { if (d < from) from = d; if (d > to) to = d; });
  const key = from + '|' + to;
  if (_finAuto.loading) return;
  if (_finAuto.key === key && (Date.now() - _finAuto.ts) < 120000) return;
  _finAuto.loading = true;
  try {
    const fromIso = new Date(new Date(from+'T00:00:00').getTime() - 86400000).toISOString();
    const toIso   = new Date(new Date(to+'T23:59:59').getTime() + 86400000).toISOString();
    const rows = [];
    let off = 0;
    while (true) {
      const { data, error } = await _SUPA.from('auto_trips')
        .select('id,trip_started_at,accepted_at,offer_value,platform')
        .eq('user_id', _supaUser.id)
        .gte('trip_started_at', fromIso).lte('trip_started_at', toIso)
        .order('trip_started_at', {ascending:true})
        .range(off, off + 999);
      if (error || !data || !data.length) break;
      rows.push(...data);
      if (data.length < 1000) break;
      off += 1000;
    }
    const byDate = {};
    rows.forEach(t => {
      const ts = autoTripTs(t); if (!ts) return;
      const d = new Date(ts);
      const ds = d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
      const b = byDate[ds] || (byDate[ds] = { sum:0, count:0, items:[] });
      b.sum += (t.offer_value || 0);
      b.count++;
      b.items.push(t);
    });
    _finAuto = { key, byDate, loading:false, ts: Date.now() };
    if (S.sec === 'financeiro') { try { renderFin(); } catch(e) {} }
  } catch(e) { _finAuto.loading = false; console.warn('[loadFinAutoTrips]', e); }
}

// Corridas: mescla Importadas + Automáticas numa lista só, ordenadas por
// horário — pedido do Yuri (24/07/2026). A tag "Captura automática" (já
// existente) identifica a origem, então não precisa mais de aba pra isso.
async function renderCorridasActive() {
  const dates = getCorridasDays().slice().sort().reverse(); // mais recente primeiro
  const plats = S.corridasPlats || new Set();
  const platFilter = t => plats.size === 0 || plats.has(t.platform||'uber');

  // Automáticas do mesmo intervalo — busca uma vez só, agrupa por data local.
  let autoByDate = {};
  if (_supaUser) {
    const range = autoCorridasDateRange();
    let q = _SUPA.from('auto_trips').select('*').eq('user_id', _supaUser.id).order('trip_started_at', {ascending:false});
    if (range) q = q.gte('trip_started_at', range.from).lte('trip_started_at', range.to);
    if (plats.size === 1) q = q.eq('platform', [...plats][0]);
    const { data, error } = await q;
    (error ? [] : (data||[])).forEach(t => {
      const ts = autoTripTs(t);
      if (!ts) return;
      const d = new Date(ts);
      const onePlat = plats.size === 1 ? [...plats][0] : null;
      const dateStr = onePlat ? operationalDateForTimestamp(onePlat, ts) : localYmdFromMs(ts);
      (autoByDate[dateStr] = autoByDate[dateStr] || []).push(t);
    });
  }

  let totalVal = 0, totalKm = 0, totalN = 0, totalMin = 0;
  const groups = [];
  dates.forEach(d => {
    const day = DB.day(d);
    const manual = (day.trips||[]).filter(platFilter)
      .map(t => ({ kind:'manual', ts: t.ts || (t.time?new Date(d+'T'+t.time+':00').getTime():0), t }));
    const auto = (autoByDate[d]||[]).filter(platFilter)
      .map(t => ({ kind:'auto', ts: autoTripTs(t), t }));
    const merged = [...manual, ...auto].sort((a,b)=>b.ts-a.ts);
    if (!merged.length) return;
    merged.forEach(x => {
      if (x.kind==='manual') { totalVal += (x.t.value||0); totalKm += (x.t.km||0); totalMin += (x.t.duration||0); }
      else {
        totalVal += (x.t.offer_value||0); totalKm += (x.t.real_km_trip||0);
        if (x.t.accepted_at && x.t.trip_ended_at) totalMin += Math.max(0,(new Date(x.t.trip_ended_at)-new Date(x.t.accepted_at))/60000);
      }
      totalN++;
    });
    groups.push({ date: d, items: merged });
  });

  setText('cor-sum-val', DB.fR(totalVal));
  setText('cor-sum-km', totalKm.toLocaleString('pt-BR',{minimumFractionDigits:1,maximumFractionDigits:1}));
  setText('cor-sum-n', totalN);
  renderDeclinedOffers(dates, plats, totalVal, totalKm, totalMin);

  const listEl = document.getElementById('cor-list');
  if (!listEl) return;
  if (!groups.length) {
    listEl.innerHTML = `<div class="empty" style="text-align:center;padding:50px 20px;color:#64748B">
      <svg viewBox="0 0 24 24" width="36" height="36" fill="none" stroke="currentColor" stroke-width="1.6" style="color:#94A3B8;margin-bottom:10px"><path d="M5 17h14M6 17l1-6 3-4h4l3 4 1 6"/><circle cx="9" cy="20" r="1.4" fill="currentColor"/><circle cx="17" cy="20" r="1.4" fill="currentColor"/></svg>
      <div style="font-weight:700;font-size:14px;color:#0F172A;margin-bottom:4px">Nenhuma corrida encontrada</div>
      <div style="font-size:12.5px">Tente mudar o período ou a plataforma do filtro</div>
    </div>`;
    reapplyOpenTripDetail();
    return;
  }
  const todayStr = DB.today();
  const fmtSep = d => {
    if (d === todayStr) return 'Hoje · ' + d.split('-').reverse().slice(0,2).join('/');
    return d.split('-').reverse().slice(0,2).join('/') + '/' + d.slice(0,4);
  };
  listEl.innerHTML = groups.map(g =>
    `<div class="day-sep">${fmtSep(g.date)}</div>`
    + g.items.map(x => x.kind==='manual' ? tripItem(x.t, g.date) : autoTripItem(x.t)).join('')
  ).join('');
  reapplyOpenTripDetail();
}
function autoCorridasDateRange() {
  const dates = getCorridasDays().slice().sort();
  if (!dates.length) return null;
  return { from: dates[0]+'T00:00:00', to: dates[dates.length-1]+'T23:59:59' };
}

// Corridas recusadas/expiradas — pedido do Yuri (24/07/2026): seção
// expansível em Corridas, R$/km e R$/h somando deslocamento (até o
// embarque) + corrida, comparando com o que foi de fato aceito. Respeita
// os mesmos filtros de plataforma/período já usados na lista principal.
function toggleDeclinedOffers() {
  const body = document.getElementById('decl-body');
  const toggle = document.getElementById('decl-toggle');
  if (!body || !toggle) return;
  const open = body.style.display !== 'none';
  body.style.display = open ? 'none' : 'block';
  toggle.classList.toggle('open', !open);
}
async function renderDeclinedOffers(dates, plats, acceptedVal, acceptedKm, acceptedMin) {
  const toggle = document.getElementById('decl-toggle');
  const body = document.getElementById('decl-body');
  const subtitle = document.getElementById('decl-subtitle');
  if (!toggle || !body || !_supaUser) return;
  const range = autoCorridasDateRange();
  if (!range) { toggle.style.display = 'none'; return; }
  let q = _SUPA.from('declined_offers').select('*').eq('user_id', _supaUser.id)
    .gte('seen_at', range.from).lte('seen_at', range.to)
    .order('seen_at', { ascending:false });
  if (plats.size === 1) q = q.eq('platform', [...plats][0]);
  const { data, error } = await q;
  const list = error ? [] : (data||[]);
  if (!list.length) { toggle.style.display = 'none'; body.style.display = 'none'; return; }
  toggle.style.display = 'flex';
  setText('decl-subtitle', list.length===1 ? '1 oferta não aceita' : list.length+' ofertas não aceitas');

  let declVal = 0, declKm = 0, declMin = 0;
  const byDate = {};
  list.forEach(o => {
    const km = (o.offer_km_pickup||0) + (o.offer_km_trip||0);
    const minTot = ((o.offer_duration_pickup_sec||0) + (o.offer_duration_trip_sec||0)) / 60;
    declVal += (o.offer_value||0); declKm += km; declMin += minTot;
    const d = new Date(o.seen_at);
    const onePlat = plats.size === 1 ? [...plats][0] : null;
    const dateStr = onePlat ? operationalDateForTimestamp(onePlat, d.getTime()) : localYmdFromMs(d.getTime());
    (byDate[dateStr] = byDate[dateStr]||[]).push({ o, km, minTot, ts: d.getTime() });
  });

  const acceptedRkm = acceptedKm>0 ? acceptedVal/acceptedKm : 0;
  const acceptedRph = acceptedMin>0 ? acceptedVal/(acceptedMin/60) : 0;
  const declRkm = declKm>0 ? declVal/declKm : 0;
  const declRph = declMin>0 ? declVal/(declMin/60) : 0;
  const better = acceptedRkm===0 || declRkm===0 ? null : declRkm < acceptedRkm;

  const todayStr = DB.today();
  const fmtSep = d => d===todayStr ? 'Hoje · '+d.split('-').reverse().slice(0,2).join('/') : d.split('-').reverse().slice(0,2).join('/')+'/'+d.slice(0,4);
  const sortedDates = Object.keys(byDate).sort().reverse();

  body.innerHTML = `
    <div class="cmp-strip">
      <div class="cmp-card g"><div class="t">Aceitas</div><div class="v">${DB.fR(acceptedRkm)}/km</div><div class="v2">${DB.fR(acceptedRph)}/h</div></div>
      <div class="cmp-card r"><div class="t">Recusadas</div><div class="v">${DB.fR(declRkm)}/km</div><div class="v2">${DB.fR(declRph)}/h</div></div>
    </div>
    ${better===null ? '' : `<div class="cmp-note">${better
      ? '<b>No geral, boa triagem:</b> o que você recusou rendia menos por km e por hora do que o que aceitou.'
      : '<b>Vale reparar:</b> algumas recusadas renderiam mais por km/hora que a média das aceitas — pode valer olhar os motivos com mais atenção.'}</div>`}
    ${sortedDates.map(d => `
      <div class="dsec">${fmtSep(d)}</div>
      ${byDate[d].sort((a,b)=>b.ts-a.ts).map(({o,km,minTot}) => {
        const plat = o.platform==='uber' ? 'Uber' : '99';
        const platCls = o.platform==='uber' ? 'uber' : 'n99';
        const rkm = km>0 ? DB.fR((o.offer_value||0)/km)+'/km' : '—';
        const hh = new Date(o.seen_at);
        const hhmm = p2(hh.getHours())+':'+p2(hh.getMinutes());
        const title = o.origin_address ? esc(extractBairro(o.origin_address)) + (o.dest_address?' → '+esc(extractBairro(o.dest_address)):'') : (plat+' — sem endereço');
        return `<div class="di-row">
          <div class="di-plat ${platCls}">${o.platform==='uber'?'U':'99'}</div>
          <div class="di-info"><h4>${title}</h4><p>${hhmm}${km>0?' · '+km.toFixed(1)+' km':''}${minTot>0?' · '+Math.round(minTot)+'m':''}</p></div>
          <div class="di-val"><strong>${DB.fR(o.offer_value||0)}</strong><span>${rkm}</span></div>
        </div>`;
      }).join('')}
    `).join('')}
  `;
}
function fmtKmComp(real, offer) {
  if (real==null && offer==null) return '—';
  let s = real!=null ? (+real).toFixed(1)+' km' : '—';
  if (offer!=null) s += ' (previsto: '+(+offer).toFixed(1)+' km)';
  return s;
}
function fmtDurComp(realMin, offerMin) {
  if (realMin==null && offerMin==null) return '—';
  let s = realMin!=null ? fmtDur(realMin) : '—';
  if (offerMin!=null) s += ' (previsto: '+fmtDur(offerMin)+')';
  return s;
}
// Extrai só o bairro de um endereço completo, pra mostrar no card da lista
// (endereço completo continua no detalhe). Formatos que a gente já viu:
// "Rua X, 123, Bairro Y" / "Loja Z, Av.
===== auto_trips @ 609350 =====
nst { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  if (error || !data) return {};
  const map = {};
  data.forEach(t => {
    const iso = t.trip_started_at || t.accepted_at;
    if (!iso) return;
    const dt = new Date(iso);
    const dateStr = dt.getFullYear()+'-'+p2(dt.getMonth()+1)+'-'+p2(dt.getDate());
    (map[dateStr] = map[dateStr] || []).push(t);
  });
  return map;
}
// Converte as automáticas de um dia (já indexadas em S.autoTripsByDate) pro
// MESMO formato de objeto que day.trips usa — value/km/time/duration/
// platform/dinheiro — assim qualquer soma/filtro que já existe funciona
// sem mudança nenhuma. NUNCA usar pra salvar (é só leitura/exibição —
// misturar isso em day.trips de verdade duplicaria dado no Supabase).
function autoTripsAsTrips(date) {
  const list = (S.autoTripsByDate && S.autoTripsByDate[date]) || [];
  return list.map(at => {
    const start = at.trip_started_at ? new Date(at.trip_started_at) : null;
    const end   = at.trip_ended_at ? new Date(at.trip_ended_at) : null;
    const durMin = start && end ? Math.max(0, Math.round((end-start)/60000))
                 : Math.round((at.offer_duration_trip_sec||0)/60);
    return {
      id: 'auto_'+at.id, _auto: true,
      platform: at.platform || 'uber',
      time: start ? p2(start.getHours())+':'+p2(start.getMinutes()) : '',
      duration: durMin,
      value: parseFloat(at.offer_value)||0,
      km: parseFloat(at.real_km_trip ?? at.offer_km_trip) || 0,
      dinheiro: !!at.dinheiro,
      origin:'', dest:'', category:''
    };
  });
}
// Todas as corridas de um dia — Importadas + Automáticas juntas, no mesmo
// formato. Uso exclusivo de leitura/agregação (gráficos, insights, somas).
function dayTripsAll(date) {
  return DB.day(date).trips.concat(autoTripsAsTrips(date));
}

// ── Corridas automaticas no Financeiro (soma por dia, cacheada em memoria) ──
// auto_trips vivem so no Supabase; o Financeiro cobre Mes/Ano, entao busca em
// lote (paginado) o intervalo do periodo + periodo anterior + mes corrente,
// agrupa por data LOCAL (fuso: margem de 1 dia em cada ponta da query).
let _finAuto = { key:'', byDate:{}, loading:false, ts:0 };
function finAutoDay(ds) { return _finAuto.byDate[ds] || { sum:0, count:0, items:[] }; }
async function loadFinAutoTrips() {
  if (!_supaUser) return;
  const today = new Date();
  const dim = new Date(today.getFullYear(), today.getMonth()+1, 0).getDate();
  const all = [...getFinDays(), ...getFinDaysPrev(),
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-01',
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-'+p2(dim)];
  let from = all[0], to = all[0];
  all.forEach(d => { if (d < from) from = d; if (d > to) to = d; });
  const key = from + '|' + to;
  if (_finAuto.loading) return;
  if (_finAuto.key === key && (Date.now() - _finAuto.ts) < 120000) return;
  _finAuto.loading = true;
  try {
    const fromIso = new Date(new Date(from+'T00:00:00').getTime() - 86400000).toISOString();
    const toIso   = new Date(new Date(to+'T23:59:59').getTime() + 86400000).toISOString();
    const rows = [];
    let off = 0;
    while (true) {
      const { data, error } = await _SUPA.from('auto_trips')
        .select('id,trip_started_at,accepted_at,offer_value,platform')
        .eq('user_id', _supaUser.id)
        .gte('trip_started_at', fromIso).lte('trip_started_at', toIso)
        .order('trip_started_at', {ascending:true})
        .range(off, off + 999);
      if (error || !data || !data.length) break;
      rows.push(...data);
      if (data.length < 1000) break;
      off += 1000;
    }
    const byDate = {};
    rows.forEach(t => {
      const ts = autoTripTs(t); if (!ts) return;
      const d = new Date(ts);
      const ds = d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
      const b = byDate[ds] || (byDate[ds] = { sum:0, count:0, items:[] });
      b.sum += (t.offer_value || 0);
      b.count++;
      b.items.push(t);
    });
    _finAuto = { key, byDate, loading:false, ts: Date.now() };
    if (S.sec === 'financeiro') { try { renderFin(); } catch(e) {} }
  } catch(e) { _finAuto.loading = false; console.warn('[loadFinAutoTrips]', e); }
}

// Corridas: mescla Importadas + Automáticas numa lista só, ordenadas por
// horário — pedido do Yuri (24/07/2026). A tag "Captura automática" (já
// existente) identifica a origem, então não precisa mais de aba pra isso.
async function renderCorridasActive() {
  const dates = getCorridasDays().slice().sort().reverse(); // mais recente primeiro
  const plats = S.corridasPlats || new Set();
  const platFilter = t => plats.size === 0 || plats.has(t.platform||'uber');

  // Automáticas do mesmo intervalo — busca uma vez só, agrupa por data local.
  let autoByDate = {};
  if (_supaUser) {
    const range = autoCorridasDateRange();
    let q = _SUPA.from('auto_trips').select('*').eq('user_id', _supaUser.id).order('trip_started_at', {ascending:false});
    if (range) q = q.gte('trip_started_at', range.from).lte('trip_started_at', range.to);
    if (plats.size === 1) q = q.eq('platform', [...plats][0]);
    const { data, error } = await q;
    (error ? [] : (data||[])).forEach(t => {
      const ts = autoTripTs(t);
      if (!ts) return;
      const d = new Date(ts);
      const onePlat = plats.size === 1 ? [...plats][0] : null;
      const dateStr = onePlat ? operationalDateForTimestamp(onePlat, ts) : localYmdFromMs(ts);
      (autoByDate[dateStr] = autoByDate[dateStr] || []).push(t);
    });
  }

  let totalVal = 0, totalKm = 0, totalN = 0, totalMin = 0;
  const groups = [];
  dates.forEach(d => {
    const day = DB.day(d);
    const manual = (day.trips||[]).filter(platFilter)
      .map(t => ({ kind:'manual', ts: t.ts || (t.time?new Date(d+'T'+t.time+':00').getTime():0), t }));
    const auto = (autoByDate[d]||[]).filter(platFilter)
      .map(t => ({ kind:'auto', ts: autoTripTs(t), t }));
    const merged = [...manual, ...auto].sort((a,b)=>b.ts-a.ts);
    if (!merged.length) return;
    merged.forEach(x => {
      if (x.kind==='manual') { totalVal += (x.t.value||0); totalKm += (x.t.km||0); totalMin += (x.t.duration||0); }
      else {
        totalVal += (x.t.offer_value||0); totalKm += (x.t.real_km_trip||0);
        if (x.t.accepted_at && x.t.trip_ended_at) totalMin += Math.max(0,(new Date(x.t.trip_ended_at)-new Date(x.t.accepted_at))/60000);
      }
      totalN++;
    });
    groups.push({ date: d, items: merged });
  });

  setText('cor-sum-val', DB.fR(totalVal));
  setText('cor-sum-km', totalKm.toLocaleString('pt-BR',{minimumFractionDigits:1,maximumFractionDigits:1}));
  setText('cor-sum-n', totalN);
  renderDeclinedOffers(dates, plats, totalVal, totalKm, totalMin);

  const listEl = document.getElementById('cor-list');
  if (!listEl) return;
  if (!groups.length) {
    listEl.innerHTML = `<div class="empty" style="text-align:center;padding:50px 20px;color:#64748B">
      <svg viewBox="0 0 24 24" width="36" height="36" fill="none" stroke="currentColor" stroke-width="1.6" style="color:#94A3B8;margin-bottom:10px"><path d="M5 17h14M6 17l1-6 3-4h4l3 4 1 6"/><circle cx="9" cy="20" r="1.4" fill="currentColor"/><circle cx="17" cy="20" r="1.4" fill="currentColor"/></svg>
      <div style="font-weight:700;font-size:14px;color:#0F172A;margin-bottom:4px">Nenhuma corrida encontrada</div>
      <div style="font-size:12.5px">Tente mudar o período ou a plataforma do filtro</div>
    </div>`;
    reapplyOpenTripDetail();
    return;
  }
  const todayStr = DB.today();
  const fmtSep = d => {
    if (d === todayStr) return 'Hoje · ' + d.split('-').reverse().slice(0,2).join('/');
    return d.split('-').reverse().slice(0,2).join('/') + '/' + d.slice(0,4);
  };
  listEl.innerHTML = groups.map(g =>
    `<div class="day-sep">${fmtSep(g.date)}</div>`
    + g.items.map(x => x.kind==='manual' ? tripItem(x.t, g.date) : autoTripItem(x.t)).join('')
  ).join('');
  reapplyOpenTripDetail();
}
function autoCorridasDateRange() {
  const dates = getCorridasDays().slice().sort();
  if (!dates.length) return null;
  return { from: dates[0]+'T00:00:00', to: dates[dates.length-1]+'T23:59:59' };
}

// Corridas recusadas/expiradas — pedido do Yuri (24/07/2026): seção
// expansível em Corridas, R$/km e R$/h somando deslocamento (até o
// embarque) + corrida, comparando com o que foi de fato aceito. Respeita
// os mesmos filtros de plataforma/período já usados na lista principal.
function toggleDeclinedOffers() {
  const body = document.getElementById('decl-body');
  const toggle = document.getElementById('decl-toggle');
  if (!body || !toggle) return;
  const open = body.style.display !== 'none';
  body.style.display = open ? 'none' : 'block';
  toggle.classList.toggle('open', !open);
}
async function renderDeclinedOffers(dates, plats, acceptedVal, acceptedKm, acceptedMin) {
  const toggle = document.getElementById('decl-toggle');
  const body = document.getElementById('decl-body');
  const subtitle = document.getElementById('decl-subtitle');
  if (!toggle || !body || !_supaUser) return;
  const range = autoCorridasDateRange();
  if (!range) { toggle.style.display = 'none'; return; }
  let q = _SUPA.from('declined_offers').select('*').eq('user_id', _supaUser.id)
    .gte('seen_at', range.from).lte('seen_at', range.to)
    .order('seen_at', { ascending:false });
  if (plats.size === 1) q = q.eq('platform', [...plats][0]);
  const { data, error } = await q;
  const list = error ? [] : (data||[]);
  if (!list.length) { toggle.style.display = 'none'; body.style.display = 'none'; return; }
  toggle.style.display = 'flex';
  setText('decl-subtitle', list.length===1 ? '1 oferta não aceita' : list.length+' ofertas não aceitas');

  let declVal = 0, declKm = 0, declMin = 0;
  const byDate = {};
  list.forEach(o => {
    const km = (o.offer_km_pickup||0) + (o.offer_km_trip||0);
    const minTot = ((o.offer_duration_pickup_sec||0) + (o.offer_duration_trip_sec||0)) / 60;
    declVal += (o.offer_value||0); declKm += km; declMin += minTot;
    const d = new Date(o.seen_at);
    const onePlat = plats.size === 1 ? [...plats][0] : null;
    const dateStr = onePlat ? operationalDateForTimestamp(onePlat, d.getTime()) : localYmdFromMs(d.getTime());
    (byDate[dateStr] = byDate[dateStr]||[]).push({ o, km, minTot, ts: d.getTime() });
  });

  const acceptedRkm = acceptedKm>0 ? acceptedVal/acceptedKm : 0;
  const acceptedRph = acceptedMin>0 ? acceptedVal/(acceptedMin/60) : 0;
  const declRkm = declKm>0 ? declVal/declKm : 0;
  const declRph = declMin>0 ? declVal/(declMin/60) : 0;
  const better = acceptedRkm===0 || declRkm===0 ? null : declRkm < acceptedRkm;

  const todayStr = DB.today();
  const fmtSep = d => d===todayStr ? 'Hoje · '+d.split('-').reverse().slice(0,2).join('/') : d.split('-').reverse().slice(0,2).join('/')+'/'+d.slice(0,4);
  const sortedDates = Object.keys(byDate).sort().reverse();

  body.innerHTML = `
    <div class="cmp-strip">
      <div class="cmp-card g"><div class="t">Aceitas</div><div class="v">${DB.fR(acceptedRkm)}/km</div><div class="v2">${DB.fR(acceptedRph)}/h</div></div>
      <div class="cmp-card r"><div class="t">Recusadas</div><div class="v">${DB.fR(declRkm)}/km</div><div class="v2">${DB.fR(declRph)}/h</div></div>
    </div>
    ${better===null ? '' : `<div class="cmp-note">${better
      ? '<b>No geral, boa triagem:</b> o que você recusou rendia menos por km e por hora do que o que aceitou.'
      : '<b>Vale reparar:</b> algumas recusadas renderiam mais por km/hora que a média das aceitas — pode valer olhar os motivos com mais atenção.'}</div>`}
    ${sortedDates.map(d => `
      <div class="dsec">${fmtSep(d)}</div>
      ${byDate[d].sort((a,b)=>b.ts-a.ts).map(({o,km,minTot}) => {
        const plat = o.platform==='uber' ? 'Uber' : '99';
        const platCls = o.platform==='uber' ? 'uber' : 'n99';
        const rkm = km>0 ? DB.fR((o.offer_value||0)/km)+'/km' : '—';
        const hh = new Date(o.seen_at);
        const hhmm = p2(hh.getHours())+':'+p2(hh.getMinutes());
        const title = o.origin_address ? esc(extractBairro(o.origin_address)) + (o.dest_address?' → '+esc(extractBairro(o.dest_address)):'') : (plat+' — sem endereço');
        return `<div class="di-row">
          <div class="di-plat ${platCls}">${o.platform==='uber'?'U':'99'}</div>
          <div class="di-info"><h4>${title}</h4><p>${hhmm}${km>0?' · '+km.toFixed(1)+' km':''}${minTot>0?' · '+Math.round(minTot)+'m':''}</p></div>
          <div class="di-val"><strong>${DB.fR(o.offer_value||0)}</strong><span>${rkm}</span></div>
        </div>`;
      }).join('')}
    `).join('')}
  `;
}
function fmtKmComp(real, offer) {
  if (real==null && offer==null) return '—';
  let s = real!=null ? (+real).toFixed(1)+' km' : '—';
  if (offer!=null) s += ' (previsto: '+(+offer).toFixed(1)+' km)';
  return s;
}
function fmtDurComp(realMin, offerMin) {
  if (realMin==null && offerMin==null) return '—';
  let s = realMin!=null ? fmtDur(realMin) : '—';
  if (offerMin!=null) s += ' (previsto: '+fmtDur(offerMin)+')';
  return s;
}
// Extrai só o bairro de um endereço completo, pra mostrar no card da lista
// (endereço completo continua no detalhe). Formatos que a gente já viu:
// "Rua X, 123, Bairro Y" / "Loja Z, Av. W, 123 - Bairro Y" / "Rua X, 123,
// Bairro Y, Teresópoli" (com cidade grudada no fim).
function extractBairro(addr) {
  if (!addr) return '';
  let s = String(addr).trim();
  s = s.replace(/,?\s*\d{5}-?\d{3}.*$/i, '');           // CEP e o que vier depois
  s = s.replace(/,?\s*Brasil\s*$/i, '');                 // país no fim
  s = s.replace(/,?\s*-?\s*RJ\s*$/i, '');                // UF no fim
  s = s.replace(/,\s*Teresópolis?\s*$/i, '');            // cidade grudada no fim
  const dashIdx = s.lastIndexOf(' - ');
  if (dashIdx !== -1) return s.slice(dashIdx + 3).trim() || s.trim();
  const parts = s.split(',').map(p => p.trim()).filter(Boolean);
  for (let i = parts.length - 1; i >= 0; i--) {
    if (!/^\d+$/.test(parts[i])) return parts[i];
  }
  return parts[parts.length - 1] || s;
}
function autoTripItem(t) {
  const dest = t.dest_address ? ' → '+esc(extractBairro(t.dest_address)) : '';
  const tid = 'ta'+Date.now().toString(36)+Math.random().toString(36).slice(2,8);
  const cashSvg = `<svg viewBox="0 0 24 24" width="13" height="13" fill="currentColor"><rect x="2" y="7" width="20" height="13" rx="2"/><path d="M2 10h20" stroke="white" stroke-width="1.2" fill="none"/><path d="M2 17h20" stroke="white" stroke-width="1.2" fill="none"/><circle cx="12" cy="13" r="2.5" fill="white"/></svg>`;
  const sourceTag = t.capture_source === 'importada' ? `<span class="chip chip-import">Importada</span>` : `<span class="chip chip-auto">Captura automática</span>`;
  const extras = [sourceTag];
  if (t.dinheiro) extras.push(`<span class="tap-legend-wrap chip-icon chip-cash" onclick="showTapLegend(event,'Pago em dinheiro')">${cashSvg}</spa
===== auto_trips @ 642119 =====
xt = 'display:none;align-items:flex-start;justify-content:space-between;gap:10px;background:#FFF7ED;border:1px solid #FDBA74;border-radius:14px;padding:11px 12px;margin:0 0 10px;cursor:pointer';
    permWarn.innerHTML = '<div style="min-width:0"><div style="font-size:12.5px;font-weight:800;color:#9A3412">⚠ Funções do MōB incompletas</div><div id="journey-perm-warning-text" style="font-size:11px;color:#C2410C;line-height:1.35;margin-top:2px"></div></div><span style="font-size:11px;font-weight:800;color:#9A3412;white-space:nowrap">Ativar ›</span>';
    jc.parentNode.insertBefore(permWarn, jc);
  }
  renderFlashPermissionWarnings();
  const cardDate = day?.date || S.curDate || DB.today();
  // Card de sessão atual (timer/km/botão) só faz sentido em "hoje" — em dia
  // passado não existe jornada "em andamento" possível, então esse card
  // some por completo. O restante da função continua rodando igual: os
  // valores que ela calcula (jc-trips, jc-total, jc-kmtotal, jc-kmativo,
  // jc-kmdesl, jc-rsh, jc-rskm) agora moram no card "Total do dia" — que é
  // um elemento IRMÃO de journey-card, não um filho, e precisa continuar
  // sendo populado em qualquer dia, passado ou presente.
  const jcWrap = document.getElementById('journey-wrap');
  if (jcWrap) jcWrap.style.display = isToday ? '' : 'none';
  const g = S.gps;
  const finished = day.sessions || [];
  const finishedElapsed = finished.reduce((s,x)=>s+(x.elapsed||0),0);
  const finishedKm = finished.reduce((s,x)=>s+(x.km||0),0);
  const live = isToday && g.st === 'running';
  const liveElapsed = live ? getElapsed() : 0;
  const liveKm = live ? g.km : 0;
  const totalElapsed = Math.max(0, finishedElapsed + liveElapsed + (day.tempoAdjust || 0));
  const totalKm = finishedKm + liveKm + (day.kmAdjust || 0);
  const state = isToday ? g.st : 'idle';

  let cls = 'idle';
  if (state === 'running') cls = 'running';
  else if (finished.length > 0) cls = 'ended';
  jc.className = 'journey ' + cls;

  setText('jc-status-text',
    state === 'running' ? 'Online' :
    finished.length > 0 ? 'Concluído' : 'Iniciar'
  );
  const th = Math.floor(liveElapsed/3600), tm = Math.floor(liveElapsed%3600/60), ts = liveElapsed%60;
  setText('jc-timer', p2(th)+':'+p2(tm)+':'+p2(ts));
  setText('jc-sub',
    state === 'running' ? 'Sessão atual · iniciada '+fmtHM(g.start) :
    finished.length > 0 ? finished.length+(finished.length===1?' jornada hoje':' jornadas hoje') :
    'Nenhuma sessão iniciada'
  );
  setText('jc-km', liveKm.toFixed(1));
  const tth = Math.floor(totalElapsed/3600), ttm = Math.floor(totalElapsed%3600/60);
  setText('jc-total', tth+'h'+p2(ttm)+'m');
  setText('jc-kmtotal', totalKm.toFixed(1)+' km');
  // CORRIGIDO (23/07/2026, confirmado com dado real pelo Yuri): esses 4
  // KPIs do card principal (corridas, km ativo, R$/h, R$/km) só somavam
  // day.trips (Importadas) — sessão só com automáticas mostrava tudo
  // zerado mesmo com corridas de verdade rodando. Mesma correção que já
  // tinha sido feita em rev/sessionItem/renderMetasDoDia, faltava aqui.
  const autoTripsHoje = day.autoTrips || [];

  // Banner "aguardando confirmação" — pedido do Yuri (16/08/2026), substitui
  // o botão fixo "Importar corridas". Conta corridas que ainda não foram
  // confirmadas por vídeo do histórico (status 'capturada' ou 'estimada') —
  // 'confirmada' só existe depois do vídeo, então isso mede exatamente o
  // que falta conferir naquele dia. Aparece em qualquer dia navegado, some
  // sozinho quando não sobra pendência.
  const pcbEl = document.getElementById('pending-confirm-banner');
  if (pcbEl) {
    // Não interpreta cache ainda carregando como zero corridas. Durante refresh,
    // mantém o último estado visual válido do mesmo dia.
    const autoTripsReady = S._autoTripsDataDate === cardDate;
    if (autoTripsReady) {
      const pendingCount = autoTripsHoje.filter(t => t.status !== 'confirmada' || t.value_needs_review === true).length;
      pcbEl.dataset.dataDate = cardDate;
      if (pendingCount > 0) {
        pcbEl.style.display = 'flex';
        setText('pcb-title', pendingCount + (pendingCount===1 ? ' corrida aguardando confirmação' : ' corridas aguardando confirmação'));
      } else {
        pcbEl.style.display = 'none';
      }
    } else if (pcbEl.dataset.dataDate && pcbEl.dataset.dataDate !== cardDate) {
      pcbEl.style.display = 'none';
      pcbEl.dataset.dataDate = cardDate;
    }
  }

  // Barra Online/Buscando passageiro/Em corrida — real, vinda da máquina de
  // estados do APK (ver computeStatusBreakdown). No card da jornada ATUAL
  // só faz sentido enquanto a sessão está rodando (é o que está sendo
  // contado agora); ao encerrar, o dado sai daqui e já está consolidado no
  // "Total do dia" logo abaixo — pedido do Yuri (16/08/2026), evita
  // duplicar/deixar a mesma barra "grudada" depois de já ter encerrado.
  // As barras são atualizadas exclusivamente pela timeline contínua V3.
  // A rotina base não sobrescreve mais a leitura real com auto_trips.
  const jcBd = document.getElementById('jc-breakdown');
  const tdBdRow = document.getElementById('td-bd-row');

  setText('jc-trips', day.trips.length + autoTripsHoje.length);
  setText('jc-trips2', day.trips.length + autoTripsHoje.length);
  const aceitasCount = day.trips.length + autoTripsHoje.length;
  const declinedCount = (S.declined || []).length;
  setText('jc-aceitas', aceitasCount);
  setText('jc-recusadas', declinedCount);
  const corridasSubEl = document.getElementById('jc-corridas-sub');
  if (corridasSubEl) corridasSubEl.style.display = (aceitasCount>0 || declinedCount>0) ? 'flex' : 'none';
  const kmAtivo = day.trips.reduce((s,t)=>s+(t.km||0),0)
                + autoTripsHoje.reduce((s,t)=>s+(parseFloat(t.real_km_trip ?? t.offer_km_trip)||0),0);
  const kmDesl = Math.max(0, totalKm - kmAtivo);
  setText('jc-kmativo', kmAtivo.toFixed(1));
  setText('jc-kmdesl', kmDesl.toFixed(1));
  const ganhosDia = day.trips.reduce((s,t)=>s+(t.value||0),0)
                  + autoTripsHoje.reduce((s,t)=>s+(t.offer_value||0),0);
  const horasDecimal = totalElapsed > 0 ? totalElapsed/3600 : 0;
  const rsh  = horasDecimal > 0 ? ganhosDia/horasDecimal : null;
  const rskm = totalKm > 0 ? ganhosDia/totalKm : null;
  setText('jc-rsh', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  setText('jc-rsh2', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm2', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  // Cor por indicador — pedido do Yuri (23/07/2026, mockup aprovado):
  // mesmos limiares vermelho/verde que o motorista já ajusta na régua do
  // MōB Flash (Indicadores → R$/km e R$/hora), cada indicador troca de cor
  // por conta própria. Sem dado ainda (sessão não rodou tempo/km) fica
  // neutro (verde), não faz sentido alarmar antes de ter dado de verdade.
  const rateEl = document.getElementById('jc-rate-hora');
  const kmEl   = document.getElementById('jc-rate-km');
  const rateEl2 = document.getElementById('jc-rate-hora2');
  const kmEl2   = document.getElementById('jc-rate-km2');
  const rateCls = rsh !== null ? rateColorClass('rhora', rsh) : '';
  const kmCls   = rskm !== null ? rateColorClass('rkm', rskm) : '';
  if (rateEl)  rateEl.className  = 'td-rcard ' + rateCls;
  if (kmEl)    kmEl.className    = 'td-rcard ' + kmCls;
  if (rateEl2) rateEl2.className = 'rate-cell ' + rateCls;
  if (kmEl2)   kmEl2.className   = 'rate-cell ' + kmCls;

  const actions = document.getElementById('jc-actions');
  if (!actions) return;
  if (!isToday) {
    actions.innerHTML = '';
  } else if (state === 'idle') {
    actions.innerHTML = `<button class="j-round-btn play" onclick="gpsStart()" aria-label="Iniciar jornada"><svg viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg></button>`;
  } else if (state === 'running') {
    actions.innerHTML = '<button class="j-round-btn stop" onclick="confirmStop()" aria-label="Encerrar jornada"><svg viewBox="0 0 24 24" fill="currentColor"><rect x="6" y="6" width="12" height="12"/></svg></button>';
  }
}

function agMinutesBetween(start, end) {
  if (!start || !end) return 0;
  const [h1,m1] = start.split(':').map(Number);
  const [h2,m2] = end.split(':').map(Number);
  return Math.max(0, (h2*60+m2) - (h1*60+m1));
}
function openMetasHojeModal() {
  const date = S.curDate || DB.today();
  const day = DB.day(date);
  const dow = new Date(date+'T12:00').getDay();
  const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
  const meta = DB.meta();
  const ov = day.metaOverride || null;
  const dimMes = new Date(new Date(date+'T12:00').getFullYear(), new Date(date+'T12:00').getMonth()+1, 0).getDate();
  const moneyGoal = (ov && ov.money) ? ov.money : ((plan && plan.moneyGoal) ? plan.moneyGoal : (meta.general ? meta.general/dimMes : 0));
  const kmGoal = (ov && ov.km) ? ov.km : (plan && plan.kmGoal ? plan.kmGoal : 0);
  const schedMin = (ov && ov.hours) ? ov.hours*60 : (plan ? (plan.schedules||[]).reduce((s,b)=>s+agMinutesBetween(b.start,b.end),0) : 0);
  const mEl = document.getElementById('mh-money'); if (mEl) mEl.value = moneyGoal ? moneyGoal.toFixed(2) : '';
  const kEl = document.getElementById('mh-km'); if (kEl) kEl.value = kmGoal || '';
  const hEl = document.getElementById('mh-hours'); if (hEl) hEl.value = schedMin ? (schedMin/60).toFixed(1) : '';
  const subEl = document.getElementById('mh-sub');
  if (subEl) {
    const isToday = date === DB.today();
    subEl.textContent = isToday ? 'Vale só para hoje' : ('Vale só para ' + new Date(date+'T12:00').toLocaleDateString('pt-BR',{weekday:'long',day:'numeric',month:'long'}));
  }
  openMo('mo-metas-hoje');
}
function saveMetasHoje() {
  const date = S.curDate || DB.today();
  const day = DB.day(date);
  const money = parseFloat((document.getElementById('mh-money')||{}).value) || 0;
  const km = parseFloat((document.getElementById('mh-km')||{}).value) || 0;
  const hours = parseFloat((document.getElementById('mh-hours')||{}).value) || 0;
  day.metaOverride = { money, km, hours };
  DB.saveDay(day);
  closeMo('mo-metas-hoje');
  try { renderMetasDoDia(day); } catch(e) {}
  showToast('Metas de hoje atualizadas!', 'success');
}
function renderMetasDoDia(day) {
  const stack = document.getElementById('metas-dia-stack');
  if (!stack) return;
  const rev = day.trips.reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0)
            + (day.autoTrips||[]).reduce((s,t)=>s+(t.offer_value||0),0);
  const isToday = day.date === DB.today();
  const liveOn = isToday && S.gps.st==='running';
  const totalKm = (day.sessions||[]).reduce((s,x)=>s+(x.km||0),0) + (liveOn ? S.gps.km : 0) + (day.kmAdjust || 0);
  const totalElapsed = Math.max(0, (day.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0) + (liveOn ? getElapsed() : 0) + (day.tempoAdjust || 0));
  const dow = new Date(day.date+'T12:00').getDay();
  const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
  const meta = DB.meta();
  const ov = day.metaOverride || null; // ajuste específico deste dia (via modal 'Metas de hoje')
  const dimMes = new Date(new Date(day.date+'T12:00').getFullYear(), new Date(day.date+'T12:00').getMonth()+1, 0).getDate();
  const moneyGoal = (ov && ov.money) ? ov.money : ((plan && plan.moneyGoal) ? plan.moneyGoal : (meta.general ? meta.general/dimMes : 0));

  const ICO_MONEY = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>';
  const ICO_KM = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76"/></svg>';
  const ICO_TIME = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>';

  let html = '';
  if (moneyGoal) {
    const pct = Math.min(100, Math.round(rev/moneyGoal*100));
    html += `<div class="goal-item"><div class="g-ico money">${ICO_MONEY}</div><div class="goal-body"><div class="goal-top"><strong>Financeira</strong><span>${DB.fRI(rev)} de ${DB.fRI(moneyGoal)} · ${pct}%</span></div><div class="gbar money"><i style="width:${pct}%"></i></div></div></div>`;
  } else {
    html += `<div class="goal-item"><div class="g-ico money">${ICO_MONEY}</div><div class="goal-locked" onclick="openPlanJornada()"><span>Financeira — meta não definida</span><b>Planejar</b></div></div>`;
  }
  const kmGoal = (ov && ov.km) ? ov.km : (plan && plan.kmGoal ? plan.kmGoal : 0);
  if (kmGoal) {
    const pct = Math.min(100, Math.round(totalKm/kmGoal*100));
    html += `<div class="goal-item"><div class="g-ico km">${ICO_KM}</div><div class="goal-body"><div class="goal-top"><strong>Km</strong><span>${totalKm.toFixed(1)} de ${kmGoal} km · ${pct}%</span></div><div class="gbar km"><i style="width:${pct}%"></i></div></div></div>`;
  } else {
    html += `<div class="goal-item"><div class="g-ico km">${ICO_KM}</div><div class="goal-locked" onclick="openPlanJornada()"><span>Km — meta não definida</span><b>Planejar</b></div></div>`;
  }
  const schedMin = (ov && ov.hours) ? ov.hours*60 : (plan ? (plan.schedules||[]).reduce((s,b)=>s+agMinutesBetween(b.start,b.end),0) : 0);
  if (schedMin > 0) {
    const liveMin = Math.round(totalElapsed/60);
    const pct = Math.min(100, Math.round(liveMin/schedMin*100));
    html += `<div class="goal-item"><div class="g-ico time">${ICO_TIME}</div><div class="goal-body"><div class="goal-top"><strong>Tempo online</strong><span>${Math.floor(liveMin/60)}h${p2(liveMin%60)} de ${Math.floor(schedMin/60)}h${p2(schedMin%60)} · ${pct}%</span></div><div class="gbar time"><i style="width:${pct}%"></i></div></div></div>`;
  } else {
    html += `<div class="goal-item"><div class="g-ico time">${ICO_TIME}</div><div class="goal-locked" onclick="openPlanJornada()"><span>Tempo online — meta não definida</span><b>Planejar</b></div></div>`;
  }
  stack.innerHTML = html;
}

// ── Planejar Jornada ─────────────────────────────────────────
let pjSelDays = [];
let pjSchedules = [];
const PJ_DOW = ['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'];
function openPlanJornada() {
  pjSelDays = []; pjSchedules = [];
  document.querySelectorAll('.pj-day').forEach(b => b.classList.remove('on'));
  const mEl = document.getElementById('pj-money'); if (mEl) mEl.value = '';
  const kEl = document.getElementById('pj-km'); if (kEl) kEl.value = '';
  renderPjSchedules();
  renderPjList();
  navTo('planejar-jornada');
}
function pjToggleDay(d) {
  const i = pjSelDays.indexOf(d);
  if (i === -1) pjSelDays.push(d); else pjSelDays.splice(i,1);
  const btn = document.querySelector('.pj-day[data-d="'+d+'"]');
  if (btn) btn.classList.toggle('on', pjSelDays.includes(d));
}
function p
===== auto_trips @ 769847 =====
iesId || DB.uid()) : it.seriesId,
      status: _lancStatusSel || it.status || 'pago'
    });
    if (date !== oldDate) {
      oldDay[listKey].splice(idx, 1);
      DB.saveDay(oldDay);
      const newDay = DB.day(date);
      if (!newDay[listKey]) newDay[listKey] = [];
      newDay[listKey].push(it);
      DB.saveDay(newDay);
    } else {
      DB.saveDay(oldDay);
    }
    SYNC.push(listKey, {
      id: it.id, date: it.date, category: it.category, category_label: it.categoryLabel,
      amount: it.amount, description: it.desc, scope: it.scope,
      recorrente: it.recorrente, freq: it.freq, series_id: it.seriesId,
      status: it.status, due_date: it.date || null
    });
    _lancResetEdit();
    closeMo('mo-lancamento');
    document.getElementById('l-valor').value = '';
    document.getElementById('l-desc').value = '';
    showToast('Alterações salvas');
    setTimeout(() => {
      try { renderJornada(); } catch(e) {}
      try { renderHome(); } catch(e) {}
      try { renderFin(); } catch(e) {}
      try { renderLancamentos(); } catch(e) {}
    }, 60);
    return;
  }

  const day  = DB.day(date);
  const entry = {
    id: DB.uid(), category: cat?.id || '', categoryLabel: cat?.l || '',
    desc: document.getElementById('l-desc')?.value || '',
    amount: val, date, scope: S.lancScope,
    recorrente: lancRecOn, freq: lancFreqSel,
    seriesId: lancRecOn ? DB.uid() : null,
    status: _lancStatusSel || (S.lancType==='receita'?'pago':'a_pagar')
  };
  if (S.lancType === 'receita') day.revenues.push(entry);
  else day.expenses.push(entry);
  DB.saveDay(day);
  SYNC.push(S.lancType === 'receita' ? 'revenues' : 'expenses', {
    id: entry.id, date: entry.date, category: entry.category, category_label: entry.categoryLabel,
    amount: entry.amount, description: entry.desc, scope: entry.scope,
    recorrente: entry.recorrente, freq: entry.freq, series_id: entry.seriesId,
    status: entry.status || (S.lancType==='receita'?'pago':'a_pagar'),
    due_date: entry.date || null
  });
  closeMo('mo-lancamento');
  document.getElementById('l-valor').value = '';
  document.getElementById('l-desc').value = '';
  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
    try { renderFin(); } catch(e) {}
    try { if (typeof renderLancamentos === 'function') renderLancamentos(); } catch(e) {}
  }, 80);
}

// ── CORRIDA FORM ──────────────────────────────────────────────
function setCorPlat(p) {
  S.corPlat = p;
  ['uber','99','particular'].forEach(x => {
    const el = document.getElementById('cp-'+x); if (el) el.classList.toggle('active', x === p);
  });
}
function saveCorrida() {
  const val = parseFloat(document.getElementById('c-val')?.value || 0);
  if (!val) { showToast('Informe o valor', 'error'); return; }
  const day = DB.day(S.curDate);
  const trip = {
    id: DB.uid(), platform: S.corPlat,
    time:     document.getElementById('c-time')?.value || '',
    duration: parseInt(document.getElementById('c-dur')?.value) || 0,
    origin:   document.getElementById('c-orig')?.value || '',
    dest:     document.getElementById('c-dest')?.value || '',
    value: val, km: parseFloat(document.getElementById('c-km')?.value) || 0
  };
  day.trips.push(trip);
  DB.saveDay(day);
  SYNC.push('trips', { id: trip.id, date: S.curDate, platform: trip.platform, time: trip.time || null,
    duration: trip.duration, origin: trip.origin, dest: trip.dest, value: trip.value, km: trip.km,
    category: '', surge: false, airport: false, source: 'manual' });
  closeMo('mo-corrida');
  ['c-val','c-km','c-orig','c-dest','c-dur'].forEach(id => { const el=document.getElementById(id); if(el)el.value=''; });
  renderJornada();
}

// ── KM FORM ───────────────────────────────────────────────────
function saveKm() {
  const v = parseFloat(document.getElementById('km-inp')?.value);
  if (!v) { showToast('Informe o valor', 'error'); return; }
  const day = DB.day(S.curDate); day.kmTotal = v; DB.saveDay(day);
  closeMo('mo-km'); renderJornada();
}

// ── AJUSTE MANUAL DE KM (corrige imprecisão do GPS) ─────────────
let kmAdjBase = 0;
// ── Editar valor de uma corrida automática direto no card (23/07/2026,
// mockup aprovado) — ao salvar, vira Confirmada (agora tem um valor que o
// próprio motorista conferiu, mais confiável que a oferta estimada).
let autoTripValEditId = null;
function openAutoTripValueEdit(id, currentValue) {
  autoTripValEditId = id;
  const inp = document.getElementById('autotripval-val');
  if (inp) inp.value = (currentValue||0).toFixed(2);
  openMo('mo-autotrip-value');
}
async function saveAutoTripValueEdit() {
  const inp = document.getElementById('autotripval-val');
  const newVal = parseFloat(inp?.value);
  if (isNaN(newVal) || newVal < 0) { showToast('Valor inválido', 'error'); return; }
  if (!autoTripValEditId) { closeMo('mo-autotrip-value'); return; }
  if (!_supaUser) { showToast('Sem conexão com a nuvem.', 'error'); return; }
  try {
    const { error } = await _SUPA.from('auto_trips')
      .update({ offer_value: newVal, status: 'confirmada' })
      .eq('id', autoTripValEditId);
    if (error) throw error;
    closeMo('mo-autotrip-value');
    showToast('Valor atualizado.');
    // Cache de automáticas fica potencialmente desatualizado em 3 telas —
    // força recarregar todas que dependem dela, só a que está ativa
    // agora re-renderiza na hora (mesmo padrão do refresh de 60s/25s).
    S.autoTripsDate = null; S.autoTripsHomeRange = null;
    if (S.sec === 'jornada') renderJornada();
    if (S.sec === 'relatorios') renderHomeGraphs();
    if (S.sec === 'corridas') renderCorridasActive();
  } catch (e) {
    console.error('saveAutoTripValueEdit', e);
    showToast('Erro ao salvar. Tente de novo.', 'error');
  }
}
function openKmAdjust() {
  const day = DB.day(S.curDate);
  const isToday = S.curDate === DB.today();
  const live = isToday && S.gps.st === 'running';
  const liveKm = live ? S.gps.km : 0;
  const finishedKm = (day.sessions || []).reduce((s,x)=>s+(x.km||0),0);
  kmAdjBase = finishedKm + liveKm;
  const current = kmAdjBase + (day.kmAdjust || 0);
  const inp = document.getElementById('kmadj-val');
  if (inp) inp.value = current.toFixed(1);
  openMo('mo-km-adjust');
}
function kmAdjStep(delta) {
  const inp = document.getElementById('kmadj-val');
  if (!inp) return;
  const v = Math.max(0, (parseFloat(inp.value) || 0) + delta);
  inp.value = v.toFixed(1);
}
function saveKmAdjust() {
  const inp = document.getElementById('kmadj-val');
  const newTotal = parseFloat(inp?.value);
  if (isNaN(newTotal) || newTotal < 0) { showToast('Valor inválido', 'error'); return; }
  const day = DB.day(S.curDate);
  day.kmAdjust = parseFloat((newTotal - kmAdjBase).toFixed(2));
  DB.saveDay(day);
  SYNC.push('day_meta', { date: S.curDate, km_adjust: day.kmAdjust, km_total_legacy: day.kmTotal ?? null });
  closeMo('mo-km-adjust');
  renderJornada();
  showToast('Km total ajustado.');
}

// ── Ajuste de tempo total (mesma lógica do km: botões nas laterais + campo editável por texto) ──
let tempoAdjBaseSecs = 0, tempoAdjCurSecs = 0;
function openTempoAdjust() {
  const day = DB.day(S.curDate);
  const isToday = S.curDate === DB.today();
  const live = isToday && S.gps.st === 'running';
  const liveElapsed = live ? getElapsed() : 0;
  const finishedElapsed = (day.sessions || []).reduce((s,x)=>s+(x.elapsed||0),0);
  tempoAdjBaseSecs = finishedElapsed + liveElapsed;
  tempoAdjCurSecs = Math.max(0, tempoAdjBaseSecs + (day.tempoAdjust || 0));
  updateTempoAdjDisplay();
  openMo('mo-tempo-adjust');
}
// Aceita "2h30m", "2h 30m", "2h", "45m" ou número puro (minutos)
function parseTempoAdjInput(text) {
  text = (text || '').trim().toLowerCase().replace(/\s+/g, '');
  if (!text) return null;
  let h = 0, m = 0;
  const full = text.match(/^(\d+)h(\d+)?m?$/);
  const justH = text.match(/^(\d+)h$/);
  const justM = text.match(/^(\d+)m$/);
  const justNum = text.match(/^(\d+)$/);
  if (full) { h = parseInt(full[1]); m = parseInt(full[2] || 0); }
  else if (justH) { h = parseInt(justH[1]); }
  else if (justM) { m = parseInt(justM[1]); }
  else if (justNum) { m = parseInt(justNum[1]); }
  else return null;
  return Math.max(0, h*3600 + m*60);
}
function updateTempoAdjDisplay() {
  const inp = document.getElementById('tempoadj-val');
  if (inp) inp.value = fmtDur(tempoAdjCurSecs / 60);
}
function tempoAdjStep(deltaMin) {
  tempoAdjCurSecs = Math.max(0, tempoAdjCurSecs + deltaMin * 60);
  updateTempoAdjDisplay();
}
function tempoAdjInputCommit() {
  const inp = document.getElementById('tempoadj-val');
  if (!inp) return;
  const parsed = parseTempoAdjInput(inp.value);
  if (parsed === null) { updateTempoAdjDisplay(); return; }
  tempoAdjCurSecs = parsed;
  updateTempoAdjDisplay();
}
function saveTempoAdjust() {
  tempoAdjInputCommit();
  const day = DB.day(S.curDate);
  day.tempoAdjust = Math.round(tempoAdjCurSecs - tempoAdjBaseSecs);
  DB.saveDay(day);
  SYNC.push('day_meta', { date: S.curDate, tempo_adjust_secs: day.tempoAdjust });
  closeMo('mo-tempo-adjust');
  renderJornada();
  showToast('Tempo total ajustado.');
}

// ── IMPORT AI ─────────────────────────────────────────────────
// Detecta vídeo por MIME type, com fallback por extensão do nome do
// arquivo — em alguns celulares (Live Photo/Motion Photo, ou o picker de
// Arquivos) o navegador reporta file.type vazio ou incorreto, e o print
// (imagem) acabava sendo classificado errado e descartado.
function isVideoFile(f) {
  if (f.type) return f.type.startsWith('video/');
  return /\.(mp4|mov|webm|mkv|avi|m4v|3gp)$/i.test(f.name||'');
}
function handleImport(input, plat) {
  if (!input.files.length) return;
  const files = Array.from(input.files);
  closeMo('mo-import');
  const videos = files.filter(isVideoFile);
  const images = files.filter(f => !isVideoFile(f));
  if (!videos.length && !images.length) return;
  processImportBatch(images, videos, plat);
}
// Processa imagens E vídeos da mesma seleção juntos — antes, se um vídeo
// viesse junto (ex: Live Photo confundida com foto), TODAS as imagens da
// seleção eram silenciosamente descartadas (só processava videos[0]).
let importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };

function openImportPrecheck() {
  ['ipc-uber-count','ipc-uber-value','ipc-99-count','ipc-99-value'].forEach(id => {
    const el = document.getElementById(id); if (el) el.value = '';
  });
  openMo('mo-import-precheck');
}
function _readPrecheckField(id) {
  const v = document.getElementById(id)?.value;
  return (v === '' || v === undefined || v === null) ? null : parseFloat(String(v).replace(',','.'));
}
function importPrecheckNext() {
  importPrecheck = {
    uberCount: _readPrecheckField('ipc-uber-count'),
    uberValue: _readPrecheckField('ipc-uber-value'),
    p99Count:  _readPrecheckField('ipc-99-count'),
    p99Value:  _readPrecheckField('ipc-99-value')
  };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}
function importPrecheckSkip() {
  importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}

function renderImportRing(found, target, foundVal, targetVal, statusHtml, isWarn) {
  const hasTarget = target > 0;
  const pct = hasTarget ? Math.min(100, found/target*100) : 0;
  const deg = Math.round(pct*3.6);
  const ringColor = isWarn ? '#F59E0B' : '#22C55E';
  const ringBg = hasTarget ? `conic-gradient(${ringColor} 0deg ${deg}deg, #E2E8F0 ${deg}deg 360deg)` : '#E2E8F0';
  const countHtml = hasTarget ? `${found}<span style="font-size:16px;color:#94A3B8">/${target}</span>` : `${found}`;
  const valHtml = hasTarget ? `${DB.fRI(foundVal)} de ${DB.fRI(targetVal)} já identificados` : `${DB.fRI(foundVal)} identificados até agora`;
  return `<div style="display:flex;flex-direction:column;align-items:center;padding:30px 0 10px">
    <div style="width:130px;height:130px;border-radius:50%;background:${ringBg};display:flex;align-items:center;justify-content:center;margin-bottom:20px;position:relative;transition:background .4s">
      <div style="position:absolute;inset:8px;border-radius:50%;background:#fff"></div>
      <div style="position:relative;text-align:center">
        <div style="font-family:'Poppins',sans-serif;font-weight:900;font-size:28px;color:#0F172A">${countHtml}</div>
        <div style="font-size:10px;color:#64748B;font-weight:700;margin-top:2px">CORRIDAS</div>
      </div>
    </div>
    <div style="font-size:13.5px;font-weight:700;color:#0F172A;margin-bottom:5px;display:flex;align-items:center;gap:6px">${statusHtml}</div>
    <div style="font-size:12px;color:#64748B">${valHtml}</div>
  </div>`;
}

// Tenta de novo (até 3x) antes de desistir de um print/frame — evita que um
// erro passageiro de rede derrube a corrida inteira do import silenciosamente.
async function extractWithRetry(fn, onRetry, maxAttempts) {
  maxAttempts = maxAttempts || 3;
  let lastErr;
  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    try { return await fn(); }
    catch(e) {
      lastErr = e;
      if (attempt < maxAttempts) { onRetry(attempt+1, maxAttempts); await new Promise(r => setTimeout(r, 800)); }
    }
  }
  throw lastErr;
}

// Sobe o vídeo original pro Storage (bucket import-videos, retenção de
// 180 dias) e registra em import_videos — pedido do Yuri (16/08/2026):
// base pra confirmação de valor real (campeonatos/incentivos futuros) e
// pra dar dá pra conferir manualmente qualquer corrida em disputa. Falha
// de upload NUNCA trava o import (retorna null, segue sem vínculo) — o
// vídeo é auditoria extra, não pode ser bloqueio de fluxo.
async function uploadImportVideo(file, plat, operationalDate) {
  if (!_supaUser) return null;
  try {
    const ext = (file.name && file.name.includes('.')) ? file.name.split('.').pop().toLowerCase() : 'mp4';
    const opDate = operationalDate || S.curDate || DB.today();
    const opPlat = normalizeOperationalPlatform(plat);
    const opRange = operationalRangeForPlatform(opPlat, opDate);
    const path = `${_supaUser.id}/${opDate}/${opPlat}/${DB.uid()}.${ext}`;
    const { error: upErr } = await _SUPA.storage.from('import-videos').upload(path, file, {
      contentType: file.type || 'video/mp4'
    });
    if (upErr) { console.warn('upload vídeo falhou (segue sem vínculo):', upErr); return null; }
    const { data, error } = await _SUPA.from('import_videos').insert({
      user_id: _supaUser.id, storage_path: path, file_size_bytes: file.size || null,
      platform: opPlat, operational_date: opDate,
      period_start: new Date(opRange.startMs).toISOString(),
      period_end: new Date(opRange.endMs).toISOString()
    }).select('id').single();
    if (error) { console.warn('registro import_videos falhou (segue sem vínculo):', error); return null; }
    return data.id;
  } catch(e) { console.warn('uploadImportVideo erro:', e); return null; }
}
async function processImportBatch(images, videos, plat) {
  const operationalDate = S.curDate || DB.
===== auto_trips @ 789426 =====
ot, false);
  }
  let videoFrameReadFailed = false;
  for (let v = 0; v < videos.length; v++) {
    try {
      updateRing('Preparando vídeo ' + dot, false);
      const frames = await extractVideoFrames(videos[v], (done, total) => {
        updateRing('Preparando vídeo · ' + done + '/' + total + ' quadros ' + dot, false);
      });
      for (let i = 0; i < frames.length; i++) {
        try {
          const trips = await extractWithRetry(
            () => extractTripsFromBase64(frames[i], 'image/jpeg', plat),
            () => updateRing(retryIcon + ' Processando novamente...', true)
          );
          absorb(trips);
        } catch(e) { console.warn('Frame erro (desistiu após 3 tentativas):', e); }
        updateRing('Analisando vídeo · ' + (i+1) + '/' + frames.length + ' quadros ' + dot, false);
      }
    } catch(e) { videoFrameReadFailed = true; console.warn('Erro vídeo '+(v+1)+':', e); }
  }
  if (videos.length && videoFrameReadFailed && unique.length === 0) {
    showToast('Não consegui decodificar os quadros deste vídeo neste aparelho. O arquivo pode ser válido; tente novamente.', 'error');
    closeMo('mo-import-confirm');
    return;
  }

  // Só envia o arquivo original depois que a leitura local terminou.
  // Em celular/WebView, fazer upload e seek/decodificação simultaneamente
  // do mesmo File grande pode bloquear o decoder e deixar a tela parada.
  if (videos.length) updateRing('Salvando vídeo para conferência ' + dot, false);
  const _videoUploadResults = await Promise.all(
    videos.map(file => uploadImportVideo(file, operationalPlat, operationalDate))
  );
  window._pendingImportVideoIds = _videoUploadResults.filter(Boolean);
  if (videos.length && window._pendingImportVideoIds.length < videos.length) {
    showToast('Corridas importadas, mas o vídeo de auditoria não pôde ser salvo — confira sua conexão.', 'error');
  }
  flushDetail(); // fragmento de detalhe que ficou aberto no fim do vídeo

  // Resolve conflitos de horário: a regra pedida pra IA é "cada horário =
  // uma corrida só", mas o dedup acima só barra repetição idêntica
  // (mesmo horário E mesmo valor) — se o mesmo horário veio com valores
  // DIFERENTES em frames diferentes (mistura de cartão vizinho num frame
  // só), os dois passavam pra lista como corridas separadas, inflando a
  // contagem e o total. Aqui, pra cada (plataforma, horário), fica só a
  // leitura de valor que mais se repetiu entre os frames — a leitura
  // certa tende a se confirmar igual em vários frames seguidos (o vídeo
  // se sobrepõe), enquanto uma leitura errada por mistura de cartão
  // costuma aparecer uma vez só.
  function resolveTimeConflicts(list) {
    const groups = new Map();
    list.forEach(t => {
      const k = (t.platform || '') + '_' + t.time;
      (groups.get(k) || groups.set(k, []).get(k)).push(t);
    });
    const resolved = [];
    groups.forEach(items => {
      if (items.length === 1) { resolved.push(items[0]); return; }
      const byValue = new Map();
      items.forEach(t => {
        const vk = (t.value || 0).toFixed(2);
        (byValue.get(vk) || byValue.set(vk, []).get(vk)).push(t);
      });
      let best = null;
      byValue.forEach(arr => { if (!best || arr.length > best.length) best = arr; });
      // dentro do grupo vencedor, prefere o item com endereço mais completo
      let rep = best[0];
      best.forEach(t => {
        const len = (t.originFull || '').length + (t.destFull || '').length;
        const repLen = (rep.originFull || '').length + (rep.destFull || '').length;
        if (len > repLen) rep = t;
      });
      resolved.push(rep);
    });
    return resolved;
  }
  const resolvedUnique = resolveTimeConflicts(unique);
  unique.length = 0;
  unique.push(...resolvedUnique);

  // Funde os detalhes nas corridas da lista — SÓ completa km/tempo/endereço
  // de uma corrida que a LISTA já confirmou (com valor de verdade). Nunca
  // cria corrida nova a partir de detalhe sozinho: sem valor, não tem como
  // confirmar que é real, e foi exatamente isso que gerava corrida fantasma
  // no import (fragmento de detalhe virando corrida "avulsa" por engano).
  details.forEach(d => {
    let cand = null;
    if (d.time) cand = unique.find(u => u.time === d.time && !u._enriched);
    if (!cand && (d.originFull || d.destFull)) {
      cand = unique.find(u => !u._enriched &&
        ((d.originFull && u.originFull === d.originFull) || (d.destFull && u.destFull === d.destFull)));
    }
    if (cand) {
      cand._enriched = true;
      if (d.km != null && d.km > 0)       { cand.km = d.km; cand.kmEstimado = false; }
      if (d.duration != null && d.duration > 0) cand.duration = d.duration;
      // Endereço do detalhe é a linha inteira, sem truncar — prefere ele.
      if (d.originFull) { cand.originFull = d.originFull; cand.origin = d.origin; }
      if (d.destFull)   { cand.destFull = d.destFull; cand.dest = d.dest; }
    }
  });

  const day = DB.day(S.curDate);

  // Casa cada corrida do vídeo com auto_trips do dia ANTES de mostrar a
  // tela (pedido do Yuri, 16/08/2026) — permite separar "vai só corrigir
  // valor" de "é corrida nova" já na seleção, e esconder o que não muda
  // nada em vez de mostrar tudo misturado.
  const autoTripsPool = (await loadAutoTripsForOperationalDay(operationalPlat, operationalDate)) || [];
  const numEq = (a,b,tol) => Math.abs((a||0)-(b||0)) <= (tol||0.02);
  const annotated = unique.map((t,i) => {
    const matched = matchAutoTrip(t, autoTripsPool);
    if (matched) autoTripsPool.splice(autoTripsPool.indexOf(matched), 1); // consumido — nunca casa duas vezes
    const base = {...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _exists: isTripDuplicate(t, day.trips)};
    if (matched) {
      const newVal = parseFloat(t.value)||0;
      return {...base, _matchedAutoTripId: matched.id, _isNew: false,
        _oldValue: matched.offer_value, _changed: !numEq(matched.offer_value, newVal, 0.02)};
    }
    return {...base, _matchedAutoTripId: null, _isNew: true, _changed: true};
  });
  // Só mostra o que de fato vai mudar algo — casada sem diferença de valor
  // não aparece (nada a fazer), nova sem valor legível também não aparece
  // (sem valor não dá pra confirmar que é corrida de verdade).
  window._pendingTrips = annotated.filter(t => t._isNew ? (parseFloat(t.value)||0) > 0 : t._changed);
  // Corrida NOVA nunca confia no endereço do vídeo sem validar — mesma
  // regra do Kotlin (looksLikeAddress). Sem validação, fica sem endereço
  // em vez de arriscar lixo/mensagem de chat virando corrida (pedido
  // explícito do Yuri: "nunca endereço, a não ser que garanta").
  window._pendingTrips.forEach(t => {
    if (!t._isNew) return;
    if (t.originFull && !looksLikeAddress(t.originFull)) { t.originFull = ''; t.origin = ''; }
    if (t.destFull && !looksLikeAddress(t.destFull)) { t.destFull = ''; t.dest = ''; }
  });

  // Se algo foi declarado no pre-check, mostra a conferencia antes da lista.
  // Se pulou (tudo null), vai direto pra lista de confirmacao de sempre.
  const hasPrecheck = importPrecheck.uberCount != null || importPrecheck.p99Count != null;
  if (hasPrecheck) showImportMatch(unique); else showImportConfirm();
}

function showImportMatch(unique) {
  const uberFound = unique.filter(t => t.platform === 'uber');
  const p99Found  = unique.filter(t => t.platform === '99');
  const uberFoundCount = uberFound.length, uberFoundVal = uberFound.reduce((s,t)=>s+(t.value||0),0);
  const p99FoundCount  = p99Found.length,  p99FoundVal  = p99Found.reduce((s,t)=>s+(t.value||0),0);

  const checks = [];
  let anyMissing = false, anyExtra = false;
  // Checagem EXATA: encontrar MAIS do que o declarado é tão errado quanto
  // encontrar menos (significa corrida lida em dobro/fundida). 18/17 não
  // pode aparecer como 'bateu certinho'. Valor com tolerância de 1 centavo.
  const evalPlat = (label, foundCount, foundVal, decCount, decVal) => {
    let ok = foundCount === decCount;
    if (ok && decVal != null) ok = Math.abs(foundVal - decVal) < 0.01;
    if (!ok) { if (foundCount > decCount || (decVal != null && foundVal > decVal + 0.009)) anyExtra = true; else anyMissing = true; }
    checks.push({label, ok, txt: `${foundCount}/${decCount} · ${DB.fRI(foundVal)}${decVal != null ? ' de ' + DB.fRI(decVal) : ''}`});
  };
  if (importPrecheck.uberCount != null) evalPlat('Uber', uberFoundCount, uberFoundVal, importPrecheck.uberCount, importPrecheck.uberValue);
  if (importPrecheck.p99Count != null) evalPlat('99', p99FoundCount, p99FoundVal, importPrecheck.p99Count, importPrecheck.p99Value);
  const allOk = !anyMissing && !anyExtra;
  const pillsHtml = checks.map(c => `<span style="font-size:11px;font-weight:700;padding:4px 10px;border-radius:99px;background:${c.ok?'#DCFCE7':'#FEE2E2'};color:${c.ok?'#16A34A':'#DC2626'}">${c.label} ${c.txt} ${c.ok?'✓':'✗'}</span>`).join('');

  const warnTitle = anyExtra ? 'Achei corridas a mais' : 'Faltou alguma coisa';
  const warnMsg = anyExtra
    ? 'Alguma corrida foi lida em dobro ou misturada. Na lista, desmarque as que estiverem repetidas ou com dados estranhos antes de importar.'
    : 'Confira se subiu todos os prints/vídeos desse app';
  const cardHtml = allOk
    ? `<div style="border-radius:16px;padding:16px;text-align:center;margin-bottom:14px;background:#ECFDF3;border:1.5px solid #BBF7D0">
        <div style="width:44px;height:44px;border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto 10px;background:#DCFCE7;color:#16A34A"><svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg></div>
        <b style="display:block;font-size:14px;color:#0F172A;margin-bottom:4px">Importação validada</b>
        <span style="font-size:12px;color:#64748B;line-height:1.5">Os valores conferem com o que você informou</span>
        <div style="display:flex;gap:8px;justify-content:center;margin-top:10px;flex-wrap:wrap">${pillsHtml}</div>
      </div>`
    : `<div style="border-radius:16px;padding:16px;text-align:center;margin-bottom:14px;background:#FFFBEB;border:1.5px solid #FDE68A">
        <div style="width:44px;height:44px;border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto 10px;background:#FEF3C7;color:#D97706"><svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.4"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg></div>
        <b style="display:block;font-size:14px;color:#0F172A;margin-bottom:4px">${warnTitle}</b>
        <span style="font-size:12px;color:#64748B;line-height:1.5">${warnMsg}</span>
        <div style="display:flex;gap:8px;justify-content:center;margin-top:10px;flex-wrap:wrap">${pillsHtml}</div>
      </div>`;

  document.getElementById('ic-title').textContent = 'Conferência';
  document.getElementById('ic-body').innerHTML = cardHtml;
  const btn = document.getElementById('ic-btn');
  btn.style.display = 'block';
  btn.textContent = allOk ? 'Ver corridas encontradas' : 'Ver o que encontrei';
  btn.onclick = () => { showImportConfirm(); btn.onclick = confirmImport; };
}

function showSpinner(msg) {
  return '<div style="text-align:center;padding:28px 16px">' +
    '<div style="font-size:28px;animation:vspin 1s linear infinite;display:inline-block">&#9881;</div>' +
    '<div style="margin-top:12px;font-size:13px;color:#64748B;font-weight:500">' + msg + '</div></div>';
}

// Normaliza "7:42" e "07:42" pro mesmo formato — a IA não le a hora de forma
// 100% consistente entre frames/prints (zero à esquerda variando), e sem isso
// a mesma corrida real passava pelo dedup do import como se fossem duas.
function normTime(t) {
  const m = String(t||'').match(/(\d{1,2}):(\d{2})/);
  return m ? m[1].padStart(2,'0')+':'+m[2] : String(t||'');
}
async function processMultipleImports(files, plat) {
  const total = files.length;
  document.getElementById('ic-title').textContent = 'Analisando ' + total + ' print' + (total > 1 ? 's' : '') + '...';
  document.getElementById('ic-body').innerHTML = showSpinner('Processando 1 de ' + total + '...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  const allTrips = [];
  for (let i = 0; i < files.length; i++) {
    document.getElementById('ic-body').innerHTML = showSpinner('Processando print ' + (i+1) + ' de ' + total + '...');
    try {
      const trips = await extractTripsFromImage(files[i], plat);
      allTrips.push(...trips);
    } catch(e) { console.warn('Erro print '+(i+1)+':', e); }
  }
  const day = DB.day(S.curDate);
  const seen = new Set();
  const unique = allTrips.filter(t => {
    if (!(t.value > 0)) return false; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
    t.time = normTime(t.time);
    const key = t.time+'_'+(t.value||0).toFixed(2);
    if (seen.has(key)) return false;
    seen.add(key); return true;
  });
  window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
    platform: t.platform === '99' ? '99' : 'uber',
    _exists: isTripDuplicate(t, day.trips) // pre-mark duplicates
  }));
  showImportConfirm();
}

async function processVideoImport(file, plat) {
  document.getElementById('ic-title').textContent = 'Extraindo frames do vídeo...';
  document.getElementById('ic-body').innerHTML = showSpinner('Analisando vídeo...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  try {
    const frames = await extractVideoFrames(file);
    const allTrips = [];
    for (let i = 0; i < frames.length; i++) {
      document.getElementById('ic-body').innerHTML = showSpinner('Analisando frame ' + (i+1) + ' de ' + frames.length + '...');
      try {
        const trips = await extractTripsFromBase64(frames[i], 'image/jpeg', plat);
        allTrips.push(...trips);
      } catch(e) { console.warn('Frame erro:', e); }
    }
    const dayV = DB.day(S.curDate);
    const seenV = new Set();
    const unique = allTrips.filter(t => {
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seenV.has(key)) return false;
      seenV.add(key); return true;
    });
    const _dayV = DB.day(S.curDate);
    window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _dup: isTripDuplicate(t, _dayV.trips)}));
    showImportConfirm();
  } catch(e) {
    closeMo('mo-import-confirm');
    showToast('Erro ao processar vídeo: ' + e.message + '. Use MP4 ou prints do histórico.', 'error');
    console.error('Video error:', e);
  }
}


// APK 1.3.9+: o WebView de alguns aparelhos não consegue decodificar certos
// screen recordings mesmo quando o arquivo é válido. Quando o bridge nativo
// estiver disponível, o Android decodifica o vídeo e devolve só os JPEGs.
const _nativeVideoExtractRequests = new Map();
window.__mobNativeVideoFrame = function(token, dataUrl) {
===== auto_trips @ 840118 =====
orrige valor (verde), depois o que é novo — nunca
  // visto na captura ao vivo, vira "importada" (âmbar).
  const changedRows = [], newRows = [];
  trips.forEach((t,i) => { (t._isNew ? newRows : changedRows).push(importRowHtml(t,i)); });

  let groupsHtml = '';
  if (changedRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#22C55E"></span><span style="font-size:12.5px;font-weight:800">Valor vai mudar</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${changedRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0;margin-bottom:${newRows.length?'10px':'0'}">${changedRows.join('')}</div>`;
  }
  if (newRows.length) {
    groupsHtml += `<div style="display:flex;align-items:center;gap:8px;margin:14px 4px 8px"><span style="width:8px;height:8px;border-radius:50%;background:#F59E0B"></span><span style="font-size:12.5px;font-weight:800">Novas</span><span style="font-size:11px;font-weight:700;color:#64748B;margin-left:auto">${newRows.length}</span></div>` +
      `<div style="background:#fff;border-radius:16px;overflow:hidden;border:1px solid #E2E8F0">${newRows.join('')}</div>`;
  }

  document.getElementById('ic-body').innerHTML = '<div id="ic-declared">' + importDeclaredHtml() + '</div>' +
    '<div id="ic-summary">' + importSummaryHtml() + '</div>' + groupsHtml;
}
function toggleImportTrip(i) {
  const t = (window._pendingTrips||[])[i]; if (!t) return;
  t._sel = !t._sel;
  const chkColor = t._isNew ? '#F59E0B' : '#22C55E';
  const el = document.getElementById('ict-'+i);
  if (el) { el.style.background=t._sel?chkColor:'transparent'; el.style.borderColor=t._sel?chkColor:'#E2E8F0'; el.textContent=t._sel?'✓':''; }
  updateImportButtonLabel();
  updateImportSummary();
}

// Detecta corrida duplicada (hora+valor, hora+km, valor+rota)
// Versão do código — atualizar para verificar se cache está correto
const _APP_CODE_VER = "v24-trip99-fix";

// Normaliza texto de rota pra comparação: minúsculas, sem acentos, sem
// espaços extras — "Barra do Imbuí" e "barra do imbui" viram iguais.
// (OCR de frames diferentes do mesmo vídeo varia acentuação → sem isso,
// a mesma corrida escapa do detector de duplicata.)
function _normRoute(s) {
  return (s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')
    .replace(/\s+/g,' ').trim();
}
function isTripDuplicate(newT, existing) {
  return existing.some(t => {
    // Plataformas diferentes = corridas diferentes (nunca duplicata)
    const samePlat = (t.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const sameTime = t.time && newT.time && t.time === newT.time;
    const sameVal  = Math.abs((t.value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && t.km > 0 && Math.abs((t.km||0)-(newT.km||0)) < 0.5;
    // 99 não reporta km — usar origem+destino como sinal de rota nesse caso
    // (comparação normalizada: acento/caixa não separam corridas)
    const sameRoute = !!(t.origin && newT.origin && t.dest && newT.dest
                       && _normRoute(t.origin) === _normRoute(newT.origin)
                       && _normRoute(t.dest) === _normRoute(newT.dest));
    const nearTime = t.time && newT.time && (() => {
      const [h1,m1] = t.time.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    // Mesmo horário + mesmo valor = duplicata clara
    if (sameTime && sameVal) return true;
    // Mesmo horário EXATO + mesma rota = duplicata mesmo com valor diferente.
    // Protege contra fusão de frames no OCR de vídeo (rota de uma corrida +
    // valor do cartão vizinho). Duas corridas reais no MESMO minuto com a
    // MESMA origem e MESMO destino não existem na prática.
    if (sameTime && sameRoute) return true;
    // Mesmo horário + mesma km (proteção contra OCR com valor diferente)
    if (sameTime && sameKm && newT.km > 0) return true;
    // Próximo horário + mesmo valor + mesma km = duplicata provável
    if (sameVal && nearTime && sameKm) return true;
    // Próximo horário + mesmo valor + mesma rota (sem km, ex: 99) = duplicata provável
    if (sameVal && nearTime && sameRoute) return true;
    // REMOVIDO: sameVal && sameRoute sem tempo = falso positivo entre plataformas
    return false;
  });
}

// ── Casar corrida importada com corrida automática já capturada ────────────
// Pedido do Yuri (23/07/2026): ao importar, se a corrida já foi capturada
// automaticamente, ATUALIZA a automática com o valor real (confirmado pela
// tela de histórico da plataforma — mais confiável que a oferta estimada),
// em vez de criar uma corrida manual duplicada. Só corridas GENUINAMENTE
// novas (sem correspondência) seguem pro fluxo normal de criar.
// Mesmos critérios do isTripDuplicate acima, adaptados pros campos de
// auto_trips (trip_started_at em vez de time, offer_value em vez de value,
// real_km_trip em vez de km, origin_address/dest_address em vez de
// origin/dest) — time da automática convertido pro fuso local em HH:MM.
function _autoTripLocalTime(at) {
  const ts = at.trip_started_at || at.accepted_at;
  if (!ts) return null;
  const dt = new Date(ts);
  return String(dt.getHours()).padStart(2,'0')+':'+String(dt.getMinutes()).padStart(2,'0');
}
function matchAutoTrip(newT, autoTrips) {
  return autoTrips.find(at => {
    const samePlat = (at.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const atTime = _autoTripLocalTime(at);
    const sameTime = atTime && newT.time && atTime === newT.time;
    const sameVal  = Math.abs((at.offer_value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && at.real_km_trip > 0 && Math.abs((at.real_km_trip||0)-(newT.km||0)) < 0.5;
    const sameRoute = !!(at.origin_address && newT.origin && at.dest_address && newT.dest
                       && _normRoute(at.origin_address).includes(_normRoute(newT.origin))
                       && _normRoute(at.dest_address).includes(_normRoute(newT.dest)));
    const nearTime = atTime && newT.time && (() => {
      const [h1,m1] = atTime.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    if (sameTime && sameVal) return true;
    if (sameTime && sameRoute) return true;
    if (sameTime && sameKm && newT.km > 0) return true;
    if (sameVal && nearTime && sameKm) return true;
    if (sameVal && nearTime && sameRoute) return true;
    return false;
  }) || null;
}

// ── Reconciliação de sessões × corridas ───────────────────────────────────
// Regra híbrida: < 15 min → agrupa na sessão + ajusta horários
//                >= 15 min → cria nova sessão
function reconcileSessionsForDay(dateStr, dryRun = false) {
  const THRESHOLD = 15 * 60 * 1000; // 15 minutos em ms
  const day = DB.day(dateStr);
  
  const trips = (day.trips || [])
    .map(t => {
      let ts = t.ts || 0;
      if (!ts && t.time) ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
      return { ...t, _ts: ts };
    })
    .filter(t => t._ts > 0)
    .sort((a, b) => a._ts - b._ts);
  
  if (!trips.length) return { grouped: 0, created: 0, expanded: 0, sessions: day.sessions || [] };
  
  // Cópia profunda das sessões para não mutar durante dry run
  const sessions = JSON.parse(JSON.stringify(day.sessions || []));
  let grouped = 0, created = 0, expanded = 0;
  
  for (const trip of trips) {
    const ts  = trip._ts;
    const dur = (trip.duration || 15) * 60 * 1000;
    const end = ts + dur;
    
    // Já está dentro de uma sessão existente?
    const inside = sessions.find(s =>
      s.start && ts >= (s.start - 1000) && (!s.end || ts <= (s.end + 1000))
    );
    if (inside) continue;
    
    // Sessão mais próxima (distância ao início ou fim)
    let best = null, bestDist = Infinity;
    for (const s of sessions) {
      if (!s.start) continue;
      const d = Math.min(
        Math.abs(ts - s.start),
        s.end ? Math.abs(ts - s.end) : Math.abs(ts - s.start),
        s.end ? Math.abs(end - s.end) : Infinity
      );
      if (d < bestDist) { bestDist = d; best = s; }
    }
    
    if (best && bestDist < THRESHOLD) {
      // Agrupar + expandir horários da sessão
      if (ts < best.start) { best.start = ts; expanded++; }
      if (!best.end || end > best.end) { best.end = end; expanded++; }
      grouped++;
    } else {
      // Criar nova sessão para cobrir esta corrida
      sessions.push({
        id: DB.uid(),
        start: ts,
        end:   end,
        km:    parseFloat((trip.km || 0).toFixed(3)),
        elapsed: Math.round((trip.duration || 15) * 60)
      });
      sessions.sort((a, b) => (a.start || 0) - (b.start || 0));
      created++;
    }
  }
  
  if (!dryRun) {
    day.sessions = sessions;
    DB.saveDay(day);
    // Sincronizar novas/atualizadas sessões com Supabase
    if (_supaUser) {
      sessions.forEach(s => {
        _SUPA.from('sessions').upsert({
          id: s.id, user_id: _supaUser.id, date: dateStr,
          start_ms: s.start, end_ms: s.end,
          km: s.km || 0, elapsed_secs: s.elapsed || 0,
          paused_ms: 0, is_paused: false
        }, { onConflict: 'id' }).catch(() => {});
      });
    }
  }
  
  return { grouped, created, expanded, sessions };
}

// Rodar reconciliação em todos os dias com corridas (migração única)
async function reconcileAllExistingDays() {
  if (localStorage.getItem('sm_session_reconcile_v1')) return;
  
  const dates = DB.allDates();
  let totalGrouped = 0, totalCreated = 0;
  
  for (const dateStr of dates) {
    const day = DB.day(dateStr);
    if (!(day.trips || []).length) continue;
    const r = reconcileSessionsForDay(dateStr);
    totalGrouped += r.grouped;
    totalCreated += r.created;
  }
  
  localStorage.setItem('sm_session_reconcile_v1', '1');
  if (totalCreated > 0 || totalGrouped > 0) {
    showToast(
      `${totalCreated} sessão(ões) criada(s) e ${totalGrouped} corrida(s) agrupada(s) automaticamente.`,
      'info', 5000
    );
    setTimeout(() => { try { renderJornada(); } catch(e) {} }, 80);
  }
}

async function confirmImport() {
  const selected = (window._pendingTrips||[]).filter(t=>t._sel);
  if (!selected.length) { closeMo('mo-import-confirm'); return; }
  const day = DB.day(S.curDate);
  let added=0, skipped=0, updated=0;
  const toUpdateAuto = [];
  const toPushAuto = [];
  const videoId = (window._pendingImportVideoIds||[])[0] || null;
  selected.forEach(t => {
    // Já casada com uma auto_trip existente (matching já feito em
    // processImportBatch, antes da tela abrir) — só atualiza o valor,
    // nunca o endereço (esse já veio validado pela captura ao vivo/GPS).
    if (t._matchedAutoTripId) {
      toUpdateAuto.push({ id: t._matchedAutoTripId, value: parseFloat(t.value)||0, km: parseFloat(t.km)||0,
        dinheiro: !!t.dinheiro });
      updated++;
      return;
    }
    // Nova — nunca vista na captura ao vivo. Vira auto_trips já Confirmada
    // pelo vídeo; 'Importada' agora é somente a tag de origem — unifica com o resto dos
    // dados em vez de virar corrida "manual" separada. Endereço já
    // passou pela validação looksLikeAddress lá em processImportBatch
    // (se não passou, já veio vazio aqui — nunca inventa).
    let startMs;
    try {
      const opDate = window._pendingImportOperationalDate || S.curDate || DB.today();
      startMs = operationalTimestampForTime(t.platform || window._pendingImportPlatform, opDate, t.time||'00:00');
    } catch(e) { startMs = NaN; }
    if (!startMs || isNaN(startMs)) startMs = Date.now();
    const endMs = t.duration > 0 ? startMs + t.duration*60000 : null;
    toPushAuto.push({
      user_id: _supaUser.id,
      platform: t.platform === '99' ? '99' : 'uber',
      accepted_at: new Date(startMs).toISOString(),
      trip_started_at: new Date(startMs).toISOString(),
      trip_ended_at: endMs ? new Date(endMs).toISOString() : null,
      offer_value: parseFloat(t.value)||0,
      real_km_trip: parseFloat(t.km)||0,
      origin_address: t.originFull || null,
      dest_address: t.destFull || null,
      dinheiro: !!t.dinheiro,
      status: 'confirmada',
      capture_source: 'importada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId
    });
    added++;
  });
  // Aplica as atualizações nas automáticas casadas — valor real por cima do
  // estimado, km real quando a importação trouxe (senão mantém o que já
  // tinha), status vira confirmada (agora tem valor confirmado de verdade).
  toUpdateAuto.forEach(u => {
    _SUPA.from('auto_trips').update({
      offer_value: u.value,
      real_km_trip: u.km > 0 ? u.km : undefined,
      dinheiro: u.dinheiro,
      status: 'confirmada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId || undefined
    }).eq('id', u.id).then(() => {}).catch(() => {});
  });
  // Corridas novas — insere de uma vez em auto_trips.
  if (toPushAuto.length) {
    _SUPA.from('auto_trips').insert(toPushAuto).then(() => {}).catch((e) => console.warn('insert importada falhou', e));
  }
  // Atualiza a contagem de corridas confirmadas/criadas por esse vídeo —
  // só informativo, útil pra revisão manual depois.
  if (videoId) {
    _SUPA.from('import_videos').update({ trip_count: toUpdateAuto.length + toPushAuto.length }).eq('id', videoId).then(()=>{}).catch(()=>{});
  }
  window._pendingImportVideoIds = null;
  if (toUpdateAuto.length || toPushAuto.length) { S.autoTripsDate = null; } // força recarregar no próximo renderJornada
  DB.saveDay(day); closeMo('mo-import-confirm'); window._pendingTrips=null;

  const dateHint = S.curDate !== DB.today()
    ? ` (data: ${S.curDate.split('-').reverse().slice(0,2).join('/')})` : '';
  const parts = [];
  if (added > 0) parts.push(`${added} corrida${added!==1?'s':''} nova${added!==1?'s':''} importada${added!==1?'s':''}${dateHint}`);
  if (updated > 0) parts.push(`${updated} já capturada${updated!==1?'s':''} automaticamente, valor confirmado`);
  if (skipped > 0) parts.push(`${skipped} ignorada${skipped!==1?'s':''} (já existente${skipped!==1?'s':''})`);
  const baseMsg = parts.length ? parts.join('. ')+'.' : 'Nenhuma corrida nova.';

  try{renderJornada();}catch(e){}
  try{renderHome();}catch(e){}
  // Garantir que Corridas mostra a data certa independente do filtro
  if (S.curDate !== DB.today()) S.corridasPeriod = 'tudo';
  try{renderCorridasActive();}catch(e){}
  showToast(baseMsg, skipped > 0 ? 'info' : 'success');
}

// ══════════════════════════════════════════════
// IMPORTAÇÃO: histórico oficial do Uber (.zip)
// driver_lifetime_trips-0.csv já traz, por corrida, o valor líquido
// (driver_upfront_fare_local), km e duração — usamos direto daí, sem
// tentar casar com driver_payments por horário (testado com um export
// real: 
===== auto_trips @ 846231 =====
       && _normRoute(at.dest_address).includes(_normRoute(newT.dest)));
    const nearTime = atTime && newT.time && (() => {
      const [h1,m1] = atTime.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    if (sameTime && sameVal) return true;
    if (sameTime && sameRoute) return true;
    if (sameTime && sameKm && newT.km > 0) return true;
    if (sameVal && nearTime && sameKm) return true;
    if (sameVal && nearTime && sameRoute) return true;
    return false;
  }) || null;
}

// ── Reconciliação de sessões × corridas ───────────────────────────────────
// Regra híbrida: < 15 min → agrupa na sessão + ajusta horários
//                >= 15 min → cria nova sessão
function reconcileSessionsForDay(dateStr, dryRun = false) {
  const THRESHOLD = 15 * 60 * 1000; // 15 minutos em ms
  const day = DB.day(dateStr);
  
  const trips = (day.trips || [])
    .map(t => {
      let ts = t.ts || 0;
      if (!ts && t.time) ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
      return { ...t, _ts: ts };
    })
    .filter(t => t._ts > 0)
    .sort((a, b) => a._ts - b._ts);
  
  if (!trips.length) return { grouped: 0, created: 0, expanded: 0, sessions: day.sessions || [] };
  
  // Cópia profunda das sessões para não mutar durante dry run
  const sessions = JSON.parse(JSON.stringify(day.sessions || []));
  let grouped = 0, created = 0, expanded = 0;
  
  for (const trip of trips) {
    const ts  = trip._ts;
    const dur = (trip.duration || 15) * 60 * 1000;
    const end = ts + dur;
    
    // Já está dentro de uma sessão existente?
    const inside = sessions.find(s =>
      s.start && ts >= (s.start - 1000) && (!s.end || ts <= (s.end + 1000))
    );
    if (inside) continue;
    
    // Sessão mais próxima (distância ao início ou fim)
    let best = null, bestDist = Infinity;
    for (const s of sessions) {
      if (!s.start) continue;
      const d = Math.min(
        Math.abs(ts - s.start),
        s.end ? Math.abs(ts - s.end) : Math.abs(ts - s.start),
        s.end ? Math.abs(end - s.end) : Infinity
      );
      if (d < bestDist) { bestDist = d; best = s; }
    }
    
    if (best && bestDist < THRESHOLD) {
      // Agrupar + expandir horários da sessão
      if (ts < best.start) { best.start = ts; expanded++; }
      if (!best.end || end > best.end) { best.end = end; expanded++; }
      grouped++;
    } else {
      // Criar nova sessão para cobrir esta corrida
      sessions.push({
        id: DB.uid(),
        start: ts,
        end:   end,
        km:    parseFloat((trip.km || 0).toFixed(3)),
        elapsed: Math.round((trip.duration || 15) * 60)
      });
      sessions.sort((a, b) => (a.start || 0) - (b.start || 0));
      created++;
    }
  }
  
  if (!dryRun) {
    day.sessions = sessions;
    DB.saveDay(day);
    // Sincronizar novas/atualizadas sessões com Supabase
    if (_supaUser) {
      sessions.forEach(s => {
        _SUPA.from('sessions').upsert({
          id: s.id, user_id: _supaUser.id, date: dateStr,
          start_ms: s.start, end_ms: s.end,
          km: s.km || 0, elapsed_secs: s.elapsed || 0,
          paused_ms: 0, is_paused: false
        }, { onConflict: 'id' }).catch(() => {});
      });
    }
  }
  
  return { grouped, created, expanded, sessions };
}

// Rodar reconciliação em todos os dias com corridas (migração única)
async function reconcileAllExistingDays() {
  if (localStorage.getItem('sm_session_reconcile_v1')) return;
  
  const dates = DB.allDates();
  let totalGrouped = 0, totalCreated = 0;
  
  for (const dateStr of dates) {
    const day = DB.day(dateStr);
    if (!(day.trips || []).length) continue;
    const r = reconcileSessionsForDay(dateStr);
    totalGrouped += r.grouped;
    totalCreated += r.created;
  }
  
  localStorage.setItem('sm_session_reconcile_v1', '1');
  if (totalCreated > 0 || totalGrouped > 0) {
    showToast(
      `${totalCreated} sessão(ões) criada(s) e ${totalGrouped} corrida(s) agrupada(s) automaticamente.`,
      'info', 5000
    );
    setTimeout(() => { try { renderJornada(); } catch(e) {} }, 80);
  }
}

async function confirmImport() {
  const selected = (window._pendingTrips||[]).filter(t=>t._sel);
  if (!selected.length) { closeMo('mo-import-confirm'); return; }
  const day = DB.day(S.curDate);
  let added=0, skipped=0, updated=0;
  const toUpdateAuto = [];
  const toPushAuto = [];
  const videoId = (window._pendingImportVideoIds||[])[0] || null;
  selected.forEach(t => {
    // Já casada com uma auto_trip existente (matching já feito em
    // processImportBatch, antes da tela abrir) — só atualiza o valor,
    // nunca o endereço (esse já veio validado pela captura ao vivo/GPS).
    if (t._matchedAutoTripId) {
      toUpdateAuto.push({ id: t._matchedAutoTripId, value: parseFloat(t.value)||0, km: parseFloat(t.km)||0,
        dinheiro: !!t.dinheiro });
      updated++;
      return;
    }
    // Nova — nunca vista na captura ao vivo. Vira auto_trips já Confirmada
    // pelo vídeo; 'Importada' agora é somente a tag de origem — unifica com o resto dos
    // dados em vez de virar corrida "manual" separada. Endereço já
    // passou pela validação looksLikeAddress lá em processImportBatch
    // (se não passou, já veio vazio aqui — nunca inventa).
    let startMs;
    try {
      const opDate = window._pendingImportOperationalDate || S.curDate || DB.today();
      startMs = operationalTimestampForTime(t.platform || window._pendingImportPlatform, opDate, t.time||'00:00');
    } catch(e) { startMs = NaN; }
    if (!startMs || isNaN(startMs)) startMs = Date.now();
    const endMs = t.duration > 0 ? startMs + t.duration*60000 : null;
    toPushAuto.push({
      user_id: _supaUser.id,
      platform: t.platform === '99' ? '99' : 'uber',
      accepted_at: new Date(startMs).toISOString(),
      trip_started_at: new Date(startMs).toISOString(),
      trip_ended_at: endMs ? new Date(endMs).toISOString() : null,
      offer_value: parseFloat(t.value)||0,
      real_km_trip: parseFloat(t.km)||0,
      origin_address: t.originFull || null,
      dest_address: t.destFull || null,
      dinheiro: !!t.dinheiro,
      status: 'confirmada',
      capture_source: 'importada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId
    });
    added++;
  });
  // Aplica as atualizações nas automáticas casadas — valor real por cima do
  // estimado, km real quando a importação trouxe (senão mantém o que já
  // tinha), status vira confirmada (agora tem valor confirmado de verdade).
  toUpdateAuto.forEach(u => {
    _SUPA.from('auto_trips').update({
      offer_value: u.value,
      real_km_trip: u.km > 0 ? u.km : undefined,
      dinheiro: u.dinheiro,
      status: 'confirmada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId || undefined
    }).eq('id', u.id).then(() => {}).catch(() => {});
  });
  // Corridas novas — insere de uma vez em auto_trips.
  if (toPushAuto.length) {
    _SUPA.from('auto_trips').insert(toPushAuto).then(() => {}).catch((e) => console.warn('insert importada falhou', e));
  }
  // Atualiza a contagem de corridas confirmadas/criadas por esse vídeo —
  // só informativo, útil pra revisão manual depois.
  if (videoId) {
    _SUPA.from('import_videos').update({ trip_count: toUpdateAuto.length + toPushAuto.length }).eq('id', videoId).then(()=>{}).catch(()=>{});
  }
  window._pendingImportVideoIds = null;
  if (toUpdateAuto.length || toPushAuto.length) { S.autoTripsDate = null; } // força recarregar no próximo renderJornada
  DB.saveDay(day); closeMo('mo-import-confirm'); window._pendingTrips=null;

  const dateHint = S.curDate !== DB.today()
    ? ` (data: ${S.curDate.split('-').reverse().slice(0,2).join('/')})` : '';
  const parts = [];
  if (added > 0) parts.push(`${added} corrida${added!==1?'s':''} nova${added!==1?'s':''} importada${added!==1?'s':''}${dateHint}`);
  if (updated > 0) parts.push(`${updated} já capturada${updated!==1?'s':''} automaticamente, valor confirmado`);
  if (skipped > 0) parts.push(`${skipped} ignorada${skipped!==1?'s':''} (já existente${skipped!==1?'s':''})`);
  const baseMsg = parts.length ? parts.join('. ')+'.' : 'Nenhuma corrida nova.';

  try{renderJornada();}catch(e){}
  try{renderHome();}catch(e){}
  // Garantir que Corridas mostra a data certa independente do filtro
  if (S.curDate !== DB.today()) S.corridasPeriod = 'tudo';
  try{renderCorridasActive();}catch(e){}
  showToast(baseMsg, skipped > 0 ? 'info' : 'success');
}

// ══════════════════════════════════════════════
// IMPORTAÇÃO: histórico oficial do Uber (.zip)
// driver_lifetime_trips-0.csv já traz, por corrida, o valor líquido
// (driver_upfront_fare_local), km e duração — usamos direto daí, sem
// tentar casar com driver_payments por horário (testado com um export
// real: o casamento por horário troca valores entre corridas vizinhas).
// driver_payments-0.csv só é usado pra somar gorjeta/incentivo do
// período inteiro, como informação, sem tentar atribuir por corrida.
// ══════════════════════════════════════════════

function uzRound2(n) { return Math.round(n * 100) / 100; }

// Aceita "10.50" e "10,50" — exportações da Uber variam o separador por região/locale.
function uzNum(raw) {
  if (raw == null || raw === '') return 0;
  return parseFloat(String(raw).replace(',', '.')) || 0;
}

function uzParseTs(raw) {
  if (!raw) return null;
  // O timestamp do zip vem em UTC (tem 'Z' no final) — converte pra Brasília (UTC-3)
  const raw2 = String(raw).trim();
  try {
    const d = new Date(raw2.includes('T') ? raw2 : raw2.replace(' ', 'T') + 'Z');
    if (isNaN(d.getTime())) return null;
    // subtrai 3h pra Brasília
    const brt = new Date(d.getTime() - 3 * 60 * 60 * 1000);
    const pad = n => String(n).padStart(2, '0');
    const dateStr = brt.getUTCFullYear() + '-' + pad(brt.getUTCMonth()+1) + '-' + pad(brt.getUTCDate());
    const timeStr = pad(brt.getUTCHours()) + ':' + pad(brt.getUTCMinutes());
    return { dateStr, timeStr };
  } catch(e) { return null; }
}

// Soma gorjeta e incentivo/promoção do período inteiro (informativo, não por corrida).
function uzAggregatePayments(rows) {
  let tip = 0, promo = 0;
  for (const r of rows) {
    const amt = uzNum(r['Local Amount']);
    if (r['Category'] === 'tip') tip += amt;
    if (r['Category'] === 'existing_driver_incentive') promo += amt;
  }
  return { tip: uzRound2(tip), promo: uzRound2(promo) };
}

// Monta as corridas concluídas direto do driver_lifetime_trips. Corridas canceladas
// (status diferente de 'completed') são ignoradas — não são corridas de fato.
function uzBuildTrips(rows) {
  const trips = [];
  for (const r of rows) {
    if (r['status'] !== 'completed') continue;
    const begin = uzParseTs(r['begintrip_timestamp_local']) || uzParseTs(r['request_timestamp_local']);
    if (!begin) continue;
    const value = uzNum(r['driver_upfront_fare_local']) || uzNum(r['original_fare_local']);
    trips.push({
      date: begin.dateStr, time: begin.timeStr,
      value: uzRound2(Math.max(0, value)),
      km: r['trip_distance_miles'] ? uzRound2(uzNum(r['trip_distance_miles']) * 1.60934) : 0,
      // o app guarda duração em MINUTOS (a IA de import por foto também usa minutos);
      // o export da Uber vem em segundos, por isso a divisão por 60 aqui.
      duration: r['trip_duration_seconds'] ? Math.round(uzNum(r['trip_duration_seconds']) / 60) : 0,
      category: r['global_product_name'] || '',
      surge: r['is_surged'] === 'true',
      airport: r['is_airport_trip'] === 'true',
      // dinamico junta tarifa dinamica (surge) + promocao/desconto da corrida num
      // valor só — já incluso no "value", mostrado separado só pra detalhar a corrida.
      // Pode ficar negativo se o desconto for maior que o surge.
      dinamico:    uzRound2(Math.max(0, uzNum(r['surge_fare_local'])) + uzNum(r['promotion_local'])),
      dinheiro:   r['is_cash_trip'] === 'true',
      pedagio:    uzNum(r['toll_amount_local']) > 0,
      serviceFee: (r['service_fee'] !== undefined && r['service_fee'] !== '') ? parseFloat(r['service_fee']) : null,
      taxaZero:   r['service_fee'] === '0' || r['service_fee'] === '0.0',
    });
  }
  return trips;
}

function uzFindEntry(zip, suffix) {
  const f = Object.values(zip.files).find(x => !x.dir && x.name.endsWith(suffix));
  if (!f) throw new Error('Não encontrei "' + suffix + '" no zip. Confira se é o arquivo certo da Uber.');
  return f;
}

let _uzResult = null;

async function handleUberZipFile(input) {
  const file = input.files[0]; if (!file) return;
  input.value = '';
  openMo('mo-uberzip');
  document.getElementById('uz-proc').style.display = 'block';
  document.getElementById('uz-res').style.display = 'none';
  const stepEl = document.getElementById('uz-proc-step');
  const subEl  = document.getElementById('uz-proc-sub');
  const barEl  = document.getElementById('uz-proc-bar');
  const setStep = (msg, pct) => { if (stepEl) stepEl.textContent = msg; if (barEl) barEl.style.width = pct + '%'; };

  try {
    setStep('Lendo arquivo .zip...', 10);
    const zip = await JSZip.loadAsync(file);
    const paymentsEntry = uzFindEntry(zip, 'driver_payments-0.csv');
    const tripsEntry     = uzFindEntry(zip, 'driver_lifetime_trips-0.csv');

    setStep('Lendo corridas...', 35);
    const [paymentsCsv, tripsCsv] = await Promise.all([
      paymentsEntry.async('string'), tripsEntry.async('string')
    ]);
    const paymentsRows = Papa.parse(paymentsCsv, { header: true, skipEmptyLines: true }).data;
    const tripsRows     = Papa.parse(tripsCsv, { header: true, skipEmptyLines: true }).data;

    setStep('Organizando corridas concluídas...', 55);
    await new Promise(r => setTimeout(r, 0));
    const trips = uzBuildTrips(tripsRows);
    if (!trips.length) throw new Error('Não encontrei nenhuma corrida concluída no arquivo. Os nomes das colunas podem ter mudado — me envia o cabeçalho do driver_lifetime_trips-0.csv pra eu ajustar.');
    const agg = uzAggregatePayments(paymentsRows);

    setStep('Comparando com seu histórico...', 80);
    await new Promise(r => setTimeout(r, 0));

    let added = 0, dup = 0, totalValue = 0;
    let minDate = null, maxDate = null;
    const toImport = [];
    const dayCache = {};
    for (const t of trips) {
      if (!minDate || t.date < minDate) minDate = t.date;
      if (!maxDate || t.date > maxDate) maxDate = t.date;
      if (!dayCache[t.date]) dayCache[t.date] = DB.day(t.date);
      const day = dayCache[t.date];
      const isDup = day.trips.some(x => x.time === t.time && Math.abs((x.value || 0) - t.value) < 0.02)
        || toImport.some(x => x.date === t.date && x.time === t.time && Math.abs(x.value - t.value) < 0.02);
      if (isDup) { dup++; continue; }
      added++; totalValue += t.value;
      toImport.push(t);
    }

    setStep('Pronto!', 100);
    _uzResult = { toImport, added, dup, totalValue: uzRound2(totalValue),
      totalTip: agg.
===== auto_trips @ 847869 =====
xistente?
    const inside = sessions.find(s =>
      s.start && ts >= (s.start - 1000) && (!s.end || ts <= (s.end + 1000))
    );
    if (inside) continue;
    
    // Sessão mais próxima (distância ao início ou fim)
    let best = null, bestDist = Infinity;
    for (const s of sessions) {
      if (!s.start) continue;
      const d = Math.min(
        Math.abs(ts - s.start),
        s.end ? Math.abs(ts - s.end) : Math.abs(ts - s.start),
        s.end ? Math.abs(end - s.end) : Infinity
      );
      if (d < bestDist) { bestDist = d; best = s; }
    }
    
    if (best && bestDist < THRESHOLD) {
      // Agrupar + expandir horários da sessão
      if (ts < best.start) { best.start = ts; expanded++; }
      if (!best.end || end > best.end) { best.end = end; expanded++; }
      grouped++;
    } else {
      // Criar nova sessão para cobrir esta corrida
      sessions.push({
        id: DB.uid(),
        start: ts,
        end:   end,
        km:    parseFloat((trip.km || 0).toFixed(3)),
        elapsed: Math.round((trip.duration || 15) * 60)
      });
      sessions.sort((a, b) => (a.start || 0) - (b.start || 0));
      created++;
    }
  }
  
  if (!dryRun) {
    day.sessions = sessions;
    DB.saveDay(day);
    // Sincronizar novas/atualizadas sessões com Supabase
    if (_supaUser) {
      sessions.forEach(s => {
        _SUPA.from('sessions').upsert({
          id: s.id, user_id: _supaUser.id, date: dateStr,
          start_ms: s.start, end_ms: s.end,
          km: s.km || 0, elapsed_secs: s.elapsed || 0,
          paused_ms: 0, is_paused: false
        }, { onConflict: 'id' }).catch(() => {});
      });
    }
  }
  
  return { grouped, created, expanded, sessions };
}

// Rodar reconciliação em todos os dias com corridas (migração única)
async function reconcileAllExistingDays() {
  if (localStorage.getItem('sm_session_reconcile_v1')) return;
  
  const dates = DB.allDates();
  let totalGrouped = 0, totalCreated = 0;
  
  for (const dateStr of dates) {
    const day = DB.day(dateStr);
    if (!(day.trips || []).length) continue;
    const r = reconcileSessionsForDay(dateStr);
    totalGrouped += r.grouped;
    totalCreated += r.created;
  }
  
  localStorage.setItem('sm_session_reconcile_v1', '1');
  if (totalCreated > 0 || totalGrouped > 0) {
    showToast(
      `${totalCreated} sessão(ões) criada(s) e ${totalGrouped} corrida(s) agrupada(s) automaticamente.`,
      'info', 5000
    );
    setTimeout(() => { try { renderJornada(); } catch(e) {} }, 80);
  }
}

async function confirmImport() {
  const selected = (window._pendingTrips||[]).filter(t=>t._sel);
  if (!selected.length) { closeMo('mo-import-confirm'); return; }
  const day = DB.day(S.curDate);
  let added=0, skipped=0, updated=0;
  const toUpdateAuto = [];
  const toPushAuto = [];
  const videoId = (window._pendingImportVideoIds||[])[0] || null;
  selected.forEach(t => {
    // Já casada com uma auto_trip existente (matching já feito em
    // processImportBatch, antes da tela abrir) — só atualiza o valor,
    // nunca o endereço (esse já veio validado pela captura ao vivo/GPS).
    if (t._matchedAutoTripId) {
      toUpdateAuto.push({ id: t._matchedAutoTripId, value: parseFloat(t.value)||0, km: parseFloat(t.km)||0,
        dinheiro: !!t.dinheiro });
      updated++;
      return;
    }
    // Nova — nunca vista na captura ao vivo. Vira auto_trips já Confirmada
    // pelo vídeo; 'Importada' agora é somente a tag de origem — unifica com o resto dos
    // dados em vez de virar corrida "manual" separada. Endereço já
    // passou pela validação looksLikeAddress lá em processImportBatch
    // (se não passou, já veio vazio aqui — nunca inventa).
    let startMs;
    try {
      const opDate = window._pendingImportOperationalDate || S.curDate || DB.today();
      startMs = operationalTimestampForTime(t.platform || window._pendingImportPlatform, opDate, t.time||'00:00');
    } catch(e) { startMs = NaN; }
    if (!startMs || isNaN(startMs)) startMs = Date.now();
    const endMs = t.duration > 0 ? startMs + t.duration*60000 : null;
    toPushAuto.push({
      user_id: _supaUser.id,
      platform: t.platform === '99' ? '99' : 'uber',
      accepted_at: new Date(startMs).toISOString(),
      trip_started_at: new Date(startMs).toISOString(),
      trip_ended_at: endMs ? new Date(endMs).toISOString() : null,
      offer_value: parseFloat(t.value)||0,
      real_km_trip: parseFloat(t.km)||0,
      origin_address: t.originFull || null,
      dest_address: t.destFull || null,
      dinheiro: !!t.dinheiro,
      status: 'confirmada',
      capture_source: 'importada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId
    });
    added++;
  });
  // Aplica as atualizações nas automáticas casadas — valor real por cima do
  // estimado, km real quando a importação trouxe (senão mantém o que já
  // tinha), status vira confirmada (agora tem valor confirmado de verdade).
  toUpdateAuto.forEach(u => {
    _SUPA.from('auto_trips').update({
      offer_value: u.value,
      real_km_trip: u.km > 0 ? u.km : undefined,
      dinheiro: u.dinheiro,
      status: 'confirmada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId || undefined
    }).eq('id', u.id).then(() => {}).catch(() => {});
  });
  // Corridas novas — insere de uma vez em auto_trips.
  if (toPushAuto.length) {
    _SUPA.from('auto_trips').insert(toPushAuto).then(() => {}).catch((e) => console.warn('insert importada falhou', e));
  }
  // Atualiza a contagem de corridas confirmadas/criadas por esse vídeo —
  // só informativo, útil pra revisão manual depois.
  if (videoId) {
    _SUPA.from('import_videos').update({ trip_count: toUpdateAuto.length + toPushAuto.length }).eq('id', videoId).then(()=>{}).catch(()=>{});
  }
  window._pendingImportVideoIds = null;
  if (toUpdateAuto.length || toPushAuto.length) { S.autoTripsDate = null; } // força recarregar no próximo renderJornada
  DB.saveDay(day); closeMo('mo-import-confirm'); window._pendingTrips=null;

  const dateHint = S.curDate !== DB.today()
    ? ` (data: ${S.curDate.split('-').reverse().slice(0,2).join('/')})` : '';
  const parts = [];
  if (added > 0) parts.push(`${added} corrida${added!==1?'s':''} nova${added!==1?'s':''} importada${added!==1?'s':''}${dateHint}`);
  if (updated > 0) parts.push(`${updated} já capturada${updated!==1?'s':''} automaticamente, valor confirmado`);
  if (skipped > 0) parts.push(`${skipped} ignorada${skipped!==1?'s':''} (já existente${skipped!==1?'s':''})`);
  const baseMsg = parts.length ? parts.join('. ')+'.' : 'Nenhuma corrida nova.';

  try{renderJornada();}catch(e){}
  try{renderHome();}catch(e){}
  // Garantir que Corridas mostra a data certa independente do filtro
  if (S.curDate !== DB.today()) S.corridasPeriod = 'tudo';
  try{renderCorridasActive();}catch(e){}
  showToast(baseMsg, skipped > 0 ? 'info' : 'success');
}

// ══════════════════════════════════════════════
// IMPORTAÇÃO: histórico oficial do Uber (.zip)
// driver_lifetime_trips-0.csv já traz, por corrida, o valor líquido
// (driver_upfront_fare_local), km e duração — usamos direto daí, sem
// tentar casar com driver_payments por horário (testado com um export
// real: o casamento por horário troca valores entre corridas vizinhas).
// driver_payments-0.csv só é usado pra somar gorjeta/incentivo do
// período inteiro, como informação, sem tentar atribuir por corrida.
// ══════════════════════════════════════════════

function uzRound2(n) { return Math.round(n * 100) / 100; }

// Aceita "10.50" e "10,50" — exportações da Uber variam o separador por região/locale.
function uzNum(raw) {
  if (raw == null || raw === '') return 0;
  return parseFloat(String(raw).replace(',', '.')) || 0;
}

function uzParseTs(raw) {
  if (!raw) return null;
  // O timestamp do zip vem em UTC (tem 'Z' no final) — converte pra Brasília (UTC-3)
  const raw2 = String(raw).trim();
  try {
    const d = new Date(raw2.includes('T') ? raw2 : raw2.replace(' ', 'T') + 'Z');
    if (isNaN(d.getTime())) return null;
    // subtrai 3h pra Brasília
    const brt = new Date(d.getTime() - 3 * 60 * 60 * 1000);
    const pad = n => String(n).padStart(2, '0');
    const dateStr = brt.getUTCFullYear() + '-' + pad(brt.getUTCMonth()+1) + '-' + pad(brt.getUTCDate());
    const timeStr = pad(brt.getUTCHours()) + ':' + pad(brt.getUTCMinutes());
    return { dateStr, timeStr };
  } catch(e) { return null; }
}

// Soma gorjeta e incentivo/promoção do período inteiro (informativo, não por corrida).
function uzAggregatePayments(rows) {
  let tip = 0, promo = 0;
  for (const r of rows) {
    const amt = uzNum(r['Local Amount']);
    if (r['Category'] === 'tip') tip += amt;
    if (r['Category'] === 'existing_driver_incentive') promo += amt;
  }
  return { tip: uzRound2(tip), promo: uzRound2(promo) };
}

// Monta as corridas concluídas direto do driver_lifetime_trips. Corridas canceladas
// (status diferente de 'completed') são ignoradas — não são corridas de fato.
function uzBuildTrips(rows) {
  const trips = [];
  for (const r of rows) {
    if (r['status'] !== 'completed') continue;
    const begin = uzParseTs(r['begintrip_timestamp_local']) || uzParseTs(r['request_timestamp_local']);
    if (!begin) continue;
    const value = uzNum(r['driver_upfront_fare_local']) || uzNum(r['original_fare_local']);
    trips.push({
      date: begin.dateStr, time: begin.timeStr,
      value: uzRound2(Math.max(0, value)),
      km: r['trip_distance_miles'] ? uzRound2(uzNum(r['trip_distance_miles']) * 1.60934) : 0,
      // o app guarda duração em MINUTOS (a IA de import por foto também usa minutos);
      // o export da Uber vem em segundos, por isso a divisão por 60 aqui.
      duration: r['trip_duration_seconds'] ? Math.round(uzNum(r['trip_duration_seconds']) / 60) : 0,
      category: r['global_product_name'] || '',
      surge: r['is_surged'] === 'true',
      airport: r['is_airport_trip'] === 'true',
      // dinamico junta tarifa dinamica (surge) + promocao/desconto da corrida num
      // valor só — já incluso no "value", mostrado separado só pra detalhar a corrida.
      // Pode ficar negativo se o desconto for maior que o surge.
      dinamico:    uzRound2(Math.max(0, uzNum(r['surge_fare_local'])) + uzNum(r['promotion_local'])),
      dinheiro:   r['is_cash_trip'] === 'true',
      pedagio:    uzNum(r['toll_amount_local']) > 0,
      serviceFee: (r['service_fee'] !== undefined && r['service_fee'] !== '') ? parseFloat(r['service_fee']) : null,
      taxaZero:   r['service_fee'] === '0' || r['service_fee'] === '0.0',
    });
  }
  return trips;
}

function uzFindEntry(zip, suffix) {
  const f = Object.values(zip.files).find(x => !x.dir && x.name.endsWith(suffix));
  if (!f) throw new Error('Não encontrei "' + suffix + '" no zip. Confira se é o arquivo certo da Uber.');
  return f;
}

let _uzResult = null;

async function handleUberZipFile(input) {
  const file = input.files[0]; if (!file) return;
  input.value = '';
  openMo('mo-uberzip');
  document.getElementById('uz-proc').style.display = 'block';
  document.getElementById('uz-res').style.display = 'none';
  const stepEl = document.getElementById('uz-proc-step');
  const subEl  = document.getElementById('uz-proc-sub');
  const barEl  = document.getElementById('uz-proc-bar');
  const setStep = (msg, pct) => { if (stepEl) stepEl.textContent = msg; if (barEl) barEl.style.width = pct + '%'; };

  try {
    setStep('Lendo arquivo .zip...', 10);
    const zip = await JSZip.loadAsync(file);
    const paymentsEntry = uzFindEntry(zip, 'driver_payments-0.csv');
    const tripsEntry     = uzFindEntry(zip, 'driver_lifetime_trips-0.csv');

    setStep('Lendo corridas...', 35);
    const [paymentsCsv, tripsCsv] = await Promise.all([
      paymentsEntry.async('string'), tripsEntry.async('string')
    ]);
    const paymentsRows = Papa.parse(paymentsCsv, { header: true, skipEmptyLines: true }).data;
    const tripsRows     = Papa.parse(tripsCsv, { header: true, skipEmptyLines: true }).data;

    setStep('Organizando corridas concluídas...', 55);
    await new Promise(r => setTimeout(r, 0));
    const trips = uzBuildTrips(tripsRows);
    if (!trips.length) throw new Error('Não encontrei nenhuma corrida concluída no arquivo. Os nomes das colunas podem ter mudado — me envia o cabeçalho do driver_lifetime_trips-0.csv pra eu ajustar.');
    const agg = uzAggregatePayments(paymentsRows);

    setStep('Comparando com seu histórico...', 80);
    await new Promise(r => setTimeout(r, 0));

    let added = 0, dup = 0, totalValue = 0;
    let minDate = null, maxDate = null;
    const toImport = [];
    const dayCache = {};
    for (const t of trips) {
      if (!minDate || t.date < minDate) minDate = t.date;
      if (!maxDate || t.date > maxDate) maxDate = t.date;
      if (!dayCache[t.date]) dayCache[t.date] = DB.day(t.date);
      const day = dayCache[t.date];
      const isDup = day.trips.some(x => x.time === t.time && Math.abs((x.value || 0) - t.value) < 0.02)
        || toImport.some(x => x.date === t.date && x.time === t.time && Math.abs(x.value - t.value) < 0.02);
      if (isDup) { dup++; continue; }
      added++; totalValue += t.value;
      toImport.push(t);
    }

    setStep('Pronto!', 100);
    _uzResult = { toImport, added, dup, totalValue: uzRound2(totalValue),
      totalTip: agg.tip, totalPromo: agg.promo, minDate, maxDate, totalFound: trips.length, allTrips: trips };

    setTimeout(() => uzShowResult(_uzResult), 350);

  } catch (err) {
    console.error('uberZipImport', err);
    setStep('Erro ao processar', 0);
    if (subEl) subEl.textContent = err.message || 'Tente novamente.';
  }
}

function enrichExistingTrips(zipTrips) {
  if (!zipTrips || !zipTrips.length) return 0;
  // Montar índice por data+hora (±2 min) → dados do zip
  const idx = {};
  for (const zt of zipTrips) {
    const [hStr, mStr] = zt.time.split(':');
    const h = parseInt(hStr), m = parseInt(mStr);
    for (let d = -2; d <= 2; d++) {
      let mm = m + d, hh = h;
      if (mm < 0) { hh--; mm += 60; } if (mm >= 60) { hh++; mm -= 60; }
      const key = zt.date + '_' + String(hh).padStart(2,'0') + ':' + String(mm).padStart(2,'0');
      if (!idx[key]) idx[key] = zt;
    }
  }
  let enriched = 0;
  for (const date of DB.allDates()) {
    const day = DB.day(date);
    let changed = false;
    for (const t of (day.trips || [])) {
      if (t.platform !== 'uber') continue;
      // Ignorar corridas já enriquecidas
      if (t.dinheiro !== undefined && t.pedagio !== undefined && t.serviceFee !== undefined) continue;
      const key = date + '_' + t.time;
      const zt = idx[key];
      if (!zt) continue;
      // Confirmar pelo valor (±R$0,50)
      if (Math.abs((t.value||0) - (zt.value||0)) > 0.5) continue;
      // Enriquecer apenas campos ausentes
      let updated = false;
      if (t.dinheiro === undefined) { t.dinheiro = !!zt.dinheiro; updated = true; }
      if (t.pedagio === undefined)  { t.pedagio  = !!zt.p
===== auto_trips @ 948843 =====
artMobiNative.updateFloatingStatus === 'function') {
    try { SmartMobiNative.updateFloatingStatus(g.st); } catch(e) {}
  }
  // Se o app ficou fechado/em segundo plano e a jornada cruzou a meia-noite
  // nesse meio tempo, avisa agora que reabriu (dá um tempo pro render inicial
  // da tela terminar antes de empilhar o modal por cima).
  setTimeout(checkMidnightCrossing, 600);
}
const _CAR_IMGS = {
  idle:    "data:image/png;base64,",
  running: "data:image/png;base64,",
  paused:  "data:image/png;base64,",
};
function syncNavButton() {
  const g = S.gps;
  let cls = 'idle';
  if (g.st === 'running') cls = 'running';
  else if ((DB.day(DB.today()).sessions||[]).length > 0) cls = 'ended';
  updateGpsBtn(cls);
}
function updateGpsBtn(state) {
  const btn = document.getElementById('gps-btn');
  if (!btn) return;
  btn.className = 'start' + (state && state !== 'idle' ? ' '+state : '');
  const img = document.getElementById('gps-icon-img');
  if (img) img.src = state === 'running' ? _CAR_IMGS.running :
                     state === 'paused'  ? _CAR_IMGS.paused  : _CAR_IMGS.idle;
}
function hav(a,b,c,d) {
  const R=6371, dA=(c-a)*Math.PI/180, dB=(d-b)*Math.PI/180;
  return R*2*Math.asin(Math.sqrt(Math.sin(dA/2)**2+Math.cos(a*Math.PI/180)*Math.cos(c*Math.PI/180)*Math.sin(dB/2)**2));
}

// ── PERFIL ────────────────────────────────────────────────────
function authSignOutBtn() { showConfirm('Sair da conta?', authSignOut, { confirmLabel: 'Sair' }); }
function perfilAvatarColor() { return feedAvColor((_supaUser && _supaUser.id) || ''); }

function perfilRenderAvatar() {
  const cfg = DB.cfg();
  const initEl = document.getElementById('p-av-initials');
  const imgEl  = document.getElementById('p-av-photo');
  const url = cfg.avatarUrl;
  if (!initEl || !imgEl) return;
  if (url) {
    imgEl.src = url; imgEl.style.display = 'block';
    initEl.style.display = 'none';
  } else {
    initEl.textContent = feedInitials(cfg.name || 'Motorista');
    initEl.style.background = perfilAvatarColor();
    initEl.style.display = 'flex';
    imgEl.style.display = 'none';
  }
  const removeOpt = document.getElementById('p-remove-foto-opt');
  if (removeOpt) removeOpt.style.display = url ? 'flex' : 'none';
}

async function perfilLoadStats() {
  const setStat = (id, v) => { const el = document.getElementById(id); if (el) el.textContent = (v == null ? '—' : v); };
  if (!_SUPA || !_supaUser) return;
  try {
    const [tripsRes, autoRes, postsRes] = await Promise.all([
      _SUPA.from('trips').select('id', { count:'exact', head:true }).eq('user_id', _supaUser.id),
      _SUPA.from('auto_trips').select('id', { count:'exact', head:true }).eq('user_id', _supaUser.id),
      _SUPA.from('feed_posts').select('votes_up').eq('user_id', _supaUser.id)
    ]);
    const corridas = (tripsRes.count || 0) + (autoRes.count || 0);
    const posts = postsRes.data || [];
    const conf = posts.reduce((s,p) => s + (p.votes_up || 0), 0);
    setStat('p-stat-corridas', corridas);
    setStat('p-stat-alertas', posts.length);
    setStat('p-stat-conf', conf);
  } catch (e) { console.warn('perfil stats', e); }
}

function openPerfil() {
  const cfg = DB.cfg();
  if (cfg.name) {const el=document.getElementById('p-nome');if(el)el.value=cfg.name;}
  if (cfg.plate) {const el=document.getElementById('p-placa');if(el)el.value=cfg.plate;}
  const cidEl = document.getElementById('p-cidade'); if (cidEl) cidEl.value = cfg.city || '';
  const vTxt = document.getElementById('p-veiculo-txt');
  if (vTxt) vTxt.textContent = cfg.vehicle || 'Selecionar veículo';

  const nameEl = document.getElementById('p-av-name');
  if (nameEl) nameEl.textContent = cfg.name || 'Motorista MōB';
  const MESES_CURTO = ['jan','fev','mar','abr','mai','jun','jul','ago','set','out','nov','dez'];
  const created = (_supaUser && _supaUser.created_at) ? new Date(_supaUser.created_at) : null;
  const memberSince = created ? MESES_CURTO[created.getMonth()]+'/'+created.getFullYear() : '—';
  const subEl = document.getElementById('p-av-sub');
  if (subEl) subEl.textContent = (cfg.city ? cfg.city+' · ' : '') + 'na MōB desde '+memberSince;
  perfilRenderAvatar();
  perfilLoadStats();

  const emailEl = document.getElementById('acc-email');
  if (emailEl) emailEl.textContent = (_supaUser && _supaUser.email) || '—';
  // Plano: ainda nao ha integracao de pagamento real, todo mundo aparece como Free por enquanto.
  const plan = cfg.plan || 'free';
  const badgeEl = document.getElementById('acc-badge');
  const upgradeBtn = document.getElementById('acc-upgrade-btn');
  const expEl = document.getElementById('acc-exp');
  if (badgeEl) { badgeEl.textContent = plan === 'pro' ? 'Pro' : 'Free'; badgeEl.classList.toggle('pro', plan === 'pro'); }
  if (upgradeBtn) upgradeBtn.style.display = plan === 'pro' ? 'none' : 'block';
  if (expEl) {
    if (plan === 'pro' && cfg.planExpiresAt) { expEl.style.display='block'; expEl.textContent = 'Renova em ' + new Date(cfg.planExpiresAt).toLocaleDateString('pt-BR'); }
    else expEl.style.display = 'none';
  }

  openMo('mo-perfil');
}
function openUpgrade() {
  // Placeholder — ainda sem fluxo de pagamento real, so o botao por enquanto.
  showToast('Em breve você poderá assinar o MōB Pro por aqui.', 'info');
}
function openConfig() {
  const cfg = DB.cfg();
  const ovEl = document.getElementById('cfg-overlay');
  if (ovEl) ovEl.checked = cfg.overlayEnabled !== false;
  const darkEl = document.getElementById('cfg-dark');
  if (darkEl) darkEl.checked = !!cfg.darkMode;

  const pending = DB.syncQueue().length;
  const dot = document.getElementById('cfg-sync-dot');
  const sub = document.getElementById('cfg-sync-sub');
  if (dot) dot.classList.toggle('pending', pending > 0);
  if (sub) sub.textContent = pending > 0 ? (pending+' lançamento'+(pending!==1?'s':'')+' aguardando conexão') : 'Tudo sincronizado';

  const verEl = document.getElementById('cfg-version');
  if (verEl) verEl.textContent = 'MōB · v' + (window.SmartMobiNative?.getVersion?.() || '—');

  openMo('mo-config');
}
function toggleDarkMode(el) {
  const cfg = DB.cfg();
  cfg.darkMode = el.checked;
  DB.saveCfg(cfg);
  applyDarkMode(cfg.darkMode);
}
function applyDarkMode(on) {
  document.documentElement.classList.toggle('dark', !!on);
}
function toggleOverlayPref(el) {
  const cfg = DB.cfg();
  cfg.overlayEnabled = el.checked;
  DB.saveCfg(cfg);
  if (!window.SmartMobiNative) return;
  try {
    if (el.checked) {
      // Se a jornada já estiver rolando, mostra o ícone na hora, sem esperar o próximo sync.
      if (S.gps.st === 'running') {
        SmartMobiNative.startFloating?.(S.gps.start, S.gps.km || 0);
        SmartMobiNative.updateFloatingStatus?.(S.gps.st);
      }
    } else {
      SmartMobiNative.hideFloatingOnly?.();
    }
  } catch(e) {}
}
async function savePerfil() {
  const cfg = DB.cfg();
  cfg.name    = document.getElementById('p-nome')?.value || '';
  cfg.city    = document.getElementById('p-cidade')?.value || '';
  cfg.plate   = document.getElementById('p-placa')?.value || '';

  DB.saveCfg(cfg);
  // Grava na nuvem também — antes esta função só salvava local, então uma
  // edição de nome nunca chegava em profiles.name, e o feed (que lê de lá
  // pros posts de outros usuários) continuava mostrando o nome antigo.
  if (_SUPA && _supaUser) {
    try {
      await _SUPA.from('profiles').upsert(
        { id:_supaUser.id, name:cfg.name, city:cfg.city, plate:cfg.plate, vehicle:cfg.vehicle||null },
        { onConflict:'id' }
      );
    } catch(e) { console.warn('perfil upsert', e); }
  }
  closeMo('mo-perfil');
  renderMais();
  if (S.sec==='relatorios') renderHome();
  showToast('Perfil salvo!', 'success');
}

// ── Foto de perfil ────────────────────────────────────────────
function perfilOpenPhotoSheet() { openMo('mo-perfil-foto'); }
function perfilPickCamera()  { closeMo('mo-perfil-foto'); document.getElementById('p-file-camera')?.click(); }
function perfilPickGallery() { closeMo('mo-perfil-foto'); document.getElementById('p-file-gallery')?.click(); }

// Corta em quadrado (cover, centralizado) e comprime antes de subir —
// evita gastar dados do motorista com foto de câmera em resolução total.
function perfilResizeImage(file, size, quality) {
  size = size || 480; quality = quality || 0.85;
  return new Promise((resolve, reject) => {
    const img = new Image();
    const url = URL.createObjectURL(file);
    img.onload = () => {
      URL.revokeObjectURL(url);
      const side = Math.min(img.width, img.height);
      const sx = (img.width - side) / 2, sy = (img.height - side) / 2;
      const canvas = document.createElement('canvas');
      canvas.width = size; canvas.height = size;
      const ctx = canvas.getContext('2d');
      ctx.drawImage(img, sx, sy, side, side, 0, 0, size, size);
      canvas.toBlob(blob => blob ? resolve(blob) : reject(new Error('toBlob falhou')), 'image/jpeg', quality);
    };
    img.onerror = () => { URL.revokeObjectURL(url); reject(new Error('Imagem inválida')); };
    img.src = url;
  });
}

async function perfilFileSelected(inputEl) {
  const file = inputEl.files && inputEl.files[0];
  inputEl.value = ''; // permite selecionar o mesmo arquivo de novo depois
  if (!file) return;
  if (!file.type || !file.type.startsWith('image/')) { showToast('Selecione uma imagem.'); return; }
  await perfilUploadAvatar(file);
}

async function perfilUploadAvatar(file) {
  if (!_SUPA || !_supaUser) return;
  showToast('Enviando foto…', 'info');
  try {
    const blob = await perfilResizeImage(file, 480, 0.85);
    const path = _supaUser.id + '/avatar.jpg';
    const { error: upErr } = await _SUPA.storage.from('avatars').upload(path, blob, { upsert: true, contentType: 'image/jpeg' });
    if (upErr) throw upErr;
    const { data: pub } = _SUPA.storage.from('avatars').getPublicUrl(path);
    const url = pub.publicUrl + '?t=' + Date.now(); // cache-busting — mesmo path, arquivo novo
    await _SUPA.from('profiles').upsert({ id:_supaUser.id, avatar_url:url }, { onConflict:'id' });
    const cfg = DB.cfg(); cfg.avatarUrl = url; DB.saveCfg(cfg);
    perfilRenderAvatar();
    showToast('Foto atualizada!', 'success');
  } catch (e) {
    console.warn('avatar upload', e);
    showToast('Erro ao enviar a foto. Tente de novo.');
  }
}

async function perfilRemovePhoto() {
  closeMo('mo-perfil-foto');
  const cfg = DB.cfg();
  if (!cfg.avatarUrl) return;
  if (_SUPA && _supaUser) {
    try {
      await _SUPA.storage.from('avatars').remove([_supaUser.id + '/avatar.jpg']);
      await _SUPA.from('profiles').upsert({ id:_supaUser.id, avatar_url:null }, { onConflict:'id' });
    } catch (e) { console.warn('remove avatar', e); }
  }
  cfg.avatarUrl = null; DB.saveCfg(cfg);
  perfilRenderAvatar();
  showToast('Foto removida.');
}

// ── SELETOR DE VEÍCULO (marca → modelo → ano) ────────────────────
const VEHICLE_BRANDS = {
  'Chevrolet': ['Onix','Onix Plus','Prisma','Spin','Tracker','Joy','Cobalt'],
  'Fiat': ['Mobi','Argo','Cronos','Pulse','Strada','Uno','Siena'],
  'Volkswagen': ['Gol','Voyage','Polo','Virtus','T-Cross','Nivus','Saveiro','Up!'],
  'Hyundai': ['HB20','HB20S','Creta','Tucson','i30'],
  'Toyota': ['Etios','Yaris','Corolla','Corolla Cross','SW4'],
  'Honda': ['Fit','City','HR-V','Civic','WR-V'],
  'Renault': ['Kwid','Sandero','Logan','Duster','Stepway'],
  'Nissan': ['March','Versa','Kicks','Sentra'],
  'Ford': ['Ka','Ka Sedan','EcoSport','Fiesta'],
  'Jeep': ['Renegade','Compass'],
  'Citroën': ['C3','C4 Cactus'],
  'Peugeot': ['208','2008'],
  'Mitsubishi': ['Space Star','ASX'],
  'Kia': ['Picanto','Soul','Sportage'],
  'BYD': ['Dolphin Mini','Dolphin','Yuan Plus','Song Plus','Seal'],
  'GWM': ['Haval H6'],
  'Caoa Chery': ['Tiggo 2','Tiggo 3X','Tiggo 5X'],
  'Outra marca': []
};
function vpPopulateMarcas() {
  const sel = document.getElementById('vp-marca');
  if (!sel) return;
  sel.innerHTML = Object.keys(VEHICLE_BRANDS).map(b => `<option value="${b}">${b}</option>`).join('');
}
function vpPopulateAnos() {
  const sel = document.getElementById('vp-ano');
  if (!sel) return;
  const max = new Date().getFullYear() + 1;
  let opts = '';
  for (let y = max; y >= 1995; y--) opts += `<option value="${y}">${y}</option>`;
  sel.innerHTML = opts;
}
function vpOnMarcaChange() {
  const marca = document.getElementById('vp-marca')?.value;
  const outraWrap = document.getElementById('vp-marca-outra-wrap');
  if (outraWrap) outraWrap.style.display = marca === 'Outra marca' ? 'block' : 'none';
  const modelos = VEHICLE_BRANDS[marca] || [];
  const modSel = document.getElementById('vp-modelo');
  if (modSel) modSel.innerHTML = modelos.map(m =>
===== auto_trips @ 1753113 =====
).filter(t=>{
      try{ const ts=typeof autoTripTs==='function'?autoTripTs(t):Date.parse(t.trip_started_at||t.accepted_at||'')||0; return ts&&ts>=startMs; }
      catch(e){return false;}
    });
    return {manual,auto};
  }

  function mergedTodaySegments(){
    const live = (S && S.gps && S.gps.st==='running') ? nativeStatusTimeline() : [];
    const sid = S&&S.gps ? S.gps.sessionId : null;
    const cloud = (statusSegmentsDate===S.curDate ? statusSegments : []).filter(x=>!sid || x.session_id!==sid);
    return cloud.concat(live);
  }

  function renderStatusTimeline(){
    try{
      if(!S||!DB||S.sec!=='jornada') return;
      ensureNativeJourneySession();
      if(S.curDate && statusSegmentsDate!==S.curDate) loadStatusSegments(S.curDate);

      const day=DB.day(S.curDate||DB.today());
      const isToday=S.curDate===DB.today();
      const live=isToday&&S.gps&&S.gps.st==='running'&&S.gps.start;

      // Card verde: somente a Jornada atual.
      if(live){
        const elapsed=Math.max(0,typeof getElapsed==='function'?getElapsed():0);
        const km=Math.max(0,Number(S.gps.km)||0);
        const trips=currentSessionTrips(day,Number(S.gps.start)||0);
        const ganhos=trips.manual.reduce((a,t)=>a+(Number(t.value)||0),0)+trips.auto.reduce((a,t)=>a+(Number(t.offer_value)||0),0);
        if(typeof setText==='function'){
          setText('jc-trips2',trips.manual.length+trips.auto.length);
          setText('jc-rsh2',elapsed>0?DB.fRI(ganhos/(elapsed/3600)):'R$0,00');
          setText('jc-rskm2',km>0?DB.fRI(ganhos/km):'R$0,00');
        }
        const native=nativeStatusTimeline();
        const bd=breakdownFromSegments(elapsed,km,native,S.gps.sessionId);
        const jc=document.getElementById('jc-breakdown');
        if(jc){ jc.style.display=''; setHTML('jc-bar',buildSbarHtml(bd)); setHTML('jc-blist',buildSrowsHtml(bd)); }
      }

      // Total do dia: soma trechos persistidos de TODAS as jornadas + trecho vivo.
      const finished=(day.sessions||[]);
      const totalElapsed=finished.reduce((a,x)=>a+(x.elapsed||0),0)+(live?getElapsed():0)+(day.tempoAdjust||0);
      const totalKm=finished.reduce((a,x)=>a+(x.km||0),0)+(live?(Number(S.gps.km)||0):0)+(day.kmAdjust||0);
      const td=document.getElementById('td-bd-row');
      if(td&&totalElapsed>0){
        const bd=breakdownFromSegments(totalElapsed,totalKm,mergedTodaySegments(),live?S.gps.sessionId:null);
        td.style.display=''; setHTML('td-bd-bar',buildSbarHtml(bd)); setHTML('td-bd-list',buildSrowsHtml(bd));
      }
    }catch(e){ console.warn('[JourneyStatusTimeline]',e); }
  }
  // Compatibilidade apenas: V3 abaixo é o único renderizador automático.
  // Expõe somente o vínculo da sessão nativa para o V3 poder garantir que
  // uma Jornada recém-iniciada comece a gravar Online/Buscar/Corrida mesmo
  // sem depender do renderizador legado da barra.
  window.ensureNativeJourneySession=ensureNativeJourneySession;
  window.renderStatusTimeline=renderStatusTimeline;
})();
</script>



<!-- JOURNEY_CONTINUOUS_OPERATIONAL_V3 -->
<script>
(function(){
  // ── Datas e dias operacionais ───────────────────────────────────────
  window.localYmdFromMs = function(ms){
    const d = new Date(Number(ms)||Date.now());
    return d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
  };
  window.normalizeOperationalPlatform = function(p){
    return String(p||'uber').toLowerCase() === '99' ? '99' : 'uber';
  };
  window.civilDayBounds = function(dateStr){
    const start = new Date(dateStr+'T00:00:00');
    const end = new Date(start); end.setDate(end.getDate()+1);
    return {startMs:start.getTime(), endMs:end.getTime()};
  };
  window.operationalRangeForPlatform = function(plat,dateStr){
    const p = normalizeOperationalPlatform(plat);
    const start = new Date(dateStr+'T00:00:00');
    if(p === 'uber') start.setHours(4,0,0,0);
    const end = new Date(start); end.setDate(end.getDate()+1);
    return {platform:p, operationalDate:dateStr, startMs:start.getTime(), endMs:end.getTime()};
  };
  window.operationalDateForTimestamp = function(plat,ts){
    const p = normalizeOperationalPlatform(plat);
    const d = new Date(Number(ts));
    // Uber: tudo de 00:00 até 03:59 ainda pertence ao dia operacional anterior.
    if(p === 'uber' && d.getHours() < 4) d.setDate(d.getDate()-1);
    return localYmdFromMs(d.getTime());
  };
  window.operationalTimestampForTime = function(plat,dateStr,hhmm){
    const p = normalizeOperationalPlatform(plat);
    const parts = String(hhmm||'00:00').split(':').map(Number);
    const h = Number.isFinite(parts[0]) ? parts[0] : 0;
    const m = Number.isFinite(parts[1]) ? parts[1] : 0;
    const d = new Date(dateStr+'T00:00:00');
    d.setHours(h,m,0,0);
    if(p === 'uber' && h < 4) d.setDate(d.getDate()+1);
    return d.getTime();
  };
  window.loadAutoTripsForOperationalDay = async function(plat,dateStr){
    if(!_supaUser) return [];
    const r = operationalRangeForPlatform(plat,dateStr);
    let q = _SUPA.from('auto_trips').select('*')
      .eq('user_id',_supaUser.id)
      .eq('platform',normalizeOperationalPlatform(plat))
      .gte('trip_started_at',new Date(r.startMs).toISOString())
      .lt('trip_started_at',new Date(r.endMs).toISOString())
      .order('trip_started_at',{ascending:true});
    const {data,error} = await q;
    return error ? [] : (data||[]);
  };

  // ── Filtro da tela Corridas ────────────────────────────────────────
  const _civilGetCorridasDays = window.getCorridasDays;
  window.getCorridasDays = function(){
    const plats = S.corridasPlats || new Set();
    const onePlat = plats.size === 1 ? [...plats][0] : null;
    if(!onePlat) return _civilGetCorridasDays();
    const anchorStr = operationalDateForTimestamp(onePlat,Date.now());
    const anchor = new Date(anchorStr+'T12:00:00');
    const fmt = d => localYmdFromMs(d.getTime());
    const p = S.corridasPeriod;
    if(p==='date' && S.corridasDate) return [S.corridasDate];
    if(p==='hoje') return [anchorStr];
    if(p==='7d') { const out=[]; for(let i=0;i<7;i++){const d=new Date(anchor);d.setDate(d.getDate()-i);out.push(fmt(d));} return out; }
    if(p==='mes') {
      const y=anchor.getFullYear(), m=anchor.getMonth();
      const dim=new Date(y,m+1,0).getDate(), out=[];
      for(let day=1;day<=dim;day++) out.push(y+'-'+p2(m+1)+'-'+p2(day));
      return out;
    }
    return _civilGetCorridasDays();
  };
  window.autoCorridasDateRange = function(){
    const dates = getCorridasDays().slice().sort();
    if(!dates.length) return null;
    const plats = S.corridasPlats || new Set();
    const onePlat = plats.size===1 ? [...plats][0] : null;
    if(onePlat){
      const a=operationalRangeForPlatform(onePlat,dates[0]);
      const b=operationalRangeForPlatform(onePlat,dates[dates.length-1]);
      return {from:new Date(a.startMs).toISOString(),to:new Date(b.endMs-1).toISOString()};
    }
    const a=civilDayBounds(dates[0]), b=civilDayBounds(dates[dates.length-1]);
    return {from:new Date(a.startMs).toISOString(),to:new Date(b.endMs-1).toISOString()};
  };

  // ── Jornada contínua: nunca mais pergunta/divide à meia-noite ─────
  window.checkMidnightCrossing = function(){ return; };
  window.openMidnightModal = function(){ S._midnightModalOpen=false; try{closeMo('mo-midnight');}catch(e){} };
  window.resolveMidnightSplit = async function(){ S._midnightModalOpen=false; try{closeMo('mo-midnight');}catch(e){} };
  window.splitJourneyAtMidnight = async function(){ return; };
  window.openPostStopMidnightModal = function(sess,onDone){
    S._midnightIsPostStop=false; S._postStopMidnightSess=null; S._postMidnightCallback=null; S._midnightModalOpen=false;
    try{closeMo('mo-midnight');}catch(e){}
    if(typeof onDone==='function') onDone();
  };
  window.resolvePostStopMidnight = async function(){ S._midnightIsPostStop=false; S._midnightModalOpen=false; try{closeMo('mo-midnight');}catch(e){} };
  try { if(S && S.gps) S.gps.forceDate = null; } catch(e) {}

  // ── Corridas de uma Jornada: busca por INTERVALO REAL, não por data ──
  const journeyAutoCache = new Map();
  const journeyStatusCache = new Map();
  const overlapSessionCache = new Map();
  const civilStatusCache = new Map();

  function journeyKey(sess){ return String(sess?.id || ((sess?.start||0)+'_'+(sess?.end||'live'))); }
  function sessionEnd(sess){ return Number(sess?.end || ((S?.gps?.sessionId && sess?.id===S.gps.sessionId) ? Date.now() : Date.now())); }
  function overlaps(start,end,a,b){ return Number(start||0) < b && Number(end||Date.now()) >= a; }

  async function ensureJourneyAutoTrips(sess){
    if(!_supaUser || !sess?.start) return;
    const k=journeyKey(sess), old=journeyAutoCache.get(k);
    const ttl=sess.end?120000:12000;
    if(old && (old.loading || Date.now()-old.ts<ttl)) return;
    journeyAutoCache.set(k,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    try{
      const end=sessionEnd(sess);
      const {data,error}=await _SUPA.from('auto_trips').select('*')
        .eq('user_id',_supaUser.id)
        .gte('trip_started_at',new Date(Number(sess.start)).toISOString())
        .lte('trip_started_at',new Date(end).toISOString())
        .order('trip_started_at',{ascending:true});
      if(!error) journeyAutoCache.set(k,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){ journeyAutoCache.set(k,{loading:false,ts:Date.now(),rows:old?.rows||[]}); }
    try{ if(S.sec==='jornada') setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0); }catch(e){}
  }
  function journeyAutoTrips(sess,fallback){
    const k=journeyKey(sess), c=journeyAutoCache.get(k);
    ensureJourneyAutoTrips(sess);
    const rows=(c&&Array.isArray(c.rows)&&c.rows.length)?c.rows:(fallback||[]);
    const a=Number(sess?.start||0), b=sessionEnd(sess);
    const seen=new Set();
    return rows.filter(t=>{const ts=autoTripTs(t); if(!ts||ts<a||ts>b)return false; const id=t.id||ts+'_'+t.offer_value; if(seen.has(id))return false; seen.add(id); return true;});
  }
  function journeyManualTrips(sess){
    const a=Number(sess?.start||0), b=sessionEnd(sess), out=[], seen=new Set();
    (DB.allDates?DB.allDates():[]).forEach(ds=>{
      const d=DB.day(ds);
      (d.trips||[]).forEach(t=>{
        let ts=Number(t.ts)||0;
        if(!ts&&t.time) ts=new Date(ds+'T'+t.time+':00').getTime();
        if(!ts||ts<a||ts>b)return;
        const id=t.id||ts+'_'+(t.value||0); if(seen.has(id))return; seen.add(id);
        out.push(Object.assign({},t,{ts,_ts:ts}));
      });
    });
    return out;
  }

  // ── Timeline real Online/Buscar/Corrida por Jornada ────────────────
  async function ensureJourneyStatus(sess){
    if(!_supaUser || !sess?.id) return;
    const k=journeyKey(sess), old=journeyStatusCache.get(k), ttl=sess.end?120000:8000;
    if(old && (old.loading || Date.now()-old.ts<ttl)) return;
    journeyStatusCache.set(k,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    try{
      const {data,error}=await _SUPA.from('journey_status_segments').select('*')
        .eq('user_id',_supaUser.id).eq('session_id',sess.id).order('start_ms',{ascending:true});
      if(!error) journeyStatusCache.set(k,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){ journeyStatusCache.set(k,{loading:false,ts:Date.now(),rows:old?.rows||[]}); }
    try{ if(S.sec==='jornada') setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0); }catch(e){}
  }
  function statusBreakdownRows(rows,elapsedSec,kmTotal,clipStart,clipEnd,liveSessionId){
    const out={total:Math.max(0,Number(elapsedSec)||0),online:{sec:0,km:0,pct:0},buscando:{sec:0,km:0,pct:0},corrida:{sec:0,km:0,pct:0}};
    (rows||[]).forEach(seg=>{
      const key=seg.status==='buscar'?'buscando':seg.status; if(!out[key])return;
      const rawStart=Number(seg.start_ms)||0;
      const isLive=!!liveSessionId&&seg.session_id===liveSessionId&&seg.end_ms==null;
      const rawEnd=Number(seg.end_ms||(isLive?Date.now():seg.last_seen_ms)||rawStart);
      if(rawEnd<=rawStart)return;
      const a=Math.max(rawStart,clipStart==null?rawStart:clipStart);
      const b=Math.min(rawEnd,clipEnd==null?rawEnd:clipEnd);
      if(b<=a)return;
      const sec=(b-a)/1000;
      let sk=Number(seg.start_km)||0;
      let ek=Number(seg.end_km!=null?seg.end_km:(isLive&&S?.gps?S.gps.km:seg.last_km))||sk;
      const frac=(b-a)/(rawEnd-rawStart);
      const km=Math.max(0,(ek-sk)*frac);
      out[key].sec+=sec; out[key].km+=km;
    });
    const tracked=out.online.sec+out.buscando.sec+out.corrida.sec;
    const trackedKm=out.online.km+out.buscando.km+out.corrida.km;
    if(out.total<=0) out.total=tracked;
    else {
      out.online.sec+=Math.max(0,out.total-tracked);
      out.online.km+=Math.max(0,(Number(kmTotal)||0)-trackedKm);
    }
    const pct=v=>out.total>0?Math.round(Math.max(0,v)/out.total*100):0;
    out.online.pct=pct(out.online.sec);out.buscando.pct=pct(out.buscando.sec);out.corrida.pct=pct(out.corrida.sec);
    return out;
  }
  window.journeyBreakdownForSession = function(sess,elapsedSec,kmTotal,fallbackAuto){
    if(!sess?.id) return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
    ensureJourneyStatus(sess);
    const c=journeyStatusCache.get(journeyKey(sess));
    if(c&&c.rows&&c.rows.length) return statusBreakdownRows(c.rows,elapsedSec,kmTotal,null,null,(S?.gps?.st==='running'?S.gps.sessionId:null));
    // Jornada ativa nunca some enquanto a timeline chega: sem trechos ainda,
    // todo o tempo/km conhecido fica como Online e depois é redistribuído
    // quando Buscar/Corrida forem registrados.
    if(!c || c.loading) return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
    return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
  };

  // ── Jornadas que atravessam a data selecionada ────────────────────
  async function ensureOverlapSessions(dateStr){
    if(!_supaUser||!dateStr)return;
    const old=overlapSessionCache.get(dateStr);
    if(old&&(old.loading||Date.now()-old.ts<30000))return;
    overlapSessionCache.set(dateStr,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    const b=civilDayBounds(dateStr);
    try{
      const {data,error}=await _SUPA.from('sessions').select('*').eq('user_id',_supaUser.id)
        .lt('start_ms',b.endMs).or(`end_ms.is.null,end_ms.gte.${b.startMs}`).order('start_ms',{ascending:true});
      if(!error){
        const rows=data||[];
        overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows});
        // Hidratamos o cache local no DIA DE INÍCIO; nunca criamos uma segunda Jornada na virada.
        rows.filter(r=>r.end_ms).forEach(r=>{
          const ds=localYmdFromMs(r.start_ms), day=DB.day(ds); if(!day.sessions)day.sessions=[];
          const item={id:r.id,start:Number(r.start_ms),end:Number(r.end_ms),km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0};
          const i=day.sessions.findIndex(x=>x.id===item.id); if(i>=0)day.sessions[i]=item; else day.sessions.push(item); DB.saveDay(day);
        });
      }
    }catch(e){
===== auto_trips @ 1757075 =====
nalDate:dateStr, startMs:start.getTime(), endMs:end.getTime()};
  };
  window.operationalDateForTimestamp = function(plat,ts){
    const p = normalizeOperationalPlatform(plat);
    const d = new Date(Number(ts));
    // Uber: tudo de 00:00 até 03:59 ainda pertence ao dia operacional anterior.
    if(p === 'uber' && d.getHours() < 4) d.setDate(d.getDate()-1);
    return localYmdFromMs(d.getTime());
  };
  window.operationalTimestampForTime = function(plat,dateStr,hhmm){
    const p = normalizeOperationalPlatform(plat);
    const parts = String(hhmm||'00:00').split(':').map(Number);
    const h = Number.isFinite(parts[0]) ? parts[0] : 0;
    const m = Number.isFinite(parts[1]) ? parts[1] : 0;
    const d = new Date(dateStr+'T00:00:00');
    d.setHours(h,m,0,0);
    if(p === 'uber' && h < 4) d.setDate(d.getDate()+1);
    return d.getTime();
  };
  window.loadAutoTripsForOperationalDay = async function(plat,dateStr){
    if(!_supaUser) return [];
    const r = operationalRangeForPlatform(plat,dateStr);
    let q = _SUPA.from('auto_trips').select('*')
      .eq('user_id',_supaUser.id)
      .eq('platform',normalizeOperationalPlatform(plat))
      .gte('trip_started_at',new Date(r.startMs).toISOString())
      .lt('trip_started_at',new Date(r.endMs).toISOString())
      .order('trip_started_at',{ascending:true});
    const {data,error} = await q;
    return error ? [] : (data||[]);
  };

  // ── Filtro da tela Corridas ────────────────────────────────────────
  const _civilGetCorridasDays = window.getCorridasDays;
  window.getCorridasDays = function(){
    const plats = S.corridasPlats || new Set();
    const onePlat = plats.size === 1 ? [...plats][0] : null;
    if(!onePlat) return _civilGetCorridasDays();
    const anchorStr = operationalDateForTimestamp(onePlat,Date.now());
    const anchor = new Date(anchorStr+'T12:00:00');
    const fmt = d => localYmdFromMs(d.getTime());
    const p = S.corridasPeriod;
    if(p==='date' && S.corridasDate) return [S.corridasDate];
    if(p==='hoje') return [anchorStr];
    if(p==='7d') { const out=[]; for(let i=0;i<7;i++){const d=new Date(anchor);d.setDate(d.getDate()-i);out.push(fmt(d));} return out; }
    if(p==='mes') {
      const y=anchor.getFullYear(), m=anchor.getMonth();
      const dim=new Date(y,m+1,0).getDate(), out=[];
      for(let day=1;day<=dim;day++) out.push(y+'-'+p2(m+1)+'-'+p2(day));
      return out;
    }
    return _civilGetCorridasDays();
  };
  window.autoCorridasDateRange = function(){
    const dates = getCorridasDays().slice().sort();
    if(!dates.length) return null;
    const plats = S.corridasPlats || new Set();
    const onePlat = plats.size===1 ? [...plats][0] : null;
    if(onePlat){
      const a=operationalRangeForPlatform(onePlat,dates[0]);
      const b=operationalRangeForPlatform(onePlat,dates[dates.length-1]);
      return {from:new Date(a.startMs).toISOString(),to:new Date(b.endMs-1).toISOString()};
    }
    const a=civilDayBounds(dates[0]), b=civilDayBounds(dates[dates.length-1]);
    return {from:new Date(a.startMs).toISOString(),to:new Date(b.endMs-1).toISOString()};
  };

  // ── Jornada contínua: nunca mais pergunta/divide à meia-noite ─────
  window.checkMidnightCrossing = function(){ return; };
  window.openMidnightModal = function(){ S._midnightModalOpen=false; try{closeMo('mo-midnight');}catch(e){} };
  window.resolveMidnightSplit = async function(){ S._midnightModalOpen=false; try{closeMo('mo-midnight');}catch(e){} };
  window.splitJourneyAtMidnight = async function(){ return; };
  window.openPostStopMidnightModal = function(sess,onDone){
    S._midnightIsPostStop=false; S._postStopMidnightSess=null; S._postMidnightCallback=null; S._midnightModalOpen=false;
    try{closeMo('mo-midnight');}catch(e){}
    if(typeof onDone==='function') onDone();
  };
  window.resolvePostStopMidnight = async function(){ S._midnightIsPostStop=false; S._midnightModalOpen=false; try{closeMo('mo-midnight');}catch(e){} };
  try { if(S && S.gps) S.gps.forceDate = null; } catch(e) {}

  // ── Corridas de uma Jornada: busca por INTERVALO REAL, não por data ──
  const journeyAutoCache = new Map();
  const journeyStatusCache = new Map();
  const overlapSessionCache = new Map();
  const civilStatusCache = new Map();

  function journeyKey(sess){ return String(sess?.id || ((sess?.start||0)+'_'+(sess?.end||'live'))); }
  function sessionEnd(sess){ return Number(sess?.end || ((S?.gps?.sessionId && sess?.id===S.gps.sessionId) ? Date.now() : Date.now())); }
  function overlaps(start,end,a,b){ return Number(start||0) < b && Number(end||Date.now()) >= a; }

  async function ensureJourneyAutoTrips(sess){
    if(!_supaUser || !sess?.start) return;
    const k=journeyKey(sess), old=journeyAutoCache.get(k);
    const ttl=sess.end?120000:12000;
    if(old && (old.loading || Date.now()-old.ts<ttl)) return;
    journeyAutoCache.set(k,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    try{
      const end=sessionEnd(sess);
      const {data,error}=await _SUPA.from('auto_trips').select('*')
        .eq('user_id',_supaUser.id)
        .gte('trip_started_at',new Date(Number(sess.start)).toISOString())
        .lte('trip_started_at',new Date(end).toISOString())
        .order('trip_started_at',{ascending:true});
      if(!error) journeyAutoCache.set(k,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){ journeyAutoCache.set(k,{loading:false,ts:Date.now(),rows:old?.rows||[]}); }
    try{ if(S.sec==='jornada') setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0); }catch(e){}
  }
  function journeyAutoTrips(sess,fallback){
    const k=journeyKey(sess), c=journeyAutoCache.get(k);
    ensureJourneyAutoTrips(sess);
    const rows=(c&&Array.isArray(c.rows)&&c.rows.length)?c.rows:(fallback||[]);
    const a=Number(sess?.start||0), b=sessionEnd(sess);
    const seen=new Set();
    return rows.filter(t=>{const ts=autoTripTs(t); if(!ts||ts<a||ts>b)return false; const id=t.id||ts+'_'+t.offer_value; if(seen.has(id))return false; seen.add(id); return true;});
  }
  function journeyManualTrips(sess){
    const a=Number(sess?.start||0), b=sessionEnd(sess), out=[], seen=new Set();
    (DB.allDates?DB.allDates():[]).forEach(ds=>{
      const d=DB.day(ds);
      (d.trips||[]).forEach(t=>{
        let ts=Number(t.ts)||0;
        if(!ts&&t.time) ts=new Date(ds+'T'+t.time+':00').getTime();
        if(!ts||ts<a||ts>b)return;
        const id=t.id||ts+'_'+(t.value||0); if(seen.has(id))return; seen.add(id);
        out.push(Object.assign({},t,{ts,_ts:ts}));
      });
    });
    return out;
  }

  // ── Timeline real Online/Buscar/Corrida por Jornada ────────────────
  async function ensureJourneyStatus(sess){
    if(!_supaUser || !sess?.id) return;
    const k=journeyKey(sess), old=journeyStatusCache.get(k), ttl=sess.end?120000:8000;
    if(old && (old.loading || Date.now()-old.ts<ttl)) return;
    journeyStatusCache.set(k,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    try{
      const {data,error}=await _SUPA.from('journey_status_segments').select('*')
        .eq('user_id',_supaUser.id).eq('session_id',sess.id).order('start_ms',{ascending:true});
      if(!error) journeyStatusCache.set(k,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){ journeyStatusCache.set(k,{loading:false,ts:Date.now(),rows:old?.rows||[]}); }
    try{ if(S.sec==='jornada') setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0); }catch(e){}
  }
  function statusBreakdownRows(rows,elapsedSec,kmTotal,clipStart,clipEnd,liveSessionId){
    const out={total:Math.max(0,Number(elapsedSec)||0),online:{sec:0,km:0,pct:0},buscando:{sec:0,km:0,pct:0},corrida:{sec:0,km:0,pct:0}};
    (rows||[]).forEach(seg=>{
      const key=seg.status==='buscar'?'buscando':seg.status; if(!out[key])return;
      const rawStart=Number(seg.start_ms)||0;
      const isLive=!!liveSessionId&&seg.session_id===liveSessionId&&seg.end_ms==null;
      const rawEnd=Number(seg.end_ms||(isLive?Date.now():seg.last_seen_ms)||rawStart);
      if(rawEnd<=rawStart)return;
      const a=Math.max(rawStart,clipStart==null?rawStart:clipStart);
      const b=Math.min(rawEnd,clipEnd==null?rawEnd:clipEnd);
      if(b<=a)return;
      const sec=(b-a)/1000;
      let sk=Number(seg.start_km)||0;
      let ek=Number(seg.end_km!=null?seg.end_km:(isLive&&S?.gps?S.gps.km:seg.last_km))||sk;
      const frac=(b-a)/(rawEnd-rawStart);
      const km=Math.max(0,(ek-sk)*frac);
      out[key].sec+=sec; out[key].km+=km;
    });
    const tracked=out.online.sec+out.buscando.sec+out.corrida.sec;
    const trackedKm=out.online.km+out.buscando.km+out.corrida.km;
    if(out.total<=0) out.total=tracked;
    else {
      out.online.sec+=Math.max(0,out.total-tracked);
      out.online.km+=Math.max(0,(Number(kmTotal)||0)-trackedKm);
    }
    const pct=v=>out.total>0?Math.round(Math.max(0,v)/out.total*100):0;
    out.online.pct=pct(out.online.sec);out.buscando.pct=pct(out.buscando.sec);out.corrida.pct=pct(out.corrida.sec);
    return out;
  }
  window.journeyBreakdownForSession = function(sess,elapsedSec,kmTotal,fallbackAuto){
    if(!sess?.id) return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
    ensureJourneyStatus(sess);
    const c=journeyStatusCache.get(journeyKey(sess));
    if(c&&c.rows&&c.rows.length) return statusBreakdownRows(c.rows,elapsedSec,kmTotal,null,null,(S?.gps?.st==='running'?S.gps.sessionId:null));
    // Jornada ativa nunca some enquanto a timeline chega: sem trechos ainda,
    // todo o tempo/km conhecido fica como Online e depois é redistribuído
    // quando Buscar/Corrida forem registrados.
    if(!c || c.loading) return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
    return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
  };

  // ── Jornadas que atravessam a data selecionada ────────────────────
  async function ensureOverlapSessions(dateStr){
    if(!_supaUser||!dateStr)return;
    const old=overlapSessionCache.get(dateStr);
    if(old&&(old.loading||Date.now()-old.ts<30000))return;
    overlapSessionCache.set(dateStr,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    const b=civilDayBounds(dateStr);
    try{
      const {data,error}=await _SUPA.from('sessions').select('*').eq('user_id',_supaUser.id)
        .lt('start_ms',b.endMs).or(`end_ms.is.null,end_ms.gte.${b.startMs}`).order('start_ms',{ascending:true});
      if(!error){
        const rows=data||[];
        overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows});
        // Hidratamos o cache local no DIA DE INÍCIO; nunca criamos uma segunda Jornada na virada.
        rows.filter(r=>r.end_ms).forEach(r=>{
          const ds=localYmdFromMs(r.start_ms), day=DB.day(ds); if(!day.sessions)day.sessions=[];
          const item={id:r.id,start:Number(r.start_ms),end:Number(r.end_ms),km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0};
          const i=day.sessions.findIndex(x=>x.id===item.id); if(i>=0)day.sessions[i]=item; else day.sessions.push(item); DB.saveDay(day);
        });
      }
    }catch(e){overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows:old?.rows||[]});}
    try{if(S.sec==='jornada')setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0);}catch(e){}
  }
  function overlappingJourneyDescriptors(dateStr){
    const b=civilDayBounds(dateStr), map=new Map();
    (DB.allDates?DB.allDates():[]).forEach(ds=>{
      const day=DB.day(ds);(day.sessions||[]).forEach((sess,idx)=>{
        if(!overlaps(sess.start,sess.end,b.startMs,b.endMs))return;
        map.set(String(sess.id||sess.start),{sess,sourceDate:ds,origIdx:idx});
      });
    });
    const remote=overlapSessionCache.get(dateStr)?.rows||[];
    remote.forEach(r=>{
      if(!overlaps(r.start_ms,r.end_ms,b.startMs,b.endMs))return;
      const id=String(r.id||r.start_ms);if(map.has(id))return;
      const ds=localYmdFromMs(r.start_ms), d=DB.day(ds), idx=(d.sessions||[]).findIndex(x=>x.id===r.id);
      map.set(id,{sess:{id:r.id,start:Number(r.start_ms),end:r.end_ms?Number(r.end_ms):null,km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0},sourceDate:ds,origIdx:idx});
    });
    if(S?.gps?.st==='running'&&S.gps.start&&overlaps(S.gps.start,null,b.startMs,b.endMs)){
      const id=String(S.gps.sessionId||S.gps.start);
      map.set(id,{sess:{id:S.gps.sessionId,start:S.gps.start,end:null,km:S.gps.km||0,elapsed:getElapsed()},sourceDate:localYmdFromMs(S.gps.start),origIdx:-1,live:true});
    }
    return [...map.values()].sort((a,b)=>(a.sess.start||0)-(b.sess.start||0));
  }
  function journeyLabel(desc,dateStr){
    const sess=desc.sess, src=desc.sourceDate||localYmdFromMs(sess.start);
    if(desc.live) return src===dateStr?'Jornada atual':'Jornada atual · continuação';
    const srcSessions=(DB.day(src).sessions||[]).slice().sort((a,b)=>(a.start||0)-(b.start||0));
    const pos=Math.max(0,srcSessions.findIndex(x=>x.id===sess.id));
    let label='Jornada '+(pos+1);
    const endDate=sess.end?localYmdFromMs(sess.end):src;
    if(src<dateStr) label+=' · continuação'; else if(endDate>dateStr) label+=' · continua';
    return label;
  }
  function renderContinuousSessionList(){
    const el=document.getElementById('sessions-list'), toggle=document.getElementById('sessions-toggle');
    if(!el||!S?.curDate)return;
    ensureOverlapSessions(S.curDate);
    const desc=overlappingJourneyDescriptors(S.curDate).sort((a,b)=>(b.sess.start||0)-(a.sess.start||0));
    const shown=S.sessionsExpanded?desc:desc.slice(0,3);
    const html=shown.map(x=>{
      ensureJourneyAutoTrips(x.sess);ensureJourneyStatus(x.sess);
      const synthetic={date:x.sourceDate,trips:journeyManualTrips(x.sess),autoTrips:journeyAutoTrips(x.sess,DB.day(x.sourceDate).autoTrips||[])};
      return sessionItem(journeyLabel(x,S.curDate),x.sess,x.origIdx,x.sourceDate,synthetic,!!x.live);
    });
    el.innerHTML=html.length?html.join(''):'<div class="empty">Nenhuma sessão registrada</div>';
    if(toggle){toggle.style.display=desc.length>3?'':'none';toggle.innerHTML=S.sessionsExpanded?'Ver menos':'Ver todas &#8250;';}
  }

  // Link de corrida ↔ Jornada agora procura TODAS as sessões; uma corrida de 01:30
  // pode pertencer à Jornada iniciada no dia anterior.
  window.isAutoTripLinkedToSession = function(t){
    const ts=autoTripTs(t);if(!ts)return false;
    for(const ds of (DB.allDates?DB.allDates():[])){
      if((DB.day(ds).sessions||[]).some(sess=>ts>=(sess.start||0)&&(!sess.end||ts<=sess.end)))return true;
    }
    return !!(S?.gps?.st==='running'&&S.gps.start&&ts>=S.gps.start);
  };

  function civilJourneyTotals(dateStr){
    const b=civilDayBounds(dateStr), seen=new Set();
    let elapsed=0, km=0;
    const add=sess=>{
      const start=Number(sess?.start||0); if(!start)return;
      const live=!!(S?.gps?.st==='running' && S.gps.sessionId && sess?.id===S.gps.sessionId);
      let end=Number(sess?.end||0);
      const recordedElapsed=Math.max(0,Number(sess?.elapsed)||0);
      if(!end) end=live?Date.now():(recordedElapsed>0?sta
===== loadAuto @ 565339 =====
rtId}-${i}">${d.label}</span>
    </div>`;
  }).join('');
  return bubHtml + gridHtml + `<div class="hg-bars-row">${barsHtml}</div>`;
}
function hgFitColWidths(containerId) {
  const row = document.querySelector('#'+containerId+' .hg-bars-row');
  if (!row) return;
  const cols = row.querySelectorAll('.hg-bar-col');
  const n = cols.length;
  if (!n) return;
  const visibleN = Math.min(n, 12);
  const w = Math.max(24, row.clientWidth / visibleN);
  const barW = Math.min(64, Math.round(w * 0.62));
  cols.forEach(c => {
    c.style.flex = '0 0 '+w+'px';
    const bar = c.querySelector('.hg-bar');
    if (bar) bar.style.maxWidth = barW+'px';
  });
}
function showChartInfo(btn, text) {
  const tip = document.getElementById('hg-info-tip');
  if (!tip) return;
  const sameOpen = tip.classList.contains('show') && tip._btn === btn;
  tip.classList.remove('show');
  if (sameOpen) { tip._btn = null; return; }
  tip.textContent = text;
  tip._btn = btn;
  requestAnimationFrame(() => {
    const r = btn.getBoundingClientRect();
    const margin = 12;
    const tw = tip.offsetWidth, th = tip.offsetHeight;
    let left = r.left + r.width/2 - tw/2;
    if (left < margin) left = margin;
    if (left + tw > window.innerWidth - margin) left = window.innerWidth - margin - tw;
    const top = r.top - th - 10;
    tip.style.left = left+'px';
    tip.style.top = Math.max(margin, top)+'px';
    tip.style.setProperty('--hg-tip-arrow', (r.left + r.width/2 - left - 6)+'px');
    tip.classList.add('show');
  });
}
const S_hgActiveBar = {};
function hgToggleBar(chartId, i) {
  const wasActive = S_hgActiveBar[chartId] === i;
  const root = document.getElementById('hg-chart-'+chartId);
  if (root) {
    root.querySelectorAll('.hg-bar.dim,.hg-bar.sel').forEach(b=>b.classList.remove('dim','sel'));
    root.querySelectorAll('.hg-bar-lbl.sel').forEach(s=>s.classList.remove('sel'));
  }
  const bub = root ? root.querySelector('.hg-bar-bubble') : null;
  if (bub) bub.classList.remove('show');
  if (wasActive) { delete S_hgActiveBar[chartId]; return; }
  S_hgActiveBar[chartId] = i;
  const bar = document.getElementById('hgbar-'+chartId+'-'+i);
  const lbl = document.getElementById('hglbl-'+chartId+'-'+i);
  if (bar) bar.classList.add('sel');
  if (lbl) lbl.classList.add('sel');
  if (root) root.querySelectorAll('.hg-bar').forEach(b => { if (b !== bar) b.classList.add('dim'); });
  if (bub && bar) {
    // monta o conteúdo do tooltip com os dados em data-*
    const val  = parseFloat(bar.dataset.val);
    const avg  = parseFloat(bar.dataset.avg);
    const max  = parseFloat(bar.dataset.max);
    const min  = parseFloat(bar.dataset.min);
    const qtd  = parseInt(bar.dataset.qtd);
    const lbl_ = bar.dataset.lbl;
    const prev = bar.dataset.prev ? parseFloat(bar.dataset.prev) : null;
    const prevLbl = bar.dataset.prevlbl || '';
    const rich = bar.dataset.rich === '1';
    const qtdTxt = (rich && qtd > 1) ? ` <span class="hg-bb-qtd">(${qtd})</span>` : '';
    let html = `<div class="hg-bb-val">${DB.fR(val)}${qtdTxt}</div>`;
    if (rich && qtd > 1) {
      html += `<div class="hg-bb-row">≈ ${DB.fR(avg)} · ▲${DB.fR(max)} · ▼${DB.fR(min)}</div>`;
    }
    if (rich && prev != null && prev > 0.001) {
      const pct = Math.round((val - prev) / prev * 100);
      const arrow = pct > 0 ? '▲' : (pct < 0 ? '▼' : '–');
      html += `<div class="hg-bb-row">${arrow} ${Math.abs(pct)}% vs ${prevLbl}</div>`;
    }
    bub.innerHTML = html;
    // posiciona o tooltip acima da barra, alinhado ao centro dela,
    // mas clamped pra não sair do chart nem da tela
    bub.classList.add('show');
    requestAnimationFrame(() => {
      const barRect = bar.getBoundingClientRect();
      const rootRect = root.getBoundingClientRect();
      const bw = bub.offsetWidth;
      const margin = 10;
      let left = barRect.left - rootRect.left + barRect.width/2 - bw/2;
      if (left < margin) left = margin;
      if (left + bw > rootRect.width - margin) left = rootRect.width - margin - bw;
      bub.style.left = left + 'px';
      bub.style.bottom = (rootRect.bottom - barRect.top + 8) + 'px';
    });
  }
}
function hgGroupBy(items, keyFn, valFn) {
  const m = {};
  items.forEach(it => { const k = keyFn(it); m[k] = (m[k]||0) + valFn(it); });
  return m;
}

function renderHomeGraphs() {
  if (!S || !DB || !getHomeDays) return;
  const dates = getHomeDays();
  // Corridas automáticas do período inteiro (mais o período anterior, que
  // o insight usa pra comparação) — busca em lote UMA VEZ, cacheada pelo
  // intervalo. Refresh próprio a cada 60s enquanto na Home (setInterval no
  // fim do arquivo), pra pegar corrida nova sem precisar sair e voltar.
  const prevDatesForRange = getHomeDaysPrev ? getHomeDaysPrev() : [];
  const allDatesForRange = dates.concat(prevDatesForRange).sort();
  const rangeKey = (allDatesForRange[0]||'') + '..' + (allDatesForRange[allDatesForRange.length-1]||'');
  if (S.autoTripsHomeRange !== rangeKey) {
    S.autoTripsHomeRange = rangeKey;
    loadAutoTripsIndexed(allDatesForRange[0], allDatesForRange[allDatesForRange.length-1]).then(map => {
      if (S.autoTripsHomeRange !== rangeKey) return; // período já mudou de novo
      S.autoTripsByDate = map;
      if (S.sec === 'relatorios') renderHomeGraphs();
    });
  }
  const days = dates.map(d => {
    const day = DB.day(d);
    return { ...day, trips: day.trips.concat(autoTripsAsTrips(d)) };
  });
  const rev   = days.reduce((s,d) => s + d.trips.reduce((a,t)=>a+(t.value||0),0) + d.revenues.reduce((a,r)=>a+(r.amount||0),0), 0);
  const exp   = days.reduce((s,d) => s + d.expenses.reduce((a,e)=>a+(e.amount||0),0), 0);
  const km    = days.reduce((s,d) => s + d.trips.reduce((a,t)=>a+(t.km||0),0), 0);
  const tc    = days.reduce((s,d) => s + d.trips.length, 0);
  const hrs   = days.reduce((s,d) => s + d.trips.reduce((a,t)=>a+(t.duration||0),0), 0) / 60;
  const profit = rev - exp;
  const pctL   = rev > 0 ? Math.round(profit/rev*100) : 0;

  setText('hg-amount', DB.fR(rev));
  setText('hg-sub', tc+' corrida'+(tc!==1?'s':'')+' · '+km.toFixed(1)+' km');
  setText('hg-rpkm', km > 0 ? DB.fR(rev/km) : '—');
  setText('hg-rphr', hrs > 0 ? DB.fR(rev/hrs) : '—');
  setText('hg-trips', tc);
  setText('hg-exp', DB.fRI(exp));
  setText('hg-profit', DB.fRI(profit));
  setText('hg-pct', pctL+'%');
  renderHgInsight(rev, tc, km, exp, hrs, pctL);

  renderHgGoals(dates, days, rev);
  renderHgChartDia();
  renderHgChartHora();
  renderHgChartMes();
  renderHgPlatformPie(days);
  renderHgCatPie(days);

  // Últimos lançamentos
  const allEntries = days.flatMap(d => [
    ...d.trips.map(t=>({...t, _type:'corrida', _date:d.date})),
    ...d.expenses.map(e=>({...e, _type:'despesa', _date:d.date})),
    ...d.revenues.map(r=>({...r, _type:'receita', _date:d.date}))
  ]).sort((a,b) => (b._date+(b.time||'')).localeCompare(a._date+(a.time||''))).slice(0,5);

  const entriesEl = document.getElementById('home-entries');
  if (entriesEl) {
    if (!allEntries.length) {
      entriesEl.innerHTML = '<div class="empty">Nenhum lançamento ainda</div>';
    } else {
      entriesEl.innerHTML = allEntries.map(e => entryCard(e)).join('');
    }
  }
}
function renderHome() { renderHomeGraphs(); }

// ── Metas do período (financeiro, km, tempo online — vem do Planejar Jornada) ──
function renderHgGoals(dates, days, rev) {
  const el = document.getElementById('hg-goals');
  if (!el) return;
  const plans = DB.planJornada();
  let moneyGoal = 0, kmGoal = 0, timeGoalMin = 0, hasAnyPlan = false;
  dates.forEach(dt => {
    const dow = new Date(dt+'T12:00').getDay();
    plans.filter(p => p.days && p.days.includes(dow)).forEach(p => {
      hasAnyPlan = true;
      moneyGoal += p.moneyGoal || 0;
      kmGoal += p.kmGoal || 0;
      timeGoalMin += (p.schedules||[]).reduce((s,b)=>s+agMinutesBetween(b.start,b.end),0);
    });
  });
  const isToday = d => d === DB.today();
  const liveOn = S.gps.st==='running';
  const kmReal = days.reduce((s,d,i) => {
    const liveKm = (liveOn && isToday(dates[i])) ? S.gps.km : 0;
    return s + (d.sessions||[]).reduce((a,x)=>a+(x.km||0),0) + liveKm + (d.kmAdjust||0);
  }, 0);
  const elapsedReal = days.reduce((s,d,i) => {
    const liveEl = (liveOn && isToday(dates[i])) ? getElapsed() : 0;
    return s + (d.sessions||[]).reduce((a,x)=>a+(x.elapsed||0),0) + liveEl + (d.tempoAdjust||0);
  }, 0);
  const timeRealMin = Math.round(elapsedReal/60);

  let html = '';
  if (moneyGoal > 0) {
    const pct = Math.min(100, Math.round(rev/moneyGoal*100));
    html += '<div><div class="hg-goal-row"><span>Meta financeira</span><span>'+DB.fRI(rev)+' / '+DB.fRI(moneyGoal)+' · '+pct+'%</span></div><div class="hg-goal-bar money"><i style="width:'+pct+'%"></i></div></div>';
  } else {
    html += '<div class="hg-goal-locked" onclick="openPlanJornada()"><span>Meta financeira não definida</span><b>Planejar</b></div>';
  }
  if (timeGoalMin > 0) {
    const pct = Math.min(100, Math.round(timeRealMin/timeGoalMin*100));
    html += '<div><div class="hg-goal-row"><span>Tempo online</span><span>'+Math.floor(timeRealMin/60)+'h'+p2(timeRealMin%60)+' / '+Math.floor(timeGoalMin/60)+'h'+p2(timeGoalMin%60)+' · '+pct+'%</span></div><div class="hg-goal-bar time"><i style="width:'+pct+'%"></i></div></div>';
  } else {
    html += '<div class="hg-goal-locked" onclick="openPlanJornada()"><span>Tempo online não definido</span><b>Planejar</b></div>';
  }
  if (kmGoal > 0) {
    const pct = Math.min(100, Math.round(kmReal/kmGoal*100));
    html += '<div><div class="hg-goal-row"><span>Quilometragem</span><span>'+kmReal.toFixed(1)+' / '+kmGoal.toFixed(0)+' km · '+pct+'%</span></div><div class="hg-goal-bar km"><i style="width:'+pct+'%"></i></div></div>';
  } else {
    html += '<div class="hg-goal-locked" onclick="openPlanJornada()"><span>Quilometragem não definida</span><b>Planejar</b></div>';
  }
  el.innerHTML = html;
}

// ── Motor genérico de "balde" — usado pelos 3 gráficos de barra da Home.
// Pega as datas do período selecionado (getHomeDays), agrupa cada dia num balde
// (dia da semana / semana / hora / turno / mês / ano, depende do gráfico+modo) e
// guarda os valores individuais de cada dia que caiu naquele balde — não só a soma.
// Quando um balde tem mais de 1 dia (ex: todas as sextas do mês), o toque mostra
// média/maior/menor/qtd de dias em vez de só o total, sem poluir a barra.
function hgBucketStats(dates, keyFn, valueFn, order) {
  const buckets = {};
  dates.forEach(date => {
    const ks = keyFn(date); // pode retornar 1 chave ou um array de {key,value} (caso da Hora)
    if (ks == null) return;
    const list = Array.isArray(ks) ? ks : [{key: ks, value: valueFn ? valueFn(date) : null}];
    list.forEach(({key, value}) => {
      if (value == null || value <= 0.001) return;
      (buckets[key] = buckets[key] || []).push(value);
    });
  });
  const keys = order ? order.filter(k => buckets[k]) : Object.keys(buckets).sort();
  return keys.map(k => {
    const arr = buckets[k];
    const sum = arr.reduce((a,b)=>a+b, 0);
    return { label: k, value: sum, max: Math.max(...arr), min: Math.min(...arr), avg: sum/arr.length, qtd: arr.length };
  });
}
function hgDayTotal(date) {
  const day = DB.day(date);
  return dayTripsAll(date).reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0);
}
function hgWeekStart(date) {
  const d = new Date(date+'T00:00:00'); const dow = (d.getDay()+6)%7; d.setDate(d.getDate()-dow);
  return p2(d.getDate())+'/'+p2(d.getMonth()+1);
}

// ── Por Dia/Semana ──
function setHgDiaMode(m) {
  S.hgDiaMode = m;
  ['d','w'].forEach(x => { const e=document.getElementById('hg-dia-'+x); if(e) e.classList.toggle('active', x===m); });
  renderHgChartDia();
}
function renderHgChartDia() {
  const el = document.getElementById('hg-chart-dia');
  if (!el) return;
  const dates = getHomeDays();
  let data;
  if (S.hgDiaMode === 'd') {
    const lbls = ['Seg','Ter','Qua','Qui','Sex','Sáb','Dom'];
    const keyFn = date => lbls[(new Date(date+'T00:00:00').getDay()+6)%7];
    data = hgBucketStats(dates, keyFn, hgDayTotal, lbls);
  } else {
    const keyFn = hgWeekStart;
    const order = [...new Set(dates.map(keyFn))];
    data = hgBucketStats(dates, keyFn, hgDayTotal, order);
  }
  el.innerHTML = hgBarsHtml(data, 'dia');
  hgFitColWidths('hg-chart-dia');
  renderHgHighlight('hg-dia-insight', data, 'fraco', d => 'Dia mais fraco: '+d.label+' (média '+DB.fR(d.avg)+')');
}

// ── Por Hora/Turno ──
function setHgHoraMode(m) {
  S.hgHoraMode = m;
  ['h','t'].forEach(x => { const e=document.getElementById('hg-hora-'+x); if(e) e.classList.toggle('active', x===m); });
  renderHgChartHora();
}
function renderHgChartHora() {
  const el = document.getElementById('hg-chart-hora');
  if (!el) return;
  const dates = getHomeDays();
  const turnos = [
    {label:'Madrugada', test:h=>h>=0&&h<6}, {label:'Manhã', test:h=>h>=6&&h<12},
    {label:'Tarde', test:h=>h>=12&&h<18}, {label:'Noite', test:h=>h>=18&&h<24}
  ];
  // pra cada dia, quebra os valores por hora (ou turno) ANTES de agregar entre dias —
  // assim um dia com 2 corridas na mesma hora conta certo, e dias diferentes na mesma
  // hora ficam no mesmo balde.
  const keyFn = date => {
    const trips = dayTripsAll(date).filter(t => t.time);
    const perKey = {};
    trips.forEach(t => {
      const hr = parseInt(t.time.split(':')[0]);
      const k = S.hgHoraMode === 'h' ? (hr+'h') : turnos.find(tu=>tu.test(hr)).label;
      perKey[k] = (perKey[k]||0) + (t.value||0);
    });
    return Object.keys(perKey).map(k => ({key:k, value:perKey[k]}));
  };
  const order = S.hgHoraMode === 'h' ? Array.from({length:24},(_,h)=>h+'h') : turnos.map(t=>t.label);
  const data = hgBucketStats(dates, keyFn, null, order);
  el.innerHTML = hgBarsHtml(data, 'hora', false);
  hgFitColWidths('hg-chart-hora');
  renderHgHighlight('hg-hora-insight', data, 'forte', d => 'Melhor horário: '+d.label+' (média '+DB.fR(d.avg)+')');
}

// ── Por Mês/Ano ──
function setHgMesMode(m) {
  S.hgMesMode = m;
  ['m','y'].forEach(x => { const e=document.getElementById('hg-mes-'+x); if(e) e.classList.toggle('active', x===m); });
  renderHgChartMes();
}
function renderHgChartMes() {
  const el = document.getElementById('hg-chart-mes');
  if (!el) return;
  const dates = getHomeDays();
  const mesesLbl = ['Jan','Fev','Mar','Abr','Mai','Jun','Jul','Ago','Set','Out','Nov','Dez'];
  let data;
  if (S.hgMesMode === 'm') {
    const keyFn = date => mesesLbl[parseInt(date.split('-')[1],10)-1];
    const order = [...new Set(dates.map(keyFn))];
    data = hgBucketStats(dates, keyFn, hgDayTotal, order);
  } else {
    const keyFn = date => date.split('-')[0];
    const order = [...new Set(dates.map(keyFn))];
    data = hgBucketStats(dates, keyFn, hgDayTotal, order);
  }
  // Com período Semana/Mês selecionado em cima, isso aqui naturalmente vira 1 barra só
  // (só existe 1 mês/ano dentro do recorte) — é o esperado, segue a mesma regra dos outros.
  el.innerHTML = hgBarsHtml(data, 'mes');
  hgFitColWidths('hg-chart-me
===== loadAuto @ 602846 =====
das sozinhas', 'success');
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
  } catch (e) { console.error('_autoRefreshPendingTrips', e); }
}
setTimeout(()=>{ _autoRefreshPendingTrips(); setInterval(_autoRefreshPendingTrips, 5*60*1000); }, 6000);

// Rotina automática nº2 (16/08/2026, pedido do Yuri: "não quero usuário
// editando, tem que partir da gente interno... a cada X min confere o
// que tá no registro e corrige"). Diferente da rotina acima (que só olha
// corridas AINDA sem valor confirmado), essa reconfere corridas que JÁ
// estão 'confirmada' — pra pegar o caso real que aconteceu (valor errado
// que já tinha virado card normal na tela). Roda a cada 10min, olha só as
// últimas 24h (corrida mais velha que isso já foi conferida o suficiente
// de vezes). REGRA DE OURO: só aplica sozinha quando a mudança é pequena/
// confiável — mudança grande NUNCA aplica sem humano olhar (mesmo critério
// do botão manual), só marca value_needs_review pra aparecer sinalizada,
// sem sumir da tela nem dos totais (não é "fantasma", é corrida real que
// só precisa de confirmação do valor).
//
// MUDOU (16/08/2026): mirava em status='confirmada', mas agora
// 'confirmada' só nasce depois do vídeo do histórico (ver
// confirmImport()) — reconferir isso aqui não faz mais sentido, vídeo já
// É a fonte mais confiável que existe. Passa a mirar em 'estimada' (ciclo
// da captura ao vivo completo, só com valor da oferta) — mesmo espírito
// de antes, refinando o valor com a melhor leitura do log disponível
// enquanto o vídeo de confirmação não chega.
async function _autoReconcileConfirmedTrips() {
  if (!_supaUser) return;
  try {
    const { data, error } = await _SUPA.from('auto_trips').select('id')
      .eq('user_id', _supaUser.id).eq('status', 'estimada')
      .is('data_quality_flag', null)
      .gte('accepted_at', new Date(Date.now() - 24*60*60*1000).toISOString())
      .limit(15);
    if (error || !data || !data.length) return;
    let fixedQuiet = 0, flaggedForReview = 0;
    for (const row of data) {
      try {
        const r = await _computeAutoTripRefresh(row.id);
        if (!r.ok || !r.changes.length) continue;
        if (r.valueBigChange) {
          await _SUPA.from('auto_trips').update({ value_needs_review: true }).eq('id', row.id);
          flaggedForReview++;
        } else {
          await _applyAutoTripRefresh(r.tripId, r.patch);
          fixedQuiet++;
        }
      } catch(e) {}
    }
    if (fixedQuiet > 0 || flaggedForReview > 0) {
      S.autoTripsDate = null; S.autoTripsHomeRange = null;
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHomeGraphs();
      if (S.sec === 'corridas') renderCorridasActive();
    }
    // Silenciosa de propósito — mudança pequena não precisa de aviso, e
    // mudança grande já fica visível pelo badge "Valor a confirmar" na
    // tela Corridas, sem precisar interromper com toast.
  } catch (e) { console.error('_autoReconcileConfirmedTrips', e); }
}
setTimeout(()=>{ _autoReconcileConfirmedTrips(); setInterval(_autoReconcileConfirmedTrips, 10*60*1000); }, 9000);
// Balãozinho discreto ao tocar num símbolo (dinâmico, vínculo à jornada,
// dinheiro) — pedido do Yuri (24/07/2026). Some sozinho, não navega nem
// abre nada.
function showTapLegend(event, text) {
  event.stopPropagation();
  document.querySelectorAll('.tap-legend-tip').forEach(el => el.remove());
  const wrap = event.currentTarget;
  const tip = document.createElement('div');
  tip.className = 'tap-legend-tip';
  tip.textContent = text;
  wrap.appendChild(tip);
  setTimeout(() => tip.remove(), 2500);
}
// Legenda de comparação previsto × real (Deslocamento/Corrida coloridos) —
// abre/fecha um painel fixo embaixo da linha, diferente do balãozinho dos
// símbolos porque tem mais texto (2 linhas de números).
function toggleCmpLegend(event, id) {
  event.stopPropagation();
  const el = document.getElementById(id);
  if (el) el.style.display = el.style.display === 'none' ? '' : 'none';
}
async function loadAutoTripsForDay(dateStr) {
  if (!_supaUser) return [];
  // O dia da Jornada é o dia LOCAL do motorista. Converte meia-noite local
  // para UTC antes de consultar timestamptz — evita perder corridas após 21h
  // no Brasil quando o relógio UTC já virou para o dia seguinte.
  const fromLocal = new Date(dateStr+'T00:00:00');
  const toLocal = new Date(fromLocal); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  // null = falha de rede/consulta; [] = consulta válida sem corridas.
  return error ? null : (data||[]);
}
// Recusadas do dia — mesmo padrão de loadAutoTripsForDay, pra alimentar o
// contador "X aceitas / Y recusadas" do Total do dia. Tabela já existe e já
// é usada na tela Corridas (seção expansível); aqui só reaproveita pro
// recorte de UM dia em vez do período todo.
async function loadDeclinedOffersForDay(dateStr) {
  if (!_supaUser) return [];
  const from = dateStr+'T00:00:00', to = dateStr+'T23:59:59';
  const { data, error } = await _SUPA.from('declined_offers').select('id')
    .eq('user_id', _supaUser.id)
    .gte('seen_at', from).lte('seen_at', to);
  return error ? [] : (data||[]);
}

// ── Corridas automáticas → alimentam a Home (pedido do Yuri, 23/07/2026) ──
// Mesmo gap da Jornada, só que espalhado pela Home inteira: valor, km,
// corridas, todos os gráficos e o insight de IA só olhavam day.trips
// (Importadas) e ficavam cegos pra qualquer dia 100% automático. Em vez de
// corrigir função por função (arriscado, muita superfície), busca TODAS as
// automáticas do período de UMA VEZ (não uma consulta por dia — helper
// abaixo cobre semana/mês/ano inteiros numa chamada só) e injeta como
// corridas "de mentira" no formato de trip normal — todo código que já lê
// day.trips corretamente passa a enxergar as automáticas de graça, sem
// precisar tocar em cada gráfico individualmente.
async function loadAutoTripsIndexed(fromDate, toDate) {
  if (!_supaUser) return {};
  const fromLocal = new Date(fromDate+'T00:00:00');
  const toLocal = new Date(toDate+'T00:00:00'); toLocal.setDate(toLocal.getDate()+1);
  const { data, error } = await _SUPA.from('auto_trips').select('*')
    .eq('user_id', _supaUser.id)
    .gte('trip_started_at', fromLocal.toISOString()).lt('trip_started_at', toLocal.toISOString())
    .order('trip_started_at', {ascending:true});
  if (error || !data) return {};
  const map = {};
  data.forEach(t => {
    const iso = t.trip_started_at || t.accepted_at;
    if (!iso) return;
    const dt = new Date(iso);
    const dateStr = dt.getFullYear()+'-'+p2(dt.getMonth()+1)+'-'+p2(dt.getDate());
    (map[dateStr] = map[dateStr] || []).push(t);
  });
  return map;
}
// Converte as automáticas de um dia (já indexadas em S.autoTripsByDate) pro
// MESMO formato de objeto que day.trips usa — value/km/time/duration/
// platform/dinheiro — assim qualquer soma/filtro que já existe funciona
// sem mudança nenhuma. NUNCA usar pra salvar (é só leitura/exibição —
// misturar isso em day.trips de verdade duplicaria dado no Supabase).
function autoTripsAsTrips(date) {
  const list = (S.autoTripsByDate && S.autoTripsByDate[date]) || [];
  return list.map(at => {
    const start = at.trip_started_at ? new Date(at.trip_started_at) : null;
    const end   = at.trip_ended_at ? new Date(at.trip_ended_at) : null;
    const durMin = start && end ? Math.max(0, Math.round((end-start)/60000))
                 : Math.round((at.offer_duration_trip_sec||0)/60);
    return {
      id: 'auto_'+at.id, _auto: true,
      platform: at.platform || 'uber',
      time: start ? p2(start.getHours())+':'+p2(start.getMinutes()) : '',
      duration: durMin,
      value: parseFloat(at.offer_value)||0,
      km: parseFloat(at.real_km_trip ?? at.offer_km_trip) || 0,
      dinheiro: !!at.dinheiro,
      origin:'', dest:'', category:''
    };
  });
}
// Todas as corridas de um dia — Importadas + Automáticas juntas, no mesmo
// formato. Uso exclusivo de leitura/agregação (gráficos, insights, somas).
function dayTripsAll(date) {
  return DB.day(date).trips.concat(autoTripsAsTrips(date));
}

// ── Corridas automaticas no Financeiro (soma por dia, cacheada em memoria) ──
// auto_trips vivem so no Supabase; o Financeiro cobre Mes/Ano, entao busca em
// lote (paginado) o intervalo do periodo + periodo anterior + mes corrente,
// agrupa por data LOCAL (fuso: margem de 1 dia em cada ponta da query).
let _finAuto = { key:'', byDate:{}, loading:false, ts:0 };
function finAutoDay(ds) { return _finAuto.byDate[ds] || { sum:0, count:0, items:[] }; }
async function loadFinAutoTrips() {
  if (!_supaUser) return;
  const today = new Date();
  const dim = new Date(today.getFullYear(), today.getMonth()+1, 0).getDate();
  const all = [...getFinDays(), ...getFinDaysPrev(),
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-01',
    today.getFullYear()+'-'+p2(today.getMonth()+1)+'-'+p2(dim)];
  let from = all[0], to = all[0];
  all.forEach(d => { if (d < from) from = d; if (d > to) to = d; });
  const key = from + '|' + to;
  if (_finAuto.loading) return;
  if (_finAuto.key === key && (Date.now() - _finAuto.ts) < 120000) return;
  _finAuto.loading = true;
  try {
    const fromIso = new Date(new Date(from+'T00:00:00').getTime() - 86400000).toISOString();
    const toIso   = new Date(new Date(to+'T23:59:59').getTime() + 86400000).toISOString();
    const rows = [];
    let off = 0;
    while (true) {
      const { data, error } = await _SUPA.from('auto_trips')
        .select('id,trip_started_at,accepted_at,offer_value,platform')
        .eq('user_id', _supaUser.id)
        .gte('trip_started_at', fromIso).lte('trip_started_at', toIso)
        .order('trip_started_at', {ascending:true})
        .range(off, off + 999);
      if (error || !data || !data.length) break;
      rows.push(...data);
      if (data.length < 1000) break;
      off += 1000;
    }
    const byDate = {};
    rows.forEach(t => {
      const ts = autoTripTs(t); if (!ts) return;
      const d = new Date(ts);
      const ds = d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
      const b = byDate[ds] || (byDate[ds] = { sum:0, count:0, items:[] });
      b.sum += (t.offer_value || 0);
      b.count++;
      b.items.push(t);
    });
    _finAuto = { key, byDate, loading:false, ts: Date.now() };
    if (S.sec === 'financeiro') { try { renderFin(); } catch(e) {} }
  } catch(e) { _finAuto.loading = false; console.warn('[loadFinAutoTrips]', e); }
}

// Corridas: mescla Importadas + Automáticas numa lista só, ordenadas por
// horário — pedido do Yuri (24/07/2026). A tag "Captura automática" (já
// existente) identifica a origem, então não precisa mais de aba pra isso.
async function renderCorridasActive() {
  const dates = getCorridasDays().slice().sort().reverse(); // mais recente primeiro
  const plats = S.corridasPlats || new Set();
  const platFilter = t => plats.size === 0 || plats.has(t.platform||'uber');

  // Automáticas do mesmo intervalo — busca uma vez só, agrupa por data local.
  let autoByDate = {};
  if (_supaUser) {
    const range = autoCorridasDateRange();
    let q = _SUPA.from('auto_trips').select('*').eq('user_id', _supaUser.id).order('trip_started_at', {ascending:false});
    if (range) q = q.gte('trip_started_at', range.from).lte('trip_started_at', range.to);
    if (plats.size === 1) q = q.eq('platform', [...plats][0]);
    const { data, error } = await q;
    (error ? [] : (data||[])).forEach(t => {
      const ts = autoTripTs(t);
      if (!ts) return;
      const d = new Date(ts);
      const onePlat = plats.size === 1 ? [...plats][0] : null;
      const dateStr = onePlat ? operationalDateForTimestamp(onePlat, ts) : localYmdFromMs(ts);
      (autoByDate[dateStr] = autoByDate[dateStr] || []).push(t);
    });
  }

  let totalVal = 0, totalKm = 0, totalN = 0, totalMin = 0;
  const groups = [];
  dates.forEach(d => {
    const day = DB.day(d);
    const manual = (day.trips||[]).filter(platFilter)
      .map(t => ({ kind:'manual', ts: t.ts || (t.time?new Date(d+'T'+t.time+':00').getTime():0), t }));
    const auto = (autoByDate[d]||[]).filter(platFilter)
      .map(t => ({ kind:'auto', ts: autoTripTs(t), t }));
    const merged = [...manual, ...auto].sort((a,b)=>b.ts-a.ts);
    if (!merged.length) return;
    merged.forEach(x => {
      if (x.kind==='manual') { totalVal += (x.t.value||0); totalKm += (x.t.km||0); totalMin += (x.t.duration||0); }
      else {
        totalVal += (x.t.offer_value||0); totalKm += (x.t.real_km_trip||0);
        if (x.t.accepted_at && x.t.trip_ended_at) totalMin += Math.max(0,(new Date(x.t.trip_ended_at)-new Date(x.t.accepted_at))/60000);
      }
      totalN++;
    });
    groups.push({ date: d, items: merged });
  });

  setText('cor-sum-val', DB.fR(totalVal));
  setText('cor-sum-km', totalKm.toLocaleString('pt-BR',{minimumFractionDigits:1,maximumFractionDigits:1}));
  setText('cor-sum-n', totalN);
  renderDeclinedOffers(dates, plats, totalVal, totalKm, totalMin);

  const listEl = document.getElementById('cor-list');
  if (!listEl) return;
  if (!groups.length) {
    listEl.innerHTML = `<div class="empty" style="text-align:center;padding:50px 20px;color:#64748B">
      <svg viewBox="0 0 24 24" width="36" height="36" fill="none" stroke="currentColor" stroke-width="1.6" style="color:#94A3B8;margin-bottom:10px"><path d="M5 17h14M6 17l1-6 3-4h4l3 4 1 6"/><circle cx="9" cy="20" r="1.4" fill="currentColor"/><circle cx="17" cy="20" r="1.4" fill="currentColor"/></svg>
      <div style="font-weight:700;font-size:14px;color:#0F172A;margin-bottom:4px">Nenhuma corrida encontrada</div>
      <div style="font-size:12.5px">Tente mudar o período ou a plataforma do filtro</div>
    </div>`;
    reapplyOpenTripDetail();
    return;
  }
  const todayStr = DB.today();
  const fmtSep = d => {
    if (d === todayStr) return 'Hoje · ' + d.split('-').reverse().slice(0,2).join('/');
    return d.split('-').reverse().slice(0,2).join('/') + '/' + d.slice(0,4);
  };
  listEl.innerHTML = groups.map(g =>
    `<div class="day-sep">${fmtSep(g.date)}</div>`
    + g.items.map(x => x.kind==='manual' ? tripItem(x.t, g.date) : autoTripItem(x.t)).join('')
  ).join('');
  reapplyOpenTripDetail();
}
function autoCorridasDateRange() {
  const dates = getCorridasDays().slice().sort();
  if (!dates.length) return null;
  return { from: dates[0]+'T00:00:00', to: dates[dates.length-1]+'T23:59:59' };
}

// Corridas recusadas/expiradas — pedido do Yuri (24/07/2026): seção
// expansível em Corridas, R$/km e R$/h somando deslocamento (até o
// embarque) + corrida, comparando com o que foi de fato aceito. Respeita
// os me
===== loadAuto @ 632876 =====
= isAutoTripLinkedToSession(t);
  const linkSvg = `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round"><path d="M9 17H7A5 5 0 0 1 7 7h2M15 7h2a5 5 0 1 1 0 10h-2M8 12h8"/></svg>`;
  const linkTag = `<span class="tap-legend-wrap link-tag ${linked?'on':'off'}" onclick="showTapLegend(event,'${linked?'Vinculada a uma jornada':'Não está em nenhuma jornada'}')">${linkSvg}</span>`;
  const perfStrip = perfStripHtml(t.offer_value, (kmPickupVal||0)+(kmDestVal||0), (durPickupVal||0)+(durDestVal||0));
  return `<div class="trip-item-wrap" id="tw-${tid}">
    <div class="li-row trip-top ${statusCls}" id="tr-${tid}" onclick="toggleTripDetail('${tid}')">
      <div class="li-ico-col">${hhmmTrip?`<span class="li-ico-time">${hhmmTrip}</span>`:''}${platIcon(t.platform)}</div>
      <div class="trip-content-col">
        <div class="trip-title-line"><h3 class="trip-title" style="white-space:normal">${t.origin_address?esc(extractBairro(t.origin_address)):'—'}${dest}</h3>${linkTag}</div>
        <p class="trip-subline">${subLine}</p>
        <div class="li-extra">${extras.join('')}</div>
      </div>
      <div class="li-val">
        <div style="display:flex;align-items:center;gap:5px;justify-content:flex-end">
          <strong>${DB.fR(t.offer_value)}</strong>
          <button class="val-edit-btn" onclick="event.stopPropagation();openAutoTripValueEdit('${t.id}',${t.offer_value||0})" aria-label="Editar valor da corrida">${pencilSvg}</button>
        </div>
        <span class="auto-status ${statusCls}">${statusTxt}</span>
      </div>
    </div>
    <div class="trip-detail-panel" id="td-${tid}" style="display:none">${perfStrip}${detRows}</div>
  </div>`;
}
// Corrige o caso em que uma corrida foi apagada direto no Supabase (ex:
// correção manual) mas continua aparecendo no app. Desde que o pullAll()
// passou a reconciliar automaticamente (removendo local o que já foi
// confirmado sincronizado antes e sumiu da nuvem, sem tocar em nada
// pendente na fila), esse botão só precisa rodar o mesmo pullAll() de
// novo — sem duplicar a lógica de segurança em outro lugar.
async function resyncCorridasFromCloud() {
  if (!_supaUser) { showToast('Sem conexão com a nuvem.'); return; }
  showConfirm('Recarregar da nuvem agora?\nCorridas confirmadas sincronizadas antes que sumiram do servidor saem do celular. Nada que ainda esteja pendente de subir é tocado.', async () => {
    try {
      await SYNC.pullAll();
      renderCorridasActive();
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHome();
      /* sincronização silenciosa: sem toast de sucesso */
    } catch (e) {
      console.error('resyncCorridasFromCloud', e);
      showToast('Erro ao recarregar da nuvem.');
    }
  }, { confirmLabel: 'Recarregar' });
}
function confirmDeleteTrip(date, id) {
  const day = DB.day(date);
  const t = (day.trips||[]).find(x => x.id === id);
  if (!t) return;
  const dest = t.dest ? ' → '+esc(t.dest) : '';
  showConfirm(`Excluir essa corrida?\n${DB.fR(t.value)} · ${t.origin?esc(t.origin):'—'}${dest}`, () => {
    day.trips = day.trips.filter(x => x.id !== id);
    DB.saveDay(day);
    SYNC.del('trips', { id });
    renderCorridasActive();
    if (date === S.curDate) { renderJornada(); }
    showToast('Corrida excluída.');
  }, { confirmLabel: 'Excluir' });
}
function toggleSessionsView() { S.sessionsExpanded = !S.sessionsExpanded; renderJornada(); }
function toggleLancView() { S.lancExpanded = !S.lancExpanded; renderJornada(); }

function renderJornada() {
  if (!S || !DB) return;
  try { updateCustoBanner(); } catch(e) {}
  if (!S.curDate) S.curDate = DB.today();
  const today = DB.today(), d = S.curDate, isToday = d === today;
  const dt    = new Date(d+'T12:00');
  const dows  = ['Domingo','Segunda-feira','Terça-feira','Quarta-feira','Quinta-feira','Sexta-feira','Sábado'];
  const ms    = ['jan','fev','mar','abr','mai','jun','jul','ago','set','out','nov','dez'];
  const msLong= ['janeiro','fevereiro','março','abril','maio','junho','julho','agosto','setembro','outubro','novembro','dezembro'];
  const dateEl = document.getElementById('dn-date');
  // Formato compacto: "23 jun 2026" — cabe em 1 linha
  if (dateEl) dateEl.textContent = isToday ? 'Hoje' : (dt.getDate()+' de '+msLong[dt.getMonth()]+' de '+dt.getFullYear());
  const dowEl = document.getElementById('dn-dow');
  // Dia da semana + badge "Hoje" na mesma linha
  if (dowEl) dowEl.innerHTML = dows[dt.getDay()];
  const day = DB.day(d);
  // Corridas automáticas do dia — busca assíncrona, cacheada por data (não
  // refaz a busca em toda chamada de renderJornada, que roda com frequência
  // inclusive a cada tick do timer da jornada ativa). Refresh periódico via
  // _syncTick (a cada 1min) cobre corridas novas capturadas durante o dia.
  if (S.autoTripsDate !== d) {
    const keepCurrent = S._autoTripsDataDate === d ? (S.autoTrips || []) : [];
    S.autoTripsDate = d;
    S.autoTrips = keepCurrent;
    loadAutoTripsForDay(d).then(list => {
      if (S.autoTripsDate !== d || !Array.isArray(list)) return; // erro mantém último dado válido
      S.autoTrips = list;
      S._autoTripsDataDate = d;
      renderJornada();
    });
  }
  day.autoTrips = S.autoTrips || [];
  // Recusadas do dia — mesmo padrão de cache do autoTrips acima. Lida aqui
  // (S.declined) e usada dentro de renderJourneyCard(), que roda logo a
  // seguir e populate o card "Total do dia".
  if (S.declinedDate !== d) {
    S.declinedDate = d;
    S.declined = [];
    loadDeclinedOffersForDay(d).then(list => {
      if (S.declinedDate !== d) return;
      S.declined = list;
      renderJornada();
    });
  }
  const rev = day.trips.reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0)
            + day.autoTrips.reduce((s,t)=>s+(t.offer_value||0),0);
  const exp = day.expenses.reduce((s,e)=>s+(e.amount||0),0);
  setText('d-rev', DB.fRI(rev));
  setText('d-exp', DB.fRI(exp));
  const netEl = document.getElementById('d-net');
  if (netEl) { netEl.textContent = DB.fRI(rev-exp); netEl.className = 'v ' + (rev-exp>=0 ? 'green' : 'red'); }

  renderJourneyCard(day, isToday);
  renderMetasDoDia(day);

  // Sessões do dia (3 mais recentes, ou todas — sessão atual sempre aparece)
  const sessEl = document.getElementById('sessions-list');
  const sessToggleBtn = document.getElementById('sessions-toggle');
  if (sessEl) {
    const total = (day.sessions||[]).length;
    // Numera pela ORDEM DE HORÁRIO real (start), não pela ordem em que foi
    // salva/sincronizada no aparelho — evita "Jornada 2" mostrar um horário
    // mais tarde que "Jornada 3". origIdx continua apontando pro índice
    // verdadeiro em day.sessions, então editar/apagar continua certo.
    const byTime = (day.sessions||[]).map((s,i)=>i)
      .sort((a,b) => (day.sessions[a].start||0) - (day.sessions[b].start||0));
    const finishedAll = byTime.slice().reverse()
      .map((origIdx,ri)=>sessionItem('Jornada '+(total-ri), day.sessions[origIdx], origIdx, S.curDate, day, false));
    const finishedShown = S.sessionsExpanded ? finishedAll : finishedAll.slice(0,3);
    const liveRow = (isToday && S.gps.st==='running') ? [sessionItem('Jornada atual', {start:S.gps.start,end:null,km:S.gps.km,elapsed:getElapsed()}, -1, S.curDate, day, true)] : [];
    const rows = [...liveRow, ...finishedShown];
    sessEl.innerHTML = rows.length ? rows.join('') : '<div class="empty">Nenhuma sessão registrada</div>';
  }
  if (sessToggleBtn) {
    sessToggleBtn.style.display = (day.sessions||[]).length > 3 ? '' : 'none';
    sessToggleBtn.innerHTML = S.sessionsExpanded ? 'Ver menos' : 'Ver todas &#8250;';
  }
  // Corridas: removido daqui (23/07/2026, pedido do Yuri) — já ficam
  // acessíveis dentro do detalhe de cada jornada (seta expande e lista via
  // sessionItem, que já mescla Importadas + Automáticas). Lista de
  // Importadas ainda mora na tela Corridas separada (navTo('corridas')).
  // Lançamentos (5 mais recentes, ou todos)
  const lEl = document.getElementById('jorn-lanc-list');
  const lancToggleBtn = document.getElementById('lanc-toggle');
  if (lEl) {
    const today = DB.today();
    const allItems = [
      ...day.revenues.map(r=>Object.assign({},r,{_type:'receita'})),
      ...day.expenses.map(e=>Object.assign({},e,{_type:'despesa'}))
    ];
    // Mostrar todos os lançamentos do dia sem filtro de tab
    const items = allItems;
    const show = S.lancExpanded ? items : items.slice(0,5);
    lEl.innerHTML = show.length ? show.map(it => lancItemJornada(it)).join('') : '<div class="empty">Nenhum lançamento registrado</div>';
    if (lancToggleBtn) {
      lancToggleBtn.style.display = items.length > 5 ? '' : 'none';
      lancToggleBtn.innerHTML = S.lancExpanded ? 'Ver menos' : 'Ver todas &#8250;';
    }
  }
  reapplyOpenTripDetail();
  reapplyOpenJss();
}

function renderJourneyCard(day, isToday) {
  const jc = document.getElementById('journey-card');
  if (!jc) return;
  let permWarn = document.getElementById('journey-perm-warning');
  if (!permWarn && jc.parentNode) {
    permWarn = document.createElement('div'); permWarn.id = 'journey-perm-warning'; permWarn.onclick = openFlashPermissionSetup;
    permWarn.style.cssText = 'display:none;align-items:flex-start;justify-content:space-between;gap:10px;background:#FFF7ED;border:1px solid #FDBA74;border-radius:14px;padding:11px 12px;margin:0 0 10px;cursor:pointer';
    permWarn.innerHTML = '<div style="min-width:0"><div style="font-size:12.5px;font-weight:800;color:#9A3412">⚠ Funções do MōB incompletas</div><div id="journey-perm-warning-text" style="font-size:11px;color:#C2410C;line-height:1.35;margin-top:2px"></div></div><span style="font-size:11px;font-weight:800;color:#9A3412;white-space:nowrap">Ativar ›</span>';
    jc.parentNode.insertBefore(permWarn, jc);
  }
  renderFlashPermissionWarnings();
  const cardDate = day?.date || S.curDate || DB.today();
  // Card de sessão atual (timer/km/botão) só faz sentido em "hoje" — em dia
  // passado não existe jornada "em andamento" possível, então esse card
  // some por completo. O restante da função continua rodando igual: os
  // valores que ela calcula (jc-trips, jc-total, jc-kmtotal, jc-kmativo,
  // jc-kmdesl, jc-rsh, jc-rskm) agora moram no card "Total do dia" — que é
  // um elemento IRMÃO de journey-card, não um filho, e precisa continuar
  // sendo populado em qualquer dia, passado ou presente.
  const jcWrap = document.getElementById('journey-wrap');
  if (jcWrap) jcWrap.style.display = isToday ? '' : 'none';
  const g = S.gps;
  const finished = day.sessions || [];
  const finishedElapsed = finished.reduce((s,x)=>s+(x.elapsed||0),0);
  const finishedKm = finished.reduce((s,x)=>s+(x.km||0),0);
  const live = isToday && g.st === 'running';
  const liveElapsed = live ? getElapsed() : 0;
  const liveKm = live ? g.km : 0;
  const totalElapsed = Math.max(0, finishedElapsed + liveElapsed + (day.tempoAdjust || 0));
  const totalKm = finishedKm + liveKm + (day.kmAdjust || 0);
  const state = isToday ? g.st : 'idle';

  let cls = 'idle';
  if (state === 'running') cls = 'running';
  else if (finished.length > 0) cls = 'ended';
  jc.className = 'journey ' + cls;

  setText('jc-status-text',
    state === 'running' ? 'Online' :
    finished.length > 0 ? 'Concluído' : 'Iniciar'
  );
  const th = Math.floor(liveElapsed/3600), tm = Math.floor(liveElapsed%3600/60), ts = liveElapsed%60;
  setText('jc-timer', p2(th)+':'+p2(tm)+':'+p2(ts));
  setText('jc-sub',
    state === 'running' ? 'Sessão atual · iniciada '+fmtHM(g.start) :
    finished.length > 0 ? finished.length+(finished.length===1?' jornada hoje':' jornadas hoje') :
    'Nenhuma sessão iniciada'
  );
  setText('jc-km', liveKm.toFixed(1));
  const tth = Math.floor(totalElapsed/3600), ttm = Math.floor(totalElapsed%3600/60);
  setText('jc-total', tth+'h'+p2(ttm)+'m');
  setText('jc-kmtotal', totalKm.toFixed(1)+' km');
  // CORRIGIDO (23/07/2026, confirmado com dado real pelo Yuri): esses 4
  // KPIs do card principal (corridas, km ativo, R$/h, R$/km) só somavam
  // day.trips (Importadas) — sessão só com automáticas mostrava tudo
  // zerado mesmo com corridas de verdade rodando. Mesma correção que já
  // tinha sido feita em rev/sessionItem/renderMetasDoDia, faltava aqui.
  const autoTripsHoje = day.autoTrips || [];

  // Banner "aguardando confirmação" — pedido do Yuri (16/08/2026), substitui
  // o botão fixo "Importar corridas". Conta corridas que ainda não foram
  // confirmadas por vídeo do histórico (status 'capturada' ou 'estimada') —
  // 'confirmada' só existe depois do vídeo, então isso mede exatamente o
  // que falta conferir naquele dia. Aparece em qualquer dia navegado, some
  // sozinho quando não sobra pendência.
  const pcbEl = document.getElementById('pending-confirm-banner');
  if (pcbEl) {
    // Não interpreta cache ainda carregando como zero corridas. Durante refresh,
    // mantém o último estado visual válido do mesmo dia.
    const autoTripsReady = S._autoTripsDataDate === cardDate;
    if (autoTripsReady) {
      const pendingCount = autoTripsHoje.filter(t => t.status !== 'confirmada' || t.value_needs_review === true).length;
      pcbEl.dataset.dataDate = cardDate;
      if (pendingCount > 0) {
        pcbEl.style.display = 'flex';
        setText('pcb-title', pendingCount + (pendingCount===1 ? ' corrida aguardando confirmação' : ' corridas aguardando confirmação'));
      } else {
        pcbEl.style.display = 'none';
      }
    } else if (pcbEl.dataset.dataDate && pcbEl.dataset.dataDate !== cardDate) {
      pcbEl.style.display = 'none';
      pcbEl.dataset.dataDate = cardDate;
    }
  }

  // Barra Online/Buscando passageiro/Em corrida — real, vinda da máquina de
  // estados do APK (ver computeStatusBreakdown). No card da jornada ATUAL
  // só faz sentido enquanto a sessão está rodando (é o que está sendo
  // contado agora); ao encerrar, o dado sai daqui e já está consolidado no
  // "Total do dia" logo abaixo — pedido do Yuri (16/08/2026), evita
  // duplicar/deixar a mesma barra "grudada" depois de já ter encerrado.
  // As barras são atualizadas exclusivamente pela timeline contínua V3.
  // A rotina base não sobrescreve mais a leitura real com auto_trips.
  const jcBd = document.getElementById('jc-breakdown');
  const tdBdRow = document.getElementById('td-bd-row');

  setText('jc-trips', day.trips.length + autoTripsHoje.length);
  setText('jc-trips2', day.trips.length + autoTripsHoje.length);
  const aceitasCount = day.trips.length + autoTripsHoje.length;
  const declinedCount = (S.declined || []).length;
  setText('jc-aceitas', aceitasCount);
  setText('jc-recusadas', declinedCount);
  const corridasSubEl = document.getElementById('jc-corridas-sub');
  if (corridasSubEl) corridasSubEl.style.display = (aceitasCount>0 || declinedCount>0) ? 'flex' : 'none';
  const kmAtivo = day.trips.reduce((s,t)=>s+(t.km||0),0)
                + autoTripsHoje.reduce((s,t)=>s+(parseFloat(t.real_km_trip ?
===== loadAuto @ 656261 =====
><b>Planejar</b></div></div>`;
  }
  stack.innerHTML = html;
}

// ── Planejar Jornada ─────────────────────────────────────────
let pjSelDays = [];
let pjSchedules = [];
const PJ_DOW = ['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'];
function openPlanJornada() {
  pjSelDays = []; pjSchedules = [];
  document.querySelectorAll('.pj-day').forEach(b => b.classList.remove('on'));
  const mEl = document.getElementById('pj-money'); if (mEl) mEl.value = '';
  const kEl = document.getElementById('pj-km'); if (kEl) kEl.value = '';
  renderPjSchedules();
  renderPjList();
  navTo('planejar-jornada');
}
function pjToggleDay(d) {
  const i = pjSelDays.indexOf(d);
  if (i === -1) pjSelDays.push(d); else pjSelDays.splice(i,1);
  const btn = document.querySelector('.pj-day[data-d="'+d+'"]');
  if (btn) btn.classList.toggle('on', pjSelDays.includes(d));
}
function pjAddSchedule() { pjSchedules.push({start:'', end:''}); renderPjSchedules(); }
function pjRemoveSchedule(idx) { pjSchedules.splice(idx, 1); renderPjSchedules(); }
function renderPjSchedules() {
  const el = document.getElementById('pj-schedules');
  if (!el) return;
  if (!pjSchedules.length) { el.innerHTML = '<p style="font-size:12px;color:var(--muted);margin:2px 0">Nenhum horário adicionado</p>'; return; }
  el.innerHTML = pjSchedules.map((s,i) => `
    <div class="pj-sched-row">
      <input type="time" class="ag-input" value="${s.start}" onchange="pjSchedules[${i}].start=this.value">
      <span>→</span>
      <input type="time" class="ag-input" value="${s.end}" onchange="pjSchedules[${i}].end=this.value">
      <button class="pj-sched-del" onclick="pjRemoveSchedule(${i})">&#x2715;</button>
    </div>
  `).join('');
}
function pjSaveEntry() {
  if (!pjSelDays.length) { showToast('Selecione ao menos um dia', 'error'); return; }
  const validSched = pjSchedules.filter(s => s.start && s.end);
  const moneyGoal = parseFloat(document.getElementById('pj-money')?.value||0) || null;
  const kmGoal = parseFloat(document.getElementById('pj-km')?.value||0) || null;
  const plans = DB.planJornada();
  const entry = { id: DB.uid(), days: pjSelDays.slice(), moneyGoal, kmGoal, schedules: validSched };
  plans.push(entry);
  DB.savePlanJornada(plans);
  SYNC.push('plan_jornada', { id: entry.id, days: entry.days, money_goal: entry.moneyGoal,
    km_goal: entry.kmGoal, schedules: entry.schedules });
  pjSelDays = []; pjSchedules = [];
  document.querySelectorAll('.pj-day').forEach(b => b.classList.remove('on'));
  const mEl = document.getElementById('pj-money'); if (mEl) mEl.value = '';
  const kEl = document.getElementById('pj-km'); if (kEl) kEl.value = '';
  renderPjSchedules();
  renderPjList();
  renderMetasDoDia(DB.day(S.curDate));
  showToast('Jornada planejada salva!');
}
function pjRemoveEntry(id) {
  showConfirm('Remover essa jornada planejada?', () => {
    DB.savePlanJornada(DB.planJornada().filter(p => p.id !== id));
    SYNC.del('plan_jornada', { id });
    renderPjList();
    renderMetasDoDia(DB.day(S.curDate));
  });
}
function renderPjList() {
  const el = document.getElementById('pj-list');
  if (!el) return;
  const plans = DB.planJornada();
  if (!plans.length) { el.innerHTML = '<div class="empty">Nenhuma jornada planejada ainda</div>'; return; }
  el.innerHTML = plans.map(p => {
    const daysTxt = (p.days||[]).slice().sort().map(d=>PJ_DOW[d]).join(' · ');
    const goalParts = [];
    if (p.moneyGoal) goalParts.push(DB.fRI(p.moneyGoal));
    if (p.kmGoal) goalParts.push(p.kmGoal+' km');
    const goalTxt = goalParts.length ? goalParts.join(' · ') : 'Sem meta definida';
    const chips = (p.schedules||[]).map(s => `<span class="pj-chip">${s.start} → ${s.end}</span>`).join('');
    return `<div class="pj-card">
      <div class="pj-card-top"><div class="pj-card-days">${daysTxt}</div><button class="pj-card-del" onclick="pjRemoveEntry('${p.id}')">&#x2715;</button></div>
      <div class="pj-card-goal">${goalTxt}</div>
      <div class="pj-card-times">${chips}</div>
    </div>`;
  }).join('');
}

function sessionItem(label, sess, origIdx, dateStr, day, live) {
  const startMs = sess.start || 0, endMs = sess.end || 0;
  const km = sess.km || 0, elapsedSecs = sess.elapsed || 0;
  const dur = fmtDur((elapsedSecs||0)/60);
  const range = fmtHM(startMs) + '–' + (endMs ? fmtHM(endMs) : (live ? 'Agora' : '—'));
  const status = live ? 'Online' : 'Encerrada';
  const badgeCls = live ? 'live' : 'end';
  const cardId = 'jss-' + dateStr + '-' + origIdx;

  // Corridas desta sessão — filtrar por horário (suporta t.ts e t.time HH:MM)
  const trips = (day?.trips||[]).filter(t => {
    let ts = t.ts || 0;
    if (!ts && t.time && dateStr) {
      // Construir timestamp a partir de "HH:MM" + data da sessão
      ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
    }
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  // Corridas automáticas desta sessão — mesmo filtro por horário, usando
  // trip_started_at/accepted_at (autoTripTs, ver loadAutoTripsForDay).
  const autoTrips = (day?.autoTrips||[]).filter(t => {
    const ts = autoTripTs(t);
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  const corridas = trips.length + autoTrips.length;
  const ganhos = trips.reduce((s,t)=>s+(t.value||0),0) + autoTrips.reduce((s,t)=>s+(t.offer_value||0),0);
  // Km ativo = soma do km de cada corrida realizada (tempo 'em corrida')
  // Km deslocamento = km total do GPS MENOS o km ativo (tempo rodando sem
  // corrida — indo pro ponto, retornando pra casa, esperando chamado etc.)
  const kmAtivo = trips.reduce((s,t)=>s+(parseFloat(t.km)||0),0)
                + autoTrips.reduce((s,t)=>s+(parseFloat(t.real_km_trip ?? t.offer_km_trip)||0),0);
  const kmDesloc = Math.max(0, km - kmAtivo);

  const horasDecimal = elapsedSecs > 0 ? elapsedSecs/3600 : 0;
  const rshVal = horasDecimal > 0 ? ganhos/horasDecimal : null;
  const rskmVal = km > 0 ? ganhos/km : null;
  const rsPorHora = rshVal !== null ? DB.fR(rshVal) : '—';
  const rsPorKm = rskmVal !== null ? DB.fR(rskmVal) : '—';
  const rshCls = rshVal !== null ? rateColorClass('rhora', rshVal) : '';
  const rskmCls = rskmVal !== null ? rateColorClass('rkm', rskmVal) : '';
  // Lucro da sessão = soma do lucro de cada corrida (mesmo cálculo do
  // perfStripHtml/detalhe de corrida: valor - custoPorKm*km), só quando o
  // custo do veículo está configurado — igual já vale por corrida.
  const cvCfg = DB.cfg().custoVeiculo;
  const hasCusto = !!(cvCfg && cvCfg.configurado);
  const lucroVal = hasCusto ? ganhos - (cvCfg.custoPorKm||0) * kmAtivo : null;
  const lucroCls = lucroVal !== null ? rateColorClass('lucro', lucroVal) : '';
  // Barra Online/Buscando passageiro/Em corrida desta sessão — mesma fonte
  // real da máquina de estados do APK, agora escopada só às autoTrips que
  // caem dentro do horário desta sessão (já filtradas acima).
  const sessBd = (typeof journeyBreakdownForSession === 'function')
    ? journeyBreakdownForSession(sess, elapsedSecs, km, autoTrips)
    : computeStatusBreakdown(elapsedSecs, km, autoTrips);
  const bdListId = 'bl-' + cardId;
  const editBtn = !live ? `<button class="jss-act" onclick="event.stopPropagation();editJss('${dateStr}',${origIdx})" title="Editar"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg></button>
        <button class="jss-act del" onclick="event.stopPropagation();deleteJss('${dateStr}',${origIdx})" title="Excluir"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/></svg></button>` : '';

  return `<div class="jss-card ${badgeCls}" id="${cardId}">
    <div class="jss-row" onclick="toggleJss('${cardId}')">
      <div class="jss-info">
        <div class="jss-top"><span class="jss-name">${label}</span><span class="jss-badge ${badgeCls}">${status}</span>${editBtn}</div>
        <div class="jss-sub">${range} · ${dur}</div>
      </div>
      <div style="text-align:right;flex-shrink:0;display:flex;flex-direction:column;align-items:flex-end;gap:2px">
        <span style="font-size:14px;font-weight:800;color:var(--text)">${(sess.km||0).toFixed(1)} km</span>
        <span class="jss-ganhos">${DB.fR(ganhos)}</span>
      </div>
      <div class="jss-chev"><svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="6 9 12 15 18 9"/></svg></div>
    </div>
    <div class="jss-rate-row">
      <div class="jss-rcard ${rshCls}"><strong>${rsPorHora}</strong><span>R$/hora</span></div>
      <div class="jss-rcard ${rskmCls}"><strong>${rsPorKm}</strong><span>R$/km</span></div>
      ${hasCusto ? `<div class="jss-rcard ${lucroCls}"><strong>${DB.fR(lucroVal)}</strong><span>Lucro</span></div>` : `<div class="jss-rcard"><strong>—</strong><span>Lucro</span></div>`}
    </div>
    ${elapsedSecs > 0 ? `<div class="jss-statusbar">
      <div class="sbar-wrap" onclick="event.stopPropagation();toggleBar('${bdListId}')"><div class="sbar">${buildSbarHtml(sessBd)}</div></div>
      <div class="sbar-list" id="${bdListId}">${buildSrowsHtml(sessBd)}</div>
    </div>` : ''}
    <div class="jss-trips">
      <div class="jss-trips-head">
        <h4>Corridas desta jornada</h4>
        ${corridas > 0 ? `<span>${corridas} corrida${corridas !== 1 ? 's' : ''}</span>` : ''}
      </div>
      ${corridas > 0
        ? [
            ...trips.map(t => ({ ts: t.ts || (t.time?new Date(dateStr+'T'+t.time+':00').getTime():0), html: tripItem(t) })),
            ...autoTrips.map(t => ({ ts: autoTripTs(t), html: autoTripItem(t) })),
          ].sort((a,b)=>b.ts-a.ts).map(x=>x.html).join('')
        : '<div class="jss-trips-empty">Nenhuma corrida nesta jornada</div>'}
    </div>
  </div>`;

}
// ── Editar sessão ─────────────────────────────────────────────────────────
function editJss(dateStr, idx) {
  const day = DB.day(dateStr);
  const sess = (day.sessions || [])[idx];
  if (!sess) return;

  document.getElementById('jss-edit-date').value = dateStr;
  document.getElementById('jss-edit-idx').value  = idx;
  document.getElementById('jss-edit-km').value   = (sess.km || 0).toFixed(1);

  // Hora início e fim a partir dos timestamps da sessão
  const fmt = ms => {
    if (!ms) return '';
    const d = new Date(ms);
    return String(d.getHours()).padStart(2,'0') + ':' + String(d.getMinutes()).padStart(2,'0');
  };
  document.getElementById('jss-edit-inicio').value = fmt(sess.start);
  document.getElementById('jss-edit-fim').value    = fmt(sess.end);

  openMo('mo-jss-edit');
}

function saveJss() {
  const dateStr = document.getElementById('jss-edit-date').value;
  const idx     = parseInt(document.getElementById('jss-edit-idx').value);
  const km      = parseFloat(document.getElementById('jss-edit-km').value) || 0;
  const inicioStr = document.getElementById('jss-edit-inicio').value; // "HH:MM"
  const fimStr    = document.getElementById('jss-edit-fim').value;    // "HH:MM"

  const day = DB.day(dateStr);
  if (!day.sessions || !day.sessions[idx]) { closeMo('mo-jss-edit'); return; }

  const sess = day.sessions[idx];

  // Recalcular timestamps de início e fim mantendo a data original
  const toMs = (base, timeStr) => {
    if (!timeStr) return base;
    const [hh, mm] = timeStr.split(':').map(Number);
    const d = new Date(base || Date.now());
    d.setHours(hh, mm, 0, 0);
    return d.getTime();
  };
  const newStart = toMs(sess.start, inicioStr);
  const newEnd   = toMs(sess.end || sess.start, fimStr);
  // Elapsed recalculado a partir da diferença início→fim
  const elapsed  = newEnd > newStart ? Math.round((newEnd - newStart) / 1000) : (sess.elapsed || 0);

  sess.km      = km;
  sess.start   = newStart;
  sess.end     = newEnd;
  sess.elapsed = elapsed;
  DB.saveDay(day);

  // Sincronizar com Supabase
  if (_supaUser && sess.id) {
    _SUPA.from('sessions').update({
      km:           parseFloat(km.toFixed(3)),
      start_ms:     newStart,
      end_ms:       newEnd,
      elapsed_secs: elapsed
    }).eq('id', sess.id).then(() => {}).catch(() => {});
  }

  closeMo('mo-jss-edit');
  renderJornada();
  if (S.sec === 'relatorios') renderHome();
  showToast('Sessão atualizada');
}

function deleteJss(dateStr, idx) {
  showConfirm('Excluir esta sessão?', () => {
    const day = DB.day(dateStr);
    if (!day.sessions || !day.sessions[idx]) return;

    const sess = day.sessions[idx];
    day.sessions.splice(idx, 1);
    DB.saveDay(day);

    // Remover do Supabase (delete real, não update)
    if (sess.id) {
      SYNC.del('sessions', { id: sess.id });
    }

    renderJornada();
    if (S.sec === 'relatorios') renderHome();
    showToast('Sessão excluída');
  }, { confirmLabel: 'Excluir', danger: true, cancelLabel: 'Cancelar' });
}


// CORRIGIDO (15/08/2026, relatado pelo Yuri): mesmo bug do toggleTripDetail
// (comentário abaixo, 24/07) só que no card da jornada — toggleJss só
// mexia numa classe CSS, sem guardar estado. O refresh automático de 25s
// (_refreshAutoTripsIfOnJornada) recria os cards do zero sempre fechados,
// então a expansão "voltava a fechar sozinha" em segundos. Mesmo padrão
// de correção: guarda o id aberto, reaplica depois de qualquer redesenho.
let _openJssId = null;
function toggleJss(id){
  const el=document.getElementById(id);
  if(!el) return;
  const willOpen = !el.classList.contains('open');
  el.classList.toggle('open');
  _openJssId = willOpen ? id : null;
}
function reapplyOpenJss(){
  if (!_openJssId) return;
  const el = document.getElementById(_openJssId);
  if (el) el.classList.add('open');
}
// CORRIGIDO (24/07/2026, relatado pelo Yuri): o painel de detalhe de uma
// corrida é só um display:none/block num elemento — quando a tela
// redesenha os cards (o refresh automático de 25s/60s da Jornada/Home,
// por exemplo), os elementos são recriados do zero, sempre fechados por
// padrão, perdendo o que estava aberto na hora errada (parecia fechar
// sozinho, sem motivo). Guarda qual tid está aberto e reaplica depois de
// qualquer redesenho.
let _openTripDetailTid = null;
// Linha vertical da rota (Aceite/Embarque/Destino) tem que parar exatamente
// no centro do último marcador (pin de Destino) — pedido do Yuri
// (16/08/2026): como os marcadores têm tamanhos diferentes (círculo
// pequeno vs pin maior), um valor fixo de CSS nunca acerta em todo caso,
// então mede a posição real na tela depois de renderizado.
function sizeRouteLine(panelEl) {
  try {
    const block = panelEl.querySelector('.route-block');
    if (!block) return;
    const marks = block.querySelectorAll('.route-mark');
    const line = block.querySelector('.route-line');
    if (!line || marks.length < 2) return;
    const first = marks[0], last = mar
===== loadAuto @ 789685 =====
rando vídeo · ' + done + '/' + total + ' quadros ' + dot, false);
      });
      for (let i = 0; i < frames.length; i++) {
        try {
          const trips = await extractWithRetry(
            () => extractTripsFromBase64(frames[i], 'image/jpeg', plat),
            () => updateRing(retryIcon + ' Processando novamente...', true)
          );
          absorb(trips);
        } catch(e) { console.warn('Frame erro (desistiu após 3 tentativas):', e); }
        updateRing('Analisando vídeo · ' + (i+1) + '/' + frames.length + ' quadros ' + dot, false);
      }
    } catch(e) { videoFrameReadFailed = true; console.warn('Erro vídeo '+(v+1)+':', e); }
  }
  if (videos.length && videoFrameReadFailed && unique.length === 0) {
    showToast('Não consegui decodificar os quadros deste vídeo neste aparelho. O arquivo pode ser válido; tente novamente.', 'error');
    closeMo('mo-import-confirm');
    return;
  }

  // Só envia o arquivo original depois que a leitura local terminou.
  // Em celular/WebView, fazer upload e seek/decodificação simultaneamente
  // do mesmo File grande pode bloquear o decoder e deixar a tela parada.
  if (videos.length) updateRing('Salvando vídeo para conferência ' + dot, false);
  const _videoUploadResults = await Promise.all(
    videos.map(file => uploadImportVideo(file, operationalPlat, operationalDate))
  );
  window._pendingImportVideoIds = _videoUploadResults.filter(Boolean);
  if (videos.length && window._pendingImportVideoIds.length < videos.length) {
    showToast('Corridas importadas, mas o vídeo de auditoria não pôde ser salvo — confira sua conexão.', 'error');
  }
  flushDetail(); // fragmento de detalhe que ficou aberto no fim do vídeo

  // Resolve conflitos de horário: a regra pedida pra IA é "cada horário =
  // uma corrida só", mas o dedup acima só barra repetição idêntica
  // (mesmo horário E mesmo valor) — se o mesmo horário veio com valores
  // DIFERENTES em frames diferentes (mistura de cartão vizinho num frame
  // só), os dois passavam pra lista como corridas separadas, inflando a
  // contagem e o total. Aqui, pra cada (plataforma, horário), fica só a
  // leitura de valor que mais se repetiu entre os frames — a leitura
  // certa tende a se confirmar igual em vários frames seguidos (o vídeo
  // se sobrepõe), enquanto uma leitura errada por mistura de cartão
  // costuma aparecer uma vez só.
  function resolveTimeConflicts(list) {
    const groups = new Map();
    list.forEach(t => {
      const k = (t.platform || '') + '_' + t.time;
      (groups.get(k) || groups.set(k, []).get(k)).push(t);
    });
    const resolved = [];
    groups.forEach(items => {
      if (items.length === 1) { resolved.push(items[0]); return; }
      const byValue = new Map();
      items.forEach(t => {
        const vk = (t.value || 0).toFixed(2);
        (byValue.get(vk) || byValue.set(vk, []).get(vk)).push(t);
      });
      let best = null;
      byValue.forEach(arr => { if (!best || arr.length > best.length) best = arr; });
      // dentro do grupo vencedor, prefere o item com endereço mais completo
      let rep = best[0];
      best.forEach(t => {
        const len = (t.originFull || '').length + (t.destFull || '').length;
        const repLen = (rep.originFull || '').length + (rep.destFull || '').length;
        if (len > repLen) rep = t;
      });
      resolved.push(rep);
    });
    return resolved;
  }
  const resolvedUnique = resolveTimeConflicts(unique);
  unique.length = 0;
  unique.push(...resolvedUnique);

  // Funde os detalhes nas corridas da lista — SÓ completa km/tempo/endereço
  // de uma corrida que a LISTA já confirmou (com valor de verdade). Nunca
  // cria corrida nova a partir de detalhe sozinho: sem valor, não tem como
  // confirmar que é real, e foi exatamente isso que gerava corrida fantasma
  // no import (fragmento de detalhe virando corrida "avulsa" por engano).
  details.forEach(d => {
    let cand = null;
    if (d.time) cand = unique.find(u => u.time === d.time && !u._enriched);
    if (!cand && (d.originFull || d.destFull)) {
      cand = unique.find(u => !u._enriched &&
        ((d.originFull && u.originFull === d.originFull) || (d.destFull && u.destFull === d.destFull)));
    }
    if (cand) {
      cand._enriched = true;
      if (d.km != null && d.km > 0)       { cand.km = d.km; cand.kmEstimado = false; }
      if (d.duration != null && d.duration > 0) cand.duration = d.duration;
      // Endereço do detalhe é a linha inteira, sem truncar — prefere ele.
      if (d.originFull) { cand.originFull = d.originFull; cand.origin = d.origin; }
      if (d.destFull)   { cand.destFull = d.destFull; cand.dest = d.dest; }
    }
  });

  const day = DB.day(S.curDate);

  // Casa cada corrida do vídeo com auto_trips do dia ANTES de mostrar a
  // tela (pedido do Yuri, 16/08/2026) — permite separar "vai só corrigir
  // valor" de "é corrida nova" já na seleção, e esconder o que não muda
  // nada em vez de mostrar tudo misturado.
  const autoTripsPool = (await loadAutoTripsForOperationalDay(operationalPlat, operationalDate)) || [];
  const numEq = (a,b,tol) => Math.abs((a||0)-(b||0)) <= (tol||0.02);
  const annotated = unique.map((t,i) => {
    const matched = matchAutoTrip(t, autoTripsPool);
    if (matched) autoTripsPool.splice(autoTripsPool.indexOf(matched), 1); // consumido — nunca casa duas vezes
    const base = {...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _exists: isTripDuplicate(t, day.trips)};
    if (matched) {
      const newVal = parseFloat(t.value)||0;
      return {...base, _matchedAutoTripId: matched.id, _isNew: false,
        _oldValue: matched.offer_value, _changed: !numEq(matched.offer_value, newVal, 0.02)};
    }
    return {...base, _matchedAutoTripId: null, _isNew: true, _changed: true};
  });
  // Só mostra o que de fato vai mudar algo — casada sem diferença de valor
  // não aparece (nada a fazer), nova sem valor legível também não aparece
  // (sem valor não dá pra confirmar que é corrida de verdade).
  window._pendingTrips = annotated.filter(t => t._isNew ? (parseFloat(t.value)||0) > 0 : t._changed);
  // Corrida NOVA nunca confia no endereço do vídeo sem validar — mesma
  // regra do Kotlin (looksLikeAddress). Sem validação, fica sem endereço
  // em vez de arriscar lixo/mensagem de chat virando corrida (pedido
  // explícito do Yuri: "nunca endereço, a não ser que garanta").
  window._pendingTrips.forEach(t => {
    if (!t._isNew) return;
    if (t.originFull && !looksLikeAddress(t.originFull)) { t.originFull = ''; t.origin = ''; }
    if (t.destFull && !looksLikeAddress(t.destFull)) { t.destFull = ''; t.dest = ''; }
  });

  // Se algo foi declarado no pre-check, mostra a conferencia antes da lista.
  // Se pulou (tudo null), vai direto pra lista de confirmacao de sempre.
  const hasPrecheck = importPrecheck.uberCount != null || importPrecheck.p99Count != null;
  if (hasPrecheck) showImportMatch(unique); else showImportConfirm();
}

function showImportMatch(unique) {
  const uberFound = unique.filter(t => t.platform === 'uber');
  const p99Found  = unique.filter(t => t.platform === '99');
  const uberFoundCount = uberFound.length, uberFoundVal = uberFound.reduce((s,t)=>s+(t.value||0),0);
  const p99FoundCount  = p99Found.length,  p99FoundVal  = p99Found.reduce((s,t)=>s+(t.value||0),0);

  const checks = [];
  let anyMissing = false, anyExtra = false;
  // Checagem EXATA: encontrar MAIS do que o declarado é tão errado quanto
  // encontrar menos (significa corrida lida em dobro/fundida). 18/17 não
  // pode aparecer como 'bateu certinho'. Valor com tolerância de 1 centavo.
  const evalPlat = (label, foundCount, foundVal, decCount, decVal) => {
    let ok = foundCount === decCount;
    if (ok && decVal != null) ok = Math.abs(foundVal - decVal) < 0.01;
    if (!ok) { if (foundCount > decCount || (decVal != null && foundVal > decVal + 0.009)) anyExtra = true; else anyMissing = true; }
    checks.push({label, ok, txt: `${foundCount}/${decCount} · ${DB.fRI(foundVal)}${decVal != null ? ' de ' + DB.fRI(decVal) : ''}`});
  };
  if (importPrecheck.uberCount != null) evalPlat('Uber', uberFoundCount, uberFoundVal, importPrecheck.uberCount, importPrecheck.uberValue);
  if (importPrecheck.p99Count != null) evalPlat('99', p99FoundCount, p99FoundVal, importPrecheck.p99Count, importPrecheck.p99Value);
  const allOk = !anyMissing && !anyExtra;
  const pillsHtml = checks.map(c => `<span style="font-size:11px;font-weight:700;padding:4px 10px;border-radius:99px;background:${c.ok?'#DCFCE7':'#FEE2E2'};color:${c.ok?'#16A34A':'#DC2626'}">${c.label} ${c.txt} ${c.ok?'✓':'✗'}</span>`).join('');

  const warnTitle = anyExtra ? 'Achei corridas a mais' : 'Faltou alguma coisa';
  const warnMsg = anyExtra
    ? 'Alguma corrida foi lida em dobro ou misturada. Na lista, desmarque as que estiverem repetidas ou com dados estranhos antes de importar.'
    : 'Confira se subiu todos os prints/vídeos desse app';
  const cardHtml = allOk
    ? `<div style="border-radius:16px;padding:16px;text-align:center;margin-bottom:14px;background:#ECFDF3;border:1.5px solid #BBF7D0">
        <div style="width:44px;height:44px;border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto 10px;background:#DCFCE7;color:#16A34A"><svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg></div>
        <b style="display:block;font-size:14px;color:#0F172A;margin-bottom:4px">Importação validada</b>
        <span style="font-size:12px;color:#64748B;line-height:1.5">Os valores conferem com o que você informou</span>
        <div style="display:flex;gap:8px;justify-content:center;margin-top:10px;flex-wrap:wrap">${pillsHtml}</div>
      </div>`
    : `<div style="border-radius:16px;padding:16px;text-align:center;margin-bottom:14px;background:#FFFBEB;border:1.5px solid #FDE68A">
        <div style="width:44px;height:44px;border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto 10px;background:#FEF3C7;color:#D97706"><svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2.4"><circle cx="12" cy="12" r="9"/><path d="M12 8v4M12 16h.01"/></svg></div>
        <b style="display:block;font-size:14px;color:#0F172A;margin-bottom:4px">${warnTitle}</b>
        <span style="font-size:12px;color:#64748B;line-height:1.5">${warnMsg}</span>
        <div style="display:flex;gap:8px;justify-content:center;margin-top:10px;flex-wrap:wrap">${pillsHtml}</div>
      </div>`;

  document.getElementById('ic-title').textContent = 'Conferência';
  document.getElementById('ic-body').innerHTML = cardHtml;
  const btn = document.getElementById('ic-btn');
  btn.style.display = 'block';
  btn.textContent = allOk ? 'Ver corridas encontradas' : 'Ver o que encontrei';
  btn.onclick = () => { showImportConfirm(); btn.onclick = confirmImport; };
}

function showSpinner(msg) {
  return '<div style="text-align:center;padding:28px 16px">' +
    '<div style="font-size:28px;animation:vspin 1s linear infinite;display:inline-block">&#9881;</div>' +
    '<div style="margin-top:12px;font-size:13px;color:#64748B;font-weight:500">' + msg + '</div></div>';
}

// Normaliza "7:42" e "07:42" pro mesmo formato — a IA não le a hora de forma
// 100% consistente entre frames/prints (zero à esquerda variando), e sem isso
// a mesma corrida real passava pelo dedup do import como se fossem duas.
function normTime(t) {
  const m = String(t||'').match(/(\d{1,2}):(\d{2})/);
  return m ? m[1].padStart(2,'0')+':'+m[2] : String(t||'');
}
async function processMultipleImports(files, plat) {
  const total = files.length;
  document.getElementById('ic-title').textContent = 'Analisando ' + total + ' print' + (total > 1 ? 's' : '') + '...';
  document.getElementById('ic-body').innerHTML = showSpinner('Processando 1 de ' + total + '...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  const allTrips = [];
  for (let i = 0; i < files.length; i++) {
    document.getElementById('ic-body').innerHTML = showSpinner('Processando print ' + (i+1) + ' de ' + total + '...');
    try {
      const trips = await extractTripsFromImage(files[i], plat);
      allTrips.push(...trips);
    } catch(e) { console.warn('Erro print '+(i+1)+':', e); }
  }
  const day = DB.day(S.curDate);
  const seen = new Set();
  const unique = allTrips.filter(t => {
    if (!(t.value > 0)) return false; // fusão/cartão cortado gera R$0,00 — nunca é corrida real
    t.time = normTime(t.time);
    const key = t.time+'_'+(t.value||0).toFixed(2);
    if (seen.has(key)) return false;
    seen.add(key); return true;
  });
  window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
    platform: t.platform === '99' ? '99' : 'uber',
    _exists: isTripDuplicate(t, day.trips) // pre-mark duplicates
  }));
  showImportConfirm();
}

async function processVideoImport(file, plat) {
  document.getElementById('ic-title').textContent = 'Extraindo frames do vídeo...';
  document.getElementById('ic-body').innerHTML = showSpinner('Analisando vídeo...');
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');
  try {
    const frames = await extractVideoFrames(file);
    const allTrips = [];
    for (let i = 0; i < frames.length; i++) {
      document.getElementById('ic-body').innerHTML = showSpinner('Analisando frame ' + (i+1) + ' de ' + frames.length + '...');
      try {
        const trips = await extractTripsFromBase64(frames[i], 'image/jpeg', plat);
        allTrips.push(...trips);
      } catch(e) { console.warn('Frame erro:', e); }
    }
    const dayV = DB.day(S.curDate);
    const seenV = new Set();
    const unique = allTrips.filter(t => {
      t.time = normTime(t.time);
      const key = t.time+'_'+(t.value||0).toFixed(2);
      if (seenV.has(key)) return false;
      seenV.add(key); return true;
    });
    const _dayV = DB.day(S.curDate);
    window._pendingTrips = unique.map((t,i) => ({...t, _sel:true, _idx:i,
      platform: t.platform === '99' ? '99' : 'uber',
      _dup: isTripDuplicate(t, _dayV.trips)}));
    showImportConfirm();
  } catch(e) {
    closeMo('mo-import-confirm');
    showToast('Erro ao processar vídeo: ' + e.message + '. Use MP4 ou prints do histórico.', 'error');
    console.error('Video error:', e);
  }
}


// APK 1.3.9+: o WebView de alguns aparelhos não consegue decodificar certos
// screen recordings mesmo quando o arquivo é válido. Quando o bridge nativo
// estiver disponível, o Android decodifica o vídeo e devolve só os JPEGs.
const _nativeVideoExtractRequests = new Map();
window.__mobNativeVideoFrame = function(token, dataUrl) {
  const r = _nativeVideoExtractRequests.get(token);
  if (!r || !dataUrl) return;
  const raw = String(dataUrl).includes(',') ? String(dataUrl).split(',').pop() : String(dataUrl);
  if (raw) r.frames.push(raw);
};
window.__mobNativeVideoProgress = function(t
===== loadAuto @ 870354 =====
  // Marca como confirmadamente sincronizada — só depois disso é seguro
        // remover local se sumir do banco (ver _pullTripsCorrections)
        if (!error && !t._synced) { t._synced = true; dayChanged = true; }
      }
      if (dayChanged) DB.saveDay(day);
    }
  } catch(e) { console.warn('[autoSync]', e); }
}
// ── Traz de volta correções feitas direto no banco (ex: km ajustado
// manualmente) — sem isso, o auto-sync só empurrava dado local pro
// Supabase e nunca puxava correções de volta pro celular.
async function _pullTripsCorrections() {
  if (!_supaUser || !navigator.onLine) return;
  try {
    const cutoff = new Date(Date.now()-7*24*3600*1000).toISOString().slice(0,10); // últimos 7 dias
    const { data: remote, error } = await _SUPA.from('trips').select('*')
      .eq('user_id', _supaUser.id).gte('date', cutoff);
    if (error || !remote) return;
    let changed = false;
    const remoteIds = new Set(remote.map(r => r.id));
    remote.forEach(r => {
      const day = DB.day(r.date);
      if (!day.trips) return;
      const idx = day.trips.findIndex(t => t.id === r.id);
      if (idx === -1) return;
      const local = day.trips[idx];
      // Só sobrescreve os campos que podem ter sido corrigidos manualmente
      // no banco (km, plataforma, taxa, pedágio) — não mexe em nada que o
      // usuário possa ter editado localmente enquanto offline.
      const rKm = parseFloat(r.km)||0;
      const localHasEstimate = local.kmEstimado && (parseFloat(local.km)||0) > 0;
      if (localHasEstimate && rKm === 0 && !r.km_estimado) {
        // Estimativa local ainda não chegou no banco (upsert falhou/pendente)
        // — reenviar em vez de apagar a estimativa
        _SUPA.from('trips').upsert({ id: local.id, user_id: _supaUser.id,
          km: parseFloat(local.km), km_estimado: true }, { onConflict: 'id' }).catch(()=>{});
      } else if (Math.abs((local.km||0) - rKm) > 0.05 || !!local.kmEstimado !== !!r.km_estimado) {
        local.km = rKm; local.kmEstimado = !!r.km_estimado; changed = true;
      }
      if (r.platform && local.platform !== r.platform) {
        local.platform = r.platform; changed = true;
      }
      if (changed) { day.trips[idx] = local; DB.saveDay(day); }
    });
    // Remove local corridas que foram apagadas direto no banco — só quando
    // a corrida já tinha sido confirmadamente sincronizada antes (_synced),
    // pra nunca arriscar apagar algo criado localmente ainda não enviado.
    const dates = DB.allDates().filter(d => d >= cutoff);
    for (const date of dates) {
      const day = DB.day(date);
      if (!day.trips || !day.trips.length) continue;
      const before = day.trips.length;
      day.trips = day.trips.filter(t => !(t._synced && t.id && !remoteIds.has(t.id)));
      if (day.trips.length !== before) { DB.saveDay(day); changed = true; }
    }
    if (changed) {
      try { if (S.sec==='corridas') renderCorridasActive(); } catch(e) {}
      try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
      try { renderHome(); } catch(e) {}
    }
  } catch(e) { console.warn('[_pullTripsCorrections]', e); }
}
async function _syncTick() {
  // ORDEM CRÍTICA: pull primeiro, push depois. Se o push rodasse antes,
  // ele reenviaria o valor local (potencialmente errado/desatualizado)
  // pro banco ANTES do celular ter chance de buscar uma correção feita
  // direto no banco — criando um ciclo onde a correção nunca 'gruda'.
  await _pullTripsCorrections();
  await _autoSyncLocalData();
  _backfillKmEstimates();
}
// Migração única: limpa prefixo "Bairro da/do" de corridas já salvas localmente
(function _migrateBairroNames(){
  try {
    if (localStorage.getItem('sm_bairro_clean_v1')) return;
    DB.allDates().forEach(dateStr => {
      const day = DB.day(dateStr); let ch = false;
      (day.trips||[]).forEach(t => {
        const o = _cleanBairro(t.origin||''), d = _cleanBairro(t.dest||'');
        if (o !== (t.origin||'')) { t.origin = o; ch = true; }
        if (d !== (t.dest||''))   { t.dest = d;  ch = true; }
      });
      if (ch) DB.saveDay(day);
    });
    localStorage.setItem('sm_bairro_clean_v1','1');
  } catch(e) { console.warn('[_migrateBairroNames]', e); }
})();
setTimeout(()=>{ _syncTick(); setInterval(_syncTick, 60*1000); }, 3000); // primeira vez em 3s, depois a cada 1 minuto

// Corridas automáticas novas capturadas durante o dia (pedido do Yuri,
// 23/07/2026) — INTERVALO PRÓPRIO, separado do _syncTick de propósito: se
// qualquer etapa do _syncTick (correção de trips, auto-sync, backfill de
// km) travar ou lançar erro, essa atualização não pode ficar refém disso —
// foi exatamente isso que aconteceu (corridas paravam de aparecer na
// Jornada ativa depois de um tempo, travadas no que existia no momento do
// primeiro carregamento do dia). 25s pra sentir a corrida nova rápido sem
// martelar o Supabase.
async function _refreshAutoTripsIfOnJornada() {
  if (!(S && S.sec === 'jornada' && S.curDate)) return;
  try {
    const list = await loadAutoTripsForDay(S.curDate);
    if (!Array.isArray(list)) return; // falha não vira "zero corridas"
    S.autoTrips = list; S.autoTripsDate = S.curDate; S._autoTripsDataDate = S.curDate;
    renderJornada();
  } catch(e) {}
}
setTimeout(()=>{ _refreshAutoTripsIfOnJornada(); setInterval(_refreshAutoTripsIfOnJornada, 25*1000); }, 4000);

// CORRIGIDO (24/07/2026, confirmado pelo Yuri: corridas terminadas até
// 22:21 não apareciam numa tela aberta desde antes) — a tela Corridas
// nunca tinha refresh automático como a Jornada já tem. Se ficasse parado
// nela sem trocar filtro/período nem sair e voltar, ficava presa no que
// carregou da última vez, mesmo com corridas novas terminando por trás.
// Mesmo padrão de intervalo da Jornada.
async function _refreshCorridasIfOnScreen() {
  if (!(S && S.sec === 'corridas')) return;
  try { await renderCorridasActive(); } catch(e) {}
}
setTimeout(()=>{ _refreshCorridasIfOnScreen(); setInterval(_refreshCorridasIfOnScreen, 25*1000); }, 4500);

// Mesma lógica pra Home (pedido do Yuri, 23/07/2026) — a checagem de cache
// em renderHomeGraphs() só refaz a busca quando o INTERVALO DE DATAS muda
// (troca de semana/mês/etc), nunca por tempo — uma corrida nova no meio do
// dia nunca apareceria sozinha sem isso. 60s (não precisa ser tão rápido
// quanto a Jornada, que é tempo real; Home é histórico/agregado).
async function _refreshAutoTripsIfOnHome() {
  if (!(S && S.sec === 'relatorios')) return;
  S.autoTripsHomeRange = null; // força o próximo renderHomeGraphs a rebuscar
  try { renderHomeGraphs(); } catch(e) {}
}
setTimeout(()=>{ setInterval(_refreshAutoTripsIfOnHome, 60*1000); }, 5000);

// ── Status de rede: indicador visual + reconciliação ao voltar online ──
let _netSyncing = false;
function updateNetStatus() {
  const badge = document.getElementById("net-status-badge");
  const dot = document.getElementById("net-status-dot");
  const txt = document.getElementById("net-status-text");
  if (!badge) return;
  if (!navigator.onLine) {
    badge.style.display = "flex";
    badge.style.background = "#FEF2F2";
    dot.style.background = "#EF4444";
    txt.style.color = "#B91C1C";
    txt.textContent = "Offline — dados salvos localmente";
  } else if (_netSyncing) {
    badge.style.display = "flex";
    badge.style.background = "#FFF7ED";
    dot.style.background = "#F59E0B";
    txt.style.color = "#C2410C";
    txt.textContent = "Sincronizando...";
  } else {
    badge.style.display = "none"; // tudo ok, não precisa mostrar nada
  }
}
window.addEventListener("offline", updateNetStatus);
window.addEventListener("online", async () => {
  _netSyncing = true; updateNetStatus();
  try { await SYNC.flushQueue(); await reconcileJornadaHoje(); } catch(e) {}
  _netSyncing = false; updateNetStatus();
  try { renderJornada(); } catch(e) {}
});

// ── Reconciliação: Supabase é a fonte de verdade, local é o cache ──────
async function reconcileJornadaHoje() {
  if (!_supaUser || !navigator.onLine) return;
  const today = DB.today();
  try {
    const {data: remote, error} = await _SUPA.from("sessions").select("*")
      .eq("user_id", _supaUser.id).eq("date", today);
    if (error || !remote) return;
    const day = DB.day(today);
    if (!day.sessions) day.sessions = [];
    let changed = false;
    // 1. Toda sessão remota vira a verdade local (upsert por id)
    remote.forEach(r => {
      if (!r.end_ms) return; // sessão ainda ativa remotamente — não mexe (pode ser a atual)
      const local = { id: r.id, start: r.start_ms, end: r.end_ms, km: parseFloat(r.km)||0, elapsed: r.elapsed_secs||0 };
      const idx = day.sessions.findIndex(s => s.id === r.id);
      if (idx === -1) { day.sessions.push(local); changed = true; }
      else if (day.sessions[idx].end !== local.end || day.sessions[idx].km !== local.km) {
        day.sessions[idx] = local; changed = true;
      }
    });
    // 2. Sessão local encerrada que não existe no remoto — reenviar (não apagar)
    const remoteIds = new Set(remote.map(r => r.id));
    for (const s of day.sessions) {
      if (!remoteIds.has(s.id) && s.end) {
        await _SUPA.from("sessions").upsert({
          id: s.id, user_id: _supaUser.id, date: today,
          start_ms: s.start, end_ms: s.end, km: s.km, elapsed_secs: s.elapsed,
          paused_ms: 0, is_paused: false
        }, {onConflict:"id"}).catch(()=>{});
      }
    }
    // 3. Sessão ativa: se remoto diz que já encerrou mas o app local acha que continua
    if (S.gps.st !== "idle" && S.gps.sessionId) {
      const remoteActive = remote.find(r => r.id === S.gps.sessionId);
      if (remoteActive && remoteActive.end_ms) {
        S.gps.st = "idle"; S.gps.start = null;
        clearInterval(S.gps.ti);
        changed = true;
      }
    }
    if (changed) DB.saveDay(day);
  } catch(e) { console.warn("[reconcileJornadaHoje]", e); }
}

// ── Estimativa de km por endereço (Nominatim geocode + OSRM route) ────
// Usado quando a plataforma não informa km (ex: 99 Motorista). Gratuito,
// mas com throttle de 1 req/s no Nominatim — processar em fila sequencial.
let _geoQueueBusy = false;
const _geoQueue = [];
// Remove prefixo "Bairro da/do/de/das/dos" do nome de bairro (a 99 escreve
// "Bairro da São Pedro" — queremos exibir/geocodificar só "São Pedro")
function _cleanBairro(name) {
  if (!name) return name;
  return name.replace(/^\s*bairro\s+(d[aeo]s?\s+)?/i, '').trim();
}
// Limpa lixo comum do OCR no endereço antes de geocodificar: vírgulas soltas,
// país truncado ("Bra"/"Bras"), número de prédio grudado ("1211- Rua X"),
// e o prefixo "Bairro da" que confunde o Nominatim.
function _cleanGeoAddr(addr) {
  if (!addr) return addr;
  let a = addr.trim();
  a = a.replace(/,\s*(BR|Brasil|Bras?i?l?|Bra)\.?\s*$/i, '');
  a = a.replace(/[,\s]+$/, '');
  a = a.replace(/^\d+\s*-\s*/, '');
  a = a.replace(/\bbairro\s+(d[aeo]s?\s+)/gi, '');
  return a.replace(/\s{2,}/g, ' ').trim();
}
// Cache persistente de geocodificação: os mesmos bairros/endereços repetem
// muito no dia a dia — resolver 1x e reaproveitar corta o tempo da fila de
// ~5s/corrida pra quase zero nas repetidas (e economiza chamadas ao Nominatim).
const _geoCache = (() => {
  try { return JSON.parse(localStorage.getItem('sm_geo_cache_v1') || '{}'); }
  catch(e) { return {}; }
})();
function _saveGeoCache() {
  try {
    // limita a 400 entradas pra não inchar o localStorage
    const keys = Object.keys(_geoCache);
    if (keys.length > 400) keys.slice(0, keys.length - 400).forEach(k => delete _geoCache[k]);
    localStorage.setItem('sm_geo_cache_v1', JSON.stringify(_geoCache));
  } catch(e) {}
}
// Throttle global do Nominatim (1 req/s) — embutido aqui pra que cache hits
// não paguem espera nenhuma.
let _lastNominatimMs = 0;
async function _nominatimThrottle() {
  const wait = 1100 - (Date.now() - _lastNominatimMs);
  if (wait > 0) await new Promise(r => setTimeout(r, wait));
  _lastNominatimMs = Date.now();
}
async function _geocodeAddress(query) {
  if (!query) return null;
  const key = query.toLowerCase().trim();
  if (_geoCache[key]) return _geoCache[key];
  try {
    await _nominatimThrottle();
    const url = "https://nominatim.openstreetmap.org/search?format=json&limit=1&countrycodes=br&q=" + encodeURIComponent(query);
    const r = await fetch(url, { headers: { "Accept-Language": "pt-BR" } });
    const data = await r.json();
    if (!data || !data.length) return null;
    const pt = { lat: parseFloat(data[0].lat), lon: parseFloat(data[0].lon) };
    _geoCache[key] = pt; _saveGeoCache();
    return pt;
  } catch(e) { console.warn("[_geocodeAddress]", e); return null; }
}
async function _routeDistanceOSRM(p1, p2) {
  try {
    const url = `https://router.project-osrm.org/route/v1/driving/${p1.lon},${p1.lat};${p2.lon},${p2.lat}?overview=false`;
    const r = await fetch(url);
    const data = await r.json();
    if (!data.routes || !data.routes.length) return null;
    return data.routes[0].distance / 1000; // metros → km
  } catch(e) { console.warn("[_routeDistanceOSRM]", e); return null; }
}
// Verifica se o endereço tem informação suficiente pra geocodificar com
// confiança (nome de rua real) — 'Tijuca' ou '230, Tijuca' sozinhos são
// ambíguos demais (podem cair em qualquer ponto do bairro).
function _hasReliableAddress(addr) {
  if (!addr) return false;
  const firstPart = addr.split(',')[0].trim();
  // Primeira parte só com dígitos (número de prédio solto, sem rua) → não confiável
  if (/^\d+$/.test(firstPart)) return false;
  // Primeira parte com menos de 4 letras (ex: nome de bairro sozinho) → não confiável
  if (firstPart.replace(/[^a-zA-ZÀ-ÿ]/g,'').length < 4) return false;
  return true;
}
// Geocodifica com fallback: tenta o endereço completo; se falhar, tenta só
// da parte do bairro em diante (ex: "Rua X, Ermitage, Teresópolis - RJ" →
// "Ermitage, Teresópolis - RJ") — menos preciso, mas suficiente pra estimar km.
async function _geocodeWithFallback(addr) {
  const clean = _cleanGeoAddr(addr);
  let p = await _geocodeAddress(clean);
  if (p) return p;
  const parts = clean.split(',').map(s=>s.trim()).filter(Boolean);
  if (parts.length >= 2) {
    p = await _geocodeAddress(parts.slice(1).join(', '));
  }
  return p;
}
async function _estimateTripKm(origin, dest) {
  if (!_hasReliableAddress(origin) || !_hasReliableAddress(dest)) {
    console.warn('[_estimateTripKm] Endereço sem rua identificável — pulando estimativa:', origin, '/', dest);
    return null;
  }
  const p1 = await _geocodeWithFallback(origin);
  const p2 = await _geocodeWithFallback(dest);
  if (!p1 || !p2) return null;
  // Sanidade: se os pontos geocodificados ficam absurdamente longe um do
  // outro (linha reta > 80km), a geocodificação quase certamente resolveu
  // um endereço ambíguo pro lugar errado — descarta em vez de salvar lixo
  const straightKm = _haversineKm(p1.lat, p1.lon, p2.lat, p2.lon);
  if (straightKm > 80) {
    console.warn('[_estimateTripKm] Descartado — distância implausível:', straightKm.toFixed(1), 'km entre', origin, 'e
===== R$/hora @ 417917 =====
span><strong id="fin-rec">R$ 0,00</strong><small>gorjetas, b&#244;nus...</small></div>
        <div class="fin-metric r"><span>Despesas</span><strong id="fin-desp">R$ 0,00</strong><small id="fin-desp-sub">&nbsp;</small></div>
        <div class="fin-metric"><span>Lucro</span><strong id="fin-lucro">R$ 0,00</strong><small id="fin-lucro-sub">&nbsp;</small></div>
      </div>
    </section>
    <section class="card cardPad" style="margin-bottom:14px">
      <div class="cardHead"><h2>Objetivos</h2></div>
      <div class="obj-switch">
        <button class="obj-btn active" id="ob-rec" onclick="setObjTab('rec')">Receitas</button>
        <button class="obj-btn" id="ob-desp" onclick="setObjTab('desp')">Despesas</button>
      </div>
      <div id="obj-rec-list"></div>
      <div id="obj-desp-list" style="display:none"></div>
      <div class="cfg-link" onclick="openObjConfig()">Configurar objetivos</div>
    </section>
    <section class="card cardPad" style="margin-bottom:14px">
      <div class="cardHead"><h2>Fluxo</h2></div>
      <div class="fin-flow" id="fin-flow"></div>
      <div class="fin-flow-legend">
        <span><i style="background:#22C55E"></i>Entradas (com corridas)</span>
        <span><i style="background:#FCA5A5"></i>Sa&#237;das</span>
      </div>
    </section>
    <section class="card cardPad" style="margin-bottom:14px">
      <div class="cardHead"><h2>Ações rápidas</h2></div>
      <div class="fin-actions">
        <button class="fin-action" onclick="openLanc('receita')"><div class="ico green"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12h14"/></svg></div>Receita</button>
        <button class="fin-action" onclick="openLanc('despesa')"><div class="ico red"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12h14"/></svg></div>Despesa</button>
        <button class="fin-action" onclick="navTo('lancamentos')"><div class="ico purple"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 19V5a2 2 0 0 1 2-2h12v18H6a2 2 0 0 1-2-2Z"/></svg></div>Histórico</button>
        <button class="fin-action" onclick="navTo('relatorios')"><div class="ico orange"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 3v18h18"/><path d="m19 9-5 5-4-4-4 4"/></svg></div>Relatório</button>
      </div>
    </section>
    <section class="card" style="margin-bottom:14px">
      <div class="cardPad cardHead"><h2>Últimas movimentações</h2><button onclick="navTo('lancamentos')">Ver tudo</button></div>
      <div id="fin-mov"><div class="empty">Nenhuma movimentação</div></div>
    </section>
    <div style="height:8px"></div>
  </div>
</div>

<!-- ══ TELA RELATÓRIOS (placeholder — visão geral + alertas vêm na sequência) ══ -->
<div class="screen" id="sc-relatorios">
  <div style="display:flex;align-items:center;gap:10px;padding:14px 16px 6px">
    <button onclick="navTo('financeiro')" style="width:32px;height:32px;border-radius:10px;background:#F1F5F9;border:0;display:flex;align-items:center;justify-content:center;color:#0F172A;cursor:pointer;flex-shrink:0"><svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M19 12H5M12 19l-7-7 7-7"/></svg></button>
    <h2 style="font-size:17px;letter-spacing:-.02em;flex:1">Relatórios</h2>
  </div>
  <div style="padding:0 16px">
    <div class="ad-carousel" id="ad-carousel-0"></div>
  </div>
  <div class="hg-info-tip" id="hg-info-tip"></div>
  <section class="hg-period-card" style="margin:12px 16px 0">
    <div class="hg-period-top">
      <div class="hg-period-tabs">
        <button id="hg-tab-w" class="active" onclick="setHomePeriod('w')">Semana</button>
        <button id="hg-tab-m" onclick="setHomePeriod('m')">Mês</button>
        <button id="hg-tab-y" onclick="setHomePeriod('y')">Ano</button>
      </div>
      <button class="hg-filter-btn" onclick="openHgFilter()"><svg viewBox="0 0 24 24"><path d="M3 6h18"/><path d="M7 12h10"/><path d="M10 18h4"/></svg></button>
    </div>
    <div class="hg-main-value">
      <label>Faturamento</label>
      <h1 id="hg-amount">R$ 0</h1>
      <p id="hg-sub">0 corridas · 0,0 km</p>
    </div>
    <div class="hg-insight" id="hg-insight" style="display:none">
      <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;opacity:.5;margin-top:2px"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
      <p id="hg-insight-txt"></p>
    </div>
    <div class="hg-metrics">
      <div class="hg-metric"><strong id="hg-rpkm">—</strong><span>R$/Km</span></div>
      <div class="hg-metric"><strong id="hg-rphr">—</strong><span>R$/Hora</span></div>
      <div class="hg-metric"><strong id="hg-trips">0</strong><span>Corridas</span></div>
      <div class="hg-metric"><strong id="hg-exp">R$ 0</strong><span>Despesas</span></div>
      <div class="hg-metric"><strong id="hg-profit">R$ 0</strong><span>Lucro</span></div>
      <div class="hg-metric"><strong id="hg-pct">0%</strong><span>% Lucro</span></div>
    </div>
    <div class="hg-goals" id="hg-goals"></div>
  </section>

  <section class="hg-chart-card" style="margin:14px 16px 0">
    <div class="hg-chart-head"><div><h2>Dia/Semana <button class="hg-info-btn" onclick="showChartInfo(this,'Soma por dia da semana, dentro do período selecionado acima.')">?</button></h2></div>
      <div class="hg-mini-tabs"><button class="active" id="hg-dia-d" onclick="setHgDiaMode('d')">Dia</button><button id="hg-dia-w" onclick="setHgDiaMode('w')">Semana</button></div>
    </div>
    <p class="hg-chart-highlight" id="hg-dia-insight"></p>
    <div class="hg-bar-chart" id="hg-chart-dia"></div>
  </section>

  <section class="hg-chart-card" style="margin:14px 16px 0">
    <div class="hg-chart-head"><div><h2>Hora/Turno <button class="hg-info-btn" onclick="showChartInfo(this,'Soma por horário, dentro do período selecionado acima.')">?</button></h2></div>
      <div class="hg-mini-tabs"><button class="active" id="hg-hora-h" onclick="setHgHoraMode('h')">Hora</button><button id="hg-hora-t" onclick="setHgHoraMode('t')">Turno</button></div>
    </div>
    <p class="hg-chart-highlight" id="hg-hora-insight"></p>
    <div class="hg-bar-chart hour" id="hg-chart-hora"></div>
  </section>

  <div class="ad-carousel" id="ad-carousel-1"></div>

  <section class="hg-chart-card" style="margin:14px 16px 0">
    <div class="hg-chart-head"><div><h2>Mês/Ano <button class="hg-info-btn" onclick="showChartInfo(this,'Soma por mês/ano, dentro do período selecionado acima — com Semana ou Mês escolhido aqui em cima, vira 1 barra só.')">?</button></h2></div>
      <div class="hg-mini-tabs"><button class="active" id="hg-mes-m" onclick="setHgMesMode('m')">Mês</button><button id="hg-mes-y" onclick="setHgMesMode('y')">Ano</button></div>
    </div>
    <div class="hg-bar-chart month" id="hg-chart-mes"></div>
  </section>

  <section class="hg-chart-card" style="margin:14px 16px 0">
    <div class="hg-chart-head"><div><h2>Plataformas <button class="hg-info-btn" onclick="showChartInfo(this,'Distribuição por app ou origem.')">?</button></h2></div></div>
    <div class="hg-pie-wrap">
      <div class="hg-pie" id="hg-pie-plat"></div>
      <div class="hg-pie-list" id="hg-pie-plat-list"></div>
    </div>
  </section>

  <section class="hg-chart-card" style="margin:14px 16px 0">
    <div class="hg-chart-head"><div><h2>Receitas x Despesas <button class="hg-info-btn" onclick="showChartInfo(this,'Distribuição por categoria conforme o tipo selecionado.')">?</button></h2></div></div>
    <div class="hg-category-tabs"><button class="active" id="hg-rd-receita" onclick="setHgRevDesp('receita')">Receitas</button><button id="hg-rd-despesa" onclick="setHgRevDesp('despesa')">Despesas</button></div>
    <div class="hg-pie-wrap">
      <div class="hg-pie" id="hg-pie-cat"></div>
      <div class="hg-pie-list" id="hg-pie-cat-list"></div>
    </div>
    <div class="hg-category-panel" id="hg-cat-panel"></div>
  </section>
  <section class="entries" style="margin:14px 16px 0">
    <div class="eheader"><h3>Últimos lançamentos</h3><button onclick="navTo('lancamentos')">Ver tudo</button></div>
    <div id="home-entries"><div class="empty">Nenhum lançamento ainda</div></div>
  </section>
  <div style="height:8px"></div>
</div>

<!-- ══ TELA LANÇAMENTOS ═══════════════════════════════════════════════════ -->
<div class="screen" id="sc-lancamentos">
  <div style="padding:8px 16px 14px">
    <div class="lanc-total-card">
      <div><div class="l">Saldo no período</div><div class="v" id="lt-val">R$ 0,00</div></div>
      <div class="side"><div class="l" id="lt-period-label">7 dias</div><div class="v2" id="lt-count">0 itens</div></div>
    </div>
    <div class="type-switch" style="margin-bottom:8px">
      <button class="type-btn active" id="lf-all" onclick="setLancFilter('all')">Todos</button>
      <button class="type-btn" id="lf-rec" onclick="setLancFilter('rec')">Receitas</button>
      <button class="type-btn" id="lf-dep" onclick="setLancFilter('dep')">Despesas</button>
    </div>
    <div class="lanc-status-row">
      <button class="pchip active" id="lst-all" onclick="setLancListStatus('all')">Todos</button>
      <button class="pchip" id="lst-apagar" onclick="setLancListStatus('a_pagar')">A pagar</button>
      <button class="pchip" id="lst-pago" onclick="setLancListStatus('pago')">Pago</button>
    </div>
    <div class="lanc-period-row" style="position:relative">
      <button class="pchip active" id="lpp-7d" onclick="setLancPeriod('7d')">7 dias</button>
      <button class="pchip" id="lpp-mes" onclick="setLancPeriod('mes')">Mês</button>
      <button class="pchip" id="lpp-tudo" onclick="setLancPeriod('tudo')">Tudo</button>
      <button id="lanc-date-btn" onclick="toggleLancDate()" title="Buscar por data" style="width:32px;min-width:32px;height:32px;flex-shrink:0;border-radius:9px;border:1.5px solid #E2E8F0;background:#fff;display:flex;align-items:center;justify-content:center;cursor:pointer"><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#475569" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg></button>
      <input type="date" id="lanc-date-input" onchange="setLancDate(this.value)" style="position:absolute;width:0;height:0;opacity:0;pointer-events:none">
    </div>
  </div>
  <div style="padding:0 16px"><div id="lanc-list"><div class="empty">Nenhum lançamento</div></div></div>
  <div style="height:8px"></div>
</div>

<!-- ══ TELA CORRIDAS ═══════════════════════════════════════════════════ -->
<div class="screen" id="sc-corridas">
  <div style="display:flex;align-items:center;gap:10px;padding:14px 16px 6px">
    <button onclick="navTo('jornada')" style="width:32px;height:32px;border-radius:10px;background:#F1F5F9;border:0;display:flex;align-items:center;justify-content:center;color:#0F172A;cursor:pointer;flex-shrink:0"><svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M19 12H5M12 19l-7-7 7-7"/></svg></button>
    <h2 style="font-size:17px;letter-spacing:-.02em;flex:1">Corridas</h2>
    <span id="cor-cnt" style="display:none"></span>
  </div>
  <div style="padding:10px 16px 4px">
    <div class="type-switch" id="cor-plat-switch" style="margin-bottom:8px">
      <button class="type-btn" id="cor-plat-uber" onclick="toggleCorridasPlat('uber')">Uber</button>
      <button class="type-btn" id="cor-plat-99" onclick="toggleCorridasPlat('99')">99</button>
    </div>
    <div style="display:flex;gap:5px;align-items:center;padding-bottom:6px;position:relative" id="cor-per-row">
      <button class="pchip active" id="cpb-hoje" style="flex:1" onclick="setCorridasPeriod('hoje')">Hoje</button>
      <button class="pchip" id="cpb-7d" style="flex:1" onclick="setCorridasPeriod('7d')">7 dias</button>
      <button class="pchip" id="cpb-mes" style="flex:1" onclick="setCorridasPeriod('mes')">Mês</button>
      <button class="pchip" id="cpb-tudo" style="flex:1" onclick="setCorridasPeriod('tudo')">Tudo</button>
      <button id="cor-date-btn" onclick="toggleCorDate()" title="Buscar por data" style="width:34px;min-width:34px;height:34px;flex-shrink:0;border-radius:10px;border:1.5px solid #E2E8F0;background:#fff;display:flex;align-items:center;justify-content:center;cursor:pointer"><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#475569" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg></button>
      <button id="cor-resync-btn" onclick="resyncCorridasFromCloud()" title="Recarregar da nuvem" style="width:34px;min-width:34px;height:34px;flex-shrink:0;border-radius:10px;border:1.5px solid #E2E8F0;background:#fff;display:flex;align-items:center;justify-content:center;cursor:pointer"><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="#475569" stroke-width="2"><path d="M21 12a9 9 0 1 1-2.6-6.4"/><path d="M21 4v6h-6"/></svg></button>
    </div>
    <input type="date" id="cor-date-input" onchange="setCorridasDate(this.value)" style="position:absolute;width:0;height:0;opacity:0;pointer-events:none">
  </div>

  <div style="display:flex;background:#fff;border-top:1px solid #E2E8F0;border-bottom:1px solid #E2E8F0;padding:10px 16px" id="cor-summary">
    <div style="flex:1;text-align:center"><span style="display:block;font-size:9px;color:#94A3B8;font-weight:800;text-transform:uppercase;letter-spacing:.06em;margin-bottom:3px">Total</span><strong id="cor-sum-val" style="font-size:14px;font-weight:800;color:#0F172A">R$ 0,00</strong></div>
    <div style="flex:1;text-align:center;border-left:1px solid #F1F5F9"><span style="display:block;font-size:9px;color:#94A3B8;font-weight:800;text-transform:uppercase;letter-spacing:.06em;margin-bottom:3px">Km</span><strong id="cor-sum-km" style="font-size:14px;font-weight:800;color:#0F172A">0,0</strong></div>
    <div style="flex:1;text-align:center;border-left:1px solid #F1F5F9"><span style="display:block;font-size:9px;color:#94A3B8;font-weight:800;text-transform:uppercase;letter-spacing:.06em;margin-bottom:3px">Corridas</span><strong id="cor-sum-n" style="font-size:14px;font-weight:800;color:#0F172A">0</strong></div>
  </div>
  <div class="decl-toggle" id="decl-toggle" onclick="toggleDeclinedOffers()" style="display:none">
    <div class="decl-icon"><svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-wi
===== R$/hora @ 643791 =====
hedElapsed + liveElapsed + (day.tempoAdjust || 0));
  const totalKm = finishedKm + liveKm + (day.kmAdjust || 0);
  const state = isToday ? g.st : 'idle';

  let cls = 'idle';
  if (state === 'running') cls = 'running';
  else if (finished.length > 0) cls = 'ended';
  jc.className = 'journey ' + cls;

  setText('jc-status-text',
    state === 'running' ? 'Online' :
    finished.length > 0 ? 'Concluído' : 'Iniciar'
  );
  const th = Math.floor(liveElapsed/3600), tm = Math.floor(liveElapsed%3600/60), ts = liveElapsed%60;
  setText('jc-timer', p2(th)+':'+p2(tm)+':'+p2(ts));
  setText('jc-sub',
    state === 'running' ? 'Sessão atual · iniciada '+fmtHM(g.start) :
    finished.length > 0 ? finished.length+(finished.length===1?' jornada hoje':' jornadas hoje') :
    'Nenhuma sessão iniciada'
  );
  setText('jc-km', liveKm.toFixed(1));
  const tth = Math.floor(totalElapsed/3600), ttm = Math.floor(totalElapsed%3600/60);
  setText('jc-total', tth+'h'+p2(ttm)+'m');
  setText('jc-kmtotal', totalKm.toFixed(1)+' km');
  // CORRIGIDO (23/07/2026, confirmado com dado real pelo Yuri): esses 4
  // KPIs do card principal (corridas, km ativo, R$/h, R$/km) só somavam
  // day.trips (Importadas) — sessão só com automáticas mostrava tudo
  // zerado mesmo com corridas de verdade rodando. Mesma correção que já
  // tinha sido feita em rev/sessionItem/renderMetasDoDia, faltava aqui.
  const autoTripsHoje = day.autoTrips || [];

  // Banner "aguardando confirmação" — pedido do Yuri (16/08/2026), substitui
  // o botão fixo "Importar corridas". Conta corridas que ainda não foram
  // confirmadas por vídeo do histórico (status 'capturada' ou 'estimada') —
  // 'confirmada' só existe depois do vídeo, então isso mede exatamente o
  // que falta conferir naquele dia. Aparece em qualquer dia navegado, some
  // sozinho quando não sobra pendência.
  const pcbEl = document.getElementById('pending-confirm-banner');
  if (pcbEl) {
    // Não interpreta cache ainda carregando como zero corridas. Durante refresh,
    // mantém o último estado visual válido do mesmo dia.
    const autoTripsReady = S._autoTripsDataDate === cardDate;
    if (autoTripsReady) {
      const pendingCount = autoTripsHoje.filter(t => t.status !== 'confirmada' || t.value_needs_review === true).length;
      pcbEl.dataset.dataDate = cardDate;
      if (pendingCount > 0) {
        pcbEl.style.display = 'flex';
        setText('pcb-title', pendingCount + (pendingCount===1 ? ' corrida aguardando confirmação' : ' corridas aguardando confirmação'));
      } else {
        pcbEl.style.display = 'none';
      }
    } else if (pcbEl.dataset.dataDate && pcbEl.dataset.dataDate !== cardDate) {
      pcbEl.style.display = 'none';
      pcbEl.dataset.dataDate = cardDate;
    }
  }

  // Barra Online/Buscando passageiro/Em corrida — real, vinda da máquina de
  // estados do APK (ver computeStatusBreakdown). No card da jornada ATUAL
  // só faz sentido enquanto a sessão está rodando (é o que está sendo
  // contado agora); ao encerrar, o dado sai daqui e já está consolidado no
  // "Total do dia" logo abaixo — pedido do Yuri (16/08/2026), evita
  // duplicar/deixar a mesma barra "grudada" depois de já ter encerrado.
  // As barras são atualizadas exclusivamente pela timeline contínua V3.
  // A rotina base não sobrescreve mais a leitura real com auto_trips.
  const jcBd = document.getElementById('jc-breakdown');
  const tdBdRow = document.getElementById('td-bd-row');

  setText('jc-trips', day.trips.length + autoTripsHoje.length);
  setText('jc-trips2', day.trips.length + autoTripsHoje.length);
  const aceitasCount = day.trips.length + autoTripsHoje.length;
  const declinedCount = (S.declined || []).length;
  setText('jc-aceitas', aceitasCount);
  setText('jc-recusadas', declinedCount);
  const corridasSubEl = document.getElementById('jc-corridas-sub');
  if (corridasSubEl) corridasSubEl.style.display = (aceitasCount>0 || declinedCount>0) ? 'flex' : 'none';
  const kmAtivo = day.trips.reduce((s,t)=>s+(t.km||0),0)
                + autoTripsHoje.reduce((s,t)=>s+(parseFloat(t.real_km_trip ?? t.offer_km_trip)||0),0);
  const kmDesl = Math.max(0, totalKm - kmAtivo);
  setText('jc-kmativo', kmAtivo.toFixed(1));
  setText('jc-kmdesl', kmDesl.toFixed(1));
  const ganhosDia = day.trips.reduce((s,t)=>s+(t.value||0),0)
                  + autoTripsHoje.reduce((s,t)=>s+(t.offer_value||0),0);
  const horasDecimal = totalElapsed > 0 ? totalElapsed/3600 : 0;
  const rsh  = horasDecimal > 0 ? ganhosDia/horasDecimal : null;
  const rskm = totalKm > 0 ? ganhosDia/totalKm : null;
  setText('jc-rsh', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  setText('jc-rsh2', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm2', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  // Cor por indicador — pedido do Yuri (23/07/2026, mockup aprovado):
  // mesmos limiares vermelho/verde que o motorista já ajusta na régua do
  // MōB Flash (Indicadores → R$/km e R$/hora), cada indicador troca de cor
  // por conta própria. Sem dado ainda (sessão não rodou tempo/km) fica
  // neutro (verde), não faz sentido alarmar antes de ter dado de verdade.
  const rateEl = document.getElementById('jc-rate-hora');
  const kmEl   = document.getElementById('jc-rate-km');
  const rateEl2 = document.getElementById('jc-rate-hora2');
  const kmEl2   = document.getElementById('jc-rate-km2');
  const rateCls = rsh !== null ? rateColorClass('rhora', rsh) : '';
  const kmCls   = rskm !== null ? rateColorClass('rkm', rskm) : '';
  if (rateEl)  rateEl.className  = 'td-rcard ' + rateCls;
  if (kmEl)    kmEl.className    = 'td-rcard ' + kmCls;
  if (rateEl2) rateEl2.className = 'rate-cell ' + rateCls;
  if (kmEl2)   kmEl2.className   = 'rate-cell ' + kmCls;

  const actions = document.getElementById('jc-actions');
  if (!actions) return;
  if (!isToday) {
    actions.innerHTML = '';
  } else if (state === 'idle') {
    actions.innerHTML = `<button class="j-round-btn play" onclick="gpsStart()" aria-label="Iniciar jornada"><svg viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg></button>`;
  } else if (state === 'running') {
    actions.innerHTML = '<button class="j-round-btn stop" onclick="confirmStop()" aria-label="Encerrar jornada"><svg viewBox="0 0 24 24" fill="currentColor"><rect x="6" y="6" width="12" height="12"/></svg></button>';
  }
}

function agMinutesBetween(start, end) {
  if (!start || !end) return 0;
  const [h1,m1] = start.split(':').map(Number);
  const [h2,m2] = end.split(':').map(Number);
  return Math.max(0, (h2*60+m2) - (h1*60+m1));
}
function openMetasHojeModal() {
  const date = S.curDate || DB.today();
  const day = DB.day(date);
  const dow = new Date(date+'T12:00').getDay();
  const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
  const meta = DB.meta();
  const ov = day.metaOverride || null;
  const dimMes = new Date(new Date(date+'T12:00').getFullYear(), new Date(date+'T12:00').getMonth()+1, 0).getDate();
  const moneyGoal = (ov && ov.money) ? ov.money : ((plan && plan.moneyGoal) ? plan.moneyGoal : (meta.general ? meta.general/dimMes : 0));
  const kmGoal = (ov && ov.km) ? ov.km : (plan && plan.kmGoal ? plan.kmGoal : 0);
  const schedMin = (ov && ov.hours) ? ov.hours*60 : (plan ? (plan.schedules||[]).reduce((s,b)=>s+agMinutesBetween(b.start,b.end),0) : 0);
  const mEl = document.getElementById('mh-money'); if (mEl) mEl.value = moneyGoal ? moneyGoal.toFixed(2) : '';
  const kEl = document.getElementById('mh-km'); if (kEl) kEl.value = kmGoal || '';
  const hEl = document.getElementById('mh-hours'); if (hEl) hEl.value = schedMin ? (schedMin/60).toFixed(1) : '';
  const subEl = document.getElementById('mh-sub');
  if (subEl) {
    const isToday = date === DB.today();
    subEl.textContent = isToday ? 'Vale só para hoje' : ('Vale só para ' + new Date(date+'T12:00').toLocaleDateString('pt-BR',{weekday:'long',day:'numeric',month:'long'}));
  }
  openMo('mo-metas-hoje');
}
function saveMetasHoje() {
  const date = S.curDate || DB.today();
  const day = DB.day(date);
  const money = parseFloat((document.getElementById('mh-money')||{}).value) || 0;
  const km = parseFloat((document.getElementById('mh-km')||{}).value) || 0;
  const hours = parseFloat((document.getElementById('mh-hours')||{}).value) || 0;
  day.metaOverride = { money, km, hours };
  DB.saveDay(day);
  closeMo('mo-metas-hoje');
  try { renderMetasDoDia(day); } catch(e) {}
  showToast('Metas de hoje atualizadas!', 'success');
}
function renderMetasDoDia(day) {
  const stack = document.getElementById('metas-dia-stack');
  if (!stack) return;
  const rev = day.trips.reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0)
            + (day.autoTrips||[]).reduce((s,t)=>s+(t.offer_value||0),0);
  const isToday = day.date === DB.today();
  const liveOn = isToday && S.gps.st==='running';
  const totalKm = (day.sessions||[]).reduce((s,x)=>s+(x.km||0),0) + (liveOn ? S.gps.km : 0) + (day.kmAdjust || 0);
  const totalElapsed = Math.max(0, (day.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0) + (liveOn ? getElapsed() : 0) + (day.tempoAdjust || 0));
  const dow = new Date(day.date+'T12:00').getDay();
  const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
  const meta = DB.meta();
  const ov = day.metaOverride || null; // ajuste específico deste dia (via modal 'Metas de hoje')
  const dimMes = new Date(new Date(day.date+'T12:00').getFullYear(), new Date(day.date+'T12:00').getMonth()+1, 0).getDate();
  const moneyGoal = (ov && ov.money) ? ov.money : ((plan && plan.moneyGoal) ? plan.moneyGoal : (meta.general ? meta.general/dimMes : 0));

  const ICO_MONEY = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>';
  const ICO_KM = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76"/></svg>';
  const ICO_TIME = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>';

  let html = '';
  if (moneyGoal) {
    const pct = Math.min(100, Math.round(rev/moneyGoal*100));
    html += `<div class="goal-item"><div class="g-ico money">${ICO_MONEY}</div><div class="goal-body"><div class="goal-top"><strong>Financeira</strong><span>${DB.fRI(rev)} de ${DB.fRI(moneyGoal)} · ${pct}%</span></div><div class="gbar money"><i style="width:${pct}%"></i></div></div></div>`;
  } else {
    html += `<div class="goal-item"><div class="g-ico money">${ICO_MONEY}</div><div class="goal-locked" onclick="openPlanJornada()"><span>Financeira — meta não definida</span><b>Planejar</b></div></div>`;
  }
  const kmGoal = (ov && ov.km) ? ov.km : (plan && plan.kmGoal ? plan.kmGoal : 0);
  if (kmGoal) {
    const pct = Math.min(100, Math.round(totalKm/kmGoal*100));
    html += `<div class="goal-item"><div class="g-ico km">${ICO_KM}</div><div class="goal-body"><div class="goal-top"><strong>Km</strong><span>${totalKm.toFixed(1)} de ${kmGoal} km · ${pct}%</span></div><div class="gbar km"><i style="width:${pct}%"></i></div></div></div>`;
  } else {
    html += `<div class="goal-item"><div class="g-ico km">${ICO_KM}</div><div class="goal-locked" onclick="openPlanJornada()"><span>Km — meta não definida</span><b>Planejar</b></div></div>`;
  }
  const schedMin = (ov && ov.hours) ? ov.hours*60 : (plan ? (plan.schedules||[]).reduce((s,b)=>s+agMinutesBetween(b.start,b.end),0) : 0);
  if (schedMin > 0) {
    const liveMin = Math.round(totalElapsed/60);
    const pct = Math.min(100, Math.round(liveMin/schedMin*100));
    html += `<div class="goal-item"><div class="g-ico time">${ICO_TIME}</div><div class="goal-body"><div class="goal-top"><strong>Tempo online</strong><span>${Math.floor(liveMin/60)}h${p2(liveMin%60)} de ${Math.floor(schedMin/60)}h${p2(schedMin%60)} · ${pct}%</span></div><div class="gbar time"><i style="width:${pct}%"></i></div></div></div>`;
  } else {
    html += `<div class="goal-item"><div class="g-ico time">${ICO_TIME}</div><div class="goal-locked" onclick="openPlanJornada()"><span>Tempo online — meta não definida</span><b>Planejar</b></div></div>`;
  }
  stack.innerHTML = html;
}

// ── Planejar Jornada ─────────────────────────────────────────
let pjSelDays = [];
let pjSchedules = [];
const PJ_DOW = ['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'];
function openPlanJornada() {
  pjSelDays = []; pjSchedules = [];
  document.querySelectorAll('.pj-day').forEach(b => b.classList.remove('on'));
  const mEl = document.getElementById('pj-money'); if (mEl) mEl.value = '';
  const kEl = document.getElementById('pj-km'); if (kEl) kEl.value = '';
  renderPjSchedules();
  renderPjList();
  navTo('planejar-jornada');
}
function pjToggleDay(d) {
  const i = pjSelDays.indexOf(d);
  if (i === -1) pjSelDays.push(d); else pjSelDays.splice(i,1);
  const btn = document.querySelector('.pj-day[data-d="'+d+'"]');
  if (btn) btn.classList.toggle('on', pjSelDays.includes(d));
}
function pjAddSchedule() { pjSchedules.push({start:'', end:''}); renderPjSchedules(); }
function pjRemoveSchedule(idx) { pjSchedules.splice(idx, 1); renderPjSchedules(); }
function renderPjSchedules() {
  const el = document.getElementById('pj-schedules');
  if (!el) return;
  if (!pjSchedules.length) { el.innerHTML = '<p style="font-size:12px;color:var(--muted);margin:2px 0">Nenhum horário adicionado</p>'; return; }
  el.innerHTML = pjSchedules.map((s,i) => `
    <div class="pj-sched-row">
      <input type="time" class="ag-input" value="${s.start}" onchange="pjSchedules[${i}].start=this.value">
      <span>→</span>
      <input type="time" class="ag-input" value="${s.end}" onchange="pjSchedules[${i}].end=this.value">
      <button class="pj-sched-del" onclick="pjRemoveSchedule(${i})">&#x2715;</button>
    </div>
  `).join('');
}
function pjSaveEntry() {
  if (!pjSelDays.length) { showToast('Selecione ao menos um dia', 'error'); return; }
  const validSched = pjSchedules.filter(s => s.start && s.end);
  const moneyGoal = parseFloat(document.getElementById('pj-money')?.value||0) || null;
  const kmGoal = parseFloat(document.getElementById('pj-km')?.value||0) || null;
  const plans = DB.planJornada();
  const entry = { id: DB.uid(), days: pjSelDays.slice(), moneyGoal, kmGoal, schedules: validSched };
  plans.push(entry);
  DB.savePlanJornada(plans);
  SYNC.push('plan_jornada', { id: entry.id, days: entry.days, money_goal: entry.moneyGoal,
    km_goal: entry.kmGoal, schedules: entry.schedules });
  pjSelDays = []; pjSchedules = [];
  document.querySelectorAll('.pj-day').forEach(b => b.classList.remove('on'));
  const mEl = document.getElementById('pj-money
===== R$/hora @ 660081 =====
-del" onclick="pjRemoveEntry('${p.id}')">&#x2715;</button></div>
      <div class="pj-card-goal">${goalTxt}</div>
      <div class="pj-card-times">${chips}</div>
    </div>`;
  }).join('');
}

function sessionItem(label, sess, origIdx, dateStr, day, live) {
  const startMs = sess.start || 0, endMs = sess.end || 0;
  const km = sess.km || 0, elapsedSecs = sess.elapsed || 0;
  const dur = fmtDur((elapsedSecs||0)/60);
  const range = fmtHM(startMs) + '–' + (endMs ? fmtHM(endMs) : (live ? 'Agora' : '—'));
  const status = live ? 'Online' : 'Encerrada';
  const badgeCls = live ? 'live' : 'end';
  const cardId = 'jss-' + dateStr + '-' + origIdx;

  // Corridas desta sessão — filtrar por horário (suporta t.ts e t.time HH:MM)
  const trips = (day?.trips||[]).filter(t => {
    let ts = t.ts || 0;
    if (!ts && t.time && dateStr) {
      // Construir timestamp a partir de "HH:MM" + data da sessão
      ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
    }
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  // Corridas automáticas desta sessão — mesmo filtro por horário, usando
  // trip_started_at/accepted_at (autoTripTs, ver loadAutoTripsForDay).
  const autoTrips = (day?.autoTrips||[]).filter(t => {
    const ts = autoTripTs(t);
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  const corridas = trips.length + autoTrips.length;
  const ganhos = trips.reduce((s,t)=>s+(t.value||0),0) + autoTrips.reduce((s,t)=>s+(t.offer_value||0),0);
  // Km ativo = soma do km de cada corrida realizada (tempo 'em corrida')
  // Km deslocamento = km total do GPS MENOS o km ativo (tempo rodando sem
  // corrida — indo pro ponto, retornando pra casa, esperando chamado etc.)
  const kmAtivo = trips.reduce((s,t)=>s+(parseFloat(t.km)||0),0)
                + autoTrips.reduce((s,t)=>s+(parseFloat(t.real_km_trip ?? t.offer_km_trip)||0),0);
  const kmDesloc = Math.max(0, km - kmAtivo);

  const horasDecimal = elapsedSecs > 0 ? elapsedSecs/3600 : 0;
  const rshVal = horasDecimal > 0 ? ganhos/horasDecimal : null;
  const rskmVal = km > 0 ? ganhos/km : null;
  const rsPorHora = rshVal !== null ? DB.fR(rshVal) : '—';
  const rsPorKm = rskmVal !== null ? DB.fR(rskmVal) : '—';
  const rshCls = rshVal !== null ? rateColorClass('rhora', rshVal) : '';
  const rskmCls = rskmVal !== null ? rateColorClass('rkm', rskmVal) : '';
  // Lucro da sessão = soma do lucro de cada corrida (mesmo cálculo do
  // perfStripHtml/detalhe de corrida: valor - custoPorKm*km), só quando o
  // custo do veículo está configurado — igual já vale por corrida.
  const cvCfg = DB.cfg().custoVeiculo;
  const hasCusto = !!(cvCfg && cvCfg.configurado);
  const lucroVal = hasCusto ? ganhos - (cvCfg.custoPorKm||0) * kmAtivo : null;
  const lucroCls = lucroVal !== null ? rateColorClass('lucro', lucroVal) : '';
  // Barra Online/Buscando passageiro/Em corrida desta sessão — mesma fonte
  // real da máquina de estados do APK, agora escopada só às autoTrips que
  // caem dentro do horário desta sessão (já filtradas acima).
  const sessBd = (typeof journeyBreakdownForSession === 'function')
    ? journeyBreakdownForSession(sess, elapsedSecs, km, autoTrips)
    : computeStatusBreakdown(elapsedSecs, km, autoTrips);
  const bdListId = 'bl-' + cardId;
  const editBtn = !live ? `<button class="jss-act" onclick="event.stopPropagation();editJss('${dateStr}',${origIdx})" title="Editar"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg></button>
        <button class="jss-act del" onclick="event.stopPropagation();deleteJss('${dateStr}',${origIdx})" title="Excluir"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/></svg></button>` : '';

  return `<div class="jss-card ${badgeCls}" id="${cardId}">
    <div class="jss-row" onclick="toggleJss('${cardId}')">
      <div class="jss-info">
        <div class="jss-top"><span class="jss-name">${label}</span><span class="jss-badge ${badgeCls}">${status}</span>${editBtn}</div>
        <div class="jss-sub">${range} · ${dur}</div>
      </div>
      <div style="text-align:right;flex-shrink:0;display:flex;flex-direction:column;align-items:flex-end;gap:2px">
        <span style="font-size:14px;font-weight:800;color:var(--text)">${(sess.km||0).toFixed(1)} km</span>
        <span class="jss-ganhos">${DB.fR(ganhos)}</span>
      </div>
      <div class="jss-chev"><svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="6 9 12 15 18 9"/></svg></div>
    </div>
    <div class="jss-rate-row">
      <div class="jss-rcard ${rshCls}"><strong>${rsPorHora}</strong><span>R$/hora</span></div>
      <div class="jss-rcard ${rskmCls}"><strong>${rsPorKm}</strong><span>R$/km</span></div>
      ${hasCusto ? `<div class="jss-rcard ${lucroCls}"><strong>${DB.fR(lucroVal)}</strong><span>Lucro</span></div>` : `<div class="jss-rcard"><strong>—</strong><span>Lucro</span></div>`}
    </div>
    ${elapsedSecs > 0 ? `<div class="jss-statusbar">
      <div class="sbar-wrap" onclick="event.stopPropagation();toggleBar('${bdListId}')"><div class="sbar">${buildSbarHtml(sessBd)}</div></div>
      <div class="sbar-list" id="${bdListId}">${buildSrowsHtml(sessBd)}</div>
    </div>` : ''}
    <div class="jss-trips">
      <div class="jss-trips-head">
        <h4>Corridas desta jornada</h4>
        ${corridas > 0 ? `<span>${corridas} corrida${corridas !== 1 ? 's' : ''}</span>` : ''}
      </div>
      ${corridas > 0
        ? [
            ...trips.map(t => ({ ts: t.ts || (t.time?new Date(dateStr+'T'+t.time+':00').getTime():0), html: tripItem(t) })),
            ...autoTrips.map(t => ({ ts: autoTripTs(t), html: autoTripItem(t) })),
          ].sort((a,b)=>b.ts-a.ts).map(x=>x.html).join('')
        : '<div class="jss-trips-empty">Nenhuma corrida nesta jornada</div>'}
    </div>
  </div>`;

}
// ── Editar sessão ─────────────────────────────────────────────────────────
function editJss(dateStr, idx) {
  const day = DB.day(dateStr);
  const sess = (day.sessions || [])[idx];
  if (!sess) return;

  document.getElementById('jss-edit-date').value = dateStr;
  document.getElementById('jss-edit-idx').value  = idx;
  document.getElementById('jss-edit-km').value   = (sess.km || 0).toFixed(1);

  // Hora início e fim a partir dos timestamps da sessão
  const fmt = ms => {
    if (!ms) return '';
    const d = new Date(ms);
    return String(d.getHours()).padStart(2,'0') + ':' + String(d.getMinutes()).padStart(2,'0');
  };
  document.getElementById('jss-edit-inicio').value = fmt(sess.start);
  document.getElementById('jss-edit-fim').value    = fmt(sess.end);

  openMo('mo-jss-edit');
}

function saveJss() {
  const dateStr = document.getElementById('jss-edit-date').value;
  const idx     = parseInt(document.getElementById('jss-edit-idx').value);
  const km      = parseFloat(document.getElementById('jss-edit-km').value) || 0;
  const inicioStr = document.getElementById('jss-edit-inicio').value; // "HH:MM"
  const fimStr    = document.getElementById('jss-edit-fim').value;    // "HH:MM"

  const day = DB.day(dateStr);
  if (!day.sessions || !day.sessions[idx]) { closeMo('mo-jss-edit'); return; }

  const sess = day.sessions[idx];

  // Recalcular timestamps de início e fim mantendo a data original
  const toMs = (base, timeStr) => {
    if (!timeStr) return base;
    const [hh, mm] = timeStr.split(':').map(Number);
    const d = new Date(base || Date.now());
    d.setHours(hh, mm, 0, 0);
    return d.getTime();
  };
  const newStart = toMs(sess.start, inicioStr);
  const newEnd   = toMs(sess.end || sess.start, fimStr);
  // Elapsed recalculado a partir da diferença início→fim
  const elapsed  = newEnd > newStart ? Math.round((newEnd - newStart) / 1000) : (sess.elapsed || 0);

  sess.km      = km;
  sess.start   = newStart;
  sess.end     = newEnd;
  sess.elapsed = elapsed;
  DB.saveDay(day);

  // Sincronizar com Supabase
  if (_supaUser && sess.id) {
    _SUPA.from('sessions').update({
      km:           parseFloat(km.toFixed(3)),
      start_ms:     newStart,
      end_ms:       newEnd,
      elapsed_secs: elapsed
    }).eq('id', sess.id).then(() => {}).catch(() => {});
  }

  closeMo('mo-jss-edit');
  renderJornada();
  if (S.sec === 'relatorios') renderHome();
  showToast('Sessão atualizada');
}

function deleteJss(dateStr, idx) {
  showConfirm('Excluir esta sessão?', () => {
    const day = DB.day(dateStr);
    if (!day.sessions || !day.sessions[idx]) return;

    const sess = day.sessions[idx];
    day.sessions.splice(idx, 1);
    DB.saveDay(day);

    // Remover do Supabase (delete real, não update)
    if (sess.id) {
      SYNC.del('sessions', { id: sess.id });
    }

    renderJornada();
    if (S.sec === 'relatorios') renderHome();
    showToast('Sessão excluída');
  }, { confirmLabel: 'Excluir', danger: true, cancelLabel: 'Cancelar' });
}


// CORRIGIDO (15/08/2026, relatado pelo Yuri): mesmo bug do toggleTripDetail
// (comentário abaixo, 24/07) só que no card da jornada — toggleJss só
// mexia numa classe CSS, sem guardar estado. O refresh automático de 25s
// (_refreshAutoTripsIfOnJornada) recria os cards do zero sempre fechados,
// então a expansão "voltava a fechar sozinha" em segundos. Mesmo padrão
// de correção: guarda o id aberto, reaplica depois de qualquer redesenho.
let _openJssId = null;
function toggleJss(id){
  const el=document.getElementById(id);
  if(!el) return;
  const willOpen = !el.classList.contains('open');
  el.classList.toggle('open');
  _openJssId = willOpen ? id : null;
}
function reapplyOpenJss(){
  if (!_openJssId) return;
  const el = document.getElementById(_openJssId);
  if (el) el.classList.add('open');
}
// CORRIGIDO (24/07/2026, relatado pelo Yuri): o painel de detalhe de uma
// corrida é só um display:none/block num elemento — quando a tela
// redesenha os cards (o refresh automático de 25s/60s da Jornada/Home,
// por exemplo), os elementos são recriados do zero, sempre fechados por
// padrão, perdendo o que estava aberto na hora errada (parecia fechar
// sozinho, sem motivo). Guarda qual tid está aberto e reaplica depois de
// qualquer redesenho.
let _openTripDetailTid = null;
// Linha vertical da rota (Aceite/Embarque/Destino) tem que parar exatamente
// no centro do último marcador (pin de Destino) — pedido do Yuri
// (16/08/2026): como os marcadores têm tamanhos diferentes (círculo
// pequeno vs pin maior), um valor fixo de CSS nunca acerta em todo caso,
// então mede a posição real na tela depois de renderizado.
function sizeRouteLine(panelEl) {
  try {
    const block = panelEl.querySelector('.route-block');
    if (!block) return;
    const marks = block.querySelectorAll('.route-mark');
    const line = block.querySelector('.route-line');
    if (!line || marks.length < 2) return;
    const first = marks[0], last = marks[marks.length - 1];
    const blockTop = block.getBoundingClientRect().top;
    const firstMid = first.getBoundingClientRect().top - blockTop + first.offsetHeight / 2;
    const lastMid = last.getBoundingClientRect().top - blockTop + last.offsetHeight / 2;
    if (lastMid <= firstMid) return;
    line.style.top = firstMid + 'px';
    line.style.height = (lastMid - firstMid) + 'px';
  } catch (e) {}
}
function toggleTripDetail(tid) {
  const det = document.getElementById('td-'+tid);
  const row = document.getElementById('tr-'+tid);
  if (!det||!row) return;
  const open = det.style.display!=='none';
  document.querySelectorAll('.trip-detail-panel').forEach(el=>el.style.display='none');
  document.querySelectorAll('.li-row[id^="tr-"]').forEach(el=>el.classList.remove('open'));
  if (!open){det.style.display='block';row.classList.add('open');_openTripDetailTid=tid;sizeRouteLine(det);}
  else {_openTripDetailTid=null;}
}
// Chamar depois de qualquer redesenho que possa ter recriado os cards de
// corrida (renderJornada, renderCorridasActive etc.) — reabre sozinho se
// o mesmo tid ainda existir na tela.
function reapplyOpenTripDetail() {
  if (!_openTripDetailTid) return;
  const det = document.getElementById('td-'+_openTripDetailTid);
  const row = document.getElementById('tr-'+_openTripDetailTid);
  if (det && row) { det.style.display='block'; row.classList.add('open'); sizeRouteLine(det); }
}

function esc(s){return String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
// Escapa texto externo (Nominatim/Overpass) pra uso dentro de um literal JS
// de aspas simples que por sua vez está dentro de um atributo onclick="..."
// de aspas duplas — precisa fugir dos dois delimitadores ao mesmo tempo,
// senão um "/' no rótulo devolvido pela API quebra o atributo e injeta HTML.
function escJsAttr(s){return String(s==null?'':s).replace(/\\/g,'\\\\').replace(/'/g,"\\'").replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;');}
// ── Logos das plataformas (assets originais processados: fundo removido) ──
const LOGO_UBER = 'data:image/png;base64,
===== R$/hora @ 704655 =====
v-h2">Manutenção</div>
      <div class="field"><label class="cv-lbl">Km rodado por mês</label><div class="cv-inp-wrap"><input type="number" value="${d.kmMes??''}" oninput="custoSetField('kmMes',this.value)"><span>km</span></div>
      ${sug ? `<button type="button" class="cv-sug" onclick="custoUseSuggestedKm()"><svg viewBox="0 0 24 24" width="10" height="10" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"/></svg>Sugestão: ${sug.toLocaleString('pt-BR')} km/mês (média do seu histórico) · toque para usar</button>` : `<div class="cv-hint">Sem histórico suficiente ainda pra sugerir — pode digitar direto</div>`}
      </div>
      <div class="field"><label class="cv-lbl">Custo médio de manutenção a cada 10.000km</label><div class="cv-inp-wrap"><span>R$</span><input type="number" value="${d.custoManut10k??''}" oninput="custoSetField('custoManut10k',this.value)"></div>
      <div class="cv-hint">Óleo, filtros, pneus, revisões — soma tudo dividido pelos 10 mil km</div></div>
      <button class="cv-btn" onclick="custoGoNext()">Continuar</button>`;
  }
  else if (stepId === 'extras') {
    body = head + `<div class="cv-h2">Outros custos mensais</div><div class="cv-sub">Preencha o que se aplicar — pode deixar em branco o que não tiver.</div>
      <div class="field"><label class="cv-lbl">Seguro do veículo (mensal)</label><div class="cv-inp-wrap"><span>R$</span><input type="number" value="${d.seguroMensal??''}" oninput="custoSetField('seguroMensal',this.value)"></div></div>
      <div class="field"><label class="cv-lbl">IPVA (valor anual)</label><div class="cv-inp-wrap"><span>R$</span><input type="number" value="${d.ipvaAnual??''}" oninput="custoSetField('ipvaAnual',this.value)"></div>
      <div class="cv-hint">Divide automaticamente por 12 meses</div></div>
      <div class="field"><label class="cv-lbl">Outros (lavagem, estacionamento fixo, etc.)</label><div class="cv-inp-wrap"><span>R$</span><input type="number" value="${d.outrosExtras??''}" oninput="custoSetField('outrosExtras',this.value)"></div></div>
      <div class="cv-hint" style="margin:-6px 0 12px">O total desses três é dividido pelos dias trabalhados no mês</div>
      <button class="cv-btn" onclick="custoGoNext()">Ver resultado</button>`;
  }
  else if (stepId === 'resultado') {
    const calc = calcCustoPorKm(d);
    const extrasNovo = (d.seguroMensal||0) + ((d.ipvaAnual||0)/12) + (d.outrosExtras||0);
    const fixoMensal = ((d.situacao !== 'quitado') ? (d.parcelaMensal||0) : 0) + (extrasNovo > 0 ? extrasNovo : (d.extrasMensal||0));
    const custoDia = d.diasTrabalhados ? (fixoMensal / d.diasTrabalhados) : 0;
    const fuelLabel = d.combustivel === 'eletrico' ? 'Recarga' : 'Consumo';
    body = head + `<div class="cv-h2">Seu custo real por km</div><div class="cv-sub">Isso passa a descontar automaticamente de cada corrida.</div>
      <div class="cv-result"><div class="l">Custo total por km rodado</div><div class="v">${DB.fRI(calc.total)} <small>/km</small></div></div>
      <div class="cv-breakdown">
        <div class="cv-brow"><span class="k">${fuelLabel}</span><span class="v">${DB.fRI(calc.combKm)}/km</span></div>
        <div class="cv-brow"><span class="k">Manutenção</span><span class="v">${DB.fRI(calc.manutKm)}/km</span></div>
        ${d.depreciacaoPct ? `<div class="cv-brow"><span class="k">Depreciação (${d.depreciacaoPct}% a.a.)</span><span class="v">${DB.fRI(calc.depKm)}/km</span></div>` : ''}
        ${fixoMensal ? `<div class="cv-brow"><span class="k">Parcela + extras</span><span class="v">${DB.fRI(calc.fixoKm)}/km</span></div>` : ''}
      </div>
      ${custoDia ? `<div class="cv-daily"><svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="#D97706" stroke-width="2.4" style="flex:none;margin-top:1px"><circle cx="12" cy="12" r="9"/><path d="M12 8v4l2.5 2.5"/></svg><span>Seu carro custa <b>${DB.fRI(custoDia)} por dia trabalhado</b> só de parcela + extras — antes disso você ainda não está no lucro.</span></div>` : ''}
      <button class="cv-btn" onclick="custoFinish()">Confirmar e ativar</button>
      <button class="cv-btn-ghost" onclick="custoBack()">Ajustar valores</button>`;
  }
  const bodyEl = document.getElementById('custo-body');
  if (bodyEl) bodyEl.innerHTML = body;
}
// Banner discreto e fechável na Jornada, só enquanto não configurado.
// Fechar (X) esconde por 7 dias (não incomoda toda vez que abre o app).
function updateCustoBanner() {
  const el = document.getElementById('jorn-custo-banner');
  if (!el) return;
  const cfg = DB.cfg();
  if (cfg.custoVeiculo?.configurado) { el.style.display = 'none'; return; }
  const dismissedAt = cfg.custoBannerDismissedAt;
  if (dismissedAt && (Date.now() - dismissedAt) < 7*24*60*60*1000) { el.style.display = 'none'; return; }
  el.style.display = 'flex';
}
function dismissCustoBanner() {
  const cfg = DB.cfg(); cfg.custoBannerDismissedAt = Date.now(); DB.saveCfg(cfg);
  updateCustoBanner();
}

// Indicadores individuais por corrida (R$/km, R$/hora, Lucro) — mockup
// aprovado (25/07/2026), pedido do Yuri: "bater o olho" no desempenho de
// cada corrida específica, não só nos totais do dia. Reaproveita os MESMOS
// limiares de cor (rateColorClass/FLASH_KPI_DEFS) já usados na régua do
// MōB Flash — a mesma calibração que avalia a oferta ANTES de aceitar
// passa a avaliar o resultado DEPOIS de rodar. km/hora somam
// deslocamento+corrida (pra automáticas) ou usam o valor único já salvo
// (pra importadas). Lucro só aparece com custo do veículo configurado —
// sem isso, mostra o convite pra configurar em vez de inventar número.
function perfStripHtml(value, km, durMin) {
  if (!value || !km || km <= 0) return '';
  const rkm = value / km;
  const rhora = durMin > 0 ? value / (durMin/60) : null;
  const cvCfg = DB.cfg().custoVeiculo;
  const hasCusto = !!(cvCfg && cvCfg.configurado);
  const lucro = hasCusto ? value - (cvCfg.custoPorKm||0) * km : null;
  const n = hasCusto ? 3 : 2;
  const rkmCls = rateColorClass('rkm', rkm);
  const rhoraCls = rhora!=null ? rateColorClass('rhora', rhora) : '';
  const lucroCls = lucro!=null ? rateColorClass('lucro', lucro) : '';
  let html = `<div class="perf-strip" style="grid-template-columns:repeat(${n},1fr)">
    <div class="perf-cell ${rkmCls}"><strong>${DB.fR(rkm)}</strong><span>por km</span></div>
    ${rhora!=null?`<div class="perf-cell ${rhoraCls}"><strong>${DB.fR(rhora)}</strong><span>por hora</span></div>`:''}
    ${hasCusto?`<div class="perf-cell ${lucroCls}"><strong>${DB.fR(lucro)}</strong><span>lucro</span></div>`:''}
  </div>`;
  if (!hasCusto) {
    html += `<div class="cfg-note"><svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="#64748B" stroke-width="2" style="flex:none"><circle cx="12" cy="12" r="9"/><path d="M12 8v5M12 16h.01"/></svg><span>Configure o <a onclick="event.stopPropagation();openCustoVeiculo()">custo do veículo</a> pra ver o lucro real de cada corrida.</span></div>`;
  }
  return html;
}
function tripItem(t, tripDate) {
  const plat = {uber:'Uber','99':'99',particular:'Particular'}[t.platform||'uber'] || 'Uber';
  const dest = t.dest ? ' → '+esc(t.dest) : '';
  const dur = t.duration ? ' · '+fmtDur(t.duration) : '';
  // tid sempre único por renderização (nunca reusa t.id) — evita colisão de
  // DOM id quando a mesma corrida aparece em mais de uma tela ao mesmo tempo
  // (ex: dentro da Jornada e na tela Corridas), o que fazia o clique parecer
  // não fazer nada (abria o painel de detalhe da OUTRA instância, escondida)
  const tid = 'ti'+Date.now().toString(36)+Math.random().toString(36).slice(2,8);
  const cashSvg = `<svg viewBox="0 0 24 24" width="13" height="13" fill="currentColor"><rect x="2" y="7" width="20" height="13" rx="2"/><path d="M2 10h20" stroke="white" stroke-width="1.2" fill="none"/><path d="M2 17h20" stroke="white" stroke-width="1.2" fill="none"/><circle cx="12" cy="13" r="2.5" fill="white"/></svg>`;
  const extras = [];
  if (t.dinamico>0) extras.push(`<span class="chip chip-d"><svg viewBox="0 0 24 24" width="9" height="9" fill="currentColor"><path d="M13 2 3 14h7l-1 8 10-12h-7z"/></svg><strong>${DB.fR(t.dinamico)}</strong></span>`);
  if (t.tip>0)     extras.push(`<span class="chip chip-g"><svg viewBox="0 0 24 24" width="9" height="9" fill="currentColor"><path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.29 1.5 4.05 3 5.5l7 7Z"/></svg><strong>${DB.fR(t.tip)}</strong></span>`);
  // Taxa: só mostra quando há dado real do zip ou OCR confirmando 0%
  let _feePct = null;
  if (t.serviceFee !== undefined && t.serviceFee !== null) {
    _feePct = t.serviceFee === 0 ? '0%' : (t.serviceFee*100).toFixed(1).replace('.',',')+'%';
  } else if (t.taxaZero) { _feePct = '0%'; }
  if (_feePct !== null) extras.push(`<span class="chip chip-taxa">${_feePct}</span>`);
  if (t.pedagio)    extras.push(`<span class="chip-icon chip-toll"><svg viewBox="0 0 24 24" width="11" height="11" fill="currentColor"><rect x="3" y="3" width="2" height="18" rx="1"/><rect x="19" y="3" width="2" height="18" rx="1"/><rect x="5" y="10" width="11" height="3" rx="1.5"/></svg></span>`);
  if (t.dinheiro)  extras.push(`<span class="chip-icon chip-cash">${cashSvg}</span>`);
  const extraRow = extras.length?`<div class="li-extra">${extras.join('')}</div>`:'';
  const perfStrip = perfStripHtml(t.value, t.km, t.duration);
  const detRows = [
    `<div class="det-row"><span>Plataforma</span><strong>${plat}</strong></div>`,
    t.origin?`<div class="det-row"><span>Origem</span><strong>${esc(t.originFull||t.origin)}</strong></div>`:'',
    t.dest?`<div class="det-row"><span>Destino</span><strong>${esc(t.destFull||t.dest)}</strong></div>`:'',
    t.km>0?`<div class="det-row"><span>Distância${t.kmEstimado?' (estimada)':''}</span><strong>${t.kmEstimado?'~':''}${(t.km||0).toFixed(1)} km</strong></div>`:'',
    t.pedagioValor>0?`<div class="det-row"><span>Pedágio</span><strong>${DB.fR(t.pedagioValor)}</strong></div>`:'',
    t.duration>0?`<div class="det-row"><span>Duração</span><strong>${fmtDur(t.duration)}</strong></div>`:'',
    t.dinamico>0?`<div class="det-row"><span>Preço dinâmico</span><strong style="color:#F59E0B">+ ${DB.fR(t.dinamico)}</strong></div>`:'',
    t.tip>0?`<div class="det-row"><span>Gorjeta</span><strong style="color:#7C3AED">+ ${DB.fR(t.tip)}</strong></div>`:'',
    (_feePct!==null)?`<div class="det-row"><span>Taxa Uber</span><strong style="${_feePct==='0%'?'color:#15803D':''}">${_feePct}</strong></div>`:'',
    t.pedagio?`<div class="det-row"><span>Pedágio</span><strong>Sim</strong></div>`:'',
    t.dinheiro?`<div class="det-row"><span>Pagamento</span><strong>Dinheiro</strong></div>`:'',
    tripDate?`<button class="det-del-btn" onclick="event.stopPropagation();confirmDeleteTrip('${tripDate}','${t.id}')"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/></svg> Excluir corrida</button>`:'',
  ].filter(Boolean).join('');
  const cvCfg = DB.cfg().custoVeiculo;
  let liqRow = '';
  if (cvCfg?.configurado && t.value > 0) {
    const liq = t.value - (cvCfg.custoPorKm||0) * (t.km||0);
    const pct = Math.round(liq / t.value * 100);
    liqRow = `<span class="li-liq">${DB.fR(liq)} (${pct}%)</span>`;
  }
  return `<div class="trip-item-wrap" id="tw-${tid}">
    <div class="li-row" id="tr-${tid}" onclick="toggleTripDetail('${tid}')">
      ${platIcon(t.platform)}
      <div class="li-info"><h3 class="trip-title">${t.origin?esc(t.origin):'—'}${dest}</h3><p>${plat} · ${t.kmEstimado?'~':''}${(t.km||0).toFixed(1)} km${t.kmEstimado?' (estim.)':''}${dur}</p>${extraRow}</div>
      <div class="li-val"><strong>${DB.fR(t.value)}</strong>${t.time?`<span class="li-time">${t.time}</span>`:''}${liqRow}</div>
    </div>
    <div class="trip-detail-panel" id="td-${tid}" style="display:none">${perfStrip}${detRows}</div>
  </div>`;
}
function lancItemJornada(it) {
  const isRec = it._type === 'receita';
  const catKey = (it._type || 'despesa') + '_' + (it.scope || 'operacional');
  const cats = DB.cats(catKey) || DEFAULT_CATS[catKey] || [];
  const cat = cats.find(c => c.id === it.category);
  const icoSvg = CAT_ICONS[cat?.icon] || CAT_ICONS.plus;
  // Categoria como título principal (descrição vai pro detalhe). Se o
  // lançamento estiver vinculado a uma corrida (it.platform), usa o
  // resumo do trajeto (ex: "Centro - Botafogo") em vez do nome da categoria.
  const routeTitle = (it.platform && (it.origin || it.dest))
    ? (it.origin||'—') + (it.dest ? ' - '+it.dest : '') : null;
  const categoryTitle = routeTitle || it.categoryLabel || cat?.label || it.category || (isRec ? 'Receita' : 'Despesa');
  const platBox = it.platform ? platIcon(it.platform) : `<div class="li-ico ${isRec?'green':'red'}">${icoSvg}</div>`;
  const val = isRec ? '+'+DB.fR(it.amount) : '-'+DB.fR(it.amount);
  const today = DB.today();
  const status = it.status || 'pago';
  const vencido = status === 'a_pagar' && it.date && it.date < today;
  const badgeCls = status==='pago' ? 'st-badge st-pago' : vencido ? 'st-badge st-vencido' : 'st-badge st-apagar';
  const badgeTxt = status==='pago' ? (isRec?'Recebido':'Pago') : vencido ? 'Vencido' : 'A pagar';
  const rowCls = 'li-row-j ' + (status==='pago' ? 'pago' : vencido ? 'vencido' : 'apagar');
  const fmtD = d => d ? d.split('-').slice(1).reverse().join('/') : '';
  const subTxt = (it.scope==='pessoal'?'Pessoal':'Operacional') + (it.date?' · '+fmtD(it.date):'');

  const rows = [
    it.desc    ? `<div class="lanc-detail-row"><span>Descrição</span><strong>${it.desc}</strong></div>` : '',
    it.date    ? `<div class="lanc-detail-row"><span>Data</span><strong>${fmtD(it.date)}</strong></div>` : '',
    `<div class="lanc-detail-row"><span>Tipo</span><strong>${isRec?'Receita':'Despesa'}</strong></div>`,
  ].filter(Boolean).join('');

  const refArgs = `'${it.id}','${it.date||today}','${it._type||'despesa'}'`;
  const pagarBtn = status !== 'pago'
    ? `<button class="ld-btn pago" onclick="event.stopPropagation();markLancPago(${refArgs})">
        <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>Marcar pago
      </button>` : '';
  const actionsRow = `<div class="ld-actions">
      <button class="ld-btn edit" onclick="event.stopPropagation();editLanc(${refArgs})">
        <svg viewBox="0 0 24 24"><path d="M17 3a2.85 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"/></svg>Editar
      </button>
      ${pagarBtn}
      <button class="ld-btn del" onclick="event.stopPropagation();delLanc(${refArgs})" aria-label="Excluir">
        <svg viewBox="0 0 24 24"><path d="M3 6h18M8 6V4h8v2M19 6l-1 14H6L5 6"/></svg>
      </button>
    </div>`;

  return `<div class="lanc-item-wrap" id="li-wrap-${it.id}">
    <div class="${rowCls}" id="li-row-${it.id}" onclick="toggleLancDetail('${it.id}')">
      ${platBox}
      <div style="flex:1;min-width:0">
        
===== R$/hora @ 986109 =====
      const r = new FileReader();
      r.onload = () => res(r.result);
      r.onerror = () => rej(r.error);
      r.readAsText(file, 'utf-8');
    });

    const data = JSON.parse(text);
    const keys = Object.keys(data).filter(k => k.startsWith('sm_day_'));
    let added = 0, skipped = 0, days_count = keys.length;

    for (let i = 0; i < keys.length; i++) {
      const key    = keys[i];
      const newDay = data[key];
      const curDay = DB.get(key) || {date: newDay.date, trips:[], expenses:[], revenues:[], kmTotal:null, session:null};

      // Add only non-duplicate trips
      newDay.trips.forEach(t => {
        const dup = curDay.trips.some(ex =>
          ex.time === t.time && Math.abs((ex.value||0)-(t.value||0)) < 0.02
        );
        if (!dup) { curDay.trips.push(t); added++; }
        else skipped++;
      });

      // Set kmTotal if not already set
      if (!curDay.kmTotal && newDay.kmTotal) curDay.kmTotal = newDay.kmTotal;

      DB.set(key, curDay);

      // Update progress
      const pct = Math.round((i+1)/days_count*100);
      if (barEl) barEl.style.width = pct+'%';
      if (msgEl) msgEl.textContent = `Importando... dia ${i+1} de ${days_count} (${pct}%)`;

      // Yield to UI every 10 days
      if (i % 10 === 0) await new Promise(r => setTimeout(r, 0));
    }

    if (msgEl) msgEl.textContent = `Concluído! ${added} corridas importadas, ${skipped} já existiam.`;
    if (barEl) barEl.style.width = '100%';
    setTimeout(() => {
      closeMo('mo-hist-import');
      try { renderHome(); } catch(e) {}
      try { renderJornada(); } catch(e) {}
      showToast(`Histórico importado com sucesso!\n${added} corridas adicionadas em ${days_count} dias.`, 'success');
    }, 1200);

  } catch(err) {
    if (msgEl) msgEl.textContent = 'Erro: ' + err.message;
    console.error(err);
  }
}



// ── WAKE LOCK (mantém GPS ativo em segundo plano) ─────────────────────────
let _wakeLock = null;

async function requestWakeLock() {
  if (!('wakeLock' in navigator)) return;
  try {
    _wakeLock = await navigator.wakeLock.request('screen');
    _wakeLock.addEventListener('release', () => {
      _wakeLock = null;
      // Se GPS ainda ativo, tenta reativar automaticamente
      if (S.gps.st === 'running') requestWakeLock();
    });
    console.log('SmartMobi: Wake Lock ativado — GPS continua em segundo plano');
  } catch(e) {
    console.warn('Wake Lock não disponível:', e.message);
  }
}

async function releaseWakeLock() {
  if (_wakeLock) {
    await _wakeLock.release();
    _wakeLock = null;
  }
}

// Reativa Wake Lock quando usuário volta pro app com GPS ativo
document.addEventListener('visibilitychange', async () => {
  if (document.visibilityState === 'visible' && S.gps.st === 'running') {
    await requestWakeLock();
  }
});



// ── NATIVE OVERLAY BRIDGE ──────────────────────────────────────────────────
// Chamado pelo app nativo quando uma corrida é capturada pelo Accessibility Service
function receiveOverlayTrip(trip) {
  if (!trip || !trip.value) return;
  const day = DB.day(S.curDate);
  if (isTripDuplicate(trip, day.trips)) return;
  trip.id = trip.id || DB.uid();
  day.trips.push(trip);
  DB.saveDay(day);
  SYNC.push('trips', { id: trip.id, date: S.curDate, platform: trip.platform || 'uber',
    time: trip.time || null, duration: trip.duration || 0, origin: trip.origin || '',
    dest: trip.dest || '', value: parseFloat(trip.value) || 0, km: parseFloat(trip.km) || 0,
    category: trip.category || '', surge: trip.surge || false, airport: trip.airport || false,
    source: 'overlay' });
  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
  }, 80);
  // Vibração de confirmação
  if (window.SmartMobiNative) SmartMobiNative.vibrate(80);
}

// Banner de configuração para o app nativo
function showNativeBanner(msg) {
  const existing = document.getElementById('native-banner');
  if (existing) existing.remove();
  const el = document.createElement('div');
  el.id = 'native-banner';
  el.style.cssText = 'position:fixed;top:0;left:0;right:0;z-index:9999;background:#0F172A;color:#fff;font-size:12px;font-weight:600;padding:10px 16px;display:flex;align-items:center;justify-content:space-between;gap:8px';
  el.innerHTML = '<span>' + msg + '</span><button onclick="SmartMobiNative&&SmartMobiNative.openA11ySettings()" style="background:#22C55E;border:0;color:#fff;border-radius:8px;padding:4px 10px;font-size:11px;font-weight:700;cursor:pointer">Configurar</button>';
  document.body.prepend(el);
  setTimeout(() => el.remove(), 6000);
}

function onNativeReady() {
  // App rodando como APK nativo
  console.log('SmartMobi Native v' + (SmartMobiNative?.getVersion?.() || '?'));
  syncFlashConfigToNative();
}

// ── MōB Flash: "Indicadores" que aparecem no card (números coloridos) ──
const FLASH_KPI_DEFS = [
  { key:'rkm',    label:'R$ por km',          short:'R$/KM',   icon:'$',  defRed:1.2, defGreen:2.0 },
  { key:'rhora',  label:'R$ por hora',        short:'R$/HORA', icon:'\u23F1', defRed:25,  defGreen:45 },
  { key:'rmin',   label:'R$ por minuto',      short:'R$/MIN',  icon:'\u23F2', defRed:0.6, defGreen:1.0 },
  { key:'margem', label:'% Lucro',            short:'% LUCRO', icon:'%',  defRed:15,  defGreen:40 },
  { key:'lucro',  label:'Lucro em R$',        short:'R$ LUCRO',icon:'R$', defRed:5,   defGreen:15 },
];
const FLASH_DEFAULT_ON = { rkm:true, rhora:true, rmin:false, margem:true, lucro:true };
const FLASH_PREVIEW_SAMPLE = { rkm:2.37, rhora:49, rmin:0.58, margem:12, lucro:4 };

// Reaproveita o mesmo limiar vermelho/verde do MōB Flash (Indicadores) pra
// colorir a faixa de destaque R$/h · R$/km da Jornada (23/07/2026, pedido
// do Yuri) — mesma calibração em dois lugares, sem duplicar configuração.
// value <= red → 'r' | value >= green → '' (verde, classe base) | entre os
// dois → 'o'. Se o motorista nunca mexeu na régua, cai no padrão de
// fábrica (defRed/defGreen do FLASH_KPI_DEFS).
function rateColorClass(kpiKey, value) {
  const def = FLASH_KPI_DEFS.find(d => d.key === kpiKey);
  if (!def) return '';
  const cfg = (DB.cfg().flashReader && DB.cfg().flashReader.kpis && DB.cfg().flashReader.kpis[kpiKey]) || {};
  const red = cfg.red !== undefined ? cfg.red : def.defRed;
  const green = cfg.green !== undefined ? cfg.green : def.defGreen;
  if (value <= red) return 'r';
  if (value >= green) return '';
  return 'o';
}

// ── MōB Flash: "Recusas" — regras que recusam a corrida sozinhas, mesmo
// com os Indicadores financeiros bons. PEDIDO (16-17/07/2026): saiu do
// grupo de KPIs pra virar sua própria seção, com frase explicando o
// efeito de cada uma ("Recusar ... " em vez de "Recusa se ...").
// "parada" é bool puro (sem valor numérico); os outros 3 numéricos
// ("num") usam o mesmo painel de edição; "endereco" é lista (embarque
// OU destino) e abre uma tela própria.
const FLASH_RECUSA_DEFS = [
  { key:'parada', label:'Corrida com parada extra', type:'bool',
    icon:'\u26A0', phrase: () => 'Recusar corrida com parada extra' },
  { key:'nota', label:'Nota do passageiro', type:'num', unit:'', step:0.1, min:1, max:5,
    icon:'\u2605', phrase: v => `Recusar nota do passageiro abaixo de ${flashFmtNum(v)}` },
  { key:'passageiroNovo', label:'Passageiro novo', type:'num', unit:'corridas', step:1, min:0, max:200,
    icon:'#', phrase: v => `Recusar passageiro com menos de ${Math.round(v)} corridas` },
  { key:'distancia', label:'Distância deslocamento', type:'num', unit:'km', step:0.5, min:0.5, max:30,
    icon:'KM', phrase: v => `Recusar buscar com mais de ${flashFmtNum(v)} km` },
  { key:'endereco', label:'Endereço recusado', type:'list',
    icon:'\u2298', phrase: () => 'Recusar embarque ou destino em locais bloqueados' },
];
const FLASH_RECUSA_DEFAULT = {
  parada:  { enabled:true },
  nota:    { enabled:false, value:4.5 },
  passageiroNovo: { enabled:true, value:5 },
  distancia: { enabled:false, value:3.0 },
  endereco:  { enabled:true, list:[] },
};
function flashFmtNum(v) { return (Math.round(v*10)/10).toString().replace('.', ','); }

let flashDraft = null;

function getFlashDefaults() {
  const kpis = {};
  FLASH_KPI_DEFS.forEach(d => { kpis[d.key] = { enabled: FLASH_DEFAULT_ON[d.key], red: d.defRed, green: d.defGreen }; });
  const recusas = {};
  FLASH_RECUSA_DEFS.forEach(d => { recusas[d.key] = JSON.parse(JSON.stringify(FLASH_RECUSA_DEFAULT[d.key])); });
  return { enabled: true, kpis, recusas };
}

// Carrega o draft (config salva + defaults) — usado tanto pelo "Mais > MōB
// Flash" quanto pelo assistente de Iniciar Jornada, sempre o mesmo dado.
async function _flashLoadDraft() {
  if (!_bootPullDone) { showToast('Sincronizando seus dados...'); await _waitBootPull(); }
  const cfg = DB.cfg();
  const saved = cfg.flashReader;
  const defaults = getFlashDefaults();
  flashDraft = { enabled: saved && saved.enabled !== undefined ? saved.enabled : defaults.enabled, kpis: {}, recusas: {} };
  FLASH_KPI_DEFS.forEach(d => {
    const s = (saved && saved.kpis && saved.kpis[d.key]) || {};
    flashDraft.kpis[d.key] = {
      enabled: s.enabled !== undefined ? s.enabled : defaults.kpis[d.key].enabled,
      red: s.red !== undefined ? s.red : defaults.kpis[d.key].red,
      green: s.green !== undefined ? s.green : defaults.kpis[d.key].green
    };
  });
  FLASH_RECUSA_DEFS.forEach(d => {
    const s = (saved && saved.recusas && saved.recusas[d.key]) || {};
    const def = defaults.recusas[d.key];
    if (d.type === 'list') {
      flashDraft.recusas[d.key] = {
        enabled: s.enabled !== undefined ? s.enabled : def.enabled,
        // PEDIDO (17/07/2026): cada item agora é {label, type:'rua'|'bairro'}
        // pra dar pra mostrar a tag na lista — normaliza entradas antigas
        // (string solta, de antes dessa mudança) na hora de carregar.
        list: Array.isArray(s.list) ? s.list.map(_flashNormEnderecoItem).filter(Boolean) : def.list.slice()
      };
    } else {
      flashDraft.recusas[d.key] = {
        enabled: s.enabled !== undefined ? s.enabled : def.enabled,
        value: s.value !== undefined ? s.value : def.value
      };
    }
  });
}

let flashWizardMode = 'settings';   // 'settings' (Mais > MōB Flash) ou 'start' (Iniciar Jornada)
let flashWizardOnComplete = null;   // callback chamado ao terminar/pular, só no modo 'start'

// Entrada pelo "Mais" — mesma tela do assistente, só que sem o vínculo com
// iniciar jornada. Termina com "Salvar", não com "Iniciar jornada".
async function openFlashConfig() {
  await _flashLoadDraft();
  flashWizardMode = 'settings';
  flashWizardOnComplete = null;
  _flashWizardShowStep(1);
  renderFlashKpiList();
  renderFlashRecusaList();
  renderFlashPreview();
  renderFlashPerms();
  openMo('mo-flash');
}

// Entrada pelo botão de Iniciar Jornada — mesmo assistente, terminando em
// "Iniciar jornada" (ou pulando com "Iniciar sem o MōB Flash"). onComplete
// é chamado exatamente uma vez, ao final de qualquer um dos dois caminhos.
async function openFlashWizardForJourney(onComplete) {
  await _flashLoadDraft();
  flashWizardMode = 'start';
  flashWizardOnComplete = onComplete;
  _flashWizardShowStep(1);
  renderFlashKpiList();
  renderFlashRecusaList();
  renderFlashPreview();
  renderFlashPerms();
  openMo('mo-flash');
}

function _flashWizardShowStep(n) {
  document.getElementById('flash-step1').style.display = n === 1 ? '' : 'none';
  document.getElementById('flash-step2').style.display = n === 2 ? '' : 'none';
  document.getElementById('flash-ruler').style.display = 'none';
  document.getElementById('flash-recusa-num').style.display = 'none';
  document.getElementById('flash-enderecos').style.display = 'none';
  // Pular só faz sentido quando tem uma jornada esperando pra iniciar —
  // no modo settings (Mais > MōB Flash) esses links somem.
  const skip1 = document.getElementById('flash-skip1');
  const skip2 = document.getElementById('flash-skip2');
  const primary2 = document.getElementById('flash-step2-primary');
  if (skip1) skip1.style.display = (flashWizardMode === 'start') ? '' : 'none';
  if (skip2) skip2.style.display = (flashWizardMode === 'start') ? '' : 'none';
  if (primary2) primary2.textContent = (flashWizardMode === 'start') ? 'Iniciar jornada' : 'Salvar configurações';
}

function flashWizardNext() {
  _flashWizardShowStep(2);
  renderFlashPerms();
}

function flashWizardBack() {
  _flashWizardShowStep(1);
}

// "Pular tudo" (passo 1) e "Iniciar sem o MōB Flash" (passo 2) — mesmo
// efeito: desliga o Flash, salva e segue o fluxo sem exigir nada mais.
function flashWizardSkip() {
  flashDraft.enabled = false;
  flashConfigSave(true);
}

// startFlash=true → veio do botão principal ("Iniciar jornada"/"Salvar").
// startFlash=false → veio de um link de pular ("Iniciar sem o Flash").
function flashWizardFinish(startFlash) {
  if (startFlash) {
    // Card pode mostrar só a recusa (17/07/2026), sem indicador nenhum —
    // então "tem algo pra mostrar" agora conta os dois grupos.
    const anyOn = Object.values(flashDraft.kpis).some(k => k.enabled) ||
                  Object.values(flashDraft.recusas).some(r => r.enabled);
    flashDraft.enabled = anyOn;
  } else {
    flashDraft.enabled = false;
  }
  flashConfigSave(true);
}

// ── Régua de limite vermelho/verde (substitui os dois campos de texto) ──
const FLASH_RULER_BOUNDS = { rkm:[0,5], rhora:[0,100], rmin:[0,3], margem:[0,100], lucro:[0,50] };
let flashRulerKey = null;
let flashRulerDrag = null; // 'red' | 'green' | null

function flashOpenRuler(key) {
  flashRulerKey = key;
  const d = FLASH_KPI_DEFS.find(x => x.key === key);
  document.getElementById('flash-ruler-ico').textContent = d.icon;
  document.getElementById('flash-ruler-title').textContent = d.label;
  document.getElementById('flash-step1').style.display = 'none';
  document.getElementById('flash-step2').style.display = 'none';
  document.getElementById('flash-ruler').style.display = '';
  _flashRulerRender();
}

function flashRulerClose() {
  flashRulerKey = null;
  _flashWizardShowStep(1); // régua só é aberta a partir do passo 1 (lista de KPIs)
  renderFlashKpiList();
  renderFlashRecusaList();
  renderFlashPreview();
}

function _flashRulerPct(key, val) {
  const [lo, hi] = FLASH_RULER_BOUNDS[key];
  return Math.max(0, Math.min(100, ((val - lo) / (hi - lo)) * 100));
}
function _flashRulerVal(key, pct) {
  const [lo, hi] = FLASH_RULER_BOUNDS[key];
  return lo + (pct / 100) * (hi - lo);
}
function _flashRulerFmt(key, v) {
  return key === 'margem' ? Math.round(v) + '%' : v.toFixed(2).replace('.', ',');
}

function _flashRulerRender() {
  const k = flashDraft.kpis[flashRulerKey];
  const redPct = _flashRulerPct(flashRulerKey, k.red);
  const greenPct = _flashRulerPct(flashRulerKey, k.green);
  const redEl = document.getElementById('flash-ruler-red');
  const greenEl = document.getElementById('flash-ruler-green');
  redEl.style.left = redPct + '%';
  greenEl.style.left = greenPct + '%';
  document.getElementById('flash-ruler-red-val').textContent = _flashRulerFmt(flashRul
===== porHora @ 657230 =====
pjSchedules.splice(idx, 1); renderPjSchedules(); }
function renderPjSchedules() {
  const el = document.getElementById('pj-schedules');
  if (!el) return;
  if (!pjSchedules.length) { el.innerHTML = '<p style="font-size:12px;color:var(--muted);margin:2px 0">Nenhum horário adicionado</p>'; return; }
  el.innerHTML = pjSchedules.map((s,i) => `
    <div class="pj-sched-row">
      <input type="time" class="ag-input" value="${s.start}" onchange="pjSchedules[${i}].start=this.value">
      <span>→</span>
      <input type="time" class="ag-input" value="${s.end}" onchange="pjSchedules[${i}].end=this.value">
      <button class="pj-sched-del" onclick="pjRemoveSchedule(${i})">&#x2715;</button>
    </div>
  `).join('');
}
function pjSaveEntry() {
  if (!pjSelDays.length) { showToast('Selecione ao menos um dia', 'error'); return; }
  const validSched = pjSchedules.filter(s => s.start && s.end);
  const moneyGoal = parseFloat(document.getElementById('pj-money')?.value||0) || null;
  const kmGoal = parseFloat(document.getElementById('pj-km')?.value||0) || null;
  const plans = DB.planJornada();
  const entry = { id: DB.uid(), days: pjSelDays.slice(), moneyGoal, kmGoal, schedules: validSched };
  plans.push(entry);
  DB.savePlanJornada(plans);
  SYNC.push('plan_jornada', { id: entry.id, days: entry.days, money_goal: entry.moneyGoal,
    km_goal: entry.kmGoal, schedules: entry.schedules });
  pjSelDays = []; pjSchedules = [];
  document.querySelectorAll('.pj-day').forEach(b => b.classList.remove('on'));
  const mEl = document.getElementById('pj-money'); if (mEl) mEl.value = '';
  const kEl = document.getElementById('pj-km'); if (kEl) kEl.value = '';
  renderPjSchedules();
  renderPjList();
  renderMetasDoDia(DB.day(S.curDate));
  showToast('Jornada planejada salva!');
}
function pjRemoveEntry(id) {
  showConfirm('Remover essa jornada planejada?', () => {
    DB.savePlanJornada(DB.planJornada().filter(p => p.id !== id));
    SYNC.del('plan_jornada', { id });
    renderPjList();
    renderMetasDoDia(DB.day(S.curDate));
  });
}
function renderPjList() {
  const el = document.getElementById('pj-list');
  if (!el) return;
  const plans = DB.planJornada();
  if (!plans.length) { el.innerHTML = '<div class="empty">Nenhuma jornada planejada ainda</div>'; return; }
  el.innerHTML = plans.map(p => {
    const daysTxt = (p.days||[]).slice().sort().map(d=>PJ_DOW[d]).join(' · ');
    const goalParts = [];
    if (p.moneyGoal) goalParts.push(DB.fRI(p.moneyGoal));
    if (p.kmGoal) goalParts.push(p.kmGoal+' km');
    const goalTxt = goalParts.length ? goalParts.join(' · ') : 'Sem meta definida';
    const chips = (p.schedules||[]).map(s => `<span class="pj-chip">${s.start} → ${s.end}</span>`).join('');
    return `<div class="pj-card">
      <div class="pj-card-top"><div class="pj-card-days">${daysTxt}</div><button class="pj-card-del" onclick="pjRemoveEntry('${p.id}')">&#x2715;</button></div>
      <div class="pj-card-goal">${goalTxt}</div>
      <div class="pj-card-times">${chips}</div>
    </div>`;
  }).join('');
}

function sessionItem(label, sess, origIdx, dateStr, day, live) {
  const startMs = sess.start || 0, endMs = sess.end || 0;
  const km = sess.km || 0, elapsedSecs = sess.elapsed || 0;
  const dur = fmtDur((elapsedSecs||0)/60);
  const range = fmtHM(startMs) + '–' + (endMs ? fmtHM(endMs) : (live ? 'Agora' : '—'));
  const status = live ? 'Online' : 'Encerrada';
  const badgeCls = live ? 'live' : 'end';
  const cardId = 'jss-' + dateStr + '-' + origIdx;

  // Corridas desta sessão — filtrar por horário (suporta t.ts e t.time HH:MM)
  const trips = (day?.trips||[]).filter(t => {
    let ts = t.ts || 0;
    if (!ts && t.time && dateStr) {
      // Construir timestamp a partir de "HH:MM" + data da sessão
      ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
    }
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  // Corridas automáticas desta sessão — mesmo filtro por horário, usando
  // trip_started_at/accepted_at (autoTripTs, ver loadAutoTripsForDay).
  const autoTrips = (day?.autoTrips||[]).filter(t => {
    const ts = autoTripTs(t);
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  const corridas = trips.length + autoTrips.length;
  const ganhos = trips.reduce((s,t)=>s+(t.value||0),0) + autoTrips.reduce((s,t)=>s+(t.offer_value||0),0);
  // Km ativo = soma do km de cada corrida realizada (tempo 'em corrida')
  // Km deslocamento = km total do GPS MENOS o km ativo (tempo rodando sem
  // corrida — indo pro ponto, retornando pra casa, esperando chamado etc.)
  const kmAtivo = trips.reduce((s,t)=>s+(parseFloat(t.km)||0),0)
                + autoTrips.reduce((s,t)=>s+(parseFloat(t.real_km_trip ?? t.offer_km_trip)||0),0);
  const kmDesloc = Math.max(0, km - kmAtivo);

  const horasDecimal = elapsedSecs > 0 ? elapsedSecs/3600 : 0;
  const rshVal = horasDecimal > 0 ? ganhos/horasDecimal : null;
  const rskmVal = km > 0 ? ganhos/km : null;
  const rsPorHora = rshVal !== null ? DB.fR(rshVal) : '—';
  const rsPorKm = rskmVal !== null ? DB.fR(rskmVal) : '—';
  const rshCls = rshVal !== null ? rateColorClass('rhora', rshVal) : '';
  const rskmCls = rskmVal !== null ? rateColorClass('rkm', rskmVal) : '';
  // Lucro da sessão = soma do lucro de cada corrida (mesmo cálculo do
  // perfStripHtml/detalhe de corrida: valor - custoPorKm*km), só quando o
  // custo do veículo está configurado — igual já vale por corrida.
  const cvCfg = DB.cfg().custoVeiculo;
  const hasCusto = !!(cvCfg && cvCfg.configurado);
  const lucroVal = hasCusto ? ganhos - (cvCfg.custoPorKm||0) * kmAtivo : null;
  const lucroCls = lucroVal !== null ? rateColorClass('lucro', lucroVal) : '';
  // Barra Online/Buscando passageiro/Em corrida desta sessão — mesma fonte
  // real da máquina de estados do APK, agora escopada só às autoTrips que
  // caem dentro do horário desta sessão (já filtradas acima).
  const sessBd = (typeof journeyBreakdownForSession === 'function')
    ? journeyBreakdownForSession(sess, elapsedSecs, km, autoTrips)
    : computeStatusBreakdown(elapsedSecs, km, autoTrips);
  const bdListId = 'bl-' + cardId;
  const editBtn = !live ? `<button class="jss-act" onclick="event.stopPropagation();editJss('${dateStr}',${origIdx})" title="Editar"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg></button>
        <button class="jss-act del" onclick="event.stopPropagation();deleteJss('${dateStr}',${origIdx})" title="Excluir"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/></svg></button>` : '';

  return `<div class="jss-card ${badgeCls}" id="${cardId}">
    <div class="jss-row" onclick="toggleJss('${cardId}')">
      <div class="jss-info">
        <div class="jss-top"><span class="jss-name">${label}</span><span class="jss-badge ${badgeCls}">${status}</span>${editBtn}</div>
        <div class="jss-sub">${range} · ${dur}</div>
      </div>
      <div style="text-align:right;flex-shrink:0;display:flex;flex-direction:column;align-items:flex-end;gap:2px">
        <span style="font-size:14px;font-weight:800;color:var(--text)">${(sess.km||0).toFixed(1)} km</span>
        <span class="jss-ganhos">${DB.fR(ganhos)}</span>
      </div>
      <div class="jss-chev"><svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="6 9 12 15 18 9"/></svg></div>
    </div>
    <div class="jss-rate-row">
      <div class="jss-rcard ${rshCls}"><strong>${rsPorHora}</strong><span>R$/hora</span></div>
      <div class="jss-rcard ${rskmCls}"><strong>${rsPorKm}</strong><span>R$/km</span></div>
      ${hasCusto ? `<div class="jss-rcard ${lucroCls}"><strong>${DB.fR(lucroVal)}</strong><span>Lucro</span></div>` : `<div class="jss-rcard"><strong>—</strong><span>Lucro</span></div>`}
    </div>
    ${elapsedSecs > 0 ? `<div class="jss-statusbar">
      <div class="sbar-wrap" onclick="event.stopPropagation();toggleBar('${bdListId}')"><div class="sbar">${buildSbarHtml(sessBd)}</div></div>
      <div class="sbar-list" id="${bdListId}">${buildSrowsHtml(sessBd)}</div>
    </div>` : ''}
    <div class="jss-trips">
      <div class="jss-trips-head">
        <h4>Corridas desta jornada</h4>
        ${corridas > 0 ? `<span>${corridas} corrida${corridas !== 1 ? 's' : ''}</span>` : ''}
      </div>
      ${corridas > 0
        ? [
            ...trips.map(t => ({ ts: t.ts || (t.time?new Date(dateStr+'T'+t.time+':00').getTime():0), html: tripItem(t) })),
            ...autoTrips.map(t => ({ ts: autoTripTs(t), html: autoTripItem(t) })),
          ].sort((a,b)=>b.ts-a.ts).map(x=>x.html).join('')
        : '<div class="jss-trips-empty">Nenhuma corrida nesta jornada</div>'}
    </div>
  </div>`;

}
// ── Editar sessão ─────────────────────────────────────────────────────────
function editJss(dateStr, idx) {
  const day = DB.day(dateStr);
  const sess = (day.sessions || [])[idx];
  if (!sess) return;

  document.getElementById('jss-edit-date').value = dateStr;
  document.getElementById('jss-edit-idx').value  = idx;
  document.getElementById('jss-edit-km').value   = (sess.km || 0).toFixed(1);

  // Hora início e fim a partir dos timestamps da sessão
  const fmt = ms => {
    if (!ms) return '';
    const d = new Date(ms);
    return String(d.getHours()).padStart(2,'0') + ':' + String(d.getMinutes()).padStart(2,'0');
  };
  document.getElementById('jss-edit-inicio').value = fmt(sess.start);
  document.getElementById('jss-edit-fim').value    = fmt(sess.end);

  openMo('mo-jss-edit');
}

function saveJss() {
  const dateStr = document.getElementById('jss-edit-date').value;
  const idx     = parseInt(document.getElementById('jss-edit-idx').value);
  const km      = parseFloat(document.getElementById('jss-edit-km').value) || 0;
  const inicioStr = document.getElementById('jss-edit-inicio').value; // "HH:MM"
  const fimStr    = document.getElementById('jss-edit-fim').value;    // "HH:MM"

  const day = DB.day(dateStr);
  if (!day.sessions || !day.sessions[idx]) { closeMo('mo-jss-edit'); return; }

  const sess = day.sessions[idx];

  // Recalcular timestamps de início e fim mantendo a data original
  const toMs = (base, timeStr) => {
    if (!timeStr) return base;
    const [hh, mm] = timeStr.split(':').map(Number);
    const d = new Date(base || Date.now());
    d.setHours(hh, mm, 0, 0);
    return d.getTime();
  };
  const newStart = toMs(sess.start, inicioStr);
  const newEnd   = toMs(sess.end || sess.start, fimStr);
  // Elapsed recalculado a partir da diferença início→fim
  const elapsed  = newEnd > newStart ? Math.round((newEnd - newStart) / 1000) : (sess.elapsed || 0);

  sess.km      = km;
  sess.start   = newStart;
  sess.end     = newEnd;
  sess.elapsed = elapsed;
  DB.saveDay(day);

  // Sincronizar com Supabase
  if (_supaUser && sess.id) {
    _SUPA.from('sessions').update({
      km:           parseFloat(km.toFixed(3)),
      start_ms:     newStart,
      end_ms:       newEnd,
      elapsed_secs: elapsed
    }).eq('id', sess.id).then(() => {}).catch(() => {});
  }

  closeMo('mo-jss-edit');
  renderJornada();
  if (S.sec === 'relatorios') renderHome();
  showToast('Sessão atualizada');
}

function deleteJss(dateStr, idx) {
  showConfirm('Excluir esta sessão?', () => {
    const day = DB.day(dateStr);
    if (!day.sessions || !day.sessions[idx]) return;

    const sess = day.sessions[idx];
    day.sessions.splice(idx, 1);
    DB.saveDay(day);

    // Remover do Supabase (delete real, não update)
    if (sess.id) {
      SYNC.del('sessions', { id: sess.id });
    }

    renderJornada();
    if (S.sec === 'relatorios') renderHome();
    showToast('Sessão excluída');
  }, { confirmLabel: 'Excluir', danger: true, cancelLabel: 'Cancelar' });
}


// CORRIGIDO (15/08/2026, relatado pelo Yuri): mesmo bug do toggleTripDetail
// (comentário abaixo, 24/07) só que no card da jornada — toggleJss só
// mexia numa classe CSS, sem guardar estado. O refresh automático de 25s
// (_refreshAutoTripsIfOnJornada) recria os cards do zero sempre fechados,
// então a expansão "voltava a fechar sozinha" em segundos. Mesmo padrão
// de correção: guarda o id aberto, reaplica depois de qualquer redesenho.
let _openJssId = null;
function toggleJss(id){
  const el=document.getElementById(id);
  if(!el) return;
  const willOpen = !el.classList.contains('open');
  el.classList.toggle('open');
  _openJssId = willOpen ? id : null;
}
function reapplyOpenJss(){
  if (!_openJssId) return;
  const el = document.getElementById(_openJssId);
  if (el) el.classList.add('open');
}
// CORRIGIDO (24/07/2026, relatado pelo Yuri): o painel de detalhe de uma
// corrida é só um display:none/block num elemento — quando a tela
// redesenha os cards (o refresh automático de 25s/60s da Jornada/Home,
// por exemplo), os elementos são recriados do zero, sempre fechados por
// padrão, perdendo o que estava aberto na hora errada (parecia fechar
// sozinho, sem motivo). Guarda qual tid está aberto e reaplica depois de
// qualquer redesenho.
let _openTripDetailTid = null;
// Linha vertical da rota (Aceite/Embarque/Destino) tem que parar exatamente
// no centro do último marcador (pin de Destino) — pedido do Yuri
// (16/08/2026): como os marcadores têm tamanhos diferentes (círculo
// pequeno vs pin maior), um valor fixo de CSS nunca acerta em todo caso,
// então mede a posição real na tela depois de renderizado.
function sizeRouteLine(panelEl) {
  try {
    const block = panelEl.querySelector('.route-block');
    if (!block) return;
    const marks = block.querySelectorAll('.route-mark');
    const line = block.querySelector('.route-line');
    if (!line || marks.length < 2) return;
    const first = marks[0], last = marks[marks.length - 1];
    const blockTop = block.getBoundingClientRect().top;
    const firstMid = first.getBoundingClientRect().top - blockTop + first.offsetHeight / 2;
    const lastMid = last.getBoundingClientRect().top - blockTop + last.offsetHeight / 2;
    if (lastMid <= firstMid) return;
    line.style.top = firstMid + 'px';
    line.style.height = (lastMid - firstMid) + 'px';
  } catch (e) {}
}
function toggleTripDetail(tid) {
  const det = document.getElementById('td-'+tid);
  const row = document.getElementById('tr-'+tid);
  if (!det||!row) return;
  const open = det.style.display!=='none';
  document.querySelectorAll('.trip-detail-panel').forEach(el=>el.style.display='none');
  document.querySelectorAll('.li-row[id^="tr-"]').forEach(el=>el.classList.remove('open'));
  if (!open){det.style.display='block';row.classList.add('open');_openTripDetailTid=tid;sizeRouteLine(det);}
  else {_openTripDetailTid=null;}
}
// Chamar depois de qualquer redesen
===== total do dia @ 32748 =====
on}
.sbar{height:22px;border-radius:8px;overflow:hidden;display:flex;background:#F1F5F9;margin:0;pointer-events:none}
.sbar i{display:flex;align-items:center;justify-content:center;height:100%;font-size:9.5px;font-weight:800;color:#fff}
.sbar-list{max-height:0;overflow:hidden;transition:max-height .3s ease}
.sbar-list.open{max-height:220px;margin-top:10px}
.sbar-list .srow{display:flex;align-items:center;gap:8px;padding:6px 0;border-top:1px solid var(--line)}
.sbar-list .srow:first-child{border-top:0}
.sbar-list .sdot{width:8px;height:8px;border-radius:50%;flex-shrink:0}
.sbar-list .srow b{flex:1;font-size:11px;font-weight:800;color:var(--text);text-transform:uppercase;letter-spacing:.03em}
.sbar-list .srow .pct{font-size:10px;font-weight:800;color:var(--muted);width:28px;text-align:right}
.sbar-list .srow .stempo{font-size:10.5px;font-weight:800;color:var(--text);width:48px;text-align:right}
.sbar-list .srow .skm{font-size:10.5px;font-weight:700;color:var(--muted);width:56px;text-align:right}
.td-row.bd .sbar{background:rgba(255,255,255,.10)}
.td-row.bd .sbar-list.open{margin-top:12px}
.td-row.bd .sbar-list .srow{border-top:1px solid rgba(255,255,255,.14)}
.td-row.bd .sbar-list .srow b,.td-row.bd .sbar-list .srow .stempo{color:#fff}
.td-row.bd .sbar-list .srow .pct,.td-row.bd .sbar-list .srow .skm{color:rgba(255,255,255,.6)}
.jss-statusbar{padding:2px 14px 12px}
/* .journey.paused removido */
.j-head{display:flex;justify-content:space-between;align-items:center;margin-bottom:12px;gap:8px}
.j-status{display:none}
.j-status i{width:8px;height:8px;border-radius:50%;background:#fff;flex-shrink:0}
.journey.running .j-status i{animation:pulse2 2s infinite}
@keyframes pulse2{0%,100%{box-shadow:0 0 0 0 rgba(255,255,255,.5)}50%{box-shadow:0 0 0 5px rgba(255,255,255,0)}}
.j-import{background:rgba(255,255,255,.16);border:1px solid rgba(255,255,255,.22);border-radius:999px;padding:6px 11px;font-size:11px;font-weight:760;color:#fff;display:inline-flex;align-items:center;gap:5px;cursor:pointer;border:0;touch-action:manipulation;margin-left:auto}
.j-import svg{width:13px;height:13px}
.j-toprow{display:flex;align-items:center;gap:10px;margin-bottom:12px}
.j-current{display:grid;grid-template-columns:1.3fr 1fr 1fr;gap:8px;flex:1;min-width:0}
.j-current h2{font-family:'Inter',system-ui,sans-serif;font-size:22px;font-weight:800;letter-spacing:-.02em;line-height:1}
.j-current p{font-size:10.5px;font-weight:600;opacity:.85;margin-top:4px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.j-km{font-size:12px;font-weight:620}
.j-km strong{display:block;font-family:'Inter',system-ui,sans-serif;font-size:22px;font-weight:800;letter-spacing:-.02em;line-height:1}
.j-km span{display:block;margin-top:4px;opacity:.85;font-size:10.5px;font-weight:700;text-transform:uppercase;letter-spacing:.03em}
.j-rates2{display:grid;grid-template-columns:1fr 1fr;gap:8px;flex:1;min-width:0}
.j-bottomrow{display:flex;align-items:center;gap:10px}
.j-metrics{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin-bottom:8px}
.j-mini{background:transparent;border:none;border-radius:0;padding:6px 4px;text-align:center}
.j-mini strong{display:block;font-family:'Inter',system-ui,sans-serif;font-size:22px;font-weight:800;text-align:center}
.j-mini span{display:block;font-size:9.5px;font-weight:700;text-transform:uppercase;letter-spacing:.05em;opacity:.78;margin-top:3px}
.j-metrics2{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin-bottom:14px}
.j-mini.sm{padding:6px 5px;background:rgba(255,255,255,.12);border-radius:12px}
.j-mini.sm strong{font-size:13px}
.j-mini.sm span{font-size:8.5px}
.km-edit-btn{width:18px;height:18px;border-radius:50%;border:0;background:rgba(255,255,255,.22);color:#fff;display:flex;align-items:center;justify-content:center;cursor:pointer;touch-action:manipulation;flex-shrink:0;padding:0}
.val-edit-btn{width:20px;height:20px;border-radius:7px;border:0;background:#F1F5F9;color:#0A2F6B;display:flex;align-items:center;justify-content:center;cursor:pointer;touch-action:manipulation;flex-shrink:0;padding:0}
.j-actions{flex-shrink:0}
.j-round-btn{width:76px;height:76px;border-radius:50%;background:#fff;border:0;display:flex;align-items:center;justify-content:center;cursor:pointer;touch-action:manipulation;box-shadow:0 6px 16px rgba(0,0,0,.18)}
.j-round-btn svg{width:61px;height:61px}
.j-round-btn.play svg{color:#22C55E}
.j-round-btn.stop svg{color:#EF4444}
.j-btn{height:52px;border:0;border-radius:16px;font-size:14px;font-weight:780;display:flex;align-items:center;justify-content:center;gap:7px;cursor:pointer;touch-action:manipulation;width:100%;position:relative;z-index:2}
.j-btn svg{width:17px;height:17px}
.j-btn.solid-light{background:#fff;color:var(--green2)}
.j-btn.solid-light.danger{color:var(--red)}
.j-btn.ghost{background:rgba(15,23,42,.22);border:1px solid rgba(255,255,255,.25);color:#fff}
.j-btn.go{background:#fff;color:var(--green2);width:auto;display:inline-flex;padding:0 32px;font-size:15px}

/* ── Total do dia (redesenho 15/08/2026) ── */
.td-card{background:#0D3A7D;border-radius:22px;overflow:hidden;box-shadow:0 4px 18px rgba(15,23,42,.08)}
.td-row{position:relative;overflow:hidden;padding:15px 16px;border-top:1px solid rgba(255,255,255,.14)}
.td-row:first-child{border-top:0}
.td-wm{position:absolute;color:#fff;opacity:.09;pointer-events:none;z-index:0;top:-18%;height:136%;aspect-ratio:1/1}
.td-wm.left{left:-36px}
.td-wm.right{right:-40px}
.td-row>*:not(.td-wm){position:relative;z-index:1}
.td-corridas{display:flex;align-items:baseline;gap:9px}
.td-corridas strong{font-size:32px;font-weight:800;color:#fff;letter-spacing:-.03em;line-height:1}
.td-corridas span{font-size:12px;font-weight:800;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.04em}
.td-sub{display:flex;align-items:center;gap:8px;margin-top:8px}
.td-chip{display:inline-flex;align-items:center;gap:5px;font-size:11.5px;font-weight:800}
.td-chip.ok{color:#4ADE80}
.td-chip.no{color:#F87171}
.td-vline{width:1px;height:12px;background:rgba(255,255,255,.2)}
.td-grid1{text-align:center}
.td-grid1 strong{display:block;font-size:22px;font-weight:800;color:#fff;letter-spacing:-.02em}
.td-grid1 span{display:block;font-size:9.5px;font-weight:800;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.04em;margin-top:5px}
.tk-split{display:flex;align-items:stretch}
.tk-half{flex:1;position:relative;overflow:hidden;min-width:0;text-align:center;padding:2px 0}
.tk-half.left{padding-left:8px}
.tk-half.right{padding-right:8px}
.tk-divider{width:1px;background:rgba(255,255,255,.18);align-self:stretch;margin:0 4px}
.tk-half strong{display:block;font-size:22px;font-weight:800;color:#fff;letter-spacing:-.02em;line-height:1;position:relative;z-index:1}
.tk-half span{display:block;font-size:9.5px;font-weight:800;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.04em;margin-top:6px;position:relative;z-index:1}
.tk-half .td-wm{top:50%;transform:translateY(-50%);height:150px;width:150px}
.td-grid3{display:grid;grid-template-columns:repeat(3,1fr);padding-right:56px}
.td-grid3 .cell{text-align:center}
.td-grid3 .cell+.cell{border-left:1px solid rgba(255,255,255,.16)}
.td-grid3 strong{display:block;font-size:18px;font-weight:800;color:#fff;letter-spacing:-.02em}
.td-grid3 span{display:block;font-size:8.5px;font-weight:800;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.03em;margin-top:5px}
.td-rates{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:11px}
.td-rcard{border-radius:12px;padding:8px 6px;text-align:center;border:1.5px solid rgba(34,197,94,.4);background:rgba(34,197,94,.10)}
.td-rcard.o{border-color:rgba(245,158,11,.4);background:rgba(245,158,11,.10)}
.td-rcard.r{border-color:rgba(239,68,68,.4);background:rgba(239,68,68,.10)}
.td-rcard strong{display:block;font-size:16px;font-weight:800;color:#fff}
.td-rcard span{display:block;font-size:8.5px;font-weight:800;color:rgba(255,255,255,.85);text-transform:uppercase;letter-spacing:.04em;margin-top:2px}
.td-fin-divider{border-top:1px dashed rgba(255,255,255,.22);margin:0 0 11px}
.td-fin{display:grid;grid-template-columns:repeat(3,1fr);gap:6px}
.td-fin .cell{text-align:center}
.td-fin strong{display:block;font-size:15px;font-weight:800;letter-spacing:-.02em}
.td-fin strong.g{color:#4ADE80}
.td-fin strong.r{color:#F87171}
.td-fin span{display:block;font-size:8.5px;font-weight:800;color:rgba(255,255,255,.6);text-transform:uppercase;letter-spacing:.03em;margin-top:4px}

/* ── Metas do dia ── */
.gbar{height:10px;background:#eef2f7;border-radius:999px;overflow:hidden}
.gbar i{display:block;height:100%;border-radius:999px}
.gbar.money i{background:linear-gradient(90deg,#8b5cf6,#0ea5e9)}
.gbar.km i{background:linear-gradient(90deg,#22C55E,#0EA5E9)}
.gbar.time i{background:linear-gradient(90deg,#F59E0B,#EF4444)}
.goal-item{display:flex;align-items:center;gap:10px;padding:10px 0}
.goal-item:first-child{padding-top:0}
.goal-item:last-child{padding-bottom:0}
.obj-goal-item{border-bottom:1px solid #edf1f6;padding-bottom:13px;margin-bottom:13px}
.obj-goal-item:last-child{border-bottom:0;padding-bottom:0;margin-bottom:0}
.g-ico{width:28px;height:28px;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.g-ico svg{width:20px;height:20px}
.g-ico.money{color:#8b5cf6}
.g-ico.km{color:#0EA5E9}
.g-ico.time{color:#F59E0B}
.goal-body{flex:1;min-width:0}
.metas-wrap{position:relative;overflow:hidden;background:var(--card);border-radius:20px;padding:16px;box-shadow:0 4px 18px rgba(15,23,42,.07)}
.metas-wm{position:absolute;width:230px;height:230px;top:50%;left:-86px;transform:translateY(-50%);color:#3B82F6;opacity:.12;pointer-events:none;z-index:0}
.metas-wrap #metas-dia-stack{position:relative;z-index:1;padding-left:58px}
.goal-top{display:flex;justify-content:space-between;align-items:center;font-size:12.5px;font-weight:650;color:#344054;margin-bottom:8px}
.goal-top span:last-child{color:var(--muted);font-weight:600}
.goal-locked{display:flex;align-items:center;justify-content:space-between;background:#F8FAFC;border:1px dashed #D8E0EA;border-radius:14px;padding:10px 12px;cursor:pointer;touch-action:manipulation;flex:1;min-width:0}
.goal-locked span{font-size:12px;color:var(--muted);font-weight:600}
.goal-edit-link{display:block;width:100%;border:0;background:transparent;color:var(--muted);font-size:11.5px;font-weight:700;text-align:center;margin-top:12px;padding-top:12px;border-top:1px solid #edf1f6;cursor:pointer;touch-action:manipulation}
.goal-locked b{font-size:12px;color:#0A2F6B;font-weight:800}

/* ── Listas (sessões / corridas / lançamentos) ── */
.li-row{display:flex;gap:12px;align-items:center;background:#fff;border:1px solid var(--line);border-radius:20px;padding:12px;box-shadow:0 3px 14px rgba(15,23,42,.06)}
.li-row+.li-row{margin-top:9px}
.li-ico{width:38px;height:38px;border-radius:14px;display:flex;align-items:center;justify-content:center;flex:0 0 38px;overflow:hidden}
.li-ico svg{width:18px;height:18px;stroke-width:2;fill:none;stroke:currentColor}
.li-ico.green{background:#ECFDF3;color:var(--green2)}.li-ico.red{background:#FEF2F2;color:var(--red)}
.li-ico.plat-uber{background:#000}.li-ico.plat-uber img{width:19px}
.li-ico.plat-99{background:#F1F5F9}.li-ico.plat-99 img{width:26px;height:26px}
.li-ico.plat-generic{background:#F1F5F9;color:#64748B}
.li-info{flex:1;min-width:0}.li-info h3{font-size:13.5px;font-weight:760}.li-info p{font-size:11.5px;color:var(--muted);margin-top:2px}
.trip-title{font-size:12.5px!important;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.li-val{text-align:right;font-size:11px;color:var(--muted);font-weight:600;flex:0 0 auto}.li-val strong{display:block;color:var(--text);font-size:13.5px;font-weight:760;font-family:'Inter',system-ui,sans-serif}.li-time{display:block;color:var(--muted2);font-size:10.5px;font-weight:600;margin-top:2px}
.li-liq{display:block;color:#16A34A;font-size:11px;font-weight:800;margin-top:2px}
.li-extra{display:flex;gap:6px;margin-top:5px}.chip{display:inline-flex;align-items:center;gap:3px;font-size:10.5px;font-weight:800;padding:2.5px 7px;border-radius:8px;font-family:'Inter',system-ui,sans-serif;letter-spacing:-.01em}.chip svg{flex-shrink:0}.chip-d{background:#FFF7ED;color:#F59E0B}.chip-g{background:#F5F3FF;color:#7C3AED}.chip-taxa{background:#FEF9C3;color:#854D0E;display:inline-flex;align-items:center;font-size:10.5px;font-weight:800;padding:2.5px 7px;border-radius:8px}.chip-toll{background:#F1F5F9;color:#475569;display:inline-flex;align-items:center;justify-content:center;width:22px;height:20px;border-radius:7px;flex-shrink:0}.chip-icon{display:inline-flex;align-items:center;justify-content:center;width:22px;height:20px;border-radius:7px;flex-shrink:0}.chip-cash{background:#DCFCE7;color:#15803D}.chip-auto{background:#EFF6FF;color:#0EA5E9}.chip-import{background:#F5F3FF;color:#7C3AED}.auto-status{display:inline-block;font-size:9.5px;font-weight:800;padding:2.5px 7px;border-radius:99px;margin-top:4px;white-space:nowrap}.auto-status.pend{background:#FEF3C7;color:#92400E}.auto-status.estim{background:#EFF6FF;color:#0369A1}.auto-status.conf{background:#DCFCE7;color:#15803D}.auto-status.susp{background:#FEE2E2;color:#B91C1C}.trip-item-wrap{margin-bottom:9px}
.li-ico-col{display:flex;flex-direction:column;align-items:center;gap:4px;flex:0 0 38px}
.li-ico-time{font-size:10.5px;font-weight:800;color:var(--blue);white-space:nowrap;line-height:1.3}
/* corrida automática: card único (li-row de sempre), ícone+horário à
   esquerda, e título/km-tempo/tags empilhados juntos numa coluna central —
   pedido do Yuri (16/08/2026, mockup "Opção 1" aprovado): antes o card
   tinha 3 recuos diferentes (cabeçalho, categoria com margin-left:62px, e
   painel de detalhe sem recuo), o que fazia o card parecer "encolher" de
   largura ao abrir o detalhe. Agora é tudo uma coluna só, mesmo recuo. */
.li-row.trip-top{align-items:flex-start}
.trip-content-col{flex:1;min-width:0}
.trip-title-line{display:flex;align-items:flex-start;gap:6px}
.trip-subline{font-size:11px;color:var(--muted);margin:4px 0 0;font-weight:600}
.trip-top .li-extra{margin-top:5px}
.link-tag{width:15px;height:15px;flex-shrink:0;display:inline-flex;cursor:pointer}
.link-tag.on{color:#16A34A}
.link-tag.off{color:#CBD5E1}
.tap-legend-wrap{position:relative;display:inline-flex;cursor:pointer}
.tap-legend-tip{position:absolute;bottom:calc(100% + 7px);left:50%;transform:translateX(-50%);background:#0F172A;color:#fff;font-size:10.5px;font-weight:700;padding:6px 10px;border-radius:9px;white-space:nowrap;box-shadow:0 6px 16px rgba(0,0,0,.25);z-index:5;pointer-events:none}
.tap-legend-tip::after{content:'';position:absolute;top:100%;left:50%;transform:translateX(-50%);border:5px solid transparent;border-top-color:#0F172A}
.cmp{cursor:pointer}
.cmp.g{color:#16A34A}
.cmp.r{color:#DC2626}
.cmp-legend{background:#F8FAFC;border:1px solid var(--line);border-radius:12px;padding:9px 11px;margin:2px 0 8px;font-size:11px;color:#64748B;lin
===== total do dia @ 404477 =====
lters">
    <div class="feed-chip on" data-type="" onclick="feedSetFilter('',this)">Tudo</div>
    <div class="feed-chip" data-type="blitz" onclick="feedSetFilter('blitz',this)">Blitz</div>
    <div class="feed-chip" data-type="risco" onclick="feedSetFilter('risco',this)">Risco</div>
    <div class="feed-chip" data-type="transito" onclick="feedSetFilter('transito',this)">Trânsito</div>
    <div class="feed-chip" data-type="demanda" onclick="feedSetFilter('demanda',this)">Demanda</div>
    <div class="feed-chip" data-type="dinamico" onclick="feedSetFilter('dinamico',this)">Dinâmico</div>
    <div class="feed-chip" data-type="fila" onclick="feedSetFilter('fila',this)">Fila</div>
  </div>
  <div class="feed-list" id="feed-list"><div class="feed-empty">Carregando alertas…</div></div>
  <div class="feed-fab-wrap">
    <button class="feed-fab" onclick="feedOpenComposer()" aria-label="Publicar alerta">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4z"/></svg>
    </button>
  </div>
</div>

<!-- ══ TELA JORNADA ══════════════════════════════════════════════════════ -->
<div class="screen" id="sc-jornada">
  <div id="jorn-custo-banner" class="cv-banner" style="display:none;margin:8px 16px 0" onclick="openCustoVeiculo()">
    <div class="txt"><b>Veja seu lucro líquido real</b>Configure o custo do seu veículo em 2 min</div>
    <button class="x" onclick="event.stopPropagation();dismissCustoBanner()">&#x2715;</button>
  </div>
  <div style="padding:4px 16px 0">
    <div id="net-status-badge" style="display:none;align-items:center;gap:5px;justify-content:center;padding:4px 10px;border-radius:99px;font-size:10.5px;font-weight:800;margin:0 auto 6px;width:fit-content"><span id="net-status-dot" style="width:6px;height:6px;border-radius:50%"></span><span id="net-status-text"></span></div>
    <div class="daybar">
      <button class="arrow" onclick="chgDay(-1)">&#8249;</button>
      <div class="datebox" onclick="document.getElementById('jorn-date-input').showPicker ? document.getElementById('jorn-date-input').showPicker() : document.getElementById('jorn-date-input').click()">
        <div style="display:flex;align-items:center;justify-content:center;gap:5px">
          <h1 id="dn-date">Hoje</h1>
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="#94A3B8" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
        </div>
        <p id="dn-dow"></p>
      </div>
      <button class="arrow" onclick="chgDay(1)">&#8250;</button>
      <input type="date" id="jorn-date-input" onchange="setJornadaDate(this.value)" style="position:absolute;opacity:0;pointer-events:none;width:1px;height:1px">
    </div>
  </div>

  <div id="pending-confirm-banner" class="pending-confirm-banner" style="display:none" onclick="openImportPrecheck()">
    <div class="pcb-ico">
      <svg viewBox="0 0 24 24" width="19" height="19" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M23 7l-7 5 7 5V7z"/><rect x="1" y="5" width="15" height="14" rx="2"/></svg>
    </div>
    <div class="pcb-txt">
      <div class="pcb-title" id="pcb-title">corridas aguardando confirmação</div>
      <div class="pcb-sub">Toque para enviar o vídeo do histórico</div>
    </div>
    <svg class="pcb-chev" viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18l6-6-6-6"/></svg>
  </div>

  <div class="journey-wrap" id="journey-wrap">
  <div class="journey idle" id="journey-card">
    <div class="j-status" style="display:none"><i></i><span id="jc-status-text">Iniciar</span></div>
    <div class="j-toprow">
      <div class="j-current" style="grid-template-columns:1.3fr 1fr 1fr;flex:1">
        <div><h2 id="jc-timer">00:00:00</h2><p id="jc-sub">Nenhuma sessão iniciada</p></div>
        <div class="j-km"><strong id="jc-km">0,0</strong><span>Km</span></div>
        <div class="j-km"><strong id="jc-trips2">0</strong><span>Corridas</span></div>
      </div>
    </div>
    <div class="j-bottomrow">
      <div class="j-rates2">
        <div class="rate-cell" id="jc-rate-hora2"><strong id="jc-rsh2">R$0,00</strong><span>R$ / hora</span></div>
        <div class="rate-cell" id="jc-rate-km2"><strong id="jc-rskm2">R$0,00</strong><span>R$ / km</span></div>
      </div>
      <div class="j-actions" id="jc-actions"></div>
    </div>
  </div>
  <div class="journey-breakdown" id="jc-breakdown" style="display:none">
    <div class="sbar-wrap" onclick="toggleBar('jc-blist')"><div class="sbar" id="jc-bar"></div></div>
    <div class="sbar-list" id="jc-blist"></div>
  </div>
  </div>

  <div class="section" style="padding:16px 16px 8px"><h2>Total do dia</h2></div>
  <div style="padding:0 16px 14px">
    <div class="td-card">

      <div class="td-row">
        <svg class="td-wm right" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"><path d="M3 13l1.5-4.5A2 2 0 0 1 6.4 7h11.2a2 2 0 0 1 1.9 1.5L21 13"/><path d="M3 13h18v4a1 1 0 0 1-1 1h-1a1 1 0 0 1-1-1v-1H6v1a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1v-4z"/><circle cx="7.5" cy="17" r="1.3"/><circle cx="16.5" cy="17" r="1.3"/></svg>
        <div class="td-corridas"><strong id="jc-trips">0</strong><span>Corridas</span></div>
        <div class="td-sub" id="jc-corridas-sub" style="display:none">
          <span class="td-chip ok"><svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span id="jc-aceitas">0</span> aceitas</span>
          <span class="td-vline"></span>
          <span class="td-chip no"><svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg><span id="jc-recusadas">0</span> recusadas</span>
        </div>
      </div>

      <div class="td-row">
        <div class="tk-split">
          <div class="tk-half left">
            <svg class="td-wm" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9.5"/><path d="M12 6.5v5.5l4 2.3"/></svg>
            <div style="display:flex;align-items:center;justify-content:center;gap:6px;position:relative;z-index:1">
              <strong id="jc-total">0h00</strong>
              <button class="km-edit-btn" onclick="openTempoAdjust()" aria-label="Editar tempo total">
                <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M17 3a2.85 2.85 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"/></svg>
              </button>
            </div>
            <span>Tempo total</span>
          </div>
          <div class="tk-divider"></div>
          <div class="tk-half right">
            <svg class="td-wm" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round"><path d="M4 23L9 1M20 23L15 1"/><path d="M12 3.5v3M12 10v3M12 16.5v3"/></svg>
            <div style="display:flex;align-items:center;justify-content:center;gap:5px;position:relative;z-index:1">
              <strong id="jc-kmtotal">0,0 km</strong>
              <button class="km-edit-btn" onclick="openKmAdjust()" aria-label="Editar km total">
                <svg viewBox="0 0 24 24" width="10" height="10" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M17 3a2.85 2.85 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"/></svg>
              </button>
            </div>
            <span>Km total</span>
          </div>
        </div>
      </div>

      <div class="td-row">
        <svg class="td-wm left" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="0.6" stroke-linecap="round" stroke-linejoin="round"><path d="M12 1v22"/><path d="M17 5.5c0-2-2.2-3.3-5-3.3s-5 1.5-5 3.7c0 2.5 2.2 3.5 5 4.3c3.2.9 5.4 1.9 5.4 4.5c0 2.5-2.4 3.8-5.4 3.8s-5.4-1.4-5.4-3.8"/></svg>
        <div class="td-rates">
          <div class="td-rcard" id="jc-rate-hora"><strong id="jc-rsh">R$0,00</strong><span>R$ / hora</span></div>
          <div class="td-rcard" id="jc-rate-km"><strong id="jc-rskm">R$0,00</strong><span>R$ / km</span></div>
        </div>
        <div class="td-fin-divider"></div>
        <div class="td-fin">
          <div class="cell"><strong class="g" id="d-rev">R$0</strong><span>Receitas</span></div>
          <div class="cell"><strong class="r" id="d-exp">R$0</strong><span>Despesas</span></div>
          <div class="cell"><strong id="d-net">R$0</strong><span>Resultado</span></div>
        </div>
      </div>

      <div class="td-row bd" id="td-bd-row" style="display:none">
        <div class="sbar" id="td-bd-bar"></div>
        <div class="sbar-list open" id="td-bd-list"></div>
      </div>

    </div>
  </div>

  <!-- Metas do dia: pedido do Yuri (23/07/2026) — sai de dentro do card
       azul da jornada (onde competia por espaço com timer/km/corridas) e
       vira seção solta, igual "Lançamentos" logo abaixo (título + link no
       fundo da tela, sem balão/card branco por trás). renderMetasDoDia()
       não muda nada — só o container onde a mesma função escreve. -->
  <div class="section" style="padding:0 16px 8px"><h2>Metas do dia</h2><button class="add" onclick="openMetasHojeModal()">Editar &#8250;</button></div>
  <div style="padding:0 16px 4px">
    <div class="metas-wrap">
      <svg class="metas-wm" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1"><circle cx="12" cy="12" r="9.5"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2.2"/></svg>
      <div id="metas-dia-stack"></div>
    </div>
  </div>

  <div class="section" style="padding:16px 16px 8px"><h2>Jornadas do dia</h2><button class="add" onclick="toggleSessionsView()" id="sessions-toggle" style="display:none">Ver todas &#8250;</button></div>
  <div style="padding:0 16px"><div id="sessions-list"><div class="empty">Nenhuma sessão registrada</div></div></div>

  <div class="section" style="padding:16px 16px 8px"><h2>Lançamentos</h2><div style="display:flex;gap:8px;align-items:center"><button class="add" onclick="toggleLancView()" id="lanc-toggle" style="display:none">Ver todas &#8250;</button><button class="add" onclick="openLanc('receita')">+ Adicionar</button></div></div>
  <div style="padding:0 16px"><div id="jorn-lanc-list"><div class="empty">Nenhum lançamento registrado</div></div></div>

  <div style="height:8px"></div>
</div>

<!-- ══ TELA FINANCEIRO ════════════════════════════════════════════════════ -->
<div class="screen" id="sc-financeiro">
  <div class="wrap">
    <section class="hero" style="margin-bottom:14px">
      <div class="fin-hero-top">
        <div class="fin-ptabs">
          <button id="fp-today" onclick="setFinPer('today')">Hoje</button>
          <button id="fp-month" class="on" onclick="setFinPer('month')">Mês</button>
          <button id="fp-year" onclick="setFinPer('year')">Ano</button>
        </div>
        <button class="fin-filter-btn" id="fin-filter-btn" onclick="openFinFilter()"><svg viewBox="0 0 24 24" width="17" height="17" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M3 6h18"/><path d="M7 12h10"/><path d="M10 18h4"/></svg></button>
      </div>
      <div class="heroTop">
        <div><div class="label">Saldo líquido</div><div class="amount" id="fin-amount">R$ 0,00</div><div class="sub" id="fin-sub">Entradas R$ 0 · Saídas R$ 0</div></div>
      </div>
      <div class="fin-range-chip" id="fin-range-chip" style="display:none">
        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.3"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
        <span id="fin-range-txt"></span>
        <button onclick="event.stopPropagation();clearFinFilter()">&#x2715;</button>
      </div>
      <div class="heroGrid">
        <div class="heroCell"><strong id="fin-pct-lucro">0%</strong><span>% lucro</span></div>
        <div class="heroCell"><strong id="fin-media">R$ 0</strong><span>média dia</span></div>
        <div class="heroCell"><strong id="fin-vs-ant">—</strong><span id="fin-vs-ant-lbl">vs mês ant.</span></div>
      </div>
    </section>
    <section class="card cardPad" style="margin-bottom:14px">
      <div class="cardHead"><h2>Resumo</h2><div class="seg"><button class="on" id="fs-all" onclick="setFinScope('all')">Tudo</button><button id="fs-prof" onclick="setFinScope('prof')">Prof.</button><button id="fs-pes" onclick="setFinScope('pes')">Pessoal</button></div></div>
      <div class="fin-metrics">
        <div class="fin-metric g"><span>Corridas</span><strong id="fin-corr">R$ 0,00</strong><small id="fin-corr-sub">&nbsp;</small></div>
        <div class="fin-metric g"><span>Receitas extras</span><strong id="fin-rec">R$ 0,00</strong><small>gorjetas, b&#244;nus...</small></div>
        <div class="fin-metric r"><span>Despesas</span><strong id="fin-desp">R$ 0,00</strong><small id="fin-desp-sub">&nbsp;</small></div>
        <div class="fin-metric"><span>Lucro</span><strong id="fin-lucro">R$ 0,00</strong><small id="fin-lucro-sub">&nbsp;</small></div>
      </div>
    </section>
    <section class="card cardPad" style="margin-bottom:14px">
      <div class="cardHead"><h2>Objetivos</h2></div>
      <div class="obj-switch">
        <button class="obj-btn active" id="ob-rec" onclick="setObjTab('rec')">Receitas</button>
        <button class="obj-btn" id="ob-desp" onclick="setObjTab('desp')">Despesas</button>
      </div>
      <div id="obj-rec-list"></div>
      <div id="obj-desp-list" style="display:none"></div>
      <div class="cfg-link" onclick="openObjConfig()">Configurar objetivos</div>
    </section>
    <section class="card cardPad" style="margin-bottom:14px">
      <div class="cardHead"><h2>Fluxo</h2></div>
      <div class="fin-flow" id="fin-flow"></div>
      <div class="fin-flow-legend">
        <span><i style="background:#22C55E"></i>Entradas (com corridas)</span>
        <span><i style="background:#FCA5A5"></i>Sa&#237;das</span>
      </div>
    </section>
    <section class="card cardPad" style="margin-bottom:14px">
      <div class="cardHead"><h2>Ações rápidas</h2></div>
      <div class="fin-actions">
        <button class="fin-action" onclick="openLanc('receita')"><div class="ico green"><svg viewBox="0 0 24 24" 
===== total do dia @ 638164 =====
 — mesmo padrão de cache do autoTrips acima. Lida aqui
  // (S.declined) e usada dentro de renderJourneyCard(), que roda logo a
  // seguir e populate o card "Total do dia".
  if (S.declinedDate !== d) {
    S.declinedDate = d;
    S.declined = [];
    loadDeclinedOffersForDay(d).then(list => {
      if (S.declinedDate !== d) return;
      S.declined = list;
      renderJornada();
    });
  }
  const rev = day.trips.reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0)
            + day.autoTrips.reduce((s,t)=>s+(t.offer_value||0),0);
  const exp = day.expenses.reduce((s,e)=>s+(e.amount||0),0);
  setText('d-rev', DB.fRI(rev));
  setText('d-exp', DB.fRI(exp));
  const netEl = document.getElementById('d-net');
  if (netEl) { netEl.textContent = DB.fRI(rev-exp); netEl.className = 'v ' + (rev-exp>=0 ? 'green' : 'red'); }

  renderJourneyCard(day, isToday);
  renderMetasDoDia(day);

  // Sessões do dia (3 mais recentes, ou todas — sessão atual sempre aparece)
  const sessEl = document.getElementById('sessions-list');
  const sessToggleBtn = document.getElementById('sessions-toggle');
  if (sessEl) {
    const total = (day.sessions||[]).length;
    // Numera pela ORDEM DE HORÁRIO real (start), não pela ordem em que foi
    // salva/sincronizada no aparelho — evita "Jornada 2" mostrar um horário
    // mais tarde que "Jornada 3". origIdx continua apontando pro índice
    // verdadeiro em day.sessions, então editar/apagar continua certo.
    const byTime = (day.sessions||[]).map((s,i)=>i)
      .sort((a,b) => (day.sessions[a].start||0) - (day.sessions[b].start||0));
    const finishedAll = byTime.slice().reverse()
      .map((origIdx,ri)=>sessionItem('Jornada '+(total-ri), day.sessions[origIdx], origIdx, S.curDate, day, false));
    const finishedShown = S.sessionsExpanded ? finishedAll : finishedAll.slice(0,3);
    const liveRow = (isToday && S.gps.st==='running') ? [sessionItem('Jornada atual', {start:S.gps.start,end:null,km:S.gps.km,elapsed:getElapsed()}, -1, S.curDate, day, true)] : [];
    const rows = [...liveRow, ...finishedShown];
    sessEl.innerHTML = rows.length ? rows.join('') : '<div class="empty">Nenhuma sessão registrada</div>';
  }
  if (sessToggleBtn) {
    sessToggleBtn.style.display = (day.sessions||[]).length > 3 ? '' : 'none';
    sessToggleBtn.innerHTML = S.sessionsExpanded ? 'Ver menos' : 'Ver todas &#8250;';
  }
  // Corridas: removido daqui (23/07/2026, pedido do Yuri) — já ficam
  // acessíveis dentro do detalhe de cada jornada (seta expande e lista via
  // sessionItem, que já mescla Importadas + Automáticas). Lista de
  // Importadas ainda mora na tela Corridas separada (navTo('corridas')).
  // Lançamentos (5 mais recentes, ou todos)
  const lEl = document.getElementById('jorn-lanc-list');
  const lancToggleBtn = document.getElementById('lanc-toggle');
  if (lEl) {
    const today = DB.today();
    const allItems = [
      ...day.revenues.map(r=>Object.assign({},r,{_type:'receita'})),
      ...day.expenses.map(e=>Object.assign({},e,{_type:'despesa'}))
    ];
    // Mostrar todos os lançamentos do dia sem filtro de tab
    const items = allItems;
    const show = S.lancExpanded ? items : items.slice(0,5);
    lEl.innerHTML = show.length ? show.map(it => lancItemJornada(it)).join('') : '<div class="empty">Nenhum lançamento registrado</div>';
    if (lancToggleBtn) {
      lancToggleBtn.style.display = items.length > 5 ? '' : 'none';
      lancToggleBtn.innerHTML = S.lancExpanded ? 'Ver menos' : 'Ver todas &#8250;';
    }
  }
  reapplyOpenTripDetail();
  reapplyOpenJss();
}

function renderJourneyCard(day, isToday) {
  const jc = document.getElementById('journey-card');
  if (!jc) return;
  let permWarn = document.getElementById('journey-perm-warning');
  if (!permWarn && jc.parentNode) {
    permWarn = document.createElement('div'); permWarn.id = 'journey-perm-warning'; permWarn.onclick = openFlashPermissionSetup;
    permWarn.style.cssText = 'display:none;align-items:flex-start;justify-content:space-between;gap:10px;background:#FFF7ED;border:1px solid #FDBA74;border-radius:14px;padding:11px 12px;margin:0 0 10px;cursor:pointer';
    permWarn.innerHTML = '<div style="min-width:0"><div style="font-size:12.5px;font-weight:800;color:#9A3412">⚠ Funções do MōB incompletas</div><div id="journey-perm-warning-text" style="font-size:11px;color:#C2410C;line-height:1.35;margin-top:2px"></div></div><span style="font-size:11px;font-weight:800;color:#9A3412;white-space:nowrap">Ativar ›</span>';
    jc.parentNode.insertBefore(permWarn, jc);
  }
  renderFlashPermissionWarnings();
  const cardDate = day?.date || S.curDate || DB.today();
  // Card de sessão atual (timer/km/botão) só faz sentido em "hoje" — em dia
  // passado não existe jornada "em andamento" possível, então esse card
  // some por completo. O restante da função continua rodando igual: os
  // valores que ela calcula (jc-trips, jc-total, jc-kmtotal, jc-kmativo,
  // jc-kmdesl, jc-rsh, jc-rskm) agora moram no card "Total do dia" — que é
  // um elemento IRMÃO de journey-card, não um filho, e precisa continuar
  // sendo populado em qualquer dia, passado ou presente.
  const jcWrap = document.getElementById('journey-wrap');
  if (jcWrap) jcWrap.style.display = isToday ? '' : 'none';
  const g = S.gps;
  const finished = day.sessions || [];
  const finishedElapsed = finished.reduce((s,x)=>s+(x.elapsed||0),0);
  const finishedKm = finished.reduce((s,x)=>s+(x.km||0),0);
  const live = isToday && g.st === 'running';
  const liveElapsed = live ? getElapsed() : 0;
  const liveKm = live ? g.km : 0;
  const totalElapsed = Math.max(0, finishedElapsed + liveElapsed + (day.tempoAdjust || 0));
  const totalKm = finishedKm + liveKm + (day.kmAdjust || 0);
  const state = isToday ? g.st : 'idle';

  let cls = 'idle';
  if (state === 'running') cls = 'running';
  else if (finished.length > 0) cls = 'ended';
  jc.className = 'journey ' + cls;

  setText('jc-status-text',
    state === 'running' ? 'Online' :
    finished.length > 0 ? 'Concluído' : 'Iniciar'
  );
  const th = Math.floor(liveElapsed/3600), tm = Math.floor(liveElapsed%3600/60), ts = liveElapsed%60;
  setText('jc-timer', p2(th)+':'+p2(tm)+':'+p2(ts));
  setText('jc-sub',
    state === 'running' ? 'Sessão atual · iniciada '+fmtHM(g.start) :
    finished.length > 0 ? finished.length+(finished.length===1?' jornada hoje':' jornadas hoje') :
    'Nenhuma sessão iniciada'
  );
  setText('jc-km', liveKm.toFixed(1));
  const tth = Math.floor(totalElapsed/3600), ttm = Math.floor(totalElapsed%3600/60);
  setText('jc-total', tth+'h'+p2(ttm)+'m');
  setText('jc-kmtotal', totalKm.toFixed(1)+' km');
  // CORRIGIDO (23/07/2026, confirmado com dado real pelo Yuri): esses 4
  // KPIs do card principal (corridas, km ativo, R$/h, R$/km) só somavam
  // day.trips (Importadas) — sessão só com automáticas mostrava tudo
  // zerado mesmo com corridas de verdade rodando. Mesma correção que já
  // tinha sido feita em rev/sessionItem/renderMetasDoDia, faltava aqui.
  const autoTripsHoje = day.autoTrips || [];

  // Banner "aguardando confirmação" — pedido do Yuri (16/08/2026), substitui
  // o botão fixo "Importar corridas". Conta corridas que ainda não foram
  // confirmadas por vídeo do histórico (status 'capturada' ou 'estimada') —
  // 'confirmada' só existe depois do vídeo, então isso mede exatamente o
  // que falta conferir naquele dia. Aparece em qualquer dia navegado, some
  // sozinho quando não sobra pendência.
  const pcbEl = document.getElementById('pending-confirm-banner');
  if (pcbEl) {
    // Não interpreta cache ainda carregando como zero corridas. Durante refresh,
    // mantém o último estado visual válido do mesmo dia.
    const autoTripsReady = S._autoTripsDataDate === cardDate;
    if (autoTripsReady) {
      const pendingCount = autoTripsHoje.filter(t => t.status !== 'confirmada' || t.value_needs_review === true).length;
      pcbEl.dataset.dataDate = cardDate;
      if (pendingCount > 0) {
        pcbEl.style.display = 'flex';
        setText('pcb-title', pendingCount + (pendingCount===1 ? ' corrida aguardando confirmação' : ' corridas aguardando confirmação'));
      } else {
        pcbEl.style.display = 'none';
      }
    } else if (pcbEl.dataset.dataDate && pcbEl.dataset.dataDate !== cardDate) {
      pcbEl.style.display = 'none';
      pcbEl.dataset.dataDate = cardDate;
    }
  }

  // Barra Online/Buscando passageiro/Em corrida — real, vinda da máquina de
  // estados do APK (ver computeStatusBreakdown). No card da jornada ATUAL
  // só faz sentido enquanto a sessão está rodando (é o que está sendo
  // contado agora); ao encerrar, o dado sai daqui e já está consolidado no
  // "Total do dia" logo abaixo — pedido do Yuri (16/08/2026), evita
  // duplicar/deixar a mesma barra "grudada" depois de já ter encerrado.
  // As barras são atualizadas exclusivamente pela timeline contínua V3.
  // A rotina base não sobrescreve mais a leitura real com auto_trips.
  const jcBd = document.getElementById('jc-breakdown');
  const tdBdRow = document.getElementById('td-bd-row');

  setText('jc-trips', day.trips.length + autoTripsHoje.length);
  setText('jc-trips2', day.trips.length + autoTripsHoje.length);
  const aceitasCount = day.trips.length + autoTripsHoje.length;
  const declinedCount = (S.declined || []).length;
  setText('jc-aceitas', aceitasCount);
  setText('jc-recusadas', declinedCount);
  const corridasSubEl = document.getElementById('jc-corridas-sub');
  if (corridasSubEl) corridasSubEl.style.display = (aceitasCount>0 || declinedCount>0) ? 'flex' : 'none';
  const kmAtivo = day.trips.reduce((s,t)=>s+(t.km||0),0)
                + autoTripsHoje.reduce((s,t)=>s+(parseFloat(t.real_km_trip ?? t.offer_km_trip)||0),0);
  const kmDesl = Math.max(0, totalKm - kmAtivo);
  setText('jc-kmativo', kmAtivo.toFixed(1));
  setText('jc-kmdesl', kmDesl.toFixed(1));
  const ganhosDia = day.trips.reduce((s,t)=>s+(t.value||0),0)
                  + autoTripsHoje.reduce((s,t)=>s+(t.offer_value||0),0);
  const horasDecimal = totalElapsed > 0 ? totalElapsed/3600 : 0;
  const rsh  = horasDecimal > 0 ? ganhosDia/horasDecimal : null;
  const rskm = totalKm > 0 ? ganhosDia/totalKm : null;
  setText('jc-rsh', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  setText('jc-rsh2', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm2', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  // Cor por indicador — pedido do Yuri (23/07/2026, mockup aprovado):
  // mesmos limiares vermelho/verde que o motorista já ajusta na régua do
  // MōB Flash (Indicadores → R$/km e R$/hora), cada indicador troca de cor
  // por conta própria. Sem dado ainda (sessão não rodou tempo/km) fica
  // neutro (verde), não faz sentido alarmar antes de ter dado de verdade.
  const rateEl = document.getElementById('jc-rate-hora');
  const kmEl   = document.getElementById('jc-rate-km');
  const rateEl2 = document.getElementById('jc-rate-hora2');
  const kmEl2   = document.getElementById('jc-rate-km2');
  const rateCls = rsh !== null ? rateColorClass('rhora', rsh) : '';
  const kmCls   = rskm !== null ? rateColorClass('rkm', rskm) : '';
  if (rateEl)  rateEl.className  = 'td-rcard ' + rateCls;
  if (kmEl)    kmEl.className    = 'td-rcard ' + kmCls;
  if (rateEl2) rateEl2.className = 'rate-cell ' + rateCls;
  if (kmEl2)   kmEl2.className   = 'rate-cell ' + kmCls;

  const actions = document.getElementById('jc-actions');
  if (!actions) return;
  if (!isToday) {
    actions.innerHTML = '';
  } else if (state === 'idle') {
    actions.innerHTML = `<button class="j-round-btn play" onclick="gpsStart()" aria-label="Iniciar jornada"><svg viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg></button>`;
  } else if (state === 'running') {
    actions.innerHTML = '<button class="j-round-btn stop" onclick="confirmStop()" aria-label="Encerrar jornada"><svg viewBox="0 0 24 24" fill="currentColor"><rect x="6" y="6" width="12" height="12"/></svg></button>';
  }
}

function agMinutesBetween(start, end) {
  if (!start || !end) return 0;
  const [h1,m1] = start.split(':').map(Number);
  const [h2,m2] = end.split(':').map(Number);
  return Math.max(0, (h2*60+m2) - (h1*60+m1));
}
function openMetasHojeModal() {
  const date = S.curDate || DB.today();
  const day = DB.day(date);
  const dow = new Date(date+'T12:00').getDay();
  const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
  const meta = DB.meta();
  const ov = day.metaOverride || null;
  const dimMes = new Date(new Date(date+'T12:00').getFullYear(), new Date(date+'T12:00').getMonth()+1, 0).getDate();
  const moneyGoal = (ov && ov.money) ? ov.money : ((plan && plan.moneyGoal) ? plan.moneyGoal : (meta.general ? meta.general/dimMes : 0));
  const kmGoal = (ov && ov.km) ? ov.km : (plan && plan.kmGoal ? plan.kmGoal : 0);
  const schedMin = (ov && ov.hours) ? ov.hours*60 : (plan ? (plan.schedules||[]).reduce((s,b)=>s+agMinutesBetween(b.start,b.end),0) : 0);
  const mEl = document.getElementById('mh-money'); if (mEl) mEl.value = moneyGoal ? moneyGoal.toFixed(2) : '';
  const kEl = document.getElementById('mh-km'); if (kEl) kEl.value = kmGoal || '';
  const hEl = document.getElementById('mh-hours'); if (hEl) hEl.value = schedMin ? (schedMin/60).toFixed(1) : '';
  const subEl = document.getElementById('mh-sub');
  if (subEl) {
    const isToday = date === DB.today();
    subEl.textContent = isToday ? 'Vale só para hoje' : ('Vale só para ' + new Date(date+'T12:00').toLocaleDateString('pt-BR',{weekday:'long',day:'numeric',month:'long'}));
  }
  openMo('mo-metas-hoje');
}
function saveMetasHoje() {
  const date = S.curDate || DB.today();
  const day = DB.day(date);
  const money = parseFloat((document.getElementById('mh-money')||{}).value) || 0;
  const km = parseFloat((document.getElementById('mh-km')||{}).value) || 0;
  const hours = parseFloat((document.getElementById('mh-hours')||{}).value) || 0;
  day.metaOverride = { money, km, hours };
  DB.saveDay(day);
  closeMo('mo-metas-hoje');
  try { renderMetasDoDia(day); } catch(e) {}
  showToast('Metas de hoje atualizadas!', 'success');
}
function renderMetasDoDia(day) {
  const stack = document.getElementById('metas-dia-stack');
  if (!stack) return;
  const rev = day.trips.reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0)
            + (day.autoTrips||[]).reduce((s,t)=>s+(t.offer_value||0),0);
  const isToday = day.date === DB.today();
  const liveOn = isToday && S.gps.st==='running';
  const totalKm = (day.sessions||[]).reduce((s,x)=>s+(x.km||0),0) + (liveOn ? S.gps.km : 0) + (day.kmAdjust || 0);
  const totalElapsed = Math.max(0, (day.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0) + (liveOn ? getElapsed() : 0) + (day.tempoAdjust || 0));
  const dow = new Date(day.date+'T12:00').getDay();
  const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
  const meta = DB.meta();
  const ov = day.metaOverride || null; // ajuste e
===== total do dia @ 641851 =====
!jc) return;
  let permWarn = document.getElementById('journey-perm-warning');
  if (!permWarn && jc.parentNode) {
    permWarn = document.createElement('div'); permWarn.id = 'journey-perm-warning'; permWarn.onclick = openFlashPermissionSetup;
    permWarn.style.cssText = 'display:none;align-items:flex-start;justify-content:space-between;gap:10px;background:#FFF7ED;border:1px solid #FDBA74;border-radius:14px;padding:11px 12px;margin:0 0 10px;cursor:pointer';
    permWarn.innerHTML = '<div style="min-width:0"><div style="font-size:12.5px;font-weight:800;color:#9A3412">⚠ Funções do MōB incompletas</div><div id="journey-perm-warning-text" style="font-size:11px;color:#C2410C;line-height:1.35;margin-top:2px"></div></div><span style="font-size:11px;font-weight:800;color:#9A3412;white-space:nowrap">Ativar ›</span>';
    jc.parentNode.insertBefore(permWarn, jc);
  }
  renderFlashPermissionWarnings();
  const cardDate = day?.date || S.curDate || DB.today();
  // Card de sessão atual (timer/km/botão) só faz sentido em "hoje" — em dia
  // passado não existe jornada "em andamento" possível, então esse card
  // some por completo. O restante da função continua rodando igual: os
  // valores que ela calcula (jc-trips, jc-total, jc-kmtotal, jc-kmativo,
  // jc-kmdesl, jc-rsh, jc-rskm) agora moram no card "Total do dia" — que é
  // um elemento IRMÃO de journey-card, não um filho, e precisa continuar
  // sendo populado em qualquer dia, passado ou presente.
  const jcWrap = document.getElementById('journey-wrap');
  if (jcWrap) jcWrap.style.display = isToday ? '' : 'none';
  const g = S.gps;
  const finished = day.sessions || [];
  const finishedElapsed = finished.reduce((s,x)=>s+(x.elapsed||0),0);
  const finishedKm = finished.reduce((s,x)=>s+(x.km||0),0);
  const live = isToday && g.st === 'running';
  const liveElapsed = live ? getElapsed() : 0;
  const liveKm = live ? g.km : 0;
  const totalElapsed = Math.max(0, finishedElapsed + liveElapsed + (day.tempoAdjust || 0));
  const totalKm = finishedKm + liveKm + (day.kmAdjust || 0);
  const state = isToday ? g.st : 'idle';

  let cls = 'idle';
  if (state === 'running') cls = 'running';
  else if (finished.length > 0) cls = 'ended';
  jc.className = 'journey ' + cls;

  setText('jc-status-text',
    state === 'running' ? 'Online' :
    finished.length > 0 ? 'Concluído' : 'Iniciar'
  );
  const th = Math.floor(liveElapsed/3600), tm = Math.floor(liveElapsed%3600/60), ts = liveElapsed%60;
  setText('jc-timer', p2(th)+':'+p2(tm)+':'+p2(ts));
  setText('jc-sub',
    state === 'running' ? 'Sessão atual · iniciada '+fmtHM(g.start) :
    finished.length > 0 ? finished.length+(finished.length===1?' jornada hoje':' jornadas hoje') :
    'Nenhuma sessão iniciada'
  );
  setText('jc-km', liveKm.toFixed(1));
  const tth = Math.floor(totalElapsed/3600), ttm = Math.floor(totalElapsed%3600/60);
  setText('jc-total', tth+'h'+p2(ttm)+'m');
  setText('jc-kmtotal', totalKm.toFixed(1)+' km');
  // CORRIGIDO (23/07/2026, confirmado com dado real pelo Yuri): esses 4
  // KPIs do card principal (corridas, km ativo, R$/h, R$/km) só somavam
  // day.trips (Importadas) — sessão só com automáticas mostrava tudo
  // zerado mesmo com corridas de verdade rodando. Mesma correção que já
  // tinha sido feita em rev/sessionItem/renderMetasDoDia, faltava aqui.
  const autoTripsHoje = day.autoTrips || [];

  // Banner "aguardando confirmação" — pedido do Yuri (16/08/2026), substitui
  // o botão fixo "Importar corridas". Conta corridas que ainda não foram
  // confirmadas por vídeo do histórico (status 'capturada' ou 'estimada') —
  // 'confirmada' só existe depois do vídeo, então isso mede exatamente o
  // que falta conferir naquele dia. Aparece em qualquer dia navegado, some
  // sozinho quando não sobra pendência.
  const pcbEl = document.getElementById('pending-confirm-banner');
  if (pcbEl) {
    // Não interpreta cache ainda carregando como zero corridas. Durante refresh,
    // mantém o último estado visual válido do mesmo dia.
    const autoTripsReady = S._autoTripsDataDate === cardDate;
    if (autoTripsReady) {
      const pendingCount = autoTripsHoje.filter(t => t.status !== 'confirmada' || t.value_needs_review === true).length;
      pcbEl.dataset.dataDate = cardDate;
      if (pendingCount > 0) {
        pcbEl.style.display = 'flex';
        setText('pcb-title', pendingCount + (pendingCount===1 ? ' corrida aguardando confirmação' : ' corridas aguardando confirmação'));
      } else {
        pcbEl.style.display = 'none';
      }
    } else if (pcbEl.dataset.dataDate && pcbEl.dataset.dataDate !== cardDate) {
      pcbEl.style.display = 'none';
      pcbEl.dataset.dataDate = cardDate;
    }
  }

  // Barra Online/Buscando passageiro/Em corrida — real, vinda da máquina de
  // estados do APK (ver computeStatusBreakdown). No card da jornada ATUAL
  // só faz sentido enquanto a sessão está rodando (é o que está sendo
  // contado agora); ao encerrar, o dado sai daqui e já está consolidado no
  // "Total do dia" logo abaixo — pedido do Yuri (16/08/2026), evita
  // duplicar/deixar a mesma barra "grudada" depois de já ter encerrado.
  // As barras são atualizadas exclusivamente pela timeline contínua V3.
  // A rotina base não sobrescreve mais a leitura real com auto_trips.
  const jcBd = document.getElementById('jc-breakdown');
  const tdBdRow = document.getElementById('td-bd-row');

  setText('jc-trips', day.trips.length + autoTripsHoje.length);
  setText('jc-trips2', day.trips.length + autoTripsHoje.length);
  const aceitasCount = day.trips.length + autoTripsHoje.length;
  const declinedCount = (S.declined || []).length;
  setText('jc-aceitas', aceitasCount);
  setText('jc-recusadas', declinedCount);
  const corridasSubEl = document.getElementById('jc-corridas-sub');
  if (corridasSubEl) corridasSubEl.style.display = (aceitasCount>0 || declinedCount>0) ? 'flex' : 'none';
  const kmAtivo = day.trips.reduce((s,t)=>s+(t.km||0),0)
                + autoTripsHoje.reduce((s,t)=>s+(parseFloat(t.real_km_trip ?? t.offer_km_trip)||0),0);
  const kmDesl = Math.max(0, totalKm - kmAtivo);
  setText('jc-kmativo', kmAtivo.toFixed(1));
  setText('jc-kmdesl', kmDesl.toFixed(1));
  const ganhosDia = day.trips.reduce((s,t)=>s+(t.value||0),0)
                  + autoTripsHoje.reduce((s,t)=>s+(t.offer_value||0),0);
  const horasDecimal = totalElapsed > 0 ? totalElapsed/3600 : 0;
  const rsh  = horasDecimal > 0 ? ganhosDia/horasDecimal : null;
  const rskm = totalKm > 0 ? ganhosDia/totalKm : null;
  setText('jc-rsh', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  setText('jc-rsh2', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm2', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  // Cor por indicador — pedido do Yuri (23/07/2026, mockup aprovado):
  // mesmos limiares vermelho/verde que o motorista já ajusta na régua do
  // MōB Flash (Indicadores → R$/km e R$/hora), cada indicador troca de cor
  // por conta própria. Sem dado ainda (sessão não rodou tempo/km) fica
  // neutro (verde), não faz sentido alarmar antes de ter dado de verdade.
  const rateEl = document.getElementById('jc-rate-hora');
  const kmEl   = document.getElementById('jc-rate-km');
  const rateEl2 = document.getElementById('jc-rate-hora2');
  const kmEl2   = document.getElementById('jc-rate-km2');
  const rateCls = rsh !== null ? rateColorClass('rhora', rsh) : '';
  const kmCls   = rskm !== null ? rateColorClass('rkm', rskm) : '';
  if (rateEl)  rateEl.className  = 'td-rcard ' + rateCls;
  if (kmEl)    kmEl.className    = 'td-rcard ' + kmCls;
  if (rateEl2) rateEl2.className = 'rate-cell ' + rateCls;
  if (kmEl2)   kmEl2.className   = 'rate-cell ' + kmCls;

  const actions = document.getElementById('jc-actions');
  if (!actions) return;
  if (!isToday) {
    actions.innerHTML = '';
  } else if (state === 'idle') {
    actions.innerHTML = `<button class="j-round-btn play" onclick="gpsStart()" aria-label="Iniciar jornada"><svg viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg></button>`;
  } else if (state === 'running') {
    actions.innerHTML = '<button class="j-round-btn stop" onclick="confirmStop()" aria-label="Encerrar jornada"><svg viewBox="0 0 24 24" fill="currentColor"><rect x="6" y="6" width="12" height="12"/></svg></button>';
  }
}

function agMinutesBetween(start, end) {
  if (!start || !end) return 0;
  const [h1,m1] = start.split(':').map(Number);
  const [h2,m2] = end.split(':').map(Number);
  return Math.max(0, (h2*60+m2) - (h1*60+m1));
}
function openMetasHojeModal() {
  const date = S.curDate || DB.today();
  const day = DB.day(date);
  const dow = new Date(date+'T12:00').getDay();
  const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
  const meta = DB.meta();
  const ov = day.metaOverride || null;
  const dimMes = new Date(new Date(date+'T12:00').getFullYear(), new Date(date+'T12:00').getMonth()+1, 0).getDate();
  const moneyGoal = (ov && ov.money) ? ov.money : ((plan && plan.moneyGoal) ? plan.moneyGoal : (meta.general ? meta.general/dimMes : 0));
  const kmGoal = (ov && ov.km) ? ov.km : (plan && plan.kmGoal ? plan.kmGoal : 0);
  const schedMin = (ov && ov.hours) ? ov.hours*60 : (plan ? (plan.schedules||[]).reduce((s,b)=>s+agMinutesBetween(b.start,b.end),0) : 0);
  const mEl = document.getElementById('mh-money'); if (mEl) mEl.value = moneyGoal ? moneyGoal.toFixed(2) : '';
  const kEl = document.getElementById('mh-km'); if (kEl) kEl.value = kmGoal || '';
  const hEl = document.getElementById('mh-hours'); if (hEl) hEl.value = schedMin ? (schedMin/60).toFixed(1) : '';
  const subEl = document.getElementById('mh-sub');
  if (subEl) {
    const isToday = date === DB.today();
    subEl.textContent = isToday ? 'Vale só para hoje' : ('Vale só para ' + new Date(date+'T12:00').toLocaleDateString('pt-BR',{weekday:'long',day:'numeric',month:'long'}));
  }
  openMo('mo-metas-hoje');
}
function saveMetasHoje() {
  const date = S.curDate || DB.today();
  const day = DB.day(date);
  const money = parseFloat((document.getElementById('mh-money')||{}).value) || 0;
  const km = parseFloat((document.getElementById('mh-km')||{}).value) || 0;
  const hours = parseFloat((document.getElementById('mh-hours')||{}).value) || 0;
  day.metaOverride = { money, km, hours };
  DB.saveDay(day);
  closeMo('mo-metas-hoje');
  try { renderMetasDoDia(day); } catch(e) {}
  showToast('Metas de hoje atualizadas!', 'success');
}
function renderMetasDoDia(day) {
  const stack = document.getElementById('metas-dia-stack');
  if (!stack) return;
  const rev = day.trips.reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0)
            + (day.autoTrips||[]).reduce((s,t)=>s+(t.offer_value||0),0);
  const isToday = day.date === DB.today();
  const liveOn = isToday && S.gps.st==='running';
  const totalKm = (day.sessions||[]).reduce((s,x)=>s+(x.km||0),0) + (liveOn ? S.gps.km : 0) + (day.kmAdjust || 0);
  const totalElapsed = Math.max(0, (day.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0) + (liveOn ? getElapsed() : 0) + (day.tempoAdjust || 0));
  const dow = new Date(day.date+'T12:00').getDay();
  const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
  const meta = DB.meta();
  const ov = day.metaOverride || null; // ajuste específico deste dia (via modal 'Metas de hoje')
  const dimMes = new Date(new Date(day.date+'T12:00').getFullYear(), new Date(day.date+'T12:00').getMonth()+1, 0).getDate();
  const moneyGoal = (ov && ov.money) ? ov.money : ((plan && plan.moneyGoal) ? plan.moneyGoal : (meta.general ? meta.general/dimMes : 0));

  const ICO_MONEY = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>';
  const ICO_KM = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76"/></svg>';
  const ICO_TIME = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>';

  let html = '';
  if (moneyGoal) {
    const pct = Math.min(100, Math.round(rev/moneyGoal*100));
    html += `<div class="goal-item"><div class="g-ico money">${ICO_MONEY}</div><div class="goal-body"><div class="goal-top"><strong>Financeira</strong><span>${DB.fRI(rev)} de ${DB.fRI(moneyGoal)} · ${pct}%</span></div><div class="gbar money"><i style="width:${pct}%"></i></div></div></div>`;
  } else {
    html += `<div class="goal-item"><div class="g-ico money">${ICO_MONEY}</div><div class="goal-locked" onclick="openPlanJornada()"><span>Financeira — meta não definida</span><b>Planejar</b></div></div>`;
  }
  const kmGoal = (ov && ov.km) ? ov.km : (plan && plan.kmGoal ? plan.kmGoal : 0);
  if (kmGoal) {
    const pct = Math.min(100, Math.round(totalKm/kmGoal*100));
    html += `<div class="goal-item"><div class="g-ico km">${ICO_KM}</div><div class="goal-body"><div class="goal-top"><strong>Km</strong><span>${totalKm.toFixed(1)} de ${kmGoal} km · ${pct}%</span></div><div class="gbar km"><i style="width:${pct}%"></i></div></div></div>`;
  } else {
    html += `<div class="goal-item"><div class="g-ico km">${ICO_KM}</div><div class="goal-locked" onclick="openPlanJornada()"><span>Km — meta não definida</span><b>Planejar</b></div></div>`;
  }
  const schedMin = (ov && ov.hours) ? ov.hours*60 : (plan ? (plan.schedules||[]).reduce((s,b)=>s+agMinutesBetween(b.start,b.end),0) : 0);
  if (schedMin > 0) {
    const liveMin = Math.round(totalElapsed/60);
    const pct = Math.min(100, Math.round(liveMin/schedMin*100));
    html += `<div class="goal-item"><div class="g-ico time">${ICO_TIME}</div><div class="goal-body"><div class="goal-top"><strong>Tempo online</strong><span>${Math.floor(liveMin/60)}h${p2(liveMin%60)} de ${Math.floor(schedMin/60)}h${p2(schedMin%60)} · ${pct}%</span></div><div class="gbar time"><i style="width:${pct}%"></i></div></div></div>`;
  } else {
    html += `<div class="goal-item"><div class="g-ico time">${ICO_TIME}</div><div class="goal-locked" onclick="openPlanJornada()"><span>Tempo online — meta não definida</span><b>Planejar</b></div></div>`;
  }
  stack.innerHTML = html;
}

// ── Planejar Jornada ─────────────────────────────────────────
let pjSelDays = [];
let pjSchedules = [];
const PJ_DOW = ['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'];
function openPlanJornada() {
  pjSelDays = []; pjSchedules = [];
  document.querySelectorAll('.pj-day').forEach(b => b.classList.remove('on'));
  const mEl = document.getElementById('pj-money'); if (mEl) mEl.value = '';
  const kEl = document.getElementById('pj-km'); if (kEl) kEl.value = '';
  renderPjSchedules();
  renderPjList();
  navTo('planejar-jornada
===== total do dia @ 1749998 =====
umber(S.gps.start)) < 15000;
      SmartMobiNative.setJourneySession(S.gps.sessionId, DB.today(), Number(S.gps.start), Number(S.gps.km)||0, isNew);
      nativeSessionBound = S.gps.sessionId;
    }catch(e){}
  }

  async function loadStatusSegments(dateStr){
    if(!_supaUser || statusLoading) return;
    statusLoading = true;
    try{
      const {data,error} = await _SUPA.from('journey_status_segments').select('*')
        .eq('user_id', _supaUser.id).eq('date', dateStr).order('start_ms',{ascending:true});
      if(!error){ statusSegments = data||[]; statusSegmentsDate = dateStr; }
    }catch(e){} finally { statusLoading = false; }
  }

  function segmentEndMs(seg, isLiveSession){
    if(seg.end_ms != null) return Number(seg.end_ms)||Number(seg.start_ms)||0;
    if(isLiveSession) return Date.now();
    return Number(seg.last_seen_ms)||Number(seg.start_ms)||0;
  }
  function segmentEndKm(seg, isLiveSession){
    if(seg.end_km != null) return Number(seg.end_km)||0;
    if(isLiveSession && S && S.gps) return Number(S.gps.km)||0;
    return Number(seg.last_km)||Number(seg.start_km)||0;
  }

  function breakdownFromSegments(elapsedSec, kmTotal, segments, liveSessionId){
    const out = {
      total: Math.max(0,Number(elapsedSec)||0),
      online:{sec:0,km:0,pct:0}, buscando:{sec:0,km:0,pct:0}, corrida:{sec:0,km:0,pct:0}
    };
    (segments||[]).forEach(seg=>{
      const status = seg.status === 'buscar' ? 'buscando' : seg.status;
      if(!out[status]) return;
      const start = Number(seg.start_ms)||0;
      const live = !!liveSessionId && seg.session_id === liveSessionId && seg.end_ms == null;
      const end = segmentEndMs(seg, live);
      const sec = Math.max(0,(end-start)/1000);
      const skm = Number(seg.start_km)||0;
      const ekm = segmentEndKm(seg, live);
      out[status].sec += sec;
      out[status].km += Math.max(0,ekm-skm);
    });
    // Tempo/km que ainda não tem trecho explícito continua como Online, mas
    // NUNCA apaga os trechos Buscar/Corrida já registrados.
    const trackedSec = out.online.sec+out.buscando.sec+out.corrida.sec;
    const trackedKm = out.online.km+out.buscando.km+out.corrida.km;
    out.online.sec += Math.max(0,out.total-trackedSec);
    out.online.km += Math.max(0,(Number(kmTotal)||0)-trackedKm);
    const pct = v => out.total>0 ? Math.round(Math.max(0,v)/out.total*100) : 0;
    out.online.pct=pct(out.online.sec); out.buscando.pct=pct(out.buscando.sec); out.corrida.pct=pct(out.corrida.sec);
    return out;
  }

  function manualTripTs(t,dateStr){
    if(!t) return 0;
    let ts=Number(t.ts)||0;
    if(!ts&&t.created_at) ts=Date.parse(t.created_at)||0;
    if(!ts&&t.accepted_at) ts=Date.parse(t.accepted_at)||0;
    if(!ts&&t.trip_started_at) ts=Date.parse(t.trip_started_at)||0;
    if(!ts&&t.time&&dateStr) ts=new Date(dateStr+'T'+t.time+':00').getTime();
    return ts||0;
  }

  function currentSessionTrips(day,startMs){
    const dateStr=day&&day.date?day.date:DB.today();
    const manual=(day&&day.trips?day.trips:[]).filter(t=>manualTripTs(t,dateStr)>=startMs);
    const auto=(day&&day.autoTrips?day.autoTrips:[]).filter(t=>{
      try{ const ts=typeof autoTripTs==='function'?autoTripTs(t):Date.parse(t.trip_started_at||t.accepted_at||'')||0; return ts&&ts>=startMs; }
      catch(e){return false;}
    });
    return {manual,auto};
  }

  function mergedTodaySegments(){
    const live = (S && S.gps && S.gps.st==='running') ? nativeStatusTimeline() : [];
    const sid = S&&S.gps ? S.gps.sessionId : null;
    const cloud = (statusSegmentsDate===S.curDate ? statusSegments : []).filter(x=>!sid || x.session_id!==sid);
    return cloud.concat(live);
  }

  function renderStatusTimeline(){
    try{
      if(!S||!DB||S.sec!=='jornada') return;
      ensureNativeJourneySession();
      if(S.curDate && statusSegmentsDate!==S.curDate) loadStatusSegments(S.curDate);

      const day=DB.day(S.curDate||DB.today());
      const isToday=S.curDate===DB.today();
      const live=isToday&&S.gps&&S.gps.st==='running'&&S.gps.start;

      // Card verde: somente a Jornada atual.
      if(live){
        const elapsed=Math.max(0,typeof getElapsed==='function'?getElapsed():0);
        const km=Math.max(0,Number(S.gps.km)||0);
        const trips=currentSessionTrips(day,Number(S.gps.start)||0);
        const ganhos=trips.manual.reduce((a,t)=>a+(Number(t.value)||0),0)+trips.auto.reduce((a,t)=>a+(Number(t.offer_value)||0),0);
        if(typeof setText==='function'){
          setText('jc-trips2',trips.manual.length+trips.auto.length);
          setText('jc-rsh2',elapsed>0?DB.fRI(ganhos/(elapsed/3600)):'R$0,00');
          setText('jc-rskm2',km>0?DB.fRI(ganhos/km):'R$0,00');
        }
        const native=nativeStatusTimeline();
        const bd=breakdownFromSegments(elapsed,km,native,S.gps.sessionId);
        const jc=document.getElementById('jc-breakdown');
        if(jc){ jc.style.display=''; setHTML('jc-bar',buildSbarHtml(bd)); setHTML('jc-blist',buildSrowsHtml(bd)); }
      }

      // Total do dia: soma trechos persistidos de TODAS as jornadas + trecho vivo.
      const finished=(day.sessions||[]);
      const totalElapsed=finished.reduce((a,x)=>a+(x.elapsed||0),0)+(live?getElapsed():0)+(day.tempoAdjust||0);
      const totalKm=finished.reduce((a,x)=>a+(x.km||0),0)+(live?(Number(S.gps.km)||0):0)+(day.kmAdjust||0);
      const td=document.getElementById('td-bd-row');
      if(td&&totalElapsed>0){
        const bd=breakdownFromSegments(totalElapsed,totalKm,mergedTodaySegments(),live?S.gps.sessionId:null);
        td.style.display=''; setHTML('td-bd-bar',buildSbarHtml(bd)); setHTML('td-bd-list',buildSrowsHtml(bd));
      }
    }catch(e){ console.warn('[JourneyStatusTimeline]',e); }
  }
  // Compatibilidade apenas: V3 abaixo é o único renderizador automático.
  // Expõe somente o vínculo da sessão nativa para o V3 poder garantir que
  // uma Jornada recém-iniciada comece a gravar Online/Buscar/Corrida mesmo
  // sem depender do renderizador legado da barra.
  window.ensureNativeJourneySession=ensureNativeJourneySession;
  window.renderStatusTimeline=renderStatusTimeline;
})();
</script>



<!-- JOURNEY_CONTINUOUS_OPERATIONAL_V3 -->
<script>
(function(){
  // ── Datas e dias operacionais ───────────────────────────────────────
  window.localYmdFromMs = function(ms){
    const d = new Date(Number(ms)||Date.now());
    return d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
  };
  window.normalizeOperationalPlatform = function(p){
    return String(p||'uber').toLowerCase() === '99' ? '99' : 'uber';
  };
  window.civilDayBounds = function(dateStr){
    const start = new Date(dateStr+'T00:00:00');
    const end = new Date(start); end.setDate(end.getDate()+1);
    return {startMs:start.getTime(), endMs:end.getTime()};
  };
  window.operationalRangeForPlatform = function(plat,dateStr){
    const p = normalizeOperationalPlatform(plat);
    const start = new Date(dateStr+'T00:00:00');
    if(p === 'uber') start.setHours(4,0,0,0);
    const end = new Date(start); end.setDate(end.getDate()+1);
    return {platform:p, operationalDate:dateStr, startMs:start.getTime(), endMs:end.getTime()};
  };
  window.operationalDateForTimestamp = function(plat,ts){
    const p = normalizeOperationalPlatform(plat);
    const d = new Date(Number(ts));
    // Uber: tudo de 00:00 até 03:59 ainda pertence ao dia operacional anterior.
    if(p === 'uber' && d.getHours() < 4) d.setDate(d.getDate()-1);
    return localYmdFromMs(d.getTime());
  };
  window.operationalTimestampForTime = function(plat,dateStr,hhmm){
    const p = normalizeOperationalPlatform(plat);
    const parts = String(hhmm||'00:00').split(':').map(Number);
    const h = Number.isFinite(parts[0]) ? parts[0] : 0;
    const m = Number.isFinite(parts[1]) ? parts[1] : 0;
    const d = new Date(dateStr+'T00:00:00');
    d.setHours(h,m,0,0);
    if(p === 'uber' && h < 4) d.setDate(d.getDate()+1);
    return d.getTime();
  };
  window.loadAutoTripsForOperationalDay = async function(plat,dateStr){
    if(!_supaUser) return [];
    const r = operationalRangeForPlatform(plat,dateStr);
    let q = _SUPA.from('auto_trips').select('*')
      .eq('user_id',_supaUser.id)
      .eq('platform',normalizeOperationalPlatform(plat))
      .gte('trip_started_at',new Date(r.startMs).toISOString())
      .lt('trip_started_at',new Date(r.endMs).toISOString())
      .order('trip_started_at',{ascending:true});
    const {data,error} = await q;
    return error ? [] : (data||[]);
  };

  // ── Filtro da tela Corridas ────────────────────────────────────────
  const _civilGetCorridasDays = window.getCorridasDays;
  window.getCorridasDays = function(){
    const plats = S.corridasPlats || new Set();
    const onePlat = plats.size === 1 ? [...plats][0] : null;
    if(!onePlat) return _civilGetCorridasDays();
    const anchorStr = operationalDateForTimestamp(onePlat,Date.now());
    const anchor = new Date(anchorStr+'T12:00:00');
    const fmt = d => localYmdFromMs(d.getTime());
    const p = S.corridasPeriod;
    if(p==='date' && S.corridasDate) return [S.corridasDate];
    if(p==='hoje') return [anchorStr];
    if(p==='7d') { const out=[]; for(let i=0;i<7;i++){const d=new Date(anchor);d.setDate(d.getDate()-i);out.push(fmt(d));} return out; }
    if(p==='mes') {
      const y=anchor.getFullYear(), m=anchor.getMonth();
      const dim=new Date(y,m+1,0).getDate(), out=[];
      for(let day=1;day<=dim;day++) out.push(y+'-'+p2(m+1)+'-'+p2(day));
      return out;
    }
    return _civilGetCorridasDays();
  };
  window.autoCorridasDateRange = function(){
    const dates = getCorridasDays().slice().sort();
    if(!dates.length) return null;
    const plats = S.corridasPlats || new Set();
    const onePlat = plats.size===1 ? [...plats][0] : null;
    if(onePlat){
      const a=operationalRangeForPlatform(onePlat,dates[0]);
      const b=operationalRangeForPlatform(onePlat,dates[dates.length-1]);
      return {from:new Date(a.startMs).toISOString(),to:new Date(b.endMs-1).toISOString()};
    }
    const a=civilDayBounds(dates[0]), b=civilDayBounds(dates[dates.length-1]);
    return {from:new Date(a.startMs).toISOString(),to:new Date(b.endMs-1).toISOString()};
  };

  // ── Jornada contínua: nunca mais pergunta/divide à meia-noite ─────
  window.checkMidnightCrossing = function(){ return; };
  window.openMidnightModal = function(){ S._midnightModalOpen=false; try{closeMo('mo-midnight');}catch(e){} };
  window.resolveMidnightSplit = async function(){ S._midnightModalOpen=false; try{closeMo('mo-midnight');}catch(e){} };
  window.splitJourneyAtMidnight = async function(){ return; };
  window.openPostStopMidnightModal = function(sess,onDone){
    S._midnightIsPostStop=false; S._postStopMidnightSess=null; S._postMidnightCallback=null; S._midnightModalOpen=false;
    try{closeMo('mo-midnight');}catch(e){}
    if(typeof onDone==='function') onDone();
  };
  window.resolvePostStopMidnight = async function(){ S._midnightIsPostStop=false; S._midnightModalOpen=false; try{closeMo('mo-midnight');}catch(e){} };
  try { if(S && S.gps) S.gps.forceDate = null; } catch(e) {}

  // ── Corridas de uma Jornada: busca por INTERVALO REAL, não por data ──
  const journeyAutoCache = new Map();
  const journeyStatusCache = new Map();
  const overlapSessionCache = new Map();
  const civilStatusCache = new Map();

  function journeyKey(sess){ return String(sess?.id || ((sess?.start||0)+'_'+(sess?.end||'live'))); }
  function sessionEnd(sess){ return Number(sess?.end || ((S?.gps?.sessionId && sess?.id===S.gps.sessionId) ? Date.now() : Date.now())); }
  function overlaps(start,end,a,b){ return Number(start||0) < b && Number(end||Date.now()) >= a; }

  async function ensureJourneyAutoTrips(sess){
    if(!_supaUser || !sess?.start) return;
    const k=journeyKey(sess), old=journeyAutoCache.get(k);
    const ttl=sess.end?120000:12000;
    if(old && (old.loading || Date.now()-old.ts<ttl)) return;
    journeyAutoCache.set(k,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    try{
      const end=sessionEnd(sess);
      const {data,error}=await _SUPA.from('auto_trips').select('*')
        .eq('user_id',_supaUser.id)
        .gte('trip_started_at',new Date(Number(sess.start)).toISOString())
        .lte('trip_started_at',new Date(end).toISOString())
        .order('trip_started_at',{ascending:true});
      if(!error) journeyAutoCache.set(k,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){ journeyAutoCache.set(k,{loading:false,ts:Date.now(),rows:old?.rows||[]}); }
    try{ if(S.sec==='jornada') setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0); }catch(e){}
  }
  function journeyAutoTrips(sess,fallback){
    const k=journeyKey(sess), c=journeyAutoCache.get(k);
    ensureJourneyAutoTrips(sess);
    const rows=(c&&Array.isArray(c.rows)&&c.rows.length)?c.rows:(fallback||[]);
    const a=Number(sess?.start||0), b=sessionEnd(sess);
    const seen=new Set();
    return rows.filter(t=>{const ts=autoTripTs(t); if(!ts||ts<a||ts>b)return false; const id=t.id||ts+'_'+t.offer_value; if(seen.has(id))return false; seen.add(id); return true;});
  }
  function journeyManualTrips(sess){
    const a=Number(sess?.start||0), b=sessionEnd(sess), out=[], seen=new Set();
    (DB.allDates?DB.allDates():[]).forEach(ds=>{
      const d=DB.day(ds);
      (d.trips||[]).forEach(t=>{
        let ts=Number(t.ts)||0;
        if(!ts&&t.time) ts=new Date(ds+'T'+t.time+':00').getTime();
        if(!ts||ts<a||ts>b)return;
        const id=t.id||ts+'_'+(t.value||0); if(seen.has(id))return; seen.add(id);
        out.push(Object.assign({},t,{ts,_ts:ts}));
      });
    });
    return out;
  }

  // ── Timeline real Online/Buscar/Corrida por Jornada ────────────────
  async function ensureJourneyStatus(sess){
    if(!_supaUser || !sess?.id) return;
    const k=journeyKey(sess), old=journeyStatusCache.get(k), ttl=sess.end?120000:8000;
    if(old && (old.loading || Date.now()-old.ts<ttl)) return;
    journeyStatusCache.set(k,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    try{
      const {data,error}=await _SUPA.from('journey_status_segments').select('*')
        .eq('user_id',_supaUser.id).eq('session_id',sess.id).order('start_ms',{ascending:true});
      if(!error) journeyStatusCache.set(k,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){ journeyStatusCache.set(k,{loading:false,ts:Date.now(),rows:old?.rows||[]}); }
    try{ if(S.sec==='jornada') setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0); }catch(e){}
  }
  function statusBreakdownRows(rows,elapsedSec,kmTotal,clipStart,clipEnd,liveSessionId){
    const out={total:Math.max(0,Number(elapsedSec)||0),online:{sec:0,km:0,pct:0},buscando:{sec:0,km:0,pct:0},corrida:{sec:0,km:0,pct:0}};
    (rows||[]).forEach(seg=>{
      const key=seg.status==='buscar'?'buscando':seg.status; if(!out[key])return;
      const rawStart=Number(seg.start_ms)||0;
      const isLive=!!liveSessionId&&seg.session_id===liveSessionId&&seg.end_ms==null;
    
===== total do dia @ 1768001 =====
 if(i>=0)day.sessions[i]=item; else day.sessions.push(item); DB.saveDay(day);
        });
      }
    }catch(e){overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows:old?.rows||[]});}
    try{if(S.sec==='jornada')setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0);}catch(e){}
  }
  function overlappingJourneyDescriptors(dateStr){
    const b=civilDayBounds(dateStr), map=new Map();
    (DB.allDates?DB.allDates():[]).forEach(ds=>{
      const day=DB.day(ds);(day.sessions||[]).forEach((sess,idx)=>{
        if(!overlaps(sess.start,sess.end,b.startMs,b.endMs))return;
        map.set(String(sess.id||sess.start),{sess,sourceDate:ds,origIdx:idx});
      });
    });
    const remote=overlapSessionCache.get(dateStr)?.rows||[];
    remote.forEach(r=>{
      if(!overlaps(r.start_ms,r.end_ms,b.startMs,b.endMs))return;
      const id=String(r.id||r.start_ms);if(map.has(id))return;
      const ds=localYmdFromMs(r.start_ms), d=DB.day(ds), idx=(d.sessions||[]).findIndex(x=>x.id===r.id);
      map.set(id,{sess:{id:r.id,start:Number(r.start_ms),end:r.end_ms?Number(r.end_ms):null,km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0},sourceDate:ds,origIdx:idx});
    });
    if(S?.gps?.st==='running'&&S.gps.start&&overlaps(S.gps.start,null,b.startMs,b.endMs)){
      const id=String(S.gps.sessionId||S.gps.start);
      map.set(id,{sess:{id:S.gps.sessionId,start:S.gps.start,end:null,km:S.gps.km||0,elapsed:getElapsed()},sourceDate:localYmdFromMs(S.gps.start),origIdx:-1,live:true});
    }
    return [...map.values()].sort((a,b)=>(a.sess.start||0)-(b.sess.start||0));
  }
  function journeyLabel(desc,dateStr){
    const sess=desc.sess, src=desc.sourceDate||localYmdFromMs(sess.start);
    if(desc.live) return src===dateStr?'Jornada atual':'Jornada atual · continuação';
    const srcSessions=(DB.day(src).sessions||[]).slice().sort((a,b)=>(a.start||0)-(b.start||0));
    const pos=Math.max(0,srcSessions.findIndex(x=>x.id===sess.id));
    let label='Jornada '+(pos+1);
    const endDate=sess.end?localYmdFromMs(sess.end):src;
    if(src<dateStr) label+=' · continuação'; else if(endDate>dateStr) label+=' · continua';
    return label;
  }
  function renderContinuousSessionList(){
    const el=document.getElementById('sessions-list'), toggle=document.getElementById('sessions-toggle');
    if(!el||!S?.curDate)return;
    ensureOverlapSessions(S.curDate);
    const desc=overlappingJourneyDescriptors(S.curDate).sort((a,b)=>(b.sess.start||0)-(a.sess.start||0));
    const shown=S.sessionsExpanded?desc:desc.slice(0,3);
    const html=shown.map(x=>{
      ensureJourneyAutoTrips(x.sess);ensureJourneyStatus(x.sess);
      const synthetic={date:x.sourceDate,trips:journeyManualTrips(x.sess),autoTrips:journeyAutoTrips(x.sess,DB.day(x.sourceDate).autoTrips||[])};
      return sessionItem(journeyLabel(x,S.curDate),x.sess,x.origIdx,x.sourceDate,synthetic,!!x.live);
    });
    el.innerHTML=html.length?html.join(''):'<div class="empty">Nenhuma sessão registrada</div>';
    if(toggle){toggle.style.display=desc.length>3?'':'none';toggle.innerHTML=S.sessionsExpanded?'Ver menos':'Ver todas &#8250;';}
  }

  // Link de corrida ↔ Jornada agora procura TODAS as sessões; uma corrida de 01:30
  // pode pertencer à Jornada iniciada no dia anterior.
  window.isAutoTripLinkedToSession = function(t){
    const ts=autoTripTs(t);if(!ts)return false;
    for(const ds of (DB.allDates?DB.allDates():[])){
      if((DB.day(ds).sessions||[]).some(sess=>ts>=(sess.start||0)&&(!sess.end||ts<=sess.end)))return true;
    }
    return !!(S?.gps?.st==='running'&&S.gps.start&&ts>=S.gps.start);
  };

  function civilJourneyTotals(dateStr){
    const b=civilDayBounds(dateStr), seen=new Set();
    let elapsed=0, km=0;
    const add=sess=>{
      const start=Number(sess?.start||0); if(!start)return;
      const live=!!(S?.gps?.st==='running' && S.gps.sessionId && sess?.id===S.gps.sessionId);
      let end=Number(sess?.end||0);
      const recordedElapsed=Math.max(0,Number(sess?.elapsed)||0);
      if(!end) end=live?Date.now():(recordedElapsed>0?start+recordedElapsed*1000:0);
      if(!end||end<=start)return;
      const a=Math.max(start,b.startMs), z=Math.min(end,b.endMs); if(z<=a)return;
      const key=String(sess?.id||start+'_'+end); if(seen.has(key))return; seen.add(key);
      const wall=Math.max(1,end-start), frac=(z-a)/wall;
      const baseElapsed=recordedElapsed>0?recordedElapsed:wall/1000;
      elapsed+=Math.max(0,baseElapsed*frac);
      km+=Math.max(0,Number(sess?.km)||0)*frac;
    };
    try{(DB.allDates?DB.allDates():[]).forEach(ds=>(DB.day(ds).sessions||[]).forEach(add));}catch(e){}
    if(S?.gps?.st==='running'&&S.gps.start){
      add({id:S.gps.sessionId,start:S.gps.start,end:null,elapsed:getElapsed(),km:S.gps.km||0});
    }
    try{
      const day=DB.day(dateStr);
      elapsed=Math.max(0,elapsed+(Number(day.tempoAdjust)||0));
      km=Math.max(0,km+(Number(day.kmAdjust)||0));
    }catch(e){}
    return {elapsed,km};
  }

  // ── Barra Total do dia = corte CIVIL virtual da timeline ───────────
  async function ensureCivilStatus(dateStr){
    if(!_supaUser||!dateStr)return;
    const old=civilStatusCache.get(dateStr);if(old&&(old.loading||Date.now()-old.ts<8000))return;
    civilStatusCache.set(dateStr,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    const b=civilDayBounds(dateStr);
    try{
      const {data,error}=await _SUPA.from('journey_status_segments').select('*').eq('user_id',_supaUser.id)
        .lt('start_ms',b.endMs).or(`end_ms.is.null,end_ms.gte.${b.startMs}`).order('start_ms',{ascending:true});
      if(!error)civilStatusCache.set(dateStr,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){civilStatusCache.set(dateStr,{loading:false,ts:Date.now(),rows:old?.rows||[]});}
    try{if(S.sec==='jornada')setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0);}catch(e){}
  }
  function renderContinuousStatusBars(){
    if(!S?.curDate)return;
    ensureCivilStatus(S.curDate);
    const c=civilStatusCache.get(S.curDate), rows=c?.rows||[];
    const tdRow=document.getElementById('td-bd-row');
    const totals=civilJourneyTotals(S.curDate);
    if(rows.length){
      const b=civilDayBounds(S.curDate), bd=statusBreakdownRows(rows,totals.elapsed,totals.km,b.startMs,b.endMs,(S?.gps?.st==='running'?S.gps.sessionId:null));
      if(tdRow)tdRow.style.display='';
      try{setHTML('td-bd-bar',buildSbarHtml(bd));setHTML('td-bd-list',buildSrowsHtml(bd));}catch(e){}
    } else if(c && !c.loading) {
      const day=DB.day(S.curDate);
      const autos=(day.autoTrips&&day.autoTrips.length)?day.autoTrips:((S._autoTripsDataDate===S.curDate&&Array.isArray(S.autoTrips))?S.autoTrips:[]);
      const bd=computeStatusBreakdown(totals.elapsed,totals.km,autos);
      const hasJourney=totals.elapsed>0||totals.km>0||autos.length>0;
      if(tdRow)tdRow.style.display=hasJourney?'':'none';
      if(hasJourney){try{setHTML('td-bd-bar',buildSbarHtml(bd));setHTML('td-bd-list',buildSrowsHtml(bd));}catch(e){}}
    }

    const jcRow=document.getElementById('jc-breakdown');
    if(S?.gps?.st==='running'&&S.gps.sessionId){
      const sess={id:S.gps.sessionId,start:S.gps.start,end:null,km:S.gps.km||0,elapsed:getElapsed()};
      // Garante o vínculo com JourneyStatusTracker mesmo que o renderizador
      // legado esteja desativado. Isso faz a timeline nativa começar a gravar.
      try{ if(typeof window.ensureNativeJourneySession==='function') window.ensureNativeJourneySession(); }catch(e){}
      ensureJourneyStatus(sess);
      const bd=journeyBreakdownForSession(sess,getElapsed(),S.gps.km||0,[]);
      // Enquanto não existe trecho detalhado, o fallback acima mostra 100%
      // Online. Jornada ativa nunca deve esconder a barra.
      if(jcRow)jcRow.style.display='';
      try{setHTML('jc-bar',buildSbarHtml(bd));setHTML('jc-blist',buildSrowsHtml(bd));}catch(e){}
    } else if(jcRow) {
      jcRow.style.display='none';
    }
  }

  const previousRenderJornada=window.renderJornada;
  window.renderJornada=function(){
    previousRenderJornada();
    try{renderContinuousSessionList();}catch(e){console.warn('[continuous journeys]',e);}
    try{renderContinuousStatusBars();}catch(e){console.warn('[continuous status]',e);}
  };

  setInterval(()=>{try{if(S.sec==='jornada')renderContinuousStatusBars();}catch(e){}},1000);
  setTimeout(()=>{try{if(S.sec==='jornada')renderJornada();}catch(e){}},600);
})();
</script>

</body>
</html>
===== sessions @ 409793 =====
 0 1 1.9 1.5L21 13"/><path d="M3 13h18v4a1 1 0 0 1-1 1h-1a1 1 0 0 1-1-1v-1H6v1a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1v-4z"/><circle cx="7.5" cy="17" r="1.3"/><circle cx="16.5" cy="17" r="1.3"/></svg>
        <div class="td-corridas"><strong id="jc-trips">0</strong><span>Corridas</span></div>
        <div class="td-sub" id="jc-corridas-sub" style="display:none">
          <span class="td-chip ok"><svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg><span id="jc-aceitas">0</span> aceitas</span>
          <span class="td-vline"></span>
          <span class="td-chip no"><svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg><span id="jc-recusadas">0</span> recusadas</span>
        </div>
      </div>

      <div class="td-row">
        <div class="tk-split">
          <div class="tk-half left">
            <svg class="td-wm" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9.5"/><path d="M12 6.5v5.5l4 2.3"/></svg>
            <div style="display:flex;align-items:center;justify-content:center;gap:6px;position:relative;z-index:1">
              <strong id="jc-total">0h00</strong>
              <button class="km-edit-btn" onclick="openTempoAdjust()" aria-label="Editar tempo total">
                <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M17 3a2.85 2.85 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"/></svg>
              </button>
            </div>
            <span>Tempo total</span>
          </div>
          <div class="tk-divider"></div>
          <div class="tk-half right">
            <svg class="td-wm" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round"><path d="M4 23L9 1M20 23L15 1"/><path d="M12 3.5v3M12 10v3M12 16.5v3"/></svg>
            <div style="display:flex;align-items:center;justify-content:center;gap:5px;position:relative;z-index:1">
              <strong id="jc-kmtotal">0,0 km</strong>
              <button class="km-edit-btn" onclick="openKmAdjust()" aria-label="Editar km total">
                <svg viewBox="0 0 24 24" width="10" height="10" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M17 3a2.85 2.85 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"/></svg>
              </button>
            </div>
            <span>Km total</span>
          </div>
        </div>
      </div>

      <div class="td-row">
        <svg class="td-wm left" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="0.6" stroke-linecap="round" stroke-linejoin="round"><path d="M12 1v22"/><path d="M17 5.5c0-2-2.2-3.3-5-3.3s-5 1.5-5 3.7c0 2.5 2.2 3.5 5 4.3c3.2.9 5.4 1.9 5.4 4.5c0 2.5-2.4 3.8-5.4 3.8s-5.4-1.4-5.4-3.8"/></svg>
        <div class="td-rates">
          <div class="td-rcard" id="jc-rate-hora"><strong id="jc-rsh">R$0,00</strong><span>R$ / hora</span></div>
          <div class="td-rcard" id="jc-rate-km"><strong id="jc-rskm">R$0,00</strong><span>R$ / km</span></div>
        </div>
        <div class="td-fin-divider"></div>
        <div class="td-fin">
          <div class="cell"><strong class="g" id="d-rev">R$0</strong><span>Receitas</span></div>
          <div class="cell"><strong class="r" id="d-exp">R$0</strong><span>Despesas</span></div>
          <div class="cell"><strong id="d-net">R$0</strong><span>Resultado</span></div>
        </div>
      </div>

      <div class="td-row bd" id="td-bd-row" style="display:none">
        <div class="sbar" id="td-bd-bar"></div>
        <div class="sbar-list open" id="td-bd-list"></div>
      </div>

    </div>
  </div>

  <!-- Metas do dia: pedido do Yuri (23/07/2026) — sai de dentro do card
       azul da jornada (onde competia por espaço com timer/km/corridas) e
       vira seção solta, igual "Lançamentos" logo abaixo (título + link no
       fundo da tela, sem balão/card branco por trás). renderMetasDoDia()
       não muda nada — só o container onde a mesma função escreve. -->
  <div class="section" style="padding:0 16px 8px"><h2>Metas do dia</h2><button class="add" onclick="openMetasHojeModal()">Editar &#8250;</button></div>
  <div style="padding:0 16px 4px">
    <div class="metas-wrap">
      <svg class="metas-wm" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1"><circle cx="12" cy="12" r="9.5"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2.2"/></svg>
      <div id="metas-dia-stack"></div>
    </div>
  </div>

  <div class="section" style="padding:16px 16px 8px"><h2>Jornadas do dia</h2><button class="add" onclick="toggleSessionsView()" id="sessions-toggle" style="display:none">Ver todas &#8250;</button></div>
  <div style="padding:0 16px"><div id="sessions-list"><div class="empty">Nenhuma sessão registrada</div></div></div>

  <div class="section" style="padding:16px 16px 8px"><h2>Lançamentos</h2><div style="display:flex;gap:8px;align-items:center"><button class="add" onclick="toggleLancView()" id="lanc-toggle" style="display:none">Ver todas &#8250;</button><button class="add" onclick="openLanc('receita')">+ Adicionar</button></div></div>
  <div style="padding:0 16px"><div id="jorn-lanc-list"><div class="empty">Nenhum lançamento registrado</div></div></div>

  <div style="height:8px"></div>
</div>

<!-- ══ TELA FINANCEIRO ════════════════════════════════════════════════════ -->
<div class="screen" id="sc-financeiro">
  <div class="wrap">
    <section class="hero" style="margin-bottom:14px">
      <div class="fin-hero-top">
        <div class="fin-ptabs">
          <button id="fp-today" onclick="setFinPer('today')">Hoje</button>
          <button id="fp-month" class="on" onclick="setFinPer('month')">Mês</button>
          <button id="fp-year" onclick="setFinPer('year')">Ano</button>
        </div>
        <button class="fin-filter-btn" id="fin-filter-btn" onclick="openFinFilter()"><svg viewBox="0 0 24 24" width="17" height="17" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M3 6h18"/><path d="M7 12h10"/><path d="M10 18h4"/></svg></button>
      </div>
      <div class="heroTop">
        <div><div class="label">Saldo líquido</div><div class="amount" id="fin-amount">R$ 0,00</div><div class="sub" id="fin-sub">Entradas R$ 0 · Saídas R$ 0</div></div>
      </div>
      <div class="fin-range-chip" id="fin-range-chip" style="display:none">
        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.3"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
        <span id="fin-range-txt"></span>
        <button onclick="event.stopPropagation();clearFinFilter()">&#x2715;</button>
      </div>
      <div class="heroGrid">
        <div class="heroCell"><strong id="fin-pct-lucro">0%</strong><span>% lucro</span></div>
        <div class="heroCell"><strong id="fin-media">R$ 0</strong><span>média dia</span></div>
        <div class="heroCell"><strong id="fin-vs-ant">—</strong><span id="fin-vs-ant-lbl">vs mês ant.</span></div>
      </div>
    </section>
    <section class="card cardPad" style="margin-bottom:14px">
      <div class="cardHead"><h2>Resumo</h2><div class="seg"><button class="on" id="fs-all" onclick="setFinScope('all')">Tudo</button><button id="fs-prof" onclick="setFinScope('prof')">Prof.</button><button id="fs-pes" onclick="setFinScope('pes')">Pessoal</button></div></div>
      <div class="fin-metrics">
        <div class="fin-metric g"><span>Corridas</span><strong id="fin-corr">R$ 0,00</strong><small id="fin-corr-sub">&nbsp;</small></div>
        <div class="fin-metric g"><span>Receitas extras</span><strong id="fin-rec">R$ 0,00</strong><small>gorjetas, b&#244;nus...</small></div>
        <div class="fin-metric r"><span>Despesas</span><strong id="fin-desp">R$ 0,00</strong><small id="fin-desp-sub">&nbsp;</small></div>
        <div class="fin-metric"><span>Lucro</span><strong id="fin-lucro">R$ 0,00</strong><small id="fin-lucro-sub">&nbsp;</small></div>
      </div>
    </section>
    <section class="card cardPad" style="margin-bottom:14px">
      <div class="cardHead"><h2>Objetivos</h2></div>
      <div class="obj-switch">
        <button class="obj-btn active" id="ob-rec" onclick="setObjTab('rec')">Receitas</button>
        <button class="obj-btn" id="ob-desp" onclick="setObjTab('desp')">Despesas</button>
      </div>
      <div id="obj-rec-list"></div>
      <div id="obj-desp-list" style="display:none"></div>
      <div class="cfg-link" onclick="openObjConfig()">Configurar objetivos</div>
    </section>
    <section class="card cardPad" style="margin-bottom:14px">
      <div class="cardHead"><h2>Fluxo</h2></div>
      <div class="fin-flow" id="fin-flow"></div>
      <div class="fin-flow-legend">
        <span><i style="background:#22C55E"></i>Entradas (com corridas)</span>
        <span><i style="background:#FCA5A5"></i>Sa&#237;das</span>
      </div>
    </section>
    <section class="card cardPad" style="margin-bottom:14px">
      <div class="cardHead"><h2>Ações rápidas</h2></div>
      <div class="fin-actions">
        <button class="fin-action" onclick="openLanc('receita')"><div class="ico green"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12h14"/></svg></div>Receita</button>
        <button class="fin-action" onclick="openLanc('despesa')"><div class="ico red"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12h14"/></svg></div>Despesa</button>
        <button class="fin-action" onclick="navTo('lancamentos')"><div class="ico purple"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 19V5a2 2 0 0 1 2-2h12v18H6a2 2 0 0 1-2-2Z"/></svg></div>Histórico</button>
        <button class="fin-action" onclick="navTo('relatorios')"><div class="ico orange"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 3v18h18"/><path d="m19 9-5 5-4-4-4 4"/></svg></div>Relatório</button>
      </div>
    </section>
    <section class="card" style="margin-bottom:14px">
      <div class="cardPad cardHead"><h2>Últimas movimentações</h2><button onclick="navTo('lancamentos')">Ver tudo</button></div>
      <div id="fin-mov"><div class="empty">Nenhuma movimentação</div></div>
    </section>
    <div style="height:8px"></div>
  </div>
</div>

<!-- ══ TELA RELATÓRIOS (placeholder — visão geral + alertas vêm na sequência) ══ -->
<div class="screen" id="sc-relatorios">
  <div style="display:flex;align-items:center;gap:10px;padding:14px 16px 6px">
    <button onclick="navTo('financeiro')" style="width:32px;height:32px;border-radius:10px;background:#F1F5F9;border:0;display:flex;align-items:center;justify-content:center;color:#0F172A;cursor:pointer;flex-shrink:0"><svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M19 12H5M12 19l-7-7 7-7"/></svg></button>
    <h2 style="font-size:17px;letter-spacing:-.02em;flex:1">Relatórios</h2>
  </div>
  <div style="padding:0 16px">
    <div class="ad-carousel" id="ad-carousel-0"></div>
  </div>
  <div class="hg-info-tip" id="hg-info-tip"></div>
  <section class="hg-period-card" style="margin:12px 16px 0">
    <div class="hg-period-top">
      <div class="hg-period-tabs">
        <button id="hg-tab-w" class="active" onclick="setHomePeriod('w')">Semana</button>
        <button id="hg-tab-m" onclick="setHomePeriod('m')">Mês</button>
        <button id="hg-tab-y" onclick="setHomePeriod('y')">Ano</button>
      </div>
      <button class="hg-filter-btn" onclick="openHgFilter()"><svg viewBox="0 0 24 24"><path d="M3 6h18"/><path d="M7 12h10"/><path d="M10 18h4"/></svg></button>
    </div>
    <div class="hg-main-value">
      <label>Faturamento</label>
      <h1 id="hg-amount">R$ 0</h1>
      <p id="hg-sub">0 corridas · 0,0 km</p>
    </div>
    <div class="hg-insight" id="hg-insight" style="display:none">
      <svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink:0;opacity:.5;margin-top:2px"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
      <p id="hg-insight-txt"></p>
    </div>
    <div class="hg-metrics">
      <div class="hg-metric"><strong id="hg-rpkm">—</strong><span>R$/Km</span></div>
      <div class="hg-metric"><strong id="hg-rphr">—</strong><span>R$/Hora</span></div>
      <div class="hg-metric"><strong id="hg-trips">0</strong><span>Corridas</span></div>
      <div class="hg-metric"><strong id="hg-exp">R$ 0</strong><span>Despesas</span></div>
      <div class="hg-metric"><strong id="hg-profit">R$ 0</strong><span>Lucro</span></div>
      <div class="hg-metric"><strong id="hg-pct">0%</strong><span>% Lucro</span></div>
    </div>
    <div class="hg-goals" id="hg-goals"></div>
  </section>

  <section class="hg-chart-card" style="margin:14px 16px 0">
    <div class="hg-chart-head"><div><h2>Dia/Semana <button class="hg-info-btn" onclick="showChartInfo(this,'Soma por dia da semana, dentro do período selecionado acima.')">?</button></h2></div>
      <div class="hg-mini-tabs"><button class="active" id="hg-dia-d" onclick="setHgDiaMode('d')">Dia</button><button id="hg-dia-w" onclick="setHgDiaMode('w')">Semana</button></div>
    </div>
    <p class="hg-chart-highlight" id="hg-dia-insight"></p>
    <div class="hg-bar-chart" id="hg-chart-dia"></div>
  </section>

  <section class="hg-chart-card" style="margin:14px 16px 0">
    <div class="hg-chart-head"><div><h2>Hora/Turno <button class="hg-info-btn" onclick="showChartInfo(this,'Soma por horário, dentro do período selecionado acima.')">?</button></h2></div>
      <div class="hg-mini-tabs"><button class="active" id="hg-hora-h" onclick="setHgHoraMode('h')">Hora</button><button id="hg-hora-t" onclick="setHgHoraMode('t')">Turno</button></div>
    </div>
    <p class="hg-chart-highlight" id="hg-hora-insight"></p>
    <div class="hg-bar-chart hour" id="hg-chart-hora"></div>
  </section>

  <div class="ad-carousel" id="ad-carousel-1"></div>

  <section class="hg-chart-card" style="margin:14px 16px 0">
    <div class="hg-chart-head"><div><h2>Mês/Ano <button class="hg-info-btn" onclick="showChartInfo(this,'Soma por mês/ano, dentro do período selecionado acim
===== sessions @ 535308 =====
    <div>Selecione o arquivo <strong>smartmobi_historico_uber.json</strong> gerado pelo sistema. Corridas j&#225; existentes n&#227;o ser&#227;o duplicadas.</div>
    </div>
    <label style="display:flex;flex-direction:column;align-items:center;gap:10px;border:2px dashed #E2E8F0;border-radius:18px;padding:24px;cursor:pointer;background:#F8FAFC;margin-bottom:14px">
      <svg viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="#94A3B8" stroke-width="1.5"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M17 8 12 3 7 8"/><path d="M12 3v12"/></svg>
      <div style="font-size:14px;font-weight:700;color:#64748B">Selecionar arquivo .json</div>
      <div style="font-size:11px;color:#94A3B8">Toque para abrir o gerenciador de arquivos</div>
      <input type="file" accept=".json,application/json" style="display:none" onclick="this.value=null" onchange="loadHistFile(this)">
    </label>
    <div id="hist-progress" style="display:none">
      <div style="background:#F1F5F9;border-radius:10px;height:8px;overflow:hidden;margin-bottom:8px"><div id="hist-bar" style="height:100%;background:linear-gradient(90deg,#0EA5E9,#22C55E);border-radius:10px;width:0%;transition:width .3s"></div></div>
      <div style="font-size:12px;color:#64748B;text-align:center" id="hist-msg">Processando...</div>
    </div>
  </div>
</div>

<!-- MODAL: Notificações (placeholder) -->
<div class="mo ctr" id="mo-notif">
  <div class="sheet ctr" style="max-width:380px">
    <div class="sheet-head"><h2>Notificações</h2><button class="xbtn" onclick="closeMo('mo-notif')">&#x2715;</button></div>
    <div style="padding:20px;text-align:center;color:#94A3B8">
      <svg viewBox="0 0 24 24" width="40" height="40" fill="none" stroke="#CBD5E1" stroke-width="1.5" style="margin-bottom:12px"><path d="M18 8a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9"/><path d="M13.7 21a2 2 0 0 1-3.4 0"/></svg>
      <div style="font-size:14px;font-weight:600;color:#64748B;margin-bottom:6px">Nenhuma notificação</div>
      <div style="font-size:12px">Novidades do MōB aparecerão aqui</div>
    </div>
    <button class="btn-p" onclick="closeMo('mo-notif')" style="margin-top:8px">Fechar</button>
  </div>
</div>
<!-- MODAL: Categorias -->
<div class="screen" id="sc-categorias">
  <div style="display:flex;align-items:center;gap:10px;padding:14px 16px 6px">
    <button onclick="navTo('mais')" style="width:32px;height:32px;border-radius:10px;background:#F1F5F9;border:0;display:flex;align-items:center;justify-content:center;color:#0F172A;cursor:pointer;flex-shrink:0"><svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M19 12H5M12 19l-7-7 7-7"/></svg></button>
    <h2 style="font-size:17px;letter-spacing:-.02em;flex:1">Categorias</h2>
  </div>
  <div style="font-size:13px;color:#64748B;margin:0 16px 14px">Gerencie suas categorias de despesa e receita.</div>
  <div id="cats-groups" style="padding:0 16px 24px"></div>
</div>

<div class="mo" id="mo-obj-config">
  <div class="sheet">
    <div class="sheet-handle"></div>
    <div class="sheet-head" style="padding:0 0 12px"><h2>Configurar objetivos</h2><button class="xbtn" onclick="closeMo('mo-obj-config')">&#x2715;</button></div>
    <div class="obj-switch">
      <button class="obj-btn active" id="ocfg-rec" onclick="setObjCfgTab('rec')">Receitas</button>
      <button class="obj-btn" id="ocfg-desp" onclick="setObjCfgTab('desp')">Despesas</button>
    </div>
    <div id="objcfg-rec">
      <div class="type-switch2">
        <button class="type-btn2 active" id="ocfg-rec-operacional" onclick="setObjCfgGroup('rec','operacional')">Operacional</button>
        <button class="type-btn2" id="ocfg-rec-pessoal" onclick="setObjCfgGroup('rec','pessoal')">Pessoal</button>
      </div>
      <div id="objcfg-rec-rows"></div>
    </div>
    <div id="objcfg-desp" style="display:none">
      <div class="type-switch2">
        <button class="type-btn2 active" id="ocfg-desp-operacional" onclick="setObjCfgGroup('desp','operacional')">Operacional</button>
        <button class="type-btn2" id="ocfg-desp-pessoal" onclick="setObjCfgGroup('desp','pessoal')">Pessoal</button>
      </div>
      <div id="objcfg-desp-rows"></div>
    </div>
    <button class="btn-p" onclick="saveObjCfg()">Salvar objetivos</button>
  </div>
</div>


</div>
<script>
// ============================================================
// SmartMobi v20 — Camada de funcionalidade
// Design: GPT  |  JS/Dados: Claude
// ============================================================

// ── DATA LAYER ───────────────────────────────────────────────
const DB = {
  get: k => { try { return JSON.parse(localStorage.getItem(k)); } catch(e) { return null; } },
  set: (k,v) => localStorage.setItem(k, JSON.stringify(v)),
  del: k => localStorage.removeItem(k),
  day: d => { const x = DB.get('sm_day_'+d) || {date:d, trips:[], expenses:[], revenues:[], kmTotal:null, session:null}; if (!x.sessions) x.sessions = []; return x; },
  saveDay: d => DB.set('sm_day_'+d.date, d),
  allDates: () => Object.keys(localStorage).filter(k => k.startsWith('sm_day_')).map(k => k.slice(7)).sort(),
  events: () => DB.get('sm_events') || [],
  saveEvents: e => DB.set('sm_events', e),
  planJornada: () => DB.get('sm_plan_jornada') || [],
  savePlanJornada: p => DB.set('sm_plan_jornada', p),
  cfg: () => DB.get('sm_cfg') || {name:'', plate:'', vehicle:'', goal:0, favModule:'financeiro'},
  saveCfg: c => DB.set('sm_cfg', c),
  cats: t => DB.get('sm_cats_'+t) || DEFAULT_CATS[t],
  saveCats: (t,c) => DB.set('sm_cats_'+t, c),
  objGoals: t => DB.get('sm_obj_'+t) || {},
  saveObjGoals: (t,g) => DB.set('sm_obj_'+t, g),
  syncQueue: () => DB.get('sm_sync_queue') || [],
  saveSyncQueue: q => DB.set('sm_sync_queue', q),
  meta: () => DB.get('sm_meta') || {type:'monthly', general:9000, varyByDay:false, days:{}},
  saveMeta: m => DB.set('sm_meta', m),
  uid: () => Date.now().toString(36) + Math.random().toString(36).slice(2,5),
  today: () => { const d=new Date(); return d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate()); },
  fR: v => 'R$ ' + Number(v||0).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2}),
  fRI: v => 'R$ ' + Number(v||0).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2}),
};
function p2(n) { return String(n).padStart(2,'0'); }
// Formata duração em minutos: "5m", "32m", "1h05m" (sem zero à esquerda quando é só minutos).
function fmtDur(totalMin) {
  const min = Math.max(0, Math.round(totalMin || 0));
  const h = Math.floor(min / 60), m = min % 60;
  return (h > 0 ? h + 'h' + p2(m) : String(m)) + 'm';
}

// Detalhamento Online/Buscando passageiro/Em corrida — puxado direto da
// máquina de estados do APK (accepted_at/pickup_started_at/trip_started_at/
// trip_ended_at, gravados em auto_trips a cada transição real de tela).
// BUSCANDO = trip_started_at - pickup_started_at (deslocamento até o
// passageiro); EM CORRIDA = trip_ended_at - trip_started_at; ONLINE = todo
// o resto do tempo da sessão (esperando oferta / avaliando oferta). Só usa
// corridas automáticas (autoTrips) — corridas importadas manualmente não
// têm esses timestamps porque não vieram da leitura de tela em tempo real.
function computeStatusBreakdown(elapsedSec, kmTotal, autoTrips) {
  let corridaSec = 0, buscandoSec = 0, corridaKm = 0, buscandoKm = 0;
  (autoTrips || []).forEach(t => {
    if (t.trip_started_at && t.trip_ended_at) {
      const s = (new Date(t.trip_ended_at) - new Date(t.trip_started_at)) / 1000;
      if (s > 0) corridaSec += s;
      corridaKm += parseFloat(t.real_km_trip ?? t.offer_km_trip) || 0;
    }
    if (t.pickup_started_at && t.trip_started_at) {
      const s = (new Date(t.trip_started_at) - new Date(t.pickup_started_at)) / 1000;
      if (s > 0) buscandoSec += s;
      buscandoKm += parseFloat(t.real_km_pickup ?? t.offer_km_pickup) || 0;
    }
  });
  const total = Math.max(0, elapsedSec || 0);
  const onlineSec = Math.max(0, total - corridaSec - buscandoSec);
  const onlineKm = Math.max(0, (kmTotal || 0) - corridaKm - buscandoKm);
  const pct = s => total > 0 ? Math.round(s / total * 100) : 0;
  return {
    total,
    online:   { sec: onlineSec,   km: onlineKm,   pct: pct(onlineSec) },
    buscando: { sec: buscandoSec, km: buscandoKm, pct: pct(buscandoSec) },
    corrida:  { sec: corridaSec,  km: corridaKm,  pct: pct(corridaSec) },
  };
}

const STATUS_SEGMENTS = [
  { key: 'online',   label: 'ONLINE',              color: '#22C55E' },
  { key: 'buscando', label: 'BUSCANDO PASSAGEIRO',  color: '#F97316' },
  { key: 'corrida',  label: 'EM CORRIDA',           color: '#3B82F6' },
];

function buildSbarHtml(bd) {
  return STATUS_SEGMENTS.map(s => {
    const pct = bd[s.key].pct;
    if (pct <= 0) return '';
    const showText = pct >= 12;
    return `<i style="width:${pct}%;background:${s.color}">${showText ? pct+'%' : ''}</i>`;
  }).join('');
}

function buildSrowsHtml(bd) {
  return STATUS_SEGMENTS.map(s => {
    const seg = bd[s.key];
    return `<div class="srow"><span class="sdot" style="background:${s.color}"></span><b>${s.label}</b><span class="pct">${seg.pct}%</span><span class="stempo">${fmtDur(seg.sec/60)}</span><span class="skm">${seg.km.toFixed(1)} km</span></div>`;
  }).join('');
}

function toggleBar(listId) {
  const el = document.getElementById(listId);
  if (el) el.classList.toggle('open');
}

// ── DEFAULT CATEGORIES ────────────────────────────────────────
const DEFAULT_CATS = {
  despesa_operacional: [
    {id:'comb', l:'Combustível', icon:'fuel'},
    {id:'alim', l:'Alimentação', icon:'food'},
    {id:'man',  l:'Manutenção', icon:'wrench'},
    {id:'park', l:'Estacionamento', icon:'parking'},
    {id:'ped',  l:'Pedágio', icon:'road'},
    {id:'lav',  l:'Lavagem', icon:'car'},
    {id:'seg',  l:'Seguro', icon:'card'},
    {id:'out',  l:'Outros', icon:'plus'}
  ],
  despesa_pessoal: [
    {id:'alug', l:'Aluguel', icon:'home'},
    {id:'merc', l:'Mercado', icon:'card'},
    {id:'alim', l:'Alimentação', icon:'food'},
    {id:'cart', l:'Cartão', icon:'card'},
    {id:'trans', l:'Transporte', icon:'car'},
    {id:'saude', l:'Saúde', icon:'plus'},
    {id:'lazer', l:'Lazer', icon:'gift'},
    {id:'out',  l:'Outros', icon:'plus'}
  ],
  receita_operacional: [
    {id:'gort', l:'Gorjeta', icon:'coin'},
    {id:'bon',  l:'Bônus', icon:'gift'},
    {id:'reemb', l:'Reembolso', icon:'card'},
    {id:'serv', l:'Serviço extra', icon:'briefcase'},
    {id:'part', l:'Particular', icon:'user'},
    {id:'inc',  l:'Incentivo', icon:'plus'}
  ],
  receita_pessoal: [
    {id:'sal',  l:'Salário', icon:'briefcase'},
    {id:'rend', l:'Renda extra', icon:'coin'},
    {id:'pix',  l:'Pix recebido', icon:'card'},
    {id:'reemb', l:'Reembolso', icon:'card'},
    {id:'pres', l:'Presente', icon:'gift'},
    {id:'out',  l:'Outros', icon:'plus'}
  ]
};

// ── GLOBAL STATE ──────────────────────────────────────────────
let S = {
  sec: 'home',
  homePeriod: 'w',
  hgCustomStart: null, hgCustomEnd: null,
  hgDiaMode: 'd', hgHoraMode: 'h', hgMesMode: 'm', hgRevDesp: 'receita',
  finPer: 'month',
  finScope: 'all',
  lancFilter: 'all',
  lancStatus: 'all',
  lancPeriod: '7d',
  lancDate: null,
  curDate: DB.today(),
  lancType: 'receita',
  lancScope: 'operacional',
  lancRecFreq: 'Semanal',
  corPlat: 'uber',
  corridasPeriod: 'hoje', corridasDate: null,
  corridasPlat: 'all',
  tripsExpanded: false,
  sessionsExpanded: false,
  lancExpanded: false,
  _midnightIsPostStop: false,
  _postStopMidnightSess: null,
  _postMidnightCallback: null,
  _midnightModalOpen: false,
  gps: {
    st: 'idle',  // idle | running
    start: null, pausedMs: 0, pauseAt: null,
    km: 0, pts: [], wid: null, ti: null,
    forceDate: null // override manual (opção 'deixar em ontem' na virada de meia-noite)
  }
};

// ── MODULES (for Mais screen) ─────────────────────────────────
const MODULES = [
  {id:'financeiro', label:'Financeiro', sub:'Gestão e análise financeira', color:'blue',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 3v18M17 7H9.5a3.5 3.5 0 0 0 0 7H14a3.5 3.5 0 0 1 0 7H6"/></svg>'},
  {id:'planejar-jornada', label:'Planejar jornada', sub:'Defina dias, metas e horários de trabalho', color:'green',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18M8 14h.01M12 14h.01M16 14h.01M8 18h.01M12 18h.01"/></svg>'},
  {id:'lancamentos', label:'Lançamentos', sub:'Histórico de receitas e despesas', color:'orange',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 19V5a2 2 0 0 1 2-2h12v18H6a2 2 0 0 1-2-2Z"/><path d="M8 7h8M8 11h8M8 15h5"/></svg>'},
];

const MODULE_COLORS = {blue:'#EFF6FF', purple:'#F5F3FF', green:'#ECFDF3', orange:'#FFF7ED'};
const MODULE_ICONS_COLOR = {blue:'#0A2F6B', purple:'#7C3AED', green:'#22C55E', orange:'#F59E0B'};

// ── NAVIGATION ────────────────────────────────────────────────
function navTo(sec) {
  // Ocultar/mostrar nav na tela de Informes (o painel usa z-index alto mas o cat-row precisa da nav escondida)
  const bottomNav = document.querySelector('.bottom');
  if (bottomNav) bottomNav.style.display = 'flex';
  document.querySelectorAll('.screen').forEach(s => {
    s.classList.remove('on');
    s.style.display = '';  // limpa inline style para CSS funcionar
  });
  document.querySelectorAll('.nav').forEach(n => n.classList.remove('active'));
  const sc = document.getElementById('sc-'+sec);
  if (sc) { sc.classList.add('on'); sc.style.display = 'block'; }
  // Map sub-screens to their nav parent
  const navMap = {financeiro:'fav', lancamentos:'fav', relatorios:'fav', 'planejar-jornada':'mais', corridas:'jornada', agenda:'mais', informes:'agenda', categorias:'mais'};
  const navId = navMap[sec] || sec;
  const bn = document.getElementById('bn-'+navId);
  if (bn) bn.classList.add('active');
  S.sec = sec;
  try { localStorage.setItem('sm_last_screen', JSON.stringify({sec, ts: Date.now()})); } catch(e) {}
  const ms = document.getElementById('main-scroll');
  if (ms) ms.scrollTop = 0;
  if (sec === 'home')       renderFeedHome();
  if (sec === 'relatorios') renderHome();
  if (sec === 'jornada') {
    renderJornada(); // render imediato com dado local (nunca trava esperando rede)
    updateNetStatus();
    if (_supaUser && navigator.onLine) {
      reconcileJornadaHoje().then(() => renderJornada()).catch(()=>{});
      // Se o app local acha que está idle mas existe jornada aberta no
      // Supabase há mais de 15min (provável órfã), fecha automaticamente
      if (S.gps.st === 'idle') {
        _closeOrphanSessions().then(n => { if (n) renderJornada(); }).catch(()=>{});
      }
    }
  }
  if (sec === 'financeiro') renderFin();
  if (sec === 'lancamentos') renderLancamentos();
  if (sec ==
===== sessions @ 541768 =====
m zero à esquerda quando é só minutos).
function fmtDur(totalMin) {
  const min = Math.max(0, Math.round(totalMin || 0));
  const h = Math.floor(min / 60), m = min % 60;
  return (h > 0 ? h + 'h' + p2(m) : String(m)) + 'm';
}

// Detalhamento Online/Buscando passageiro/Em corrida — puxado direto da
// máquina de estados do APK (accepted_at/pickup_started_at/trip_started_at/
// trip_ended_at, gravados em auto_trips a cada transição real de tela).
// BUSCANDO = trip_started_at - pickup_started_at (deslocamento até o
// passageiro); EM CORRIDA = trip_ended_at - trip_started_at; ONLINE = todo
// o resto do tempo da sessão (esperando oferta / avaliando oferta). Só usa
// corridas automáticas (autoTrips) — corridas importadas manualmente não
// têm esses timestamps porque não vieram da leitura de tela em tempo real.
function computeStatusBreakdown(elapsedSec, kmTotal, autoTrips) {
  let corridaSec = 0, buscandoSec = 0, corridaKm = 0, buscandoKm = 0;
  (autoTrips || []).forEach(t => {
    if (t.trip_started_at && t.trip_ended_at) {
      const s = (new Date(t.trip_ended_at) - new Date(t.trip_started_at)) / 1000;
      if (s > 0) corridaSec += s;
      corridaKm += parseFloat(t.real_km_trip ?? t.offer_km_trip) || 0;
    }
    if (t.pickup_started_at && t.trip_started_at) {
      const s = (new Date(t.trip_started_at) - new Date(t.pickup_started_at)) / 1000;
      if (s > 0) buscandoSec += s;
      buscandoKm += parseFloat(t.real_km_pickup ?? t.offer_km_pickup) || 0;
    }
  });
  const total = Math.max(0, elapsedSec || 0);
  const onlineSec = Math.max(0, total - corridaSec - buscandoSec);
  const onlineKm = Math.max(0, (kmTotal || 0) - corridaKm - buscandoKm);
  const pct = s => total > 0 ? Math.round(s / total * 100) : 0;
  return {
    total,
    online:   { sec: onlineSec,   km: onlineKm,   pct: pct(onlineSec) },
    buscando: { sec: buscandoSec, km: buscandoKm, pct: pct(buscandoSec) },
    corrida:  { sec: corridaSec,  km: corridaKm,  pct: pct(corridaSec) },
  };
}

const STATUS_SEGMENTS = [
  { key: 'online',   label: 'ONLINE',              color: '#22C55E' },
  { key: 'buscando', label: 'BUSCANDO PASSAGEIRO',  color: '#F97316' },
  { key: 'corrida',  label: 'EM CORRIDA',           color: '#3B82F6' },
];

function buildSbarHtml(bd) {
  return STATUS_SEGMENTS.map(s => {
    const pct = bd[s.key].pct;
    if (pct <= 0) return '';
    const showText = pct >= 12;
    return `<i style="width:${pct}%;background:${s.color}">${showText ? pct+'%' : ''}</i>`;
  }).join('');
}

function buildSrowsHtml(bd) {
  return STATUS_SEGMENTS.map(s => {
    const seg = bd[s.key];
    return `<div class="srow"><span class="sdot" style="background:${s.color}"></span><b>${s.label}</b><span class="pct">${seg.pct}%</span><span class="stempo">${fmtDur(seg.sec/60)}</span><span class="skm">${seg.km.toFixed(1)} km</span></div>`;
  }).join('');
}

function toggleBar(listId) {
  const el = document.getElementById(listId);
  if (el) el.classList.toggle('open');
}

// ── DEFAULT CATEGORIES ────────────────────────────────────────
const DEFAULT_CATS = {
  despesa_operacional: [
    {id:'comb', l:'Combustível', icon:'fuel'},
    {id:'alim', l:'Alimentação', icon:'food'},
    {id:'man',  l:'Manutenção', icon:'wrench'},
    {id:'park', l:'Estacionamento', icon:'parking'},
    {id:'ped',  l:'Pedágio', icon:'road'},
    {id:'lav',  l:'Lavagem', icon:'car'},
    {id:'seg',  l:'Seguro', icon:'card'},
    {id:'out',  l:'Outros', icon:'plus'}
  ],
  despesa_pessoal: [
    {id:'alug', l:'Aluguel', icon:'home'},
    {id:'merc', l:'Mercado', icon:'card'},
    {id:'alim', l:'Alimentação', icon:'food'},
    {id:'cart', l:'Cartão', icon:'card'},
    {id:'trans', l:'Transporte', icon:'car'},
    {id:'saude', l:'Saúde', icon:'plus'},
    {id:'lazer', l:'Lazer', icon:'gift'},
    {id:'out',  l:'Outros', icon:'plus'}
  ],
  receita_operacional: [
    {id:'gort', l:'Gorjeta', icon:'coin'},
    {id:'bon',  l:'Bônus', icon:'gift'},
    {id:'reemb', l:'Reembolso', icon:'card'},
    {id:'serv', l:'Serviço extra', icon:'briefcase'},
    {id:'part', l:'Particular', icon:'user'},
    {id:'inc',  l:'Incentivo', icon:'plus'}
  ],
  receita_pessoal: [
    {id:'sal',  l:'Salário', icon:'briefcase'},
    {id:'rend', l:'Renda extra', icon:'coin'},
    {id:'pix',  l:'Pix recebido', icon:'card'},
    {id:'reemb', l:'Reembolso', icon:'card'},
    {id:'pres', l:'Presente', icon:'gift'},
    {id:'out',  l:'Outros', icon:'plus'}
  ]
};

// ── GLOBAL STATE ──────────────────────────────────────────────
let S = {
  sec: 'home',
  homePeriod: 'w',
  hgCustomStart: null, hgCustomEnd: null,
  hgDiaMode: 'd', hgHoraMode: 'h', hgMesMode: 'm', hgRevDesp: 'receita',
  finPer: 'month',
  finScope: 'all',
  lancFilter: 'all',
  lancStatus: 'all',
  lancPeriod: '7d',
  lancDate: null,
  curDate: DB.today(),
  lancType: 'receita',
  lancScope: 'operacional',
  lancRecFreq: 'Semanal',
  corPlat: 'uber',
  corridasPeriod: 'hoje', corridasDate: null,
  corridasPlat: 'all',
  tripsExpanded: false,
  sessionsExpanded: false,
  lancExpanded: false,
  _midnightIsPostStop: false,
  _postStopMidnightSess: null,
  _postMidnightCallback: null,
  _midnightModalOpen: false,
  gps: {
    st: 'idle',  // idle | running
    start: null, pausedMs: 0, pauseAt: null,
    km: 0, pts: [], wid: null, ti: null,
    forceDate: null // override manual (opção 'deixar em ontem' na virada de meia-noite)
  }
};

// ── MODULES (for Mais screen) ─────────────────────────────────
const MODULES = [
  {id:'financeiro', label:'Financeiro', sub:'Gestão e análise financeira', color:'blue',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 3v18M17 7H9.5a3.5 3.5 0 0 0 0 7H14a3.5 3.5 0 0 1 0 7H6"/></svg>'},
  {id:'planejar-jornada', label:'Planejar jornada', sub:'Defina dias, metas e horários de trabalho', color:'green',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18M8 14h.01M12 14h.01M16 14h.01M8 18h.01M12 18h.01"/></svg>'},
  {id:'lancamentos', label:'Lançamentos', sub:'Histórico de receitas e despesas', color:'orange',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 19V5a2 2 0 0 1 2-2h12v18H6a2 2 0 0 1-2-2Z"/><path d="M8 7h8M8 11h8M8 15h5"/></svg>'},
];

const MODULE_COLORS = {blue:'#EFF6FF', purple:'#F5F3FF', green:'#ECFDF3', orange:'#FFF7ED'};
const MODULE_ICONS_COLOR = {blue:'#0A2F6B', purple:'#7C3AED', green:'#22C55E', orange:'#F59E0B'};

// ── NAVIGATION ────────────────────────────────────────────────
function navTo(sec) {
  // Ocultar/mostrar nav na tela de Informes (o painel usa z-index alto mas o cat-row precisa da nav escondida)
  const bottomNav = document.querySelector('.bottom');
  if (bottomNav) bottomNav.style.display = 'flex';
  document.querySelectorAll('.screen').forEach(s => {
    s.classList.remove('on');
    s.style.display = '';  // limpa inline style para CSS funcionar
  });
  document.querySelectorAll('.nav').forEach(n => n.classList.remove('active'));
  const sc = document.getElementById('sc-'+sec);
  if (sc) { sc.classList.add('on'); sc.style.display = 'block'; }
  // Map sub-screens to their nav parent
  const navMap = {financeiro:'fav', lancamentos:'fav', relatorios:'fav', 'planejar-jornada':'mais', corridas:'jornada', agenda:'mais', informes:'agenda', categorias:'mais'};
  const navId = navMap[sec] || sec;
  const bn = document.getElementById('bn-'+navId);
  if (bn) bn.classList.add('active');
  S.sec = sec;
  try { localStorage.setItem('sm_last_screen', JSON.stringify({sec, ts: Date.now()})); } catch(e) {}
  const ms = document.getElementById('main-scroll');
  if (ms) ms.scrollTop = 0;
  if (sec === 'home')       renderFeedHome();
  if (sec === 'relatorios') renderHome();
  if (sec === 'jornada') {
    renderJornada(); // render imediato com dado local (nunca trava esperando rede)
    updateNetStatus();
    if (_supaUser && navigator.onLine) {
      reconcileJornadaHoje().then(() => renderJornada()).catch(()=>{});
      // Se o app local acha que está idle mas existe jornada aberta no
      // Supabase há mais de 15min (provável órfã), fecha automaticamente
      if (S.gps.st === 'idle') {
        _closeOrphanSessions().then(n => { if (n) renderJornada(); }).catch(()=>{});
      }
    }
  }
  if (sec === 'financeiro') renderFin();
  if (sec === 'lancamentos') renderLancamentos();
  if (sec === 'corridas') {
    S.corridasPlats = new Set(); // reseta sempre — nenhum filtro marcado = mostra tudo
    document.getElementById('cor-plat-uber')?.classList.remove('active');
    document.getElementById('cor-plat-99')?.classList.remove('active');
    renderCorridasActive();
  }
  if (sec === 'mais')       renderMais();
  if (sec === 'categorias') renderCatsModal();
  if (sec === 'agenda')     { renderAgenda(); agScrollToFirst(); }
  if (sec === 'informes')   renderInformes();
}
function openSuporte() {
  const el = document.getElementById('suporte-version');
  if (el) {
    const v = window.SmartMobiNative?.getVersion?.();
    el.textContent = v ? ('App nativo v' + v) : 'Acessando pelo navegador (sem app nativo)';
  }
  openMo('mo-suporte');
}
function navToFav() {
  const cfg = DB.cfg();
  navTo(cfg.favModule || 'financeiro');
}
function openMo(id) { const el = document.getElementById(id); if (el) el.classList.add('on'); }
function closeMo(id) { const el = document.getElementById(id); if (el) el.classList.remove('on'); }
document.addEventListener('click', e => {
  if (e.target.classList.contains('mo')) e.target.classList.remove('on');
});

// ── MAIS SCREEN ───────────────────────────────────────────────
function renderMais() {
  const vEl = document.getElementById("app-code-ver");
  if (vEl) vEl.textContent = typeof _APP_CODE_VER !== "undefined" ? _APP_CODE_VER : "?";

  const cfg = DB.cfg();
  const favId = cfg.favModule || 'financeiro';
  const container = document.getElementById('mais-modules');
  if (!container) return;
  container.innerHTML = MODULES.map(m => `
    <div style="background:#fff;border-radius:20px;border:1px solid #E2E8F0;box-shadow:0 2px 10px rgba(15,23,42,.05);padding:14px 16px;display:flex;align-items:center;gap:12px;cursor:pointer" onclick="navTo('${m.id}')">
      <div style="width:42px;height:42px;border-radius:14px;background:${MODULE_COLORS[m.color]};display:flex;align-items:center;justify-content:center;color:${MODULE_ICONS_COLOR[m.color]};flex-shrink:0">${m.icon}</div>
      <div style="flex:1"><div style="font-size:14px;font-weight:700">${m.label}</div><div style="font-size:12px;color:#64748B;margin-top:2px">${m.sub}</div></div>
      <button onclick="event.stopPropagation();setFavModule('${m.id}')" style="border:none;background:${m.id===favId?MODULE_COLORS[m.color]:'transparent'};border-radius:8px;padding:6px;cursor:pointer;color:${m.id===favId?MODULE_ICONS_COLOR[m.color]:'#94A3B8'};display:flex;align-items:center;justify-content:center" title="Fixar no menu">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="${m.id===favId?'currentColor':'none'}" stroke="currentColor" stroke-width="2"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
      </button>
    </div>`
  ).join('');
  // Update perfil sub
  const sub = document.getElementById('mais-perfil-sub');
  if (sub && cfg.name) sub.textContent = cfg.name + (cfg.vehicle ? ' · ' + cfg.vehicle : '');
  const custoSub = document.getElementById('mais-custo-sub');
  if (custoSub) {
    custoSub.textContent = cfg.custoVeiculo?.configurado
      ? DB.fRI(cfg.custoVeiculo.custoPorKm) + '/km configurado'
      : 'Configure pra ver o lucro líquido real';
  }
}
function setFavModule(id) {
  const cfg = DB.cfg(); cfg.favModule = id; DB.saveCfg(cfg);
  updateFavBtn();
  renderMais();
}
function updateFavBtn() {
  const cfg = DB.cfg();
  const mod = MODULES.find(m => m.id === (cfg.favModule || 'financeiro')) || MODULES[0];
  const lblEl = document.getElementById('fav-lbl');
  const icoEl = document.getElementById('fav-icon');
  if (lblEl) lblEl.textContent = mod.label;
  if (icoEl) icoEl.outerHTML = mod.icon.replace('<svg', '<svg id="fav-icon"');
}

// ── HOME ──────────────────────────────────────────────────────
function setHomePeriod(p) {
  S.homePeriod = p;
  ['w','m','y'].forEach(x => {
    const el = document.getElementById('hg-tab-'+x);
    if (el) el.classList.toggle('active', x === p);
  });
  renderHomeGraphs();
}
function openHgFilter() {
  const sEl = document.getElementById('hg-filter-start'), eEl = document.getElementById('hg-filter-end');
  if (sEl) sEl.value = S.hgCustomStart || DB.today();
  if (eEl) eEl.value = S.hgCustomEnd || DB.today();
  openMo('mo-hg-filter');
}
function applyHgFilter() {
  const s = document.getElementById('hg-filter-start')?.value;
  const e = document.getElementById('hg-filter-end')?.value;
  if (!s || !e || s > e) { showToast('Escolha um intervalo de datas válido', 'error'); return; }
  S.hgCustomStart = s; S.hgCustomEnd = e; S.homePeriod = 'custom';
  ['w','m','y'].forEach(x => { const el = document.getElementById('hg-tab-'+x); if (el) el.classList.toggle('active', false); });
  closeMo('mo-hg-filter');
  renderHomeGraphs();
}
function getHomeDays() {
  const today = new Date();
  const fmt = d => d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
  const p = S.homePeriod;
  if (p === 'custom' && S.hgCustomStart && S.hgCustomEnd) {
    const days = [];
    for (let d = new Date(S.hgCustomStart+'T12:00'); fmt(d) <= S.hgCustomEnd; d.setDate(d.getDate()+1)) days.push(fmt(new Date(d)));
    return days;
  }
  if (p === 'w') {
    const dow = (today.getDay() + 6) % 7;
    return Array.from({length: dow+1}, (_,i) => { const d=new Date(today); d.setDate(today.getDate()-dow+i); return fmt(d); });
  }
  if (p === 'm') {
    return Array.from({length: today.getDate()}, (_,i) => {
      return today.getFullYear()+'-'+p2(today.getMonth()+1)+'-'+p2(i+1);
    });
  }
  // year: all days of current year up to today
  const start = new Date(today.getFullYear(), 0, 1);
  const days = [];
  for (let d = new Date(start); d <= today; d.setDate(d.getDate()+1)) days.push(fmt(new Date(d)));
  return days;
}
function getHomeDaysPrev() {
  const today = new Date();
  const fmt = d => d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
  const p = S.homePeriod;
  if (p === 'w') {
    const dow = (today.getDay() + 6) % 7; // dias decorridos nesta semana (0=seg)
    return Array.from({length: dow+1}, (_,i) => { const d=new Date(today); d.setDate(today.getDate()-dow-7+i); return fmt(d); });
  }
  if (p === 'm') {
    const prevMonth = new Date(today.getFullYear(), today.getMonth()-1, 1);
    const dim = new Date(prevMonth.getFullYear(), prevMonth.getMonth()+1, 0).getDate();
    const n = Math.min(today.getDate(), dim);
    return Array.from({length:n}, (
===== sessions @ 545122 =====
avagem', icon:'car'},
    {id:'seg',  l:'Seguro', icon:'card'},
    {id:'out',  l:'Outros', icon:'plus'}
  ],
  despesa_pessoal: [
    {id:'alug', l:'Aluguel', icon:'home'},
    {id:'merc', l:'Mercado', icon:'card'},
    {id:'alim', l:'Alimentação', icon:'food'},
    {id:'cart', l:'Cartão', icon:'card'},
    {id:'trans', l:'Transporte', icon:'car'},
    {id:'saude', l:'Saúde', icon:'plus'},
    {id:'lazer', l:'Lazer', icon:'gift'},
    {id:'out',  l:'Outros', icon:'plus'}
  ],
  receita_operacional: [
    {id:'gort', l:'Gorjeta', icon:'coin'},
    {id:'bon',  l:'Bônus', icon:'gift'},
    {id:'reemb', l:'Reembolso', icon:'card'},
    {id:'serv', l:'Serviço extra', icon:'briefcase'},
    {id:'part', l:'Particular', icon:'user'},
    {id:'inc',  l:'Incentivo', icon:'plus'}
  ],
  receita_pessoal: [
    {id:'sal',  l:'Salário', icon:'briefcase'},
    {id:'rend', l:'Renda extra', icon:'coin'},
    {id:'pix',  l:'Pix recebido', icon:'card'},
    {id:'reemb', l:'Reembolso', icon:'card'},
    {id:'pres', l:'Presente', icon:'gift'},
    {id:'out',  l:'Outros', icon:'plus'}
  ]
};

// ── GLOBAL STATE ──────────────────────────────────────────────
let S = {
  sec: 'home',
  homePeriod: 'w',
  hgCustomStart: null, hgCustomEnd: null,
  hgDiaMode: 'd', hgHoraMode: 'h', hgMesMode: 'm', hgRevDesp: 'receita',
  finPer: 'month',
  finScope: 'all',
  lancFilter: 'all',
  lancStatus: 'all',
  lancPeriod: '7d',
  lancDate: null,
  curDate: DB.today(),
  lancType: 'receita',
  lancScope: 'operacional',
  lancRecFreq: 'Semanal',
  corPlat: 'uber',
  corridasPeriod: 'hoje', corridasDate: null,
  corridasPlat: 'all',
  tripsExpanded: false,
  sessionsExpanded: false,
  lancExpanded: false,
  _midnightIsPostStop: false,
  _postStopMidnightSess: null,
  _postMidnightCallback: null,
  _midnightModalOpen: false,
  gps: {
    st: 'idle',  // idle | running
    start: null, pausedMs: 0, pauseAt: null,
    km: 0, pts: [], wid: null, ti: null,
    forceDate: null // override manual (opção 'deixar em ontem' na virada de meia-noite)
  }
};

// ── MODULES (for Mais screen) ─────────────────────────────────
const MODULES = [
  {id:'financeiro', label:'Financeiro', sub:'Gestão e análise financeira', color:'blue',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 3v18M17 7H9.5a3.5 3.5 0 0 0 0 7H14a3.5 3.5 0 0 1 0 7H6"/></svg>'},
  {id:'planejar-jornada', label:'Planejar jornada', sub:'Defina dias, metas e horários de trabalho', color:'green',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18M8 14h.01M12 14h.01M16 14h.01M8 18h.01M12 18h.01"/></svg>'},
  {id:'lancamentos', label:'Lançamentos', sub:'Histórico de receitas e despesas', color:'orange',
    icon:'<svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 19V5a2 2 0 0 1 2-2h12v18H6a2 2 0 0 1-2-2Z"/><path d="M8 7h8M8 11h8M8 15h5"/></svg>'},
];

const MODULE_COLORS = {blue:'#EFF6FF', purple:'#F5F3FF', green:'#ECFDF3', orange:'#FFF7ED'};
const MODULE_ICONS_COLOR = {blue:'#0A2F6B', purple:'#7C3AED', green:'#22C55E', orange:'#F59E0B'};

// ── NAVIGATION ────────────────────────────────────────────────
function navTo(sec) {
  // Ocultar/mostrar nav na tela de Informes (o painel usa z-index alto mas o cat-row precisa da nav escondida)
  const bottomNav = document.querySelector('.bottom');
  if (bottomNav) bottomNav.style.display = 'flex';
  document.querySelectorAll('.screen').forEach(s => {
    s.classList.remove('on');
    s.style.display = '';  // limpa inline style para CSS funcionar
  });
  document.querySelectorAll('.nav').forEach(n => n.classList.remove('active'));
  const sc = document.getElementById('sc-'+sec);
  if (sc) { sc.classList.add('on'); sc.style.display = 'block'; }
  // Map sub-screens to their nav parent
  const navMap = {financeiro:'fav', lancamentos:'fav', relatorios:'fav', 'planejar-jornada':'mais', corridas:'jornada', agenda:'mais', informes:'agenda', categorias:'mais'};
  const navId = navMap[sec] || sec;
  const bn = document.getElementById('bn-'+navId);
  if (bn) bn.classList.add('active');
  S.sec = sec;
  try { localStorage.setItem('sm_last_screen', JSON.stringify({sec, ts: Date.now()})); } catch(e) {}
  const ms = document.getElementById('main-scroll');
  if (ms) ms.scrollTop = 0;
  if (sec === 'home')       renderFeedHome();
  if (sec === 'relatorios') renderHome();
  if (sec === 'jornada') {
    renderJornada(); // render imediato com dado local (nunca trava esperando rede)
    updateNetStatus();
    if (_supaUser && navigator.onLine) {
      reconcileJornadaHoje().then(() => renderJornada()).catch(()=>{});
      // Se o app local acha que está idle mas existe jornada aberta no
      // Supabase há mais de 15min (provável órfã), fecha automaticamente
      if (S.gps.st === 'idle') {
        _closeOrphanSessions().then(n => { if (n) renderJornada(); }).catch(()=>{});
      }
    }
  }
  if (sec === 'financeiro') renderFin();
  if (sec === 'lancamentos') renderLancamentos();
  if (sec === 'corridas') {
    S.corridasPlats = new Set(); // reseta sempre — nenhum filtro marcado = mostra tudo
    document.getElementById('cor-plat-uber')?.classList.remove('active');
    document.getElementById('cor-plat-99')?.classList.remove('active');
    renderCorridasActive();
  }
  if (sec === 'mais')       renderMais();
  if (sec === 'categorias') renderCatsModal();
  if (sec === 'agenda')     { renderAgenda(); agScrollToFirst(); }
  if (sec === 'informes')   renderInformes();
}
function openSuporte() {
  const el = document.getElementById('suporte-version');
  if (el) {
    const v = window.SmartMobiNative?.getVersion?.();
    el.textContent = v ? ('App nativo v' + v) : 'Acessando pelo navegador (sem app nativo)';
  }
  openMo('mo-suporte');
}
function navToFav() {
  const cfg = DB.cfg();
  navTo(cfg.favModule || 'financeiro');
}
function openMo(id) { const el = document.getElementById(id); if (el) el.classList.add('on'); }
function closeMo(id) { const el = document.getElementById(id); if (el) el.classList.remove('on'); }
document.addEventListener('click', e => {
  if (e.target.classList.contains('mo')) e.target.classList.remove('on');
});

// ── MAIS SCREEN ───────────────────────────────────────────────
function renderMais() {
  const vEl = document.getElementById("app-code-ver");
  if (vEl) vEl.textContent = typeof _APP_CODE_VER !== "undefined" ? _APP_CODE_VER : "?";

  const cfg = DB.cfg();
  const favId = cfg.favModule || 'financeiro';
  const container = document.getElementById('mais-modules');
  if (!container) return;
  container.innerHTML = MODULES.map(m => `
    <div style="background:#fff;border-radius:20px;border:1px solid #E2E8F0;box-shadow:0 2px 10px rgba(15,23,42,.05);padding:14px 16px;display:flex;align-items:center;gap:12px;cursor:pointer" onclick="navTo('${m.id}')">
      <div style="width:42px;height:42px;border-radius:14px;background:${MODULE_COLORS[m.color]};display:flex;align-items:center;justify-content:center;color:${MODULE_ICONS_COLOR[m.color]};flex-shrink:0">${m.icon}</div>
      <div style="flex:1"><div style="font-size:14px;font-weight:700">${m.label}</div><div style="font-size:12px;color:#64748B;margin-top:2px">${m.sub}</div></div>
      <button onclick="event.stopPropagation();setFavModule('${m.id}')" style="border:none;background:${m.id===favId?MODULE_COLORS[m.color]:'transparent'};border-radius:8px;padding:6px;cursor:pointer;color:${m.id===favId?MODULE_ICONS_COLOR[m.color]:'#94A3B8'};display:flex;align-items:center;justify-content:center" title="Fixar no menu">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="${m.id===favId?'currentColor':'none'}" stroke="currentColor" stroke-width="2"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
      </button>
    </div>`
  ).join('');
  // Update perfil sub
  const sub = document.getElementById('mais-perfil-sub');
  if (sub && cfg.name) sub.textContent = cfg.name + (cfg.vehicle ? ' · ' + cfg.vehicle : '');
  const custoSub = document.getElementById('mais-custo-sub');
  if (custoSub) {
    custoSub.textContent = cfg.custoVeiculo?.configurado
      ? DB.fRI(cfg.custoVeiculo.custoPorKm) + '/km configurado'
      : 'Configure pra ver o lucro líquido real';
  }
}
function setFavModule(id) {
  const cfg = DB.cfg(); cfg.favModule = id; DB.saveCfg(cfg);
  updateFavBtn();
  renderMais();
}
function updateFavBtn() {
  const cfg = DB.cfg();
  const mod = MODULES.find(m => m.id === (cfg.favModule || 'financeiro')) || MODULES[0];
  const lblEl = document.getElementById('fav-lbl');
  const icoEl = document.getElementById('fav-icon');
  if (lblEl) lblEl.textContent = mod.label;
  if (icoEl) icoEl.outerHTML = mod.icon.replace('<svg', '<svg id="fav-icon"');
}

// ── HOME ──────────────────────────────────────────────────────
function setHomePeriod(p) {
  S.homePeriod = p;
  ['w','m','y'].forEach(x => {
    const el = document.getElementById('hg-tab-'+x);
    if (el) el.classList.toggle('active', x === p);
  });
  renderHomeGraphs();
}
function openHgFilter() {
  const sEl = document.getElementById('hg-filter-start'), eEl = document.getElementById('hg-filter-end');
  if (sEl) sEl.value = S.hgCustomStart || DB.today();
  if (eEl) eEl.value = S.hgCustomEnd || DB.today();
  openMo('mo-hg-filter');
}
function applyHgFilter() {
  const s = document.getElementById('hg-filter-start')?.value;
  const e = document.getElementById('hg-filter-end')?.value;
  if (!s || !e || s > e) { showToast('Escolha um intervalo de datas válido', 'error'); return; }
  S.hgCustomStart = s; S.hgCustomEnd = e; S.homePeriod = 'custom';
  ['w','m','y'].forEach(x => { const el = document.getElementById('hg-tab-'+x); if (el) el.classList.toggle('active', false); });
  closeMo('mo-hg-filter');
  renderHomeGraphs();
}
function getHomeDays() {
  const today = new Date();
  const fmt = d => d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
  const p = S.homePeriod;
  if (p === 'custom' && S.hgCustomStart && S.hgCustomEnd) {
    const days = [];
    for (let d = new Date(S.hgCustomStart+'T12:00'); fmt(d) <= S.hgCustomEnd; d.setDate(d.getDate()+1)) days.push(fmt(new Date(d)));
    return days;
  }
  if (p === 'w') {
    const dow = (today.getDay() + 6) % 7;
    return Array.from({length: dow+1}, (_,i) => { const d=new Date(today); d.setDate(today.getDate()-dow+i); return fmt(d); });
  }
  if (p === 'm') {
    return Array.from({length: today.getDate()}, (_,i) => {
      return today.getFullYear()+'-'+p2(today.getMonth()+1)+'-'+p2(i+1);
    });
  }
  // year: all days of current year up to today
  const start = new Date(today.getFullYear(), 0, 1);
  const days = [];
  for (let d = new Date(start); d <= today; d.setDate(d.getDate()+1)) days.push(fmt(new Date(d)));
  return days;
}
function getHomeDaysPrev() {
  const today = new Date();
  const fmt = d => d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
  const p = S.homePeriod;
  if (p === 'w') {
    const dow = (today.getDay() + 6) % 7; // dias decorridos nesta semana (0=seg)
    return Array.from({length: dow+1}, (_,i) => { const d=new Date(today); d.setDate(today.getDate()-dow-7+i); return fmt(d); });
  }
  if (p === 'm') {
    const prevMonth = new Date(today.getFullYear(), today.getMonth()-1, 1);
    const dim = new Date(prevMonth.getFullYear(), prevMonth.getMonth()+1, 0).getDate();
    const n = Math.min(today.getDate(), dim);
    return Array.from({length:n}, (_,i)=> prevMonth.getFullYear()+'-'+p2(prevMonth.getMonth()+1)+'-'+p2(i+1));
  }
  if (p === 'y') {
    const start = new Date(today.getFullYear()-1, 0, 1);
    const end = new Date(today.getFullYear()-1, today.getMonth(), today.getDate());
    const days = [];
    for (let d = new Date(start); d <= end; d.setDate(d.getDate()+1)) days.push(fmt(new Date(d)));
    return days;
  }
  return null; // periodo customizado: sem comparativo
}
// Resumo por regras (sem IA) do periodo atual vs o mesmo recorte do periodo anterior.
// Algumas variacoes de frase por situacao, pra nao ficar robotico sempre igual.
async function renderHgInsight(rev, tc, km, exp, hrs, pctL) {
  const el  = document.getElementById('hg-insight');
  const txt = document.getElementById('hg-insight-txt');
  if (!el || !txt) return;
  if (tc === 0) { el.style.display = 'none'; return; }

  // cache por período + dia — máx 3 chamadas/dia (uma por filtro)
  const hoje = new Date().toISOString().slice(0, 10);
  const cacheKey = 'sm_insight_' + (S.homePeriod||'w') + '_' + hoje;
  const cached = localStorage.getItem(cacheKey);
  if (cached) { txt.textContent = cached; el.style.display = 'flex'; return; }

  txt.textContent = 'Analisando…';
  el.style.display = 'flex';

  // período anterior
  const prevDates = getHomeDaysPrev ? getHomeDaysPrev() : null;
  let revPrev = null, tcPrev = null;
  if (prevDates && prevDates.length) {
    const pd = prevDates.map(d => ({ ...DB.day(d), trips: DB.day(d).trips.concat(autoTripsAsTrips(d)) }));
    revPrev = pd.reduce((s,d) => s + d.trips.reduce((a,t)=>a+(t.value||0),0) + d.revenues.reduce((a,r)=>a+(r.amount||0),0), 0);
    tcPrev  = pd.reduce((s,d) => s + d.trips.length, 0);
  }

  // melhor/pior dia da semana
  const lblsDia = ['Seg','Ter','Qua','Qui','Sex','Sáb','Dom'];
  const dates = getHomeDays();
  const diaData = hgBucketStats(dates,
    date => lblsDia[(new Date(date+'T00:00:00').getDay()+6)%7],
    hgDayTotal, lblsDia).filter(d => d.value > 0);
  const melhorDia = diaData.length ? diaData.reduce((a,b)=>b.avg>a.avg?b:a).label : null;
  const piorDia   = diaData.length > 1 ? diaData.reduce((a,b)=>b.avg<a.avg?b:a).label : null;

  // melhor hora
  const horaOrder = Array.from({length:24},(_,h)=>h+'h');
  const horaKeyFn = date => {
    const trips = dayTripsAll(date).filter(t => t.time);
    const pk = {};
    trips.forEach(t => { const h = parseInt(t.time.split(':')[0])+'h'; pk[h]=(pk[h]||0)+(t.value||0); });
    return Object.keys(pk).map(k=>({key:k,value:pk[k]}));
  };
  const horaData = hgBucketStats(dates, horaKeyFn, null, horaOrder).filter(d=>d.value>0);
  const melhorHora = horaData.length ? horaData.reduce((a,b)=>b.avg>a.avg?b:a).label : null;

  // % meta
  let metaPct = null;
  try {
    const plan = DB.get ? DB.get('sm_plan_jornada') : null;
    if (plan && plan.moneyGoal > 0) metaPct = Math.round(rev / plan.moneyGoal * 100);
  } catch(e) {}

  const diasTrabalhados = dates.filter(d => dayTripsAll(d).length > 0).length || 1;
  const nDays = diasTrabalhados; // ritmo por dia TRABALHADO, não por dia do período
  const rpkm  = km > 0 ? rev/km : 0;
  const rphr  = hrs > 0 ? rev/hrs : 0;

  // confiabilidade dos dados de hora — só conta corridas que vieram com hora registrada
  const corridasComHora = dates.reduce((s,d) => s + dayTripsAll(d).filter(t => t.time && t.time.trim()).length, 
===== sessions @ 568389 =====
if (rich && qtd > 1) {
      html += `<div class="hg-bb-row">≈ ${DB.fR(avg)} · ▲${DB.fR(max)} · ▼${DB.fR(min)}</div>`;
    }
    if (rich && prev != null && prev > 0.001) {
      const pct = Math.round((val - prev) / prev * 100);
      const arrow = pct > 0 ? '▲' : (pct < 0 ? '▼' : '–');
      html += `<div class="hg-bb-row">${arrow} ${Math.abs(pct)}% vs ${prevLbl}</div>`;
    }
    bub.innerHTML = html;
    // posiciona o tooltip acima da barra, alinhado ao centro dela,
    // mas clamped pra não sair do chart nem da tela
    bub.classList.add('show');
    requestAnimationFrame(() => {
      const barRect = bar.getBoundingClientRect();
      const rootRect = root.getBoundingClientRect();
      const bw = bub.offsetWidth;
      const margin = 10;
      let left = barRect.left - rootRect.left + barRect.width/2 - bw/2;
      if (left < margin) left = margin;
      if (left + bw > rootRect.width - margin) left = rootRect.width - margin - bw;
      bub.style.left = left + 'px';
      bub.style.bottom = (rootRect.bottom - barRect.top + 8) + 'px';
    });
  }
}
function hgGroupBy(items, keyFn, valFn) {
  const m = {};
  items.forEach(it => { const k = keyFn(it); m[k] = (m[k]||0) + valFn(it); });
  return m;
}

function renderHomeGraphs() {
  if (!S || !DB || !getHomeDays) return;
  const dates = getHomeDays();
  // Corridas automáticas do período inteiro (mais o período anterior, que
  // o insight usa pra comparação) — busca em lote UMA VEZ, cacheada pelo
  // intervalo. Refresh próprio a cada 60s enquanto na Home (setInterval no
  // fim do arquivo), pra pegar corrida nova sem precisar sair e voltar.
  const prevDatesForRange = getHomeDaysPrev ? getHomeDaysPrev() : [];
  const allDatesForRange = dates.concat(prevDatesForRange).sort();
  const rangeKey = (allDatesForRange[0]||'') + '..' + (allDatesForRange[allDatesForRange.length-1]||'');
  if (S.autoTripsHomeRange !== rangeKey) {
    S.autoTripsHomeRange = rangeKey;
    loadAutoTripsIndexed(allDatesForRange[0], allDatesForRange[allDatesForRange.length-1]).then(map => {
      if (S.autoTripsHomeRange !== rangeKey) return; // período já mudou de novo
      S.autoTripsByDate = map;
      if (S.sec === 'relatorios') renderHomeGraphs();
    });
  }
  const days = dates.map(d => {
    const day = DB.day(d);
    return { ...day, trips: day.trips.concat(autoTripsAsTrips(d)) };
  });
  const rev   = days.reduce((s,d) => s + d.trips.reduce((a,t)=>a+(t.value||0),0) + d.revenues.reduce((a,r)=>a+(r.amount||0),0), 0);
  const exp   = days.reduce((s,d) => s + d.expenses.reduce((a,e)=>a+(e.amount||0),0), 0);
  const km    = days.reduce((s,d) => s + d.trips.reduce((a,t)=>a+(t.km||0),0), 0);
  const tc    = days.reduce((s,d) => s + d.trips.length, 0);
  const hrs   = days.reduce((s,d) => s + d.trips.reduce((a,t)=>a+(t.duration||0),0), 0) / 60;
  const profit = rev - exp;
  const pctL   = rev > 0 ? Math.round(profit/rev*100) : 0;

  setText('hg-amount', DB.fR(rev));
  setText('hg-sub', tc+' corrida'+(tc!==1?'s':'')+' · '+km.toFixed(1)+' km');
  setText('hg-rpkm', km > 0 ? DB.fR(rev/km) : '—');
  setText('hg-rphr', hrs > 0 ? DB.fR(rev/hrs) : '—');
  setText('hg-trips', tc);
  setText('hg-exp', DB.fRI(exp));
  setText('hg-profit', DB.fRI(profit));
  setText('hg-pct', pctL+'%');
  renderHgInsight(rev, tc, km, exp, hrs, pctL);

  renderHgGoals(dates, days, rev);
  renderHgChartDia();
  renderHgChartHora();
  renderHgChartMes();
  renderHgPlatformPie(days);
  renderHgCatPie(days);

  // Últimos lançamentos
  const allEntries = days.flatMap(d => [
    ...d.trips.map(t=>({...t, _type:'corrida', _date:d.date})),
    ...d.expenses.map(e=>({...e, _type:'despesa', _date:d.date})),
    ...d.revenues.map(r=>({...r, _type:'receita', _date:d.date}))
  ]).sort((a,b) => (b._date+(b.time||'')).localeCompare(a._date+(a.time||''))).slice(0,5);

  const entriesEl = document.getElementById('home-entries');
  if (entriesEl) {
    if (!allEntries.length) {
      entriesEl.innerHTML = '<div class="empty">Nenhum lançamento ainda</div>';
    } else {
      entriesEl.innerHTML = allEntries.map(e => entryCard(e)).join('');
    }
  }
}
function renderHome() { renderHomeGraphs(); }

// ── Metas do período (financeiro, km, tempo online — vem do Planejar Jornada) ──
function renderHgGoals(dates, days, rev) {
  const el = document.getElementById('hg-goals');
  if (!el) return;
  const plans = DB.planJornada();
  let moneyGoal = 0, kmGoal = 0, timeGoalMin = 0, hasAnyPlan = false;
  dates.forEach(dt => {
    const dow = new Date(dt+'T12:00').getDay();
    plans.filter(p => p.days && p.days.includes(dow)).forEach(p => {
      hasAnyPlan = true;
      moneyGoal += p.moneyGoal || 0;
      kmGoal += p.kmGoal || 0;
      timeGoalMin += (p.schedules||[]).reduce((s,b)=>s+agMinutesBetween(b.start,b.end),0);
    });
  });
  const isToday = d => d === DB.today();
  const liveOn = S.gps.st==='running';
  const kmReal = days.reduce((s,d,i) => {
    const liveKm = (liveOn && isToday(dates[i])) ? S.gps.km : 0;
    return s + (d.sessions||[]).reduce((a,x)=>a+(x.km||0),0) + liveKm + (d.kmAdjust||0);
  }, 0);
  const elapsedReal = days.reduce((s,d,i) => {
    const liveEl = (liveOn && isToday(dates[i])) ? getElapsed() : 0;
    return s + (d.sessions||[]).reduce((a,x)=>a+(x.elapsed||0),0) + liveEl + (d.tempoAdjust||0);
  }, 0);
  const timeRealMin = Math.round(elapsedReal/60);

  let html = '';
  if (moneyGoal > 0) {
    const pct = Math.min(100, Math.round(rev/moneyGoal*100));
    html += '<div><div class="hg-goal-row"><span>Meta financeira</span><span>'+DB.fRI(rev)+' / '+DB.fRI(moneyGoal)+' · '+pct+'%</span></div><div class="hg-goal-bar money"><i style="width:'+pct+'%"></i></div></div>';
  } else {
    html += '<div class="hg-goal-locked" onclick="openPlanJornada()"><span>Meta financeira não definida</span><b>Planejar</b></div>';
  }
  if (timeGoalMin > 0) {
    const pct = Math.min(100, Math.round(timeRealMin/timeGoalMin*100));
    html += '<div><div class="hg-goal-row"><span>Tempo online</span><span>'+Math.floor(timeRealMin/60)+'h'+p2(timeRealMin%60)+' / '+Math.floor(timeGoalMin/60)+'h'+p2(timeGoalMin%60)+' · '+pct+'%</span></div><div class="hg-goal-bar time"><i style="width:'+pct+'%"></i></div></div>';
  } else {
    html += '<div class="hg-goal-locked" onclick="openPlanJornada()"><span>Tempo online não definido</span><b>Planejar</b></div>';
  }
  if (kmGoal > 0) {
    const pct = Math.min(100, Math.round(kmReal/kmGoal*100));
    html += '<div><div class="hg-goal-row"><span>Quilometragem</span><span>'+kmReal.toFixed(1)+' / '+kmGoal.toFixed(0)+' km · '+pct+'%</span></div><div class="hg-goal-bar km"><i style="width:'+pct+'%"></i></div></div>';
  } else {
    html += '<div class="hg-goal-locked" onclick="openPlanJornada()"><span>Quilometragem não definida</span><b>Planejar</b></div>';
  }
  el.innerHTML = html;
}

// ── Motor genérico de "balde" — usado pelos 3 gráficos de barra da Home.
// Pega as datas do período selecionado (getHomeDays), agrupa cada dia num balde
// (dia da semana / semana / hora / turno / mês / ano, depende do gráfico+modo) e
// guarda os valores individuais de cada dia que caiu naquele balde — não só a soma.
// Quando um balde tem mais de 1 dia (ex: todas as sextas do mês), o toque mostra
// média/maior/menor/qtd de dias em vez de só o total, sem poluir a barra.
function hgBucketStats(dates, keyFn, valueFn, order) {
  const buckets = {};
  dates.forEach(date => {
    const ks = keyFn(date); // pode retornar 1 chave ou um array de {key,value} (caso da Hora)
    if (ks == null) return;
    const list = Array.isArray(ks) ? ks : [{key: ks, value: valueFn ? valueFn(date) : null}];
    list.forEach(({key, value}) => {
      if (value == null || value <= 0.001) return;
      (buckets[key] = buckets[key] || []).push(value);
    });
  });
  const keys = order ? order.filter(k => buckets[k]) : Object.keys(buckets).sort();
  return keys.map(k => {
    const arr = buckets[k];
    const sum = arr.reduce((a,b)=>a+b, 0);
    return { label: k, value: sum, max: Math.max(...arr), min: Math.min(...arr), avg: sum/arr.length, qtd: arr.length };
  });
}
function hgDayTotal(date) {
  const day = DB.day(date);
  return dayTripsAll(date).reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0);
}
function hgWeekStart(date) {
  const d = new Date(date+'T00:00:00'); const dow = (d.getDay()+6)%7; d.setDate(d.getDate()-dow);
  return p2(d.getDate())+'/'+p2(d.getMonth()+1);
}

// ── Por Dia/Semana ──
function setHgDiaMode(m) {
  S.hgDiaMode = m;
  ['d','w'].forEach(x => { const e=document.getElementById('hg-dia-'+x); if(e) e.classList.toggle('active', x===m); });
  renderHgChartDia();
}
function renderHgChartDia() {
  const el = document.getElementById('hg-chart-dia');
  if (!el) return;
  const dates = getHomeDays();
  let data;
  if (S.hgDiaMode === 'd') {
    const lbls = ['Seg','Ter','Qua','Qui','Sex','Sáb','Dom'];
    const keyFn = date => lbls[(new Date(date+'T00:00:00').getDay()+6)%7];
    data = hgBucketStats(dates, keyFn, hgDayTotal, lbls);
  } else {
    const keyFn = hgWeekStart;
    const order = [...new Set(dates.map(keyFn))];
    data = hgBucketStats(dates, keyFn, hgDayTotal, order);
  }
  el.innerHTML = hgBarsHtml(data, 'dia');
  hgFitColWidths('hg-chart-dia');
  renderHgHighlight('hg-dia-insight', data, 'fraco', d => 'Dia mais fraco: '+d.label+' (média '+DB.fR(d.avg)+')');
}

// ── Por Hora/Turno ──
function setHgHoraMode(m) {
  S.hgHoraMode = m;
  ['h','t'].forEach(x => { const e=document.getElementById('hg-hora-'+x); if(e) e.classList.toggle('active', x===m); });
  renderHgChartHora();
}
function renderHgChartHora() {
  const el = document.getElementById('hg-chart-hora');
  if (!el) return;
  const dates = getHomeDays();
  const turnos = [
    {label:'Madrugada', test:h=>h>=0&&h<6}, {label:'Manhã', test:h=>h>=6&&h<12},
    {label:'Tarde', test:h=>h>=12&&h<18}, {label:'Noite', test:h=>h>=18&&h<24}
  ];
  // pra cada dia, quebra os valores por hora (ou turno) ANTES de agregar entre dias —
  // assim um dia com 2 corridas na mesma hora conta certo, e dias diferentes na mesma
  // hora ficam no mesmo balde.
  const keyFn = date => {
    const trips = dayTripsAll(date).filter(t => t.time);
    const perKey = {};
    trips.forEach(t => {
      const hr = parseInt(t.time.split(':')[0]);
      const k = S.hgHoraMode === 'h' ? (hr+'h') : turnos.find(tu=>tu.test(hr)).label;
      perKey[k] = (perKey[k]||0) + (t.value||0);
    });
    return Object.keys(perKey).map(k => ({key:k, value:perKey[k]}));
  };
  const order = S.hgHoraMode === 'h' ? Array.from({length:24},(_,h)=>h+'h') : turnos.map(t=>t.label);
  const data = hgBucketStats(dates, keyFn, null, order);
  el.innerHTML = hgBarsHtml(data, 'hora', false);
  hgFitColWidths('hg-chart-hora');
  renderHgHighlight('hg-hora-insight', data, 'forte', d => 'Melhor horário: '+d.label+' (média '+DB.fR(d.avg)+')');
}

// ── Por Mês/Ano ──
function setHgMesMode(m) {
  S.hgMesMode = m;
  ['m','y'].forEach(x => { const e=document.getElementById('hg-mes-'+x); if(e) e.classList.toggle('active', x===m); });
  renderHgChartMes();
}
function renderHgChartMes() {
  const el = document.getElementById('hg-chart-mes');
  if (!el) return;
  const dates = getHomeDays();
  const mesesLbl = ['Jan','Fev','Mar','Abr','Mai','Jun','Jul','Ago','Set','Out','Nov','Dez'];
  let data;
  if (S.hgMesMode === 'm') {
    const keyFn = date => mesesLbl[parseInt(date.split('-')[1],10)-1];
    const order = [...new Set(dates.map(keyFn))];
    data = hgBucketStats(dates, keyFn, hgDayTotal, order);
  } else {
    const keyFn = date => date.split('-')[0];
    const order = [...new Set(dates.map(keyFn))];
    data = hgBucketStats(dates, keyFn, hgDayTotal, order);
  }
  // Com período Semana/Mês selecionado em cima, isso aqui naturalmente vira 1 barra só
  // (só existe 1 mês/ano dentro do recorte) — é o esperado, segue a mesma regra dos outros.
  el.innerHTML = hgBarsHtml(data, 'mes');
  hgFitColWidths('hg-chart-mes');
}

// Destaque automático (sem precisar tocar) — pega o balde de maior/menor média.
// tipo 'forte' pega o maior, 'fraco' pega o menor (entre os que têm dado).
function renderHgHighlight(elId, data, tipo, fmt) {
  const el = document.getElementById(elId);
  if (!el) return;
  if (!data.length) { el.textContent = ''; return; }
  const best = data.reduce((a,b) => (tipo==='forte' ? (b.avg>a.avg?b:a) : (b.avg<a.avg?b:a)));
  el.textContent = fmt(best);
}

// ── Plataformas (fixo, sem tabs) ──
function renderHgPlatformPie(days) {
  const allTrips = days.flatMap(d => d.trips);
  const uberR = allTrips.filter(t=>t.platform==='uber').reduce((s,t)=>s+(t.value||0),0);
  const p99R  = allTrips.filter(t=>t.platform==='99').reduce((s,t)=>s+(t.value||0),0);
  const privR = allTrips.filter(t=>t.platform==='particular').reduce((s,t)=>s+(t.value||0),0);
  const outR  = allTrips.filter(t=>!['uber','99','particular'].includes(t.platform)).reduce((s,t)=>s+(t.value||0),0);
  hgBuildPie([
    {label:'Uber', value:uberR, color:'#0ea5e9'},
    {label:'99', value:p99R, color:'#22c55e'},
    {label:'Particular', value:privR, color:'#8b5cf6'},
    {label:'Outros', value:outR, color:'#f59e0b'}
  ], 'hg-pie-plat', 'hg-pie-plat-list');
}

// ── Receitas x Despesas (com tabs) ──
function setHgRevDesp(m) {
  S.hgRevDesp = m;
  ['receita','despesa'].forEach(x => { const e=document.getElementById('hg-rd-'+x); if(e) e.classList.toggle('active', x===m); });
  renderHgCatPie(getHomeDays().map(d => ({ ...DB.day(d), trips: DB.day(d).trips.concat(autoTripsAsTrips(d)) })));
}
function renderHgCatPie(days) {
  let items;
  if (S.hgRevDesp === 'receita') {
    const allTrips = days.flatMap(d => d.trips);
    const byPlat = [
      {label:'Uber', value: allTrips.filter(t=>t.platform==='uber').reduce((s,t)=>s+(t.value||0),0)},
      {label:'99', value: allTrips.filter(t=>t.platform==='99').reduce((s,t)=>s+(t.value||0),0)},
      {label:'Particular', value: allTrips.filter(t=>t.platform==='particular').reduce((s,t)=>s+(t.value||0),0)}
    ];
    const allRev = days.flatMap(d => d.revenues);
    const byCat = hgGroupBy(allRev, r => r.categoryLabel || r.category || 'Outros', r => r.amount||0);
    const catItems = Object.keys(byCat).map(k => ({label:k, value: byCat[k]}));
    items = byPlat.concat(catItems);
  } else {
    const allExp = days.flatMap(d => d.expenses);
    const byCat = hgGroupBy(allExp, e => e.categoryLabel || e.category || 'Outros', e => e.amount||0);
    items = Object.keys(byCat).map(k => ({label:k, value: byCat[k]}));
  }
  items.sort((a,b) => b.value - a.value);
  if (items.length > 6) {
    const top = items.slice(0,6);
    const restSum = items.slice(6).reduce((s,i)=>s+i.value,0);
    items = top.concat(restSum > 0 ? [{label:'Outros', value: restSum}] : []);
  }
  hgBuildPie(items, 'hg-pie-cat', 'hg-pie-cat-list', v => DB.fRI(v));
  const panelEl = document.getElementById('hg-cat-panel');
  if (panelEl) panelEl.innerHTML = hgCatPanelHtml(items);
}
function entryCard(e) {
  const isRev  = e._type === 'corrida' || e
===== sessions @ 568603 =====
 / prev * 100);
      const arrow = pct > 0 ? '▲' : (pct < 0 ? '▼' : '–');
      html += `<div class="hg-bb-row">${arrow} ${Math.abs(pct)}% vs ${prevLbl}</div>`;
    }
    bub.innerHTML = html;
    // posiciona o tooltip acima da barra, alinhado ao centro dela,
    // mas clamped pra não sair do chart nem da tela
    bub.classList.add('show');
    requestAnimationFrame(() => {
      const barRect = bar.getBoundingClientRect();
      const rootRect = root.getBoundingClientRect();
      const bw = bub.offsetWidth;
      const margin = 10;
      let left = barRect.left - rootRect.left + barRect.width/2 - bw/2;
      if (left < margin) left = margin;
      if (left + bw > rootRect.width - margin) left = rootRect.width - margin - bw;
      bub.style.left = left + 'px';
      bub.style.bottom = (rootRect.bottom - barRect.top + 8) + 'px';
    });
  }
}
function hgGroupBy(items, keyFn, valFn) {
  const m = {};
  items.forEach(it => { const k = keyFn(it); m[k] = (m[k]||0) + valFn(it); });
  return m;
}

function renderHomeGraphs() {
  if (!S || !DB || !getHomeDays) return;
  const dates = getHomeDays();
  // Corridas automáticas do período inteiro (mais o período anterior, que
  // o insight usa pra comparação) — busca em lote UMA VEZ, cacheada pelo
  // intervalo. Refresh próprio a cada 60s enquanto na Home (setInterval no
  // fim do arquivo), pra pegar corrida nova sem precisar sair e voltar.
  const prevDatesForRange = getHomeDaysPrev ? getHomeDaysPrev() : [];
  const allDatesForRange = dates.concat(prevDatesForRange).sort();
  const rangeKey = (allDatesForRange[0]||'') + '..' + (allDatesForRange[allDatesForRange.length-1]||'');
  if (S.autoTripsHomeRange !== rangeKey) {
    S.autoTripsHomeRange = rangeKey;
    loadAutoTripsIndexed(allDatesForRange[0], allDatesForRange[allDatesForRange.length-1]).then(map => {
      if (S.autoTripsHomeRange !== rangeKey) return; // período já mudou de novo
      S.autoTripsByDate = map;
      if (S.sec === 'relatorios') renderHomeGraphs();
    });
  }
  const days = dates.map(d => {
    const day = DB.day(d);
    return { ...day, trips: day.trips.concat(autoTripsAsTrips(d)) };
  });
  const rev   = days.reduce((s,d) => s + d.trips.reduce((a,t)=>a+(t.value||0),0) + d.revenues.reduce((a,r)=>a+(r.amount||0),0), 0);
  const exp   = days.reduce((s,d) => s + d.expenses.reduce((a,e)=>a+(e.amount||0),0), 0);
  const km    = days.reduce((s,d) => s + d.trips.reduce((a,t)=>a+(t.km||0),0), 0);
  const tc    = days.reduce((s,d) => s + d.trips.length, 0);
  const hrs   = days.reduce((s,d) => s + d.trips.reduce((a,t)=>a+(t.duration||0),0), 0) / 60;
  const profit = rev - exp;
  const pctL   = rev > 0 ? Math.round(profit/rev*100) : 0;

  setText('hg-amount', DB.fR(rev));
  setText('hg-sub', tc+' corrida'+(tc!==1?'s':'')+' · '+km.toFixed(1)+' km');
  setText('hg-rpkm', km > 0 ? DB.fR(rev/km) : '—');
  setText('hg-rphr', hrs > 0 ? DB.fR(rev/hrs) : '—');
  setText('hg-trips', tc);
  setText('hg-exp', DB.fRI(exp));
  setText('hg-profit', DB.fRI(profit));
  setText('hg-pct', pctL+'%');
  renderHgInsight(rev, tc, km, exp, hrs, pctL);

  renderHgGoals(dates, days, rev);
  renderHgChartDia();
  renderHgChartHora();
  renderHgChartMes();
  renderHgPlatformPie(days);
  renderHgCatPie(days);

  // Últimos lançamentos
  const allEntries = days.flatMap(d => [
    ...d.trips.map(t=>({...t, _type:'corrida', _date:d.date})),
    ...d.expenses.map(e=>({...e, _type:'despesa', _date:d.date})),
    ...d.revenues.map(r=>({...r, _type:'receita', _date:d.date}))
  ]).sort((a,b) => (b._date+(b.time||'')).localeCompare(a._date+(a.time||''))).slice(0,5);

  const entriesEl = document.getElementById('home-entries');
  if (entriesEl) {
    if (!allEntries.length) {
      entriesEl.innerHTML = '<div class="empty">Nenhum lançamento ainda</div>';
    } else {
      entriesEl.innerHTML = allEntries.map(e => entryCard(e)).join('');
    }
  }
}
function renderHome() { renderHomeGraphs(); }

// ── Metas do período (financeiro, km, tempo online — vem do Planejar Jornada) ──
function renderHgGoals(dates, days, rev) {
  const el = document.getElementById('hg-goals');
  if (!el) return;
  const plans = DB.planJornada();
  let moneyGoal = 0, kmGoal = 0, timeGoalMin = 0, hasAnyPlan = false;
  dates.forEach(dt => {
    const dow = new Date(dt+'T12:00').getDay();
    plans.filter(p => p.days && p.days.includes(dow)).forEach(p => {
      hasAnyPlan = true;
      moneyGoal += p.moneyGoal || 0;
      kmGoal += p.kmGoal || 0;
      timeGoalMin += (p.schedules||[]).reduce((s,b)=>s+agMinutesBetween(b.start,b.end),0);
    });
  });
  const isToday = d => d === DB.today();
  const liveOn = S.gps.st==='running';
  const kmReal = days.reduce((s,d,i) => {
    const liveKm = (liveOn && isToday(dates[i])) ? S.gps.km : 0;
    return s + (d.sessions||[]).reduce((a,x)=>a+(x.km||0),0) + liveKm + (d.kmAdjust||0);
  }, 0);
  const elapsedReal = days.reduce((s,d,i) => {
    const liveEl = (liveOn && isToday(dates[i])) ? getElapsed() : 0;
    return s + (d.sessions||[]).reduce((a,x)=>a+(x.elapsed||0),0) + liveEl + (d.tempoAdjust||0);
  }, 0);
  const timeRealMin = Math.round(elapsedReal/60);

  let html = '';
  if (moneyGoal > 0) {
    const pct = Math.min(100, Math.round(rev/moneyGoal*100));
    html += '<div><div class="hg-goal-row"><span>Meta financeira</span><span>'+DB.fRI(rev)+' / '+DB.fRI(moneyGoal)+' · '+pct+'%</span></div><div class="hg-goal-bar money"><i style="width:'+pct+'%"></i></div></div>';
  } else {
    html += '<div class="hg-goal-locked" onclick="openPlanJornada()"><span>Meta financeira não definida</span><b>Planejar</b></div>';
  }
  if (timeGoalMin > 0) {
    const pct = Math.min(100, Math.round(timeRealMin/timeGoalMin*100));
    html += '<div><div class="hg-goal-row"><span>Tempo online</span><span>'+Math.floor(timeRealMin/60)+'h'+p2(timeRealMin%60)+' / '+Math.floor(timeGoalMin/60)+'h'+p2(timeGoalMin%60)+' · '+pct+'%</span></div><div class="hg-goal-bar time"><i style="width:'+pct+'%"></i></div></div>';
  } else {
    html += '<div class="hg-goal-locked" onclick="openPlanJornada()"><span>Tempo online não definido</span><b>Planejar</b></div>';
  }
  if (kmGoal > 0) {
    const pct = Math.min(100, Math.round(kmReal/kmGoal*100));
    html += '<div><div class="hg-goal-row"><span>Quilometragem</span><span>'+kmReal.toFixed(1)+' / '+kmGoal.toFixed(0)+' km · '+pct+'%</span></div><div class="hg-goal-bar km"><i style="width:'+pct+'%"></i></div></div>';
  } else {
    html += '<div class="hg-goal-locked" onclick="openPlanJornada()"><span>Quilometragem não definida</span><b>Planejar</b></div>';
  }
  el.innerHTML = html;
}

// ── Motor genérico de "balde" — usado pelos 3 gráficos de barra da Home.
// Pega as datas do período selecionado (getHomeDays), agrupa cada dia num balde
// (dia da semana / semana / hora / turno / mês / ano, depende do gráfico+modo) e
// guarda os valores individuais de cada dia que caiu naquele balde — não só a soma.
// Quando um balde tem mais de 1 dia (ex: todas as sextas do mês), o toque mostra
// média/maior/menor/qtd de dias em vez de só o total, sem poluir a barra.
function hgBucketStats(dates, keyFn, valueFn, order) {
  const buckets = {};
  dates.forEach(date => {
    const ks = keyFn(date); // pode retornar 1 chave ou um array de {key,value} (caso da Hora)
    if (ks == null) return;
    const list = Array.isArray(ks) ? ks : [{key: ks, value: valueFn ? valueFn(date) : null}];
    list.forEach(({key, value}) => {
      if (value == null || value <= 0.001) return;
      (buckets[key] = buckets[key] || []).push(value);
    });
  });
  const keys = order ? order.filter(k => buckets[k]) : Object.keys(buckets).sort();
  return keys.map(k => {
    const arr = buckets[k];
    const sum = arr.reduce((a,b)=>a+b, 0);
    return { label: k, value: sum, max: Math.max(...arr), min: Math.min(...arr), avg: sum/arr.length, qtd: arr.length };
  });
}
function hgDayTotal(date) {
  const day = DB.day(date);
  return dayTripsAll(date).reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0);
}
function hgWeekStart(date) {
  const d = new Date(date+'T00:00:00'); const dow = (d.getDay()+6)%7; d.setDate(d.getDate()-dow);
  return p2(d.getDate())+'/'+p2(d.getMonth()+1);
}

// ── Por Dia/Semana ──
function setHgDiaMode(m) {
  S.hgDiaMode = m;
  ['d','w'].forEach(x => { const e=document.getElementById('hg-dia-'+x); if(e) e.classList.toggle('active', x===m); });
  renderHgChartDia();
}
function renderHgChartDia() {
  const el = document.getElementById('hg-chart-dia');
  if (!el) return;
  const dates = getHomeDays();
  let data;
  if (S.hgDiaMode === 'd') {
    const lbls = ['Seg','Ter','Qua','Qui','Sex','Sáb','Dom'];
    const keyFn = date => lbls[(new Date(date+'T00:00:00').getDay()+6)%7];
    data = hgBucketStats(dates, keyFn, hgDayTotal, lbls);
  } else {
    const keyFn = hgWeekStart;
    const order = [...new Set(dates.map(keyFn))];
    data = hgBucketStats(dates, keyFn, hgDayTotal, order);
  }
  el.innerHTML = hgBarsHtml(data, 'dia');
  hgFitColWidths('hg-chart-dia');
  renderHgHighlight('hg-dia-insight', data, 'fraco', d => 'Dia mais fraco: '+d.label+' (média '+DB.fR(d.avg)+')');
}

// ── Por Hora/Turno ──
function setHgHoraMode(m) {
  S.hgHoraMode = m;
  ['h','t'].forEach(x => { const e=document.getElementById('hg-hora-'+x); if(e) e.classList.toggle('active', x===m); });
  renderHgChartHora();
}
function renderHgChartHora() {
  const el = document.getElementById('hg-chart-hora');
  if (!el) return;
  const dates = getHomeDays();
  const turnos = [
    {label:'Madrugada', test:h=>h>=0&&h<6}, {label:'Manhã', test:h=>h>=6&&h<12},
    {label:'Tarde', test:h=>h>=12&&h<18}, {label:'Noite', test:h=>h>=18&&h<24}
  ];
  // pra cada dia, quebra os valores por hora (ou turno) ANTES de agregar entre dias —
  // assim um dia com 2 corridas na mesma hora conta certo, e dias diferentes na mesma
  // hora ficam no mesmo balde.
  const keyFn = date => {
    const trips = dayTripsAll(date).filter(t => t.time);
    const perKey = {};
    trips.forEach(t => {
      const hr = parseInt(t.time.split(':')[0]);
      const k = S.hgHoraMode === 'h' ? (hr+'h') : turnos.find(tu=>tu.test(hr)).label;
      perKey[k] = (perKey[k]||0) + (t.value||0);
    });
    return Object.keys(perKey).map(k => ({key:k, value:perKey[k]}));
  };
  const order = S.hgHoraMode === 'h' ? Array.from({length:24},(_,h)=>h+'h') : turnos.map(t=>t.label);
  const data = hgBucketStats(dates, keyFn, null, order);
  el.innerHTML = hgBarsHtml(data, 'hora', false);
  hgFitColWidths('hg-chart-hora');
  renderHgHighlight('hg-hora-insight', data, 'forte', d => 'Melhor horário: '+d.label+' (média '+DB.fR(d.avg)+')');
}

// ── Por Mês/Ano ──
function setHgMesMode(m) {
  S.hgMesMode = m;
  ['m','y'].forEach(x => { const e=document.getElementById('hg-mes-'+x); if(e) e.classList.toggle('active', x===m); });
  renderHgChartMes();
}
function renderHgChartMes() {
  const el = document.getElementById('hg-chart-mes');
  if (!el) return;
  const dates = getHomeDays();
  const mesesLbl = ['Jan','Fev','Mar','Abr','Mai','Jun','Jul','Ago','Set','Out','Nov','Dez'];
  let data;
  if (S.hgMesMode === 'm') {
    const keyFn = date => mesesLbl[parseInt(date.split('-')[1],10)-1];
    const order = [...new Set(dates.map(keyFn))];
    data = hgBucketStats(dates, keyFn, hgDayTotal, order);
  } else {
    const keyFn = date => date.split('-')[0];
    const order = [...new Set(dates.map(keyFn))];
    data = hgBucketStats(dates, keyFn, hgDayTotal, order);
  }
  // Com período Semana/Mês selecionado em cima, isso aqui naturalmente vira 1 barra só
  // (só existe 1 mês/ano dentro do recorte) — é o esperado, segue a mesma regra dos outros.
  el.innerHTML = hgBarsHtml(data, 'mes');
  hgFitColWidths('hg-chart-mes');
}

// Destaque automático (sem precisar tocar) — pega o balde de maior/menor média.
// tipo 'forte' pega o maior, 'fraco' pega o menor (entre os que têm dado).
function renderHgHighlight(elId, data, tipo, fmt) {
  const el = document.getElementById(elId);
  if (!el) return;
  if (!data.length) { el.textContent = ''; return; }
  const best = data.reduce((a,b) => (tipo==='forte' ? (b.avg>a.avg?b:a) : (b.avg<a.avg?b:a)));
  el.textContent = fmt(best);
}

// ── Plataformas (fixo, sem tabs) ──
function renderHgPlatformPie(days) {
  const allTrips = days.flatMap(d => d.trips);
  const uberR = allTrips.filter(t=>t.platform==='uber').reduce((s,t)=>s+(t.value||0),0);
  const p99R  = allTrips.filter(t=>t.platform==='99').reduce((s,t)=>s+(t.value||0),0);
  const privR = allTrips.filter(t=>t.platform==='particular').reduce((s,t)=>s+(t.value||0),0);
  const outR  = allTrips.filter(t=>!['uber','99','particular'].includes(t.platform)).reduce((s,t)=>s+(t.value||0),0);
  hgBuildPie([
    {label:'Uber', value:uberR, color:'#0ea5e9'},
    {label:'99', value:p99R, color:'#22c55e'},
    {label:'Particular', value:privR, color:'#8b5cf6'},
    {label:'Outros', value:outR, color:'#f59e0b'}
  ], 'hg-pie-plat', 'hg-pie-plat-list');
}

// ── Receitas x Despesas (com tabs) ──
function setHgRevDesp(m) {
  S.hgRevDesp = m;
  ['receita','despesa'].forEach(x => { const e=document.getElementById('hg-rd-'+x); if(e) e.classList.toggle('active', x===m); });
  renderHgCatPie(getHomeDays().map(d => ({ ...DB.day(d), trips: DB.day(d).trips.concat(autoTripsAsTrips(d)) })));
}
function renderHgCatPie(days) {
  let items;
  if (S.hgRevDesp === 'receita') {
    const allTrips = days.flatMap(d => d.trips);
    const byPlat = [
      {label:'Uber', value: allTrips.filter(t=>t.platform==='uber').reduce((s,t)=>s+(t.value||0),0)},
      {label:'99', value: allTrips.filter(t=>t.platform==='99').reduce((s,t)=>s+(t.value||0),0)},
      {label:'Particular', value: allTrips.filter(t=>t.platform==='particular').reduce((s,t)=>s+(t.value||0),0)}
    ];
    const allRev = days.flatMap(d => d.revenues);
    const byCat = hgGroupBy(allRev, r => r.categoryLabel || r.category || 'Outros', r => r.amount||0);
    const catItems = Object.keys(byCat).map(k => ({label:k, value: byCat[k]}));
    items = byPlat.concat(catItems);
  } else {
    const allExp = days.flatMap(d => d.expenses);
    const byCat = hgGroupBy(allExp, e => e.categoryLabel || e.category || 'Outros', e => e.amount||0);
    items = Object.keys(byCat).map(k => ({label:k, value: byCat[k]}));
  }
  items.sort((a,b) => b.value - a.value);
  if (items.length > 6) {
    const top = items.slice(0,6);
    const restSum = items.slice(6).reduce((s,i)=>s+i.value,0);
    items = top.concat(restSum > 0 ? [{label:'Outros', value: restSum}] : []);
  }
  hgBuildPie(items, 'hg-pie-cat', 'hg-pie-cat-list', v => DB.fRI(v));
  const panelEl = document.getElementById('hg-cat-panel');
  if (panelEl) panelEl.innerHTML = hgCatPanelHtml(items);
}
function entryCard(e) {
  const isRev  = e._type === 'corrida' || e._type === 'receita';
  const val    = e.value || e.amount || 0;
  const label  = e._type === 'corrida' ? ((e.origin||'—')+(e.dest?' → '+e.dest:'')) : (e.desc || e.category || e._type);
  const color  = isRev ? 'va
===== sessions @ 583218 =====
 = document.getElementById('hg-cat-panel');
  if (panelEl) panelEl.innerHTML = hgCatPanelHtml(items);
}
function entryCard(e) {
  const isRev  = e._type === 'corrida' || e._type === 'receita';
  const val    = e.value || e.amount || 0;
  const label  = e._type === 'corrida' ? ((e.origin||'—')+(e.dest?' → '+e.dest:'')) : (e.desc || e.category || e._type);
  const color  = isRev ? 'var(--green2)' : 'var(--red)';
  const sign   = isRev ? '+' : '-';
  return `<div style="display:flex;align-items:center;gap:10px;padding:11px 0;border-bottom:1px solid #E2E8F0">
    <div style="flex:1;min-width:0"><div style="font-size:13px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${label}</div><div style="font-size:11px;color:#64748B;margin-top:2px">${e._type} · ${e._date}</div></div>
    <div style="font-family:'Inter',system-ui,sans-serif;font-weight:700;font-size:14px;color:${color};flex-shrink:0">${sign}${DB.fRI(val)}</div>
  </div>`;
}

// ── JORNADA ───────────────────────────────────────────────────
function chgDay(d) {
  const dt = new Date(S.curDate+'T12:00'); dt.setDate(dt.getDate()+d);
  S.curDate = dt.getFullYear()+'-'+p2(dt.getMonth()+1)+'-'+p2(dt.getDate());
  renderJornada();
}
function setJornadaDate(dateStr) {
  if (!dateStr) return;
  S.curDate = dateStr;
  renderJornada();
}
function fmtHM(ms) { if (!ms) return '--:--'; const dd=new Date(ms); return p2(dd.getHours())+':'+p2(dd.getMinutes()); }
function toggleTripsView() { S.tripsExpanded = !S.tripsExpanded; renderJornada(); }

// ── TELA CORRIDAS (lista dedicada, com filtro de período/plataforma e exclusão) ──
function getCorridasDays() {
  const today=new Date();
  const fmt=d=>d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
  const p=S.corridasPeriod;
  if(p==='date'&&S.corridasDate) return [S.corridasDate];
  if(p==='hoje') return [fmt(today)];
  if(p==='7d') { const out=[]; for(let i=0;i<7;i++){const d=new Date(today);d.setDate(d.getDate()-i);out.push(fmt(d));} return out; }
  if(p==='mes') { const m=today.getMonth(),y=today.getFullYear(); return DB.allDates().filter(d=>d.startsWith(y+'-'+p2(m+1))); }
  return DB.allDates();
}
function setCorridasPeriod(p) {
  S.corridasPeriod=p; S.corridasDate=null;
  ['hoje','7d','mes','tudo'].forEach(id=>{ const b=document.getElementById('cpb-'+id); if(b) b.classList.toggle('active',id===p); });
  const dp=document.getElementById('cor-date-picker'),db=document.getElementById('cor-date-btn');
  if(dp) dp.style.display='none'; if(db){db.style.background='#fff';db.style.borderColor='#E2E8F0';}
  renderCorridasActive();
}
function toggleCorDate() {
  const inp=document.getElementById('cor-date-input');
  if(inp) inp.showPicker ? inp.showPicker() : inp.click();
}
function setCorridasDate(d) {
  if(!d) return; S.corridasDate=d; S.corridasPeriod='date';
  ['hoje','7d','mes','tudo'].forEach(id=>{ const b=document.getElementById('cpb-'+id); if(b) b.classList.remove('active'); });
  const db=document.getElementById('cor-date-btn'); if(db){db.style.background='#EFF6FF';db.style.borderColor='#2563EB';}
  renderCorridasActive();
}
function toggleCorridasPlat(p) {
  // Multi-select agora (pedido do Yuri, 24/07/2026): nenhum marcado = mostra
  // tudo (padrão), marcar um filtra só aquele, marcar os dois volta a
  // mostrar tudo — sem precisar de um botão "Todas" separado.
  if (!S.corridasPlats) S.corridasPlats = new Set();
  if (S.corridasPlats.has(p)) S.corridasPlats.delete(p); else S.corridasPlats.add(p);
  const ub = document.getElementById('cor-plat-uber'), n99 = document.getElementById('cor-plat-99');
  if (ub) ub.classList.toggle('active', S.corridasPlats.has('uber'));
  if (n99) n99.classList.toggle('active', S.corridasPlats.has('99'));
  renderCorridasActive();
}
// ── Corridas: alterna entre Importadas (trips locais) e Automáticas (auto_trips no Supabase) ──
// ── Corridas automáticas → alimentam a Jornada (pedido do Yuri, 23/07/2026) ──
// Busca auto_trips do dia pra somar em ganhos/km/corridas da Jornada e da
// sessão certa dentro dela — sem se misturar com day.trips (Importadas),
// só somando junto na hora de mostrar total. Timestamp de referência:
// trip_started_at (início real da corrida) — cai sempre dentro da sessão
// (Jornada) que estava rodando, diferente de accepted_at que pode ser
// alguns segundos antes ainda na sessão anterior num caso raro de virada.
function autoTripTs(t) {
  const iso = t.trip_started_at || t.accepted_at;
  return iso ? new Date(iso).getTime() : 0;
}
// Corrida cai dentro do horário de alguma jornada do dia dela? Pedido do
// Yuri (24/07/2026) — o vínculo nunca é salvo, é sempre calculado (mesma
// lógica de sessionItem/renderJornada) — então dá pra checar pra QUALQUER
// corrida, de qualquer tela, sem precisar saber de antemão qual sessão.
function isAutoTripLinkedToSession(t) {
  const ts = autoTripTs(t);
  if (!ts) return false;
  const d = new Date(ts);
  const dateStr = d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
  const sessions = (DB.day(dateStr).sessions) || [];
  return sessions.some(s => {
    const start = s.start || 0, end = s.end || 0;
    return ts >= start && (!end || ts <= end);
  });
}

// ── Reconferir corrida usando o texto bruto do log (24/07/2026) ────────────
// Pedido do Yuri: em vez de tentar "adivinhar" o valor certo de uma corrida
// já salva errada, reaplica a MESMA lógica de leitura (já corrigida no app
// nativo em 24/07/2026) em cima do texto ORIGINAL da tela, salvo em
// trip_reader_log — usa o dado real daquele instante, não um chute.
// Porta fiel de parseOffer() do TripReaderService.kt (Kotlin) — mesmas
// regras, mesma ordem, incluindo as 3 correções de 24/07 (R$/km nunca
// compete como valor total; dinâmico soma os 2 tipos da 99 + Uber; linha
// de avaliação/corridas nunca vira endereço).
function moneyToDoubleJS(raw) {
  const clean = raw.replace('R$','').trim().replace(/\./g,'').replace(',','.');
  const n = parseFloat(clean);
  return isNaN(n) ? null : n;
}
function extractMoneyJS(s) {
  // CORRIGIDO (24/07/2026) - mesma correção do Kotlin: centavos passam a
  // ser obrigatórios, evita aceitar "R$96" como valor quando o OCR engoliu
  // a vírgula de "R$9,60".
  const re = /R\$\s?\d{1,4}[.,]\d{2}/g;
  const out = []; let m;
  while ((m = re.exec(s))) if (!out.includes(m[0])) out.push(m[0]);
  return out;
}
// Validação de endereço — GLOBAL (promovida de dentro de parseOfferJS em
// 16/08/2026 pra ser reutilizável também na importação de vídeo, onde
// endereço de corrida nova nunca pode ser aceito sem essa checagem —
// pedido explícito do Yuri: "nunca endereço, a não ser que garanta que
// não vai vir lixo ou inventado"). Mesma lógica usada no Kotlin.
function looksLikeAddress(s) {
  const sl = s.toLowerCase().trim();
  if (sl.length < 6) return false;
  if (sl.includes('parada')) return false;
  if (sl.includes('não afeta') || sl.includes('nao afeta')) return false;
  if (/^\(?\d{1,3}([.,]\d+)?\s*(min|km|m)\b/.test(sl)) return false;
  if (/^[x%\d.,\s]+$/.test(sl)) return false;
  if (sl.includes('r$')) return false;
  if (sl.includes('tarifa') || sl.includes('incl.') || sl.includes('incluído') || sl.includes('incluido')) return false;
  if (sl.includes('taxa de espera') || sl.includes('espera longa')) return false;
  if (sl === 'online' || sl === 'buscando' || sl === 'offline' || sl === 'conectar') return false;
  if (sl.includes('perfil essencial') || sl.includes('perfil premium') || sl.includes('perfil prata')) return false;
  if (sl.includes('pgto. no app') || sl === 'dinheiro' || sl === 'negocia' || sl === 'qr code') return false;
  if (/[1-5][.,]\d{2}.{0,15}corrid/.test(sl)) return false;
  if (sl.includes('verif.') || sl.includes('cpf e cart')) return false;
  const hasStreetWord = /\b(rua|av\.?|avenida|estrada|travessa|alameda|rodovia|pra[çc]a|largo|ladeira|rod\.?|via\b)/i.test(sl);
  const hasNumberPattern = /[^\d],\s*\d{1,5}\b/.test(sl);
  if (!hasStreetWord && !hasNumberPattern) return false;
  return true;
}
function parseOfferJS(texts) {
  const joinedRaw = texts.join('  ');
  const low = joinedRaw.toLowerCase()
    .replace(/\b[lI](?=[.,]\d)/g, '1')
    .replace(/(?<=\d[.,])[lI]\b/g, '1');

  let valor = null;
  let m = /aceitar por\s*r\$\s*([\d.]+,\d{2})/.exec(low);
  if (m) valor = moneyToDoubleJS(m[1]);

  let rkmDirect = null;
  m = /r\$\s*([\d.]+,\d{2})\s*\/\s*km/.exec(low);
  if (m) rkmDirect = moneyToDoubleJS(m[1]);

  let totMin = 0, totKm = 0, legs = 0;
  const legsList = [];
  function addLeg(minsStr, distStr, unit) {
    const mins = parseInt(minsStr, 10);
    const dist = parseFloat(distStr.replace(',', '.'));
    if (isNaN(mins) || isNaN(dist)) return;
    const km2 = unit === 'm' ? dist/1000 : dist;
    if (mins >= 1 && mins <= 90 && km2 >= 0.05 && km2 <= 80) {
      totMin += mins; totKm += km2; legs++; legsList.push([mins, km2]);
    }
  }
  let re = /(\d{1,3})\s*min(?:utos)?\s*\((\d+(?:[.,]\d+)?)\s*(m|km)\)/g;
  while ((m = re.exec(low))) addLeg(m[1], m[2], m[3]);
  re = /\(\s*(\d{1,3})\s*min(?:utos)?\s+(\d+(?:[.,]\d+)?)\s*(m|km)\s*\)/g;
  while ((m = re.exec(low))) addLeg(m[1], m[2], m[3]);

  let km = (legs > 0 && totKm > 0) ? totKm : null;
  let min = (legs > 0 && totMin > 0) ? totMin : null;
  const kmPickup = legsList[0] ? legsList[0][1] : null;
  const kmTrip = legsList[1] ? legsList[1][1] : null;
  const minPickup = legsList[0] ? legsList[0][0] : null;
  const minTrip = legsList[1] ? legsList[1][0] : null;

  let dinamicoBase = 0, dinamicoEspera = 0, dinamicoEmbarque = 0, dinamicoDeslocamento = 0, dinamicoUber = 0;
  m = /r\$\s*([\d.]+,\d{2})\s*tarifa base din[aâ]mica/.exec(low);
  if (m) dinamicoBase = moneyToDoubleJS(m[1]) || 0;
  m = /r\$\s*([\d.]+,\d{2})\s*por espera longa/.exec(low);
  if (m) dinamicoEspera = moneyToDoubleJS(m[1]) || 0;
  m = /r\$\s*([\d.]+,\d{2})\s*por embarque distante/.exec(low);
  if (m) dinamicoEmbarque = moneyToDoubleJS(m[1]) || 0;
  m = /r\$\s*([\d.]+,\d{2})\s*por deslocamento/.exec(low);
  if (m) dinamicoDeslocamento = moneyToDoubleJS(m[1]) || 0;
  m = /\+\s*r\$\s*([\d.]+,\d{2})\s*inclu[ií]do/.exec(low);
  if (m) dinamicoUber = moneyToDoubleJS(m[1]) || 0;
  const dinamico = dinamicoBase + dinamicoEspera + dinamicoEmbarque + dinamicoDeslocamento + dinamicoUber;

  if (valor === null) {
    let monies = extractMoneyJS(joinedRaw).map(moneyToDoubleJS).filter(v => v !== null && v > 0);
    if (rkmDirect !== null) monies = monies.filter(v => Math.abs(v - rkmDirect) > 0.01);
    if (rkmDirect !== null && km !== null) {
      const found = monies.find(v => Math.abs(v/km - rkmDirect)/rkmDirect < 0.35);
      valor = found !== undefined ? found : null;
    } else if (km !== null && km > 0) {
      const found = monies.find(v => (v/km) >= 0.4 && (v/km) <= 12.0);
      valor = found !== undefined ? found : (monies[0] !== undefined ? monies[0] : null);
    } else {
      valor = monies[0] !== undefined ? monies[0] : null;
    }
  }
  if (km === null && valor !== null && rkmDirect !== null && rkmDirect > 0) km = valor / rkmDirect;

  const legLineRe = /^\(?\s*\d{1,3}\s*min(?:utos)?/i;
  const addrCandidates = [];
  for (let i = 0; i < texts.length; i++) {
    if (legLineRe.test(texts[i])) {
      for (let j = i+1; j <= Math.min(i+3, texts.length-1); j++) {
        const cand = (texts[j]||'').trim();
        if (!cand) break;
        const nl = cand.toLowerCase();
        if (legLineRe.test(cand)) break;
        if (nl.includes('aceitar') || nl.includes('selecionar') || nl.includes('escolher')) break;
        if (looksLikeAddress(cand)) { addrCandidates.push(cand); break; }
      }
    }
  }
  return {
    valor, km, min, rkmDirect, dinamico,
    origem: addrCandidates[0] || null, destino: addrCandidates[1] || null,
    kmPickup, kmTrip, minPickup, minTrip
  };
}
// Busca a leitura mais completa (mais linhas no 'raw') da oferta dessa
// corrida, num intervalo de 10min antes do aceite até 30s depois — mesma
// janela usada pelo cache de oferta no app nativo.
async function fetchBestOfferTexts(t) {
  // Retorna { texts, reason } em vez de só null/array — pedido do Yuri
  // (24/07/2026): precisa ver o motivo exato na tela (sem abrir console do
  // navegador) quando não achar nada, pra investigar mais rápido.
  if (!_supaUser) return { texts: null, reason: 'sem sessão ativa' };
  const plat = t.platform === 'uber' ? 'UBER' : '99';
  const anchor = t.accepted_at || t.trip_started_at;
  if (!anchor) return { texts: null, reason: 'corrida sem horário de aceite salvo' };
  const anchorMs = new Date(anchor).getTime();
  const from = new Date(anchorMs - 10*60*1000).toISOString();
  const to = new Date(anchorMs + 30*1000).toISOString();
  const { data, error } = await _SUPA.from('trip_reader_log').select('texts,created_at')
    .eq('user_id', _supaUser.id).eq('platform', plat)
    .gte('created_at', from).lte('created_at', to)
    .order('created_at', { ascending: false })
    // CONFIRMADO EM DADO REAL (24/07/2026): um intervalo de 10min30s desses
    // teve 523 linhas no total (todo tipo de leitura, não só oferta) — o
    // limite de 500 cortava fora justamente as leituras mais antigas, que
    // eram as que tinham a oferta. Margem bem folgada agora.
    .limit(3000);
  if (error) {
    console.error('fetchBestOfferTexts', error);
    return { texts: null, reason: 'erro na busca: '+(error.message || error.code || '?') };
  }
  if (!data || !data.length) return { texts: null, reason: `0 linhas no log entre ${from.slice(11,19)} e ${to.slice(11,19)} (plataforma ${plat})` };
  let best = null, bestLen = -1, offerRows = 0;
  data.forEach(row => {
    const st = row.texts && row.texts.state;
    if (st !== 'OFERTA' && st !== 'OFERTA_OCR') return;
    offerRows++;
    const raw = row.texts && row.texts.raw;
    if (Array.isArray(raw) && raw.length > bestLen) { best = raw; bestLen = raw.length; }
  });
  if (!best) return { texts: null, reason: `${data.length} linhas no log, nenhuma de oferta (plataforma ${plat})` };
  return { texts: best, reason: null };
}
// Lógica central, sem toast/spinner — usada tanto pelo botão manual quanto
// pela rotina automática (24/07/2026). Só CALCULA, não salva — quem chama
// decide se aplica direto ou pede confirmação primeiro (ver
// refreshAutoTripFromLog, 16/08/2026: mudança de valor grande demais pra
// aplicar sem avisar).
async function _computeAutoTripRefresh(tripId) {
  const { data: rows, error: e1 } = await _SUPA.from('auto_trips').select('*').eq('id', tripId).limit(1);
  if (e1 || !rows || !rows.length) return { ok:false, reason:'corrida não encontrada' };
  const t = rows[0];
  const { texts, reason } = await fetchBestOfferTexts(t);
  if (!texts) return { ok:false, reason };
  const parsed = parseOfferJS(texts);
  if (parsed.valor == null) return { ok:false, reason:'texto insuficiente pra reconferir' };
  // Só os campos NUMÉRICOS — testado contra casos reais, o valor sai
  // confiável em várias leituras diferentes da mesma oferta, mas o
  // endereço às vezes sai lixo ("% LUCRO" etc) mesmo na leitura 
===== sessions @ 631258 =====
7 12z"/><circle cx="12" cy="9" r="2.4"/></svg></div>
      <div class="route-body">
        <div><div class="route-label">Destino</div><span class="route-addr">${esc(t.dest_address)}</span></div>
        ${destino.timeHtml}
      </div>
    </div>` : ''}
  </div>` : '';
  const refreshSvg = `<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12a9 9 0 1 1-2.64-6.36M21 3v6h-6"/></svg>`;
  const detRows = [
    t.passenger_name?`<div class="det-row"><span>Passageiro</span><strong>${esc(t.passenger_name)}</strong></div>`:'',
    routeBlock,
    // Reconferir com o log — pedido do Yuri (24/07/2026): reaplica a
    // leitura corrigida em cima do texto original da tela daquele
    // instante, em vez de tentar adivinhar o valor certo.
    `<button class="det-refresh-btn" id="refbtn-${tid}" onclick="event.stopPropagation();refreshAutoTripFromLog('${t.id}',document.getElementById('refbtn-${tid}'))">${refreshSvg}Atualizar informações</button>`,
  ].filter(Boolean).join('');
  const pencilSvg = `<svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M17 3a2.85 2.85 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"/></svg>`;
  // Vínculo à jornada — pedido do Yuri (24/07/2026): ícone verde se a
  // corrida cai dentro do horário de alguma jornada, cinza se não (jornada
  // excluída, ou nunca teve jornada aberta ali) — sinal pra ajustar horário
  // de uma jornada se quiser trazer ela de volta.
  const linked = isAutoTripLinkedToSession(t);
  const linkSvg = `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round"><path d="M9 17H7A5 5 0 0 1 7 7h2M15 7h2a5 5 0 1 1 0 10h-2M8 12h8"/></svg>`;
  const linkTag = `<span class="tap-legend-wrap link-tag ${linked?'on':'off'}" onclick="showTapLegend(event,'${linked?'Vinculada a uma jornada':'Não está em nenhuma jornada'}')">${linkSvg}</span>`;
  const perfStrip = perfStripHtml(t.offer_value, (kmPickupVal||0)+(kmDestVal||0), (durPickupVal||0)+(durDestVal||0));
  return `<div class="trip-item-wrap" id="tw-${tid}">
    <div class="li-row trip-top ${statusCls}" id="tr-${tid}" onclick="toggleTripDetail('${tid}')">
      <div class="li-ico-col">${hhmmTrip?`<span class="li-ico-time">${hhmmTrip}</span>`:''}${platIcon(t.platform)}</div>
      <div class="trip-content-col">
        <div class="trip-title-line"><h3 class="trip-title" style="white-space:normal">${t.origin_address?esc(extractBairro(t.origin_address)):'—'}${dest}</h3>${linkTag}</div>
        <p class="trip-subline">${subLine}</p>
        <div class="li-extra">${extras.join('')}</div>
      </div>
      <div class="li-val">
        <div style="display:flex;align-items:center;gap:5px;justify-content:flex-end">
          <strong>${DB.fR(t.offer_value)}</strong>
          <button class="val-edit-btn" onclick="event.stopPropagation();openAutoTripValueEdit('${t.id}',${t.offer_value||0})" aria-label="Editar valor da corrida">${pencilSvg}</button>
        </div>
        <span class="auto-status ${statusCls}">${statusTxt}</span>
      </div>
    </div>
    <div class="trip-detail-panel" id="td-${tid}" style="display:none">${perfStrip}${detRows}</div>
  </div>`;
}
// Corrige o caso em que uma corrida foi apagada direto no Supabase (ex:
// correção manual) mas continua aparecendo no app. Desde que o pullAll()
// passou a reconciliar automaticamente (removendo local o que já foi
// confirmado sincronizado antes e sumiu da nuvem, sem tocar em nada
// pendente na fila), esse botão só precisa rodar o mesmo pullAll() de
// novo — sem duplicar a lógica de segurança em outro lugar.
async function resyncCorridasFromCloud() {
  if (!_supaUser) { showToast('Sem conexão com a nuvem.'); return; }
  showConfirm('Recarregar da nuvem agora?\nCorridas confirmadas sincronizadas antes que sumiram do servidor saem do celular. Nada que ainda esteja pendente de subir é tocado.', async () => {
    try {
      await SYNC.pullAll();
      renderCorridasActive();
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHome();
      /* sincronização silenciosa: sem toast de sucesso */
    } catch (e) {
      console.error('resyncCorridasFromCloud', e);
      showToast('Erro ao recarregar da nuvem.');
    }
  }, { confirmLabel: 'Recarregar' });
}
function confirmDeleteTrip(date, id) {
  const day = DB.day(date);
  const t = (day.trips||[]).find(x => x.id === id);
  if (!t) return;
  const dest = t.dest ? ' → '+esc(t.dest) : '';
  showConfirm(`Excluir essa corrida?\n${DB.fR(t.value)} · ${t.origin?esc(t.origin):'—'}${dest}`, () => {
    day.trips = day.trips.filter(x => x.id !== id);
    DB.saveDay(day);
    SYNC.del('trips', { id });
    renderCorridasActive();
    if (date === S.curDate) { renderJornada(); }
    showToast('Corrida excluída.');
  }, { confirmLabel: 'Excluir' });
}
function toggleSessionsView() { S.sessionsExpanded = !S.sessionsExpanded; renderJornada(); }
function toggleLancView() { S.lancExpanded = !S.lancExpanded; renderJornada(); }

function renderJornada() {
  if (!S || !DB) return;
  try { updateCustoBanner(); } catch(e) {}
  if (!S.curDate) S.curDate = DB.today();
  const today = DB.today(), d = S.curDate, isToday = d === today;
  const dt    = new Date(d+'T12:00');
  const dows  = ['Domingo','Segunda-feira','Terça-feira','Quarta-feira','Quinta-feira','Sexta-feira','Sábado'];
  const ms    = ['jan','fev','mar','abr','mai','jun','jul','ago','set','out','nov','dez'];
  const msLong= ['janeiro','fevereiro','março','abril','maio','junho','julho','agosto','setembro','outubro','novembro','dezembro'];
  const dateEl = document.getElementById('dn-date');
  // Formato compacto: "23 jun 2026" — cabe em 1 linha
  if (dateEl) dateEl.textContent = isToday ? 'Hoje' : (dt.getDate()+' de '+msLong[dt.getMonth()]+' de '+dt.getFullYear());
  const dowEl = document.getElementById('dn-dow');
  // Dia da semana + badge "Hoje" na mesma linha
  if (dowEl) dowEl.innerHTML = dows[dt.getDay()];
  const day = DB.day(d);
  // Corridas automáticas do dia — busca assíncrona, cacheada por data (não
  // refaz a busca em toda chamada de renderJornada, que roda com frequência
  // inclusive a cada tick do timer da jornada ativa). Refresh periódico via
  // _syncTick (a cada 1min) cobre corridas novas capturadas durante o dia.
  if (S.autoTripsDate !== d) {
    const keepCurrent = S._autoTripsDataDate === d ? (S.autoTrips || []) : [];
    S.autoTripsDate = d;
    S.autoTrips = keepCurrent;
    loadAutoTripsForDay(d).then(list => {
      if (S.autoTripsDate !== d || !Array.isArray(list)) return; // erro mantém último dado válido
      S.autoTrips = list;
      S._autoTripsDataDate = d;
      renderJornada();
    });
  }
  day.autoTrips = S.autoTrips || [];
  // Recusadas do dia — mesmo padrão de cache do autoTrips acima. Lida aqui
  // (S.declined) e usada dentro de renderJourneyCard(), que roda logo a
  // seguir e populate o card "Total do dia".
  if (S.declinedDate !== d) {
    S.declinedDate = d;
    S.declined = [];
    loadDeclinedOffersForDay(d).then(list => {
      if (S.declinedDate !== d) return;
      S.declined = list;
      renderJornada();
    });
  }
  const rev = day.trips.reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0)
            + day.autoTrips.reduce((s,t)=>s+(t.offer_value||0),0);
  const exp = day.expenses.reduce((s,e)=>s+(e.amount||0),0);
  setText('d-rev', DB.fRI(rev));
  setText('d-exp', DB.fRI(exp));
  const netEl = document.getElementById('d-net');
  if (netEl) { netEl.textContent = DB.fRI(rev-exp); netEl.className = 'v ' + (rev-exp>=0 ? 'green' : 'red'); }

  renderJourneyCard(day, isToday);
  renderMetasDoDia(day);

  // Sessões do dia (3 mais recentes, ou todas — sessão atual sempre aparece)
  const sessEl = document.getElementById('sessions-list');
  const sessToggleBtn = document.getElementById('sessions-toggle');
  if (sessEl) {
    const total = (day.sessions||[]).length;
    // Numera pela ORDEM DE HORÁRIO real (start), não pela ordem em que foi
    // salva/sincronizada no aparelho — evita "Jornada 2" mostrar um horário
    // mais tarde que "Jornada 3". origIdx continua apontando pro índice
    // verdadeiro em day.sessions, então editar/apagar continua certo.
    const byTime = (day.sessions||[]).map((s,i)=>i)
      .sort((a,b) => (day.sessions[a].start||0) - (day.sessions[b].start||0));
    const finishedAll = byTime.slice().reverse()
      .map((origIdx,ri)=>sessionItem('Jornada '+(total-ri), day.sessions[origIdx], origIdx, S.curDate, day, false));
    const finishedShown = S.sessionsExpanded ? finishedAll : finishedAll.slice(0,3);
    const liveRow = (isToday && S.gps.st==='running') ? [sessionItem('Jornada atual', {start:S.gps.start,end:null,km:S.gps.km,elapsed:getElapsed()}, -1, S.curDate, day, true)] : [];
    const rows = [...liveRow, ...finishedShown];
    sessEl.innerHTML = rows.length ? rows.join('') : '<div class="empty">Nenhuma sessão registrada</div>';
  }
  if (sessToggleBtn) {
    sessToggleBtn.style.display = (day.sessions||[]).length > 3 ? '' : 'none';
    sessToggleBtn.innerHTML = S.sessionsExpanded ? 'Ver menos' : 'Ver todas &#8250;';
  }
  // Corridas: removido daqui (23/07/2026, pedido do Yuri) — já ficam
  // acessíveis dentro do detalhe de cada jornada (seta expande e lista via
  // sessionItem, que já mescla Importadas + Automáticas). Lista de
  // Importadas ainda mora na tela Corridas separada (navTo('corridas')).
  // Lançamentos (5 mais recentes, ou todos)
  const lEl = document.getElementById('jorn-lanc-list');
  const lancToggleBtn = document.getElementById('lanc-toggle');
  if (lEl) {
    const today = DB.today();
    const allItems = [
      ...day.revenues.map(r=>Object.assign({},r,{_type:'receita'})),
      ...day.expenses.map(e=>Object.assign({},e,{_type:'despesa'}))
    ];
    // Mostrar todos os lançamentos do dia sem filtro de tab
    const items = allItems;
    const show = S.lancExpanded ? items : items.slice(0,5);
    lEl.innerHTML = show.length ? show.map(it => lancItemJornada(it)).join('') : '<div class="empty">Nenhum lançamento registrado</div>';
    if (lancToggleBtn) {
      lancToggleBtn.style.display = items.length > 5 ? '' : 'none';
      lancToggleBtn.innerHTML = S.lancExpanded ? 'Ver menos' : 'Ver todas &#8250;';
    }
  }
  reapplyOpenTripDetail();
  reapplyOpenJss();
}

function renderJourneyCard(day, isToday) {
  const jc = document.getElementById('journey-card');
  if (!jc) return;
  let permWarn = document.getElementById('journey-perm-warning');
  if (!permWarn && jc.parentNode) {
    permWarn = document.createElement('div'); permWarn.id = 'journey-perm-warning'; permWarn.onclick = openFlashPermissionSetup;
    permWarn.style.cssText = 'display:none;align-items:flex-start;justify-content:space-between;gap:10px;background:#FFF7ED;border:1px solid #FDBA74;border-radius:14px;padding:11px 12px;margin:0 0 10px;cursor:pointer';
    permWarn.innerHTML = '<div style="min-width:0"><div style="font-size:12.5px;font-weight:800;color:#9A3412">⚠ Funções do MōB incompletas</div><div id="journey-perm-warning-text" style="font-size:11px;color:#C2410C;line-height:1.35;margin-top:2px"></div></div><span style="font-size:11px;font-weight:800;color:#9A3412;white-space:nowrap">Ativar ›</span>';
    jc.parentNode.insertBefore(permWarn, jc);
  }
  renderFlashPermissionWarnings();
  const cardDate = day?.date || S.curDate || DB.today();
  // Card de sessão atual (timer/km/botão) só faz sentido em "hoje" — em dia
  // passado não existe jornada "em andamento" possível, então esse card
  // some por completo. O restante da função continua rodando igual: os
  // valores que ela calcula (jc-trips, jc-total, jc-kmtotal, jc-kmativo,
  // jc-kmdesl, jc-rsh, jc-rskm) agora moram no card "Total do dia" — que é
  // um elemento IRMÃO de journey-card, não um filho, e precisa continuar
  // sendo populado em qualquer dia, passado ou presente.
  const jcWrap = document.getElementById('journey-wrap');
  if (jcWrap) jcWrap.style.display = isToday ? '' : 'none';
  const g = S.gps;
  const finished = day.sessions || [];
  const finishedElapsed = finished.reduce((s,x)=>s+(x.elapsed||0),0);
  const finishedKm = finished.reduce((s,x)=>s+(x.km||0),0);
  const live = isToday && g.st === 'running';
  const liveElapsed = live ? getElapsed() : 0;
  const liveKm = live ? g.km : 0;
  const totalElapsed = Math.max(0, finishedElapsed + liveElapsed + (day.tempoAdjust || 0));
  const totalKm = finishedKm + liveKm + (day.kmAdjust || 0);
  const state = isToday ? g.st : 'idle';

  let cls = 'idle';
  if (state === 'running') cls = 'running';
  else if (finished.length > 0) cls = 'ended';
  jc.className = 'journey ' + cls;

  setText('jc-status-text',
    state === 'running' ? 'Online' :
    finished.length > 0 ? 'Concluído' : 'Iniciar'
  );
  const th = Math.floor(liveElapsed/3600), tm = Math.floor(liveElapsed%3600/60), ts = liveElapsed%60;
  setText('jc-timer', p2(th)+':'+p2(tm)+':'+p2(ts));
  setText('jc-sub',
    state === 'running' ? 'Sessão atual · iniciada '+fmtHM(g.start) :
    finished.length > 0 ? finished.length+(finished.length===1?' jornada hoje':' jornadas hoje') :
    'Nenhuma sessão iniciada'
  );
  setText('jc-km', liveKm.toFixed(1));
  const tth = Math.floor(totalElapsed/3600), ttm = Math.floor(totalElapsed%3600/60);
  setText('jc-total', tth+'h'+p2(ttm)+'m');
  setText('jc-kmtotal', totalKm.toFixed(1)+' km');
  // CORRIGIDO (23/07/2026, confirmado com dado real pelo Yuri): esses 4
  // KPIs do card principal (corridas, km ativo, R$/h, R$/km) só somavam
  // day.trips (Importadas) — sessão só com automáticas mostrava tudo
  // zerado mesmo com corridas de verdade rodando. Mesma correção que já
  // tinha sido feita em rev/sessionItem/renderMetasDoDia, faltava aqui.
  const autoTripsHoje = day.autoTrips || [];

  // Banner "aguardando confirmação" — pedido do Yuri (16/08/2026), substitui
  // o botão fixo "Importar corridas". Conta corridas que ainda não foram
  // confirmadas por vídeo do histórico (status 'capturada' ou 'estimada') —
  // 'confirmada' só existe depois do vídeo, então isso mede exatamente o
  // que falta conferir naquele dia. Aparece em qualquer dia navegado, some
  // sozinho quando não sobra pendência.
  const pcbEl = document.getElementById('pending-confirm-banner');
  if (pcbEl) {
    // Não interpreta cache ainda carregando como zero corridas. Durante refresh,
    // mantém o último estado visual válido do mesmo dia.
    const autoTripsReady = S._autoTripsDataDate === cardDate;
    if (autoTripsReady) {
      const pendingCount = autoTripsHoje.filter(t => t.status !== 'confirmada' || t.value_needs_review === true).length;
      pcbEl.dataset.dataDate = cardDate;
      if (pendingCount > 0) {
        pcbEl.style.display = 'flex';
        setText('pcb-title', pendingCount + (pendingCount===1 ? ' corrida aguar
===== sessions @ 634201 =====
ton class="val-edit-btn" onclick="event.stopPropagation();openAutoTripValueEdit('${t.id}',${t.offer_value||0})" aria-label="Editar valor da corrida">${pencilSvg}</button>
        </div>
        <span class="auto-status ${statusCls}">${statusTxt}</span>
      </div>
    </div>
    <div class="trip-detail-panel" id="td-${tid}" style="display:none">${perfStrip}${detRows}</div>
  </div>`;
}
// Corrige o caso em que uma corrida foi apagada direto no Supabase (ex:
// correção manual) mas continua aparecendo no app. Desde que o pullAll()
// passou a reconciliar automaticamente (removendo local o que já foi
// confirmado sincronizado antes e sumiu da nuvem, sem tocar em nada
// pendente na fila), esse botão só precisa rodar o mesmo pullAll() de
// novo — sem duplicar a lógica de segurança em outro lugar.
async function resyncCorridasFromCloud() {
  if (!_supaUser) { showToast('Sem conexão com a nuvem.'); return; }
  showConfirm('Recarregar da nuvem agora?\nCorridas confirmadas sincronizadas antes que sumiram do servidor saem do celular. Nada que ainda esteja pendente de subir é tocado.', async () => {
    try {
      await SYNC.pullAll();
      renderCorridasActive();
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHome();
      /* sincronização silenciosa: sem toast de sucesso */
    } catch (e) {
      console.error('resyncCorridasFromCloud', e);
      showToast('Erro ao recarregar da nuvem.');
    }
  }, { confirmLabel: 'Recarregar' });
}
function confirmDeleteTrip(date, id) {
  const day = DB.day(date);
  const t = (day.trips||[]).find(x => x.id === id);
  if (!t) return;
  const dest = t.dest ? ' → '+esc(t.dest) : '';
  showConfirm(`Excluir essa corrida?\n${DB.fR(t.value)} · ${t.origin?esc(t.origin):'—'}${dest}`, () => {
    day.trips = day.trips.filter(x => x.id !== id);
    DB.saveDay(day);
    SYNC.del('trips', { id });
    renderCorridasActive();
    if (date === S.curDate) { renderJornada(); }
    showToast('Corrida excluída.');
  }, { confirmLabel: 'Excluir' });
}
function toggleSessionsView() { S.sessionsExpanded = !S.sessionsExpanded; renderJornada(); }
function toggleLancView() { S.lancExpanded = !S.lancExpanded; renderJornada(); }

function renderJornada() {
  if (!S || !DB) return;
  try { updateCustoBanner(); } catch(e) {}
  if (!S.curDate) S.curDate = DB.today();
  const today = DB.today(), d = S.curDate, isToday = d === today;
  const dt    = new Date(d+'T12:00');
  const dows  = ['Domingo','Segunda-feira','Terça-feira','Quarta-feira','Quinta-feira','Sexta-feira','Sábado'];
  const ms    = ['jan','fev','mar','abr','mai','jun','jul','ago','set','out','nov','dez'];
  const msLong= ['janeiro','fevereiro','março','abril','maio','junho','julho','agosto','setembro','outubro','novembro','dezembro'];
  const dateEl = document.getElementById('dn-date');
  // Formato compacto: "23 jun 2026" — cabe em 1 linha
  if (dateEl) dateEl.textContent = isToday ? 'Hoje' : (dt.getDate()+' de '+msLong[dt.getMonth()]+' de '+dt.getFullYear());
  const dowEl = document.getElementById('dn-dow');
  // Dia da semana + badge "Hoje" na mesma linha
  if (dowEl) dowEl.innerHTML = dows[dt.getDay()];
  const day = DB.day(d);
  // Corridas automáticas do dia — busca assíncrona, cacheada por data (não
  // refaz a busca em toda chamada de renderJornada, que roda com frequência
  // inclusive a cada tick do timer da jornada ativa). Refresh periódico via
  // _syncTick (a cada 1min) cobre corridas novas capturadas durante o dia.
  if (S.autoTripsDate !== d) {
    const keepCurrent = S._autoTripsDataDate === d ? (S.autoTrips || []) : [];
    S.autoTripsDate = d;
    S.autoTrips = keepCurrent;
    loadAutoTripsForDay(d).then(list => {
      if (S.autoTripsDate !== d || !Array.isArray(list)) return; // erro mantém último dado válido
      S.autoTrips = list;
      S._autoTripsDataDate = d;
      renderJornada();
    });
  }
  day.autoTrips = S.autoTrips || [];
  // Recusadas do dia — mesmo padrão de cache do autoTrips acima. Lida aqui
  // (S.declined) e usada dentro de renderJourneyCard(), que roda logo a
  // seguir e populate o card "Total do dia".
  if (S.declinedDate !== d) {
    S.declinedDate = d;
    S.declined = [];
    loadDeclinedOffersForDay(d).then(list => {
      if (S.declinedDate !== d) return;
      S.declined = list;
      renderJornada();
    });
  }
  const rev = day.trips.reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0)
            + day.autoTrips.reduce((s,t)=>s+(t.offer_value||0),0);
  const exp = day.expenses.reduce((s,e)=>s+(e.amount||0),0);
  setText('d-rev', DB.fRI(rev));
  setText('d-exp', DB.fRI(exp));
  const netEl = document.getElementById('d-net');
  if (netEl) { netEl.textContent = DB.fRI(rev-exp); netEl.className = 'v ' + (rev-exp>=0 ? 'green' : 'red'); }

  renderJourneyCard(day, isToday);
  renderMetasDoDia(day);

  // Sessões do dia (3 mais recentes, ou todas — sessão atual sempre aparece)
  const sessEl = document.getElementById('sessions-list');
  const sessToggleBtn = document.getElementById('sessions-toggle');
  if (sessEl) {
    const total = (day.sessions||[]).length;
    // Numera pela ORDEM DE HORÁRIO real (start), não pela ordem em que foi
    // salva/sincronizada no aparelho — evita "Jornada 2" mostrar um horário
    // mais tarde que "Jornada 3". origIdx continua apontando pro índice
    // verdadeiro em day.sessions, então editar/apagar continua certo.
    const byTime = (day.sessions||[]).map((s,i)=>i)
      .sort((a,b) => (day.sessions[a].start||0) - (day.sessions[b].start||0));
    const finishedAll = byTime.slice().reverse()
      .map((origIdx,ri)=>sessionItem('Jornada '+(total-ri), day.sessions[origIdx], origIdx, S.curDate, day, false));
    const finishedShown = S.sessionsExpanded ? finishedAll : finishedAll.slice(0,3);
    const liveRow = (isToday && S.gps.st==='running') ? [sessionItem('Jornada atual', {start:S.gps.start,end:null,km:S.gps.km,elapsed:getElapsed()}, -1, S.curDate, day, true)] : [];
    const rows = [...liveRow, ...finishedShown];
    sessEl.innerHTML = rows.length ? rows.join('') : '<div class="empty">Nenhuma sessão registrada</div>';
  }
  if (sessToggleBtn) {
    sessToggleBtn.style.display = (day.sessions||[]).length > 3 ? '' : 'none';
    sessToggleBtn.innerHTML = S.sessionsExpanded ? 'Ver menos' : 'Ver todas &#8250;';
  }
  // Corridas: removido daqui (23/07/2026, pedido do Yuri) — já ficam
  // acessíveis dentro do detalhe de cada jornada (seta expande e lista via
  // sessionItem, que já mescla Importadas + Automáticas). Lista de
  // Importadas ainda mora na tela Corridas separada (navTo('corridas')).
  // Lançamentos (5 mais recentes, ou todos)
  const lEl = document.getElementById('jorn-lanc-list');
  const lancToggleBtn = document.getElementById('lanc-toggle');
  if (lEl) {
    const today = DB.today();
    const allItems = [
      ...day.revenues.map(r=>Object.assign({},r,{_type:'receita'})),
      ...day.expenses.map(e=>Object.assign({},e,{_type:'despesa'}))
    ];
    // Mostrar todos os lançamentos do dia sem filtro de tab
    const items = allItems;
    const show = S.lancExpanded ? items : items.slice(0,5);
    lEl.innerHTML = show.length ? show.map(it => lancItemJornada(it)).join('') : '<div class="empty">Nenhum lançamento registrado</div>';
    if (lancToggleBtn) {
      lancToggleBtn.style.display = items.length > 5 ? '' : 'none';
      lancToggleBtn.innerHTML = S.lancExpanded ? 'Ver menos' : 'Ver todas &#8250;';
    }
  }
  reapplyOpenTripDetail();
  reapplyOpenJss();
}

function renderJourneyCard(day, isToday) {
  const jc = document.getElementById('journey-card');
  if (!jc) return;
  let permWarn = document.getElementById('journey-perm-warning');
  if (!permWarn && jc.parentNode) {
    permWarn = document.createElement('div'); permWarn.id = 'journey-perm-warning'; permWarn.onclick = openFlashPermissionSetup;
    permWarn.style.cssText = 'display:none;align-items:flex-start;justify-content:space-between;gap:10px;background:#FFF7ED;border:1px solid #FDBA74;border-radius:14px;padding:11px 12px;margin:0 0 10px;cursor:pointer';
    permWarn.innerHTML = '<div style="min-width:0"><div style="font-size:12.5px;font-weight:800;color:#9A3412">⚠ Funções do MōB incompletas</div><div id="journey-perm-warning-text" style="font-size:11px;color:#C2410C;line-height:1.35;margin-top:2px"></div></div><span style="font-size:11px;font-weight:800;color:#9A3412;white-space:nowrap">Ativar ›</span>';
    jc.parentNode.insertBefore(permWarn, jc);
  }
  renderFlashPermissionWarnings();
  const cardDate = day?.date || S.curDate || DB.today();
  // Card de sessão atual (timer/km/botão) só faz sentido em "hoje" — em dia
  // passado não existe jornada "em andamento" possível, então esse card
  // some por completo. O restante da função continua rodando igual: os
  // valores que ela calcula (jc-trips, jc-total, jc-kmtotal, jc-kmativo,
  // jc-kmdesl, jc-rsh, jc-rskm) agora moram no card "Total do dia" — que é
  // um elemento IRMÃO de journey-card, não um filho, e precisa continuar
  // sendo populado em qualquer dia, passado ou presente.
  const jcWrap = document.getElementById('journey-wrap');
  if (jcWrap) jcWrap.style.display = isToday ? '' : 'none';
  const g = S.gps;
  const finished = day.sessions || [];
  const finishedElapsed = finished.reduce((s,x)=>s+(x.elapsed||0),0);
  const finishedKm = finished.reduce((s,x)=>s+(x.km||0),0);
  const live = isToday && g.st === 'running';
  const liveElapsed = live ? getElapsed() : 0;
  const liveKm = live ? g.km : 0;
  const totalElapsed = Math.max(0, finishedElapsed + liveElapsed + (day.tempoAdjust || 0));
  const totalKm = finishedKm + liveKm + (day.kmAdjust || 0);
  const state = isToday ? g.st : 'idle';

  let cls = 'idle';
  if (state === 'running') cls = 'running';
  else if (finished.length > 0) cls = 'ended';
  jc.className = 'journey ' + cls;

  setText('jc-status-text',
    state === 'running' ? 'Online' :
    finished.length > 0 ? 'Concluído' : 'Iniciar'
  );
  const th = Math.floor(liveElapsed/3600), tm = Math.floor(liveElapsed%3600/60), ts = liveElapsed%60;
  setText('jc-timer', p2(th)+':'+p2(tm)+':'+p2(ts));
  setText('jc-sub',
    state === 'running' ? 'Sessão atual · iniciada '+fmtHM(g.start) :
    finished.length > 0 ? finished.length+(finished.length===1?' jornada hoje':' jornadas hoje') :
    'Nenhuma sessão iniciada'
  );
  setText('jc-km', liveKm.toFixed(1));
  const tth = Math.floor(totalElapsed/3600), ttm = Math.floor(totalElapsed%3600/60);
  setText('jc-total', tth+'h'+p2(ttm)+'m');
  setText('jc-kmtotal', totalKm.toFixed(1)+' km');
  // CORRIGIDO (23/07/2026, confirmado com dado real pelo Yuri): esses 4
  // KPIs do card principal (corridas, km ativo, R$/h, R$/km) só somavam
  // day.trips (Importadas) — sessão só com automáticas mostrava tudo
  // zerado mesmo com corridas de verdade rodando. Mesma correção que já
  // tinha sido feita em rev/sessionItem/renderMetasDoDia, faltava aqui.
  const autoTripsHoje = day.autoTrips || [];

  // Banner "aguardando confirmação" — pedido do Yuri (16/08/2026), substitui
  // o botão fixo "Importar corridas". Conta corridas que ainda não foram
  // confirmadas por vídeo do histórico (status 'capturada' ou 'estimada') —
  // 'confirmada' só existe depois do vídeo, então isso mede exatamente o
  // que falta conferir naquele dia. Aparece em qualquer dia navegado, some
  // sozinho quando não sobra pendência.
  const pcbEl = document.getElementById('pending-confirm-banner');
  if (pcbEl) {
    // Não interpreta cache ainda carregando como zero corridas. Durante refresh,
    // mantém o último estado visual válido do mesmo dia.
    const autoTripsReady = S._autoTripsDataDate === cardDate;
    if (autoTripsReady) {
      const pendingCount = autoTripsHoje.filter(t => t.status !== 'confirmada' || t.value_needs_review === true).length;
      pcbEl.dataset.dataDate = cardDate;
      if (pendingCount > 0) {
        pcbEl.style.display = 'flex';
        setText('pcb-title', pendingCount + (pendingCount===1 ? ' corrida aguardando confirmação' : ' corridas aguardando confirmação'));
      } else {
        pcbEl.style.display = 'none';
      }
    } else if (pcbEl.dataset.dataDate && pcbEl.dataset.dataDate !== cardDate) {
      pcbEl.style.display = 'none';
      pcbEl.dataset.dataDate = cardDate;
    }
  }

  // Barra Online/Buscando passageiro/Em corrida — real, vinda da máquina de
  // estados do APK (ver computeStatusBreakdown). No card da jornada ATUAL
  // só faz sentido enquanto a sessão está rodando (é o que está sendo
  // contado agora); ao encerrar, o dado sai daqui e já está consolidado no
  // "Total do dia" logo abaixo — pedido do Yuri (16/08/2026), evita
  // duplicar/deixar a mesma barra "grudada" depois de já ter encerrado.
  // As barras são atualizadas exclusivamente pela timeline contínua V3.
  // A rotina base não sobrescreve mais a leitura real com auto_trips.
  const jcBd = document.getElementById('jc-breakdown');
  const tdBdRow = document.getElementById('td-bd-row');

  setText('jc-trips', day.trips.length + autoTripsHoje.length);
  setText('jc-trips2', day.trips.length + autoTripsHoje.length);
  const aceitasCount = day.trips.length + autoTripsHoje.length;
  const declinedCount = (S.declined || []).length;
  setText('jc-aceitas', aceitasCount);
  setText('jc-recusadas', declinedCount);
  const corridasSubEl = document.getElementById('jc-corridas-sub');
  if (corridasSubEl) corridasSubEl.style.display = (aceitasCount>0 || declinedCount>0) ? 'flex' : 'none';
  const kmAtivo = day.trips.reduce((s,t)=>s+(t.km||0),0)
                + autoTripsHoje.reduce((s,t)=>s+(parseFloat(t.real_km_trip ?? t.offer_km_trip)||0),0);
  const kmDesl = Math.max(0, totalKm - kmAtivo);
  setText('jc-kmativo', kmAtivo.toFixed(1));
  setText('jc-kmdesl', kmDesl.toFixed(1));
  const ganhosDia = day.trips.reduce((s,t)=>s+(t.value||0),0)
                  + autoTripsHoje.reduce((s,t)=>s+(t.offer_value||0),0);
  const horasDecimal = totalElapsed > 0 ? totalElapsed/3600 : 0;
  const rsh  = horasDecimal > 0 ? ganhosDia/horasDecimal : null;
  const rskm = totalKm > 0 ? ganhosDia/totalKm : null;
  setText('jc-rsh', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  setText('jc-rsh2', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm2', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  // Cor por indicador — pedido do Yuri (23/07/2026, mockup aprovado):
  // mesmos limiares vermelho/verde que o motorista já ajusta na régua do
  // MōB Flash (Indicadores → R$/km e R$/hora), cada indicador troca de cor
  // por conta própria. Sem dado ainda (sessão não rodou tempo/km) fica
  // neutro (verde), não faz sentido alarmar antes de ter dado de verdade.
  const rateEl = document.getElementById('jc-rate-hora');
  const kmEl   = document.getElementById('jc-rate-km');
  const rateEl2 = document.getElementById('jc-rate-hora2');
  const kmEl2   = document.getElementById('jc-rate-
===== sessions @ 634969 =====
r a lógica de segurança em outro lugar.
async function resyncCorridasFromCloud() {
  if (!_supaUser) { showToast('Sem conexão com a nuvem.'); return; }
  showConfirm('Recarregar da nuvem agora?\nCorridas confirmadas sincronizadas antes que sumiram do servidor saem do celular. Nada que ainda esteja pendente de subir é tocado.', async () => {
    try {
      await SYNC.pullAll();
      renderCorridasActive();
      if (S.sec === 'jornada') renderJornada();
      if (S.sec === 'relatorios') renderHome();
      /* sincronização silenciosa: sem toast de sucesso */
    } catch (e) {
      console.error('resyncCorridasFromCloud', e);
      showToast('Erro ao recarregar da nuvem.');
    }
  }, { confirmLabel: 'Recarregar' });
}
function confirmDeleteTrip(date, id) {
  const day = DB.day(date);
  const t = (day.trips||[]).find(x => x.id === id);
  if (!t) return;
  const dest = t.dest ? ' → '+esc(t.dest) : '';
  showConfirm(`Excluir essa corrida?\n${DB.fR(t.value)} · ${t.origin?esc(t.origin):'—'}${dest}`, () => {
    day.trips = day.trips.filter(x => x.id !== id);
    DB.saveDay(day);
    SYNC.del('trips', { id });
    renderCorridasActive();
    if (date === S.curDate) { renderJornada(); }
    showToast('Corrida excluída.');
  }, { confirmLabel: 'Excluir' });
}
function toggleSessionsView() { S.sessionsExpanded = !S.sessionsExpanded; renderJornada(); }
function toggleLancView() { S.lancExpanded = !S.lancExpanded; renderJornada(); }

function renderJornada() {
  if (!S || !DB) return;
  try { updateCustoBanner(); } catch(e) {}
  if (!S.curDate) S.curDate = DB.today();
  const today = DB.today(), d = S.curDate, isToday = d === today;
  const dt    = new Date(d+'T12:00');
  const dows  = ['Domingo','Segunda-feira','Terça-feira','Quarta-feira','Quinta-feira','Sexta-feira','Sábado'];
  const ms    = ['jan','fev','mar','abr','mai','jun','jul','ago','set','out','nov','dez'];
  const msLong= ['janeiro','fevereiro','março','abril','maio','junho','julho','agosto','setembro','outubro','novembro','dezembro'];
  const dateEl = document.getElementById('dn-date');
  // Formato compacto: "23 jun 2026" — cabe em 1 linha
  if (dateEl) dateEl.textContent = isToday ? 'Hoje' : (dt.getDate()+' de '+msLong[dt.getMonth()]+' de '+dt.getFullYear());
  const dowEl = document.getElementById('dn-dow');
  // Dia da semana + badge "Hoje" na mesma linha
  if (dowEl) dowEl.innerHTML = dows[dt.getDay()];
  const day = DB.day(d);
  // Corridas automáticas do dia — busca assíncrona, cacheada por data (não
  // refaz a busca em toda chamada de renderJornada, que roda com frequência
  // inclusive a cada tick do timer da jornada ativa). Refresh periódico via
  // _syncTick (a cada 1min) cobre corridas novas capturadas durante o dia.
  if (S.autoTripsDate !== d) {
    const keepCurrent = S._autoTripsDataDate === d ? (S.autoTrips || []) : [];
    S.autoTripsDate = d;
    S.autoTrips = keepCurrent;
    loadAutoTripsForDay(d).then(list => {
      if (S.autoTripsDate !== d || !Array.isArray(list)) return; // erro mantém último dado válido
      S.autoTrips = list;
      S._autoTripsDataDate = d;
      renderJornada();
    });
  }
  day.autoTrips = S.autoTrips || [];
  // Recusadas do dia — mesmo padrão de cache do autoTrips acima. Lida aqui
  // (S.declined) e usada dentro de renderJourneyCard(), que roda logo a
  // seguir e populate o card "Total do dia".
  if (S.declinedDate !== d) {
    S.declinedDate = d;
    S.declined = [];
    loadDeclinedOffersForDay(d).then(list => {
      if (S.declinedDate !== d) return;
      S.declined = list;
      renderJornada();
    });
  }
  const rev = day.trips.reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0)
            + day.autoTrips.reduce((s,t)=>s+(t.offer_value||0),0);
  const exp = day.expenses.reduce((s,e)=>s+(e.amount||0),0);
  setText('d-rev', DB.fRI(rev));
  setText('d-exp', DB.fRI(exp));
  const netEl = document.getElementById('d-net');
  if (netEl) { netEl.textContent = DB.fRI(rev-exp); netEl.className = 'v ' + (rev-exp>=0 ? 'green' : 'red'); }

  renderJourneyCard(day, isToday);
  renderMetasDoDia(day);

  // Sessões do dia (3 mais recentes, ou todas — sessão atual sempre aparece)
  const sessEl = document.getElementById('sessions-list');
  const sessToggleBtn = document.getElementById('sessions-toggle');
  if (sessEl) {
    const total = (day.sessions||[]).length;
    // Numera pela ORDEM DE HORÁRIO real (start), não pela ordem em que foi
    // salva/sincronizada no aparelho — evita "Jornada 2" mostrar um horário
    // mais tarde que "Jornada 3". origIdx continua apontando pro índice
    // verdadeiro em day.sessions, então editar/apagar continua certo.
    const byTime = (day.sessions||[]).map((s,i)=>i)
      .sort((a,b) => (day.sessions[a].start||0) - (day.sessions[b].start||0));
    const finishedAll = byTime.slice().reverse()
      .map((origIdx,ri)=>sessionItem('Jornada '+(total-ri), day.sessions[origIdx], origIdx, S.curDate, day, false));
    const finishedShown = S.sessionsExpanded ? finishedAll : finishedAll.slice(0,3);
    const liveRow = (isToday && S.gps.st==='running') ? [sessionItem('Jornada atual', {start:S.gps.start,end:null,km:S.gps.km,elapsed:getElapsed()}, -1, S.curDate, day, true)] : [];
    const rows = [...liveRow, ...finishedShown];
    sessEl.innerHTML = rows.length ? rows.join('') : '<div class="empty">Nenhuma sessão registrada</div>';
  }
  if (sessToggleBtn) {
    sessToggleBtn.style.display = (day.sessions||[]).length > 3 ? '' : 'none';
    sessToggleBtn.innerHTML = S.sessionsExpanded ? 'Ver menos' : 'Ver todas &#8250;';
  }
  // Corridas: removido daqui (23/07/2026, pedido do Yuri) — já ficam
  // acessíveis dentro do detalhe de cada jornada (seta expande e lista via
  // sessionItem, que já mescla Importadas + Automáticas). Lista de
  // Importadas ainda mora na tela Corridas separada (navTo('corridas')).
  // Lançamentos (5 mais recentes, ou todos)
  const lEl = document.getElementById('jorn-lanc-list');
  const lancToggleBtn = document.getElementById('lanc-toggle');
  if (lEl) {
    const today = DB.today();
    const allItems = [
      ...day.revenues.map(r=>Object.assign({},r,{_type:'receita'})),
      ...day.expenses.map(e=>Object.assign({},e,{_type:'despesa'}))
    ];
    // Mostrar todos os lançamentos do dia sem filtro de tab
    const items = allItems;
    const show = S.lancExpanded ? items : items.slice(0,5);
    lEl.innerHTML = show.length ? show.map(it => lancItemJornada(it)).join('') : '<div class="empty">Nenhum lançamento registrado</div>';
    if (lancToggleBtn) {
      lancToggleBtn.style.display = items.length > 5 ? '' : 'none';
      lancToggleBtn.innerHTML = S.lancExpanded ? 'Ver menos' : 'Ver todas &#8250;';
    }
  }
  reapplyOpenTripDetail();
  reapplyOpenJss();
}

function renderJourneyCard(day, isToday) {
  const jc = document.getElementById('journey-card');
  if (!jc) return;
  let permWarn = document.getElementById('journey-perm-warning');
  if (!permWarn && jc.parentNode) {
    permWarn = document.createElement('div'); permWarn.id = 'journey-perm-warning'; permWarn.onclick = openFlashPermissionSetup;
    permWarn.style.cssText = 'display:none;align-items:flex-start;justify-content:space-between;gap:10px;background:#FFF7ED;border:1px solid #FDBA74;border-radius:14px;padding:11px 12px;margin:0 0 10px;cursor:pointer';
    permWarn.innerHTML = '<div style="min-width:0"><div style="font-size:12.5px;font-weight:800;color:#9A3412">⚠ Funções do MōB incompletas</div><div id="journey-perm-warning-text" style="font-size:11px;color:#C2410C;line-height:1.35;margin-top:2px"></div></div><span style="font-size:11px;font-weight:800;color:#9A3412;white-space:nowrap">Ativar ›</span>';
    jc.parentNode.insertBefore(permWarn, jc);
  }
  renderFlashPermissionWarnings();
  const cardDate = day?.date || S.curDate || DB.today();
  // Card de sessão atual (timer/km/botão) só faz sentido em "hoje" — em dia
  // passado não existe jornada "em andamento" possível, então esse card
  // some por completo. O restante da função continua rodando igual: os
  // valores que ela calcula (jc-trips, jc-total, jc-kmtotal, jc-kmativo,
  // jc-kmdesl, jc-rsh, jc-rskm) agora moram no card "Total do dia" — que é
  // um elemento IRMÃO de journey-card, não um filho, e precisa continuar
  // sendo populado em qualquer dia, passado ou presente.
  const jcWrap = document.getElementById('journey-wrap');
  if (jcWrap) jcWrap.style.display = isToday ? '' : 'none';
  const g = S.gps;
  const finished = day.sessions || [];
  const finishedElapsed = finished.reduce((s,x)=>s+(x.elapsed||0),0);
  const finishedKm = finished.reduce((s,x)=>s+(x.km||0),0);
  const live = isToday && g.st === 'running';
  const liveElapsed = live ? getElapsed() : 0;
  const liveKm = live ? g.km : 0;
  const totalElapsed = Math.max(0, finishedElapsed + liveElapsed + (day.tempoAdjust || 0));
  const totalKm = finishedKm + liveKm + (day.kmAdjust || 0);
  const state = isToday ? g.st : 'idle';

  let cls = 'idle';
  if (state === 'running') cls = 'running';
  else if (finished.length > 0) cls = 'ended';
  jc.className = 'journey ' + cls;

  setText('jc-status-text',
    state === 'running' ? 'Online' :
    finished.length > 0 ? 'Concluído' : 'Iniciar'
  );
  const th = Math.floor(liveElapsed/3600), tm = Math.floor(liveElapsed%3600/60), ts = liveElapsed%60;
  setText('jc-timer', p2(th)+':'+p2(tm)+':'+p2(ts));
  setText('jc-sub',
    state === 'running' ? 'Sessão atual · iniciada '+fmtHM(g.start) :
    finished.length > 0 ? finished.length+(finished.length===1?' jornada hoje':' jornadas hoje') :
    'Nenhuma sessão iniciada'
  );
  setText('jc-km', liveKm.toFixed(1));
  const tth = Math.floor(totalElapsed/3600), ttm = Math.floor(totalElapsed%3600/60);
  setText('jc-total', tth+'h'+p2(ttm)+'m');
  setText('jc-kmtotal', totalKm.toFixed(1)+' km');
  // CORRIGIDO (23/07/2026, confirmado com dado real pelo Yuri): esses 4
  // KPIs do card principal (corridas, km ativo, R$/h, R$/km) só somavam
  // day.trips (Importadas) — sessão só com automáticas mostrava tudo
  // zerado mesmo com corridas de verdade rodando. Mesma correção que já
  // tinha sido feita em rev/sessionItem/renderMetasDoDia, faltava aqui.
  const autoTripsHoje = day.autoTrips || [];

  // Banner "aguardando confirmação" — pedido do Yuri (16/08/2026), substitui
  // o botão fixo "Importar corridas". Conta corridas que ainda não foram
  // confirmadas por vídeo do histórico (status 'capturada' ou 'estimada') —
  // 'confirmada' só existe depois do vídeo, então isso mede exatamente o
  // que falta conferir naquele dia. Aparece em qualquer dia navegado, some
  // sozinho quando não sobra pendência.
  const pcbEl = document.getElementById('pending-confirm-banner');
  if (pcbEl) {
    // Não interpreta cache ainda carregando como zero corridas. Durante refresh,
    // mantém o último estado visual válido do mesmo dia.
    const autoTripsReady = S._autoTripsDataDate === cardDate;
    if (autoTripsReady) {
      const pendingCount = autoTripsHoje.filter(t => t.status !== 'confirmada' || t.value_needs_review === true).length;
      pcbEl.dataset.dataDate = cardDate;
      if (pendingCount > 0) {
        pcbEl.style.display = 'flex';
        setText('pcb-title', pendingCount + (pendingCount===1 ? ' corrida aguardando confirmação' : ' corridas aguardando confirmação'));
      } else {
        pcbEl.style.display = 'none';
      }
    } else if (pcbEl.dataset.dataDate && pcbEl.dataset.dataDate !== cardDate) {
      pcbEl.style.display = 'none';
      pcbEl.dataset.dataDate = cardDate;
    }
  }

  // Barra Online/Buscando passageiro/Em corrida — real, vinda da máquina de
  // estados do APK (ver computeStatusBreakdown). No card da jornada ATUAL
  // só faz sentido enquanto a sessão está rodando (é o que está sendo
  // contado agora); ao encerrar, o dado sai daqui e já está consolidado no
  // "Total do dia" logo abaixo — pedido do Yuri (16/08/2026), evita
  // duplicar/deixar a mesma barra "grudada" depois de já ter encerrado.
  // As barras são atualizadas exclusivamente pela timeline contínua V3.
  // A rotina base não sobrescreve mais a leitura real com auto_trips.
  const jcBd = document.getElementById('jc-breakdown');
  const tdBdRow = document.getElementById('td-bd-row');

  setText('jc-trips', day.trips.length + autoTripsHoje.length);
  setText('jc-trips2', day.trips.length + autoTripsHoje.length);
  const aceitasCount = day.trips.length + autoTripsHoje.length;
  const declinedCount = (S.declined || []).length;
  setText('jc-aceitas', aceitasCount);
  setText('jc-recusadas', declinedCount);
  const corridasSubEl = document.getElementById('jc-corridas-sub');
  if (corridasSubEl) corridasSubEl.style.display = (aceitasCount>0 || declinedCount>0) ? 'flex' : 'none';
  const kmAtivo = day.trips.reduce((s,t)=>s+(t.km||0),0)
                + autoTripsHoje.reduce((s,t)=>s+(parseFloat(t.real_km_trip ?? t.offer_km_trip)||0),0);
  const kmDesl = Math.max(0, totalKm - kmAtivo);
  setText('jc-kmativo', kmAtivo.toFixed(1));
  setText('jc-kmdesl', kmDesl.toFixed(1));
  const ganhosDia = day.trips.reduce((s,t)=>s+(t.value||0),0)
                  + autoTripsHoje.reduce((s,t)=>s+(t.offer_value||0),0);
  const horasDecimal = totalElapsed > 0 ? totalElapsed/3600 : 0;
  const rsh  = horasDecimal > 0 ? ganhosDia/horasDecimal : null;
  const rskm = totalKm > 0 ? ganhosDia/totalKm : null;
  setText('jc-rsh', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  setText('jc-rsh2', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm2', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  // Cor por indicador — pedido do Yuri (23/07/2026, mockup aprovado):
  // mesmos limiares vermelho/verde que o motorista já ajusta na régua do
  // MōB Flash (Indicadores → R$/km e R$/hora), cada indicador troca de cor
  // por conta própria. Sem dado ainda (sessão não rodou tempo/km) fica
  // neutro (verde), não faz sentido alarmar antes de ter dado de verdade.
  const rateEl = document.getElementById('jc-rate-hora');
  const kmEl   = document.getElementById('jc-rate-km');
  const rateEl2 = document.getElementById('jc-rate-hora2');
  const kmEl2   = document.getElementById('jc-rate-km2');
  const rateCls = rsh !== null ? rateColorClass('rhora', rsh) : '';
  const kmCls   = rskm !== null ? rateColorClass('rkm', rskm) : '';
  if (rateEl)  rateEl.className  = 'td-rcard ' + rateCls;
  if (kmEl)    kmEl.className    = 'td-rcard ' + kmCls;
  if (rateEl2) rateEl2.className = 'rate-cell ' + rateCls;
  if (kmEl2)   kmEl2.className   = 'rate-cell ' + kmCls;

  const actions = document.getElementById('jc-actions');
  if (!actions) return;
  if (!isToday) {
    actions.innerHTML = '';
  } else if (state === 'idle') {
    actions.innerHTML = `<button class="j-round-btn play" onclick="gpsStart()" aria-label="Iniciar jornada"><svg viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg></button>`;
  } else if (state === 'running') {
  
===== sessions @ 638478 =====
edDate !== d) return;
      S.declined = list;
      renderJornada();
    });
  }
  const rev = day.trips.reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0)
            + day.autoTrips.reduce((s,t)=>s+(t.offer_value||0),0);
  const exp = day.expenses.reduce((s,e)=>s+(e.amount||0),0);
  setText('d-rev', DB.fRI(rev));
  setText('d-exp', DB.fRI(exp));
  const netEl = document.getElementById('d-net');
  if (netEl) { netEl.textContent = DB.fRI(rev-exp); netEl.className = 'v ' + (rev-exp>=0 ? 'green' : 'red'); }

  renderJourneyCard(day, isToday);
  renderMetasDoDia(day);

  // Sessões do dia (3 mais recentes, ou todas — sessão atual sempre aparece)
  const sessEl = document.getElementById('sessions-list');
  const sessToggleBtn = document.getElementById('sessions-toggle');
  if (sessEl) {
    const total = (day.sessions||[]).length;
    // Numera pela ORDEM DE HORÁRIO real (start), não pela ordem em que foi
    // salva/sincronizada no aparelho — evita "Jornada 2" mostrar um horário
    // mais tarde que "Jornada 3". origIdx continua apontando pro índice
    // verdadeiro em day.sessions, então editar/apagar continua certo.
    const byTime = (day.sessions||[]).map((s,i)=>i)
      .sort((a,b) => (day.sessions[a].start||0) - (day.sessions[b].start||0));
    const finishedAll = byTime.slice().reverse()
      .map((origIdx,ri)=>sessionItem('Jornada '+(total-ri), day.sessions[origIdx], origIdx, S.curDate, day, false));
    const finishedShown = S.sessionsExpanded ? finishedAll : finishedAll.slice(0,3);
    const liveRow = (isToday && S.gps.st==='running') ? [sessionItem('Jornada atual', {start:S.gps.start,end:null,km:S.gps.km,elapsed:getElapsed()}, -1, S.curDate, day, true)] : [];
    const rows = [...liveRow, ...finishedShown];
    sessEl.innerHTML = rows.length ? rows.join('') : '<div class="empty">Nenhuma sessão registrada</div>';
  }
  if (sessToggleBtn) {
    sessToggleBtn.style.display = (day.sessions||[]).length > 3 ? '' : 'none';
    sessToggleBtn.innerHTML = S.sessionsExpanded ? 'Ver menos' : 'Ver todas &#8250;';
  }
  // Corridas: removido daqui (23/07/2026, pedido do Yuri) — já ficam
  // acessíveis dentro do detalhe de cada jornada (seta expande e lista via
  // sessionItem, que já mescla Importadas + Automáticas). Lista de
  // Importadas ainda mora na tela Corridas separada (navTo('corridas')).
  // Lançamentos (5 mais recentes, ou todos)
  const lEl = document.getElementById('jorn-lanc-list');
  const lancToggleBtn = document.getElementById('lanc-toggle');
  if (lEl) {
    const today = DB.today();
    const allItems = [
      ...day.revenues.map(r=>Object.assign({},r,{_type:'receita'})),
      ...day.expenses.map(e=>Object.assign({},e,{_type:'despesa'}))
    ];
    // Mostrar todos os lançamentos do dia sem filtro de tab
    const items = allItems;
    const show = S.lancExpanded ? items : items.slice(0,5);
    lEl.innerHTML = show.length ? show.map(it => lancItemJornada(it)).join('') : '<div class="empty">Nenhum lançamento registrado</div>';
    if (lancToggleBtn) {
      lancToggleBtn.style.display = items.length > 5 ? '' : 'none';
      lancToggleBtn.innerHTML = S.lancExpanded ? 'Ver menos' : 'Ver todas &#8250;';
    }
  }
  reapplyOpenTripDetail();
  reapplyOpenJss();
}

function renderJourneyCard(day, isToday) {
  const jc = document.getElementById('journey-card');
  if (!jc) return;
  let permWarn = document.getElementById('journey-perm-warning');
  if (!permWarn && jc.parentNode) {
    permWarn = document.createElement('div'); permWarn.id = 'journey-perm-warning'; permWarn.onclick = openFlashPermissionSetup;
    permWarn.style.cssText = 'display:none;align-items:flex-start;justify-content:space-between;gap:10px;background:#FFF7ED;border:1px solid #FDBA74;border-radius:14px;padding:11px 12px;margin:0 0 10px;cursor:pointer';
    permWarn.innerHTML = '<div style="min-width:0"><div style="font-size:12.5px;font-weight:800;color:#9A3412">⚠ Funções do MōB incompletas</div><div id="journey-perm-warning-text" style="font-size:11px;color:#C2410C;line-height:1.35;margin-top:2px"></div></div><span style="font-size:11px;font-weight:800;color:#9A3412;white-space:nowrap">Ativar ›</span>';
    jc.parentNode.insertBefore(permWarn, jc);
  }
  renderFlashPermissionWarnings();
  const cardDate = day?.date || S.curDate || DB.today();
  // Card de sessão atual (timer/km/botão) só faz sentido em "hoje" — em dia
  // passado não existe jornada "em andamento" possível, então esse card
  // some por completo. O restante da função continua rodando igual: os
  // valores que ela calcula (jc-trips, jc-total, jc-kmtotal, jc-kmativo,
  // jc-kmdesl, jc-rsh, jc-rskm) agora moram no card "Total do dia" — que é
  // um elemento IRMÃO de journey-card, não um filho, e precisa continuar
  // sendo populado em qualquer dia, passado ou presente.
  const jcWrap = document.getElementById('journey-wrap');
  if (jcWrap) jcWrap.style.display = isToday ? '' : 'none';
  const g = S.gps;
  const finished = day.sessions || [];
  const finishedElapsed = finished.reduce((s,x)=>s+(x.elapsed||0),0);
  const finishedKm = finished.reduce((s,x)=>s+(x.km||0),0);
  const live = isToday && g.st === 'running';
  const liveElapsed = live ? getElapsed() : 0;
  const liveKm = live ? g.km : 0;
  const totalElapsed = Math.max(0, finishedElapsed + liveElapsed + (day.tempoAdjust || 0));
  const totalKm = finishedKm + liveKm + (day.kmAdjust || 0);
  const state = isToday ? g.st : 'idle';

  let cls = 'idle';
  if (state === 'running') cls = 'running';
  else if (finished.length > 0) cls = 'ended';
  jc.className = 'journey ' + cls;

  setText('jc-status-text',
    state === 'running' ? 'Online' :
    finished.length > 0 ? 'Concluído' : 'Iniciar'
  );
  const th = Math.floor(liveElapsed/3600), tm = Math.floor(liveElapsed%3600/60), ts = liveElapsed%60;
  setText('jc-timer', p2(th)+':'+p2(tm)+':'+p2(ts));
  setText('jc-sub',
    state === 'running' ? 'Sessão atual · iniciada '+fmtHM(g.start) :
    finished.length > 0 ? finished.length+(finished.length===1?' jornada hoje':' jornadas hoje') :
    'Nenhuma sessão iniciada'
  );
  setText('jc-km', liveKm.toFixed(1));
  const tth = Math.floor(totalElapsed/3600), ttm = Math.floor(totalElapsed%3600/60);
  setText('jc-total', tth+'h'+p2(ttm)+'m');
  setText('jc-kmtotal', totalKm.toFixed(1)+' km');
  // CORRIGIDO (23/07/2026, confirmado com dado real pelo Yuri): esses 4
  // KPIs do card principal (corridas, km ativo, R$/h, R$/km) só somavam
  // day.trips (Importadas) — sessão só com automáticas mostrava tudo
  // zerado mesmo com corridas de verdade rodando. Mesma correção que já
  // tinha sido feita em rev/sessionItem/renderMetasDoDia, faltava aqui.
  const autoTripsHoje = day.autoTrips || [];

  // Banner "aguardando confirmação" — pedido do Yuri (16/08/2026), substitui
  // o botão fixo "Importar corridas". Conta corridas que ainda não foram
  // confirmadas por vídeo do histórico (status 'capturada' ou 'estimada') —
  // 'confirmada' só existe depois do vídeo, então isso mede exatamente o
  // que falta conferir naquele dia. Aparece em qualquer dia navegado, some
  // sozinho quando não sobra pendência.
  const pcbEl = document.getElementById('pending-confirm-banner');
  if (pcbEl) {
    // Não interpreta cache ainda carregando como zero corridas. Durante refresh,
    // mantém o último estado visual válido do mesmo dia.
    const autoTripsReady = S._autoTripsDataDate === cardDate;
    if (autoTripsReady) {
      const pendingCount = autoTripsHoje.filter(t => t.status !== 'confirmada' || t.value_needs_review === true).length;
      pcbEl.dataset.dataDate = cardDate;
      if (pendingCount > 0) {
        pcbEl.style.display = 'flex';
        setText('pcb-title', pendingCount + (pendingCount===1 ? ' corrida aguardando confirmação' : ' corridas aguardando confirmação'));
      } else {
        pcbEl.style.display = 'none';
      }
    } else if (pcbEl.dataset.dataDate && pcbEl.dataset.dataDate !== cardDate) {
      pcbEl.style.display = 'none';
      pcbEl.dataset.dataDate = cardDate;
    }
  }

  // Barra Online/Buscando passageiro/Em corrida — real, vinda da máquina de
  // estados do APK (ver computeStatusBreakdown). No card da jornada ATUAL
  // só faz sentido enquanto a sessão está rodando (é o que está sendo
  // contado agora); ao encerrar, o dado sai daqui e já está consolidado no
  // "Total do dia" logo abaixo — pedido do Yuri (16/08/2026), evita
  // duplicar/deixar a mesma barra "grudada" depois de já ter encerrado.
  // As barras são atualizadas exclusivamente pela timeline contínua V3.
  // A rotina base não sobrescreve mais a leitura real com auto_trips.
  const jcBd = document.getElementById('jc-breakdown');
  const tdBdRow = document.getElementById('td-bd-row');

  setText('jc-trips', day.trips.length + autoTripsHoje.length);
  setText('jc-trips2', day.trips.length + autoTripsHoje.length);
  const aceitasCount = day.trips.length + autoTripsHoje.length;
  const declinedCount = (S.declined || []).length;
  setText('jc-aceitas', aceitasCount);
  setText('jc-recusadas', declinedCount);
  const corridasSubEl = document.getElementById('jc-corridas-sub');
  if (corridasSubEl) corridasSubEl.style.display = (aceitasCount>0 || declinedCount>0) ? 'flex' : 'none';
  const kmAtivo = day.trips.reduce((s,t)=>s+(t.km||0),0)
                + autoTripsHoje.reduce((s,t)=>s+(parseFloat(t.real_km_trip ?? t.offer_km_trip)||0),0);
  const kmDesl = Math.max(0, totalKm - kmAtivo);
  setText('jc-kmativo', kmAtivo.toFixed(1));
  setText('jc-kmdesl', kmDesl.toFixed(1));
  const ganhosDia = day.trips.reduce((s,t)=>s+(t.value||0),0)
                  + autoTripsHoje.reduce((s,t)=>s+(t.offer_value||0),0);
  const horasDecimal = totalElapsed > 0 ? totalElapsed/3600 : 0;
  const rsh  = horasDecimal > 0 ? ganhosDia/horasDecimal : null;
  const rskm = totalKm > 0 ? ganhosDia/totalKm : null;
  setText('jc-rsh', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  setText('jc-rsh2', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm2', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  // Cor por indicador — pedido do Yuri (23/07/2026, mockup aprovado):
  // mesmos limiares vermelho/verde que o motorista já ajusta na régua do
  // MōB Flash (Indicadores → R$/km e R$/hora), cada indicador troca de cor
  // por conta própria. Sem dado ainda (sessão não rodou tempo/km) fica
  // neutro (verde), não faz sentido alarmar antes de ter dado de verdade.
  const rateEl = document.getElementById('jc-rate-hora');
  const kmEl   = document.getElementById('jc-rate-km');
  const rateEl2 = document.getElementById('jc-rate-hora2');
  const kmEl2   = document.getElementById('jc-rate-km2');
  const rateCls = rsh !== null ? rateColorClass('rhora', rsh) : '';
  const kmCls   = rskm !== null ? rateColorClass('rkm', rskm) : '';
  if (rateEl)  rateEl.className  = 'td-rcard ' + rateCls;
  if (kmEl)    kmEl.className    = 'td-rcard ' + kmCls;
  if (rateEl2) rateEl2.className = 'rate-cell ' + rateCls;
  if (kmEl2)   kmEl2.className   = 'rate-cell ' + kmCls;

  const actions = document.getElementById('jc-actions');
  if (!actions) return;
  if (!isToday) {
    actions.innerHTML = '';
  } else if (state === 'idle') {
    actions.innerHTML = `<button class="j-round-btn play" onclick="gpsStart()" aria-label="Iniciar jornada"><svg viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg></button>`;
  } else if (state === 'running') {
    actions.innerHTML = '<button class="j-round-btn stop" onclick="confirmStop()" aria-label="Encerrar jornada"><svg viewBox="0 0 24 24" fill="currentColor"><rect x="6" y="6" width="12" height="12"/></svg></button>';
  }
}

function agMinutesBetween(start, end) {
  if (!start || !end) return 0;
  const [h1,m1] = start.split(':').map(Number);
  const [h2,m2] = end.split(':').map(Number);
  return Math.max(0, (h2*60+m2) - (h1*60+m1));
}
function openMetasHojeModal() {
  const date = S.curDate || DB.today();
  const day = DB.day(date);
  const dow = new Date(date+'T12:00').getDay();
  const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
  const meta = DB.meta();
  const ov = day.metaOverride || null;
  const dimMes = new Date(new Date(date+'T12:00').getFullYear(), new Date(date+'T12:00').getMonth()+1, 0).getDate();
  const moneyGoal = (ov && ov.money) ? ov.money : ((plan && plan.moneyGoal) ? plan.moneyGoal : (meta.general ? meta.general/dimMes : 0));
  const kmGoal = (ov && ov.km) ? ov.km : (plan && plan.kmGoal ? plan.kmGoal : 0);
  const schedMin = (ov && ov.hours) ? ov.hours*60 : (plan ? (plan.schedules||[]).reduce((s,b)=>s+agMinutesBetween(b.start,b.end),0) : 0);
  const mEl = document.getElementById('mh-money'); if (mEl) mEl.value = moneyGoal ? moneyGoal.toFixed(2) : '';
  const kEl = document.getElementById('mh-km'); if (kEl) kEl.value = kmGoal || '';
  const hEl = document.getElementById('mh-hours'); if (hEl) hEl.value = schedMin ? (schedMin/60).toFixed(1) : '';
  const subEl = document.getElementById('mh-sub');
  if (subEl) {
    const isToday = date === DB.today();
    subEl.textContent = isToday ? 'Vale só para hoje' : ('Vale só para ' + new Date(date+'T12:00').toLocaleDateString('pt-BR',{weekday:'long',day:'numeric',month:'long'}));
  }
  openMo('mo-metas-hoje');
}
function saveMetasHoje() {
  const date = S.curDate || DB.today();
  const day = DB.day(date);
  const money = parseFloat((document.getElementById('mh-money')||{}).value) || 0;
  const km = parseFloat((document.getElementById('mh-km')||{}).value) || 0;
  const hours = parseFloat((document.getElementById('mh-hours')||{}).value) || 0;
  day.metaOverride = { money, km, hours };
  DB.saveDay(day);
  closeMo('mo-metas-hoje');
  try { renderMetasDoDia(day); } catch(e) {}
  showToast('Metas de hoje atualizadas!', 'success');
}
function renderMetasDoDia(day) {
  const stack = document.getElementById('metas-dia-stack');
  if (!stack) return;
  const rev = day.trips.reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0)
            + (day.autoTrips||[]).reduce((s,t)=>s+(t.offer_value||0),0);
  const isToday = day.date === DB.today();
  const liveOn = isToday && S.gps.st==='running';
  const totalKm = (day.sessions||[]).reduce((s,x)=>s+(x.km||0),0) + (liveOn ? S.gps.km : 0) + (day.kmAdjust || 0);
  const totalElapsed = Math.max(0, (day.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0) + (liveOn ? getElapsed() : 0) + (day.tempoAdjust || 0));
  const dow = new Date(day.date+'T12:00').getDay();
  const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
  const meta = DB.meta();
  const ov = day.metaOverride || null; // ajuste específico deste dia (via modal 'Metas de hoje')
  const dimMes = new Date(new Date(day.date+'T12:00').getFullYear(), new Date(day.date+'T12:00').getMonth()+1, 0).getDate();
  const moneyGoal = (ov && ov.money) ? ov.money : ((plan && plan.moneyGoal) ? plan.moneyGoal : (meta.general ? meta.general/dimMes : 0));

  
===== sessions @ 647724 =====
? 'flex' : 'none';
  const kmAtivo = day.trips.reduce((s,t)=>s+(t.km||0),0)
                + autoTripsHoje.reduce((s,t)=>s+(parseFloat(t.real_km_trip ?? t.offer_km_trip)||0),0);
  const kmDesl = Math.max(0, totalKm - kmAtivo);
  setText('jc-kmativo', kmAtivo.toFixed(1));
  setText('jc-kmdesl', kmDesl.toFixed(1));
  const ganhosDia = day.trips.reduce((s,t)=>s+(t.value||0),0)
                  + autoTripsHoje.reduce((s,t)=>s+(t.offer_value||0),0);
  const horasDecimal = totalElapsed > 0 ? totalElapsed/3600 : 0;
  const rsh  = horasDecimal > 0 ? ganhosDia/horasDecimal : null;
  const rskm = totalKm > 0 ? ganhosDia/totalKm : null;
  setText('jc-rsh', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  setText('jc-rsh2', rsh !== null ? DB.fRI(rsh) : 'R$0,00');
  setText('jc-rskm2', rskm !== null ? DB.fRI(rskm) : 'R$0,00');
  // Cor por indicador — pedido do Yuri (23/07/2026, mockup aprovado):
  // mesmos limiares vermelho/verde que o motorista já ajusta na régua do
  // MōB Flash (Indicadores → R$/km e R$/hora), cada indicador troca de cor
  // por conta própria. Sem dado ainda (sessão não rodou tempo/km) fica
  // neutro (verde), não faz sentido alarmar antes de ter dado de verdade.
  const rateEl = document.getElementById('jc-rate-hora');
  const kmEl   = document.getElementById('jc-rate-km');
  const rateEl2 = document.getElementById('jc-rate-hora2');
  const kmEl2   = document.getElementById('jc-rate-km2');
  const rateCls = rsh !== null ? rateColorClass('rhora', rsh) : '';
  const kmCls   = rskm !== null ? rateColorClass('rkm', rskm) : '';
  if (rateEl)  rateEl.className  = 'td-rcard ' + rateCls;
  if (kmEl)    kmEl.className    = 'td-rcard ' + kmCls;
  if (rateEl2) rateEl2.className = 'rate-cell ' + rateCls;
  if (kmEl2)   kmEl2.className   = 'rate-cell ' + kmCls;

  const actions = document.getElementById('jc-actions');
  if (!actions) return;
  if (!isToday) {
    actions.innerHTML = '';
  } else if (state === 'idle') {
    actions.innerHTML = `<button class="j-round-btn play" onclick="gpsStart()" aria-label="Iniciar jornada"><svg viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg></button>`;
  } else if (state === 'running') {
    actions.innerHTML = '<button class="j-round-btn stop" onclick="confirmStop()" aria-label="Encerrar jornada"><svg viewBox="0 0 24 24" fill="currentColor"><rect x="6" y="6" width="12" height="12"/></svg></button>';
  }
}

function agMinutesBetween(start, end) {
  if (!start || !end) return 0;
  const [h1,m1] = start.split(':').map(Number);
  const [h2,m2] = end.split(':').map(Number);
  return Math.max(0, (h2*60+m2) - (h1*60+m1));
}
function openMetasHojeModal() {
  const date = S.curDate || DB.today();
  const day = DB.day(date);
  const dow = new Date(date+'T12:00').getDay();
  const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
  const meta = DB.meta();
  const ov = day.metaOverride || null;
  const dimMes = new Date(new Date(date+'T12:00').getFullYear(), new Date(date+'T12:00').getMonth()+1, 0).getDate();
  const moneyGoal = (ov && ov.money) ? ov.money : ((plan && plan.moneyGoal) ? plan.moneyGoal : (meta.general ? meta.general/dimMes : 0));
  const kmGoal = (ov && ov.km) ? ov.km : (plan && plan.kmGoal ? plan.kmGoal : 0);
  const schedMin = (ov && ov.hours) ? ov.hours*60 : (plan ? (plan.schedules||[]).reduce((s,b)=>s+agMinutesBetween(b.start,b.end),0) : 0);
  const mEl = document.getElementById('mh-money'); if (mEl) mEl.value = moneyGoal ? moneyGoal.toFixed(2) : '';
  const kEl = document.getElementById('mh-km'); if (kEl) kEl.value = kmGoal || '';
  const hEl = document.getElementById('mh-hours'); if (hEl) hEl.value = schedMin ? (schedMin/60).toFixed(1) : '';
  const subEl = document.getElementById('mh-sub');
  if (subEl) {
    const isToday = date === DB.today();
    subEl.textContent = isToday ? 'Vale só para hoje' : ('Vale só para ' + new Date(date+'T12:00').toLocaleDateString('pt-BR',{weekday:'long',day:'numeric',month:'long'}));
  }
  openMo('mo-metas-hoje');
}
function saveMetasHoje() {
  const date = S.curDate || DB.today();
  const day = DB.day(date);
  const money = parseFloat((document.getElementById('mh-money')||{}).value) || 0;
  const km = parseFloat((document.getElementById('mh-km')||{}).value) || 0;
  const hours = parseFloat((document.getElementById('mh-hours')||{}).value) || 0;
  day.metaOverride = { money, km, hours };
  DB.saveDay(day);
  closeMo('mo-metas-hoje');
  try { renderMetasDoDia(day); } catch(e) {}
  showToast('Metas de hoje atualizadas!', 'success');
}
function renderMetasDoDia(day) {
  const stack = document.getElementById('metas-dia-stack');
  if (!stack) return;
  const rev = day.trips.reduce((s,t)=>s+(t.value||0),0) + day.revenues.reduce((s,r)=>s+(r.amount||0),0)
            + (day.autoTrips||[]).reduce((s,t)=>s+(t.offer_value||0),0);
  const isToday = day.date === DB.today();
  const liveOn = isToday && S.gps.st==='running';
  const totalKm = (day.sessions||[]).reduce((s,x)=>s+(x.km||0),0) + (liveOn ? S.gps.km : 0) + (day.kmAdjust || 0);
  const totalElapsed = Math.max(0, (day.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0) + (liveOn ? getElapsed() : 0) + (day.tempoAdjust || 0));
  const dow = new Date(day.date+'T12:00').getDay();
  const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
  const meta = DB.meta();
  const ov = day.metaOverride || null; // ajuste específico deste dia (via modal 'Metas de hoje')
  const dimMes = new Date(new Date(day.date+'T12:00').getFullYear(), new Date(day.date+'T12:00').getMonth()+1, 0).getDate();
  const moneyGoal = (ov && ov.money) ? ov.money : ((plan && plan.moneyGoal) ? plan.moneyGoal : (meta.general ? meta.general/dimMes : 0));

  const ICO_MONEY = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>';
  const ICO_KM = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76"/></svg>';
  const ICO_TIME = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>';

  let html = '';
  if (moneyGoal) {
    const pct = Math.min(100, Math.round(rev/moneyGoal*100));
    html += `<div class="goal-item"><div class="g-ico money">${ICO_MONEY}</div><div class="goal-body"><div class="goal-top"><strong>Financeira</strong><span>${DB.fRI(rev)} de ${DB.fRI(moneyGoal)} · ${pct}%</span></div><div class="gbar money"><i style="width:${pct}%"></i></div></div></div>`;
  } else {
    html += `<div class="goal-item"><div class="g-ico money">${ICO_MONEY}</div><div class="goal-locked" onclick="openPlanJornada()"><span>Financeira — meta não definida</span><b>Planejar</b></div></div>`;
  }
  const kmGoal = (ov && ov.km) ? ov.km : (plan && plan.kmGoal ? plan.kmGoal : 0);
  if (kmGoal) {
    const pct = Math.min(100, Math.round(totalKm/kmGoal*100));
    html += `<div class="goal-item"><div class="g-ico km">${ICO_KM}</div><div class="goal-body"><div class="goal-top"><strong>Km</strong><span>${totalKm.toFixed(1)} de ${kmGoal} km · ${pct}%</span></div><div class="gbar km"><i style="width:${pct}%"></i></div></div></div>`;
  } else {
    html += `<div class="goal-item"><div class="g-ico km">${ICO_KM}</div><div class="goal-locked" onclick="openPlanJornada()"><span>Km — meta não definida</span><b>Planejar</b></div></div>`;
  }
  const schedMin = (ov && ov.hours) ? ov.hours*60 : (plan ? (plan.schedules||[]).reduce((s,b)=>s+agMinutesBetween(b.start,b.end),0) : 0);
  if (schedMin > 0) {
    const liveMin = Math.round(totalElapsed/60);
    const pct = Math.min(100, Math.round(liveMin/schedMin*100));
    html += `<div class="goal-item"><div class="g-ico time">${ICO_TIME}</div><div class="goal-body"><div class="goal-top"><strong>Tempo online</strong><span>${Math.floor(liveMin/60)}h${p2(liveMin%60)} de ${Math.floor(schedMin/60)}h${p2(schedMin%60)} · ${pct}%</span></div><div class="gbar time"><i style="width:${pct}%"></i></div></div></div>`;
  } else {
    html += `<div class="goal-item"><div class="g-ico time">${ICO_TIME}</div><div class="goal-locked" onclick="openPlanJornada()"><span>Tempo online — meta não definida</span><b>Planejar</b></div></div>`;
  }
  stack.innerHTML = html;
}

// ── Planejar Jornada ─────────────────────────────────────────
let pjSelDays = [];
let pjSchedules = [];
const PJ_DOW = ['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'];
function openPlanJornada() {
  pjSelDays = []; pjSchedules = [];
  document.querySelectorAll('.pj-day').forEach(b => b.classList.remove('on'));
  const mEl = document.getElementById('pj-money'); if (mEl) mEl.value = '';
  const kEl = document.getElementById('pj-km'); if (kEl) kEl.value = '';
  renderPjSchedules();
  renderPjList();
  navTo('planejar-jornada');
}
function pjToggleDay(d) {
  const i = pjSelDays.indexOf(d);
  if (i === -1) pjSelDays.push(d); else pjSelDays.splice(i,1);
  const btn = document.querySelector('.pj-day[data-d="'+d+'"]');
  if (btn) btn.classList.toggle('on', pjSelDays.includes(d));
}
function pjAddSchedule() { pjSchedules.push({start:'', end:''}); renderPjSchedules(); }
function pjRemoveSchedule(idx) { pjSchedules.splice(idx, 1); renderPjSchedules(); }
function renderPjSchedules() {
  const el = document.getElementById('pj-schedules');
  if (!el) return;
  if (!pjSchedules.length) { el.innerHTML = '<p style="font-size:12px;color:var(--muted);margin:2px 0">Nenhum horário adicionado</p>'; return; }
  el.innerHTML = pjSchedules.map((s,i) => `
    <div class="pj-sched-row">
      <input type="time" class="ag-input" value="${s.start}" onchange="pjSchedules[${i}].start=this.value">
      <span>→</span>
      <input type="time" class="ag-input" value="${s.end}" onchange="pjSchedules[${i}].end=this.value">
      <button class="pj-sched-del" onclick="pjRemoveSchedule(${i})">&#x2715;</button>
    </div>
  `).join('');
}
function pjSaveEntry() {
  if (!pjSelDays.length) { showToast('Selecione ao menos um dia', 'error'); return; }
  const validSched = pjSchedules.filter(s => s.start && s.end);
  const moneyGoal = parseFloat(document.getElementById('pj-money')?.value||0) || null;
  const kmGoal = parseFloat(document.getElementById('pj-km')?.value||0) || null;
  const plans = DB.planJornada();
  const entry = { id: DB.uid(), days: pjSelDays.slice(), moneyGoal, kmGoal, schedules: validSched };
  plans.push(entry);
  DB.savePlanJornada(plans);
  SYNC.push('plan_jornada', { id: entry.id, days: entry.days, money_goal: entry.moneyGoal,
    km_goal: entry.kmGoal, schedules: entry.schedules });
  pjSelDays = []; pjSchedules = [];
  document.querySelectorAll('.pj-day').forEach(b => b.classList.remove('on'));
  const mEl = document.getElementById('pj-money'); if (mEl) mEl.value = '';
  const kEl = document.getElementById('pj-km'); if (kEl) kEl.value = '';
  renderPjSchedules();
  renderPjList();
  renderMetasDoDia(DB.day(S.curDate));
  showToast('Jornada planejada salva!');
}
function pjRemoveEntry(id) {
  showConfirm('Remover essa jornada planejada?', () => {
    DB.savePlanJornada(DB.planJornada().filter(p => p.id !== id));
    SYNC.del('plan_jornada', { id });
    renderPjList();
    renderMetasDoDia(DB.day(S.curDate));
  });
}
function renderPjList() {
  const el = document.getElementById('pj-list');
  if (!el) return;
  const plans = DB.planJornada();
  if (!plans.length) { el.innerHTML = '<div class="empty">Nenhuma jornada planejada ainda</div>'; return; }
  el.innerHTML = plans.map(p => {
    const daysTxt = (p.days||[]).slice().sort().map(d=>PJ_DOW[d]).join(' · ');
    const goalParts = [];
    if (p.moneyGoal) goalParts.push(DB.fRI(p.moneyGoal));
    if (p.kmGoal) goalParts.push(p.kmGoal+' km');
    const goalTxt = goalParts.length ? goalParts.join(' · ') : 'Sem meta definida';
    const chips = (p.schedules||[]).map(s => `<span class="pj-chip">${s.start} → ${s.end}</span>`).join('');
    return `<div class="pj-card">
      <div class="pj-card-top"><div class="pj-card-days">${daysTxt}</div><button class="pj-card-del" onclick="pjRemoveEntry('${p.id}')">&#x2715;</button></div>
      <div class="pj-card-goal">${goalTxt}</div>
      <div class="pj-card-times">${chips}</div>
    </div>`;
  }).join('');
}

function sessionItem(label, sess, origIdx, dateStr, day, live) {
  const startMs = sess.start || 0, endMs = sess.end || 0;
  const km = sess.km || 0, elapsedSecs = sess.elapsed || 0;
  const dur = fmtDur((elapsedSecs||0)/60);
  const range = fmtHM(startMs) + '–' + (endMs ? fmtHM(endMs) : (live ? 'Agora' : '—'));
  const status = live ? 'Online' : 'Encerrada';
  const badgeCls = live ? 'live' : 'end';
  const cardId = 'jss-' + dateStr + '-' + origIdx;

  // Corridas desta sessão — filtrar por horário (suporta t.ts e t.time HH:MM)
  const trips = (day?.trips||[]).filter(t => {
    let ts = t.ts || 0;
    if (!ts && t.time && dateStr) {
      // Construir timestamp a partir de "HH:MM" + data da sessão
      ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
    }
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  // Corridas automáticas desta sessão — mesmo filtro por horário, usando
  // trip_started_at/accepted_at (autoTripTs, ver loadAutoTripsForDay).
  const autoTrips = (day?.autoTrips||[]).filter(t => {
    const ts = autoTripTs(t);
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  const corridas = trips.length + autoTrips.length;
  const ganhos = trips.reduce((s,t)=>s+(t.value||0),0) + autoTrips.reduce((s,t)=>s+(t.offer_value||0),0);
  // Km ativo = soma do km de cada corrida realizada (tempo 'em corrida')
  // Km deslocamento = km total do GPS MENOS o km ativo (tempo rodando sem
  // corrida — indo pro ponto, retornando pra casa, esperando chamado etc.)
  const kmAtivo = trips.reduce((s,t)=>s+(parseFloat(t.km)||0),0)
                + autoTrips.reduce((s,t)=>s+(parseFloat(t.real_km_trip ?? t.offer_km_trip)||0),0);
  const kmDesloc = Math.max(0, km - kmAtivo);

  const horasDecimal = elapsedSecs > 0 ? elapsedSecs/3600 : 0;
  const rshVal = horasDecimal > 0 ? ganhos/horasDecimal : null;
  const rskmVal = km > 0 ? ganhos/km : null;
  const rsPorHora = rshVal !== null ? DB.fR(rshVal) : '—';
  const rsPorKm = rskmVal !== null ? DB.fR(rskmVal) : '—';
  const rshCls = rshVal !== null ? rateColorClass('rhora', rshVal) : '';
  const rskmCls = rskmVal !== null ? rateColorClass('rkm', rskmVal) : '';
  // Lucro da sessão = soma do lucro de cada corrida (mesmo cálculo do
  // perfStripHtml/detalhe de corrida: valor - custoPorKm*km), só quando o
  // custo do veículo está configurado — igual já vale por corrida.
  const cvCfg = DB.cfg().
===== sessions @ 661459 =====
st corridas = trips.length + autoTrips.length;
  const ganhos = trips.reduce((s,t)=>s+(t.value||0),0) + autoTrips.reduce((s,t)=>s+(t.offer_value||0),0);
  // Km ativo = soma do km de cada corrida realizada (tempo 'em corrida')
  // Km deslocamento = km total do GPS MENOS o km ativo (tempo rodando sem
  // corrida — indo pro ponto, retornando pra casa, esperando chamado etc.)
  const kmAtivo = trips.reduce((s,t)=>s+(parseFloat(t.km)||0),0)
                + autoTrips.reduce((s,t)=>s+(parseFloat(t.real_km_trip ?? t.offer_km_trip)||0),0);
  const kmDesloc = Math.max(0, km - kmAtivo);

  const horasDecimal = elapsedSecs > 0 ? elapsedSecs/3600 : 0;
  const rshVal = horasDecimal > 0 ? ganhos/horasDecimal : null;
  const rskmVal = km > 0 ? ganhos/km : null;
  const rsPorHora = rshVal !== null ? DB.fR(rshVal) : '—';
  const rsPorKm = rskmVal !== null ? DB.fR(rskmVal) : '—';
  const rshCls = rshVal !== null ? rateColorClass('rhora', rshVal) : '';
  const rskmCls = rskmVal !== null ? rateColorClass('rkm', rskmVal) : '';
  // Lucro da sessão = soma do lucro de cada corrida (mesmo cálculo do
  // perfStripHtml/detalhe de corrida: valor - custoPorKm*km), só quando o
  // custo do veículo está configurado — igual já vale por corrida.
  const cvCfg = DB.cfg().custoVeiculo;
  const hasCusto = !!(cvCfg && cvCfg.configurado);
  const lucroVal = hasCusto ? ganhos - (cvCfg.custoPorKm||0) * kmAtivo : null;
  const lucroCls = lucroVal !== null ? rateColorClass('lucro', lucroVal) : '';
  // Barra Online/Buscando passageiro/Em corrida desta sessão — mesma fonte
  // real da máquina de estados do APK, agora escopada só às autoTrips que
  // caem dentro do horário desta sessão (já filtradas acima).
  const sessBd = (typeof journeyBreakdownForSession === 'function')
    ? journeyBreakdownForSession(sess, elapsedSecs, km, autoTrips)
    : computeStatusBreakdown(elapsedSecs, km, autoTrips);
  const bdListId = 'bl-' + cardId;
  const editBtn = !live ? `<button class="jss-act" onclick="event.stopPropagation();editJss('${dateStr}',${origIdx})" title="Editar"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg></button>
        <button class="jss-act del" onclick="event.stopPropagation();deleteJss('${dateStr}',${origIdx})" title="Excluir"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/></svg></button>` : '';

  return `<div class="jss-card ${badgeCls}" id="${cardId}">
    <div class="jss-row" onclick="toggleJss('${cardId}')">
      <div class="jss-info">
        <div class="jss-top"><span class="jss-name">${label}</span><span class="jss-badge ${badgeCls}">${status}</span>${editBtn}</div>
        <div class="jss-sub">${range} · ${dur}</div>
      </div>
      <div style="text-align:right;flex-shrink:0;display:flex;flex-direction:column;align-items:flex-end;gap:2px">
        <span style="font-size:14px;font-weight:800;color:var(--text)">${(sess.km||0).toFixed(1)} km</span>
        <span class="jss-ganhos">${DB.fR(ganhos)}</span>
      </div>
      <div class="jss-chev"><svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="6 9 12 15 18 9"/></svg></div>
    </div>
    <div class="jss-rate-row">
      <div class="jss-rcard ${rshCls}"><strong>${rsPorHora}</strong><span>R$/hora</span></div>
      <div class="jss-rcard ${rskmCls}"><strong>${rsPorKm}</strong><span>R$/km</span></div>
      ${hasCusto ? `<div class="jss-rcard ${lucroCls}"><strong>${DB.fR(lucroVal)}</strong><span>Lucro</span></div>` : `<div class="jss-rcard"><strong>—</strong><span>Lucro</span></div>`}
    </div>
    ${elapsedSecs > 0 ? `<div class="jss-statusbar">
      <div class="sbar-wrap" onclick="event.stopPropagation();toggleBar('${bdListId}')"><div class="sbar">${buildSbarHtml(sessBd)}</div></div>
      <div class="sbar-list" id="${bdListId}">${buildSrowsHtml(sessBd)}</div>
    </div>` : ''}
    <div class="jss-trips">
      <div class="jss-trips-head">
        <h4>Corridas desta jornada</h4>
        ${corridas > 0 ? `<span>${corridas} corrida${corridas !== 1 ? 's' : ''}</span>` : ''}
      </div>
      ${corridas > 0
        ? [
            ...trips.map(t => ({ ts: t.ts || (t.time?new Date(dateStr+'T'+t.time+':00').getTime():0), html: tripItem(t) })),
            ...autoTrips.map(t => ({ ts: autoTripTs(t), html: autoTripItem(t) })),
          ].sort((a,b)=>b.ts-a.ts).map(x=>x.html).join('')
        : '<div class="jss-trips-empty">Nenhuma corrida nesta jornada</div>'}
    </div>
  </div>`;

}
// ── Editar sessão ─────────────────────────────────────────────────────────
function editJss(dateStr, idx) {
  const day = DB.day(dateStr);
  const sess = (day.sessions || [])[idx];
  if (!sess) return;

  document.getElementById('jss-edit-date').value = dateStr;
  document.getElementById('jss-edit-idx').value  = idx;
  document.getElementById('jss-edit-km').value   = (sess.km || 0).toFixed(1);

  // Hora início e fim a partir dos timestamps da sessão
  const fmt = ms => {
    if (!ms) return '';
    const d = new Date(ms);
    return String(d.getHours()).padStart(2,'0') + ':' + String(d.getMinutes()).padStart(2,'0');
  };
  document.getElementById('jss-edit-inicio').value = fmt(sess.start);
  document.getElementById('jss-edit-fim').value    = fmt(sess.end);

  openMo('mo-jss-edit');
}

function saveJss() {
  const dateStr = document.getElementById('jss-edit-date').value;
  const idx     = parseInt(document.getElementById('jss-edit-idx').value);
  const km      = parseFloat(document.getElementById('jss-edit-km').value) || 0;
  const inicioStr = document.getElementById('jss-edit-inicio').value; // "HH:MM"
  const fimStr    = document.getElementById('jss-edit-fim').value;    // "HH:MM"

  const day = DB.day(dateStr);
  if (!day.sessions || !day.sessions[idx]) { closeMo('mo-jss-edit'); return; }

  const sess = day.sessions[idx];

  // Recalcular timestamps de início e fim mantendo a data original
  const toMs = (base, timeStr) => {
    if (!timeStr) return base;
    const [hh, mm] = timeStr.split(':').map(Number);
    const d = new Date(base || Date.now());
    d.setHours(hh, mm, 0, 0);
    return d.getTime();
  };
  const newStart = toMs(sess.start, inicioStr);
  const newEnd   = toMs(sess.end || sess.start, fimStr);
  // Elapsed recalculado a partir da diferença início→fim
  const elapsed  = newEnd > newStart ? Math.round((newEnd - newStart) / 1000) : (sess.elapsed || 0);

  sess.km      = km;
  sess.start   = newStart;
  sess.end     = newEnd;
  sess.elapsed = elapsed;
  DB.saveDay(day);

  // Sincronizar com Supabase
  if (_supaUser && sess.id) {
    _SUPA.from('sessions').update({
      km:           parseFloat(km.toFixed(3)),
      start_ms:     newStart,
      end_ms:       newEnd,
      elapsed_secs: elapsed
    }).eq('id', sess.id).then(() => {}).catch(() => {});
  }

  closeMo('mo-jss-edit');
  renderJornada();
  if (S.sec === 'relatorios') renderHome();
  showToast('Sessão atualizada');
}

function deleteJss(dateStr, idx) {
  showConfirm('Excluir esta sessão?', () => {
    const day = DB.day(dateStr);
    if (!day.sessions || !day.sessions[idx]) return;

    const sess = day.sessions[idx];
    day.sessions.splice(idx, 1);
    DB.saveDay(day);

    // Remover do Supabase (delete real, não update)
    if (sess.id) {
      SYNC.del('sessions', { id: sess.id });
    }

    renderJornada();
    if (S.sec === 'relatorios') renderHome();
    showToast('Sessão excluída');
  }, { confirmLabel: 'Excluir', danger: true, cancelLabel: 'Cancelar' });
}


// CORRIGIDO (15/08/2026, relatado pelo Yuri): mesmo bug do toggleTripDetail
// (comentário abaixo, 24/07) só que no card da jornada — toggleJss só
// mexia numa classe CSS, sem guardar estado. O refresh automático de 25s
// (_refreshAutoTripsIfOnJornada) recria os cards do zero sempre fechados,
// então a expansão "voltava a fechar sozinha" em segundos. Mesmo padrão
// de correção: guarda o id aberto, reaplica depois de qualquer redesenho.
let _openJssId = null;
function toggleJss(id){
  const el=document.getElementById(id);
  if(!el) return;
  const willOpen = !el.classList.contains('open');
  el.classList.toggle('open');
  _openJssId = willOpen ? id : null;
}
function reapplyOpenJss(){
  if (!_openJssId) return;
  const el = document.getElementById(_openJssId);
  if (el) el.classList.add('open');
}
// CORRIGIDO (24/07/2026, relatado pelo Yuri): o painel de detalhe de uma
// corrida é só um display:none/block num elemento — quando a tela
// redesenha os cards (o refresh automático de 25s/60s da Jornada/Home,
// por exemplo), os elementos são recriados do zero, sempre fechados por
// padrão, perdendo o que estava aberto na hora errada (parecia fechar
// sozinho, sem motivo). Guarda qual tid está aberto e reaplica depois de
// qualquer redesenho.
let _openTripDetailTid = null;
// Linha vertical da rota (Aceite/Embarque/Destino) tem que parar exatamente
// no centro do último marcador (pin de Destino) — pedido do Yuri
// (16/08/2026): como os marcadores têm tamanhos diferentes (círculo
// pequeno vs pin maior), um valor fixo de CSS nunca acerta em todo caso,
// então mede a posição real na tela depois de renderizado.
function sizeRouteLine(panelEl) {
  try {
    const block = panelEl.querySelector('.route-block');
    if (!block) return;
    const marks = block.querySelectorAll('.route-mark');
    const line = block.querySelector('.route-line');
    if (!line || marks.length < 2) return;
    const first = marks[0], last = marks[marks.length - 1];
    const blockTop = block.getBoundingClientRect().top;
    const firstMid = first.getBoundingClientRect().top - blockTop + first.offsetHeight / 2;
    const lastMid = last.getBoundingClientRect().top - blockTop + last.offsetHeight / 2;
    if (lastMid <= firstMid) return;
    line.style.top = firstMid + 'px';
    line.style.height = (lastMid - firstMid) + 'px';
  } catch (e) {}
}
function toggleTripDetail(tid) {
  const det = document.getElementById('td-'+tid);
  const row = document.getElementById('tr-'+tid);
  if (!det||!row) return;
  const open = det.style.display!=='none';
  document.querySelectorAll('.trip-detail-panel').forEach(el=>el.style.display='none');
  document.querySelectorAll('.li-row[id^="tr-"]').forEach(el=>el.classList.remove('open'));
  if (!open){det.style.display='block';row.classList.add('open');_openTripDetailTid=tid;sizeRouteLine(det);}
  else {_openTripDetailTid=null;}
}
// Chamar depois de qualquer redesenho que possa ter recriado os cards de
// corrida (renderJornada, renderCorridasActive etc.) — reabre sozinho se
// o mesmo tid ainda existir na tela.
function reapplyOpenTripDetail() {
  if (!_openTripDetailTid) return;
  const det = document.getElementById('td-'+_openTripDetailTid);
  const row = document.getElementById('tr-'+_openTripDetailTid);
  if (det && row) { det.style.display='block'; row.classList.add('open'); sizeRouteLine(det); }
}

function esc(s){return String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
// Escapa texto externo (Nominatim/Overpass) pra uso dentro de um literal JS
// de aspas simples que por sua vez está dentro de um atributo onclick="..."
// de aspas duplas — precisa fugir dos dois delimitadores ao mesmo tempo,
// senão um "/' no rótulo devolvido pela API quebra o atributo e injeta HTML.
function escJsAttr(s){return String(s==null?'':s).replace(/\\/g,'\\\\').replace(/'/g,"\\'").replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;');}
// ── Logos das plataformas (assets originais processados: fundo removido) ──
const LOGO_UBER = 'data:image/png;base64,
===== sessions @ 662545 =====
cálculo do
  // perfStripHtml/detalhe de corrida: valor - custoPorKm*km), só quando o
  // custo do veículo está configurado — igual já vale por corrida.
  const cvCfg = DB.cfg().custoVeiculo;
  const hasCusto = !!(cvCfg && cvCfg.configurado);
  const lucroVal = hasCusto ? ganhos - (cvCfg.custoPorKm||0) * kmAtivo : null;
  const lucroCls = lucroVal !== null ? rateColorClass('lucro', lucroVal) : '';
  // Barra Online/Buscando passageiro/Em corrida desta sessão — mesma fonte
  // real da máquina de estados do APK, agora escopada só às autoTrips que
  // caem dentro do horário desta sessão (já filtradas acima).
  const sessBd = (typeof journeyBreakdownForSession === 'function')
    ? journeyBreakdownForSession(sess, elapsedSecs, km, autoTrips)
    : computeStatusBreakdown(elapsedSecs, km, autoTrips);
  const bdListId = 'bl-' + cardId;
  const editBtn = !live ? `<button class="jss-act" onclick="event.stopPropagation();editJss('${dateStr}',${origIdx})" title="Editar"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg></button>
        <button class="jss-act del" onclick="event.stopPropagation();deleteJss('${dateStr}',${origIdx})" title="Excluir"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/></svg></button>` : '';

  return `<div class="jss-card ${badgeCls}" id="${cardId}">
    <div class="jss-row" onclick="toggleJss('${cardId}')">
      <div class="jss-info">
        <div class="jss-top"><span class="jss-name">${label}</span><span class="jss-badge ${badgeCls}">${status}</span>${editBtn}</div>
        <div class="jss-sub">${range} · ${dur}</div>
      </div>
      <div style="text-align:right;flex-shrink:0;display:flex;flex-direction:column;align-items:flex-end;gap:2px">
        <span style="font-size:14px;font-weight:800;color:var(--text)">${(sess.km||0).toFixed(1)} km</span>
        <span class="jss-ganhos">${DB.fR(ganhos)}</span>
      </div>
      <div class="jss-chev"><svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="6 9 12 15 18 9"/></svg></div>
    </div>
    <div class="jss-rate-row">
      <div class="jss-rcard ${rshCls}"><strong>${rsPorHora}</strong><span>R$/hora</span></div>
      <div class="jss-rcard ${rskmCls}"><strong>${rsPorKm}</strong><span>R$/km</span></div>
      ${hasCusto ? `<div class="jss-rcard ${lucroCls}"><strong>${DB.fR(lucroVal)}</strong><span>Lucro</span></div>` : `<div class="jss-rcard"><strong>—</strong><span>Lucro</span></div>`}
    </div>
    ${elapsedSecs > 0 ? `<div class="jss-statusbar">
      <div class="sbar-wrap" onclick="event.stopPropagation();toggleBar('${bdListId}')"><div class="sbar">${buildSbarHtml(sessBd)}</div></div>
      <div class="sbar-list" id="${bdListId}">${buildSrowsHtml(sessBd)}</div>
    </div>` : ''}
    <div class="jss-trips">
      <div class="jss-trips-head">
        <h4>Corridas desta jornada</h4>
        ${corridas > 0 ? `<span>${corridas} corrida${corridas !== 1 ? 's' : ''}</span>` : ''}
      </div>
      ${corridas > 0
        ? [
            ...trips.map(t => ({ ts: t.ts || (t.time?new Date(dateStr+'T'+t.time+':00').getTime():0), html: tripItem(t) })),
            ...autoTrips.map(t => ({ ts: autoTripTs(t), html: autoTripItem(t) })),
          ].sort((a,b)=>b.ts-a.ts).map(x=>x.html).join('')
        : '<div class="jss-trips-empty">Nenhuma corrida nesta jornada</div>'}
    </div>
  </div>`;

}
// ── Editar sessão ─────────────────────────────────────────────────────────
function editJss(dateStr, idx) {
  const day = DB.day(dateStr);
  const sess = (day.sessions || [])[idx];
  if (!sess) return;

  document.getElementById('jss-edit-date').value = dateStr;
  document.getElementById('jss-edit-idx').value  = idx;
  document.getElementById('jss-edit-km').value   = (sess.km || 0).toFixed(1);

  // Hora início e fim a partir dos timestamps da sessão
  const fmt = ms => {
    if (!ms) return '';
    const d = new Date(ms);
    return String(d.getHours()).padStart(2,'0') + ':' + String(d.getMinutes()).padStart(2,'0');
  };
  document.getElementById('jss-edit-inicio').value = fmt(sess.start);
  document.getElementById('jss-edit-fim').value    = fmt(sess.end);

  openMo('mo-jss-edit');
}

function saveJss() {
  const dateStr = document.getElementById('jss-edit-date').value;
  const idx     = parseInt(document.getElementById('jss-edit-idx').value);
  const km      = parseFloat(document.getElementById('jss-edit-km').value) || 0;
  const inicioStr = document.getElementById('jss-edit-inicio').value; // "HH:MM"
  const fimStr    = document.getElementById('jss-edit-fim').value;    // "HH:MM"

  const day = DB.day(dateStr);
  if (!day.sessions || !day.sessions[idx]) { closeMo('mo-jss-edit'); return; }

  const sess = day.sessions[idx];

  // Recalcular timestamps de início e fim mantendo a data original
  const toMs = (base, timeStr) => {
    if (!timeStr) return base;
    const [hh, mm] = timeStr.split(':').map(Number);
    const d = new Date(base || Date.now());
    d.setHours(hh, mm, 0, 0);
    return d.getTime();
  };
  const newStart = toMs(sess.start, inicioStr);
  const newEnd   = toMs(sess.end || sess.start, fimStr);
  // Elapsed recalculado a partir da diferença início→fim
  const elapsed  = newEnd > newStart ? Math.round((newEnd - newStart) / 1000) : (sess.elapsed || 0);

  sess.km      = km;
  sess.start   = newStart;
  sess.end     = newEnd;
  sess.elapsed = elapsed;
  DB.saveDay(day);

  // Sincronizar com Supabase
  if (_supaUser && sess.id) {
    _SUPA.from('sessions').update({
      km:           parseFloat(km.toFixed(3)),
      start_ms:     newStart,
      end_ms:       newEnd,
      elapsed_secs: elapsed
    }).eq('id', sess.id).then(() => {}).catch(() => {});
  }

  closeMo('mo-jss-edit');
  renderJornada();
  if (S.sec === 'relatorios') renderHome();
  showToast('Sessão atualizada');
}

function deleteJss(dateStr, idx) {
  showConfirm('Excluir esta sessão?', () => {
    const day = DB.day(dateStr);
    if (!day.sessions || !day.sessions[idx]) return;

    const sess = day.sessions[idx];
    day.sessions.splice(idx, 1);
    DB.saveDay(day);

    // Remover do Supabase (delete real, não update)
    if (sess.id) {
      SYNC.del('sessions', { id: sess.id });
    }

    renderJornada();
    if (S.sec === 'relatorios') renderHome();
    showToast('Sessão excluída');
  }, { confirmLabel: 'Excluir', danger: true, cancelLabel: 'Cancelar' });
}


// CORRIGIDO (15/08/2026, relatado pelo Yuri): mesmo bug do toggleTripDetail
// (comentário abaixo, 24/07) só que no card da jornada — toggleJss só
// mexia numa classe CSS, sem guardar estado. O refresh automático de 25s
// (_refreshAutoTripsIfOnJornada) recria os cards do zero sempre fechados,
// então a expansão "voltava a fechar sozinha" em segundos. Mesmo padrão
// de correção: guarda o id aberto, reaplica depois de qualquer redesenho.
let _openJssId = null;
function toggleJss(id){
  const el=document.getElementById(id);
  if(!el) return;
  const willOpen = !el.classList.contains('open');
  el.classList.toggle('open');
  _openJssId = willOpen ? id : null;
}
function reapplyOpenJss(){
  if (!_openJssId) return;
  const el = document.getElementById(_openJssId);
  if (el) el.classList.add('open');
}
// CORRIGIDO (24/07/2026, relatado pelo Yuri): o painel de detalhe de uma
// corrida é só um display:none/block num elemento — quando a tela
// redesenha os cards (o refresh automático de 25s/60s da Jornada/Home,
// por exemplo), os elementos são recriados do zero, sempre fechados por
// padrão, perdendo o que estava aberto na hora errada (parecia fechar
// sozinho, sem motivo). Guarda qual tid está aberto e reaplica depois de
// qualquer redesenho.
let _openTripDetailTid = null;
// Linha vertical da rota (Aceite/Embarque/Destino) tem que parar exatamente
// no centro do último marcador (pin de Destino) — pedido do Yuri
// (16/08/2026): como os marcadores têm tamanhos diferentes (círculo
// pequeno vs pin maior), um valor fixo de CSS nunca acerta em todo caso,
// então mede a posição real na tela depois de renderizado.
function sizeRouteLine(panelEl) {
  try {
    const block = panelEl.querySelector('.route-block');
    if (!block) return;
    const marks = block.querySelectorAll('.route-mark');
    const line = block.querySelector('.route-line');
    if (!line || marks.length < 2) return;
    const first = marks[0], last = marks[marks.length - 1];
    const blockTop = block.getBoundingClientRect().top;
    const firstMid = first.getBoundingClientRect().top - blockTop + first.offsetHeight / 2;
    const lastMid = last.getBoundingClientRect().top - blockTop + last.offsetHeight / 2;
    if (lastMid <= firstMid) return;
    line.style.top = firstMid + 'px';
    line.style.height = (lastMid - firstMid) + 'px';
  } catch (e) {}
}
function toggleTripDetail(tid) {
  const det = document.getElementById('td-'+tid);
  const row = document.getElementById('tr-'+tid);
  if (!det||!row) return;
  const open = det.style.display!=='none';
  document.querySelectorAll('.trip-detail-panel').forEach(el=>el.style.display='none');
  document.querySelectorAll('.li-row[id^="tr-"]').forEach(el=>el.classList.remove('open'));
  if (!open){det.style.display='block';row.classList.add('open');_openTripDetailTid=tid;sizeRouteLine(det);}
  else {_openTripDetailTid=null;}
}
// Chamar depois de qualquer redesenho que possa ter recriado os cards de
// corrida (renderJornada, renderCorridasActive etc.) — reabre sozinho se
// o mesmo tid ainda existir na tela.
function reapplyOpenTripDetail() {
  if (!_openTripDetailTid) return;
  const det = document.getElementById('td-'+_openTripDetailTid);
  const row = document.getElementById('tr-'+_openTripDetailTid);
  if (det && row) { det.style.display='block'; row.classList.add('open'); sizeRouteLine(det); }
}

function esc(s){return String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
// Escapa texto externo (Nominatim/Overpass) pra uso dentro de um literal JS
// de aspas simples que por sua vez está dentro de um atributo onclick="..."
// de aspas duplas — precisa fugir dos dois delimitadores ao mesmo tempo,
// senão um "/' no rótulo devolvido pela API quebra o atributo e injeta HTML.
function escJsAttr(s){return String(s==null?'':s).replace(/\\/g,'\\\\').replace(/'/g,"\\'").replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;');}
// ── Logos das plataformas (assets originais processados: fundo removido) ──
const LOGO_UBER = 'data:image/png;base64,
===== sessions @ 663400 =====
tBtn = !live ? `<button class="jss-act" onclick="event.stopPropagation();editJss('${dateStr}',${origIdx})" title="Editar"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg></button>
        <button class="jss-act del" onclick="event.stopPropagation();deleteJss('${dateStr}',${origIdx})" title="Excluir"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/></svg></button>` : '';

  return `<div class="jss-card ${badgeCls}" id="${cardId}">
    <div class="jss-row" onclick="toggleJss('${cardId}')">
      <div class="jss-info">
        <div class="jss-top"><span class="jss-name">${label}</span><span class="jss-badge ${badgeCls}">${status}</span>${editBtn}</div>
        <div class="jss-sub">${range} · ${dur}</div>
      </div>
      <div style="text-align:right;flex-shrink:0;display:flex;flex-direction:column;align-items:flex-end;gap:2px">
        <span style="font-size:14px;font-weight:800;color:var(--text)">${(sess.km||0).toFixed(1)} km</span>
        <span class="jss-ganhos">${DB.fR(ganhos)}</span>
      </div>
      <div class="jss-chev"><svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="6 9 12 15 18 9"/></svg></div>
    </div>
    <div class="jss-rate-row">
      <div class="jss-rcard ${rshCls}"><strong>${rsPorHora}</strong><span>R$/hora</span></div>
      <div class="jss-rcard ${rskmCls}"><strong>${rsPorKm}</strong><span>R$/km</span></div>
      ${hasCusto ? `<div class="jss-rcard ${lucroCls}"><strong>${DB.fR(lucroVal)}</strong><span>Lucro</span></div>` : `<div class="jss-rcard"><strong>—</strong><span>Lucro</span></div>`}
    </div>
    ${elapsedSecs > 0 ? `<div class="jss-statusbar">
      <div class="sbar-wrap" onclick="event.stopPropagation();toggleBar('${bdListId}')"><div class="sbar">${buildSbarHtml(sessBd)}</div></div>
      <div class="sbar-list" id="${bdListId}">${buildSrowsHtml(sessBd)}</div>
    </div>` : ''}
    <div class="jss-trips">
      <div class="jss-trips-head">
        <h4>Corridas desta jornada</h4>
        ${corridas > 0 ? `<span>${corridas} corrida${corridas !== 1 ? 's' : ''}</span>` : ''}
      </div>
      ${corridas > 0
        ? [
            ...trips.map(t => ({ ts: t.ts || (t.time?new Date(dateStr+'T'+t.time+':00').getTime():0), html: tripItem(t) })),
            ...autoTrips.map(t => ({ ts: autoTripTs(t), html: autoTripItem(t) })),
          ].sort((a,b)=>b.ts-a.ts).map(x=>x.html).join('')
        : '<div class="jss-trips-empty">Nenhuma corrida nesta jornada</div>'}
    </div>
  </div>`;

}
// ── Editar sessão ─────────────────────────────────────────────────────────
function editJss(dateStr, idx) {
  const day = DB.day(dateStr);
  const sess = (day.sessions || [])[idx];
  if (!sess) return;

  document.getElementById('jss-edit-date').value = dateStr;
  document.getElementById('jss-edit-idx').value  = idx;
  document.getElementById('jss-edit-km').value   = (sess.km || 0).toFixed(1);

  // Hora início e fim a partir dos timestamps da sessão
  const fmt = ms => {
    if (!ms) return '';
    const d = new Date(ms);
    return String(d.getHours()).padStart(2,'0') + ':' + String(d.getMinutes()).padStart(2,'0');
  };
  document.getElementById('jss-edit-inicio').value = fmt(sess.start);
  document.getElementById('jss-edit-fim').value    = fmt(sess.end);

  openMo('mo-jss-edit');
}

function saveJss() {
  const dateStr = document.getElementById('jss-edit-date').value;
  const idx     = parseInt(document.getElementById('jss-edit-idx').value);
  const km      = parseFloat(document.getElementById('jss-edit-km').value) || 0;
  const inicioStr = document.getElementById('jss-edit-inicio').value; // "HH:MM"
  const fimStr    = document.getElementById('jss-edit-fim').value;    // "HH:MM"

  const day = DB.day(dateStr);
  if (!day.sessions || !day.sessions[idx]) { closeMo('mo-jss-edit'); return; }

  const sess = day.sessions[idx];

  // Recalcular timestamps de início e fim mantendo a data original
  const toMs = (base, timeStr) => {
    if (!timeStr) return base;
    const [hh, mm] = timeStr.split(':').map(Number);
    const d = new Date(base || Date.now());
    d.setHours(hh, mm, 0, 0);
    return d.getTime();
  };
  const newStart = toMs(sess.start, inicioStr);
  const newEnd   = toMs(sess.end || sess.start, fimStr);
  // Elapsed recalculado a partir da diferença início→fim
  const elapsed  = newEnd > newStart ? Math.round((newEnd - newStart) / 1000) : (sess.elapsed || 0);

  sess.km      = km;
  sess.start   = newStart;
  sess.end     = newEnd;
  sess.elapsed = elapsed;
  DB.saveDay(day);

  // Sincronizar com Supabase
  if (_supaUser && sess.id) {
    _SUPA.from('sessions').update({
      km:           parseFloat(km.toFixed(3)),
      start_ms:     newStart,
      end_ms:       newEnd,
      elapsed_secs: elapsed
    }).eq('id', sess.id).then(() => {}).catch(() => {});
  }

  closeMo('mo-jss-edit');
  renderJornada();
  if (S.sec === 'relatorios') renderHome();
  showToast('Sessão atualizada');
}

function deleteJss(dateStr, idx) {
  showConfirm('Excluir esta sessão?', () => {
    const day = DB.day(dateStr);
    if (!day.sessions || !day.sessions[idx]) return;

    const sess = day.sessions[idx];
    day.sessions.splice(idx, 1);
    DB.saveDay(day);

    // Remover do Supabase (delete real, não update)
    if (sess.id) {
      SYNC.del('sessions', { id: sess.id });
    }

    renderJornada();
    if (S.sec === 'relatorios') renderHome();
    showToast('Sessão excluída');
  }, { confirmLabel: 'Excluir', danger: true, cancelLabel: 'Cancelar' });
}


// CORRIGIDO (15/08/2026, relatado pelo Yuri): mesmo bug do toggleTripDetail
// (comentário abaixo, 24/07) só que no card da jornada — toggleJss só
// mexia numa classe CSS, sem guardar estado. O refresh automático de 25s
// (_refreshAutoTripsIfOnJornada) recria os cards do zero sempre fechados,
// então a expansão "voltava a fechar sozinha" em segundos. Mesmo padrão
// de correção: guarda o id aberto, reaplica depois de qualquer redesenho.
let _openJssId = null;
function toggleJss(id){
  const el=document.getElementById(id);
  if(!el) return;
  const willOpen = !el.classList.contains('open');
  el.classList.toggle('open');
  _openJssId = willOpen ? id : null;
}
function reapplyOpenJss(){
  if (!_openJssId) return;
  const el = document.getElementById(_openJssId);
  if (el) el.classList.add('open');
}
// CORRIGIDO (24/07/2026, relatado pelo Yuri): o painel de detalhe de uma
// corrida é só um display:none/block num elemento — quando a tela
// redesenha os cards (o refresh automático de 25s/60s da Jornada/Home,
// por exemplo), os elementos são recriados do zero, sempre fechados por
// padrão, perdendo o que estava aberto na hora errada (parecia fechar
// sozinho, sem motivo). Guarda qual tid está aberto e reaplica depois de
// qualquer redesenho.
let _openTripDetailTid = null;
// Linha vertical da rota (Aceite/Embarque/Destino) tem que parar exatamente
// no centro do último marcador (pin de Destino) — pedido do Yuri
// (16/08/2026): como os marcadores têm tamanhos diferentes (círculo
// pequeno vs pin maior), um valor fixo de CSS nunca acerta em todo caso,
// então mede a posição real na tela depois de renderizado.
function sizeRouteLine(panelEl) {
  try {
    const block = panelEl.querySelector('.route-block');
    if (!block) return;
    const marks = block.querySelectorAll('.route-mark');
    const line = block.querySelector('.route-line');
    if (!line || marks.length < 2) return;
    const first = marks[0], last = marks[marks.length - 1];
    const blockTop = block.getBoundingClientRect().top;
    const firstMid = first.getBoundingClientRect().top - blockTop + first.offsetHeight / 2;
    const lastMid = last.getBoundingClientRect().top - blockTop + last.offsetHeight / 2;
    if (lastMid <= firstMid) return;
    line.style.top = firstMid + 'px';
    line.style.height = (lastMid - firstMid) + 'px';
  } catch (e) {}
}
function toggleTripDetail(tid) {
  const det = document.getElementById('td-'+tid);
  const row = document.getElementById('tr-'+tid);
  if (!det||!row) return;
  const open = det.style.display!=='none';
  document.querySelectorAll('.trip-detail-panel').forEach(el=>el.style.display='none');
  document.querySelectorAll('.li-row[id^="tr-"]').forEach(el=>el.classList.remove('open'));
  if (!open){det.style.display='block';row.classList.add('open');_openTripDetailTid=tid;sizeRouteLine(det);}
  else {_openTripDetailTid=null;}
}
// Chamar depois de qualquer redesenho que possa ter recriado os cards de
// corrida (renderJornada, renderCorridasActive etc.) — reabre sozinho se
// o mesmo tid ainda existir na tela.
function reapplyOpenTripDetail() {
  if (!_openTripDetailTid) return;
  const det = document.getElementById('td-'+_openTripDetailTid);
  const row = document.getElementById('tr-'+_openTripDetailTid);
  if (det && row) { det.style.display='block'; row.classList.add('open'); sizeRouteLine(det); }
}

function esc(s){return String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
// Escapa texto externo (Nominatim/Overpass) pra uso dentro de um literal JS
// de aspas simples que por sua vez está dentro de um atributo onclick="..."
// de aspas duplas — precisa fugir dos dois delimitadores ao mesmo tempo,
// senão um "/' no rótulo devolvido pela API quebra o atributo e injeta HTML.
function escJsAttr(s){return String(s==null?'':s).replace(/\\/g,'\\\\').replace(/'/g,"\\'").replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;');}
// ── Logos das plataformas (assets originais processados: fundo removido) ──
const LOGO_UBER = 'data:image/png;base64,
===== sessions @ 663868 =====
${origIdx})" title="Excluir"><svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/></svg></button>` : '';

  return `<div class="jss-card ${badgeCls}" id="${cardId}">
    <div class="jss-row" onclick="toggleJss('${cardId}')">
      <div class="jss-info">
        <div class="jss-top"><span class="jss-name">${label}</span><span class="jss-badge ${badgeCls}">${status}</span>${editBtn}</div>
        <div class="jss-sub">${range} · ${dur}</div>
      </div>
      <div style="text-align:right;flex-shrink:0;display:flex;flex-direction:column;align-items:flex-end;gap:2px">
        <span style="font-size:14px;font-weight:800;color:var(--text)">${(sess.km||0).toFixed(1)} km</span>
        <span class="jss-ganhos">${DB.fR(ganhos)}</span>
      </div>
      <div class="jss-chev"><svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="6 9 12 15 18 9"/></svg></div>
    </div>
    <div class="jss-rate-row">
      <div class="jss-rcard ${rshCls}"><strong>${rsPorHora}</strong><span>R$/hora</span></div>
      <div class="jss-rcard ${rskmCls}"><strong>${rsPorKm}</strong><span>R$/km</span></div>
      ${hasCusto ? `<div class="jss-rcard ${lucroCls}"><strong>${DB.fR(lucroVal)}</strong><span>Lucro</span></div>` : `<div class="jss-rcard"><strong>—</strong><span>Lucro</span></div>`}
    </div>
    ${elapsedSecs > 0 ? `<div class="jss-statusbar">
      <div class="sbar-wrap" onclick="event.stopPropagation();toggleBar('${bdListId}')"><div class="sbar">${buildSbarHtml(sessBd)}</div></div>
      <div class="sbar-list" id="${bdListId}">${buildSrowsHtml(sessBd)}</div>
    </div>` : ''}
    <div class="jss-trips">
      <div class="jss-trips-head">
        <h4>Corridas desta jornada</h4>
        ${corridas > 0 ? `<span>${corridas} corrida${corridas !== 1 ? 's' : ''}</span>` : ''}
      </div>
      ${corridas > 0
        ? [
            ...trips.map(t => ({ ts: t.ts || (t.time?new Date(dateStr+'T'+t.time+':00').getTime():0), html: tripItem(t) })),
            ...autoTrips.map(t => ({ ts: autoTripTs(t), html: autoTripItem(t) })),
          ].sort((a,b)=>b.ts-a.ts).map(x=>x.html).join('')
        : '<div class="jss-trips-empty">Nenhuma corrida nesta jornada</div>'}
    </div>
  </div>`;

}
// ── Editar sessão ─────────────────────────────────────────────────────────
function editJss(dateStr, idx) {
  const day = DB.day(dateStr);
  const sess = (day.sessions || [])[idx];
  if (!sess) return;

  document.getElementById('jss-edit-date').value = dateStr;
  document.getElementById('jss-edit-idx').value  = idx;
  document.getElementById('jss-edit-km').value   = (sess.km || 0).toFixed(1);

  // Hora início e fim a partir dos timestamps da sessão
  const fmt = ms => {
    if (!ms) return '';
    const d = new Date(ms);
    return String(d.getHours()).padStart(2,'0') + ':' + String(d.getMinutes()).padStart(2,'0');
  };
  document.getElementById('jss-edit-inicio').value = fmt(sess.start);
  document.getElementById('jss-edit-fim').value    = fmt(sess.end);

  openMo('mo-jss-edit');
}

function saveJss() {
  const dateStr = document.getElementById('jss-edit-date').value;
  const idx     = parseInt(document.getElementById('jss-edit-idx').value);
  const km      = parseFloat(document.getElementById('jss-edit-km').value) || 0;
  const inicioStr = document.getElementById('jss-edit-inicio').value; // "HH:MM"
  const fimStr    = document.getElementById('jss-edit-fim').value;    // "HH:MM"

  const day = DB.day(dateStr);
  if (!day.sessions || !day.sessions[idx]) { closeMo('mo-jss-edit'); return; }

  const sess = day.sessions[idx];

  // Recalcular timestamps de início e fim mantendo a data original
  const toMs = (base, timeStr) => {
    if (!timeStr) return base;
    const [hh, mm] = timeStr.split(':').map(Number);
    const d = new Date(base || Date.now());
    d.setHours(hh, mm, 0, 0);
    return d.getTime();
  };
  const newStart = toMs(sess.start, inicioStr);
  const newEnd   = toMs(sess.end || sess.start, fimStr);
  // Elapsed recalculado a partir da diferença início→fim
  const elapsed  = newEnd > newStart ? Math.round((newEnd - newStart) / 1000) : (sess.elapsed || 0);

  sess.km      = km;
  sess.start   = newStart;
  sess.end     = newEnd;
  sess.elapsed = elapsed;
  DB.saveDay(day);

  // Sincronizar com Supabase
  if (_supaUser && sess.id) {
    _SUPA.from('sessions').update({
      km:           parseFloat(km.toFixed(3)),
      start_ms:     newStart,
      end_ms:       newEnd,
      elapsed_secs: elapsed
    }).eq('id', sess.id).then(() => {}).catch(() => {});
  }

  closeMo('mo-jss-edit');
  renderJornada();
  if (S.sec === 'relatorios') renderHome();
  showToast('Sessão atualizada');
}

function deleteJss(dateStr, idx) {
  showConfirm('Excluir esta sessão?', () => {
    const day = DB.day(dateStr);
    if (!day.sessions || !day.sessions[idx]) return;

    const sess = day.sessions[idx];
    day.sessions.splice(idx, 1);
    DB.saveDay(day);

    // Remover do Supabase (delete real, não update)
    if (sess.id) {
      SYNC.del('sessions', { id: sess.id });
    }

    renderJornada();
    if (S.sec === 'relatorios') renderHome();
    showToast('Sessão excluída');
  }, { confirmLabel: 'Excluir', danger: true, cancelLabel: 'Cancelar' });
}


// CORRIGIDO (15/08/2026, relatado pelo Yuri): mesmo bug do toggleTripDetail
// (comentário abaixo, 24/07) só que no card da jornada — toggleJss só
// mexia numa classe CSS, sem guardar estado. O refresh automático de 25s
// (_refreshAutoTripsIfOnJornada) recria os cards do zero sempre fechados,
// então a expansão "voltava a fechar sozinha" em segundos. Mesmo padrão
// de correção: guarda o id aberto, reaplica depois de qualquer redesenho.
let _openJssId = null;
function toggleJss(id){
  const el=document.getElementById(id);
  if(!el) return;
  const willOpen = !el.classList.contains('open');
  el.classList.toggle('open');
  _openJssId = willOpen ? id : null;
}
function reapplyOpenJss(){
  if (!_openJssId) return;
  const el = document.getElementById(_openJssId);
  if (el) el.classList.add('open');
}
// CORRIGIDO (24/07/2026, relatado pelo Yuri): o painel de detalhe de uma
// corrida é só um display:none/block num elemento — quando a tela
// redesenha os cards (o refresh automático de 25s/60s da Jornada/Home,
// por exemplo), os elementos são recriados do zero, sempre fechados por
// padrão, perdendo o que estava aberto na hora errada (parecia fechar
// sozinho, sem motivo). Guarda qual tid está aberto e reaplica depois de
// qualquer redesenho.
let _openTripDetailTid = null;
// Linha vertical da rota (Aceite/Embarque/Destino) tem que parar exatamente
// no centro do último marcador (pin de Destino) — pedido do Yuri
// (16/08/2026): como os marcadores têm tamanhos diferentes (círculo
// pequeno vs pin maior), um valor fixo de CSS nunca acerta em todo caso,
// então mede a posição real na tela depois de renderizado.
function sizeRouteLine(panelEl) {
  try {
    const block = panelEl.querySelector('.route-block');
    if (!block) return;
    const marks = block.querySelectorAll('.route-mark');
    const line = block.querySelector('.route-line');
    if (!line || marks.length < 2) return;
    const first = marks[0], last = marks[marks.length - 1];
    const blockTop = block.getBoundingClientRect().top;
    const firstMid = first.getBoundingClientRect().top - blockTop + first.offsetHeight / 2;
    const lastMid = last.getBoundingClientRect().top - blockTop + last.offsetHeight / 2;
    if (lastMid <= firstMid) return;
    line.style.top = firstMid + 'px';
    line.style.height = (lastMid - firstMid) + 'px';
  } catch (e) {}
}
function toggleTripDetail(tid) {
  const det = document.getElementById('td-'+tid);
  const row = document.getElementById('tr-'+tid);
  if (!det||!row) return;
  const open = det.style.display!=='none';
  document.querySelectorAll('.trip-detail-panel').forEach(el=>el.style.display='none');
  document.querySelectorAll('.li-row[id^="tr-"]').forEach(el=>el.classList.remove('open'));
  if (!open){det.style.display='block';row.classList.add('open');_openTripDetailTid=tid;sizeRouteLine(det);}
  else {_openTripDetailTid=null;}
}
// Chamar depois de qualquer redesenho que possa ter recriado os cards de
// corrida (renderJornada, renderCorridasActive etc.) — reabre sozinho se
// o mesmo tid ainda existir na tela.
function reapplyOpenTripDetail() {
  if (!_openTripDetailTid) return;
  const det = document.getElementById('td-'+_openTripDetailTid);
  const row = document.getElementById('tr-'+_openTripDetailTid);
  if (det && row) { det.style.display='block'; row.classList.add('open'); sizeRouteLine(det); }
}

function esc(s){return String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
// Escapa texto externo (Nominatim/Overpass) pra uso dentro de um literal JS
// de aspas simples que por sua vez está dentro de um atributo onclick="..."
// de aspas duplas — precisa fugir dos dois delimitadores ao mesmo tempo,
// senão um "/' no rótulo devolvido pela API quebra o atributo e injeta HTML.
function escJsAttr(s){return String(s==null?'':s).replace(/\\/g,'\\\\').replace(/'/g,"\\'").replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;');}
// ── Logos das plataformas (assets originais processados: fundo removido) ──
const LOGO_UBER = 'data:image/png;base64,
===== sessions @ 689418 =====
';
function platIcon(plat) {
  const p = (plat||'uber').toLowerCase();
  if (p === '99') return '<div class="li-ico plat-99"><img src="'+LOGO_99+'" alt="99"></div>';
  if (p === 'uber') return '<div class="li-ico plat-uber"><img src="'+LOGO_UBER+'" alt="Uber"></div>';
  return '<div class="li-ico plat-generic"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 17h14M6 17l1-6 3-4h4l3 4 1 6"/><circle cx="7.5" cy="17" r="1.5"/><circle cx="16.5" cy="17" r="1.5"/></svg></div>';
}
// ── CUSTO DO VEÍCULO (lucro líquido real por corrida) ──────────
let custoW = { step: 0, data: {} };
function custoStepList() {
  const d = custoW.data;
  const steps = ['situacao'];
  if (d.situacao === 'financiado' || d.situacao === 'alugado') steps.push('parcela');
  steps.push('combustivel');
  if (d.combustivel === 'liquido') steps.push('fuel_liquido');
  else if (d.combustivel === 'gnv') steps.push('fuel_gnv');
  else if (d.combustivel === 'eletrico') steps.push('fuel_eletrico');
  steps.push('depreciacao', 'manutencao', 'extras', 'resultado');
  return steps;
}
// Média de km/mês dos últimos meses com sessão de GPS registrada — usada
// como sugestão no passo de manutenção (nunca preenche sozinho, só sugere).
function custoSuggestKmMes() {
  const dates = DB.allDates().slice(-90);
  let totalKm = 0; const mesesSet = new Set();
  dates.forEach(d => {
    const day = DB.day(d);
    const km = (day.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
    if (km > 0) { totalKm += km; mesesSet.add(d.slice(0,7)); }
  });
  const nMeses = mesesSet.size;
  return (nMeses >= 1 && totalKm > 0) ? Math.round(totalKm / nMeses) : null;
}
function calcCustoPorKm(d) {
  let combKm = 0;
  if (d.combustivel === 'liquido' && d.consumoKmL) combKm = (d.precoLitro||0) / d.consumoKmL;
  else if (d.combustivel === 'gnv' && d.consumoKmM3) combKm = (d.precoM3||0) / d.consumoKmM3;
  else if (d.combustivel === 'eletrico' && d.autonomiaKm) combKm = (d.valorRecarga||0) / d.autonomiaKm;
  const manutKm = d.custoManut10k ? d.custoManut10k / 10000 : 0;
  const depKm = (d.depreciacaoPct && d.valorVeiculo && d.kmMes) ? (d.valorVeiculo * d.depreciacaoPct/100) / (d.kmMes * 12) : 0;
  const extrasNovo = (d.seguroMensal||0) + ((d.ipvaAnual||0)/12) + (d.outrosExtras||0);
  const fixoMensal = ((d.situacao !== 'quitado') ? (d.parcelaMensal||0) : 0) + (extrasNovo > 0 ? extrasNovo : (d.extrasMensal||0));
  const fixoKm = (fixoMensal && d.kmMes) ? fixoMensal / d.kmMes : 0;
  return { combKm, manutKm, depKm, fixoKm, total: combKm+manutKm+depKm+fixoKm };
}
async function openCustoVeiculo() {
  // Evita mostrar formulário vazio (fazendo o usuário preencher de novo)
  // por causa de a restauração da nuvem ainda não ter chegado — só espera
  // se realmente ainda estiver em andamento (senão não atrasa nada).
  if (!_bootPullDone) { showToast('Sincronizando seus dados...'); await _waitBootPull(); }
  const cfg = DB.cfg();
  custoW = { step: 0, data: Object.assign({
    situacao: null, parcelaMensal: null, diasTrabalhados: 24,
    combustivel: null,
    consumoKmL: null, precoLitro: null,
    consumoKmM3: null, precoM3: null,
    autonomiaKm: null, valorRecarga: null,
    depreciacaoPct: 0, valorVeiculo: null,
    kmMes: null, custoManut10k: null,
    extrasMensal: null, seguroMensal: null, ipvaAnual: null, outrosExtras: null
  }, cfg.custoVeiculo || {}) };
  openMo('mo-custo');
  renderCustoStep();
}
function custoBack() {
  if (custoW.step > 0) { custoW.step--; renderCustoStep(); }
  else closeMo('mo-custo');
}
function custoGoNext() { custoW.step++; renderCustoStep(); }
function custoSelectSituacao(v) { custoW.data.situacao = v; renderCustoStep(); }
function custoSelectCombustivel(v) { custoW.data.combustivel = v; renderCustoStep(); }
function custoSelectDeprec(pct) { custoW.data.depreciacaoPct = pct; renderCustoStep(); }
function custoSetField(field, val) { custoW.data[field] = val === '' ? null : parseFloat(String(val).replace(',','.')); }
function custoUseSuggestedKm() {
  const sug = custoSuggestKmMes();
  if (sug) { custoW.data.kmMes = sug; renderCustoStep(); }
}
function custoFinish() {
  const cfg = DB.cfg();
  const d = custoW.data;
  const calc = calcCustoPorKm(d);
  cfg.custoVeiculo = Object.assign({}, d, { custoPorKm: calc.total, configurado: true, atualizadoEm: DB.today() });
  DB.saveCfg(cfg);
  SYNC.pushSettings();
  syncFlashConfigToNative();
  closeMo('mo-custo');
  showToast('Custo do veículo configurado! Suas corridas já mostram o líquido.');
  try { renderJornada(); } catch(e){}
  try { if (typeof renderCorridasActive === 'function') renderCorridasActive(); } catch(e){}
  try { updateCustoBanner(); } catch(e){}
  try { renderMais(); } catch(e){}
}
function renderCustoStep() {
  const steps = custoStepList();
  const idx = Math.min(custoW.step, steps.length - 1);
  const stepId = steps[idx];
  const d = custoW.data;
  const dotsHtml = steps.map((s,i) => `<div class="cv-dot${i<=idx?' on':''}"></div>`).join('');
  const head = `<div class="cv-head"><button class="cv-back" onclick="custoBack()"><svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M19 12H5M12 19l-7-7 7-7"/></svg></button><div class="cv-dots">${dotsHtml}</div><div style="width:32px;flex:0 0 32px"></div></div>`;
  let body = '';

  if (stepId === 'situacao') {
    const opt = (val, label, sub, ico) => `<div class="cv-opt-row${d.situacao===val?' sel':''}" onclick="custoSelectSituacao('${val}')">
      <div class="cv-opt-ico">${ico}</div><div class="cv-txt"><b>${label}</b><small>${sub}</small></div>
      <div class="cv-radio${d.situacao===val?' on':''}"></div></div>`;
    body = head + `<div class="cv-h2">Como está seu carro?</div><div class="cv-sub">Isso define se entra parcela mensal na conta.</div>`
      + opt('quitado','Quitado','Carro já é seu, sem parcela','<svg viewBox="0 0 24 24" width="17" height="17" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="20 6 9 17 4 12"/></svg>')
      + opt('financiado','Financiado','Ainda pagando parcelas','<svg viewBox="0 0 24 24" width="17" height="17" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="6" width="20" height="12" rx="2"/><line x1="2" y1="10" x2="22" y2="10"/></svg>')
      + opt('alugado','Alugado','Carro por assinatura/locação','<svg viewBox="0 0 24 24" width="17" height="17" fill="none" stroke="currentColor" stroke-width="2"><circle cx="8" cy="15" r="4"/><path d="M10.5 12.5 19 4M17 6l3 3M15 8l2 2"/></svg>')
      + `<button class="cv-btn" style="${d.situacao?'':'opacity:.4;pointer-events:none'}" onclick="custoGoNext()">Continuar</button>`;
  }
  else if (stepId === 'parcela') {
    body = head + `<div class="cv-h2">Parcela e dias trabalhados</div><div class="cv-sub">Pra dividir o custo fixo do carro pelos dias que você roda.</div>
      <div class="field"><label class="cv-lbl">Valor da parcela mensal</label><div class="cv-inp-wrap"><span>R$</span><input type="number" value="${d.parcelaMensal??''}" oninput="custoSetField('parcelaMensal',this.value)"></div></div>
      <div class="field"><label class="cv-lbl">Dias trabalhados por mês</label><div class="cv-inp-wrap"><input type="number" value="${d.diasTrabalhados??''}" oninput="custoSetField('diasTrabalhados',this.value)"><span>dias</span></div></div>
      <button class="cv-btn" onclick="custoGoNext()">Continuar</button>`;
  }
  else if (stepId === 'combustivel') {
    const card = (val,label,ico) => `<div class="cv-opt-card${d.combustivel===val?' sel':''}" onclick="custoSelectCombustivel('${val}')"><div class="cv-opt-ico">${ico}</div><span>${label}</span></div>`;
    body = head + `<div class="cv-h2">Qual o combustível?</div><div class="cv-sub">As próximas perguntas mudam de acordo com a escolha.</div>
      <div class="cv-opt-grid">
        ${card('liquido','Líquido','<svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 22V6a2 2 0 0 1 2-2h6a2 2 0 0 1 2 2v16"/><path d="M3 10h10"/><path d="M15 8l3.5 3.5c.5.5.5 1 .5 1.5v5a1.5 1.5 0 0 1-3 0"/></svg>')}
        ${card('gnv','GNV','<svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2C9 6 6 9.5 6 13a6 6 0 0 0 12 0c0-3.5-3-7-6-11z"/></svg>')}
        ${card('eletrico','Elétrico','<svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2"><polygon points="13 2 3 14 11 14 9 22 21 10 13 10 13 2"/></svg>')}
      </div>
      <button class="cv-btn" style="${d.combustivel?'':'opacity:.4;pointer-events:none'}" onclick="custoGoNext()">Continuar</button>`;
  }
  else if (stepId === 'fuel_liquido') {
    body = head + `<div class="cv-h2">Consumo e preço</div><div class="cv-sub">Gasolina, etanol ou flex — usa a média que você abastece.</div>
      <div class="field"><label class="cv-lbl">Consumo médio</label><div class="cv-inp-wrap"><input type="number" value="${d.consumoKmL??''}" oninput="custoSetField('consumoKmL',this.value)"><span>km/l</span></div></div>
      <div class="field"><label class="cv-lbl">Preço médio do litro</label><div class="cv-inp-wrap"><span>R$</span><input type="number" value="${d.precoLitro??''}" oninput="custoSetField('precoLitro',this.value)"></div></div>
      <button class="cv-btn" onclick="custoGoNext()">Continuar</button>`;
  }
  else if (stepId === 'fuel_gnv') {
    body = head + `<div class="cv-h2">Consumo e preço do GNV</div>
      <div class="field"><label class="cv-lbl">Consumo médio</label><div class="cv-inp-wrap"><input type="number" value="${d.consumoKmM3??''}" oninput="custoSetField('consumoKmM3',this.value)"><span>km/m³</span></div></div>
      <div class="field"><label class="cv-lbl">Preço médio do m³</label><div class="cv-inp-wrap"><span>R$</span><input type="number" value="${d.precoM3??''}" oninput="custoSetField('precoM3',this.value)"></div></div>
      <button class="cv-btn" onclick="custoGoNext()">Continuar</button>`;
  }
  else if (stepId === 'fuel_eletrico') {
    body = head + `<div class="cv-h2">Autonomia e recarga</div>
      <div class="field"><label class="cv-lbl">Autonomia média (carga cheia)</label><div class="cv-inp-wrap"><input type="number" value="${d.autonomiaKm??''}" oninput="custoSetField('autonomiaKm',this.value)"><span>km</span></div></div>
      <div class="field"><label class="cv-lbl">Valor de uma recarga completa</label><div class="cv-inp-wrap"><span>R$</span><input type="number" value="${d.valorRecarga??''}" oninput="custoSetField('valorRecarga',this.value)"></div></div>
      <button class="cv-btn" onclick="custoGoNext()">Continuar</button>`;
  }
  else if (stepId === 'depreciacao') {
    const pctBtn = (val,label) => `<button class="cv-pct-btn${d.depreciacaoPct===val?' sel':''}" onclick="custoSelectDeprec(${val})">${label}</button>`;
    const showVeiculo = d.depreciacaoPct > 0;
    body = head + `<div class="cv-h2">Incluir depreciação?</div><div class="cv-sub">O carro perde valor com o tempo — isso também é um custo real, mesmo sem sair do bolso todo mês.</div>
      <div class="cv-pct-row">${pctBtn(0,'Não incluir')}${pctBtn(5,'5% a.a.')}${pctBtn(10,'10% a.a.')}</div>
      ${showVeiculo ? `<div class="field"><label class="cv-lbl">Valor atual do veículo (FIPE)</label><div class="cv-inp-wrap"><span>R$</span><input type="number" value="${d.valorVeiculo??''}" oninput="custoSetFi
===== sessions @ 770824 =====

      try { renderFin(); } catch(e) {}
      try { renderLancamentos(); } catch(e) {}
    }, 60);
    return;
  }

  const day  = DB.day(date);
  const entry = {
    id: DB.uid(), category: cat?.id || '', categoryLabel: cat?.l || '',
    desc: document.getElementById('l-desc')?.value || '',
    amount: val, date, scope: S.lancScope,
    recorrente: lancRecOn, freq: lancFreqSel,
    seriesId: lancRecOn ? DB.uid() : null,
    status: _lancStatusSel || (S.lancType==='receita'?'pago':'a_pagar')
  };
  if (S.lancType === 'receita') day.revenues.push(entry);
  else day.expenses.push(entry);
  DB.saveDay(day);
  SYNC.push(S.lancType === 'receita' ? 'revenues' : 'expenses', {
    id: entry.id, date: entry.date, category: entry.category, category_label: entry.categoryLabel,
    amount: entry.amount, description: entry.desc, scope: entry.scope,
    recorrente: entry.recorrente, freq: entry.freq, series_id: entry.seriesId,
    status: entry.status || (S.lancType==='receita'?'pago':'a_pagar'),
    due_date: entry.date || null
  });
  closeMo('mo-lancamento');
  document.getElementById('l-valor').value = '';
  document.getElementById('l-desc').value = '';
  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
    try { renderFin(); } catch(e) {}
    try { if (typeof renderLancamentos === 'function') renderLancamentos(); } catch(e) {}
  }, 80);
}

// ── CORRIDA FORM ──────────────────────────────────────────────
function setCorPlat(p) {
  S.corPlat = p;
  ['uber','99','particular'].forEach(x => {
    const el = document.getElementById('cp-'+x); if (el) el.classList.toggle('active', x === p);
  });
}
function saveCorrida() {
  const val = parseFloat(document.getElementById('c-val')?.value || 0);
  if (!val) { showToast('Informe o valor', 'error'); return; }
  const day = DB.day(S.curDate);
  const trip = {
    id: DB.uid(), platform: S.corPlat,
    time:     document.getElementById('c-time')?.value || '',
    duration: parseInt(document.getElementById('c-dur')?.value) || 0,
    origin:   document.getElementById('c-orig')?.value || '',
    dest:     document.getElementById('c-dest')?.value || '',
    value: val, km: parseFloat(document.getElementById('c-km')?.value) || 0
  };
  day.trips.push(trip);
  DB.saveDay(day);
  SYNC.push('trips', { id: trip.id, date: S.curDate, platform: trip.platform, time: trip.time || null,
    duration: trip.duration, origin: trip.origin, dest: trip.dest, value: trip.value, km: trip.km,
    category: '', surge: false, airport: false, source: 'manual' });
  closeMo('mo-corrida');
  ['c-val','c-km','c-orig','c-dest','c-dur'].forEach(id => { const el=document.getElementById(id); if(el)el.value=''; });
  renderJornada();
}

// ── KM FORM ───────────────────────────────────────────────────
function saveKm() {
  const v = parseFloat(document.getElementById('km-inp')?.value);
  if (!v) { showToast('Informe o valor', 'error'); return; }
  const day = DB.day(S.curDate); day.kmTotal = v; DB.saveDay(day);
  closeMo('mo-km'); renderJornada();
}

// ── AJUSTE MANUAL DE KM (corrige imprecisão do GPS) ─────────────
let kmAdjBase = 0;
// ── Editar valor de uma corrida automática direto no card (23/07/2026,
// mockup aprovado) — ao salvar, vira Confirmada (agora tem um valor que o
// próprio motorista conferiu, mais confiável que a oferta estimada).
let autoTripValEditId = null;
function openAutoTripValueEdit(id, currentValue) {
  autoTripValEditId = id;
  const inp = document.getElementById('autotripval-val');
  if (inp) inp.value = (currentValue||0).toFixed(2);
  openMo('mo-autotrip-value');
}
async function saveAutoTripValueEdit() {
  const inp = document.getElementById('autotripval-val');
  const newVal = parseFloat(inp?.value);
  if (isNaN(newVal) || newVal < 0) { showToast('Valor inválido', 'error'); return; }
  if (!autoTripValEditId) { closeMo('mo-autotrip-value'); return; }
  if (!_supaUser) { showToast('Sem conexão com a nuvem.', 'error'); return; }
  try {
    const { error } = await _SUPA.from('auto_trips')
      .update({ offer_value: newVal, status: 'confirmada' })
      .eq('id', autoTripValEditId);
    if (error) throw error;
    closeMo('mo-autotrip-value');
    showToast('Valor atualizado.');
    // Cache de automáticas fica potencialmente desatualizado em 3 telas —
    // força recarregar todas que dependem dela, só a que está ativa
    // agora re-renderiza na hora (mesmo padrão do refresh de 60s/25s).
    S.autoTripsDate = null; S.autoTripsHomeRange = null;
    if (S.sec === 'jornada') renderJornada();
    if (S.sec === 'relatorios') renderHomeGraphs();
    if (S.sec === 'corridas') renderCorridasActive();
  } catch (e) {
    console.error('saveAutoTripValueEdit', e);
    showToast('Erro ao salvar. Tente de novo.', 'error');
  }
}
function openKmAdjust() {
  const day = DB.day(S.curDate);
  const isToday = S.curDate === DB.today();
  const live = isToday && S.gps.st === 'running';
  const liveKm = live ? S.gps.km : 0;
  const finishedKm = (day.sessions || []).reduce((s,x)=>s+(x.km||0),0);
  kmAdjBase = finishedKm + liveKm;
  const current = kmAdjBase + (day.kmAdjust || 0);
  const inp = document.getElementById('kmadj-val');
  if (inp) inp.value = current.toFixed(1);
  openMo('mo-km-adjust');
}
function kmAdjStep(delta) {
  const inp = document.getElementById('kmadj-val');
  if (!inp) return;
  const v = Math.max(0, (parseFloat(inp.value) || 0) + delta);
  inp.value = v.toFixed(1);
}
function saveKmAdjust() {
  const inp = document.getElementById('kmadj-val');
  const newTotal = parseFloat(inp?.value);
  if (isNaN(newTotal) || newTotal < 0) { showToast('Valor inválido', 'error'); return; }
  const day = DB.day(S.curDate);
  day.kmAdjust = parseFloat((newTotal - kmAdjBase).toFixed(2));
  DB.saveDay(day);
  SYNC.push('day_meta', { date: S.curDate, km_adjust: day.kmAdjust, km_total_legacy: day.kmTotal ?? null });
  closeMo('mo-km-adjust');
  renderJornada();
  showToast('Km total ajustado.');
}

// ── Ajuste de tempo total (mesma lógica do km: botões nas laterais + campo editável por texto) ──
let tempoAdjBaseSecs = 0, tempoAdjCurSecs = 0;
function openTempoAdjust() {
  const day = DB.day(S.curDate);
  const isToday = S.curDate === DB.today();
  const live = isToday && S.gps.st === 'running';
  const liveElapsed = live ? getElapsed() : 0;
  const finishedElapsed = (day.sessions || []).reduce((s,x)=>s+(x.elapsed||0),0);
  tempoAdjBaseSecs = finishedElapsed + liveElapsed;
  tempoAdjCurSecs = Math.max(0, tempoAdjBaseSecs + (day.tempoAdjust || 0));
  updateTempoAdjDisplay();
  openMo('mo-tempo-adjust');
}
// Aceita "2h30m", "2h 30m", "2h", "45m" ou número puro (minutos)
function parseTempoAdjInput(text) {
  text = (text || '').trim().toLowerCase().replace(/\s+/g, '');
  if (!text) return null;
  let h = 0, m = 0;
  const full = text.match(/^(\d+)h(\d+)?m?$/);
  const justH = text.match(/^(\d+)h$/);
  const justM = text.match(/^(\d+)m$/);
  const justNum = text.match(/^(\d+)$/);
  if (full) { h = parseInt(full[1]); m = parseInt(full[2] || 0); }
  else if (justH) { h = parseInt(justH[1]); }
  else if (justM) { m = parseInt(justM[1]); }
  else if (justNum) { m = parseInt(justNum[1]); }
  else return null;
  return Math.max(0, h*3600 + m*60);
}
function updateTempoAdjDisplay() {
  const inp = document.getElementById('tempoadj-val');
  if (inp) inp.value = fmtDur(tempoAdjCurSecs / 60);
}
function tempoAdjStep(deltaMin) {
  tempoAdjCurSecs = Math.max(0, tempoAdjCurSecs + deltaMin * 60);
  updateTempoAdjDisplay();
}
function tempoAdjInputCommit() {
  const inp = document.getElementById('tempoadj-val');
  if (!inp) return;
  const parsed = parseTempoAdjInput(inp.value);
  if (parsed === null) { updateTempoAdjDisplay(); return; }
  tempoAdjCurSecs = parsed;
  updateTempoAdjDisplay();
}
function saveTempoAdjust() {
  tempoAdjInputCommit();
  const day = DB.day(S.curDate);
  day.tempoAdjust = Math.round(tempoAdjCurSecs - tempoAdjBaseSecs);
  DB.saveDay(day);
  SYNC.push('day_meta', { date: S.curDate, tempo_adjust_secs: day.tempoAdjust });
  closeMo('mo-tempo-adjust');
  renderJornada();
  showToast('Tempo total ajustado.');
}

// ── IMPORT AI ─────────────────────────────────────────────────
// Detecta vídeo por MIME type, com fallback por extensão do nome do
// arquivo — em alguns celulares (Live Photo/Motion Photo, ou o picker de
// Arquivos) o navegador reporta file.type vazio ou incorreto, e o print
// (imagem) acabava sendo classificado errado e descartado.
function isVideoFile(f) {
  if (f.type) return f.type.startsWith('video/');
  return /\.(mp4|mov|webm|mkv|avi|m4v|3gp)$/i.test(f.name||'');
}
function handleImport(input, plat) {
  if (!input.files.length) return;
  const files = Array.from(input.files);
  closeMo('mo-import');
  const videos = files.filter(isVideoFile);
  const images = files.filter(f => !isVideoFile(f));
  if (!videos.length && !images.length) return;
  processImportBatch(images, videos, plat);
}
// Processa imagens E vídeos da mesma seleção juntos — antes, se um vídeo
// viesse junto (ex: Live Photo confundida com foto), TODAS as imagens da
// seleção eram silenciosamente descartadas (só processava videos[0]).
let importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };

function openImportPrecheck() {
  ['ipc-uber-count','ipc-uber-value','ipc-99-count','ipc-99-value'].forEach(id => {
    const el = document.getElementById(id); if (el) el.value = '';
  });
  openMo('mo-import-precheck');
}
function _readPrecheckField(id) {
  const v = document.getElementById(id)?.value;
  return (v === '' || v === undefined || v === null) ? null : parseFloat(String(v).replace(',','.'));
}
function importPrecheckNext() {
  importPrecheck = {
    uberCount: _readPrecheckField('ipc-uber-count'),
    uberValue: _readPrecheckField('ipc-uber-value'),
    p99Count:  _readPrecheckField('ipc-99-count'),
    p99Value:  _readPrecheckField('ipc-99-value')
  };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}
function importPrecheckSkip() {
  importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}

function renderImportRing(found, target, foundVal, targetVal, statusHtml, isWarn) {
  const hasTarget = target > 0;
  const pct = hasTarget ? Math.min(100, found/target*100) : 0;
  const deg = Math.round(pct*3.6);
  const ringColor = isWarn ? '#F59E0B' : '#22C55E';
  const ringBg = hasTarget ? `conic-gradient(${ringColor} 0deg ${deg}deg, #E2E8F0 ${deg}deg 360deg)` : '#E2E8F0';
  const countHtml = hasTarget ? `${found}<span style="font-size:16px;color:#94A3B8">/${target}</span>` : `${found}`;
  const valHtml = hasTarget ? `${DB.fRI(foundVal)} de ${DB.fRI(targetVal)} já identificados` : `${DB.fRI(foundVal)} identificados até agora`;
  return `<div style="display:flex;flex-direction:column;align-items:center;padding:30px 0 10px">
    <div style="width:130px;height:130px;border-radius:50%;background:${ringBg};display:flex;align-items:center;justify-content:center;margin-bottom:20px;position:relative;transition:background .4s">
      <div style="position:absolute;inset:8px;border-radius:50%;background:#fff"></div>
      <div style="position:relative;text-align:center">
        <div style="font-family:'Poppins',sans-serif;font-weight:900;font-size:28px;color:#0F172A">${countHtml}</div>
        <div style="font-size:10px;color:#64748B;font-weight:700;margin-top:2px">CORRIDAS</div>
      </div>
    </div>
    <div style="font-size:13.5px;font-weight:700;color:#0F172A;margin-bottom:5px;display:flex;align-items:center;gap:6px">${statusHtml}</div>
    <div style="font-size:12px;color:#64748B">${valHtml}</div>
  </div>`;
}

// Tenta de novo (até 3x) antes de desistir de um print/frame — evita que um
// erro passageiro de rede derrube a corrida inteira do import silenciosamente.
async function extractWithRetry(fn, onRetry, maxAttempts) {
  maxAttempts = maxAttempts || 3;
  let lastErr;
  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    try { return await fn(); }
    catch(e) {
      lastErr = e;
      if (attempt < maxAttempts) { onRetry(attempt+1, maxAttempts); await new Promise(r => setTimeout(r, 800)); }
    }
  }
  throw lastErr;
}

// Sobe o vídeo original pro Storage (bucket import-videos, retenção de
// 180 dias) e registra em import_videos — pedido do Yuri (16/08/2026):
// base pra confirmação de valor real (campeonatos/incentivos futuros) e
// pra dar dá pra conferir manualmente qualquer corrida em disputa. Falha
// de upload NUNCA trava o import (retorna null, segue sem vínculo) — o
// vídeo é auditoria extra, não pode ser bloqueio de fluxo.
async function uploadImportVideo(file, plat, operationalDate) {
  if (!_supaUser) return null;
  try {
    const ext = (file.name && file.name.includes('.')) ? file.name.split('.').pop().toLowerCase() : 'mp4';
    const opDate = operationalDate || S.curDate || DB.today();
    const opPlat = normalizeOperationalPlatform(plat);
    const opRange = operationalRangeForPlatform(opPlat, opDate);
    const path = `${_supaUser.id}/${opDate}/${opPlat}/${DB.uid()}.${ext}`;
    const { error: upErr } = await _SUPA.storage.from('import-videos').upload(path, file, {
      contentType: file.type || 'video/mp4'
    });
    if (upErr) { console.warn('upload vídeo falhou (segue sem vínculo):', upErr); return null; }
    const { data, error } = await _SUPA.from('import_videos').insert({
      user_id: _supaUser.id, storage_path: path, file_size_bytes: file.size || null,
      platform: opPlat, operational_date: opDate,
      period_start: new Date(opRange.startMs).toISOString(),
      period_end: new Date(opRange.endMs).toISOString()
    }).select('id').single();
    if (error) { console.warn('registro import_videos falhou (segue sem vínculo):', error); return null; }
    return data.id;
  } catch(e) { console.warn('uploadImportVideo erro:', e); return null; }
}
async function processImportBatch(images, videos, plat) {
  const operationalDate = S.curDate || DB.today();
  const operationalPlat = normalizeOperationalPlatform(plat);
  window._pendingImportOperationalDate = operationalDate;
  window._pendingImportPlatform = operationalPlat;
  const targetCount = (importPrecheck.uberCount||0) + (importPrecheck.p99Count||0);
  const targetValue = (importPrecheck.uberValue||0) + (importPrecheck.p99Value||0);
  const totalImgs = images.length, totalVids = videos.length;
  const label = [totalImgs?`${totalImgs} print${totalImgs>1?'s':''}`:'', totalVids?`${totalVids} vídeo${totalVids>1?'s':''}`:''].filter(Boolean).join(' + ');
  document.getElementById('ic-title').textContent = 'Analisando ' + label;
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');

  const unique = [];
  const details = [];
  const seen = new Set();
  let foundCount = 0, foundVal = 0;
  let curDetail = null; // acumulador do fragmento de detalhe em aberto
  const flushDetail = () => { if (curDetail) { details.push(curDe
===== sessions @ 772172 =====
ion') renderLancamentos(); } catch(e) {}
  }, 80);
}

// ── CORRIDA FORM ──────────────────────────────────────────────
function setCorPlat(p) {
  S.corPlat = p;
  ['uber','99','particular'].forEach(x => {
    const el = document.getElementById('cp-'+x); if (el) el.classList.toggle('active', x === p);
  });
}
function saveCorrida() {
  const val = parseFloat(document.getElementById('c-val')?.value || 0);
  if (!val) { showToast('Informe o valor', 'error'); return; }
  const day = DB.day(S.curDate);
  const trip = {
    id: DB.uid(), platform: S.corPlat,
    time:     document.getElementById('c-time')?.value || '',
    duration: parseInt(document.getElementById('c-dur')?.value) || 0,
    origin:   document.getElementById('c-orig')?.value || '',
    dest:     document.getElementById('c-dest')?.value || '',
    value: val, km: parseFloat(document.getElementById('c-km')?.value) || 0
  };
  day.trips.push(trip);
  DB.saveDay(day);
  SYNC.push('trips', { id: trip.id, date: S.curDate, platform: trip.platform, time: trip.time || null,
    duration: trip.duration, origin: trip.origin, dest: trip.dest, value: trip.value, km: trip.km,
    category: '', surge: false, airport: false, source: 'manual' });
  closeMo('mo-corrida');
  ['c-val','c-km','c-orig','c-dest','c-dur'].forEach(id => { const el=document.getElementById(id); if(el)el.value=''; });
  renderJornada();
}

// ── KM FORM ───────────────────────────────────────────────────
function saveKm() {
  const v = parseFloat(document.getElementById('km-inp')?.value);
  if (!v) { showToast('Informe o valor', 'error'); return; }
  const day = DB.day(S.curDate); day.kmTotal = v; DB.saveDay(day);
  closeMo('mo-km'); renderJornada();
}

// ── AJUSTE MANUAL DE KM (corrige imprecisão do GPS) ─────────────
let kmAdjBase = 0;
// ── Editar valor de uma corrida automática direto no card (23/07/2026,
// mockup aprovado) — ao salvar, vira Confirmada (agora tem um valor que o
// próprio motorista conferiu, mais confiável que a oferta estimada).
let autoTripValEditId = null;
function openAutoTripValueEdit(id, currentValue) {
  autoTripValEditId = id;
  const inp = document.getElementById('autotripval-val');
  if (inp) inp.value = (currentValue||0).toFixed(2);
  openMo('mo-autotrip-value');
}
async function saveAutoTripValueEdit() {
  const inp = document.getElementById('autotripval-val');
  const newVal = parseFloat(inp?.value);
  if (isNaN(newVal) || newVal < 0) { showToast('Valor inválido', 'error'); return; }
  if (!autoTripValEditId) { closeMo('mo-autotrip-value'); return; }
  if (!_supaUser) { showToast('Sem conexão com a nuvem.', 'error'); return; }
  try {
    const { error } = await _SUPA.from('auto_trips')
      .update({ offer_value: newVal, status: 'confirmada' })
      .eq('id', autoTripValEditId);
    if (error) throw error;
    closeMo('mo-autotrip-value');
    showToast('Valor atualizado.');
    // Cache de automáticas fica potencialmente desatualizado em 3 telas —
    // força recarregar todas que dependem dela, só a que está ativa
    // agora re-renderiza na hora (mesmo padrão do refresh de 60s/25s).
    S.autoTripsDate = null; S.autoTripsHomeRange = null;
    if (S.sec === 'jornada') renderJornada();
    if (S.sec === 'relatorios') renderHomeGraphs();
    if (S.sec === 'corridas') renderCorridasActive();
  } catch (e) {
    console.error('saveAutoTripValueEdit', e);
    showToast('Erro ao salvar. Tente de novo.', 'error');
  }
}
function openKmAdjust() {
  const day = DB.day(S.curDate);
  const isToday = S.curDate === DB.today();
  const live = isToday && S.gps.st === 'running';
  const liveKm = live ? S.gps.km : 0;
  const finishedKm = (day.sessions || []).reduce((s,x)=>s+(x.km||0),0);
  kmAdjBase = finishedKm + liveKm;
  const current = kmAdjBase + (day.kmAdjust || 0);
  const inp = document.getElementById('kmadj-val');
  if (inp) inp.value = current.toFixed(1);
  openMo('mo-km-adjust');
}
function kmAdjStep(delta) {
  const inp = document.getElementById('kmadj-val');
  if (!inp) return;
  const v = Math.max(0, (parseFloat(inp.value) || 0) + delta);
  inp.value = v.toFixed(1);
}
function saveKmAdjust() {
  const inp = document.getElementById('kmadj-val');
  const newTotal = parseFloat(inp?.value);
  if (isNaN(newTotal) || newTotal < 0) { showToast('Valor inválido', 'error'); return; }
  const day = DB.day(S.curDate);
  day.kmAdjust = parseFloat((newTotal - kmAdjBase).toFixed(2));
  DB.saveDay(day);
  SYNC.push('day_meta', { date: S.curDate, km_adjust: day.kmAdjust, km_total_legacy: day.kmTotal ?? null });
  closeMo('mo-km-adjust');
  renderJornada();
  showToast('Km total ajustado.');
}

// ── Ajuste de tempo total (mesma lógica do km: botões nas laterais + campo editável por texto) ──
let tempoAdjBaseSecs = 0, tempoAdjCurSecs = 0;
function openTempoAdjust() {
  const day = DB.day(S.curDate);
  const isToday = S.curDate === DB.today();
  const live = isToday && S.gps.st === 'running';
  const liveElapsed = live ? getElapsed() : 0;
  const finishedElapsed = (day.sessions || []).reduce((s,x)=>s+(x.elapsed||0),0);
  tempoAdjBaseSecs = finishedElapsed + liveElapsed;
  tempoAdjCurSecs = Math.max(0, tempoAdjBaseSecs + (day.tempoAdjust || 0));
  updateTempoAdjDisplay();
  openMo('mo-tempo-adjust');
}
// Aceita "2h30m", "2h 30m", "2h", "45m" ou número puro (minutos)
function parseTempoAdjInput(text) {
  text = (text || '').trim().toLowerCase().replace(/\s+/g, '');
  if (!text) return null;
  let h = 0, m = 0;
  const full = text.match(/^(\d+)h(\d+)?m?$/);
  const justH = text.match(/^(\d+)h$/);
  const justM = text.match(/^(\d+)m$/);
  const justNum = text.match(/^(\d+)$/);
  if (full) { h = parseInt(full[1]); m = parseInt(full[2] || 0); }
  else if (justH) { h = parseInt(justH[1]); }
  else if (justM) { m = parseInt(justM[1]); }
  else if (justNum) { m = parseInt(justNum[1]); }
  else return null;
  return Math.max(0, h*3600 + m*60);
}
function updateTempoAdjDisplay() {
  const inp = document.getElementById('tempoadj-val');
  if (inp) inp.value = fmtDur(tempoAdjCurSecs / 60);
}
function tempoAdjStep(deltaMin) {
  tempoAdjCurSecs = Math.max(0, tempoAdjCurSecs + deltaMin * 60);
  updateTempoAdjDisplay();
}
function tempoAdjInputCommit() {
  const inp = document.getElementById('tempoadj-val');
  if (!inp) return;
  const parsed = parseTempoAdjInput(inp.value);
  if (parsed === null) { updateTempoAdjDisplay(); return; }
  tempoAdjCurSecs = parsed;
  updateTempoAdjDisplay();
}
function saveTempoAdjust() {
  tempoAdjInputCommit();
  const day = DB.day(S.curDate);
  day.tempoAdjust = Math.round(tempoAdjCurSecs - tempoAdjBaseSecs);
  DB.saveDay(day);
  SYNC.push('day_meta', { date: S.curDate, tempo_adjust_secs: day.tempoAdjust });
  closeMo('mo-tempo-adjust');
  renderJornada();
  showToast('Tempo total ajustado.');
}

// ── IMPORT AI ─────────────────────────────────────────────────
// Detecta vídeo por MIME type, com fallback por extensão do nome do
// arquivo — em alguns celulares (Live Photo/Motion Photo, ou o picker de
// Arquivos) o navegador reporta file.type vazio ou incorreto, e o print
// (imagem) acabava sendo classificado errado e descartado.
function isVideoFile(f) {
  if (f.type) return f.type.startsWith('video/');
  return /\.(mp4|mov|webm|mkv|avi|m4v|3gp)$/i.test(f.name||'');
}
function handleImport(input, plat) {
  if (!input.files.length) return;
  const files = Array.from(input.files);
  closeMo('mo-import');
  const videos = files.filter(isVideoFile);
  const images = files.filter(f => !isVideoFile(f));
  if (!videos.length && !images.length) return;
  processImportBatch(images, videos, plat);
}
// Processa imagens E vídeos da mesma seleção juntos — antes, se um vídeo
// viesse junto (ex: Live Photo confundida com foto), TODAS as imagens da
// seleção eram silenciosamente descartadas (só processava videos[0]).
let importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };

function openImportPrecheck() {
  ['ipc-uber-count','ipc-uber-value','ipc-99-count','ipc-99-value'].forEach(id => {
    const el = document.getElementById(id); if (el) el.value = '';
  });
  openMo('mo-import-precheck');
}
function _readPrecheckField(id) {
  const v = document.getElementById(id)?.value;
  return (v === '' || v === undefined || v === null) ? null : parseFloat(String(v).replace(',','.'));
}
function importPrecheckNext() {
  importPrecheck = {
    uberCount: _readPrecheckField('ipc-uber-count'),
    uberValue: _readPrecheckField('ipc-uber-value'),
    p99Count:  _readPrecheckField('ipc-99-count'),
    p99Value:  _readPrecheckField('ipc-99-value')
  };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}
function importPrecheckSkip() {
  importPrecheck = { uberCount: null, uberValue: null, p99Count: null, p99Value: null };
  closeMo('mo-import-precheck');
  openMo('mo-import');
}

function renderImportRing(found, target, foundVal, targetVal, statusHtml, isWarn) {
  const hasTarget = target > 0;
  const pct = hasTarget ? Math.min(100, found/target*100) : 0;
  const deg = Math.round(pct*3.6);
  const ringColor = isWarn ? '#F59E0B' : '#22C55E';
  const ringBg = hasTarget ? `conic-gradient(${ringColor} 0deg ${deg}deg, #E2E8F0 ${deg}deg 360deg)` : '#E2E8F0';
  const countHtml = hasTarget ? `${found}<span style="font-size:16px;color:#94A3B8">/${target}</span>` : `${found}`;
  const valHtml = hasTarget ? `${DB.fRI(foundVal)} de ${DB.fRI(targetVal)} já identificados` : `${DB.fRI(foundVal)} identificados até agora`;
  return `<div style="display:flex;flex-direction:column;align-items:center;padding:30px 0 10px">
    <div style="width:130px;height:130px;border-radius:50%;background:${ringBg};display:flex;align-items:center;justify-content:center;margin-bottom:20px;position:relative;transition:background .4s">
      <div style="position:absolute;inset:8px;border-radius:50%;background:#fff"></div>
      <div style="position:relative;text-align:center">
        <div style="font-family:'Poppins',sans-serif;font-weight:900;font-size:28px;color:#0F172A">${countHtml}</div>
        <div style="font-size:10px;color:#64748B;font-weight:700;margin-top:2px">CORRIDAS</div>
      </div>
    </div>
    <div style="font-size:13.5px;font-weight:700;color:#0F172A;margin-bottom:5px;display:flex;align-items:center;gap:6px">${statusHtml}</div>
    <div style="font-size:12px;color:#64748B">${valHtml}</div>
  </div>`;
}

// Tenta de novo (até 3x) antes de desistir de um print/frame — evita que um
// erro passageiro de rede derrube a corrida inteira do import silenciosamente.
async function extractWithRetry(fn, onRetry, maxAttempts) {
  maxAttempts = maxAttempts || 3;
  let lastErr;
  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    try { return await fn(); }
    catch(e) {
      lastErr = e;
      if (attempt < maxAttempts) { onRetry(attempt+1, maxAttempts); await new Promise(r => setTimeout(r, 800)); }
    }
  }
  throw lastErr;
}

// Sobe o vídeo original pro Storage (bucket import-videos, retenção de
// 180 dias) e registra em import_videos — pedido do Yuri (16/08/2026):
// base pra confirmação de valor real (campeonatos/incentivos futuros) e
// pra dar dá pra conferir manualmente qualquer corrida em disputa. Falha
// de upload NUNCA trava o import (retorna null, segue sem vínculo) — o
// vídeo é auditoria extra, não pode ser bloqueio de fluxo.
async function uploadImportVideo(file, plat, operationalDate) {
  if (!_supaUser) return null;
  try {
    const ext = (file.name && file.name.includes('.')) ? file.name.split('.').pop().toLowerCase() : 'mp4';
    const opDate = operationalDate || S.curDate || DB.today();
    const opPlat = normalizeOperationalPlatform(plat);
    const opRange = operationalRangeForPlatform(opPlat, opDate);
    const path = `${_supaUser.id}/${opDate}/${opPlat}/${DB.uid()}.${ext}`;
    const { error: upErr } = await _SUPA.storage.from('import-videos').upload(path, file, {
      contentType: file.type || 'video/mp4'
    });
    if (upErr) { console.warn('upload vídeo falhou (segue sem vínculo):', upErr); return null; }
    const { data, error } = await _SUPA.from('import_videos').insert({
      user_id: _supaUser.id, storage_path: path, file_size_bytes: file.size || null,
      platform: opPlat, operational_date: opDate,
      period_start: new Date(opRange.startMs).toISOString(),
      period_end: new Date(opRange.endMs).toISOString()
    }).select('id').single();
    if (error) { console.warn('registro import_videos falhou (segue sem vínculo):', error); return null; }
    return data.id;
  } catch(e) { console.warn('uploadImportVideo erro:', e); return null; }
}
async function processImportBatch(images, videos, plat) {
  const operationalDate = S.curDate || DB.today();
  const operationalPlat = normalizeOperationalPlatform(plat);
  window._pendingImportOperationalDate = operationalDate;
  window._pendingImportPlatform = operationalPlat;
  const targetCount = (importPrecheck.uberCount||0) + (importPrecheck.p99Count||0);
  const targetValue = (importPrecheck.uberValue||0) + (importPrecheck.p99Value||0);
  const totalImgs = images.length, totalVids = videos.length;
  const label = [totalImgs?`${totalImgs} print${totalImgs>1?'s':''}`:'', totalVids?`${totalVids} vídeo${totalVids>1?'s':''}`:''].filter(Boolean).join(' + ');
  document.getElementById('ic-title').textContent = 'Analisando ' + label;
  document.getElementById('ic-btn').style.display = 'none';
  openMo('mo-import-confirm');

  const unique = [];
  const details = [];
  const seen = new Set();
  let foundCount = 0, foundVal = 0;
  let curDetail = null; // acumulador do fragmento de detalhe em aberto
  const flushDetail = () => { if (curDetail) { details.push(curDetail); curDetail = null; } };
  const mergeDetailFrag = (t) => {
    // Decide se é o MESMO cartão de detalhe (outro pedaço do mesmo scroll)
    // usando horário e endereço — nunca valor. Valor podia vir ligeiramente
    // diferente entre frames ("Você ganhou" vs "Total") e isso fazia o
    // mesmo cartão virar DOIS fragmentos, que depois viravam duas corridas
    // (uma real + uma fantasma) no import.
    const conflicts = (a, b) => a != null && b != null && a !== b;
    const sameRide = curDetail && (
      (t.time && curDetail.time) ? t.time === curDetail.time
        : (!conflicts(curDetail.originFull, t.originFull) && !conflicts(curDetail.destFull, t.destFull))
    );
    if (sameRide) {
      // mesmo cartão — preenche só o que esse fragmento ainda não tinha
      if (curDetail.km == null && t.km != null) curDetail.km = t.km;
      if (curDetail.duration == null && t.duration != null) curDetail.duration = t.duration;
      if (!curDetail.time && t.time) curDetail.time = t.time;
      if (!curDetail.originFull && t.originFull) { curDetail.originFull = t.originFull; curDetail.origin = t.origin; }
      if (!curDetail.destFull && t.destFull) { curDetail.destFull = t.destFull; curDetail.dest = t.dest; }
    } else {
      // horário ou endereço batendo diferente do acumulador — é outra
      // corrida: fecha o anterior 
===== sessions @ 842017 =====
mmary();
}

// Detecta corrida duplicada (hora+valor, hora+km, valor+rota)
// Versão do código — atualizar para verificar se cache está correto
const _APP_CODE_VER = "v24-trip99-fix";

// Normaliza texto de rota pra comparação: minúsculas, sem acentos, sem
// espaços extras — "Barra do Imbuí" e "barra do imbui" viram iguais.
// (OCR de frames diferentes do mesmo vídeo varia acentuação → sem isso,
// a mesma corrida escapa do detector de duplicata.)
function _normRoute(s) {
  return (s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')
    .replace(/\s+/g,' ').trim();
}
function isTripDuplicate(newT, existing) {
  return existing.some(t => {
    // Plataformas diferentes = corridas diferentes (nunca duplicata)
    const samePlat = (t.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const sameTime = t.time && newT.time && t.time === newT.time;
    const sameVal  = Math.abs((t.value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && t.km > 0 && Math.abs((t.km||0)-(newT.km||0)) < 0.5;
    // 99 não reporta km — usar origem+destino como sinal de rota nesse caso
    // (comparação normalizada: acento/caixa não separam corridas)
    const sameRoute = !!(t.origin && newT.origin && t.dest && newT.dest
                       && _normRoute(t.origin) === _normRoute(newT.origin)
                       && _normRoute(t.dest) === _normRoute(newT.dest));
    const nearTime = t.time && newT.time && (() => {
      const [h1,m1] = t.time.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    // Mesmo horário + mesmo valor = duplicata clara
    if (sameTime && sameVal) return true;
    // Mesmo horário EXATO + mesma rota = duplicata mesmo com valor diferente.
    // Protege contra fusão de frames no OCR de vídeo (rota de uma corrida +
    // valor do cartão vizinho). Duas corridas reais no MESMO minuto com a
    // MESMA origem e MESMO destino não existem na prática.
    if (sameTime && sameRoute) return true;
    // Mesmo horário + mesma km (proteção contra OCR com valor diferente)
    if (sameTime && sameKm && newT.km > 0) return true;
    // Próximo horário + mesmo valor + mesma km = duplicata provável
    if (sameVal && nearTime && sameKm) return true;
    // Próximo horário + mesmo valor + mesma rota (sem km, ex: 99) = duplicata provável
    if (sameVal && nearTime && sameRoute) return true;
    // REMOVIDO: sameVal && sameRoute sem tempo = falso positivo entre plataformas
    return false;
  });
}

// ── Casar corrida importada com corrida automática já capturada ────────────
// Pedido do Yuri (23/07/2026): ao importar, se a corrida já foi capturada
// automaticamente, ATUALIZA a automática com o valor real (confirmado pela
// tela de histórico da plataforma — mais confiável que a oferta estimada),
// em vez de criar uma corrida manual duplicada. Só corridas GENUINAMENTE
// novas (sem correspondência) seguem pro fluxo normal de criar.
// Mesmos critérios do isTripDuplicate acima, adaptados pros campos de
// auto_trips (trip_started_at em vez de time, offer_value em vez de value,
// real_km_trip em vez de km, origin_address/dest_address em vez de
// origin/dest) — time da automática convertido pro fuso local em HH:MM.
function _autoTripLocalTime(at) {
  const ts = at.trip_started_at || at.accepted_at;
  if (!ts) return null;
  const dt = new Date(ts);
  return String(dt.getHours()).padStart(2,'0')+':'+String(dt.getMinutes()).padStart(2,'0');
}
function matchAutoTrip(newT, autoTrips) {
  return autoTrips.find(at => {
    const samePlat = (at.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const atTime = _autoTripLocalTime(at);
    const sameTime = atTime && newT.time && atTime === newT.time;
    const sameVal  = Math.abs((at.offer_value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && at.real_km_trip > 0 && Math.abs((at.real_km_trip||0)-(newT.km||0)) < 0.5;
    const sameRoute = !!(at.origin_address && newT.origin && at.dest_address && newT.dest
                       && _normRoute(at.origin_address).includes(_normRoute(newT.origin))
                       && _normRoute(at.dest_address).includes(_normRoute(newT.dest)));
    const nearTime = atTime && newT.time && (() => {
      const [h1,m1] = atTime.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    if (sameTime && sameVal) return true;
    if (sameTime && sameRoute) return true;
    if (sameTime && sameKm && newT.km > 0) return true;
    if (sameVal && nearTime && sameKm) return true;
    if (sameVal && nearTime && sameRoute) return true;
    return false;
  }) || null;
}

// ── Reconciliação de sessões × corridas ───────────────────────────────────
// Regra híbrida: < 15 min → agrupa na sessão + ajusta horários
//                >= 15 min → cria nova sessão
function reconcileSessionsForDay(dateStr, dryRun = false) {
  const THRESHOLD = 15 * 60 * 1000; // 15 minutos em ms
  const day = DB.day(dateStr);
  
  const trips = (day.trips || [])
    .map(t => {
      let ts = t.ts || 0;
      if (!ts && t.time) ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
      return { ...t, _ts: ts };
    })
    .filter(t => t._ts > 0)
    .sort((a, b) => a._ts - b._ts);
  
  if (!trips.length) return { grouped: 0, created: 0, expanded: 0, sessions: day.sessions || [] };
  
  // Cópia profunda das sessões para não mutar durante dry run
  const sessions = JSON.parse(JSON.stringify(day.sessions || []));
  let grouped = 0, created = 0, expanded = 0;
  
  for (const trip of trips) {
    const ts  = trip._ts;
    const dur = (trip.duration || 15) * 60 * 1000;
    const end = ts + dur;
    
    // Já está dentro de uma sessão existente?
    const inside = sessions.find(s =>
      s.start && ts >= (s.start - 1000) && (!s.end || ts <= (s.end + 1000))
    );
    if (inside) continue;
    
    // Sessão mais próxima (distância ao início ou fim)
    let best = null, bestDist = Infinity;
    for (const s of sessions) {
      if (!s.start) continue;
      const d = Math.min(
        Math.abs(ts - s.start),
        s.end ? Math.abs(ts - s.end) : Math.abs(ts - s.start),
        s.end ? Math.abs(end - s.end) : Infinity
      );
      if (d < bestDist) { bestDist = d; best = s; }
    }
    
    if (best && bestDist < THRESHOLD) {
      // Agrupar + expandir horários da sessão
      if (ts < best.start) { best.start = ts; expanded++; }
      if (!best.end || end > best.end) { best.end = end; expanded++; }
      grouped++;
    } else {
      // Criar nova sessão para cobrir esta corrida
      sessions.push({
        id: DB.uid(),
        start: ts,
        end:   end,
        km:    parseFloat((trip.km || 0).toFixed(3)),
        elapsed: Math.round((trip.duration || 15) * 60)
      });
      sessions.sort((a, b) => (a.start || 0) - (b.start || 0));
      created++;
    }
  }
  
  if (!dryRun) {
    day.sessions = sessions;
    DB.saveDay(day);
    // Sincronizar novas/atualizadas sessões com Supabase
    if (_supaUser) {
      sessions.forEach(s => {
        _SUPA.from('sessions').upsert({
          id: s.id, user_id: _supaUser.id, date: dateStr,
          start_ms: s.start, end_ms: s.end,
          km: s.km || 0, elapsed_secs: s.elapsed || 0,
          paused_ms: 0, is_paused: false
        }, { onConflict: 'id' }).catch(() => {});
      });
    }
  }
  
  return { grouped, created, expanded, sessions };
}

// Rodar reconciliação em todos os dias com corridas (migração única)
async function reconcileAllExistingDays() {
  if (localStorage.getItem('sm_session_reconcile_v1')) return;
  
  const dates = DB.allDates();
  let totalGrouped = 0, totalCreated = 0;
  
  for (const dateStr of dates) {
    const day = DB.day(dateStr);
    if (!(day.trips || []).length) continue;
    const r = reconcileSessionsForDay(dateStr);
    totalGrouped += r.grouped;
    totalCreated += r.created;
  }
  
  localStorage.setItem('sm_session_reconcile_v1', '1');
  if (totalCreated > 0 || totalGrouped > 0) {
    showToast(
      `${totalCreated} sessão(ões) criada(s) e ${totalGrouped} corrida(s) agrupada(s) automaticamente.`,
      'info', 5000
    );
    setTimeout(() => { try { renderJornada(); } catch(e) {} }, 80);
  }
}

async function confirmImport() {
  const selected = (window._pendingTrips||[]).filter(t=>t._sel);
  if (!selected.length) { closeMo('mo-import-confirm'); return; }
  const day = DB.day(S.curDate);
  let added=0, skipped=0, updated=0;
  const toUpdateAuto = [];
  const toPushAuto = [];
  const videoId = (window._pendingImportVideoIds||[])[0] || null;
  selected.forEach(t => {
    // Já casada com uma auto_trip existente (matching já feito em
    // processImportBatch, antes da tela abrir) — só atualiza o valor,
    // nunca o endereço (esse já veio validado pela captura ao vivo/GPS).
    if (t._matchedAutoTripId) {
      toUpdateAuto.push({ id: t._matchedAutoTripId, value: parseFloat(t.value)||0, km: parseFloat(t.km)||0,
        dinheiro: !!t.dinheiro });
      updated++;
      return;
    }
    // Nova — nunca vista na captura ao vivo. Vira auto_trips já Confirmada
    // pelo vídeo; 'Importada' agora é somente a tag de origem — unifica com o resto dos
    // dados em vez de virar corrida "manual" separada. Endereço já
    // passou pela validação looksLikeAddress lá em processImportBatch
    // (se não passou, já veio vazio aqui — nunca inventa).
    let startMs;
    try {
      const opDate = window._pendingImportOperationalDate || S.curDate || DB.today();
      startMs = operationalTimestampForTime(t.platform || window._pendingImportPlatform, opDate, t.time||'00:00');
    } catch(e) { startMs = NaN; }
    if (!startMs || isNaN(startMs)) startMs = Date.now();
    const endMs = t.duration > 0 ? startMs + t.duration*60000 : null;
    toPushAuto.push({
      user_id: _supaUser.id,
      platform: t.platform === '99' ? '99' : 'uber',
      accepted_at: new Date(startMs).toISOString(),
      trip_started_at: new Date(startMs).toISOString(),
      trip_ended_at: endMs ? new Date(endMs).toISOString() : null,
      offer_value: parseFloat(t.value)||0,
      real_km_trip: parseFloat(t.km)||0,
      origin_address: t.originFull || null,
      dest_address: t.destFull || null,
      dinheiro: !!t.dinheiro,
      status: 'confirmada',
      capture_source: 'importada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId
    });
    added++;
  });
  // Aplica as atualizações nas automáticas casadas — valor real por cima do
  // estimado, km real quando a importação trouxe (senão mantém o que já
  // tinha), status vira confirmada (agora tem valor confirmado de verdade).
  toUpdateAuto.forEach(u => {
    _SUPA.from('auto_trips').update({
      offer_value: u.value,
      real_km_trip: u.km > 0 ? u.km : undefined,
      dinheiro: u.dinheiro,
      status: 'confirmada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId || undefined
    }).eq('id', u.id).then(() => {}).catch(() => {});
  });
  // Corridas novas — insere de uma vez em auto_trips.
  if (toPushAuto.length) {
    _SUPA.from('auto_trips').insert(toPushAuto).then(() => {}).catch((e) => console.warn('insert importada falhou', e));
  }
  // Atualiza a contagem de corridas confirmadas/criadas por esse vídeo —
  // só informativo, útil pra revisão manual depois.
  if (videoId) {
    _SUPA.from('import_videos').update({ trip_count: toUpdateAuto.length + toPushAuto.length }).eq('id', videoId).then(()=>{}).catch(()=>{});
  }
  window._pendingImportVideoIds = null;
  if (toUpdateAuto.length || toPushAuto.length) { S.autoTripsDate = null; } // força recarregar no próximo renderJornada
  DB.saveDay(day); closeMo('mo-import-confirm'); window._pendingTrips=null;

  const dateHint = S.curDate !== DB.today()
    ? ` (data: ${S.curDate.split('-').reverse().slice(0,2).join('/')})` : '';
  const parts = [];
  if (added > 0) parts.push(`${added} corrida${added!==1?'s':''} nova${added!==1?'s':''} importada${added!==1?'s':''}${dateHint}`);
  if (updated > 0) parts.push(`${updated} já capturada${updated!==1?'s':''} automaticamente, valor confirmado`);
  if (skipped > 0) parts.push(`${skipped} ignorada${skipped!==1?'s':''} (já existente${skipped!==1?'s':''})`);
  const baseMsg = parts.length ? parts.join('. ')+'.' : 'Nenhuma corrida nova.';

  try{renderJornada();}catch(e){}
  try{renderHome();}catch(e){}
  // Garantir que Corridas mostra a data certa independente do filtro
  if (S.curDate !== DB.today()) S.corridasPeriod = 'tudo';
  try{renderCorridasActive();}catch(e){}
  showToast(baseMsg, skipped > 0 ? 'info' : 'success');
}

// ══════════════════════════════════════════════
// IMPORTAÇÃO: histórico oficial do Uber (.zip)
// driver_lifetime_trips-0.csv já traz, por corrida, o valor líquido
// (driver_upfront_fare_local), km e duração — usamos direto daí, sem
// tentar casar com driver_payments por horário (testado com um export
// real: o casamento por horário troca valores entre corridas vizinhas).
// driver_payments-0.csv só é usado pra somar gorjeta/incentivo do
// período inteiro, como informação, sem tentar atribuir por corrida.
// ══════════════════════════════════════════════

function uzRound2(n) { return Math.round(n * 100) / 100; }

// Aceita "10.50" e "10,50" — exportações da Uber variam o separador por região/locale.
function uzNum(raw) {
  if (raw == null || raw === '') return 0;
  return parseFloat(String(raw).replace(',', '.')) || 0;
}

function uzParseTs(raw) {
  if (!raw) return null;
  // O timestamp do zip vem em UTC (tem 'Z' no final) — converte pra Brasília (UTC-3)
  const raw2 = String(raw).trim();
  try {
    const d = new Date(raw2.includes('T') ? raw2 : raw2.replace(' ', 'T') + 'Z');
    if (isNaN(d.getTime())) return null;
    // subtrai 3h pra Brasília
    const brt = new Date(d.getTime() - 3 * 60 * 60 * 1000);
    const pad = n => String(n).padStart(2, '0');
    const dateStr = brt.getUTCFullYear() + '-' + pad(brt.getUTCMonth()+1) + '-' + pad(brt.getUTCDate());
    const timeStr = pad(brt.getUTCHours()) + ':' + pad(brt.getUTCMinutes());
    return { dateStr, timeStr };
  } catch(e) { return null; }
}

// Soma gorjeta e incentivo/promoção do período inteiro (informativo, não por corrida).
function uzAggregatePayments(rows) {
  let tip = 0, promo = 0;
  for (const r of rows) {
    const amt = uzNum(r['Local Amount']);
    if (r['Category'] === 'tip') tip += amt;
    if (r['Category'] === 'existing_driver_incentive') promo += amt;
  }
  return { tip: uzRound2(tip), promo: uzRound2(promo) };
}

// Monta as corridas concluídas direto do driver_lifetime_trips. Corridas canceladas
// (status diferente de 'completed') são ignoradas — não são corridas de fato.
function uzBuildTrips(rows) {
  const trips = [];
  for (const r of rows) {
    if (r['status'] !== 'completed') continue;
===== sessions @ 842480 =====
normRoute(s) {
  return (s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')
    .replace(/\s+/g,' ').trim();
}
function isTripDuplicate(newT, existing) {
  return existing.some(t => {
    // Plataformas diferentes = corridas diferentes (nunca duplicata)
    const samePlat = (t.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const sameTime = t.time && newT.time && t.time === newT.time;
    const sameVal  = Math.abs((t.value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && t.km > 0 && Math.abs((t.km||0)-(newT.km||0)) < 0.5;
    // 99 não reporta km — usar origem+destino como sinal de rota nesse caso
    // (comparação normalizada: acento/caixa não separam corridas)
    const sameRoute = !!(t.origin && newT.origin && t.dest && newT.dest
                       && _normRoute(t.origin) === _normRoute(newT.origin)
                       && _normRoute(t.dest) === _normRoute(newT.dest));
    const nearTime = t.time && newT.time && (() => {
      const [h1,m1] = t.time.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    // Mesmo horário + mesmo valor = duplicata clara
    if (sameTime && sameVal) return true;
    // Mesmo horário EXATO + mesma rota = duplicata mesmo com valor diferente.
    // Protege contra fusão de frames no OCR de vídeo (rota de uma corrida +
    // valor do cartão vizinho). Duas corridas reais no MESMO minuto com a
    // MESMA origem e MESMO destino não existem na prática.
    if (sameTime && sameRoute) return true;
    // Mesmo horário + mesma km (proteção contra OCR com valor diferente)
    if (sameTime && sameKm && newT.km > 0) return true;
    // Próximo horário + mesmo valor + mesma km = duplicata provável
    if (sameVal && nearTime && sameKm) return true;
    // Próximo horário + mesmo valor + mesma rota (sem km, ex: 99) = duplicata provável
    if (sameVal && nearTime && sameRoute) return true;
    // REMOVIDO: sameVal && sameRoute sem tempo = falso positivo entre plataformas
    return false;
  });
}

// ── Casar corrida importada com corrida automática já capturada ────────────
// Pedido do Yuri (23/07/2026): ao importar, se a corrida já foi capturada
// automaticamente, ATUALIZA a automática com o valor real (confirmado pela
// tela de histórico da plataforma — mais confiável que a oferta estimada),
// em vez de criar uma corrida manual duplicada. Só corridas GENUINAMENTE
// novas (sem correspondência) seguem pro fluxo normal de criar.
// Mesmos critérios do isTripDuplicate acima, adaptados pros campos de
// auto_trips (trip_started_at em vez de time, offer_value em vez de value,
// real_km_trip em vez de km, origin_address/dest_address em vez de
// origin/dest) — time da automática convertido pro fuso local em HH:MM.
function _autoTripLocalTime(at) {
  const ts = at.trip_started_at || at.accepted_at;
  if (!ts) return null;
  const dt = new Date(ts);
  return String(dt.getHours()).padStart(2,'0')+':'+String(dt.getMinutes()).padStart(2,'0');
}
function matchAutoTrip(newT, autoTrips) {
  return autoTrips.find(at => {
    const samePlat = (at.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const atTime = _autoTripLocalTime(at);
    const sameTime = atTime && newT.time && atTime === newT.time;
    const sameVal  = Math.abs((at.offer_value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && at.real_km_trip > 0 && Math.abs((at.real_km_trip||0)-(newT.km||0)) < 0.5;
    const sameRoute = !!(at.origin_address && newT.origin && at.dest_address && newT.dest
                       && _normRoute(at.origin_address).includes(_normRoute(newT.origin))
                       && _normRoute(at.dest_address).includes(_normRoute(newT.dest)));
    const nearTime = atTime && newT.time && (() => {
      const [h1,m1] = atTime.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    if (sameTime && sameVal) return true;
    if (sameTime && sameRoute) return true;
    if (sameTime && sameKm && newT.km > 0) return true;
    if (sameVal && nearTime && sameKm) return true;
    if (sameVal && nearTime && sameRoute) return true;
    return false;
  }) || null;
}

// ── Reconciliação de sessões × corridas ───────────────────────────────────
// Regra híbrida: < 15 min → agrupa na sessão + ajusta horários
//                >= 15 min → cria nova sessão
function reconcileSessionsForDay(dateStr, dryRun = false) {
  const THRESHOLD = 15 * 60 * 1000; // 15 minutos em ms
  const day = DB.day(dateStr);
  
  const trips = (day.trips || [])
    .map(t => {
      let ts = t.ts || 0;
      if (!ts && t.time) ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
      return { ...t, _ts: ts };
    })
    .filter(t => t._ts > 0)
    .sort((a, b) => a._ts - b._ts);
  
  if (!trips.length) return { grouped: 0, created: 0, expanded: 0, sessions: day.sessions || [] };
  
  // Cópia profunda das sessões para não mutar durante dry run
  const sessions = JSON.parse(JSON.stringify(day.sessions || []));
  let grouped = 0, created = 0, expanded = 0;
  
  for (const trip of trips) {
    const ts  = trip._ts;
    const dur = (trip.duration || 15) * 60 * 1000;
    const end = ts + dur;
    
    // Já está dentro de uma sessão existente?
    const inside = sessions.find(s =>
      s.start && ts >= (s.start - 1000) && (!s.end || ts <= (s.end + 1000))
    );
    if (inside) continue;
    
    // Sessão mais próxima (distância ao início ou fim)
    let best = null, bestDist = Infinity;
    for (const s of sessions) {
      if (!s.start) continue;
      const d = Math.min(
        Math.abs(ts - s.start),
        s.end ? Math.abs(ts - s.end) : Math.abs(ts - s.start),
        s.end ? Math.abs(end - s.end) : Infinity
      );
      if (d < bestDist) { bestDist = d; best = s; }
    }
    
    if (best && bestDist < THRESHOLD) {
      // Agrupar + expandir horários da sessão
      if (ts < best.start) { best.start = ts; expanded++; }
      if (!best.end || end > best.end) { best.end = end; expanded++; }
      grouped++;
    } else {
      // Criar nova sessão para cobrir esta corrida
      sessions.push({
        id: DB.uid(),
        start: ts,
        end:   end,
        km:    parseFloat((trip.km || 0).toFixed(3)),
        elapsed: Math.round((trip.duration || 15) * 60)
      });
      sessions.sort((a, b) => (a.start || 0) - (b.start || 0));
      created++;
    }
  }
  
  if (!dryRun) {
    day.sessions = sessions;
    DB.saveDay(day);
    // Sincronizar novas/atualizadas sessões com Supabase
    if (_supaUser) {
      sessions.forEach(s => {
        _SUPA.from('sessions').upsert({
          id: s.id, user_id: _supaUser.id, date: dateStr,
          start_ms: s.start, end_ms: s.end,
          km: s.km || 0, elapsed_secs: s.elapsed || 0,
          paused_ms: 0, is_paused: false
        }, { onConflict: 'id' }).catch(() => {});
      });
    }
  }
  
  return { grouped, created, expanded, sessions };
}

// Rodar reconciliação em todos os dias com corridas (migração única)
async function reconcileAllExistingDays() {
  if (localStorage.getItem('sm_session_reconcile_v1')) return;
  
  const dates = DB.allDates();
  let totalGrouped = 0, totalCreated = 0;
  
  for (const dateStr of dates) {
    const day = DB.day(dateStr);
    if (!(day.trips || []).length) continue;
    const r = reconcileSessionsForDay(dateStr);
    totalGrouped += r.grouped;
    totalCreated += r.created;
  }
  
  localStorage.setItem('sm_session_reconcile_v1', '1');
  if (totalCreated > 0 || totalGrouped > 0) {
    showToast(
      `${totalCreated} sessão(ões) criada(s) e ${totalGrouped} corrida(s) agrupada(s) automaticamente.`,
      'info', 5000
    );
    setTimeout(() => { try { renderJornada(); } catch(e) {} }, 80);
  }
}

async function confirmImport() {
  const selected = (window._pendingTrips||[]).filter(t=>t._sel);
  if (!selected.length) { closeMo('mo-import-confirm'); return; }
  const day = DB.day(S.curDate);
  let added=0, skipped=0, updated=0;
  const toUpdateAuto = [];
  const toPushAuto = [];
  const videoId = (window._pendingImportVideoIds||[])[0] || null;
  selected.forEach(t => {
    // Já casada com uma auto_trip existente (matching já feito em
    // processImportBatch, antes da tela abrir) — só atualiza o valor,
    // nunca o endereço (esse já veio validado pela captura ao vivo/GPS).
    if (t._matchedAutoTripId) {
      toUpdateAuto.push({ id: t._matchedAutoTripId, value: parseFloat(t.value)||0, km: parseFloat(t.km)||0,
        dinheiro: !!t.dinheiro });
      updated++;
      return;
    }
    // Nova — nunca vista na captura ao vivo. Vira auto_trips já Confirmada
    // pelo vídeo; 'Importada' agora é somente a tag de origem — unifica com o resto dos
    // dados em vez de virar corrida "manual" separada. Endereço já
    // passou pela validação looksLikeAddress lá em processImportBatch
    // (se não passou, já veio vazio aqui — nunca inventa).
    let startMs;
    try {
      const opDate = window._pendingImportOperationalDate || S.curDate || DB.today();
      startMs = operationalTimestampForTime(t.platform || window._pendingImportPlatform, opDate, t.time||'00:00');
    } catch(e) { startMs = NaN; }
    if (!startMs || isNaN(startMs)) startMs = Date.now();
    const endMs = t.duration > 0 ? startMs + t.duration*60000 : null;
    toPushAuto.push({
      user_id: _supaUser.id,
      platform: t.platform === '99' ? '99' : 'uber',
      accepted_at: new Date(startMs).toISOString(),
      trip_started_at: new Date(startMs).toISOString(),
      trip_ended_at: endMs ? new Date(endMs).toISOString() : null,
      offer_value: parseFloat(t.value)||0,
      real_km_trip: parseFloat(t.km)||0,
      origin_address: t.originFull || null,
      dest_address: t.destFull || null,
      dinheiro: !!t.dinheiro,
      status: 'confirmada',
      capture_source: 'importada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId
    });
    added++;
  });
  // Aplica as atualizações nas automáticas casadas — valor real por cima do
  // estimado, km real quando a importação trouxe (senão mantém o que já
  // tinha), status vira confirmada (agora tem valor confirmado de verdade).
  toUpdateAuto.forEach(u => {
    _SUPA.from('auto_trips').update({
      offer_value: u.value,
      real_km_trip: u.km > 0 ? u.km : undefined,
      dinheiro: u.dinheiro,
      status: 'confirmada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId || undefined
    }).eq('id', u.id).then(() => {}).catch(() => {});
  });
  // Corridas novas — insere de uma vez em auto_trips.
  if (toPushAuto.length) {
    _SUPA.from('auto_trips').insert(toPushAuto).then(() => {}).catch((e) => console.warn('insert importada falhou', e));
  }
  // Atualiza a contagem de corridas confirmadas/criadas por esse vídeo —
  // só informativo, útil pra revisão manual depois.
  if (videoId) {
    _SUPA.from('import_videos').update({ trip_count: toUpdateAuto.length + toPushAuto.length }).eq('id', videoId).then(()=>{}).catch(()=>{});
  }
  window._pendingImportVideoIds = null;
  if (toUpdateAuto.length || toPushAuto.length) { S.autoTripsDate = null; } // força recarregar no próximo renderJornada
  DB.saveDay(day); closeMo('mo-import-confirm'); window._pendingTrips=null;

  const dateHint = S.curDate !== DB.today()
    ? ` (data: ${S.curDate.split('-').reverse().slice(0,2).join('/')})` : '';
  const parts = [];
  if (added > 0) parts.push(`${added} corrida${added!==1?'s':''} nova${added!==1?'s':''} importada${added!==1?'s':''}${dateHint}`);
  if (updated > 0) parts.push(`${updated} já capturada${updated!==1?'s':''} automaticamente, valor confirmado`);
  if (skipped > 0) parts.push(`${skipped} ignorada${skipped!==1?'s':''} (já existente${skipped!==1?'s':''})`);
  const baseMsg = parts.length ? parts.join('. ')+'.' : 'Nenhuma corrida nova.';

  try{renderJornada();}catch(e){}
  try{renderHome();}catch(e){}
  // Garantir que Corridas mostra a data certa independente do filtro
  if (S.curDate !== DB.today()) S.corridasPeriod = 'tudo';
  try{renderCorridasActive();}catch(e){}
  showToast(baseMsg, skipped > 0 ? 'info' : 'success');
}

// ══════════════════════════════════════════════
// IMPORTAÇÃO: histórico oficial do Uber (.zip)
// driver_lifetime_trips-0.csv já traz, por corrida, o valor líquido
// (driver_upfront_fare_local), km e duração — usamos direto daí, sem
// tentar casar com driver_payments por horário (testado com um export
// real: o casamento por horário troca valores entre corridas vizinhas).
// driver_payments-0.csv só é usado pra somar gorjeta/incentivo do
// período inteiro, como informação, sem tentar atribuir por corrida.
// ══════════════════════════════════════════════

function uzRound2(n) { return Math.round(n * 100) / 100; }

// Aceita "10.50" e "10,50" — exportações da Uber variam o separador por região/locale.
function uzNum(raw) {
  if (raw == null || raw === '') return 0;
  return parseFloat(String(raw).replace(',', '.')) || 0;
}

function uzParseTs(raw) {
  if (!raw) return null;
  // O timestamp do zip vem em UTC (tem 'Z' no final) — converte pra Brasília (UTC-3)
  const raw2 = String(raw).trim();
  try {
    const d = new Date(raw2.includes('T') ? raw2 : raw2.replace(' ', 'T') + 'Z');
    if (isNaN(d.getTime())) return null;
    // subtrai 3h pra Brasília
    const brt = new Date(d.getTime() - 3 * 60 * 60 * 1000);
    const pad = n => String(n).padStart(2, '0');
    const dateStr = brt.getUTCFullYear() + '-' + pad(brt.getUTCMonth()+1) + '-' + pad(brt.getUTCDate());
    const timeStr = pad(brt.getUTCHours()) + ':' + pad(brt.getUTCMinutes());
    return { dateStr, timeStr };
  } catch(e) { return null; }
}

// Soma gorjeta e incentivo/promoção do período inteiro (informativo, não por corrida).
function uzAggregatePayments(rows) {
  let tip = 0, promo = 0;
  for (const r of rows) {
    const amt = uzNum(r['Local Amount']);
    if (r['Category'] === 'tip') tip += amt;
    if (r['Category'] === 'existing_driver_incentive') promo += amt;
  }
  return { tip: uzRound2(tip), promo: uzRound2(promo) };
}

// Monta as corridas concluídas direto do driver_lifetime_trips. Corridas canceladas
// (status diferente de 'completed') são ignoradas — não são corridas de fato.
function uzBuildTrips(rows) {
  const trips = [];
  for (const r of rows) {
    if (r['status'] !== 'completed') continue;
    const begin = uzParseTs(r['begintrip_timestamp_local']) || uzParseTs(r['request_timestamp_local']);
    if (!begin) continue;
    const value = uzNum(r['driver_upfront_fare_local']) || uzNum(r['original_fare_local']);
    trips.push({
      date: begin.dateStr, time: begin.timeStr,
      value: uzRound2(Math.max(0, value)),
      km: r['trip_distance_miles'] ? uzRound2(uzNum(r['trip_distance_miles']) * 1.60934) : 0,
      // o app guarda duração em MINUT
===== sessions @ 842898 =====
& t.time === newT.time;
    const sameVal  = Math.abs((t.value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && t.km > 0 && Math.abs((t.km||0)-(newT.km||0)) < 0.5;
    // 99 não reporta km — usar origem+destino como sinal de rota nesse caso
    // (comparação normalizada: acento/caixa não separam corridas)
    const sameRoute = !!(t.origin && newT.origin && t.dest && newT.dest
                       && _normRoute(t.origin) === _normRoute(newT.origin)
                       && _normRoute(t.dest) === _normRoute(newT.dest));
    const nearTime = t.time && newT.time && (() => {
      const [h1,m1] = t.time.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    // Mesmo horário + mesmo valor = duplicata clara
    if (sameTime && sameVal) return true;
    // Mesmo horário EXATO + mesma rota = duplicata mesmo com valor diferente.
    // Protege contra fusão de frames no OCR de vídeo (rota de uma corrida +
    // valor do cartão vizinho). Duas corridas reais no MESMO minuto com a
    // MESMA origem e MESMO destino não existem na prática.
    if (sameTime && sameRoute) return true;
    // Mesmo horário + mesma km (proteção contra OCR com valor diferente)
    if (sameTime && sameKm && newT.km > 0) return true;
    // Próximo horário + mesmo valor + mesma km = duplicata provável
    if (sameVal && nearTime && sameKm) return true;
    // Próximo horário + mesmo valor + mesma rota (sem km, ex: 99) = duplicata provável
    if (sameVal && nearTime && sameRoute) return true;
    // REMOVIDO: sameVal && sameRoute sem tempo = falso positivo entre plataformas
    return false;
  });
}

// ── Casar corrida importada com corrida automática já capturada ────────────
// Pedido do Yuri (23/07/2026): ao importar, se a corrida já foi capturada
// automaticamente, ATUALIZA a automática com o valor real (confirmado pela
// tela de histórico da plataforma — mais confiável que a oferta estimada),
// em vez de criar uma corrida manual duplicada. Só corridas GENUINAMENTE
// novas (sem correspondência) seguem pro fluxo normal de criar.
// Mesmos critérios do isTripDuplicate acima, adaptados pros campos de
// auto_trips (trip_started_at em vez de time, offer_value em vez de value,
// real_km_trip em vez de km, origin_address/dest_address em vez de
// origin/dest) — time da automática convertido pro fuso local em HH:MM.
function _autoTripLocalTime(at) {
  const ts = at.trip_started_at || at.accepted_at;
  if (!ts) return null;
  const dt = new Date(ts);
  return String(dt.getHours()).padStart(2,'0')+':'+String(dt.getMinutes()).padStart(2,'0');
}
function matchAutoTrip(newT, autoTrips) {
  return autoTrips.find(at => {
    const samePlat = (at.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const atTime = _autoTripLocalTime(at);
    const sameTime = atTime && newT.time && atTime === newT.time;
    const sameVal  = Math.abs((at.offer_value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && at.real_km_trip > 0 && Math.abs((at.real_km_trip||0)-(newT.km||0)) < 0.5;
    const sameRoute = !!(at.origin_address && newT.origin && at.dest_address && newT.dest
                       && _normRoute(at.origin_address).includes(_normRoute(newT.origin))
                       && _normRoute(at.dest_address).includes(_normRoute(newT.dest)));
    const nearTime = atTime && newT.time && (() => {
      const [h1,m1] = atTime.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    if (sameTime && sameVal) return true;
    if (sameTime && sameRoute) return true;
    if (sameTime && sameKm && newT.km > 0) return true;
    if (sameVal && nearTime && sameKm) return true;
    if (sameVal && nearTime && sameRoute) return true;
    return false;
  }) || null;
}

// ── Reconciliação de sessões × corridas ───────────────────────────────────
// Regra híbrida: < 15 min → agrupa na sessão + ajusta horários
//                >= 15 min → cria nova sessão
function reconcileSessionsForDay(dateStr, dryRun = false) {
  const THRESHOLD = 15 * 60 * 1000; // 15 minutos em ms
  const day = DB.day(dateStr);
  
  const trips = (day.trips || [])
    .map(t => {
      let ts = t.ts || 0;
      if (!ts && t.time) ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
      return { ...t, _ts: ts };
    })
    .filter(t => t._ts > 0)
    .sort((a, b) => a._ts - b._ts);
  
  if (!trips.length) return { grouped: 0, created: 0, expanded: 0, sessions: day.sessions || [] };
  
  // Cópia profunda das sessões para não mutar durante dry run
  const sessions = JSON.parse(JSON.stringify(day.sessions || []));
  let grouped = 0, created = 0, expanded = 0;
  
  for (const trip of trips) {
    const ts  = trip._ts;
    const dur = (trip.duration || 15) * 60 * 1000;
    const end = ts + dur;
    
    // Já está dentro de uma sessão existente?
    const inside = sessions.find(s =>
      s.start && ts >= (s.start - 1000) && (!s.end || ts <= (s.end + 1000))
    );
    if (inside) continue;
    
    // Sessão mais próxima (distância ao início ou fim)
    let best = null, bestDist = Infinity;
    for (const s of sessions) {
      if (!s.start) continue;
      const d = Math.min(
        Math.abs(ts - s.start),
        s.end ? Math.abs(ts - s.end) : Math.abs(ts - s.start),
        s.end ? Math.abs(end - s.end) : Infinity
      );
      if (d < bestDist) { bestDist = d; best = s; }
    }
    
    if (best && bestDist < THRESHOLD) {
      // Agrupar + expandir horários da sessão
      if (ts < best.start) { best.start = ts; expanded++; }
      if (!best.end || end > best.end) { best.end = end; expanded++; }
      grouped++;
    } else {
      // Criar nova sessão para cobrir esta corrida
      sessions.push({
        id: DB.uid(),
        start: ts,
        end:   end,
        km:    parseFloat((trip.km || 0).toFixed(3)),
        elapsed: Math.round((trip.duration || 15) * 60)
      });
      sessions.sort((a, b) => (a.start || 0) - (b.start || 0));
      created++;
    }
  }
  
  if (!dryRun) {
    day.sessions = sessions;
    DB.saveDay(day);
    // Sincronizar novas/atualizadas sessões com Supabase
    if (_supaUser) {
      sessions.forEach(s => {
        _SUPA.from('sessions').upsert({
          id: s.id, user_id: _supaUser.id, date: dateStr,
          start_ms: s.start, end_ms: s.end,
          km: s.km || 0, elapsed_secs: s.elapsed || 0,
          paused_ms: 0, is_paused: false
        }, { onConflict: 'id' }).catch(() => {});
      });
    }
  }
  
  return { grouped, created, expanded, sessions };
}

// Rodar reconciliação em todos os dias com corridas (migração única)
async function reconcileAllExistingDays() {
  if (localStorage.getItem('sm_session_reconcile_v1')) return;
  
  const dates = DB.allDates();
  let totalGrouped = 0, totalCreated = 0;
  
  for (const dateStr of dates) {
    const day = DB.day(dateStr);
    if (!(day.trips || []).length) continue;
    const r = reconcileSessionsForDay(dateStr);
    totalGrouped += r.grouped;
    totalCreated += r.created;
  }
  
  localStorage.setItem('sm_session_reconcile_v1', '1');
  if (totalCreated > 0 || totalGrouped > 0) {
    showToast(
      `${totalCreated} sessão(ões) criada(s) e ${totalGrouped} corrida(s) agrupada(s) automaticamente.`,
      'info', 5000
    );
    setTimeout(() => { try { renderJornada(); } catch(e) {} }, 80);
  }
}

async function confirmImport() {
  const selected = (window._pendingTrips||[]).filter(t=>t._sel);
  if (!selected.length) { closeMo('mo-import-confirm'); return; }
  const day = DB.day(S.curDate);
  let added=0, skipped=0, updated=0;
  const toUpdateAuto = [];
  const toPushAuto = [];
  const videoId = (window._pendingImportVideoIds||[])[0] || null;
  selected.forEach(t => {
    // Já casada com uma auto_trip existente (matching já feito em
    // processImportBatch, antes da tela abrir) — só atualiza o valor,
    // nunca o endereço (esse já veio validado pela captura ao vivo/GPS).
    if (t._matchedAutoTripId) {
      toUpdateAuto.push({ id: t._matchedAutoTripId, value: parseFloat(t.value)||0, km: parseFloat(t.km)||0,
        dinheiro: !!t.dinheiro });
      updated++;
      return;
    }
    // Nova — nunca vista na captura ao vivo. Vira auto_trips já Confirmada
    // pelo vídeo; 'Importada' agora é somente a tag de origem — unifica com o resto dos
    // dados em vez de virar corrida "manual" separada. Endereço já
    // passou pela validação looksLikeAddress lá em processImportBatch
    // (se não passou, já veio vazio aqui — nunca inventa).
    let startMs;
    try {
      const opDate = window._pendingImportOperationalDate || S.curDate || DB.today();
      startMs = operationalTimestampForTime(t.platform || window._pendingImportPlatform, opDate, t.time||'00:00');
    } catch(e) { startMs = NaN; }
    if (!startMs || isNaN(startMs)) startMs = Date.now();
    const endMs = t.duration > 0 ? startMs + t.duration*60000 : null;
    toPushAuto.push({
      user_id: _supaUser.id,
      platform: t.platform === '99' ? '99' : 'uber',
      accepted_at: new Date(startMs).toISOString(),
      trip_started_at: new Date(startMs).toISOString(),
      trip_ended_at: endMs ? new Date(endMs).toISOString() : null,
      offer_value: parseFloat(t.value)||0,
      real_km_trip: parseFloat(t.km)||0,
      origin_address: t.originFull || null,
      dest_address: t.destFull || null,
      dinheiro: !!t.dinheiro,
      status: 'confirmada',
      capture_source: 'importada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId
    });
    added++;
  });
  // Aplica as atualizações nas automáticas casadas — valor real por cima do
  // estimado, km real quando a importação trouxe (senão mantém o que já
  // tinha), status vira confirmada (agora tem valor confirmado de verdade).
  toUpdateAuto.forEach(u => {
    _SUPA.from('auto_trips').update({
      offer_value: u.value,
      real_km_trip: u.km > 0 ? u.km : undefined,
      dinheiro: u.dinheiro,
      status: 'confirmada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId || undefined
    }).eq('id', u.id).then(() => {}).catch(() => {});
  });
  // Corridas novas — insere de uma vez em auto_trips.
  if (toPushAuto.length) {
    _SUPA.from('auto_trips').insert(toPushAuto).then(() => {}).catch((e) => console.warn('insert importada falhou', e));
  }
  // Atualiza a contagem de corridas confirmadas/criadas por esse vídeo —
  // só informativo, útil pra revisão manual depois.
  if (videoId) {
    _SUPA.from('import_videos').update({ trip_count: toUpdateAuto.length + toPushAuto.length }).eq('id', videoId).then(()=>{}).catch(()=>{});
  }
  window._pendingImportVideoIds = null;
  if (toUpdateAuto.length || toPushAuto.length) { S.autoTripsDate = null; } // força recarregar no próximo renderJornada
  DB.saveDay(day); closeMo('mo-import-confirm'); window._pendingTrips=null;

  const dateHint = S.curDate !== DB.today()
    ? ` (data: ${S.curDate.split('-').reverse().slice(0,2).join('/')})` : '';
  const parts = [];
  if (added > 0) parts.push(`${added} corrida${added!==1?'s':''} nova${added!==1?'s':''} importada${added!==1?'s':''}${dateHint}`);
  if (updated > 0) parts.push(`${updated} já capturada${updated!==1?'s':''} automaticamente, valor confirmado`);
  if (skipped > 0) parts.push(`${skipped} ignorada${skipped!==1?'s':''} (já existente${skipped!==1?'s':''})`);
  const baseMsg = parts.length ? parts.join('. ')+'.' : 'Nenhuma corrida nova.';

  try{renderJornada();}catch(e){}
  try{renderHome();}catch(e){}
  // Garantir que Corridas mostra a data certa independente do filtro
  if (S.curDate !== DB.today()) S.corridasPeriod = 'tudo';
  try{renderCorridasActive();}catch(e){}
  showToast(baseMsg, skipped > 0 ? 'info' : 'success');
}

// ══════════════════════════════════════════════
// IMPORTAÇÃO: histórico oficial do Uber (.zip)
// driver_lifetime_trips-0.csv já traz, por corrida, o valor líquido
// (driver_upfront_fare_local), km e duração — usamos direto daí, sem
// tentar casar com driver_payments por horário (testado com um export
// real: o casamento por horário troca valores entre corridas vizinhas).
// driver_payments-0.csv só é usado pra somar gorjeta/incentivo do
// período inteiro, como informação, sem tentar atribuir por corrida.
// ══════════════════════════════════════════════

function uzRound2(n) { return Math.round(n * 100) / 100; }

// Aceita "10.50" e "10,50" — exportações da Uber variam o separador por região/locale.
function uzNum(raw) {
  if (raw == null || raw === '') return 0;
  return parseFloat(String(raw).replace(',', '.')) || 0;
}

function uzParseTs(raw) {
  if (!raw) return null;
  // O timestamp do zip vem em UTC (tem 'Z' no final) — converte pra Brasília (UTC-3)
  const raw2 = String(raw).trim();
  try {
    const d = new Date(raw2.includes('T') ? raw2 : raw2.replace(' ', 'T') + 'Z');
    if (isNaN(d.getTime())) return null;
    // subtrai 3h pra Brasília
    const brt = new Date(d.getTime() - 3 * 60 * 60 * 1000);
    const pad = n => String(n).padStart(2, '0');
    const dateStr = brt.getUTCFullYear() + '-' + pad(brt.getUTCMonth()+1) + '-' + pad(brt.getUTCDate());
    const timeStr = pad(brt.getUTCHours()) + ':' + pad(brt.getUTCMinutes());
    return { dateStr, timeStr };
  } catch(e) { return null; }
}

// Soma gorjeta e incentivo/promoção do período inteiro (informativo, não por corrida).
function uzAggregatePayments(rows) {
  let tip = 0, promo = 0;
  for (const r of rows) {
    const amt = uzNum(r['Local Amount']);
    if (r['Category'] === 'tip') tip += amt;
    if (r['Category'] === 'existing_driver_incentive') promo += amt;
  }
  return { tip: uzRound2(tip), promo: uzRound2(promo) };
}

// Monta as corridas concluídas direto do driver_lifetime_trips. Corridas canceladas
// (status diferente de 'completed') são ignoradas — não são corridas de fato.
function uzBuildTrips(rows) {
  const trips = [];
  for (const r of rows) {
    if (r['status'] !== 'completed') continue;
    const begin = uzParseTs(r['begintrip_timestamp_local']) || uzParseTs(r['request_timestamp_local']);
    if (!begin) continue;
    const value = uzNum(r['driver_upfront_fare_local']) || uzNum(r['original_fare_local']);
    trips.push({
      date: begin.dateStr, time: begin.timeStr,
      value: uzRound2(Math.max(0, value)),
      km: r['trip_distance_miles'] ? uzRound2(uzNum(r['trip_distance_miles']) * 1.60934) : 0,
      // o app guarda duração em MINUTOS (a IA de import por foto também usa minutos);
      // o export da Uber vem em segundos, por isso a divisão por 60 aqui.
      duration: r['trip_duration_seconds'] ? Math.round(uzNum(r['trip_duration_seconds']) / 60) : 0,
      category: r['global_product_name'] || '',
      surge: r['is_surged'] === 'true',
      airport: r['is_airport_trip'] === 'true',
      // dinamico junta tarifa dinamica (surge) + promoca
===== sessions @ 843739 =====
ameVal) return true;
    // Mesmo horário EXATO + mesma rota = duplicata mesmo com valor diferente.
    // Protege contra fusão de frames no OCR de vídeo (rota de uma corrida +
    // valor do cartão vizinho). Duas corridas reais no MESMO minuto com a
    // MESMA origem e MESMO destino não existem na prática.
    if (sameTime && sameRoute) return true;
    // Mesmo horário + mesma km (proteção contra OCR com valor diferente)
    if (sameTime && sameKm && newT.km > 0) return true;
    // Próximo horário + mesmo valor + mesma km = duplicata provável
    if (sameVal && nearTime && sameKm) return true;
    // Próximo horário + mesmo valor + mesma rota (sem km, ex: 99) = duplicata provável
    if (sameVal && nearTime && sameRoute) return true;
    // REMOVIDO: sameVal && sameRoute sem tempo = falso positivo entre plataformas
    return false;
  });
}

// ── Casar corrida importada com corrida automática já capturada ────────────
// Pedido do Yuri (23/07/2026): ao importar, se a corrida já foi capturada
// automaticamente, ATUALIZA a automática com o valor real (confirmado pela
// tela de histórico da plataforma — mais confiável que a oferta estimada),
// em vez de criar uma corrida manual duplicada. Só corridas GENUINAMENTE
// novas (sem correspondência) seguem pro fluxo normal de criar.
// Mesmos critérios do isTripDuplicate acima, adaptados pros campos de
// auto_trips (trip_started_at em vez de time, offer_value em vez de value,
// real_km_trip em vez de km, origin_address/dest_address em vez de
// origin/dest) — time da automática convertido pro fuso local em HH:MM.
function _autoTripLocalTime(at) {
  const ts = at.trip_started_at || at.accepted_at;
  if (!ts) return null;
  const dt = new Date(ts);
  return String(dt.getHours()).padStart(2,'0')+':'+String(dt.getMinutes()).padStart(2,'0');
}
function matchAutoTrip(newT, autoTrips) {
  return autoTrips.find(at => {
    const samePlat = (at.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const atTime = _autoTripLocalTime(at);
    const sameTime = atTime && newT.time && atTime === newT.time;
    const sameVal  = Math.abs((at.offer_value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && at.real_km_trip > 0 && Math.abs((at.real_km_trip||0)-(newT.km||0)) < 0.5;
    const sameRoute = !!(at.origin_address && newT.origin && at.dest_address && newT.dest
                       && _normRoute(at.origin_address).includes(_normRoute(newT.origin))
                       && _normRoute(at.dest_address).includes(_normRoute(newT.dest)));
    const nearTime = atTime && newT.time && (() => {
      const [h1,m1] = atTime.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    if (sameTime && sameVal) return true;
    if (sameTime && sameRoute) return true;
    if (sameTime && sameKm && newT.km > 0) return true;
    if (sameVal && nearTime && sameKm) return true;
    if (sameVal && nearTime && sameRoute) return true;
    return false;
  }) || null;
}

// ── Reconciliação de sessões × corridas ───────────────────────────────────
// Regra híbrida: < 15 min → agrupa na sessão + ajusta horários
//                >= 15 min → cria nova sessão
function reconcileSessionsForDay(dateStr, dryRun = false) {
  const THRESHOLD = 15 * 60 * 1000; // 15 minutos em ms
  const day = DB.day(dateStr);
  
  const trips = (day.trips || [])
    .map(t => {
      let ts = t.ts || 0;
      if (!ts && t.time) ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
      return { ...t, _ts: ts };
    })
    .filter(t => t._ts > 0)
    .sort((a, b) => a._ts - b._ts);
  
  if (!trips.length) return { grouped: 0, created: 0, expanded: 0, sessions: day.sessions || [] };
  
  // Cópia profunda das sessões para não mutar durante dry run
  const sessions = JSON.parse(JSON.stringify(day.sessions || []));
  let grouped = 0, created = 0, expanded = 0;
  
  for (const trip of trips) {
    const ts  = trip._ts;
    const dur = (trip.duration || 15) * 60 * 1000;
    const end = ts + dur;
    
    // Já está dentro de uma sessão existente?
    const inside = sessions.find(s =>
      s.start && ts >= (s.start - 1000) && (!s.end || ts <= (s.end + 1000))
    );
    if (inside) continue;
    
    // Sessão mais próxima (distância ao início ou fim)
    let best = null, bestDist = Infinity;
    for (const s of sessions) {
      if (!s.start) continue;
      const d = Math.min(
        Math.abs(ts - s.start),
        s.end ? Math.abs(ts - s.end) : Math.abs(ts - s.start),
        s.end ? Math.abs(end - s.end) : Infinity
      );
      if (d < bestDist) { bestDist = d; best = s; }
    }
    
    if (best && bestDist < THRESHOLD) {
      // Agrupar + expandir horários da sessão
      if (ts < best.start) { best.start = ts; expanded++; }
      if (!best.end || end > best.end) { best.end = end; expanded++; }
      grouped++;
    } else {
      // Criar nova sessão para cobrir esta corrida
      sessions.push({
        id: DB.uid(),
        start: ts,
        end:   end,
        km:    parseFloat((trip.km || 0).toFixed(3)),
        elapsed: Math.round((trip.duration || 15) * 60)
      });
      sessions.sort((a, b) => (a.start || 0) - (b.start || 0));
      created++;
    }
  }
  
  if (!dryRun) {
    day.sessions = sessions;
    DB.saveDay(day);
    // Sincronizar novas/atualizadas sessões com Supabase
    if (_supaUser) {
      sessions.forEach(s => {
        _SUPA.from('sessions').upsert({
          id: s.id, user_id: _supaUser.id, date: dateStr,
          start_ms: s.start, end_ms: s.end,
          km: s.km || 0, elapsed_secs: s.elapsed || 0,
          paused_ms: 0, is_paused: false
        }, { onConflict: 'id' }).catch(() => {});
      });
    }
  }
  
  return { grouped, created, expanded, sessions };
}

// Rodar reconciliação em todos os dias com corridas (migração única)
async function reconcileAllExistingDays() {
  if (localStorage.getItem('sm_session_reconcile_v1')) return;
  
  const dates = DB.allDates();
  let totalGrouped = 0, totalCreated = 0;
  
  for (const dateStr of dates) {
    const day = DB.day(dateStr);
    if (!(day.trips || []).length) continue;
    const r = reconcileSessionsForDay(dateStr);
    totalGrouped += r.grouped;
    totalCreated += r.created;
  }
  
  localStorage.setItem('sm_session_reconcile_v1', '1');
  if (totalCreated > 0 || totalGrouped > 0) {
    showToast(
      `${totalCreated} sessão(ões) criada(s) e ${totalGrouped} corrida(s) agrupada(s) automaticamente.`,
      'info', 5000
    );
    setTimeout(() => { try { renderJornada(); } catch(e) {} }, 80);
  }
}

async function confirmImport() {
  const selected = (window._pendingTrips||[]).filter(t=>t._sel);
  if (!selected.length) { closeMo('mo-import-confirm'); return; }
  const day = DB.day(S.curDate);
  let added=0, skipped=0, updated=0;
  const toUpdateAuto = [];
  const toPushAuto = [];
  const videoId = (window._pendingImportVideoIds||[])[0] || null;
  selected.forEach(t => {
    // Já casada com uma auto_trip existente (matching já feito em
    // processImportBatch, antes da tela abrir) — só atualiza o valor,
    // nunca o endereço (esse já veio validado pela captura ao vivo/GPS).
    if (t._matchedAutoTripId) {
      toUpdateAuto.push({ id: t._matchedAutoTripId, value: parseFloat(t.value)||0, km: parseFloat(t.km)||0,
        dinheiro: !!t.dinheiro });
      updated++;
      return;
    }
    // Nova — nunca vista na captura ao vivo. Vira auto_trips já Confirmada
    // pelo vídeo; 'Importada' agora é somente a tag de origem — unifica com o resto dos
    // dados em vez de virar corrida "manual" separada. Endereço já
    // passou pela validação looksLikeAddress lá em processImportBatch
    // (se não passou, já veio vazio aqui — nunca inventa).
    let startMs;
    try {
      const opDate = window._pendingImportOperationalDate || S.curDate || DB.today();
      startMs = operationalTimestampForTime(t.platform || window._pendingImportPlatform, opDate, t.time||'00:00');
    } catch(e) { startMs = NaN; }
    if (!startMs || isNaN(startMs)) startMs = Date.now();
    const endMs = t.duration > 0 ? startMs + t.duration*60000 : null;
    toPushAuto.push({
      user_id: _supaUser.id,
      platform: t.platform === '99' ? '99' : 'uber',
      accepted_at: new Date(startMs).toISOString(),
      trip_started_at: new Date(startMs).toISOString(),
      trip_ended_at: endMs ? new Date(endMs).toISOString() : null,
      offer_value: parseFloat(t.value)||0,
      real_km_trip: parseFloat(t.km)||0,
      origin_address: t.originFull || null,
      dest_address: t.destFull || null,
      dinheiro: !!t.dinheiro,
      status: 'confirmada',
      capture_source: 'importada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId
    });
    added++;
  });
  // Aplica as atualizações nas automáticas casadas — valor real por cima do
  // estimado, km real quando a importação trouxe (senão mantém o que já
  // tinha), status vira confirmada (agora tem valor confirmado de verdade).
  toUpdateAuto.forEach(u => {
    _SUPA.from('auto_trips').update({
      offer_value: u.value,
      real_km_trip: u.km > 0 ? u.km : undefined,
      dinheiro: u.dinheiro,
      status: 'confirmada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId || undefined
    }).eq('id', u.id).then(() => {}).catch(() => {});
  });
  // Corridas novas — insere de uma vez em auto_trips.
  if (toPushAuto.length) {
    _SUPA.from('auto_trips').insert(toPushAuto).then(() => {}).catch((e) => console.warn('insert importada falhou', e));
  }
  // Atualiza a contagem de corridas confirmadas/criadas por esse vídeo —
  // só informativo, útil pra revisão manual depois.
  if (videoId) {
    _SUPA.from('import_videos').update({ trip_count: toUpdateAuto.length + toPushAuto.length }).eq('id', videoId).then(()=>{}).catch(()=>{});
  }
  window._pendingImportVideoIds = null;
  if (toUpdateAuto.length || toPushAuto.length) { S.autoTripsDate = null; } // força recarregar no próximo renderJornada
  DB.saveDay(day); closeMo('mo-import-confirm'); window._pendingTrips=null;

  const dateHint = S.curDate !== DB.today()
    ? ` (data: ${S.curDate.split('-').reverse().slice(0,2).join('/')})` : '';
  const parts = [];
  if (added > 0) parts.push(`${added} corrida${added!==1?'s':''} nova${added!==1?'s':''} importada${added!==1?'s':''}${dateHint}`);
  if (updated > 0) parts.push(`${updated} já capturada${updated!==1?'s':''} automaticamente, valor confirmado`);
  if (skipped > 0) parts.push(`${skipped} ignorada${skipped!==1?'s':''} (já existente${skipped!==1?'s':''})`);
  const baseMsg = parts.length ? parts.join('. ')+'.' : 'Nenhuma corrida nova.';

  try{renderJornada();}catch(e){}
  try{renderHome();}catch(e){}
  // Garantir que Corridas mostra a data certa independente do filtro
  if (S.curDate !== DB.today()) S.corridasPeriod = 'tudo';
  try{renderCorridasActive();}catch(e){}
  showToast(baseMsg, skipped > 0 ? 'info' : 'success');
}

// ══════════════════════════════════════════════
// IMPORTAÇÃO: histórico oficial do Uber (.zip)
// driver_lifetime_trips-0.csv já traz, por corrida, o valor líquido
// (driver_upfront_fare_local), km e duração — usamos direto daí, sem
// tentar casar com driver_payments por horário (testado com um export
// real: o casamento por horário troca valores entre corridas vizinhas).
// driver_payments-0.csv só é usado pra somar gorjeta/incentivo do
// período inteiro, como informação, sem tentar atribuir por corrida.
// ══════════════════════════════════════════════

function uzRound2(n) { return Math.round(n * 100) / 100; }

// Aceita "10.50" e "10,50" — exportações da Uber variam o separador por região/locale.
function uzNum(raw) {
  if (raw == null || raw === '') return 0;
  return parseFloat(String(raw).replace(',', '.')) || 0;
}

function uzParseTs(raw) {
  if (!raw) return null;
  // O timestamp do zip vem em UTC (tem 'Z' no final) — converte pra Brasília (UTC-3)
  const raw2 = String(raw).trim();
  try {
    const d = new Date(raw2.includes('T') ? raw2 : raw2.replace(' ', 'T') + 'Z');
    if (isNaN(d.getTime())) return null;
    // subtrai 3h pra Brasília
    const brt = new Date(d.getTime() - 3 * 60 * 60 * 1000);
    const pad = n => String(n).padStart(2, '0');
    const dateStr = brt.getUTCFullYear() + '-' + pad(brt.getUTCMonth()+1) + '-' + pad(brt.getUTCDate());
    const timeStr = pad(brt.getUTCHours()) + ':' + pad(brt.getUTCMinutes());
    return { dateStr, timeStr };
  } catch(e) { return null; }
}

// Soma gorjeta e incentivo/promoção do período inteiro (informativo, não por corrida).
function uzAggregatePayments(rows) {
  let tip = 0, promo = 0;
  for (const r of rows) {
    const amt = uzNum(r['Local Amount']);
    if (r['Category'] === 'tip') tip += amt;
    if (r['Category'] === 'existing_driver_incentive') promo += amt;
  }
  return { tip: uzRound2(tip), promo: uzRound2(promo) };
}

// Monta as corridas concluídas direto do driver_lifetime_trips. Corridas canceladas
// (status diferente de 'completed') são ignoradas — não são corridas de fato.
function uzBuildTrips(rows) {
  const trips = [];
  for (const r of rows) {
    if (r['status'] !== 'completed') continue;
    const begin = uzParseTs(r['begintrip_timestamp_local']) || uzParseTs(r['request_timestamp_local']);
    if (!begin) continue;
    const value = uzNum(r['driver_upfront_fare_local']) || uzNum(r['original_fare_local']);
    trips.push({
      date: begin.dateStr, time: begin.timeStr,
      value: uzRound2(Math.max(0, value)),
      km: r['trip_distance_miles'] ? uzRound2(uzNum(r['trip_distance_miles']) * 1.60934) : 0,
      // o app guarda duração em MINUTOS (a IA de import por foto também usa minutos);
      // o export da Uber vem em segundos, por isso a divisão por 60 aqui.
      duration: r['trip_duration_seconds'] ? Math.round(uzNum(r['trip_duration_seconds']) / 60) : 0,
      category: r['global_product_name'] || '',
      surge: r['is_surged'] === 'true',
      airport: r['is_airport_trip'] === 'true',
      // dinamico junta tarifa dinamica (surge) + promocao/desconto da corrida num
      // valor só — já incluso no "value", mostrado separado só pra detalhar a corrida.
      // Pode ficar negativo se o desconto for maior que o surge.
      dinamico:    uzRound2(Math.max(0, uzNum(r['surge_fare_local'])) + uzNum(r['promotion_local'])),
      dinheiro:   r['is_cash_trip'] === 'true',
      pedagio:    uzNum(r['toll_amount_local']) > 0,
      serviceFee: (r['service_fee'] !== undefined && r['service_fee'] !== '') ? parseFloat(r['service_fee']) : null,
      taxaZero:   r['service_fee'] === '0' || r['service_fee'] === '0.0',
    });
  }
  return trips;
}

function uzFindEntry(zip, suffix) {
  const f = Object.values(zip.files).find(x => !x.dir && x.name.endsWith(suffix));
  if (!f) throw new Error('Não encontrei "' + suffix + '" no zip. Confira se é o arquivo certo da Uber.');
  return f
===== sessions @ 844226 =====
   // Próximo horário + mesmo valor + mesma km = duplicata provável
    if (sameVal && nearTime && sameKm) return true;
    // Próximo horário + mesmo valor + mesma rota (sem km, ex: 99) = duplicata provável
    if (sameVal && nearTime && sameRoute) return true;
    // REMOVIDO: sameVal && sameRoute sem tempo = falso positivo entre plataformas
    return false;
  });
}

// ── Casar corrida importada com corrida automática já capturada ────────────
// Pedido do Yuri (23/07/2026): ao importar, se a corrida já foi capturada
// automaticamente, ATUALIZA a automática com o valor real (confirmado pela
// tela de histórico da plataforma — mais confiável que a oferta estimada),
// em vez de criar uma corrida manual duplicada. Só corridas GENUINAMENTE
// novas (sem correspondência) seguem pro fluxo normal de criar.
// Mesmos critérios do isTripDuplicate acima, adaptados pros campos de
// auto_trips (trip_started_at em vez de time, offer_value em vez de value,
// real_km_trip em vez de km, origin_address/dest_address em vez de
// origin/dest) — time da automática convertido pro fuso local em HH:MM.
function _autoTripLocalTime(at) {
  const ts = at.trip_started_at || at.accepted_at;
  if (!ts) return null;
  const dt = new Date(ts);
  return String(dt.getHours()).padStart(2,'0')+':'+String(dt.getMinutes()).padStart(2,'0');
}
function matchAutoTrip(newT, autoTrips) {
  return autoTrips.find(at => {
    const samePlat = (at.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const atTime = _autoTripLocalTime(at);
    const sameTime = atTime && newT.time && atTime === newT.time;
    const sameVal  = Math.abs((at.offer_value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && at.real_km_trip > 0 && Math.abs((at.real_km_trip||0)-(newT.km||0)) < 0.5;
    const sameRoute = !!(at.origin_address && newT.origin && at.dest_address && newT.dest
                       && _normRoute(at.origin_address).includes(_normRoute(newT.origin))
                       && _normRoute(at.dest_address).includes(_normRoute(newT.dest)));
    const nearTime = atTime && newT.time && (() => {
      const [h1,m1] = atTime.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    if (sameTime && sameVal) return true;
    if (sameTime && sameRoute) return true;
    if (sameTime && sameKm && newT.km > 0) return true;
    if (sameVal && nearTime && sameKm) return true;
    if (sameVal && nearTime && sameRoute) return true;
    return false;
  }) || null;
}

// ── Reconciliação de sessões × corridas ───────────────────────────────────
// Regra híbrida: < 15 min → agrupa na sessão + ajusta horários
//                >= 15 min → cria nova sessão
function reconcileSessionsForDay(dateStr, dryRun = false) {
  const THRESHOLD = 15 * 60 * 1000; // 15 minutos em ms
  const day = DB.day(dateStr);
  
  const trips = (day.trips || [])
    .map(t => {
      let ts = t.ts || 0;
      if (!ts && t.time) ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
      return { ...t, _ts: ts };
    })
    .filter(t => t._ts > 0)
    .sort((a, b) => a._ts - b._ts);
  
  if (!trips.length) return { grouped: 0, created: 0, expanded: 0, sessions: day.sessions || [] };
  
  // Cópia profunda das sessões para não mutar durante dry run
  const sessions = JSON.parse(JSON.stringify(day.sessions || []));
  let grouped = 0, created = 0, expanded = 0;
  
  for (const trip of trips) {
    const ts  = trip._ts;
    const dur = (trip.duration || 15) * 60 * 1000;
    const end = ts + dur;
    
    // Já está dentro de uma sessão existente?
    const inside = sessions.find(s =>
      s.start && ts >= (s.start - 1000) && (!s.end || ts <= (s.end + 1000))
    );
    if (inside) continue;
    
    // Sessão mais próxima (distância ao início ou fim)
    let best = null, bestDist = Infinity;
    for (const s of sessions) {
      if (!s.start) continue;
      const d = Math.min(
        Math.abs(ts - s.start),
        s.end ? Math.abs(ts - s.end) : Math.abs(ts - s.start),
        s.end ? Math.abs(end - s.end) : Infinity
      );
      if (d < bestDist) { bestDist = d; best = s; }
    }
    
    if (best && bestDist < THRESHOLD) {
      // Agrupar + expandir horários da sessão
      if (ts < best.start) { best.start = ts; expanded++; }
      if (!best.end || end > best.end) { best.end = end; expanded++; }
      grouped++;
    } else {
      // Criar nova sessão para cobrir esta corrida
      sessions.push({
        id: DB.uid(),
        start: ts,
        end:   end,
        km:    parseFloat((trip.km || 0).toFixed(3)),
        elapsed: Math.round((trip.duration || 15) * 60)
      });
      sessions.sort((a, b) => (a.start || 0) - (b.start || 0));
      created++;
    }
  }
  
  if (!dryRun) {
    day.sessions = sessions;
    DB.saveDay(day);
    // Sincronizar novas/atualizadas sessões com Supabase
    if (_supaUser) {
      sessions.forEach(s => {
        _SUPA.from('sessions').upsert({
          id: s.id, user_id: _supaUser.id, date: dateStr,
          start_ms: s.start, end_ms: s.end,
          km: s.km || 0, elapsed_secs: s.elapsed || 0,
          paused_ms: 0, is_paused: false
        }, { onConflict: 'id' }).catch(() => {});
      });
    }
  }
  
  return { grouped, created, expanded, sessions };
}

// Rodar reconciliação em todos os dias com corridas (migração única)
async function reconcileAllExistingDays() {
  if (localStorage.getItem('sm_session_reconcile_v1')) return;
  
  const dates = DB.allDates();
  let totalGrouped = 0, totalCreated = 0;
  
  for (const dateStr of dates) {
    const day = DB.day(dateStr);
    if (!(day.trips || []).length) continue;
    const r = reconcileSessionsForDay(dateStr);
    totalGrouped += r.grouped;
    totalCreated += r.created;
  }
  
  localStorage.setItem('sm_session_reconcile_v1', '1');
  if (totalCreated > 0 || totalGrouped > 0) {
    showToast(
      `${totalCreated} sessão(ões) criada(s) e ${totalGrouped} corrida(s) agrupada(s) automaticamente.`,
      'info', 5000
    );
    setTimeout(() => { try { renderJornada(); } catch(e) {} }, 80);
  }
}

async function confirmImport() {
  const selected = (window._pendingTrips||[]).filter(t=>t._sel);
  if (!selected.length) { closeMo('mo-import-confirm'); return; }
  const day = DB.day(S.curDate);
  let added=0, skipped=0, updated=0;
  const toUpdateAuto = [];
  const toPushAuto = [];
  const videoId = (window._pendingImportVideoIds||[])[0] || null;
  selected.forEach(t => {
    // Já casada com uma auto_trip existente (matching já feito em
    // processImportBatch, antes da tela abrir) — só atualiza o valor,
    // nunca o endereço (esse já veio validado pela captura ao vivo/GPS).
    if (t._matchedAutoTripId) {
      toUpdateAuto.push({ id: t._matchedAutoTripId, value: parseFloat(t.value)||0, km: parseFloat(t.km)||0,
        dinheiro: !!t.dinheiro });
      updated++;
      return;
    }
    // Nova — nunca vista na captura ao vivo. Vira auto_trips já Confirmada
    // pelo vídeo; 'Importada' agora é somente a tag de origem — unifica com o resto dos
    // dados em vez de virar corrida "manual" separada. Endereço já
    // passou pela validação looksLikeAddress lá em processImportBatch
    // (se não passou, já veio vazio aqui — nunca inventa).
    let startMs;
    try {
      const opDate = window._pendingImportOperationalDate || S.curDate || DB.today();
      startMs = operationalTimestampForTime(t.platform || window._pendingImportPlatform, opDate, t.time||'00:00');
    } catch(e) { startMs = NaN; }
    if (!startMs || isNaN(startMs)) startMs = Date.now();
    const endMs = t.duration > 0 ? startMs + t.duration*60000 : null;
    toPushAuto.push({
      user_id: _supaUser.id,
      platform: t.platform === '99' ? '99' : 'uber',
      accepted_at: new Date(startMs).toISOString(),
      trip_started_at: new Date(startMs).toISOString(),
      trip_ended_at: endMs ? new Date(endMs).toISOString() : null,
      offer_value: parseFloat(t.value)||0,
      real_km_trip: parseFloat(t.km)||0,
      origin_address: t.originFull || null,
      dest_address: t.destFull || null,
      dinheiro: !!t.dinheiro,
      status: 'confirmada',
      capture_source: 'importada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId
    });
    added++;
  });
  // Aplica as atualizações nas automáticas casadas — valor real por cima do
  // estimado, km real quando a importação trouxe (senão mantém o que já
  // tinha), status vira confirmada (agora tem valor confirmado de verdade).
  toUpdateAuto.forEach(u => {
    _SUPA.from('auto_trips').update({
      offer_value: u.value,
      real_km_trip: u.km > 0 ? u.km : undefined,
      dinheiro: u.dinheiro,
      status: 'confirmada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId || undefined
    }).eq('id', u.id).then(() => {}).catch(() => {});
  });
  // Corridas novas — insere de uma vez em auto_trips.
  if (toPushAuto.length) {
    _SUPA.from('auto_trips').insert(toPushAuto).then(() => {}).catch((e) => console.warn('insert importada falhou', e));
  }
  // Atualiza a contagem de corridas confirmadas/criadas por esse vídeo —
  // só informativo, útil pra revisão manual depois.
  if (videoId) {
    _SUPA.from('import_videos').update({ trip_count: toUpdateAuto.length + toPushAuto.length }).eq('id', videoId).then(()=>{}).catch(()=>{});
  }
  window._pendingImportVideoIds = null;
  if (toUpdateAuto.length || toPushAuto.length) { S.autoTripsDate = null; } // força recarregar no próximo renderJornada
  DB.saveDay(day); closeMo('mo-import-confirm'); window._pendingTrips=null;

  const dateHint = S.curDate !== DB.today()
    ? ` (data: ${S.curDate.split('-').reverse().slice(0,2).join('/')})` : '';
  const parts = [];
  if (added > 0) parts.push(`${added} corrida${added!==1?'s':''} nova${added!==1?'s':''} importada${added!==1?'s':''}${dateHint}`);
  if (updated > 0) parts.push(`${updated} já capturada${updated!==1?'s':''} automaticamente, valor confirmado`);
  if (skipped > 0) parts.push(`${skipped} ignorada${skipped!==1?'s':''} (já existente${skipped!==1?'s':''})`);
  const baseMsg = parts.length ? parts.join('. ')+'.' : 'Nenhuma corrida nova.';

  try{renderJornada();}catch(e){}
  try{renderHome();}catch(e){}
  // Garantir que Corridas mostra a data certa independente do filtro
  if (S.curDate !== DB.today()) S.corridasPeriod = 'tudo';
  try{renderCorridasActive();}catch(e){}
  showToast(baseMsg, skipped > 0 ? 'info' : 'success');
}

// ══════════════════════════════════════════════
// IMPORTAÇÃO: histórico oficial do Uber (.zip)
// driver_lifetime_trips-0.csv já traz, por corrida, o valor líquido
// (driver_upfront_fare_local), km e duração — usamos direto daí, sem
// tentar casar com driver_payments por horário (testado com um export
// real: o casamento por horário troca valores entre corridas vizinhas).
// driver_payments-0.csv só é usado pra somar gorjeta/incentivo do
// período inteiro, como informação, sem tentar atribuir por corrida.
// ══════════════════════════════════════════════

function uzRound2(n) { return Math.round(n * 100) / 100; }

// Aceita "10.50" e "10,50" — exportações da Uber variam o separador por região/locale.
function uzNum(raw) {
  if (raw == null || raw === '') return 0;
  return parseFloat(String(raw).replace(',', '.')) || 0;
}

function uzParseTs(raw) {
  if (!raw) return null;
  // O timestamp do zip vem em UTC (tem 'Z' no final) — converte pra Brasília (UTC-3)
  const raw2 = String(raw).trim();
  try {
    const d = new Date(raw2.includes('T') ? raw2 : raw2.replace(' ', 'T') + 'Z');
    if (isNaN(d.getTime())) return null;
    // subtrai 3h pra Brasília
    const brt = new Date(d.getTime() - 3 * 60 * 60 * 1000);
    const pad = n => String(n).padStart(2, '0');
    const dateStr = brt.getUTCFullYear() + '-' + pad(brt.getUTCMonth()+1) + '-' + pad(brt.getUTCDate());
    const timeStr = pad(brt.getUTCHours()) + ':' + pad(brt.getUTCMinutes());
    return { dateStr, timeStr };
  } catch(e) { return null; }
}

// Soma gorjeta e incentivo/promoção do período inteiro (informativo, não por corrida).
function uzAggregatePayments(rows) {
  let tip = 0, promo = 0;
  for (const r of rows) {
    const amt = uzNum(r['Local Amount']);
    if (r['Category'] === 'tip') tip += amt;
    if (r['Category'] === 'existing_driver_incentive') promo += amt;
  }
  return { tip: uzRound2(tip), promo: uzRound2(promo) };
}

// Monta as corridas concluídas direto do driver_lifetime_trips. Corridas canceladas
// (status diferente de 'completed') são ignoradas — não são corridas de fato.
function uzBuildTrips(rows) {
  const trips = [];
  for (const r of rows) {
    if (r['status'] !== 'completed') continue;
    const begin = uzParseTs(r['begintrip_timestamp_local']) || uzParseTs(r['request_timestamp_local']);
    if (!begin) continue;
    const value = uzNum(r['driver_upfront_fare_local']) || uzNum(r['original_fare_local']);
    trips.push({
      date: begin.dateStr, time: begin.timeStr,
      value: uzRound2(Math.max(0, value)),
      km: r['trip_distance_miles'] ? uzRound2(uzNum(r['trip_distance_miles']) * 1.60934) : 0,
      // o app guarda duração em MINUTOS (a IA de import por foto também usa minutos);
      // o export da Uber vem em segundos, por isso a divisão por 60 aqui.
      duration: r['trip_duration_seconds'] ? Math.round(uzNum(r['trip_duration_seconds']) / 60) : 0,
      category: r['global_product_name'] || '',
      surge: r['is_surged'] === 'true',
      airport: r['is_airport_trip'] === 'true',
      // dinamico junta tarifa dinamica (surge) + promocao/desconto da corrida num
      // valor só — já incluso no "value", mostrado separado só pra detalhar a corrida.
      // Pode ficar negativo se o desconto for maior que o surge.
      dinamico:    uzRound2(Math.max(0, uzNum(r['surge_fare_local'])) + uzNum(r['promotion_local'])),
      dinheiro:   r['is_cash_trip'] === 'true',
      pedagio:    uzNum(r['toll_amount_local']) > 0,
      serviceFee: (r['service_fee'] !== undefined && r['service_fee'] !== '') ? parseFloat(r['service_fee']) : null,
      taxaZero:   r['service_fee'] === '0' || r['service_fee'] === '0.0',
    });
  }
  return trips;
}

function uzFindEntry(zip, suffix) {
  const f = Object.values(zip.files).find(x => !x.dir && x.name.endsWith(suffix));
  if (!f) throw new Error('Não encontrei "' + suffix + '" no zip. Confira se é o arquivo certo da Uber.');
  return f;
}

let _uzResult = null;

async function handleUberZipFile(input) {
  const file = input.files[0]; if (!file) return;
  input.value = '';
  openMo('mo-uberzip');
  document.getElementById('uz-proc').style.display = 'block';
  document.getElementById('uz-res').style.display = 'none';
  const stepEl = document.getElementById('uz-proc-step');
  const subEl  = document.getElementById('uz-proc-sub');
  const barEl  = document.getElementById('uz-proc-bar');
  const setStep = (msg, pct) 
===== sessions @ 844961 =====
idas GENUINAMENTE
// novas (sem correspondência) seguem pro fluxo normal de criar.
// Mesmos critérios do isTripDuplicate acima, adaptados pros campos de
// auto_trips (trip_started_at em vez de time, offer_value em vez de value,
// real_km_trip em vez de km, origin_address/dest_address em vez de
// origin/dest) — time da automática convertido pro fuso local em HH:MM.
function _autoTripLocalTime(at) {
  const ts = at.trip_started_at || at.accepted_at;
  if (!ts) return null;
  const dt = new Date(ts);
  return String(dt.getHours()).padStart(2,'0')+':'+String(dt.getMinutes()).padStart(2,'0');
}
function matchAutoTrip(newT, autoTrips) {
  return autoTrips.find(at => {
    const samePlat = (at.platform||'uber') === (newT.platform||'uber');
    if (!samePlat) return false;
    const atTime = _autoTripLocalTime(at);
    const sameTime = atTime && newT.time && atTime === newT.time;
    const sameVal  = Math.abs((at.offer_value||0)-(newT.value||0)) < 0.02;
    const sameKm   = newT.km > 0 && at.real_km_trip > 0 && Math.abs((at.real_km_trip||0)-(newT.km||0)) < 0.5;
    const sameRoute = !!(at.origin_address && newT.origin && at.dest_address && newT.dest
                       && _normRoute(at.origin_address).includes(_normRoute(newT.origin))
                       && _normRoute(at.dest_address).includes(_normRoute(newT.dest)));
    const nearTime = atTime && newT.time && (() => {
      const [h1,m1] = atTime.split(':').map(Number);
      const [h2,m2] = newT.time.split(':').map(Number);
      return Math.abs((h1*60+m1)-(h2*60+m2)) <= 5;
    })();
    if (sameTime && sameVal) return true;
    if (sameTime && sameRoute) return true;
    if (sameTime && sameKm && newT.km > 0) return true;
    if (sameVal && nearTime && sameKm) return true;
    if (sameVal && nearTime && sameRoute) return true;
    return false;
  }) || null;
}

// ── Reconciliação de sessões × corridas ───────────────────────────────────
// Regra híbrida: < 15 min → agrupa na sessão + ajusta horários
//                >= 15 min → cria nova sessão
function reconcileSessionsForDay(dateStr, dryRun = false) {
  const THRESHOLD = 15 * 60 * 1000; // 15 minutos em ms
  const day = DB.day(dateStr);
  
  const trips = (day.trips || [])
    .map(t => {
      let ts = t.ts || 0;
      if (!ts && t.time) ts = new Date(dateStr + 'T' + t.time + ':00').getTime();
      return { ...t, _ts: ts };
    })
    .filter(t => t._ts > 0)
    .sort((a, b) => a._ts - b._ts);
  
  if (!trips.length) return { grouped: 0, created: 0, expanded: 0, sessions: day.sessions || [] };
  
  // Cópia profunda das sessões para não mutar durante dry run
  const sessions = JSON.parse(JSON.stringify(day.sessions || []));
  let grouped = 0, created = 0, expanded = 0;
  
  for (const trip of trips) {
    const ts  = trip._ts;
    const dur = (trip.duration || 15) * 60 * 1000;
    const end = ts + dur;
    
    // Já está dentro de uma sessão existente?
    const inside = sessions.find(s =>
      s.start && ts >= (s.start - 1000) && (!s.end || ts <= (s.end + 1000))
    );
    if (inside) continue;
    
    // Sessão mais próxima (distância ao início ou fim)
    let best = null, bestDist = Infinity;
    for (const s of sessions) {
      if (!s.start) continue;
      const d = Math.min(
        Math.abs(ts - s.start),
        s.end ? Math.abs(ts - s.end) : Math.abs(ts - s.start),
        s.end ? Math.abs(end - s.end) : Infinity
      );
      if (d < bestDist) { bestDist = d; best = s; }
    }
    
    if (best && bestDist < THRESHOLD) {
      // Agrupar + expandir horários da sessão
      if (ts < best.start) { best.start = ts; expanded++; }
      if (!best.end || end > best.end) { best.end = end; expanded++; }
      grouped++;
    } else {
      // Criar nova sessão para cobrir esta corrida
      sessions.push({
        id: DB.uid(),
        start: ts,
        end:   end,
        km:    parseFloat((trip.km || 0).toFixed(3)),
        elapsed: Math.round((trip.duration || 15) * 60)
      });
      sessions.sort((a, b) => (a.start || 0) - (b.start || 0));
      created++;
    }
  }
  
  if (!dryRun) {
    day.sessions = sessions;
    DB.saveDay(day);
    // Sincronizar novas/atualizadas sessões com Supabase
    if (_supaUser) {
      sessions.forEach(s => {
        _SUPA.from('sessions').upsert({
          id: s.id, user_id: _supaUser.id, date: dateStr,
          start_ms: s.start, end_ms: s.end,
          km: s.km || 0, elapsed_secs: s.elapsed || 0,
          paused_ms: 0, is_paused: false
        }, { onConflict: 'id' }).catch(() => {});
      });
    }
  }
  
  return { grouped, created, expanded, sessions };
}

// Rodar reconciliação em todos os dias com corridas (migração única)
async function reconcileAllExistingDays() {
  if (localStorage.getItem('sm_session_reconcile_v1')) return;
  
  const dates = DB.allDates();
  let totalGrouped = 0, totalCreated = 0;
  
  for (const dateStr of dates) {
    const day = DB.day(dateStr);
    if (!(day.trips || []).length) continue;
    const r = reconcileSessionsForDay(dateStr);
    totalGrouped += r.grouped;
    totalCreated += r.created;
  }
  
  localStorage.setItem('sm_session_reconcile_v1', '1');
  if (totalCreated > 0 || totalGrouped > 0) {
    showToast(
      `${totalCreated} sessão(ões) criada(s) e ${totalGrouped} corrida(s) agrupada(s) automaticamente.`,
      'info', 5000
    );
    setTimeout(() => { try { renderJornada(); } catch(e) {} }, 80);
  }
}

async function confirmImport() {
  const selected = (window._pendingTrips||[]).filter(t=>t._sel);
  if (!selected.length) { closeMo('mo-import-confirm'); return; }
  const day = DB.day(S.curDate);
  let added=0, skipped=0, updated=0;
  const toUpdateAuto = [];
  const toPushAuto = [];
  const videoId = (window._pendingImportVideoIds||[])[0] || null;
  selected.forEach(t => {
    // Já casada com uma auto_trip existente (matching já feito em
    // processImportBatch, antes da tela abrir) — só atualiza o valor,
    // nunca o endereço (esse já veio validado pela captura ao vivo/GPS).
    if (t._matchedAutoTripId) {
      toUpdateAuto.push({ id: t._matchedAutoTripId, value: parseFloat(t.value)||0, km: parseFloat(t.km)||0,
        dinheiro: !!t.dinheiro });
      updated++;
      return;
    }
    // Nova — nunca vista na captura ao vivo. Vira auto_trips já Confirmada
    // pelo vídeo; 'Importada' agora é somente a tag de origem — unifica com o resto dos
    // dados em vez de virar corrida "manual" separada. Endereço já
    // passou pela validação looksLikeAddress lá em processImportBatch
    // (se não passou, já veio vazio aqui — nunca inventa).
    let startMs;
    try {
      const opDate = window._pendingImportOperationalDate || S.curDate || DB.today();
      startMs = operationalTimestampForTime(t.platform || window._pendingImportPlatform, opDate, t.time||'00:00');
    } catch(e) { startMs = NaN; }
    if (!startMs || isNaN(startMs)) startMs = Date.now();
    const endMs = t.duration > 0 ? startMs + t.duration*60000 : null;
    toPushAuto.push({
      user_id: _supaUser.id,
      platform: t.platform === '99' ? '99' : 'uber',
      accepted_at: new Date(startMs).toISOString(),
      trip_started_at: new Date(startMs).toISOString(),
      trip_ended_at: endMs ? new Date(endMs).toISOString() : null,
      offer_value: parseFloat(t.value)||0,
      real_km_trip: parseFloat(t.km)||0,
      origin_address: t.originFull || null,
      dest_address: t.destFull || null,
      dinheiro: !!t.dinheiro,
      status: 'confirmada',
      capture_source: 'importada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId
    });
    added++;
  });
  // Aplica as atualizações nas automáticas casadas — valor real por cima do
  // estimado, km real quando a importação trouxe (senão mantém o que já
  // tinha), status vira confirmada (agora tem valor confirmado de verdade).
  toUpdateAuto.forEach(u => {
    _SUPA.from('auto_trips').update({
      offer_value: u.value,
      real_km_trip: u.km > 0 ? u.km : undefined,
      dinheiro: u.dinheiro,
      status: 'confirmada',
      value_needs_review: false,
      data_quality_flag: null,
      import_video_id: videoId || undefined
    }).eq('id', u.id).then(() => {}).catch(() => {});
  });
  // Corridas novas — insere de uma vez em auto_trips.
  if (toPushAuto.length) {
    _SUPA.from('auto_trips').insert(toPushAuto).then(() => {}).catch((e) => console.warn('insert importada falhou', e));
  }
  // Atualiza a contagem de corridas confirmadas/criadas por esse vídeo —
  // só informativo, útil pra revisão manual depois.
  if (videoId) {
    _SUPA.from('import_videos').update({ trip_count: toUpdateAuto.length + toPushAuto.length }).eq('id', videoId).then(()=>{}).catch(()=>{});
  }
  window._pendingImportVideoIds = null;
  if (toUpdateAuto.length || toPushAuto.length) { S.autoTripsDate = null; } // força recarregar no próximo renderJornada
  DB.saveDay(day); closeMo('mo-import-confirm'); window._pendingTrips=null;

  const dateHint = S.curDate !== DB.today()
    ? ` (data: ${S.curDate.split('-').reverse().slice(0,2).join('/')})` : '';
  const parts = [];
  if (added > 0) parts.push(`${added} corrida${added!==1?'s':''} nova${added!==1?'s':''} importada${added!==1?'s':''}${dateHint}`);
  if (updated > 0) parts.push(`${updated} já capturada${updated!==1?'s':''} automaticamente, valor confirmado`);
  if (skipped > 0) parts.push(`${skipped} ignorada${skipped!==1?'s':''} (já existente${skipped!==1?'s':''})`);
  const baseMsg = parts.length ? parts.join('. ')+'.' : 'Nenhuma corrida nova.';

  try{renderJornada();}catch(e){}
  try{renderHome();}catch(e){}
  // Garantir que Corridas mostra a data certa independente do filtro
  if (S.curDate !== DB.today()) S.corridasPeriod = 'tudo';
  try{renderCorridasActive();}catch(e){}
  showToast(baseMsg, skipped > 0 ? 'info' : 'success');
}

// ══════════════════════════════════════════════
// IMPORTAÇÃO: histórico oficial do Uber (.zip)
// driver_lifetime_trips-0.csv já traz, por corrida, o valor líquido
// (driver_upfront_fare_local), km e duração — usamos direto daí, sem
// tentar casar com driver_payments por horário (testado com um export
// real: o casamento por horário troca valores entre corridas vizinhas).
// driver_payments-0.csv só é usado pra somar gorjeta/incentivo do
// período inteiro, como informação, sem tentar atribuir por corrida.
// ══════════════════════════════════════════════

function uzRound2(n) { return Math.round(n * 100) / 100; }

// Aceita "10.50" e "10,50" — exportações da Uber variam o separador por região/locale.
function uzNum(raw) {
  if (raw == null || raw === '') return 0;
  return parseFloat(String(raw).replace(',', '.')) || 0;
}

function uzParseTs(raw) {
  if (!raw) return null;
  // O timestamp do zip vem em UTC (tem 'Z' no final) — converte pra Brasília (UTC-3)
  const raw2 = String(raw).trim();
  try {
    const d = new Date(raw2.includes('T') ? raw2 : raw2.replace(' ', 'T') + 'Z');
    if (isNaN(d.getTime())) return null;
    // subtrai 3h pra Brasília
    const brt = new Date(d.getTime() - 3 * 60 * 60 * 1000);
    const pad = n => String(n).padStart(2, '0');
    const dateStr = brt.getUTCFullYear() + '-' + pad(brt.getUTCMonth()+1) + '-' + pad(brt.getUTCDate());
    const timeStr = pad(brt.getUTCHours()) + ':' + pad(brt.getUTCMinutes());
    return { dateStr, timeStr };
  } catch(e) { return null; }
}

// Soma gorjeta e incentivo/promoção do período inteiro (informativo, não por corrida).
function uzAggregatePayments(rows) {
  let tip = 0, promo = 0;
  for (const r of rows) {
    const amt = uzNum(r['Local Amount']);
    if (r['Category'] === 'tip') tip += amt;
    if (r['Category'] === 'existing_driver_incentive') promo += amt;
  }
  return { tip: uzRound2(tip), promo: uzRound2(promo) };
}

// Monta as corridas concluídas direto do driver_lifetime_trips. Corridas canceladas
// (status diferente de 'completed') são ignoradas — não são corridas de fato.
function uzBuildTrips(rows) {
  const trips = [];
  for (const r of rows) {
    if (r['status'] !== 'completed') continue;
    const begin = uzParseTs(r['begintrip_timestamp_local']) || uzParseTs(r['request_timestamp_local']);
    if (!begin) continue;
    const value = uzNum(r['driver_upfront_fare_local']) || uzNum(r['original_fare_local']);
    trips.push({
      date: begin.dateStr, time: begin.timeStr,
      value: uzRound2(Math.max(0, value)),
      km: r['trip_distance_miles'] ? uzRound2(uzNum(r['trip_distance_miles']) * 1.60934) : 0,
      // o app guarda duração em MINUTOS (a IA de import por foto também usa minutos);
      // o export da Uber vem em segundos, por isso a divisão por 60 aqui.
      duration: r['trip_duration_seconds'] ? Math.round(uzNum(r['trip_duration_seconds']) / 60) : 0,
      category: r['global_product_name'] || '',
      surge: r['is_surged'] === 'true',
      airport: r['is_airport_trip'] === 'true',
      // dinamico junta tarifa dinamica (surge) + promocao/desconto da corrida num
      // valor só — já incluso no "value", mostrado separado só pra detalhar a corrida.
      // Pode ficar negativo se o desconto for maior que o surge.
      dinamico:    uzRound2(Math.max(0, uzNum(r['surge_fare_local'])) + uzNum(r['promotion_local'])),
      dinheiro:   r['is_cash_trip'] === 'true',
      pedagio:    uzNum(r['toll_amount_local']) > 0,
      serviceFee: (r['service_fee'] !== undefined && r['service_fee'] !== '') ? parseFloat(r['service_fee']) : null,
      taxaZero:   r['service_fee'] === '0' || r['service_fee'] === '0.0',
    });
  }
  return trips;
}

function uzFindEntry(zip, suffix) {
  const f = Object.values(zip.files).find(x => !x.dir && x.name.endsWith(suffix));
  if (!f) throw new Error('Não encontrei "' + suffix + '" no zip. Confira se é o arquivo certo da Uber.');
  return f;
}

let _uzResult = null;

async function handleUberZipFile(input) {
  const file = input.files[0]; if (!file) return;
  input.value = '';
  openMo('mo-uberzip');
  document.getElementById('uz-proc').style.display = 'block';
  document.getElementById('uz-res').style.display = 'none';
  const stepEl = document.getElementById('uz-proc-step');
  const subEl  = document.getElementById('uz-proc-sub');
  const barEl  = document.getElementById('uz-proc-bar');
  const setStep = (msg, pct) => { if (stepEl) stepEl.textContent = msg; if (barEl) barEl.style.width = pct + '%'; };

  try {
    setStep('Lendo arquivo .zip...', 10);
    const zip = await JSZip.loadAsync(file);
    const paymentsEntry = uzFindEntry(zip, 'driver_payments-0.csv');
    const tripsEntry     = uzFindEntry(zip, 'driver_lifetime_trips-0.csv');

    setStep('Lendo corridas...', 35);
    const [paymentsCsv, tripsCsv] = await Promise.all([
      paymentsEntry.async('string'), tripsEntry.async('string')
    ]);
    const paymentsRows = Papa.parse(paymentsCsv, { header: true, skipEmptyLines: true }).data;
    const tripsRows     = Papa.parse(tripsCsv, { header: true, skipEmptyLines: true }).data;

    setStep('Organizando corridas concluídas...', 
===== sessions @ 873427 =====
onsole.warn('[_pullTripsCorrections]', e); }
}
async function _syncTick() {
  // ORDEM CRÍTICA: pull primeiro, push depois. Se o push rodasse antes,
  // ele reenviaria o valor local (potencialmente errado/desatualizado)
  // pro banco ANTES do celular ter chance de buscar uma correção feita
  // direto no banco — criando um ciclo onde a correção nunca 'gruda'.
  await _pullTripsCorrections();
  await _autoSyncLocalData();
  _backfillKmEstimates();
}
// Migração única: limpa prefixo "Bairro da/do" de corridas já salvas localmente
(function _migrateBairroNames(){
  try {
    if (localStorage.getItem('sm_bairro_clean_v1')) return;
    DB.allDates().forEach(dateStr => {
      const day = DB.day(dateStr); let ch = false;
      (day.trips||[]).forEach(t => {
        const o = _cleanBairro(t.origin||''), d = _cleanBairro(t.dest||'');
        if (o !== (t.origin||'')) { t.origin = o; ch = true; }
        if (d !== (t.dest||''))   { t.dest = d;  ch = true; }
      });
      if (ch) DB.saveDay(day);
    });
    localStorage.setItem('sm_bairro_clean_v1','1');
  } catch(e) { console.warn('[_migrateBairroNames]', e); }
})();
setTimeout(()=>{ _syncTick(); setInterval(_syncTick, 60*1000); }, 3000); // primeira vez em 3s, depois a cada 1 minuto

// Corridas automáticas novas capturadas durante o dia (pedido do Yuri,
// 23/07/2026) — INTERVALO PRÓPRIO, separado do _syncTick de propósito: se
// qualquer etapa do _syncTick (correção de trips, auto-sync, backfill de
// km) travar ou lançar erro, essa atualização não pode ficar refém disso —
// foi exatamente isso que aconteceu (corridas paravam de aparecer na
// Jornada ativa depois de um tempo, travadas no que existia no momento do
// primeiro carregamento do dia). 25s pra sentir a corrida nova rápido sem
// martelar o Supabase.
async function _refreshAutoTripsIfOnJornada() {
  if (!(S && S.sec === 'jornada' && S.curDate)) return;
  try {
    const list = await loadAutoTripsForDay(S.curDate);
    if (!Array.isArray(list)) return; // falha não vira "zero corridas"
    S.autoTrips = list; S.autoTripsDate = S.curDate; S._autoTripsDataDate = S.curDate;
    renderJornada();
  } catch(e) {}
}
setTimeout(()=>{ _refreshAutoTripsIfOnJornada(); setInterval(_refreshAutoTripsIfOnJornada, 25*1000); }, 4000);

// CORRIGIDO (24/07/2026, confirmado pelo Yuri: corridas terminadas até
// 22:21 não apareciam numa tela aberta desde antes) — a tela Corridas
// nunca tinha refresh automático como a Jornada já tem. Se ficasse parado
// nela sem trocar filtro/período nem sair e voltar, ficava presa no que
// carregou da última vez, mesmo com corridas novas terminando por trás.
// Mesmo padrão de intervalo da Jornada.
async function _refreshCorridasIfOnScreen() {
  if (!(S && S.sec === 'corridas')) return;
  try { await renderCorridasActive(); } catch(e) {}
}
setTimeout(()=>{ _refreshCorridasIfOnScreen(); setInterval(_refreshCorridasIfOnScreen, 25*1000); }, 4500);

// Mesma lógica pra Home (pedido do Yuri, 23/07/2026) — a checagem de cache
// em renderHomeGraphs() só refaz a busca quando o INTERVALO DE DATAS muda
// (troca de semana/mês/etc), nunca por tempo — uma corrida nova no meio do
// dia nunca apareceria sozinha sem isso. 60s (não precisa ser tão rápido
// quanto a Jornada, que é tempo real; Home é histórico/agregado).
async function _refreshAutoTripsIfOnHome() {
  if (!(S && S.sec === 'relatorios')) return;
  S.autoTripsHomeRange = null; // força o próximo renderHomeGraphs a rebuscar
  try { renderHomeGraphs(); } catch(e) {}
}
setTimeout(()=>{ setInterval(_refreshAutoTripsIfOnHome, 60*1000); }, 5000);

// ── Status de rede: indicador visual + reconciliação ao voltar online ──
let _netSyncing = false;
function updateNetStatus() {
  const badge = document.getElementById("net-status-badge");
  const dot = document.getElementById("net-status-dot");
  const txt = document.getElementById("net-status-text");
  if (!badge) return;
  if (!navigator.onLine) {
    badge.style.display = "flex";
    badge.style.background = "#FEF2F2";
    dot.style.background = "#EF4444";
    txt.style.color = "#B91C1C";
    txt.textContent = "Offline — dados salvos localmente";
  } else if (_netSyncing) {
    badge.style.display = "flex";
    badge.style.background = "#FFF7ED";
    dot.style.background = "#F59E0B";
    txt.style.color = "#C2410C";
    txt.textContent = "Sincronizando...";
  } else {
    badge.style.display = "none"; // tudo ok, não precisa mostrar nada
  }
}
window.addEventListener("offline", updateNetStatus);
window.addEventListener("online", async () => {
  _netSyncing = true; updateNetStatus();
  try { await SYNC.flushQueue(); await reconcileJornadaHoje(); } catch(e) {}
  _netSyncing = false; updateNetStatus();
  try { renderJornada(); } catch(e) {}
});

// ── Reconciliação: Supabase é a fonte de verdade, local é o cache ──────
async function reconcileJornadaHoje() {
  if (!_supaUser || !navigator.onLine) return;
  const today = DB.today();
  try {
    const {data: remote, error} = await _SUPA.from("sessions").select("*")
      .eq("user_id", _supaUser.id).eq("date", today);
    if (error || !remote) return;
    const day = DB.day(today);
    if (!day.sessions) day.sessions = [];
    let changed = false;
    // 1. Toda sessão remota vira a verdade local (upsert por id)
    remote.forEach(r => {
      if (!r.end_ms) return; // sessão ainda ativa remotamente — não mexe (pode ser a atual)
      const local = { id: r.id, start: r.start_ms, end: r.end_ms, km: parseFloat(r.km)||0, elapsed: r.elapsed_secs||0 };
      const idx = day.sessions.findIndex(s => s.id === r.id);
      if (idx === -1) { day.sessions.push(local); changed = true; }
      else if (day.sessions[idx].end !== local.end || day.sessions[idx].km !== local.km) {
        day.sessions[idx] = local; changed = true;
      }
    });
    // 2. Sessão local encerrada que não existe no remoto — reenviar (não apagar)
    const remoteIds = new Set(remote.map(r => r.id));
    for (const s of day.sessions) {
      if (!remoteIds.has(s.id) && s.end) {
        await _SUPA.from("sessions").upsert({
          id: s.id, user_id: _supaUser.id, date: today,
          start_ms: s.start, end_ms: s.end, km: s.km, elapsed_secs: s.elapsed,
          paused_ms: 0, is_paused: false
        }, {onConflict:"id"}).catch(()=>{});
      }
    }
    // 3. Sessão ativa: se remoto diz que já encerrou mas o app local acha que continua
    if (S.gps.st !== "idle" && S.gps.sessionId) {
      const remoteActive = remote.find(r => r.id === S.gps.sessionId);
      if (remoteActive && remoteActive.end_ms) {
        S.gps.st = "idle"; S.gps.start = null;
        clearInterval(S.gps.ti);
        changed = true;
      }
    }
    if (changed) DB.saveDay(day);
  } catch(e) { console.warn("[reconcileJornadaHoje]", e); }
}

// ── Estimativa de km por endereço (Nominatim geocode + OSRM route) ────
// Usado quando a plataforma não informa km (ex: 99 Motorista). Gratuito,
// mas com throttle de 1 req/s no Nominatim — processar em fila sequencial.
let _geoQueueBusy = false;
const _geoQueue = [];
// Remove prefixo "Bairro da/do/de/das/dos" do nome de bairro (a 99 escreve
// "Bairro da São Pedro" — queremos exibir/geocodificar só "São Pedro")
function _cleanBairro(name) {
  if (!name) return name;
  return name.replace(/^\s*bairro\s+(d[aeo]s?\s+)?/i, '').trim();
}
// Limpa lixo comum do OCR no endereço antes de geocodificar: vírgulas soltas,
// país truncado ("Bra"/"Bras"), número de prédio grudado ("1211- Rua X"),
// e o prefixo "Bairro da" que confunde o Nominatim.
function _cleanGeoAddr(addr) {
  if (!addr) return addr;
  let a = addr.trim();
  a = a.replace(/,\s*(BR|Brasil|Bras?i?l?|Bra)\.?\s*$/i, '');
  a = a.replace(/[,\s]+$/, '');
  a = a.replace(/^\d+\s*-\s*/, '');
  a = a.replace(/\bbairro\s+(d[aeo]s?\s+)/gi, '');
  return a.replace(/\s{2,}/g, ' ').trim();
}
// Cache persistente de geocodificação: os mesmos bairros/endereços repetem
// muito no dia a dia — resolver 1x e reaproveitar corta o tempo da fila de
// ~5s/corrida pra quase zero nas repetidas (e economiza chamadas ao Nominatim).
const _geoCache = (() => {
  try { return JSON.parse(localStorage.getItem('sm_geo_cache_v1') || '{}'); }
  catch(e) { return {}; }
})();
function _saveGeoCache() {
  try {
    // limita a 400 entradas pra não inchar o localStorage
    const keys = Object.keys(_geoCache);
    if (keys.length > 400) keys.slice(0, keys.length - 400).forEach(k => delete _geoCache[k]);
    localStorage.setItem('sm_geo_cache_v1', JSON.stringify(_geoCache));
  } catch(e) {}
}
// Throttle global do Nominatim (1 req/s) — embutido aqui pra que cache hits
// não paguem espera nenhuma.
let _lastNominatimMs = 0;
async function _nominatimThrottle() {
  const wait = 1100 - (Date.now() - _lastNominatimMs);
  if (wait > 0) await new Promise(r => setTimeout(r, wait));
  _lastNominatimMs = Date.now();
}
async function _geocodeAddress(query) {
  if (!query) return null;
  const key = query.toLowerCase().trim();
  if (_geoCache[key]) return _geoCache[key];
  try {
    await _nominatimThrottle();
    const url = "https://nominatim.openstreetmap.org/search?format=json&limit=1&countrycodes=br&q=" + encodeURIComponent(query);
    const r = await fetch(url, { headers: { "Accept-Language": "pt-BR" } });
    const data = await r.json();
    if (!data || !data.length) return null;
    const pt = { lat: parseFloat(data[0].lat), lon: parseFloat(data[0].lon) };
    _geoCache[key] = pt; _saveGeoCache();
    return pt;
  } catch(e) { console.warn("[_geocodeAddress]", e); return null; }
}
async function _routeDistanceOSRM(p1, p2) {
  try {
    const url = `https://router.project-osrm.org/route/v1/driving/${p1.lon},${p1.lat};${p2.lon},${p2.lat}?overview=false`;
    const r = await fetch(url);
    const data = await r.json();
    if (!data.routes || !data.routes.length) return null;
    return data.routes[0].distance / 1000; // metros → km
  } catch(e) { console.warn("[_routeDistanceOSRM]", e); return null; }
}
// Verifica se o endereço tem informação suficiente pra geocodificar com
// confiança (nome de rua real) — 'Tijuca' ou '230, Tijuca' sozinhos são
// ambíguos demais (podem cair em qualquer ponto do bairro).
function _hasReliableAddress(addr) {
  if (!addr) return false;
  const firstPart = addr.split(',')[0].trim();
  // Primeira parte só com dígitos (número de prédio solto, sem rua) → não confiável
  if (/^\d+$/.test(firstPart)) return false;
  // Primeira parte com menos de 4 letras (ex: nome de bairro sozinho) → não confiável
  if (firstPart.replace(/[^a-zA-ZÀ-ÿ]/g,'').length < 4) return false;
  return true;
}
// Geocodifica com fallback: tenta o endereço completo; se falhar, tenta só
// da parte do bairro em diante (ex: "Rua X, Ermitage, Teresópolis - RJ" →
// "Ermitage, Teresópolis - RJ") — menos preciso, mas suficiente pra estimar km.
async function _geocodeWithFallback(addr) {
  const clean = _cleanGeoAddr(addr);
  let p = await _geocodeAddress(clean);
  if (p) return p;
  const parts = clean.split(',').map(s=>s.trim()).filter(Boolean);
  if (parts.length >= 2) {
    p = await _geocodeAddress(parts.slice(1).join(', '));
  }
  return p;
}
async function _estimateTripKm(origin, dest) {
  if (!_hasReliableAddress(origin) || !_hasReliableAddress(dest)) {
    console.warn('[_estimateTripKm] Endereço sem rua identificável — pulando estimativa:', origin, '/', dest);
    return null;
  }
  const p1 = await _geocodeWithFallback(origin);
  const p2 = await _geocodeWithFallback(dest);
  if (!p1 || !p2) return null;
  // Sanidade: se os pontos geocodificados ficam absurdamente longe um do
  // outro (linha reta > 80km), a geocodificação quase certamente resolveu
  // um endereço ambíguo pro lugar errado — descarta em vez de salvar lixo
  const straightKm = _haversineKm(p1.lat, p1.lon, p2.lat, p2.lon);
  if (straightKm > 80) {
    console.warn('[_estimateTripKm] Descartado — distância implausível:', straightKm.toFixed(1), 'km entre', origin, 'e', dest);
    return null;
  }
  const km = await _routeDistanceOSRM(p1, p2);
  if (km === null) return null;
  // Segunda checagem: a rota real também não deveria passar de ~120km
  // pra corrida urbana local (margem maior que a linha reta, por curvas)
  if (km > 120) {
    console.warn('[_estimateTripKm] Descartado — rota implausível:', km.toFixed(1), 'km');
    return null;
  }
  return parseFloat(km.toFixed(1));
}
function _haversineKm(lat1, lon1, lat2, lon2) {
  const R = 6371;
  const toRad = d => d * Math.PI / 180;
  const dLat = toRad(lat2-lat1), dLon = toRad(lon2-lon1);
  const a = Math.sin(dLat/2)**2 + Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLon/2)**2;
  return R * 2 * Math.asin(Math.sqrt(a));
}
// Fila: processa uma corrida por vez (respeitando o throttle acima),
// atualiza o trip local + Supabase quando o km chegar, e re-renderiza.
const _geoTried = new Set(); // trips já tentadas nesta sessão (evita loop)
function _queueKmEstimate(dateStr, tripId, origin, dest) {
  if (_geoTried.has(tripId)) return;
  _geoTried.add(tripId);
  _geoQueue.push({ dateStr, tripId, origin, dest });
  if (!_geoQueueBusy) _processGeoQueue();
}
// Backfill: corridas que ficaram com km=0 (fila interrompida ao fechar o
// app, rede fora etc.) entram de novo na fila — 1 tentativa por sessão por
// corrida. Roda a cada _syncTick; a guarda _geoTried evita repetição.
function _backfillKmEstimates() {
  try {
    const cutoff = new Date(Date.now()-7*24*3600*1000).toISOString().slice(0,10);
    DB.allDates().filter(d => d >= cutoff).forEach(dateStr => {
      const day = DB.day(dateStr);
      (day.trips||[]).forEach(t => {
        if ((parseFloat(t.km)||0) > 0 || t.kmEstimado || _geoTried.has(t.id)) return;
        const o = t.originFull || t.origin, d = t.destFull || t.dest;
        if (o && d) _queueKmEstimate(dateStr, t.id, o, d);
      });
    });
  } catch(e) { console.warn('[_backfillKmEstimates]', e); }
}
async function _processGeoQueue() {
  _geoQueueBusy = true;
  while (_geoQueue.length) {
    const job = _geoQueue.shift();
    try {
      const km = await _estimateTripKm(job.origin, job.dest);
      if (km !== null) {
        const day = DB.day(job.dateStr);
        const t = (day.trips||[]).find(x => x.id === job.tripId);
        if (t) {
          t.km = km; t.kmEstimado = true;
          DB.saveDay(day);
          if (_supaUser) {
            _SUPA.from("trips").upsert({ id: t.id, user_id: _supaUser.id, km, km_estimado: true },
              { onConflict: "id" }).catch(()=>{});
          }
          try { if (S.sec === "jornada") renderJornada(); if (S.sec === "corridas") renderCorridasActive(); } catch(e) {}
        }
      }
    } catch(e) { console.warn("[_processGeoQueue]", e); }
  }
  _geoQueueBusy = false;
}

function _gcss(){try{return new Set(JSON.parse(localStorage.getItem('sm_gps_closed_set')||'[]'));}catch(e){return new Set();}}
function _addCss(id,endMs){
  try{
    const s=_gcss();s.add(id);
    localStorage.setItem('sm_gps_closed_set',JSON.stringify([...s].slice(-30)));
    localStorage.setItem
===== sessions @ 873964 =====
function _migrateBairroNames(){
  try {
    if (localStorage.getItem('sm_bairro_clean_v1')) return;
    DB.allDates().forEach(dateStr => {
      const day = DB.day(dateStr); let ch = false;
      (day.trips||[]).forEach(t => {
        const o = _cleanBairro(t.origin||''), d = _cleanBairro(t.dest||'');
        if (o !== (t.origin||'')) { t.origin = o; ch = true; }
        if (d !== (t.dest||''))   { t.dest = d;  ch = true; }
      });
      if (ch) DB.saveDay(day);
    });
    localStorage.setItem('sm_bairro_clean_v1','1');
  } catch(e) { console.warn('[_migrateBairroNames]', e); }
})();
setTimeout(()=>{ _syncTick(); setInterval(_syncTick, 60*1000); }, 3000); // primeira vez em 3s, depois a cada 1 minuto

// Corridas automáticas novas capturadas durante o dia (pedido do Yuri,
// 23/07/2026) — INTERVALO PRÓPRIO, separado do _syncTick de propósito: se
// qualquer etapa do _syncTick (correção de trips, auto-sync, backfill de
// km) travar ou lançar erro, essa atualização não pode ficar refém disso —
// foi exatamente isso que aconteceu (corridas paravam de aparecer na
// Jornada ativa depois de um tempo, travadas no que existia no momento do
// primeiro carregamento do dia). 25s pra sentir a corrida nova rápido sem
// martelar o Supabase.
async function _refreshAutoTripsIfOnJornada() {
  if (!(S && S.sec === 'jornada' && S.curDate)) return;
  try {
    const list = await loadAutoTripsForDay(S.curDate);
    if (!Array.isArray(list)) return; // falha não vira "zero corridas"
    S.autoTrips = list; S.autoTripsDate = S.curDate; S._autoTripsDataDate = S.curDate;
    renderJornada();
  } catch(e) {}
}
setTimeout(()=>{ _refreshAutoTripsIfOnJornada(); setInterval(_refreshAutoTripsIfOnJornada, 25*1000); }, 4000);

// CORRIGIDO (24/07/2026, confirmado pelo Yuri: corridas terminadas até
// 22:21 não apareciam numa tela aberta desde antes) — a tela Corridas
// nunca tinha refresh automático como a Jornada já tem. Se ficasse parado
// nela sem trocar filtro/período nem sair e voltar, ficava presa no que
// carregou da última vez, mesmo com corridas novas terminando por trás.
// Mesmo padrão de intervalo da Jornada.
async function _refreshCorridasIfOnScreen() {
  if (!(S && S.sec === 'corridas')) return;
  try { await renderCorridasActive(); } catch(e) {}
}
setTimeout(()=>{ _refreshCorridasIfOnScreen(); setInterval(_refreshCorridasIfOnScreen, 25*1000); }, 4500);

// Mesma lógica pra Home (pedido do Yuri, 23/07/2026) — a checagem de cache
// em renderHomeGraphs() só refaz a busca quando o INTERVALO DE DATAS muda
// (troca de semana/mês/etc), nunca por tempo — uma corrida nova no meio do
// dia nunca apareceria sozinha sem isso. 60s (não precisa ser tão rápido
// quanto a Jornada, que é tempo real; Home é histórico/agregado).
async function _refreshAutoTripsIfOnHome() {
  if (!(S && S.sec === 'relatorios')) return;
  S.autoTripsHomeRange = null; // força o próximo renderHomeGraphs a rebuscar
  try { renderHomeGraphs(); } catch(e) {}
}
setTimeout(()=>{ setInterval(_refreshAutoTripsIfOnHome, 60*1000); }, 5000);

// ── Status de rede: indicador visual + reconciliação ao voltar online ──
let _netSyncing = false;
function updateNetStatus() {
  const badge = document.getElementById("net-status-badge");
  const dot = document.getElementById("net-status-dot");
  const txt = document.getElementById("net-status-text");
  if (!badge) return;
  if (!navigator.onLine) {
    badge.style.display = "flex";
    badge.style.background = "#FEF2F2";
    dot.style.background = "#EF4444";
    txt.style.color = "#B91C1C";
    txt.textContent = "Offline — dados salvos localmente";
  } else if (_netSyncing) {
    badge.style.display = "flex";
    badge.style.background = "#FFF7ED";
    dot.style.background = "#F59E0B";
    txt.style.color = "#C2410C";
    txt.textContent = "Sincronizando...";
  } else {
    badge.style.display = "none"; // tudo ok, não precisa mostrar nada
  }
}
window.addEventListener("offline", updateNetStatus);
window.addEventListener("online", async () => {
  _netSyncing = true; updateNetStatus();
  try { await SYNC.flushQueue(); await reconcileJornadaHoje(); } catch(e) {}
  _netSyncing = false; updateNetStatus();
  try { renderJornada(); } catch(e) {}
});

// ── Reconciliação: Supabase é a fonte de verdade, local é o cache ──────
async function reconcileJornadaHoje() {
  if (!_supaUser || !navigator.onLine) return;
  const today = DB.today();
  try {
    const {data: remote, error} = await _SUPA.from("sessions").select("*")
      .eq("user_id", _supaUser.id).eq("date", today);
    if (error || !remote) return;
    const day = DB.day(today);
    if (!day.sessions) day.sessions = [];
    let changed = false;
    // 1. Toda sessão remota vira a verdade local (upsert por id)
    remote.forEach(r => {
      if (!r.end_ms) return; // sessão ainda ativa remotamente — não mexe (pode ser a atual)
      const local = { id: r.id, start: r.start_ms, end: r.end_ms, km: parseFloat(r.km)||0, elapsed: r.elapsed_secs||0 };
      const idx = day.sessions.findIndex(s => s.id === r.id);
      if (idx === -1) { day.sessions.push(local); changed = true; }
      else if (day.sessions[idx].end !== local.end || day.sessions[idx].km !== local.km) {
        day.sessions[idx] = local; changed = true;
      }
    });
    // 2. Sessão local encerrada que não existe no remoto — reenviar (não apagar)
    const remoteIds = new Set(remote.map(r => r.id));
    for (const s of day.sessions) {
      if (!remoteIds.has(s.id) && s.end) {
        await _SUPA.from("sessions").upsert({
          id: s.id, user_id: _supaUser.id, date: today,
          start_ms: s.start, end_ms: s.end, km: s.km, elapsed_secs: s.elapsed,
          paused_ms: 0, is_paused: false
        }, {onConflict:"id"}).catch(()=>{});
      }
    }
    // 3. Sessão ativa: se remoto diz que já encerrou mas o app local acha que continua
    if (S.gps.st !== "idle" && S.gps.sessionId) {
      const remoteActive = remote.find(r => r.id === S.gps.sessionId);
      if (remoteActive && remoteActive.end_ms) {
        S.gps.st = "idle"; S.gps.start = null;
        clearInterval(S.gps.ti);
        changed = true;
      }
    }
    if (changed) DB.saveDay(day);
  } catch(e) { console.warn("[reconcileJornadaHoje]", e); }
}

// ── Estimativa de km por endereço (Nominatim geocode + OSRM route) ────
// Usado quando a plataforma não informa km (ex: 99 Motorista). Gratuito,
// mas com throttle de 1 req/s no Nominatim — processar em fila sequencial.
let _geoQueueBusy = false;
const _geoQueue = [];
// Remove prefixo "Bairro da/do/de/das/dos" do nome de bairro (a 99 escreve
// "Bairro da São Pedro" — queremos exibir/geocodificar só "São Pedro")
function _cleanBairro(name) {
  if (!name) return name;
  return name.replace(/^\s*bairro\s+(d[aeo]s?\s+)?/i, '').trim();
}
// Limpa lixo comum do OCR no endereço antes de geocodificar: vírgulas soltas,
// país truncado ("Bra"/"Bras"), número de prédio grudado ("1211- Rua X"),
// e o prefixo "Bairro da" que confunde o Nominatim.
function _cleanGeoAddr(addr) {
  if (!addr) return addr;
  let a = addr.trim();
  a = a.replace(/,\s*(BR|Brasil|Bras?i?l?|Bra)\.?\s*$/i, '');
  a = a.replace(/[,\s]+$/, '');
  a = a.replace(/^\d+\s*-\s*/, '');
  a = a.replace(/\bbairro\s+(d[aeo]s?\s+)/gi, '');
  return a.replace(/\s{2,}/g, ' ').trim();
}
// Cache persistente de geocodificação: os mesmos bairros/endereços repetem
// muito no dia a dia — resolver 1x e reaproveitar corta o tempo da fila de
// ~5s/corrida pra quase zero nas repetidas (e economiza chamadas ao Nominatim).
const _geoCache = (() => {
  try { return JSON.parse(localStorage.getItem('sm_geo_cache_v1') || '{}'); }
  catch(e) { return {}; }
})();
function _saveGeoCache() {
  try {
    // limita a 400 entradas pra não inchar o localStorage
    const keys = Object.keys(_geoCache);
    if (keys.length > 400) keys.slice(0, keys.length - 400).forEach(k => delete _geoCache[k]);
    localStorage.setItem('sm_geo_cache_v1', JSON.stringify(_geoCache));
  } catch(e) {}
}
// Throttle global do Nominatim (1 req/s) — embutido aqui pra que cache hits
// não paguem espera nenhuma.
let _lastNominatimMs = 0;
async function _nominatimThrottle() {
  const wait = 1100 - (Date.now() - _lastNominatimMs);
  if (wait > 0) await new Promise(r => setTimeout(r, wait));
  _lastNominatimMs = Date.now();
}
async function _geocodeAddress(query) {
  if (!query) return null;
  const key = query.toLowerCase().trim();
  if (_geoCache[key]) return _geoCache[key];
  try {
    await _nominatimThrottle();
    const url = "https://nominatim.openstreetmap.org/search?format=json&limit=1&countrycodes=br&q=" + encodeURIComponent(query);
    const r = await fetch(url, { headers: { "Accept-Language": "pt-BR" } });
    const data = await r.json();
    if (!data || !data.length) return null;
    const pt = { lat: parseFloat(data[0].lat), lon: parseFloat(data[0].lon) };
    _geoCache[key] = pt; _saveGeoCache();
    return pt;
  } catch(e) { console.warn("[_geocodeAddress]", e); return null; }
}
async function _routeDistanceOSRM(p1, p2) {
  try {
    const url = `https://router.project-osrm.org/route/v1/driving/${p1.lon},${p1.lat};${p2.lon},${p2.lat}?overview=false`;
    const r = await fetch(url);
    const data = await r.json();
    if (!data.routes || !data.routes.length) return null;
    return data.routes[0].distance / 1000; // metros → km
  } catch(e) { console.warn("[_routeDistanceOSRM]", e); return null; }
}
// Verifica se o endereço tem informação suficiente pra geocodificar com
// confiança (nome de rua real) — 'Tijuca' ou '230, Tijuca' sozinhos são
// ambíguos demais (podem cair em qualquer ponto do bairro).
function _hasReliableAddress(addr) {
  if (!addr) return false;
  const firstPart = addr.split(',')[0].trim();
  // Primeira parte só com dígitos (número de prédio solto, sem rua) → não confiável
  if (/^\d+$/.test(firstPart)) return false;
  // Primeira parte com menos de 4 letras (ex: nome de bairro sozinho) → não confiável
  if (firstPart.replace(/[^a-zA-ZÀ-ÿ]/g,'').length < 4) return false;
  return true;
}
// Geocodifica com fallback: tenta o endereço completo; se falhar, tenta só
// da parte do bairro em diante (ex: "Rua X, Ermitage, Teresópolis - RJ" →
// "Ermitage, Teresópolis - RJ") — menos preciso, mas suficiente pra estimar km.
async function _geocodeWithFallback(addr) {
  const clean = _cleanGeoAddr(addr);
  let p = await _geocodeAddress(clean);
  if (p) return p;
  const parts = clean.split(',').map(s=>s.trim()).filter(Boolean);
  if (parts.length >= 2) {
    p = await _geocodeAddress(parts.slice(1).join(', '));
  }
  return p;
}
async function _estimateTripKm(origin, dest) {
  if (!_hasReliableAddress(origin) || !_hasReliableAddress(dest)) {
    console.warn('[_estimateTripKm] Endereço sem rua identificável — pulando estimativa:', origin, '/', dest);
    return null;
  }
  const p1 = await _geocodeWithFallback(origin);
  const p2 = await _geocodeWithFallback(dest);
  if (!p1 || !p2) return null;
  // Sanidade: se os pontos geocodificados ficam absurdamente longe um do
  // outro (linha reta > 80km), a geocodificação quase certamente resolveu
  // um endereço ambíguo pro lugar errado — descarta em vez de salvar lixo
  const straightKm = _haversineKm(p1.lat, p1.lon, p2.lat, p2.lon);
  if (straightKm > 80) {
    console.warn('[_estimateTripKm] Descartado — distância implausível:', straightKm.toFixed(1), 'km entre', origin, 'e', dest);
    return null;
  }
  const km = await _routeDistanceOSRM(p1, p2);
  if (km === null) return null;
  // Segunda checagem: a rota real também não deveria passar de ~120km
  // pra corrida urbana local (margem maior que a linha reta, por curvas)
  if (km > 120) {
    console.warn('[_estimateTripKm] Descartado — rota implausível:', km.toFixed(1), 'km');
    return null;
  }
  return parseFloat(km.toFixed(1));
}
function _haversineKm(lat1, lon1, lat2, lon2) {
  const R = 6371;
  const toRad = d => d * Math.PI / 180;
  const dLat = toRad(lat2-lat1), dLon = toRad(lon2-lon1);
  const a = Math.sin(dLat/2)**2 + Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLon/2)**2;
  return R * 2 * Math.asin(Math.sqrt(a));
}
// Fila: processa uma corrida por vez (respeitando o throttle acima),
// atualiza o trip local + Supabase quando o km chegar, e re-renderiza.
const _geoTried = new Set(); // trips já tentadas nesta sessão (evita loop)
function _queueKmEstimate(dateStr, tripId, origin, dest) {
  if (_geoTried.has(tripId)) return;
  _geoTried.add(tripId);
  _geoQueue.push({ dateStr, tripId, origin, dest });
  if (!_geoQueueBusy) _processGeoQueue();
}
// Backfill: corridas que ficaram com km=0 (fila interrompida ao fechar o
// app, rede fora etc.) entram de novo na fila — 1 tentativa por sessão por
// corrida. Roda a cada _syncTick; a guarda _geoTried evita repetição.
function _backfillKmEstimates() {
  try {
    const cutoff = new Date(Date.now()-7*24*3600*1000).toISOString().slice(0,10);
    DB.allDates().filter(d => d >= cutoff).forEach(dateStr => {
      const day = DB.day(dateStr);
      (day.trips||[]).forEach(t => {
        if ((parseFloat(t.km)||0) > 0 || t.kmEstimado || _geoTried.has(t.id)) return;
        const o = t.originFull || t.origin, d = t.destFull || t.dest;
        if (o && d) _queueKmEstimate(dateStr, t.id, o, d);
      });
    });
  } catch(e) { console.warn('[_backfillKmEstimates]', e); }
}
async function _processGeoQueue() {
  _geoQueueBusy = true;
  while (_geoQueue.length) {
    const job = _geoQueue.shift();
    try {
      const km = await _estimateTripKm(job.origin, job.dest);
      if (km !== null) {
        const day = DB.day(job.dateStr);
        const t = (day.trips||[]).find(x => x.id === job.tripId);
        if (t) {
          t.km = km; t.kmEstimado = true;
          DB.saveDay(day);
          if (_supaUser) {
            _SUPA.from("trips").upsert({ id: t.id, user_id: _supaUser.id, km, km_estimado: true },
              { onConflict: "id" }).catch(()=>{});
          }
          try { if (S.sec === "jornada") renderJornada(); if (S.sec === "corridas") renderCorridasActive(); } catch(e) {}
        }
      }
    } catch(e) { console.warn("[_processGeoQueue]", e); }
  }
  _geoQueueBusy = false;
}

function _gcss(){try{return new Set(JSON.parse(localStorage.getItem('sm_gps_closed_set')||'[]'));}catch(e){return new Set();}}
function _addCss(id,endMs){
  try{
    const s=_gcss();s.add(id);
    localStorage.setItem('sm_gps_closed_set',JSON.stringify([...s].slice(-30)));
    localStorage.setItem('sm_gps_last_closed',JSON.stringify({id,end:endMs}));
  }catch(e){}
}

// ── Trava de segurança: nunca permite duas jornadas abertas ao mesmo tempo.
// Roda antes de iniciar uma nova jornada. Se encontrar alguma jornada aberta
// no Supabase (end_ms=null) — de uma aba/instância esquecida, do serviço nativo
// que não parou, ou de um fechamento anterior que falhou — fecha automaticamente
// com os últimos dados que o servidor tem, e avisa o usuário.
async function _closeOrphanSessions() {
  if (!_supaUser || !navigator.onLine) retu
===== sessions @ 874390 =====
}
      });
      if (ch) DB.saveDay(day);
    });
    localStorage.setItem('sm_bairro_clean_v1','1');
  } catch(e) { console.warn('[_migrateBairroNames]', e); }
})();
setTimeout(()=>{ _syncTick(); setInterval(_syncTick, 60*1000); }, 3000); // primeira vez em 3s, depois a cada 1 minuto

// Corridas automáticas novas capturadas durante o dia (pedido do Yuri,
// 23/07/2026) — INTERVALO PRÓPRIO, separado do _syncTick de propósito: se
// qualquer etapa do _syncTick (correção de trips, auto-sync, backfill de
// km) travar ou lançar erro, essa atualização não pode ficar refém disso —
// foi exatamente isso que aconteceu (corridas paravam de aparecer na
// Jornada ativa depois de um tempo, travadas no que existia no momento do
// primeiro carregamento do dia). 25s pra sentir a corrida nova rápido sem
// martelar o Supabase.
async function _refreshAutoTripsIfOnJornada() {
  if (!(S && S.sec === 'jornada' && S.curDate)) return;
  try {
    const list = await loadAutoTripsForDay(S.curDate);
    if (!Array.isArray(list)) return; // falha não vira "zero corridas"
    S.autoTrips = list; S.autoTripsDate = S.curDate; S._autoTripsDataDate = S.curDate;
    renderJornada();
  } catch(e) {}
}
setTimeout(()=>{ _refreshAutoTripsIfOnJornada(); setInterval(_refreshAutoTripsIfOnJornada, 25*1000); }, 4000);

// CORRIGIDO (24/07/2026, confirmado pelo Yuri: corridas terminadas até
// 22:21 não apareciam numa tela aberta desde antes) — a tela Corridas
// nunca tinha refresh automático como a Jornada já tem. Se ficasse parado
// nela sem trocar filtro/período nem sair e voltar, ficava presa no que
// carregou da última vez, mesmo com corridas novas terminando por trás.
// Mesmo padrão de intervalo da Jornada.
async function _refreshCorridasIfOnScreen() {
  if (!(S && S.sec === 'corridas')) return;
  try { await renderCorridasActive(); } catch(e) {}
}
setTimeout(()=>{ _refreshCorridasIfOnScreen(); setInterval(_refreshCorridasIfOnScreen, 25*1000); }, 4500);

// Mesma lógica pra Home (pedido do Yuri, 23/07/2026) — a checagem de cache
// em renderHomeGraphs() só refaz a busca quando o INTERVALO DE DATAS muda
// (troca de semana/mês/etc), nunca por tempo — uma corrida nova no meio do
// dia nunca apareceria sozinha sem isso. 60s (não precisa ser tão rápido
// quanto a Jornada, que é tempo real; Home é histórico/agregado).
async function _refreshAutoTripsIfOnHome() {
  if (!(S && S.sec === 'relatorios')) return;
  S.autoTripsHomeRange = null; // força o próximo renderHomeGraphs a rebuscar
  try { renderHomeGraphs(); } catch(e) {}
}
setTimeout(()=>{ setInterval(_refreshAutoTripsIfOnHome, 60*1000); }, 5000);

// ── Status de rede: indicador visual + reconciliação ao voltar online ──
let _netSyncing = false;
function updateNetStatus() {
  const badge = document.getElementById("net-status-badge");
  const dot = document.getElementById("net-status-dot");
  const txt = document.getElementById("net-status-text");
  if (!badge) return;
  if (!navigator.onLine) {
    badge.style.display = "flex";
    badge.style.background = "#FEF2F2";
    dot.style.background = "#EF4444";
    txt.style.color = "#B91C1C";
    txt.textContent = "Offline — dados salvos localmente";
  } else if (_netSyncing) {
    badge.style.display = "flex";
    badge.style.background = "#FFF7ED";
    dot.style.background = "#F59E0B";
    txt.style.color = "#C2410C";
    txt.textContent = "Sincronizando...";
  } else {
    badge.style.display = "none"; // tudo ok, não precisa mostrar nada
  }
}
window.addEventListener("offline", updateNetStatus);
window.addEventListener("online", async () => {
  _netSyncing = true; updateNetStatus();
  try { await SYNC.flushQueue(); await reconcileJornadaHoje(); } catch(e) {}
  _netSyncing = false; updateNetStatus();
  try { renderJornada(); } catch(e) {}
});

// ── Reconciliação: Supabase é a fonte de verdade, local é o cache ──────
async function reconcileJornadaHoje() {
  if (!_supaUser || !navigator.onLine) return;
  const today = DB.today();
  try {
    const {data: remote, error} = await _SUPA.from("sessions").select("*")
      .eq("user_id", _supaUser.id).eq("date", today);
    if (error || !remote) return;
    const day = DB.day(today);
    if (!day.sessions) day.sessions = [];
    let changed = false;
    // 1. Toda sessão remota vira a verdade local (upsert por id)
    remote.forEach(r => {
      if (!r.end_ms) return; // sessão ainda ativa remotamente — não mexe (pode ser a atual)
      const local = { id: r.id, start: r.start_ms, end: r.end_ms, km: parseFloat(r.km)||0, elapsed: r.elapsed_secs||0 };
      const idx = day.sessions.findIndex(s => s.id === r.id);
      if (idx === -1) { day.sessions.push(local); changed = true; }
      else if (day.sessions[idx].end !== local.end || day.sessions[idx].km !== local.km) {
        day.sessions[idx] = local; changed = true;
      }
    });
    // 2. Sessão local encerrada que não existe no remoto — reenviar (não apagar)
    const remoteIds = new Set(remote.map(r => r.id));
    for (const s of day.sessions) {
      if (!remoteIds.has(s.id) && s.end) {
        await _SUPA.from("sessions").upsert({
          id: s.id, user_id: _supaUser.id, date: today,
          start_ms: s.start, end_ms: s.end, km: s.km, elapsed_secs: s.elapsed,
          paused_ms: 0, is_paused: false
        }, {onConflict:"id"}).catch(()=>{});
      }
    }
    // 3. Sessão ativa: se remoto diz que já encerrou mas o app local acha que continua
    if (S.gps.st !== "idle" && S.gps.sessionId) {
      const remoteActive = remote.find(r => r.id === S.gps.sessionId);
      if (remoteActive && remoteActive.end_ms) {
        S.gps.st = "idle"; S.gps.start = null;
        clearInterval(S.gps.ti);
        changed = true;
      }
    }
    if (changed) DB.saveDay(day);
  } catch(e) { console.warn("[reconcileJornadaHoje]", e); }
}

// ── Estimativa de km por endereço (Nominatim geocode + OSRM route) ────
// Usado quando a plataforma não informa km (ex: 99 Motorista). Gratuito,
// mas com throttle de 1 req/s no Nominatim — processar em fila sequencial.
let _geoQueueBusy = false;
const _geoQueue = [];
// Remove prefixo "Bairro da/do/de/das/dos" do nome de bairro (a 99 escreve
// "Bairro da São Pedro" — queremos exibir/geocodificar só "São Pedro")
function _cleanBairro(name) {
  if (!name) return name;
  return name.replace(/^\s*bairro\s+(d[aeo]s?\s+)?/i, '').trim();
}
// Limpa lixo comum do OCR no endereço antes de geocodificar: vírgulas soltas,
// país truncado ("Bra"/"Bras"), número de prédio grudado ("1211- Rua X"),
// e o prefixo "Bairro da" que confunde o Nominatim.
function _cleanGeoAddr(addr) {
  if (!addr) return addr;
  let a = addr.trim();
  a = a.replace(/,\s*(BR|Brasil|Bras?i?l?|Bra)\.?\s*$/i, '');
  a = a.replace(/[,\s]+$/, '');
  a = a.replace(/^\d+\s*-\s*/, '');
  a = a.replace(/\bbairro\s+(d[aeo]s?\s+)/gi, '');
  return a.replace(/\s{2,}/g, ' ').trim();
}
// Cache persistente de geocodificação: os mesmos bairros/endereços repetem
// muito no dia a dia — resolver 1x e reaproveitar corta o tempo da fila de
// ~5s/corrida pra quase zero nas repetidas (e economiza chamadas ao Nominatim).
const _geoCache = (() => {
  try { return JSON.parse(localStorage.getItem('sm_geo_cache_v1') || '{}'); }
  catch(e) { return {}; }
})();
function _saveGeoCache() {
  try {
    // limita a 400 entradas pra não inchar o localStorage
    const keys = Object.keys(_geoCache);
    if (keys.length > 400) keys.slice(0, keys.length - 400).forEach(k => delete _geoCache[k]);
    localStorage.setItem('sm_geo_cache_v1', JSON.stringify(_geoCache));
  } catch(e) {}
}
// Throttle global do Nominatim (1 req/s) — embutido aqui pra que cache hits
// não paguem espera nenhuma.
let _lastNominatimMs = 0;
async function _nominatimThrottle() {
  const wait = 1100 - (Date.now() - _lastNominatimMs);
  if (wait > 0) await new Promise(r => setTimeout(r, wait));
  _lastNominatimMs = Date.now();
}
async function _geocodeAddress(query) {
  if (!query) return null;
  const key = query.toLowerCase().trim();
  if (_geoCache[key]) return _geoCache[key];
  try {
    await _nominatimThrottle();
    const url = "https://nominatim.openstreetmap.org/search?format=json&limit=1&countrycodes=br&q=" + encodeURIComponent(query);
    const r = await fetch(url, { headers: { "Accept-Language": "pt-BR" } });
    const data = await r.json();
    if (!data || !data.length) return null;
    const pt = { lat: parseFloat(data[0].lat), lon: parseFloat(data[0].lon) };
    _geoCache[key] = pt; _saveGeoCache();
    return pt;
  } catch(e) { console.warn("[_geocodeAddress]", e); return null; }
}
async function _routeDistanceOSRM(p1, p2) {
  try {
    const url = `https://router.project-osrm.org/route/v1/driving/${p1.lon},${p1.lat};${p2.lon},${p2.lat}?overview=false`;
    const r = await fetch(url);
    const data = await r.json();
    if (!data.routes || !data.routes.length) return null;
    return data.routes[0].distance / 1000; // metros → km
  } catch(e) { console.warn("[_routeDistanceOSRM]", e); return null; }
}
// Verifica se o endereço tem informação suficiente pra geocodificar com
// confiança (nome de rua real) — 'Tijuca' ou '230, Tijuca' sozinhos são
// ambíguos demais (podem cair em qualquer ponto do bairro).
function _hasReliableAddress(addr) {
  if (!addr) return false;
  const firstPart = addr.split(',')[0].trim();
  // Primeira parte só com dígitos (número de prédio solto, sem rua) → não confiável
  if (/^\d+$/.test(firstPart)) return false;
  // Primeira parte com menos de 4 letras (ex: nome de bairro sozinho) → não confiável
  if (firstPart.replace(/[^a-zA-ZÀ-ÿ]/g,'').length < 4) return false;
  return true;
}
// Geocodifica com fallback: tenta o endereço completo; se falhar, tenta só
// da parte do bairro em diante (ex: "Rua X, Ermitage, Teresópolis - RJ" →
// "Ermitage, Teresópolis - RJ") — menos preciso, mas suficiente pra estimar km.
async function _geocodeWithFallback(addr) {
  const clean = _cleanGeoAddr(addr);
  let p = await _geocodeAddress(clean);
  if (p) return p;
  const parts = clean.split(',').map(s=>s.trim()).filter(Boolean);
  if (parts.length >= 2) {
    p = await _geocodeAddress(parts.slice(1).join(', '));
  }
  return p;
}
async function _estimateTripKm(origin, dest) {
  if (!_hasReliableAddress(origin) || !_hasReliableAddress(dest)) {
    console.warn('[_estimateTripKm] Endereço sem rua identificável — pulando estimativa:', origin, '/', dest);
    return null;
  }
  const p1 = await _geocodeWithFallback(origin);
  const p2 = await _geocodeWithFallback(dest);
  if (!p1 || !p2) return null;
  // Sanidade: se os pontos geocodificados ficam absurdamente longe um do
  // outro (linha reta > 80km), a geocodificação quase certamente resolveu
  // um endereço ambíguo pro lugar errado — descarta em vez de salvar lixo
  const straightKm = _haversineKm(p1.lat, p1.lon, p2.lat, p2.lon);
  if (straightKm > 80) {
    console.warn('[_estimateTripKm] Descartado — distância implausível:', straightKm.toFixed(1), 'km entre', origin, 'e', dest);
    return null;
  }
  const km = await _routeDistanceOSRM(p1, p2);
  if (km === null) return null;
  // Segunda checagem: a rota real também não deveria passar de ~120km
  // pra corrida urbana local (margem maior que a linha reta, por curvas)
  if (km > 120) {
    console.warn('[_estimateTripKm] Descartado — rota implausível:', km.toFixed(1), 'km');
    return null;
  }
  return parseFloat(km.toFixed(1));
}
function _haversineKm(lat1, lon1, lat2, lon2) {
  const R = 6371;
  const toRad = d => d * Math.PI / 180;
  const dLat = toRad(lat2-lat1), dLon = toRad(lon2-lon1);
  const a = Math.sin(dLat/2)**2 + Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLon/2)**2;
  return R * 2 * Math.asin(Math.sqrt(a));
}
// Fila: processa uma corrida por vez (respeitando o throttle acima),
// atualiza o trip local + Supabase quando o km chegar, e re-renderiza.
const _geoTried = new Set(); // trips já tentadas nesta sessão (evita loop)
function _queueKmEstimate(dateStr, tripId, origin, dest) {
  if (_geoTried.has(tripId)) return;
  _geoTried.add(tripId);
  _geoQueue.push({ dateStr, tripId, origin, dest });
  if (!_geoQueueBusy) _processGeoQueue();
}
// Backfill: corridas que ficaram com km=0 (fila interrompida ao fechar o
// app, rede fora etc.) entram de novo na fila — 1 tentativa por sessão por
// corrida. Roda a cada _syncTick; a guarda _geoTried evita repetição.
function _backfillKmEstimates() {
  try {
    const cutoff = new Date(Date.now()-7*24*3600*1000).toISOString().slice(0,10);
    DB.allDates().filter(d => d >= cutoff).forEach(dateStr => {
      const day = DB.day(dateStr);
      (day.trips||[]).forEach(t => {
        if ((parseFloat(t.km)||0) > 0 || t.kmEstimado || _geoTried.has(t.id)) return;
        const o = t.originFull || t.origin, d = t.destFull || t.dest;
        if (o && d) _queueKmEstimate(dateStr, t.id, o, d);
      });
    });
  } catch(e) { console.warn('[_backfillKmEstimates]', e); }
}
async function _processGeoQueue() {
  _geoQueueBusy = true;
  while (_geoQueue.length) {
    const job = _geoQueue.shift();
    try {
      const km = await _estimateTripKm(job.origin, job.dest);
      if (km !== null) {
        const day = DB.day(job.dateStr);
        const t = (day.trips||[]).find(x => x.id === job.tripId);
        if (t) {
          t.km = km; t.kmEstimado = true;
          DB.saveDay(day);
          if (_supaUser) {
            _SUPA.from("trips").upsert({ id: t.id, user_id: _supaUser.id, km, km_estimado: true },
              { onConflict: "id" }).catch(()=>{});
          }
          try { if (S.sec === "jornada") renderJornada(); if (S.sec === "corridas") renderCorridasActive(); } catch(e) {}
        }
      }
    } catch(e) { console.warn("[_processGeoQueue]", e); }
  }
  _geoQueueBusy = false;
}

function _gcss(){try{return new Set(JSON.parse(localStorage.getItem('sm_gps_closed_set')||'[]'));}catch(e){return new Set();}}
function _addCss(id,endMs){
  try{
    const s=_gcss();s.add(id);
    localStorage.setItem('sm_gps_closed_set',JSON.stringify([...s].slice(-30)));
    localStorage.setItem('sm_gps_last_closed',JSON.stringify({id,end:endMs}));
  }catch(e){}
}

// ── Trava de segurança: nunca permite duas jornadas abertas ao mesmo tempo.
// Roda antes de iniciar uma nova jornada. Se encontrar alguma jornada aberta
// no Supabase (end_ms=null) — de uma aba/instância esquecida, do serviço nativo
// que não parou, ou de um fechamento anterior que falhou — fecha automaticamente
// com os últimos dados que o servidor tem, e avisa o usuário.
async function _closeOrphanSessions() {
  if (!_supaUser || !navigator.onLine) return null;
  try {
    const { data: allOpen } = await _SUPA.from("sessions").select("*")
      .eq("user_id", _supaUser.id).is("end_ms", null);
    if (!allOpen || !allOpen.length) return null;
    const staleCutoff = Date.now() - 10*60*1000; // 10 min sem checkpoint = órfã
    const orphans = allOpen.filter(o => {
      if (o.id === S.gps.sessionId) return false; // nunca fecha a sessão desta própria instância
      const 
===== sessions @ 883908 =====
nfiável
  if (firstPart.replace(/[^a-zA-ZÀ-ÿ]/g,'').length < 4) return false;
  return true;
}
// Geocodifica com fallback: tenta o endereço completo; se falhar, tenta só
// da parte do bairro em diante (ex: "Rua X, Ermitage, Teresópolis - RJ" →
// "Ermitage, Teresópolis - RJ") — menos preciso, mas suficiente pra estimar km.
async function _geocodeWithFallback(addr) {
  const clean = _cleanGeoAddr(addr);
  let p = await _geocodeAddress(clean);
  if (p) return p;
  const parts = clean.split(',').map(s=>s.trim()).filter(Boolean);
  if (parts.length >= 2) {
    p = await _geocodeAddress(parts.slice(1).join(', '));
  }
  return p;
}
async function _estimateTripKm(origin, dest) {
  if (!_hasReliableAddress(origin) || !_hasReliableAddress(dest)) {
    console.warn('[_estimateTripKm] Endereço sem rua identificável — pulando estimativa:', origin, '/', dest);
    return null;
  }
  const p1 = await _geocodeWithFallback(origin);
  const p2 = await _geocodeWithFallback(dest);
  if (!p1 || !p2) return null;
  // Sanidade: se os pontos geocodificados ficam absurdamente longe um do
  // outro (linha reta > 80km), a geocodificação quase certamente resolveu
  // um endereço ambíguo pro lugar errado — descarta em vez de salvar lixo
  const straightKm = _haversineKm(p1.lat, p1.lon, p2.lat, p2.lon);
  if (straightKm > 80) {
    console.warn('[_estimateTripKm] Descartado — distância implausível:', straightKm.toFixed(1), 'km entre', origin, 'e', dest);
    return null;
  }
  const km = await _routeDistanceOSRM(p1, p2);
  if (km === null) return null;
  // Segunda checagem: a rota real também não deveria passar de ~120km
  // pra corrida urbana local (margem maior que a linha reta, por curvas)
  if (km > 120) {
    console.warn('[_estimateTripKm] Descartado — rota implausível:', km.toFixed(1), 'km');
    return null;
  }
  return parseFloat(km.toFixed(1));
}
function _haversineKm(lat1, lon1, lat2, lon2) {
  const R = 6371;
  const toRad = d => d * Math.PI / 180;
  const dLat = toRad(lat2-lat1), dLon = toRad(lon2-lon1);
  const a = Math.sin(dLat/2)**2 + Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLon/2)**2;
  return R * 2 * Math.asin(Math.sqrt(a));
}
// Fila: processa uma corrida por vez (respeitando o throttle acima),
// atualiza o trip local + Supabase quando o km chegar, e re-renderiza.
const _geoTried = new Set(); // trips já tentadas nesta sessão (evita loop)
function _queueKmEstimate(dateStr, tripId, origin, dest) {
  if (_geoTried.has(tripId)) return;
  _geoTried.add(tripId);
  _geoQueue.push({ dateStr, tripId, origin, dest });
  if (!_geoQueueBusy) _processGeoQueue();
}
// Backfill: corridas que ficaram com km=0 (fila interrompida ao fechar o
// app, rede fora etc.) entram de novo na fila — 1 tentativa por sessão por
// corrida. Roda a cada _syncTick; a guarda _geoTried evita repetição.
function _backfillKmEstimates() {
  try {
    const cutoff = new Date(Date.now()-7*24*3600*1000).toISOString().slice(0,10);
    DB.allDates().filter(d => d >= cutoff).forEach(dateStr => {
      const day = DB.day(dateStr);
      (day.trips||[]).forEach(t => {
        if ((parseFloat(t.km)||0) > 0 || t.kmEstimado || _geoTried.has(t.id)) return;
        const o = t.originFull || t.origin, d = t.destFull || t.dest;
        if (o && d) _queueKmEstimate(dateStr, t.id, o, d);
      });
    });
  } catch(e) { console.warn('[_backfillKmEstimates]', e); }
}
async function _processGeoQueue() {
  _geoQueueBusy = true;
  while (_geoQueue.length) {
    const job = _geoQueue.shift();
    try {
      const km = await _estimateTripKm(job.origin, job.dest);
      if (km !== null) {
        const day = DB.day(job.dateStr);
        const t = (day.trips||[]).find(x => x.id === job.tripId);
        if (t) {
          t.km = km; t.kmEstimado = true;
          DB.saveDay(day);
          if (_supaUser) {
            _SUPA.from("trips").upsert({ id: t.id, user_id: _supaUser.id, km, km_estimado: true },
              { onConflict: "id" }).catch(()=>{});
          }
          try { if (S.sec === "jornada") renderJornada(); if (S.sec === "corridas") renderCorridasActive(); } catch(e) {}
        }
      }
    } catch(e) { console.warn("[_processGeoQueue]", e); }
  }
  _geoQueueBusy = false;
}

function _gcss(){try{return new Set(JSON.parse(localStorage.getItem('sm_gps_closed_set')||'[]'));}catch(e){return new Set();}}
function _addCss(id,endMs){
  try{
    const s=_gcss();s.add(id);
    localStorage.setItem('sm_gps_closed_set',JSON.stringify([...s].slice(-30)));
    localStorage.setItem('sm_gps_last_closed',JSON.stringify({id,end:endMs}));
  }catch(e){}
}

// ── Trava de segurança: nunca permite duas jornadas abertas ao mesmo tempo.
// Roda antes de iniciar uma nova jornada. Se encontrar alguma jornada aberta
// no Supabase (end_ms=null) — de uma aba/instância esquecida, do serviço nativo
// que não parou, ou de um fechamento anterior que falhou — fecha automaticamente
// com os últimos dados que o servidor tem, e avisa o usuário.
async function _closeOrphanSessions() {
  if (!_supaUser || !navigator.onLine) return null;
  try {
    const { data: allOpen } = await _SUPA.from("sessions").select("*")
      .eq("user_id", _supaUser.id).is("end_ms", null);
    if (!allOpen || !allOpen.length) return null;
    const staleCutoff = Date.now() - 10*60*1000; // 10 min sem checkpoint = órfã
    const orphans = allOpen.filter(o => {
      if (o.id === S.gps.sessionId) return false; // nunca fecha a sessão desta própria instância
      const lastSeen = o.updated_at ? new Date(o.updated_at).getTime() : 0;
      return lastSeen < staleCutoff; // só órfã se não recebeu checkpoint recente
    });
    if (!orphans.length) return null;
    for (const o of orphans) {
      const estEnd = o.start_ms + (o.elapsed_secs||0)*1000;
      await _SUPA.from("sessions").upsert({
        id: o.id, user_id: _supaUser.id, end_ms: Math.max(estEnd, o.start_ms+60000),
        is_paused: false, paused_ms: 0
      }, {onConflict:"id"}).catch(()=>{});
      // Atualiza também o histórico local do dia, se existir
      try {
        const day = DB.day(o.date);
        if (day.sessions) {
          const idx = day.sessions.findIndex(s => s.id === o.id);
          const sess = { id:o.id, start:o.start_ms, end:estEnd, km:parseFloat(o.km)||0, elapsed:o.elapsed_secs||0 };
          if (idx >= 0) day.sessions[idx] = sess; else day.sessions.push(sess);
          DB.saveDay(day);
        }
      } catch(e) {}
    }
    console.warn(`[_closeOrphanSessions] ${orphans.length} jornada(s) órfã(s) encerrada(s) automaticamente`);
    return orphans.length;
  } catch(e) { console.error("[_closeOrphanSessions]", e); return null; }
}

// Ponto de entrada real de "iniciar jornada" — agora passa pelo assistente
// (métricas + permissões) antes de chamar a lógica de verdade. Os dois
// lugares que chamavam gpsStart() direto (botão "Iniciar" e o toque no
// ícone central quando idle) não precisaram mudar nada — ganham o
// assistente de graça só por causa dessa troca aqui.

// Recuperação resiliente da jornada: a nuvem é fonte de continuidade quando a
// WebView/localStorage perde o estado, mas o serviço Android continuou ativo.
let _journeyCloudRecoverBusy = false;
async function recoverOpenJourneyFromCloud() {
  if (_journeyCloudRecoverBusy || !_supaUser || !navigator.onLine) return false;
  if (S.gps.st === 'running' || S.gps.st === 'paused') return false;
  _journeyCloudRecoverBusy = true;
  try {
    const cutoff = Date.now() - 16*3600*1000;
    const { data: rows } = await _SUPA.from('sessions').select('*')
      .eq('user_id',_supaUser.id).is('end_ms',null)
      .gte('start_ms',cutoff).order('start_ms',{ascending:false}).limit(1);
    if (!rows || !rows.length) return false;
    const ss = rows[0];
    const g = S.gps;
    let bestKm = parseFloat(ss.km)||0;
    try {
      const { data: segs } = await _SUPA.from('journey_status_segments')
        .select('last_km,end_km,start_km,last_seen_ms')
        .eq('session_id',ss.id).order('last_seen_ms',{ascending:false}).limit(40);
      bestKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),bestKm);
    } catch(e) {}
    if (window.SmartMobiNative) {
      try {
        const nk = SmartMobiNative.getGpsKm?.();
        if (typeof nk === 'number' && isFinite(nk)) bestKm = Math.max(bestKm,nk);
      } catch(e) {}
    }
    g.st='running'; g.sessionId=ss.id; g.start=Number(ss.start_ms);
    g.km=bestKm; g.pausedMs=Number(ss.paused_ms)||0; g.pa=null;
    g.pts=[]; g.lastFixTime=null; g.forceDate=ss.date||null;
    const d=DB.day(ss.date||DB.today());
    g.baseKm=(d.sessions||[]).reduce((a,x)=>a+(x.km||0),0);
    g.baseElapsed=(d.sessions||[]).reduce((a,x)=>a+(x.elapsed||0),0);
    persistGps();
    if (g.ti) clearInterval(g.ti);
    if (g.wid) { try{navigator.geolocation.clearWatch(g.wid)}catch(e){} }
    g.ti=setInterval(gpsUpdate,1000);
    if (navigator.geolocation) g.wid=navigator.geolocation.watchPosition(gpsOnPosition,null,{enableHighAccuracy:true,maximumAge:3000,timeout:10000});
    try {
      if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
        SmartMobiNative.startFloating?.(g.start,g.km||0);
        SmartMobiNative.updateFloatingStatus?.('running');
      }
    } catch(e) {}
    syncNavButton(); requestWakeLock();
    try{renderJornada()}catch(e){} try{renderHome()}catch(e){}
    // Corrige imediatamente o snapshot defasado da sessão na nuvem.
    _persistGpsCloud().catch(()=>{});
    console.warn('[recoverOpenJourneyFromCloud] jornada aberta recuperada',ss.id,'km',bestKm);
    return true;
  } catch(e) {
    console.warn('[recoverOpenJourneyFromCloud]',e);
    return false;
  } finally { _journeyCloudRecoverBusy=false; }
}
// Tenta após autenticação/carregamento e também quando o app volta ao foreground.
setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),2200);
document.addEventListener('visibilitychange',()=>{
  if (!document.hidden) setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350);
});
window.addEventListener('focus',()=>setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350));

function gpsStart() {
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  _gpsStartCore();
}

async function _gpsStartCore() {
  // Guard: impede reentrada — clique duplo/rápido não pode criar uma
  // segunda jornada sobrepondo a que acabou de iniciar
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  // Feedback visual IMEDIATO: o card e o botão do menu ficam verdes no
  // instante do clique, sem esperar a checagem de continuidade no Supabase
  // (que tem um await de rede e antes atrasava a virada do card pra verde —
  // só mudava depois de tocar no menu). Se a checagem abaixo encontrar uma
  // sessão em aberto, ela apenas reaproveita o mesmo estado 'running'.
  S.gps.st = 'running';
  try { syncNavButton(); } catch(e) {}
  try { if (S.sec === 'jornada') renderJornada(); } catch(e) {}
  // ═══ CONTINUIDADE: antes de criar uma jornada nova do zero, verifica
  // se já existe uma sessão em aberto (sem end_ms) nas últimas 12h. Se
  // existir, RETOMA ela em vez de começar outra — protege contra perda
  // de km quando gpsStart() é chamado de forma inesperada (ex: toque no
  // botão flutuante nativo) enquanto uma jornada anterior ainda deveria
  // estar rodando mas o app local perdeu o estado dela.
  if (_supaUser && navigator.onLine) {
    try {
      const cutoff = Date.now() - 12*3600*1000;
      const { data: open } = await _SUPA.from('sessions').select('*')
        .eq('user_id', _supaUser.id).is('end_ms', null)
        .gte('start_ms', cutoff).order('start_ms', {ascending:false}).limit(1);
      if (open && open.length) {
        const s = open[0];
        const g2 = S.gps;
        g2.st = 'running'; g2.sessionId = s.id; g2.start = s.start_ms;
        g2.km = parseFloat(s.km)||0; g2.pa = null;
        // O serviço Android continua rastreando a jornada em background.
        // Portanto, NUNCA transforma o tempo fora da WebView em pausa automática:
        // isso fazia uma jornada de 1h reaparecer com segundos/minutos ao reabrir.
        // Só respeita pausas explicitamente persistidas (o fluxo atual nem usa pausa).
        g2.pausedMs = s.paused_ms || 0;
        // Usa o maior km conhecido entre a sessão e os segmentos nativos da mesma jornada.
        try {
          const { data: segs } = await _SUPA.from('journey_status_segments')
            .select('last_km,end_km,start_km,last_seen_ms')
            .eq('session_id', s.id).order('last_seen_ms',{ascending:false}).limit(20);
          const segKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),0);
          if (segKm > g2.km) g2.km = segKm;
        } catch(e) {}
        g2.pts = []; g2.lastFixTime = null;
        const day1 = DB.day(s.date || DB.today());
        g2.baseKm = (day1.sessions||[]).reduce((sum,x)=>sum+(x.km||0),0);
        g2.baseElapsed = (day1.sessions||[]).reduce((sum,x)=>sum+(x.elapsed||0),0);
        if (g2.ti) { try{ clearInterval(g2.ti); }catch(e){} }
        if (g2.wid) { try{ navigator.geolocation.clearWatch(g2.wid); }catch(e){} }
        persistGps();
        syncNavButton();
        g2.ti = setInterval(gpsUpdate, 1000);
        if (navigator.geolocation) {
          g2.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
        }
        requestWakeLock();
        // Religa o widget/serviço nativo semeado com o estado da jornada
        // retomada — sem isso o rastreamento em background ficava morto
        if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
          try {
            SmartMobiNative.startFloating?.(g2.start, g2.km || 0);
            SmartMobiNative.updateFloatingStatus?.('running');
          } catch(e) {}
        }
        showToast('Uma jornada em aberto foi encontrada e retomada (não iniciamos outra do zero).', 'info');
        setTimeout(() => { try{renderJornada();}catch(e){} try{renderHome();}catch(e){} }, 80);
        return; // NÃO cria sessão nova — já retomou a existente
      }
    } catch(e) { console.warn('[gpsStart] checagem de continuidade falhou:', e); }
  }
  // Defesa: limpa qualquer interval/watcher residual de uma sessão anterior
  // que não tenha sido corretamente encerrada (evita contagem duplicada de km)
  if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
  if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
  const g = S.gps; g.st='running'; g.start=Date.now(); g.km=0; g.pts=[]; g.pausedMs=0; g.pa=null; g.forceDate=null;
  // Fecha em segundo plano qualquer jornada órfã de verdade (mais de 12h,
  // já não é candidata a retomada acima) — não bloqueia o início desta
  _closeOrphanSessions().then(n => { if (n) showToast(n+' jornada(s) antiga(s) que ficaram aberta(s) foram encerrada(s) automaticamente.', 'info'); }).catch(()=>
===== sessions @ 884697 =====
ndereço sem rua identificável — pulando estimativa:', origin, '/', dest);
    return null;
  }
  const p1 = await _geocodeWithFallback(origin);
  const p2 = await _geocodeWithFallback(dest);
  if (!p1 || !p2) return null;
  // Sanidade: se os pontos geocodificados ficam absurdamente longe um do
  // outro (linha reta > 80km), a geocodificação quase certamente resolveu
  // um endereço ambíguo pro lugar errado — descarta em vez de salvar lixo
  const straightKm = _haversineKm(p1.lat, p1.lon, p2.lat, p2.lon);
  if (straightKm > 80) {
    console.warn('[_estimateTripKm] Descartado — distância implausível:', straightKm.toFixed(1), 'km entre', origin, 'e', dest);
    return null;
  }
  const km = await _routeDistanceOSRM(p1, p2);
  if (km === null) return null;
  // Segunda checagem: a rota real também não deveria passar de ~120km
  // pra corrida urbana local (margem maior que a linha reta, por curvas)
  if (km > 120) {
    console.warn('[_estimateTripKm] Descartado — rota implausível:', km.toFixed(1), 'km');
    return null;
  }
  return parseFloat(km.toFixed(1));
}
function _haversineKm(lat1, lon1, lat2, lon2) {
  const R = 6371;
  const toRad = d => d * Math.PI / 180;
  const dLat = toRad(lat2-lat1), dLon = toRad(lon2-lon1);
  const a = Math.sin(dLat/2)**2 + Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLon/2)**2;
  return R * 2 * Math.asin(Math.sqrt(a));
}
// Fila: processa uma corrida por vez (respeitando o throttle acima),
// atualiza o trip local + Supabase quando o km chegar, e re-renderiza.
const _geoTried = new Set(); // trips já tentadas nesta sessão (evita loop)
function _queueKmEstimate(dateStr, tripId, origin, dest) {
  if (_geoTried.has(tripId)) return;
  _geoTried.add(tripId);
  _geoQueue.push({ dateStr, tripId, origin, dest });
  if (!_geoQueueBusy) _processGeoQueue();
}
// Backfill: corridas que ficaram com km=0 (fila interrompida ao fechar o
// app, rede fora etc.) entram de novo na fila — 1 tentativa por sessão por
// corrida. Roda a cada _syncTick; a guarda _geoTried evita repetição.
function _backfillKmEstimates() {
  try {
    const cutoff = new Date(Date.now()-7*24*3600*1000).toISOString().slice(0,10);
    DB.allDates().filter(d => d >= cutoff).forEach(dateStr => {
      const day = DB.day(dateStr);
      (day.trips||[]).forEach(t => {
        if ((parseFloat(t.km)||0) > 0 || t.kmEstimado || _geoTried.has(t.id)) return;
        const o = t.originFull || t.origin, d = t.destFull || t.dest;
        if (o && d) _queueKmEstimate(dateStr, t.id, o, d);
      });
    });
  } catch(e) { console.warn('[_backfillKmEstimates]', e); }
}
async function _processGeoQueue() {
  _geoQueueBusy = true;
  while (_geoQueue.length) {
    const job = _geoQueue.shift();
    try {
      const km = await _estimateTripKm(job.origin, job.dest);
      if (km !== null) {
        const day = DB.day(job.dateStr);
        const t = (day.trips||[]).find(x => x.id === job.tripId);
        if (t) {
          t.km = km; t.kmEstimado = true;
          DB.saveDay(day);
          if (_supaUser) {
            _SUPA.from("trips").upsert({ id: t.id, user_id: _supaUser.id, km, km_estimado: true },
              { onConflict: "id" }).catch(()=>{});
          }
          try { if (S.sec === "jornada") renderJornada(); if (S.sec === "corridas") renderCorridasActive(); } catch(e) {}
        }
      }
    } catch(e) { console.warn("[_processGeoQueue]", e); }
  }
  _geoQueueBusy = false;
}

function _gcss(){try{return new Set(JSON.parse(localStorage.getItem('sm_gps_closed_set')||'[]'));}catch(e){return new Set();}}
function _addCss(id,endMs){
  try{
    const s=_gcss();s.add(id);
    localStorage.setItem('sm_gps_closed_set',JSON.stringify([...s].slice(-30)));
    localStorage.setItem('sm_gps_last_closed',JSON.stringify({id,end:endMs}));
  }catch(e){}
}

// ── Trava de segurança: nunca permite duas jornadas abertas ao mesmo tempo.
// Roda antes de iniciar uma nova jornada. Se encontrar alguma jornada aberta
// no Supabase (end_ms=null) — de uma aba/instância esquecida, do serviço nativo
// que não parou, ou de um fechamento anterior que falhou — fecha automaticamente
// com os últimos dados que o servidor tem, e avisa o usuário.
async function _closeOrphanSessions() {
  if (!_supaUser || !navigator.onLine) return null;
  try {
    const { data: allOpen } = await _SUPA.from("sessions").select("*")
      .eq("user_id", _supaUser.id).is("end_ms", null);
    if (!allOpen || !allOpen.length) return null;
    const staleCutoff = Date.now() - 10*60*1000; // 10 min sem checkpoint = órfã
    const orphans = allOpen.filter(o => {
      if (o.id === S.gps.sessionId) return false; // nunca fecha a sessão desta própria instância
      const lastSeen = o.updated_at ? new Date(o.updated_at).getTime() : 0;
      return lastSeen < staleCutoff; // só órfã se não recebeu checkpoint recente
    });
    if (!orphans.length) return null;
    for (const o of orphans) {
      const estEnd = o.start_ms + (o.elapsed_secs||0)*1000;
      await _SUPA.from("sessions").upsert({
        id: o.id, user_id: _supaUser.id, end_ms: Math.max(estEnd, o.start_ms+60000),
        is_paused: false, paused_ms: 0
      }, {onConflict:"id"}).catch(()=>{});
      // Atualiza também o histórico local do dia, se existir
      try {
        const day = DB.day(o.date);
        if (day.sessions) {
          const idx = day.sessions.findIndex(s => s.id === o.id);
          const sess = { id:o.id, start:o.start_ms, end:estEnd, km:parseFloat(o.km)||0, elapsed:o.elapsed_secs||0 };
          if (idx >= 0) day.sessions[idx] = sess; else day.sessions.push(sess);
          DB.saveDay(day);
        }
      } catch(e) {}
    }
    console.warn(`[_closeOrphanSessions] ${orphans.length} jornada(s) órfã(s) encerrada(s) automaticamente`);
    return orphans.length;
  } catch(e) { console.error("[_closeOrphanSessions]", e); return null; }
}

// Ponto de entrada real de "iniciar jornada" — agora passa pelo assistente
// (métricas + permissões) antes de chamar a lógica de verdade. Os dois
// lugares que chamavam gpsStart() direto (botão "Iniciar" e o toque no
// ícone central quando idle) não precisaram mudar nada — ganham o
// assistente de graça só por causa dessa troca aqui.

// Recuperação resiliente da jornada: a nuvem é fonte de continuidade quando a
// WebView/localStorage perde o estado, mas o serviço Android continuou ativo.
let _journeyCloudRecoverBusy = false;
async function recoverOpenJourneyFromCloud() {
  if (_journeyCloudRecoverBusy || !_supaUser || !navigator.onLine) return false;
  if (S.gps.st === 'running' || S.gps.st === 'paused') return false;
  _journeyCloudRecoverBusy = true;
  try {
    const cutoff = Date.now() - 16*3600*1000;
    const { data: rows } = await _SUPA.from('sessions').select('*')
      .eq('user_id',_supaUser.id).is('end_ms',null)
      .gte('start_ms',cutoff).order('start_ms',{ascending:false}).limit(1);
    if (!rows || !rows.length) return false;
    const ss = rows[0];
    const g = S.gps;
    let bestKm = parseFloat(ss.km)||0;
    try {
      const { data: segs } = await _SUPA.from('journey_status_segments')
        .select('last_km,end_km,start_km,last_seen_ms')
        .eq('session_id',ss.id).order('last_seen_ms',{ascending:false}).limit(40);
      bestKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),bestKm);
    } catch(e) {}
    if (window.SmartMobiNative) {
      try {
        const nk = SmartMobiNative.getGpsKm?.();
        if (typeof nk === 'number' && isFinite(nk)) bestKm = Math.max(bestKm,nk);
      } catch(e) {}
    }
    g.st='running'; g.sessionId=ss.id; g.start=Number(ss.start_ms);
    g.km=bestKm; g.pausedMs=Number(ss.paused_ms)||0; g.pa=null;
    g.pts=[]; g.lastFixTime=null; g.forceDate=ss.date||null;
    const d=DB.day(ss.date||DB.today());
    g.baseKm=(d.sessions||[]).reduce((a,x)=>a+(x.km||0),0);
    g.baseElapsed=(d.sessions||[]).reduce((a,x)=>a+(x.elapsed||0),0);
    persistGps();
    if (g.ti) clearInterval(g.ti);
    if (g.wid) { try{navigator.geolocation.clearWatch(g.wid)}catch(e){} }
    g.ti=setInterval(gpsUpdate,1000);
    if (navigator.geolocation) g.wid=navigator.geolocation.watchPosition(gpsOnPosition,null,{enableHighAccuracy:true,maximumAge:3000,timeout:10000});
    try {
      if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
        SmartMobiNative.startFloating?.(g.start,g.km||0);
        SmartMobiNative.updateFloatingStatus?.('running');
      }
    } catch(e) {}
    syncNavButton(); requestWakeLock();
    try{renderJornada()}catch(e){} try{renderHome()}catch(e){}
    // Corrige imediatamente o snapshot defasado da sessão na nuvem.
    _persistGpsCloud().catch(()=>{});
    console.warn('[recoverOpenJourneyFromCloud] jornada aberta recuperada',ss.id,'km',bestKm);
    return true;
  } catch(e) {
    console.warn('[recoverOpenJourneyFromCloud]',e);
    return false;
  } finally { _journeyCloudRecoverBusy=false; }
}
// Tenta após autenticação/carregamento e também quando o app volta ao foreground.
setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),2200);
document.addEventListener('visibilitychange',()=>{
  if (!document.hidden) setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350);
});
window.addEventListener('focus',()=>setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350));

function gpsStart() {
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  _gpsStartCore();
}

async function _gpsStartCore() {
  // Guard: impede reentrada — clique duplo/rápido não pode criar uma
  // segunda jornada sobrepondo a que acabou de iniciar
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  // Feedback visual IMEDIATO: o card e o botão do menu ficam verdes no
  // instante do clique, sem esperar a checagem de continuidade no Supabase
  // (que tem um await de rede e antes atrasava a virada do card pra verde —
  // só mudava depois de tocar no menu). Se a checagem abaixo encontrar uma
  // sessão em aberto, ela apenas reaproveita o mesmo estado 'running'.
  S.gps.st = 'running';
  try { syncNavButton(); } catch(e) {}
  try { if (S.sec === 'jornada') renderJornada(); } catch(e) {}
  // ═══ CONTINUIDADE: antes de criar uma jornada nova do zero, verifica
  // se já existe uma sessão em aberto (sem end_ms) nas últimas 12h. Se
  // existir, RETOMA ela em vez de começar outra — protege contra perda
  // de km quando gpsStart() é chamado de forma inesperada (ex: toque no
  // botão flutuante nativo) enquanto uma jornada anterior ainda deveria
  // estar rodando mas o app local perdeu o estado dela.
  if (_supaUser && navigator.onLine) {
    try {
      const cutoff = Date.now() - 12*3600*1000;
      const { data: open } = await _SUPA.from('sessions').select('*')
        .eq('user_id', _supaUser.id).is('end_ms', null)
        .gte('start_ms', cutoff).order('start_ms', {ascending:false}).limit(1);
      if (open && open.length) {
        const s = open[0];
        const g2 = S.gps;
        g2.st = 'running'; g2.sessionId = s.id; g2.start = s.start_ms;
        g2.km = parseFloat(s.km)||0; g2.pa = null;
        // O serviço Android continua rastreando a jornada em background.
        // Portanto, NUNCA transforma o tempo fora da WebView em pausa automática:
        // isso fazia uma jornada de 1h reaparecer com segundos/minutos ao reabrir.
        // Só respeita pausas explicitamente persistidas (o fluxo atual nem usa pausa).
        g2.pausedMs = s.paused_ms || 0;
        // Usa o maior km conhecido entre a sessão e os segmentos nativos da mesma jornada.
        try {
          const { data: segs } = await _SUPA.from('journey_status_segments')
            .select('last_km,end_km,start_km,last_seen_ms')
            .eq('session_id', s.id).order('last_seen_ms',{ascending:false}).limit(20);
          const segKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),0);
          if (segKm > g2.km) g2.km = segKm;
        } catch(e) {}
        g2.pts = []; g2.lastFixTime = null;
        const day1 = DB.day(s.date || DB.today());
        g2.baseKm = (day1.sessions||[]).reduce((sum,x)=>sum+(x.km||0),0);
        g2.baseElapsed = (day1.sessions||[]).reduce((sum,x)=>sum+(x.elapsed||0),0);
        if (g2.ti) { try{ clearInterval(g2.ti); }catch(e){} }
        if (g2.wid) { try{ navigator.geolocation.clearWatch(g2.wid); }catch(e){} }
        persistGps();
        syncNavButton();
        g2.ti = setInterval(gpsUpdate, 1000);
        if (navigator.geolocation) {
          g2.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
        }
        requestWakeLock();
        // Religa o widget/serviço nativo semeado com o estado da jornada
        // retomada — sem isso o rastreamento em background ficava morto
        if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
          try {
            SmartMobiNative.startFloating?.(g2.start, g2.km || 0);
            SmartMobiNative.updateFloatingStatus?.('running');
          } catch(e) {}
        }
        showToast('Uma jornada em aberto foi encontrada e retomada (não iniciamos outra do zero).', 'info');
        setTimeout(() => { try{renderJornada();}catch(e){} try{renderHome();}catch(e){} }, 80);
        return; // NÃO cria sessão nova — já retomou a existente
      }
    } catch(e) { console.warn('[gpsStart] checagem de continuidade falhou:', e); }
  }
  // Defesa: limpa qualquer interval/watcher residual de uma sessão anterior
  // que não tenha sido corretamente encerrada (evita contagem duplicada de km)
  if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
  if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
  const g = S.gps; g.st='running'; g.start=Date.now(); g.km=0; g.pts=[]; g.pausedMs=0; g.pa=null; g.forceDate=null;
  // Fecha em segundo plano qualquer jornada órfã de verdade (mais de 12h,
  // já não é candidata a retomada acima) — não bloqueia o início desta
  _closeOrphanSessions().then(n => { if (n) showToast(n+' jornada(s) antiga(s) que ficaram aberta(s) foram encerrada(s) automaticamente.', 'info'); }).catch(()=>{});
  g.sessionId = DB.uid(); // ID único desta sessão, usado para upsert no cloud
  const day0 = DB.day(DB.today());
  g.baseKm = (day0.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (day0.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);
  // Inicia floating widget nativo (se o usuário não tiver desativado)
  if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      if (typeof SmartMobiNative.startFloating === 'function')
        SmartMobiNative.startFloating(g.start, 0);
      if (typeof SmartMobiNative.updateFloatingStatus === 'function')
        SmartMobiNative.updateFloatingStatus('running');
    } catch(e) {}
  }
  syncNavButton();
  persistGps();
  // Registra imediatamente no Supabase — não espera checkpoint
  if (_supaUser) {
   
===== sessions @ 885010 =====
 reta > 80km), a geocodificação quase certamente resolveu
  // um endereço ambíguo pro lugar errado — descarta em vez de salvar lixo
  const straightKm = _haversineKm(p1.lat, p1.lon, p2.lat, p2.lon);
  if (straightKm > 80) {
    console.warn('[_estimateTripKm] Descartado — distância implausível:', straightKm.toFixed(1), 'km entre', origin, 'e', dest);
    return null;
  }
  const km = await _routeDistanceOSRM(p1, p2);
  if (km === null) return null;
  // Segunda checagem: a rota real também não deveria passar de ~120km
  // pra corrida urbana local (margem maior que a linha reta, por curvas)
  if (km > 120) {
    console.warn('[_estimateTripKm] Descartado — rota implausível:', km.toFixed(1), 'km');
    return null;
  }
  return parseFloat(km.toFixed(1));
}
function _haversineKm(lat1, lon1, lat2, lon2) {
  const R = 6371;
  const toRad = d => d * Math.PI / 180;
  const dLat = toRad(lat2-lat1), dLon = toRad(lon2-lon1);
  const a = Math.sin(dLat/2)**2 + Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLon/2)**2;
  return R * 2 * Math.asin(Math.sqrt(a));
}
// Fila: processa uma corrida por vez (respeitando o throttle acima),
// atualiza o trip local + Supabase quando o km chegar, e re-renderiza.
const _geoTried = new Set(); // trips já tentadas nesta sessão (evita loop)
function _queueKmEstimate(dateStr, tripId, origin, dest) {
  if (_geoTried.has(tripId)) return;
  _geoTried.add(tripId);
  _geoQueue.push({ dateStr, tripId, origin, dest });
  if (!_geoQueueBusy) _processGeoQueue();
}
// Backfill: corridas que ficaram com km=0 (fila interrompida ao fechar o
// app, rede fora etc.) entram de novo na fila — 1 tentativa por sessão por
// corrida. Roda a cada _syncTick; a guarda _geoTried evita repetição.
function _backfillKmEstimates() {
  try {
    const cutoff = new Date(Date.now()-7*24*3600*1000).toISOString().slice(0,10);
    DB.allDates().filter(d => d >= cutoff).forEach(dateStr => {
      const day = DB.day(dateStr);
      (day.trips||[]).forEach(t => {
        if ((parseFloat(t.km)||0) > 0 || t.kmEstimado || _geoTried.has(t.id)) return;
        const o = t.originFull || t.origin, d = t.destFull || t.dest;
        if (o && d) _queueKmEstimate(dateStr, t.id, o, d);
      });
    });
  } catch(e) { console.warn('[_backfillKmEstimates]', e); }
}
async function _processGeoQueue() {
  _geoQueueBusy = true;
  while (_geoQueue.length) {
    const job = _geoQueue.shift();
    try {
      const km = await _estimateTripKm(job.origin, job.dest);
      if (km !== null) {
        const day = DB.day(job.dateStr);
        const t = (day.trips||[]).find(x => x.id === job.tripId);
        if (t) {
          t.km = km; t.kmEstimado = true;
          DB.saveDay(day);
          if (_supaUser) {
            _SUPA.from("trips").upsert({ id: t.id, user_id: _supaUser.id, km, km_estimado: true },
              { onConflict: "id" }).catch(()=>{});
          }
          try { if (S.sec === "jornada") renderJornada(); if (S.sec === "corridas") renderCorridasActive(); } catch(e) {}
        }
      }
    } catch(e) { console.warn("[_processGeoQueue]", e); }
  }
  _geoQueueBusy = false;
}

function _gcss(){try{return new Set(JSON.parse(localStorage.getItem('sm_gps_closed_set')||'[]'));}catch(e){return new Set();}}
function _addCss(id,endMs){
  try{
    const s=_gcss();s.add(id);
    localStorage.setItem('sm_gps_closed_set',JSON.stringify([...s].slice(-30)));
    localStorage.setItem('sm_gps_last_closed',JSON.stringify({id,end:endMs}));
  }catch(e){}
}

// ── Trava de segurança: nunca permite duas jornadas abertas ao mesmo tempo.
// Roda antes de iniciar uma nova jornada. Se encontrar alguma jornada aberta
// no Supabase (end_ms=null) — de uma aba/instância esquecida, do serviço nativo
// que não parou, ou de um fechamento anterior que falhou — fecha automaticamente
// com os últimos dados que o servidor tem, e avisa o usuário.
async function _closeOrphanSessions() {
  if (!_supaUser || !navigator.onLine) return null;
  try {
    const { data: allOpen } = await _SUPA.from("sessions").select("*")
      .eq("user_id", _supaUser.id).is("end_ms", null);
    if (!allOpen || !allOpen.length) return null;
    const staleCutoff = Date.now() - 10*60*1000; // 10 min sem checkpoint = órfã
    const orphans = allOpen.filter(o => {
      if (o.id === S.gps.sessionId) return false; // nunca fecha a sessão desta própria instância
      const lastSeen = o.updated_at ? new Date(o.updated_at).getTime() : 0;
      return lastSeen < staleCutoff; // só órfã se não recebeu checkpoint recente
    });
    if (!orphans.length) return null;
    for (const o of orphans) {
      const estEnd = o.start_ms + (o.elapsed_secs||0)*1000;
      await _SUPA.from("sessions").upsert({
        id: o.id, user_id: _supaUser.id, end_ms: Math.max(estEnd, o.start_ms+60000),
        is_paused: false, paused_ms: 0
      }, {onConflict:"id"}).catch(()=>{});
      // Atualiza também o histórico local do dia, se existir
      try {
        const day = DB.day(o.date);
        if (day.sessions) {
          const idx = day.sessions.findIndex(s => s.id === o.id);
          const sess = { id:o.id, start:o.start_ms, end:estEnd, km:parseFloat(o.km)||0, elapsed:o.elapsed_secs||0 };
          if (idx >= 0) day.sessions[idx] = sess; else day.sessions.push(sess);
          DB.saveDay(day);
        }
      } catch(e) {}
    }
    console.warn(`[_closeOrphanSessions] ${orphans.length} jornada(s) órfã(s) encerrada(s) automaticamente`);
    return orphans.length;
  } catch(e) { console.error("[_closeOrphanSessions]", e); return null; }
}

// Ponto de entrada real de "iniciar jornada" — agora passa pelo assistente
// (métricas + permissões) antes de chamar a lógica de verdade. Os dois
// lugares que chamavam gpsStart() direto (botão "Iniciar" e o toque no
// ícone central quando idle) não precisaram mudar nada — ganham o
// assistente de graça só por causa dessa troca aqui.

// Recuperação resiliente da jornada: a nuvem é fonte de continuidade quando a
// WebView/localStorage perde o estado, mas o serviço Android continuou ativo.
let _journeyCloudRecoverBusy = false;
async function recoverOpenJourneyFromCloud() {
  if (_journeyCloudRecoverBusy || !_supaUser || !navigator.onLine) return false;
  if (S.gps.st === 'running' || S.gps.st === 'paused') return false;
  _journeyCloudRecoverBusy = true;
  try {
    const cutoff = Date.now() - 16*3600*1000;
    const { data: rows } = await _SUPA.from('sessions').select('*')
      .eq('user_id',_supaUser.id).is('end_ms',null)
      .gte('start_ms',cutoff).order('start_ms',{ascending:false}).limit(1);
    if (!rows || !rows.length) return false;
    const ss = rows[0];
    const g = S.gps;
    let bestKm = parseFloat(ss.km)||0;
    try {
      const { data: segs } = await _SUPA.from('journey_status_segments')
        .select('last_km,end_km,start_km,last_seen_ms')
        .eq('session_id',ss.id).order('last_seen_ms',{ascending:false}).limit(40);
      bestKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),bestKm);
    } catch(e) {}
    if (window.SmartMobiNative) {
      try {
        const nk = SmartMobiNative.getGpsKm?.();
        if (typeof nk === 'number' && isFinite(nk)) bestKm = Math.max(bestKm,nk);
      } catch(e) {}
    }
    g.st='running'; g.sessionId=ss.id; g.start=Number(ss.start_ms);
    g.km=bestKm; g.pausedMs=Number(ss.paused_ms)||0; g.pa=null;
    g.pts=[]; g.lastFixTime=null; g.forceDate=ss.date||null;
    const d=DB.day(ss.date||DB.today());
    g.baseKm=(d.sessions||[]).reduce((a,x)=>a+(x.km||0),0);
    g.baseElapsed=(d.sessions||[]).reduce((a,x)=>a+(x.elapsed||0),0);
    persistGps();
    if (g.ti) clearInterval(g.ti);
    if (g.wid) { try{navigator.geolocation.clearWatch(g.wid)}catch(e){} }
    g.ti=setInterval(gpsUpdate,1000);
    if (navigator.geolocation) g.wid=navigator.geolocation.watchPosition(gpsOnPosition,null,{enableHighAccuracy:true,maximumAge:3000,timeout:10000});
    try {
      if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
        SmartMobiNative.startFloating?.(g.start,g.km||0);
        SmartMobiNative.updateFloatingStatus?.('running');
      }
    } catch(e) {}
    syncNavButton(); requestWakeLock();
    try{renderJornada()}catch(e){} try{renderHome()}catch(e){}
    // Corrige imediatamente o snapshot defasado da sessão na nuvem.
    _persistGpsCloud().catch(()=>{});
    console.warn('[recoverOpenJourneyFromCloud] jornada aberta recuperada',ss.id,'km',bestKm);
    return true;
  } catch(e) {
    console.warn('[recoverOpenJourneyFromCloud]',e);
    return false;
  } finally { _journeyCloudRecoverBusy=false; }
}
// Tenta após autenticação/carregamento e também quando o app volta ao foreground.
setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),2200);
document.addEventListener('visibilitychange',()=>{
  if (!document.hidden) setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350);
});
window.addEventListener('focus',()=>setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350));

function gpsStart() {
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  _gpsStartCore();
}

async function _gpsStartCore() {
  // Guard: impede reentrada — clique duplo/rápido não pode criar uma
  // segunda jornada sobrepondo a que acabou de iniciar
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  // Feedback visual IMEDIATO: o card e o botão do menu ficam verdes no
  // instante do clique, sem esperar a checagem de continuidade no Supabase
  // (que tem um await de rede e antes atrasava a virada do card pra verde —
  // só mudava depois de tocar no menu). Se a checagem abaixo encontrar uma
  // sessão em aberto, ela apenas reaproveita o mesmo estado 'running'.
  S.gps.st = 'running';
  try { syncNavButton(); } catch(e) {}
  try { if (S.sec === 'jornada') renderJornada(); } catch(e) {}
  // ═══ CONTINUIDADE: antes de criar uma jornada nova do zero, verifica
  // se já existe uma sessão em aberto (sem end_ms) nas últimas 12h. Se
  // existir, RETOMA ela em vez de começar outra — protege contra perda
  // de km quando gpsStart() é chamado de forma inesperada (ex: toque no
  // botão flutuante nativo) enquanto uma jornada anterior ainda deveria
  // estar rodando mas o app local perdeu o estado dela.
  if (_supaUser && navigator.onLine) {
    try {
      const cutoff = Date.now() - 12*3600*1000;
      const { data: open } = await _SUPA.from('sessions').select('*')
        .eq('user_id', _supaUser.id).is('end_ms', null)
        .gte('start_ms', cutoff).order('start_ms', {ascending:false}).limit(1);
      if (open && open.length) {
        const s = open[0];
        const g2 = S.gps;
        g2.st = 'running'; g2.sessionId = s.id; g2.start = s.start_ms;
        g2.km = parseFloat(s.km)||0; g2.pa = null;
        // O serviço Android continua rastreando a jornada em background.
        // Portanto, NUNCA transforma o tempo fora da WebView em pausa automática:
        // isso fazia uma jornada de 1h reaparecer com segundos/minutos ao reabrir.
        // Só respeita pausas explicitamente persistidas (o fluxo atual nem usa pausa).
        g2.pausedMs = s.paused_ms || 0;
        // Usa o maior km conhecido entre a sessão e os segmentos nativos da mesma jornada.
        try {
          const { data: segs } = await _SUPA.from('journey_status_segments')
            .select('last_km,end_km,start_km,last_seen_ms')
            .eq('session_id', s.id).order('last_seen_ms',{ascending:false}).limit(20);
          const segKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),0);
          if (segKm > g2.km) g2.km = segKm;
        } catch(e) {}
        g2.pts = []; g2.lastFixTime = null;
        const day1 = DB.day(s.date || DB.today());
        g2.baseKm = (day1.sessions||[]).reduce((sum,x)=>sum+(x.km||0),0);
        g2.baseElapsed = (day1.sessions||[]).reduce((sum,x)=>sum+(x.elapsed||0),0);
        if (g2.ti) { try{ clearInterval(g2.ti); }catch(e){} }
        if (g2.wid) { try{ navigator.geolocation.clearWatch(g2.wid); }catch(e){} }
        persistGps();
        syncNavButton();
        g2.ti = setInterval(gpsUpdate, 1000);
        if (navigator.geolocation) {
          g2.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
        }
        requestWakeLock();
        // Religa o widget/serviço nativo semeado com o estado da jornada
        // retomada — sem isso o rastreamento em background ficava morto
        if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
          try {
            SmartMobiNative.startFloating?.(g2.start, g2.km || 0);
            SmartMobiNative.updateFloatingStatus?.('running');
          } catch(e) {}
        }
        showToast('Uma jornada em aberto foi encontrada e retomada (não iniciamos outra do zero).', 'info');
        setTimeout(() => { try{renderJornada();}catch(e){} try{renderHome();}catch(e){} }, 80);
        return; // NÃO cria sessão nova — já retomou a existente
      }
    } catch(e) { console.warn('[gpsStart] checagem de continuidade falhou:', e); }
  }
  // Defesa: limpa qualquer interval/watcher residual de uma sessão anterior
  // que não tenha sido corretamente encerrada (evita contagem duplicada de km)
  if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
  if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
  const g = S.gps; g.st='running'; g.start=Date.now(); g.km=0; g.pts=[]; g.pausedMs=0; g.pa=null; g.forceDate=null;
  // Fecha em segundo plano qualquer jornada órfã de verdade (mais de 12h,
  // já não é candidata a retomada acima) — não bloqueia o início desta
  _closeOrphanSessions().then(n => { if (n) showToast(n+' jornada(s) antiga(s) que ficaram aberta(s) foram encerrada(s) automaticamente.', 'info'); }).catch(()=>{});
  g.sessionId = DB.uid(); // ID único desta sessão, usado para upsert no cloud
  const day0 = DB.day(DB.today());
  g.baseKm = (day0.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (day0.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);
  // Inicia floating widget nativo (se o usuário não tiver desativado)
  if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      if (typeof SmartMobiNative.startFloating === 'function')
        SmartMobiNative.startFloating(g.start, 0);
      if (typeof SmartMobiNative.updateFloatingStatus === 'function')
        SmartMobiNative.updateFloatingStatus('running');
    } catch(e) {}
  }
  syncNavButton();
  persistGps();
  // Registra imediatamente no Supabase — não espera checkpoint
  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: g.sessionId, user_id: _supaUser.id, date: DB.today(),
      start_ms: g.start, end_ms: null, km: 0, elapsed_secs: 0,
      paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) 
===== sessions @ 885528 =====
~120km
  // pra corrida urbana local (margem maior que a linha reta, por curvas)
  if (km > 120) {
    console.warn('[_estimateTripKm] Descartado — rota implausível:', km.toFixed(1), 'km');
    return null;
  }
  return parseFloat(km.toFixed(1));
}
function _haversineKm(lat1, lon1, lat2, lon2) {
  const R = 6371;
  const toRad = d => d * Math.PI / 180;
  const dLat = toRad(lat2-lat1), dLon = toRad(lon2-lon1);
  const a = Math.sin(dLat/2)**2 + Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLon/2)**2;
  return R * 2 * Math.asin(Math.sqrt(a));
}
// Fila: processa uma corrida por vez (respeitando o throttle acima),
// atualiza o trip local + Supabase quando o km chegar, e re-renderiza.
const _geoTried = new Set(); // trips já tentadas nesta sessão (evita loop)
function _queueKmEstimate(dateStr, tripId, origin, dest) {
  if (_geoTried.has(tripId)) return;
  _geoTried.add(tripId);
  _geoQueue.push({ dateStr, tripId, origin, dest });
  if (!_geoQueueBusy) _processGeoQueue();
}
// Backfill: corridas que ficaram com km=0 (fila interrompida ao fechar o
// app, rede fora etc.) entram de novo na fila — 1 tentativa por sessão por
// corrida. Roda a cada _syncTick; a guarda _geoTried evita repetição.
function _backfillKmEstimates() {
  try {
    const cutoff = new Date(Date.now()-7*24*3600*1000).toISOString().slice(0,10);
    DB.allDates().filter(d => d >= cutoff).forEach(dateStr => {
      const day = DB.day(dateStr);
      (day.trips||[]).forEach(t => {
        if ((parseFloat(t.km)||0) > 0 || t.kmEstimado || _geoTried.has(t.id)) return;
        const o = t.originFull || t.origin, d = t.destFull || t.dest;
        if (o && d) _queueKmEstimate(dateStr, t.id, o, d);
      });
    });
  } catch(e) { console.warn('[_backfillKmEstimates]', e); }
}
async function _processGeoQueue() {
  _geoQueueBusy = true;
  while (_geoQueue.length) {
    const job = _geoQueue.shift();
    try {
      const km = await _estimateTripKm(job.origin, job.dest);
      if (km !== null) {
        const day = DB.day(job.dateStr);
        const t = (day.trips||[]).find(x => x.id === job.tripId);
        if (t) {
          t.km = km; t.kmEstimado = true;
          DB.saveDay(day);
          if (_supaUser) {
            _SUPA.from("trips").upsert({ id: t.id, user_id: _supaUser.id, km, km_estimado: true },
              { onConflict: "id" }).catch(()=>{});
          }
          try { if (S.sec === "jornada") renderJornada(); if (S.sec === "corridas") renderCorridasActive(); } catch(e) {}
        }
      }
    } catch(e) { console.warn("[_processGeoQueue]", e); }
  }
  _geoQueueBusy = false;
}

function _gcss(){try{return new Set(JSON.parse(localStorage.getItem('sm_gps_closed_set')||'[]'));}catch(e){return new Set();}}
function _addCss(id,endMs){
  try{
    const s=_gcss();s.add(id);
    localStorage.setItem('sm_gps_closed_set',JSON.stringify([...s].slice(-30)));
    localStorage.setItem('sm_gps_last_closed',JSON.stringify({id,end:endMs}));
  }catch(e){}
}

// ── Trava de segurança: nunca permite duas jornadas abertas ao mesmo tempo.
// Roda antes de iniciar uma nova jornada. Se encontrar alguma jornada aberta
// no Supabase (end_ms=null) — de uma aba/instância esquecida, do serviço nativo
// que não parou, ou de um fechamento anterior que falhou — fecha automaticamente
// com os últimos dados que o servidor tem, e avisa o usuário.
async function _closeOrphanSessions() {
  if (!_supaUser || !navigator.onLine) return null;
  try {
    const { data: allOpen } = await _SUPA.from("sessions").select("*")
      .eq("user_id", _supaUser.id).is("end_ms", null);
    if (!allOpen || !allOpen.length) return null;
    const staleCutoff = Date.now() - 10*60*1000; // 10 min sem checkpoint = órfã
    const orphans = allOpen.filter(o => {
      if (o.id === S.gps.sessionId) return false; // nunca fecha a sessão desta própria instância
      const lastSeen = o.updated_at ? new Date(o.updated_at).getTime() : 0;
      return lastSeen < staleCutoff; // só órfã se não recebeu checkpoint recente
    });
    if (!orphans.length) return null;
    for (const o of orphans) {
      const estEnd = o.start_ms + (o.elapsed_secs||0)*1000;
      await _SUPA.from("sessions").upsert({
        id: o.id, user_id: _supaUser.id, end_ms: Math.max(estEnd, o.start_ms+60000),
        is_paused: false, paused_ms: 0
      }, {onConflict:"id"}).catch(()=>{});
      // Atualiza também o histórico local do dia, se existir
      try {
        const day = DB.day(o.date);
        if (day.sessions) {
          const idx = day.sessions.findIndex(s => s.id === o.id);
          const sess = { id:o.id, start:o.start_ms, end:estEnd, km:parseFloat(o.km)||0, elapsed:o.elapsed_secs||0 };
          if (idx >= 0) day.sessions[idx] = sess; else day.sessions.push(sess);
          DB.saveDay(day);
        }
      } catch(e) {}
    }
    console.warn(`[_closeOrphanSessions] ${orphans.length} jornada(s) órfã(s) encerrada(s) automaticamente`);
    return orphans.length;
  } catch(e) { console.error("[_closeOrphanSessions]", e); return null; }
}

// Ponto de entrada real de "iniciar jornada" — agora passa pelo assistente
// (métricas + permissões) antes de chamar a lógica de verdade. Os dois
// lugares que chamavam gpsStart() direto (botão "Iniciar" e o toque no
// ícone central quando idle) não precisaram mudar nada — ganham o
// assistente de graça só por causa dessa troca aqui.

// Recuperação resiliente da jornada: a nuvem é fonte de continuidade quando a
// WebView/localStorage perde o estado, mas o serviço Android continuou ativo.
let _journeyCloudRecoverBusy = false;
async function recoverOpenJourneyFromCloud() {
  if (_journeyCloudRecoverBusy || !_supaUser || !navigator.onLine) return false;
  if (S.gps.st === 'running' || S.gps.st === 'paused') return false;
  _journeyCloudRecoverBusy = true;
  try {
    const cutoff = Date.now() - 16*3600*1000;
    const { data: rows } = await _SUPA.from('sessions').select('*')
      .eq('user_id',_supaUser.id).is('end_ms',null)
      .gte('start_ms',cutoff).order('start_ms',{ascending:false}).limit(1);
    if (!rows || !rows.length) return false;
    const ss = rows[0];
    const g = S.gps;
    let bestKm = parseFloat(ss.km)||0;
    try {
      const { data: segs } = await _SUPA.from('journey_status_segments')
        .select('last_km,end_km,start_km,last_seen_ms')
        .eq('session_id',ss.id).order('last_seen_ms',{ascending:false}).limit(40);
      bestKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),bestKm);
    } catch(e) {}
    if (window.SmartMobiNative) {
      try {
        const nk = SmartMobiNative.getGpsKm?.();
        if (typeof nk === 'number' && isFinite(nk)) bestKm = Math.max(bestKm,nk);
      } catch(e) {}
    }
    g.st='running'; g.sessionId=ss.id; g.start=Number(ss.start_ms);
    g.km=bestKm; g.pausedMs=Number(ss.paused_ms)||0; g.pa=null;
    g.pts=[]; g.lastFixTime=null; g.forceDate=ss.date||null;
    const d=DB.day(ss.date||DB.today());
    g.baseKm=(d.sessions||[]).reduce((a,x)=>a+(x.km||0),0);
    g.baseElapsed=(d.sessions||[]).reduce((a,x)=>a+(x.elapsed||0),0);
    persistGps();
    if (g.ti) clearInterval(g.ti);
    if (g.wid) { try{navigator.geolocation.clearWatch(g.wid)}catch(e){} }
    g.ti=setInterval(gpsUpdate,1000);
    if (navigator.geolocation) g.wid=navigator.geolocation.watchPosition(gpsOnPosition,null,{enableHighAccuracy:true,maximumAge:3000,timeout:10000});
    try {
      if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
        SmartMobiNative.startFloating?.(g.start,g.km||0);
        SmartMobiNative.updateFloatingStatus?.('running');
      }
    } catch(e) {}
    syncNavButton(); requestWakeLock();
    try{renderJornada()}catch(e){} try{renderHome()}catch(e){}
    // Corrige imediatamente o snapshot defasado da sessão na nuvem.
    _persistGpsCloud().catch(()=>{});
    console.warn('[recoverOpenJourneyFromCloud] jornada aberta recuperada',ss.id,'km',bestKm);
    return true;
  } catch(e) {
    console.warn('[recoverOpenJourneyFromCloud]',e);
    return false;
  } finally { _journeyCloudRecoverBusy=false; }
}
// Tenta após autenticação/carregamento e também quando o app volta ao foreground.
setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),2200);
document.addEventListener('visibilitychange',()=>{
  if (!document.hidden) setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350);
});
window.addEventListener('focus',()=>setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350));

function gpsStart() {
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  _gpsStartCore();
}

async function _gpsStartCore() {
  // Guard: impede reentrada — clique duplo/rápido não pode criar uma
  // segunda jornada sobrepondo a que acabou de iniciar
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  // Feedback visual IMEDIATO: o card e o botão do menu ficam verdes no
  // instante do clique, sem esperar a checagem de continuidade no Supabase
  // (que tem um await de rede e antes atrasava a virada do card pra verde —
  // só mudava depois de tocar no menu). Se a checagem abaixo encontrar uma
  // sessão em aberto, ela apenas reaproveita o mesmo estado 'running'.
  S.gps.st = 'running';
  try { syncNavButton(); } catch(e) {}
  try { if (S.sec === 'jornada') renderJornada(); } catch(e) {}
  // ═══ CONTINUIDADE: antes de criar uma jornada nova do zero, verifica
  // se já existe uma sessão em aberto (sem end_ms) nas últimas 12h. Se
  // existir, RETOMA ela em vez de começar outra — protege contra perda
  // de km quando gpsStart() é chamado de forma inesperada (ex: toque no
  // botão flutuante nativo) enquanto uma jornada anterior ainda deveria
  // estar rodando mas o app local perdeu o estado dela.
  if (_supaUser && navigator.onLine) {
    try {
      const cutoff = Date.now() - 12*3600*1000;
      const { data: open } = await _SUPA.from('sessions').select('*')
        .eq('user_id', _supaUser.id).is('end_ms', null)
        .gte('start_ms', cutoff).order('start_ms', {ascending:false}).limit(1);
      if (open && open.length) {
        const s = open[0];
        const g2 = S.gps;
        g2.st = 'running'; g2.sessionId = s.id; g2.start = s.start_ms;
        g2.km = parseFloat(s.km)||0; g2.pa = null;
        // O serviço Android continua rastreando a jornada em background.
        // Portanto, NUNCA transforma o tempo fora da WebView em pausa automática:
        // isso fazia uma jornada de 1h reaparecer com segundos/minutos ao reabrir.
        // Só respeita pausas explicitamente persistidas (o fluxo atual nem usa pausa).
        g2.pausedMs = s.paused_ms || 0;
        // Usa o maior km conhecido entre a sessão e os segmentos nativos da mesma jornada.
        try {
          const { data: segs } = await _SUPA.from('journey_status_segments')
            .select('last_km,end_km,start_km,last_seen_ms')
            .eq('session_id', s.id).order('last_seen_ms',{ascending:false}).limit(20);
          const segKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),0);
          if (segKm > g2.km) g2.km = segKm;
        } catch(e) {}
        g2.pts = []; g2.lastFixTime = null;
        const day1 = DB.day(s.date || DB.today());
        g2.baseKm = (day1.sessions||[]).reduce((sum,x)=>sum+(x.km||0),0);
        g2.baseElapsed = (day1.sessions||[]).reduce((sum,x)=>sum+(x.elapsed||0),0);
        if (g2.ti) { try{ clearInterval(g2.ti); }catch(e){} }
        if (g2.wid) { try{ navigator.geolocation.clearWatch(g2.wid); }catch(e){} }
        persistGps();
        syncNavButton();
        g2.ti = setInterval(gpsUpdate, 1000);
        if (navigator.geolocation) {
          g2.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
        }
        requestWakeLock();
        // Religa o widget/serviço nativo semeado com o estado da jornada
        // retomada — sem isso o rastreamento em background ficava morto
        if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
          try {
            SmartMobiNative.startFloating?.(g2.start, g2.km || 0);
            SmartMobiNative.updateFloatingStatus?.('running');
          } catch(e) {}
        }
        showToast('Uma jornada em aberto foi encontrada e retomada (não iniciamos outra do zero).', 'info');
        setTimeout(() => { try{renderJornada();}catch(e){} try{renderHome();}catch(e){} }, 80);
        return; // NÃO cria sessão nova — já retomou a existente
      }
    } catch(e) { console.warn('[gpsStart] checagem de continuidade falhou:', e); }
  }
  // Defesa: limpa qualquer interval/watcher residual de uma sessão anterior
  // que não tenha sido corretamente encerrada (evita contagem duplicada de km)
  if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
  if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
  const g = S.gps; g.st='running'; g.start=Date.now(); g.km=0; g.pts=[]; g.pausedMs=0; g.pa=null; g.forceDate=null;
  // Fecha em segundo plano qualquer jornada órfã de verdade (mais de 12h,
  // já não é candidata a retomada acima) — não bloqueia o início desta
  _closeOrphanSessions().then(n => { if (n) showToast(n+' jornada(s) antiga(s) que ficaram aberta(s) foram encerrada(s) automaticamente.', 'info'); }).catch(()=>{});
  g.sessionId = DB.uid(); // ID único desta sessão, usado para upsert no cloud
  const day0 = DB.day(DB.today());
  g.baseKm = (day0.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (day0.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);
  // Inicia floating widget nativo (se o usuário não tiver desativado)
  if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      if (typeof SmartMobiNative.startFloating === 'function')
        SmartMobiNative.startFloating(g.start, 0);
      if (typeof SmartMobiNative.updateFloatingStatus === 'function')
        SmartMobiNative.updateFloatingStatus('running');
    } catch(e) {}
  }
  syncNavButton();
  persistGps();
  // Registra imediatamente no Supabase — não espera checkpoint
  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: g.sessionId, user_id: _supaUser.id, date: DB.today(),
      start_ms: g.start, end_ms: null, km: 0, elapsed_secs: 0,
      paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  // Verifica permissão overlay
  if (window.SmartMobiNative && typeof SmartMobiNative.hasOverlay === 'function') {
    if (!SmartMobiNative.hasOverlay()) {
      setTimeout(() => showToast('Para ver a bolinha flutuante sobre o Uber:\nConfigurações → Apps → MōB → Aparecer sobre outros apps → Ativar', 'info'), 500);
    }
  }
  requestWakeLock();
  // Render forçado, sem depender d
===== sessions @ 886431 =====
eue.push({ dateStr, tripId, origin, dest });
  if (!_geoQueueBusy) _processGeoQueue();
}
// Backfill: corridas que ficaram com km=0 (fila interrompida ao fechar o
// app, rede fora etc.) entram de novo na fila — 1 tentativa por sessão por
// corrida. Roda a cada _syncTick; a guarda _geoTried evita repetição.
function _backfillKmEstimates() {
  try {
    const cutoff = new Date(Date.now()-7*24*3600*1000).toISOString().slice(0,10);
    DB.allDates().filter(d => d >= cutoff).forEach(dateStr => {
      const day = DB.day(dateStr);
      (day.trips||[]).forEach(t => {
        if ((parseFloat(t.km)||0) > 0 || t.kmEstimado || _geoTried.has(t.id)) return;
        const o = t.originFull || t.origin, d = t.destFull || t.dest;
        if (o && d) _queueKmEstimate(dateStr, t.id, o, d);
      });
    });
  } catch(e) { console.warn('[_backfillKmEstimates]', e); }
}
async function _processGeoQueue() {
  _geoQueueBusy = true;
  while (_geoQueue.length) {
    const job = _geoQueue.shift();
    try {
      const km = await _estimateTripKm(job.origin, job.dest);
      if (km !== null) {
        const day = DB.day(job.dateStr);
        const t = (day.trips||[]).find(x => x.id === job.tripId);
        if (t) {
          t.km = km; t.kmEstimado = true;
          DB.saveDay(day);
          if (_supaUser) {
            _SUPA.from("trips").upsert({ id: t.id, user_id: _supaUser.id, km, km_estimado: true },
              { onConflict: "id" }).catch(()=>{});
          }
          try { if (S.sec === "jornada") renderJornada(); if (S.sec === "corridas") renderCorridasActive(); } catch(e) {}
        }
      }
    } catch(e) { console.warn("[_processGeoQueue]", e); }
  }
  _geoQueueBusy = false;
}

function _gcss(){try{return new Set(JSON.parse(localStorage.getItem('sm_gps_closed_set')||'[]'));}catch(e){return new Set();}}
function _addCss(id,endMs){
  try{
    const s=_gcss();s.add(id);
    localStorage.setItem('sm_gps_closed_set',JSON.stringify([...s].slice(-30)));
    localStorage.setItem('sm_gps_last_closed',JSON.stringify({id,end:endMs}));
  }catch(e){}
}

// ── Trava de segurança: nunca permite duas jornadas abertas ao mesmo tempo.
// Roda antes de iniciar uma nova jornada. Se encontrar alguma jornada aberta
// no Supabase (end_ms=null) — de uma aba/instância esquecida, do serviço nativo
// que não parou, ou de um fechamento anterior que falhou — fecha automaticamente
// com os últimos dados que o servidor tem, e avisa o usuário.
async function _closeOrphanSessions() {
  if (!_supaUser || !navigator.onLine) return null;
  try {
    const { data: allOpen } = await _SUPA.from("sessions").select("*")
      .eq("user_id", _supaUser.id).is("end_ms", null);
    if (!allOpen || !allOpen.length) return null;
    const staleCutoff = Date.now() - 10*60*1000; // 10 min sem checkpoint = órfã
    const orphans = allOpen.filter(o => {
      if (o.id === S.gps.sessionId) return false; // nunca fecha a sessão desta própria instância
      const lastSeen = o.updated_at ? new Date(o.updated_at).getTime() : 0;
      return lastSeen < staleCutoff; // só órfã se não recebeu checkpoint recente
    });
    if (!orphans.length) return null;
    for (const o of orphans) {
      const estEnd = o.start_ms + (o.elapsed_secs||0)*1000;
      await _SUPA.from("sessions").upsert({
        id: o.id, user_id: _supaUser.id, end_ms: Math.max(estEnd, o.start_ms+60000),
        is_paused: false, paused_ms: 0
      }, {onConflict:"id"}).catch(()=>{});
      // Atualiza também o histórico local do dia, se existir
      try {
        const day = DB.day(o.date);
        if (day.sessions) {
          const idx = day.sessions.findIndex(s => s.id === o.id);
          const sess = { id:o.id, start:o.start_ms, end:estEnd, km:parseFloat(o.km)||0, elapsed:o.elapsed_secs||0 };
          if (idx >= 0) day.sessions[idx] = sess; else day.sessions.push(sess);
          DB.saveDay(day);
        }
      } catch(e) {}
    }
    console.warn(`[_closeOrphanSessions] ${orphans.length} jornada(s) órfã(s) encerrada(s) automaticamente`);
    return orphans.length;
  } catch(e) { console.error("[_closeOrphanSessions]", e); return null; }
}

// Ponto de entrada real de "iniciar jornada" — agora passa pelo assistente
// (métricas + permissões) antes de chamar a lógica de verdade. Os dois
// lugares que chamavam gpsStart() direto (botão "Iniciar" e o toque no
// ícone central quando idle) não precisaram mudar nada — ganham o
// assistente de graça só por causa dessa troca aqui.

// Recuperação resiliente da jornada: a nuvem é fonte de continuidade quando a
// WebView/localStorage perde o estado, mas o serviço Android continuou ativo.
let _journeyCloudRecoverBusy = false;
async function recoverOpenJourneyFromCloud() {
  if (_journeyCloudRecoverBusy || !_supaUser || !navigator.onLine) return false;
  if (S.gps.st === 'running' || S.gps.st === 'paused') return false;
  _journeyCloudRecoverBusy = true;
  try {
    const cutoff = Date.now() - 16*3600*1000;
    const { data: rows } = await _SUPA.from('sessions').select('*')
      .eq('user_id',_supaUser.id).is('end_ms',null)
      .gte('start_ms',cutoff).order('start_ms',{ascending:false}).limit(1);
    if (!rows || !rows.length) return false;
    const ss = rows[0];
    const g = S.gps;
    let bestKm = parseFloat(ss.km)||0;
    try {
      const { data: segs } = await _SUPA.from('journey_status_segments')
        .select('last_km,end_km,start_km,last_seen_ms')
        .eq('session_id',ss.id).order('last_seen_ms',{ascending:false}).limit(40);
      bestKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),bestKm);
    } catch(e) {}
    if (window.SmartMobiNative) {
      try {
        const nk = SmartMobiNative.getGpsKm?.();
        if (typeof nk === 'number' && isFinite(nk)) bestKm = Math.max(bestKm,nk);
      } catch(e) {}
    }
    g.st='running'; g.sessionId=ss.id; g.start=Number(ss.start_ms);
    g.km=bestKm; g.pausedMs=Number(ss.paused_ms)||0; g.pa=null;
    g.pts=[]; g.lastFixTime=null; g.forceDate=ss.date||null;
    const d=DB.day(ss.date||DB.today());
    g.baseKm=(d.sessions||[]).reduce((a,x)=>a+(x.km||0),0);
    g.baseElapsed=(d.sessions||[]).reduce((a,x)=>a+(x.elapsed||0),0);
    persistGps();
    if (g.ti) clearInterval(g.ti);
    if (g.wid) { try{navigator.geolocation.clearWatch(g.wid)}catch(e){} }
    g.ti=setInterval(gpsUpdate,1000);
    if (navigator.geolocation) g.wid=navigator.geolocation.watchPosition(gpsOnPosition,null,{enableHighAccuracy:true,maximumAge:3000,timeout:10000});
    try {
      if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
        SmartMobiNative.startFloating?.(g.start,g.km||0);
        SmartMobiNative.updateFloatingStatus?.('running');
      }
    } catch(e) {}
    syncNavButton(); requestWakeLock();
    try{renderJornada()}catch(e){} try{renderHome()}catch(e){}
    // Corrige imediatamente o snapshot defasado da sessão na nuvem.
    _persistGpsCloud().catch(()=>{});
    console.warn('[recoverOpenJourneyFromCloud] jornada aberta recuperada',ss.id,'km',bestKm);
    return true;
  } catch(e) {
    console.warn('[recoverOpenJourneyFromCloud]',e);
    return false;
  } finally { _journeyCloudRecoverBusy=false; }
}
// Tenta após autenticação/carregamento e também quando o app volta ao foreground.
setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),2200);
document.addEventListener('visibilitychange',()=>{
  if (!document.hidden) setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350);
});
window.addEventListener('focus',()=>setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350));

function gpsStart() {
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  _gpsStartCore();
}

async function _gpsStartCore() {
  // Guard: impede reentrada — clique duplo/rápido não pode criar uma
  // segunda jornada sobrepondo a que acabou de iniciar
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  // Feedback visual IMEDIATO: o card e o botão do menu ficam verdes no
  // instante do clique, sem esperar a checagem de continuidade no Supabase
  // (que tem um await de rede e antes atrasava a virada do card pra verde —
  // só mudava depois de tocar no menu). Se a checagem abaixo encontrar uma
  // sessão em aberto, ela apenas reaproveita o mesmo estado 'running'.
  S.gps.st = 'running';
  try { syncNavButton(); } catch(e) {}
  try { if (S.sec === 'jornada') renderJornada(); } catch(e) {}
  // ═══ CONTINUIDADE: antes de criar uma jornada nova do zero, verifica
  // se já existe uma sessão em aberto (sem end_ms) nas últimas 12h. Se
  // existir, RETOMA ela em vez de começar outra — protege contra perda
  // de km quando gpsStart() é chamado de forma inesperada (ex: toque no
  // botão flutuante nativo) enquanto uma jornada anterior ainda deveria
  // estar rodando mas o app local perdeu o estado dela.
  if (_supaUser && navigator.onLine) {
    try {
      const cutoff = Date.now() - 12*3600*1000;
      const { data: open } = await _SUPA.from('sessions').select('*')
        .eq('user_id', _supaUser.id).is('end_ms', null)
        .gte('start_ms', cutoff).order('start_ms', {ascending:false}).limit(1);
      if (open && open.length) {
        const s = open[0];
        const g2 = S.gps;
        g2.st = 'running'; g2.sessionId = s.id; g2.start = s.start_ms;
        g2.km = parseFloat(s.km)||0; g2.pa = null;
        // O serviço Android continua rastreando a jornada em background.
        // Portanto, NUNCA transforma o tempo fora da WebView em pausa automática:
        // isso fazia uma jornada de 1h reaparecer com segundos/minutos ao reabrir.
        // Só respeita pausas explicitamente persistidas (o fluxo atual nem usa pausa).
        g2.pausedMs = s.paused_ms || 0;
        // Usa o maior km conhecido entre a sessão e os segmentos nativos da mesma jornada.
        try {
          const { data: segs } = await _SUPA.from('journey_status_segments')
            .select('last_km,end_km,start_km,last_seen_ms')
            .eq('session_id', s.id).order('last_seen_ms',{ascending:false}).limit(20);
          const segKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),0);
          if (segKm > g2.km) g2.km = segKm;
        } catch(e) {}
        g2.pts = []; g2.lastFixTime = null;
        const day1 = DB.day(s.date || DB.today());
        g2.baseKm = (day1.sessions||[]).reduce((sum,x)=>sum+(x.km||0),0);
        g2.baseElapsed = (day1.sessions||[]).reduce((sum,x)=>sum+(x.elapsed||0),0);
        if (g2.ti) { try{ clearInterval(g2.ti); }catch(e){} }
        if (g2.wid) { try{ navigator.geolocation.clearWatch(g2.wid); }catch(e){} }
        persistGps();
        syncNavButton();
        g2.ti = setInterval(gpsUpdate, 1000);
        if (navigator.geolocation) {
          g2.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
        }
        requestWakeLock();
        // Religa o widget/serviço nativo semeado com o estado da jornada
        // retomada — sem isso o rastreamento em background ficava morto
        if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
          try {
            SmartMobiNative.startFloating?.(g2.start, g2.km || 0);
            SmartMobiNative.updateFloatingStatus?.('running');
          } catch(e) {}
        }
        showToast('Uma jornada em aberto foi encontrada e retomada (não iniciamos outra do zero).', 'info');
        setTimeout(() => { try{renderJornada();}catch(e){} try{renderHome();}catch(e){} }, 80);
        return; // NÃO cria sessão nova — já retomou a existente
      }
    } catch(e) { console.warn('[gpsStart] checagem de continuidade falhou:', e); }
  }
  // Defesa: limpa qualquer interval/watcher residual de uma sessão anterior
  // que não tenha sido corretamente encerrada (evita contagem duplicada de km)
  if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
  if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
  const g = S.gps; g.st='running'; g.start=Date.now(); g.km=0; g.pts=[]; g.pausedMs=0; g.pa=null; g.forceDate=null;
  // Fecha em segundo plano qualquer jornada órfã de verdade (mais de 12h,
  // já não é candidata a retomada acima) — não bloqueia o início desta
  _closeOrphanSessions().then(n => { if (n) showToast(n+' jornada(s) antiga(s) que ficaram aberta(s) foram encerrada(s) automaticamente.', 'info'); }).catch(()=>{});
  g.sessionId = DB.uid(); // ID único desta sessão, usado para upsert no cloud
  const day0 = DB.day(DB.today());
  g.baseKm = (day0.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (day0.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);
  // Inicia floating widget nativo (se o usuário não tiver desativado)
  if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      if (typeof SmartMobiNative.startFloating === 'function')
        SmartMobiNative.startFloating(g.start, 0);
      if (typeof SmartMobiNative.updateFloatingStatus === 'function')
        SmartMobiNative.updateFloatingStatus('running');
    } catch(e) {}
  }
  syncNavButton();
  persistGps();
  // Registra imediatamente no Supabase — não espera checkpoint
  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: g.sessionId, user_id: _supaUser.id, date: DB.today(),
      start_ms: g.start, end_ms: null, km: 0, elapsed_secs: 0,
      paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  // Verifica permissão overlay
  if (window.SmartMobiNative && typeof SmartMobiNative.hasOverlay === 'function') {
    if (!SmartMobiNative.hasOverlay()) {
      setTimeout(() => showToast('Para ver a bolinha flutuante sobre o Uber:\nConfigurações → Apps → MōB → Aparecer sobre outros apps → Ativar', 'info'), 500);
    }
  }
  requestWakeLock();
  // Render forçado, sem depender de qual tela está ativa no momento do
  // clique — mesmo padrão robusto já usado no gpsStop()
  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
  }, 80);
}

function confirmStop() {
  showConfirm('Encerrar a jornada agora?', gpsStop, { confirmLabel: 'Encerrar', cancelLabel: 'Cancelar' });
}
function showToast(msg, type) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const icons = {
    success: { bg: '#ECFDF3', color: '#16A34A', svg: '<path d="M20 6 9 17l-5-5"/>' },
    error:   { bg: '#FEF2F2', color: '#EF4444', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 8v4M12 16h.01"/>' },
    info:    { bg: '#EFF6FF', color: '#0A2F6B', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/>' },
  };
  const ic = icons[type] || icons.success;
  const el = document.createElement('di
===== sessions @ 887540 =====
day(job.dateStr);
        const t = (day.trips||[]).find(x => x.id === job.tripId);
        if (t) {
          t.km = km; t.kmEstimado = true;
          DB.saveDay(day);
          if (_supaUser) {
            _SUPA.from("trips").upsert({ id: t.id, user_id: _supaUser.id, km, km_estimado: true },
              { onConflict: "id" }).catch(()=>{});
          }
          try { if (S.sec === "jornada") renderJornada(); if (S.sec === "corridas") renderCorridasActive(); } catch(e) {}
        }
      }
    } catch(e) { console.warn("[_processGeoQueue]", e); }
  }
  _geoQueueBusy = false;
}

function _gcss(){try{return new Set(JSON.parse(localStorage.getItem('sm_gps_closed_set')||'[]'));}catch(e){return new Set();}}
function _addCss(id,endMs){
  try{
    const s=_gcss();s.add(id);
    localStorage.setItem('sm_gps_closed_set',JSON.stringify([...s].slice(-30)));
    localStorage.setItem('sm_gps_last_closed',JSON.stringify({id,end:endMs}));
  }catch(e){}
}

// ── Trava de segurança: nunca permite duas jornadas abertas ao mesmo tempo.
// Roda antes de iniciar uma nova jornada. Se encontrar alguma jornada aberta
// no Supabase (end_ms=null) — de uma aba/instância esquecida, do serviço nativo
// que não parou, ou de um fechamento anterior que falhou — fecha automaticamente
// com os últimos dados que o servidor tem, e avisa o usuário.
async function _closeOrphanSessions() {
  if (!_supaUser || !navigator.onLine) return null;
  try {
    const { data: allOpen } = await _SUPA.from("sessions").select("*")
      .eq("user_id", _supaUser.id).is("end_ms", null);
    if (!allOpen || !allOpen.length) return null;
    const staleCutoff = Date.now() - 10*60*1000; // 10 min sem checkpoint = órfã
    const orphans = allOpen.filter(o => {
      if (o.id === S.gps.sessionId) return false; // nunca fecha a sessão desta própria instância
      const lastSeen = o.updated_at ? new Date(o.updated_at).getTime() : 0;
      return lastSeen < staleCutoff; // só órfã se não recebeu checkpoint recente
    });
    if (!orphans.length) return null;
    for (const o of orphans) {
      const estEnd = o.start_ms + (o.elapsed_secs||0)*1000;
      await _SUPA.from("sessions").upsert({
        id: o.id, user_id: _supaUser.id, end_ms: Math.max(estEnd, o.start_ms+60000),
        is_paused: false, paused_ms: 0
      }, {onConflict:"id"}).catch(()=>{});
      // Atualiza também o histórico local do dia, se existir
      try {
        const day = DB.day(o.date);
        if (day.sessions) {
          const idx = day.sessions.findIndex(s => s.id === o.id);
          const sess = { id:o.id, start:o.start_ms, end:estEnd, km:parseFloat(o.km)||0, elapsed:o.elapsed_secs||0 };
          if (idx >= 0) day.sessions[idx] = sess; else day.sessions.push(sess);
          DB.saveDay(day);
        }
      } catch(e) {}
    }
    console.warn(`[_closeOrphanSessions] ${orphans.length} jornada(s) órfã(s) encerrada(s) automaticamente`);
    return orphans.length;
  } catch(e) { console.error("[_closeOrphanSessions]", e); return null; }
}

// Ponto de entrada real de "iniciar jornada" — agora passa pelo assistente
// (métricas + permissões) antes de chamar a lógica de verdade. Os dois
// lugares que chamavam gpsStart() direto (botão "Iniciar" e o toque no
// ícone central quando idle) não precisaram mudar nada — ganham o
// assistente de graça só por causa dessa troca aqui.

// Recuperação resiliente da jornada: a nuvem é fonte de continuidade quando a
// WebView/localStorage perde o estado, mas o serviço Android continuou ativo.
let _journeyCloudRecoverBusy = false;
async function recoverOpenJourneyFromCloud() {
  if (_journeyCloudRecoverBusy || !_supaUser || !navigator.onLine) return false;
  if (S.gps.st === 'running' || S.gps.st === 'paused') return false;
  _journeyCloudRecoverBusy = true;
  try {
    const cutoff = Date.now() - 16*3600*1000;
    const { data: rows } = await _SUPA.from('sessions').select('*')
      .eq('user_id',_supaUser.id).is('end_ms',null)
      .gte('start_ms',cutoff).order('start_ms',{ascending:false}).limit(1);
    if (!rows || !rows.length) return false;
    const ss = rows[0];
    const g = S.gps;
    let bestKm = parseFloat(ss.km)||0;
    try {
      const { data: segs } = await _SUPA.from('journey_status_segments')
        .select('last_km,end_km,start_km,last_seen_ms')
        .eq('session_id',ss.id).order('last_seen_ms',{ascending:false}).limit(40);
      bestKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),bestKm);
    } catch(e) {}
    if (window.SmartMobiNative) {
      try {
        const nk = SmartMobiNative.getGpsKm?.();
        if (typeof nk === 'number' && isFinite(nk)) bestKm = Math.max(bestKm,nk);
      } catch(e) {}
    }
    g.st='running'; g.sessionId=ss.id; g.start=Number(ss.start_ms);
    g.km=bestKm; g.pausedMs=Number(ss.paused_ms)||0; g.pa=null;
    g.pts=[]; g.lastFixTime=null; g.forceDate=ss.date||null;
    const d=DB.day(ss.date||DB.today());
    g.baseKm=(d.sessions||[]).reduce((a,x)=>a+(x.km||0),0);
    g.baseElapsed=(d.sessions||[]).reduce((a,x)=>a+(x.elapsed||0),0);
    persistGps();
    if (g.ti) clearInterval(g.ti);
    if (g.wid) { try{navigator.geolocation.clearWatch(g.wid)}catch(e){} }
    g.ti=setInterval(gpsUpdate,1000);
    if (navigator.geolocation) g.wid=navigator.geolocation.watchPosition(gpsOnPosition,null,{enableHighAccuracy:true,maximumAge:3000,timeout:10000});
    try {
      if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
        SmartMobiNative.startFloating?.(g.start,g.km||0);
        SmartMobiNative.updateFloatingStatus?.('running');
      }
    } catch(e) {}
    syncNavButton(); requestWakeLock();
    try{renderJornada()}catch(e){} try{renderHome()}catch(e){}
    // Corrige imediatamente o snapshot defasado da sessão na nuvem.
    _persistGpsCloud().catch(()=>{});
    console.warn('[recoverOpenJourneyFromCloud] jornada aberta recuperada',ss.id,'km',bestKm);
    return true;
  } catch(e) {
    console.warn('[recoverOpenJourneyFromCloud]',e);
    return false;
  } finally { _journeyCloudRecoverBusy=false; }
}
// Tenta após autenticação/carregamento e também quando o app volta ao foreground.
setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),2200);
document.addEventListener('visibilitychange',()=>{
  if (!document.hidden) setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350);
});
window.addEventListener('focus',()=>setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350));

function gpsStart() {
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  _gpsStartCore();
}

async function _gpsStartCore() {
  // Guard: impede reentrada — clique duplo/rápido não pode criar uma
  // segunda jornada sobrepondo a que acabou de iniciar
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  // Feedback visual IMEDIATO: o card e o botão do menu ficam verdes no
  // instante do clique, sem esperar a checagem de continuidade no Supabase
  // (que tem um await de rede e antes atrasava a virada do card pra verde —
  // só mudava depois de tocar no menu). Se a checagem abaixo encontrar uma
  // sessão em aberto, ela apenas reaproveita o mesmo estado 'running'.
  S.gps.st = 'running';
  try { syncNavButton(); } catch(e) {}
  try { if (S.sec === 'jornada') renderJornada(); } catch(e) {}
  // ═══ CONTINUIDADE: antes de criar uma jornada nova do zero, verifica
  // se já existe uma sessão em aberto (sem end_ms) nas últimas 12h. Se
  // existir, RETOMA ela em vez de começar outra — protege contra perda
  // de km quando gpsStart() é chamado de forma inesperada (ex: toque no
  // botão flutuante nativo) enquanto uma jornada anterior ainda deveria
  // estar rodando mas o app local perdeu o estado dela.
  if (_supaUser && navigator.onLine) {
    try {
      const cutoff = Date.now() - 12*3600*1000;
      const { data: open } = await _SUPA.from('sessions').select('*')
        .eq('user_id', _supaUser.id).is('end_ms', null)
        .gte('start_ms', cutoff).order('start_ms', {ascending:false}).limit(1);
      if (open && open.length) {
        const s = open[0];
        const g2 = S.gps;
        g2.st = 'running'; g2.sessionId = s.id; g2.start = s.start_ms;
        g2.km = parseFloat(s.km)||0; g2.pa = null;
        // O serviço Android continua rastreando a jornada em background.
        // Portanto, NUNCA transforma o tempo fora da WebView em pausa automática:
        // isso fazia uma jornada de 1h reaparecer com segundos/minutos ao reabrir.
        // Só respeita pausas explicitamente persistidas (o fluxo atual nem usa pausa).
        g2.pausedMs = s.paused_ms || 0;
        // Usa o maior km conhecido entre a sessão e os segmentos nativos da mesma jornada.
        try {
          const { data: segs } = await _SUPA.from('journey_status_segments')
            .select('last_km,end_km,start_km,last_seen_ms')
            .eq('session_id', s.id).order('last_seen_ms',{ascending:false}).limit(20);
          const segKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),0);
          if (segKm > g2.km) g2.km = segKm;
        } catch(e) {}
        g2.pts = []; g2.lastFixTime = null;
        const day1 = DB.day(s.date || DB.today());
        g2.baseKm = (day1.sessions||[]).reduce((sum,x)=>sum+(x.km||0),0);
        g2.baseElapsed = (day1.sessions||[]).reduce((sum,x)=>sum+(x.elapsed||0),0);
        if (g2.ti) { try{ clearInterval(g2.ti); }catch(e){} }
        if (g2.wid) { try{ navigator.geolocation.clearWatch(g2.wid); }catch(e){} }
        persistGps();
        syncNavButton();
        g2.ti = setInterval(gpsUpdate, 1000);
        if (navigator.geolocation) {
          g2.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
        }
        requestWakeLock();
        // Religa o widget/serviço nativo semeado com o estado da jornada
        // retomada — sem isso o rastreamento em background ficava morto
        if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
          try {
            SmartMobiNative.startFloating?.(g2.start, g2.km || 0);
            SmartMobiNative.updateFloatingStatus?.('running');
          } catch(e) {}
        }
        showToast('Uma jornada em aberto foi encontrada e retomada (não iniciamos outra do zero).', 'info');
        setTimeout(() => { try{renderJornada();}catch(e){} try{renderHome();}catch(e){} }, 80);
        return; // NÃO cria sessão nova — já retomou a existente
      }
    } catch(e) { console.warn('[gpsStart] checagem de continuidade falhou:', e); }
  }
  // Defesa: limpa qualquer interval/watcher residual de uma sessão anterior
  // que não tenha sido corretamente encerrada (evita contagem duplicada de km)
  if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
  if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
  const g = S.gps; g.st='running'; g.start=Date.now(); g.km=0; g.pts=[]; g.pausedMs=0; g.pa=null; g.forceDate=null;
  // Fecha em segundo plano qualquer jornada órfã de verdade (mais de 12h,
  // já não é candidata a retomada acima) — não bloqueia o início desta
  _closeOrphanSessions().then(n => { if (n) showToast(n+' jornada(s) antiga(s) que ficaram aberta(s) foram encerrada(s) automaticamente.', 'info'); }).catch(()=>{});
  g.sessionId = DB.uid(); // ID único desta sessão, usado para upsert no cloud
  const day0 = DB.day(DB.today());
  g.baseKm = (day0.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (day0.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);
  // Inicia floating widget nativo (se o usuário não tiver desativado)
  if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      if (typeof SmartMobiNative.startFloating === 'function')
        SmartMobiNative.startFloating(g.start, 0);
      if (typeof SmartMobiNative.updateFloatingStatus === 'function')
        SmartMobiNative.updateFloatingStatus('running');
    } catch(e) {}
  }
  syncNavButton();
  persistGps();
  // Registra imediatamente no Supabase — não espera checkpoint
  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: g.sessionId, user_id: _supaUser.id, date: DB.today(),
      start_ms: g.start, end_ms: null, km: 0, elapsed_secs: 0,
      paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  // Verifica permissão overlay
  if (window.SmartMobiNative && typeof SmartMobiNative.hasOverlay === 'function') {
    if (!SmartMobiNative.hasOverlay()) {
      setTimeout(() => showToast('Para ver a bolinha flutuante sobre o Uber:\nConfigurações → Apps → MōB → Aparecer sobre outros apps → Ativar', 'info'), 500);
    }
  }
  requestWakeLock();
  // Render forçado, sem depender de qual tela está ativa no momento do
  // clique — mesmo padrão robusto já usado no gpsStop()
  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
  }, 80);
}

function confirmStop() {
  showConfirm('Encerrar a jornada agora?', gpsStop, { confirmLabel: 'Encerrar', cancelLabel: 'Cancelar' });
}
function showToast(msg, type) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const icons = {
    success: { bg: '#ECFDF3', color: '#16A34A', svg: '<path d="M20 6 9 17l-5-5"/>' },
    error:   { bg: '#FEF2F2', color: '#EF4444', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 8v4M12 16h.01"/>' },
    info:    { bg: '#EFF6FF', color: '#0A2F6B', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/>' },
  };
  const ic = icons[type] || icons.success;
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(15,23,42,.35)';
  el.innerHTML = `
    <div style="background:#fff;border-radius:26px;padding:32px 26px;max-width:320px;width:100%;text-align:center;box-shadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:60px;height:60px;border-radius:50%;background:${ic.bg};color:${ic.color};display:flex;align-items:center;justify-content:center;margin:0 auto 16px">
        <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round">${ic.svg}</svg>
      </div>
      <div style="font-size:15.5px;font-weight:700;color:#0F172A;line-height:1.45;white-space:pre-line">${msg}</div>
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', () => el.remove());
  setTimeout(() => { if (document.getElementById('app-toast-center') === el) el.remove(); }, 3400);
}
function showEndJourneyToast(km) {
  const existing = document.ge
===== sessions @ 887605 =====
d === job.tripId);
        if (t) {
          t.km = km; t.kmEstimado = true;
          DB.saveDay(day);
          if (_supaUser) {
            _SUPA.from("trips").upsert({ id: t.id, user_id: _supaUser.id, km, km_estimado: true },
              { onConflict: "id" }).catch(()=>{});
          }
          try { if (S.sec === "jornada") renderJornada(); if (S.sec === "corridas") renderCorridasActive(); } catch(e) {}
        }
      }
    } catch(e) { console.warn("[_processGeoQueue]", e); }
  }
  _geoQueueBusy = false;
}

function _gcss(){try{return new Set(JSON.parse(localStorage.getItem('sm_gps_closed_set')||'[]'));}catch(e){return new Set();}}
function _addCss(id,endMs){
  try{
    const s=_gcss();s.add(id);
    localStorage.setItem('sm_gps_closed_set',JSON.stringify([...s].slice(-30)));
    localStorage.setItem('sm_gps_last_closed',JSON.stringify({id,end:endMs}));
  }catch(e){}
}

// ── Trava de segurança: nunca permite duas jornadas abertas ao mesmo tempo.
// Roda antes de iniciar uma nova jornada. Se encontrar alguma jornada aberta
// no Supabase (end_ms=null) — de uma aba/instância esquecida, do serviço nativo
// que não parou, ou de um fechamento anterior que falhou — fecha automaticamente
// com os últimos dados que o servidor tem, e avisa o usuário.
async function _closeOrphanSessions() {
  if (!_supaUser || !navigator.onLine) return null;
  try {
    const { data: allOpen } = await _SUPA.from("sessions").select("*")
      .eq("user_id", _supaUser.id).is("end_ms", null);
    if (!allOpen || !allOpen.length) return null;
    const staleCutoff = Date.now() - 10*60*1000; // 10 min sem checkpoint = órfã
    const orphans = allOpen.filter(o => {
      if (o.id === S.gps.sessionId) return false; // nunca fecha a sessão desta própria instância
      const lastSeen = o.updated_at ? new Date(o.updated_at).getTime() : 0;
      return lastSeen < staleCutoff; // só órfã se não recebeu checkpoint recente
    });
    if (!orphans.length) return null;
    for (const o of orphans) {
      const estEnd = o.start_ms + (o.elapsed_secs||0)*1000;
      await _SUPA.from("sessions").upsert({
        id: o.id, user_id: _supaUser.id, end_ms: Math.max(estEnd, o.start_ms+60000),
        is_paused: false, paused_ms: 0
      }, {onConflict:"id"}).catch(()=>{});
      // Atualiza também o histórico local do dia, se existir
      try {
        const day = DB.day(o.date);
        if (day.sessions) {
          const idx = day.sessions.findIndex(s => s.id === o.id);
          const sess = { id:o.id, start:o.start_ms, end:estEnd, km:parseFloat(o.km)||0, elapsed:o.elapsed_secs||0 };
          if (idx >= 0) day.sessions[idx] = sess; else day.sessions.push(sess);
          DB.saveDay(day);
        }
      } catch(e) {}
    }
    console.warn(`[_closeOrphanSessions] ${orphans.length} jornada(s) órfã(s) encerrada(s) automaticamente`);
    return orphans.length;
  } catch(e) { console.error("[_closeOrphanSessions]", e); return null; }
}

// Ponto de entrada real de "iniciar jornada" — agora passa pelo assistente
// (métricas + permissões) antes de chamar a lógica de verdade. Os dois
// lugares que chamavam gpsStart() direto (botão "Iniciar" e o toque no
// ícone central quando idle) não precisaram mudar nada — ganham o
// assistente de graça só por causa dessa troca aqui.

// Recuperação resiliente da jornada: a nuvem é fonte de continuidade quando a
// WebView/localStorage perde o estado, mas o serviço Android continuou ativo.
let _journeyCloudRecoverBusy = false;
async function recoverOpenJourneyFromCloud() {
  if (_journeyCloudRecoverBusy || !_supaUser || !navigator.onLine) return false;
  if (S.gps.st === 'running' || S.gps.st === 'paused') return false;
  _journeyCloudRecoverBusy = true;
  try {
    const cutoff = Date.now() - 16*3600*1000;
    const { data: rows } = await _SUPA.from('sessions').select('*')
      .eq('user_id',_supaUser.id).is('end_ms',null)
      .gte('start_ms',cutoff).order('start_ms',{ascending:false}).limit(1);
    if (!rows || !rows.length) return false;
    const ss = rows[0];
    const g = S.gps;
    let bestKm = parseFloat(ss.km)||0;
    try {
      const { data: segs } = await _SUPA.from('journey_status_segments')
        .select('last_km,end_km,start_km,last_seen_ms')
        .eq('session_id',ss.id).order('last_seen_ms',{ascending:false}).limit(40);
      bestKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),bestKm);
    } catch(e) {}
    if (window.SmartMobiNative) {
      try {
        const nk = SmartMobiNative.getGpsKm?.();
        if (typeof nk === 'number' && isFinite(nk)) bestKm = Math.max(bestKm,nk);
      } catch(e) {}
    }
    g.st='running'; g.sessionId=ss.id; g.start=Number(ss.start_ms);
    g.km=bestKm; g.pausedMs=Number(ss.paused_ms)||0; g.pa=null;
    g.pts=[]; g.lastFixTime=null; g.forceDate=ss.date||null;
    const d=DB.day(ss.date||DB.today());
    g.baseKm=(d.sessions||[]).reduce((a,x)=>a+(x.km||0),0);
    g.baseElapsed=(d.sessions||[]).reduce((a,x)=>a+(x.elapsed||0),0);
    persistGps();
    if (g.ti) clearInterval(g.ti);
    if (g.wid) { try{navigator.geolocation.clearWatch(g.wid)}catch(e){} }
    g.ti=setInterval(gpsUpdate,1000);
    if (navigator.geolocation) g.wid=navigator.geolocation.watchPosition(gpsOnPosition,null,{enableHighAccuracy:true,maximumAge:3000,timeout:10000});
    try {
      if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
        SmartMobiNative.startFloating?.(g.start,g.km||0);
        SmartMobiNative.updateFloatingStatus?.('running');
      }
    } catch(e) {}
    syncNavButton(); requestWakeLock();
    try{renderJornada()}catch(e){} try{renderHome()}catch(e){}
    // Corrige imediatamente o snapshot defasado da sessão na nuvem.
    _persistGpsCloud().catch(()=>{});
    console.warn('[recoverOpenJourneyFromCloud] jornada aberta recuperada',ss.id,'km',bestKm);
    return true;
  } catch(e) {
    console.warn('[recoverOpenJourneyFromCloud]',e);
    return false;
  } finally { _journeyCloudRecoverBusy=false; }
}
// Tenta após autenticação/carregamento e também quando o app volta ao foreground.
setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),2200);
document.addEventListener('visibilitychange',()=>{
  if (!document.hidden) setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350);
});
window.addEventListener('focus',()=>setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350));

function gpsStart() {
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  _gpsStartCore();
}

async function _gpsStartCore() {
  // Guard: impede reentrada — clique duplo/rápido não pode criar uma
  // segunda jornada sobrepondo a que acabou de iniciar
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  // Feedback visual IMEDIATO: o card e o botão do menu ficam verdes no
  // instante do clique, sem esperar a checagem de continuidade no Supabase
  // (que tem um await de rede e antes atrasava a virada do card pra verde —
  // só mudava depois de tocar no menu). Se a checagem abaixo encontrar uma
  // sessão em aberto, ela apenas reaproveita o mesmo estado 'running'.
  S.gps.st = 'running';
  try { syncNavButton(); } catch(e) {}
  try { if (S.sec === 'jornada') renderJornada(); } catch(e) {}
  // ═══ CONTINUIDADE: antes de criar uma jornada nova do zero, verifica
  // se já existe uma sessão em aberto (sem end_ms) nas últimas 12h. Se
  // existir, RETOMA ela em vez de começar outra — protege contra perda
  // de km quando gpsStart() é chamado de forma inesperada (ex: toque no
  // botão flutuante nativo) enquanto uma jornada anterior ainda deveria
  // estar rodando mas o app local perdeu o estado dela.
  if (_supaUser && navigator.onLine) {
    try {
      const cutoff = Date.now() - 12*3600*1000;
      const { data: open } = await _SUPA.from('sessions').select('*')
        .eq('user_id', _supaUser.id).is('end_ms', null)
        .gte('start_ms', cutoff).order('start_ms', {ascending:false}).limit(1);
      if (open && open.length) {
        const s = open[0];
        const g2 = S.gps;
        g2.st = 'running'; g2.sessionId = s.id; g2.start = s.start_ms;
        g2.km = parseFloat(s.km)||0; g2.pa = null;
        // O serviço Android continua rastreando a jornada em background.
        // Portanto, NUNCA transforma o tempo fora da WebView em pausa automática:
        // isso fazia uma jornada de 1h reaparecer com segundos/minutos ao reabrir.
        // Só respeita pausas explicitamente persistidas (o fluxo atual nem usa pausa).
        g2.pausedMs = s.paused_ms || 0;
        // Usa o maior km conhecido entre a sessão e os segmentos nativos da mesma jornada.
        try {
          const { data: segs } = await _SUPA.from('journey_status_segments')
            .select('last_km,end_km,start_km,last_seen_ms')
            .eq('session_id', s.id).order('last_seen_ms',{ascending:false}).limit(20);
          const segKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),0);
          if (segKm > g2.km) g2.km = segKm;
        } catch(e) {}
        g2.pts = []; g2.lastFixTime = null;
        const day1 = DB.day(s.date || DB.today());
        g2.baseKm = (day1.sessions||[]).reduce((sum,x)=>sum+(x.km||0),0);
        g2.baseElapsed = (day1.sessions||[]).reduce((sum,x)=>sum+(x.elapsed||0),0);
        if (g2.ti) { try{ clearInterval(g2.ti); }catch(e){} }
        if (g2.wid) { try{ navigator.geolocation.clearWatch(g2.wid); }catch(e){} }
        persistGps();
        syncNavButton();
        g2.ti = setInterval(gpsUpdate, 1000);
        if (navigator.geolocation) {
          g2.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
        }
        requestWakeLock();
        // Religa o widget/serviço nativo semeado com o estado da jornada
        // retomada — sem isso o rastreamento em background ficava morto
        if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
          try {
            SmartMobiNative.startFloating?.(g2.start, g2.km || 0);
            SmartMobiNative.updateFloatingStatus?.('running');
          } catch(e) {}
        }
        showToast('Uma jornada em aberto foi encontrada e retomada (não iniciamos outra do zero).', 'info');
        setTimeout(() => { try{renderJornada();}catch(e){} try{renderHome();}catch(e){} }, 80);
        return; // NÃO cria sessão nova — já retomou a existente
      }
    } catch(e) { console.warn('[gpsStart] checagem de continuidade falhou:', e); }
  }
  // Defesa: limpa qualquer interval/watcher residual de uma sessão anterior
  // que não tenha sido corretamente encerrada (evita contagem duplicada de km)
  if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
  if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
  const g = S.gps; g.st='running'; g.start=Date.now(); g.km=0; g.pts=[]; g.pausedMs=0; g.pa=null; g.forceDate=null;
  // Fecha em segundo plano qualquer jornada órfã de verdade (mais de 12h,
  // já não é candidata a retomada acima) — não bloqueia o início desta
  _closeOrphanSessions().then(n => { if (n) showToast(n+' jornada(s) antiga(s) que ficaram aberta(s) foram encerrada(s) automaticamente.', 'info'); }).catch(()=>{});
  g.sessionId = DB.uid(); // ID único desta sessão, usado para upsert no cloud
  const day0 = DB.day(DB.today());
  g.baseKm = (day0.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (day0.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);
  // Inicia floating widget nativo (se o usuário não tiver desativado)
  if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      if (typeof SmartMobiNative.startFloating === 'function')
        SmartMobiNative.startFloating(g.start, 0);
      if (typeof SmartMobiNative.updateFloatingStatus === 'function')
        SmartMobiNative.updateFloatingStatus('running');
    } catch(e) {}
  }
  syncNavButton();
  persistGps();
  // Registra imediatamente no Supabase — não espera checkpoint
  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: g.sessionId, user_id: _supaUser.id, date: DB.today(),
      start_ms: g.start, end_ms: null, km: 0, elapsed_secs: 0,
      paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  // Verifica permissão overlay
  if (window.SmartMobiNative && typeof SmartMobiNative.hasOverlay === 'function') {
    if (!SmartMobiNative.hasOverlay()) {
      setTimeout(() => showToast('Para ver a bolinha flutuante sobre o Uber:\nConfigurações → Apps → MōB → Aparecer sobre outros apps → Ativar', 'info'), 500);
    }
  }
  requestWakeLock();
  // Render forçado, sem depender de qual tela está ativa no momento do
  // clique — mesmo padrão robusto já usado no gpsStop()
  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
  }, 80);
}

function confirmStop() {
  showConfirm('Encerrar a jornada agora?', gpsStop, { confirmLabel: 'Encerrar', cancelLabel: 'Cancelar' });
}
function showToast(msg, type) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const icons = {
    success: { bg: '#ECFDF3', color: '#16A34A', svg: '<path d="M20 6 9 17l-5-5"/>' },
    error:   { bg: '#FEF2F2', color: '#EF4444', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 8v4M12 16h.01"/>' },
    info:    { bg: '#EFF6FF', color: '#0A2F6B', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/>' },
  };
  const ic = icons[type] || icons.success;
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(15,23,42,.35)';
  el.innerHTML = `
    <div style="background:#fff;border-radius:26px;padding:32px 26px;max-width:320px;width:100%;text-align:center;box-shadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:60px;height:60px;border-radius:50%;background:${ic.bg};color:${ic.color};display:flex;align-items:center;justify-content:center;margin:0 auto 16px">
        <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round">${ic.svg}</svg>
      </div>
      <div style="font-size:15.5px;font-weight:700;color:#0F172A;line-height:1.45;white-space:pre-line">${msg}</div>
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', () => el.remove());
  setTimeout(() => { if (document.getElementById('app-toast-center') === el) el.remove(); }, 3400);
}
function showEndJourneyToast(km) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove()
===== sessions @ 890442 =====
omaticamente`);
    return orphans.length;
  } catch(e) { console.error("[_closeOrphanSessions]", e); return null; }
}

// Ponto de entrada real de "iniciar jornada" — agora passa pelo assistente
// (métricas + permissões) antes de chamar a lógica de verdade. Os dois
// lugares que chamavam gpsStart() direto (botão "Iniciar" e o toque no
// ícone central quando idle) não precisaram mudar nada — ganham o
// assistente de graça só por causa dessa troca aqui.

// Recuperação resiliente da jornada: a nuvem é fonte de continuidade quando a
// WebView/localStorage perde o estado, mas o serviço Android continuou ativo.
let _journeyCloudRecoverBusy = false;
async function recoverOpenJourneyFromCloud() {
  if (_journeyCloudRecoverBusy || !_supaUser || !navigator.onLine) return false;
  if (S.gps.st === 'running' || S.gps.st === 'paused') return false;
  _journeyCloudRecoverBusy = true;
  try {
    const cutoff = Date.now() - 16*3600*1000;
    const { data: rows } = await _SUPA.from('sessions').select('*')
      .eq('user_id',_supaUser.id).is('end_ms',null)
      .gte('start_ms',cutoff).order('start_ms',{ascending:false}).limit(1);
    if (!rows || !rows.length) return false;
    const ss = rows[0];
    const g = S.gps;
    let bestKm = parseFloat(ss.km)||0;
    try {
      const { data: segs } = await _SUPA.from('journey_status_segments')
        .select('last_km,end_km,start_km,last_seen_ms')
        .eq('session_id',ss.id).order('last_seen_ms',{ascending:false}).limit(40);
      bestKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),bestKm);
    } catch(e) {}
    if (window.SmartMobiNative) {
      try {
        const nk = SmartMobiNative.getGpsKm?.();
        if (typeof nk === 'number' && isFinite(nk)) bestKm = Math.max(bestKm,nk);
      } catch(e) {}
    }
    g.st='running'; g.sessionId=ss.id; g.start=Number(ss.start_ms);
    g.km=bestKm; g.pausedMs=Number(ss.paused_ms)||0; g.pa=null;
    g.pts=[]; g.lastFixTime=null; g.forceDate=ss.date||null;
    const d=DB.day(ss.date||DB.today());
    g.baseKm=(d.sessions||[]).reduce((a,x)=>a+(x.km||0),0);
    g.baseElapsed=(d.sessions||[]).reduce((a,x)=>a+(x.elapsed||0),0);
    persistGps();
    if (g.ti) clearInterval(g.ti);
    if (g.wid) { try{navigator.geolocation.clearWatch(g.wid)}catch(e){} }
    g.ti=setInterval(gpsUpdate,1000);
    if (navigator.geolocation) g.wid=navigator.geolocation.watchPosition(gpsOnPosition,null,{enableHighAccuracy:true,maximumAge:3000,timeout:10000});
    try {
      if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
        SmartMobiNative.startFloating?.(g.start,g.km||0);
        SmartMobiNative.updateFloatingStatus?.('running');
      }
    } catch(e) {}
    syncNavButton(); requestWakeLock();
    try{renderJornada()}catch(e){} try{renderHome()}catch(e){}
    // Corrige imediatamente o snapshot defasado da sessão na nuvem.
    _persistGpsCloud().catch(()=>{});
    console.warn('[recoverOpenJourneyFromCloud] jornada aberta recuperada',ss.id,'km',bestKm);
    return true;
  } catch(e) {
    console.warn('[recoverOpenJourneyFromCloud]',e);
    return false;
  } finally { _journeyCloudRecoverBusy=false; }
}
// Tenta após autenticação/carregamento e também quando o app volta ao foreground.
setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),2200);
document.addEventListener('visibilitychange',()=>{
  if (!document.hidden) setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350);
});
window.addEventListener('focus',()=>setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350));

function gpsStart() {
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  _gpsStartCore();
}

async function _gpsStartCore() {
  // Guard: impede reentrada — clique duplo/rápido não pode criar uma
  // segunda jornada sobrepondo a que acabou de iniciar
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  // Feedback visual IMEDIATO: o card e o botão do menu ficam verdes no
  // instante do clique, sem esperar a checagem de continuidade no Supabase
  // (que tem um await de rede e antes atrasava a virada do card pra verde —
  // só mudava depois de tocar no menu). Se a checagem abaixo encontrar uma
  // sessão em aberto, ela apenas reaproveita o mesmo estado 'running'.
  S.gps.st = 'running';
  try { syncNavButton(); } catch(e) {}
  try { if (S.sec === 'jornada') renderJornada(); } catch(e) {}
  // ═══ CONTINUIDADE: antes de criar uma jornada nova do zero, verifica
  // se já existe uma sessão em aberto (sem end_ms) nas últimas 12h. Se
  // existir, RETOMA ela em vez de começar outra — protege contra perda
  // de km quando gpsStart() é chamado de forma inesperada (ex: toque no
  // botão flutuante nativo) enquanto uma jornada anterior ainda deveria
  // estar rodando mas o app local perdeu o estado dela.
  if (_supaUser && navigator.onLine) {
    try {
      const cutoff = Date.now() - 12*3600*1000;
      const { data: open } = await _SUPA.from('sessions').select('*')
        .eq('user_id', _supaUser.id).is('end_ms', null)
        .gte('start_ms', cutoff).order('start_ms', {ascending:false}).limit(1);
      if (open && open.length) {
        const s = open[0];
        const g2 = S.gps;
        g2.st = 'running'; g2.sessionId = s.id; g2.start = s.start_ms;
        g2.km = parseFloat(s.km)||0; g2.pa = null;
        // O serviço Android continua rastreando a jornada em background.
        // Portanto, NUNCA transforma o tempo fora da WebView em pausa automática:
        // isso fazia uma jornada de 1h reaparecer com segundos/minutos ao reabrir.
        // Só respeita pausas explicitamente persistidas (o fluxo atual nem usa pausa).
        g2.pausedMs = s.paused_ms || 0;
        // Usa o maior km conhecido entre a sessão e os segmentos nativos da mesma jornada.
        try {
          const { data: segs } = await _SUPA.from('journey_status_segments')
            .select('last_km,end_km,start_km,last_seen_ms')
            .eq('session_id', s.id).order('last_seen_ms',{ascending:false}).limit(20);
          const segKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),0);
          if (segKm > g2.km) g2.km = segKm;
        } catch(e) {}
        g2.pts = []; g2.lastFixTime = null;
        const day1 = DB.day(s.date || DB.today());
        g2.baseKm = (day1.sessions||[]).reduce((sum,x)=>sum+(x.km||0),0);
        g2.baseElapsed = (day1.sessions||[]).reduce((sum,x)=>sum+(x.elapsed||0),0);
        if (g2.ti) { try{ clearInterval(g2.ti); }catch(e){} }
        if (g2.wid) { try{ navigator.geolocation.clearWatch(g2.wid); }catch(e){} }
        persistGps();
        syncNavButton();
        g2.ti = setInterval(gpsUpdate, 1000);
        if (navigator.geolocation) {
          g2.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
        }
        requestWakeLock();
        // Religa o widget/serviço nativo semeado com o estado da jornada
        // retomada — sem isso o rastreamento em background ficava morto
        if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
          try {
            SmartMobiNative.startFloating?.(g2.start, g2.km || 0);
            SmartMobiNative.updateFloatingStatus?.('running');
          } catch(e) {}
        }
        showToast('Uma jornada em aberto foi encontrada e retomada (não iniciamos outra do zero).', 'info');
        setTimeout(() => { try{renderJornada();}catch(e){} try{renderHome();}catch(e){} }, 80);
        return; // NÃO cria sessão nova — já retomou a existente
      }
    } catch(e) { console.warn('[gpsStart] checagem de continuidade falhou:', e); }
  }
  // Defesa: limpa qualquer interval/watcher residual de uma sessão anterior
  // que não tenha sido corretamente encerrada (evita contagem duplicada de km)
  if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
  if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
  const g = S.gps; g.st='running'; g.start=Date.now(); g.km=0; g.pts=[]; g.pausedMs=0; g.pa=null; g.forceDate=null;
  // Fecha em segundo plano qualquer jornada órfã de verdade (mais de 12h,
  // já não é candidata a retomada acima) — não bloqueia o início desta
  _closeOrphanSessions().then(n => { if (n) showToast(n+' jornada(s) antiga(s) que ficaram aberta(s) foram encerrada(s) automaticamente.', 'info'); }).catch(()=>{});
  g.sessionId = DB.uid(); // ID único desta sessão, usado para upsert no cloud
  const day0 = DB.day(DB.today());
  g.baseKm = (day0.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (day0.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);
  // Inicia floating widget nativo (se o usuário não tiver desativado)
  if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      if (typeof SmartMobiNative.startFloating === 'function')
        SmartMobiNative.startFloating(g.start, 0);
      if (typeof SmartMobiNative.updateFloatingStatus === 'function')
        SmartMobiNative.updateFloatingStatus('running');
    } catch(e) {}
  }
  syncNavButton();
  persistGps();
  // Registra imediatamente no Supabase — não espera checkpoint
  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: g.sessionId, user_id: _supaUser.id, date: DB.today(),
      start_ms: g.start, end_ms: null, km: 0, elapsed_secs: 0,
      paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  // Verifica permissão overlay
  if (window.SmartMobiNative && typeof SmartMobiNative.hasOverlay === 'function') {
    if (!SmartMobiNative.hasOverlay()) {
      setTimeout(() => showToast('Para ver a bolinha flutuante sobre o Uber:\nConfigurações → Apps → MōB → Aparecer sobre outros apps → Ativar', 'info'), 500);
    }
  }
  requestWakeLock();
  // Render forçado, sem depender de qual tela está ativa no momento do
  // clique — mesmo padrão robusto já usado no gpsStop()
  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
  }, 80);
}

function confirmStop() {
  showConfirm('Encerrar a jornada agora?', gpsStop, { confirmLabel: 'Encerrar', cancelLabel: 'Cancelar' });
}
function showToast(msg, type) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const icons = {
    success: { bg: '#ECFDF3', color: '#16A34A', svg: '<path d="M20 6 9 17l-5-5"/>' },
    error:   { bg: '#FEF2F2', color: '#EF4444', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 8v4M12 16h.01"/>' },
    info:    { bg: '#EFF6FF', color: '#0A2F6B', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/>' },
  };
  const ic = icons[type] || icons.success;
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(15,23,42,.35)';
  el.innerHTML = `
    <div style="background:#fff;border-radius:26px;padding:32px 26px;max-width:320px;width:100%;text-align:center;box-shadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:60px;height:60px;border-radius:50%;background:${ic.bg};color:${ic.color};display:flex;align-items:center;justify-content:center;margin:0 auto 16px">
        <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round">${ic.svg}</svg>
      </div>
      <div style="font-size:15.5px;font-weight:700;color:#0F172A;line-height:1.45;white-space:pre-line">${msg}</div>
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', () => el.remove());
  setTimeout(() => { if (document.getElementById('app-toast-center') === el) el.remove(); }, 3400);
}
function showEndJourneyToast(km) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(15,23,42,.35)';
  el.innerHTML = `
    <div style="background:#fff;border-radius:26px;padding:36px 28px;max-width:320px;width:100%;text-align:center;box-shadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:68px;height:68px;border-radius:50%;background:#ECFDF3;color:#16A34A;display:flex;align-items:center;justify-content:center;margin:0 auto 18px">
        <svg viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
      </div>
      <div style="font-size:18px;font-weight:800;color:#0F172A;margin-bottom:6px">Jornada encerrada</div>
      <div style="font-size:14.5px;color:#64748B">${km.toFixed(1)} km registrados</div>
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', () => el.remove());
  setTimeout(() => el.remove(), 3500);
}
// Resumo de fechamento de jornada — mostrado ao encerrar. Antes de importar
// as corridas do dia, só temos dados de GPS (tempo, km, km/h médio). Depois
// de importar, mostramos o conjunto completo (ganhos, km ativo/desloc,
// R$/h, R$/km, corridas, Uber/99, meta).
function closeJourneySummary() {
  const el = document.getElementById('app-toast-center');
  if (el) el.remove();
}
function showJourneySummary(sess) {
  const day = DB.day(DB.today());
  // Escopo é a jornada (sessão) que acabou de ser encerrada, não o dia
  // inteiro — se a pessoa já tinha outras sessões hoje, elas não entram
  // aqui (aparecem cada uma no próprio card ao expandir a lista de
  // sessões da tela Jornada).
  sess = sess || (day.sessions || [])[(day.sessions||[]).length - 1] || {};
  const totalElapsed = sess.elapsed || 0;
  const totalKm = sess.km || 0;
  const startMs = sess.start || 0, endMs = sess.end || Date.now();
  // Verifica se a jornada cruzou a meia-noite (iniciou ontem, encerrou hoje)
  const crossedMidnight = startMs && endMs && _ymd(startMs) !== _ymd(endMs);
  const midResolvedKey = 'sm_midnight_resolved_' + (sess.id||'x') + '_' + DB.today();
  const needsMidnight = crossedMidnight && !localStorage.getItem(midResolvedKey);
  // Expõe sess globalmente para os onclick do footer
  window._jsSummarySess = sess;
  // Mesma lógica de sessionItem(): corrida pertence a esta sessão se o
  // horário dela cai dentro da janela [start, end] da sessão.
  const trips = (day.trips || []).filter(t => {
    let ts = t.ts || 0;
    if (!ts && t.time) ts = new Date(day.date + 'T' + t.time + ':00').getTime();
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  const hasTrips = trips.
===== sessions @ 891835 =====
last_seen_ms')
        .eq('session_id',ss.id).order('last_seen_ms',{ascending:false}).limit(40);
      bestKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),bestKm);
    } catch(e) {}
    if (window.SmartMobiNative) {
      try {
        const nk = SmartMobiNative.getGpsKm?.();
        if (typeof nk === 'number' && isFinite(nk)) bestKm = Math.max(bestKm,nk);
      } catch(e) {}
    }
    g.st='running'; g.sessionId=ss.id; g.start=Number(ss.start_ms);
    g.km=bestKm; g.pausedMs=Number(ss.paused_ms)||0; g.pa=null;
    g.pts=[]; g.lastFixTime=null; g.forceDate=ss.date||null;
    const d=DB.day(ss.date||DB.today());
    g.baseKm=(d.sessions||[]).reduce((a,x)=>a+(x.km||0),0);
    g.baseElapsed=(d.sessions||[]).reduce((a,x)=>a+(x.elapsed||0),0);
    persistGps();
    if (g.ti) clearInterval(g.ti);
    if (g.wid) { try{navigator.geolocation.clearWatch(g.wid)}catch(e){} }
    g.ti=setInterval(gpsUpdate,1000);
    if (navigator.geolocation) g.wid=navigator.geolocation.watchPosition(gpsOnPosition,null,{enableHighAccuracy:true,maximumAge:3000,timeout:10000});
    try {
      if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
        SmartMobiNative.startFloating?.(g.start,g.km||0);
        SmartMobiNative.updateFloatingStatus?.('running');
      }
    } catch(e) {}
    syncNavButton(); requestWakeLock();
    try{renderJornada()}catch(e){} try{renderHome()}catch(e){}
    // Corrige imediatamente o snapshot defasado da sessão na nuvem.
    _persistGpsCloud().catch(()=>{});
    console.warn('[recoverOpenJourneyFromCloud] jornada aberta recuperada',ss.id,'km',bestKm);
    return true;
  } catch(e) {
    console.warn('[recoverOpenJourneyFromCloud]',e);
    return false;
  } finally { _journeyCloudRecoverBusy=false; }
}
// Tenta após autenticação/carregamento e também quando o app volta ao foreground.
setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),2200);
document.addEventListener('visibilitychange',()=>{
  if (!document.hidden) setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350);
});
window.addEventListener('focus',()=>setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350));

function gpsStart() {
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  _gpsStartCore();
}

async function _gpsStartCore() {
  // Guard: impede reentrada — clique duplo/rápido não pode criar uma
  // segunda jornada sobrepondo a que acabou de iniciar
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  // Feedback visual IMEDIATO: o card e o botão do menu ficam verdes no
  // instante do clique, sem esperar a checagem de continuidade no Supabase
  // (que tem um await de rede e antes atrasava a virada do card pra verde —
  // só mudava depois de tocar no menu). Se a checagem abaixo encontrar uma
  // sessão em aberto, ela apenas reaproveita o mesmo estado 'running'.
  S.gps.st = 'running';
  try { syncNavButton(); } catch(e) {}
  try { if (S.sec === 'jornada') renderJornada(); } catch(e) {}
  // ═══ CONTINUIDADE: antes de criar uma jornada nova do zero, verifica
  // se já existe uma sessão em aberto (sem end_ms) nas últimas 12h. Se
  // existir, RETOMA ela em vez de começar outra — protege contra perda
  // de km quando gpsStart() é chamado de forma inesperada (ex: toque no
  // botão flutuante nativo) enquanto uma jornada anterior ainda deveria
  // estar rodando mas o app local perdeu o estado dela.
  if (_supaUser && navigator.onLine) {
    try {
      const cutoff = Date.now() - 12*3600*1000;
      const { data: open } = await _SUPA.from('sessions').select('*')
        .eq('user_id', _supaUser.id).is('end_ms', null)
        .gte('start_ms', cutoff).order('start_ms', {ascending:false}).limit(1);
      if (open && open.length) {
        const s = open[0];
        const g2 = S.gps;
        g2.st = 'running'; g2.sessionId = s.id; g2.start = s.start_ms;
        g2.km = parseFloat(s.km)||0; g2.pa = null;
        // O serviço Android continua rastreando a jornada em background.
        // Portanto, NUNCA transforma o tempo fora da WebView em pausa automática:
        // isso fazia uma jornada de 1h reaparecer com segundos/minutos ao reabrir.
        // Só respeita pausas explicitamente persistidas (o fluxo atual nem usa pausa).
        g2.pausedMs = s.paused_ms || 0;
        // Usa o maior km conhecido entre a sessão e os segmentos nativos da mesma jornada.
        try {
          const { data: segs } = await _SUPA.from('journey_status_segments')
            .select('last_km,end_km,start_km,last_seen_ms')
            .eq('session_id', s.id).order('last_seen_ms',{ascending:false}).limit(20);
          const segKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),0);
          if (segKm > g2.km) g2.km = segKm;
        } catch(e) {}
        g2.pts = []; g2.lastFixTime = null;
        const day1 = DB.day(s.date || DB.today());
        g2.baseKm = (day1.sessions||[]).reduce((sum,x)=>sum+(x.km||0),0);
        g2.baseElapsed = (day1.sessions||[]).reduce((sum,x)=>sum+(x.elapsed||0),0);
        if (g2.ti) { try{ clearInterval(g2.ti); }catch(e){} }
        if (g2.wid) { try{ navigator.geolocation.clearWatch(g2.wid); }catch(e){} }
        persistGps();
        syncNavButton();
        g2.ti = setInterval(gpsUpdate, 1000);
        if (navigator.geolocation) {
          g2.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
        }
        requestWakeLock();
        // Religa o widget/serviço nativo semeado com o estado da jornada
        // retomada — sem isso o rastreamento em background ficava morto
        if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
          try {
            SmartMobiNative.startFloating?.(g2.start, g2.km || 0);
            SmartMobiNative.updateFloatingStatus?.('running');
          } catch(e) {}
        }
        showToast('Uma jornada em aberto foi encontrada e retomada (não iniciamos outra do zero).', 'info');
        setTimeout(() => { try{renderJornada();}catch(e){} try{renderHome();}catch(e){} }, 80);
        return; // NÃO cria sessão nova — já retomou a existente
      }
    } catch(e) { console.warn('[gpsStart] checagem de continuidade falhou:', e); }
  }
  // Defesa: limpa qualquer interval/watcher residual de uma sessão anterior
  // que não tenha sido corretamente encerrada (evita contagem duplicada de km)
  if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
  if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
  const g = S.gps; g.st='running'; g.start=Date.now(); g.km=0; g.pts=[]; g.pausedMs=0; g.pa=null; g.forceDate=null;
  // Fecha em segundo plano qualquer jornada órfã de verdade (mais de 12h,
  // já não é candidata a retomada acima) — não bloqueia o início desta
  _closeOrphanSessions().then(n => { if (n) showToast(n+' jornada(s) antiga(s) que ficaram aberta(s) foram encerrada(s) automaticamente.', 'info'); }).catch(()=>{});
  g.sessionId = DB.uid(); // ID único desta sessão, usado para upsert no cloud
  const day0 = DB.day(DB.today());
  g.baseKm = (day0.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (day0.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);
  // Inicia floating widget nativo (se o usuário não tiver desativado)
  if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      if (typeof SmartMobiNative.startFloating === 'function')
        SmartMobiNative.startFloating(g.start, 0);
      if (typeof SmartMobiNative.updateFloatingStatus === 'function')
        SmartMobiNative.updateFloatingStatus('running');
    } catch(e) {}
  }
  syncNavButton();
  persistGps();
  // Registra imediatamente no Supabase — não espera checkpoint
  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: g.sessionId, user_id: _supaUser.id, date: DB.today(),
      start_ms: g.start, end_ms: null, km: 0, elapsed_secs: 0,
      paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  // Verifica permissão overlay
  if (window.SmartMobiNative && typeof SmartMobiNative.hasOverlay === 'function') {
    if (!SmartMobiNative.hasOverlay()) {
      setTimeout(() => showToast('Para ver a bolinha flutuante sobre o Uber:\nConfigurações → Apps → MōB → Aparecer sobre outros apps → Ativar', 'info'), 500);
    }
  }
  requestWakeLock();
  // Render forçado, sem depender de qual tela está ativa no momento do
  // clique — mesmo padrão robusto já usado no gpsStop()
  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
  }, 80);
}

function confirmStop() {
  showConfirm('Encerrar a jornada agora?', gpsStop, { confirmLabel: 'Encerrar', cancelLabel: 'Cancelar' });
}
function showToast(msg, type) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const icons = {
    success: { bg: '#ECFDF3', color: '#16A34A', svg: '<path d="M20 6 9 17l-5-5"/>' },
    error:   { bg: '#FEF2F2', color: '#EF4444', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 8v4M12 16h.01"/>' },
    info:    { bg: '#EFF6FF', color: '#0A2F6B', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/>' },
  };
  const ic = icons[type] || icons.success;
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(15,23,42,.35)';
  el.innerHTML = `
    <div style="background:#fff;border-radius:26px;padding:32px 26px;max-width:320px;width:100%;text-align:center;box-shadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:60px;height:60px;border-radius:50%;background:${ic.bg};color:${ic.color};display:flex;align-items:center;justify-content:center;margin:0 auto 16px">
        <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round">${ic.svg}</svg>
      </div>
      <div style="font-size:15.5px;font-weight:700;color:#0F172A;line-height:1.45;white-space:pre-line">${msg}</div>
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', () => el.remove());
  setTimeout(() => { if (document.getElementById('app-toast-center') === el) el.remove(); }, 3400);
}
function showEndJourneyToast(km) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(15,23,42,.35)';
  el.innerHTML = `
    <div style="background:#fff;border-radius:26px;padding:36px 28px;max-width:320px;width:100%;text-align:center;box-shadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:68px;height:68px;border-radius:50%;background:#ECFDF3;color:#16A34A;display:flex;align-items:center;justify-content:center;margin:0 auto 18px">
        <svg viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
      </div>
      <div style="font-size:18px;font-weight:800;color:#0F172A;margin-bottom:6px">Jornada encerrada</div>
      <div style="font-size:14.5px;color:#64748B">${km.toFixed(1)} km registrados</div>
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', () => el.remove());
  setTimeout(() => el.remove(), 3500);
}
// Resumo de fechamento de jornada — mostrado ao encerrar. Antes de importar
// as corridas do dia, só temos dados de GPS (tempo, km, km/h médio). Depois
// de importar, mostramos o conjunto completo (ganhos, km ativo/desloc,
// R$/h, R$/km, corridas, Uber/99, meta).
function closeJourneySummary() {
  const el = document.getElementById('app-toast-center');
  if (el) el.remove();
}
function showJourneySummary(sess) {
  const day = DB.day(DB.today());
  // Escopo é a jornada (sessão) que acabou de ser encerrada, não o dia
  // inteiro — se a pessoa já tinha outras sessões hoje, elas não entram
  // aqui (aparecem cada uma no próprio card ao expandir a lista de
  // sessões da tela Jornada).
  sess = sess || (day.sessions || [])[(day.sessions||[]).length - 1] || {};
  const totalElapsed = sess.elapsed || 0;
  const totalKm = sess.km || 0;
  const startMs = sess.start || 0, endMs = sess.end || Date.now();
  // Verifica se a jornada cruzou a meia-noite (iniciou ontem, encerrou hoje)
  const crossedMidnight = startMs && endMs && _ymd(startMs) !== _ymd(endMs);
  const midResolvedKey = 'sm_midnight_resolved_' + (sess.id||'x') + '_' + DB.today();
  const needsMidnight = crossedMidnight && !localStorage.getItem(midResolvedKey);
  // Expõe sess globalmente para os onclick do footer
  window._jsSummarySess = sess;
  // Mesma lógica de sessionItem(): corrida pertence a esta sessão se o
  // horário dela cai dentro da janela [start, end] da sessão.
  const trips = (day.trips || []).filter(t => {
    let ts = t.ts || 0;
    if (!ts && t.time) ts = new Date(day.date + 'T' + t.time + ':00').getTime();
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  const hasTrips = trips.length > 0;
  const th = Math.floor(totalElapsed/3600), tm = Math.floor(totalElapsed%3600/60);
  const kmh = totalElapsed > 0 ? (totalKm/(totalElapsed/3600)) : 0;

  const kpiCard = (v,l) => `<div style="background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.1);border-radius:12px;padding:9px 8px;text-align:center">
      <div style="font-family:'Poppins',sans-serif;font-weight:700;font-size:16px">${v}</div>
      <div style="font-size:9.5px;color:rgba(255,255,255,.5);font-weight:600;margin-top:2px;text-transform:uppercase;letter-spacing:.04em">${l}</div>
    </div>`;

  let cardInner, extraSection, footer;

  if (!hasTrips) {
    cardInner = `
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
        <span style="font-size:11px;font-weight:700;color:rgba(255,255,255,.55);text-transform:uppercase;letter-spacing:.06em">Resumo da sessão</span>
        <span style="font-size:10px;font-weight:800;background:rgba(34,197,94,.18);color:#4ADE80;border:1px solid rgba(74,222,128,.35);border-radius:99px;padding:3px 10px">ENCERRADA</span>
      </div>
      <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:32px;line-height:1;letter-spacing:-.01em">${th}h ${p2(tm)}min</div>
      <div style="font-size:11px;color:rgba(255,255,255,.55);font-weight:600;margin-top:4px">tempo online hoje</div>
      <div s
===== sessions @ 893761 =====
nJourneyFromCloud().catch(()=>{}),2200);
document.addEventListener('visibilitychange',()=>{
  if (!document.hidden) setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350);
});
window.addEventListener('focus',()=>setTimeout(()=>recoverOpenJourneyFromCloud().catch(()=>{}),350));

function gpsStart() {
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  _gpsStartCore();
}

async function _gpsStartCore() {
  // Guard: impede reentrada — clique duplo/rápido não pode criar uma
  // segunda jornada sobrepondo a que acabou de iniciar
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  // Feedback visual IMEDIATO: o card e o botão do menu ficam verdes no
  // instante do clique, sem esperar a checagem de continuidade no Supabase
  // (que tem um await de rede e antes atrasava a virada do card pra verde —
  // só mudava depois de tocar no menu). Se a checagem abaixo encontrar uma
  // sessão em aberto, ela apenas reaproveita o mesmo estado 'running'.
  S.gps.st = 'running';
  try { syncNavButton(); } catch(e) {}
  try { if (S.sec === 'jornada') renderJornada(); } catch(e) {}
  // ═══ CONTINUIDADE: antes de criar uma jornada nova do zero, verifica
  // se já existe uma sessão em aberto (sem end_ms) nas últimas 12h. Se
  // existir, RETOMA ela em vez de começar outra — protege contra perda
  // de km quando gpsStart() é chamado de forma inesperada (ex: toque no
  // botão flutuante nativo) enquanto uma jornada anterior ainda deveria
  // estar rodando mas o app local perdeu o estado dela.
  if (_supaUser && navigator.onLine) {
    try {
      const cutoff = Date.now() - 12*3600*1000;
      const { data: open } = await _SUPA.from('sessions').select('*')
        .eq('user_id', _supaUser.id).is('end_ms', null)
        .gte('start_ms', cutoff).order('start_ms', {ascending:false}).limit(1);
      if (open && open.length) {
        const s = open[0];
        const g2 = S.gps;
        g2.st = 'running'; g2.sessionId = s.id; g2.start = s.start_ms;
        g2.km = parseFloat(s.km)||0; g2.pa = null;
        // O serviço Android continua rastreando a jornada em background.
        // Portanto, NUNCA transforma o tempo fora da WebView em pausa automática:
        // isso fazia uma jornada de 1h reaparecer com segundos/minutos ao reabrir.
        // Só respeita pausas explicitamente persistidas (o fluxo atual nem usa pausa).
        g2.pausedMs = s.paused_ms || 0;
        // Usa o maior km conhecido entre a sessão e os segmentos nativos da mesma jornada.
        try {
          const { data: segs } = await _SUPA.from('journey_status_segments')
            .select('last_km,end_km,start_km,last_seen_ms')
            .eq('session_id', s.id).order('last_seen_ms',{ascending:false}).limit(20);
          const segKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),0);
          if (segKm > g2.km) g2.km = segKm;
        } catch(e) {}
        g2.pts = []; g2.lastFixTime = null;
        const day1 = DB.day(s.date || DB.today());
        g2.baseKm = (day1.sessions||[]).reduce((sum,x)=>sum+(x.km||0),0);
        g2.baseElapsed = (day1.sessions||[]).reduce((sum,x)=>sum+(x.elapsed||0),0);
        if (g2.ti) { try{ clearInterval(g2.ti); }catch(e){} }
        if (g2.wid) { try{ navigator.geolocation.clearWatch(g2.wid); }catch(e){} }
        persistGps();
        syncNavButton();
        g2.ti = setInterval(gpsUpdate, 1000);
        if (navigator.geolocation) {
          g2.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
        }
        requestWakeLock();
        // Religa o widget/serviço nativo semeado com o estado da jornada
        // retomada — sem isso o rastreamento em background ficava morto
        if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
          try {
            SmartMobiNative.startFloating?.(g2.start, g2.km || 0);
            SmartMobiNative.updateFloatingStatus?.('running');
          } catch(e) {}
        }
        showToast('Uma jornada em aberto foi encontrada e retomada (não iniciamos outra do zero).', 'info');
        setTimeout(() => { try{renderJornada();}catch(e){} try{renderHome();}catch(e){} }, 80);
        return; // NÃO cria sessão nova — já retomou a existente
      }
    } catch(e) { console.warn('[gpsStart] checagem de continuidade falhou:', e); }
  }
  // Defesa: limpa qualquer interval/watcher residual de uma sessão anterior
  // que não tenha sido corretamente encerrada (evita contagem duplicada de km)
  if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
  if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
  const g = S.gps; g.st='running'; g.start=Date.now(); g.km=0; g.pts=[]; g.pausedMs=0; g.pa=null; g.forceDate=null;
  // Fecha em segundo plano qualquer jornada órfã de verdade (mais de 12h,
  // já não é candidata a retomada acima) — não bloqueia o início desta
  _closeOrphanSessions().then(n => { if (n) showToast(n+' jornada(s) antiga(s) que ficaram aberta(s) foram encerrada(s) automaticamente.', 'info'); }).catch(()=>{});
  g.sessionId = DB.uid(); // ID único desta sessão, usado para upsert no cloud
  const day0 = DB.day(DB.today());
  g.baseKm = (day0.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (day0.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);
  // Inicia floating widget nativo (se o usuário não tiver desativado)
  if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      if (typeof SmartMobiNative.startFloating === 'function')
        SmartMobiNative.startFloating(g.start, 0);
      if (typeof SmartMobiNative.updateFloatingStatus === 'function')
        SmartMobiNative.updateFloatingStatus('running');
    } catch(e) {}
  }
  syncNavButton();
  persistGps();
  // Registra imediatamente no Supabase — não espera checkpoint
  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: g.sessionId, user_id: _supaUser.id, date: DB.today(),
      start_ms: g.start, end_ms: null, km: 0, elapsed_secs: 0,
      paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  // Verifica permissão overlay
  if (window.SmartMobiNative && typeof SmartMobiNative.hasOverlay === 'function') {
    if (!SmartMobiNative.hasOverlay()) {
      setTimeout(() => showToast('Para ver a bolinha flutuante sobre o Uber:\nConfigurações → Apps → MōB → Aparecer sobre outros apps → Ativar', 'info'), 500);
    }
  }
  requestWakeLock();
  // Render forçado, sem depender de qual tela está ativa no momento do
  // clique — mesmo padrão robusto já usado no gpsStop()
  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
  }, 80);
}

function confirmStop() {
  showConfirm('Encerrar a jornada agora?', gpsStop, { confirmLabel: 'Encerrar', cancelLabel: 'Cancelar' });
}
function showToast(msg, type) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const icons = {
    success: { bg: '#ECFDF3', color: '#16A34A', svg: '<path d="M20 6 9 17l-5-5"/>' },
    error:   { bg: '#FEF2F2', color: '#EF4444', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 8v4M12 16h.01"/>' },
    info:    { bg: '#EFF6FF', color: '#0A2F6B', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/>' },
  };
  const ic = icons[type] || icons.success;
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(15,23,42,.35)';
  el.innerHTML = `
    <div style="background:#fff;border-radius:26px;padding:32px 26px;max-width:320px;width:100%;text-align:center;box-shadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:60px;height:60px;border-radius:50%;background:${ic.bg};color:${ic.color};display:flex;align-items:center;justify-content:center;margin:0 auto 16px">
        <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round">${ic.svg}</svg>
      </div>
      <div style="font-size:15.5px;font-weight:700;color:#0F172A;line-height:1.45;white-space:pre-line">${msg}</div>
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', () => el.remove());
  setTimeout(() => { if (document.getElementById('app-toast-center') === el) el.remove(); }, 3400);
}
function showEndJourneyToast(km) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(15,23,42,.35)';
  el.innerHTML = `
    <div style="background:#fff;border-radius:26px;padding:36px 28px;max-width:320px;width:100%;text-align:center;box-shadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:68px;height:68px;border-radius:50%;background:#ECFDF3;color:#16A34A;display:flex;align-items:center;justify-content:center;margin:0 auto 18px">
        <svg viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
      </div>
      <div style="font-size:18px;font-weight:800;color:#0F172A;margin-bottom:6px">Jornada encerrada</div>
      <div style="font-size:14.5px;color:#64748B">${km.toFixed(1)} km registrados</div>
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', () => el.remove());
  setTimeout(() => el.remove(), 3500);
}
// Resumo de fechamento de jornada — mostrado ao encerrar. Antes de importar
// as corridas do dia, só temos dados de GPS (tempo, km, km/h médio). Depois
// de importar, mostramos o conjunto completo (ganhos, km ativo/desloc,
// R$/h, R$/km, corridas, Uber/99, meta).
function closeJourneySummary() {
  const el = document.getElementById('app-toast-center');
  if (el) el.remove();
}
function showJourneySummary(sess) {
  const day = DB.day(DB.today());
  // Escopo é a jornada (sessão) que acabou de ser encerrada, não o dia
  // inteiro — se a pessoa já tinha outras sessões hoje, elas não entram
  // aqui (aparecem cada uma no próprio card ao expandir a lista de
  // sessões da tela Jornada).
  sess = sess || (day.sessions || [])[(day.sessions||[]).length - 1] || {};
  const totalElapsed = sess.elapsed || 0;
  const totalKm = sess.km || 0;
  const startMs = sess.start || 0, endMs = sess.end || Date.now();
  // Verifica se a jornada cruzou a meia-noite (iniciou ontem, encerrou hoje)
  const crossedMidnight = startMs && endMs && _ymd(startMs) !== _ymd(endMs);
  const midResolvedKey = 'sm_midnight_resolved_' + (sess.id||'x') + '_' + DB.today();
  const needsMidnight = crossedMidnight && !localStorage.getItem(midResolvedKey);
  // Expõe sess globalmente para os onclick do footer
  window._jsSummarySess = sess;
  // Mesma lógica de sessionItem(): corrida pertence a esta sessão se o
  // horário dela cai dentro da janela [start, end] da sessão.
  const trips = (day.trips || []).filter(t => {
    let ts = t.ts || 0;
    if (!ts && t.time) ts = new Date(day.date + 'T' + t.time + ':00').getTime();
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  const hasTrips = trips.length > 0;
  const th = Math.floor(totalElapsed/3600), tm = Math.floor(totalElapsed%3600/60);
  const kmh = totalElapsed > 0 ? (totalKm/(totalElapsed/3600)) : 0;

  const kpiCard = (v,l) => `<div style="background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.1);border-radius:12px;padding:9px 8px;text-align:center">
      <div style="font-family:'Poppins',sans-serif;font-weight:700;font-size:16px">${v}</div>
      <div style="font-size:9.5px;color:rgba(255,255,255,.5);font-weight:600;margin-top:2px;text-transform:uppercase;letter-spacing:.04em">${l}</div>
    </div>`;

  let cardInner, extraSection, footer;

  if (!hasTrips) {
    cardInner = `
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
        <span style="font-size:11px;font-weight:700;color:rgba(255,255,255,.55);text-transform:uppercase;letter-spacing:.06em">Resumo da sessão</span>
        <span style="font-size:10px;font-weight:800;background:rgba(34,197,94,.18);color:#4ADE80;border:1px solid rgba(74,222,128,.35);border-radius:99px;padding:3px 10px">ENCERRADA</span>
      </div>
      <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:32px;line-height:1;letter-spacing:-.01em">${th}h ${p2(tm)}min</div>
      <div style="font-size:11px;color:rgba(255,255,255,.55);font-weight:600;margin-top:4px">tempo online hoje</div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px">
        ${totalKm > 0.1 ? kpiCard(totalKm.toFixed(1), 'Km percorrido') : kpiCard('—', 'Km percorrido')}
        ${totalKm > 0.1 ? kpiCard(kmh.toFixed(1), 'Km/h médio') : kpiCard('—', 'Km/h médio')}
      </div>`;
    extraSection = `
      <div class="cv-daily" style="display:flex;gap:10px;background:#FFFBEB;border:1px solid #FDE68A;border-radius:12px;padding:11px 12px;margin:12px 0;align-items:flex-start">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#D97706" stroke-width="2.4" stroke-linecap="round" style="flex:none;margin-top:1px"><circle cx="12" cy="12" r="9"/><path d="M12 8v4l2.5 2.5"/></svg>
        <span style="font-size:12px;color:#92400E;font-weight:500;line-height:1.45">Km ativo, corridas, ganhos, R$/h, R$/km e meta aparecem aqui depois que você <b>importar as corridas de hoje</b>.</span>
      </div>`;
    footer = `
      <button onclick="closeJourneySummary(); ${needsMidnight} ? openPostStopMidnightModal(window._jsSummarySess, openImportPrecheck) : openImportPrecheck()" style="width:100%;height:48px;border:none;border-radius:14px;font-family:'Inter',system-ui,sans-serif;font-size:14px;font-weight:800;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:8px;background:#22C55E;color:#fff;box-shadow:0 6px 16px rgba(34,197,94,.35)">
        <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M7 10l5 5 5-5"/><path d="M12 15V3"/></svg>
        Importar corridas de hoje
      </button>
      <div style="font-size:11px;color:var(--muted);text-align:center;line-height:1.5;margin-top:8px;padding:0 6px">Escolha as fotos/vídeos do histórico Uber ou 99 — pode selecionar vários de uma vez.</div>
      <button onclick="clos
===== sessions @ 894046 =====
;

function gpsStart() {
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  _gpsStartCore();
}

async function _gpsStartCore() {
  // Guard: impede reentrada — clique duplo/rápido não pode criar uma
  // segunda jornada sobrepondo a que acabou de iniciar
  if (S.gps.st === 'running' || S.gps.st === 'paused') return;
  // Feedback visual IMEDIATO: o card e o botão do menu ficam verdes no
  // instante do clique, sem esperar a checagem de continuidade no Supabase
  // (que tem um await de rede e antes atrasava a virada do card pra verde —
  // só mudava depois de tocar no menu). Se a checagem abaixo encontrar uma
  // sessão em aberto, ela apenas reaproveita o mesmo estado 'running'.
  S.gps.st = 'running';
  try { syncNavButton(); } catch(e) {}
  try { if (S.sec === 'jornada') renderJornada(); } catch(e) {}
  // ═══ CONTINUIDADE: antes de criar uma jornada nova do zero, verifica
  // se já existe uma sessão em aberto (sem end_ms) nas últimas 12h. Se
  // existir, RETOMA ela em vez de começar outra — protege contra perda
  // de km quando gpsStart() é chamado de forma inesperada (ex: toque no
  // botão flutuante nativo) enquanto uma jornada anterior ainda deveria
  // estar rodando mas o app local perdeu o estado dela.
  if (_supaUser && navigator.onLine) {
    try {
      const cutoff = Date.now() - 12*3600*1000;
      const { data: open } = await _SUPA.from('sessions').select('*')
        .eq('user_id', _supaUser.id).is('end_ms', null)
        .gte('start_ms', cutoff).order('start_ms', {ascending:false}).limit(1);
      if (open && open.length) {
        const s = open[0];
        const g2 = S.gps;
        g2.st = 'running'; g2.sessionId = s.id; g2.start = s.start_ms;
        g2.km = parseFloat(s.km)||0; g2.pa = null;
        // O serviço Android continua rastreando a jornada em background.
        // Portanto, NUNCA transforma o tempo fora da WebView em pausa automática:
        // isso fazia uma jornada de 1h reaparecer com segundos/minutos ao reabrir.
        // Só respeita pausas explicitamente persistidas (o fluxo atual nem usa pausa).
        g2.pausedMs = s.paused_ms || 0;
        // Usa o maior km conhecido entre a sessão e os segmentos nativos da mesma jornada.
        try {
          const { data: segs } = await _SUPA.from('journey_status_segments')
            .select('last_km,end_km,start_km,last_seen_ms')
            .eq('session_id', s.id).order('last_seen_ms',{ascending:false}).limit(20);
          const segKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),0);
          if (segKm > g2.km) g2.km = segKm;
        } catch(e) {}
        g2.pts = []; g2.lastFixTime = null;
        const day1 = DB.day(s.date || DB.today());
        g2.baseKm = (day1.sessions||[]).reduce((sum,x)=>sum+(x.km||0),0);
        g2.baseElapsed = (day1.sessions||[]).reduce((sum,x)=>sum+(x.elapsed||0),0);
        if (g2.ti) { try{ clearInterval(g2.ti); }catch(e){} }
        if (g2.wid) { try{ navigator.geolocation.clearWatch(g2.wid); }catch(e){} }
        persistGps();
        syncNavButton();
        g2.ti = setInterval(gpsUpdate, 1000);
        if (navigator.geolocation) {
          g2.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
        }
        requestWakeLock();
        // Religa o widget/serviço nativo semeado com o estado da jornada
        // retomada — sem isso o rastreamento em background ficava morto
        if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
          try {
            SmartMobiNative.startFloating?.(g2.start, g2.km || 0);
            SmartMobiNative.updateFloatingStatus?.('running');
          } catch(e) {}
        }
        showToast('Uma jornada em aberto foi encontrada e retomada (não iniciamos outra do zero).', 'info');
        setTimeout(() => { try{renderJornada();}catch(e){} try{renderHome();}catch(e){} }, 80);
        return; // NÃO cria sessão nova — já retomou a existente
      }
    } catch(e) { console.warn('[gpsStart] checagem de continuidade falhou:', e); }
  }
  // Defesa: limpa qualquer interval/watcher residual de uma sessão anterior
  // que não tenha sido corretamente encerrada (evita contagem duplicada de km)
  if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
  if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
  const g = S.gps; g.st='running'; g.start=Date.now(); g.km=0; g.pts=[]; g.pausedMs=0; g.pa=null; g.forceDate=null;
  // Fecha em segundo plano qualquer jornada órfã de verdade (mais de 12h,
  // já não é candidata a retomada acima) — não bloqueia o início desta
  _closeOrphanSessions().then(n => { if (n) showToast(n+' jornada(s) antiga(s) que ficaram aberta(s) foram encerrada(s) automaticamente.', 'info'); }).catch(()=>{});
  g.sessionId = DB.uid(); // ID único desta sessão, usado para upsert no cloud
  const day0 = DB.day(DB.today());
  g.baseKm = (day0.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (day0.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);
  // Inicia floating widget nativo (se o usuário não tiver desativado)
  if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      if (typeof SmartMobiNative.startFloating === 'function')
        SmartMobiNative.startFloating(g.start, 0);
      if (typeof SmartMobiNative.updateFloatingStatus === 'function')
        SmartMobiNative.updateFloatingStatus('running');
    } catch(e) {}
  }
  syncNavButton();
  persistGps();
  // Registra imediatamente no Supabase — não espera checkpoint
  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: g.sessionId, user_id: _supaUser.id, date: DB.today(),
      start_ms: g.start, end_ms: null, km: 0, elapsed_secs: 0,
      paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  // Verifica permissão overlay
  if (window.SmartMobiNative && typeof SmartMobiNative.hasOverlay === 'function') {
    if (!SmartMobiNative.hasOverlay()) {
      setTimeout(() => showToast('Para ver a bolinha flutuante sobre o Uber:\nConfigurações → Apps → MōB → Aparecer sobre outros apps → Ativar', 'info'), 500);
    }
  }
  requestWakeLock();
  // Render forçado, sem depender de qual tela está ativa no momento do
  // clique — mesmo padrão robusto já usado no gpsStop()
  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
  }, 80);
}

function confirmStop() {
  showConfirm('Encerrar a jornada agora?', gpsStop, { confirmLabel: 'Encerrar', cancelLabel: 'Cancelar' });
}
function showToast(msg, type) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const icons = {
    success: { bg: '#ECFDF3', color: '#16A34A', svg: '<path d="M20 6 9 17l-5-5"/>' },
    error:   { bg: '#FEF2F2', color: '#EF4444', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 8v4M12 16h.01"/>' },
    info:    { bg: '#EFF6FF', color: '#0A2F6B', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/>' },
  };
  const ic = icons[type] || icons.success;
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(15,23,42,.35)';
  el.innerHTML = `
    <div style="background:#fff;border-radius:26px;padding:32px 26px;max-width:320px;width:100%;text-align:center;box-shadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:60px;height:60px;border-radius:50%;background:${ic.bg};color:${ic.color};display:flex;align-items:center;justify-content:center;margin:0 auto 16px">
        <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round">${ic.svg}</svg>
      </div>
      <div style="font-size:15.5px;font-weight:700;color:#0F172A;line-height:1.45;white-space:pre-line">${msg}</div>
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', () => el.remove());
  setTimeout(() => { if (document.getElementById('app-toast-center') === el) el.remove(); }, 3400);
}
function showEndJourneyToast(km) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(15,23,42,.35)';
  el.innerHTML = `
    <div style="background:#fff;border-radius:26px;padding:36px 28px;max-width:320px;width:100%;text-align:center;box-shadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:68px;height:68px;border-radius:50%;background:#ECFDF3;color:#16A34A;display:flex;align-items:center;justify-content:center;margin:0 auto 18px">
        <svg viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
      </div>
      <div style="font-size:18px;font-weight:800;color:#0F172A;margin-bottom:6px">Jornada encerrada</div>
      <div style="font-size:14.5px;color:#64748B">${km.toFixed(1)} km registrados</div>
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', () => el.remove());
  setTimeout(() => el.remove(), 3500);
}
// Resumo de fechamento de jornada — mostrado ao encerrar. Antes de importar
// as corridas do dia, só temos dados de GPS (tempo, km, km/h médio). Depois
// de importar, mostramos o conjunto completo (ganhos, km ativo/desloc,
// R$/h, R$/km, corridas, Uber/99, meta).
function closeJourneySummary() {
  const el = document.getElementById('app-toast-center');
  if (el) el.remove();
}
function showJourneySummary(sess) {
  const day = DB.day(DB.today());
  // Escopo é a jornada (sessão) que acabou de ser encerrada, não o dia
  // inteiro — se a pessoa já tinha outras sessões hoje, elas não entram
  // aqui (aparecem cada uma no próprio card ao expandir a lista de
  // sessões da tela Jornada).
  sess = sess || (day.sessions || [])[(day.sessions||[]).length - 1] || {};
  const totalElapsed = sess.elapsed || 0;
  const totalKm = sess.km || 0;
  const startMs = sess.start || 0, endMs = sess.end || Date.now();
  // Verifica se a jornada cruzou a meia-noite (iniciou ontem, encerrou hoje)
  const crossedMidnight = startMs && endMs && _ymd(startMs) !== _ymd(endMs);
  const midResolvedKey = 'sm_midnight_resolved_' + (sess.id||'x') + '_' + DB.today();
  const needsMidnight = crossedMidnight && !localStorage.getItem(midResolvedKey);
  // Expõe sess globalmente para os onclick do footer
  window._jsSummarySess = sess;
  // Mesma lógica de sessionItem(): corrida pertence a esta sessão se o
  // horário dela cai dentro da janela [start, end] da sessão.
  const trips = (day.trips || []).filter(t => {
    let ts = t.ts || 0;
    if (!ts && t.time) ts = new Date(day.date + 'T' + t.time + ':00').getTime();
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  const hasTrips = trips.length > 0;
  const th = Math.floor(totalElapsed/3600), tm = Math.floor(totalElapsed%3600/60);
  const kmh = totalElapsed > 0 ? (totalKm/(totalElapsed/3600)) : 0;

  const kpiCard = (v,l) => `<div style="background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.1);border-radius:12px;padding:9px 8px;text-align:center">
      <div style="font-family:'Poppins',sans-serif;font-weight:700;font-size:16px">${v}</div>
      <div style="font-size:9.5px;color:rgba(255,255,255,.5);font-weight:600;margin-top:2px;text-transform:uppercase;letter-spacing:.04em">${l}</div>
    </div>`;

  let cardInner, extraSection, footer;

  if (!hasTrips) {
    cardInner = `
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
        <span style="font-size:11px;font-weight:700;color:rgba(255,255,255,.55);text-transform:uppercase;letter-spacing:.06em">Resumo da sessão</span>
        <span style="font-size:10px;font-weight:800;background:rgba(34,197,94,.18);color:#4ADE80;border:1px solid rgba(74,222,128,.35);border-radius:99px;padding:3px 10px">ENCERRADA</span>
      </div>
      <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:32px;line-height:1;letter-spacing:-.01em">${th}h ${p2(tm)}min</div>
      <div style="font-size:11px;color:rgba(255,255,255,.55);font-weight:600;margin-top:4px">tempo online hoje</div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px">
        ${totalKm > 0.1 ? kpiCard(totalKm.toFixed(1), 'Km percorrido') : kpiCard('—', 'Km percorrido')}
        ${totalKm > 0.1 ? kpiCard(kmh.toFixed(1), 'Km/h médio') : kpiCard('—', 'Km/h médio')}
      </div>`;
    extraSection = `
      <div class="cv-daily" style="display:flex;gap:10px;background:#FFFBEB;border:1px solid #FDE68A;border-radius:12px;padding:11px 12px;margin:12px 0;align-items:flex-start">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#D97706" stroke-width="2.4" stroke-linecap="round" style="flex:none;margin-top:1px"><circle cx="12" cy="12" r="9"/><path d="M12 8v4l2.5 2.5"/></svg>
        <span style="font-size:12px;color:#92400E;font-weight:500;line-height:1.45">Km ativo, corridas, ganhos, R$/h, R$/km e meta aparecem aqui depois que você <b>importar as corridas de hoje</b>.</span>
      </div>`;
    footer = `
      <button onclick="closeJourneySummary(); ${needsMidnight} ? openPostStopMidnightModal(window._jsSummarySess, openImportPrecheck) : openImportPrecheck()" style="width:100%;height:48px;border:none;border-radius:14px;font-family:'Inter',system-ui,sans-serif;font-size:14px;font-weight:800;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:8px;background:#22C55E;color:#fff;box-shadow:0 6px 16px rgba(34,197,94,.35)">
        <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M7 10l5 5 5-5"/><path d="M12 15V3"/></svg>
        Importar corridas de hoje
      </button>
      <div style="font-size:11px;color:var(--muted);text-align:center;line-height:1.5;margin-top:8px;padding:0 6px">Escolha as fotos/vídeos do histórico Uber ou 99 — pode selecionar vários de uma vez.</div>
      <button onclick="closeJourneySummary(); if(${needsMidnight}) openPostStopMidnightModal(window._jsSummarySess, null)" style="width:100%;height:40px;border:none;background:transparent;color:var(--muted);font-weight:700;font-size:13px;margin-top:4px;cursor:pointer">Deixar para depois</button>`;
  } else {
  
===== sessions @ 894710 =====
as reaproveita o mesmo estado 'running'.
  S.gps.st = 'running';
  try { syncNavButton(); } catch(e) {}
  try { if (S.sec === 'jornada') renderJornada(); } catch(e) {}
  // ═══ CONTINUIDADE: antes de criar uma jornada nova do zero, verifica
  // se já existe uma sessão em aberto (sem end_ms) nas últimas 12h. Se
  // existir, RETOMA ela em vez de começar outra — protege contra perda
  // de km quando gpsStart() é chamado de forma inesperada (ex: toque no
  // botão flutuante nativo) enquanto uma jornada anterior ainda deveria
  // estar rodando mas o app local perdeu o estado dela.
  if (_supaUser && navigator.onLine) {
    try {
      const cutoff = Date.now() - 12*3600*1000;
      const { data: open } = await _SUPA.from('sessions').select('*')
        .eq('user_id', _supaUser.id).is('end_ms', null)
        .gte('start_ms', cutoff).order('start_ms', {ascending:false}).limit(1);
      if (open && open.length) {
        const s = open[0];
        const g2 = S.gps;
        g2.st = 'running'; g2.sessionId = s.id; g2.start = s.start_ms;
        g2.km = parseFloat(s.km)||0; g2.pa = null;
        // O serviço Android continua rastreando a jornada em background.
        // Portanto, NUNCA transforma o tempo fora da WebView em pausa automática:
        // isso fazia uma jornada de 1h reaparecer com segundos/minutos ao reabrir.
        // Só respeita pausas explicitamente persistidas (o fluxo atual nem usa pausa).
        g2.pausedMs = s.paused_ms || 0;
        // Usa o maior km conhecido entre a sessão e os segmentos nativos da mesma jornada.
        try {
          const { data: segs } = await _SUPA.from('journey_status_segments')
            .select('last_km,end_km,start_km,last_seen_ms')
            .eq('session_id', s.id).order('last_seen_ms',{ascending:false}).limit(20);
          const segKm = (segs||[]).reduce((m,x)=>Math.max(m,parseFloat(x.last_km)||0,parseFloat(x.end_km)||0,parseFloat(x.start_km)||0),0);
          if (segKm > g2.km) g2.km = segKm;
        } catch(e) {}
        g2.pts = []; g2.lastFixTime = null;
        const day1 = DB.day(s.date || DB.today());
        g2.baseKm = (day1.sessions||[]).reduce((sum,x)=>sum+(x.km||0),0);
        g2.baseElapsed = (day1.sessions||[]).reduce((sum,x)=>sum+(x.elapsed||0),0);
        if (g2.ti) { try{ clearInterval(g2.ti); }catch(e){} }
        if (g2.wid) { try{ navigator.geolocation.clearWatch(g2.wid); }catch(e){} }
        persistGps();
        syncNavButton();
        g2.ti = setInterval(gpsUpdate, 1000);
        if (navigator.geolocation) {
          g2.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
        }
        requestWakeLock();
        // Religa o widget/serviço nativo semeado com o estado da jornada
        // retomada — sem isso o rastreamento em background ficava morto
        if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
          try {
            SmartMobiNative.startFloating?.(g2.start, g2.km || 0);
            SmartMobiNative.updateFloatingStatus?.('running');
          } catch(e) {}
        }
        showToast('Uma jornada em aberto foi encontrada e retomada (não iniciamos outra do zero).', 'info');
        setTimeout(() => { try{renderJornada();}catch(e){} try{renderHome();}catch(e){} }, 80);
        return; // NÃO cria sessão nova — já retomou a existente
      }
    } catch(e) { console.warn('[gpsStart] checagem de continuidade falhou:', e); }
  }
  // Defesa: limpa qualquer interval/watcher residual de uma sessão anterior
  // que não tenha sido corretamente encerrada (evita contagem duplicada de km)
  if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
  if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
  const g = S.gps; g.st='running'; g.start=Date.now(); g.km=0; g.pts=[]; g.pausedMs=0; g.pa=null; g.forceDate=null;
  // Fecha em segundo plano qualquer jornada órfã de verdade (mais de 12h,
  // já não é candidata a retomada acima) — não bloqueia o início desta
  _closeOrphanSessions().then(n => { if (n) showToast(n+' jornada(s) antiga(s) que ficaram aberta(s) foram encerrada(s) automaticamente.', 'info'); }).catch(()=>{});
  g.sessionId = DB.uid(); // ID único desta sessão, usado para upsert no cloud
  const day0 = DB.day(DB.today());
  g.baseKm = (day0.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (day0.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);
  // Inicia floating widget nativo (se o usuário não tiver desativado)
  if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      if (typeof SmartMobiNative.startFloating === 'function')
        SmartMobiNative.startFloating(g.start, 0);
      if (typeof SmartMobiNative.updateFloatingStatus === 'function')
        SmartMobiNative.updateFloatingStatus('running');
    } catch(e) {}
  }
  syncNavButton();
  persistGps();
  // Registra imediatamente no Supabase — não espera checkpoint
  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: g.sessionId, user_id: _supaUser.id, date: DB.today(),
      start_ms: g.start, end_ms: null, km: 0, elapsed_secs: 0,
      paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  // Verifica permissão overlay
  if (window.SmartMobiNative && typeof SmartMobiNative.hasOverlay === 'function') {
    if (!SmartMobiNative.hasOverlay()) {
      setTimeout(() => showToast('Para ver a bolinha flutuante sobre o Uber:\nConfigurações → Apps → MōB → Aparecer sobre outros apps → Ativar', 'info'), 500);
    }
  }
  requestWakeLock();
  // Render forçado, sem depender de qual tela está ativa no momento do
  // clique — mesmo padrão robusto já usado no gpsStop()
  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
  }, 80);
}

function confirmStop() {
  showConfirm('Encerrar a jornada agora?', gpsStop, { confirmLabel: 'Encerrar', cancelLabel: 'Cancelar' });
}
function showToast(msg, type) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const icons = {
    success: { bg: '#ECFDF3', color: '#16A34A', svg: '<path d="M20 6 9 17l-5-5"/>' },
    error:   { bg: '#FEF2F2', color: '#EF4444', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 8v4M12 16h.01"/>' },
    info:    { bg: '#EFF6FF', color: '#0A2F6B', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/>' },
  };
  const ic = icons[type] || icons.success;
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(15,23,42,.35)';
  el.innerHTML = `
    <div style="background:#fff;border-radius:26px;padding:32px 26px;max-width:320px;width:100%;text-align:center;box-shadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:60px;height:60px;border-radius:50%;background:${ic.bg};color:${ic.color};display:flex;align-items:center;justify-content:center;margin:0 auto 16px">
        <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round">${ic.svg}</svg>
      </div>
      <div style="font-size:15.5px;font-weight:700;color:#0F172A;line-height:1.45;white-space:pre-line">${msg}</div>
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', () => el.remove());
  setTimeout(() => { if (document.getElementById('app-toast-center') === el) el.remove(); }, 3400);
}
function showEndJourneyToast(km) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(15,23,42,.35)';
  el.innerHTML = `
    <div style="background:#fff;border-radius:26px;padding:36px 28px;max-width:320px;width:100%;text-align:center;box-shadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:68px;height:68px;border-radius:50%;background:#ECFDF3;color:#16A34A;display:flex;align-items:center;justify-content:center;margin:0 auto 18px">
        <svg viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
      </div>
      <div style="font-size:18px;font-weight:800;color:#0F172A;margin-bottom:6px">Jornada encerrada</div>
      <div style="font-size:14.5px;color:#64748B">${km.toFixed(1)} km registrados</div>
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', () => el.remove());
  setTimeout(() => el.remove(), 3500);
}
// Resumo de fechamento de jornada — mostrado ao encerrar. Antes de importar
// as corridas do dia, só temos dados de GPS (tempo, km, km/h médio). Depois
// de importar, mostramos o conjunto completo (ganhos, km ativo/desloc,
// R$/h, R$/km, corridas, Uber/99, meta).
function closeJourneySummary() {
  const el = document.getElementById('app-toast-center');
  if (el) el.remove();
}
function showJourneySummary(sess) {
  const day = DB.day(DB.today());
  // Escopo é a jornada (sessão) que acabou de ser encerrada, não o dia
  // inteiro — se a pessoa já tinha outras sessões hoje, elas não entram
  // aqui (aparecem cada uma no próprio card ao expandir a lista de
  // sessões da tela Jornada).
  sess = sess || (day.sessions || [])[(day.sessions||[]).length - 1] || {};
  const totalElapsed = sess.elapsed || 0;
  const totalKm = sess.km || 0;
  const startMs = sess.start || 0, endMs = sess.end || Date.now();
  // Verifica se a jornada cruzou a meia-noite (iniciou ontem, encerrou hoje)
  const crossedMidnight = startMs && endMs && _ymd(startMs) !== _ymd(endMs);
  const midResolvedKey = 'sm_midnight_resolved_' + (sess.id||'x') + '_' + DB.today();
  const needsMidnight = crossedMidnight && !localStorage.getItem(midResolvedKey);
  // Expõe sess globalmente para os onclick do footer
  window._jsSummarySess = sess;
  // Mesma lógica de sessionItem(): corrida pertence a esta sessão se o
  // horário dela cai dentro da janela [start, end] da sessão.
  const trips = (day.trips || []).filter(t => {
    let ts = t.ts || 0;
    if (!ts && t.time) ts = new Date(day.date + 'T' + t.time + ':00').getTime();
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  const hasTrips = trips.length > 0;
  const th = Math.floor(totalElapsed/3600), tm = Math.floor(totalElapsed%3600/60);
  const kmh = totalElapsed > 0 ? (totalKm/(totalElapsed/3600)) : 0;

  const kpiCard = (v,l) => `<div style="background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.1);border-radius:12px;padding:9px 8px;text-align:center">
      <div style="font-family:'Poppins',sans-serif;font-weight:700;font-size:16px">${v}</div>
      <div style="font-size:9.5px;color:rgba(255,255,255,.5);font-weight:600;margin-top:2px;text-transform:uppercase;letter-spacing:.04em">${l}</div>
    </div>`;

  let cardInner, extraSection, footer;

  if (!hasTrips) {
    cardInner = `
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
        <span style="font-size:11px;font-weight:700;color:rgba(255,255,255,.55);text-transform:uppercase;letter-spacing:.06em">Resumo da sessão</span>
        <span style="font-size:10px;font-weight:800;background:rgba(34,197,94,.18);color:#4ADE80;border:1px solid rgba(74,222,128,.35);border-radius:99px;padding:3px 10px">ENCERRADA</span>
      </div>
      <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:32px;line-height:1;letter-spacing:-.01em">${th}h ${p2(tm)}min</div>
      <div style="font-size:11px;color:rgba(255,255,255,.55);font-weight:600;margin-top:4px">tempo online hoje</div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px">
        ${totalKm > 0.1 ? kpiCard(totalKm.toFixed(1), 'Km percorrido') : kpiCard('—', 'Km percorrido')}
        ${totalKm > 0.1 ? kpiCard(kmh.toFixed(1), 'Km/h médio') : kpiCard('—', 'Km/h médio')}
      </div>`;
    extraSection = `
      <div class="cv-daily" style="display:flex;gap:10px;background:#FFFBEB;border:1px solid #FDE68A;border-radius:12px;padding:11px 12px;margin:12px 0;align-items:flex-start">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#D97706" stroke-width="2.4" stroke-linecap="round" style="flex:none;margin-top:1px"><circle cx="12" cy="12" r="9"/><path d="M12 8v4l2.5 2.5"/></svg>
        <span style="font-size:12px;color:#92400E;font-weight:500;line-height:1.45">Km ativo, corridas, ganhos, R$/h, R$/km e meta aparecem aqui depois que você <b>importar as corridas de hoje</b>.</span>
      </div>`;
    footer = `
      <button onclick="closeJourneySummary(); ${needsMidnight} ? openPostStopMidnightModal(window._jsSummarySess, openImportPrecheck) : openImportPrecheck()" style="width:100%;height:48px;border:none;border-radius:14px;font-family:'Inter',system-ui,sans-serif;font-size:14px;font-weight:800;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:8px;background:#22C55E;color:#fff;box-shadow:0 6px 16px rgba(34,197,94,.35)">
        <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M7 10l5 5 5-5"/><path d="M12 15V3"/></svg>
        Importar corridas de hoje
      </button>
      <div style="font-size:11px;color:var(--muted);text-align:center;line-height:1.5;margin-top:8px;padding:0 6px">Escolha as fotos/vídeos do histórico Uber ou 99 — pode selecionar vários de uma vez.</div>
      <button onclick="closeJourneySummary(); if(${needsMidnight}) openPostStopMidnightModal(window._jsSummarySess, null)" style="width:100%;height:40px;border:none;background:transparent;color:var(--muted);font-weight:700;font-size:13px;margin-top:4px;cursor:pointer">Deixar para depois</button>`;
  } else {
    const ganhos = trips.reduce((s,t)=>s+(t.value||0),0);
    const kmAtivo = trips.reduce((s,t)=>s+(t.km||0),0);
    const kmDesl = Math.max(0, totalKm - kmAtivo);
    const rsh = totalElapsed>0 ? ganhos/(totalElapsed/3600) : 0;
    const rskm = totalKm>0 ? ganhos/totalKm : 0;
    const uberTrips = trips.filter(t=>(t.platform||'uber')==='uber');
    const p99Trips  = trips.filter(t=>t.platform==='99');
    const uberR = uberTrips.reduce((s,t)=>s+(t.value||0),0);
    const p99R  = p99Trips.reduce((s,t)=>s+(t.value||0),0);

    const dow = new Date(day.date+'T12:00').getDay();
    const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
    co
===== sessions @ 899439 =====
peof SmartMobiNative.updateFloatingStatus === 'function')
        SmartMobiNative.updateFloatingStatus('running');
    } catch(e) {}
  }
  syncNavButton();
  persistGps();
  // Registra imediatamente no Supabase — não espera checkpoint
  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: g.sessionId, user_id: _supaUser.id, date: DB.today(),
      start_ms: g.start, end_ms: null, km: 0, elapsed_secs: 0,
      paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  // Verifica permissão overlay
  if (window.SmartMobiNative && typeof SmartMobiNative.hasOverlay === 'function') {
    if (!SmartMobiNative.hasOverlay()) {
      setTimeout(() => showToast('Para ver a bolinha flutuante sobre o Uber:\nConfigurações → Apps → MōB → Aparecer sobre outros apps → Ativar', 'info'), 500);
    }
  }
  requestWakeLock();
  // Render forçado, sem depender de qual tela está ativa no momento do
  // clique — mesmo padrão robusto já usado no gpsStop()
  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
  }, 80);
}

function confirmStop() {
  showConfirm('Encerrar a jornada agora?', gpsStop, { confirmLabel: 'Encerrar', cancelLabel: 'Cancelar' });
}
function showToast(msg, type) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const icons = {
    success: { bg: '#ECFDF3', color: '#16A34A', svg: '<path d="M20 6 9 17l-5-5"/>' },
    error:   { bg: '#FEF2F2', color: '#EF4444', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 8v4M12 16h.01"/>' },
    info:    { bg: '#EFF6FF', color: '#0A2F6B', svg: '<circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/>' },
  };
  const ic = icons[type] || icons.success;
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(15,23,42,.35)';
  el.innerHTML = `
    <div style="background:#fff;border-radius:26px;padding:32px 26px;max-width:320px;width:100%;text-align:center;box-shadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:60px;height:60px;border-radius:50%;background:${ic.bg};color:${ic.color};display:flex;align-items:center;justify-content:center;margin:0 auto 16px">
        <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round">${ic.svg}</svg>
      </div>
      <div style="font-size:15.5px;font-weight:700;color:#0F172A;line-height:1.45;white-space:pre-line">${msg}</div>
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', () => el.remove());
  setTimeout(() => { if (document.getElementById('app-toast-center') === el) el.remove(); }, 3400);
}
function showEndJourneyToast(km) {
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(15,23,42,.35)';
  el.innerHTML = `
    <div style="background:#fff;border-radius:26px;padding:36px 28px;max-width:320px;width:100%;text-align:center;box-shadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:68px;height:68px;border-radius:50%;background:#ECFDF3;color:#16A34A;display:flex;align-items:center;justify-content:center;margin:0 auto 18px">
        <svg viewBox="0 0 24 24" width="32" height="32" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
      </div>
      <div style="font-size:18px;font-weight:800;color:#0F172A;margin-bottom:6px">Jornada encerrada</div>
      <div style="font-size:14.5px;color:#64748B">${km.toFixed(1)} km registrados</div>
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', () => el.remove());
  setTimeout(() => el.remove(), 3500);
}
// Resumo de fechamento de jornada — mostrado ao encerrar. Antes de importar
// as corridas do dia, só temos dados de GPS (tempo, km, km/h médio). Depois
// de importar, mostramos o conjunto completo (ganhos, km ativo/desloc,
// R$/h, R$/km, corridas, Uber/99, meta).
function closeJourneySummary() {
  const el = document.getElementById('app-toast-center');
  if (el) el.remove();
}
function showJourneySummary(sess) {
  const day = DB.day(DB.today());
  // Escopo é a jornada (sessão) que acabou de ser encerrada, não o dia
  // inteiro — se a pessoa já tinha outras sessões hoje, elas não entram
  // aqui (aparecem cada uma no próprio card ao expandir a lista de
  // sessões da tela Jornada).
  sess = sess || (day.sessions || [])[(day.sessions||[]).length - 1] || {};
  const totalElapsed = sess.elapsed || 0;
  const totalKm = sess.km || 0;
  const startMs = sess.start || 0, endMs = sess.end || Date.now();
  // Verifica se a jornada cruzou a meia-noite (iniciou ontem, encerrou hoje)
  const crossedMidnight = startMs && endMs && _ymd(startMs) !== _ymd(endMs);
  const midResolvedKey = 'sm_midnight_resolved_' + (sess.id||'x') + '_' + DB.today();
  const needsMidnight = crossedMidnight && !localStorage.getItem(midResolvedKey);
  // Expõe sess globalmente para os onclick do footer
  window._jsSummarySess = sess;
  // Mesma lógica de sessionItem(): corrida pertence a esta sessão se o
  // horário dela cai dentro da janela [start, end] da sessão.
  const trips = (day.trips || []).filter(t => {
    let ts = t.ts || 0;
    if (!ts && t.time) ts = new Date(day.date + 'T' + t.time + ':00').getTime();
    if (!ts) return false;
    return ts >= startMs && (!endMs || ts <= endMs);
  });
  const hasTrips = trips.length > 0;
  const th = Math.floor(totalElapsed/3600), tm = Math.floor(totalElapsed%3600/60);
  const kmh = totalElapsed > 0 ? (totalKm/(totalElapsed/3600)) : 0;

  const kpiCard = (v,l) => `<div style="background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.1);border-radius:12px;padding:9px 8px;text-align:center">
      <div style="font-family:'Poppins',sans-serif;font-weight:700;font-size:16px">${v}</div>
      <div style="font-size:9.5px;color:rgba(255,255,255,.5);font-weight:600;margin-top:2px;text-transform:uppercase;letter-spacing:.04em">${l}</div>
    </div>`;

  let cardInner, extraSection, footer;

  if (!hasTrips) {
    cardInner = `
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
        <span style="font-size:11px;font-weight:700;color:rgba(255,255,255,.55);text-transform:uppercase;letter-spacing:.06em">Resumo da sessão</span>
        <span style="font-size:10px;font-weight:800;background:rgba(34,197,94,.18);color:#4ADE80;border:1px solid rgba(74,222,128,.35);border-radius:99px;padding:3px 10px">ENCERRADA</span>
      </div>
      <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:32px;line-height:1;letter-spacing:-.01em">${th}h ${p2(tm)}min</div>
      <div style="font-size:11px;color:rgba(255,255,255,.55);font-weight:600;margin-top:4px">tempo online hoje</div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px">
        ${totalKm > 0.1 ? kpiCard(totalKm.toFixed(1), 'Km percorrido') : kpiCard('—', 'Km percorrido')}
        ${totalKm > 0.1 ? kpiCard(kmh.toFixed(1), 'Km/h médio') : kpiCard('—', 'Km/h médio')}
      </div>`;
    extraSection = `
      <div class="cv-daily" style="display:flex;gap:10px;background:#FFFBEB;border:1px solid #FDE68A;border-radius:12px;padding:11px 12px;margin:12px 0;align-items:flex-start">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#D97706" stroke-width="2.4" stroke-linecap="round" style="flex:none;margin-top:1px"><circle cx="12" cy="12" r="9"/><path d="M12 8v4l2.5 2.5"/></svg>
        <span style="font-size:12px;color:#92400E;font-weight:500;line-height:1.45">Km ativo, corridas, ganhos, R$/h, R$/km e meta aparecem aqui depois que você <b>importar as corridas de hoje</b>.</span>
      </div>`;
    footer = `
      <button onclick="closeJourneySummary(); ${needsMidnight} ? openPostStopMidnightModal(window._jsSummarySess, openImportPrecheck) : openImportPrecheck()" style="width:100%;height:48px;border:none;border-radius:14px;font-family:'Inter',system-ui,sans-serif;font-size:14px;font-weight:800;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:8px;background:#22C55E;color:#fff;box-shadow:0 6px 16px rgba(34,197,94,.35)">
        <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M7 10l5 5 5-5"/><path d="M12 15V3"/></svg>
        Importar corridas de hoje
      </button>
      <div style="font-size:11px;color:var(--muted);text-align:center;line-height:1.5;margin-top:8px;padding:0 6px">Escolha as fotos/vídeos do histórico Uber ou 99 — pode selecionar vários de uma vez.</div>
      <button onclick="closeJourneySummary(); if(${needsMidnight}) openPostStopMidnightModal(window._jsSummarySess, null)" style="width:100%;height:40px;border:none;background:transparent;color:var(--muted);font-weight:700;font-size:13px;margin-top:4px;cursor:pointer">Deixar para depois</button>`;
  } else {
    const ganhos = trips.reduce((s,t)=>s+(t.value||0),0);
    const kmAtivo = trips.reduce((s,t)=>s+(t.km||0),0);
    const kmDesl = Math.max(0, totalKm - kmAtivo);
    const rsh = totalElapsed>0 ? ganhos/(totalElapsed/3600) : 0;
    const rskm = totalKm>0 ? ganhos/totalKm : 0;
    const uberTrips = trips.filter(t=>(t.platform||'uber')==='uber');
    const p99Trips  = trips.filter(t=>t.platform==='99');
    const uberR = uberTrips.reduce((s,t)=>s+(t.value||0),0);
    const p99R  = p99Trips.reduce((s,t)=>s+(t.value||0),0);

    const dow = new Date(day.date+'T12:00').getDay();
    const plan = DB.planJornada().find(p => p.days && p.days.includes(dow));
    const meta = DB.meta();
    const ov = day.metaOverride || null;
    const dimMes = new Date(new Date(day.date+'T12:00').getFullYear(), new Date(day.date+'T12:00').getMonth()+1, 0).getDate();
    const revTotal = ganhos + (day.revenues||[]).reduce((s,r)=>s+(r.amount||0),0);
    const moneyGoal = (ov && ov.money) ? ov.money : ((plan && plan.moneyGoal) ? plan.moneyGoal : (meta.general ? meta.general/dimMes : 0));

    let metaHtml = '';
    if (moneyGoal) {
      const pct = Math.round(revTotal/moneyGoal*100);
      metaHtml = `
        <div style="margin-top:14px">
          <div style="display:flex;justify-content:space-between;font-size:11px;font-weight:700;margin-bottom:6px">
            <span style="color:rgba(255,255,255,.6)">Meta do dia · ${DB.fRI(moneyGoal)}</span>
            <span style="color:#4ADE80">${pct}%${pct>=100?' &#10003;':''}</span>
          </div>
          <div style="height:8px;border-radius:99px;background:rgba(255,255,255,.12);overflow:hidden">
            <div style="height:100%;border-radius:99px;background:linear-gradient(90deg,#22C55E,#4ADE80);width:${Math.min(100,pct)}%"></div>
          </div>
        </div>`;
    }

    cardInner = `
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
        <span style="font-size:11px;font-weight:700;color:rgba(255,255,255,.55);text-transform:uppercase;letter-spacing:.06em">Ganhos do dia</span>
        <span style="font-size:10px;font-weight:800;background:rgba(34,197,94,.18);color:#4ADE80;border:1px solid rgba(74,222,128,.35);border-radius:99px;padding:3px 10px">${trips.length} CORRIDA${trips.length===1?'':'S'}</span>
      </div>
      <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:32px;line-height:1;letter-spacing:-.01em">${DB.fRI(ganhos)}</div>
      <div style="font-size:11px;color:rgba(255,255,255,.55);font-weight:600;margin-top:4px">bruto · ${th}h ${p2(tm)}min online</div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:14px">
        ${kpiCard(kmAtivo.toFixed(1), 'Km ativo')}
        ${kpiCard(kmDesl.toFixed(1), 'Km desloc.')}
        ${kpiCard(DB.fRI(rsh).replace('R$ ',''), 'R$ / hora')}
        ${kpiCard(rskm.toFixed(2).replace('.',','), 'R$ / km')}
      </div>
      ${metaHtml}`;

    extraSection = `
      <div style="font-size:11px;font-weight:800;color:var(--muted);text-transform:uppercase;letter-spacing:.05em;margin:14px 0 6px">Uber / 99</div>
      <div style="display:flex;gap:8px;margin-bottom:14px">
        <div style="flex:1;border:1px solid var(--line);border-radius:12px;padding:10px 12px;background:var(--soft-line)">
          <div style="font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.05em;color:var(--muted)">Uber</div>
          <div style="font-family:'Poppins',sans-serif;font-weight:700;font-size:15px;color:var(--text);margin-top:2px">${DB.fRI(uberR)}</div>
          <div style="font-size:10px;color:var(--muted2);font-weight:600">${uberTrips.length} corrida${uberTrips.length===1?'':'s'}</div>
        </div>
        <div style="flex:1;border:1px solid var(--line);border-radius:12px;padding:10px 12px;background:var(--soft-line)">
          <div style="font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.05em;color:var(--muted)">99</div>
          <div style="font-family:'Poppins',sans-serif;font-weight:700;font-size:15px;color:var(--text);margin-top:2px">${DB.fRI(p99R)}</div>
          <div style="font-size:10px;color:var(--muted2);font-weight:600">${p99Trips.length} corrida${p99Trips.length===1?'':'s'}</div>
        </div>
      </div>`;

    footer = `<button onclick="closeJourneySummary()" style="width:100%;height:48px;border:none;border-radius:14px;font-family:'Inter',system-ui,sans-serif;font-size:14px;font-weight:800;cursor:pointer;background:#0A2F6B;color:#fff">Fechar</button>`;
  }

  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:flex-end;justify-content:center;background:rgba(15,23,42,.45)';
  const dateLabel = new Date(day.date+'T12:00').toLocaleDateString('pt-BR',{weekday:'long',day:'numeric',month:'long'});
  el.innerHTML = `
    <div style="background:var(--card);border-radius:24px 24px 0 0;padding:10px 18px 26px;box-shadow:0 -12px 40px rgba(10,47,107,.35);max-width:420px;width:100%;max-height:88vh;overflow-y:auto;font-family:'Inter',system-ui,sans-serif">
      <div style="width:44px;height:5px;border-radius:99px;background:var(--line);margin:0 auto 14px"></div>
      <div style="font-family:'
===== sessions @ 914707 =====
v>
      <div style="background:linear-gradient(145deg,#0A2F6B 0%,#0D3E8C 60%,#0A2F6B 100%);border-radius:18px;padding:16px;color:#fff;margin-bottom:12px">
        ${cardInner}
      </div>
      ${extraSection}
      ${footer}
    </div>`;
  document.body.appendChild(el);
  el.addEventListener('click', (e) => { if (e.target === el) closeJourneySummary(); });
}
// Confirmação central (substitui o confirm() nativo do navegador), com a identidade visual do app.
function showConfirm(msg, onConfirm, opts) {
  opts = opts || {};
  const existing = document.getElementById('app-toast-center'); if (existing) existing.remove();
  const danger = opts.danger !== false;
  const el = document.createElement('div');
  el.id = 'app-toast-center';
  el.style.cssText = 'position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;padding:24px;background:rgba(15,23,42,.35)';
  el.innerHTML = `
    <div style="background:#fff;border-radius:26px;padding:30px 26px 22px;max-width:320px;width:100%;text-align:center;box-shadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:56px;height:56px;border-radius:50%;background:${danger ? '#FEF2F2' : '#EFF6FF'};color:${danger ? '#EF4444' : '#0A2F6B'};display:flex;align-items:center;justify-content:center;margin:0 auto 16px">
        <svg viewBox="0 0 24 24" width="26" height="26" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 8v4M12 16h.01"/></svg>
      </div>
      <div style="font-size:15.5px;font-weight:700;color:#0F172A;line-height:1.4;margin-bottom:20px;white-space:pre-line">${msg}</div>
      <div style="display:flex;gap:8px">
        <button id="app-confirm-cancel" style="flex:1;height:46px;border:1.5px solid #E2E8F0;background:#F8FAFC;color:#0F172A;border-radius:14px;font-size:13.5px;font-weight:800;cursor:pointer;font-family:inherit">${opts.cancelLabel || 'Cancelar'}</button>
        <button id="app-confirm-ok" style="flex:1;height:46px;border:0;background:${danger ? '#EF4444' : '#16A34A'};color:#fff;border-radius:14px;font-size:13.5px;font-weight:800;cursor:pointer;font-family:inherit">${opts.confirmLabel || 'Confirmar'}</button>
      </div>
    </div>`;
  document.body.appendChild(el);
  document.getElementById('app-confirm-cancel').onclick = () => el.remove();
  document.getElementById('app-confirm-ok').onclick = () => { el.remove(); onConfirm(); };
}

function gpsPause() { /* removido — app usa só Iniciar / Encerrar */ }
function gpsStop() {
  const g = S.gps;
  if (g.st === 'idle' || !g.start) return; // guard: evita duplo encerramento

  // ═══ FASE 1 — CLEANUP CRÍTICO. Cada linha em try/catch isolado: uma
  // exceção em qualquer uma NUNCA pode impedir as outras de rodar. Isso
  // garante que a jornada SEMPRE é marcada como encerrada local e no
  // Supabase, mesmo se algo mais adiante falhar. ═══
  const cloudId = g.sessionId || DB.uid();
  const startMs = g.start, kmSnap = g.km || 0, endMs = Date.now();
  // Se o motorista escolheu "deixar tudo em ontem" no alerta de virada de
  // meia-noite, g.forceDate trava a data em vez de usar DB.today() (que
  // seria o dia do ENCERRAMENTO, não o do início) — ver resolveMidnightSplit().
  const closeDate = _ymd(startMs); // Jornada contínua: identidade fica no dia em que começou
  let elapsedSnap = 0;
  try { elapsedSnap = getElapsed(); } catch(e) { console.error('[gpsStop] getElapsed:', e); }

  g.st = 'idle';
  if (g.wid) { try { navigator.geolocation.clearWatch(g.wid); } catch(e) { console.error('[gpsStop] clearWatch:', e); } }
  if (g.ti)  { try { clearInterval(g.ti); } catch(e) { console.error('[gpsStop] clearInterval:', e); } }
  if (window.SmartMobiNative && typeof SmartMobiNative.stopFloating === 'function') {
    try { SmartMobiNative.stopFloating(); } catch(e) { console.error('[gpsStop] stopFloating:', e); }
  }
  // Apaga o marcador local IMEDIATAMENTE — é isso que impede a app de
  // 'ressuscitar' esta jornada como ativa da próxima vez que abrir.
  try { DB.del('sm_gps_session'); } catch(e) { console.error('[gpsStop] DB.del:', e); }
  try { _addCss(cloudId, endMs); } catch(e) { console.error('[gpsStop] _addCss:', e); }
  // Avisa o Supabase do fechamento JÁ, antes de qualquer lógica que possa
  // falhar — o servidor precisa saber que a jornada acabou o quanto antes.
  if (_supaUser) {
    // 'Enfileira primeiro, tenta depois' — se o app for morto pelo Android
    // NO MEIO da chamada de rede (antes dela resolver ou rejeitar), o
    // fechamento não pode se perder. A intenção já está salva no localStorage
    // ANTES de qualquer requisição de rede começar; só é removida da fila
    // se a tentativa imediata realmente confirmar sucesso.
    const closeRow = { id: cloudId, date: closeDate, start_ms: startMs, end_ms: endMs,
      km: parseFloat(kmSnap.toFixed(3)), elapsed_secs: elapsedSnap, paused_ms: 0, is_paused: false };
    const queueId = SYNC.enqueue({ type: 'push', table: 'sessions', row: closeRow });
    _SUPA.from('sessions').upsert(Object.assign({}, closeRow, { user_id: _supaUser.id }),
      { onConflict: 'id' }
    ).then(({error}) => { if (!error) SYNC.dequeue(queueId); })
     .catch(()=>{}); // falhou — fica na fila, flushQueue() tenta de novo depois
  }
  g.start=null; g.pausedMs=0; g.km=0; g.pts=[]; g.baseKm=0; g.baseElapsed=0; g.pa=null;
  g.sessionId = null; g.forceDate = null;
  try { syncNavButton(); } catch(e) { console.error('[gpsStop] syncNavButton:', e); }
  try { releaseWakeLock(); } catch(e) { console.error('[gpsStop] releaseWakeLock:', e); }

  // ═══ FASE 2 — detalhamento local (lista de jornadas do dia). Se algo
  // aqui falhar, a jornada JÁ está marcada como encerrada na Fase 1 —
  // o pior caso é essa jornada específica não aparecer na listagem local
  // até a próxima reconciliação com o Supabase, nunca ficar 'presa'. ═══
  let sess = { id: cloudId, start: startMs, end: endMs, km: kmSnap, elapsed: elapsedSnap };
  try {
    const day = DB.day(closeDate);
    if (!day.sessions) day.sessions = [];
    const _sIdx = day.sessions.findIndex(s => s.id === cloudId);
    if (_sIdx >= 0) day.sessions[_sIdx] = sess; else day.sessions.push(sess);
    const totalKm = day.sessions.reduce((s,x)=>s+(x.km||0),0);
    const totalElapsed = day.sessions.reduce((s,x)=>s+(x.elapsed||0),0);
    day.session = { km: totalKm, elapsed: totalElapsed };
    if (!day.kmTotal && totalKm > 0.1) day.kmTotal = parseFloat(totalKm.toFixed(1));
    DB.saveDay(day);
  } catch(e) { console.error('[gpsStop] Falha ao salvar detalhe local (fechamento já confirmado):', e); }

  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
  }, 80);
  if (elapsedSnap > 60) setTimeout(() => showJourneySummary(sess), 500);
}
function getElapsed() {
  const g = S.gps; if (!g.start) return 0;
  const p = g.pausedMs + (g.pa ? Date.now()-g.pa : 0);
  return Math.floor((Date.now()-g.start-p)/1000);
}
let _gpsCloudTick = 0;
function gpsUpdate() {
  _gpsCloudTick++;
  if (_gpsCloudTick % 30 === 0) { persistGps(); _persistGpsCloud().catch(()=>{}); } // a cada 30s: local + nuvem
  // Roda independente de qual dia o usuário está vendo na tela — a virada
  // de meia-noite tem que ser detectada mesmo se ele estiver olhando outro
  // dia no momento.
  checkMidnightCrossing();
  // Só atualiza os elementos visuais se o usuário estiver vendo hoje.
  // Se navegar para outro dia, o timer continua rodando internamente mas
  // não sobrescreve o card que está mostrando dados daquele dia.
  if (S.curDate !== DB.today()) return;
  renderJourneyCard(DB.day(S.curDate), true);
}

// ── PERSISTÊNCIA DA SESSÃO GPS (sobrevive a reload do WebView) ─────────────
function persistGps() {
  const g = S.gps;
  if (g.st === 'idle') { DB.del('sm_gps_session'); return; }
  const lastPt = g.pts && g.pts.length ? g.pts[g.pts.length-1] : null;
  DB.set('sm_gps_session', {
    st: g.st, start: g.start, pausedMs: g.pausedMs, pa: g.pa,
    km: g.km, lastPt, baseKm: g.baseKm, baseElapsed: g.baseElapsed,
    sessionId: g.sessionId, forceDate: g.forceDate || null
  });
  // Checkpoint no Supabase (não bloqueia)
  _persistGpsCloud().catch(()=>{});
}

async function _persistGpsCloud() {
  if (!_supaUser || !S.gps.start || S.gps.st === 'idle') return;
  // Defesa contra instância fantasma: se outra aba/instância desta MESMA
  // origem já encerrou a jornada (apagou o marcador local), esta instância
  // deve se autodestruir em vez de continuar fazendo checkpoint de uma
  // jornada que já não existe mais oficialmente. localStorage é compartilhado
  // entre abas da mesma origem — isso funciona mesmo sem comunicação direta.
  const localMarker = DB.get('sm_gps_session');
  if (!localMarker || (localMarker.st !== 'running' && localMarker.st !== 'paused')) {
    console.warn('[_persistGpsCloud] Marcador local ausente — outra instância já encerrou. Auto-parando esta.');
    S.gps.st = 'idle'; S.gps.start = null;
    if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
    if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
    return;
  }
  const g = S.gps;
  if (!g.sessionId) g.sessionId = DB.uid();
  const snapId = g.sessionId; // capturar antes do await
  await _SUPA.from('sessions').upsert({
    id:         snapId,
    user_id:    _supaUser.id,
    date:       _ymd(g.start), // Jornada contínua: não troca de identidade à meia-noite
    start_ms:   g.start,
    end_ms:     null,
    km:         parseFloat((g.km||0).toFixed(3)),
    elapsed_secs: getElapsed(),
    paused_ms:  0,
    is_paused:  false,
    km_desloc:  null
  }, { onConflict: 'id' });
  // Guard de corrida: se gpsStop() foi chamado enquanto este await estava
  // pendente, o nosso write (end_ms=null) pode ter chegado ao Supabase
  // DEPOIS do write de fechamento, reabrindo a sessão. Re-aplicar o tombstone.
  if (S.gps.st === 'idle') {
    try {
      if (_gcss().has(snapId)) {
        const _cl=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');
        const _em=(_cl&&_cl.id===snapId)?_cl.end:Date.now();
        await _SUPA.from('sessions').upsert({id:snapId,user_id:_supaUser.id,end_ms:_em,is_paused:false,paused_ms:0},{onConflict:'id'});
        console.log('[_persistGpsCloud] Race condition — fechamento re-aplicado:',snapId);
      }
    } catch(e) {}
  }
}

// ══════════════════════════════════════════════════════════════════════
// JORNADA CRUZOU A MEIA-NOITE — detecta, pergunta pro motorista, aplica.
// ══════════════════════════════════════════════════════════════════════
function _ymd(ms) { const d = new Date(ms); return d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate()); }

// Chamado a cada tick do gpsUpdate() (1x/s) e ao restaurar a sessão no boot.
// Barato o bastante pra rodar toda hora: só comparação de strings até achar
// uma virada real; o trabalho pesado (abrir modal) só acontece uma vez por
// virada, travado pelo localStorage (sm_midnight_resolved_*).
function checkMidnightCrossing() {
  const g = S.gps;
  if (g.st !== 'running' || !g.start) return;
  if (S._midnightModalOpen) return; // já perguntando, não reprocessa
  const startDate = _ymd(g.start);
  const today = DB.today();
  if (startDate === today) return; // ainda no mesmo dia — nada a fazer
  if (g.forceDate) return; // motorista já resolveu essa jornada (opção 'ontem')
  const resolvedKey = 'sm_midnight_resolved_' + (g.sessionId||'x') + '_' + today;
  if (localStorage.getItem(resolvedKey)) return; // já perguntado/resolvido pra essa virada
  openMidnightModal(startDate, today);
}

function openMidnightModal(startDate, todayStr) {
  S._midnightModalOpen = true;
  const g = S.gps;
  const sD = new Date(g.start), nD = new Date();
  const startLabel = p2(sD.getDate())+'/'+p2(sD.getMonth()+1)+' · '+p2(sD.getHours())+':'+p2(sD.getMinutes());
  const nowLabel = p2(nD.getDate())+'/'+p2(nD.getMonth()+1)+' · '+p2(nD.getHours())+':'+p2(nD.getMinutes());
  const startDD = startDate.split('-')[2]+'/'+startDate.split('-')[1];
  const todayDD = todayStr.split('-')[2]+'/'+todayStr.split('-')[1];
  document.getElementById('mn-sheet').innerHTML = `
    <div class="sheet-handle"></div>
    <div style="width:52px;height:52px;border-radius:16px;background:#FFFBEB;border:1.5px solid #FDE68A;display:flex;align-items:center;justify-content:center;margin-bottom:14px">
      <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="#D97706" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/></svg>
    </div>
    <h2 style="font-family:'Poppins',sans-serif;font-weight:800;font-size:17px;color:#0F172A;margin-bottom:6px;line-height:1.3">Sua jornada passou da meia-noite</h2>
    <p style="font-size:13px;color:#64748B;line-height:1.55;margin-bottom:6px">Você iniciou <b style="color:#0F172A">ontem</b> e continua rodando. Como quer contar essa jornada?</p>
    <div style="display:flex;align-items:center;gap:8px;margin:16px 0 18px;background:#F7F9FC;border:1px solid #E2E8F0;border-radius:14px;padding:12px 14px">
      <div style="flex:1;text-align:center">
        <div style="font-size:10px;color:#94A3B8;font-weight:700;text-transform:uppercase;letter-spacing:.04em">Início</div>
        <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:15px;color:#0F172A;margin-top:2px">${startDD} · ${startLabel.split(' · ')[1]}</div>
      </div>
      <div style="color:#CBD5E1;font-size:16px">→</div>
      <div style="flex:1;text-align:center">
        <div style="font-size:10px;color:#94A3B8;font-weight:700;text-transform:uppercase;letter-spacing:.04em">Agora</div>
        <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:15px;color:#0F172A;margin-top:2px">${todayDD} · ${nowLabel.split(' · ')[1]}</div>
      </div>
    </div>
    <button class="mn-opt" onclick="resolveMidnightSplit('split')" style="display:flex;align-items:center;gap:12px;width:100%;background:#F0FDF4;border:1.5px solid #BBF7D0;border-radius:16px;padding:13px 14px;margin-bottom:10px;text-align:left;cursor:pointer">
      <div style="width:38px;height:38px;border-radius:11px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#DCFCE7;color:#16A34A"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M12 3v18M5 12h14"/></svg></div>
      <div style="flex:1"><div style="font-size:13.5px;font-weight:700;color:#0F172A">Dividir até meia-noite <span style="font-size:9px;font-weight:800;color:#16A34A;background:#DCFCE7;padding:2px 7px;border-radius:99px;margin-left:6px">RECOMENDADO</span></div>
      <div style="font-size:11px;color:#94A3B8;margin-top:1px;line-height:1.4">Fecha a jornada de ontem às 23:59:59 e abre uma nova hoje às 00:00. Corridas ficam no dia de cada uma.</div></div>
    </button>
    <button class="mn-opt" onclick="resolveMidnightSplit('yesterday')" style="display:flex;align-items:center;gap:12px;width:100%;background:#
===== sessions @ 915749 =====
hadow:0 24px 60px rgba(15,23,42,.25)">
      <div style="width:56px;height:56px;border-radius:50%;background:${danger ? '#FEF2F2' : '#EFF6FF'};color:${danger ? '#EF4444' : '#0A2F6B'};display:flex;align-items:center;justify-content:center;margin:0 auto 16px">
        <svg viewBox="0 0 24 24" width="26" height="26" fill="none" stroke="currentColor" stroke-width="2.3" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 8v4M12 16h.01"/></svg>
      </div>
      <div style="font-size:15.5px;font-weight:700;color:#0F172A;line-height:1.4;margin-bottom:20px;white-space:pre-line">${msg}</div>
      <div style="display:flex;gap:8px">
        <button id="app-confirm-cancel" style="flex:1;height:46px;border:1.5px solid #E2E8F0;background:#F8FAFC;color:#0F172A;border-radius:14px;font-size:13.5px;font-weight:800;cursor:pointer;font-family:inherit">${opts.cancelLabel || 'Cancelar'}</button>
        <button id="app-confirm-ok" style="flex:1;height:46px;border:0;background:${danger ? '#EF4444' : '#16A34A'};color:#fff;border-radius:14px;font-size:13.5px;font-weight:800;cursor:pointer;font-family:inherit">${opts.confirmLabel || 'Confirmar'}</button>
      </div>
    </div>`;
  document.body.appendChild(el);
  document.getElementById('app-confirm-cancel').onclick = () => el.remove();
  document.getElementById('app-confirm-ok').onclick = () => { el.remove(); onConfirm(); };
}

function gpsPause() { /* removido — app usa só Iniciar / Encerrar */ }
function gpsStop() {
  const g = S.gps;
  if (g.st === 'idle' || !g.start) return; // guard: evita duplo encerramento

  // ═══ FASE 1 — CLEANUP CRÍTICO. Cada linha em try/catch isolado: uma
  // exceção em qualquer uma NUNCA pode impedir as outras de rodar. Isso
  // garante que a jornada SEMPRE é marcada como encerrada local e no
  // Supabase, mesmo se algo mais adiante falhar. ═══
  const cloudId = g.sessionId || DB.uid();
  const startMs = g.start, kmSnap = g.km || 0, endMs = Date.now();
  // Se o motorista escolheu "deixar tudo em ontem" no alerta de virada de
  // meia-noite, g.forceDate trava a data em vez de usar DB.today() (que
  // seria o dia do ENCERRAMENTO, não o do início) — ver resolveMidnightSplit().
  const closeDate = _ymd(startMs); // Jornada contínua: identidade fica no dia em que começou
  let elapsedSnap = 0;
  try { elapsedSnap = getElapsed(); } catch(e) { console.error('[gpsStop] getElapsed:', e); }

  g.st = 'idle';
  if (g.wid) { try { navigator.geolocation.clearWatch(g.wid); } catch(e) { console.error('[gpsStop] clearWatch:', e); } }
  if (g.ti)  { try { clearInterval(g.ti); } catch(e) { console.error('[gpsStop] clearInterval:', e); } }
  if (window.SmartMobiNative && typeof SmartMobiNative.stopFloating === 'function') {
    try { SmartMobiNative.stopFloating(); } catch(e) { console.error('[gpsStop] stopFloating:', e); }
  }
  // Apaga o marcador local IMEDIATAMENTE — é isso que impede a app de
  // 'ressuscitar' esta jornada como ativa da próxima vez que abrir.
  try { DB.del('sm_gps_session'); } catch(e) { console.error('[gpsStop] DB.del:', e); }
  try { _addCss(cloudId, endMs); } catch(e) { console.error('[gpsStop] _addCss:', e); }
  // Avisa o Supabase do fechamento JÁ, antes de qualquer lógica que possa
  // falhar — o servidor precisa saber que a jornada acabou o quanto antes.
  if (_supaUser) {
    // 'Enfileira primeiro, tenta depois' — se o app for morto pelo Android
    // NO MEIO da chamada de rede (antes dela resolver ou rejeitar), o
    // fechamento não pode se perder. A intenção já está salva no localStorage
    // ANTES de qualquer requisição de rede começar; só é removida da fila
    // se a tentativa imediata realmente confirmar sucesso.
    const closeRow = { id: cloudId, date: closeDate, start_ms: startMs, end_ms: endMs,
      km: parseFloat(kmSnap.toFixed(3)), elapsed_secs: elapsedSnap, paused_ms: 0, is_paused: false };
    const queueId = SYNC.enqueue({ type: 'push', table: 'sessions', row: closeRow });
    _SUPA.from('sessions').upsert(Object.assign({}, closeRow, { user_id: _supaUser.id }),
      { onConflict: 'id' }
    ).then(({error}) => { if (!error) SYNC.dequeue(queueId); })
     .catch(()=>{}); // falhou — fica na fila, flushQueue() tenta de novo depois
  }
  g.start=null; g.pausedMs=0; g.km=0; g.pts=[]; g.baseKm=0; g.baseElapsed=0; g.pa=null;
  g.sessionId = null; g.forceDate = null;
  try { syncNavButton(); } catch(e) { console.error('[gpsStop] syncNavButton:', e); }
  try { releaseWakeLock(); } catch(e) { console.error('[gpsStop] releaseWakeLock:', e); }

  // ═══ FASE 2 — detalhamento local (lista de jornadas do dia). Se algo
  // aqui falhar, a jornada JÁ está marcada como encerrada na Fase 1 —
  // o pior caso é essa jornada específica não aparecer na listagem local
  // até a próxima reconciliação com o Supabase, nunca ficar 'presa'. ═══
  let sess = { id: cloudId, start: startMs, end: endMs, km: kmSnap, elapsed: elapsedSnap };
  try {
    const day = DB.day(closeDate);
    if (!day.sessions) day.sessions = [];
    const _sIdx = day.sessions.findIndex(s => s.id === cloudId);
    if (_sIdx >= 0) day.sessions[_sIdx] = sess; else day.sessions.push(sess);
    const totalKm = day.sessions.reduce((s,x)=>s+(x.km||0),0);
    const totalElapsed = day.sessions.reduce((s,x)=>s+(x.elapsed||0),0);
    day.session = { km: totalKm, elapsed: totalElapsed };
    if (!day.kmTotal && totalKm > 0.1) day.kmTotal = parseFloat(totalKm.toFixed(1));
    DB.saveDay(day);
  } catch(e) { console.error('[gpsStop] Falha ao salvar detalhe local (fechamento já confirmado):', e); }

  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
  }, 80);
  if (elapsedSnap > 60) setTimeout(() => showJourneySummary(sess), 500);
}
function getElapsed() {
  const g = S.gps; if (!g.start) return 0;
  const p = g.pausedMs + (g.pa ? Date.now()-g.pa : 0);
  return Math.floor((Date.now()-g.start-p)/1000);
}
let _gpsCloudTick = 0;
function gpsUpdate() {
  _gpsCloudTick++;
  if (_gpsCloudTick % 30 === 0) { persistGps(); _persistGpsCloud().catch(()=>{}); } // a cada 30s: local + nuvem
  // Roda independente de qual dia o usuário está vendo na tela — a virada
  // de meia-noite tem que ser detectada mesmo se ele estiver olhando outro
  // dia no momento.
  checkMidnightCrossing();
  // Só atualiza os elementos visuais se o usuário estiver vendo hoje.
  // Se navegar para outro dia, o timer continua rodando internamente mas
  // não sobrescreve o card que está mostrando dados daquele dia.
  if (S.curDate !== DB.today()) return;
  renderJourneyCard(DB.day(S.curDate), true);
}

// ── PERSISTÊNCIA DA SESSÃO GPS (sobrevive a reload do WebView) ─────────────
function persistGps() {
  const g = S.gps;
  if (g.st === 'idle') { DB.del('sm_gps_session'); return; }
  const lastPt = g.pts && g.pts.length ? g.pts[g.pts.length-1] : null;
  DB.set('sm_gps_session', {
    st: g.st, start: g.start, pausedMs: g.pausedMs, pa: g.pa,
    km: g.km, lastPt, baseKm: g.baseKm, baseElapsed: g.baseElapsed,
    sessionId: g.sessionId, forceDate: g.forceDate || null
  });
  // Checkpoint no Supabase (não bloqueia)
  _persistGpsCloud().catch(()=>{});
}

async function _persistGpsCloud() {
  if (!_supaUser || !S.gps.start || S.gps.st === 'idle') return;
  // Defesa contra instância fantasma: se outra aba/instância desta MESMA
  // origem já encerrou a jornada (apagou o marcador local), esta instância
  // deve se autodestruir em vez de continuar fazendo checkpoint de uma
  // jornada que já não existe mais oficialmente. localStorage é compartilhado
  // entre abas da mesma origem — isso funciona mesmo sem comunicação direta.
  const localMarker = DB.get('sm_gps_session');
  if (!localMarker || (localMarker.st !== 'running' && localMarker.st !== 'paused')) {
    console.warn('[_persistGpsCloud] Marcador local ausente — outra instância já encerrou. Auto-parando esta.');
    S.gps.st = 'idle'; S.gps.start = null;
    if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
    if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
    return;
  }
  const g = S.gps;
  if (!g.sessionId) g.sessionId = DB.uid();
  const snapId = g.sessionId; // capturar antes do await
  await _SUPA.from('sessions').upsert({
    id:         snapId,
    user_id:    _supaUser.id,
    date:       _ymd(g.start), // Jornada contínua: não troca de identidade à meia-noite
    start_ms:   g.start,
    end_ms:     null,
    km:         parseFloat((g.km||0).toFixed(3)),
    elapsed_secs: getElapsed(),
    paused_ms:  0,
    is_paused:  false,
    km_desloc:  null
  }, { onConflict: 'id' });
  // Guard de corrida: se gpsStop() foi chamado enquanto este await estava
  // pendente, o nosso write (end_ms=null) pode ter chegado ao Supabase
  // DEPOIS do write de fechamento, reabrindo a sessão. Re-aplicar o tombstone.
  if (S.gps.st === 'idle') {
    try {
      if (_gcss().has(snapId)) {
        const _cl=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');
        const _em=(_cl&&_cl.id===snapId)?_cl.end:Date.now();
        await _SUPA.from('sessions').upsert({id:snapId,user_id:_supaUser.id,end_ms:_em,is_paused:false,paused_ms:0},{onConflict:'id'});
        console.log('[_persistGpsCloud] Race condition — fechamento re-aplicado:',snapId);
      }
    } catch(e) {}
  }
}

// ══════════════════════════════════════════════════════════════════════
// JORNADA CRUZOU A MEIA-NOITE — detecta, pergunta pro motorista, aplica.
// ══════════════════════════════════════════════════════════════════════
function _ymd(ms) { const d = new Date(ms); return d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate()); }

// Chamado a cada tick do gpsUpdate() (1x/s) e ao restaurar a sessão no boot.
// Barato o bastante pra rodar toda hora: só comparação de strings até achar
// uma virada real; o trabalho pesado (abrir modal) só acontece uma vez por
// virada, travado pelo localStorage (sm_midnight_resolved_*).
function checkMidnightCrossing() {
  const g = S.gps;
  if (g.st !== 'running' || !g.start) return;
  if (S._midnightModalOpen) return; // já perguntando, não reprocessa
  const startDate = _ymd(g.start);
  const today = DB.today();
  if (startDate === today) return; // ainda no mesmo dia — nada a fazer
  if (g.forceDate) return; // motorista já resolveu essa jornada (opção 'ontem')
  const resolvedKey = 'sm_midnight_resolved_' + (g.sessionId||'x') + '_' + today;
  if (localStorage.getItem(resolvedKey)) return; // já perguntado/resolvido pra essa virada
  openMidnightModal(startDate, today);
}

function openMidnightModal(startDate, todayStr) {
  S._midnightModalOpen = true;
  const g = S.gps;
  const sD = new Date(g.start), nD = new Date();
  const startLabel = p2(sD.getDate())+'/'+p2(sD.getMonth()+1)+' · '+p2(sD.getHours())+':'+p2(sD.getMinutes());
  const nowLabel = p2(nD.getDate())+'/'+p2(nD.getMonth()+1)+' · '+p2(nD.getHours())+':'+p2(nD.getMinutes());
  const startDD = startDate.split('-')[2]+'/'+startDate.split('-')[1];
  const todayDD = todayStr.split('-')[2]+'/'+todayStr.split('-')[1];
  document.getElementById('mn-sheet').innerHTML = `
    <div class="sheet-handle"></div>
    <div style="width:52px;height:52px;border-radius:16px;background:#FFFBEB;border:1.5px solid #FDE68A;display:flex;align-items:center;justify-content:center;margin-bottom:14px">
      <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="#D97706" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/></svg>
    </div>
    <h2 style="font-family:'Poppins',sans-serif;font-weight:800;font-size:17px;color:#0F172A;margin-bottom:6px;line-height:1.3">Sua jornada passou da meia-noite</h2>
    <p style="font-size:13px;color:#64748B;line-height:1.55;margin-bottom:6px">Você iniciou <b style="color:#0F172A">ontem</b> e continua rodando. Como quer contar essa jornada?</p>
    <div style="display:flex;align-items:center;gap:8px;margin:16px 0 18px;background:#F7F9FC;border:1px solid #E2E8F0;border-radius:14px;padding:12px 14px">
      <div style="flex:1;text-align:center">
        <div style="font-size:10px;color:#94A3B8;font-weight:700;text-transform:uppercase;letter-spacing:.04em">Início</div>
        <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:15px;color:#0F172A;margin-top:2px">${startDD} · ${startLabel.split(' · ')[1]}</div>
      </div>
      <div style="color:#CBD5E1;font-size:16px">→</div>
      <div style="flex:1;text-align:center">
        <div style="font-size:10px;color:#94A3B8;font-weight:700;text-transform:uppercase;letter-spacing:.04em">Agora</div>
        <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:15px;color:#0F172A;margin-top:2px">${todayDD} · ${nowLabel.split(' · ')[1]}</div>
      </div>
    </div>
    <button class="mn-opt" onclick="resolveMidnightSplit('split')" style="display:flex;align-items:center;gap:12px;width:100%;background:#F0FDF4;border:1.5px solid #BBF7D0;border-radius:16px;padding:13px 14px;margin-bottom:10px;text-align:left;cursor:pointer">
      <div style="width:38px;height:38px;border-radius:11px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#DCFCE7;color:#16A34A"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M12 3v18M5 12h14"/></svg></div>
      <div style="flex:1"><div style="font-size:13.5px;font-weight:700;color:#0F172A">Dividir até meia-noite <span style="font-size:9px;font-weight:800;color:#16A34A;background:#DCFCE7;padding:2px 7px;border-radius:99px;margin-left:6px">RECOMENDADO</span></div>
      <div style="font-size:11px;color:#94A3B8;margin-top:1px;line-height:1.4">Fecha a jornada de ontem às 23:59:59 e abre uma nova hoje às 00:00. Corridas ficam no dia de cada uma.</div></div>
    </button>
    <button class="mn-opt" onclick="resolveMidnightSplit('yesterday')" style="display:flex;align-items:center;gap:12px;width:100%;background:#fff;border:1.5px solid #E2E8F0;border-radius:16px;padding:13px 14px;margin-bottom:10px;text-align:left;cursor:pointer">
      <div style="width:38px;height:38px;border-radius:11px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#F1F5F9;color:#64748B"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M11 19l-7-7 7-7M4 12h16"/></svg></div>
      <div style="flex:1"><div style="font-size:13.5px;font-weight:700;color:#0F172A">Deixar tudo em ontem (${startDD})</div>
      <div style="font-size:11px;color:#94A3B8;margin-top:1px;line-height:1.4">A jornada inteira, incluindo o tempo de hoje, conta no dia que começou.</div></div>
    </button>
    <button class="mn-opt" onclick="resolveMidnightSplit('today')" style="display:flex;align-items:center;gap:12px;width:100%;background:#fff;border:1.5px solid #E2E8F0;border-radius:16px;padding:13px 14px;margin-bottom:10px;text-align:left;cursor:pointer">
      <div style="width:38px;height:38px;border-r
===== sessions @ 919000 =====
ossa
  // falhar — o servidor precisa saber que a jornada acabou o quanto antes.
  if (_supaUser) {
    // 'Enfileira primeiro, tenta depois' — se o app for morto pelo Android
    // NO MEIO da chamada de rede (antes dela resolver ou rejeitar), o
    // fechamento não pode se perder. A intenção já está salva no localStorage
    // ANTES de qualquer requisição de rede começar; só é removida da fila
    // se a tentativa imediata realmente confirmar sucesso.
    const closeRow = { id: cloudId, date: closeDate, start_ms: startMs, end_ms: endMs,
      km: parseFloat(kmSnap.toFixed(3)), elapsed_secs: elapsedSnap, paused_ms: 0, is_paused: false };
    const queueId = SYNC.enqueue({ type: 'push', table: 'sessions', row: closeRow });
    _SUPA.from('sessions').upsert(Object.assign({}, closeRow, { user_id: _supaUser.id }),
      { onConflict: 'id' }
    ).then(({error}) => { if (!error) SYNC.dequeue(queueId); })
     .catch(()=>{}); // falhou — fica na fila, flushQueue() tenta de novo depois
  }
  g.start=null; g.pausedMs=0; g.km=0; g.pts=[]; g.baseKm=0; g.baseElapsed=0; g.pa=null;
  g.sessionId = null; g.forceDate = null;
  try { syncNavButton(); } catch(e) { console.error('[gpsStop] syncNavButton:', e); }
  try { releaseWakeLock(); } catch(e) { console.error('[gpsStop] releaseWakeLock:', e); }

  // ═══ FASE 2 — detalhamento local (lista de jornadas do dia). Se algo
  // aqui falhar, a jornada JÁ está marcada como encerrada na Fase 1 —
  // o pior caso é essa jornada específica não aparecer na listagem local
  // até a próxima reconciliação com o Supabase, nunca ficar 'presa'. ═══
  let sess = { id: cloudId, start: startMs, end: endMs, km: kmSnap, elapsed: elapsedSnap };
  try {
    const day = DB.day(closeDate);
    if (!day.sessions) day.sessions = [];
    const _sIdx = day.sessions.findIndex(s => s.id === cloudId);
    if (_sIdx >= 0) day.sessions[_sIdx] = sess; else day.sessions.push(sess);
    const totalKm = day.sessions.reduce((s,x)=>s+(x.km||0),0);
    const totalElapsed = day.sessions.reduce((s,x)=>s+(x.elapsed||0),0);
    day.session = { km: totalKm, elapsed: totalElapsed };
    if (!day.kmTotal && totalKm > 0.1) day.kmTotal = parseFloat(totalKm.toFixed(1));
    DB.saveDay(day);
  } catch(e) { console.error('[gpsStop] Falha ao salvar detalhe local (fechamento já confirmado):', e); }

  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
  }, 80);
  if (elapsedSnap > 60) setTimeout(() => showJourneySummary(sess), 500);
}
function getElapsed() {
  const g = S.gps; if (!g.start) return 0;
  const p = g.pausedMs + (g.pa ? Date.now()-g.pa : 0);
  return Math.floor((Date.now()-g.start-p)/1000);
}
let _gpsCloudTick = 0;
function gpsUpdate() {
  _gpsCloudTick++;
  if (_gpsCloudTick % 30 === 0) { persistGps(); _persistGpsCloud().catch(()=>{}); } // a cada 30s: local + nuvem
  // Roda independente de qual dia o usuário está vendo na tela — a virada
  // de meia-noite tem que ser detectada mesmo se ele estiver olhando outro
  // dia no momento.
  checkMidnightCrossing();
  // Só atualiza os elementos visuais se o usuário estiver vendo hoje.
  // Se navegar para outro dia, o timer continua rodando internamente mas
  // não sobrescreve o card que está mostrando dados daquele dia.
  if (S.curDate !== DB.today()) return;
  renderJourneyCard(DB.day(S.curDate), true);
}

// ── PERSISTÊNCIA DA SESSÃO GPS (sobrevive a reload do WebView) ─────────────
function persistGps() {
  const g = S.gps;
  if (g.st === 'idle') { DB.del('sm_gps_session'); return; }
  const lastPt = g.pts && g.pts.length ? g.pts[g.pts.length-1] : null;
  DB.set('sm_gps_session', {
    st: g.st, start: g.start, pausedMs: g.pausedMs, pa: g.pa,
    km: g.km, lastPt, baseKm: g.baseKm, baseElapsed: g.baseElapsed,
    sessionId: g.sessionId, forceDate: g.forceDate || null
  });
  // Checkpoint no Supabase (não bloqueia)
  _persistGpsCloud().catch(()=>{});
}

async function _persistGpsCloud() {
  if (!_supaUser || !S.gps.start || S.gps.st === 'idle') return;
  // Defesa contra instância fantasma: se outra aba/instância desta MESMA
  // origem já encerrou a jornada (apagou o marcador local), esta instância
  // deve se autodestruir em vez de continuar fazendo checkpoint de uma
  // jornada que já não existe mais oficialmente. localStorage é compartilhado
  // entre abas da mesma origem — isso funciona mesmo sem comunicação direta.
  const localMarker = DB.get('sm_gps_session');
  if (!localMarker || (localMarker.st !== 'running' && localMarker.st !== 'paused')) {
    console.warn('[_persistGpsCloud] Marcador local ausente — outra instância já encerrou. Auto-parando esta.');
    S.gps.st = 'idle'; S.gps.start = null;
    if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
    if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
    return;
  }
  const g = S.gps;
  if (!g.sessionId) g.sessionId = DB.uid();
  const snapId = g.sessionId; // capturar antes do await
  await _SUPA.from('sessions').upsert({
    id:         snapId,
    user_id:    _supaUser.id,
    date:       _ymd(g.start), // Jornada contínua: não troca de identidade à meia-noite
    start_ms:   g.start,
    end_ms:     null,
    km:         parseFloat((g.km||0).toFixed(3)),
    elapsed_secs: getElapsed(),
    paused_ms:  0,
    is_paused:  false,
    km_desloc:  null
  }, { onConflict: 'id' });
  // Guard de corrida: se gpsStop() foi chamado enquanto este await estava
  // pendente, o nosso write (end_ms=null) pode ter chegado ao Supabase
  // DEPOIS do write de fechamento, reabrindo a sessão. Re-aplicar o tombstone.
  if (S.gps.st === 'idle') {
    try {
      if (_gcss().has(snapId)) {
        const _cl=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');
        const _em=(_cl&&_cl.id===snapId)?_cl.end:Date.now();
        await _SUPA.from('sessions').upsert({id:snapId,user_id:_supaUser.id,end_ms:_em,is_paused:false,paused_ms:0},{onConflict:'id'});
        console.log('[_persistGpsCloud] Race condition — fechamento re-aplicado:',snapId);
      }
    } catch(e) {}
  }
}

// ══════════════════════════════════════════════════════════════════════
// JORNADA CRUZOU A MEIA-NOITE — detecta, pergunta pro motorista, aplica.
// ══════════════════════════════════════════════════════════════════════
function _ymd(ms) { const d = new Date(ms); return d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate()); }

// Chamado a cada tick do gpsUpdate() (1x/s) e ao restaurar a sessão no boot.
// Barato o bastante pra rodar toda hora: só comparação de strings até achar
// uma virada real; o trabalho pesado (abrir modal) só acontece uma vez por
// virada, travado pelo localStorage (sm_midnight_resolved_*).
function checkMidnightCrossing() {
  const g = S.gps;
  if (g.st !== 'running' || !g.start) return;
  if (S._midnightModalOpen) return; // já perguntando, não reprocessa
  const startDate = _ymd(g.start);
  const today = DB.today();
  if (startDate === today) return; // ainda no mesmo dia — nada a fazer
  if (g.forceDate) return; // motorista já resolveu essa jornada (opção 'ontem')
  const resolvedKey = 'sm_midnight_resolved_' + (g.sessionId||'x') + '_' + today;
  if (localStorage.getItem(resolvedKey)) return; // já perguntado/resolvido pra essa virada
  openMidnightModal(startDate, today);
}

function openMidnightModal(startDate, todayStr) {
  S._midnightModalOpen = true;
  const g = S.gps;
  const sD = new Date(g.start), nD = new Date();
  const startLabel = p2(sD.getDate())+'/'+p2(sD.getMonth()+1)+' · '+p2(sD.getHours())+':'+p2(sD.getMinutes());
  const nowLabel = p2(nD.getDate())+'/'+p2(nD.getMonth()+1)+' · '+p2(nD.getHours())+':'+p2(nD.getMinutes());
  const startDD = startDate.split('-')[2]+'/'+startDate.split('-')[1];
  const todayDD = todayStr.split('-')[2]+'/'+todayStr.split('-')[1];
  document.getElementById('mn-sheet').innerHTML = `
    <div class="sheet-handle"></div>
    <div style="width:52px;height:52px;border-radius:16px;background:#FFFBEB;border:1.5px solid #FDE68A;display:flex;align-items:center;justify-content:center;margin-bottom:14px">
      <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="#D97706" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/></svg>
    </div>
    <h2 style="font-family:'Poppins',sans-serif;font-weight:800;font-size:17px;color:#0F172A;margin-bottom:6px;line-height:1.3">Sua jornada passou da meia-noite</h2>
    <p style="font-size:13px;color:#64748B;line-height:1.55;margin-bottom:6px">Você iniciou <b style="color:#0F172A">ontem</b> e continua rodando. Como quer contar essa jornada?</p>
    <div style="display:flex;align-items:center;gap:8px;margin:16px 0 18px;background:#F7F9FC;border:1px solid #E2E8F0;border-radius:14px;padding:12px 14px">
      <div style="flex:1;text-align:center">
        <div style="font-size:10px;color:#94A3B8;font-weight:700;text-transform:uppercase;letter-spacing:.04em">Início</div>
        <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:15px;color:#0F172A;margin-top:2px">${startDD} · ${startLabel.split(' · ')[1]}</div>
      </div>
      <div style="color:#CBD5E1;font-size:16px">→</div>
      <div style="flex:1;text-align:center">
        <div style="font-size:10px;color:#94A3B8;font-weight:700;text-transform:uppercase;letter-spacing:.04em">Agora</div>
        <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:15px;color:#0F172A;margin-top:2px">${todayDD} · ${nowLabel.split(' · ')[1]}</div>
      </div>
    </div>
    <button class="mn-opt" onclick="resolveMidnightSplit('split')" style="display:flex;align-items:center;gap:12px;width:100%;background:#F0FDF4;border:1.5px solid #BBF7D0;border-radius:16px;padding:13px 14px;margin-bottom:10px;text-align:left;cursor:pointer">
      <div style="width:38px;height:38px;border-radius:11px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#DCFCE7;color:#16A34A"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M12 3v18M5 12h14"/></svg></div>
      <div style="flex:1"><div style="font-size:13.5px;font-weight:700;color:#0F172A">Dividir até meia-noite <span style="font-size:9px;font-weight:800;color:#16A34A;background:#DCFCE7;padding:2px 7px;border-radius:99px;margin-left:6px">RECOMENDADO</span></div>
      <div style="font-size:11px;color:#94A3B8;margin-top:1px;line-height:1.4">Fecha a jornada de ontem às 23:59:59 e abre uma nova hoje às 00:00. Corridas ficam no dia de cada uma.</div></div>
    </button>
    <button class="mn-opt" onclick="resolveMidnightSplit('yesterday')" style="display:flex;align-items:center;gap:12px;width:100%;background:#fff;border:1.5px solid #E2E8F0;border-radius:16px;padding:13px 14px;margin-bottom:10px;text-align:left;cursor:pointer">
      <div style="width:38px;height:38px;border-radius:11px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#F1F5F9;color:#64748B"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M11 19l-7-7 7-7M4 12h16"/></svg></div>
      <div style="flex:1"><div style="font-size:13.5px;font-weight:700;color:#0F172A">Deixar tudo em ontem (${startDD})</div>
      <div style="font-size:11px;color:#94A3B8;margin-top:1px;line-height:1.4">A jornada inteira, incluindo o tempo de hoje, conta no dia que começou.</div></div>
    </button>
    <button class="mn-opt" onclick="resolveMidnightSplit('today')" style="display:flex;align-items:center;gap:12px;width:100%;background:#fff;border:1.5px solid #E2E8F0;border-radius:16px;padding:13px 14px;margin-bottom:10px;text-align:left;cursor:pointer">
      <div style="width:38px;height:38px;border-radius:11px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#F1F5F9;color:#64748B"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M13 5l7 7-7 7M20 12H4"/></svg></div>
      <div style="flex:1"><div style="font-size:13.5px;font-weight:700;color:#0F172A">Jogar tudo pra hoje (${todayDD})</div>
      <div style="font-size:11px;color:#94A3B8;margin-top:1px;line-height:1.4">A jornada inteira, desde ontem à noite, conta no dia de hoje.</div></div>
    </button>
    <div style="font-size:10.5px;color:#94A3B8;text-align:center;margin-top:4px;line-height:1.5">A jornada continua rodando normalmente enquanto você decide.</div>`;
  openMo('mo-midnight');
}

// mode: 'split' | 'yesterday' | 'today'
async function resolveMidnightSplit(mode) {
  // Se o modal foi aberto pós-encerramento (pelo showJourneySummary), delega
  if (S._midnightIsPostStop) { await resolvePostStopMidnight(mode); return; }
  const g = S.gps;
  if (!g.start) { S._midnightModalOpen = false; closeMo('mo-midnight'); return; } // jornada foi encerrada enquanto o modal estava aberto
  const startDate = _ymd(g.start);
  const todayStr = DB.today();
  const resolvedKey = 'sm_midnight_resolved_' + (g.sessionId||'x') + '_' + todayStr;

  if (mode === 'yesterday') {
    g.forceDate = startDate; // trava a data — ver uso em gpsStop/_persistGpsCloud
  } else if (mode === 'today') {
    g.forceDate = null; // já é o comportamento padrão (date=DB.today() no encerramento)
  } else if (mode === 'split') {
    try { await splitJourneyAtMidnight(startDate, todayStr); }
    catch(e) { console.error('[resolveMidnightSplit] split falhou:', e); showToast('Não consegui dividir agora, tento de novo mais tarde.', 'error'); S._midnightModalOpen = false; closeMo('mo-midnight'); return; }
  }
  localStorage.setItem(resolvedKey, '1');
  S._midnightModalOpen = false;
  closeMo('mo-midnight');
  persistGps();
}

async function splitJourneyAtMidnight(startDate, todayStr) {
  const g = S.gps;
  const midnightMs = new Date(todayStr + 'T00:00:00').getTime();

  // Km exato no instante da virada: prioriza o snapshot capturado pelo
  // GpsService nativo (Kotlin) — ele marca um alarme pro segundo exato da
  // meia-noite e salva o km ali, mesmo com o app em segundo plano. Só cai
  // pra estimativa por tempo (proporcional) se o snapshot não existir —
  // ex: APK antigo sem essa versão, ou app rodou sem o serviço nativo.
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      const snapDate = SmartMobiNative.getMidnightSnapshotDate();
      if (snapDate === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, Date.now() - g.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - g.start));
    kmAtMidnight = (g.km || 0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, g.km || 0); // nunca passa do total acumulado

  // Duração de ontem estimada a partir do pa
===== sessions @ 919851 =====
}
    ).then(({error}) => { if (!error) SYNC.dequeue(queueId); })
     .catch(()=>{}); // falhou — fica na fila, flushQueue() tenta de novo depois
  }
  g.start=null; g.pausedMs=0; g.km=0; g.pts=[]; g.baseKm=0; g.baseElapsed=0; g.pa=null;
  g.sessionId = null; g.forceDate = null;
  try { syncNavButton(); } catch(e) { console.error('[gpsStop] syncNavButton:', e); }
  try { releaseWakeLock(); } catch(e) { console.error('[gpsStop] releaseWakeLock:', e); }

  // ═══ FASE 2 — detalhamento local (lista de jornadas do dia). Se algo
  // aqui falhar, a jornada JÁ está marcada como encerrada na Fase 1 —
  // o pior caso é essa jornada específica não aparecer na listagem local
  // até a próxima reconciliação com o Supabase, nunca ficar 'presa'. ═══
  let sess = { id: cloudId, start: startMs, end: endMs, km: kmSnap, elapsed: elapsedSnap };
  try {
    const day = DB.day(closeDate);
    if (!day.sessions) day.sessions = [];
    const _sIdx = day.sessions.findIndex(s => s.id === cloudId);
    if (_sIdx >= 0) day.sessions[_sIdx] = sess; else day.sessions.push(sess);
    const totalKm = day.sessions.reduce((s,x)=>s+(x.km||0),0);
    const totalElapsed = day.sessions.reduce((s,x)=>s+(x.elapsed||0),0);
    day.session = { km: totalKm, elapsed: totalElapsed };
    if (!day.kmTotal && totalKm > 0.1) day.kmTotal = parseFloat(totalKm.toFixed(1));
    DB.saveDay(day);
  } catch(e) { console.error('[gpsStop] Falha ao salvar detalhe local (fechamento já confirmado):', e); }

  setTimeout(() => {
    try { renderJornada(); } catch(e) {}
    try { renderHome(); } catch(e) {}
  }, 80);
  if (elapsedSnap > 60) setTimeout(() => showJourneySummary(sess), 500);
}
function getElapsed() {
  const g = S.gps; if (!g.start) return 0;
  const p = g.pausedMs + (g.pa ? Date.now()-g.pa : 0);
  return Math.floor((Date.now()-g.start-p)/1000);
}
let _gpsCloudTick = 0;
function gpsUpdate() {
  _gpsCloudTick++;
  if (_gpsCloudTick % 30 === 0) { persistGps(); _persistGpsCloud().catch(()=>{}); } // a cada 30s: local + nuvem
  // Roda independente de qual dia o usuário está vendo na tela — a virada
  // de meia-noite tem que ser detectada mesmo se ele estiver olhando outro
  // dia no momento.
  checkMidnightCrossing();
  // Só atualiza os elementos visuais se o usuário estiver vendo hoje.
  // Se navegar para outro dia, o timer continua rodando internamente mas
  // não sobrescreve o card que está mostrando dados daquele dia.
  if (S.curDate !== DB.today()) return;
  renderJourneyCard(DB.day(S.curDate), true);
}

// ── PERSISTÊNCIA DA SESSÃO GPS (sobrevive a reload do WebView) ─────────────
function persistGps() {
  const g = S.gps;
  if (g.st === 'idle') { DB.del('sm_gps_session'); return; }
  const lastPt = g.pts && g.pts.length ? g.pts[g.pts.length-1] : null;
  DB.set('sm_gps_session', {
    st: g.st, start: g.start, pausedMs: g.pausedMs, pa: g.pa,
    km: g.km, lastPt, baseKm: g.baseKm, baseElapsed: g.baseElapsed,
    sessionId: g.sessionId, forceDate: g.forceDate || null
  });
  // Checkpoint no Supabase (não bloqueia)
  _persistGpsCloud().catch(()=>{});
}

async function _persistGpsCloud() {
  if (!_supaUser || !S.gps.start || S.gps.st === 'idle') return;
  // Defesa contra instância fantasma: se outra aba/instância desta MESMA
  // origem já encerrou a jornada (apagou o marcador local), esta instância
  // deve se autodestruir em vez de continuar fazendo checkpoint de uma
  // jornada que já não existe mais oficialmente. localStorage é compartilhado
  // entre abas da mesma origem — isso funciona mesmo sem comunicação direta.
  const localMarker = DB.get('sm_gps_session');
  if (!localMarker || (localMarker.st !== 'running' && localMarker.st !== 'paused')) {
    console.warn('[_persistGpsCloud] Marcador local ausente — outra instância já encerrou. Auto-parando esta.');
    S.gps.st = 'idle'; S.gps.start = null;
    if (S.gps.wid) { try{ navigator.geolocation.clearWatch(S.gps.wid); }catch(e){} }
    if (S.gps.ti) { try{ clearInterval(S.gps.ti); }catch(e){} }
    return;
  }
  const g = S.gps;
  if (!g.sessionId) g.sessionId = DB.uid();
  const snapId = g.sessionId; // capturar antes do await
  await _SUPA.from('sessions').upsert({
    id:         snapId,
    user_id:    _supaUser.id,
    date:       _ymd(g.start), // Jornada contínua: não troca de identidade à meia-noite
    start_ms:   g.start,
    end_ms:     null,
    km:         parseFloat((g.km||0).toFixed(3)),
    elapsed_secs: getElapsed(),
    paused_ms:  0,
    is_paused:  false,
    km_desloc:  null
  }, { onConflict: 'id' });
  // Guard de corrida: se gpsStop() foi chamado enquanto este await estava
  // pendente, o nosso write (end_ms=null) pode ter chegado ao Supabase
  // DEPOIS do write de fechamento, reabrindo a sessão. Re-aplicar o tombstone.
  if (S.gps.st === 'idle') {
    try {
      if (_gcss().has(snapId)) {
        const _cl=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');
        const _em=(_cl&&_cl.id===snapId)?_cl.end:Date.now();
        await _SUPA.from('sessions').upsert({id:snapId,user_id:_supaUser.id,end_ms:_em,is_paused:false,paused_ms:0},{onConflict:'id'});
        console.log('[_persistGpsCloud] Race condition — fechamento re-aplicado:',snapId);
      }
    } catch(e) {}
  }
}

// ══════════════════════════════════════════════════════════════════════
// JORNADA CRUZOU A MEIA-NOITE — detecta, pergunta pro motorista, aplica.
// ══════════════════════════════════════════════════════════════════════
function _ymd(ms) { const d = new Date(ms); return d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate()); }

// Chamado a cada tick do gpsUpdate() (1x/s) e ao restaurar a sessão no boot.
// Barato o bastante pra rodar toda hora: só comparação de strings até achar
// uma virada real; o trabalho pesado (abrir modal) só acontece uma vez por
// virada, travado pelo localStorage (sm_midnight_resolved_*).
function checkMidnightCrossing() {
  const g = S.gps;
  if (g.st !== 'running' || !g.start) return;
  if (S._midnightModalOpen) return; // já perguntando, não reprocessa
  const startDate = _ymd(g.start);
  const today = DB.today();
  if (startDate === today) return; // ainda no mesmo dia — nada a fazer
  if (g.forceDate) return; // motorista já resolveu essa jornada (opção 'ontem')
  const resolvedKey = 'sm_midnight_resolved_' + (g.sessionId||'x') + '_' + today;
  if (localStorage.getItem(resolvedKey)) return; // já perguntado/resolvido pra essa virada
  openMidnightModal(startDate, today);
}

function openMidnightModal(startDate, todayStr) {
  S._midnightModalOpen = true;
  const g = S.gps;
  const sD = new Date(g.start), nD = new Date();
  const startLabel = p2(sD.getDate())+'/'+p2(sD.getMonth()+1)+' · '+p2(sD.getHours())+':'+p2(sD.getMinutes());
  const nowLabel = p2(nD.getDate())+'/'+p2(nD.getMonth()+1)+' · '+p2(nD.getHours())+':'+p2(nD.getMinutes());
  const startDD = startDate.split('-')[2]+'/'+startDate.split('-')[1];
  const todayDD = todayStr.split('-')[2]+'/'+todayStr.split('-')[1];
  document.getElementById('mn-sheet').innerHTML = `
    <div class="sheet-handle"></div>
    <div style="width:52px;height:52px;border-radius:16px;background:#FFFBEB;border:1.5px solid #FDE68A;display:flex;align-items:center;justify-content:center;margin-bottom:14px">
      <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="#D97706" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/></svg>
    </div>
    <h2 style="font-family:'Poppins',sans-serif;font-weight:800;font-size:17px;color:#0F172A;margin-bottom:6px;line-height:1.3">Sua jornada passou da meia-noite</h2>
    <p style="font-size:13px;color:#64748B;line-height:1.55;margin-bottom:6px">Você iniciou <b style="color:#0F172A">ontem</b> e continua rodando. Como quer contar essa jornada?</p>
    <div style="display:flex;align-items:center;gap:8px;margin:16px 0 18px;background:#F7F9FC;border:1px solid #E2E8F0;border-radius:14px;padding:12px 14px">
      <div style="flex:1;text-align:center">
        <div style="font-size:10px;color:#94A3B8;font-weight:700;text-transform:uppercase;letter-spacing:.04em">Início</div>
        <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:15px;color:#0F172A;margin-top:2px">${startDD} · ${startLabel.split(' · ')[1]}</div>
      </div>
      <div style="color:#CBD5E1;font-size:16px">→</div>
      <div style="flex:1;text-align:center">
        <div style="font-size:10px;color:#94A3B8;font-weight:700;text-transform:uppercase;letter-spacing:.04em">Agora</div>
        <div style="font-family:'Poppins',sans-serif;font-weight:800;font-size:15px;color:#0F172A;margin-top:2px">${todayDD} · ${nowLabel.split(' · ')[1]}</div>
      </div>
    </div>
    <button class="mn-opt" onclick="resolveMidnightSplit('split')" style="display:flex;align-items:center;gap:12px;width:100%;background:#F0FDF4;border:1.5px solid #BBF7D0;border-radius:16px;padding:13px 14px;margin-bottom:10px;text-align:left;cursor:pointer">
      <div style="width:38px;height:38px;border-radius:11px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#DCFCE7;color:#16A34A"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M12 3v18M5 12h14"/></svg></div>
      <div style="flex:1"><div style="font-size:13.5px;font-weight:700;color:#0F172A">Dividir até meia-noite <span style="font-size:9px;font-weight:800;color:#16A34A;background:#DCFCE7;padding:2px 7px;border-radius:99px;margin-left:6px">RECOMENDADO</span></div>
      <div style="font-size:11px;color:#94A3B8;margin-top:1px;line-height:1.4">Fecha a jornada de ontem às 23:59:59 e abre uma nova hoje às 00:00. Corridas ficam no dia de cada uma.</div></div>
    </button>
    <button class="mn-opt" onclick="resolveMidnightSplit('yesterday')" style="display:flex;align-items:center;gap:12px;width:100%;background:#fff;border:1.5px solid #E2E8F0;border-radius:16px;padding:13px 14px;margin-bottom:10px;text-align:left;cursor:pointer">
      <div style="width:38px;height:38px;border-radius:11px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#F1F5F9;color:#64748B"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M11 19l-7-7 7-7M4 12h16"/></svg></div>
      <div style="flex:1"><div style="font-size:13.5px;font-weight:700;color:#0F172A">Deixar tudo em ontem (${startDD})</div>
      <div style="font-size:11px;color:#94A3B8;margin-top:1px;line-height:1.4">A jornada inteira, incluindo o tempo de hoje, conta no dia que começou.</div></div>
    </button>
    <button class="mn-opt" onclick="resolveMidnightSplit('today')" style="display:flex;align-items:center;gap:12px;width:100%;background:#fff;border:1.5px solid #E2E8F0;border-radius:16px;padding:13px 14px;margin-bottom:10px;text-align:left;cursor:pointer">
      <div style="width:38px;height:38px;border-radius:11px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#F1F5F9;color:#64748B"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M13 5l7 7-7 7M20 12H4"/></svg></div>
      <div style="flex:1"><div style="font-size:13.5px;font-weight:700;color:#0F172A">Jogar tudo pra hoje (${todayDD})</div>
      <div style="font-size:11px;color:#94A3B8;margin-top:1px;line-height:1.4">A jornada inteira, desde ontem à noite, conta no dia de hoje.</div></div>
    </button>
    <div style="font-size:10.5px;color:#94A3B8;text-align:center;margin-top:4px;line-height:1.5">A jornada continua rodando normalmente enquanto você decide.</div>`;
  openMo('mo-midnight');
}

// mode: 'split' | 'yesterday' | 'today'
async function resolveMidnightSplit(mode) {
  // Se o modal foi aberto pós-encerramento (pelo showJourneySummary), delega
  if (S._midnightIsPostStop) { await resolvePostStopMidnight(mode); return; }
  const g = S.gps;
  if (!g.start) { S._midnightModalOpen = false; closeMo('mo-midnight'); return; } // jornada foi encerrada enquanto o modal estava aberto
  const startDate = _ymd(g.start);
  const todayStr = DB.today();
  const resolvedKey = 'sm_midnight_resolved_' + (g.sessionId||'x') + '_' + todayStr;

  if (mode === 'yesterday') {
    g.forceDate = startDate; // trava a data — ver uso em gpsStop/_persistGpsCloud
  } else if (mode === 'today') {
    g.forceDate = null; // já é o comportamento padrão (date=DB.today() no encerramento)
  } else if (mode === 'split') {
    try { await splitJourneyAtMidnight(startDate, todayStr); }
    catch(e) { console.error('[resolveMidnightSplit] split falhou:', e); showToast('Não consegui dividir agora, tento de novo mais tarde.', 'error'); S._midnightModalOpen = false; closeMo('mo-midnight'); return; }
  }
  localStorage.setItem(resolvedKey, '1');
  S._midnightModalOpen = false;
  closeMo('mo-midnight');
  persistGps();
}

async function splitJourneyAtMidnight(startDate, todayStr) {
  const g = S.gps;
  const midnightMs = new Date(todayStr + 'T00:00:00').getTime();

  // Km exato no instante da virada: prioriza o snapshot capturado pelo
  // GpsService nativo (Kotlin) — ele marca um alarme pro segundo exato da
  // meia-noite e salva o km ali, mesmo com o app em segundo plano. Só cai
  // pra estimativa por tempo (proporcional) se o snapshot não existir —
  // ex: APK antigo sem essa versão, ou app rodou sem o serviço nativo.
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      const snapDate = SmartMobiNative.getMidnightSnapshotDate();
      if (snapDate === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, Date.now() - g.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - g.start));
    kmAtMidnight = (g.km || 0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, g.km || 0); // nunca passa do total acumulado

  // Duração de ontem estimada a partir do pausedMs acumulado até agora —
  // aproximação: assume que a pausa (se houve) ficou toda de um lado ou
  // outro da virada, o que cobre o caso comum (raro pausar bem em cima da
  // meia-noite).
  const elapsedYesterday = Math.max(0, Math.round((midnightMs - g.start - (g.pausedMs||0))/1000));

  // ── Fecha a sessão de ontem ──
  const sessYesterday = { id: g.sessionId, start: g.start, end: midnightMs,
    km: parseFloat(kmAtMidnight.toFixed(3)), elapsed: elapsedYesterday };
  const dayY = DB.day(startDate);
  if (!dayY.sessions) dayY.sessions = [];
  const idxY = dayY.sessions.findIndex(s => s.id === sessYesterday.id);
  if (idxY >= 0) dayY.sessions[idxY] = sessYesterday; else dayY.sessions.push(sessYesterday);
  DB.saveDay(dayY);
  SYNC.push('sessions', { id: sessYesterday.id, date: startDate, start_ms: sessYesterday.start,
    end_ms: s
===== sessions @ 929526 =====
de cada uma.</div></div>
    </button>
    <button class="mn-opt" onclick="resolveMidnightSplit('yesterday')" style="display:flex;align-items:center;gap:12px;width:100%;background:#fff;border:1.5px solid #E2E8F0;border-radius:16px;padding:13px 14px;margin-bottom:10px;text-align:left;cursor:pointer">
      <div style="width:38px;height:38px;border-radius:11px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#F1F5F9;color:#64748B"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M11 19l-7-7 7-7M4 12h16"/></svg></div>
      <div style="flex:1"><div style="font-size:13.5px;font-weight:700;color:#0F172A">Deixar tudo em ontem (${startDD})</div>
      <div style="font-size:11px;color:#94A3B8;margin-top:1px;line-height:1.4">A jornada inteira, incluindo o tempo de hoje, conta no dia que começou.</div></div>
    </button>
    <button class="mn-opt" onclick="resolveMidnightSplit('today')" style="display:flex;align-items:center;gap:12px;width:100%;background:#fff;border:1.5px solid #E2E8F0;border-radius:16px;padding:13px 14px;margin-bottom:10px;text-align:left;cursor:pointer">
      <div style="width:38px;height:38px;border-radius:11px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#F1F5F9;color:#64748B"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M13 5l7 7-7 7M20 12H4"/></svg></div>
      <div style="flex:1"><div style="font-size:13.5px;font-weight:700;color:#0F172A">Jogar tudo pra hoje (${todayDD})</div>
      <div style="font-size:11px;color:#94A3B8;margin-top:1px;line-height:1.4">A jornada inteira, desde ontem à noite, conta no dia de hoje.</div></div>
    </button>
    <div style="font-size:10.5px;color:#94A3B8;text-align:center;margin-top:4px;line-height:1.5">A jornada continua rodando normalmente enquanto você decide.</div>`;
  openMo('mo-midnight');
}

// mode: 'split' | 'yesterday' | 'today'
async function resolveMidnightSplit(mode) {
  // Se o modal foi aberto pós-encerramento (pelo showJourneySummary), delega
  if (S._midnightIsPostStop) { await resolvePostStopMidnight(mode); return; }
  const g = S.gps;
  if (!g.start) { S._midnightModalOpen = false; closeMo('mo-midnight'); return; } // jornada foi encerrada enquanto o modal estava aberto
  const startDate = _ymd(g.start);
  const todayStr = DB.today();
  const resolvedKey = 'sm_midnight_resolved_' + (g.sessionId||'x') + '_' + todayStr;

  if (mode === 'yesterday') {
    g.forceDate = startDate; // trava a data — ver uso em gpsStop/_persistGpsCloud
  } else if (mode === 'today') {
    g.forceDate = null; // já é o comportamento padrão (date=DB.today() no encerramento)
  } else if (mode === 'split') {
    try { await splitJourneyAtMidnight(startDate, todayStr); }
    catch(e) { console.error('[resolveMidnightSplit] split falhou:', e); showToast('Não consegui dividir agora, tento de novo mais tarde.', 'error'); S._midnightModalOpen = false; closeMo('mo-midnight'); return; }
  }
  localStorage.setItem(resolvedKey, '1');
  S._midnightModalOpen = false;
  closeMo('mo-midnight');
  persistGps();
}

async function splitJourneyAtMidnight(startDate, todayStr) {
  const g = S.gps;
  const midnightMs = new Date(todayStr + 'T00:00:00').getTime();

  // Km exato no instante da virada: prioriza o snapshot capturado pelo
  // GpsService nativo (Kotlin) — ele marca um alarme pro segundo exato da
  // meia-noite e salva o km ali, mesmo com o app em segundo plano. Só cai
  // pra estimativa por tempo (proporcional) se o snapshot não existir —
  // ex: APK antigo sem essa versão, ou app rodou sem o serviço nativo.
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      const snapDate = SmartMobiNative.getMidnightSnapshotDate();
      if (snapDate === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, Date.now() - g.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - g.start));
    kmAtMidnight = (g.km || 0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, g.km || 0); // nunca passa do total acumulado

  // Duração de ontem estimada a partir do pausedMs acumulado até agora —
  // aproximação: assume que a pausa (se houve) ficou toda de um lado ou
  // outro da virada, o que cobre o caso comum (raro pausar bem em cima da
  // meia-noite).
  const elapsedYesterday = Math.max(0, Math.round((midnightMs - g.start - (g.pausedMs||0))/1000));

  // ── Fecha a sessão de ontem ──
  const sessYesterday = { id: g.sessionId, start: g.start, end: midnightMs,
    km: parseFloat(kmAtMidnight.toFixed(3)), elapsed: elapsedYesterday };
  const dayY = DB.day(startDate);
  if (!dayY.sessions) dayY.sessions = [];
  const idxY = dayY.sessions.findIndex(s => s.id === sessYesterday.id);
  if (idxY >= 0) dayY.sessions[idxY] = sessYesterday; else dayY.sessions.push(sessYesterday);
  DB.saveDay(dayY);
  SYNC.push('sessions', { id: sessYesterday.id, date: startDate, start_ms: sessYesterday.start,
    end_ms: sessYesterday.end, km: sessYesterday.km, elapsed_secs: sessYesterday.elapsed,
    paused_ms: 0, is_paused: false });
  try { reconcileSessionsForDay(startDate); } catch(e) {}

  // ── Abre a sessão de hoje, continuando a rodar sem interromper o GPS ──
  const newId = DB.uid();
  const remainingKm = Math.max(0, (g.km || 0) - kmAtMidnight);
  g.sessionId = newId;
  g.start = midnightMs;
  g.km = remainingKm;
  g.pausedMs = 0; g.pa = null;
  g.pts = []; g.lastFixTime = null;
  g.forceDate = null; // sessão nova já nasce em 'hoje' — sem override
  const dayT = DB.day(todayStr);
  g.baseKm = (dayT.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (dayT.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);

  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: newId, user_id: _supaUser.id, date: todayStr,
      start_ms: midnightMs, end_ms: null, km: parseFloat(remainingKm.toFixed(3)),
      elapsed_secs: 0, paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  // Religa o widget/serviço nativo com o novo sessionId/km — sem isso ele
  // continuaria reportando pro id antigo (que já fechamos acima).
  if (window.SmartMobiNative) {
    try { SmartMobiNative.startFloating?.(g.start, g.km || 0); } catch(e) {}
    try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} // consumido — não reaplicar
  }

  showToast('Jornada dividida na meia-noite' + (estimated ? ' (km estimado por tempo — sem GPS exato disponível)' : ' (km exato do GPS)') + '.', 'info', 4500);
  try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
}

// ── Midnight pós-encerramento (acionado pelo showJourneySummary) ──────────
// Reutiliza o mesmo modal mo-midnight, mas com flag _midnightIsPostStop para
// resolveMidnightSplit() delegar ao resolvePostStopMidnight().
function openPostStopMidnightModal(sess, onDone) {
  if (!sess || !sess.start || !sess.end) { if (onDone) onDone(); return; }
  S._midnightIsPostStop = true;
  S._postStopMidnightSess = sess;
  S._postMidnightCallback = onDone || null;
  openMidnightModal(_ymd(sess.start), _ymd(sess.end));
}

async function resolvePostStopMidnight(mode) {
  const sess = S._postStopMidnightSess;
  const onDone = S._postMidnightCallback;
  S._midnightIsPostStop = false;
  S._postStopMidnightSess = null;
  S._postMidnightCallback = null;
  S._midnightModalOpen = false;
  closeMo('mo-midnight');
  if (!sess) { if (onDone) onDone(); return; }

  const startDate = _ymd(sess.start);
  const todayStr = _ymd(sess.end);
  const resolvedKey = 'sm_midnight_resolved_' + (sess.id||'x') + '_' + todayStr;
  localStorage.setItem(resolvedKey, '1');

  if (mode === 'today') {
    // Sessão já foi salva em hoje por gpsStop — nada a mover
    showToast('Jornada mantida em hoje.', 'info');
  } else if (mode === 'yesterday') {
    // Move sessão de hoje para ontem
    const dayToday = DB.day(todayStr);
    dayToday.sessions = (dayToday.sessions||[]).filter(s => s.id !== sess.id);
    const tk = dayToday.sessions.reduce((s,x)=>s+(x.km||0),0);
    dayToday.session = { km:tk, elapsed: dayToday.sessions.reduce((s,x)=>s+(x.elapsed||0),0) };
    DB.saveDay(dayToday);
    const dayYest = DB.day(startDate);
    if (!dayYest.sessions) dayYest.sessions = [];
    if (!dayYest.sessions.find(s => s.id === sess.id)) dayYest.sessions.push(sess);
    DB.saveDay(dayYest);
    SYNC.push('sessions', { id: sess.id, date: startDate, start_ms: sess.start,
      end_ms: sess.end, km: sess.km, elapsed_secs: sess.elapsed, paused_ms:0, is_paused:false });
    showToast('Jornada mantida em ontem.', 'info');
  } else if (mode === 'split') {
    try { await splitPostStopMidnight(sess, startDate, todayStr); }
    catch(e) { console.error('[resolvePostStopMidnight] split falhou:', e); showToast('Erro ao dividir. Tente novamente.','error'); }
  }
  try { renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
  if (onDone) setTimeout(onDone, 300);
}

async function splitPostStopMidnight(sess, startDate, todayStr) {
  const midnightMs = new Date(todayStr + 'T00:00:00').getTime();
  // km na meia-noite: tenta snapshot nativo, senão estima por proporção de tempo
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      if (SmartMobiNative.getMidnightSnapshotDate() === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, sess.end - sess.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - sess.start));
    kmAtMidnight = (sess.km||0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, sess.km||0);
  const elapsedYest = Math.max(0, Math.round((midnightMs - sess.start) / 1000));
  const elapsedToday = Math.max(0, (sess.elapsed||0) - elapsedYest);
  const kmToday = parseFloat(Math.max(0, (sess.km||0) - kmAtMidnight).toFixed(3));
  // Sessão de ontem (mantém o ID original)
  const sessYest = { id: sess.id, start: sess.start, end: midnightMs,
    km: parseFloat(kmAtMidnight.toFixed(3)), elapsed: elapsedYest };
  const dayYest = DB.day(startDate);
  if (!dayYest.sessions) dayYest.sessions = [];
  const iy = dayYest.sessions.findIndex(s => s.id === sess.id);
  if (iy >= 0) dayYest.sessions[iy] = sessYest; else dayYest.sessions.push(sessYest);
  DB.saveDay(dayYest);
  SYNC.push('sessions', { id: sessYest.id, date: startDate, start_ms: sessYest.start,
    end_ms: sessYest.end, km: sessYest.km, elapsed_secs: sessYest.elapsed, paused_ms:0, is_paused:false });
  // Sessão de hoje (novo ID, parte após meia-noite)
  const newId = DB.uid();
  const sessToday = { id: newId, start: midnightMs, end: sess.end, km: kmToday, elapsed: elapsedToday };
  const dayToday = DB.day(todayStr);
  if (!dayToday.sessions) dayToday.sessions = [];
  dayToday.sessions = dayToday.sessions.filter(s => s.id !== sess.id); // remove original
  dayToday.sessions.push(sessToday);
  DB.saveDay(dayToday);
  SYNC.push('sessions', { id: sessToday.id, date: todayStr, start_ms: sessToday.start,
    end_ms: sessToday.end, km: sessToday.km, elapsed_secs: sessToday.elapsed, paused_ms:0, is_paused:false });
  try { reconcileSessionsForDay(startDate); } catch(e) {}
  if (window.SmartMobiNative) { try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} }
  showToast('Jornada dividida na meia-noite' + (estimated?' (km estimado)':' (km do GPS)') + '.', 'info', 4500);
}

function restoreGpsSession() {
  const g = S.gps;
  // Já está rodando nesta instância do app (ex: showApp() foi chamado de novo
  // pelo Supabase reemitindo SIGNED_IN ao voltar do background) — não faz nada,
  // senão sobrescreve o km/timer atuais com o último snapshot salvo e duplica
  // o watchPosition/setInterval, fazendo o km "resetar"/contar dobrado.
  if (g.ti) return;
  const saved = DB.get('sm_gps_session');
  if (!saved || (saved.st !== 'running' && saved.st !== 'paused')) return;
  // Sessão muito antiga (app ficou fechado por mais de 16h) — não tenta retomar
  const ageMs = Date.now() - (saved.start || 0);
  if (!saved.start || ageMs > 16*3600*1000) { DB.del('sm_gps_session'); return; }
  // Defesa extra: se o Supabase já sabe que esta sessão foi encerrada
  // (ex: gpsStop rodou parcialmente numa tentativa anterior — fechou lá
  // mas não apagou o marcador local), NÃO resume. Confia no servidor.
  if (_supaUser && saved.sessionId && navigator.onLine) {
    _SUPA.from('sessions').select('end_ms,km,start_ms').eq('id', saved.sessionId).maybeSingle()
      .then(({data}) => {
        // Recupera km/start do checkpoint na nuvem se forem melhores que o
        // snapshot local (marcador local pode estar defasado se o app ficou
        // muito tempo em background sem JS rodando).
        if (data && !data.end_ms && S.gps.st !== 'idle' && S.gps.sessionId === saved.sessionId) {
          const cloudKm = parseFloat(data.km) || 0;
          if (cloudKm > (S.gps.km || 0)) { S.gps.km = cloudKm; S.gps.pts = []; S.gps.lastFixTime = null; }
          if (data.start_ms && Number(data.start_ms) < (S.gps.start || Infinity)) S.gps.start = Number(data.start_ms);
          try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
        }
        if (data && data.end_ms) {
          console.warn('[restoreGpsSession] Sessão já encerrada no Supabase — descartando marcador local stale');
          DB.del('sm_gps_session');
          if (S.gps.st !== 'idle') {
            S.gps.st = 'idle'; S.gps.start = null;
            if (S.gps.wid) { try{navigator.geolocation.clearWatch(S.gps.wid);}catch(e){} }
            if (S.gps.ti) clearInterval(S.gps.ti);
            try { renderJornada(); renderHome(); } catch(e) {}
          }
        }
      }).catch(()=>{});
  }
  // Limpeza defensiva — garante que não sobra nenhum watcher/timer órfão
  if (g.wid) { try{ navigator.geolocation.clearWatch(g.wid); }catch(e){} }
  if (g.ti) clearInterval(g.ti);
  g.st = saved.st; g.start = saved.start; g.pausedMs = saved.pausedMs || 0; g.pa = saved.pa || null;
  g.km = saved.km || 0; g.pts = saved.lastPt ? [saved.lastPt] : [];
  g.baseKm = saved.baseKm || 0; g.baseElapsed = saved.baseElapsed || 0;
  g.sessionId = saved.sessionId || null; // restaura ID para continuar o upsert correto
  g.forceDate = saved.forceDate || null;
  // O snapshot acima é só o último valor salvo antes do app ir pra segundo plano (o
  // watchPosition do JS para quando o app não está em foreground). Se o app nativo
  // tiver um GpsService rodando, ele é a font
===== sessions @ 929650 =====
:flex;align-items:center;gap:12px;width:100%;background:#fff;border:1.5px solid #E2E8F0;border-radius:16px;padding:13px 14px;margin-bottom:10px;text-align:left;cursor:pointer">
      <div style="width:38px;height:38px;border-radius:11px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#F1F5F9;color:#64748B"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M11 19l-7-7 7-7M4 12h16"/></svg></div>
      <div style="flex:1"><div style="font-size:13.5px;font-weight:700;color:#0F172A">Deixar tudo em ontem (${startDD})</div>
      <div style="font-size:11px;color:#94A3B8;margin-top:1px;line-height:1.4">A jornada inteira, incluindo o tempo de hoje, conta no dia que começou.</div></div>
    </button>
    <button class="mn-opt" onclick="resolveMidnightSplit('today')" style="display:flex;align-items:center;gap:12px;width:100%;background:#fff;border:1.5px solid #E2E8F0;border-radius:16px;padding:13px 14px;margin-bottom:10px;text-align:left;cursor:pointer">
      <div style="width:38px;height:38px;border-radius:11px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#F1F5F9;color:#64748B"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M13 5l7 7-7 7M20 12H4"/></svg></div>
      <div style="flex:1"><div style="font-size:13.5px;font-weight:700;color:#0F172A">Jogar tudo pra hoje (${todayDD})</div>
      <div style="font-size:11px;color:#94A3B8;margin-top:1px;line-height:1.4">A jornada inteira, desde ontem à noite, conta no dia de hoje.</div></div>
    </button>
    <div style="font-size:10.5px;color:#94A3B8;text-align:center;margin-top:4px;line-height:1.5">A jornada continua rodando normalmente enquanto você decide.</div>`;
  openMo('mo-midnight');
}

// mode: 'split' | 'yesterday' | 'today'
async function resolveMidnightSplit(mode) {
  // Se o modal foi aberto pós-encerramento (pelo showJourneySummary), delega
  if (S._midnightIsPostStop) { await resolvePostStopMidnight(mode); return; }
  const g = S.gps;
  if (!g.start) { S._midnightModalOpen = false; closeMo('mo-midnight'); return; } // jornada foi encerrada enquanto o modal estava aberto
  const startDate = _ymd(g.start);
  const todayStr = DB.today();
  const resolvedKey = 'sm_midnight_resolved_' + (g.sessionId||'x') + '_' + todayStr;

  if (mode === 'yesterday') {
    g.forceDate = startDate; // trava a data — ver uso em gpsStop/_persistGpsCloud
  } else if (mode === 'today') {
    g.forceDate = null; // já é o comportamento padrão (date=DB.today() no encerramento)
  } else if (mode === 'split') {
    try { await splitJourneyAtMidnight(startDate, todayStr); }
    catch(e) { console.error('[resolveMidnightSplit] split falhou:', e); showToast('Não consegui dividir agora, tento de novo mais tarde.', 'error'); S._midnightModalOpen = false; closeMo('mo-midnight'); return; }
  }
  localStorage.setItem(resolvedKey, '1');
  S._midnightModalOpen = false;
  closeMo('mo-midnight');
  persistGps();
}

async function splitJourneyAtMidnight(startDate, todayStr) {
  const g = S.gps;
  const midnightMs = new Date(todayStr + 'T00:00:00').getTime();

  // Km exato no instante da virada: prioriza o snapshot capturado pelo
  // GpsService nativo (Kotlin) — ele marca um alarme pro segundo exato da
  // meia-noite e salva o km ali, mesmo com o app em segundo plano. Só cai
  // pra estimativa por tempo (proporcional) se o snapshot não existir —
  // ex: APK antigo sem essa versão, ou app rodou sem o serviço nativo.
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      const snapDate = SmartMobiNative.getMidnightSnapshotDate();
      if (snapDate === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, Date.now() - g.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - g.start));
    kmAtMidnight = (g.km || 0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, g.km || 0); // nunca passa do total acumulado

  // Duração de ontem estimada a partir do pausedMs acumulado até agora —
  // aproximação: assume que a pausa (se houve) ficou toda de um lado ou
  // outro da virada, o que cobre o caso comum (raro pausar bem em cima da
  // meia-noite).
  const elapsedYesterday = Math.max(0, Math.round((midnightMs - g.start - (g.pausedMs||0))/1000));

  // ── Fecha a sessão de ontem ──
  const sessYesterday = { id: g.sessionId, start: g.start, end: midnightMs,
    km: parseFloat(kmAtMidnight.toFixed(3)), elapsed: elapsedYesterday };
  const dayY = DB.day(startDate);
  if (!dayY.sessions) dayY.sessions = [];
  const idxY = dayY.sessions.findIndex(s => s.id === sessYesterday.id);
  if (idxY >= 0) dayY.sessions[idxY] = sessYesterday; else dayY.sessions.push(sessYesterday);
  DB.saveDay(dayY);
  SYNC.push('sessions', { id: sessYesterday.id, date: startDate, start_ms: sessYesterday.start,
    end_ms: sessYesterday.end, km: sessYesterday.km, elapsed_secs: sessYesterday.elapsed,
    paused_ms: 0, is_paused: false });
  try { reconcileSessionsForDay(startDate); } catch(e) {}

  // ── Abre a sessão de hoje, continuando a rodar sem interromper o GPS ──
  const newId = DB.uid();
  const remainingKm = Math.max(0, (g.km || 0) - kmAtMidnight);
  g.sessionId = newId;
  g.start = midnightMs;
  g.km = remainingKm;
  g.pausedMs = 0; g.pa = null;
  g.pts = []; g.lastFixTime = null;
  g.forceDate = null; // sessão nova já nasce em 'hoje' — sem override
  const dayT = DB.day(todayStr);
  g.baseKm = (dayT.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (dayT.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);

  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: newId, user_id: _supaUser.id, date: todayStr,
      start_ms: midnightMs, end_ms: null, km: parseFloat(remainingKm.toFixed(3)),
      elapsed_secs: 0, paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  // Religa o widget/serviço nativo com o novo sessionId/km — sem isso ele
  // continuaria reportando pro id antigo (que já fechamos acima).
  if (window.SmartMobiNative) {
    try { SmartMobiNative.startFloating?.(g.start, g.km || 0); } catch(e) {}
    try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} // consumido — não reaplicar
  }

  showToast('Jornada dividida na meia-noite' + (estimated ? ' (km estimado por tempo — sem GPS exato disponível)' : ' (km exato do GPS)') + '.', 'info', 4500);
  try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
}

// ── Midnight pós-encerramento (acionado pelo showJourneySummary) ──────────
// Reutiliza o mesmo modal mo-midnight, mas com flag _midnightIsPostStop para
// resolveMidnightSplit() delegar ao resolvePostStopMidnight().
function openPostStopMidnightModal(sess, onDone) {
  if (!sess || !sess.start || !sess.end) { if (onDone) onDone(); return; }
  S._midnightIsPostStop = true;
  S._postStopMidnightSess = sess;
  S._postMidnightCallback = onDone || null;
  openMidnightModal(_ymd(sess.start), _ymd(sess.end));
}

async function resolvePostStopMidnight(mode) {
  const sess = S._postStopMidnightSess;
  const onDone = S._postMidnightCallback;
  S._midnightIsPostStop = false;
  S._postStopMidnightSess = null;
  S._postMidnightCallback = null;
  S._midnightModalOpen = false;
  closeMo('mo-midnight');
  if (!sess) { if (onDone) onDone(); return; }

  const startDate = _ymd(sess.start);
  const todayStr = _ymd(sess.end);
  const resolvedKey = 'sm_midnight_resolved_' + (sess.id||'x') + '_' + todayStr;
  localStorage.setItem(resolvedKey, '1');

  if (mode === 'today') {
    // Sessão já foi salva em hoje por gpsStop — nada a mover
    showToast('Jornada mantida em hoje.', 'info');
  } else if (mode === 'yesterday') {
    // Move sessão de hoje para ontem
    const dayToday = DB.day(todayStr);
    dayToday.sessions = (dayToday.sessions||[]).filter(s => s.id !== sess.id);
    const tk = dayToday.sessions.reduce((s,x)=>s+(x.km||0),0);
    dayToday.session = { km:tk, elapsed: dayToday.sessions.reduce((s,x)=>s+(x.elapsed||0),0) };
    DB.saveDay(dayToday);
    const dayYest = DB.day(startDate);
    if (!dayYest.sessions) dayYest.sessions = [];
    if (!dayYest.sessions.find(s => s.id === sess.id)) dayYest.sessions.push(sess);
    DB.saveDay(dayYest);
    SYNC.push('sessions', { id: sess.id, date: startDate, start_ms: sess.start,
      end_ms: sess.end, km: sess.km, elapsed_secs: sess.elapsed, paused_ms:0, is_paused:false });
    showToast('Jornada mantida em ontem.', 'info');
  } else if (mode === 'split') {
    try { await splitPostStopMidnight(sess, startDate, todayStr); }
    catch(e) { console.error('[resolvePostStopMidnight] split falhou:', e); showToast('Erro ao dividir. Tente novamente.','error'); }
  }
  try { renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
  if (onDone) setTimeout(onDone, 300);
}

async function splitPostStopMidnight(sess, startDate, todayStr) {
  const midnightMs = new Date(todayStr + 'T00:00:00').getTime();
  // km na meia-noite: tenta snapshot nativo, senão estima por proporção de tempo
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      if (SmartMobiNative.getMidnightSnapshotDate() === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, sess.end - sess.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - sess.start));
    kmAtMidnight = (sess.km||0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, sess.km||0);
  const elapsedYest = Math.max(0, Math.round((midnightMs - sess.start) / 1000));
  const elapsedToday = Math.max(0, (sess.elapsed||0) - elapsedYest);
  const kmToday = parseFloat(Math.max(0, (sess.km||0) - kmAtMidnight).toFixed(3));
  // Sessão de ontem (mantém o ID original)
  const sessYest = { id: sess.id, start: sess.start, end: midnightMs,
    km: parseFloat(kmAtMidnight.toFixed(3)), elapsed: elapsedYest };
  const dayYest = DB.day(startDate);
  if (!dayYest.sessions) dayYest.sessions = [];
  const iy = dayYest.sessions.findIndex(s => s.id === sess.id);
  if (iy >= 0) dayYest.sessions[iy] = sessYest; else dayYest.sessions.push(sessYest);
  DB.saveDay(dayYest);
  SYNC.push('sessions', { id: sessYest.id, date: startDate, start_ms: sessYest.start,
    end_ms: sessYest.end, km: sessYest.km, elapsed_secs: sessYest.elapsed, paused_ms:0, is_paused:false });
  // Sessão de hoje (novo ID, parte após meia-noite)
  const newId = DB.uid();
  const sessToday = { id: newId, start: midnightMs, end: sess.end, km: kmToday, elapsed: elapsedToday };
  const dayToday = DB.day(todayStr);
  if (!dayToday.sessions) dayToday.sessions = [];
  dayToday.sessions = dayToday.sessions.filter(s => s.id !== sess.id); // remove original
  dayToday.sessions.push(sessToday);
  DB.saveDay(dayToday);
  SYNC.push('sessions', { id: sessToday.id, date: todayStr, start_ms: sessToday.start,
    end_ms: sessToday.end, km: sessToday.km, elapsed_secs: sessToday.elapsed, paused_ms:0, is_paused:false });
  try { reconcileSessionsForDay(startDate); } catch(e) {}
  if (window.SmartMobiNative) { try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} }
  showToast('Jornada dividida na meia-noite' + (estimated?' (km estimado)':' (km do GPS)') + '.', 'info', 4500);
}

function restoreGpsSession() {
  const g = S.gps;
  // Já está rodando nesta instância do app (ex: showApp() foi chamado de novo
  // pelo Supabase reemitindo SIGNED_IN ao voltar do background) — não faz nada,
  // senão sobrescreve o km/timer atuais com o último snapshot salvo e duplica
  // o watchPosition/setInterval, fazendo o km "resetar"/contar dobrado.
  if (g.ti) return;
  const saved = DB.get('sm_gps_session');
  if (!saved || (saved.st !== 'running' && saved.st !== 'paused')) return;
  // Sessão muito antiga (app ficou fechado por mais de 16h) — não tenta retomar
  const ageMs = Date.now() - (saved.start || 0);
  if (!saved.start || ageMs > 16*3600*1000) { DB.del('sm_gps_session'); return; }
  // Defesa extra: se o Supabase já sabe que esta sessão foi encerrada
  // (ex: gpsStop rodou parcialmente numa tentativa anterior — fechou lá
  // mas não apagou o marcador local), NÃO resume. Confia no servidor.
  if (_supaUser && saved.sessionId && navigator.onLine) {
    _SUPA.from('sessions').select('end_ms,km,start_ms').eq('id', saved.sessionId).maybeSingle()
      .then(({data}) => {
        // Recupera km/start do checkpoint na nuvem se forem melhores que o
        // snapshot local (marcador local pode estar defasado se o app ficou
        // muito tempo em background sem JS rodando).
        if (data && !data.end_ms && S.gps.st !== 'idle' && S.gps.sessionId === saved.sessionId) {
          const cloudKm = parseFloat(data.km) || 0;
          if (cloudKm > (S.gps.km || 0)) { S.gps.km = cloudKm; S.gps.pts = []; S.gps.lastFixTime = null; }
          if (data.start_ms && Number(data.start_ms) < (S.gps.start || Infinity)) S.gps.start = Number(data.start_ms);
          try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
        }
        if (data && data.end_ms) {
          console.warn('[restoreGpsSession] Sessão já encerrada no Supabase — descartando marcador local stale');
          DB.del('sm_gps_session');
          if (S.gps.st !== 'idle') {
            S.gps.st = 'idle'; S.gps.start = null;
            if (S.gps.wid) { try{navigator.geolocation.clearWatch(S.gps.wid);}catch(e){} }
            if (S.gps.ti) clearInterval(S.gps.ti);
            try { renderJornada(); renderHome(); } catch(e) {}
          }
        }
      }).catch(()=>{});
  }
  // Limpeza defensiva — garante que não sobra nenhum watcher/timer órfão
  if (g.wid) { try{ navigator.geolocation.clearWatch(g.wid); }catch(e){} }
  if (g.ti) clearInterval(g.ti);
  g.st = saved.st; g.start = saved.start; g.pausedMs = saved.pausedMs || 0; g.pa = saved.pa || null;
  g.km = saved.km || 0; g.pts = saved.lastPt ? [saved.lastPt] : [];
  g.baseKm = saved.baseKm || 0; g.baseElapsed = saved.baseElapsed || 0;
  g.sessionId = saved.sessionId || null; // restaura ID para continuar o upsert correto
  g.forceDate = saved.forceDate || null;
  // O snapshot acima é só o último valor salvo antes do app ir pra segundo plano (o
  // watchPosition do JS para quando o app não está em foreground). Se o app nativo
  // tiver um GpsService rodando, ele é a fonte mais atual — corrige aqui, antes do
  // primeiro render, pra não mostrar um km/tempo defasado por um instante na tela.
  
===== sessions @ 930450 =====
ton class="mn-opt" onclick="resolveMidnightSplit('today')" style="display:flex;align-items:center;gap:12px;width:100%;background:#fff;border:1.5px solid #E2E8F0;border-radius:16px;padding:13px 14px;margin-bottom:10px;text-align:left;cursor:pointer">
      <div style="width:38px;height:38px;border-radius:11px;flex-shrink:0;display:flex;align-items:center;justify-content:center;background:#F1F5F9;color:#64748B"><svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M13 5l7 7-7 7M20 12H4"/></svg></div>
      <div style="flex:1"><div style="font-size:13.5px;font-weight:700;color:#0F172A">Jogar tudo pra hoje (${todayDD})</div>
      <div style="font-size:11px;color:#94A3B8;margin-top:1px;line-height:1.4">A jornada inteira, desde ontem à noite, conta no dia de hoje.</div></div>
    </button>
    <div style="font-size:10.5px;color:#94A3B8;text-align:center;margin-top:4px;line-height:1.5">A jornada continua rodando normalmente enquanto você decide.</div>`;
  openMo('mo-midnight');
}

// mode: 'split' | 'yesterday' | 'today'
async function resolveMidnightSplit(mode) {
  // Se o modal foi aberto pós-encerramento (pelo showJourneySummary), delega
  if (S._midnightIsPostStop) { await resolvePostStopMidnight(mode); return; }
  const g = S.gps;
  if (!g.start) { S._midnightModalOpen = false; closeMo('mo-midnight'); return; } // jornada foi encerrada enquanto o modal estava aberto
  const startDate = _ymd(g.start);
  const todayStr = DB.today();
  const resolvedKey = 'sm_midnight_resolved_' + (g.sessionId||'x') + '_' + todayStr;

  if (mode === 'yesterday') {
    g.forceDate = startDate; // trava a data — ver uso em gpsStop/_persistGpsCloud
  } else if (mode === 'today') {
    g.forceDate = null; // já é o comportamento padrão (date=DB.today() no encerramento)
  } else if (mode === 'split') {
    try { await splitJourneyAtMidnight(startDate, todayStr); }
    catch(e) { console.error('[resolveMidnightSplit] split falhou:', e); showToast('Não consegui dividir agora, tento de novo mais tarde.', 'error'); S._midnightModalOpen = false; closeMo('mo-midnight'); return; }
  }
  localStorage.setItem(resolvedKey, '1');
  S._midnightModalOpen = false;
  closeMo('mo-midnight');
  persistGps();
}

async function splitJourneyAtMidnight(startDate, todayStr) {
  const g = S.gps;
  const midnightMs = new Date(todayStr + 'T00:00:00').getTime();

  // Km exato no instante da virada: prioriza o snapshot capturado pelo
  // GpsService nativo (Kotlin) — ele marca um alarme pro segundo exato da
  // meia-noite e salva o km ali, mesmo com o app em segundo plano. Só cai
  // pra estimativa por tempo (proporcional) se o snapshot não existir —
  // ex: APK antigo sem essa versão, ou app rodou sem o serviço nativo.
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      const snapDate = SmartMobiNative.getMidnightSnapshotDate();
      if (snapDate === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, Date.now() - g.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - g.start));
    kmAtMidnight = (g.km || 0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, g.km || 0); // nunca passa do total acumulado

  // Duração de ontem estimada a partir do pausedMs acumulado até agora —
  // aproximação: assume que a pausa (se houve) ficou toda de um lado ou
  // outro da virada, o que cobre o caso comum (raro pausar bem em cima da
  // meia-noite).
  const elapsedYesterday = Math.max(0, Math.round((midnightMs - g.start - (g.pausedMs||0))/1000));

  // ── Fecha a sessão de ontem ──
  const sessYesterday = { id: g.sessionId, start: g.start, end: midnightMs,
    km: parseFloat(kmAtMidnight.toFixed(3)), elapsed: elapsedYesterday };
  const dayY = DB.day(startDate);
  if (!dayY.sessions) dayY.sessions = [];
  const idxY = dayY.sessions.findIndex(s => s.id === sessYesterday.id);
  if (idxY >= 0) dayY.sessions[idxY] = sessYesterday; else dayY.sessions.push(sessYesterday);
  DB.saveDay(dayY);
  SYNC.push('sessions', { id: sessYesterday.id, date: startDate, start_ms: sessYesterday.start,
    end_ms: sessYesterday.end, km: sessYesterday.km, elapsed_secs: sessYesterday.elapsed,
    paused_ms: 0, is_paused: false });
  try { reconcileSessionsForDay(startDate); } catch(e) {}

  // ── Abre a sessão de hoje, continuando a rodar sem interromper o GPS ──
  const newId = DB.uid();
  const remainingKm = Math.max(0, (g.km || 0) - kmAtMidnight);
  g.sessionId = newId;
  g.start = midnightMs;
  g.km = remainingKm;
  g.pausedMs = 0; g.pa = null;
  g.pts = []; g.lastFixTime = null;
  g.forceDate = null; // sessão nova já nasce em 'hoje' — sem override
  const dayT = DB.day(todayStr);
  g.baseKm = (dayT.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (dayT.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);

  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: newId, user_id: _supaUser.id, date: todayStr,
      start_ms: midnightMs, end_ms: null, km: parseFloat(remainingKm.toFixed(3)),
      elapsed_secs: 0, paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  // Religa o widget/serviço nativo com o novo sessionId/km — sem isso ele
  // continuaria reportando pro id antigo (que já fechamos acima).
  if (window.SmartMobiNative) {
    try { SmartMobiNative.startFloating?.(g.start, g.km || 0); } catch(e) {}
    try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} // consumido — não reaplicar
  }

  showToast('Jornada dividida na meia-noite' + (estimated ? ' (km estimado por tempo — sem GPS exato disponível)' : ' (km exato do GPS)') + '.', 'info', 4500);
  try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
}

// ── Midnight pós-encerramento (acionado pelo showJourneySummary) ──────────
// Reutiliza o mesmo modal mo-midnight, mas com flag _midnightIsPostStop para
// resolveMidnightSplit() delegar ao resolvePostStopMidnight().
function openPostStopMidnightModal(sess, onDone) {
  if (!sess || !sess.start || !sess.end) { if (onDone) onDone(); return; }
  S._midnightIsPostStop = true;
  S._postStopMidnightSess = sess;
  S._postMidnightCallback = onDone || null;
  openMidnightModal(_ymd(sess.start), _ymd(sess.end));
}

async function resolvePostStopMidnight(mode) {
  const sess = S._postStopMidnightSess;
  const onDone = S._postMidnightCallback;
  S._midnightIsPostStop = false;
  S._postStopMidnightSess = null;
  S._postMidnightCallback = null;
  S._midnightModalOpen = false;
  closeMo('mo-midnight');
  if (!sess) { if (onDone) onDone(); return; }

  const startDate = _ymd(sess.start);
  const todayStr = _ymd(sess.end);
  const resolvedKey = 'sm_midnight_resolved_' + (sess.id||'x') + '_' + todayStr;
  localStorage.setItem(resolvedKey, '1');

  if (mode === 'today') {
    // Sessão já foi salva em hoje por gpsStop — nada a mover
    showToast('Jornada mantida em hoje.', 'info');
  } else if (mode === 'yesterday') {
    // Move sessão de hoje para ontem
    const dayToday = DB.day(todayStr);
    dayToday.sessions = (dayToday.sessions||[]).filter(s => s.id !== sess.id);
    const tk = dayToday.sessions.reduce((s,x)=>s+(x.km||0),0);
    dayToday.session = { km:tk, elapsed: dayToday.sessions.reduce((s,x)=>s+(x.elapsed||0),0) };
    DB.saveDay(dayToday);
    const dayYest = DB.day(startDate);
    if (!dayYest.sessions) dayYest.sessions = [];
    if (!dayYest.sessions.find(s => s.id === sess.id)) dayYest.sessions.push(sess);
    DB.saveDay(dayYest);
    SYNC.push('sessions', { id: sess.id, date: startDate, start_ms: sess.start,
      end_ms: sess.end, km: sess.km, elapsed_secs: sess.elapsed, paused_ms:0, is_paused:false });
    showToast('Jornada mantida em ontem.', 'info');
  } else if (mode === 'split') {
    try { await splitPostStopMidnight(sess, startDate, todayStr); }
    catch(e) { console.error('[resolvePostStopMidnight] split falhou:', e); showToast('Erro ao dividir. Tente novamente.','error'); }
  }
  try { renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
  if (onDone) setTimeout(onDone, 300);
}

async function splitPostStopMidnight(sess, startDate, todayStr) {
  const midnightMs = new Date(todayStr + 'T00:00:00').getTime();
  // km na meia-noite: tenta snapshot nativo, senão estima por proporção de tempo
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      if (SmartMobiNative.getMidnightSnapshotDate() === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, sess.end - sess.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - sess.start));
    kmAtMidnight = (sess.km||0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, sess.km||0);
  const elapsedYest = Math.max(0, Math.round((midnightMs - sess.start) / 1000));
  const elapsedToday = Math.max(0, (sess.elapsed||0) - elapsedYest);
  const kmToday = parseFloat(Math.max(0, (sess.km||0) - kmAtMidnight).toFixed(3));
  // Sessão de ontem (mantém o ID original)
  const sessYest = { id: sess.id, start: sess.start, end: midnightMs,
    km: parseFloat(kmAtMidnight.toFixed(3)), elapsed: elapsedYest };
  const dayYest = DB.day(startDate);
  if (!dayYest.sessions) dayYest.sessions = [];
  const iy = dayYest.sessions.findIndex(s => s.id === sess.id);
  if (iy >= 0) dayYest.sessions[iy] = sessYest; else dayYest.sessions.push(sessYest);
  DB.saveDay(dayYest);
  SYNC.push('sessions', { id: sessYest.id, date: startDate, start_ms: sessYest.start,
    end_ms: sessYest.end, km: sessYest.km, elapsed_secs: sessYest.elapsed, paused_ms:0, is_paused:false });
  // Sessão de hoje (novo ID, parte após meia-noite)
  const newId = DB.uid();
  const sessToday = { id: newId, start: midnightMs, end: sess.end, km: kmToday, elapsed: elapsedToday };
  const dayToday = DB.day(todayStr);
  if (!dayToday.sessions) dayToday.sessions = [];
  dayToday.sessions = dayToday.sessions.filter(s => s.id !== sess.id); // remove original
  dayToday.sessions.push(sessToday);
  DB.saveDay(dayToday);
  SYNC.push('sessions', { id: sessToday.id, date: todayStr, start_ms: sessToday.start,
    end_ms: sessToday.end, km: sessToday.km, elapsed_secs: sessToday.elapsed, paused_ms:0, is_paused:false });
  try { reconcileSessionsForDay(startDate); } catch(e) {}
  if (window.SmartMobiNative) { try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} }
  showToast('Jornada dividida na meia-noite' + (estimated?' (km estimado)':' (km do GPS)') + '.', 'info', 4500);
}

function restoreGpsSession() {
  const g = S.gps;
  // Já está rodando nesta instância do app (ex: showApp() foi chamado de novo
  // pelo Supabase reemitindo SIGNED_IN ao voltar do background) — não faz nada,
  // senão sobrescreve o km/timer atuais com o último snapshot salvo e duplica
  // o watchPosition/setInterval, fazendo o km "resetar"/contar dobrado.
  if (g.ti) return;
  const saved = DB.get('sm_gps_session');
  if (!saved || (saved.st !== 'running' && saved.st !== 'paused')) return;
  // Sessão muito antiga (app ficou fechado por mais de 16h) — não tenta retomar
  const ageMs = Date.now() - (saved.start || 0);
  if (!saved.start || ageMs > 16*3600*1000) { DB.del('sm_gps_session'); return; }
  // Defesa extra: se o Supabase já sabe que esta sessão foi encerrada
  // (ex: gpsStop rodou parcialmente numa tentativa anterior — fechou lá
  // mas não apagou o marcador local), NÃO resume. Confia no servidor.
  if (_supaUser && saved.sessionId && navigator.onLine) {
    _SUPA.from('sessions').select('end_ms,km,start_ms').eq('id', saved.sessionId).maybeSingle()
      .then(({data}) => {
        // Recupera km/start do checkpoint na nuvem se forem melhores que o
        // snapshot local (marcador local pode estar defasado se o app ficou
        // muito tempo em background sem JS rodando).
        if (data && !data.end_ms && S.gps.st !== 'idle' && S.gps.sessionId === saved.sessionId) {
          const cloudKm = parseFloat(data.km) || 0;
          if (cloudKm > (S.gps.km || 0)) { S.gps.km = cloudKm; S.gps.pts = []; S.gps.lastFixTime = null; }
          if (data.start_ms && Number(data.start_ms) < (S.gps.start || Infinity)) S.gps.start = Number(data.start_ms);
          try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
        }
        if (data && data.end_ms) {
          console.warn('[restoreGpsSession] Sessão já encerrada no Supabase — descartando marcador local stale');
          DB.del('sm_gps_session');
          if (S.gps.st !== 'idle') {
            S.gps.st = 'idle'; S.gps.start = null;
            if (S.gps.wid) { try{navigator.geolocation.clearWatch(S.gps.wid);}catch(e){} }
            if (S.gps.ti) clearInterval(S.gps.ti);
            try { renderJornada(); renderHome(); } catch(e) {}
          }
        }
      }).catch(()=>{});
  }
  // Limpeza defensiva — garante que não sobra nenhum watcher/timer órfão
  if (g.wid) { try{ navigator.geolocation.clearWatch(g.wid); }catch(e){} }
  if (g.ti) clearInterval(g.ti);
  g.st = saved.st; g.start = saved.start; g.pausedMs = saved.pausedMs || 0; g.pa = saved.pa || null;
  g.km = saved.km || 0; g.pts = saved.lastPt ? [saved.lastPt] : [];
  g.baseKm = saved.baseKm || 0; g.baseElapsed = saved.baseElapsed || 0;
  g.sessionId = saved.sessionId || null; // restaura ID para continuar o upsert correto
  g.forceDate = saved.forceDate || null;
  // O snapshot acima é só o último valor salvo antes do app ir pra segundo plano (o
  // watchPosition do JS para quando o app não está em foreground). Se o app nativo
  // tiver um GpsService rodando, ele é a fonte mais atual — corrige aqui, antes do
  // primeiro render, pra não mostrar um km/tempo defasado por um instante na tela.
  if (window.SmartMobiNative && typeof SmartMobiNative.isGpsRunning === 'function') {
    try {
      if (SmartMobiNative.isGpsRunning()) {
        // So confia no km nativo se a sessao ja estiver rodando ha mais de alguns
        // segundos. Logo apos um gpsStart(), o sinal de reset enviado pro servico
        // nativo pode ainda nao ter sido processado — ler o km nesse instante corre
        // o risco de pegar um valor antigo (de antes do reset), fazendo o numero
        // "pular" pra um valor maior por um instante ate se corrigir sozinho.
        const sessionAgeMs = Date.now() - (g.start || 0);
        if (sessionAgeMs > 8000) {
          const nativeKm = SmartMobiNative.getGpsKm();
          const nativeStart = SmartMobiNative.getGpsStartTime();
          const nativePaused = SmartM
===== sessions @ 932789 =====
gps;
  const midnightMs = new Date(todayStr + 'T00:00:00').getTime();

  // Km exato no instante da virada: prioriza o snapshot capturado pelo
  // GpsService nativo (Kotlin) — ele marca um alarme pro segundo exato da
  // meia-noite e salva o km ali, mesmo com o app em segundo plano. Só cai
  // pra estimativa por tempo (proporcional) se o snapshot não existir —
  // ex: APK antigo sem essa versão, ou app rodou sem o serviço nativo.
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      const snapDate = SmartMobiNative.getMidnightSnapshotDate();
      if (snapDate === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, Date.now() - g.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - g.start));
    kmAtMidnight = (g.km || 0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, g.km || 0); // nunca passa do total acumulado

  // Duração de ontem estimada a partir do pausedMs acumulado até agora —
  // aproximação: assume que a pausa (se houve) ficou toda de um lado ou
  // outro da virada, o que cobre o caso comum (raro pausar bem em cima da
  // meia-noite).
  const elapsedYesterday = Math.max(0, Math.round((midnightMs - g.start - (g.pausedMs||0))/1000));

  // ── Fecha a sessão de ontem ──
  const sessYesterday = { id: g.sessionId, start: g.start, end: midnightMs,
    km: parseFloat(kmAtMidnight.toFixed(3)), elapsed: elapsedYesterday };
  const dayY = DB.day(startDate);
  if (!dayY.sessions) dayY.sessions = [];
  const idxY = dayY.sessions.findIndex(s => s.id === sessYesterday.id);
  if (idxY >= 0) dayY.sessions[idxY] = sessYesterday; else dayY.sessions.push(sessYesterday);
  DB.saveDay(dayY);
  SYNC.push('sessions', { id: sessYesterday.id, date: startDate, start_ms: sessYesterday.start,
    end_ms: sessYesterday.end, km: sessYesterday.km, elapsed_secs: sessYesterday.elapsed,
    paused_ms: 0, is_paused: false });
  try { reconcileSessionsForDay(startDate); } catch(e) {}

  // ── Abre a sessão de hoje, continuando a rodar sem interromper o GPS ──
  const newId = DB.uid();
  const remainingKm = Math.max(0, (g.km || 0) - kmAtMidnight);
  g.sessionId = newId;
  g.start = midnightMs;
  g.km = remainingKm;
  g.pausedMs = 0; g.pa = null;
  g.pts = []; g.lastFixTime = null;
  g.forceDate = null; // sessão nova já nasce em 'hoje' — sem override
  const dayT = DB.day(todayStr);
  g.baseKm = (dayT.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (dayT.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);

  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: newId, user_id: _supaUser.id, date: todayStr,
      start_ms: midnightMs, end_ms: null, km: parseFloat(remainingKm.toFixed(3)),
      elapsed_secs: 0, paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  // Religa o widget/serviço nativo com o novo sessionId/km — sem isso ele
  // continuaria reportando pro id antigo (que já fechamos acima).
  if (window.SmartMobiNative) {
    try { SmartMobiNative.startFloating?.(g.start, g.km || 0); } catch(e) {}
    try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} // consumido — não reaplicar
  }

  showToast('Jornada dividida na meia-noite' + (estimated ? ' (km estimado por tempo — sem GPS exato disponível)' : ' (km exato do GPS)') + '.', 'info', 4500);
  try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
}

// ── Midnight pós-encerramento (acionado pelo showJourneySummary) ──────────
// Reutiliza o mesmo modal mo-midnight, mas com flag _midnightIsPostStop para
// resolveMidnightSplit() delegar ao resolvePostStopMidnight().
function openPostStopMidnightModal(sess, onDone) {
  if (!sess || !sess.start || !sess.end) { if (onDone) onDone(); return; }
  S._midnightIsPostStop = true;
  S._postStopMidnightSess = sess;
  S._postMidnightCallback = onDone || null;
  openMidnightModal(_ymd(sess.start), _ymd(sess.end));
}

async function resolvePostStopMidnight(mode) {
  const sess = S._postStopMidnightSess;
  const onDone = S._postMidnightCallback;
  S._midnightIsPostStop = false;
  S._postStopMidnightSess = null;
  S._postMidnightCallback = null;
  S._midnightModalOpen = false;
  closeMo('mo-midnight');
  if (!sess) { if (onDone) onDone(); return; }

  const startDate = _ymd(sess.start);
  const todayStr = _ymd(sess.end);
  const resolvedKey = 'sm_midnight_resolved_' + (sess.id||'x') + '_' + todayStr;
  localStorage.setItem(resolvedKey, '1');

  if (mode === 'today') {
    // Sessão já foi salva em hoje por gpsStop — nada a mover
    showToast('Jornada mantida em hoje.', 'info');
  } else if (mode === 'yesterday') {
    // Move sessão de hoje para ontem
    const dayToday = DB.day(todayStr);
    dayToday.sessions = (dayToday.sessions||[]).filter(s => s.id !== sess.id);
    const tk = dayToday.sessions.reduce((s,x)=>s+(x.km||0),0);
    dayToday.session = { km:tk, elapsed: dayToday.sessions.reduce((s,x)=>s+(x.elapsed||0),0) };
    DB.saveDay(dayToday);
    const dayYest = DB.day(startDate);
    if (!dayYest.sessions) dayYest.sessions = [];
    if (!dayYest.sessions.find(s => s.id === sess.id)) dayYest.sessions.push(sess);
    DB.saveDay(dayYest);
    SYNC.push('sessions', { id: sess.id, date: startDate, start_ms: sess.start,
      end_ms: sess.end, km: sess.km, elapsed_secs: sess.elapsed, paused_ms:0, is_paused:false });
    showToast('Jornada mantida em ontem.', 'info');
  } else if (mode === 'split') {
    try { await splitPostStopMidnight(sess, startDate, todayStr); }
    catch(e) { console.error('[resolvePostStopMidnight] split falhou:', e); showToast('Erro ao dividir. Tente novamente.','error'); }
  }
  try { renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
  if (onDone) setTimeout(onDone, 300);
}

async function splitPostStopMidnight(sess, startDate, todayStr) {
  const midnightMs = new Date(todayStr + 'T00:00:00').getTime();
  // km na meia-noite: tenta snapshot nativo, senão estima por proporção de tempo
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      if (SmartMobiNative.getMidnightSnapshotDate() === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, sess.end - sess.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - sess.start));
    kmAtMidnight = (sess.km||0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, sess.km||0);
  const elapsedYest = Math.max(0, Math.round((midnightMs - sess.start) / 1000));
  const elapsedToday = Math.max(0, (sess.elapsed||0) - elapsedYest);
  const kmToday = parseFloat(Math.max(0, (sess.km||0) - kmAtMidnight).toFixed(3));
  // Sessão de ontem (mantém o ID original)
  const sessYest = { id: sess.id, start: sess.start, end: midnightMs,
    km: parseFloat(kmAtMidnight.toFixed(3)), elapsed: elapsedYest };
  const dayYest = DB.day(startDate);
  if (!dayYest.sessions) dayYest.sessions = [];
  const iy = dayYest.sessions.findIndex(s => s.id === sess.id);
  if (iy >= 0) dayYest.sessions[iy] = sessYest; else dayYest.sessions.push(sessYest);
  DB.saveDay(dayYest);
  SYNC.push('sessions', { id: sessYest.id, date: startDate, start_ms: sessYest.start,
    end_ms: sessYest.end, km: sessYest.km, elapsed_secs: sessYest.elapsed, paused_ms:0, is_paused:false });
  // Sessão de hoje (novo ID, parte após meia-noite)
  const newId = DB.uid();
  const sessToday = { id: newId, start: midnightMs, end: sess.end, km: kmToday, elapsed: elapsedToday };
  const dayToday = DB.day(todayStr);
  if (!dayToday.sessions) dayToday.sessions = [];
  dayToday.sessions = dayToday.sessions.filter(s => s.id !== sess.id); // remove original
  dayToday.sessions.push(sessToday);
  DB.saveDay(dayToday);
  SYNC.push('sessions', { id: sessToday.id, date: todayStr, start_ms: sessToday.start,
    end_ms: sessToday.end, km: sessToday.km, elapsed_secs: sessToday.elapsed, paused_ms:0, is_paused:false });
  try { reconcileSessionsForDay(startDate); } catch(e) {}
  if (window.SmartMobiNative) { try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} }
  showToast('Jornada dividida na meia-noite' + (estimated?' (km estimado)':' (km do GPS)') + '.', 'info', 4500);
}

function restoreGpsSession() {
  const g = S.gps;
  // Já está rodando nesta instância do app (ex: showApp() foi chamado de novo
  // pelo Supabase reemitindo SIGNED_IN ao voltar do background) — não faz nada,
  // senão sobrescreve o km/timer atuais com o último snapshot salvo e duplica
  // o watchPosition/setInterval, fazendo o km "resetar"/contar dobrado.
  if (g.ti) return;
  const saved = DB.get('sm_gps_session');
  if (!saved || (saved.st !== 'running' && saved.st !== 'paused')) return;
  // Sessão muito antiga (app ficou fechado por mais de 16h) — não tenta retomar
  const ageMs = Date.now() - (saved.start || 0);
  if (!saved.start || ageMs > 16*3600*1000) { DB.del('sm_gps_session'); return; }
  // Defesa extra: se o Supabase já sabe que esta sessão foi encerrada
  // (ex: gpsStop rodou parcialmente numa tentativa anterior — fechou lá
  // mas não apagou o marcador local), NÃO resume. Confia no servidor.
  if (_supaUser && saved.sessionId && navigator.onLine) {
    _SUPA.from('sessions').select('end_ms,km,start_ms').eq('id', saved.sessionId).maybeSingle()
      .then(({data}) => {
        // Recupera km/start do checkpoint na nuvem se forem melhores que o
        // snapshot local (marcador local pode estar defasado se o app ficou
        // muito tempo em background sem JS rodando).
        if (data && !data.end_ms && S.gps.st !== 'idle' && S.gps.sessionId === saved.sessionId) {
          const cloudKm = parseFloat(data.km) || 0;
          if (cloudKm > (S.gps.km || 0)) { S.gps.km = cloudKm; S.gps.pts = []; S.gps.lastFixTime = null; }
          if (data.start_ms && Number(data.start_ms) < (S.gps.start || Infinity)) S.gps.start = Number(data.start_ms);
          try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
        }
        if (data && data.end_ms) {
          console.warn('[restoreGpsSession] Sessão já encerrada no Supabase — descartando marcador local stale');
          DB.del('sm_gps_session');
          if (S.gps.st !== 'idle') {
            S.gps.st = 'idle'; S.gps.start = null;
            if (S.gps.wid) { try{navigator.geolocation.clearWatch(S.gps.wid);}catch(e){} }
            if (S.gps.ti) clearInterval(S.gps.ti);
            try { renderJornada(); renderHome(); } catch(e) {}
          }
        }
      }).catch(()=>{});
  }
  // Limpeza defensiva — garante que não sobra nenhum watcher/timer órfão
  if (g.wid) { try{ navigator.geolocation.clearWatch(g.wid); }catch(e){} }
  if (g.ti) clearInterval(g.ti);
  g.st = saved.st; g.start = saved.start; g.pausedMs = saved.pausedMs || 0; g.pa = saved.pa || null;
  g.km = saved.km || 0; g.pts = saved.lastPt ? [saved.lastPt] : [];
  g.baseKm = saved.baseKm || 0; g.baseElapsed = saved.baseElapsed || 0;
  g.sessionId = saved.sessionId || null; // restaura ID para continuar o upsert correto
  g.forceDate = saved.forceDate || null;
  // O snapshot acima é só o último valor salvo antes do app ir pra segundo plano (o
  // watchPosition do JS para quando o app não está em foreground). Se o app nativo
  // tiver um GpsService rodando, ele é a fonte mais atual — corrige aqui, antes do
  // primeiro render, pra não mostrar um km/tempo defasado por um instante na tela.
  if (window.SmartMobiNative && typeof SmartMobiNative.isGpsRunning === 'function') {
    try {
      if (SmartMobiNative.isGpsRunning()) {
        // So confia no km nativo se a sessao ja estiver rodando ha mais de alguns
        // segundos. Logo apos um gpsStart(), o sinal de reset enviado pro servico
        // nativo pode ainda nao ter sido processado — ler o km nesse instante corre
        // o risco de pegar um valor antigo (de antes do reset), fazendo o numero
        // "pular" pra um valor maior por um instante ate se corrigir sozinho.
        const sessionAgeMs = Date.now() - (g.start || 0);
        if (sessionAgeMs > 8000) {
          const nativeKm = SmartMobiNative.getGpsKm();
          const nativeStart = SmartMobiNative.getGpsStartTime();
          const nativePaused = SmartMobiNative.getGpsPausedMs();
          // Mesmo guard de plausibilidade do nativeSyncGps — protege contra
          // herdar km de uma sessão fantasma anterior que o nativo nunca zerou
          const elapsedMinR = sessionAgeMs / 60000;
          const maxPlausibleR = (elapsedMinR * 3) + 0.5;
          if (typeof nativeKm === 'number' && nativeKm > maxPlausibleR) {
            console.warn('[restoreGpsSession] km nativo implausível ignorado:', nativeKm, 'em', elapsedMinR.toFixed(1), 'min');
          } else
          // So adota o km nativo se ele for MAIOR que o que a sessao web ja tem.
          // Nunca deixa essa reconciliacao DIMINUIR o km exibido — isso e o que
          // causava o "pulo pra baixo" quando havia uma correcao manual (kmAdjust)
          // aplicada: o servico nativo nao sabe nada sobre essa correcao, entao
          // confiar nele sem essa guarda jogava o numero de volta pro valor cru,
          // descartando a edicao do usuario.
          if (typeof nativeKm === 'number' && nativeKm > g.km) {
            g.km = nativeKm;
            // Limpa referencia JS para evitar dupla contagem no primeiro fix
            g.pts = [];
            g.lastFixTime = null;
          }
          // GUARD: só adota o start nativo se for ANTERIOR ao que já temos.
          // Se o Android matou e reiniciou o GpsService (START_STICKY), o
          // start nativo vira a hora do RESTART — adotar ele fazia a jornada
          // "recomeçar" e o upsert (mesmo sessionId) sobrescrevia o start_ms
          // da sessão no Supabase, perdendo todo o histórico do dia.
          if (nativeStart && nativeStart < g.start) g.start = nativeStart;
          else if (nativeStart && nativeStart > g.start + 60000) {
            // Serviço nativo reiniciou do zero — re-semeia com o estado correto
            console.warn('[restoreGpsSession] Serviço nativo reiniciado (start nativo mais novo) — re-semeando com start/km da sessão real');
            try { SmartMobiNative.startFloating?.(g.start, g.km || 0); } catch(e) {}
          }
          // Guard: não diminui pausedMs com valor menor do nativo
          if (typeof nativePaused === 'number' && nativePaused >= (g.pausedMs || 0)) {
            g.pausedMs = nativePaused;
          }
        }
      }
    } catch(e) {}
  }
  syncNavButton();
  g.ti = setInterval(gps
===== sessions @ 933114 =====
roporcional) se o snapshot não existir —
  // ex: APK antigo sem essa versão, ou app rodou sem o serviço nativo.
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      const snapDate = SmartMobiNative.getMidnightSnapshotDate();
      if (snapDate === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, Date.now() - g.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - g.start));
    kmAtMidnight = (g.km || 0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, g.km || 0); // nunca passa do total acumulado

  // Duração de ontem estimada a partir do pausedMs acumulado até agora —
  // aproximação: assume que a pausa (se houve) ficou toda de um lado ou
  // outro da virada, o que cobre o caso comum (raro pausar bem em cima da
  // meia-noite).
  const elapsedYesterday = Math.max(0, Math.round((midnightMs - g.start - (g.pausedMs||0))/1000));

  // ── Fecha a sessão de ontem ──
  const sessYesterday = { id: g.sessionId, start: g.start, end: midnightMs,
    km: parseFloat(kmAtMidnight.toFixed(3)), elapsed: elapsedYesterday };
  const dayY = DB.day(startDate);
  if (!dayY.sessions) dayY.sessions = [];
  const idxY = dayY.sessions.findIndex(s => s.id === sessYesterday.id);
  if (idxY >= 0) dayY.sessions[idxY] = sessYesterday; else dayY.sessions.push(sessYesterday);
  DB.saveDay(dayY);
  SYNC.push('sessions', { id: sessYesterday.id, date: startDate, start_ms: sessYesterday.start,
    end_ms: sessYesterday.end, km: sessYesterday.km, elapsed_secs: sessYesterday.elapsed,
    paused_ms: 0, is_paused: false });
  try { reconcileSessionsForDay(startDate); } catch(e) {}

  // ── Abre a sessão de hoje, continuando a rodar sem interromper o GPS ──
  const newId = DB.uid();
  const remainingKm = Math.max(0, (g.km || 0) - kmAtMidnight);
  g.sessionId = newId;
  g.start = midnightMs;
  g.km = remainingKm;
  g.pausedMs = 0; g.pa = null;
  g.pts = []; g.lastFixTime = null;
  g.forceDate = null; // sessão nova já nasce em 'hoje' — sem override
  const dayT = DB.day(todayStr);
  g.baseKm = (dayT.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (dayT.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);

  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: newId, user_id: _supaUser.id, date: todayStr,
      start_ms: midnightMs, end_ms: null, km: parseFloat(remainingKm.toFixed(3)),
      elapsed_secs: 0, paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  // Religa o widget/serviço nativo com o novo sessionId/km — sem isso ele
  // continuaria reportando pro id antigo (que já fechamos acima).
  if (window.SmartMobiNative) {
    try { SmartMobiNative.startFloating?.(g.start, g.km || 0); } catch(e) {}
    try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} // consumido — não reaplicar
  }

  showToast('Jornada dividida na meia-noite' + (estimated ? ' (km estimado por tempo — sem GPS exato disponível)' : ' (km exato do GPS)') + '.', 'info', 4500);
  try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
}

// ── Midnight pós-encerramento (acionado pelo showJourneySummary) ──────────
// Reutiliza o mesmo modal mo-midnight, mas com flag _midnightIsPostStop para
// resolveMidnightSplit() delegar ao resolvePostStopMidnight().
function openPostStopMidnightModal(sess, onDone) {
  if (!sess || !sess.start || !sess.end) { if (onDone) onDone(); return; }
  S._midnightIsPostStop = true;
  S._postStopMidnightSess = sess;
  S._postMidnightCallback = onDone || null;
  openMidnightModal(_ymd(sess.start), _ymd(sess.end));
}

async function resolvePostStopMidnight(mode) {
  const sess = S._postStopMidnightSess;
  const onDone = S._postMidnightCallback;
  S._midnightIsPostStop = false;
  S._postStopMidnightSess = null;
  S._postMidnightCallback = null;
  S._midnightModalOpen = false;
  closeMo('mo-midnight');
  if (!sess) { if (onDone) onDone(); return; }

  const startDate = _ymd(sess.start);
  const todayStr = _ymd(sess.end);
  const resolvedKey = 'sm_midnight_resolved_' + (sess.id||'x') + '_' + todayStr;
  localStorage.setItem(resolvedKey, '1');

  if (mode === 'today') {
    // Sessão já foi salva em hoje por gpsStop — nada a mover
    showToast('Jornada mantida em hoje.', 'info');
  } else if (mode === 'yesterday') {
    // Move sessão de hoje para ontem
    const dayToday = DB.day(todayStr);
    dayToday.sessions = (dayToday.sessions||[]).filter(s => s.id !== sess.id);
    const tk = dayToday.sessions.reduce((s,x)=>s+(x.km||0),0);
    dayToday.session = { km:tk, elapsed: dayToday.sessions.reduce((s,x)=>s+(x.elapsed||0),0) };
    DB.saveDay(dayToday);
    const dayYest = DB.day(startDate);
    if (!dayYest.sessions) dayYest.sessions = [];
    if (!dayYest.sessions.find(s => s.id === sess.id)) dayYest.sessions.push(sess);
    DB.saveDay(dayYest);
    SYNC.push('sessions', { id: sess.id, date: startDate, start_ms: sess.start,
      end_ms: sess.end, km: sess.km, elapsed_secs: sess.elapsed, paused_ms:0, is_paused:false });
    showToast('Jornada mantida em ontem.', 'info');
  } else if (mode === 'split') {
    try { await splitPostStopMidnight(sess, startDate, todayStr); }
    catch(e) { console.error('[resolvePostStopMidnight] split falhou:', e); showToast('Erro ao dividir. Tente novamente.','error'); }
  }
  try { renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
  if (onDone) setTimeout(onDone, 300);
}

async function splitPostStopMidnight(sess, startDate, todayStr) {
  const midnightMs = new Date(todayStr + 'T00:00:00').getTime();
  // km na meia-noite: tenta snapshot nativo, senão estima por proporção de tempo
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      if (SmartMobiNative.getMidnightSnapshotDate() === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, sess.end - sess.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - sess.start));
    kmAtMidnight = (sess.km||0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, sess.km||0);
  const elapsedYest = Math.max(0, Math.round((midnightMs - sess.start) / 1000));
  const elapsedToday = Math.max(0, (sess.elapsed||0) - elapsedYest);
  const kmToday = parseFloat(Math.max(0, (sess.km||0) - kmAtMidnight).toFixed(3));
  // Sessão de ontem (mantém o ID original)
  const sessYest = { id: sess.id, start: sess.start, end: midnightMs,
    km: parseFloat(kmAtMidnight.toFixed(3)), elapsed: elapsedYest };
  const dayYest = DB.day(startDate);
  if (!dayYest.sessions) dayYest.sessions = [];
  const iy = dayYest.sessions.findIndex(s => s.id === sess.id);
  if (iy >= 0) dayYest.sessions[iy] = sessYest; else dayYest.sessions.push(sessYest);
  DB.saveDay(dayYest);
  SYNC.push('sessions', { id: sessYest.id, date: startDate, start_ms: sessYest.start,
    end_ms: sessYest.end, km: sessYest.km, elapsed_secs: sessYest.elapsed, paused_ms:0, is_paused:false });
  // Sessão de hoje (novo ID, parte após meia-noite)
  const newId = DB.uid();
  const sessToday = { id: newId, start: midnightMs, end: sess.end, km: kmToday, elapsed: elapsedToday };
  const dayToday = DB.day(todayStr);
  if (!dayToday.sessions) dayToday.sessions = [];
  dayToday.sessions = dayToday.sessions.filter(s => s.id !== sess.id); // remove original
  dayToday.sessions.push(sessToday);
  DB.saveDay(dayToday);
  SYNC.push('sessions', { id: sessToday.id, date: todayStr, start_ms: sessToday.start,
    end_ms: sessToday.end, km: sessToday.km, elapsed_secs: sessToday.elapsed, paused_ms:0, is_paused:false });
  try { reconcileSessionsForDay(startDate); } catch(e) {}
  if (window.SmartMobiNative) { try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} }
  showToast('Jornada dividida na meia-noite' + (estimated?' (km estimado)':' (km do GPS)') + '.', 'info', 4500);
}

function restoreGpsSession() {
  const g = S.gps;
  // Já está rodando nesta instância do app (ex: showApp() foi chamado de novo
  // pelo Supabase reemitindo SIGNED_IN ao voltar do background) — não faz nada,
  // senão sobrescreve o km/timer atuais com o último snapshot salvo e duplica
  // o watchPosition/setInterval, fazendo o km "resetar"/contar dobrado.
  if (g.ti) return;
  const saved = DB.get('sm_gps_session');
  if (!saved || (saved.st !== 'running' && saved.st !== 'paused')) return;
  // Sessão muito antiga (app ficou fechado por mais de 16h) — não tenta retomar
  const ageMs = Date.now() - (saved.start || 0);
  if (!saved.start || ageMs > 16*3600*1000) { DB.del('sm_gps_session'); return; }
  // Defesa extra: se o Supabase já sabe que esta sessão foi encerrada
  // (ex: gpsStop rodou parcialmente numa tentativa anterior — fechou lá
  // mas não apagou o marcador local), NÃO resume. Confia no servidor.
  if (_supaUser && saved.sessionId && navigator.onLine) {
    _SUPA.from('sessions').select('end_ms,km,start_ms').eq('id', saved.sessionId).maybeSingle()
      .then(({data}) => {
        // Recupera km/start do checkpoint na nuvem se forem melhores que o
        // snapshot local (marcador local pode estar defasado se o app ficou
        // muito tempo em background sem JS rodando).
        if (data && !data.end_ms && S.gps.st !== 'idle' && S.gps.sessionId === saved.sessionId) {
          const cloudKm = parseFloat(data.km) || 0;
          if (cloudKm > (S.gps.km || 0)) { S.gps.km = cloudKm; S.gps.pts = []; S.gps.lastFixTime = null; }
          if (data.start_ms && Number(data.start_ms) < (S.gps.start || Infinity)) S.gps.start = Number(data.start_ms);
          try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
        }
        if (data && data.end_ms) {
          console.warn('[restoreGpsSession] Sessão já encerrada no Supabase — descartando marcador local stale');
          DB.del('sm_gps_session');
          if (S.gps.st !== 'idle') {
            S.gps.st = 'idle'; S.gps.start = null;
            if (S.gps.wid) { try{navigator.geolocation.clearWatch(S.gps.wid);}catch(e){} }
            if (S.gps.ti) clearInterval(S.gps.ti);
            try { renderJornada(); renderHome(); } catch(e) {}
          }
        }
      }).catch(()=>{});
  }
  // Limpeza defensiva — garante que não sobra nenhum watcher/timer órfão
  if (g.wid) { try{ navigator.geolocation.clearWatch(g.wid); }catch(e){} }
  if (g.ti) clearInterval(g.ti);
  g.st = saved.st; g.start = saved.start; g.pausedMs = saved.pausedMs || 0; g.pa = saved.pa || null;
  g.km = saved.km || 0; g.pts = saved.lastPt ? [saved.lastPt] : [];
  g.baseKm = saved.baseKm || 0; g.baseElapsed = saved.baseElapsed || 0;
  g.sessionId = saved.sessionId || null; // restaura ID para continuar o upsert correto
  g.forceDate = saved.forceDate || null;
  // O snapshot acima é só o último valor salvo antes do app ir pra segundo plano (o
  // watchPosition do JS para quando o app não está em foreground). Se o app nativo
  // tiver um GpsService rodando, ele é a fonte mais atual — corrige aqui, antes do
  // primeiro render, pra não mostrar um km/tempo defasado por um instante na tela.
  if (window.SmartMobiNative && typeof SmartMobiNative.isGpsRunning === 'function') {
    try {
      if (SmartMobiNative.isGpsRunning()) {
        // So confia no km nativo se a sessao ja estiver rodando ha mais de alguns
        // segundos. Logo apos um gpsStart(), o sinal de reset enviado pro servico
        // nativo pode ainda nao ter sido processado — ler o km nesse instante corre
        // o risco de pegar um valor antigo (de antes do reset), fazendo o numero
        // "pular" pra um valor maior por um instante ate se corrigir sozinho.
        const sessionAgeMs = Date.now() - (g.start || 0);
        if (sessionAgeMs > 8000) {
          const nativeKm = SmartMobiNative.getGpsKm();
          const nativeStart = SmartMobiNative.getGpsStartTime();
          const nativePaused = SmartMobiNative.getGpsPausedMs();
          // Mesmo guard de plausibilidade do nativeSyncGps — protege contra
          // herdar km de uma sessão fantasma anterior que o nativo nunca zerou
          const elapsedMinR = sessionAgeMs / 60000;
          const maxPlausibleR = (elapsedMinR * 3) + 0.5;
          if (typeof nativeKm === 'number' && nativeKm > maxPlausibleR) {
            console.warn('[restoreGpsSession] km nativo implausível ignorado:', nativeKm, 'em', elapsedMinR.toFixed(1), 'min');
          } else
          // So adota o km nativo se ele for MAIOR que o que a sessao web ja tem.
          // Nunca deixa essa reconciliacao DIMINUIR o km exibido — isso e o que
          // causava o "pulo pra baixo" quando havia uma correcao manual (kmAdjust)
          // aplicada: o servico nativo nao sabe nada sobre essa correcao, entao
          // confiar nele sem essa guarda jogava o numero de volta pro valor cru,
          // descartando a edicao do usuario.
          if (typeof nativeKm === 'number' && nativeKm > g.km) {
            g.km = nativeKm;
            // Limpa referencia JS para evitar dupla contagem no primeiro fix
            g.pts = [];
            g.lastFixTime = null;
          }
          // GUARD: só adota o start nativo se for ANTERIOR ao que já temos.
          // Se o Android matou e reiniciou o GpsService (START_STICKY), o
          // start nativo vira a hora do RESTART — adotar ele fazia a jornada
          // "recomeçar" e o upsert (mesmo sessionId) sobrescrevia o start_ms
          // da sessão no Supabase, perdendo todo o histórico do dia.
          if (nativeStart && nativeStart < g.start) g.start = nativeStart;
          else if (nativeStart && nativeStart > g.start + 60000) {
            // Serviço nativo reiniciou do zero — re-semeia com o estado correto
            console.warn('[restoreGpsSession] Serviço nativo reiniciado (start nativo mais novo) — re-semeando com start/km da sessão real');
            try { SmartMobiNative.startFloating?.(g.start, g.km || 0); } catch(e) {}
          }
          // Guard: não diminui pausedMs com valor menor do nativo
          if (typeof nativePaused === 'number' && nativePaused >= (g.pausedMs || 0)) {
            g.pausedMs = nativePaused;
          }
        }
      }
    } catch(e) {}
  }
  syncNavButton();
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  if (g.st === 'running') requestWakeLock();
  // Se há jornada em aberto mas o serviço nativo está MORTO (Android matou
  // o processo e o START
===== sessions @ 935171 =====
0) - kmAtMidnight);
  g.sessionId = newId;
  g.start = midnightMs;
  g.km = remainingKm;
  g.pausedMs = 0; g.pa = null;
  g.pts = []; g.lastFixTime = null;
  g.forceDate = null; // sessão nova já nasce em 'hoje' — sem override
  const dayT = DB.day(todayStr);
  g.baseKm = (dayT.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (dayT.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);

  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: newId, user_id: _supaUser.id, date: todayStr,
      start_ms: midnightMs, end_ms: null, km: parseFloat(remainingKm.toFixed(3)),
      elapsed_secs: 0, paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  // Religa o widget/serviço nativo com o novo sessionId/km — sem isso ele
  // continuaria reportando pro id antigo (que já fechamos acima).
  if (window.SmartMobiNative) {
    try { SmartMobiNative.startFloating?.(g.start, g.km || 0); } catch(e) {}
    try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} // consumido — não reaplicar
  }

  showToast('Jornada dividida na meia-noite' + (estimated ? ' (km estimado por tempo — sem GPS exato disponível)' : ' (km exato do GPS)') + '.', 'info', 4500);
  try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
}

// ── Midnight pós-encerramento (acionado pelo showJourneySummary) ──────────
// Reutiliza o mesmo modal mo-midnight, mas com flag _midnightIsPostStop para
// resolveMidnightSplit() delegar ao resolvePostStopMidnight().
function openPostStopMidnightModal(sess, onDone) {
  if (!sess || !sess.start || !sess.end) { if (onDone) onDone(); return; }
  S._midnightIsPostStop = true;
  S._postStopMidnightSess = sess;
  S._postMidnightCallback = onDone || null;
  openMidnightModal(_ymd(sess.start), _ymd(sess.end));
}

async function resolvePostStopMidnight(mode) {
  const sess = S._postStopMidnightSess;
  const onDone = S._postMidnightCallback;
  S._midnightIsPostStop = false;
  S._postStopMidnightSess = null;
  S._postMidnightCallback = null;
  S._midnightModalOpen = false;
  closeMo('mo-midnight');
  if (!sess) { if (onDone) onDone(); return; }

  const startDate = _ymd(sess.start);
  const todayStr = _ymd(sess.end);
  const resolvedKey = 'sm_midnight_resolved_' + (sess.id||'x') + '_' + todayStr;
  localStorage.setItem(resolvedKey, '1');

  if (mode === 'today') {
    // Sessão já foi salva em hoje por gpsStop — nada a mover
    showToast('Jornada mantida em hoje.', 'info');
  } else if (mode === 'yesterday') {
    // Move sessão de hoje para ontem
    const dayToday = DB.day(todayStr);
    dayToday.sessions = (dayToday.sessions||[]).filter(s => s.id !== sess.id);
    const tk = dayToday.sessions.reduce((s,x)=>s+(x.km||0),0);
    dayToday.session = { km:tk, elapsed: dayToday.sessions.reduce((s,x)=>s+(x.elapsed||0),0) };
    DB.saveDay(dayToday);
    const dayYest = DB.day(startDate);
    if (!dayYest.sessions) dayYest.sessions = [];
    if (!dayYest.sessions.find(s => s.id === sess.id)) dayYest.sessions.push(sess);
    DB.saveDay(dayYest);
    SYNC.push('sessions', { id: sess.id, date: startDate, start_ms: sess.start,
      end_ms: sess.end, km: sess.km, elapsed_secs: sess.elapsed, paused_ms:0, is_paused:false });
    showToast('Jornada mantida em ontem.', 'info');
  } else if (mode === 'split') {
    try { await splitPostStopMidnight(sess, startDate, todayStr); }
    catch(e) { console.error('[resolvePostStopMidnight] split falhou:', e); showToast('Erro ao dividir. Tente novamente.','error'); }
  }
  try { renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
  if (onDone) setTimeout(onDone, 300);
}

async function splitPostStopMidnight(sess, startDate, todayStr) {
  const midnightMs = new Date(todayStr + 'T00:00:00').getTime();
  // km na meia-noite: tenta snapshot nativo, senão estima por proporção de tempo
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      if (SmartMobiNative.getMidnightSnapshotDate() === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, sess.end - sess.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - sess.start));
    kmAtMidnight = (sess.km||0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, sess.km||0);
  const elapsedYest = Math.max(0, Math.round((midnightMs - sess.start) / 1000));
  const elapsedToday = Math.max(0, (sess.elapsed||0) - elapsedYest);
  const kmToday = parseFloat(Math.max(0, (sess.km||0) - kmAtMidnight).toFixed(3));
  // Sessão de ontem (mantém o ID original)
  const sessYest = { id: sess.id, start: sess.start, end: midnightMs,
    km: parseFloat(kmAtMidnight.toFixed(3)), elapsed: elapsedYest };
  const dayYest = DB.day(startDate);
  if (!dayYest.sessions) dayYest.sessions = [];
  const iy = dayYest.sessions.findIndex(s => s.id === sess.id);
  if (iy >= 0) dayYest.sessions[iy] = sessYest; else dayYest.sessions.push(sessYest);
  DB.saveDay(dayYest);
  SYNC.push('sessions', { id: sessYest.id, date: startDate, start_ms: sessYest.start,
    end_ms: sessYest.end, km: sessYest.km, elapsed_secs: sessYest.elapsed, paused_ms:0, is_paused:false });
  // Sessão de hoje (novo ID, parte após meia-noite)
  const newId = DB.uid();
  const sessToday = { id: newId, start: midnightMs, end: sess.end, km: kmToday, elapsed: elapsedToday };
  const dayToday = DB.day(todayStr);
  if (!dayToday.sessions) dayToday.sessions = [];
  dayToday.sessions = dayToday.sessions.filter(s => s.id !== sess.id); // remove original
  dayToday.sessions.push(sessToday);
  DB.saveDay(dayToday);
  SYNC.push('sessions', { id: sessToday.id, date: todayStr, start_ms: sessToday.start,
    end_ms: sessToday.end, km: sessToday.km, elapsed_secs: sessToday.elapsed, paused_ms:0, is_paused:false });
  try { reconcileSessionsForDay(startDate); } catch(e) {}
  if (window.SmartMobiNative) { try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} }
  showToast('Jornada dividida na meia-noite' + (estimated?' (km estimado)':' (km do GPS)') + '.', 'info', 4500);
}

function restoreGpsSession() {
  const g = S.gps;
  // Já está rodando nesta instância do app (ex: showApp() foi chamado de novo
  // pelo Supabase reemitindo SIGNED_IN ao voltar do background) — não faz nada,
  // senão sobrescreve o km/timer atuais com o último snapshot salvo e duplica
  // o watchPosition/setInterval, fazendo o km "resetar"/contar dobrado.
  if (g.ti) return;
  const saved = DB.get('sm_gps_session');
  if (!saved || (saved.st !== 'running' && saved.st !== 'paused')) return;
  // Sessão muito antiga (app ficou fechado por mais de 16h) — não tenta retomar
  const ageMs = Date.now() - (saved.start || 0);
  if (!saved.start || ageMs > 16*3600*1000) { DB.del('sm_gps_session'); return; }
  // Defesa extra: se o Supabase já sabe que esta sessão foi encerrada
  // (ex: gpsStop rodou parcialmente numa tentativa anterior — fechou lá
  // mas não apagou o marcador local), NÃO resume. Confia no servidor.
  if (_supaUser && saved.sessionId && navigator.onLine) {
    _SUPA.from('sessions').select('end_ms,km,start_ms').eq('id', saved.sessionId).maybeSingle()
      .then(({data}) => {
        // Recupera km/start do checkpoint na nuvem se forem melhores que o
        // snapshot local (marcador local pode estar defasado se o app ficou
        // muito tempo em background sem JS rodando).
        if (data && !data.end_ms && S.gps.st !== 'idle' && S.gps.sessionId === saved.sessionId) {
          const cloudKm = parseFloat(data.km) || 0;
          if (cloudKm > (S.gps.km || 0)) { S.gps.km = cloudKm; S.gps.pts = []; S.gps.lastFixTime = null; }
          if (data.start_ms && Number(data.start_ms) < (S.gps.start || Infinity)) S.gps.start = Number(data.start_ms);
          try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
        }
        if (data && data.end_ms) {
          console.warn('[restoreGpsSession] Sessão já encerrada no Supabase — descartando marcador local stale');
          DB.del('sm_gps_session');
          if (S.gps.st !== 'idle') {
            S.gps.st = 'idle'; S.gps.start = null;
            if (S.gps.wid) { try{navigator.geolocation.clearWatch(S.gps.wid);}catch(e){} }
            if (S.gps.ti) clearInterval(S.gps.ti);
            try { renderJornada(); renderHome(); } catch(e) {}
          }
        }
      }).catch(()=>{});
  }
  // Limpeza defensiva — garante que não sobra nenhum watcher/timer órfão
  if (g.wid) { try{ navigator.geolocation.clearWatch(g.wid); }catch(e){} }
  if (g.ti) clearInterval(g.ti);
  g.st = saved.st; g.start = saved.start; g.pausedMs = saved.pausedMs || 0; g.pa = saved.pa || null;
  g.km = saved.km || 0; g.pts = saved.lastPt ? [saved.lastPt] : [];
  g.baseKm = saved.baseKm || 0; g.baseElapsed = saved.baseElapsed || 0;
  g.sessionId = saved.sessionId || null; // restaura ID para continuar o upsert correto
  g.forceDate = saved.forceDate || null;
  // O snapshot acima é só o último valor salvo antes do app ir pra segundo plano (o
  // watchPosition do JS para quando o app não está em foreground). Se o app nativo
  // tiver um GpsService rodando, ele é a fonte mais atual — corrige aqui, antes do
  // primeiro render, pra não mostrar um km/tempo defasado por um instante na tela.
  if (window.SmartMobiNative && typeof SmartMobiNative.isGpsRunning === 'function') {
    try {
      if (SmartMobiNative.isGpsRunning()) {
        // So confia no km nativo se a sessao ja estiver rodando ha mais de alguns
        // segundos. Logo apos um gpsStart(), o sinal de reset enviado pro servico
        // nativo pode ainda nao ter sido processado — ler o km nesse instante corre
        // o risco de pegar um valor antigo (de antes do reset), fazendo o numero
        // "pular" pra um valor maior por um instante ate se corrigir sozinho.
        const sessionAgeMs = Date.now() - (g.start || 0);
        if (sessionAgeMs > 8000) {
          const nativeKm = SmartMobiNative.getGpsKm();
          const nativeStart = SmartMobiNative.getGpsStartTime();
          const nativePaused = SmartMobiNative.getGpsPausedMs();
          // Mesmo guard de plausibilidade do nativeSyncGps — protege contra
          // herdar km de uma sessão fantasma anterior que o nativo nunca zerou
          const elapsedMinR = sessionAgeMs / 60000;
          const maxPlausibleR = (elapsedMinR * 3) + 0.5;
          if (typeof nativeKm === 'number' && nativeKm > maxPlausibleR) {
            console.warn('[restoreGpsSession] km nativo implausível ignorado:', nativeKm, 'em', elapsedMinR.toFixed(1), 'min');
          } else
          // So adota o km nativo se ele for MAIOR que o que a sessao web ja tem.
          // Nunca deixa essa reconciliacao DIMINUIR o km exibido — isso e o que
          // causava o "pulo pra baixo" quando havia uma correcao manual (kmAdjust)
          // aplicada: o servico nativo nao sabe nada sobre essa correcao, entao
          // confiar nele sem essa guarda jogava o numero de volta pro valor cru,
          // descartando a edicao do usuario.
          if (typeof nativeKm === 'number' && nativeKm > g.km) {
            g.km = nativeKm;
            // Limpa referencia JS para evitar dupla contagem no primeiro fix
            g.pts = [];
            g.lastFixTime = null;
          }
          // GUARD: só adota o start nativo se for ANTERIOR ao que já temos.
          // Se o Android matou e reiniciou o GpsService (START_STICKY), o
          // start nativo vira a hora do RESTART — adotar ele fazia a jornada
          // "recomeçar" e o upsert (mesmo sessionId) sobrescrevia o start_ms
          // da sessão no Supabase, perdendo todo o histórico do dia.
          if (nativeStart && nativeStart < g.start) g.start = nativeStart;
          else if (nativeStart && nativeStart > g.start + 60000) {
            // Serviço nativo reiniciou do zero — re-semeia com o estado correto
            console.warn('[restoreGpsSession] Serviço nativo reiniciado (start nativo mais novo) — re-semeando com start/km da sessão real');
            try { SmartMobiNative.startFloating?.(g.start, g.km || 0); } catch(e) {}
          }
          // Guard: não diminui pausedMs com valor menor do nativo
          if (typeof nativePaused === 'number' && nativePaused >= (g.pausedMs || 0)) {
            g.pausedMs = nativePaused;
          }
        }
      }
    } catch(e) {}
  }
  syncNavButton();
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  if (g.st === 'running') requestWakeLock();
  // Se há jornada em aberto mas o serviço nativo está MORTO (Android matou
  // o processo e o START_STICKY ainda não religou, ou foi removido de vez),
  // religa ele semeado com o start/km corretos — a jornada continua de onde
  // parou em vez de ficar sem rastreamento em background.
  if (g.st === 'running' && window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      const nativeUp = typeof SmartMobiNative.isGpsRunning === 'function' && SmartMobiNative.isGpsRunning();
      if (!nativeUp && typeof SmartMobiNative.startFloating === 'function') {
        console.warn('[restoreGpsSession] Serviço nativo morto com jornada em aberto — religando com start/km da sessão');
        SmartMobiNative.startFloating(g.start, g.km || 0);
      }
    } catch(e) {}
  }
  if (window.SmartMobiNative && typeof SmartMobiNative.updateFloatingStatus === 'function') {
    try { SmartMobiNative.updateFloatingStatus(g.st); } catch(e) {}
  }
  // Se o app ficou fechado/em segundo plano e a jornada cruzou a meia-noite
  // nesse meio tempo, avisa agora que reabriu (dá um tempo pro render inicial
  // da tela terminar antes de empilhar o modal por cima).
  setTimeout(checkMidnightCrossing, 600);
}
const _CAR_IMGS = {
  idle:    "data:image/png;base64,",
  running: "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAABMAA
===== sessions @ 935225 =====
 midnightMs;
  g.km = remainingKm;
  g.pausedMs = 0; g.pa = null;
  g.pts = []; g.lastFixTime = null;
  g.forceDate = null; // sessão nova já nasce em 'hoje' — sem override
  const dayT = DB.day(todayStr);
  g.baseKm = (dayT.sessions||[]).reduce((s,x)=>s+(x.km||0),0);
  g.baseElapsed = (dayT.sessions||[]).reduce((s,x)=>s+(x.elapsed||0),0);

  if (_supaUser) {
    _SUPA.from('sessions').upsert({
      id: newId, user_id: _supaUser.id, date: todayStr,
      start_ms: midnightMs, end_ms: null, km: parseFloat(remainingKm.toFixed(3)),
      elapsed_secs: 0, paused_ms: 0, is_paused: false
    }, { onConflict: 'id' }).catch(()=>{});
  }
  // Religa o widget/serviço nativo com o novo sessionId/km — sem isso ele
  // continuaria reportando pro id antigo (que já fechamos acima).
  if (window.SmartMobiNative) {
    try { SmartMobiNative.startFloating?.(g.start, g.km || 0); } catch(e) {}
    try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} // consumido — não reaplicar
  }

  showToast('Jornada dividida na meia-noite' + (estimated ? ' (km estimado por tempo — sem GPS exato disponível)' : ' (km exato do GPS)') + '.', 'info', 4500);
  try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
}

// ── Midnight pós-encerramento (acionado pelo showJourneySummary) ──────────
// Reutiliza o mesmo modal mo-midnight, mas com flag _midnightIsPostStop para
// resolveMidnightSplit() delegar ao resolvePostStopMidnight().
function openPostStopMidnightModal(sess, onDone) {
  if (!sess || !sess.start || !sess.end) { if (onDone) onDone(); return; }
  S._midnightIsPostStop = true;
  S._postStopMidnightSess = sess;
  S._postMidnightCallback = onDone || null;
  openMidnightModal(_ymd(sess.start), _ymd(sess.end));
}

async function resolvePostStopMidnight(mode) {
  const sess = S._postStopMidnightSess;
  const onDone = S._postMidnightCallback;
  S._midnightIsPostStop = false;
  S._postStopMidnightSess = null;
  S._postMidnightCallback = null;
  S._midnightModalOpen = false;
  closeMo('mo-midnight');
  if (!sess) { if (onDone) onDone(); return; }

  const startDate = _ymd(sess.start);
  const todayStr = _ymd(sess.end);
  const resolvedKey = 'sm_midnight_resolved_' + (sess.id||'x') + '_' + todayStr;
  localStorage.setItem(resolvedKey, '1');

  if (mode === 'today') {
    // Sessão já foi salva em hoje por gpsStop — nada a mover
    showToast('Jornada mantida em hoje.', 'info');
  } else if (mode === 'yesterday') {
    // Move sessão de hoje para ontem
    const dayToday = DB.day(todayStr);
    dayToday.sessions = (dayToday.sessions||[]).filter(s => s.id !== sess.id);
    const tk = dayToday.sessions.reduce((s,x)=>s+(x.km||0),0);
    dayToday.session = { km:tk, elapsed: dayToday.sessions.reduce((s,x)=>s+(x.elapsed||0),0) };
    DB.saveDay(dayToday);
    const dayYest = DB.day(startDate);
    if (!dayYest.sessions) dayYest.sessions = [];
    if (!dayYest.sessions.find(s => s.id === sess.id)) dayYest.sessions.push(sess);
    DB.saveDay(dayYest);
    SYNC.push('sessions', { id: sess.id, date: startDate, start_ms: sess.start,
      end_ms: sess.end, km: sess.km, elapsed_secs: sess.elapsed, paused_ms:0, is_paused:false });
    showToast('Jornada mantida em ontem.', 'info');
  } else if (mode === 'split') {
    try { await splitPostStopMidnight(sess, startDate, todayStr); }
    catch(e) { console.error('[resolvePostStopMidnight] split falhou:', e); showToast('Erro ao dividir. Tente novamente.','error'); }
  }
  try { renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
  if (onDone) setTimeout(onDone, 300);
}

async function splitPostStopMidnight(sess, startDate, todayStr) {
  const midnightMs = new Date(todayStr + 'T00:00:00').getTime();
  // km na meia-noite: tenta snapshot nativo, senão estima por proporção de tempo
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      if (SmartMobiNative.getMidnightSnapshotDate() === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, sess.end - sess.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - sess.start));
    kmAtMidnight = (sess.km||0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, sess.km||0);
  const elapsedYest = Math.max(0, Math.round((midnightMs - sess.start) / 1000));
  const elapsedToday = Math.max(0, (sess.elapsed||0) - elapsedYest);
  const kmToday = parseFloat(Math.max(0, (sess.km||0) - kmAtMidnight).toFixed(3));
  // Sessão de ontem (mantém o ID original)
  const sessYest = { id: sess.id, start: sess.start, end: midnightMs,
    km: parseFloat(kmAtMidnight.toFixed(3)), elapsed: elapsedYest };
  const dayYest = DB.day(startDate);
  if (!dayYest.sessions) dayYest.sessions = [];
  const iy = dayYest.sessions.findIndex(s => s.id === sess.id);
  if (iy >= 0) dayYest.sessions[iy] = sessYest; else dayYest.sessions.push(sessYest);
  DB.saveDay(dayYest);
  SYNC.push('sessions', { id: sessYest.id, date: startDate, start_ms: sessYest.start,
    end_ms: sessYest.end, km: sessYest.km, elapsed_secs: sessYest.elapsed, paused_ms:0, is_paused:false });
  // Sessão de hoje (novo ID, parte após meia-noite)
  const newId = DB.uid();
  const sessToday = { id: newId, start: midnightMs, end: sess.end, km: kmToday, elapsed: elapsedToday };
  const dayToday = DB.day(todayStr);
  if (!dayToday.sessions) dayToday.sessions = [];
  dayToday.sessions = dayToday.sessions.filter(s => s.id !== sess.id); // remove original
  dayToday.sessions.push(sessToday);
  DB.saveDay(dayToday);
  SYNC.push('sessions', { id: sessToday.id, date: todayStr, start_ms: sessToday.start,
    end_ms: sessToday.end, km: sessToday.km, elapsed_secs: sessToday.elapsed, paused_ms:0, is_paused:false });
  try { reconcileSessionsForDay(startDate); } catch(e) {}
  if (window.SmartMobiNative) { try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} }
  showToast('Jornada dividida na meia-noite' + (estimated?' (km estimado)':' (km do GPS)') + '.', 'info', 4500);
}

function restoreGpsSession() {
  const g = S.gps;
  // Já está rodando nesta instância do app (ex: showApp() foi chamado de novo
  // pelo Supabase reemitindo SIGNED_IN ao voltar do background) — não faz nada,
  // senão sobrescreve o km/timer atuais com o último snapshot salvo e duplica
  // o watchPosition/setInterval, fazendo o km "resetar"/contar dobrado.
  if (g.ti) return;
  const saved = DB.get('sm_gps_session');
  if (!saved || (saved.st !== 'running' && saved.st !== 'paused')) return;
  // Sessão muito antiga (app ficou fechado por mais de 16h) — não tenta retomar
  const ageMs = Date.now() - (saved.start || 0);
  if (!saved.start || ageMs > 16*3600*1000) { DB.del('sm_gps_session'); return; }
  // Defesa extra: se o Supabase já sabe que esta sessão foi encerrada
  // (ex: gpsStop rodou parcialmente numa tentativa anterior — fechou lá
  // mas não apagou o marcador local), NÃO resume. Confia no servidor.
  if (_supaUser && saved.sessionId && navigator.onLine) {
    _SUPA.from('sessions').select('end_ms,km,start_ms').eq('id', saved.sessionId).maybeSingle()
      .then(({data}) => {
        // Recupera km/start do checkpoint na nuvem se forem melhores que o
        // snapshot local (marcador local pode estar defasado se o app ficou
        // muito tempo em background sem JS rodando).
        if (data && !data.end_ms && S.gps.st !== 'idle' && S.gps.sessionId === saved.sessionId) {
          const cloudKm = parseFloat(data.km) || 0;
          if (cloudKm > (S.gps.km || 0)) { S.gps.km = cloudKm; S.gps.pts = []; S.gps.lastFixTime = null; }
          if (data.start_ms && Number(data.start_ms) < (S.gps.start || Infinity)) S.gps.start = Number(data.start_ms);
          try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
        }
        if (data && data.end_ms) {
          console.warn('[restoreGpsSession] Sessão já encerrada no Supabase — descartando marcador local stale');
          DB.del('sm_gps_session');
          if (S.gps.st !== 'idle') {
            S.gps.st = 'idle'; S.gps.start = null;
            if (S.gps.wid) { try{navigator.geolocation.clearWatch(S.gps.wid);}catch(e){} }
            if (S.gps.ti) clearInterval(S.gps.ti);
            try { renderJornada(); renderHome(); } catch(e) {}
          }
        }
      }).catch(()=>{});
  }
  // Limpeza defensiva — garante que não sobra nenhum watcher/timer órfão
  if (g.wid) { try{ navigator.geolocation.clearWatch(g.wid); }catch(e){} }
  if (g.ti) clearInterval(g.ti);
  g.st = saved.st; g.start = saved.start; g.pausedMs = saved.pausedMs || 0; g.pa = saved.pa || null;
  g.km = saved.km || 0; g.pts = saved.lastPt ? [saved.lastPt] : [];
  g.baseKm = saved.baseKm || 0; g.baseElapsed = saved.baseElapsed || 0;
  g.sessionId = saved.sessionId || null; // restaura ID para continuar o upsert correto
  g.forceDate = saved.forceDate || null;
  // O snapshot acima é só o último valor salvo antes do app ir pra segundo plano (o
  // watchPosition do JS para quando o app não está em foreground). Se o app nativo
  // tiver um GpsService rodando, ele é a fonte mais atual — corrige aqui, antes do
  // primeiro render, pra não mostrar um km/tempo defasado por um instante na tela.
  if (window.SmartMobiNative && typeof SmartMobiNative.isGpsRunning === 'function') {
    try {
      if (SmartMobiNative.isGpsRunning()) {
        // So confia no km nativo se a sessao ja estiver rodando ha mais de alguns
        // segundos. Logo apos um gpsStart(), o sinal de reset enviado pro servico
        // nativo pode ainda nao ter sido processado — ler o km nesse instante corre
        // o risco de pegar um valor antigo (de antes do reset), fazendo o numero
        // "pular" pra um valor maior por um instante ate se corrigir sozinho.
        const sessionAgeMs = Date.now() - (g.start || 0);
        if (sessionAgeMs > 8000) {
          const nativeKm = SmartMobiNative.getGpsKm();
          const nativeStart = SmartMobiNative.getGpsStartTime();
          const nativePaused = SmartMobiNative.getGpsPausedMs();
          // Mesmo guard de plausibilidade do nativeSyncGps — protege contra
          // herdar km de uma sessão fantasma anterior que o nativo nunca zerou
          const elapsedMinR = sessionAgeMs / 60000;
          const maxPlausibleR = (elapsedMinR * 3) + 0.5;
          if (typeof nativeKm === 'number' && nativeKm > maxPlausibleR) {
            console.warn('[restoreGpsSession] km nativo implausível ignorado:', nativeKm, 'em', elapsedMinR.toFixed(1), 'min');
          } else
          // So adota o km nativo se ele for MAIOR que o que a sessao web ja tem.
          // Nunca deixa essa reconciliacao DIMINUIR o km exibido — isso e o que
          // causava o "pulo pra baixo" quando havia uma correcao manual (kmAdjust)
          // aplicada: o servico nativo nao sabe nada sobre essa correcao, entao
          // confiar nele sem essa guarda jogava o numero de volta pro valor cru,
          // descartando a edicao do usuario.
          if (typeof nativeKm === 'number' && nativeKm > g.km) {
            g.km = nativeKm;
            // Limpa referencia JS para evitar dupla contagem no primeiro fix
            g.pts = [];
            g.lastFixTime = null;
          }
          // GUARD: só adota o start nativo se for ANTERIOR ao que já temos.
          // Se o Android matou e reiniciou o GpsService (START_STICKY), o
          // start nativo vira a hora do RESTART — adotar ele fazia a jornada
          // "recomeçar" e o upsert (mesmo sessionId) sobrescrevia o start_ms
          // da sessão no Supabase, perdendo todo o histórico do dia.
          if (nativeStart && nativeStart < g.start) g.start = nativeStart;
          else if (nativeStart && nativeStart > g.start + 60000) {
            // Serviço nativo reiniciou do zero — re-semeia com o estado correto
            console.warn('[restoreGpsSession] Serviço nativo reiniciado (start nativo mais novo) — re-semeando com start/km da sessão real');
            try { SmartMobiNative.startFloating?.(g.start, g.km || 0); } catch(e) {}
          }
          // Guard: não diminui pausedMs com valor menor do nativo
          if (typeof nativePaused === 'number' && nativePaused >= (g.pausedMs || 0)) {
            g.pausedMs = nativePaused;
          }
        }
      }
    } catch(e) {}
  }
  syncNavButton();
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  if (g.st === 'running') requestWakeLock();
  // Se há jornada em aberto mas o serviço nativo está MORTO (Android matou
  // o processo e o START_STICKY ainda não religou, ou foi removido de vez),
  // religa ele semeado com o start/km corretos — a jornada continua de onde
  // parou em vez de ficar sem rastreamento em background.
  if (g.st === 'running' && window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      const nativeUp = typeof SmartMobiNative.isGpsRunning === 'function' && SmartMobiNative.isGpsRunning();
      if (!nativeUp && typeof SmartMobiNative.startFloating === 'function') {
        console.warn('[restoreGpsSession] Serviço nativo morto com jornada em aberto — religando com start/km da sessão');
        SmartMobiNative.startFloating(g.start, g.km || 0);
      }
    } catch(e) {}
  }
  if (window.SmartMobiNative && typeof SmartMobiNative.updateFloatingStatus === 'function') {
    try { SmartMobiNative.updateFloatingStatus(g.st); } catch(e) {}
  }
  // Se o app ficou fechado/em segundo plano e a jornada cruzou a meia-noite
  // nesse meio tempo, avisa agora que reabriu (dá um tempo pro render inicial
  // da tela terminar antes de empilhar o modal por cima).
  setTimeout(checkMidnightCrossing, 600);
}
const _CAR_IMGS = {
  idle:    "data:image/png;base64,",
  running: "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAABMAAAAoCAYAAAAc7cGiAAACRklEQVR4nO2UPWhTURTH/+fe95GksaA1tZg
===== sessions @ 935943 =====
continuaria reportando pro id antigo (que já fechamos acima).
  if (window.SmartMobiNative) {
    try { SmartMobiNative.startFloating?.(g.start, g.km || 0); } catch(e) {}
    try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} // consumido — não reaplicar
  }

  showToast('Jornada dividida na meia-noite' + (estimated ? ' (km estimado por tempo — sem GPS exato disponível)' : ' (km exato do GPS)') + '.', 'info', 4500);
  try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
}

// ── Midnight pós-encerramento (acionado pelo showJourneySummary) ──────────
// Reutiliza o mesmo modal mo-midnight, mas com flag _midnightIsPostStop para
// resolveMidnightSplit() delegar ao resolvePostStopMidnight().
function openPostStopMidnightModal(sess, onDone) {
  if (!sess || !sess.start || !sess.end) { if (onDone) onDone(); return; }
  S._midnightIsPostStop = true;
  S._postStopMidnightSess = sess;
  S._postMidnightCallback = onDone || null;
  openMidnightModal(_ymd(sess.start), _ymd(sess.end));
}

async function resolvePostStopMidnight(mode) {
  const sess = S._postStopMidnightSess;
  const onDone = S._postMidnightCallback;
  S._midnightIsPostStop = false;
  S._postStopMidnightSess = null;
  S._postMidnightCallback = null;
  S._midnightModalOpen = false;
  closeMo('mo-midnight');
  if (!sess) { if (onDone) onDone(); return; }

  const startDate = _ymd(sess.start);
  const todayStr = _ymd(sess.end);
  const resolvedKey = 'sm_midnight_resolved_' + (sess.id||'x') + '_' + todayStr;
  localStorage.setItem(resolvedKey, '1');

  if (mode === 'today') {
    // Sessão já foi salva em hoje por gpsStop — nada a mover
    showToast('Jornada mantida em hoje.', 'info');
  } else if (mode === 'yesterday') {
    // Move sessão de hoje para ontem
    const dayToday = DB.day(todayStr);
    dayToday.sessions = (dayToday.sessions||[]).filter(s => s.id !== sess.id);
    const tk = dayToday.sessions.reduce((s,x)=>s+(x.km||0),0);
    dayToday.session = { km:tk, elapsed: dayToday.sessions.reduce((s,x)=>s+(x.elapsed||0),0) };
    DB.saveDay(dayToday);
    const dayYest = DB.day(startDate);
    if (!dayYest.sessions) dayYest.sessions = [];
    if (!dayYest.sessions.find(s => s.id === sess.id)) dayYest.sessions.push(sess);
    DB.saveDay(dayYest);
    SYNC.push('sessions', { id: sess.id, date: startDate, start_ms: sess.start,
      end_ms: sess.end, km: sess.km, elapsed_secs: sess.elapsed, paused_ms:0, is_paused:false });
    showToast('Jornada mantida em ontem.', 'info');
  } else if (mode === 'split') {
    try { await splitPostStopMidnight(sess, startDate, todayStr); }
    catch(e) { console.error('[resolvePostStopMidnight] split falhou:', e); showToast('Erro ao dividir. Tente novamente.','error'); }
  }
  try { renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
  if (onDone) setTimeout(onDone, 300);
}

async function splitPostStopMidnight(sess, startDate, todayStr) {
  const midnightMs = new Date(todayStr + 'T00:00:00').getTime();
  // km na meia-noite: tenta snapshot nativo, senão estima por proporção de tempo
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      if (SmartMobiNative.getMidnightSnapshotDate() === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, sess.end - sess.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - sess.start));
    kmAtMidnight = (sess.km||0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, sess.km||0);
  const elapsedYest = Math.max(0, Math.round((midnightMs - sess.start) / 1000));
  const elapsedToday = Math.max(0, (sess.elapsed||0) - elapsedYest);
  const kmToday = parseFloat(Math.max(0, (sess.km||0) - kmAtMidnight).toFixed(3));
  // Sessão de ontem (mantém o ID original)
  const sessYest = { id: sess.id, start: sess.start, end: midnightMs,
    km: parseFloat(kmAtMidnight.toFixed(3)), elapsed: elapsedYest };
  const dayYest = DB.day(startDate);
  if (!dayYest.sessions) dayYest.sessions = [];
  const iy = dayYest.sessions.findIndex(s => s.id === sess.id);
  if (iy >= 0) dayYest.sessions[iy] = sessYest; else dayYest.sessions.push(sessYest);
  DB.saveDay(dayYest);
  SYNC.push('sessions', { id: sessYest.id, date: startDate, start_ms: sessYest.start,
    end_ms: sessYest.end, km: sessYest.km, elapsed_secs: sessYest.elapsed, paused_ms:0, is_paused:false });
  // Sessão de hoje (novo ID, parte após meia-noite)
  const newId = DB.uid();
  const sessToday = { id: newId, start: midnightMs, end: sess.end, km: kmToday, elapsed: elapsedToday };
  const dayToday = DB.day(todayStr);
  if (!dayToday.sessions) dayToday.sessions = [];
  dayToday.sessions = dayToday.sessions.filter(s => s.id !== sess.id); // remove original
  dayToday.sessions.push(sessToday);
  DB.saveDay(dayToday);
  SYNC.push('sessions', { id: sessToday.id, date: todayStr, start_ms: sessToday.start,
    end_ms: sessToday.end, km: sessToday.km, elapsed_secs: sessToday.elapsed, paused_ms:0, is_paused:false });
  try { reconcileSessionsForDay(startDate); } catch(e) {}
  if (window.SmartMobiNative) { try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} }
  showToast('Jornada dividida na meia-noite' + (estimated?' (km estimado)':' (km do GPS)') + '.', 'info', 4500);
}

function restoreGpsSession() {
  const g = S.gps;
  // Já está rodando nesta instância do app (ex: showApp() foi chamado de novo
  // pelo Supabase reemitindo SIGNED_IN ao voltar do background) — não faz nada,
  // senão sobrescreve o km/timer atuais com o último snapshot salvo e duplica
  // o watchPosition/setInterval, fazendo o km "resetar"/contar dobrado.
  if (g.ti) return;
  const saved = DB.get('sm_gps_session');
  if (!saved || (saved.st !== 'running' && saved.st !== 'paused')) return;
  // Sessão muito antiga (app ficou fechado por mais de 16h) — não tenta retomar
  const ageMs = Date.now() - (saved.start || 0);
  if (!saved.start || ageMs > 16*3600*1000) { DB.del('sm_gps_session'); return; }
  // Defesa extra: se o Supabase já sabe que esta sessão foi encerrada
  // (ex: gpsStop rodou parcialmente numa tentativa anterior — fechou lá
  // mas não apagou o marcador local), NÃO resume. Confia no servidor.
  if (_supaUser && saved.sessionId && navigator.onLine) {
    _SUPA.from('sessions').select('end_ms,km,start_ms').eq('id', saved.sessionId).maybeSingle()
      .then(({data}) => {
        // Recupera km/start do checkpoint na nuvem se forem melhores que o
        // snapshot local (marcador local pode estar defasado se o app ficou
        // muito tempo em background sem JS rodando).
        if (data && !data.end_ms && S.gps.st !== 'idle' && S.gps.sessionId === saved.sessionId) {
          const cloudKm = parseFloat(data.km) || 0;
          if (cloudKm > (S.gps.km || 0)) { S.gps.km = cloudKm; S.gps.pts = []; S.gps.lastFixTime = null; }
          if (data.start_ms && Number(data.start_ms) < (S.gps.start || Infinity)) S.gps.start = Number(data.start_ms);
          try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
        }
        if (data && data.end_ms) {
          console.warn('[restoreGpsSession] Sessão já encerrada no Supabase — descartando marcador local stale');
          DB.del('sm_gps_session');
          if (S.gps.st !== 'idle') {
            S.gps.st = 'idle'; S.gps.start = null;
            if (S.gps.wid) { try{navigator.geolocation.clearWatch(S.gps.wid);}catch(e){} }
            if (S.gps.ti) clearInterval(S.gps.ti);
            try { renderJornada(); renderHome(); } catch(e) {}
          }
        }
      }).catch(()=>{});
  }
  // Limpeza defensiva — garante que não sobra nenhum watcher/timer órfão
  if (g.wid) { try{ navigator.geolocation.clearWatch(g.wid); }catch(e){} }
  if (g.ti) clearInterval(g.ti);
  g.st = saved.st; g.start = saved.start; g.pausedMs = saved.pausedMs || 0; g.pa = saved.pa || null;
  g.km = saved.km || 0; g.pts = saved.lastPt ? [saved.lastPt] : [];
  g.baseKm = saved.baseKm || 0; g.baseElapsed = saved.baseElapsed || 0;
  g.sessionId = saved.sessionId || null; // restaura ID para continuar o upsert correto
  g.forceDate = saved.forceDate || null;
  // O snapshot acima é só o último valor salvo antes do app ir pra segundo plano (o
  // watchPosition do JS para quando o app não está em foreground). Se o app nativo
  // tiver um GpsService rodando, ele é a fonte mais atual — corrige aqui, antes do
  // primeiro render, pra não mostrar um km/tempo defasado por um instante na tela.
  if (window.SmartMobiNative && typeof SmartMobiNative.isGpsRunning === 'function') {
    try {
      if (SmartMobiNative.isGpsRunning()) {
        // So confia no km nativo se a sessao ja estiver rodando ha mais de alguns
        // segundos. Logo apos um gpsStart(), o sinal de reset enviado pro servico
        // nativo pode ainda nao ter sido processado — ler o km nesse instante corre
        // o risco de pegar um valor antigo (de antes do reset), fazendo o numero
        // "pular" pra um valor maior por um instante ate se corrigir sozinho.
        const sessionAgeMs = Date.now() - (g.start || 0);
        if (sessionAgeMs > 8000) {
          const nativeKm = SmartMobiNative.getGpsKm();
          const nativeStart = SmartMobiNative.getGpsStartTime();
          const nativePaused = SmartMobiNative.getGpsPausedMs();
          // Mesmo guard de plausibilidade do nativeSyncGps — protege contra
          // herdar km de uma sessão fantasma anterior que o nativo nunca zerou
          const elapsedMinR = sessionAgeMs / 60000;
          const maxPlausibleR = (elapsedMinR * 3) + 0.5;
          if (typeof nativeKm === 'number' && nativeKm > maxPlausibleR) {
            console.warn('[restoreGpsSession] km nativo implausível ignorado:', nativeKm, 'em', elapsedMinR.toFixed(1), 'min');
          } else
          // So adota o km nativo se ele for MAIOR que o que a sessao web ja tem.
          // Nunca deixa essa reconciliacao DIMINUIR o km exibido — isso e o que
          // causava o "pulo pra baixo" quando havia uma correcao manual (kmAdjust)
          // aplicada: o servico nativo nao sabe nada sobre essa correcao, entao
          // confiar nele sem essa guarda jogava o numero de volta pro valor cru,
          // descartando a edicao do usuario.
          if (typeof nativeKm === 'number' && nativeKm > g.km) {
            g.km = nativeKm;
            // Limpa referencia JS para evitar dupla contagem no primeiro fix
            g.pts = [];
            g.lastFixTime = null;
          }
          // GUARD: só adota o start nativo se for ANTERIOR ao que já temos.
          // Se o Android matou e reiniciou o GpsService (START_STICKY), o
          // start nativo vira a hora do RESTART — adotar ele fazia a jornada
          // "recomeçar" e o upsert (mesmo sessionId) sobrescrevia o start_ms
          // da sessão no Supabase, perdendo todo o histórico do dia.
          if (nativeStart && nativeStart < g.start) g.start = nativeStart;
          else if (nativeStart && nativeStart > g.start + 60000) {
            // Serviço nativo reiniciou do zero — re-semeia com o estado correto
            console.warn('[restoreGpsSession] Serviço nativo reiniciado (start nativo mais novo) — re-semeando com start/km da sessão real');
            try { SmartMobiNative.startFloating?.(g.start, g.km || 0); } catch(e) {}
          }
          // Guard: não diminui pausedMs com valor menor do nativo
          if (typeof nativePaused === 'number' && nativePaused >= (g.pausedMs || 0)) {
            g.pausedMs = nativePaused;
          }
        }
      }
    } catch(e) {}
  }
  syncNavButton();
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  if (g.st === 'running') requestWakeLock();
  // Se há jornada em aberto mas o serviço nativo está MORTO (Android matou
  // o processo e o START_STICKY ainda não religou, ou foi removido de vez),
  // religa ele semeado com o start/km corretos — a jornada continua de onde
  // parou em vez de ficar sem rastreamento em background.
  if (g.st === 'running' && window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      const nativeUp = typeof SmartMobiNative.isGpsRunning === 'function' && SmartMobiNative.isGpsRunning();
      if (!nativeUp && typeof SmartMobiNative.startFloating === 'function') {
        console.warn('[restoreGpsSession] Serviço nativo morto com jornada em aberto — religando com start/km da sessão');
        SmartMobiNative.startFloating(g.start, g.km || 0);
      }
    } catch(e) {}
  }
  if (window.SmartMobiNative && typeof SmartMobiNative.updateFloatingStatus === 'function') {
    try { SmartMobiNative.updateFloatingStatus(g.st); } catch(e) {}
  }
  // Se o app ficou fechado/em segundo plano e a jornada cruzou a meia-noite
  // nesse meio tempo, avisa agora que reabriu (dá um tempo pro render inicial
  // da tela terminar antes de empilhar o modal por cima).
  setTimeout(checkMidnightCrossing, 600);
}
const _CAR_IMGS = {
  idle:    "data:image/png;base64,",
  running: "data:image/png;base64,
===== sessions @ 937462 =====
+ '_' + todayStr;
  localStorage.setItem(resolvedKey, '1');

  if (mode === 'today') {
    // Sessão já foi salva em hoje por gpsStop — nada a mover
    showToast('Jornada mantida em hoje.', 'info');
  } else if (mode === 'yesterday') {
    // Move sessão de hoje para ontem
    const dayToday = DB.day(todayStr);
    dayToday.sessions = (dayToday.sessions||[]).filter(s => s.id !== sess.id);
    const tk = dayToday.sessions.reduce((s,x)=>s+(x.km||0),0);
    dayToday.session = { km:tk, elapsed: dayToday.sessions.reduce((s,x)=>s+(x.elapsed||0),0) };
    DB.saveDay(dayToday);
    const dayYest = DB.day(startDate);
    if (!dayYest.sessions) dayYest.sessions = [];
    if (!dayYest.sessions.find(s => s.id === sess.id)) dayYest.sessions.push(sess);
    DB.saveDay(dayYest);
    SYNC.push('sessions', { id: sess.id, date: startDate, start_ms: sess.start,
      end_ms: sess.end, km: sess.km, elapsed_secs: sess.elapsed, paused_ms:0, is_paused:false });
    showToast('Jornada mantida em ontem.', 'info');
  } else if (mode === 'split') {
    try { await splitPostStopMidnight(sess, startDate, todayStr); }
    catch(e) { console.error('[resolvePostStopMidnight] split falhou:', e); showToast('Erro ao dividir. Tente novamente.','error'); }
  }
  try { renderJornada(); } catch(e) {}
  try { renderHome(); } catch(e) {}
  if (onDone) setTimeout(onDone, 300);
}

async function splitPostStopMidnight(sess, startDate, todayStr) {
  const midnightMs = new Date(todayStr + 'T00:00:00').getTime();
  // km na meia-noite: tenta snapshot nativo, senão estima por proporção de tempo
  let kmAtMidnight = null, estimated = false;
  if (window.SmartMobiNative && typeof SmartMobiNative.getMidnightSnapshotDate === 'function') {
    try {
      if (SmartMobiNative.getMidnightSnapshotDate() === todayStr) {
        const v = SmartMobiNative.getKmAtMidnight();
        if (typeof v === 'number' && v >= 0) kmAtMidnight = v;
      }
    } catch(e) {}
  }
  if (kmAtMidnight == null) {
    const totalMs = Math.max(1, sess.end - sess.start);
    const beforeMs = Math.min(totalMs, Math.max(0, midnightMs - sess.start));
    kmAtMidnight = (sess.km||0) * (beforeMs / totalMs);
    estimated = true;
  }
  kmAtMidnight = Math.min(kmAtMidnight, sess.km||0);
  const elapsedYest = Math.max(0, Math.round((midnightMs - sess.start) / 1000));
  const elapsedToday = Math.max(0, (sess.elapsed||0) - elapsedYest);
  const kmToday = parseFloat(Math.max(0, (sess.km||0) - kmAtMidnight).toFixed(3));
  // Sessão de ontem (mantém o ID original)
  const sessYest = { id: sess.id, start: sess.start, end: midnightMs,
    km: parseFloat(kmAtMidnight.toFixed(3)), elapsed: elapsedYest };
  const dayYest = DB.day(startDate);
  if (!dayYest.sessions) dayYest.sessions = [];
  const iy = dayYest.sessions.findIndex(s => s.id === sess.id);
  if (iy >= 0) dayYest.sessions[iy] = sessYest; else dayYest.sessions.push(sessYest);
  DB.saveDay(dayYest);
  SYNC.push('sessions', { id: sessYest.id, date: startDate, start_ms: sessYest.start,
    end_ms: sessYest.end, km: sessYest.km, elapsed_secs: sessYest.elapsed, paused_ms:0, is_paused:false });
  // Sessão de hoje (novo ID, parte após meia-noite)
  const newId = DB.uid();
  const sessToday = { id: newId, start: midnightMs, end: sess.end, km: kmToday, elapsed: elapsedToday };
  const dayToday = DB.day(todayStr);
  if (!dayToday.sessions) dayToday.sessions = [];
  dayToday.sessions = dayToday.sessions.filter(s => s.id !== sess.id); // remove original
  dayToday.sessions.push(sessToday);
  DB.saveDay(dayToday);
  SYNC.push('sessions', { id: sessToday.id, date: todayStr, start_ms: sessToday.start,
    end_ms: sessToday.end, km: sessToday.km, elapsed_secs: sessToday.elapsed, paused_ms:0, is_paused:false });
  try { reconcileSessionsForDay(startDate); } catch(e) {}
  if (window.SmartMobiNative) { try { SmartMobiNative.clearMidnightSnapshot?.(); } catch(e) {} }
  showToast('Jornada dividida na meia-noite' + (estimated?' (km estimado)':' (km do GPS)') + '.', 'info', 4500);
}

function restoreGpsSession() {
  const g = S.gps;
  // Já está rodando nesta instância do app (ex: showApp() foi chamado de novo
  // pelo Supabase reemitindo SIGNED_IN ao voltar do background) — não faz nada,
  // senão sobrescreve o km/timer atuais com o último snapshot salvo e duplica
  // o watchPosition/setInterval, fazendo o km "resetar"/contar dobrado.
  if (g.ti) return;
  const saved = DB.get('sm_gps_session');
  if (!saved || (saved.st !== 'running' && saved.st !== 'paused')) return;
  // Sessão muito antiga (app ficou fechado por mais de 16h) — não tenta retomar
  const ageMs = Date.now() - (saved.start || 0);
  if (!saved.start || ageMs > 16*3600*1000) { DB.del('sm_gps_session'); return; }
  // Defesa extra: se o Supabase já sabe que esta sessão foi encerrada
  // (ex: gpsStop rodou parcialmente numa tentativa anterior — fechou lá
  // mas não apagou o marcador local), NÃO resume. Confia no servidor.
  if (_supaUser && saved.sessionId && navigator.onLine) {
    _SUPA.from('sessions').select('end_ms,km,start_ms').eq('id', saved.sessionId).maybeSingle()
      .then(({data}) => {
        // Recupera km/start do checkpoint na nuvem se forem melhores que o
        // snapshot local (marcador local pode estar defasado se o app ficou
        // muito tempo em background sem JS rodando).
        if (data && !data.end_ms && S.gps.st !== 'idle' && S.gps.sessionId === saved.sessionId) {
          const cloudKm = parseFloat(data.km) || 0;
          if (cloudKm > (S.gps.km || 0)) { S.gps.km = cloudKm; S.gps.pts = []; S.gps.lastFixTime = null; }
          if (data.start_ms && Number(data.start_ms) < (S.gps.start || Infinity)) S.gps.start = Number(data.start_ms);
          try { if (S.sec==='jornada') renderJornada(); } catch(e) {}
        }
        if (data && data.end_ms) {
          console.warn('[restoreGpsSession] Sessão já encerrada no Supabase — descartando marcador local stale');
          DB.del('sm_gps_session');
          if (S.gps.st !== 'idle') {
            S.gps.st = 'idle'; S.gps.start = null;
            if (S.gps.wid) { try{navigator.geolocation.clearWatch(S.gps.wid);}catch(e){} }
            if (S.gps.ti) clearInterval(S.gps.ti);
            try { renderJornada(); renderHome(); } catch(e) {}
          }
        }
      }).catch(()=>{});
  }
  // Limpeza defensiva — garante que não sobra nenhum watcher/timer órfão
  if (g.wid) { try{ navigator.geolocation.clearWatch(g.wid); }catch(e){} }
  if (g.ti) clearInterval(g.ti);
  g.st = saved.st; g.start = saved.start; g.pausedMs = saved.pausedMs || 0; g.pa = saved.pa || null;
  g.km = saved.km || 0; g.pts = saved.lastPt ? [saved.lastPt] : [];
  g.baseKm = saved.baseKm || 0; g.baseElapsed = saved.baseElapsed || 0;
  g.sessionId = saved.sessionId || null; // restaura ID para continuar o upsert correto
  g.forceDate = saved.forceDate || null;
  // O snapshot acima é só o último valor salvo antes do app ir pra segundo plano (o
  // watchPosition do JS para quando o app não está em foreground). Se o app nativo
  // tiver um GpsService rodando, ele é a fonte mais atual — corrige aqui, antes do
  // primeiro render, pra não mostrar um km/tempo defasado por um instante na tela.
  if (window.SmartMobiNative && typeof SmartMobiNative.isGpsRunning === 'function') {
    try {
      if (SmartMobiNative.isGpsRunning()) {
        // So confia no km nativo se a sessao ja estiver rodando ha mais de alguns
        // segundos. Logo apos um gpsStart(), o sinal de reset enviado pro servico
        // nativo pode ainda nao ter sido processado — ler o km nesse instante corre
        // o risco de pegar um valor antigo (de antes do reset), fazendo o numero
        // "pular" pra um valor maior por um instante ate se corrigir sozinho.
        const sessionAgeMs = Date.now() - (g.start || 0);
        if (sessionAgeMs > 8000) {
          const nativeKm = SmartMobiNative.getGpsKm();
          const nativeStart = SmartMobiNative.getGpsStartTime();
          const nativePaused = SmartMobiNative.getGpsPausedMs();
          // Mesmo guard de plausibilidade do nativeSyncGps — protege contra
          // herdar km de uma sessão fantasma anterior que o nativo nunca zerou
          const elapsedMinR = sessionAgeMs / 60000;
          const maxPlausibleR = (elapsedMinR * 3) + 0.5;
          if (typeof nativeKm === 'number' && nativeKm > maxPlausibleR) {
            console.warn('[restoreGpsSession] km nativo implausível ignorado:', nativeKm, 'em', elapsedMinR.toFixed(1), 'min');
          } else
          // So adota o km nativo se ele for MAIOR que o que a sessao web ja tem.
          // Nunca deixa essa reconciliacao DIMINUIR o km exibido — isso e o que
          // causava o "pulo pra baixo" quando havia uma correcao manual (kmAdjust)
          // aplicada: o servico nativo nao sabe nada sobre essa correcao, entao
          // confiar nele sem essa guarda jogava o numero de volta pro valor cru,
          // descartando a edicao do usuario.
          if (typeof nativeKm === 'number' && nativeKm > g.km) {
            g.km = nativeKm;
            // Limpa referencia JS para evitar dupla contagem no primeiro fix
            g.pts = [];
            g.lastFixTime = null;
          }
          // GUARD: só adota o start nativo se for ANTERIOR ao que já temos.
          // Se o Android matou e reiniciou o GpsService (START_STICKY), o
          // start nativo vira a hora do RESTART — adotar ele fazia a jornada
          // "recomeçar" e o upsert (mesmo sessionId) sobrescrevia o start_ms
          // da sessão no Supabase, perdendo todo o histórico do dia.
          if (nativeStart && nativeStart < g.start) g.start = nativeStart;
          else if (nativeStart && nativeStart > g.start + 60000) {
            // Serviço nativo reiniciou do zero — re-semeia com o estado correto
            console.warn('[restoreGpsSession] Serviço nativo reiniciado (start nativo mais novo) — re-semeando com start/km da sessão real');
            try { SmartMobiNative.startFloating?.(g.start, g.km || 0); } catch(e) {}
          }
          // Guard: não diminui pausedMs com valor menor do nativo
          if (typeof nativePaused === 'number' && nativePaused >= (g.pausedMs || 0)) {
            g.pausedMs = nativePaused;
          }
        }
      }
    } catch(e) {}
  }
  syncNavButton();
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  if (g.st === 'running') requestWakeLock();
  // Se há jornada em aberto mas o serviço nativo está MORTO (Android matou
  // o processo e o START_STICKY ainda não religou, ou foi removido de vez),
  // religa ele semeado com o start/km corretos — a jornada continua de onde
  // parou em vez de ficar sem rastreamento em background.
  if (g.st === 'running' && window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      const nativeUp = typeof SmartMobiNative.isGpsRunning === 'function' && SmartMobiNative.isGpsRunning();
      if (!nativeUp && typeof SmartMobiNative.startFloating === 'function') {
        console.warn('[restoreGpsSession] Serviço nativo morto com jornada em aberto — religando com start/km da sessão');
        SmartMobiNative.startFloating(g.start, g.km || 0);
      }
    } catch(e) {}
  }
  if (window.SmartMobiNative && typeof SmartMobiNative.updateFloatingStatus === 'function') {
    try { SmartMobiNative.updateFloatingStatus(g.st); } catch(e) {}
  }
  // Se o app ficou fechado/em segundo plano e a jornada cruzou a meia-noite
  // nesse meio tempo, avisa agora que reabriu (dá um tempo pro render inicial
  // da tela terminar antes de empilhar o modal por cima).
  setTimeout(checkMidnightCrossing, 600);
}
const _CAR_IMGS = {
  idle:    "data:image/png;base64,",
  running: "data:image/png;base64,",
  paused:  "data:image/png;base64,",
};
function syncNavButton() {
  const g = S.gps;
  let cls = 'idle';
  if (g.st === 'running') cls = 'running';
  else if ((DB.day(DB.today()).sessions||[]).length > 0) cls = 'ended';
  updateGpsBtn(cls);
}
function updateGpsBtn(state) {
  const btn = document.getElementById('gps-btn');
  if (!btn) return;
  btn.className = 'start' + (state && state !== 'idle' ? ' '+state : '');
  const img = document.getElementById('gps-icon-img');
  if (img) img.src = state === 'running' ? _CAR_IMGS.running :
                     state === 'paused'  ? _CAR_IMGS.paused  : _CAR_IMGS.idle;
}
function hav(a,b,c,d) {
  const R=6371, dA=(c-a)*Math.PI/180, dB=(d-b)*Math.PI/180;
  return R*2*Math.asin(Math.sqrt(Math.sin(d
===== sessions @ 946897 =====
    // "recomeçar" e o upsert (mesmo sessionId) sobrescrevia o start_ms
          // da sessão no Supabase, perdendo todo o histórico do dia.
          if (nativeStart && nativeStart < g.start) g.start = nativeStart;
          else if (nativeStart && nativeStart > g.start + 60000) {
            // Serviço nativo reiniciou do zero — re-semeia com o estado correto
            console.warn('[restoreGpsSession] Serviço nativo reiniciado (start nativo mais novo) — re-semeando com start/km da sessão real');
            try { SmartMobiNative.startFloating?.(g.start, g.km || 0); } catch(e) {}
          }
          // Guard: não diminui pausedMs com valor menor do nativo
          if (typeof nativePaused === 'number' && nativePaused >= (g.pausedMs || 0)) {
            g.pausedMs = nativePaused;
          }
        }
      }
    } catch(e) {}
  }
  syncNavButton();
  g.ti = setInterval(gpsUpdate, 1000);
  if (navigator.geolocation) {
    g.wid = navigator.geolocation.watchPosition(gpsOnPosition, null, {enableHighAccuracy:true, maximumAge:3000, timeout:10000});
  }
  if (g.st === 'running') requestWakeLock();
  // Se há jornada em aberto mas o serviço nativo está MORTO (Android matou
  // o processo e o START_STICKY ainda não religou, ou foi removido de vez),
  // religa ele semeado com o start/km corretos — a jornada continua de onde
  // parou em vez de ficar sem rastreamento em background.
  if (g.st === 'running' && window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      const nativeUp = typeof SmartMobiNative.isGpsRunning === 'function' && SmartMobiNative.isGpsRunning();
      if (!nativeUp && typeof SmartMobiNative.startFloating === 'function') {
        console.warn('[restoreGpsSession] Serviço nativo morto com jornada em aberto — religando com start/km da sessão');
        SmartMobiNative.startFloating(g.start, g.km || 0);
      }
    } catch(e) {}
  }
  if (window.SmartMobiNative && typeof SmartMobiNative.updateFloatingStatus === 'function') {
    try { SmartMobiNative.updateFloatingStatus(g.st); } catch(e) {}
  }
  // Se o app ficou fechado/em segundo plano e a jornada cruzou a meia-noite
  // nesse meio tempo, avisa agora que reabriu (dá um tempo pro render inicial
  // da tela terminar antes de empilhar o modal por cima).
  setTimeout(checkMidnightCrossing, 600);
}
const _CAR_IMGS = {
  idle:    "data:image/png;base64,",
  running: "data:image/png;base64,",
  paused:  "data:image/png;base64,",
};
function syncNavButton() {
  const g = S.gps;
  let cls = 'idle';
  if (g.st === 'running') cls = 'running';
  else if ((DB.day(DB.today()).sessions||[]).length > 0) cls = 'ended';
  updateGpsBtn(cls);
}
function updateGpsBtn(state) {
  const btn = document.getElementById('gps-btn');
  if (!btn) return;
  btn.className = 'start' + (state && state !== 'idle' ? ' '+state : '');
  const img = document.getElementById('gps-icon-img');
  if (img) img.src = state === 'running' ? _CAR_IMGS.running :
                     state === 'paused'  ? _CAR_IMGS.paused  : _CAR_IMGS.idle;
}
function hav(a,b,c,d) {
  const R=6371, dA=(c-a)*Math.PI/180, dB=(d-b)*Math.PI/180;
  return R*2*Math.asin(Math.sqrt(Math.sin(dA/2)**2+Math.cos(a*Math.PI/180)*Math.cos(c*Math.PI/180)*Math.sin(dB/2)**2));
}

// ── PERFIL ────────────────────────────────────────────────────
function authSignOutBtn() { showConfirm('Sair da conta?', authSignOut, { confirmLabel: 'Sair' }); }
function perfilAvatarColor() { return feedAvColor((_supaUser && _supaUser.id) || ''); }

function perfilRenderAvatar() {
  const cfg = DB.cfg();
  const initEl = document.getElementById('p-av-initials');
  const imgEl  = document.getElementById('p-av-photo');
  const url = cfg.avatarUrl;
  if (!initEl || !imgEl) return;
  if (url) {
    imgEl.src = url; imgEl.style.display = 'block';
    initEl.style.display = 'none';
  } else {
    initEl.textContent = feedInitials(cfg.name || 'Motorista');
    initEl.style.background = perfilAvatarColor();
    initEl.style.display = 'flex';
    imgEl.style.display = 'none';
  }
  const removeOpt = document.getElementById('p-remove-foto-opt');
  if (removeOpt) removeOpt.style.display = url ? 'flex' : 'none';
}

async function perfilLoadStats() {
  const setStat = (id, v) => { const el = document.getElementById(id); if (el) el.textContent = (v == null ? '—' : v); };
  if (!_SUPA || !_supaUser) return;
  try {
    const [tripsRes, autoRes, postsRes] = await Promise.all([
      _SUPA.from('trips').select('id', { count:'exact', head:true }).eq('user_id', _supaUser.id),
      _SUPA.from('auto_trips').select('id', { count:'exact', head:true }).eq('user_id', _supaUser.id),
      _SUPA.from('feed_posts').select('votes_up').eq('user_id', _supaUser.id)
    ]);
    const corridas = (tripsRes.count || 0) + (autoRes.count || 0);
    const posts = postsRes.data || [];
    const conf = posts.reduce((s,p) => s + (p.votes_up || 0), 0);
    setStat('p-stat-corridas', corridas);
    setStat('p-stat-alertas', posts.length);
    setStat('p-stat-conf', conf);
  } catch (e) { console.warn('perfil stats', e); }
}

function openPerfil() {
  const cfg = DB.cfg();
  if (cfg.name) {const el=document.getElementById('p-nome');if(el)el.value=cfg.name;}
  if (cfg.plate) {const el=document.getElementById('p-placa');if(el)el.value=cfg.plate;}
  const cidEl = document.getElementById('p-cidade'); if (cidEl) cidEl.value = cfg.city || '';
  const vTxt = document.getElementById('p-veiculo-txt');
  if (vTxt) vTxt.textContent = cfg.vehicle || 'Selecionar veículo';

  const nameEl = document.getElementById('p-av-name');
  if (nameEl) nameEl.textContent = cfg.name || 'Motorista MōB';
  const MESES_CURTO = ['jan','fev','mar','abr','mai','jun','jul','ago','set','out','nov','dez'];
  const created = (_supaUser && _supaUser.created_at) ? new Date(_supaUser.created_at) : null;
  const memberSince = created ? MESES_CURTO[created.getMonth()]+'/'+created.getFullYear() : '—';
  const subEl = document.getElementById('p-av-sub');
  if (subEl) subEl.textContent = (cfg.city ? cfg.city+' · ' : '') + 'na MōB desde '+memberSince;
  perfilRenderAvatar();
  perfilLoadStats();

  const emailEl = document.getElementById('acc-email');
  if (emailEl) emailEl.textContent = (_supaUser && _supaUser.email) || '—';
  // Plano: ainda nao ha integracao de pagamento real, todo mundo aparece como Free por enquanto.
  const plan = cfg.plan || 'free';
  const badgeEl = document.getElementById('acc-badge');
  const upgradeBtn = document.getElementById('acc-upgrade-btn');
  const expEl = document.getElementById('acc-exp');
  if (badgeEl) { badgeEl.textContent = plan === 'pro' ? 'Pro' : 'Free'; badgeEl.classList.toggle('pro', plan === 'pro'); }
  if (upgradeBtn) upgradeBtn.style.display = plan === 'pro' ? 'none' : 'block';
  if (expEl) {
    if (plan === 'pro' && cfg.planExpiresAt) { expEl.style.display='block'; expEl.textContent = 'Renova em ' + new Date(cfg.planExpiresAt).toLocaleDateString('pt-BR'); }
    else expEl.style.display = 'none';
  }

  openMo('mo-perfil');
}
function openUpgrade() {
  // Placeholder — ainda sem fluxo de pagamento real, so o botao por enquanto.
  showToast('Em breve você poderá assinar o MōB Pro por aqui.', 'info');
}
function openConfig() {
  const cfg = DB.cfg();
  const ovEl = document.getElementById('cfg-overlay');
  if (ovEl) ovEl.checked = cfg.overlayEnabled !== false;
  const darkEl = document.getElementById('cfg-dark');
  if (darkEl) darkEl.checked = !!cfg.darkMode;

  const pending = DB.syncQueue().length;
  const dot = document.getElementById('cfg-sync-dot');
  const sub = document.getElementById('cfg-sync-sub');
  if (dot) dot.classList.toggle('pending', pending > 0);
  if (sub) sub.textContent = pending > 0 ? (pending+' lançamento'+(pending!==1?'s':'')+' aguardando conexão') : 'Tudo sincronizado';

  const verEl = document.getElementById('cfg-version');
  if (verEl) verEl.textContent = 'MōB · v' + (window.SmartMobiNative?.getVersion?.() || '—');

  openMo('mo-config');
}
function toggleDarkMode(el) {
  const cfg = DB.cfg();
  cfg.darkMode = el.checked;
  DB.saveCfg(cfg);
  applyDarkMode(cfg.darkMode);
}
function applyDarkMode(on) {
  document.documentElement.classList.toggle('dark', !!on);
}
function toggleOverlayPref(el) {
  const cfg = DB.cfg();
  cfg.overlayEnabled = el.checked;
  DB.saveCfg(cfg);
  if (!window.SmartMobiNative) return;
  try {
    if (el.checked) {
      // Se a jornada já estiver rolando, mostra o ícone na hora, sem esperar o próximo sync.
      if (S.gps.st === 'running') {
        SmartMobiNative.startFloating?.(S.gps.start, S.gps.km || 0);
        SmartMobiNative.updateFloatingStatus?.(S.gps.st);
      }
    } else {
      SmartMobiNative.hideFloatingOnly?.();
    }
  } catch(e) {}
}
async function savePerfil() {
  const cfg = DB.cfg();
  cfg.name    = document.getElementById('p-nome')?.value || '';
  cfg.city    = document.getElementById('p-cidade')?.value || '';
  cfg.plate   = document.getElementById('p-placa')?.value || '';

  DB.saveCfg(cfg);
  // Grava na nuvem também — antes esta função só salvava local, então uma
  // edição de nome nunca chegava em profiles.name, e o feed (que lê de lá
  // pros posts de outros usuários) continuava mostrando o nome antigo.
  if (_SUPA && _supaUser) {
    try {
      await _SUPA.from('profiles').upsert(
        { id:_supaUser.id, name:cfg.name, city:cfg.city, plate:cfg.plate, vehicle:cfg.vehicle||null },
        { onConflict:'id' }
      );
    } catch(e) { console.warn('perfil upsert', e); }
  }
  closeMo('mo-perfil');
  renderMais();
  if (S.sec==='relatorios') renderHome();
  showToast('Perfil salvo!', 'success');
}

// ── Foto de perfil ────────────────────────────────────────────
function perfilOpenPhotoSheet() { openMo('mo-perfil-foto'); }
function perfilPickCamera()  { closeMo('mo-perfil-foto'); document.getElementById('p-file-camera')?.click(); }
function perfilPickGallery() { closeMo('mo-perfil-foto'); document.getElementById('p-file-gallery')?.click(); }

// Corta em quadrado (cover, centralizado) e comprime antes de subir —
// evita gastar dados do motorista com foto de câmera em resolução total.
function perfilResizeImage(file, size, quality) {
  size = size || 480; quality = quality || 0.85;
  return new Promise((resolve, reject) => {
    const img = new Image();
    const url = URL.createObjectURL(file);
    img.onload = () => {
      URL.revokeObjectURL(url);
      const side = Math.min(img.width, img.height);
      const sx = (img.width - side) / 2, sy = (img.height - side) / 2;
      const canvas = document.createElement('canvas');
      canvas.width = size; canvas.height = size;
      const ctx = canvas.getContext('2d');
      ctx.drawImage(img, sx, sy, side, side, 0, 0, size, size);
      canvas.toBlob(blob => blob ? resolve(blob) : reject(new Error('toBlob falhou')), 'image/jpeg', quality);
    };
    img.onerror = () => { URL.revokeObjectURL(url); reject(new Error('Imagem inválida')); };
    img.src = url;
  });
}

async function perfilFileSelected(inputEl) {
  const file = inputEl.files && inputEl.files[0];
  inputEl.value = ''; // permite selecionar o mesmo arquivo de novo depois
  if (!file) return;
  if (!file.type || !file.type.startsWith('image/')) { showToast('Selecione uma imagem.'); return; }
  await perfilUploadAvatar(file);
}

async function perfilUploadAvatar(file) {
  if (!_SUPA || !_supaUser) return;
  showToast('Enviando foto…', 'info');
  try {
    const blob = await perfilResizeImage(file, 480, 0.85);
    const path = _supaUser.id + '/avatar.jpg';
    const { error: upErr } = await _SUPA.storage.from('avatars').upload(path, blob, { upsert: true, contentType: 'image/jpeg' });
    if (upErr) throw upErr;
    const { data: pub } = _SUPA.storage.from('avatars').getPublicUrl(path);
    const url = pub.publicUrl + '?t=' + Date.now(); // cache-busting — mesmo path, arquivo novo
    await _SUPA.from('profiles').upsert({ id:_supaUser.id, avatar_url:url }, { onConflict:'id' });
    const cfg = DB.cfg(); cfg.avatarUrl = url; DB.saveCfg(cfg);
    perfilRenderAvatar();
    showToast('Foto atualizada!', 'success');
  } catch (e) {
    console.warn('avatar upload', e);
    showToast('Erro ao enviar a foto. Tente de novo.');
  }
}

async function perfilRemovePhoto() {
  closeMo('mo-perfil-foto');
  const cfg = DB.cfg();
  if (!cfg.avatarUrl) return;
  if (_SUPA && _supaUser) {
    try {
      await _SUPA.storage.from('avatars').remove([_supaUser.id + '/avatar.jpg']);
      await _SUPA.from('profiles').upsert({ id:_supaUser.id, avatar_url:null }, { onConflict:'id' });
    } catch (e) { console.warn('remove avatar', e); }
  }
  cfg
===== sessions @ 964823 =====
const el=document.getElementById('vp-marca-outra'); if (el) el.value = cfg.vehicleBrandCustom || ''; }
  if (cfg.vehicleModel === 'Outro modelo') { const el=document.getElementById('vp-modelo-outro'); if (el) el.value = cfg.vehicleModelCustom || ''; }
  openMo('mo-veiculo');
}
function saveVeiculoPicker() {
  const cfg = DB.cfg();
  let marca = document.getElementById('vp-marca')?.value || '';
  let modelo = document.getElementById('vp-modelo')?.value || '';
  const ano = document.getElementById('vp-ano')?.value || '';
  cfg.vehicleBrand = marca; cfg.vehicleModel = modelo; cfg.vehicleYear = ano;
  if (marca === 'Outra marca') { marca = document.getElementById('vp-marca-outra')?.value || 'Outra'; cfg.vehicleBrandCustom = marca; }
  if (modelo === 'Outro modelo') { modelo = document.getElementById('vp-modelo-outro')?.value || ''; cfg.vehicleModelCustom = modelo; }
  cfg.vehicle = [marca, modelo, ano].filter(Boolean).join(' ');
  DB.saveCfg(cfg);
  const vTxt = document.getElementById('p-veiculo-txt');
  if (vTxt) vTxt.textContent = cfg.vehicle || 'Selecionar veículo';
  closeMo('mo-veiculo');
}

// ── UTILS ─────────────────────────────────────────────────────
function setText(id, text) { const el=document.getElementById(id); if(el) el.textContent=text; }
function setHTML(id, html) { const el=document.getElementById(id); if(el) el.innerHTML=html; }
function setStyle(id, prop, val) { const el=document.getElementById(id); if(el) el.style[prop]=val; }

// ============================================================
// AGENDA — SmartMobi 1.1.0.0
// ============================================================

let agSelDate = DB.today();
let agEvType  = 'normal';
let agColor   = '#3B82F6';
const EV_TYPE_COLOR = { operacional: '#3B82F6', pessoal: '#7C3AED' };

// ── ICON SVGs por tipo ──────────────────────────────────────
const AG_ICONS = {
  jornada:  '<svg viewBox="0 0 24 24"><rect x="3" y="7" width="18" height="13" rx="2"/><path d="M8 7V5a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/><path d="M3 12h18"/></svg>',
  finance:  '<svg viewBox="0 0 24 24"><rect x="3" y="5" width="18" height="14" rx="2"/><path d="M3 10h18"/></svg>',
  default:  '<svg viewBox="0 0 24 24"><rect x="3" y="4" width="18" height="18" rx="2"/><path d="M16 2v4M8 2v4M3 10h18"/></svg>',
  car:      '<svg viewBox="0 0 24 24"><path d="M5 13l1.5-5A3 3 0 0 1 9.4 6h5.2a3 3 0 0 1 2.9 2l1.5 5"/><path d="M3 13h18v6H3z"/></svg>',
  wrench:   '<svg viewBox="0 0 24 24"><path d="M14.7 6.3a4 4 0 0 0-5 5L3 18l3 3 6.7-6.7a4 4 0 0 0 5-5z"/></svg>',
};

function agIconFor(ev) {
  if (ev.type === 'jornada')  return AG_ICONS.jornada;
  if (ev.type === 'finance')  return AG_ICONS.finance;
  return AG_ICONS.default;
}
function agColorClass(hex) {
  const map = {'#22c55e':'green', '#3B82F6':'blue', '#7C3AED':'purple'};
  return map[hex] || 'blue';
}

// ── NAVIGATION ──────────────────────────────────────────────
// Agenda de tela unica: cabecalho sticky (mes expansivel + regua 5 dias) + timeline do dia.
function renderAgenda() {
  renderAgStrip();
  renderAgTimeline();
  const panel = document.getElementById('ag-calpanel');
  if (panel && panel.classList.contains('show')) renderAgCal();
}
function agGoToday() {
  agSelDate = DB.today();
  const dt = new Date();
  agCalY = dt.getFullYear(); agCalM = dt.getMonth();
  agCloseCal();
  renderAgenda();
  agScrollToFirst();
}
function agToggleCal() {
  const bd    = document.getElementById('ag-backdrop');
  const panel = document.getElementById('ag-calpanel');
  const btn   = document.getElementById('ag-month-btn');
  if (!bd || !panel) return;
  const open = !panel.classList.contains('show');
  bd.classList.toggle('show', open);
  panel.classList.toggle('show', open);
  if (btn) btn.classList.toggle('open', open);
  if (open) {
    const dt = new Date(agSelDate + 'T12:00');
    agCalY = dt.getFullYear(); agCalM = dt.getMonth();
    renderAgCal();
  }
}
function agCloseCal() {
  document.getElementById('ag-backdrop')?.classList.remove('show');
  document.getElementById('ag-calpanel')?.classList.remove('show');
  document.getElementById('ag-month-btn')?.classList.remove('open');
}
// Rola a timeline ate o primeiro item do dia (jornada, planejado ou compromisso).
function agScrollToFirst() {
  setTimeout(() => {
    if (S.sec !== 'agenda') return;
    const sc = document.getElementById('main-scroll');
    const tl = document.getElementById('ag-timeline');
    if (!sc || !tl) return;
    let minTop = null;
    tl.querySelectorAll('.ag-card,.ag-planned').forEach(c => {
      const t = parseFloat(c.style.top) || 0;
      if (minTop === null || t < minTop) minTop = t;
    });
    if (minTop === null) minTop = 7 * 64; // dia vazio: abre as 07:00
    const y = tl.getBoundingClientRect().top - sc.getBoundingClientRect().top + sc.scrollTop + minTop - 230;
    sc.scrollTo({ top: Math.max(0, y), behavior: 'smooth' });
  }, 80);
}

// ── RESUMO ──────────────────────────────────────────────────
// Agrega jornadas realizadas do dia a partir de day.sessions (array).
// O campo legado day.session so e gravado quando a jornada encerra NESTE aparelho;
// dados vindos do Supabase (pullAll/reconciliacao) so preenchem day.sessions.
function agDaySess(ds) {
  const day = DB.day(ds);
  const closed = (day.sessions || []).filter(s => s.start && s.end);
  if (closed.length) {
    return {
      km: closed.reduce((a, s) => a + (s.km || 0), 0),
      elapsed: closed.reduce((a, s) => a + (s.elapsed || 0), 0),
      count: closed.length,
      first: Math.min(...closed.map(s => s.start)),
      last: Math.max(...closed.map(s => s.end))
    };
  }
  if (day.session) return { km: day.session.km || 0, elapsed: day.session.elapsed || 0, count: 1, first: null, last: null };
  return null;
}

// ── DIA ─────────────────────────────────────────────────────
function renderAgStrip() {
  const today = DB.today();
  const dt    = new Date(agSelDate + 'T12:00');
  const strip = document.getElementById('ag-strip');
  if (!strip) return;
  const DL = ['Dom','Seg','Ter','Qua','Qui','Sex','S\u00e1b'];
  let html = '';
  // 5 dias, selecionado sempre no centro
  for (let i = -2; i <= 2; i++) {
    const d = new Date(dt); d.setDate(dt.getDate() + i);
    const ds = d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
    const isOn    = ds === agSelDate;
    const isToday = ds === today;
    const hasJ    = !!agDaySess(ds);
    html += `<div class="ag-chip${isOn?' on':isToday?' today-chip':''}${hasJ?' has-j':''}" onclick="agSetDate('${ds}')">
      <span>${DL[d.getDay()]}</span>
      <strong>${d.getDate()}</strong>
      <div class="ag-jdot"></div>
    </div>`;
  }
  strip.innerHTML = html;
  const ml = document.getElementById('ag-month-label');
  if (ml) ml.textContent = MONTH_NAMES[dt.getMonth()] + ' ' + dt.getFullYear();
}

function renderAgTimeline() {
  const tl = document.getElementById('ag-timeline');
  if (!tl) return;
  const hours = [];
  for (let h = 0; h <= 23; h++) {
    hours.push(`<div class="ag-hour" data-h="${h}"><span>${p2(h)}:00</span><div class="ag-hlane"></div></div>`);
  }
  tl.innerHTML = hours.join('');

  const day     = DB.day(agSelDate);
  const evs     = getEventsForDate(agSelDate).filter(e => e.type !== 'jornada');
  const isToday = agSelDate === DB.today();

  // ── Planejado (preenchimento cinza, atrás de tudo, inclusive coluna de hora) ──
  const planned = getPlannedRangesForDate(agSelDate);
  planned.forEach(r => {
    const top = agTimeToY(r.start), bot = agTimeToY(r.end);
    if (bot <= top) return;
    const el = document.createElement('div');
    el.className = 'ag-planned';
    el.style.cssText = `top:${top}px;height:${bot-top}px`;
    tl.appendChild(el);
  });
  tl.querySelectorAll('.ag-hour').forEach(hEl => {
    const h = parseInt(hEl.dataset.h), hTop = h*64, hBot = hTop+64;
    const covered = planned.some(r => agTimeToY(r.start) < hBot && agTimeToY(r.end) > hTop);
    hEl.classList.toggle('in-plan', covered);
  });

  // ── Sessões Online (GPS) — somente visualização, não arrasta ──
  (day.sessions||[]).forEach(s => {
    const top = agTimeToY(fmtHM(s.start));
    const bot = agTimeToY(fmtHM(s.end));
    agAddCard(tl, top, Math.max(20,bot-top), 'online', 'Online',
      `${fmtHM(s.start)} → ${fmtHM(s.end)} · ${formatElapsed(s.elapsed)} · ${(s.km||0).toFixed(1)} km`, null);
  });
  if (isToday && S.gps.st === 'running' && S.gps.start) {
    const top = agTimeToY(fmtHM(S.gps.start));
    const now = new Date();
    const bot = agTimeToY(p2(now.getHours())+':'+p2(now.getMinutes()));
    agAddCard(tl, top, Math.max(20,bot-top), 'online', 'Online · Agora',
      `${fmtHM(S.gps.start)} → Agora · ${formatElapsed(getElapsed())} · ${(S.gps.km||0).toFixed(1)} km`, null);
  }

  // ── Compromissos — editáveis (toque) e arrastáveis/redimensionáveis ──
  evs.forEach(ev => {
    if (!ev.timeStart || ev.timeStart === '—') return;
    const top = agTimeToY(ev.timeStart);
    const bot = ev.timeEnd && ev.timeEnd !== '—' ? agTimeToY(ev.timeEnd) : top + 40;
    const typeClass = ev.type === 'pessoal' ? 'pessoal' : 'operacional';
    agAddCard(tl, top, Math.max(20,bot-top), typeClass, ev.title,
      `${ev.timeStart}${ev.timeEnd && ev.timeEnd!=='—' ? ' → '+ev.timeEnd : ''}${ev.notes?' · '+ev.notes:''}`,
      ev.id);
  });

  // Update date header
  const dt = new Date(agSelDate+'T12:00');
  const ms = ['janeiro','fevereiro','março','abril','maio','junho','julho','agosto','setembro','outubro','novembro','dezembro'];
  const ds = ['Domingo','Segunda','Terça','Quarta','Quinta','Sexta','Sábado'];
  const ddEl = document.getElementById('ag-d-date');
  if (ddEl) ddEl.textContent = ds[dt.getDay()]+', '+dt.getDate()+' de '+ms[dt.getMonth()];
}

function agAddCard(tl, top, ht, typeClass, title, meta, evId) {
  const el = document.createElement('div');
  el.className = 'ag-card ' + typeClass + (ht < 38 ? ' compact' : '');
  el.style.cssText = `top:${top}px;height:${ht}px`;
  el.innerHTML = `<div class="ft">${title}</div><div class="fm">${meta}</div>`;
  if (evId) {
    el.dataset.evId = evId;
    el.innerHTML += `<div class="ag-rsz top"></div><div class="ag-rsz bot"></div>`;
  }
  tl.appendChild(el);
  return el;
}

// ── Planejado (lê de sm_plan_jornada — preenchido futuramente pela tela Planejar Jornada) ──
function getPlannedRangesForDate(d) {
  const dow = new Date(d+'T12:00').getDay(); // 0=Dom...6=Sáb
  const out = [];
  DB.planJornada().forEach(p => {
    if (!p.days || !p.days.includes(dow)) return;
    (p.schedules||[]).forEach(s => { if (s.start && s.end) out.push({start:s.start, end:s.end}); });
  });
  return out;
}

// ── Arrastar/redimensionar compromissos na timeline (delegação única, sem acumular listeners) ──
let _agDrag = null;
function agYToTime(px) {
  const mins = Math.max(0, Math.min(1435, Math.round(px/64*60/5)*5));
  return p2(Math.floor(mins/60))+':'+p2(mins%60);
}
function agTimelineDown(e) {
  const rsz = e.target.closest('.ag-rsz');
  const card = e.target.closest('.ag-card[data-ev-id]');
  if (!card) return;
  const mode = rsz ? (rsz.classList.contains('top') ? 'top' : 'bot') : 'move';
  card._wasDragged = false;
  _agDrag = {
    card, mode,
    startY: (e.touches ? e.touches[0].clientY : e.clientY),
    startTop: parseInt(card.style.top), startH: parseInt(card.style.height),
    tl: card.parentElement
  };
  card.classList.add('dragging');
  e.preventDefault();
}
function agTimelineMove(e) {
  if (!_agDrag) return;
  const { card, mode, startY, startTop, startH, tl } = _agDrag;
  const y = (e.touches ? e.touches[0].clientY : e.clientY);
  const dy = y - startY;
  if (Math.abs(dy) > 4) card._wasDragged = true;
  const px5 = 64/12; // 64px/h ÷ 12 blocos de 5min
  const snapDy = Math.round(dy/px5)*px5;
  if (mode === 'move') {
    const newTop = Math.max(0, Math.min(startTop+snapDy, tl.scrollHeight-startH));
    card.style.top = newTop+'px';
  } else if (mode === 'top') {
    const newTop = Math.max(0, Math.min(startTop+snapDy, startTop+startH-px5));
    card.style.top = newTop+'px';
    card.style.height = (startTop+startH-newTop)+'px';
  } else if (mode === 'bot') {
    const newH = Math.max(px5, Math.min(startH+snapDy, tl.scrollHeight-startTop));
    card.style.height = newH+'px';
  }
  const t = parseInt(card.style.top), h = parseInt(card.style.height);
  card.classList.toggle('compact', h < 38);
  const fm = card.querySelector('.fm');
  if (fm) fm.textContent = agYToTime(t) + ' → ' + agYToTime(t+h);
  e.preventDefault();
}
function agTimelineUp() {
  if (!_agDrag) return;
  const { card } = _agDrag;
  card.classList.remove('dragging');
  if (card._wasDragged) {
    const t = parseInt(card.style.top), h = parseInt(card.style.height);
    agUpdateEventTime(card.dataset.evId, agYToTime(t), agYToTime(t+h));
  }
  _agDrag = null;
}
function agUpdateEventTime(id, newStart, newEnd) {
  const evs = DB.events();
  const ev = evs.find(e => e.id === id);
  if (!ev) return;
  ev.timeStart = newStart; ev.timeEnd = newEnd;
  DB.saveEvents(evs);
}
document.addEventListener('mousedown', agTimelineDown);
document.addEventListener('touchstart', agTimelineDown, {passive:false});
document.addEventListener('mousemove', agTimelineMove);
document.addEventListener('touchmove', agTimelineMove, {passive:false});
document.addEventListener('mouseup', agTimelineUp);
document.addEventListener('touchend', agTimelineUp);
document.addEventListener('click', (e) => {
  const card = e.target.closest('.ag-card[data-ev-id]');
  if (!card || card._wasDragged) return;
  openEditEv(card.dataset.evId);
});

function agTimeToY(timeStr) {
  if (!timeStr || timeStr === '—') return 0;
  const [h, m] = timeStr.split(':').map(Number);
  return Math.max(0, h * 64 + Math.round((m||0) * 64/60));
}
function agSetDate(d) {
  agSelDate = d;
  agCloseCal();
  renderAgenda();
  agScrollToFirst();
}

// ── NEW EVENT FORM ───────────────────────────────────────────
let agEditingId = null;
function openNovoEv(date, time) {
  agEditingId = null;
  const t0 = document.getElementById('ag-sheet-title'); if (t0) t0.textContent = 'Novo compromisso';
  const db = document.getElementById('ag-del-btn'); if (db) db.style.display = 'none';
  const d = date || DB.today();
  const t = time || p2(new Date().getHours())+':'+p2(new Date().getMinutes());
  const di = document.getElementById('ag-data');  if (di) di.value = d;
  const ti = document.getElementById('ag-inicio'); if (ti) ti.value = t;
  const fi = document.getElementById('ag-fim');   if (fi) fi.value = '';
  const tt = document.getElementById('ag-titulo'); if (tt) tt.value = '';
  const ob = document.getElementById('ag-obs');   if (ob) ob.value = '';
  const vi = document.getElementById('ag-valor'); if (vi) vi.value = '';
  agEvType = 'operacional'; agColor = EV_TYPE_COLOR.operacional;
  ['operacional','pessoal'].forEach(x => {
    const el = document.getElementById('ag-t-'+x); if(el) el.classList.toggle('on', x === agEvType);
  });
  openMo('mo-novo-ev');
}
function openEditEv(id) {
  const ev = DB.events().find(e => e.id === id);
  if (!ev) 
===== sessions @ 967842 =====
erAgStrip();
  renderAgTimeline();
  const panel = document.getElementById('ag-calpanel');
  if (panel && panel.classList.contains('show')) renderAgCal();
}
function agGoToday() {
  agSelDate = DB.today();
  const dt = new Date();
  agCalY = dt.getFullYear(); agCalM = dt.getMonth();
  agCloseCal();
  renderAgenda();
  agScrollToFirst();
}
function agToggleCal() {
  const bd    = document.getElementById('ag-backdrop');
  const panel = document.getElementById('ag-calpanel');
  const btn   = document.getElementById('ag-month-btn');
  if (!bd || !panel) return;
  const open = !panel.classList.contains('show');
  bd.classList.toggle('show', open);
  panel.classList.toggle('show', open);
  if (btn) btn.classList.toggle('open', open);
  if (open) {
    const dt = new Date(agSelDate + 'T12:00');
    agCalY = dt.getFullYear(); agCalM = dt.getMonth();
    renderAgCal();
  }
}
function agCloseCal() {
  document.getElementById('ag-backdrop')?.classList.remove('show');
  document.getElementById('ag-calpanel')?.classList.remove('show');
  document.getElementById('ag-month-btn')?.classList.remove('open');
}
// Rola a timeline ate o primeiro item do dia (jornada, planejado ou compromisso).
function agScrollToFirst() {
  setTimeout(() => {
    if (S.sec !== 'agenda') return;
    const sc = document.getElementById('main-scroll');
    const tl = document.getElementById('ag-timeline');
    if (!sc || !tl) return;
    let minTop = null;
    tl.querySelectorAll('.ag-card,.ag-planned').forEach(c => {
      const t = parseFloat(c.style.top) || 0;
      if (minTop === null || t < minTop) minTop = t;
    });
    if (minTop === null) minTop = 7 * 64; // dia vazio: abre as 07:00
    const y = tl.getBoundingClientRect().top - sc.getBoundingClientRect().top + sc.scrollTop + minTop - 230;
    sc.scrollTo({ top: Math.max(0, y), behavior: 'smooth' });
  }, 80);
}

// ── RESUMO ──────────────────────────────────────────────────
// Agrega jornadas realizadas do dia a partir de day.sessions (array).
// O campo legado day.session so e gravado quando a jornada encerra NESTE aparelho;
// dados vindos do Supabase (pullAll/reconciliacao) so preenchem day.sessions.
function agDaySess(ds) {
  const day = DB.day(ds);
  const closed = (day.sessions || []).filter(s => s.start && s.end);
  if (closed.length) {
    return {
      km: closed.reduce((a, s) => a + (s.km || 0), 0),
      elapsed: closed.reduce((a, s) => a + (s.elapsed || 0), 0),
      count: closed.length,
      first: Math.min(...closed.map(s => s.start)),
      last: Math.max(...closed.map(s => s.end))
    };
  }
  if (day.session) return { km: day.session.km || 0, elapsed: day.session.elapsed || 0, count: 1, first: null, last: null };
  return null;
}

// ── DIA ─────────────────────────────────────────────────────
function renderAgStrip() {
  const today = DB.today();
  const dt    = new Date(agSelDate + 'T12:00');
  const strip = document.getElementById('ag-strip');
  if (!strip) return;
  const DL = ['Dom','Seg','Ter','Qua','Qui','Sex','S\u00e1b'];
  let html = '';
  // 5 dias, selecionado sempre no centro
  for (let i = -2; i <= 2; i++) {
    const d = new Date(dt); d.setDate(dt.getDate() + i);
    const ds = d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
    const isOn    = ds === agSelDate;
    const isToday = ds === today;
    const hasJ    = !!agDaySess(ds);
    html += `<div class="ag-chip${isOn?' on':isToday?' today-chip':''}${hasJ?' has-j':''}" onclick="agSetDate('${ds}')">
      <span>${DL[d.getDay()]}</span>
      <strong>${d.getDate()}</strong>
      <div class="ag-jdot"></div>
    </div>`;
  }
  strip.innerHTML = html;
  const ml = document.getElementById('ag-month-label');
  if (ml) ml.textContent = MONTH_NAMES[dt.getMonth()] + ' ' + dt.getFullYear();
}

function renderAgTimeline() {
  const tl = document.getElementById('ag-timeline');
  if (!tl) return;
  const hours = [];
  for (let h = 0; h <= 23; h++) {
    hours.push(`<div class="ag-hour" data-h="${h}"><span>${p2(h)}:00</span><div class="ag-hlane"></div></div>`);
  }
  tl.innerHTML = hours.join('');

  const day     = DB.day(agSelDate);
  const evs     = getEventsForDate(agSelDate).filter(e => e.type !== 'jornada');
  const isToday = agSelDate === DB.today();

  // ── Planejado (preenchimento cinza, atrás de tudo, inclusive coluna de hora) ──
  const planned = getPlannedRangesForDate(agSelDate);
  planned.forEach(r => {
    const top = agTimeToY(r.start), bot = agTimeToY(r.end);
    if (bot <= top) return;
    const el = document.createElement('div');
    el.className = 'ag-planned';
    el.style.cssText = `top:${top}px;height:${bot-top}px`;
    tl.appendChild(el);
  });
  tl.querySelectorAll('.ag-hour').forEach(hEl => {
    const h = parseInt(hEl.dataset.h), hTop = h*64, hBot = hTop+64;
    const covered = planned.some(r => agTimeToY(r.start) < hBot && agTimeToY(r.end) > hTop);
    hEl.classList.toggle('in-plan', covered);
  });

  // ── Sessões Online (GPS) — somente visualização, não arrasta ──
  (day.sessions||[]).forEach(s => {
    const top = agTimeToY(fmtHM(s.start));
    const bot = agTimeToY(fmtHM(s.end));
    agAddCard(tl, top, Math.max(20,bot-top), 'online', 'Online',
      `${fmtHM(s.start)} → ${fmtHM(s.end)} · ${formatElapsed(s.elapsed)} · ${(s.km||0).toFixed(1)} km`, null);
  });
  if (isToday && S.gps.st === 'running' && S.gps.start) {
    const top = agTimeToY(fmtHM(S.gps.start));
    const now = new Date();
    const bot = agTimeToY(p2(now.getHours())+':'+p2(now.getMinutes()));
    agAddCard(tl, top, Math.max(20,bot-top), 'online', 'Online · Agora',
      `${fmtHM(S.gps.start)} → Agora · ${formatElapsed(getElapsed())} · ${(S.gps.km||0).toFixed(1)} km`, null);
  }

  // ── Compromissos — editáveis (toque) e arrastáveis/redimensionáveis ──
  evs.forEach(ev => {
    if (!ev.timeStart || ev.timeStart === '—') return;
    const top = agTimeToY(ev.timeStart);
    const bot = ev.timeEnd && ev.timeEnd !== '—' ? agTimeToY(ev.timeEnd) : top + 40;
    const typeClass = ev.type === 'pessoal' ? 'pessoal' : 'operacional';
    agAddCard(tl, top, Math.max(20,bot-top), typeClass, ev.title,
      `${ev.timeStart}${ev.timeEnd && ev.timeEnd!=='—' ? ' → '+ev.timeEnd : ''}${ev.notes?' · '+ev.notes:''}`,
      ev.id);
  });

  // Update date header
  const dt = new Date(agSelDate+'T12:00');
  const ms = ['janeiro','fevereiro','março','abril','maio','junho','julho','agosto','setembro','outubro','novembro','dezembro'];
  const ds = ['Domingo','Segunda','Terça','Quarta','Quinta','Sexta','Sábado'];
  const ddEl = document.getElementById('ag-d-date');
  if (ddEl) ddEl.textContent = ds[dt.getDay()]+', '+dt.getDate()+' de '+ms[dt.getMonth()];
}

function agAddCard(tl, top, ht, typeClass, title, meta, evId) {
  const el = document.createElement('div');
  el.className = 'ag-card ' + typeClass + (ht < 38 ? ' compact' : '');
  el.style.cssText = `top:${top}px;height:${ht}px`;
  el.innerHTML = `<div class="ft">${title}</div><div class="fm">${meta}</div>`;
  if (evId) {
    el.dataset.evId = evId;
    el.innerHTML += `<div class="ag-rsz top"></div><div class="ag-rsz bot"></div>`;
  }
  tl.appendChild(el);
  return el;
}

// ── Planejado (lê de sm_plan_jornada — preenchido futuramente pela tela Planejar Jornada) ──
function getPlannedRangesForDate(d) {
  const dow = new Date(d+'T12:00').getDay(); // 0=Dom...6=Sáb
  const out = [];
  DB.planJornada().forEach(p => {
    if (!p.days || !p.days.includes(dow)) return;
    (p.schedules||[]).forEach(s => { if (s.start && s.end) out.push({start:s.start, end:s.end}); });
  });
  return out;
}

// ── Arrastar/redimensionar compromissos na timeline (delegação única, sem acumular listeners) ──
let _agDrag = null;
function agYToTime(px) {
  const mins = Math.max(0, Math.min(1435, Math.round(px/64*60/5)*5));
  return p2(Math.floor(mins/60))+':'+p2(mins%60);
}
function agTimelineDown(e) {
  const rsz = e.target.closest('.ag-rsz');
  const card = e.target.closest('.ag-card[data-ev-id]');
  if (!card) return;
  const mode = rsz ? (rsz.classList.contains('top') ? 'top' : 'bot') : 'move';
  card._wasDragged = false;
  _agDrag = {
    card, mode,
    startY: (e.touches ? e.touches[0].clientY : e.clientY),
    startTop: parseInt(card.style.top), startH: parseInt(card.style.height),
    tl: card.parentElement
  };
  card.classList.add('dragging');
  e.preventDefault();
}
function agTimelineMove(e) {
  if (!_agDrag) return;
  const { card, mode, startY, startTop, startH, tl } = _agDrag;
  const y = (e.touches ? e.touches[0].clientY : e.clientY);
  const dy = y - startY;
  if (Math.abs(dy) > 4) card._wasDragged = true;
  const px5 = 64/12; // 64px/h ÷ 12 blocos de 5min
  const snapDy = Math.round(dy/px5)*px5;
  if (mode === 'move') {
    const newTop = Math.max(0, Math.min(startTop+snapDy, tl.scrollHeight-startH));
    card.style.top = newTop+'px';
  } else if (mode === 'top') {
    const newTop = Math.max(0, Math.min(startTop+snapDy, startTop+startH-px5));
    card.style.top = newTop+'px';
    card.style.height = (startTop+startH-newTop)+'px';
  } else if (mode === 'bot') {
    const newH = Math.max(px5, Math.min(startH+snapDy, tl.scrollHeight-startTop));
    card.style.height = newH+'px';
  }
  const t = parseInt(card.style.top), h = parseInt(card.style.height);
  card.classList.toggle('compact', h < 38);
  const fm = card.querySelector('.fm');
  if (fm) fm.textContent = agYToTime(t) + ' → ' + agYToTime(t+h);
  e.preventDefault();
}
function agTimelineUp() {
  if (!_agDrag) return;
  const { card } = _agDrag;
  card.classList.remove('dragging');
  if (card._wasDragged) {
    const t = parseInt(card.style.top), h = parseInt(card.style.height);
    agUpdateEventTime(card.dataset.evId, agYToTime(t), agYToTime(t+h));
  }
  _agDrag = null;
}
function agUpdateEventTime(id, newStart, newEnd) {
  const evs = DB.events();
  const ev = evs.find(e => e.id === id);
  if (!ev) return;
  ev.timeStart = newStart; ev.timeEnd = newEnd;
  DB.saveEvents(evs);
}
document.addEventListener('mousedown', agTimelineDown);
document.addEventListener('touchstart', agTimelineDown, {passive:false});
document.addEventListener('mousemove', agTimelineMove);
document.addEventListener('touchmove', agTimelineMove, {passive:false});
document.addEventListener('mouseup', agTimelineUp);
document.addEventListener('touchend', agTimelineUp);
document.addEventListener('click', (e) => {
  const card = e.target.closest('.ag-card[data-ev-id]');
  if (!card || card._wasDragged) return;
  openEditEv(card.dataset.evId);
});

function agTimeToY(timeStr) {
  if (!timeStr || timeStr === '—') return 0;
  const [h, m] = timeStr.split(':').map(Number);
  return Math.max(0, h * 64 + Math.round((m||0) * 64/60));
}
function agSetDate(d) {
  agSelDate = d;
  agCloseCal();
  renderAgenda();
  agScrollToFirst();
}

// ── NEW EVENT FORM ───────────────────────────────────────────
let agEditingId = null;
function openNovoEv(date, time) {
  agEditingId = null;
  const t0 = document.getElementById('ag-sheet-title'); if (t0) t0.textContent = 'Novo compromisso';
  const db = document.getElementById('ag-del-btn'); if (db) db.style.display = 'none';
  const d = date || DB.today();
  const t = time || p2(new Date().getHours())+':'+p2(new Date().getMinutes());
  const di = document.getElementById('ag-data');  if (di) di.value = d;
  const ti = document.getElementById('ag-inicio'); if (ti) ti.value = t;
  const fi = document.getElementById('ag-fim');   if (fi) fi.value = '';
  const tt = document.getElementById('ag-titulo'); if (tt) tt.value = '';
  const ob = document.getElementById('ag-obs');   if (ob) ob.value = '';
  const vi = document.getElementById('ag-valor'); if (vi) vi.value = '';
  agEvType = 'operacional'; agColor = EV_TYPE_COLOR.operacional;
  ['operacional','pessoal'].forEach(x => {
    const el = document.getElementById('ag-t-'+x); if(el) el.classList.toggle('on', x === agEvType);
  });
  openMo('mo-novo-ev');
}
function openEditEv(id) {
  const ev = DB.events().find(e => e.id === id);
  if (!ev) return;
  agEditingId = id;
  const t0 = document.getElementById('ag-sheet-title'); if (t0) t0.textContent = 'Editar compromisso';
  const db = document.getElementById('ag-del-btn'); if (db) db.style.display = 'block';
  const di = document.getElementById('ag-data');  if (di) di.value = ev.date || DB.today();
  const ti = document.getElementById('ag-inicio'); if (ti) ti.value = ev.timeStart || '';
  const fi = document.getElementById('ag-fim');   if (fi) fi.value = ev.timeEnd || '';
  const tt = document.getElementById('ag-titulo'); if (tt) tt.value = ev.title || '';
  const ob = document.getElementById('ag-obs');   if (ob) ob.value = ev.notes || '';
  const vi = document.getElementById('ag-valor'); if (vi) vi.value = ev.value || '';
  agEvType = ev.type === 'pessoal' ? 'pessoal' : 'operacional';
  agColor  = EV_TYPE_COLOR[agEvType] || EV_TYPE_COLOR.operacional;
  ['operacional','pessoal'].forEach(x => {
    const el = document.getElementById('ag-t-'+x); if(el) el.classList.toggle('on', x === agEvType);
  });
  openMo('mo-novo-ev');
}
function delEventoFromModal() {
  if (!agEditingId) return;
  showConfirm('Excluir compromisso?', () => {
    DB.saveEvents(DB.events().filter(e => e.id !== agEditingId));
    agEditingId = null;
    closeMo('mo-novo-ev');
    renderAgenda();
  });
}
function agSetEvType(t) {
  agEvType = t;
  agColor = EV_TYPE_COLOR[t] || EV_TYPE_COLOR.operacional;
  ['operacional','pessoal'].forEach(x => {
    const el = document.getElementById('ag-t-'+x); if(el) el.classList.toggle('on', x === t);
  });
}
function saveEvento() {
  const title = document.getElementById('ag-titulo')?.value?.trim();
  if (!title) { showToast('Informe o título', 'error'); return; }
  const fields = {
    title,
    date:      document.getElementById('ag-data')?.value  || DB.today(),
    timeStart: document.getElementById('ag-inicio')?.value || '',
    timeEnd:   document.getElementById('ag-fim')?.value   || '',
    type:      agEvType,
    color:     agColor,
    notes:     document.getElementById('ag-obs')?.value   || '',
    value:     parseFloat(document.getElementById('ag-valor')?.value||0)||null,
  };
  const evs = DB.events();
  if (agEditingId) {
    const ev = evs.find(e => e.id === agEditingId);
    if (ev) Object.assign(ev, fields);
  } else {
    evs.push({ id: DB.uid(), paid: false, ...fields });
  }
  DB.saveEvents(evs);
  agEditingId = null;
  closeMo('mo-novo-ev');
  renderAgenda();
}
function delEvento(id) {
  showConfirm('Excluir compromisso?', () => {
    DB.saveEvents(DB.events().filter(e => e.id !== id));
    renderAgenda();
  });
}

// ── HELPERS ─────────────────────────────────────────────────
function getEventsForDate(d) {
  return DB.events().filter(e => e.date === d);
}
function offsetDate(base, days) {
  const d = new Date(base+'T12:00'); d.setDate(d.getDate()+days);
  return d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
}
function formatElapsed(secs) {
  if (!secs) return '';
  const h = Math.floor(secs/3600), m = Math.floor(se
===== sessions @ 1026876 =====
usarComParada) — é assim que o Kotlin
  // já lê desde a v1.0.69, antes do agrupamento "Recusas" existir. Os
  // outros 4 (nota/passageiroNovo/distancia/endereco) vão aninhados em
  // "recusas", com os mesmos nomes de campo que o Kotlin já espera
  // (enabled/value/list).
  const payload = {
    enabled: fr.enabled !== false,
    custoPorKm: custoPorKm,
    kpis: fr.kpis || getFlashDefaults().kpis,
    recusarComParada: recusas.parada ? recusas.parada.enabled : true,
    recusas: {
      nota: recusas.nota,
      passageiroNovo: recusas.passageiroNovo,
      distancia: recusas.distancia,
      endereco: recusas.endereco
    }
  };
  try { SmartMobiNative.saveFlashConfig(JSON.stringify(payload)); } catch (e) {}
}



// ══════════════════════════════════════════════
// SUPABASE AUTH — email + senha
// ══════════════════════════════════════════════
let _supaUser = null;

// ── SINCRONIZAÇÃO COM SUPABASE ──────────────────────────────────────────
// Push: grava cada alteração local também na nuvem (silencioso, não trava a UI).
// Pull: no primeiro boot logado, busca tudo da nuvem e mescla no localStorage,
// pra dados sobreviverem a troca de aparelho ou reinstalação.
const SYNC = {
  // Enfileira uma operacao que falhou pra tentar de novo depois (ex: sem internet
  // no meio de uma corrida). Fica salva no localStorage, sobrevive a fechar o app.
  enqueue(op) {
    const q = DB.syncQueue();
    const id = Date.now()+'_'+Math.random().toString(36).slice(2);
    q.push(Object.assign({ id, ts: Date.now() }, op));
    DB.saveSyncQueue(q);
    this._updateBadge();
    return id;
  },
  dequeue(id) {
    const q = DB.syncQueue().filter(op => op.id !== id);
    DB.saveSyncQueue(q);
    this._updateBadge();
  },
  _updateBadge() {
    const n = DB.syncQueue().length;
    const el = document.getElementById('sync-pending-badge');
    if (el) el.style.display = n > 0 ? 'flex' : 'none';
  },
  async push(table, row) {
    if (!_supaUser) return;
    try {
      await _SUPA.from(table).upsert(Object.assign({}, row, {
        user_id: _supaUser.id, synced_at: new Date().toISOString()
      }));
    } catch (e) {
      console.error('SYNC.push', table, e);
      this.enqueue({ type: 'push', table, row });
    }
  },
  async del(table, match) {
    if (!_supaUser) return;
    try {
      let q = _SUPA.from(table).delete();
      Object.entries(match).forEach(([k, v]) => { q = q.eq(k, v); });
      await q;
    } catch (e) {
      console.error('SYNC.del', table, e);
      this.enqueue({ type: 'del', table, match });
    }
  },
  async pushCats(key) {
    if (!_supaUser) return;
    try {
      await _SUPA.from('categories').upsert({
        user_id: _supaUser.id, cat_key: key, items: DB.cats(key) || DEFAULT_CATS[key] || [],
        goals: DB.objGoals(key) || {},
        synced_at: new Date().toISOString()
      });
    } catch (e) {
      console.error('SYNC.pushCats', e);
      this.enqueue({ type: 'pushCats', key });
    }
  },
  // Tenta reenviar tudo que ficou pendente, na ordem em que foi criado. O que
  // funcionar sai da fila; o que falhar de novo fica pra proxima tentativa.
  async flushQueue() {
    if (!_supaUser || !navigator.onLine) return;
    const q = DB.syncQueue();
    if (!q.length) return;
    const remaining = [];
    for (const op of q) {
      try {
        if (op.type === 'push') {
          await _SUPA.from(op.table).upsert(Object.assign({}, op.row, {
            user_id: _supaUser.id, synced_at: new Date().toISOString()
          }));
        } else if (op.type === 'del') {
          let dq = _SUPA.from(op.table).delete();
          Object.entries(op.match).forEach(([k, v]) => { dq = dq.eq(k, v); });
          await dq;
        } else if (op.type === 'pushCats') {
          await _SUPA.from('categories').upsert({
            user_id: _supaUser.id, cat_key: op.key, items: DB.cats(op.key) || DEFAULT_CATS[op.key] || [],
            goals: DB.objGoals(op.key) || {},
            synced_at: new Date().toISOString()
          });
        }
      } catch (e) {
        console.error('SYNC.flushQueue retry falhou', op, e);
        remaining.push(op);
      }
    }
    DB.saveSyncQueue(remaining);
    this._updateBadge();
  },
  async pushFavs() {
    if (!_supaUser) return;
    try {
      await _SUPA.from('user_favorites').upsert({
        user_id: _supaUser.id,
        items: JSON.parse(localStorage.getItem('sm_inf_favs') || '[]'),
        synced_at: new Date().toISOString()
      }, { onConflict: 'user_id' });
    } catch(e) { console.error('SYNC.pushFavs', e); }
  },
  async pushSettings() {
    if (!_supaUser) return;
    try {
      await _SUPA.from('user_settings').upsert({
        user_id: _supaUser.id,
        data: DB.cfg() || {},
        synced_at: new Date().toISOString()
      }, { onConflict: 'user_id' });
    } catch(e) { console.error('SYNC.pushSettings', e); }
  },
  async pullAll() {
    if (!_supaUser) return;
    const uid = _supaUser.id;
    try {
      const [trips, expenses, revenues, sessions, planJ, cats, dayMeta, eventsRes, settingsRes, favsRes] = await Promise.all([
        (async () => { let all=[],from=0; while(true) { const {data:chunk,error} = await _SUPA.from('trips').select('*').eq('user_id',uid).range(from,from+999);if(error||!chunk||!chunk.length) break; all.push(...chunk); if(chunk.length<1000) break; from+=1000; } return {data:all}; })(),
        _SUPA.from('expenses').select('*').eq('user_id', uid),
        _SUPA.from('revenues').select('*').eq('user_id', uid),
        _SUPA.from('sessions').select('*').eq('user_id', uid),
        _SUPA.from('plan_jornada').select('*').eq('user_id', uid),
        _SUPA.from('categories').select('*').eq('user_id', uid),
        _SUPA.from('day_meta').select('*').eq('user_id', uid),
        _SUPA.from('events').select('*').eq('user_id', uid),
        _SUPA.from('user_settings').select('*').eq('user_id', uid),
        _SUPA.from('user_favorites').select('*').eq('user_id', uid)
      ]);
      const byDate = {};
      const _dirty = new Set();
      const ensure = d => byDate[d] || (byDate[d] = DB.day(d));

      (trips.data || []).forEach(t => {
        const day = ensure(t.date);
        if (!day.trips.find(x => x.id === t.id)) { _dirty.add(t.date); day.trips.push({
          id: t.id, platform: t.platform || 'uber', time: (t.time || '').slice(0, 5),
          duration: t.duration || 0, origin: t.origin || '', dest: t.dest || '',
          value: parseFloat(t.value) || 0, km: parseFloat(t.km) || 0,
          category: t.category || '', surge: t.surge || false, airport: t.airport || false,
          dinamico: parseFloat(t.dinamico) || 0, tip: parseFloat(t.tip) || 0,
          dinheiro: t.dinheiro || false, pedagio: t.pedagio || false, pedagioValor: t.pedagio_valor || 0,
          originFull: t.origin_full || t.origin || '', destFull: t.dest_full || t.dest || '',
          serviceFee: t.service_fee ?? null, taxaZero: t.taxa_zero || false,
          kmEstimado: t.km_estimado || false
        }); }
      });
      (expenses.data || []).forEach(e => {
        const day = ensure(e.date);
        if (!day.expenses.find(x => x.id === e.id)) { _dirty.add(e.date); day.expenses.push({
          id: e.id, category: e.category || '', categoryLabel: e.category_label || '',
          desc: e.description || '', amount: parseFloat(e.amount) || 0, date: e.date,
          scope: e.scope || 'operacional', recorrente: e.recorrente || false, freq: e.freq || null,
          seriesId: e.series_id || null,
          status: e.status || 'a_pagar', dueDate: e.due_date || e.date
        }); }
      });
      (revenues.data || []).forEach(r => {
        const day = ensure(r.date);
        if (!day.revenues.find(x => x.id === r.id)) { _dirty.add(r.date); day.revenues.push({
          id: r.id, category: r.category || '', categoryLabel: r.category_label || '',
          desc: r.description || '', amount: parseFloat(r.amount) || 0, date: r.date,
          scope: r.scope || 'operacional', recorrente: r.recorrente || false, freq: r.freq || null,
          seriesId: r.series_id || null,
          status: r.status || 'pago', dueDate: r.due_date || r.date
        }); }
      });
      (sessions.data || []).forEach(s => {
        const day = ensure(s.date);
        if (!day.sessions) day.sessions = [];
        // Ignorar sessões deletadas (end_ms=-1) e elapsed absurdo (> 20h)
        const sElapsed = s.elapsed_secs || 0;
        const sEnd = s.end_ms ? Number(s.end_ms) : null;
        const isDeleted = sEnd === -1;
        const isBogus = sElapsed > 72000; // > 20h = dado corrompido
        if (!isDeleted && !isBogus && !day.sessions.find(x => x.id === s.id)) {
          _dirty.add(s.date);
          day.sessions.push({
            id: s.id, start: Number(s.start_ms), end: sEnd,
            km: parseFloat(s.km) || 0, elapsed: sElapsed
          });
        }
      });
      // ── Restaurar / limpar sessões abertas no Supabase ─────────────
      if (S.gps.st === 'idle' && !DB.get('sm_gps_session')) {
        const hoje = DB.today();
        const closedSet = _gcss();
        try{const _o=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');if(_o&&_o.id)closedSet.add(_o.id);}catch(e){}

        // 1. Auto-fechar sessões de dias anteriores (nunca devem ficar abertas)
        try {
          (sessions.data||[]).filter(s=>!s.end_ms&&s.date!==hoje).forEach(s=>{
            const est=Number(s.start_ms)+(s.elapsed_secs||0)*1000||Date.now();
            _SUPA.from('sessions').upsert({id:s.id,user_id:uid,end_ms:est,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Auto-fechando sessão dia anterior:',s.id,s.date);
          });
        } catch(e) { console.warn('[pullAll] auto-close error:',e); }

        // 2. Sessões abertas de HOJE — mais recente primeiro
        const abertas=(sessions.data||[]).filter(s=>!s.end_ms&&s.date===hoje)
          .sort((a,b)=>Number(b.start_ms)-Number(a.start_ms));

        for(let i=0;i<abertas.length;i++){
          const ativa=abertas[i];
          const ageMs=Date.now()-Number(ativa.start_ms);
          // Cross-check com localStorage: sessão já foi encerrada localmente?
          const localDay=DB.day(hoje);
          const localSess=(localDay.sessions||[]).find(ls=>ls.id===ativa.id);
          const isClosedLocally=(localSess&&localSess.end)||closedSet.has(ativa.id);
          const isTooOld=ageMs>16*3600*1000;
          const isExtra=i>0; // só a mais recente pode ser restaurada

          if(isClosedLocally||isTooOld||isExtra){
            const bestEnd=(localSess&&localSess.end)||(()=>{
              const c=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');
              return(c&&c.id===ativa.id)?c.end:(Number(ativa.start_ms)+(ativa.elapsed_secs||0)*1000||Date.now());
            })();
            _SUPA.from('sessions').upsert({id:ativa.id,user_id:uid,end_ms:bestEnd,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Sessão encerrada — não restaurar:',ativa.id,{isClosedLocally,isTooOld,isExtra});
            continue;
          }
          // Sessão genuinamente ativa — restaurar
          DB.set('sm_gps_session',{st:'running',start:Number(ativa.start_ms),pausedMs:0,pa:null,
            km:parseFloat(ativa.km)||0,baseKm:0,baseElapsed:0,lastPt:null,sessionId:ativa.id});
          console.log('[pullAll] Sessão restaurada:',ativa.id);
          break;
        }
      }
      (dayMeta.data || []).forEach(m => {
        const day = ensure(m.date);
        const nKm = parseFloat(m.km_adjust) || 0;
        const nTempo = parseInt(m.tempo_adjust_secs) || 0;
        if ((day.kmAdjust||0) !== nKm || (day.tempoAdjust||0) !== nTempo) _dirty.add(m.date);
        day.kmAdjust = nKm;
        day.tempoAdjust = nTempo;
        if (m.km_total_legacy != null && day.kmTotal !== parseFloat(m.km_total_legacy)) {
          day.kmTotal = parseFloat(m.km_total_legacy); _dirty.add(m.date);
        }
      });
      // Só reescreve no localStorage os dias que realmente mudaram
      Object.entries(byDate).forEach(([d, day]) => { if (_dirty.has(d)) DB.saveDay(day); });

      // ── Reconciliação automática ─────────────────────────────────────
      // O merge acima só ADICIONA o que a nuvem tem e falta local — nunca
      // remove local o que sumiu da nuvem (ex: registro apagado direto no
      // banco). Sem isso, um item apagado assim "ressuscita" pra sempre no
      // aparelho, mesmo depois de reabrir o app. Aqui completamos o ciclo:
      // qualquer trip/despesa/receita/sessão que já foi confirmada
      // sincronizada antes e não existe mais na nuvem agora, sai do
      // localStorage também. A fila de pendências (DB.syncQueue()) é a
      // trava de segurança: nada que ainda esteja esperando pra subir é
      // tocado, mesmo que ainda não apareça na nuvem — só mexe no que já
      // tinha certeza absoluta que foi sincronizado uma vez.
      try {
        const pendingIds = { trips: new Set(), expenses: new Set(), revenues: new Set(), sessions: new Set() };
        (DB.syncQueue() || []).forEach(op => {
          if (op.type === 'push' && op.row && op.row.id && pendingIds[op.table]) pendingIds[op.table].add(op.row.id);
        });
        const cloudIds = {
          trips: new Set((trips.data || []).map(t => t.id)),
          expenses: new Set((expenses.data || []).map(e => e.id)),
          revenues: new Set((revenues.data || []).map(r => r.id)),
          sessions: new Set((sessions.data || []).map(s => s.id)),
        };
        (DB.allDates() || []).forEach(d => {
          const day = DB.day(d);
          let changed = false;
          ['trips', 'expenses', 'revenues', 'sessions'].forEach(key => {
            if (!day[key] || !day[key].length) return;
            // item sem id = registro antigo/legado, nunca mexe nele
            const kept = day[key].filter(item => !item.id || cloudIds[key].has(item.id) || pendingIds[key].has(item.id));
            if (kept.length !== day[key].length) { day[key] = kept; changed = true; }
          });
          if (changed) DB.saveDay(day);
        });
      } catch (e) { console.warn('[pullAll] reconciliação falhou:', e); }

      // ── Events ─────────────────────────────────────────────────────────
      if (eventsRes.data && eventsRes.data.length) {
        const localEvs = JSON.parse(localStorage.getItem('sm_events') || '[]');
        let changed = false;
        eventsRes.data.forEach(ev => {
          if (!localEvs.find(x => x.id === ev.id)) {
            localEvs.push({ id: ev.id, title: ev.title, date: ev.date,
              timeStart: ev.time_start, timeEnd: ev.time_end,
              type: ev.type || 'operacional', color: ev.color || '#22C55E',
              notes: ev.notes || '', paid: ev.paid || false });
            changed = true;
          }
        });
        if (changed) localStorage.setItem('sm_events', JSON.stringify(localEvs));
      }

      // ── Settings ───────────────────────────
===== sessions @ 1027397 =====
 passageiroNovo: recusas.passageiroNovo,
      distancia: recusas.distancia,
      endereco: recusas.endereco
    }
  };
  try { SmartMobiNative.saveFlashConfig(JSON.stringify(payload)); } catch (e) {}
}



// ══════════════════════════════════════════════
// SUPABASE AUTH — email + senha
// ══════════════════════════════════════════════
let _supaUser = null;

// ── SINCRONIZAÇÃO COM SUPABASE ──────────────────────────────────────────
// Push: grava cada alteração local também na nuvem (silencioso, não trava a UI).
// Pull: no primeiro boot logado, busca tudo da nuvem e mescla no localStorage,
// pra dados sobreviverem a troca de aparelho ou reinstalação.
const SYNC = {
  // Enfileira uma operacao que falhou pra tentar de novo depois (ex: sem internet
  // no meio de uma corrida). Fica salva no localStorage, sobrevive a fechar o app.
  enqueue(op) {
    const q = DB.syncQueue();
    const id = Date.now()+'_'+Math.random().toString(36).slice(2);
    q.push(Object.assign({ id, ts: Date.now() }, op));
    DB.saveSyncQueue(q);
    this._updateBadge();
    return id;
  },
  dequeue(id) {
    const q = DB.syncQueue().filter(op => op.id !== id);
    DB.saveSyncQueue(q);
    this._updateBadge();
  },
  _updateBadge() {
    const n = DB.syncQueue().length;
    const el = document.getElementById('sync-pending-badge');
    if (el) el.style.display = n > 0 ? 'flex' : 'none';
  },
  async push(table, row) {
    if (!_supaUser) return;
    try {
      await _SUPA.from(table).upsert(Object.assign({}, row, {
        user_id: _supaUser.id, synced_at: new Date().toISOString()
      }));
    } catch (e) {
      console.error('SYNC.push', table, e);
      this.enqueue({ type: 'push', table, row });
    }
  },
  async del(table, match) {
    if (!_supaUser) return;
    try {
      let q = _SUPA.from(table).delete();
      Object.entries(match).forEach(([k, v]) => { q = q.eq(k, v); });
      await q;
    } catch (e) {
      console.error('SYNC.del', table, e);
      this.enqueue({ type: 'del', table, match });
    }
  },
  async pushCats(key) {
    if (!_supaUser) return;
    try {
      await _SUPA.from('categories').upsert({
        user_id: _supaUser.id, cat_key: key, items: DB.cats(key) || DEFAULT_CATS[key] || [],
        goals: DB.objGoals(key) || {},
        synced_at: new Date().toISOString()
      });
    } catch (e) {
      console.error('SYNC.pushCats', e);
      this.enqueue({ type: 'pushCats', key });
    }
  },
  // Tenta reenviar tudo que ficou pendente, na ordem em que foi criado. O que
  // funcionar sai da fila; o que falhar de novo fica pra proxima tentativa.
  async flushQueue() {
    if (!_supaUser || !navigator.onLine) return;
    const q = DB.syncQueue();
    if (!q.length) return;
    const remaining = [];
    for (const op of q) {
      try {
        if (op.type === 'push') {
          await _SUPA.from(op.table).upsert(Object.assign({}, op.row, {
            user_id: _supaUser.id, synced_at: new Date().toISOString()
          }));
        } else if (op.type === 'del') {
          let dq = _SUPA.from(op.table).delete();
          Object.entries(op.match).forEach(([k, v]) => { dq = dq.eq(k, v); });
          await dq;
        } else if (op.type === 'pushCats') {
          await _SUPA.from('categories').upsert({
            user_id: _supaUser.id, cat_key: op.key, items: DB.cats(op.key) || DEFAULT_CATS[op.key] || [],
            goals: DB.objGoals(op.key) || {},
            synced_at: new Date().toISOString()
          });
        }
      } catch (e) {
        console.error('SYNC.flushQueue retry falhou', op, e);
        remaining.push(op);
      }
    }
    DB.saveSyncQueue(remaining);
    this._updateBadge();
  },
  async pushFavs() {
    if (!_supaUser) return;
    try {
      await _SUPA.from('user_favorites').upsert({
        user_id: _supaUser.id,
        items: JSON.parse(localStorage.getItem('sm_inf_favs') || '[]'),
        synced_at: new Date().toISOString()
      }, { onConflict: 'user_id' });
    } catch(e) { console.error('SYNC.pushFavs', e); }
  },
  async pushSettings() {
    if (!_supaUser) return;
    try {
      await _SUPA.from('user_settings').upsert({
        user_id: _supaUser.id,
        data: DB.cfg() || {},
        synced_at: new Date().toISOString()
      }, { onConflict: 'user_id' });
    } catch(e) { console.error('SYNC.pushSettings', e); }
  },
  async pullAll() {
    if (!_supaUser) return;
    const uid = _supaUser.id;
    try {
      const [trips, expenses, revenues, sessions, planJ, cats, dayMeta, eventsRes, settingsRes, favsRes] = await Promise.all([
        (async () => { let all=[],from=0; while(true) { const {data:chunk,error} = await _SUPA.from('trips').select('*').eq('user_id',uid).range(from,from+999);if(error||!chunk||!chunk.length) break; all.push(...chunk); if(chunk.length<1000) break; from+=1000; } return {data:all}; })(),
        _SUPA.from('expenses').select('*').eq('user_id', uid),
        _SUPA.from('revenues').select('*').eq('user_id', uid),
        _SUPA.from('sessions').select('*').eq('user_id', uid),
        _SUPA.from('plan_jornada').select('*').eq('user_id', uid),
        _SUPA.from('categories').select('*').eq('user_id', uid),
        _SUPA.from('day_meta').select('*').eq('user_id', uid),
        _SUPA.from('events').select('*').eq('user_id', uid),
        _SUPA.from('user_settings').select('*').eq('user_id', uid),
        _SUPA.from('user_favorites').select('*').eq('user_id', uid)
      ]);
      const byDate = {};
      const _dirty = new Set();
      const ensure = d => byDate[d] || (byDate[d] = DB.day(d));

      (trips.data || []).forEach(t => {
        const day = ensure(t.date);
        if (!day.trips.find(x => x.id === t.id)) { _dirty.add(t.date); day.trips.push({
          id: t.id, platform: t.platform || 'uber', time: (t.time || '').slice(0, 5),
          duration: t.duration || 0, origin: t.origin || '', dest: t.dest || '',
          value: parseFloat(t.value) || 0, km: parseFloat(t.km) || 0,
          category: t.category || '', surge: t.surge || false, airport: t.airport || false,
          dinamico: parseFloat(t.dinamico) || 0, tip: parseFloat(t.tip) || 0,
          dinheiro: t.dinheiro || false, pedagio: t.pedagio || false, pedagioValor: t.pedagio_valor || 0,
          originFull: t.origin_full || t.origin || '', destFull: t.dest_full || t.dest || '',
          serviceFee: t.service_fee ?? null, taxaZero: t.taxa_zero || false,
          kmEstimado: t.km_estimado || false
        }); }
      });
      (expenses.data || []).forEach(e => {
        const day = ensure(e.date);
        if (!day.expenses.find(x => x.id === e.id)) { _dirty.add(e.date); day.expenses.push({
          id: e.id, category: e.category || '', categoryLabel: e.category_label || '',
          desc: e.description || '', amount: parseFloat(e.amount) || 0, date: e.date,
          scope: e.scope || 'operacional', recorrente: e.recorrente || false, freq: e.freq || null,
          seriesId: e.series_id || null,
          status: e.status || 'a_pagar', dueDate: e.due_date || e.date
        }); }
      });
      (revenues.data || []).forEach(r => {
        const day = ensure(r.date);
        if (!day.revenues.find(x => x.id === r.id)) { _dirty.add(r.date); day.revenues.push({
          id: r.id, category: r.category || '', categoryLabel: r.category_label || '',
          desc: r.description || '', amount: parseFloat(r.amount) || 0, date: r.date,
          scope: r.scope || 'operacional', recorrente: r.recorrente || false, freq: r.freq || null,
          seriesId: r.series_id || null,
          status: r.status || 'pago', dueDate: r.due_date || r.date
        }); }
      });
      (sessions.data || []).forEach(s => {
        const day = ensure(s.date);
        if (!day.sessions) day.sessions = [];
        // Ignorar sessões deletadas (end_ms=-1) e elapsed absurdo (> 20h)
        const sElapsed = s.elapsed_secs || 0;
        const sEnd = s.end_ms ? Number(s.end_ms) : null;
        const isDeleted = sEnd === -1;
        const isBogus = sElapsed > 72000; // > 20h = dado corrompido
        if (!isDeleted && !isBogus && !day.sessions.find(x => x.id === s.id)) {
          _dirty.add(s.date);
          day.sessions.push({
            id: s.id, start: Number(s.start_ms), end: sEnd,
            km: parseFloat(s.km) || 0, elapsed: sElapsed
          });
        }
      });
      // ── Restaurar / limpar sessões abertas no Supabase ─────────────
      if (S.gps.st === 'idle' && !DB.get('sm_gps_session')) {
        const hoje = DB.today();
        const closedSet = _gcss();
        try{const _o=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');if(_o&&_o.id)closedSet.add(_o.id);}catch(e){}

        // 1. Auto-fechar sessões de dias anteriores (nunca devem ficar abertas)
        try {
          (sessions.data||[]).filter(s=>!s.end_ms&&s.date!==hoje).forEach(s=>{
            const est=Number(s.start_ms)+(s.elapsed_secs||0)*1000||Date.now();
            _SUPA.from('sessions').upsert({id:s.id,user_id:uid,end_ms:est,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Auto-fechando sessão dia anterior:',s.id,s.date);
          });
        } catch(e) { console.warn('[pullAll] auto-close error:',e); }

        // 2. Sessões abertas de HOJE — mais recente primeiro
        const abertas=(sessions.data||[]).filter(s=>!s.end_ms&&s.date===hoje)
          .sort((a,b)=>Number(b.start_ms)-Number(a.start_ms));

        for(let i=0;i<abertas.length;i++){
          const ativa=abertas[i];
          const ageMs=Date.now()-Number(ativa.start_ms);
          // Cross-check com localStorage: sessão já foi encerrada localmente?
          const localDay=DB.day(hoje);
          const localSess=(localDay.sessions||[]).find(ls=>ls.id===ativa.id);
          const isClosedLocally=(localSess&&localSess.end)||closedSet.has(ativa.id);
          const isTooOld=ageMs>16*3600*1000;
          const isExtra=i>0; // só a mais recente pode ser restaurada

          if(isClosedLocally||isTooOld||isExtra){
            const bestEnd=(localSess&&localSess.end)||(()=>{
              const c=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');
              return(c&&c.id===ativa.id)?c.end:(Number(ativa.start_ms)+(ativa.elapsed_secs||0)*1000||Date.now());
            })();
            _SUPA.from('sessions').upsert({id:ativa.id,user_id:uid,end_ms:bestEnd,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Sessão encerrada — não restaurar:',ativa.id,{isClosedLocally,isTooOld,isExtra});
            continue;
          }
          // Sessão genuinamente ativa — restaurar
          DB.set('sm_gps_session',{st:'running',start:Number(ativa.start_ms),pausedMs:0,pa:null,
            km:parseFloat(ativa.km)||0,baseKm:0,baseElapsed:0,lastPt:null,sessionId:ativa.id});
          console.log('[pullAll] Sessão restaurada:',ativa.id);
          break;
        }
      }
      (dayMeta.data || []).forEach(m => {
        const day = ensure(m.date);
        const nKm = parseFloat(m.km_adjust) || 0;
        const nTempo = parseInt(m.tempo_adjust_secs) || 0;
        if ((day.kmAdjust||0) !== nKm || (day.tempoAdjust||0) !== nTempo) _dirty.add(m.date);
        day.kmAdjust = nKm;
        day.tempoAdjust = nTempo;
        if (m.km_total_legacy != null && day.kmTotal !== parseFloat(m.km_total_legacy)) {
          day.kmTotal = parseFloat(m.km_total_legacy); _dirty.add(m.date);
        }
      });
      // Só reescreve no localStorage os dias que realmente mudaram
      Object.entries(byDate).forEach(([d, day]) => { if (_dirty.has(d)) DB.saveDay(day); });

      // ── Reconciliação automática ─────────────────────────────────────
      // O merge acima só ADICIONA o que a nuvem tem e falta local — nunca
      // remove local o que sumiu da nuvem (ex: registro apagado direto no
      // banco). Sem isso, um item apagado assim "ressuscita" pra sempre no
      // aparelho, mesmo depois de reabrir o app. Aqui completamos o ciclo:
      // qualquer trip/despesa/receita/sessão que já foi confirmada
      // sincronizada antes e não existe mais na nuvem agora, sai do
      // localStorage também. A fila de pendências (DB.syncQueue()) é a
      // trava de segurança: nada que ainda esteja esperando pra subir é
      // tocado, mesmo que ainda não apareça na nuvem — só mexe no que já
      // tinha certeza absoluta que foi sincronizado uma vez.
      try {
        const pendingIds = { trips: new Set(), expenses: new Set(), revenues: new Set(), sessions: new Set() };
        (DB.syncQueue() || []).forEach(op => {
          if (op.type === 'push' && op.row && op.row.id && pendingIds[op.table]) pendingIds[op.table].add(op.row.id);
        });
        const cloudIds = {
          trips: new Set((trips.data || []).map(t => t.id)),
          expenses: new Set((expenses.data || []).map(e => e.id)),
          revenues: new Set((revenues.data || []).map(r => r.id)),
          sessions: new Set((sessions.data || []).map(s => s.id)),
        };
        (DB.allDates() || []).forEach(d => {
          const day = DB.day(d);
          let changed = false;
          ['trips', 'expenses', 'revenues', 'sessions'].forEach(key => {
            if (!day[key] || !day[key].length) return;
            // item sem id = registro antigo/legado, nunca mexe nele
            const kept = day[key].filter(item => !item.id || cloudIds[key].has(item.id) || pendingIds[key].has(item.id));
            if (kept.length !== day[key].length) { day[key] = kept; changed = true; }
          });
          if (changed) DB.saveDay(day);
        });
      } catch (e) { console.warn('[pullAll] reconciliação falhou:', e); }

      // ── Events ─────────────────────────────────────────────────────────
      if (eventsRes.data && eventsRes.data.length) {
        const localEvs = JSON.parse(localStorage.getItem('sm_events') || '[]');
        let changed = false;
        eventsRes.data.forEach(ev => {
          if (!localEvs.find(x => x.id === ev.id)) {
            localEvs.push({ id: ev.id, title: ev.title, date: ev.date,
              timeStart: ev.time_start, timeEnd: ev.time_end,
              type: ev.type || 'operacional', color: ev.color || '#22C55E',
              notes: ev.notes || '', paid: ev.paid || false });
            changed = true;
          }
        });
        if (changed) localStorage.setItem('sm_events', JSON.stringify(localEvs));
      }

      // ── Settings ───────────────────────────────────────────────────────
      if (settingsRes.data && settingsRes.data.length && settingsRes.data[0].data) {
        const cloudCfg = settingsRes.data[0].data;
        const localCfg = DB.cfg() || {};
        let merged = null;
        // Cadastro geral (nome/placa/veículo/meta): só sobrescreve se o local
        // ainda estiver vazio — evita apagar o que já foi preenchido nesse aparelho.
        if (cloudCfg && Object.keys(cloudCfg).length > 0 && (!localCfg.name || localCfg.name === '')) {
          merged = 
===== sessions @ 1030049 =====
gator.onLine) return;
    const q = DB.syncQueue();
    if (!q.length) return;
    const remaining = [];
    for (const op of q) {
      try {
        if (op.type === 'push') {
          await _SUPA.from(op.table).upsert(Object.assign({}, op.row, {
            user_id: _supaUser.id, synced_at: new Date().toISOString()
          }));
        } else if (op.type === 'del') {
          let dq = _SUPA.from(op.table).delete();
          Object.entries(op.match).forEach(([k, v]) => { dq = dq.eq(k, v); });
          await dq;
        } else if (op.type === 'pushCats') {
          await _SUPA.from('categories').upsert({
            user_id: _supaUser.id, cat_key: op.key, items: DB.cats(op.key) || DEFAULT_CATS[op.key] || [],
            goals: DB.objGoals(op.key) || {},
            synced_at: new Date().toISOString()
          });
        }
      } catch (e) {
        console.error('SYNC.flushQueue retry falhou', op, e);
        remaining.push(op);
      }
    }
    DB.saveSyncQueue(remaining);
    this._updateBadge();
  },
  async pushFavs() {
    if (!_supaUser) return;
    try {
      await _SUPA.from('user_favorites').upsert({
        user_id: _supaUser.id,
        items: JSON.parse(localStorage.getItem('sm_inf_favs') || '[]'),
        synced_at: new Date().toISOString()
      }, { onConflict: 'user_id' });
    } catch(e) { console.error('SYNC.pushFavs', e); }
  },
  async pushSettings() {
    if (!_supaUser) return;
    try {
      await _SUPA.from('user_settings').upsert({
        user_id: _supaUser.id,
        data: DB.cfg() || {},
        synced_at: new Date().toISOString()
      }, { onConflict: 'user_id' });
    } catch(e) { console.error('SYNC.pushSettings', e); }
  },
  async pullAll() {
    if (!_supaUser) return;
    const uid = _supaUser.id;
    try {
      const [trips, expenses, revenues, sessions, planJ, cats, dayMeta, eventsRes, settingsRes, favsRes] = await Promise.all([
        (async () => { let all=[],from=0; while(true) { const {data:chunk,error} = await _SUPA.from('trips').select('*').eq('user_id',uid).range(from,from+999);if(error||!chunk||!chunk.length) break; all.push(...chunk); if(chunk.length<1000) break; from+=1000; } return {data:all}; })(),
        _SUPA.from('expenses').select('*').eq('user_id', uid),
        _SUPA.from('revenues').select('*').eq('user_id', uid),
        _SUPA.from('sessions').select('*').eq('user_id', uid),
        _SUPA.from('plan_jornada').select('*').eq('user_id', uid),
        _SUPA.from('categories').select('*').eq('user_id', uid),
        _SUPA.from('day_meta').select('*').eq('user_id', uid),
        _SUPA.from('events').select('*').eq('user_id', uid),
        _SUPA.from('user_settings').select('*').eq('user_id', uid),
        _SUPA.from('user_favorites').select('*').eq('user_id', uid)
      ]);
      const byDate = {};
      const _dirty = new Set();
      const ensure = d => byDate[d] || (byDate[d] = DB.day(d));

      (trips.data || []).forEach(t => {
        const day = ensure(t.date);
        if (!day.trips.find(x => x.id === t.id)) { _dirty.add(t.date); day.trips.push({
          id: t.id, platform: t.platform || 'uber', time: (t.time || '').slice(0, 5),
          duration: t.duration || 0, origin: t.origin || '', dest: t.dest || '',
          value: parseFloat(t.value) || 0, km: parseFloat(t.km) || 0,
          category: t.category || '', surge: t.surge || false, airport: t.airport || false,
          dinamico: parseFloat(t.dinamico) || 0, tip: parseFloat(t.tip) || 0,
          dinheiro: t.dinheiro || false, pedagio: t.pedagio || false, pedagioValor: t.pedagio_valor || 0,
          originFull: t.origin_full || t.origin || '', destFull: t.dest_full || t.dest || '',
          serviceFee: t.service_fee ?? null, taxaZero: t.taxa_zero || false,
          kmEstimado: t.km_estimado || false
        }); }
      });
      (expenses.data || []).forEach(e => {
        const day = ensure(e.date);
        if (!day.expenses.find(x => x.id === e.id)) { _dirty.add(e.date); day.expenses.push({
          id: e.id, category: e.category || '', categoryLabel: e.category_label || '',
          desc: e.description || '', amount: parseFloat(e.amount) || 0, date: e.date,
          scope: e.scope || 'operacional', recorrente: e.recorrente || false, freq: e.freq || null,
          seriesId: e.series_id || null,
          status: e.status || 'a_pagar', dueDate: e.due_date || e.date
        }); }
      });
      (revenues.data || []).forEach(r => {
        const day = ensure(r.date);
        if (!day.revenues.find(x => x.id === r.id)) { _dirty.add(r.date); day.revenues.push({
          id: r.id, category: r.category || '', categoryLabel: r.category_label || '',
          desc: r.description || '', amount: parseFloat(r.amount) || 0, date: r.date,
          scope: r.scope || 'operacional', recorrente: r.recorrente || false, freq: r.freq || null,
          seriesId: r.series_id || null,
          status: r.status || 'pago', dueDate: r.due_date || r.date
        }); }
      });
      (sessions.data || []).forEach(s => {
        const day = ensure(s.date);
        if (!day.sessions) day.sessions = [];
        // Ignorar sessões deletadas (end_ms=-1) e elapsed absurdo (> 20h)
        const sElapsed = s.elapsed_secs || 0;
        const sEnd = s.end_ms ? Number(s.end_ms) : null;
        const isDeleted = sEnd === -1;
        const isBogus = sElapsed > 72000; // > 20h = dado corrompido
        if (!isDeleted && !isBogus && !day.sessions.find(x => x.id === s.id)) {
          _dirty.add(s.date);
          day.sessions.push({
            id: s.id, start: Number(s.start_ms), end: sEnd,
            km: parseFloat(s.km) || 0, elapsed: sElapsed
          });
        }
      });
      // ── Restaurar / limpar sessões abertas no Supabase ─────────────
      if (S.gps.st === 'idle' && !DB.get('sm_gps_session')) {
        const hoje = DB.today();
        const closedSet = _gcss();
        try{const _o=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');if(_o&&_o.id)closedSet.add(_o.id);}catch(e){}

        // 1. Auto-fechar sessões de dias anteriores (nunca devem ficar abertas)
        try {
          (sessions.data||[]).filter(s=>!s.end_ms&&s.date!==hoje).forEach(s=>{
            const est=Number(s.start_ms)+(s.elapsed_secs||0)*1000||Date.now();
            _SUPA.from('sessions').upsert({id:s.id,user_id:uid,end_ms:est,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Auto-fechando sessão dia anterior:',s.id,s.date);
          });
        } catch(e) { console.warn('[pullAll] auto-close error:',e); }

        // 2. Sessões abertas de HOJE — mais recente primeiro
        const abertas=(sessions.data||[]).filter(s=>!s.end_ms&&s.date===hoje)
          .sort((a,b)=>Number(b.start_ms)-Number(a.start_ms));

        for(let i=0;i<abertas.length;i++){
          const ativa=abertas[i];
          const ageMs=Date.now()-Number(ativa.start_ms);
          // Cross-check com localStorage: sessão já foi encerrada localmente?
          const localDay=DB.day(hoje);
          const localSess=(localDay.sessions||[]).find(ls=>ls.id===ativa.id);
          const isClosedLocally=(localSess&&localSess.end)||closedSet.has(ativa.id);
          const isTooOld=ageMs>16*3600*1000;
          const isExtra=i>0; // só a mais recente pode ser restaurada

          if(isClosedLocally||isTooOld||isExtra){
            const bestEnd=(localSess&&localSess.end)||(()=>{
              const c=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');
              return(c&&c.id===ativa.id)?c.end:(Number(ativa.start_ms)+(ativa.elapsed_secs||0)*1000||Date.now());
            })();
            _SUPA.from('sessions').upsert({id:ativa.id,user_id:uid,end_ms:bestEnd,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Sessão encerrada — não restaurar:',ativa.id,{isClosedLocally,isTooOld,isExtra});
            continue;
          }
          // Sessão genuinamente ativa — restaurar
          DB.set('sm_gps_session',{st:'running',start:Number(ativa.start_ms),pausedMs:0,pa:null,
            km:parseFloat(ativa.km)||0,baseKm:0,baseElapsed:0,lastPt:null,sessionId:ativa.id});
          console.log('[pullAll] Sessão restaurada:',ativa.id);
          break;
        }
      }
      (dayMeta.data || []).forEach(m => {
        const day = ensure(m.date);
        const nKm = parseFloat(m.km_adjust) || 0;
        const nTempo = parseInt(m.tempo_adjust_secs) || 0;
        if ((day.kmAdjust||0) !== nKm || (day.tempoAdjust||0) !== nTempo) _dirty.add(m.date);
        day.kmAdjust = nKm;
        day.tempoAdjust = nTempo;
        if (m.km_total_legacy != null && day.kmTotal !== parseFloat(m.km_total_legacy)) {
          day.kmTotal = parseFloat(m.km_total_legacy); _dirty.add(m.date);
        }
      });
      // Só reescreve no localStorage os dias que realmente mudaram
      Object.entries(byDate).forEach(([d, day]) => { if (_dirty.has(d)) DB.saveDay(day); });

      // ── Reconciliação automática ─────────────────────────────────────
      // O merge acima só ADICIONA o que a nuvem tem e falta local — nunca
      // remove local o que sumiu da nuvem (ex: registro apagado direto no
      // banco). Sem isso, um item apagado assim "ressuscita" pra sempre no
      // aparelho, mesmo depois de reabrir o app. Aqui completamos o ciclo:
      // qualquer trip/despesa/receita/sessão que já foi confirmada
      // sincronizada antes e não existe mais na nuvem agora, sai do
      // localStorage também. A fila de pendências (DB.syncQueue()) é a
      // trava de segurança: nada que ainda esteja esperando pra subir é
      // tocado, mesmo que ainda não apareça na nuvem — só mexe no que já
      // tinha certeza absoluta que foi sincronizado uma vez.
      try {
        const pendingIds = { trips: new Set(), expenses: new Set(), revenues: new Set(), sessions: new Set() };
        (DB.syncQueue() || []).forEach(op => {
          if (op.type === 'push' && op.row && op.row.id && pendingIds[op.table]) pendingIds[op.table].add(op.row.id);
        });
        const cloudIds = {
          trips: new Set((trips.data || []).map(t => t.id)),
          expenses: new Set((expenses.data || []).map(e => e.id)),
          revenues: new Set((revenues.data || []).map(r => r.id)),
          sessions: new Set((sessions.data || []).map(s => s.id)),
        };
        (DB.allDates() || []).forEach(d => {
          const day = DB.day(d);
          let changed = false;
          ['trips', 'expenses', 'revenues', 'sessions'].forEach(key => {
            if (!day[key] || !day[key].length) return;
            // item sem id = registro antigo/legado, nunca mexe nele
            const kept = day[key].filter(item => !item.id || cloudIds[key].has(item.id) || pendingIds[key].has(item.id));
            if (kept.length !== day[key].length) { day[key] = kept; changed = true; }
          });
          if (changed) DB.saveDay(day);
        });
      } catch (e) { console.warn('[pullAll] reconciliação falhou:', e); }

      // ── Events ─────────────────────────────────────────────────────────
      if (eventsRes.data && eventsRes.data.length) {
        const localEvs = JSON.parse(localStorage.getItem('sm_events') || '[]');
        let changed = false;
        eventsRes.data.forEach(ev => {
          if (!localEvs.find(x => x.id === ev.id)) {
            localEvs.push({ id: ev.id, title: ev.title, date: ev.date,
              timeStart: ev.time_start, timeEnd: ev.time_end,
              type: ev.type || 'operacional', color: ev.color || '#22C55E',
              notes: ev.notes || '', paid: ev.paid || false });
            changed = true;
          }
        });
        if (changed) localStorage.setItem('sm_events', JSON.stringify(localEvs));
      }

      // ── Settings ───────────────────────────────────────────────────────
      if (settingsRes.data && settingsRes.data.length && settingsRes.data[0].data) {
        const cloudCfg = settingsRes.data[0].data;
        const localCfg = DB.cfg() || {};
        let merged = null;
        // Cadastro geral (nome/placa/veículo/meta): só sobrescreve se o local
        // ainda estiver vazio — evita apagar o que já foi preenchido nesse aparelho.
        if (cloudCfg && Object.keys(cloudCfg).length > 0 && (!localCfg.name || localCfg.name === '')) {
          merged = Object.assign({}, localCfg, cloudCfg);
        }
        // Custo do veículo e config do MōB Flash NÃO ficam presos nesse gate —
        // são formulários trabalhosos de preencher de novo, e restaurar eles
        // não tem por que depender do campo nome estar vazio ou não.
        if (cloudCfg && cloudCfg.custoVeiculo && cloudCfg.custoVeiculo.configurado &&
            (!localCfg.custoVeiculo || !localCfg.custoVeiculo.configurado)) {
          merged = merged || Object.assign({}, localCfg);
          merged.custoVeiculo = cloudCfg.custoVeiculo;
        }
        if (cloudCfg && cloudCfg.flashReader && !localCfg.flashReader) {
          merged = merged || Object.assign({}, localCfg);
          merged.flashReader = cloudCfg.flashReader;
        }
        if (merged) DB.saveCfg(merged);
      }

      // ── Favoritos ──────────────────────────────────────────────────────
      if (favsRes.data && favsRes.data.length) {
        const cloudFavs = favsRes.data[0].items || [];
        const localFavs = JSON.parse(localStorage.getItem('sm_inf_favs') || '[]');
        // Cloud tem prioridade se local estiver vazio
        if (cloudFavs.length > 0 && localFavs.length === 0) {
          localStorage.setItem('sm_inf_favs', JSON.stringify(cloudFavs));
          _infFavs = cloudFavs;
          infRenderFavs && infRenderFavs();
        }
      }

      if (planJ.data && planJ.data.length) {
        const local = DB.planJornada();
        planJ.data.forEach(p => {
          if (!local.find(x => x.id === p.id)) local.push({
            id: p.id, days: p.days || [],
            moneyGoal: p.money_goal != null ? parseFloat(p.money_goal) : null,
            kmGoal: p.km_goal != null ? parseFloat(p.km_goal) : null,
            schedules: p.schedules || []
          });
        });
        DB.savePlanJornada(local);
      }
      (cats.data || []).forEach(c => {
        if (!DB.get('sm_cats_' + c.cat_key)) DB.saveCats(c.cat_key, c.items || []);
        if (!DB.get('sm_obj_' + c.cat_key) && c.goals) DB.saveObjGoals(c.cat_key, c.goals);
      });
      // Depois de restaurar (inclusive custo/km e config do MōB Flash),
      // manda pro nativo — senão o leitor de tela só pega isso quando o
      // usuário abrir a tela de config manualmente.
      syncFlashConfigToNative();
    } catch (e) { console.error('SYNC.pullAll', e); }
  }
};

// ── Migração local → Supabase (roda 1x após atualização) ──────────────────
async function migrateLocalToCloud() {
  if (!_supaUser) return;
  if (localStorage.getItem('sm_cloud_migration_v2')) return; // já feito

  // BUG CONFIRMADO (13/07/2026): essa função disparava 3s depois de
  // showA
===== sessions @ 1030496 =====
es(op.match).forEach(([k, v]) => { dq = dq.eq(k, v); });
          await dq;
        } else if (op.type === 'pushCats') {
          await _SUPA.from('categories').upsert({
            user_id: _supaUser.id, cat_key: op.key, items: DB.cats(op.key) || DEFAULT_CATS[op.key] || [],
            goals: DB.objGoals(op.key) || {},
            synced_at: new Date().toISOString()
          });
        }
      } catch (e) {
        console.error('SYNC.flushQueue retry falhou', op, e);
        remaining.push(op);
      }
    }
    DB.saveSyncQueue(remaining);
    this._updateBadge();
  },
  async pushFavs() {
    if (!_supaUser) return;
    try {
      await _SUPA.from('user_favorites').upsert({
        user_id: _supaUser.id,
        items: JSON.parse(localStorage.getItem('sm_inf_favs') || '[]'),
        synced_at: new Date().toISOString()
      }, { onConflict: 'user_id' });
    } catch(e) { console.error('SYNC.pushFavs', e); }
  },
  async pushSettings() {
    if (!_supaUser) return;
    try {
      await _SUPA.from('user_settings').upsert({
        user_id: _supaUser.id,
        data: DB.cfg() || {},
        synced_at: new Date().toISOString()
      }, { onConflict: 'user_id' });
    } catch(e) { console.error('SYNC.pushSettings', e); }
  },
  async pullAll() {
    if (!_supaUser) return;
    const uid = _supaUser.id;
    try {
      const [trips, expenses, revenues, sessions, planJ, cats, dayMeta, eventsRes, settingsRes, favsRes] = await Promise.all([
        (async () => { let all=[],from=0; while(true) { const {data:chunk,error} = await _SUPA.from('trips').select('*').eq('user_id',uid).range(from,from+999);if(error||!chunk||!chunk.length) break; all.push(...chunk); if(chunk.length<1000) break; from+=1000; } return {data:all}; })(),
        _SUPA.from('expenses').select('*').eq('user_id', uid),
        _SUPA.from('revenues').select('*').eq('user_id', uid),
        _SUPA.from('sessions').select('*').eq('user_id', uid),
        _SUPA.from('plan_jornada').select('*').eq('user_id', uid),
        _SUPA.from('categories').select('*').eq('user_id', uid),
        _SUPA.from('day_meta').select('*').eq('user_id', uid),
        _SUPA.from('events').select('*').eq('user_id', uid),
        _SUPA.from('user_settings').select('*').eq('user_id', uid),
        _SUPA.from('user_favorites').select('*').eq('user_id', uid)
      ]);
      const byDate = {};
      const _dirty = new Set();
      const ensure = d => byDate[d] || (byDate[d] = DB.day(d));

      (trips.data || []).forEach(t => {
        const day = ensure(t.date);
        if (!day.trips.find(x => x.id === t.id)) { _dirty.add(t.date); day.trips.push({
          id: t.id, platform: t.platform || 'uber', time: (t.time || '').slice(0, 5),
          duration: t.duration || 0, origin: t.origin || '', dest: t.dest || '',
          value: parseFloat(t.value) || 0, km: parseFloat(t.km) || 0,
          category: t.category || '', surge: t.surge || false, airport: t.airport || false,
          dinamico: parseFloat(t.dinamico) || 0, tip: parseFloat(t.tip) || 0,
          dinheiro: t.dinheiro || false, pedagio: t.pedagio || false, pedagioValor: t.pedagio_valor || 0,
          originFull: t.origin_full || t.origin || '', destFull: t.dest_full || t.dest || '',
          serviceFee: t.service_fee ?? null, taxaZero: t.taxa_zero || false,
          kmEstimado: t.km_estimado || false
        }); }
      });
      (expenses.data || []).forEach(e => {
        const day = ensure(e.date);
        if (!day.expenses.find(x => x.id === e.id)) { _dirty.add(e.date); day.expenses.push({
          id: e.id, category: e.category || '', categoryLabel: e.category_label || '',
          desc: e.description || '', amount: parseFloat(e.amount) || 0, date: e.date,
          scope: e.scope || 'operacional', recorrente: e.recorrente || false, freq: e.freq || null,
          seriesId: e.series_id || null,
          status: e.status || 'a_pagar', dueDate: e.due_date || e.date
        }); }
      });
      (revenues.data || []).forEach(r => {
        const day = ensure(r.date);
        if (!day.revenues.find(x => x.id === r.id)) { _dirty.add(r.date); day.revenues.push({
          id: r.id, category: r.category || '', categoryLabel: r.category_label || '',
          desc: r.description || '', amount: parseFloat(r.amount) || 0, date: r.date,
          scope: r.scope || 'operacional', recorrente: r.recorrente || false, freq: r.freq || null,
          seriesId: r.series_id || null,
          status: r.status || 'pago', dueDate: r.due_date || r.date
        }); }
      });
      (sessions.data || []).forEach(s => {
        const day = ensure(s.date);
        if (!day.sessions) day.sessions = [];
        // Ignorar sessões deletadas (end_ms=-1) e elapsed absurdo (> 20h)
        const sElapsed = s.elapsed_secs || 0;
        const sEnd = s.end_ms ? Number(s.end_ms) : null;
        const isDeleted = sEnd === -1;
        const isBogus = sElapsed > 72000; // > 20h = dado corrompido
        if (!isDeleted && !isBogus && !day.sessions.find(x => x.id === s.id)) {
          _dirty.add(s.date);
          day.sessions.push({
            id: s.id, start: Number(s.start_ms), end: sEnd,
            km: parseFloat(s.km) || 0, elapsed: sElapsed
          });
        }
      });
      // ── Restaurar / limpar sessões abertas no Supabase ─────────────
      if (S.gps.st === 'idle' && !DB.get('sm_gps_session')) {
        const hoje = DB.today();
        const closedSet = _gcss();
        try{const _o=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');if(_o&&_o.id)closedSet.add(_o.id);}catch(e){}

        // 1. Auto-fechar sessões de dias anteriores (nunca devem ficar abertas)
        try {
          (sessions.data||[]).filter(s=>!s.end_ms&&s.date!==hoje).forEach(s=>{
            const est=Number(s.start_ms)+(s.elapsed_secs||0)*1000||Date.now();
            _SUPA.from('sessions').upsert({id:s.id,user_id:uid,end_ms:est,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Auto-fechando sessão dia anterior:',s.id,s.date);
          });
        } catch(e) { console.warn('[pullAll] auto-close error:',e); }

        // 2. Sessões abertas de HOJE — mais recente primeiro
        const abertas=(sessions.data||[]).filter(s=>!s.end_ms&&s.date===hoje)
          .sort((a,b)=>Number(b.start_ms)-Number(a.start_ms));

        for(let i=0;i<abertas.length;i++){
          const ativa=abertas[i];
          const ageMs=Date.now()-Number(ativa.start_ms);
          // Cross-check com localStorage: sessão já foi encerrada localmente?
          const localDay=DB.day(hoje);
          const localSess=(localDay.sessions||[]).find(ls=>ls.id===ativa.id);
          const isClosedLocally=(localSess&&localSess.end)||closedSet.has(ativa.id);
          const isTooOld=ageMs>16*3600*1000;
          const isExtra=i>0; // só a mais recente pode ser restaurada

          if(isClosedLocally||isTooOld||isExtra){
            const bestEnd=(localSess&&localSess.end)||(()=>{
              const c=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');
              return(c&&c.id===ativa.id)?c.end:(Number(ativa.start_ms)+(ativa.elapsed_secs||0)*1000||Date.now());
            })();
            _SUPA.from('sessions').upsert({id:ativa.id,user_id:uid,end_ms:bestEnd,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Sessão encerrada — não restaurar:',ativa.id,{isClosedLocally,isTooOld,isExtra});
            continue;
          }
          // Sessão genuinamente ativa — restaurar
          DB.set('sm_gps_session',{st:'running',start:Number(ativa.start_ms),pausedMs:0,pa:null,
            km:parseFloat(ativa.km)||0,baseKm:0,baseElapsed:0,lastPt:null,sessionId:ativa.id});
          console.log('[pullAll] Sessão restaurada:',ativa.id);
          break;
        }
      }
      (dayMeta.data || []).forEach(m => {
        const day = ensure(m.date);
        const nKm = parseFloat(m.km_adjust) || 0;
        const nTempo = parseInt(m.tempo_adjust_secs) || 0;
        if ((day.kmAdjust||0) !== nKm || (day.tempoAdjust||0) !== nTempo) _dirty.add(m.date);
        day.kmAdjust = nKm;
        day.tempoAdjust = nTempo;
        if (m.km_total_legacy != null && day.kmTotal !== parseFloat(m.km_total_legacy)) {
          day.kmTotal = parseFloat(m.km_total_legacy); _dirty.add(m.date);
        }
      });
      // Só reescreve no localStorage os dias que realmente mudaram
      Object.entries(byDate).forEach(([d, day]) => { if (_dirty.has(d)) DB.saveDay(day); });

      // ── Reconciliação automática ─────────────────────────────────────
      // O merge acima só ADICIONA o que a nuvem tem e falta local — nunca
      // remove local o que sumiu da nuvem (ex: registro apagado direto no
      // banco). Sem isso, um item apagado assim "ressuscita" pra sempre no
      // aparelho, mesmo depois de reabrir o app. Aqui completamos o ciclo:
      // qualquer trip/despesa/receita/sessão que já foi confirmada
      // sincronizada antes e não existe mais na nuvem agora, sai do
      // localStorage também. A fila de pendências (DB.syncQueue()) é a
      // trava de segurança: nada que ainda esteja esperando pra subir é
      // tocado, mesmo que ainda não apareça na nuvem — só mexe no que já
      // tinha certeza absoluta que foi sincronizado uma vez.
      try {
        const pendingIds = { trips: new Set(), expenses: new Set(), revenues: new Set(), sessions: new Set() };
        (DB.syncQueue() || []).forEach(op => {
          if (op.type === 'push' && op.row && op.row.id && pendingIds[op.table]) pendingIds[op.table].add(op.row.id);
        });
        const cloudIds = {
          trips: new Set((trips.data || []).map(t => t.id)),
          expenses: new Set((expenses.data || []).map(e => e.id)),
          revenues: new Set((revenues.data || []).map(r => r.id)),
          sessions: new Set((sessions.data || []).map(s => s.id)),
        };
        (DB.allDates() || []).forEach(d => {
          const day = DB.day(d);
          let changed = false;
          ['trips', 'expenses', 'revenues', 'sessions'].forEach(key => {
            if (!day[key] || !day[key].length) return;
            // item sem id = registro antigo/legado, nunca mexe nele
            const kept = day[key].filter(item => !item.id || cloudIds[key].has(item.id) || pendingIds[key].has(item.id));
            if (kept.length !== day[key].length) { day[key] = kept; changed = true; }
          });
          if (changed) DB.saveDay(day);
        });
      } catch (e) { console.warn('[pullAll] reconciliação falhou:', e); }

      // ── Events ─────────────────────────────────────────────────────────
      if (eventsRes.data && eventsRes.data.length) {
        const localEvs = JSON.parse(localStorage.getItem('sm_events') || '[]');
        let changed = false;
        eventsRes.data.forEach(ev => {
          if (!localEvs.find(x => x.id === ev.id)) {
            localEvs.push({ id: ev.id, title: ev.title, date: ev.date,
              timeStart: ev.time_start, timeEnd: ev.time_end,
              type: ev.type || 'operacional', color: ev.color || '#22C55E',
              notes: ev.notes || '', paid: ev.paid || false });
            changed = true;
          }
        });
        if (changed) localStorage.setItem('sm_events', JSON.stringify(localEvs));
      }

      // ── Settings ───────────────────────────────────────────────────────
      if (settingsRes.data && settingsRes.data.length && settingsRes.data[0].data) {
        const cloudCfg = settingsRes.data[0].data;
        const localCfg = DB.cfg() || {};
        let merged = null;
        // Cadastro geral (nome/placa/veículo/meta): só sobrescreve se o local
        // ainda estiver vazio — evita apagar o que já foi preenchido nesse aparelho.
        if (cloudCfg && Object.keys(cloudCfg).length > 0 && (!localCfg.name || localCfg.name === '')) {
          merged = Object.assign({}, localCfg, cloudCfg);
        }
        // Custo do veículo e config do MōB Flash NÃO ficam presos nesse gate —
        // são formulários trabalhosos de preencher de novo, e restaurar eles
        // não tem por que depender do campo nome estar vazio ou não.
        if (cloudCfg && cloudCfg.custoVeiculo && cloudCfg.custoVeiculo.configurado &&
            (!localCfg.custoVeiculo || !localCfg.custoVeiculo.configurado)) {
          merged = merged || Object.assign({}, localCfg);
          merged.custoVeiculo = cloudCfg.custoVeiculo;
        }
        if (cloudCfg && cloudCfg.flashReader && !localCfg.flashReader) {
          merged = merged || Object.assign({}, localCfg);
          merged.flashReader = cloudCfg.flashReader;
        }
        if (merged) DB.saveCfg(merged);
      }

      // ── Favoritos ──────────────────────────────────────────────────────
      if (favsRes.data && favsRes.data.length) {
        const cloudFavs = favsRes.data[0].items || [];
        const localFavs = JSON.parse(localStorage.getItem('sm_inf_favs') || '[]');
        // Cloud tem prioridade se local estiver vazio
        if (cloudFavs.length > 0 && localFavs.length === 0) {
          localStorage.setItem('sm_inf_favs', JSON.stringify(cloudFavs));
          _infFavs = cloudFavs;
          infRenderFavs && infRenderFavs();
        }
      }

      if (planJ.data && planJ.data.length) {
        const local = DB.planJornada();
        planJ.data.forEach(p => {
          if (!local.find(x => x.id === p.id)) local.push({
            id: p.id, days: p.days || [],
            moneyGoal: p.money_goal != null ? parseFloat(p.money_goal) : null,
            kmGoal: p.km_goal != null ? parseFloat(p.km_goal) : null,
            schedules: p.schedules || []
          });
        });
        DB.savePlanJornada(local);
      }
      (cats.data || []).forEach(c => {
        if (!DB.get('sm_cats_' + c.cat_key)) DB.saveCats(c.cat_key, c.items || []);
        if (!DB.get('sm_obj_' + c.cat_key) && c.goals) DB.saveObjGoals(c.cat_key, c.goals);
      });
      // Depois de restaurar (inclusive custo/km e config do MōB Flash),
      // manda pro nativo — senão o leitor de tela só pega isso quando o
      // usuário abrir a tela de config manualmente.
      syncFlashConfigToNative();
    } catch (e) { console.error('SYNC.pullAll', e); }
  }
};

// ── Migração local → Supabase (roda 1x após atualização) ──────────────────
async function migrateLocalToCloud() {
  if (!_supaUser) return;
  if (localStorage.getItem('sm_cloud_migration_v2')) return; // já feito

  // BUG CONFIRMADO (13/07/2026): essa função disparava 3s depois de
  // showApp(), sem esperar o pullAll() da nuvem terminar. pullAll faz ~10
  // consultas em paralelo (inclusive trips paginado inteiro) — em conexão
  // mais lenta facilmente passa de 3s. Se a migração rodasse ANTES do pull
  // terminar, empurrava o local (ainda sem o que tinha vindo de uma
  // instalação anterior, ex: custo por km configurado antes) por cima do
  // que já estava salvo na nuvem — apagando dado bom. A trava acima
  // (sm_cloud_migr
===== sessions @ 1031184 =====
t({
        user_id: _supaUser.id,
        items: JSON.parse(localStorage.getItem('sm_inf_favs') || '[]'),
        synced_at: new Date().toISOString()
      }, { onConflict: 'user_id' });
    } catch(e) { console.error('SYNC.pushFavs', e); }
  },
  async pushSettings() {
    if (!_supaUser) return;
    try {
      await _SUPA.from('user_settings').upsert({
        user_id: _supaUser.id,
        data: DB.cfg() || {},
        synced_at: new Date().toISOString()
      }, { onConflict: 'user_id' });
    } catch(e) { console.error('SYNC.pushSettings', e); }
  },
  async pullAll() {
    if (!_supaUser) return;
    const uid = _supaUser.id;
    try {
      const [trips, expenses, revenues, sessions, planJ, cats, dayMeta, eventsRes, settingsRes, favsRes] = await Promise.all([
        (async () => { let all=[],from=0; while(true) { const {data:chunk,error} = await _SUPA.from('trips').select('*').eq('user_id',uid).range(from,from+999);if(error||!chunk||!chunk.length) break; all.push(...chunk); if(chunk.length<1000) break; from+=1000; } return {data:all}; })(),
        _SUPA.from('expenses').select('*').eq('user_id', uid),
        _SUPA.from('revenues').select('*').eq('user_id', uid),
        _SUPA.from('sessions').select('*').eq('user_id', uid),
        _SUPA.from('plan_jornada').select('*').eq('user_id', uid),
        _SUPA.from('categories').select('*').eq('user_id', uid),
        _SUPA.from('day_meta').select('*').eq('user_id', uid),
        _SUPA.from('events').select('*').eq('user_id', uid),
        _SUPA.from('user_settings').select('*').eq('user_id', uid),
        _SUPA.from('user_favorites').select('*').eq('user_id', uid)
      ]);
      const byDate = {};
      const _dirty = new Set();
      const ensure = d => byDate[d] || (byDate[d] = DB.day(d));

      (trips.data || []).forEach(t => {
        const day = ensure(t.date);
        if (!day.trips.find(x => x.id === t.id)) { _dirty.add(t.date); day.trips.push({
          id: t.id, platform: t.platform || 'uber', time: (t.time || '').slice(0, 5),
          duration: t.duration || 0, origin: t.origin || '', dest: t.dest || '',
          value: parseFloat(t.value) || 0, km: parseFloat(t.km) || 0,
          category: t.category || '', surge: t.surge || false, airport: t.airport || false,
          dinamico: parseFloat(t.dinamico) || 0, tip: parseFloat(t.tip) || 0,
          dinheiro: t.dinheiro || false, pedagio: t.pedagio || false, pedagioValor: t.pedagio_valor || 0,
          originFull: t.origin_full || t.origin || '', destFull: t.dest_full || t.dest || '',
          serviceFee: t.service_fee ?? null, taxaZero: t.taxa_zero || false,
          kmEstimado: t.km_estimado || false
        }); }
      });
      (expenses.data || []).forEach(e => {
        const day = ensure(e.date);
        if (!day.expenses.find(x => x.id === e.id)) { _dirty.add(e.date); day.expenses.push({
          id: e.id, category: e.category || '', categoryLabel: e.category_label || '',
          desc: e.description || '', amount: parseFloat(e.amount) || 0, date: e.date,
          scope: e.scope || 'operacional', recorrente: e.recorrente || false, freq: e.freq || null,
          seriesId: e.series_id || null,
          status: e.status || 'a_pagar', dueDate: e.due_date || e.date
        }); }
      });
      (revenues.data || []).forEach(r => {
        const day = ensure(r.date);
        if (!day.revenues.find(x => x.id === r.id)) { _dirty.add(r.date); day.revenues.push({
          id: r.id, category: r.category || '', categoryLabel: r.category_label || '',
          desc: r.description || '', amount: parseFloat(r.amount) || 0, date: r.date,
          scope: r.scope || 'operacional', recorrente: r.recorrente || false, freq: r.freq || null,
          seriesId: r.series_id || null,
          status: r.status || 'pago', dueDate: r.due_date || r.date
        }); }
      });
      (sessions.data || []).forEach(s => {
        const day = ensure(s.date);
        if (!day.sessions) day.sessions = [];
        // Ignorar sessões deletadas (end_ms=-1) e elapsed absurdo (> 20h)
        const sElapsed = s.elapsed_secs || 0;
        const sEnd = s.end_ms ? Number(s.end_ms) : null;
        const isDeleted = sEnd === -1;
        const isBogus = sElapsed > 72000; // > 20h = dado corrompido
        if (!isDeleted && !isBogus && !day.sessions.find(x => x.id === s.id)) {
          _dirty.add(s.date);
          day.sessions.push({
            id: s.id, start: Number(s.start_ms), end: sEnd,
            km: parseFloat(s.km) || 0, elapsed: sElapsed
          });
        }
      });
      // ── Restaurar / limpar sessões abertas no Supabase ─────────────
      if (S.gps.st === 'idle' && !DB.get('sm_gps_session')) {
        const hoje = DB.today();
        const closedSet = _gcss();
        try{const _o=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');if(_o&&_o.id)closedSet.add(_o.id);}catch(e){}

        // 1. Auto-fechar sessões de dias anteriores (nunca devem ficar abertas)
        try {
          (sessions.data||[]).filter(s=>!s.end_ms&&s.date!==hoje).forEach(s=>{
            const est=Number(s.start_ms)+(s.elapsed_secs||0)*1000||Date.now();
            _SUPA.from('sessions').upsert({id:s.id,user_id:uid,end_ms:est,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Auto-fechando sessão dia anterior:',s.id,s.date);
          });
        } catch(e) { console.warn('[pullAll] auto-close error:',e); }

        // 2. Sessões abertas de HOJE — mais recente primeiro
        const abertas=(sessions.data||[]).filter(s=>!s.end_ms&&s.date===hoje)
          .sort((a,b)=>Number(b.start_ms)-Number(a.start_ms));

        for(let i=0;i<abertas.length;i++){
          const ativa=abertas[i];
          const ageMs=Date.now()-Number(ativa.start_ms);
          // Cross-check com localStorage: sessão já foi encerrada localmente?
          const localDay=DB.day(hoje);
          const localSess=(localDay.sessions||[]).find(ls=>ls.id===ativa.id);
          const isClosedLocally=(localSess&&localSess.end)||closedSet.has(ativa.id);
          const isTooOld=ageMs>16*3600*1000;
          const isExtra=i>0; // só a mais recente pode ser restaurada

          if(isClosedLocally||isTooOld||isExtra){
            const bestEnd=(localSess&&localSess.end)||(()=>{
              const c=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');
              return(c&&c.id===ativa.id)?c.end:(Number(ativa.start_ms)+(ativa.elapsed_secs||0)*1000||Date.now());
            })();
            _SUPA.from('sessions').upsert({id:ativa.id,user_id:uid,end_ms:bestEnd,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Sessão encerrada — não restaurar:',ativa.id,{isClosedLocally,isTooOld,isExtra});
            continue;
          }
          // Sessão genuinamente ativa — restaurar
          DB.set('sm_gps_session',{st:'running',start:Number(ativa.start_ms),pausedMs:0,pa:null,
            km:parseFloat(ativa.km)||0,baseKm:0,baseElapsed:0,lastPt:null,sessionId:ativa.id});
          console.log('[pullAll] Sessão restaurada:',ativa.id);
          break;
        }
      }
      (dayMeta.data || []).forEach(m => {
        const day = ensure(m.date);
        const nKm = parseFloat(m.km_adjust) || 0;
        const nTempo = parseInt(m.tempo_adjust_secs) || 0;
        if ((day.kmAdjust||0) !== nKm || (day.tempoAdjust||0) !== nTempo) _dirty.add(m.date);
        day.kmAdjust = nKm;
        day.tempoAdjust = nTempo;
        if (m.km_total_legacy != null && day.kmTotal !== parseFloat(m.km_total_legacy)) {
          day.kmTotal = parseFloat(m.km_total_legacy); _dirty.add(m.date);
        }
      });
      // Só reescreve no localStorage os dias que realmente mudaram
      Object.entries(byDate).forEach(([d, day]) => { if (_dirty.has(d)) DB.saveDay(day); });

      // ── Reconciliação automática ─────────────────────────────────────
      // O merge acima só ADICIONA o que a nuvem tem e falta local — nunca
      // remove local o que sumiu da nuvem (ex: registro apagado direto no
      // banco). Sem isso, um item apagado assim "ressuscita" pra sempre no
      // aparelho, mesmo depois de reabrir o app. Aqui completamos o ciclo:
      // qualquer trip/despesa/receita/sessão que já foi confirmada
      // sincronizada antes e não existe mais na nuvem agora, sai do
      // localStorage também. A fila de pendências (DB.syncQueue()) é a
      // trava de segurança: nada que ainda esteja esperando pra subir é
      // tocado, mesmo que ainda não apareça na nuvem — só mexe no que já
      // tinha certeza absoluta que foi sincronizado uma vez.
      try {
        const pendingIds = { trips: new Set(), expenses: new Set(), revenues: new Set(), sessions: new Set() };
        (DB.syncQueue() || []).forEach(op => {
          if (op.type === 'push' && op.row && op.row.id && pendingIds[op.table]) pendingIds[op.table].add(op.row.id);
        });
        const cloudIds = {
          trips: new Set((trips.data || []).map(t => t.id)),
          expenses: new Set((expenses.data || []).map(e => e.id)),
          revenues: new Set((revenues.data || []).map(r => r.id)),
          sessions: new Set((sessions.data || []).map(s => s.id)),
        };
        (DB.allDates() || []).forEach(d => {
          const day = DB.day(d);
          let changed = false;
          ['trips', 'expenses', 'revenues', 'sessions'].forEach(key => {
            if (!day[key] || !day[key].length) return;
            // item sem id = registro antigo/legado, nunca mexe nele
            const kept = day[key].filter(item => !item.id || cloudIds[key].has(item.id) || pendingIds[key].has(item.id));
            if (kept.length !== day[key].length) { day[key] = kept; changed = true; }
          });
          if (changed) DB.saveDay(day);
        });
      } catch (e) { console.warn('[pullAll] reconciliação falhou:', e); }

      // ── Events ─────────────────────────────────────────────────────────
      if (eventsRes.data && eventsRes.data.length) {
        const localEvs = JSON.parse(localStorage.getItem('sm_events') || '[]');
        let changed = false;
        eventsRes.data.forEach(ev => {
          if (!localEvs.find(x => x.id === ev.id)) {
            localEvs.push({ id: ev.id, title: ev.title, date: ev.date,
              timeStart: ev.time_start, timeEnd: ev.time_end,
              type: ev.type || 'operacional', color: ev.color || '#22C55E',
              notes: ev.notes || '', paid: ev.paid || false });
            changed = true;
          }
        });
        if (changed) localStorage.setItem('sm_events', JSON.stringify(localEvs));
      }

      // ── Settings ───────────────────────────────────────────────────────
      if (settingsRes.data && settingsRes.data.length && settingsRes.data[0].data) {
        const cloudCfg = settingsRes.data[0].data;
        const localCfg = DB.cfg() || {};
        let merged = null;
        // Cadastro geral (nome/placa/veículo/meta): só sobrescreve se o local
        // ainda estiver vazio — evita apagar o que já foi preenchido nesse aparelho.
        if (cloudCfg && Object.keys(cloudCfg).length > 0 && (!localCfg.name || localCfg.name === '')) {
          merged = Object.assign({}, localCfg, cloudCfg);
        }
        // Custo do veículo e config do MōB Flash NÃO ficam presos nesse gate —
        // são formulários trabalhosos de preencher de novo, e restaurar eles
        // não tem por que depender do campo nome estar vazio ou não.
        if (cloudCfg && cloudCfg.custoVeiculo && cloudCfg.custoVeiculo.configurado &&
            (!localCfg.custoVeiculo || !localCfg.custoVeiculo.configurado)) {
          merged = merged || Object.assign({}, localCfg);
          merged.custoVeiculo = cloudCfg.custoVeiculo;
        }
        if (cloudCfg && cloudCfg.flashReader && !localCfg.flashReader) {
          merged = merged || Object.assign({}, localCfg);
          merged.flashReader = cloudCfg.flashReader;
        }
        if (merged) DB.saveCfg(merged);
      }

      // ── Favoritos ──────────────────────────────────────────────────────
      if (favsRes.data && favsRes.data.length) {
        const cloudFavs = favsRes.data[0].items || [];
        const localFavs = JSON.parse(localStorage.getItem('sm_inf_favs') || '[]');
        // Cloud tem prioridade se local estiver vazio
        if (cloudFavs.length > 0 && localFavs.length === 0) {
          localStorage.setItem('sm_inf_favs', JSON.stringify(cloudFavs));
          _infFavs = cloudFavs;
          infRenderFavs && infRenderFavs();
        }
      }

      if (planJ.data && planJ.data.length) {
        const local = DB.planJornada();
        planJ.data.forEach(p => {
          if (!local.find(x => x.id === p.id)) local.push({
            id: p.id, days: p.days || [],
            moneyGoal: p.money_goal != null ? parseFloat(p.money_goal) : null,
            kmGoal: p.km_goal != null ? parseFloat(p.km_goal) : null,
            schedules: p.schedules || []
          });
        });
        DB.savePlanJornada(local);
      }
      (cats.data || []).forEach(c => {
        if (!DB.get('sm_cats_' + c.cat_key)) DB.saveCats(c.cat_key, c.items || []);
        if (!DB.get('sm_obj_' + c.cat_key) && c.goals) DB.saveObjGoals(c.cat_key, c.goals);
      });
      // Depois de restaurar (inclusive custo/km e config do MōB Flash),
      // manda pro nativo — senão o leitor de tela só pega isso quando o
      // usuário abrir a tela de config manualmente.
      syncFlashConfigToNative();
    } catch (e) { console.error('SYNC.pullAll', e); }
  }
};

// ── Migração local → Supabase (roda 1x após atualização) ──────────────────
async function migrateLocalToCloud() {
  if (!_supaUser) return;
  if (localStorage.getItem('sm_cloud_migration_v2')) return; // já feito

  // BUG CONFIRMADO (13/07/2026): essa função disparava 3s depois de
  // showApp(), sem esperar o pullAll() da nuvem terminar. pullAll faz ~10
  // consultas em paralelo (inclusive trips paginado inteiro) — em conexão
  // mais lenta facilmente passa de 3s. Se a migração rodasse ANTES do pull
  // terminar, empurrava o local (ainda sem o que tinha vindo de uma
  // instalação anterior, ex: custo por km configurado antes) por cima do
  // que já estava salvo na nuvem — apagando dado bom. A trava acima
  // (sm_cloud_migration_v2) só evita rodar de novo NA MESMA instalação —
  // um reinstall zera o localStorage e essa trava junto, então a função
  // roda de novo correndo contra o pull toda vez que reinstala. Correção:
  // espera _bootPullDone antes de migrar qualquer coisa, mesmo mecanismo
  // que a tela de Custo do Veículo já usa pra não mostrar formulário vazio
  // por cima de dado que só ainda não chegou.
  // Espera bem mais que os 4s padrão do _waitBootPull(): essa migração roda
  // em background sem travar nenhuma tela, então não tem custo de UX em
  // esperar mais. O risco de esperar pouco é o contrário — subir pro
  // Supabase uma corrida local que a reconciliação do pullAll() ain
===== sessions @ 1032130 =====
r||!chunk||!chunk.length) break; all.push(...chunk); if(chunk.length<1000) break; from+=1000; } return {data:all}; })(),
        _SUPA.from('expenses').select('*').eq('user_id', uid),
        _SUPA.from('revenues').select('*').eq('user_id', uid),
        _SUPA.from('sessions').select('*').eq('user_id', uid),
        _SUPA.from('plan_jornada').select('*').eq('user_id', uid),
        _SUPA.from('categories').select('*').eq('user_id', uid),
        _SUPA.from('day_meta').select('*').eq('user_id', uid),
        _SUPA.from('events').select('*').eq('user_id', uid),
        _SUPA.from('user_settings').select('*').eq('user_id', uid),
        _SUPA.from('user_favorites').select('*').eq('user_id', uid)
      ]);
      const byDate = {};
      const _dirty = new Set();
      const ensure = d => byDate[d] || (byDate[d] = DB.day(d));

      (trips.data || []).forEach(t => {
        const day = ensure(t.date);
        if (!day.trips.find(x => x.id === t.id)) { _dirty.add(t.date); day.trips.push({
          id: t.id, platform: t.platform || 'uber', time: (t.time || '').slice(0, 5),
          duration: t.duration || 0, origin: t.origin || '', dest: t.dest || '',
          value: parseFloat(t.value) || 0, km: parseFloat(t.km) || 0,
          category: t.category || '', surge: t.surge || false, airport: t.airport || false,
          dinamico: parseFloat(t.dinamico) || 0, tip: parseFloat(t.tip) || 0,
          dinheiro: t.dinheiro || false, pedagio: t.pedagio || false, pedagioValor: t.pedagio_valor || 0,
          originFull: t.origin_full || t.origin || '', destFull: t.dest_full || t.dest || '',
          serviceFee: t.service_fee ?? null, taxaZero: t.taxa_zero || false,
          kmEstimado: t.km_estimado || false
        }); }
      });
      (expenses.data || []).forEach(e => {
        const day = ensure(e.date);
        if (!day.expenses.find(x => x.id === e.id)) { _dirty.add(e.date); day.expenses.push({
          id: e.id, category: e.category || '', categoryLabel: e.category_label || '',
          desc: e.description || '', amount: parseFloat(e.amount) || 0, date: e.date,
          scope: e.scope || 'operacional', recorrente: e.recorrente || false, freq: e.freq || null,
          seriesId: e.series_id || null,
          status: e.status || 'a_pagar', dueDate: e.due_date || e.date
        }); }
      });
      (revenues.data || []).forEach(r => {
        const day = ensure(r.date);
        if (!day.revenues.find(x => x.id === r.id)) { _dirty.add(r.date); day.revenues.push({
          id: r.id, category: r.category || '', categoryLabel: r.category_label || '',
          desc: r.description || '', amount: parseFloat(r.amount) || 0, date: r.date,
          scope: r.scope || 'operacional', recorrente: r.recorrente || false, freq: r.freq || null,
          seriesId: r.series_id || null,
          status: r.status || 'pago', dueDate: r.due_date || r.date
        }); }
      });
      (sessions.data || []).forEach(s => {
        const day = ensure(s.date);
        if (!day.sessions) day.sessions = [];
        // Ignorar sessões deletadas (end_ms=-1) e elapsed absurdo (> 20h)
        const sElapsed = s.elapsed_secs || 0;
        const sEnd = s.end_ms ? Number(s.end_ms) : null;
        const isDeleted = sEnd === -1;
        const isBogus = sElapsed > 72000; // > 20h = dado corrompido
        if (!isDeleted && !isBogus && !day.sessions.find(x => x.id === s.id)) {
          _dirty.add(s.date);
          day.sessions.push({
            id: s.id, start: Number(s.start_ms), end: sEnd,
            km: parseFloat(s.km) || 0, elapsed: sElapsed
          });
        }
      });
      // ── Restaurar / limpar sessões abertas no Supabase ─────────────
      if (S.gps.st === 'idle' && !DB.get('sm_gps_session')) {
        const hoje = DB.today();
        const closedSet = _gcss();
        try{const _o=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');if(_o&&_o.id)closedSet.add(_o.id);}catch(e){}

        // 1. Auto-fechar sessões de dias anteriores (nunca devem ficar abertas)
        try {
          (sessions.data||[]).filter(s=>!s.end_ms&&s.date!==hoje).forEach(s=>{
            const est=Number(s.start_ms)+(s.elapsed_secs||0)*1000||Date.now();
            _SUPA.from('sessions').upsert({id:s.id,user_id:uid,end_ms:est,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Auto-fechando sessão dia anterior:',s.id,s.date);
          });
        } catch(e) { console.warn('[pullAll] auto-close error:',e); }

        // 2. Sessões abertas de HOJE — mais recente primeiro
        const abertas=(sessions.data||[]).filter(s=>!s.end_ms&&s.date===hoje)
          .sort((a,b)=>Number(b.start_ms)-Number(a.start_ms));

        for(let i=0;i<abertas.length;i++){
          const ativa=abertas[i];
          const ageMs=Date.now()-Number(ativa.start_ms);
          // Cross-check com localStorage: sessão já foi encerrada localmente?
          const localDay=DB.day(hoje);
          const localSess=(localDay.sessions||[]).find(ls=>ls.id===ativa.id);
          const isClosedLocally=(localSess&&localSess.end)||closedSet.has(ativa.id);
          const isTooOld=ageMs>16*3600*1000;
          const isExtra=i>0; // só a mais recente pode ser restaurada

          if(isClosedLocally||isTooOld||isExtra){
            const bestEnd=(localSess&&localSess.end)||(()=>{
              const c=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');
              return(c&&c.id===ativa.id)?c.end:(Number(ativa.start_ms)+(ativa.elapsed_secs||0)*1000||Date.now());
            })();
            _SUPA.from('sessions').upsert({id:ativa.id,user_id:uid,end_ms:bestEnd,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Sessão encerrada — não restaurar:',ativa.id,{isClosedLocally,isTooOld,isExtra});
            continue;
          }
          // Sessão genuinamente ativa — restaurar
          DB.set('sm_gps_session',{st:'running',start:Number(ativa.start_ms),pausedMs:0,pa:null,
            km:parseFloat(ativa.km)||0,baseKm:0,baseElapsed:0,lastPt:null,sessionId:ativa.id});
          console.log('[pullAll] Sessão restaurada:',ativa.id);
          break;
        }
      }
      (dayMeta.data || []).forEach(m => {
        const day = ensure(m.date);
        const nKm = parseFloat(m.km_adjust) || 0;
        const nTempo = parseInt(m.tempo_adjust_secs) || 0;
        if ((day.kmAdjust||0) !== nKm || (day.tempoAdjust||0) !== nTempo) _dirty.add(m.date);
        day.kmAdjust = nKm;
        day.tempoAdjust = nTempo;
        if (m.km_total_legacy != null && day.kmTotal !== parseFloat(m.km_total_legacy)) {
          day.kmTotal = parseFloat(m.km_total_legacy); _dirty.add(m.date);
        }
      });
      // Só reescreve no localStorage os dias que realmente mudaram
      Object.entries(byDate).forEach(([d, day]) => { if (_dirty.has(d)) DB.saveDay(day); });

      // ── Reconciliação automática ─────────────────────────────────────
      // O merge acima só ADICIONA o que a nuvem tem e falta local — nunca
      // remove local o que sumiu da nuvem (ex: registro apagado direto no
      // banco). Sem isso, um item apagado assim "ressuscita" pra sempre no
      // aparelho, mesmo depois de reabrir o app. Aqui completamos o ciclo:
      // qualquer trip/despesa/receita/sessão que já foi confirmada
      // sincronizada antes e não existe mais na nuvem agora, sai do
      // localStorage também. A fila de pendências (DB.syncQueue()) é a
      // trava de segurança: nada que ainda esteja esperando pra subir é
      // tocado, mesmo que ainda não apareça na nuvem — só mexe no que já
      // tinha certeza absoluta que foi sincronizado uma vez.
      try {
        const pendingIds = { trips: new Set(), expenses: new Set(), revenues: new Set(), sessions: new Set() };
        (DB.syncQueue() || []).forEach(op => {
          if (op.type === 'push' && op.row && op.row.id && pendingIds[op.table]) pendingIds[op.table].add(op.row.id);
        });
        const cloudIds = {
          trips: new Set((trips.data || []).map(t => t.id)),
          expenses: new Set((expenses.data || []).map(e => e.id)),
          revenues: new Set((revenues.data || []).map(r => r.id)),
          sessions: new Set((sessions.data || []).map(s => s.id)),
        };
        (DB.allDates() || []).forEach(d => {
          const day = DB.day(d);
          let changed = false;
          ['trips', 'expenses', 'revenues', 'sessions'].forEach(key => {
            if (!day[key] || !day[key].length) return;
            // item sem id = registro antigo/legado, nunca mexe nele
            const kept = day[key].filter(item => !item.id || cloudIds[key].has(item.id) || pendingIds[key].has(item.id));
            if (kept.length !== day[key].length) { day[key] = kept; changed = true; }
          });
          if (changed) DB.saveDay(day);
        });
      } catch (e) { console.warn('[pullAll] reconciliação falhou:', e); }

      // ── Events ─────────────────────────────────────────────────────────
      if (eventsRes.data && eventsRes.data.length) {
        const localEvs = JSON.parse(localStorage.getItem('sm_events') || '[]');
        let changed = false;
        eventsRes.data.forEach(ev => {
          if (!localEvs.find(x => x.id === ev.id)) {
            localEvs.push({ id: ev.id, title: ev.title, date: ev.date,
              timeStart: ev.time_start, timeEnd: ev.time_end,
              type: ev.type || 'operacional', color: ev.color || '#22C55E',
              notes: ev.notes || '', paid: ev.paid || false });
            changed = true;
          }
        });
        if (changed) localStorage.setItem('sm_events', JSON.stringify(localEvs));
      }

      // ── Settings ───────────────────────────────────────────────────────
      if (settingsRes.data && settingsRes.data.length && settingsRes.data[0].data) {
        const cloudCfg = settingsRes.data[0].data;
        const localCfg = DB.cfg() || {};
        let merged = null;
        // Cadastro geral (nome/placa/veículo/meta): só sobrescreve se o local
        // ainda estiver vazio — evita apagar o que já foi preenchido nesse aparelho.
        if (cloudCfg && Object.keys(cloudCfg).length > 0 && (!localCfg.name || localCfg.name === '')) {
          merged = Object.assign({}, localCfg, cloudCfg);
        }
        // Custo do veículo e config do MōB Flash NÃO ficam presos nesse gate —
        // são formulários trabalhosos de preencher de novo, e restaurar eles
        // não tem por que depender do campo nome estar vazio ou não.
        if (cloudCfg && cloudCfg.custoVeiculo && cloudCfg.custoVeiculo.configurado &&
            (!localCfg.custoVeiculo || !localCfg.custoVeiculo.configurado)) {
          merged = merged || Object.assign({}, localCfg);
          merged.custoVeiculo = cloudCfg.custoVeiculo;
        }
        if (cloudCfg && cloudCfg.flashReader && !localCfg.flashReader) {
          merged = merged || Object.assign({}, localCfg);
          merged.flashReader = cloudCfg.flashReader;
        }
        if (merged) DB.saveCfg(merged);
      }

      // ── Favoritos ──────────────────────────────────────────────────────
      if (favsRes.data && favsRes.data.length) {
        const cloudFavs = favsRes.data[0].items || [];
        const localFavs = JSON.parse(localStorage.getItem('sm_inf_favs') || '[]');
        // Cloud tem prioridade se local estiver vazio
        if (cloudFavs.length > 0 && localFavs.length === 0) {
          localStorage.setItem('sm_inf_favs', JSON.stringify(cloudFavs));
          _infFavs = cloudFavs;
          infRenderFavs && infRenderFavs();
        }
      }

      if (planJ.data && planJ.data.length) {
        const local = DB.planJornada();
        planJ.data.forEach(p => {
          if (!local.find(x => x.id === p.id)) local.push({
            id: p.id, days: p.days || [],
            moneyGoal: p.money_goal != null ? parseFloat(p.money_goal) : null,
            kmGoal: p.km_goal != null ? parseFloat(p.km_goal) : null,
            schedules: p.schedules || []
          });
        });
        DB.savePlanJornada(local);
      }
      (cats.data || []).forEach(c => {
        if (!DB.get('sm_cats_' + c.cat_key)) DB.saveCats(c.cat_key, c.items || []);
        if (!DB.get('sm_obj_' + c.cat_key) && c.goals) DB.saveObjGoals(c.cat_key, c.goals);
      });
      // Depois de restaurar (inclusive custo/km e config do MōB Flash),
      // manda pro nativo — senão o leitor de tela só pega isso quando o
      // usuário abrir a tela de config manualmente.
      syncFlashConfigToNative();
    } catch (e) { console.error('SYNC.pullAll', e); }
  }
};

// ── Migração local → Supabase (roda 1x após atualização) ──────────────────
async function migrateLocalToCloud() {
  if (!_supaUser) return;
  if (localStorage.getItem('sm_cloud_migration_v2')) return; // já feito

  // BUG CONFIRMADO (13/07/2026): essa função disparava 3s depois de
  // showApp(), sem esperar o pullAll() da nuvem terminar. pullAll faz ~10
  // consultas em paralelo (inclusive trips paginado inteiro) — em conexão
  // mais lenta facilmente passa de 3s. Se a migração rodasse ANTES do pull
  // terminar, empurrava o local (ainda sem o que tinha vindo de uma
  // instalação anterior, ex: custo por km configurado antes) por cima do
  // que já estava salvo na nuvem — apagando dado bom. A trava acima
  // (sm_cloud_migration_v2) só evita rodar de novo NA MESMA instalação —
  // um reinstall zera o localStorage e essa trava junto, então a função
  // roda de novo correndo contra o pull toda vez que reinstala. Correção:
  // espera _bootPullDone antes de migrar qualquer coisa, mesmo mecanismo
  // que a tela de Custo do Veículo já usa pra não mostrar formulário vazio
  // por cima de dado que só ainda não chegou.
  // Espera bem mais que os 4s padrão do _waitBootPull(): essa migração roda
  // em background sem travar nenhuma tela, então não tem custo de UX em
  // esperar mais. O risco de esperar pouco é o contrário — subir pro
  // Supabase uma corrida local que a reconciliação do pullAll() ainda não
  // teve tempo de apagar (ex: já tinha sido excluída direto no banco),
  // ressuscitando dado errado. Confirmado acontecendo em 18/07/2026.
  await _waitBootPull(30000);

  console.log('[MōB] Iniciando migração local → Supabase...');
  let pushed = 0;

  // 1. Todos os dados de dias: trips, expenses, revenues
  for (const [key, raw] of Object.entries(localStorage)) {
    if (!key.startsWith('sm_day_')) continue;
    const date = key.replace('sm_day_', '');
    try {
      const day = JSON.parse(raw);
      for (const t of day.trips || []) {
        await SYNC.push('trips', { id: t.id, date, platform: t.platform||'uber',
          time: t.time||null, duration: t.duration||0, origin: t.origin||'',
          dest: t.dest||'', value: t.value||0, km: t.km||0,
          source: t.source||'manual', surge: t.surge||false, airport: t.airport||false,
          dinamico: t.dinamico||0, tip: t.tip||0 });
        pushed++;
      }
     
===== sessions @ 1032726 =====
ngs').select('*').eq('user_id', uid),
        _SUPA.from('user_favorites').select('*').eq('user_id', uid)
      ]);
      const byDate = {};
      const _dirty = new Set();
      const ensure = d => byDate[d] || (byDate[d] = DB.day(d));

      (trips.data || []).forEach(t => {
        const day = ensure(t.date);
        if (!day.trips.find(x => x.id === t.id)) { _dirty.add(t.date); day.trips.push({
          id: t.id, platform: t.platform || 'uber', time: (t.time || '').slice(0, 5),
          duration: t.duration || 0, origin: t.origin || '', dest: t.dest || '',
          value: parseFloat(t.value) || 0, km: parseFloat(t.km) || 0,
          category: t.category || '', surge: t.surge || false, airport: t.airport || false,
          dinamico: parseFloat(t.dinamico) || 0, tip: parseFloat(t.tip) || 0,
          dinheiro: t.dinheiro || false, pedagio: t.pedagio || false, pedagioValor: t.pedagio_valor || 0,
          originFull: t.origin_full || t.origin || '', destFull: t.dest_full || t.dest || '',
          serviceFee: t.service_fee ?? null, taxaZero: t.taxa_zero || false,
          kmEstimado: t.km_estimado || false
        }); }
      });
      (expenses.data || []).forEach(e => {
        const day = ensure(e.date);
        if (!day.expenses.find(x => x.id === e.id)) { _dirty.add(e.date); day.expenses.push({
          id: e.id, category: e.category || '', categoryLabel: e.category_label || '',
          desc: e.description || '', amount: parseFloat(e.amount) || 0, date: e.date,
          scope: e.scope || 'operacional', recorrente: e.recorrente || false, freq: e.freq || null,
          seriesId: e.series_id || null,
          status: e.status || 'a_pagar', dueDate: e.due_date || e.date
        }); }
      });
      (revenues.data || []).forEach(r => {
        const day = ensure(r.date);
        if (!day.revenues.find(x => x.id === r.id)) { _dirty.add(r.date); day.revenues.push({
          id: r.id, category: r.category || '', categoryLabel: r.category_label || '',
          desc: r.description || '', amount: parseFloat(r.amount) || 0, date: r.date,
          scope: r.scope || 'operacional', recorrente: r.recorrente || false, freq: r.freq || null,
          seriesId: r.series_id || null,
          status: r.status || 'pago', dueDate: r.due_date || r.date
        }); }
      });
      (sessions.data || []).forEach(s => {
        const day = ensure(s.date);
        if (!day.sessions) day.sessions = [];
        // Ignorar sessões deletadas (end_ms=-1) e elapsed absurdo (> 20h)
        const sElapsed = s.elapsed_secs || 0;
        const sEnd = s.end_ms ? Number(s.end_ms) : null;
        const isDeleted = sEnd === -1;
        const isBogus = sElapsed > 72000; // > 20h = dado corrompido
        if (!isDeleted && !isBogus && !day.sessions.find(x => x.id === s.id)) {
          _dirty.add(s.date);
          day.sessions.push({
            id: s.id, start: Number(s.start_ms), end: sEnd,
            km: parseFloat(s.km) || 0, elapsed: sElapsed
          });
        }
      });
      // ── Restaurar / limpar sessões abertas no Supabase ─────────────
      if (S.gps.st === 'idle' && !DB.get('sm_gps_session')) {
        const hoje = DB.today();
        const closedSet = _gcss();
        try{const _o=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');if(_o&&_o.id)closedSet.add(_o.id);}catch(e){}

        // 1. Auto-fechar sessões de dias anteriores (nunca devem ficar abertas)
        try {
          (sessions.data||[]).filter(s=>!s.end_ms&&s.date!==hoje).forEach(s=>{
            const est=Number(s.start_ms)+(s.elapsed_secs||0)*1000||Date.now();
            _SUPA.from('sessions').upsert({id:s.id,user_id:uid,end_ms:est,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Auto-fechando sessão dia anterior:',s.id,s.date);
          });
        } catch(e) { console.warn('[pullAll] auto-close error:',e); }

        // 2. Sessões abertas de HOJE — mais recente primeiro
        const abertas=(sessions.data||[]).filter(s=>!s.end_ms&&s.date===hoje)
          .sort((a,b)=>Number(b.start_ms)-Number(a.start_ms));

        for(let i=0;i<abertas.length;i++){
          const ativa=abertas[i];
          const ageMs=Date.now()-Number(ativa.start_ms);
          // Cross-check com localStorage: sessão já foi encerrada localmente?
          const localDay=DB.day(hoje);
          const localSess=(localDay.sessions||[]).find(ls=>ls.id===ativa.id);
          const isClosedLocally=(localSess&&localSess.end)||closedSet.has(ativa.id);
          const isTooOld=ageMs>16*3600*1000;
          const isExtra=i>0; // só a mais recente pode ser restaurada

          if(isClosedLocally||isTooOld||isExtra){
            const bestEnd=(localSess&&localSess.end)||(()=>{
              const c=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');
              return(c&&c.id===ativa.id)?c.end:(Number(ativa.start_ms)+(ativa.elapsed_secs||0)*1000||Date.now());
            })();
            _SUPA.from('sessions').upsert({id:ativa.id,user_id:uid,end_ms:bestEnd,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Sessão encerrada — não restaurar:',ativa.id,{isClosedLocally,isTooOld,isExtra});
            continue;
          }
          // Sessão genuinamente ativa — restaurar
          DB.set('sm_gps_session',{st:'running',start:Number(ativa.start_ms),pausedMs:0,pa:null,
            km:parseFloat(ativa.km)||0,baseKm:0,baseElapsed:0,lastPt:null,sessionId:ativa.id});
          console.log('[pullAll] Sessão restaurada:',ativa.id);
          break;
        }
      }
      (dayMeta.data || []).forEach(m => {
        const day = ensure(m.date);
        const nKm = parseFloat(m.km_adjust) || 0;
        const nTempo = parseInt(m.tempo_adjust_secs) || 0;
        if ((day.kmAdjust||0) !== nKm || (day.tempoAdjust||0) !== nTempo) _dirty.add(m.date);
        day.kmAdjust = nKm;
        day.tempoAdjust = nTempo;
        if (m.km_total_legacy != null && day.kmTotal !== parseFloat(m.km_total_legacy)) {
          day.kmTotal = parseFloat(m.km_total_legacy); _dirty.add(m.date);
        }
      });
      // Só reescreve no localStorage os dias que realmente mudaram
      Object.entries(byDate).forEach(([d, day]) => { if (_dirty.has(d)) DB.saveDay(day); });

      // ── Reconciliação automática ─────────────────────────────────────
      // O merge acima só ADICIONA o que a nuvem tem e falta local — nunca
      // remove local o que sumiu da nuvem (ex: registro apagado direto no
      // banco). Sem isso, um item apagado assim "ressuscita" pra sempre no
      // aparelho, mesmo depois de reabrir o app. Aqui completamos o ciclo:
      // qualquer trip/despesa/receita/sessão que já foi confirmada
      // sincronizada antes e não existe mais na nuvem agora, sai do
      // localStorage também. A fila de pendências (DB.syncQueue()) é a
      // trava de segurança: nada que ainda esteja esperando pra subir é
      // tocado, mesmo que ainda não apareça na nuvem — só mexe no que já
      // tinha certeza absoluta que foi sincronizado uma vez.
      try {
        const pendingIds = { trips: new Set(), expenses: new Set(), revenues: new Set(), sessions: new Set() };
        (DB.syncQueue() || []).forEach(op => {
          if (op.type === 'push' && op.row && op.row.id && pendingIds[op.table]) pendingIds[op.table].add(op.row.id);
        });
        const cloudIds = {
          trips: new Set((trips.data || []).map(t => t.id)),
          expenses: new Set((expenses.data || []).map(e => e.id)),
          revenues: new Set((revenues.data || []).map(r => r.id)),
          sessions: new Set((sessions.data || []).map(s => s.id)),
        };
        (DB.allDates() || []).forEach(d => {
          const day = DB.day(d);
          let changed = false;
          ['trips', 'expenses', 'revenues', 'sessions'].forEach(key => {
            if (!day[key] || !day[key].length) return;
            // item sem id = registro antigo/legado, nunca mexe nele
            const kept = day[key].filter(item => !item.id || cloudIds[key].has(item.id) || pendingIds[key].has(item.id));
            if (kept.length !== day[key].length) { day[key] = kept; changed = true; }
          });
          if (changed) DB.saveDay(day);
        });
      } catch (e) { console.warn('[pullAll] reconciliação falhou:', e); }

      // ── Events ─────────────────────────────────────────────────────────
      if (eventsRes.data && eventsRes.data.length) {
        const localEvs = JSON.parse(localStorage.getItem('sm_events') || '[]');
        let changed = false;
        eventsRes.data.forEach(ev => {
          if (!localEvs.find(x => x.id === ev.id)) {
            localEvs.push({ id: ev.id, title: ev.title, date: ev.date,
              timeStart: ev.time_start, timeEnd: ev.time_end,
              type: ev.type || 'operacional', color: ev.color || '#22C55E',
              notes: ev.notes || '', paid: ev.paid || false });
            changed = true;
          }
        });
        if (changed) localStorage.setItem('sm_events', JSON.stringify(localEvs));
      }

      // ── Settings ───────────────────────────────────────────────────────
      if (settingsRes.data && settingsRes.data.length && settingsRes.data[0].data) {
        const cloudCfg = settingsRes.data[0].data;
        const localCfg = DB.cfg() || {};
        let merged = null;
        // Cadastro geral (nome/placa/veículo/meta): só sobrescreve se o local
        // ainda estiver vazio — evita apagar o que já foi preenchido nesse aparelho.
        if (cloudCfg && Object.keys(cloudCfg).length > 0 && (!localCfg.name || localCfg.name === '')) {
          merged = Object.assign({}, localCfg, cloudCfg);
        }
        // Custo do veículo e config do MōB Flash NÃO ficam presos nesse gate —
        // são formulários trabalhosos de preencher de novo, e restaurar eles
        // não tem por que depender do campo nome estar vazio ou não.
        if (cloudCfg && cloudCfg.custoVeiculo && cloudCfg.custoVeiculo.configurado &&
            (!localCfg.custoVeiculo || !localCfg.custoVeiculo.configurado)) {
          merged = merged || Object.assign({}, localCfg);
          merged.custoVeiculo = cloudCfg.custoVeiculo;
        }
        if (cloudCfg && cloudCfg.flashReader && !localCfg.flashReader) {
          merged = merged || Object.assign({}, localCfg);
          merged.flashReader = cloudCfg.flashReader;
        }
        if (merged) DB.saveCfg(merged);
      }

      // ── Favoritos ──────────────────────────────────────────────────────
      if (favsRes.data && favsRes.data.length) {
        const cloudFavs = favsRes.data[0].items || [];
        const localFavs = JSON.parse(localStorage.getItem('sm_inf_favs') || '[]');
        // Cloud tem prioridade se local estiver vazio
        if (cloudFavs.length > 0 && localFavs.length === 0) {
          localStorage.setItem('sm_inf_favs', JSON.stringify(cloudFavs));
          _infFavs = cloudFavs;
          infRenderFavs && infRenderFavs();
        }
      }

      if (planJ.data && planJ.data.length) {
        const local = DB.planJornada();
        planJ.data.forEach(p => {
          if (!local.find(x => x.id === p.id)) local.push({
            id: p.id, days: p.days || [],
            moneyGoal: p.money_goal != null ? parseFloat(p.money_goal) : null,
            kmGoal: p.km_goal != null ? parseFloat(p.km_goal) : null,
            schedules: p.schedules || []
          });
        });
        DB.savePlanJornada(local);
      }
      (cats.data || []).forEach(c => {
        if (!DB.get('sm_cats_' + c.cat_key)) DB.saveCats(c.cat_key, c.items || []);
        if (!DB.get('sm_obj_' + c.cat_key) && c.goals) DB.saveObjGoals(c.cat_key, c.goals);
      });
      // Depois de restaurar (inclusive custo/km e config do MōB Flash),
      // manda pro nativo — senão o leitor de tela só pega isso quando o
      // usuário abrir a tela de config manualmente.
      syncFlashConfigToNative();
    } catch (e) { console.error('SYNC.pullAll', e); }
  }
};

// ── Migração local → Supabase (roda 1x após atualização) ──────────────────
async function migrateLocalToCloud() {
  if (!_supaUser) return;
  if (localStorage.getItem('sm_cloud_migration_v2')) return; // já feito

  // BUG CONFIRMADO (13/07/2026): essa função disparava 3s depois de
  // showApp(), sem esperar o pullAll() da nuvem terminar. pullAll faz ~10
  // consultas em paralelo (inclusive trips paginado inteiro) — em conexão
  // mais lenta facilmente passa de 3s. Se a migração rodasse ANTES do pull
  // terminar, empurrava o local (ainda sem o que tinha vindo de uma
  // instalação anterior, ex: custo por km configurado antes) por cima do
  // que já estava salvo na nuvem — apagando dado bom. A trava acima
  // (sm_cloud_migration_v2) só evita rodar de novo NA MESMA instalação —
  // um reinstall zera o localStorage e essa trava junto, então a função
  // roda de novo correndo contra o pull toda vez que reinstala. Correção:
  // espera _bootPullDone antes de migrar qualquer coisa, mesmo mecanismo
  // que a tela de Custo do Veículo já usa pra não mostrar formulário vazio
  // por cima de dado que só ainda não chegou.
  // Espera bem mais que os 4s padrão do _waitBootPull(): essa migração roda
  // em background sem travar nenhuma tela, então não tem custo de UX em
  // esperar mais. O risco de esperar pouco é o contrário — subir pro
  // Supabase uma corrida local que a reconciliação do pullAll() ainda não
  // teve tempo de apagar (ex: já tinha sido excluída direto no banco),
  // ressuscitando dado errado. Confirmado acontecendo em 18/07/2026.
  await _waitBootPull(30000);

  console.log('[MōB] Iniciando migração local → Supabase...');
  let pushed = 0;

  // 1. Todos os dados de dias: trips, expenses, revenues
  for (const [key, raw] of Object.entries(localStorage)) {
    if (!key.startsWith('sm_day_')) continue;
    const date = key.replace('sm_day_', '');
    try {
      const day = JSON.parse(raw);
      for (const t of day.trips || []) {
        await SYNC.push('trips', { id: t.id, date, platform: t.platform||'uber',
          time: t.time||null, duration: t.duration||0, origin: t.origin||'',
          dest: t.dest||'', value: t.value||0, km: t.km||0,
          source: t.source||'manual', surge: t.surge||false, airport: t.airport||false,
          dinamico: t.dinamico||0, tip: t.tip||0 });
        pushed++;
      }
      for (const e of day.expenses || []) {
        await SYNC.push('expenses', { id: e.id, date: e.date||date,
          category: e.category||'', category_label: e.categoryLabel||'',
          description: e.desc||'', amount: e.amount||0, scope: e.scope||'operacional',
          recorrente: e.recorrente||false, freq: e.freq||null,
          status: e.status||'a_pagar', due_date: e.date||date });
        pushed++;
      }
      for (const r of day.revenues || []) {
        await SYNC.push('revenues', { id: r.id, date: r.date||date,
          category: r.category||'', category_label: r.category
===== sessions @ 1034926 =====
d: r.series_id || null,
          status: r.status || 'pago', dueDate: r.due_date || r.date
        }); }
      });
      (sessions.data || []).forEach(s => {
        const day = ensure(s.date);
        if (!day.sessions) day.sessions = [];
        // Ignorar sessões deletadas (end_ms=-1) e elapsed absurdo (> 20h)
        const sElapsed = s.elapsed_secs || 0;
        const sEnd = s.end_ms ? Number(s.end_ms) : null;
        const isDeleted = sEnd === -1;
        const isBogus = sElapsed > 72000; // > 20h = dado corrompido
        if (!isDeleted && !isBogus && !day.sessions.find(x => x.id === s.id)) {
          _dirty.add(s.date);
          day.sessions.push({
            id: s.id, start: Number(s.start_ms), end: sEnd,
            km: parseFloat(s.km) || 0, elapsed: sElapsed
          });
        }
      });
      // ── Restaurar / limpar sessões abertas no Supabase ─────────────
      if (S.gps.st === 'idle' && !DB.get('sm_gps_session')) {
        const hoje = DB.today();
        const closedSet = _gcss();
        try{const _o=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');if(_o&&_o.id)closedSet.add(_o.id);}catch(e){}

        // 1. Auto-fechar sessões de dias anteriores (nunca devem ficar abertas)
        try {
          (sessions.data||[]).filter(s=>!s.end_ms&&s.date!==hoje).forEach(s=>{
            const est=Number(s.start_ms)+(s.elapsed_secs||0)*1000||Date.now();
            _SUPA.from('sessions').upsert({id:s.id,user_id:uid,end_ms:est,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Auto-fechando sessão dia anterior:',s.id,s.date);
          });
        } catch(e) { console.warn('[pullAll] auto-close error:',e); }

        // 2. Sessões abertas de HOJE — mais recente primeiro
        const abertas=(sessions.data||[]).filter(s=>!s.end_ms&&s.date===hoje)
          .sort((a,b)=>Number(b.start_ms)-Number(a.start_ms));

        for(let i=0;i<abertas.length;i++){
          const ativa=abertas[i];
          const ageMs=Date.now()-Number(ativa.start_ms);
          // Cross-check com localStorage: sessão já foi encerrada localmente?
          const localDay=DB.day(hoje);
          const localSess=(localDay.sessions||[]).find(ls=>ls.id===ativa.id);
          const isClosedLocally=(localSess&&localSess.end)||closedSet.has(ativa.id);
          const isTooOld=ageMs>16*3600*1000;
          const isExtra=i>0; // só a mais recente pode ser restaurada

          if(isClosedLocally||isTooOld||isExtra){
            const bestEnd=(localSess&&localSess.end)||(()=>{
              const c=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');
              return(c&&c.id===ativa.id)?c.end:(Number(ativa.start_ms)+(ativa.elapsed_secs||0)*1000||Date.now());
            })();
            _SUPA.from('sessions').upsert({id:ativa.id,user_id:uid,end_ms:bestEnd,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Sessão encerrada — não restaurar:',ativa.id,{isClosedLocally,isTooOld,isExtra});
            continue;
          }
          // Sessão genuinamente ativa — restaurar
          DB.set('sm_gps_session',{st:'running',start:Number(ativa.start_ms),pausedMs:0,pa:null,
            km:parseFloat(ativa.km)||0,baseKm:0,baseElapsed:0,lastPt:null,sessionId:ativa.id});
          console.log('[pullAll] Sessão restaurada:',ativa.id);
          break;
        }
      }
      (dayMeta.data || []).forEach(m => {
        const day = ensure(m.date);
        const nKm = parseFloat(m.km_adjust) || 0;
        const nTempo = parseInt(m.tempo_adjust_secs) || 0;
        if ((day.kmAdjust||0) !== nKm || (day.tempoAdjust||0) !== nTempo) _dirty.add(m.date);
        day.kmAdjust = nKm;
        day.tempoAdjust = nTempo;
        if (m.km_total_legacy != null && day.kmTotal !== parseFloat(m.km_total_legacy)) {
          day.kmTotal = parseFloat(m.km_total_legacy); _dirty.add(m.date);
        }
      });
      // Só reescreve no localStorage os dias que realmente mudaram
      Object.entries(byDate).forEach(([d, day]) => { if (_dirty.has(d)) DB.saveDay(day); });

      // ── Reconciliação automática ─────────────────────────────────────
      // O merge acima só ADICIONA o que a nuvem tem e falta local — nunca
      // remove local o que sumiu da nuvem (ex: registro apagado direto no
      // banco). Sem isso, um item apagado assim "ressuscita" pra sempre no
      // aparelho, mesmo depois de reabrir o app. Aqui completamos o ciclo:
      // qualquer trip/despesa/receita/sessão que já foi confirmada
      // sincronizada antes e não existe mais na nuvem agora, sai do
      // localStorage também. A fila de pendências (DB.syncQueue()) é a
      // trava de segurança: nada que ainda esteja esperando pra subir é
      // tocado, mesmo que ainda não apareça na nuvem — só mexe no que já
      // tinha certeza absoluta que foi sincronizado uma vez.
      try {
        const pendingIds = { trips: new Set(), expenses: new Set(), revenues: new Set(), sessions: new Set() };
        (DB.syncQueue() || []).forEach(op => {
          if (op.type === 'push' && op.row && op.row.id && pendingIds[op.table]) pendingIds[op.table].add(op.row.id);
        });
        const cloudIds = {
          trips: new Set((trips.data || []).map(t => t.id)),
          expenses: new Set((expenses.data || []).map(e => e.id)),
          revenues: new Set((revenues.data || []).map(r => r.id)),
          sessions: new Set((sessions.data || []).map(s => s.id)),
        };
        (DB.allDates() || []).forEach(d => {
          const day = DB.day(d);
          let changed = false;
          ['trips', 'expenses', 'revenues', 'sessions'].forEach(key => {
            if (!day[key] || !day[key].length) return;
            // item sem id = registro antigo/legado, nunca mexe nele
            const kept = day[key].filter(item => !item.id || cloudIds[key].has(item.id) || pendingIds[key].has(item.id));
            if (kept.length !== day[key].length) { day[key] = kept; changed = true; }
          });
          if (changed) DB.saveDay(day);
        });
      } catch (e) { console.warn('[pullAll] reconciliação falhou:', e); }

      // ── Events ─────────────────────────────────────────────────────────
      if (eventsRes.data && eventsRes.data.length) {
        const localEvs = JSON.parse(localStorage.getItem('sm_events') || '[]');
        let changed = false;
        eventsRes.data.forEach(ev => {
          if (!localEvs.find(x => x.id === ev.id)) {
            localEvs.push({ id: ev.id, title: ev.title, date: ev.date,
              timeStart: ev.time_start, timeEnd: ev.time_end,
              type: ev.type || 'operacional', color: ev.color || '#22C55E',
              notes: ev.notes || '', paid: ev.paid || false });
            changed = true;
          }
        });
        if (changed) localStorage.setItem('sm_events', JSON.stringify(localEvs));
      }

      // ── Settings ───────────────────────────────────────────────────────
      if (settingsRes.data && settingsRes.data.length && settingsRes.data[0].data) {
        const cloudCfg = settingsRes.data[0].data;
        const localCfg = DB.cfg() || {};
        let merged = null;
        // Cadastro geral (nome/placa/veículo/meta): só sobrescreve se o local
        // ainda estiver vazio — evita apagar o que já foi preenchido nesse aparelho.
        if (cloudCfg && Object.keys(cloudCfg).length > 0 && (!localCfg.name || localCfg.name === '')) {
          merged = Object.assign({}, localCfg, cloudCfg);
        }
        // Custo do veículo e config do MōB Flash NÃO ficam presos nesse gate —
        // são formulários trabalhosos de preencher de novo, e restaurar eles
        // não tem por que depender do campo nome estar vazio ou não.
        if (cloudCfg && cloudCfg.custoVeiculo && cloudCfg.custoVeiculo.configurado &&
            (!localCfg.custoVeiculo || !localCfg.custoVeiculo.configurado)) {
          merged = merged || Object.assign({}, localCfg);
          merged.custoVeiculo = cloudCfg.custoVeiculo;
        }
        if (cloudCfg && cloudCfg.flashReader && !localCfg.flashReader) {
          merged = merged || Object.assign({}, localCfg);
          merged.flashReader = cloudCfg.flashReader;
        }
        if (merged) DB.saveCfg(merged);
      }

      // ── Favoritos ──────────────────────────────────────────────────────
      if (favsRes.data && favsRes.data.length) {
        const cloudFavs = favsRes.data[0].items || [];
        const localFavs = JSON.parse(localStorage.getItem('sm_inf_favs') || '[]');
        // Cloud tem prioridade se local estiver vazio
        if (cloudFavs.length > 0 && localFavs.length === 0) {
          localStorage.setItem('sm_inf_favs', JSON.stringify(cloudFavs));
          _infFavs = cloudFavs;
          infRenderFavs && infRenderFavs();
        }
      }

      if (planJ.data && planJ.data.length) {
        const local = DB.planJornada();
        planJ.data.forEach(p => {
          if (!local.find(x => x.id === p.id)) local.push({
            id: p.id, days: p.days || [],
            moneyGoal: p.money_goal != null ? parseFloat(p.money_goal) : null,
            kmGoal: p.km_goal != null ? parseFloat(p.km_goal) : null,
            schedules: p.schedules || []
          });
        });
        DB.savePlanJornada(local);
      }
      (cats.data || []).forEach(c => {
        if (!DB.get('sm_cats_' + c.cat_key)) DB.saveCats(c.cat_key, c.items || []);
        if (!DB.get('sm_obj_' + c.cat_key) && c.goals) DB.saveObjGoals(c.cat_key, c.goals);
      });
      // Depois de restaurar (inclusive custo/km e config do MōB Flash),
      // manda pro nativo — senão o leitor de tela só pega isso quando o
      // usuário abrir a tela de config manualmente.
      syncFlashConfigToNative();
    } catch (e) { console.error('SYNC.pullAll', e); }
  }
};

// ── Migração local → Supabase (roda 1x após atualização) ──────────────────
async function migrateLocalToCloud() {
  if (!_supaUser) return;
  if (localStorage.getItem('sm_cloud_migration_v2')) return; // já feito

  // BUG CONFIRMADO (13/07/2026): essa função disparava 3s depois de
  // showApp(), sem esperar o pullAll() da nuvem terminar. pullAll faz ~10
  // consultas em paralelo (inclusive trips paginado inteiro) — em conexão
  // mais lenta facilmente passa de 3s. Se a migração rodasse ANTES do pull
  // terminar, empurrava o local (ainda sem o que tinha vindo de uma
  // instalação anterior, ex: custo por km configurado antes) por cima do
  // que já estava salvo na nuvem — apagando dado bom. A trava acima
  // (sm_cloud_migration_v2) só evita rodar de novo NA MESMA instalação —
  // um reinstall zera o localStorage e essa trava junto, então a função
  // roda de novo correndo contra o pull toda vez que reinstala. Correção:
  // espera _bootPullDone antes de migrar qualquer coisa, mesmo mecanismo
  // que a tela de Custo do Veículo já usa pra não mostrar formulário vazio
  // por cima de dado que só ainda não chegou.
  // Espera bem mais que os 4s padrão do _waitBootPull(): essa migração roda
  // em background sem travar nenhuma tela, então não tem custo de UX em
  // esperar mais. O risco de esperar pouco é o contrário — subir pro
  // Supabase uma corrida local que a reconciliação do pullAll() ainda não
  // teve tempo de apagar (ex: já tinha sido excluída direto no banco),
  // ressuscitando dado errado. Confirmado acontecendo em 18/07/2026.
  await _waitBootPull(30000);

  console.log('[MōB] Iniciando migração local → Supabase...');
  let pushed = 0;

  // 1. Todos os dados de dias: trips, expenses, revenues
  for (const [key, raw] of Object.entries(localStorage)) {
    if (!key.startsWith('sm_day_')) continue;
    const date = key.replace('sm_day_', '');
    try {
      const day = JSON.parse(raw);
      for (const t of day.trips || []) {
        await SYNC.push('trips', { id: t.id, date, platform: t.platform||'uber',
          time: t.time||null, duration: t.duration||0, origin: t.origin||'',
          dest: t.dest||'', value: t.value||0, km: t.km||0,
          source: t.source||'manual', surge: t.surge||false, airport: t.airport||false,
          dinamico: t.dinamico||0, tip: t.tip||0 });
        pushed++;
      }
      for (const e of day.expenses || []) {
        await SYNC.push('expenses', { id: e.id, date: e.date||date,
          category: e.category||'', category_label: e.categoryLabel||'',
          description: e.desc||'', amount: e.amount||0, scope: e.scope||'operacional',
          recorrente: e.recorrente||false, freq: e.freq||null,
          status: e.status||'a_pagar', due_date: e.date||date });
        pushed++;
      }
      for (const r of day.revenues || []) {
        await SYNC.push('revenues', { id: r.id, date: r.date||date,
          category: r.category||'', category_label: r.categoryLabel||'',
          description: r.desc||'', amount: r.amount||0, scope: r.scope||'operacional',
          recorrente: r.recorrente||false, freq: r.freq||null,
          status: r.status||'pago', due_date: r.date||date });
        pushed++;
      }
    } catch(e) { console.warn('[MōB] migração dia', date, e); }
  }

  // 2. Favoritos
  await SYNC.pushFavs(); pushed++;

  // 3. Configurações
  await SYNC.pushSettings(); pushed++;

  // 4. Categorias
  try {
    for (const key2 of Object.keys(localStorage)) {
      if (key2.startsWith('sm_cats_')) await SYNC.pushCats(key2.replace('sm_cats_',''));
    }
  } catch(e) {}

  // 5. Eventos
  const localEvs = JSON.parse(localStorage.getItem('sm_events')||'[]');
  for (const ev of localEvs) {
    await SYNC.push('events', { id: ev.id, title: ev.title, date: ev.date,
      time_start: ev.timeStart||null, time_end: ev.timeEnd||null,
      type: ev.type||'operacional', color: ev.color||'#22C55E',
      notes: ev.notes||'', paid: ev.paid||false });
    pushed++;
  }

  localStorage.setItem('sm_cloud_migration_v2', '1');
  console.log('[MōB] Migração concluída —', pushed, 'registros enviados ao Supabase');
  showToast('Dados sincronizados com a nuvem ✓');
}

async function authInit() {
  ['app-nav','app-header'].forEach(id => { const el=document.getElementById(id);if(el)el.style.display='none'; });
  document.querySelectorAll('.screen').forEach(s => s.style.display='none');
  await new Promise(r => setTimeout(r, 300));
  const { data:{ session } } = await _SUPA.auth.getSession();
  if (session?.user) { _supaUser=session.user; await loadProfile(); showApp(); }
  else showLogin();
  _SUPA.auth.onAuthStateChange(async (ev, session) => {
    if ((ev==='SIGNED_IN' || ev==='TOKEN_REFRESHED') && session?.user) {
      _supaUser = session.user;
      // Salva tokens no storage nativo (persiste mesmo com app morto)
      if (window.SmartMobiNative?.saveSession) {
        try { SmartMobiNative.saveSession(JSON.stringify({
          access_token: session.access_token,
          refresh_token: session.refresh_token
        })); } catch(e) {}
      }
      // Salva credenciais para o GpsService usar nas notificações de reporte
      // r
===== sessions @ 1035358 =====
 isDeleted = sEnd === -1;
        const isBogus = sElapsed > 72000; // > 20h = dado corrompido
        if (!isDeleted && !isBogus && !day.sessions.find(x => x.id === s.id)) {
          _dirty.add(s.date);
          day.sessions.push({
            id: s.id, start: Number(s.start_ms), end: sEnd,
            km: parseFloat(s.km) || 0, elapsed: sElapsed
          });
        }
      });
      // ── Restaurar / limpar sessões abertas no Supabase ─────────────
      if (S.gps.st === 'idle' && !DB.get('sm_gps_session')) {
        const hoje = DB.today();
        const closedSet = _gcss();
        try{const _o=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');if(_o&&_o.id)closedSet.add(_o.id);}catch(e){}

        // 1. Auto-fechar sessões de dias anteriores (nunca devem ficar abertas)
        try {
          (sessions.data||[]).filter(s=>!s.end_ms&&s.date!==hoje).forEach(s=>{
            const est=Number(s.start_ms)+(s.elapsed_secs||0)*1000||Date.now();
            _SUPA.from('sessions').upsert({id:s.id,user_id:uid,end_ms:est,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Auto-fechando sessão dia anterior:',s.id,s.date);
          });
        } catch(e) { console.warn('[pullAll] auto-close error:',e); }

        // 2. Sessões abertas de HOJE — mais recente primeiro
        const abertas=(sessions.data||[]).filter(s=>!s.end_ms&&s.date===hoje)
          .sort((a,b)=>Number(b.start_ms)-Number(a.start_ms));

        for(let i=0;i<abertas.length;i++){
          const ativa=abertas[i];
          const ageMs=Date.now()-Number(ativa.start_ms);
          // Cross-check com localStorage: sessão já foi encerrada localmente?
          const localDay=DB.day(hoje);
          const localSess=(localDay.sessions||[]).find(ls=>ls.id===ativa.id);
          const isClosedLocally=(localSess&&localSess.end)||closedSet.has(ativa.id);
          const isTooOld=ageMs>16*3600*1000;
          const isExtra=i>0; // só a mais recente pode ser restaurada

          if(isClosedLocally||isTooOld||isExtra){
            const bestEnd=(localSess&&localSess.end)||(()=>{
              const c=JSON.parse(localStorage.getItem('sm_gps_last_closed')||'null');
              return(c&&c.id===ativa.id)?c.end:(Number(ativa.start_ms)+(ativa.elapsed_secs||0)*1000||Date.now());
            })();
            _SUPA.from('sessions').upsert({id:ativa.id,user_id:uid,end_ms:bestEnd,is_paused:false,paused_ms:0},{onConflict:'id'}).catch(()=>{});
            console.log('[pullAll] Sessão encerrada — não restaurar:',ativa.id,{isClosedLocally,isTooOld,isExtra});
            continue;
          }
          // Sessão genuinamente ativa — restaurar
          DB.set('sm_gps_session',{st:'running',start:Number(ativa.start_ms),pausedMs:0,pa:null,
            km:parseFloat(ativa.km)||0,baseKm:0,baseElapsed:0,lastPt:null,sessionId:ativa.id});
          console.log('[pullAll] Sessão restaurada:',ativa.id);
          break;
        }
      }
      (dayMeta.data || []).forEach(m => {
        const day = ensure(m.date);
        const nKm = parseFloat(m.km_adjust) || 0;
        const nTempo = parseInt(m.tempo_adjust_secs) || 0;
        if ((day.kmAdjust||0) !== nKm || (day.tempoAdjust||0) !== nTempo) _dirty.add(m.date);
        day.kmAdjust = nKm;
        day.tempoAdjust = nTempo;
        if (m.km_total_legacy != null && day.kmTotal !== parseFloat(m.km_total_legacy)) {
          day.kmTotal = parseFloat(m.km_total_legacy); _dirty.add(m.date);
        }
      });
      // Só reescreve no localStorage os dias que realmente mudaram
      Object.entries(byDate).forEach(([d, day]) => { if (_dirty.has(d)) DB.saveDay(day); });

      // ── Reconciliação automática ─────────────────────────────────────
      // O merge acima só ADICIONA o que a nuvem tem e falta local — nunca
      // remove local o que sumiu da nuvem (ex: registro apagado direto no
      // banco). Sem isso, um item apagado assim "ressuscita" pra sempre no
      // aparelho, mesmo depois de reabrir o app. Aqui completamos o ciclo:
      // qualquer trip/despesa/receita/sessão que já foi confirmada
      // sincronizada antes e não existe mais na nuvem agora, sai do
      // localStorage também. A fila de pendências (DB.syncQueue()) é a
      // trava de segurança: nada que ainda esteja esperando pra subir é
      // tocado, mesmo que ainda não apareça na nuvem — só mexe no que já
      // tinha certeza absoluta que foi sincronizado uma vez.
      try {
        const pendingIds = { trips: new Set(), expenses: new Set(), revenues: new Set(), sessions: new Set() };
        (DB.syncQueue() || []).forEach(op => {
          if (op.type === 'push' && op.row && op.row.id && pendingIds[op.table]) pendingIds[op.table].add(op.row.id);
        });
        const cloudIds = {
          trips: new Set((trips.data || []).map(t => t.id)),
          expenses: new Set((expenses.data || []).map(e => e.id)),
          revenues: new Set((revenues.data || []).map(r => r.id)),
          sessions: new Set((sessions.data || []).map(s => s.id)),
        };
        (DB.allDates() || []).forEach(d => {
          const day = DB.day(d);
          let changed = false;
          ['trips', 'expenses', 'revenues', 'sessions'].forEach(key => {
            if (!day[key] || !day[key].length) return;
            // item sem id = registro antigo/legado, nunca mexe nele
            const kept = day[key].filter(item => !item.id || cloudIds[key].has(item.id) || pendingIds[key].has(item.id));
            if (kept.length !== day[key].length) { day[key] = kept; changed = true; }
          });
          if (changed) DB.saveDay(day);
        });
      } catch (e) { console.warn('[pullAll] reconciliação falhou:', e); }

      // ── Events ─────────────────────────────────────────────────────────
      if (eventsRes.data && eventsRes.data.length) {
        const localEvs = JSON.parse(localStorage.getItem('sm_events') || '[]');
        let changed = false;
        eventsRes.data.forEach(ev => {
          if (!localEvs.find(x => x.id === ev.id)) {
            localEvs.push({ id: ev.id, title: ev.title, date: ev.date,
              timeStart: ev.time_start, timeEnd: ev.time_end,
              type: ev.type || 'operacional', color: ev.color || '#22C55E',
              notes: ev.notes || '', paid: ev.paid || false });
            changed = true;
          }
        });
        if (changed) localStorage.setItem('sm_events', JSON.stringify(localEvs));
      }

      // ── Settings ───────────────────────────────────────────────────────
      if (settingsRes.data && settingsRes.data.length && settingsRes.data[0].data) {
        const cloudCfg = settingsRes.data[0].data;
        const localCfg = DB.cfg() || {};
        let merged = null;
        // Cadastro geral (nome/placa/veículo/meta): só sobrescreve se o local
        // ainda estiver vazio — evita apagar o que já foi preenchido nesse aparelho.
        if (cloudCfg && Object.keys(cloudCfg).length > 0 && (!localCfg.name || localCfg.name === '')) {
          merged = Object.assign({}, localCfg, cloudCfg);
        }
        // Custo do veículo e config do MōB Flash NÃO ficam presos nesse gate —
        // são formulários trabalhosos de preencher de novo, e restaurar eles
        // não tem por que depender do campo nome estar vazio ou não.
        if (cloudCfg && cloudCfg.custoVeiculo && cloudCfg.custoVeiculo.configurado &&
            (!localCfg.custoVeiculo || !localCfg.custoVeiculo.configurado)) {
          merged = merged || Object.assign({}, localCfg);
          merged.custoVeiculo = cloudCfg.custoVeiculo;
        }
        if (cloudCfg && cloudCfg.flashReader && !localCfg.flashReader) {
          merged = merged || Object.assign({}, localCfg);
          merged.flashReader = cloudCfg.flashReader;
        }
        if (merged) DB.saveCfg(merged);
      }

      // ── Favoritos ──────────────────────────────────────────────────────
      if (favsRes.data && favsRes.data.length) {
        const cloudFavs = favsRes.data[0].items || [];
        const localFavs = JSON.parse(localStorage.getItem('sm_inf_favs') || '[]');
        // Cloud tem prioridade se local estiver vazio
        if (cloudFavs.length > 0 && localFavs.length === 0) {
          localStorage.setItem('sm_inf_favs', JSON.stringify(cloudFavs));
          _infFavs = cloudFavs;
          infRenderFavs && infRenderFavs();
        }
      }

      if (planJ.data && planJ.data.length) {
        const local = DB.planJornada();
        planJ.data.forEach(p => {
          if (!local.find(x => x.id === p.id)) local.push({
            id: p.id, days: p.days || [],
            moneyGoal: p.money_goal != null ? parseFloat(p.money_goal) : null,
            kmGoal: p.km_goal != null ? parseFloat(p.km_goal) : null,
            schedules: p.schedules || []
          });
        });
        DB.savePlanJornada(local);
      }
      (cats.data || []).forEach(c => {
        if (!DB.get('sm_cats_' + c.cat_key)) DB.saveCats(c.cat_key, c.items || []);
        if (!DB.get('sm_obj_' + c.cat_key) && c.goals) DB.saveObjGoals(c.cat_key, c.goals);
      });
      // Depois de restaurar (inclusive custo/km e config do MōB Flash),
      // manda pro nativo — senão o leitor de tela só pega isso quando o
      // usuário abrir a tela de config manualmente.
      syncFlashConfigToNative();
    } catch (e) { console.error('SYNC.pullAll', e); }
  }
};

// ── Migração local → Supabase (roda 1x após atualização) ──────────────────
async function migrateLocalToCloud() {
  if (!_supaUser) return;
  if (localStorage.getItem('sm_cloud_migration_v2')) return; // já feito

  // BUG CONFIRMADO (13/07/2026): essa função disparava 3s depois de
  // showApp(), sem esperar o pullAll() da nuvem terminar. pullAll faz ~10
  // consultas em paralelo (inclusive trips paginado inteiro) — em conexão
  // mais lenta facilmente passa de 3s. Se a migração rodasse ANTES do pull
  // terminar, empurrava o local (ainda sem o que tinha vindo de uma
  // instalação anterior, ex: custo por km configurado antes) por cima do
  // que já estava salvo na nuvem — apagando dado bom. A trava acima
  // (sm_cloud_migration_v2) só evita rodar de novo NA MESMA instalação —
  // um reinstall zera o localStorage e essa trava junto, então a função
  // roda de novo correndo contra o pull toda vez que reinstala. Correção:
  // espera _bootPullDone antes de migrar qualquer coisa, mesmo mecanismo
  // que a tela de Custo do Veículo já usa pra não mostrar formulário vazio
  // por cima de dado que só ainda não chegou.
  // Espera bem mais que os 4s padrão do _waitBootPull(): essa migração roda
  // em background sem travar nenhuma tela, então não tem custo de UX em
  // esperar mais. O risco de esperar pouco é o contrário — subir pro
  // Supabase uma corrida local que a reconciliação do pullAll() ainda não
  // teve tempo de apagar (ex: já tinha sido excluída direto no banco),
  // ressuscitando dado errado. Confirmado acontecendo em 18/07/2026.
  await _waitBootPull(30000);

  console.log('[MōB] Iniciando migração local → Supabase...');
  let pushed = 0;

  // 1. Todos os dados de dias: trips, expenses, revenues
  for (const [key, raw] of Object.entries(localStorage)) {
    if (!key.startsWith('sm_day_')) continue;
    const date = key.replace('sm_day_', '');
    try {
      const day = JSON.parse(raw);
      for (const t of day.trips || []) {
        await SYNC.push('trips', { id: t.id, date, platform: t.platform||'uber',
          time: t.time||null, duration: t.duration||0, origin: t.origin||'',
          dest: t.dest||'', value: t.value||0, km: t.km||0,
          source: t.source||'manual', surge: t.surge||false, airport: t.airport||false,
          dinamico: t.dinamico||0, tip: t.tip||0 });
        pushed++;
      }
      for (const e of day.expenses || []) {
        await SYNC.push('expenses', { id: e.id, date: e.date||date,
          category: e.category||'', category_label: e.categoryLabel||'',
          description: e.desc||'', amount: e.amount||0, scope: e.scope||'operacional',
          recorrente: e.recorrente||false, freq: e.freq||null,
          status: e.status||'a_pagar', due_date: e.date||date });
        pushed++;
      }
      for (const r of day.revenues || []) {
        await SYNC.push('revenues', { id: r.id, date: r.date||date,
          category: r.category||'', category_label: r.categoryLabel||'',
          description: r.desc||'', amount: r.amount||0, scope: r.scope||'operacional',
          recorrente: r.recorrente||false, freq: r.freq||null,
          status: r.status||'pago', due_date: r.date||date });
        pushed++;
      }
    } catch(e) { console.warn('[MōB] migração dia', date, e); }
  }

  // 2. Favoritos
  await SYNC.pushFavs(); pushed++;

  // 3. Configurações
  await SYNC.pushSettings(); pushed++;

  // 4. Categorias
  try {
    for (const key2 of Object.keys(localStorage)) {
      if (key2.startsWith('sm_cats_')) await SYNC.pushCats(key2.replace('sm_cats_',''));
    }
  } catch(e) {}

  // 5. Eventos
  const localEvs = JSON.parse(localStorage.getItem('sm_events')||'[]');
  for (const ev of localEvs) {
    await SYNC.push('events', { id: ev.id, title: ev.title, date: ev.date,
      time_start: ev.timeStart||null, time_end: ev.timeEnd||null,
      type: ev.type||'operacional', color: ev.color||'#22C55E',
      notes: ev.notes||'', paid: ev.paid||false });
    pushed++;
  }

  localStorage.setItem('sm_cloud_migration_v2', '1');
  console.log('[MōB] Migração concluída —', pushed, 'registros enviados ao Supabase');
  showToast('Dados sincronizados com a nuvem ✓');
}

async function authInit() {
  ['app-nav','app-header'].forEach(id => { const el=document.getElementById(id);if(el)el.style.display='none'; });
  document.querySelectorAll('.screen').forEach(s => s.style.display='none');
  await new Promise(r => setTimeout(r, 300));
  const { data:{ session } } = await _SUPA.auth.getSession();
  if (session?.user) { _supaUser=session.user; await loadProfile(); showApp(); }
  else showLogin();
  _SUPA.auth.onAuthStateChange(async (ev, session) => {
    if ((ev==='SIGNED_IN' || ev==='TOKEN_REFRESHED') && session?.user) {
      _supaUser = session.user;
      // Salva tokens no storage nativo (persiste mesmo com app morto)
      if (window.SmartMobiNative?.saveSession) {
        try { SmartMobiNative.saveSession(JSON.stringify({
          access_token: session.access_token,
          refresh_token: session.refresh_token
        })); } catch(e) {}
      }
      // Salva credenciais para o GpsService usar nas notificações de reporte
      // rápido e no alerta automático de dinâmico do feed. Reenviado também
      // a cada TOKEN_REFRESHED (autoRefreshToken troca o token ~1x/hora) —
      // antes só rodava no SIGNED_IN, então esses recursos paravam de
      // funcionar (silenciosamente) depois de ~1h de app aberto.
      if (window.SmartMobiNative?.saveUserToken) {
        try { SmartMobiNative.saveUserToken(session.user.id, session.access_token); } catch(e) {}
   
===== sessions @ 1047860 =====
false, freq: r.freq||null,
          status: r.status||'pago', due_date: r.date||date });
        pushed++;
      }
    } catch(e) { console.warn('[MōB] migração dia', date, e); }
  }

  // 2. Favoritos
  await SYNC.pushFavs(); pushed++;

  // 3. Configurações
  await SYNC.pushSettings(); pushed++;

  // 4. Categorias
  try {
    for (const key2 of Object.keys(localStorage)) {
      if (key2.startsWith('sm_cats_')) await SYNC.pushCats(key2.replace('sm_cats_',''));
    }
  } catch(e) {}

  // 5. Eventos
  const localEvs = JSON.parse(localStorage.getItem('sm_events')||'[]');
  for (const ev of localEvs) {
    await SYNC.push('events', { id: ev.id, title: ev.title, date: ev.date,
      time_start: ev.timeStart||null, time_end: ev.timeEnd||null,
      type: ev.type||'operacional', color: ev.color||'#22C55E',
      notes: ev.notes||'', paid: ev.paid||false });
    pushed++;
  }

  localStorage.setItem('sm_cloud_migration_v2', '1');
  console.log('[MōB] Migração concluída —', pushed, 'registros enviados ao Supabase');
  showToast('Dados sincronizados com a nuvem ✓');
}

async function authInit() {
  ['app-nav','app-header'].forEach(id => { const el=document.getElementById(id);if(el)el.style.display='none'; });
  document.querySelectorAll('.screen').forEach(s => s.style.display='none');
  await new Promise(r => setTimeout(r, 300));
  const { data:{ session } } = await _SUPA.auth.getSession();
  if (session?.user) { _supaUser=session.user; await loadProfile(); showApp(); }
  else showLogin();
  _SUPA.auth.onAuthStateChange(async (ev, session) => {
    if ((ev==='SIGNED_IN' || ev==='TOKEN_REFRESHED') && session?.user) {
      _supaUser = session.user;
      // Salva tokens no storage nativo (persiste mesmo com app morto)
      if (window.SmartMobiNative?.saveSession) {
        try { SmartMobiNative.saveSession(JSON.stringify({
          access_token: session.access_token,
          refresh_token: session.refresh_token
        })); } catch(e) {}
      }
      // Salva credenciais para o GpsService usar nas notificações de reporte
      // rápido e no alerta automático de dinâmico do feed. Reenviado também
      // a cada TOKEN_REFRESHED (autoRefreshToken troca o token ~1x/hora) —
      // antes só rodava no SIGNED_IN, então esses recursos paravam de
      // funcionar (silenciosamente) depois de ~1h de app aberto.
      if (window.SmartMobiNative?.saveUserToken) {
        try { SmartMobiNative.saveUserToken(session.user.id, session.access_token); } catch(e) {}
      }
      if (ev==='SIGNED_IN') { await loadProfile(); showApp(); }
    } else if (ev==='SIGNED_OUT') {
      _supaUser = null;
      if (window.SmartMobiNative?.saveSession) {
        try { SmartMobiNative.saveSession(''); } catch(e) {}
      }
      showLogin();
    }
  });
}

function lsShow(id) {
  ['lsLogin','lsReg','lsForgot'].forEach(x => { const el=document.getElementById(x);if(el)el.style.display=x===id?'block':'none'; });
}
function showLogin() {
  const el=document.getElementById('sc-login'); if(el)el.style.display='flex';
  ['app-nav','app-header'].forEach(id=>{ const e=document.getElementById(id);if(e)e.style.display='none'; });
  document.querySelectorAll('.screen').forEach(s=>s.style.display='none');
}
let _appBooted = false;
async function showApp() {
  const el=document.getElementById('sc-login'); if(el)el.style.display='none';
  const nav=document.getElementById('app-nav'); if(nav)nav.style.display='flex';
  if(_supaUser && !DB.cfg().name && _supaUser.email){
    const cfg=DB.cfg(); cfg.name=_supaUser.email.split('@')[0]; DB.saveCfg(cfg);
  }
  // Migração única de dados locais para o Supabase
  setTimeout(() => migrateLocalToCloud().catch(()=>{}), 3000);
  setTimeout(() => reconcileAllExistingDays().catch(()=>{}), 5000);
  // Limpeza única: remove corridas source='import_uber_zip' do localStorage
  // antes de reimportar com os fixes de UTC e promocao fundida no dinamico.
  // Roda só uma vez (flag sm_zip_wipe_v1). Não afeta uber_import nem import_ai.
  if (!localStorage.getItem('sm_zip_wipe_v1')) {
    let removidas = 0;
    DB.allDates().forEach(d => {
      const day = DB.day(d);
      const antes = (day.trips||[]).length;
      day.trips = (day.trips||[]).filter(t => t.source !== 'import_uber_zip');
      if (day.trips.length !== antes) { removidas += antes - day.trips.length; DB.saveDay(day); }
    });
    localStorage.setItem('sm_zip_wipe_v1', '1');
    if (removidas > 0) console.log('[boot] zip wipe: removidas', removidas, 'corridas do localStorage');
  }
  if (!_appBooted) {
    setTimeout(updateNetStatus, 500);
  }
  try{renderHome();}    catch(e){console.error('renderHome:',e);}
  try{restoreGpsSession();} catch(e){console.error('restoreGpsSession:',e);}
  try{renderJornada();}  catch(e){console.error('renderJornada:',e);}
  // Verifica jornadas órfãs (de outra aba/instância que não fechou corretamente)
  // toda vez que o app abre, não só ao navegar manualmente pra Jornada
  if (_supaUser && navigator.onLine) {
    _closeOrphanSessions().then(n => { if (n) { renderJornada(); renderHome(); } }).catch(()=>{});
  }
  try{syncNavButton();}  catch(e){console.error('syncNavButton:',e);}
  try{renderFin();}      catch(e){console.error('renderFin:',e);}
  try{updateFavBtn();}   catch(e){console.error('updateFavBtn:',e);}
  try{renderAgenda();} catch(e){console.error('renderAgenda:',e);}
  // Só navega pra Home no primeiro boot. O Supabase reemite SIGNED_IN sempre que
  // o app volta de segundo plano, o que chamava showApp() de novo e forçava a
  // tela de volta pra Home mesmo a pessoa estando em outra tela (ex: Jornada).
  if (!_appBooted) {
    _appBooted = true;
    // FIX: restaura a última tela em vez de forçar Home quando a WebView
    // recarrega (Android mata a página em background)
    let lastSec = 'home';
    try {
      const saved = JSON.parse(localStorage.getItem('sm_last_screen') || 'null');
      // Só restaura se foi salvo há menos de 30 min (senão, home mesmo)
      if (saved && saved.sec && (Date.now() - saved.ts) < 30*60*1000) lastSec = saved.sec;
    } catch(e) {}
    navTo(lastSec);
    _bootPullBg();
  }
}

// Promessa que resolve quando a restauração da nuvem (pullAll) termina no
// boot. Telas como Custo do Veículo e MōB Flash usam isso pra não mostrar
// formulário vazio e deixar o usuário preencher de novo por cima de dados
// que já estavam guardados, só porque a restauração ainda não tinha chegado.
let _bootPullResolve;
let _bootPullDone = false;
const _bootPullPromise = new Promise(r => { _bootPullResolve = r; });

// Depois de _bootPullPromise pronta: espera ela terminar (com timeout, pra
// não travar pra sempre se estiver offline) antes de liberar uma tela.
async function _waitBootPull(maxMs = 4000) {
  try { await Promise.race([_bootPullPromise, new Promise(r => setTimeout(r, maxMs))]); } catch (e) {}
}

// Pull do cloud em BACKGROUND — a UI já está visível com dados locais.
// Quando terminar, re-renderiza tudo e restaura sessão GPS do cloud se houver.
async function _bootPullBg() {
  try { await SYNC.pullAll(); } catch(e) { console.error('pullAll boot:', e); }
  try { await SYNC.flushQueue(); } catch(e) { console.error('flushQueue boot:', e); }
  try { checkRecurringLancamentos(); } catch(e) { console.error('checkRecurringLancamentos boot:', e); }
  try{restoreGpsSession();} catch(e){}
  try{syncNavButton();}  catch(e){}
  try{renderHome();}     catch(e){}
  try{renderJornada();}  catch(e){}
  try{renderFin();}      catch(e){}
  try{renderAgenda();} catch(e){}
  try{ if (S.sec==='corridas') renderCorridasActive(); } catch(e){}
  try{ if (S.sec==='lancamentos') renderLancamentos(); } catch(e){}
  _bootPullDone = true;
  _bootPullResolve();
}

function lsErr(id, msg) { const el=document.getElementById(id);if(el){el.textContent=msg;el.style.display=msg?'block':'none';} }
function lsBtn(id, txt, disabled) { const el=document.getElementById(id);if(el){el.textContent=txt;el.disabled=disabled;} }

const _MOB_LOGIN_GUARD_KEY='mob_login_guard_v1';
function mobLoginGuardLoad(){
  try{return JSON.parse(localStorage.getItem(_MOB_LOGIN_GUARD_KEY)||'{"fails":[],"blockedUntil":0}')}catch(e){return{fails:[],blockedUntil:0}}
}
function mobLoginGuardSave(g){try{localStorage.setItem(_MOB_LOGIN_GUARD_KEY,JSON.stringify(g))}catch(e){}}
function mobLoginBlockedSeconds(){const g=mobLoginGuardLoad();return Math.max(0,Math.ceil(((g.blockedUntil||0)-Date.now())/1000));}
function mobLoginGuardFail(){
  const now=Date.now(),g=mobLoginGuardLoad();
  g.fails=(g.fails||[]).filter(t=>now-t<10*60*1000);g.fails.push(now);
  if(g.fails.length>=5){g.blockedUntil=now+15*60*1000;g.fails=[];}
  mobLoginGuardSave(g);return mobLoginBlockedSeconds();
}
function mobLoginGuardClear(){try{localStorage.removeItem(_MOB_LOGIN_GUARD_KEY)}catch(e){}}

async function loginEnter() {
  const email=document.getElementById('l-email')?.value?.trim();
  const pass=document.getElementById('l-pass')?.value;
  lsErr('l-err','');
  if(!email||!pass){lsErr('l-err','Preencha email e senha');return;}
  const blocked=mobLoginBlockedSeconds();
  if(blocked>0){lsErr('l-err',`Muitas tentativas. Aguarde ${Math.ceil(blocked/60)} min e tente novamente.`);return;}
  lsBtn('l-btn-enter','Entrando...',true);
  try{
    if(_MOB_IS_TEST_ENV){
      const result=await Promise.race([
        _SUPA.auth.signInWithPassword({email,password:pass}),
        new Promise((_,rej)=>setTimeout(()=>rej(new Error('timeout')),12000))
      ]);
      if(result.error) throw new Error('invalid_credentials');
    }else{
      const ctl=new AbortController();const to=setTimeout(()=>ctl.abort(),12000);let resp;
      try{
        resp=await fetch(`${_MOB_SUPA_CONFIG.url}/functions/v1/auth-login-gate`,{
          method:'POST',signal:ctl.signal,
          headers:{'Content-Type':'application/json','apikey':_MOB_SUPA_CONFIG.key},
          body:JSON.stringify({email,password:pass})
        });
      }finally{clearTimeout(to);}
      const data=await resp.json().catch(()=>({}));
      if(!resp.ok){
        if(resp.status===429){
          const mins=Math.max(1,Math.ceil((Number(data.retry_after)||900)/60));
          lsErr('l-err',`Muitas tentativas. Aguarde ${mins} min e tente novamente.`);
          return;
        }
        mobLoginGuardFail();
        lsErr('l-err','Email ou senha inválidos.');
        return;
      }
      const set=await _SUPA.auth.setSession({access_token:data.access_token,refresh_token:data.refresh_token});
      if(set.error)throw new Error('session_error');
    }
    mobLoginGuardClear();
  }catch(err){
    if(String(err?.message||'').includes('invalid_credentials'))mobLoginGuardFail();
    lsErr('l-err',err?.name==='AbortError'?'Tempo esgotado. Verifique sua conexão.':'Não foi possível entrar. Verifique seus dados e tente novamente.');
  }finally{lsBtn('l-btn-enter','Entrar',false);}
}

async function loginRegister() {
  const name=document.getElementById('r-name')?.value?.trim();
  const email=document.getElementById('r-email')?.value?.trim();
  const phone=document.getElementById('r-phone')?.value?.trim();
  const pass=document.getElementById('r-pass')?.value;
  lsErr('r-err','');
  if(!name){lsErr('r-err','Digite seu nome');return;}
  if(!email){lsErr('r-err','Digite seu email');return;}
  if(!pass||pass.length<10){lsErr('r-err','Use uma senha com pelo menos 10 caracteres');return;}
  lsBtn('l-btn-reg','Criando conta...',true);
  try{
    const {data,error}=await _SUPA.auth.signUp({email,password:pass,options:{data:{name,phone}}});
    if(error){
      const em=String(error.message||'');
      if(em.toLowerCase().includes('password')) lsErr('r-err','Use uma senha com pelo menos 10 caracteres.');
      else if(em.toLowerCase().includes('too many')) lsErr('r-err','Muitas solicitações. Aguarde alguns minutos e tente novamente.');
      else lsErr('r-err','Não foi possível concluir o cadastro. Se você já possui conta, use Entrar ou Esqueci minha senha.');
      return;
    }
    if(data?.session){
      const cfg=DB.cfg();cfg.name=name;cfg.phone=phone;DB.saveCfg(cfg);
      try{await _SUPA.from('profiles').upsert({id:data.user.id,name,phone},{onConflict:'id'});}catch(e){}
      _supaUser=data.user;showApp();
    }else{
      lsErr('r-err','Cadastro recebido. Confira seu email para confirmar a conta antes de entrar.');
    }
  }catch(e){lsErr('r-err','Não foi possível concluir o cadastro agora. Tente novamente em alguns minutos.');}
  finally{lsBtn('l-btn-reg','Criar conta',false);}
}

async function loadProfile() {
  if(!_supaUser) return;
  const { data } = await _SUPA.from('profiles').select('*').eq('id',_supaUser.id).single();
  if (!data) return;
  const cfg = DB.cfg();
  let changed = false;
  // Só preenche o que estiver vazio local — não sobrescreve edição que o
  // usuário já tinha feito antes deste campo existir/sincronizar direito.
  if (data.name    && !cfg.name)    { cfg.name = data.name; changed = true; }
  if (data.city     && !cfg.city)    { cfg.city = data.city; changed = true; }
  if (data.plate    && !cfg.plate)   { cfg.plate = data.plate; changed = true; }
  if (data.vehicle  && !cfg.vehicle) { cfg.vehicle = data.vehicle; changed = true; }
  // Avatar é campo novo, sem edição local legada pra proteger — nuvem manda.
  if ((data.avatar_url || null) !== (cfg.avatarUrl || null)) { cfg.avatarUrl = data.avatar_url || null; changed = true; }
  if (changed) DB.saveCfg(cfg);
}

async function authSignOut() { await _SUPA.auth.signOut(); }
function toggleRegister() {}

function getCurrentUser() { return _supaUser; }






async function loginForgot() {
  const email = document.getElementById('f-email')?.value?.trim();
  const msgEl = document.getElementById('f-msg');
  const btn   = document.getElementById('l-btn-forgot');
  if (!email) { if(msgEl){msgEl.style.color='#EF4444';msgEl.textContent='Digite seu email.';msgEl.style.display='block';} return; }
  if(btn){btn.textContent='Enviando...';btn.disabled=true;}
  const { error } = await _SUPA.auth.resetPasswordForEmail(email, {
    redirectTo: 'https://yurisilva90.github.io/smartmobi/'
  });
  if(btn){btn.textContent='Enviar link';btn.disabled=false;}
  if (msgEl) {
    msgEl.style.display = 'block';
    if (error) {
      msgEl.style.color = '#EF4444';
      msgEl.textContent = 'Não foi possível processar a solicitação agora. Aguarde e tente novamente.';
    } else {
      msgEl.style.color = '#22C55E';
      msgEl.textContent = 'Se existir uma conta para esse email, você receberá as instruções de redefinição.';
    }
  }
}



// Chamada pelo nativo ao voltar ao foreground — sincroniza GPS
function nativeSyncGps(km, startMs, pausedMs) {
  if (!S || !S.gps || S.gps.st === 'idle') return;

  // GUARD: nunca deixa o sync nativo DIMINUIR o km acumulado.
  // Se o GpsService foi morto e reiniciado pelo Android, ele pode chamar
  // nativeSyncGps com km < do que ja temos em memoria. Nesse caso mantemos
  // o valor maior (o que estava em S.gps.km ou no nativo, o que for maior).
  const kmNat = (typeof km === 'number' &
===== sessions @ 1059971 =====
,{onConflict:'id'});}catch(e){}
      _supaUser=data.user;showApp();
    }else{
      lsErr('r-err','Cadastro recebido. Confira seu email para confirmar a conta antes de entrar.');
    }
  }catch(e){lsErr('r-err','Não foi possível concluir o cadastro agora. Tente novamente em alguns minutos.');}
  finally{lsBtn('l-btn-reg','Criar conta',false);}
}

async function loadProfile() {
  if(!_supaUser) return;
  const { data } = await _SUPA.from('profiles').select('*').eq('id',_supaUser.id).single();
  if (!data) return;
  const cfg = DB.cfg();
  let changed = false;
  // Só preenche o que estiver vazio local — não sobrescreve edição que o
  // usuário já tinha feito antes deste campo existir/sincronizar direito.
  if (data.name    && !cfg.name)    { cfg.name = data.name; changed = true; }
  if (data.city     && !cfg.city)    { cfg.city = data.city; changed = true; }
  if (data.plate    && !cfg.plate)   { cfg.plate = data.plate; changed = true; }
  if (data.vehicle  && !cfg.vehicle) { cfg.vehicle = data.vehicle; changed = true; }
  // Avatar é campo novo, sem edição local legada pra proteger — nuvem manda.
  if ((data.avatar_url || null) !== (cfg.avatarUrl || null)) { cfg.avatarUrl = data.avatar_url || null; changed = true; }
  if (changed) DB.saveCfg(cfg);
}

async function authSignOut() { await _SUPA.auth.signOut(); }
function toggleRegister() {}

function getCurrentUser() { return _supaUser; }






async function loginForgot() {
  const email = document.getElementById('f-email')?.value?.trim();
  const msgEl = document.getElementById('f-msg');
  const btn   = document.getElementById('l-btn-forgot');
  if (!email) { if(msgEl){msgEl.style.color='#EF4444';msgEl.textContent='Digite seu email.';msgEl.style.display='block';} return; }
  if(btn){btn.textContent='Enviando...';btn.disabled=true;}
  const { error } = await _SUPA.auth.resetPasswordForEmail(email, {
    redirectTo: 'https://yurisilva90.github.io/smartmobi/'
  });
  if(btn){btn.textContent='Enviar link';btn.disabled=false;}
  if (msgEl) {
    msgEl.style.display = 'block';
    if (error) {
      msgEl.style.color = '#EF4444';
      msgEl.textContent = 'Não foi possível processar a solicitação agora. Aguarde e tente novamente.';
    } else {
      msgEl.style.color = '#22C55E';
      msgEl.textContent = 'Se existir uma conta para esse email, você receberá as instruções de redefinição.';
    }
  }
}



// Chamada pelo nativo ao voltar ao foreground — sincroniza GPS
function nativeSyncGps(km, startMs, pausedMs) {
  if (!S || !S.gps || S.gps.st === 'idle') return;

  // GUARD: nunca deixa o sync nativo DIMINUIR o km acumulado.
  // Se o GpsService foi morto e reiniciado pelo Android, ele pode chamar
  // nativeSyncGps com km < do que ja temos em memoria. Nesse caso mantemos
  // o valor maior (o que estava em S.gps.km ou no nativo, o que for maior).
  const kmNat = (typeof km === 'number' && isFinite(km)) ? km : S.gps.km;
  // GUARD DE PLAUSIBILIDADE: se o serviço nativo Android nunca for
  // efetivamente resetado entre uma jornada e outra (não existe um comando
  // nativo pra isso hoje), ele pode reportar o km ACUMULADO de uma jornada
  // ANTERIOR logo no início de uma jornada NOVA — e como o guard acima
  // nunca deixa diminuir, esse km 'órfão' seria herdado silenciosamente.
  // Rejeita qualquer salto que implique velocidade fisicamente impossível
  // (>180km/h) dado o tempo decorrido desde o início desta sessão.
  const elapsedMin = S.gps.start ? (Date.now() - S.gps.start) / 60000 : 0;
  const maxPlausibleKm = (elapsedMin * 3) + 0.5; // 180km/h + margem
  if (kmNat > maxPlausibleKm) {
    console.warn('[nativeSyncGps] Ignorado — km implausível pro tempo decorrido:', kmNat.toFixed(1), 'km em', elapsedMin.toFixed(1), 'min (provável km órfão do serviço nativo)');
  } else if (kmNat > (S.gps.km || 0)) {
    S.gps.km = kmNat;
  }
  if (startMs && !S.gps.start) S.gps.start = startMs;
  // GUARD: nunca deixa o sync nativo DIMINUIR o pausedMs acumulado.
  // Se o GpsService reiniciou com pausedMs=0, mantemos o valor JS que é maior.
  const pauseNat = (typeof pausedMs === 'number' && isFinite(pausedMs)) ? pausedMs : 0;
  if (pauseNat >= (S.gps.pausedMs || 0)) { S.gps.pausedMs = pauseNat; }

  // LIMPA pontos de referencia JS para evitar dupla contagem.
  // Quando o app volta ao foreground, restoreSession recarrega o ultimo ponto
  // GPS salvo (g.pts = [lastPt]). Se o carro se deslocou no background, o
  // proximo disparo do watchPosition calcularia essa distancia E SOMARIA em
  // cima do km ja atualizado pelo nativo -> inflaria o total momentaneamente.
  // Zerando g.pts e lastFixTime, o proximo fix apenas registra a posicao atual
  // como nova referencia sem contar nenhuma distancia extra.
  S.gps.pts = [];
  S.gps.lastFixTime = null;

  const kmFinal = S.gps.km;
  // Só atualiza DOM se o usuário estiver vendo hoje
  if (S.curDate === DB.today()) {
    setText('jc-km', kmFinal.toFixed(1));
    const dayNow = DB.day(DB.today());
    const finKm = (dayNow.sessions||[]).reduce((a,x)=>a+(x.km||0),0);
    setText('jc-kmtotal', (finKm + kmFinal + (dayNow.kmAdjust||0)).toFixed(1)+' km');
  }

  // Recria a bolinha flutuante se o Android tiver matado o overlay.
  // startFloating e seguro de chamar de novo: se ja existe, so atualiza o km.
  if (window.SmartMobiNative && DB.cfg().overlayEnabled !== false) {
    try {
      if (typeof SmartMobiNative.startFloating === 'function') SmartMobiNative.startFloating(S.gps.start, kmFinal);
      if (typeof SmartMobiNative.updateFloatingStatus === 'function') SmartMobiNative.updateFloatingStatus('running');
    } catch(e) {}
  }
}



function togglePass(inputId, btn) {
  const inp = document.getElementById(inputId);
  if (!inp) return;
  const EYE = '<svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7Z"/><circle cx="12" cy="12" r="3"/></svg>';
  const EYE_OFF = '<svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10.94 10.94 0 0 1 12 19c-7 0-11-7-11-7a18.5 18.5 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 7 11 7a18.46 18.46 0 0 1-2.16 3.19M14.12 14.12a3 3 0 1 1-4.24-4.24"/><path d="M1 1l22 22"/></svg>';
  const isHidden = inp.type === 'password';
  inp.type = isHidden ? 'text' : 'password';
  btn.innerHTML = isHidden ? EYE_OFF : EYE;
}


// ── INIT ──────────────────────────────────────────────────────
const initCfg = DB.cfg();
if (initCfg.name) { const el=document.getElementById('p-nome'); if(el) el.value=initCfg.name; }
if (initCfg.plate) { const el=document.getElementById('p-placa'); if(el) el.value=initCfg.plate; }
const now0 = new Date();
const dateInputs = ['l-data'];
dateInputs.forEach(id => { const el=document.getElementById(id); if(el) el.value=DB.today(); });
const timeEl = document.getElementById('c-time');
if (timeEl) timeEl.value = p2(now0.getHours())+':'+p2(now0.getMinutes());
// Set type-switch initial state
setType('receita');
setScope('operacional');
renderLancCats();
// Render all screens
agCalY=new Date().getFullYear();agCalM=new Date().getMonth();


// ── Parceiros PUBLI — imagens reais ─────────────────────────────────────────
const ADS_IMG = [
  { img:'data:image/jpeg;base64,
===== sessions @ 1724936 =====
tY = 0, isDragging = false;
  function onStart(e) {
    startY = (e.touches ? e.touches[0].clientY : e.clientY);
    isDragging = true;
    panel.style.transition = 'none';
  }
  function onMove(e) {
    if (!isDragging) return;
    const dy = (e.touches ? e.touches[0].clientY : e.clientY) - startY;
    if (dy > 0) panel.style.transform = 'translateY(' + dy + 'px)';
  }
  function onEnd(e) {
    if (!isDragging) return;
    isDragging = false;
    panel.style.transition = '';
    const dy = (e.changedTouches ? e.changedTouches[0].clientY : e.clientY) - startY;
    if (dy > 100) { panel.style.transform = ''; infFecharPainel(); }
    else panel.style.transform = '';
  }
  drag.addEventListener('touchstart', onStart, {passive:true});
  drag.addEventListener('touchmove',  onMove,  {passive:true});
  drag.addEventListener('touchend',   onEnd);
  drag.addEventListener('mousedown',  onStart);
  document.addEventListener('mousemove', onMove);
  document.addEventListener('mouseup',   onEnd);
})();

['inf-ov-gps','inf-ov-venue','inf-ov-rep'].forEach(id=>{
  const el=document.getElementById(id);if(el)el.addEventListener('click',function(e){if(e.target===this)infFecharOv(id);});
});
document.addEventListener('click',e=>{
  if(!e.target.closest('#inf-search-bar')&&!e.target.closest('#inf-search-btn')&&!e.target.closest('#inf-search-dd')){
    const dd=document.getElementById('inf-search-dd');if(dd)dd.classList.remove('show');
  }
});

/* ══════════════════════════════════════════════════════════════
   FEED SOCIAL (Home) — v1
   Tabelas: feed_posts / feed_reactions / feed_comments (Supabase)
   Contadores de voto/comentário e auto-encerramento por downvote
   são calculados via trigger no banco — o client só insere/upserta
   e depois relê o post pra pegar os números atualizados.
   ══════════════════════════════════════════════════════════════ */

const FEED_TYPES = {
  blitz:    {label:'Blitz',        color:'#2563EB', expMin:60,
    icon:'<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>'},
  risco:    {label:'Risco',        color:'#EF4444', expMin:90,
    icon:'<path d="M10.3 3.9L1.8 18a2 2 0 0 0 1.7 3h17a2 2 0 0 0 1.7-3L13.7 3.9a2 2 0 0 0-3.4 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/>'},
  transito: {label:'Trânsito',     color:'#F97316', expMin:45,
    icon:'<rect x="3" y="4" width="18" height="16" rx="2"/><line x1="3" y1="10" x2="21" y2="10"/><line x1="3" y1="15" x2="21" y2="15"/>'},
  demanda:  {label:'Demanda alta', color:'#16A34A', expMin:30,
    icon:'<polyline points="23 6 13.5 15.5 8.5 10.5 1 18"/><polyline points="17 6 23 6 23 12"/>'},
  dinamico: {label:'Dinâmico',     color:'#F59E0B', expMin:30,
    icon:'<polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/>'},
  fila:     {label:'Fila',         color:'#7C3AED', expMin:45,
    icon:'<path d="M17.8 19.2L16 11l3.5-3.5C21 6 21.5 4 21 3c-1-.5-3 0-4.5 1.5L13 8 4.8 6.2c-.5-.1-.9.1-1.1.5l-.3.5c-.2.5-.1 1 .3 1.3L9 12l-2 3H4l-1 1 3 2 2 3 1-1v-3l3-2 3.5 5.3c.3.4.8.5 1.3.3l.5-.2c.4-.3.6-.7.5-1.2z"/>'}
};

let _feedFilter = '';
let _feedCoords = null;       // {lat,lng,address}
let _feedPosts  = [];
let _feedNames  = {};         // user_id -> nome
let _feedChannel = null;
let _feedCpType  = 'blitz';
let _feedCpLocOn = true;
let _feedMicActive = false;
let _feedCommentsPostId = null;

function feedHaversineKm(lat1,lng1,lat2,lng2){
  const R=6371, toRad=d=>d*Math.PI/180;
  const dLat=toRad(lat2-lat1), dLng=toRad(lng2-lng1);
  const a=Math.sin(dLat/2)**2+Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLng/2)**2;
  return R*2*Math.atan2(Math.sqrt(a),Math.sqrt(1-a));
}
function feedDistLabel(p){
  if(!_feedCoords||p.lat==null||p.lng==null) return '';
  const km=feedHaversineKm(_feedCoords.lat,_feedCoords.lng,p.lat,p.lng);
  return km<1 ? Math.round(km*1000)+' m' : km.toFixed(1).replace('.',',')+' km';
}
function feedAgeLabel(iso){
  const min=Math.max(0,Math.round((Date.now()-new Date(iso))/60000));
  if(min<1) return 'agora';
  if(min<60) return min+' min';
  return Math.floor(min/60)+'h'+(min%60?p2(min%60)+'m':'');
}
function feedInitials(name){
  const parts=(name||'').trim().split(/\s+/).filter(Boolean);
  if(!parts.length) return 'MO';
  return parts.length===1 ? parts[0].slice(0,2).toUpperCase() : (parts[0][0]+parts[parts.length-1][0]).toUpperCase();
}
function feedAvColor(uid){
  return INF_AVC[Math.abs((uid||'').split('').reduce((a,c)=>a+c.charCodeAt(0),0))%INF_AVC.length];
}

async function renderFeedHome(){
  feedComputeKpis();
  feedGetLocation();
  await feedLoadPosts();
  feedSubscribeRealtime();
}

function feedComputeKpis(){
  const dstr=DB.today();
  const day=DB.day(dstr);
  const tripsRev=(day.trips||[]).reduce((s,t)=>s+(t.value||0),0);
  const autoInfo=(typeof finAutoDay==='function')?finAutoDay(dstr):{sum:0,count:0};
  const manRev=(day.revenues||[]).reduce((s,r)=>s+(r.amount||0),0);
  const rev=tripsRev+autoInfo.sum+manRev;
  const trips=(day.trips||[]).length+autoInfo.count;
  const km=(day.sessions||[]).reduce((s,ss)=>s+(ss.km||0),0);
  const onlineSecs=(day.sessions||[]).reduce((s,ss)=>s+(ss.elapsed_secs||0),0);
  setText('feed-k-rev', DB.fRI(rev));
  setText('feed-k-trips', trips);
  setText('feed-k-rpkm', km>0 ? DB.fRI(rev/km) : '—');
  setText('feed-k-online', onlineSecs>0 ? Math.floor(onlineSecs/3600)+'h'+p2(Math.floor(onlineSecs%3600/60))+'m' : '—');
}

function feedGetLocation(){
  if(!navigator.geolocation) return;
  navigator.geolocation.getCurrentPosition(async pos=>{
    const {latitude:lat,longitude:lon}=pos.coords;
    _feedCoords={lat,lng:lon,address:null};
    try{
      const r=await fetch(`https://nominatim.openstreetmap.org/reverse?lat=${lat}&lon=${lon}&zoom=17&format=json&accept-language=pt`,{headers:{'User-Agent':'MoB/1.0'}});
      const j=await r.json();
      const addr=j.address||{};
      _feedCoords.address=addr.road||addr.pedestrian||addr.suburb||addr.neighbourhood||'';
    }catch(e){console.warn('feed loc',e);}
    feedUpdateComposerLoc();
    feedRenderList(); // recalcula distâncias com a posição atual
  },()=>{_feedCoords=null;},{timeout:10000,enableHighAccuracy:true});
}

async function feedLoadPosts(){
  const el=document.getElementById('feed-list');
  if(!_SUPA){ if(el) el.innerHTML='<div class="feed-empty">Sem conexão</div>'; return; }
  try{
    let q=_SUPA.from('feed_posts').select('*')
      .eq('status','ativo').eq('audience','comunidade')
      .gt('expires_at', new Date().toISOString())
      .order('created_at',{ascending:false}).limit(40);
    if(_feedFilter) q=q.eq('type',_feedFilter);
    const {data,error}=await q;
    if(error) throw error;
    _feedPosts=data||[];
    const uids=[...new Set(_feedPosts.map(p=>p.user_id))];
    if(uids.length){
      const {data:names}=await _SUPA.rpc('get_profile_names',{uids});
      _feedNames={}; (names||[]).forEach(n=>{_feedNames[n.id]={name:n.name, avatar:n.avatar_url};});
    }
    feedRenderList();
  }catch(e){console.warn('feed load',e); if(el) el.innerHTML='<div class="feed-empty">Erro ao carregar o feed</div>';}
}

// Avatar compartilhado entre feed, comentários e composer: foto real
// quando existe, senão iniciais coloridas (mesma cor sempre pro mesmo uid).
function feedAvatarHtml(uid, name, avatarUrl, extraClass){
  const cls = 'feed-av' + (extraClass?(' '+extraClass):'');
  if (avatarUrl) return `<img class="${cls}" src="${esc(avatarUrl)}" alt="">`;
  return `<div class="${cls}" style="background:${feedAvColor(uid)}">${esc(feedInitials(name))}</div>`;
}

function feedRenderList(){
  const el=document.getElementById('feed-list');if(!el)return;
  if(!_feedPosts.length){el.innerHTML='<div class="feed-empty">Nenhum alerta ativo agora — seja o primeiro a avisar a comunidade.</div>';return;}
  el.innerHTML=_feedPosts.map(feedPostCardHtml).join('');
}

function feedPostCardHtml(p){
  const t=FEED_TYPES[p.type]||FEED_TYPES.blitz;
  const isOwn = _supaUser && p.user_id===_supaUser.id;
  const cfg = DB.cfg();
  const myName = isOwn ? (cfg.name||'Você') : ((_feedNames[p.user_id]&&_feedNames[p.user_id].name)||'Motorista MōB');
  const myAvatar = isOwn ? (cfg.avatarUrl||null) : ((_feedNames[p.user_id]&&_feedNames[p.user_id].avatar)||null);
  const dist=feedDistLabel(p);
  const closed=p.status==='encerrado';
  const myVote=p._myVote; // preenchido depois de votar, só nesta sessão
  return `<div class="feed-post${closed?' closed':''}" onclick="feedAbrirComentarios('${p.id}')">
    ${feedAvatarHtml(p.user_id, myName, myAvatar)}
    <div class="feed-body">
      <div class="feed-head">
        <span class="feed-name">${esc(myName)}</span>
        <span class="feed-time">· ${feedAgeLabel(p.created_at)}</span>
      </div>
      <span class="feed-type" style="color:${t.color};background:color-mix(in srgb,${t.color} 10%,transparent);border:1px solid color-mix(in srgb,${t.color} 25%,transparent)"><svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">${t.icon}</svg>${t.label}</span>
      ${p.body ? `<div class="feed-text">${esc(p.body)}</div>` : ''}
      ${p.address ? `<div class="feed-loc"><svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>${esc(p.address)}${dist?' · '+dist:''}</div>` : ''}
      ${closed ? '<span class="feed-closed-badge">Encerrado pela comunidade</span>' : `
      <div class="feed-acts">
        <button class="feed-act${myVote==='up'?' up-on':''}" onclick="event.stopPropagation();feedVotar('${p.id}','up',this)">
          <svg width="14" height="14" viewBox="0 0 24 24" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 9V5a3 3 0 0 0-3-3l-4 9v11h11.3a2 2 0 0 0 2-1.7l1.4-9a2 2 0 0 0-2-2.3zM7 22H4a2 2 0 0 1-2-2v-7a2 2 0 0 1 2-2h3"/></svg>
          ${p.votes_up||0}
        </button>
        <button class="feed-act${myVote==='down'?' dn-on':''}" onclick="event.stopPropagation();feedVotar('${p.id}','down',this)">
          <svg width="14" height="14" viewBox="0 0 24 24" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M10 15v4a3 3 0 0 0 3 3l4-9V2H5.7a2 2 0 0 0-2 1.7l-1.4 9a2 2 0 0 0 2 2.3zm7-13h3a2 2 0 0 1 2 2v7a2 2 0 0 1-2 2h-3"/></svg>
          ${p.votes_dn||0}
        </button>
        <button class="feed-act" onclick="event.stopPropagation();feedAbrirComentarios('${p.id}')">
          <svg width="14" height="14" viewBox="0 0 24 24" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 11.5a8.4 8.4 0 0 1-8.5 8.3 8.6 8.6 0 0 1-3.9-.9L3 21l2.1-5.6a8.3 8.3 0 0 1-1-3.9A8.4 8.4 0 0 1 12.5 3.2 8.4 8.4 0 0 1 21 11.5z"/></svg>
          ${p.comments_count||0}
        </button>
      </div>`}
    </div>
  </div>`;
}

function feedSetFilter(type,el){
  _feedFilter=type;
  document.querySelectorAll('#feed-filters .feed-chip').forEach(c=>c.classList.remove('on'));
  el.classList.add('on');
  feedLoadPosts();
}

async function feedVotar(postId,vote,btn){
  if(!_SUPA||!_supaUser)return;
  try{
    await _SUPA.from('feed_reactions').upsert({post_id:postId,user_id:_supaUser.id,vote},{onConflict:'post_id,user_id'});
    const {data}=await _SUPA.from('feed_posts').select('votes_up,votes_dn,status').eq('id',postId).single();
    const p=_feedPosts.find(x=>x.id===postId);
    if(p&&data){p.votes_up=data.votes_up;p.votes_dn=data.votes_dn;p.status=data.status;p._myVote=vote;}
    feedRenderList();
  }catch(e){console.warn('feed voto',e);}
}

function feedSubscribeRealtime(){
  feedUnsubscribeRealtime();
  if(!_SUPA)return;
  try{
    _feedChannel=_SUPA.channel('feed-home')
      .on('postgres_changes',{event:'INSERT',schema:'public',table:'feed_posts'},payload=>{
        if(payload.new?.user_id!==_supaUser?.id) showToast('Novo alerta no feed!','info');
        feedLoadPosts();
      })
      .subscribe();
  }catch(e){console.warn('feed realtime',e);}
}
function feedUnsubscribeRealtime(){
  if(_feedChannel){try{_SUPA?.removeChannel(_feedChannel);}catch(e){}}_feedChannel=null;
}

/* ── Composer ── */
function feedOpenComposer(){
  _feedCpType='blitz';_feedCpLocOn=true;_feedMicActive=false;
  document.getElementById('feed-cp-text').value='';
  feedCpCount();
  document.querySelectorAll('.feed-t-chip').forEach(c=>c.classList.toggle('on',c.dataset.type==='blitz'));
  document.getElementById('feed-cp-mic')?.classList.remove('rec');
  const recBar=document.getElementById('feed-cp-recbar'); if(recBar) recBar.style.display='none';
  feedUpdateComposerLoc();
  const avEl=document.getElementById('feed-cp-avatar');
  if(avEl){const cfg=DB.cfg();avEl.innerHTML=feedAvatarHtml(_supaUser&&_supaUser.id,cfg.name||'Você',cfg.avatarUrl);}
  openMo('mo-feed-composer');
}
function feedCloseComposer(){ closeMo('mo-feed-composer'); }

function feedCpSelType(pid,btn){
  _feedCpType=pid;
  document.querySelectorAll('.feed-t-chip').forEach(c=>c.classList.remove('on'));
  btn.classList.add('on');
}
function feedUpdateComposerLoc(){
  const el=document.getElementById('feed-cp-loc');if(!el)return;
  el.classList.toggle('off',!_feedCpLocOn);
  const sub=el.querySelector('.sub');
  if(!_feedCoords) el.querySelector('.main').textContent='Localizando…';
  else el.querySelector('.main').textContent=_feedCpLocOn ? (_feedCoords.address||'Local atual') : 'Sem localização';
  if(sub) sub.textContent=_feedCpLocOn ? 'toque pra publicar sem localização' : 'toque pra marcar sua localização';
}
function feedToggleCpLoc(){ _feedCpLocOn=!_feedCpLocOn; feedUpdateComposerLoc(); }

function feedCpCount(){
  const ta=document.getElementById('feed-cp-text');
  const c=document.getElementById('feed-cp-count');
  if(ta&&c) c.textContent=ta.value.length+'/180';
}

function feedMicToggle(){
  const btn=document.getElementById('feed-cp-mic');
  const bar=document.getElementById('feed-cp-recbar');
  if(window.SmartMobiNative && typeof SmartMobiNative.startVoiceCapture==='function'){
    _feedMicActive=!_feedMicActive;
    btn?.classList.toggle('rec',_feedMicActive);
    if(bar) bar.style.display=_feedMicActive?'flex':'none';
    try{ _feedMicActive ? SmartMobiNative.startVoiceCapture() : SmartMobiNative.stopVoiceCapture?.(); }catch(e){}
  }else{
    showToast('Ditado por voz nativo ainda não disponível nesta versão — use o microfone do teclado pra falar direto no campo de texto.');
  }
}

async function feedPublicar(){
  if(!_SUPA||!_supaUser)return;
  const body=(document.getElementById('feed-cp-text').value||'').trim().slice(0,180);
  const btn=document.getElementById('feed-cp-post-btn');
  if(btn){btn.disabled=true;btn.textContent='Publicando…';}
  const t=FEED_TYPES[_feedCpType]||FEED_TYPES.blitz;
  const expiresAt=new Date(Date.now()+t.expMin*60000).toISOString();
  const row={
    user_id:_supaUser.id, type:_feedCpType, body:body||null,
    lat:(_feedCpLocOn&&_feedCoords)?_feedCoords.lat:null,
    lng:(_feedCpLocOn&&_feedCoords)?_feedCoords.lng:null,
    add
===== sessions @ 1750099 =====
ber(S.gps.start), Number(S.gps.km)||0, isNew);
      nativeSessionBound = S.gps.sessionId;
    }catch(e){}
  }

  async function loadStatusSegments(dateStr){
    if(!_supaUser || statusLoading) return;
    statusLoading = true;
    try{
      const {data,error} = await _SUPA.from('journey_status_segments').select('*')
        .eq('user_id', _supaUser.id).eq('date', dateStr).order('start_ms',{ascending:true});
      if(!error){ statusSegments = data||[]; statusSegmentsDate = dateStr; }
    }catch(e){} finally { statusLoading = false; }
  }

  function segmentEndMs(seg, isLiveSession){
    if(seg.end_ms != null) return Number(seg.end_ms)||Number(seg.start_ms)||0;
    if(isLiveSession) return Date.now();
    return Number(seg.last_seen_ms)||Number(seg.start_ms)||0;
  }
  function segmentEndKm(seg, isLiveSession){
    if(seg.end_km != null) return Number(seg.end_km)||0;
    if(isLiveSession && S && S.gps) return Number(S.gps.km)||0;
    return Number(seg.last_km)||Number(seg.start_km)||0;
  }

  function breakdownFromSegments(elapsedSec, kmTotal, segments, liveSessionId){
    const out = {
      total: Math.max(0,Number(elapsedSec)||0),
      online:{sec:0,km:0,pct:0}, buscando:{sec:0,km:0,pct:0}, corrida:{sec:0,km:0,pct:0}
    };
    (segments||[]).forEach(seg=>{
      const status = seg.status === 'buscar' ? 'buscando' : seg.status;
      if(!out[status]) return;
      const start = Number(seg.start_ms)||0;
      const live = !!liveSessionId && seg.session_id === liveSessionId && seg.end_ms == null;
      const end = segmentEndMs(seg, live);
      const sec = Math.max(0,(end-start)/1000);
      const skm = Number(seg.start_km)||0;
      const ekm = segmentEndKm(seg, live);
      out[status].sec += sec;
      out[status].km += Math.max(0,ekm-skm);
    });
    // Tempo/km que ainda não tem trecho explícito continua como Online, mas
    // NUNCA apaga os trechos Buscar/Corrida já registrados.
    const trackedSec = out.online.sec+out.buscando.sec+out.corrida.sec;
    const trackedKm = out.online.km+out.buscando.km+out.corrida.km;
    out.online.sec += Math.max(0,out.total-trackedSec);
    out.online.km += Math.max(0,(Number(kmTotal)||0)-trackedKm);
    const pct = v => out.total>0 ? Math.round(Math.max(0,v)/out.total*100) : 0;
    out.online.pct=pct(out.online.sec); out.buscando.pct=pct(out.buscando.sec); out.corrida.pct=pct(out.corrida.sec);
    return out;
  }

  function manualTripTs(t,dateStr){
    if(!t) return 0;
    let ts=Number(t.ts)||0;
    if(!ts&&t.created_at) ts=Date.parse(t.created_at)||0;
    if(!ts&&t.accepted_at) ts=Date.parse(t.accepted_at)||0;
    if(!ts&&t.trip_started_at) ts=Date.parse(t.trip_started_at)||0;
    if(!ts&&t.time&&dateStr) ts=new Date(dateStr+'T'+t.time+':00').getTime();
    return ts||0;
  }

  function currentSessionTrips(day,startMs){
    const dateStr=day&&day.date?day.date:DB.today();
    const manual=(day&&day.trips?day.trips:[]).filter(t=>manualTripTs(t,dateStr)>=startMs);
    const auto=(day&&day.autoTrips?day.autoTrips:[]).filter(t=>{
      try{ const ts=typeof autoTripTs==='function'?autoTripTs(t):Date.parse(t.trip_started_at||t.accepted_at||'')||0; return ts&&ts>=startMs; }
      catch(e){return false;}
    });
    return {manual,auto};
  }

  function mergedTodaySegments(){
    const live = (S && S.gps && S.gps.st==='running') ? nativeStatusTimeline() : [];
    const sid = S&&S.gps ? S.gps.sessionId : null;
    const cloud = (statusSegmentsDate===S.curDate ? statusSegments : []).filter(x=>!sid || x.session_id!==sid);
    return cloud.concat(live);
  }

  function renderStatusTimeline(){
    try{
      if(!S||!DB||S.sec!=='jornada') return;
      ensureNativeJourneySession();
      if(S.curDate && statusSegmentsDate!==S.curDate) loadStatusSegments(S.curDate);

      const day=DB.day(S.curDate||DB.today());
      const isToday=S.curDate===DB.today();
      const live=isToday&&S.gps&&S.gps.st==='running'&&S.gps.start;

      // Card verde: somente a Jornada atual.
      if(live){
        const elapsed=Math.max(0,typeof getElapsed==='function'?getElapsed():0);
        const km=Math.max(0,Number(S.gps.km)||0);
        const trips=currentSessionTrips(day,Number(S.gps.start)||0);
        const ganhos=trips.manual.reduce((a,t)=>a+(Number(t.value)||0),0)+trips.auto.reduce((a,t)=>a+(Number(t.offer_value)||0),0);
        if(typeof setText==='function'){
          setText('jc-trips2',trips.manual.length+trips.auto.length);
          setText('jc-rsh2',elapsed>0?DB.fRI(ganhos/(elapsed/3600)):'R$0,00');
          setText('jc-rskm2',km>0?DB.fRI(ganhos/km):'R$0,00');
        }
        const native=nativeStatusTimeline();
        const bd=breakdownFromSegments(elapsed,km,native,S.gps.sessionId);
        const jc=document.getElementById('jc-breakdown');
        if(jc){ jc.style.display=''; setHTML('jc-bar',buildSbarHtml(bd)); setHTML('jc-blist',buildSrowsHtml(bd)); }
      }

      // Total do dia: soma trechos persistidos de TODAS as jornadas + trecho vivo.
      const finished=(day.sessions||[]);
      const totalElapsed=finished.reduce((a,x)=>a+(x.elapsed||0),0)+(live?getElapsed():0)+(day.tempoAdjust||0);
      const totalKm=finished.reduce((a,x)=>a+(x.km||0),0)+(live?(Number(S.gps.km)||0):0)+(day.kmAdjust||0);
      const td=document.getElementById('td-bd-row');
      if(td&&totalElapsed>0){
        const bd=breakdownFromSegments(totalElapsed,totalKm,mergedTodaySegments(),live?S.gps.sessionId:null);
        td.style.display=''; setHTML('td-bd-bar',buildSbarHtml(bd)); setHTML('td-bd-list',buildSrowsHtml(bd));
      }
    }catch(e){ console.warn('[JourneyStatusTimeline]',e); }
  }
  // Compatibilidade apenas: V3 abaixo é o único renderizador automático.
  // Expõe somente o vínculo da sessão nativa para o V3 poder garantir que
  // uma Jornada recém-iniciada comece a gravar Online/Buscar/Corrida mesmo
  // sem depender do renderizador legado da barra.
  window.ensureNativeJourneySession=ensureNativeJourneySession;
  window.renderStatusTimeline=renderStatusTimeline;
})();
</script>



<!-- JOURNEY_CONTINUOUS_OPERATIONAL_V3 -->
<script>
(function(){
  // ── Datas e dias operacionais ───────────────────────────────────────
  window.localYmdFromMs = function(ms){
    const d = new Date(Number(ms)||Date.now());
    return d.getFullYear()+'-'+p2(d.getMonth()+1)+'-'+p2(d.getDate());
  };
  window.normalizeOperationalPlatform = function(p){
    return String(p||'uber').toLowerCase() === '99' ? '99' : 'uber';
  };
  window.civilDayBounds = function(dateStr){
    const start = new Date(dateStr+'T00:00:00');
    const end = new Date(start); end.setDate(end.getDate()+1);
    return {startMs:start.getTime(), endMs:end.getTime()};
  };
  window.operationalRangeForPlatform = function(plat,dateStr){
    const p = normalizeOperationalPlatform(plat);
    const start = new Date(dateStr+'T00:00:00');
    if(p === 'uber') start.setHours(4,0,0,0);
    const end = new Date(start); end.setDate(end.getDate()+1);
    return {platform:p, operationalDate:dateStr, startMs:start.getTime(), endMs:end.getTime()};
  };
  window.operationalDateForTimestamp = function(plat,ts){
    const p = normalizeOperationalPlatform(plat);
    const d = new Date(Number(ts));
    // Uber: tudo de 00:00 até 03:59 ainda pertence ao dia operacional anterior.
    if(p === 'uber' && d.getHours() < 4) d.setDate(d.getDate()-1);
    return localYmdFromMs(d.getTime());
  };
  window.operationalTimestampForTime = function(plat,dateStr,hhmm){
    const p = normalizeOperationalPlatform(plat);
    const parts = String(hhmm||'00:00').split(':').map(Number);
    const h = Number.isFinite(parts[0]) ? parts[0] : 0;
    const m = Number.isFinite(parts[1]) ? parts[1] : 0;
    const d = new Date(dateStr+'T00:00:00');
    d.setHours(h,m,0,0);
    if(p === 'uber' && h < 4) d.setDate(d.getDate()+1);
    return d.getTime();
  };
  window.loadAutoTripsForOperationalDay = async function(plat,dateStr){
    if(!_supaUser) return [];
    const r = operationalRangeForPlatform(plat,dateStr);
    let q = _SUPA.from('auto_trips').select('*')
      .eq('user_id',_supaUser.id)
      .eq('platform',normalizeOperationalPlatform(plat))
      .gte('trip_started_at',new Date(r.startMs).toISOString())
      .lt('trip_started_at',new Date(r.endMs).toISOString())
      .order('trip_started_at',{ascending:true});
    const {data,error} = await q;
    return error ? [] : (data||[]);
  };

  // ── Filtro da tela Corridas ────────────────────────────────────────
  const _civilGetCorridasDays = window.getCorridasDays;
  window.getCorridasDays = function(){
    const plats = S.corridasPlats || new Set();
    const onePlat = plats.size === 1 ? [...plats][0] : null;
    if(!onePlat) return _civilGetCorridasDays();
    const anchorStr = operationalDateForTimestamp(onePlat,Date.now());
    const anchor = new Date(anchorStr+'T12:00:00');
    const fmt = d => localYmdFromMs(d.getTime());
    const p = S.corridasPeriod;
    if(p==='date' && S.corridasDate) return [S.corridasDate];
    if(p==='hoje') return [anchorStr];
    if(p==='7d') { const out=[]; for(let i=0;i<7;i++){const d=new Date(anchor);d.setDate(d.getDate()-i);out.push(fmt(d));} return out; }
    if(p==='mes') {
      const y=anchor.getFullYear(), m=anchor.getMonth();
      const dim=new Date(y,m+1,0).getDate(), out=[];
      for(let day=1;day<=dim;day++) out.push(y+'-'+p2(m+1)+'-'+p2(day));
      return out;
    }
    return _civilGetCorridasDays();
  };
  window.autoCorridasDateRange = function(){
    const dates = getCorridasDays().slice().sort();
    if(!dates.length) return null;
    const plats = S.corridasPlats || new Set();
    const onePlat = plats.size===1 ? [...plats][0] : null;
    if(onePlat){
      const a=operationalRangeForPlatform(onePlat,dates[0]);
      const b=operationalRangeForPlatform(onePlat,dates[dates.length-1]);
      return {from:new Date(a.startMs).toISOString(),to:new Date(b.endMs-1).toISOString()};
    }
    const a=civilDayBounds(dates[0]), b=civilDayBounds(dates[dates.length-1]);
    return {from:new Date(a.startMs).toISOString(),to:new Date(b.endMs-1).toISOString()};
  };

  // ── Jornada contínua: nunca mais pergunta/divide à meia-noite ─────
  window.checkMidnightCrossing = function(){ return; };
  window.openMidnightModal = function(){ S._midnightModalOpen=false; try{closeMo('mo-midnight');}catch(e){} };
  window.resolveMidnightSplit = async function(){ S._midnightModalOpen=false; try{closeMo('mo-midnight');}catch(e){} };
  window.splitJourneyAtMidnight = async function(){ return; };
  window.openPostStopMidnightModal = function(sess,onDone){
    S._midnightIsPostStop=false; S._postStopMidnightSess=null; S._postMidnightCallback=null; S._midnightModalOpen=false;
    try{closeMo('mo-midnight');}catch(e){}
    if(typeof onDone==='function') onDone();
  };
  window.resolvePostStopMidnight = async function(){ S._midnightIsPostStop=false; S._midnightModalOpen=false; try{closeMo('mo-midnight');}catch(e){} };
  try { if(S && S.gps) S.gps.forceDate = null; } catch(e) {}

  // ── Corridas de uma Jornada: busca por INTERVALO REAL, não por data ──
  const journeyAutoCache = new Map();
  const journeyStatusCache = new Map();
  const overlapSessionCache = new Map();
  const civilStatusCache = new Map();

  function journeyKey(sess){ return String(sess?.id || ((sess?.start||0)+'_'+(sess?.end||'live'))); }
  function sessionEnd(sess){ return Number(sess?.end || ((S?.gps?.sessionId && sess?.id===S.gps.sessionId) ? Date.now() : Date.now())); }
  function overlaps(start,end,a,b){ return Number(start||0) < b && Number(end||Date.now()) >= a; }

  async function ensureJourneyAutoTrips(sess){
    if(!_supaUser || !sess?.start) return;
    const k=journeyKey(sess), old=journeyAutoCache.get(k);
    const ttl=sess.end?120000:12000;
    if(old && (old.loading || Date.now()-old.ts<ttl)) return;
    journeyAutoCache.set(k,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    try{
      const end=sessionEnd(sess);
      const {data,error}=await _SUPA.from('auto_trips').select('*')
        .eq('user_id',_supaUser.id)
        .gte('trip_started_at',new Date(Number(sess.start)).toISOString())
        .lte('trip_started_at',new Date(end).toISOString())
        .order('trip_started_at',{ascending:true});
      if(!error) journeyAutoCache.set(k,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){ journeyAutoCache.set(k,{loading:false,ts:Date.now(),rows:old?.rows||[]}); }
    try{ if(S.sec==='jornada') setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0); }catch(e){}
  }
  function journeyAutoTrips(sess,fallback){
    const k=journeyKey(sess), c=journeyAutoCache.get(k);
    ensureJourneyAutoTrips(sess);
    const rows=(c&&Array.isArray(c.rows)&&c.rows.length)?c.rows:(fallback||[]);
    const a=Number(sess?.start||0), b=sessionEnd(sess);
    const seen=new Set();
    return rows.filter(t=>{const ts=autoTripTs(t); if(!ts||ts<a||ts>b)return false; const id=t.id||ts+'_'+t.offer_value; if(seen.has(id))return false; seen.add(id); return true;});
  }
  function journeyManualTrips(sess){
    const a=Number(sess?.start||0), b=sessionEnd(sess), out=[], seen=new Set();
    (DB.allDates?DB.allDates():[]).forEach(ds=>{
      const d=DB.day(ds);
      (d.trips||[]).forEach(t=>{
        let ts=Number(t.ts)||0;
        if(!ts&&t.time) ts=new Date(ds+'T'+t.time+':00').getTime();
        if(!ts||ts<a||ts>b)return;
        const id=t.id||ts+'_'+(t.value||0); if(seen.has(id))return; seen.add(id);
        out.push(Object.assign({},t,{ts,_ts:ts}));
      });
    });
    return out;
  }

  // ── Timeline real Online/Buscar/Corrida por Jornada ────────────────
  async function ensureJourneyStatus(sess){
    if(!_supaUser || !sess?.id) return;
    const k=journeyKey(sess), old=journeyStatusCache.get(k), ttl=sess.end?120000:8000;
    if(old && (old.loading || Date.now()-old.ts<ttl)) return;
    journeyStatusCache.set(k,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    try{
      const {data,error}=await _SUPA.from('journey_status_segments').select('*')
        .eq('user_id',_supaUser.id).eq('session_id',sess.id).order('start_ms',{ascending:true});
      if(!error) journeyStatusCache.set(k,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){ journeyStatusCache.set(k,{loading:false,ts:Date.now(),rows:old?.rows||[]}); }
    try{ if(S.sec==='jornada') setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0); }catch(e){}
  }
  function statusBreakdownRows(rows,elapsedSec,kmTotal,clipStart,clipEnd,liveSessionId){
    const out={total:Math.max(0,Number(elapsedSec)||0),online:{sec:0,km:0,pct:0},buscando:{sec:0,km:0,pct:0},corrida:{sec:0,km:0,pct:0}};
    (rows||[]).forEach(seg=>{
      const key=seg.status==='buscar'?'buscando':seg.status; if(!out[key])return;
      const rawStart=Number(seg.start_ms)||0;
      const isLive=!!liveSessionId&&seg.session_id===liveSessionId&&seg.end_ms==null;
      const rawEnd=Number(seg.end_ms||(isLive?Date.now():seg.last_seen_ms)||rawStart);
      if(rawEnd<=r
===== sessions @ 1761948 =====
true,ts:Date.now(),rows:old?.rows||[]});
    try{
      const end=sessionEnd(sess);
      const {data,error}=await _SUPA.from('auto_trips').select('*')
        .eq('user_id',_supaUser.id)
        .gte('trip_started_at',new Date(Number(sess.start)).toISOString())
        .lte('trip_started_at',new Date(end).toISOString())
        .order('trip_started_at',{ascending:true});
      if(!error) journeyAutoCache.set(k,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){ journeyAutoCache.set(k,{loading:false,ts:Date.now(),rows:old?.rows||[]}); }
    try{ if(S.sec==='jornada') setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0); }catch(e){}
  }
  function journeyAutoTrips(sess,fallback){
    const k=journeyKey(sess), c=journeyAutoCache.get(k);
    ensureJourneyAutoTrips(sess);
    const rows=(c&&Array.isArray(c.rows)&&c.rows.length)?c.rows:(fallback||[]);
    const a=Number(sess?.start||0), b=sessionEnd(sess);
    const seen=new Set();
    return rows.filter(t=>{const ts=autoTripTs(t); if(!ts||ts<a||ts>b)return false; const id=t.id||ts+'_'+t.offer_value; if(seen.has(id))return false; seen.add(id); return true;});
  }
  function journeyManualTrips(sess){
    const a=Number(sess?.start||0), b=sessionEnd(sess), out=[], seen=new Set();
    (DB.allDates?DB.allDates():[]).forEach(ds=>{
      const d=DB.day(ds);
      (d.trips||[]).forEach(t=>{
        let ts=Number(t.ts)||0;
        if(!ts&&t.time) ts=new Date(ds+'T'+t.time+':00').getTime();
        if(!ts||ts<a||ts>b)return;
        const id=t.id||ts+'_'+(t.value||0); if(seen.has(id))return; seen.add(id);
        out.push(Object.assign({},t,{ts,_ts:ts}));
      });
    });
    return out;
  }

  // ── Timeline real Online/Buscar/Corrida por Jornada ────────────────
  async function ensureJourneyStatus(sess){
    if(!_supaUser || !sess?.id) return;
    const k=journeyKey(sess), old=journeyStatusCache.get(k), ttl=sess.end?120000:8000;
    if(old && (old.loading || Date.now()-old.ts<ttl)) return;
    journeyStatusCache.set(k,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    try{
      const {data,error}=await _SUPA.from('journey_status_segments').select('*')
        .eq('user_id',_supaUser.id).eq('session_id',sess.id).order('start_ms',{ascending:true});
      if(!error) journeyStatusCache.set(k,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){ journeyStatusCache.set(k,{loading:false,ts:Date.now(),rows:old?.rows||[]}); }
    try{ if(S.sec==='jornada') setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0); }catch(e){}
  }
  function statusBreakdownRows(rows,elapsedSec,kmTotal,clipStart,clipEnd,liveSessionId){
    const out={total:Math.max(0,Number(elapsedSec)||0),online:{sec:0,km:0,pct:0},buscando:{sec:0,km:0,pct:0},corrida:{sec:0,km:0,pct:0}};
    (rows||[]).forEach(seg=>{
      const key=seg.status==='buscar'?'buscando':seg.status; if(!out[key])return;
      const rawStart=Number(seg.start_ms)||0;
      const isLive=!!liveSessionId&&seg.session_id===liveSessionId&&seg.end_ms==null;
      const rawEnd=Number(seg.end_ms||(isLive?Date.now():seg.last_seen_ms)||rawStart);
      if(rawEnd<=rawStart)return;
      const a=Math.max(rawStart,clipStart==null?rawStart:clipStart);
      const b=Math.min(rawEnd,clipEnd==null?rawEnd:clipEnd);
      if(b<=a)return;
      const sec=(b-a)/1000;
      let sk=Number(seg.start_km)||0;
      let ek=Number(seg.end_km!=null?seg.end_km:(isLive&&S?.gps?S.gps.km:seg.last_km))||sk;
      const frac=(b-a)/(rawEnd-rawStart);
      const km=Math.max(0,(ek-sk)*frac);
      out[key].sec+=sec; out[key].km+=km;
    });
    const tracked=out.online.sec+out.buscando.sec+out.corrida.sec;
    const trackedKm=out.online.km+out.buscando.km+out.corrida.km;
    if(out.total<=0) out.total=tracked;
    else {
      out.online.sec+=Math.max(0,out.total-tracked);
      out.online.km+=Math.max(0,(Number(kmTotal)||0)-trackedKm);
    }
    const pct=v=>out.total>0?Math.round(Math.max(0,v)/out.total*100):0;
    out.online.pct=pct(out.online.sec);out.buscando.pct=pct(out.buscando.sec);out.corrida.pct=pct(out.corrida.sec);
    return out;
  }
  window.journeyBreakdownForSession = function(sess,elapsedSec,kmTotal,fallbackAuto){
    if(!sess?.id) return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
    ensureJourneyStatus(sess);
    const c=journeyStatusCache.get(journeyKey(sess));
    if(c&&c.rows&&c.rows.length) return statusBreakdownRows(c.rows,elapsedSec,kmTotal,null,null,(S?.gps?.st==='running'?S.gps.sessionId:null));
    // Jornada ativa nunca some enquanto a timeline chega: sem trechos ainda,
    // todo o tempo/km conhecido fica como Online e depois é redistribuído
    // quando Buscar/Corrida forem registrados.
    if(!c || c.loading) return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
    return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
  };

  // ── Jornadas que atravessam a data selecionada ────────────────────
  async function ensureOverlapSessions(dateStr){
    if(!_supaUser||!dateStr)return;
    const old=overlapSessionCache.get(dateStr);
    if(old&&(old.loading||Date.now()-old.ts<30000))return;
    overlapSessionCache.set(dateStr,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    const b=civilDayBounds(dateStr);
    try{
      const {data,error}=await _SUPA.from('sessions').select('*').eq('user_id',_supaUser.id)
        .lt('start_ms',b.endMs).or(`end_ms.is.null,end_ms.gte.${b.startMs}`).order('start_ms',{ascending:true});
      if(!error){
        const rows=data||[];
        overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows});
        // Hidratamos o cache local no DIA DE INÍCIO; nunca criamos uma segunda Jornada na virada.
        rows.filter(r=>r.end_ms).forEach(r=>{
          const ds=localYmdFromMs(r.start_ms), day=DB.day(ds); if(!day.sessions)day.sessions=[];
          const item={id:r.id,start:Number(r.start_ms),end:Number(r.end_ms),km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0};
          const i=day.sessions.findIndex(x=>x.id===item.id); if(i>=0)day.sessions[i]=item; else day.sessions.push(item); DB.saveDay(day);
        });
      }
    }catch(e){overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows:old?.rows||[]});}
    try{if(S.sec==='jornada')setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0);}catch(e){}
  }
  function overlappingJourneyDescriptors(dateStr){
    const b=civilDayBounds(dateStr), map=new Map();
    (DB.allDates?DB.allDates():[]).forEach(ds=>{
      const day=DB.day(ds);(day.sessions||[]).forEach((sess,idx)=>{
        if(!overlaps(sess.start,sess.end,b.startMs,b.endMs))return;
        map.set(String(sess.id||sess.start),{sess,sourceDate:ds,origIdx:idx});
      });
    });
    const remote=overlapSessionCache.get(dateStr)?.rows||[];
    remote.forEach(r=>{
      if(!overlaps(r.start_ms,r.end_ms,b.startMs,b.endMs))return;
      const id=String(r.id||r.start_ms);if(map.has(id))return;
      const ds=localYmdFromMs(r.start_ms), d=DB.day(ds), idx=(d.sessions||[]).findIndex(x=>x.id===r.id);
      map.set(id,{sess:{id:r.id,start:Number(r.start_ms),end:r.end_ms?Number(r.end_ms):null,km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0},sourceDate:ds,origIdx:idx});
    });
    if(S?.gps?.st==='running'&&S.gps.start&&overlaps(S.gps.start,null,b.startMs,b.endMs)){
      const id=String(S.gps.sessionId||S.gps.start);
      map.set(id,{sess:{id:S.gps.sessionId,start:S.gps.start,end:null,km:S.gps.km||0,elapsed:getElapsed()},sourceDate:localYmdFromMs(S.gps.start),origIdx:-1,live:true});
    }
    return [...map.values()].sort((a,b)=>(a.sess.start||0)-(b.sess.start||0));
  }
  function journeyLabel(desc,dateStr){
    const sess=desc.sess, src=desc.sourceDate||localYmdFromMs(sess.start);
    if(desc.live) return src===dateStr?'Jornada atual':'Jornada atual · continuação';
    const srcSessions=(DB.day(src).sessions||[]).slice().sort((a,b)=>(a.start||0)-(b.start||0));
    const pos=Math.max(0,srcSessions.findIndex(x=>x.id===sess.id));
    let label='Jornada '+(pos+1);
    const endDate=sess.end?localYmdFromMs(sess.end):src;
    if(src<dateStr) label+=' · continuação'; else if(endDate>dateStr) label+=' · continua';
    return label;
  }
  function renderContinuousSessionList(){
    const el=document.getElementById('sessions-list'), toggle=document.getElementById('sessions-toggle');
    if(!el||!S?.curDate)return;
    ensureOverlapSessions(S.curDate);
    const desc=overlappingJourneyDescriptors(S.curDate).sort((a,b)=>(b.sess.start||0)-(a.sess.start||0));
    const shown=S.sessionsExpanded?desc:desc.slice(0,3);
    const html=shown.map(x=>{
      ensureJourneyAutoTrips(x.sess);ensureJourneyStatus(x.sess);
      const synthetic={date:x.sourceDate,trips:journeyManualTrips(x.sess),autoTrips:journeyAutoTrips(x.sess,DB.day(x.sourceDate).autoTrips||[])};
      return sessionItem(journeyLabel(x,S.curDate),x.sess,x.origIdx,x.sourceDate,synthetic,!!x.live);
    });
    el.innerHTML=html.length?html.join(''):'<div class="empty">Nenhuma sessão registrada</div>';
    if(toggle){toggle.style.display=desc.length>3?'':'none';toggle.innerHTML=S.sessionsExpanded?'Ver menos':'Ver todas &#8250;';}
  }

  // Link de corrida ↔ Jornada agora procura TODAS as sessões; uma corrida de 01:30
  // pode pertencer à Jornada iniciada no dia anterior.
  window.isAutoTripLinkedToSession = function(t){
    const ts=autoTripTs(t);if(!ts)return false;
    for(const ds of (DB.allDates?DB.allDates():[])){
      if((DB.day(ds).sessions||[]).some(sess=>ts>=(sess.start||0)&&(!sess.end||ts<=sess.end)))return true;
    }
    return !!(S?.gps?.st==='running'&&S.gps.start&&ts>=S.gps.start);
  };

  function civilJourneyTotals(dateStr){
    const b=civilDayBounds(dateStr), seen=new Set();
    let elapsed=0, km=0;
    const add=sess=>{
      const start=Number(sess?.start||0); if(!start)return;
      const live=!!(S?.gps?.st==='running' && S.gps.sessionId && sess?.id===S.gps.sessionId);
      let end=Number(sess?.end||0);
      const recordedElapsed=Math.max(0,Number(sess?.elapsed)||0);
      if(!end) end=live?Date.now():(recordedElapsed>0?start+recordedElapsed*1000:0);
      if(!end||end<=start)return;
      const a=Math.max(start,b.startMs), z=Math.min(end,b.endMs); if(z<=a)return;
      const key=String(sess?.id||start+'_'+end); if(seen.has(key))return; seen.add(key);
      const wall=Math.max(1,end-start), frac=(z-a)/wall;
      const baseElapsed=recordedElapsed>0?recordedElapsed:wall/1000;
      elapsed+=Math.max(0,baseElapsed*frac);
      km+=Math.max(0,Number(sess?.km)||0)*frac;
    };
    try{(DB.allDates?DB.allDates():[]).forEach(ds=>(DB.day(ds).sessions||[]).forEach(add));}catch(e){}
    if(S?.gps?.st==='running'&&S.gps.start){
      add({id:S.gps.sessionId,start:S.gps.start,end:null,elapsed:getElapsed(),km:S.gps.km||0});
    }
    try{
      const day=DB.day(dateStr);
      elapsed=Math.max(0,elapsed+(Number(day.tempoAdjust)||0));
      km=Math.max(0,km+(Number(day.kmAdjust)||0));
    }catch(e){}
    return {elapsed,km};
  }

  // ── Barra Total do dia = corte CIVIL virtual da timeline ───────────
  async function ensureCivilStatus(dateStr){
    if(!_supaUser||!dateStr)return;
    const old=civilStatusCache.get(dateStr);if(old&&(old.loading||Date.now()-old.ts<8000))return;
    civilStatusCache.set(dateStr,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    const b=civilDayBounds(dateStr);
    try{
      const {data,error}=await _SUPA.from('journey_status_segments').select('*').eq('user_id',_supaUser.id)
        .lt('start_ms',b.endMs).or(`end_ms.is.null,end_ms.gte.${b.startMs}`).order('start_ms',{ascending:true});
      if(!error)civilStatusCache.set(dateStr,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){civilStatusCache.set(dateStr,{loading:false,ts:Date.now(),rows:old?.rows||[]});}
    try{if(S.sec==='jornada')setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0);}catch(e){}
  }
  function renderContinuousStatusBars(){
    if(!S?.curDate)return;
    ensureCivilStatus(S.curDate);
    const c=civilStatusCache.get(S.curDate), rows=c?.rows||[];
    const tdRow=document.getElementById('td-bd-row');
    const totals=civilJourneyTotals(S.curDate);
    if(rows.length){
      const b=civilDayBounds(S.curDate), bd=statusBreakdownRows(rows,totals.elapsed,totals.km,b.startMs,b.endMs,(S?.gps?.st==='running'?S.gps.sessionId:null));
      if(tdRow)tdRow.style.display='';
      try{setHTML('td-bd-bar',buildSbarHtml(bd));setHTML('td-bd-list',buildSrowsHtml(bd));}catch(e){}
    } else if(c && !c.loading) {
      const day=DB.day(S.curDate);
      const autos=(day.autoTrips&&day.autoTrips.length)?day.autoTrips:((S._autoTripsDataDate===S.curDate&&Array.isArray(S.autoTrips))?S.autoTrips:[]);
      const bd=computeStatusBreakdown(totals.elapsed,totals.km,autos);
      const hasJourney=totals.elapsed>0||totals.km>0||autos.length>0;
      if(tdRow)tdRow.style.display=hasJourney?'':'none';
      if(hasJourney){try{setHTML('td-bd-bar',buildSbarHtml(bd));setHTML('td-bd-list',buildSrowsHtml(bd));}catch(e){}}
    }

    const jcRow=document.getElementById('jc-breakdown');
    if(S?.gps?.st==='running'&&S.gps.sessionId){
      const sess={id:S.gps.sessionId,start:S.gps.start,end:null,km:S.gps.km||0,elapsed:getElapsed()};
      // Garante o vínculo com JourneyStatusTracker mesmo que o renderizador
      // legado esteja desativado. Isso faz a timeline nativa começar a gravar.
      try{ if(typeof window.ensureNativeJourneySession==='function') window.ensureNativeJourneySession(); }catch(e){}
      ensureJourneyStatus(sess);
      const bd=journeyBreakdownForSession(sess,getElapsed(),S.gps.km||0,[]);
      // Enquanto não existe trecho detalhado, o fallback acima mostra 100%
      // Online. Jornada ativa nunca deve esconder a barra.
      if(jcRow)jcRow.style.display='';
      try{setHTML('jc-bar',buildSbarHtml(bd));setHTML('jc-blist',buildSrowsHtml(bd));}catch(e){}
    } else if(jcRow) {
      jcRow.style.display='none';
    }
  }

  const previousRenderJornada=window.renderJornada;
  window.renderJornada=function(){
    previousRenderJornada();
    try{renderContinuousSessionList();}catch(e){console.warn('[continuous journeys]',e);}
    try{renderContinuousStatusBars();}catch(e){console.warn('[continuous status]',e);}
  };

  setInterval(()=>{try{if(S.sec==='jornada')renderContinuousStatusBars();}catch(e){}},1000);
  setTimeout(()=>{try{if(S.sec==='jornada')renderJornada();}catch(e){}},600);
})();
</script>

</body>
</html>
===== sessions @ 1762788 =====
isArray(c.rows)&&c.rows.length)?c.rows:(fallback||[]);
    const a=Number(sess?.start||0), b=sessionEnd(sess);
    const seen=new Set();
    return rows.filter(t=>{const ts=autoTripTs(t); if(!ts||ts<a||ts>b)return false; const id=t.id||ts+'_'+t.offer_value; if(seen.has(id))return false; seen.add(id); return true;});
  }
  function journeyManualTrips(sess){
    const a=Number(sess?.start||0), b=sessionEnd(sess), out=[], seen=new Set();
    (DB.allDates?DB.allDates():[]).forEach(ds=>{
      const d=DB.day(ds);
      (d.trips||[]).forEach(t=>{
        let ts=Number(t.ts)||0;
        if(!ts&&t.time) ts=new Date(ds+'T'+t.time+':00').getTime();
        if(!ts||ts<a||ts>b)return;
        const id=t.id||ts+'_'+(t.value||0); if(seen.has(id))return; seen.add(id);
        out.push(Object.assign({},t,{ts,_ts:ts}));
      });
    });
    return out;
  }

  // ── Timeline real Online/Buscar/Corrida por Jornada ────────────────
  async function ensureJourneyStatus(sess){
    if(!_supaUser || !sess?.id) return;
    const k=journeyKey(sess), old=journeyStatusCache.get(k), ttl=sess.end?120000:8000;
    if(old && (old.loading || Date.now()-old.ts<ttl)) return;
    journeyStatusCache.set(k,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    try{
      const {data,error}=await _SUPA.from('journey_status_segments').select('*')
        .eq('user_id',_supaUser.id).eq('session_id',sess.id).order('start_ms',{ascending:true});
      if(!error) journeyStatusCache.set(k,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){ journeyStatusCache.set(k,{loading:false,ts:Date.now(),rows:old?.rows||[]}); }
    try{ if(S.sec==='jornada') setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0); }catch(e){}
  }
  function statusBreakdownRows(rows,elapsedSec,kmTotal,clipStart,clipEnd,liveSessionId){
    const out={total:Math.max(0,Number(elapsedSec)||0),online:{sec:0,km:0,pct:0},buscando:{sec:0,km:0,pct:0},corrida:{sec:0,km:0,pct:0}};
    (rows||[]).forEach(seg=>{
      const key=seg.status==='buscar'?'buscando':seg.status; if(!out[key])return;
      const rawStart=Number(seg.start_ms)||0;
      const isLive=!!liveSessionId&&seg.session_id===liveSessionId&&seg.end_ms==null;
      const rawEnd=Number(seg.end_ms||(isLive?Date.now():seg.last_seen_ms)||rawStart);
      if(rawEnd<=rawStart)return;
      const a=Math.max(rawStart,clipStart==null?rawStart:clipStart);
      const b=Math.min(rawEnd,clipEnd==null?rawEnd:clipEnd);
      if(b<=a)return;
      const sec=(b-a)/1000;
      let sk=Number(seg.start_km)||0;
      let ek=Number(seg.end_km!=null?seg.end_km:(isLive&&S?.gps?S.gps.km:seg.last_km))||sk;
      const frac=(b-a)/(rawEnd-rawStart);
      const km=Math.max(0,(ek-sk)*frac);
      out[key].sec+=sec; out[key].km+=km;
    });
    const tracked=out.online.sec+out.buscando.sec+out.corrida.sec;
    const trackedKm=out.online.km+out.buscando.km+out.corrida.km;
    if(out.total<=0) out.total=tracked;
    else {
      out.online.sec+=Math.max(0,out.total-tracked);
      out.online.km+=Math.max(0,(Number(kmTotal)||0)-trackedKm);
    }
    const pct=v=>out.total>0?Math.round(Math.max(0,v)/out.total*100):0;
    out.online.pct=pct(out.online.sec);out.buscando.pct=pct(out.buscando.sec);out.corrida.pct=pct(out.corrida.sec);
    return out;
  }
  window.journeyBreakdownForSession = function(sess,elapsedSec,kmTotal,fallbackAuto){
    if(!sess?.id) return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
    ensureJourneyStatus(sess);
    const c=journeyStatusCache.get(journeyKey(sess));
    if(c&&c.rows&&c.rows.length) return statusBreakdownRows(c.rows,elapsedSec,kmTotal,null,null,(S?.gps?.st==='running'?S.gps.sessionId:null));
    // Jornada ativa nunca some enquanto a timeline chega: sem trechos ainda,
    // todo o tempo/km conhecido fica como Online e depois é redistribuído
    // quando Buscar/Corrida forem registrados.
    if(!c || c.loading) return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
    return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
  };

  // ── Jornadas que atravessam a data selecionada ────────────────────
  async function ensureOverlapSessions(dateStr){
    if(!_supaUser||!dateStr)return;
    const old=overlapSessionCache.get(dateStr);
    if(old&&(old.loading||Date.now()-old.ts<30000))return;
    overlapSessionCache.set(dateStr,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    const b=civilDayBounds(dateStr);
    try{
      const {data,error}=await _SUPA.from('sessions').select('*').eq('user_id',_supaUser.id)
        .lt('start_ms',b.endMs).or(`end_ms.is.null,end_ms.gte.${b.startMs}`).order('start_ms',{ascending:true});
      if(!error){
        const rows=data||[];
        overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows});
        // Hidratamos o cache local no DIA DE INÍCIO; nunca criamos uma segunda Jornada na virada.
        rows.filter(r=>r.end_ms).forEach(r=>{
          const ds=localYmdFromMs(r.start_ms), day=DB.day(ds); if(!day.sessions)day.sessions=[];
          const item={id:r.id,start:Number(r.start_ms),end:Number(r.end_ms),km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0};
          const i=day.sessions.findIndex(x=>x.id===item.id); if(i>=0)day.sessions[i]=item; else day.sessions.push(item); DB.saveDay(day);
        });
      }
    }catch(e){overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows:old?.rows||[]});}
    try{if(S.sec==='jornada')setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0);}catch(e){}
  }
  function overlappingJourneyDescriptors(dateStr){
    const b=civilDayBounds(dateStr), map=new Map();
    (DB.allDates?DB.allDates():[]).forEach(ds=>{
      const day=DB.day(ds);(day.sessions||[]).forEach((sess,idx)=>{
        if(!overlaps(sess.start,sess.end,b.startMs,b.endMs))return;
        map.set(String(sess.id||sess.start),{sess,sourceDate:ds,origIdx:idx});
      });
    });
    const remote=overlapSessionCache.get(dateStr)?.rows||[];
    remote.forEach(r=>{
      if(!overlaps(r.start_ms,r.end_ms,b.startMs,b.endMs))return;
      const id=String(r.id||r.start_ms);if(map.has(id))return;
      const ds=localYmdFromMs(r.start_ms), d=DB.day(ds), idx=(d.sessions||[]).findIndex(x=>x.id===r.id);
      map.set(id,{sess:{id:r.id,start:Number(r.start_ms),end:r.end_ms?Number(r.end_ms):null,km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0},sourceDate:ds,origIdx:idx});
    });
    if(S?.gps?.st==='running'&&S.gps.start&&overlaps(S.gps.start,null,b.startMs,b.endMs)){
      const id=String(S.gps.sessionId||S.gps.start);
      map.set(id,{sess:{id:S.gps.sessionId,start:S.gps.start,end:null,km:S.gps.km||0,elapsed:getElapsed()},sourceDate:localYmdFromMs(S.gps.start),origIdx:-1,live:true});
    }
    return [...map.values()].sort((a,b)=>(a.sess.start||0)-(b.sess.start||0));
  }
  function journeyLabel(desc,dateStr){
    const sess=desc.sess, src=desc.sourceDate||localYmdFromMs(sess.start);
    if(desc.live) return src===dateStr?'Jornada atual':'Jornada atual · continuação';
    const srcSessions=(DB.day(src).sessions||[]).slice().sort((a,b)=>(a.start||0)-(b.start||0));
    const pos=Math.max(0,srcSessions.findIndex(x=>x.id===sess.id));
    let label='Jornada '+(pos+1);
    const endDate=sess.end?localYmdFromMs(sess.end):src;
    if(src<dateStr) label+=' · continuação'; else if(endDate>dateStr) label+=' · continua';
    return label;
  }
  function renderContinuousSessionList(){
    const el=document.getElementById('sessions-list'), toggle=document.getElementById('sessions-toggle');
    if(!el||!S?.curDate)return;
    ensureOverlapSessions(S.curDate);
    const desc=overlappingJourneyDescriptors(S.curDate).sort((a,b)=>(b.sess.start||0)-(a.sess.start||0));
    const shown=S.sessionsExpanded?desc:desc.slice(0,3);
    const html=shown.map(x=>{
      ensureJourneyAutoTrips(x.sess);ensureJourneyStatus(x.sess);
      const synthetic={date:x.sourceDate,trips:journeyManualTrips(x.sess),autoTrips:journeyAutoTrips(x.sess,DB.day(x.sourceDate).autoTrips||[])};
      return sessionItem(journeyLabel(x,S.curDate),x.sess,x.origIdx,x.sourceDate,synthetic,!!x.live);
    });
    el.innerHTML=html.length?html.join(''):'<div class="empty">Nenhuma sessão registrada</div>';
    if(toggle){toggle.style.display=desc.length>3?'':'none';toggle.innerHTML=S.sessionsExpanded?'Ver menos':'Ver todas &#8250;';}
  }

  // Link de corrida ↔ Jornada agora procura TODAS as sessões; uma corrida de 01:30
  // pode pertencer à Jornada iniciada no dia anterior.
  window.isAutoTripLinkedToSession = function(t){
    const ts=autoTripTs(t);if(!ts)return false;
    for(const ds of (DB.allDates?DB.allDates():[])){
      if((DB.day(ds).sessions||[]).some(sess=>ts>=(sess.start||0)&&(!sess.end||ts<=sess.end)))return true;
    }
    return !!(S?.gps?.st==='running'&&S.gps.start&&ts>=S.gps.start);
  };

  function civilJourneyTotals(dateStr){
    const b=civilDayBounds(dateStr), seen=new Set();
    let elapsed=0, km=0;
    const add=sess=>{
      const start=Number(sess?.start||0); if(!start)return;
      const live=!!(S?.gps?.st==='running' && S.gps.sessionId && sess?.id===S.gps.sessionId);
      let end=Number(sess?.end||0);
      const recordedElapsed=Math.max(0,Number(sess?.elapsed)||0);
      if(!end) end=live?Date.now():(recordedElapsed>0?start+recordedElapsed*1000:0);
      if(!end||end<=start)return;
      const a=Math.max(start,b.startMs), z=Math.min(end,b.endMs); if(z<=a)return;
      const key=String(sess?.id||start+'_'+end); if(seen.has(key))return; seen.add(key);
      const wall=Math.max(1,end-start), frac=(z-a)/wall;
      const baseElapsed=recordedElapsed>0?recordedElapsed:wall/1000;
      elapsed+=Math.max(0,baseElapsed*frac);
      km+=Math.max(0,Number(sess?.km)||0)*frac;
    };
    try{(DB.allDates?DB.allDates():[]).forEach(ds=>(DB.day(ds).sessions||[]).forEach(add));}catch(e){}
    if(S?.gps?.st==='running'&&S.gps.start){
      add({id:S.gps.sessionId,start:S.gps.start,end:null,elapsed:getElapsed(),km:S.gps.km||0});
    }
    try{
      const day=DB.day(dateStr);
      elapsed=Math.max(0,elapsed+(Number(day.tempoAdjust)||0));
      km=Math.max(0,km+(Number(day.kmAdjust)||0));
    }catch(e){}
    return {elapsed,km};
  }

  // ── Barra Total do dia = corte CIVIL virtual da timeline ───────────
  async function ensureCivilStatus(dateStr){
    if(!_supaUser||!dateStr)return;
    const old=civilStatusCache.get(dateStr);if(old&&(old.loading||Date.now()-old.ts<8000))return;
    civilStatusCache.set(dateStr,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    const b=civilDayBounds(dateStr);
    try{
      const {data,error}=await _SUPA.from('journey_status_segments').select('*').eq('user_id',_supaUser.id)
        .lt('start_ms',b.endMs).or(`end_ms.is.null,end_ms.gte.${b.startMs}`).order('start_ms',{ascending:true});
      if(!error)civilStatusCache.set(dateStr,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){civilStatusCache.set(dateStr,{loading:false,ts:Date.now(),rows:old?.rows||[]});}
    try{if(S.sec==='jornada')setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0);}catch(e){}
  }
  function renderContinuousStatusBars(){
    if(!S?.curDate)return;
    ensureCivilStatus(S.curDate);
    const c=civilStatusCache.get(S.curDate), rows=c?.rows||[];
    const tdRow=document.getElementById('td-bd-row');
    const totals=civilJourneyTotals(S.curDate);
    if(rows.length){
      const b=civilDayBounds(S.curDate), bd=statusBreakdownRows(rows,totals.elapsed,totals.km,b.startMs,b.endMs,(S?.gps?.st==='running'?S.gps.sessionId:null));
      if(tdRow)tdRow.style.display='';
      try{setHTML('td-bd-bar',buildSbarHtml(bd));setHTML('td-bd-list',buildSrowsHtml(bd));}catch(e){}
    } else if(c && !c.loading) {
      const day=DB.day(S.curDate);
      const autos=(day.autoTrips&&day.autoTrips.length)?day.autoTrips:((S._autoTripsDataDate===S.curDate&&Array.isArray(S.autoTrips))?S.autoTrips:[]);
      const bd=computeStatusBreakdown(totals.elapsed,totals.km,autos);
      const hasJourney=totals.elapsed>0||totals.km>0||autos.length>0;
      if(tdRow)tdRow.style.display=hasJourney?'':'none';
      if(hasJourney){try{setHTML('td-bd-bar',buildSbarHtml(bd));setHTML('td-bd-list',buildSrowsHtml(bd));}catch(e){}}
    }

    const jcRow=document.getElementById('jc-breakdown');
    if(S?.gps?.st==='running'&&S.gps.sessionId){
      const sess={id:S.gps.sessionId,start:S.gps.start,end:null,km:S.gps.km||0,elapsed:getElapsed()};
      // Garante o vínculo com JourneyStatusTracker mesmo que o renderizador
      // legado esteja desativado. Isso faz a timeline nativa começar a gravar.
      try{ if(typeof window.ensureNativeJourneySession==='function') window.ensureNativeJourneySession(); }catch(e){}
      ensureJourneyStatus(sess);
      const bd=journeyBreakdownForSession(sess,getElapsed(),S.gps.km||0,[]);
      // Enquanto não existe trecho detalhado, o fallback acima mostra 100%
      // Online. Jornada ativa nunca deve esconder a barra.
      if(jcRow)jcRow.style.display='';
      try{setHTML('jc-bar',buildSbarHtml(bd));setHTML('jc-blist',buildSrowsHtml(bd));}catch(e){}
    } else if(jcRow) {
      jcRow.style.display='none';
    }
  }

  const previousRenderJornada=window.renderJornada;
  window.renderJornada=function(){
    previousRenderJornada();
    try{renderContinuousSessionList();}catch(e){console.warn('[continuous journeys]',e);}
    try{renderContinuousStatusBars();}catch(e){console.warn('[continuous status]',e);}
  };

  setInterval(()=>{try{if(S.sec==='jornada')renderContinuousStatusBars();}catch(e){}},1000);
  setTimeout(()=>{try{if(S.sec==='jornada')renderJornada();}catch(e){}},600);
})();
</script>

</body>
</html>
===== sessions @ 1763506 =====
ue||0); if(seen.has(id))return; seen.add(id);
        out.push(Object.assign({},t,{ts,_ts:ts}));
      });
    });
    return out;
  }

  // ── Timeline real Online/Buscar/Corrida por Jornada ────────────────
  async function ensureJourneyStatus(sess){
    if(!_supaUser || !sess?.id) return;
    const k=journeyKey(sess), old=journeyStatusCache.get(k), ttl=sess.end?120000:8000;
    if(old && (old.loading || Date.now()-old.ts<ttl)) return;
    journeyStatusCache.set(k,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    try{
      const {data,error}=await _SUPA.from('journey_status_segments').select('*')
        .eq('user_id',_supaUser.id).eq('session_id',sess.id).order('start_ms',{ascending:true});
      if(!error) journeyStatusCache.set(k,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){ journeyStatusCache.set(k,{loading:false,ts:Date.now(),rows:old?.rows||[]}); }
    try{ if(S.sec==='jornada') setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0); }catch(e){}
  }
  function statusBreakdownRows(rows,elapsedSec,kmTotal,clipStart,clipEnd,liveSessionId){
    const out={total:Math.max(0,Number(elapsedSec)||0),online:{sec:0,km:0,pct:0},buscando:{sec:0,km:0,pct:0},corrida:{sec:0,km:0,pct:0}};
    (rows||[]).forEach(seg=>{
      const key=seg.status==='buscar'?'buscando':seg.status; if(!out[key])return;
      const rawStart=Number(seg.start_ms)||0;
      const isLive=!!liveSessionId&&seg.session_id===liveSessionId&&seg.end_ms==null;
      const rawEnd=Number(seg.end_ms||(isLive?Date.now():seg.last_seen_ms)||rawStart);
      if(rawEnd<=rawStart)return;
      const a=Math.max(rawStart,clipStart==null?rawStart:clipStart);
      const b=Math.min(rawEnd,clipEnd==null?rawEnd:clipEnd);
      if(b<=a)return;
      const sec=(b-a)/1000;
      let sk=Number(seg.start_km)||0;
      let ek=Number(seg.end_km!=null?seg.end_km:(isLive&&S?.gps?S.gps.km:seg.last_km))||sk;
      const frac=(b-a)/(rawEnd-rawStart);
      const km=Math.max(0,(ek-sk)*frac);
      out[key].sec+=sec; out[key].km+=km;
    });
    const tracked=out.online.sec+out.buscando.sec+out.corrida.sec;
    const trackedKm=out.online.km+out.buscando.km+out.corrida.km;
    if(out.total<=0) out.total=tracked;
    else {
      out.online.sec+=Math.max(0,out.total-tracked);
      out.online.km+=Math.max(0,(Number(kmTotal)||0)-trackedKm);
    }
    const pct=v=>out.total>0?Math.round(Math.max(0,v)/out.total*100):0;
    out.online.pct=pct(out.online.sec);out.buscando.pct=pct(out.buscando.sec);out.corrida.pct=pct(out.corrida.sec);
    return out;
  }
  window.journeyBreakdownForSession = function(sess,elapsedSec,kmTotal,fallbackAuto){
    if(!sess?.id) return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
    ensureJourneyStatus(sess);
    const c=journeyStatusCache.get(journeyKey(sess));
    if(c&&c.rows&&c.rows.length) return statusBreakdownRows(c.rows,elapsedSec,kmTotal,null,null,(S?.gps?.st==='running'?S.gps.sessionId:null));
    // Jornada ativa nunca some enquanto a timeline chega: sem trechos ainda,
    // todo o tempo/km conhecido fica como Online e depois é redistribuído
    // quando Buscar/Corrida forem registrados.
    if(!c || c.loading) return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
    return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
  };

  // ── Jornadas que atravessam a data selecionada ────────────────────
  async function ensureOverlapSessions(dateStr){
    if(!_supaUser||!dateStr)return;
    const old=overlapSessionCache.get(dateStr);
    if(old&&(old.loading||Date.now()-old.ts<30000))return;
    overlapSessionCache.set(dateStr,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    const b=civilDayBounds(dateStr);
    try{
      const {data,error}=await _SUPA.from('sessions').select('*').eq('user_id',_supaUser.id)
        .lt('start_ms',b.endMs).or(`end_ms.is.null,end_ms.gte.${b.startMs}`).order('start_ms',{ascending:true});
      if(!error){
        const rows=data||[];
        overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows});
        // Hidratamos o cache local no DIA DE INÍCIO; nunca criamos uma segunda Jornada na virada.
        rows.filter(r=>r.end_ms).forEach(r=>{
          const ds=localYmdFromMs(r.start_ms), day=DB.day(ds); if(!day.sessions)day.sessions=[];
          const item={id:r.id,start:Number(r.start_ms),end:Number(r.end_ms),km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0};
          const i=day.sessions.findIndex(x=>x.id===item.id); if(i>=0)day.sessions[i]=item; else day.sessions.push(item); DB.saveDay(day);
        });
      }
    }catch(e){overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows:old?.rows||[]});}
    try{if(S.sec==='jornada')setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0);}catch(e){}
  }
  function overlappingJourneyDescriptors(dateStr){
    const b=civilDayBounds(dateStr), map=new Map();
    (DB.allDates?DB.allDates():[]).forEach(ds=>{
      const day=DB.day(ds);(day.sessions||[]).forEach((sess,idx)=>{
        if(!overlaps(sess.start,sess.end,b.startMs,b.endMs))return;
        map.set(String(sess.id||sess.start),{sess,sourceDate:ds,origIdx:idx});
      });
    });
    const remote=overlapSessionCache.get(dateStr)?.rows||[];
    remote.forEach(r=>{
      if(!overlaps(r.start_ms,r.end_ms,b.startMs,b.endMs))return;
      const id=String(r.id||r.start_ms);if(map.has(id))return;
      const ds=localYmdFromMs(r.start_ms), d=DB.day(ds), idx=(d.sessions||[]).findIndex(x=>x.id===r.id);
      map.set(id,{sess:{id:r.id,start:Number(r.start_ms),end:r.end_ms?Number(r.end_ms):null,km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0},sourceDate:ds,origIdx:idx});
    });
    if(S?.gps?.st==='running'&&S.gps.start&&overlaps(S.gps.start,null,b.startMs,b.endMs)){
      const id=String(S.gps.sessionId||S.gps.start);
      map.set(id,{sess:{id:S.gps.sessionId,start:S.gps.start,end:null,km:S.gps.km||0,elapsed:getElapsed()},sourceDate:localYmdFromMs(S.gps.start),origIdx:-1,live:true});
    }
    return [...map.values()].sort((a,b)=>(a.sess.start||0)-(b.sess.start||0));
  }
  function journeyLabel(desc,dateStr){
    const sess=desc.sess, src=desc.sourceDate||localYmdFromMs(sess.start);
    if(desc.live) return src===dateStr?'Jornada atual':'Jornada atual · continuação';
    const srcSessions=(DB.day(src).sessions||[]).slice().sort((a,b)=>(a.start||0)-(b.start||0));
    const pos=Math.max(0,srcSessions.findIndex(x=>x.id===sess.id));
    let label='Jornada '+(pos+1);
    const endDate=sess.end?localYmdFromMs(sess.end):src;
    if(src<dateStr) label+=' · continuação'; else if(endDate>dateStr) label+=' · continua';
    return label;
  }
  function renderContinuousSessionList(){
    const el=document.getElementById('sessions-list'), toggle=document.getElementById('sessions-toggle');
    if(!el||!S?.curDate)return;
    ensureOverlapSessions(S.curDate);
    const desc=overlappingJourneyDescriptors(S.curDate).sort((a,b)=>(b.sess.start||0)-(a.sess.start||0));
    const shown=S.sessionsExpanded?desc:desc.slice(0,3);
    const html=shown.map(x=>{
      ensureJourneyAutoTrips(x.sess);ensureJourneyStatus(x.sess);
      const synthetic={date:x.sourceDate,trips:journeyManualTrips(x.sess),autoTrips:journeyAutoTrips(x.sess,DB.day(x.sourceDate).autoTrips||[])};
      return sessionItem(journeyLabel(x,S.curDate),x.sess,x.origIdx,x.sourceDate,synthetic,!!x.live);
    });
    el.innerHTML=html.length?html.join(''):'<div class="empty">Nenhuma sessão registrada</div>';
    if(toggle){toggle.style.display=desc.length>3?'':'none';toggle.innerHTML=S.sessionsExpanded?'Ver menos':'Ver todas &#8250;';}
  }

  // Link de corrida ↔ Jornada agora procura TODAS as sessões; uma corrida de 01:30
  // pode pertencer à Jornada iniciada no dia anterior.
  window.isAutoTripLinkedToSession = function(t){
    const ts=autoTripTs(t);if(!ts)return false;
    for(const ds of (DB.allDates?DB.allDates():[])){
      if((DB.day(ds).sessions||[]).some(sess=>ts>=(sess.start||0)&&(!sess.end||ts<=sess.end)))return true;
    }
    return !!(S?.gps?.st==='running'&&S.gps.start&&ts>=S.gps.start);
  };

  function civilJourneyTotals(dateStr){
    const b=civilDayBounds(dateStr), seen=new Set();
    let elapsed=0, km=0;
    const add=sess=>{
      const start=Number(sess?.start||0); if(!start)return;
      const live=!!(S?.gps?.st==='running' && S.gps.sessionId && sess?.id===S.gps.sessionId);
      let end=Number(sess?.end||0);
      const recordedElapsed=Math.max(0,Number(sess?.elapsed)||0);
      if(!end) end=live?Date.now():(recordedElapsed>0?start+recordedElapsed*1000:0);
      if(!end||end<=start)return;
      const a=Math.max(start,b.startMs), z=Math.min(end,b.endMs); if(z<=a)return;
      const key=String(sess?.id||start+'_'+end); if(seen.has(key))return; seen.add(key);
      const wall=Math.max(1,end-start), frac=(z-a)/wall;
      const baseElapsed=recordedElapsed>0?recordedElapsed:wall/1000;
      elapsed+=Math.max(0,baseElapsed*frac);
      km+=Math.max(0,Number(sess?.km)||0)*frac;
    };
    try{(DB.allDates?DB.allDates():[]).forEach(ds=>(DB.day(ds).sessions||[]).forEach(add));}catch(e){}
    if(S?.gps?.st==='running'&&S.gps.start){
      add({id:S.gps.sessionId,start:S.gps.start,end:null,elapsed:getElapsed(),km:S.gps.km||0});
    }
    try{
      const day=DB.day(dateStr);
      elapsed=Math.max(0,elapsed+(Number(day.tempoAdjust)||0));
      km=Math.max(0,km+(Number(day.kmAdjust)||0));
    }catch(e){}
    return {elapsed,km};
  }

  // ── Barra Total do dia = corte CIVIL virtual da timeline ───────────
  async function ensureCivilStatus(dateStr){
    if(!_supaUser||!dateStr)return;
    const old=civilStatusCache.get(dateStr);if(old&&(old.loading||Date.now()-old.ts<8000))return;
    civilStatusCache.set(dateStr,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    const b=civilDayBounds(dateStr);
    try{
      const {data,error}=await _SUPA.from('journey_status_segments').select('*').eq('user_id',_supaUser.id)
        .lt('start_ms',b.endMs).or(`end_ms.is.null,end_ms.gte.${b.startMs}`).order('start_ms',{ascending:true});
      if(!error)civilStatusCache.set(dateStr,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){civilStatusCache.set(dateStr,{loading:false,ts:Date.now(),rows:old?.rows||[]});}
    try{if(S.sec==='jornada')setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0);}catch(e){}
  }
  function renderContinuousStatusBars(){
    if(!S?.curDate)return;
    ensureCivilStatus(S.curDate);
    const c=civilStatusCache.get(S.curDate), rows=c?.rows||[];
    const tdRow=document.getElementById('td-bd-row');
    const totals=civilJourneyTotals(S.curDate);
    if(rows.length){
      const b=civilDayBounds(S.curDate), bd=statusBreakdownRows(rows,totals.elapsed,totals.km,b.startMs,b.endMs,(S?.gps?.st==='running'?S.gps.sessionId:null));
      if(tdRow)tdRow.style.display='';
      try{setHTML('td-bd-bar',buildSbarHtml(bd));setHTML('td-bd-list',buildSrowsHtml(bd));}catch(e){}
    } else if(c && !c.loading) {
      const day=DB.day(S.curDate);
      const autos=(day.autoTrips&&day.autoTrips.length)?day.autoTrips:((S._autoTripsDataDate===S.curDate&&Array.isArray(S.autoTrips))?S.autoTrips:[]);
      const bd=computeStatusBreakdown(totals.elapsed,totals.km,autos);
      const hasJourney=totals.elapsed>0||totals.km>0||autos.length>0;
      if(tdRow)tdRow.style.display=hasJourney?'':'none';
      if(hasJourney){try{setHTML('td-bd-bar',buildSbarHtml(bd));setHTML('td-bd-list',buildSrowsHtml(bd));}catch(e){}}
    }

    const jcRow=document.getElementById('jc-breakdown');
    if(S?.gps?.st==='running'&&S.gps.sessionId){
      const sess={id:S.gps.sessionId,start:S.gps.start,end:null,km:S.gps.km||0,elapsed:getElapsed()};
      // Garante o vínculo com JourneyStatusTracker mesmo que o renderizador
      // legado esteja desativado. Isso faz a timeline nativa começar a gravar.
      try{ if(typeof window.ensureNativeJourneySession==='function') window.ensureNativeJourneySession(); }catch(e){}
      ensureJourneyStatus(sess);
      const bd=journeyBreakdownForSession(sess,getElapsed(),S.gps.km||0,[]);
      // Enquanto não existe trecho detalhado, o fallback acima mostra 100%
      // Online. Jornada ativa nunca deve esconder a barra.
      if(jcRow)jcRow.style.display='';
      try{setHTML('jc-bar',buildSbarHtml(bd));setHTML('jc-blist',buildSrowsHtml(bd));}catch(e){}
    } else if(jcRow) {
      jcRow.style.display='none';
    }
  }

  const previousRenderJornada=window.renderJornada;
  window.renderJornada=function(){
    previousRenderJornada();
    try{renderContinuousSessionList();}catch(e){console.warn('[continuous journeys]',e);}
    try{renderContinuousStatusBars();}catch(e){console.warn('[continuous status]',e);}
  };

  setInterval(()=>{try{if(S.sec==='jornada')renderContinuousStatusBars();}catch(e){}},1000);
  setTimeout(()=>{try{if(S.sec==='jornada')renderJornada();}catch(e){}},600);
})();
</script>

</body>
</html>
===== sessions @ 1764821 =====
scando':seg.status; if(!out[key])return;
      const rawStart=Number(seg.start_ms)||0;
      const isLive=!!liveSessionId&&seg.session_id===liveSessionId&&seg.end_ms==null;
      const rawEnd=Number(seg.end_ms||(isLive?Date.now():seg.last_seen_ms)||rawStart);
      if(rawEnd<=rawStart)return;
      const a=Math.max(rawStart,clipStart==null?rawStart:clipStart);
      const b=Math.min(rawEnd,clipEnd==null?rawEnd:clipEnd);
      if(b<=a)return;
      const sec=(b-a)/1000;
      let sk=Number(seg.start_km)||0;
      let ek=Number(seg.end_km!=null?seg.end_km:(isLive&&S?.gps?S.gps.km:seg.last_km))||sk;
      const frac=(b-a)/(rawEnd-rawStart);
      const km=Math.max(0,(ek-sk)*frac);
      out[key].sec+=sec; out[key].km+=km;
    });
    const tracked=out.online.sec+out.buscando.sec+out.corrida.sec;
    const trackedKm=out.online.km+out.buscando.km+out.corrida.km;
    if(out.total<=0) out.total=tracked;
    else {
      out.online.sec+=Math.max(0,out.total-tracked);
      out.online.km+=Math.max(0,(Number(kmTotal)||0)-trackedKm);
    }
    const pct=v=>out.total>0?Math.round(Math.max(0,v)/out.total*100):0;
    out.online.pct=pct(out.online.sec);out.buscando.pct=pct(out.buscando.sec);out.corrida.pct=pct(out.corrida.sec);
    return out;
  }
  window.journeyBreakdownForSession = function(sess,elapsedSec,kmTotal,fallbackAuto){
    if(!sess?.id) return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
    ensureJourneyStatus(sess);
    const c=journeyStatusCache.get(journeyKey(sess));
    if(c&&c.rows&&c.rows.length) return statusBreakdownRows(c.rows,elapsedSec,kmTotal,null,null,(S?.gps?.st==='running'?S.gps.sessionId:null));
    // Jornada ativa nunca some enquanto a timeline chega: sem trechos ainda,
    // todo o tempo/km conhecido fica como Online e depois é redistribuído
    // quando Buscar/Corrida forem registrados.
    if(!c || c.loading) return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
    return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
  };

  // ── Jornadas que atravessam a data selecionada ────────────────────
  async function ensureOverlapSessions(dateStr){
    if(!_supaUser||!dateStr)return;
    const old=overlapSessionCache.get(dateStr);
    if(old&&(old.loading||Date.now()-old.ts<30000))return;
    overlapSessionCache.set(dateStr,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    const b=civilDayBounds(dateStr);
    try{
      const {data,error}=await _SUPA.from('sessions').select('*').eq('user_id',_supaUser.id)
        .lt('start_ms',b.endMs).or(`end_ms.is.null,end_ms.gte.${b.startMs}`).order('start_ms',{ascending:true});
      if(!error){
        const rows=data||[];
        overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows});
        // Hidratamos o cache local no DIA DE INÍCIO; nunca criamos uma segunda Jornada na virada.
        rows.filter(r=>r.end_ms).forEach(r=>{
          const ds=localYmdFromMs(r.start_ms), day=DB.day(ds); if(!day.sessions)day.sessions=[];
          const item={id:r.id,start:Number(r.start_ms),end:Number(r.end_ms),km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0};
          const i=day.sessions.findIndex(x=>x.id===item.id); if(i>=0)day.sessions[i]=item; else day.sessions.push(item); DB.saveDay(day);
        });
      }
    }catch(e){overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows:old?.rows||[]});}
    try{if(S.sec==='jornada')setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0);}catch(e){}
  }
  function overlappingJourneyDescriptors(dateStr){
    const b=civilDayBounds(dateStr), map=new Map();
    (DB.allDates?DB.allDates():[]).forEach(ds=>{
      const day=DB.day(ds);(day.sessions||[]).forEach((sess,idx)=>{
        if(!overlaps(sess.start,sess.end,b.startMs,b.endMs))return;
        map.set(String(sess.id||sess.start),{sess,sourceDate:ds,origIdx:idx});
      });
    });
    const remote=overlapSessionCache.get(dateStr)?.rows||[];
    remote.forEach(r=>{
      if(!overlaps(r.start_ms,r.end_ms,b.startMs,b.endMs))return;
      const id=String(r.id||r.start_ms);if(map.has(id))return;
      const ds=localYmdFromMs(r.start_ms), d=DB.day(ds), idx=(d.sessions||[]).findIndex(x=>x.id===r.id);
      map.set(id,{sess:{id:r.id,start:Number(r.start_ms),end:r.end_ms?Number(r.end_ms):null,km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0},sourceDate:ds,origIdx:idx});
    });
    if(S?.gps?.st==='running'&&S.gps.start&&overlaps(S.gps.start,null,b.startMs,b.endMs)){
      const id=String(S.gps.sessionId||S.gps.start);
      map.set(id,{sess:{id:S.gps.sessionId,start:S.gps.start,end:null,km:S.gps.km||0,elapsed:getElapsed()},sourceDate:localYmdFromMs(S.gps.start),origIdx:-1,live:true});
    }
    return [...map.values()].sort((a,b)=>(a.sess.start||0)-(b.sess.start||0));
  }
  function journeyLabel(desc,dateStr){
    const sess=desc.sess, src=desc.sourceDate||localYmdFromMs(sess.start);
    if(desc.live) return src===dateStr?'Jornada atual':'Jornada atual · continuação';
    const srcSessions=(DB.day(src).sessions||[]).slice().sort((a,b)=>(a.start||0)-(b.start||0));
    const pos=Math.max(0,srcSessions.findIndex(x=>x.id===sess.id));
    let label='Jornada '+(pos+1);
    const endDate=sess.end?localYmdFromMs(sess.end):src;
    if(src<dateStr) label+=' · continuação'; else if(endDate>dateStr) label+=' · continua';
    return label;
  }
  function renderContinuousSessionList(){
    const el=document.getElementById('sessions-list'), toggle=document.getElementById('sessions-toggle');
    if(!el||!S?.curDate)return;
    ensureOverlapSessions(S.curDate);
    const desc=overlappingJourneyDescriptors(S.curDate).sort((a,b)=>(b.sess.start||0)-(a.sess.start||0));
    const shown=S.sessionsExpanded?desc:desc.slice(0,3);
    const html=shown.map(x=>{
      ensureJourneyAutoTrips(x.sess);ensureJourneyStatus(x.sess);
      const synthetic={date:x.sourceDate,trips:journeyManualTrips(x.sess),autoTrips:journeyAutoTrips(x.sess,DB.day(x.sourceDate).autoTrips||[])};
      return sessionItem(journeyLabel(x,S.curDate),x.sess,x.origIdx,x.sourceDate,synthetic,!!x.live);
    });
    el.innerHTML=html.length?html.join(''):'<div class="empty">Nenhuma sessão registrada</div>';
    if(toggle){toggle.style.display=desc.length>3?'':'none';toggle.innerHTML=S.sessionsExpanded?'Ver menos':'Ver todas &#8250;';}
  }

  // Link de corrida ↔ Jornada agora procura TODAS as sessões; uma corrida de 01:30
  // pode pertencer à Jornada iniciada no dia anterior.
  window.isAutoTripLinkedToSession = function(t){
    const ts=autoTripTs(t);if(!ts)return false;
    for(const ds of (DB.allDates?DB.allDates():[])){
      if((DB.day(ds).sessions||[]).some(sess=>ts>=(sess.start||0)&&(!sess.end||ts<=sess.end)))return true;
    }
    return !!(S?.gps?.st==='running'&&S.gps.start&&ts>=S.gps.start);
  };

  function civilJourneyTotals(dateStr){
    const b=civilDayBounds(dateStr), seen=new Set();
    let elapsed=0, km=0;
    const add=sess=>{
      const start=Number(sess?.start||0); if(!start)return;
      const live=!!(S?.gps?.st==='running' && S.gps.sessionId && sess?.id===S.gps.sessionId);
      let end=Number(sess?.end||0);
      const recordedElapsed=Math.max(0,Number(sess?.elapsed)||0);
      if(!end) end=live?Date.now():(recordedElapsed>0?start+recordedElapsed*1000:0);
      if(!end||end<=start)return;
      const a=Math.max(start,b.startMs), z=Math.min(end,b.endMs); if(z<=a)return;
      const key=String(sess?.id||start+'_'+end); if(seen.has(key))return; seen.add(key);
      const wall=Math.max(1,end-start), frac=(z-a)/wall;
      const baseElapsed=recordedElapsed>0?recordedElapsed:wall/1000;
      elapsed+=Math.max(0,baseElapsed*frac);
      km+=Math.max(0,Number(sess?.km)||0)*frac;
    };
    try{(DB.allDates?DB.allDates():[]).forEach(ds=>(DB.day(ds).sessions||[]).forEach(add));}catch(e){}
    if(S?.gps?.st==='running'&&S.gps.start){
      add({id:S.gps.sessionId,start:S.gps.start,end:null,elapsed:getElapsed(),km:S.gps.km||0});
    }
    try{
      const day=DB.day(dateStr);
      elapsed=Math.max(0,elapsed+(Number(day.tempoAdjust)||0));
      km=Math.max(0,km+(Number(day.kmAdjust)||0));
    }catch(e){}
    return {elapsed,km};
  }

  // ── Barra Total do dia = corte CIVIL virtual da timeline ───────────
  async function ensureCivilStatus(dateStr){
    if(!_supaUser||!dateStr)return;
    const old=civilStatusCache.get(dateStr);if(old&&(old.loading||Date.now()-old.ts<8000))return;
    civilStatusCache.set(dateStr,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    const b=civilDayBounds(dateStr);
    try{
      const {data,error}=await _SUPA.from('journey_status_segments').select('*').eq('user_id',_supaUser.id)
        .lt('start_ms',b.endMs).or(`end_ms.is.null,end_ms.gte.${b.startMs}`).order('start_ms',{ascending:true});
      if(!error)civilStatusCache.set(dateStr,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){civilStatusCache.set(dateStr,{loading:false,ts:Date.now(),rows:old?.rows||[]});}
    try{if(S.sec==='jornada')setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0);}catch(e){}
  }
  function renderContinuousStatusBars(){
    if(!S?.curDate)return;
    ensureCivilStatus(S.curDate);
    const c=civilStatusCache.get(S.curDate), rows=c?.rows||[];
    const tdRow=document.getElementById('td-bd-row');
    const totals=civilJourneyTotals(S.curDate);
    if(rows.length){
      const b=civilDayBounds(S.curDate), bd=statusBreakdownRows(rows,totals.elapsed,totals.km,b.startMs,b.endMs,(S?.gps?.st==='running'?S.gps.sessionId:null));
      if(tdRow)tdRow.style.display='';
      try{setHTML('td-bd-bar',buildSbarHtml(bd));setHTML('td-bd-list',buildSrowsHtml(bd));}catch(e){}
    } else if(c && !c.loading) {
      const day=DB.day(S.curDate);
      const autos=(day.autoTrips&&day.autoTrips.length)?day.autoTrips:((S._autoTripsDataDate===S.curDate&&Array.isArray(S.autoTrips))?S.autoTrips:[]);
      const bd=computeStatusBreakdown(totals.elapsed,totals.km,autos);
      const hasJourney=totals.elapsed>0||totals.km>0||autos.length>0;
      if(tdRow)tdRow.style.display=hasJourney?'':'none';
      if(hasJourney){try{setHTML('td-bd-bar',buildSbarHtml(bd));setHTML('td-bd-list',buildSrowsHtml(bd));}catch(e){}}
    }

    const jcRow=document.getElementById('jc-breakdown');
    if(S?.gps?.st==='running'&&S.gps.sessionId){
      const sess={id:S.gps.sessionId,start:S.gps.start,end:null,km:S.gps.km||0,elapsed:getElapsed()};
      // Garante o vínculo com JourneyStatusTracker mesmo que o renderizador
      // legado esteja desativado. Isso faz a timeline nativa começar a gravar.
      try{ if(typeof window.ensureNativeJourneySession==='function') window.ensureNativeJourneySession(); }catch(e){}
      ensureJourneyStatus(sess);
      const bd=journeyBreakdownForSession(sess,getElapsed(),S.gps.km||0,[]);
      // Enquanto não existe trecho detalhado, o fallback acima mostra 100%
      // Online. Jornada ativa nunca deve esconder a barra.
      if(jcRow)jcRow.style.display='';
      try{setHTML('jc-bar',buildSbarHtml(bd));setHTML('jc-blist',buildSrowsHtml(bd));}catch(e){}
    } else if(jcRow) {
      jcRow.style.display='none';
    }
  }

  const previousRenderJornada=window.renderJornada;
  window.renderJornada=function(){
    previousRenderJornada();
    try{renderContinuousSessionList();}catch(e){console.warn('[continuous journeys]',e);}
    try{renderContinuousStatusBars();}catch(e){console.warn('[continuous status]',e);}
  };

  setInterval(()=>{try{if(S.sec==='jornada')renderContinuousStatusBars();}catch(e){}},1000);
  setTimeout(()=>{try{if(S.sec==='jornada')renderJornada();}catch(e){}},600);
})();
</script>

</body>
</html>
===== sessions @ 1765520 =====
y].sec+=sec; out[key].km+=km;
    });
    const tracked=out.online.sec+out.buscando.sec+out.corrida.sec;
    const trackedKm=out.online.km+out.buscando.km+out.corrida.km;
    if(out.total<=0) out.total=tracked;
    else {
      out.online.sec+=Math.max(0,out.total-tracked);
      out.online.km+=Math.max(0,(Number(kmTotal)||0)-trackedKm);
    }
    const pct=v=>out.total>0?Math.round(Math.max(0,v)/out.total*100):0;
    out.online.pct=pct(out.online.sec);out.buscando.pct=pct(out.buscando.sec);out.corrida.pct=pct(out.corrida.sec);
    return out;
  }
  window.journeyBreakdownForSession = function(sess,elapsedSec,kmTotal,fallbackAuto){
    if(!sess?.id) return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
    ensureJourneyStatus(sess);
    const c=journeyStatusCache.get(journeyKey(sess));
    if(c&&c.rows&&c.rows.length) return statusBreakdownRows(c.rows,elapsedSec,kmTotal,null,null,(S?.gps?.st==='running'?S.gps.sessionId:null));
    // Jornada ativa nunca some enquanto a timeline chega: sem trechos ainda,
    // todo o tempo/km conhecido fica como Online e depois é redistribuído
    // quando Buscar/Corrida forem registrados.
    if(!c || c.loading) return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
    return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
  };

  // ── Jornadas que atravessam a data selecionada ────────────────────
  async function ensureOverlapSessions(dateStr){
    if(!_supaUser||!dateStr)return;
    const old=overlapSessionCache.get(dateStr);
    if(old&&(old.loading||Date.now()-old.ts<30000))return;
    overlapSessionCache.set(dateStr,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    const b=civilDayBounds(dateStr);
    try{
      const {data,error}=await _SUPA.from('sessions').select('*').eq('user_id',_supaUser.id)
        .lt('start_ms',b.endMs).or(`end_ms.is.null,end_ms.gte.${b.startMs}`).order('start_ms',{ascending:true});
      if(!error){
        const rows=data||[];
        overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows});
        // Hidratamos o cache local no DIA DE INÍCIO; nunca criamos uma segunda Jornada na virada.
        rows.filter(r=>r.end_ms).forEach(r=>{
          const ds=localYmdFromMs(r.start_ms), day=DB.day(ds); if(!day.sessions)day.sessions=[];
          const item={id:r.id,start:Number(r.start_ms),end:Number(r.end_ms),km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0};
          const i=day.sessions.findIndex(x=>x.id===item.id); if(i>=0)day.sessions[i]=item; else day.sessions.push(item); DB.saveDay(day);
        });
      }
    }catch(e){overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows:old?.rows||[]});}
    try{if(S.sec==='jornada')setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0);}catch(e){}
  }
  function overlappingJourneyDescriptors(dateStr){
    const b=civilDayBounds(dateStr), map=new Map();
    (DB.allDates?DB.allDates():[]).forEach(ds=>{
      const day=DB.day(ds);(day.sessions||[]).forEach((sess,idx)=>{
        if(!overlaps(sess.start,sess.end,b.startMs,b.endMs))return;
        map.set(String(sess.id||sess.start),{sess,sourceDate:ds,origIdx:idx});
      });
    });
    const remote=overlapSessionCache.get(dateStr)?.rows||[];
    remote.forEach(r=>{
      if(!overlaps(r.start_ms,r.end_ms,b.startMs,b.endMs))return;
      const id=String(r.id||r.start_ms);if(map.has(id))return;
      const ds=localYmdFromMs(r.start_ms), d=DB.day(ds), idx=(d.sessions||[]).findIndex(x=>x.id===r.id);
      map.set(id,{sess:{id:r.id,start:Number(r.start_ms),end:r.end_ms?Number(r.end_ms):null,km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0},sourceDate:ds,origIdx:idx});
    });
    if(S?.gps?.st==='running'&&S.gps.start&&overlaps(S.gps.start,null,b.startMs,b.endMs)){
      const id=String(S.gps.sessionId||S.gps.start);
      map.set(id,{sess:{id:S.gps.sessionId,start:S.gps.start,end:null,km:S.gps.km||0,elapsed:getElapsed()},sourceDate:localYmdFromMs(S.gps.start),origIdx:-1,live:true});
    }
    return [...map.values()].sort((a,b)=>(a.sess.start||0)-(b.sess.start||0));
  }
  function journeyLabel(desc,dateStr){
    const sess=desc.sess, src=desc.sourceDate||localYmdFromMs(sess.start);
    if(desc.live) return src===dateStr?'Jornada atual':'Jornada atual · continuação';
    const srcSessions=(DB.day(src).sessions||[]).slice().sort((a,b)=>(a.start||0)-(b.start||0));
    const pos=Math.max(0,srcSessions.findIndex(x=>x.id===sess.id));
    let label='Jornada '+(pos+1);
    const endDate=sess.end?localYmdFromMs(sess.end):src;
    if(src<dateStr) label+=' · continuação'; else if(endDate>dateStr) label+=' · continua';
    return label;
  }
  function renderContinuousSessionList(){
    const el=document.getElementById('sessions-list'), toggle=document.getElementById('sessions-toggle');
    if(!el||!S?.curDate)return;
    ensureOverlapSessions(S.curDate);
    const desc=overlappingJourneyDescriptors(S.curDate).sort((a,b)=>(b.sess.start||0)-(a.sess.start||0));
    const shown=S.sessionsExpanded?desc:desc.slice(0,3);
    const html=shown.map(x=>{
      ensureJourneyAutoTrips(x.sess);ensureJourneyStatus(x.sess);
      const synthetic={date:x.sourceDate,trips:journeyManualTrips(x.sess),autoTrips:journeyAutoTrips(x.sess,DB.day(x.sourceDate).autoTrips||[])};
      return sessionItem(journeyLabel(x,S.curDate),x.sess,x.origIdx,x.sourceDate,synthetic,!!x.live);
    });
    el.innerHTML=html.length?html.join(''):'<div class="empty">Nenhuma sessão registrada</div>';
    if(toggle){toggle.style.display=desc.length>3?'':'none';toggle.innerHTML=S.sessionsExpanded?'Ver menos':'Ver todas &#8250;';}
  }

  // Link de corrida ↔ Jornada agora procura TODAS as sessões; uma corrida de 01:30
  // pode pertencer à Jornada iniciada no dia anterior.
  window.isAutoTripLinkedToSession = function(t){
    const ts=autoTripTs(t);if(!ts)return false;
    for(const ds of (DB.allDates?DB.allDates():[])){
      if((DB.day(ds).sessions||[]).some(sess=>ts>=(sess.start||0)&&(!sess.end||ts<=sess.end)))return true;
    }
    return !!(S?.gps?.st==='running'&&S.gps.start&&ts>=S.gps.start);
  };

  function civilJourneyTotals(dateStr){
    const b=civilDayBounds(dateStr), seen=new Set();
    let elapsed=0, km=0;
    const add=sess=>{
      const start=Number(sess?.start||0); if(!start)return;
      const live=!!(S?.gps?.st==='running' && S.gps.sessionId && sess?.id===S.gps.sessionId);
      let end=Number(sess?.end||0);
      const recordedElapsed=Math.max(0,Number(sess?.elapsed)||0);
      if(!end) end=live?Date.now():(recordedElapsed>0?start+recordedElapsed*1000:0);
      if(!end||end<=start)return;
      const a=Math.max(start,b.startMs), z=Math.min(end,b.endMs); if(z<=a)return;
      const key=String(sess?.id||start+'_'+end); if(seen.has(key))return; seen.add(key);
      const wall=Math.max(1,end-start), frac=(z-a)/wall;
      const baseElapsed=recordedElapsed>0?recordedElapsed:wall/1000;
      elapsed+=Math.max(0,baseElapsed*frac);
      km+=Math.max(0,Number(sess?.km)||0)*frac;
    };
    try{(DB.allDates?DB.allDates():[]).forEach(ds=>(DB.day(ds).sessions||[]).forEach(add));}catch(e){}
    if(S?.gps?.st==='running'&&S.gps.start){
      add({id:S.gps.sessionId,start:S.gps.start,end:null,elapsed:getElapsed(),km:S.gps.km||0});
    }
    try{
      const day=DB.day(dateStr);
      elapsed=Math.max(0,elapsed+(Number(day.tempoAdjust)||0));
      km=Math.max(0,km+(Number(day.kmAdjust)||0));
    }catch(e){}
    return {elapsed,km};
  }

  // ── Barra Total do dia = corte CIVIL virtual da timeline ───────────
  async function ensureCivilStatus(dateStr){
    if(!_supaUser||!dateStr)return;
    const old=civilStatusCache.get(dateStr);if(old&&(old.loading||Date.now()-old.ts<8000))return;
    civilStatusCache.set(dateStr,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    const b=civilDayBounds(dateStr);
    try{
      const {data,error}=await _SUPA.from('journey_status_segments').select('*').eq('user_id',_supaUser.id)
        .lt('start_ms',b.endMs).or(`end_ms.is.null,end_ms.gte.${b.startMs}`).order('start_ms',{ascending:true});
      if(!error)civilStatusCache.set(dateStr,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){civilStatusCache.set(dateStr,{loading:false,ts:Date.now(),rows:old?.rows||[]});}
    try{if(S.sec==='jornada')setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0);}catch(e){}
  }
  function renderContinuousStatusBars(){
    if(!S?.curDate)return;
    ensureCivilStatus(S.curDate);
    const c=civilStatusCache.get(S.curDate), rows=c?.rows||[];
    const tdRow=document.getElementById('td-bd-row');
    const totals=civilJourneyTotals(S.curDate);
    if(rows.length){
      const b=civilDayBounds(S.curDate), bd=statusBreakdownRows(rows,totals.elapsed,totals.km,b.startMs,b.endMs,(S?.gps?.st==='running'?S.gps.sessionId:null));
      if(tdRow)tdRow.style.display='';
      try{setHTML('td-bd-bar',buildSbarHtml(bd));setHTML('td-bd-list',buildSrowsHtml(bd));}catch(e){}
    } else if(c && !c.loading) {
      const day=DB.day(S.curDate);
      const autos=(day.autoTrips&&day.autoTrips.length)?day.autoTrips:((S._autoTripsDataDate===S.curDate&&Array.isArray(S.autoTrips))?S.autoTrips:[]);
      const bd=computeStatusBreakdown(totals.elapsed,totals.km,autos);
      const hasJourney=totals.elapsed>0||totals.km>0||autos.length>0;
      if(tdRow)tdRow.style.display=hasJourney?'':'none';
      if(hasJourney){try{setHTML('td-bd-bar',buildSbarHtml(bd));setHTML('td-bd-list',buildSrowsHtml(bd));}catch(e){}}
    }

    const jcRow=document.getElementById('jc-breakdown');
    if(S?.gps?.st==='running'&&S.gps.sessionId){
      const sess={id:S.gps.sessionId,start:S.gps.start,end:null,km:S.gps.km||0,elapsed:getElapsed()};
      // Garante o vínculo com JourneyStatusTracker mesmo que o renderizador
      // legado esteja desativado. Isso faz a timeline nativa começar a gravar.
      try{ if(typeof window.ensureNativeJourneySession==='function') window.ensureNativeJourneySession(); }catch(e){}
      ensureJourneyStatus(sess);
      const bd=journeyBreakdownForSession(sess,getElapsed(),S.gps.km||0,[]);
      // Enquanto não existe trecho detalhado, o fallback acima mostra 100%
      // Online. Jornada ativa nunca deve esconder a barra.
      if(jcRow)jcRow.style.display='';
      try{setHTML('jc-bar',buildSbarHtml(bd));setHTML('jc-blist',buildSrowsHtml(bd));}catch(e){}
    } else if(jcRow) {
      jcRow.style.display='none';
    }
  }

  const previousRenderJornada=window.renderJornada;
  window.renderJornada=function(){
    previousRenderJornada();
    try{renderContinuousSessionList();}catch(e){console.warn('[continuous journeys]',e);}
    try{renderContinuousStatusBars();}catch(e){console.warn('[continuous status]',e);}
  };

  setInterval(()=>{try{if(S.sec==='jornada')renderContinuousStatusBars();}catch(e){}},1000);
  setTimeout(()=>{try{if(S.sec==='jornada')renderJornada();}catch(e){}},600);
})();
</script>

</body>
</html>
===== sessions @ 1766455 =====
s.sessionId:null));
    // Jornada ativa nunca some enquanto a timeline chega: sem trechos ainda,
    // todo o tempo/km conhecido fica como Online e depois é redistribuído
    // quando Buscar/Corrida forem registrados.
    if(!c || c.loading) return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
    return computeStatusBreakdown(elapsedSec,kmTotal,fallbackAuto||[]);
  };

  // ── Jornadas que atravessam a data selecionada ────────────────────
  async function ensureOverlapSessions(dateStr){
    if(!_supaUser||!dateStr)return;
    const old=overlapSessionCache.get(dateStr);
    if(old&&(old.loading||Date.now()-old.ts<30000))return;
    overlapSessionCache.set(dateStr,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    const b=civilDayBounds(dateStr);
    try{
      const {data,error}=await _SUPA.from('sessions').select('*').eq('user_id',_supaUser.id)
        .lt('start_ms',b.endMs).or(`end_ms.is.null,end_ms.gte.${b.startMs}`).order('start_ms',{ascending:true});
      if(!error){
        const rows=data||[];
        overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows});
        // Hidratamos o cache local no DIA DE INÍCIO; nunca criamos uma segunda Jornada na virada.
        rows.filter(r=>r.end_ms).forEach(r=>{
          const ds=localYmdFromMs(r.start_ms), day=DB.day(ds); if(!day.sessions)day.sessions=[];
          const item={id:r.id,start:Number(r.start_ms),end:Number(r.end_ms),km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0};
          const i=day.sessions.findIndex(x=>x.id===item.id); if(i>=0)day.sessions[i]=item; else day.sessions.push(item); DB.saveDay(day);
        });
      }
    }catch(e){overlapSessionCache.set(dateStr,{loading:false,ts:Date.now(),rows:old?.rows||[]});}
    try{if(S.sec==='jornada')setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0);}catch(e){}
  }
  function overlappingJourneyDescriptors(dateStr){
    const b=civilDayBounds(dateStr), map=new Map();
    (DB.allDates?DB.allDates():[]).forEach(ds=>{
      const day=DB.day(ds);(day.sessions||[]).forEach((sess,idx)=>{
        if(!overlaps(sess.start,sess.end,b.startMs,b.endMs))return;
        map.set(String(sess.id||sess.start),{sess,sourceDate:ds,origIdx:idx});
      });
    });
    const remote=overlapSessionCache.get(dateStr)?.rows||[];
    remote.forEach(r=>{
      if(!overlaps(r.start_ms,r.end_ms,b.startMs,b.endMs))return;
      const id=String(r.id||r.start_ms);if(map.has(id))return;
      const ds=localYmdFromMs(r.start_ms), d=DB.day(ds), idx=(d.sessions||[]).findIndex(x=>x.id===r.id);
      map.set(id,{sess:{id:r.id,start:Number(r.start_ms),end:r.end_ms?Number(r.end_ms):null,km:parseFloat(r.km)||0,elapsed:r.elapsed_secs||0},sourceDate:ds,origIdx:idx});
    });
    if(S?.gps?.st==='running'&&S.gps.start&&overlaps(S.gps.start,null,b.startMs,b.endMs)){
      const id=String(S.gps.sessionId||S.gps.start);
      map.set(id,{sess:{id:S.gps.sessionId,start:S.gps.start,end:null,km:S.gps.km||0,elapsed:getElapsed()},sourceDate:localYmdFromMs(S.gps.start),origIdx:-1,live:true});
    }
    return [...map.values()].sort((a,b)=>(a.sess.start||0)-(b.sess.start||0));
  }
  function journeyLabel(desc,dateStr){
    const sess=desc.sess, src=desc.sourceDate||localYmdFromMs(sess.start);
    if(desc.live) return src===dateStr?'Jornada atual':'Jornada atual · continuação';
    const srcSessions=(DB.day(src).sessions||[]).slice().sort((a,b)=>(a.start||0)-(b.start||0));
    const pos=Math.max(0,srcSessions.findIndex(x=>x.id===sess.id));
    let label='Jornada '+(pos+1);
    const endDate=sess.end?localYmdFromMs(sess.end):src;
    if(src<dateStr) label+=' · continuação'; else if(endDate>dateStr) label+=' · continua';
    return label;
  }
  function renderContinuousSessionList(){
    const el=document.getElementById('sessions-list'), toggle=document.getElementById('sessions-toggle');
    if(!el||!S?.curDate)return;
    ensureOverlapSessions(S.curDate);
    const desc=overlappingJourneyDescriptors(S.curDate).sort((a,b)=>(b.sess.start||0)-(a.sess.start||0));
    const shown=S.sessionsExpanded?desc:desc.slice(0,3);
    const html=shown.map(x=>{
      ensureJourneyAutoTrips(x.sess);ensureJourneyStatus(x.sess);
      const synthetic={date:x.sourceDate,trips:journeyManualTrips(x.sess),autoTrips:journeyAutoTrips(x.sess,DB.day(x.sourceDate).autoTrips||[])};
      return sessionItem(journeyLabel(x,S.curDate),x.sess,x.origIdx,x.sourceDate,synthetic,!!x.live);
    });
    el.innerHTML=html.length?html.join(''):'<div class="empty">Nenhuma sessão registrada</div>';
    if(toggle){toggle.style.display=desc.length>3?'':'none';toggle.innerHTML=S.sessionsExpanded?'Ver menos':'Ver todas &#8250;';}
  }

  // Link de corrida ↔ Jornada agora procura TODAS as sessões; uma corrida de 01:30
  // pode pertencer à Jornada iniciada no dia anterior.
  window.isAutoTripLinkedToSession = function(t){
    const ts=autoTripTs(t);if(!ts)return false;
    for(const ds of (DB.allDates?DB.allDates():[])){
      if((DB.day(ds).sessions||[]).some(sess=>ts>=(sess.start||0)&&(!sess.end||ts<=sess.end)))return true;
    }
    return !!(S?.gps?.st==='running'&&S.gps.start&&ts>=S.gps.start);
  };

  function civilJourneyTotals(dateStr){
    const b=civilDayBounds(dateStr), seen=new Set();
    let elapsed=0, km=0;
    const add=sess=>{
      const start=Number(sess?.start||0); if(!start)return;
      const live=!!(S?.gps?.st==='running' && S.gps.sessionId && sess?.id===S.gps.sessionId);
      let end=Number(sess?.end||0);
      const recordedElapsed=Math.max(0,Number(sess?.elapsed)||0);
      if(!end) end=live?Date.now():(recordedElapsed>0?start+recordedElapsed*1000:0);
      if(!end||end<=start)return;
      const a=Math.max(start,b.startMs), z=Math.min(end,b.endMs); if(z<=a)return;
      const key=String(sess?.id||start+'_'+end); if(seen.has(key))return; seen.add(key);
      const wall=Math.max(1,end-start), frac=(z-a)/wall;
      const baseElapsed=recordedElapsed>0?recordedElapsed:wall/1000;
      elapsed+=Math.max(0,baseElapsed*frac);
      km+=Math.max(0,Number(sess?.km)||0)*frac;
    };
    try{(DB.allDates?DB.allDates():[]).forEach(ds=>(DB.day(ds).sessions||[]).forEach(add));}catch(e){}
    if(S?.gps?.st==='running'&&S.gps.start){
      add({id:S.gps.sessionId,start:S.gps.start,end:null,elapsed:getElapsed(),km:S.gps.km||0});
    }
    try{
      const day=DB.day(dateStr);
      elapsed=Math.max(0,elapsed+(Number(day.tempoAdjust)||0));
      km=Math.max(0,km+(Number(day.kmAdjust)||0));
    }catch(e){}
    return {elapsed,km};
  }

  // ── Barra Total do dia = corte CIVIL virtual da timeline ───────────
  async function ensureCivilStatus(dateStr){
    if(!_supaUser||!dateStr)return;
    const old=civilStatusCache.get(dateStr);if(old&&(old.loading||Date.now()-old.ts<8000))return;
    civilStatusCache.set(dateStr,{loading:true,ts:Date.now(),rows:old?.rows||[]});
    const b=civilDayBounds(dateStr);
    try{
      const {data,error}=await _SUPA.from('journey_status_segments').select('*').eq('user_id',_supaUser.id)
        .lt('start_ms',b.endMs).or(`end_ms.is.null,end_ms.gte.${b.startMs}`).order('start_ms',{ascending:true});
      if(!error)civilStatusCache.set(dateStr,{loading:false,ts:Date.now(),rows:data||[]});
    }catch(e){civilStatusCache.set(dateStr,{loading:false,ts:Date.now(),rows:old?.rows||[]});}
    try{if(S.sec==='jornada')setTimeout(()=>{renderContinuousSessionList();renderContinuousStatusBars();},0);}catch(e){}
  }
  function renderContinuousStatusBars(){
    if(!S?.curDate)return;
    ensureCivilStatus(S.curDate);
    const c=civilStatusCache.get(S.curDate), rows=c?.rows||[];
    const tdRow=document.getElementById('td-bd-row');
    const totals=civilJourneyTotals(S.curDate);
    if(rows.length){
      const b=civilDayBounds(S.curDate), bd=statusBreakdownRows(rows,totals.elapsed,totals.km,b.startMs,b.endMs,(S?.gps?.st==='running'?S.gps.sessionId:null));
      if(tdRow)tdRow.style.display='';
      try{setHTML('td-bd-bar',buildSbarHtml(bd));setHTML('td-bd-list',buildSrowsHtml(bd));}catch(e){}
    } else if(c && !c.loading) {
      const day=DB.day(S.curDate);
      const autos=(day.autoTrips&&day.autoTrips.length)?day.autoTrips:((S._autoTripsDataDate===S.curDate&&Array.isArray(S.autoTrips))?S.autoTrips:[]);
      const bd=computeStatusBreakdown(totals.elapsed,totals.km,autos);
      const hasJourney=totals.elapsed>0||totals.km>0||autos.length>0;
      if(tdRow)tdRow.style.display=hasJourney?'':'none';
      if(hasJourney){try{setHTML('td-bd-bar',buildSbarHtml(bd));setHTML('td-bd-list',buildSrowsHtml(bd));}catch(e){}}
    }

    const jcRow=document.getElementById('jc-breakdown');
    if(S?.gps?.st==='running'&&S.gps.sessionId){
      const sess={id:S.gps.sessionId,start:S.gps.start,end:null,km:S.gps.km||0,elapsed:getElapsed()};
      // Garante o vínculo com JourneyStatusTracker mesmo que o renderizador
      // legado esteja desativado. Isso faz a timeline nativa começar a gravar.
      try{ if(typeof window.ensureNativeJourneySession==='function') window.ensureNativeJourneySession(); }catch(e){}
      ensureJourneyStatus(sess);
      const bd=journeyBreakdownForSession(sess,getElapsed(),S.gps.km||0,[]);
      // Enquanto não existe trecho detalhado, o fallback acima mostra 100%
      // Online. Jornada ativa nunca deve esconder a barra.
      if(jcRow)jcRow.style.display='';
      try{setHTML('jc-bar',buildSbarHtml(bd));setHTML('jc-blist',buildSrowsHtml(bd));}catch(e){}
    } else if(jcRow) {
      jcRow.style.display='none';
    }
  }

  const previousRenderJornada=window.renderJornada;
  window.renderJornada=function(){
    previousRenderJornada();
    try{renderContinuousSessionList();}catch(e){console.warn('[continuous journeys]',e);}
    try{renderContinuousStatusBars();}catch(e){console.warn('[continuous status]',e);}
  };

  setInterval(()=>{try{if(S.sec==='jornada')renderContinuousStatusBars();}catch(e){}},1000);
  setTimeout(()=>{try{if(S.sec==='jornada')renderJornada();}catch(e){}},600);
})();
</script>

</body>
</html>